# Claude Code handoff — 2026-06-11: profile/trophy crawl results + full-card click targets

Context: Trevor asked for a crawl of his profile, trophy-case click-throughs, moment-page field completeness, and the "only the thumbnail is clickable" problem on the in-app feature pages. Cowork crawled everything via Chrome DOM inspection (no click-driving) + DB. The data-layer bugs are ALREADY FIXED LIVE (two migrations, below); what remains for you is the click-target UX work + two small page nits. Claude Code's direct file inspection wins over this doc on any disagreement.

ALREADY FIXED LIVE BY COWORK (verify in browser, ship nothing for these):
- 3 of 6 trophy-case moments 404'd (/moment/25510 Dame, /moment/49220660 KD, /moment/1652251 Amon-Ra AllDay): resolve_moment_id's numeric path only checked moments.nft_id and the moments hydration cache misses them. Migration audit_20260611_moment_resolver_wmc_fallback_and_serial_state adds a wallet_moments_cache fallback (TS preferred on cross-collection nft-id collisions; editions join gates resolvability). All three now resolve (Dame $425 LOW, KD NO_DATA, Amon-Ra $450 STALE — AllDay ids work now too).
- SERIAL STATE panel blanks (OWNER em-dash + LAST SALE em-dash on moments Trevor provably owns, while the SAME sale showed in the activity table): get_moment_detail read owner/last_sale only from the moments row. Migration audit_20260611_get_moment_detail_serial_state_fallbacks: owner falls back to the wmc holder; last_sale falls back to nft_id/serial-keyed sales within the edition; wmc-resolved moments get a wmc-built panel. Verified: Clingan 49744949 now returns owner 0xbd94cade097e50ac + last_sale $2,100 / 2026-04-11 — and dapper.market independently confirms that exact purchase ("@Jamesdillonbond purchased... Apr 11, 2026"), so the values are right.
- Buyer/seller em-dashes in RECENT SALES/ACTIVITY are NOT a page bug: historical TS sales are buyer-blind until the running topshot-buyer-backfill drains (the drain-check task reports this morning). No code change.

Item 1 (the ask, MED-LARGE) — full-card click targets to the asset pages. Trevor's requirement: clicking ANYWHERE on a moment row/card (collection, market, sniper), a set card (sets page), or a pack row (packs page) navigates to that asset's entity page — not just the thumbnail. DOM evidence gathered tonight:
- /nba-top-shot/collection: moment rows are <tr> with cursor:pointer and ZERO <a> elements inside (the row onClick opens MomentDetailModal; the thumbnail is not even a link). File: app/(collections)/[collection]/collection/page.tsx (~2,900-line monolith).
- /nba-top-shot/sniper: same modal-first pattern, no /moment/ anchors in rows (feed rows render action links only). File: app/(collections)/[collection]/sniper/page.tsx (~2,070 lines; note disney-pinnacle has its own static sniper page too).
- /nba-top-shot/market: same monolith family — verify and treat identically. File: app/(collections)/[collection]/market/page.tsx.
- /nba-top-shot/sets: ZERO a[href*="/set/"] anchors — set cards open the in-page modal only. File: app/(collections)/[collection]/sets/page.tsx.
- /nba-top-shot/packs: ZERO a[href*="/pack/"] anchors in the table view. Files: app/(collections)/[collection]/packs/* (+ PackTable component).
Prescription (adapt to each file's actual shape): wrap the card/row's main content in a Next <Link> to the entity page — moments/editions -> /<collection>/edition/<setID:playID> (encodeURIComponent the colon) or /moment/<nft_id> where the row is serial-specific; sets -> /<collection>/set/<slug-or-id> (match the existing entity-page URL builders in lib/seo.ts / Breadcrumbs usage); packs -> /<collection>/pack/dist/<distId>. Inner action buttons (View Listing, +watch, expand) get e.stopPropagation(). Where a row currently opens MomentDetailModal, Trevor's requirement wins: row click navigates; if you want to keep the modal, demote it to an explicit "quick view" icon. Add role="link" or real anchors for keyboard/a11y and middle-click-to-new-tab support (real <Link> strongly preferred over router.push for that reason). Verify: clicking row body, image, and text all navigate on each of the 5 surfaces; inner buttons still work; tsc clean; smoke green.
Revert: git revert per page commit.

Item 2 (SMALL) — profile page title dedup: /profile/jamesdillonbond renders document.title "jamesdillonbond's Collection | Rip Packs City | Rip Packs City" — the profile metadata appends the suffix the root template already adds (same class as the 2026-06-07 pin-page fix). File: the profile page/layout metadata builder (lib/seo.ts profile helper or app/profile/[username]/layout). One-line fix.

Item 3 (SMALL, copy) — moment page "SIMILAR EDITIONS" renders bare em-dash for unpriced editions (4 of 6 on the Clingan page). Render "No sales" (or the edition's confidence label) instead of a naked dash. File: app/moment/[id]/page.tsx similar-editions section.

Note for later (no action now): dapper.market moment pages publicly expose buyer attribution AND lock state ("Locked till N days") per moment — a candidate enrichment source for lock freshness + historical buyer recovery if we ever want a second source; parked, pre-traction.

Guardrails: direct-to-main, no branches/PRs; PowerShell git; exact-path staging; npx tsc --noEmit clean; deploy READY + smoke green; maxDuration 800 cap; CRLF rules.

Expected end state: all 6 trophy moments click through (3 already fixed live — verify), full-card navigation on the 5 surfaces, profile title deduped, no bare dashes in similar-editions, tree clean and pushed with rev-list 0.
