# RPC deep-audit findings register

Persistent across monthly deep audits. **Purpose: never re-derive a settled question.**

How to use it: at the start of each deep audit, run only the one-line probe for each `VERIFIED-CLEAN` row. If it still returns the expected value, stamp the date and move on — do NOT re-audit that area. If a probe fails, that becomes a finding and earns a full investigation. Re-measure each `OPEN` item's evidence number and record whether it grew, shrank, or resolved.

Created 2026-08-09 (run 1). Last pass: **2026-08-27 (run 4)**. Full run-4 report: [deep-audit-2026-08-27.md](deep-audit-2026-08-27.md) (run 3: [deep-audit-2026-08-22.md](deep-audit-2026-08-22.md)).

⚠ **RUN-4 BULK STAMP (2026-08-27 PT):** every probe in VERIFIED-CLEAN below was re-run and returned its expected value — treat the whole section as verified 2026-08-27 even where the row still reads 08-22. One delta inside a clean row: `cron.job` count moved 93 → 100 → **99** and the set diff is fully attributed (+7 new jobs identified by first-run timestamp in the run-4 report §B-F1; −1 = jobid 256 unscheduled by this pass). One new benign row: jobid 373 `rpc-audit-underpriced-board-cost` self-unschedules 2026-08-30.

⚠ **PARTIAL RE-PROBE 2026-09-02 (PT), cloud session — 13 of the VERIFIED-CLEAN rows re-run, values below. This is NOT a bulk stamp: a row this note does not name carries its own older date.**
`rls_off_tables` **0** · `check_public_security_invariants()` **0** · `check_anon_write_surface()` **0** · `jsonb_array_length(check_secdef_anon_exec_drift())` **0** · anon-readable views without `security_invoker` **0** · anon/auth-readable matviews **0** · foreign tables in `public` **0** · extensions in `public` **0** · `Object.fromEntries(req.headers)` / `JSON.stringify(process.env)` **0** · GH Actions secrets inside a URL **0** · `.ilike` on an enum column **3** (unchanged, all `cached_listings.tier`, plain text) · TODO/FIXME **3, all prose describing a RESOLVED item — fifth independent confirmation the backlog is drained** (the row says 6; it shrank).
🚨 **ONE PROBE FAILED AND ONE PASSED WHILE BEING WRONG.** `pg_cron has no disabled/orphan jobs` reads **104 / 103** — see that row: the single inactive job is a correct, ledger-recorded pause, and the row has been reworded so a documented pause is expressible rather than reading as a failure. And **`No hardcoded credentials` returned its expected `0 real hits` while 14 live `rpc_pls_` gate keys sit in `cron.job.command`** — the probe greps `origin/main` and a cron command lives only in the database. See that row and [inbox 2026-09-02T1100Z](../overnight/inbox/2026-09-02T1100Z-all-14-pg-cron-http-jobs-put-their-gate-key-in-the-URL-and-the-credential-probe-cannot-see-them.md). ⭐ **A probe returning its expected value is not the same as the invariant holding — ask what it SEARCHED.**

⚠ **2026-08-29 DESKTOP-VM COWORK PASS (17:0x PT), touching two OPEN rows — read before re-deriving.** **R25** (pg_cron jobs with zero `pipeline_runs` observability): `run_refresh_pack_grail_metrics_mv_job()` (migration `20260829235752`, jobid 384) is now the reference shape for giving a pg_cron job heartbeat + terminal rows AND a catchable cancel → `ok=false` row; the eight `rpc_thp_leg_*` jobs could adopt it verbatim. ✅ **DONE 02:15Z (`20260830021550`): `run_thp_leg_logged()` re-points jobids 324–331 in place; pipelines `thp-leg-<name>` now carry heartbeat + terminal rows (panini control passed).** R25 exit condition met; the residual is hanging watchlist arms on the eight new names. **R29** (`job startup timeout` writes nothing): measured 7-day histogram by UTC hour — 13Z 226 · 09Z 191 · 15Z 75 · 03Z 72 · 18Z 47 · 14Z 35 · 21Z 35 · zero at 08/10/11/20/23Z; three jobs moved onto the zero hours on that evidence (198 → 11:46Z, 288/290 → 0/6/11/20Z, 383 at 10:53/20:53Z). Not a fix for R29 — a workaround that removes three victims from the band; the class stands. Full working in the 2026-08-29 ledger entries and known-issues #49–#51.


⚠ **MECHANISMS MAY BE INHERITED FROM THIS FILE. SEVERITIES, RATES AND LIVENESS CLAIMS MUST BE RE-DERIVED EVERY PASS.** Run 1 had six findings overturned and every one was a severity/rate/liveness claim; not one was a mechanism claim. Run 2 found **D2b's own ✅/❌ lists inverted** and **D32's headline example silently resolved**. Run 3 found **eleven OPEN items already fixed**, **R4's pipeline half to be a measurement artifact**, and **R7's "chronic 120 s rollback" refuted for the current window**. Re-measure before acting.

⚠ **PROBE HYGIENE.** `select count(*) from check_secdef_anon_exec_drift()` returns **1** when CLEAN — it returns one row containing a JSON array, so `count(*)` counts the row, not the findings. Use `jsonb_array_length((select check_secdef_anon_exec_drift()))` and expect **0**. ⚠ Mixed return shapes across the `check_*` family: a jsonb-array one is **1-row-clean**; a SETOF invariant (`check_public_security_invariants`, `check_anon_write_surface`) is **0-rows-clean**. Check the return type before reading the count.

⚠ **THE CHEAP CHECKS, cumulative through run 3.**
1. **Grep before you measure** — the target file's own comments, plus `docs/handoff-*.md`, `docs/audits/*.md`, and **all** of `docs/overnight/inbox/*`. Do NOT narrow to ledger-referenced handoffs; that is exactly how D28 was filed.
2. **Read one raw `pipeline_runs.extra` payload** before calling any pipeline dead, inert, stalled or broken. The `rows_found`/`rows_written` pair cannot distinguish "caught up" from "failing silently".
3. **Bound every rate by the DEPLOY, not the clock.** A rate measured across a fix is not a rate.
4. **When a threshold is allegedly exceeded and nothing is failing, doubt the comparison.**
5. **A watchlist / suppression justification is a claim with an expiry date.** Sweep the class, not the row.
6. **`check_anon_write_surface()` is blind to a route holding the SERVICE-ROLE key** — it tests the anon DB role.
7. **A cost estimate is not a measurement and one warm timing is not a cost.** Quote buffers.
8. **(run 3) Read code from `origin/main`, never a stale working tree, and cite the SYMBOL not the line number.** Run 3's clone was 134 commits behind; eleven OPEN items were already fixed inside those commits and a working-tree sweep would have re-filed all of them.
9. **(run 3) Diff the SET, not the count.** `cron.job` moved 85 → 93, which reads as +8 and is **+10 added, −2 removed**. The two removed jobs are now unnameable because no 08-15 snapshot exists.
10. **(run 3) A guard's declared scope is a measurement — take its positive control.** Re-implement the guard's own detector and run it on its declared roots first; if it returns 0 there, the guard is honest and the finding is about its SCOPE, not its logic.

---

## OPEN

| id | first seen | sev | finding | current evidence (run 3, 2026-08-22) | owner |
|---|---|---|---|---|---|
| R78 | 2026-09-03 | **P2 — the FLEET ALARM has been pinned CRITICAL for 19 consecutive runs by a condition that is deliberately unresolved, and the two red badges are one problem** | **`edge-fn-drift` is red 30/30 by design, and that alone drives `pipeline-sentinel` to CRITICAL** | A health pass over **all 19 scheduled workflows** (2026-09-03) found **NONE that stopped firing** — every one has a `schedule` run since 08-31 — and exactly two permanently red: **`edge-fn-drift` 30 failures / 0 successes** and **`pipeline-sentinel` 19 consecutive failures**. ⭐ **They are ONE problem.** `edge-fn-drift` exits 1 BY DESIGN on any drift; it currently reports `DRIFT: 5 function(s). 0 of them are safe to redeploy` — 1 deferred by decision, 4 gate-key-blocked, **each with a dated reason and a stated clearing condition**. The detector is correct and the condition is operator work (**R64 / R21**). The sentinel's CRITICAL then comes from **exactly one check — `Detector Health (GitHub Actions)`, edge-fn-drift 12x consecutive (crit at 7)** — whose own text reads *"A detector red for many days running is usually CORRECT and unread — read the LOG, not the badge."* **The arm for this hazard exists, fires correctly, and nobody had joined the two badges.** ⚠ **NOTHING IS DEMONSTRABLY MASKED TODAY and this row does not claim it is** — no other check is `critical` in the sampled run. The harm is structural: pinned at CRITICAL every run, **a NEW critical cannot change either signal.** Two `warn`s sit under it — ⛔ **CORRECTED 20 min later: BOTH BENIGN**, so calling them "real work" was an inference from alert text rather than a measurement. `allday-pack-opens-backfill` **self-cleared** (last run 03:46:02Z; 71 runs / 65 ok / 63 wrote rows in 73 h) and the unmapped backlog is severity **`info`**, draining at **outflow 4,672 vs inflow 41 in 24 h, net −4,631/day, ~9.2 days to clear**. ⭐ **The finding is unaffected and arguably strengthened — a healthy fleet under a stuck alarm looks exactly like this.** ⛔ **A real design conflict with both sides written down:** `check-edge-fn-drift.mjs` argues *"suppressing the exit is how a detector goes green while the drift stands"*; the Detector Health arm argues a long red streak is itself the failure. ⚠ **The first cost is stated with NO NUMBER; the second now has one — 30 red runs, 19 downstream.** **Candidate fix, NOT taken:** exit 0 only when the drifted set is a SUBSET of the two documented maps, exit 1 the moment anything drifts that is in neither — never green on *unacknowledged* drift. ⭐ **The discriminator already exists in the report** (`0 of them are safe to redeploy`); only the exit code and the badge conflate the cases. ⓘ Payload also confirms **R62 still open**: `email-FAILED:not_configured`. [Filing](../overnight/inbox/2026-09-03T0445Z-the-two-permanently-red-workflows-are-one-cause-and-one-consequence.md) | Trevor — it reverses an argued decision and changes the fleet alarm |
| R77 | 2026-09-03 | **P2 — a correlated tick loss is invisible to every HTTP-side alert arm, and the shape already has a precedent on the other scheduler** | **45 scheduled pipelines skipped 66 ticks inside one two-hour band and `would_have_alerted` was ZERO** | MEASURED 2026-09-03, nothing shipped. Restricting `pipeline_runs` to the **94 pipelines demonstrably on a clock** (≥20 gaps, p90/p10 ≤ 1.25 — a property of the data, never a curated list), the **2026-09-01 04:00–06:00Z** band shows **28 then 16 pipelines each missing a tick, 66 ticks in all**, against **≤5 in every other hour of the 73 h retained** — about **6× the worst other hour**. **20 of the affected are on the active watchlist and NOT ONE alerted**: every gap was shorter than that pipeline's own `max_silent_minutes`, so `cron_silent` and `detect_stalled_pipelines()` both stayed quiet. ⛔ **TWO EARLIER READINGS OF THE SAME DATA WERE WRONG and are recorded above the finding in the filing.** *"A fleet-wide outage"* died on an adjacent-day volume control (**1,093** rows in the window vs **1,114** and **1,166** on the neighbouring days; pg_cron untouched at **366 vs 372**) — nothing stopped. *"118 pipelines, 11,504 ticks"* died because the detector treated **event-driven** pipelines as scheduled: `wallet-backfill` is per-wallet, its median gap is seconds, and `gap / median` explodes into thousands of phantom misses. ⭐ **A gap is only a missed tick for something actually on a clock, and nothing in `pipeline_runs` says which rows those are.** ⚠ The two controls agree rather than conflict: volume barely moves **because the misses land on LOW-FREQUENCY pipelines**, so a volume check structurally cannot see this and a per-pipeline gap check can. ⛔ **Cause NOT established** — no deploy in the window, pg_cron unaffected, only ~5 `ok=false` rows: the ticks were not failing, they were not arriving. That points at the HTTP scheduling side and this row does not claim it. **The argument for acting:** `get_pipeline_alerts_core` already carries this exact shape for the OTHER scheduler — `pgcron_startup_timeout`, ≥5 correlated launch failures in a rolling 30 min — and has **no equivalent on the HTTP side**, where every arm is per-pipeline and therefore blind to a correlated dip shorter than any one pipeline's tolerance. ⚠ **Exit condition needs a CALIBRATION, not this sample**: three days holding one event can say 28 is an outlier and cannot say where the threshold goes, and `pipeline_runs` retains ~73 h while `pipeline_runs_daily` is six-hourly and cannot resolve a 40-minute band at all. [Filing](../overnight/inbox/2026-09-03T0300Z-fortyfive-scheduled-pipelines-skipped-66-ticks-in-one-two-hour-band-and-nothing-could-alert-on-it.md) | Claude Code — calibrate before building |
| R73 | 2026-09-02 | **P2 — the product's most expensive surface has ~0 users, and the cause is placement, not answers** | **The concierge is answering nobody: 55 real conversations all-time, last one 2026-08-16, and nearly all 55 are ours** | ⛔ **Do not audit concierge ANSWER QUALITY on this corpus** — 55 turns, mostly `tg:1755958876` (the sentinel chat) and one dashboard session, against 33+ tools and 4,200+ lines. Two structural causes, both fixed 2026-09-02 (`a6c271d9`): `SupportChatConnected` was mounted on ten signed-in/collection surfaces and **none** of the public entry points while `/insights` alone took **2,704 non-bot `funnel_events` sessions in 30 days**; and the quota was **inverted** — anon callers get 40/hr from `bump_concierge_ip_rate` while a signed-in FREE user skipped that limiter and hit `feature_quotas` `daily_limit = 5`, so signing in made it 8x stingier (now 40). ✅ **Mounts COMPLETED 2026-09-02 (`235af701`)** — home, `/about`, `/blog`, `/early-access` too; verified live as **exactly one** launcher per page across `/`, `/about`, `/blog`, `/early-access`, `/insights/*` (no double-mount, which was the risk of doing it per-page rather than in the root layout). `/edition/[id]` skipped deliberately — pure redirect route, no UI. ⚠ `collection_view` reads 16,762 sessions but events/session ≈ 1.00 (crawler signature) and only 34 sessions ever pasted a wallet — **not human volume**. **Exit:** `concierge_opened` rows (shipped BEFORE the mount on purpose) appear from real traffic within 7 days. **Falsifier:** `concierge_opened` = 0 for 7 days ⇒ the launcher is not seen at all, and the lever is `ExplainButton` on board rows (today wired into only `CollectionMomentTable` + `PortfolioSummary`), not the mount. If `concierge_opened` is healthy but `concierge_closed_without_send` dominates, the gap is the empty state. ⚠ **Two post-ship verification rows exist under `session_id = 'probe-concierge-ship-2026-09-02'` and are flagged `is_smoke_test=false`** — the smoke header is validated against `SMOKE_TEST_SESSION_TOKEN`, which this session did not hold — so **exclude that session_id from any future real-traffic count**, the same way `probe-verify-001` is excluded | shipped; watch open |
| R75 | 2026-09-02 | ✅ **P1 — SHIPPED. A read that cost 10.4 GB per call and timed the tool out in production** | **`get_fmv` pulled latest-FMV via `fmv_current.in(500 ids)`; the `DISTINCT ON` view has no per-group LIMIT, so it reads EVERY snapshot per edition and discards all but the newest** | ⛔ **THE 249x IS RETRACTED — corrected the same evening, twice, by two sessions.** `1,334,789 buffers / 16,736 ms` came from a benchmark arm written as `IN (SELECT …)` — **a subquery PostgREST never sends** (one session used `LIMIT n`, the other a CTE `ORDER BY external_id`; both plan as a hash semi-join over the fully materialised view). PostgREST sends `edition_id = ANY($1)`, which **reaches the partitioned index**. **Corrected: 500 ids, warm 25,330 buffers / 1,070 ms vs the RPC's 2,002 / 4.0 ms (17x); cold, on a heavier Base Set id set, 42,342 / 9,998 ms vs 5,359 / 470 ms (8x).** ⚠ **Both are right and neither is "the" number — quote a range and say what was cold.** ⭐ **The mechanism is ROW AMPLIFICATION, not a missing pushdown:** no per-group `LIMIT`, so ~35 snapshot rows per edition (~80 for Base Set) are read and `Unique` throws all but one away — ⚠ **so this worsens on its own as `fmv_snapshots` grows.** 👉 **"Reaches the index" is not "is cheap."** ⭐⭐ **The lesson that outlived the number: MEASURE THE SHAPE THE CLIENT ACTUALLY SENDS.** `IN (SELECT …)`, `= ANY(<array literal>)` and `= ANY($1)` are three different queries with three different plans, and the one that is easiest to type in a SQL console is the one the app never issues; read the PostgREST wrapper out of `pg_stat_statements` and reproduce THAT. ⚠ **A benchmark arm 38x worse than the same query issued the production way is measuring the harness.** ⚠ Judge on BUFFERS; the wall clocks are warm/cold contaminated. ⛔ Not merely slow — **the symptom was real even though the attributed cost was not**: ~10 s sat inside a 60 s lambda that also runs the tool loop, and a live probe answered *"The FMV lookup timed out on that one."* ⚠ The function's own `COMMENT ON FUNCTION` still says 249x — fix on the next FMV migration, not with a standalone `PGRST002` burst. ⭐ **The previous fix was CORRECT and that is the lesson**: the route chose `fmv_current` deliberately to dodge the 1,000-row clamp on raw `fmv_snapshots` (~34 rows/edition ⇒ ~17k rows ⇒ ~34% of editions silently lost, skewing percentiles toward hot ones) — right reasoning, cost never measured. **A fix chosen on correctness grounds and never costed is a live defect with a good alibi.** Fixed by `get_editions_latest_fmv` (migrations `20260902225408` + `225443`), which satisfies both constraints; diffed against the view on the same 500 ids at **500 = 500, zero rows differing either way**. `computed_at <= now()` is load-bearing for the index bound (verified zero future-dated snapshots). ⚠ **The second migration is the other lesson: the first threw 42804 at RUNTIME** because `confidence` is the enum `fmv_confidence`, not text — **CREATE succeeded; plpgsql checks the row type when the query runs, so such a function is not verified by applying cleanly, only by being called.** Paired with `8ba9b962`: the same path capped `editions` at 500 with NO ORDER BY and reported percentiles over it — "Base Set" matches **5,016 editions across two set names** and the first 500 covered one, so p10/p50/p90 described 10% of the filter (players never bind the cap: 0 of 1,414, max 170; **4 of 191 sets do, max 4,732**). Now carries `population_matched` / `scanned` / `truncated` / `truncation_note` + a deterministic order. **Verified live:** the same question returns in 14.3 s opening *"the Base Set has 5,016 Common editions matching that filter, and I can only price a slice of 500"*. **Falsifier:** a `get_fmv` turn that exceeds ~20 s again, or a truncated distribution presented without the sample caveat | shipped |
| R76 | 2026-09-02 | ✅ **P2 — CLOSED. The three sites that mattered are fixed; the thirteen this row pointed at are deliberately NOT worth a pass** | **Reading `fmv_current` off its `DISTINCT ON` key materialises the view; reading it ON the key still drags every snapshot row per edition** | ⛔ **This row was published WRONG and is corrected in place — three claims retracted, and the retractions are worth more than the finding.** **(1)** `= ANY(<3>)` 1,331,405 / `= ANY(<500>)` 1,334,789 buffers / 16,736 ms / 249x were **harness artifacts** — measured with `IN (SELECT …)`, a subquery PostgREST never sends. Real, proportional: **1 / 3 / 10 / 100 / 500 ids = 6 / 23 / 311 / 5,312 / 25,330 buffers, ~50–70 per edition** (warm); a second session cold on heavier ids got 42,342 at 500. **(2)** ⛔ *"the cost is FIXED, not proportional"* — **retracted; a three-id call is 23 buffers, not 1,331,405.** **(3)** ⛔ *"`.eq()` is fine, `.in()` is not"* — **the axis is the KEY, not the operator**, and this one **did harm**: it cleared the only two genuinely pathological sites as safe. ⭐ **MEASURE THE SHAPE THE CLIENT ACTUALLY SENDS** — `IN (SELECT …)`, `= ANY(<array literal>)` and `= ANY($1)` are three different plans; **a benchmark arm 38x worse than the production spelling is measuring the harness.** ✅ **SHIPPED `c280d337b`:** `sniper-feed:1165` (All Day FMV map, **424,475 → ~26,000 buffers**), `overview-stats:105` (**1,331,923 / 14,085 ms → 909 / 39 ms**, and it has **no caller** — landmine removal), `support-chat:2237` (**933,871 / 1,390 ms → 6 / 0.06 ms**, on every live "what is this worth" turn; it was also less CORRECT — `external_id` unscoped, and Top Shot stores every moment twice). ⚠ **Two of those three were on this row's SAFE list**, and the second is why a grep could not settle it: **the offending column is in an EMBEDDED table, not the read's own.** Guarded ban-at-zero: `__tests__/fmv-current-reads-are-keyed-on-edition-id.test.ts` asserts an `edition_id` qual on every `.from("fmv_current")` in `app/` + `lib/`, plus the count it inspected. ⭐ **The ranking inverted the framing:** one collection-scoped shape cost **31,052,866 blocks over 43 calls** against **8,691,506 over 4,443** for every id-list shape combined — **3.6x the buffers from 1% of the calls, out of one file.** ⭐ **A file count is not a cost model, and "sixteen routes" was a file count.** The hot id-list shape averages **2,042 blks/call ≈ 29 editions**; its 6.8 s/call must not be quoted as either shape's cost. ⚠ `pg_stat_statements` sits at 4,905/5,000 and is EVICTING — every total is a LOWER BOUND, and absence from it is not evidence a query never ran. **18 sites, not 16** — `profile/watchlist:74` and `watchlist:67` reach the view via `selectInChunks(supabase, "fmv_current", …)`; ⚠ **grep the bare quoted table name.** **Revised exit (the old one, "drive the `.in()` count to zero", was the wrong target):** convert an id-list site only when a wider helper already exists for another reason, or when its large-list tail becomes the common call — `recent-sales` is capped at 50 ids and should be left alone. **Falsifier:** a large-id-list, cold-edition site where the per-id LATERAL measures WORSE | closed |
| R74 | 2026-09-02 | ✅ **P3 — CLOSED, all three shipped same day** | **~~cheapest-set-to-complete~~ / ~~silent `max_tokens`~~ / ~~iteration exhaustion is invisible~~ — all three shipped `235af701` + `90fc6c38`** | **(1) ✅** `get_cheapest_sets_to_complete` + migration `20260902144913`. The 2026-07-12 transcript (ids 4821/4822) asked *"what is the cheapest set for me to complete right now?"* twice and got *"the set completion tool requires a specific set name"* both times — **asking the user to name a set is asking them the question they asked us.** Floor semantics copied verbatim from `get_topshot_set_completion_plan`; 1,879 buffers / 91 ms; service-role only, anon EXECUTE false, secdef drift 0, committed file md5-equal to prod `prosrc`. Verified live: *The Challenge: Pro*, $2.61, 57% done. ⛔ **Re-read before touching the ordering:** summing only the missing plays that HAVE a listing makes an UNCOMPLETABLE set look CHEAP (missing 40, two listed, sums to two and ranks first), so every row carries `missing_listed` + `fully_buyable`, `fully_buyable` sorts first, and quoting a partial cost as the cost to finish is forbidden in both the reading_note and the tool description. **(2) ✅** `max_tokens` now appends an explicit "ask me to continue" marker instead of returning truncated prose silently. ⚠ **NEVER fired** — longest `bot_response` over 5,643 rows is **2,107 chars** against a ~4,000-char cap, p95 **687**, zero rows in the 3,600–4,200 band. **Insurance, not a live defect — do not re-file it, and do not raise `max_tokens` citing this row.** **(3) ✅** iteration exhaustion now writes `category='iteration_limit'` instead of sharing a category with every other answer. ⚠ **Deliberately distinct from the error categories** — the degraded-share smoke check counts `concierge_unavailable`, and an exhaustion is a CAPACITY signal, not a dependency outage; folding it in would re-create the manufactured-evidence shape that check was fixed for on 2026-08-16 (`63bfc654`). No CHECK constraint on the column; verified before shipping a new value into it. Count: `select count(*) from support_conversations where category='iteration_limit' and is_smoke_test is not true;`. **Falsifier for (2):** a `bot_response` in the 3,600–4,200 band appears — then it was live, not insurance. **Falsifier for (3):** `iteration_limit` rows accumulate on real traffic ⇒ five hops is too few, not a rare edge | closed |
| R71 | 2026-08-29 | **P1 — the alerting PLANE is down, and both halves are credentials, not code** | **The sentinel detects and cannot deliver: Telegram send FAILS, email is unconfigured, and the arm that watches the three daily detectors is NOT CONFIGURED** | Observed on one CRITICAL run (`pipeline-sentinel` 33283636751, 00:34Z), which is the point — it had correctly found three dead Top Shot GraphQL pipelines. **Three independent gaps.** **(1) Telegram**: `notifications` carried `telegram-FAILED`, so the token IS configured (the push site sat inside `if (TOKEN && CHAT_ID)`) and the SEND failed. ✅ **The reason is now published** (`telegram-FAILED:<reason>`, redacted, in the response AND `pipeline_runs.extra`) — before this the reason existed only in a `console.error`. ⚠ **So this row does not yet say WHAT is wrong, deliberately: the next sentinel run says it.** Read `extra->'notifications'` on the newest `pipeline='sentinel'` row; `http_401` ⇒ revoked/rotated bot token (Trevor), `threw:` ⇒ egress. **(2) Email**: `RESEND_API_KEY` / `ALERT_EMAIL` unset (R62). Previously this produced **no entry at all** — absence, indistinguishable from "no alert was needed"; it now reads `email-FAILED:not_configured`. **(3) `GITHUB_ACTIONS_READ_TOKEN` is unset in Vercel**, so the sentinel's own Detector Health arm reports `[NOT CONFIGURED] … the three daily detectors (edge-fn-drift, db-pin-staleness, migration-parity) are unwatched: a correct one can stay red indefinitely with nobody reading it`. 🚨 **That is not hypothetical — `edge-fn-drift` has failed 21 CONSECUTIVE runs since 2026-08-08** (19 genuinely drifted functions + a tier-2 census that reads 0 of 38 bodies), correctly, unread. ⛔ **No code fix exists for any of the three; all are credentials or a deploy.** **Exit:** a sentinel run whose `notifications` contains a bare `telegram` (or `email`), and a Detector Health arm that is not `[NOT CONFIGURED]`. **Falsifier:** if the next run reports `telegram-FAILED:not_configured`, the token was never set and the `telegram-FAILED` reading above was wrong about which branch fired | **Trevor (three credentials)** |
| D12b | 2026-08-22 | ~~P0~~ **✅ RESOLVED 2026-08-22** | **D12 had RECURRED on a second surface: `/nba-top-shot/analytics` published a 99-day-old single row as Top Shot order-book depth** | **FIXED.** Re-derived before acting: `ts_listings` = **1 row**, `max(ingested_at) = 2026-05-15`, **99.3 days stale**. `OrderBookCard` now has a retired-source branch placed **before** the failed/count tests (for Top Shot the fetch outcome is irrelevant — a SUCCESSFUL read of a dead table is not a market fact). Facts + copy centralised in new `lib/analytics/ts-listings-retired.ts` so this is not a third copy-paste. Both present-tense claims corrected: `lib/analytics/methodology.ts` and the **indexed** `app/(analytics)/analytics/listings/page.tsx` description. ⚠ The DB-side fix was **NOT** taken and the reasoning is recorded in the module: nulling the RPC leg would publish "No live listings" for a collection with 12,259 live `low_ask` rows. New **ban-at-zero** guard `__tests__/retired-orderbook-source-not-rendered-ratchet.test.ts` walks `app/`+`components/` rather than naming files, and was **proven to fail** before being trusted. Tests **inverted, not deleted** (the 12,400-for-Top-Shot assertion was pinning the defect; failed/empty cases retargeted to All Day, a live source). ⚠⚠ **Building the guard exposed a bigger systemic finding — see R42.** ⚠ **SEVERITY CORRECTED 2026-08-22:** two of the three surfaces §1 called public are **auth-gated and were never indexable** — `/analytics/listings` and `/analytics/methodology` both **307 to `/login`** (measured with redirects disabled; `proxy.ts` gates the whole `(analytics)` group and its own comment says so). Their copy was still false and the fixes stand, but they were **stale internal copy, not an indexed public claim**. ✅ The core finding is unaffected: `/nba-top-shot/analytics` returns **200** anonymously, which is why the 99-day-old row really was published as market depth. ⚠ The trap is the documented one — a fetch that follows the 302 returns the login shell's metadata and reads like the page's own; only a redirect-disabled request settles it | ✅ closed |
| R42 | 2026-08-22 | ~~P1~~ **✅ RESOLVED 2026-08-22 — sweep COMPLETE, and MY PROPOSED FIX WAS ITSELF BLIND** | **Guards shared a copy-pasted `stripComments` that blanked real source before the guard ever looked at it** | **FIXED: 24 guards migrated to `scripts/lib/strip-comments.mjs`, 0 defective copies left** (the 2 remaining instances are deliberate — the helper's own documentation and a test keeping the shape as a NEGATIVE CONTROL). ⚠⚠ **TWO CORRECTIONS TO THIS FILING, both found by re-derivation rather than review.** (1) **The state-machine stripper I recommended lifting was ALSO blind** — it had no REGEX-LITERAL state, so a regex ending in an escaped slash presents a bare `//` and blanks the rest of the line: **80 occurrences across 66 files, including the guards' own block-comment replace bodies**, i.e. the recommended fix would have blanked the very code implementing it. **Lifting it verbatim would have swapped one blind stripper for another.** (2) **My "20 guards" UNDERCOUNTED**: 30 more files run the block regex with **no line strip at all**, and the swallow comes from the **block regex alone** — the line-strip ordering was never what made it dangerous. Re-measured exposure: **103,590 chars across 49 product files** over one guard's roots. ⚠ **Per-guard exposure was ANSWERED, not assumed** ("still passes" and "was never blind" are different claims): `pinnacle-router-fmv-same-row` was genuinely blind and still passes (a real negative result); `check-unbounded-server-reads` was never affected (17 under both strippers); ⚠ the three guards targeting `supabase/functions/**` are **UNMEASURED, not clean** — that tree was never walked. ✅ **`lib/seo.ts` is DISCHARGED empirically**, not left as a scary open item: 141 of its lines were invisible incl. `OG_INHERITED`/`TWITTER_INHERITED`, but both guards walking `lib/` are green over the now-visible file (one mutation-tested at `seo.ts:196`), and the R10 guard that owns OG inheritance uses no stripper at all. ⚠ **This does NOT re-validate past guard results** — every population reported before the sweep was measured against a partly-blanked corpus. Real casualties found and fixed: **32 hydration sites** | ✅ closed |
| R18 | 2026-08-22 | ~~P1~~ **✅ RESOLVED 2026-08-22** | **`/insights/candy-mlb` panels published a failed read as a market fact, contradicted by the page banner ~200px above** | **FIXED.** The rows array cannot tell the three states apart — a failed read and a genuinely empty section both arrive as `[]` — so the panels now consume `degraded`, the only thing that knows. `sectionEmptyCopy()` + `sectionBadge()` wired to **all six** panels (Deals, Offer spread, Serials, Scarcity, Holders, Players), **not just the reported one**, because fixing one of two is what turned D12 into D12b earlier the same day. ⚠ The tab badge was the same defect in miniature: a failed section rendered a badge of **0**, and a badge is a COUNT — it now renders no badge. 6 tests, **proven to fail** (reverting the spread panel reddens 3), including a **no-change control** (a genuine zero must still say so), a **sibling control**, and a **join pin** asserting every label the client string-matches still exists in `lib/insights/candy-board.ts` — a server-only rename would otherwise fall back to the healthy copy with every behavioural test still green | ✅ closed |
| R43 | 2026-08-22 | ~~P1~~ **✅ RESOLVED — the ONE defect is fixed and the GENERALIZATION IS REFUTED** | **A production bundle rendered a user-facing sentence the committed source does not contain** | **The single instance is real and fixed** (D12b disclosure: production rendered *"…switched off on 2026-05-26**written on** 2026-05-15…"*, losing `" and its last row was "`; verified in a real browser from Trevor's box, and the served JS chunk itself was wrong with the constants inlined). 🚨 **BUT THE GENERALIZATION DOES NOT HOLD, AND THE BLANKET LINT RULE MUST NOT BE WRITTEN.** The fold is **CONSTANT-FOLDING**, which requires **every** interpolation in the chain to be compile-time constant — the D12b site qualified because both were module-level string-literal `const`s, which is exactly why the chunk had the dates inlined. ⚠ **A template carrying a runtime interpolation cannot be folded at all, so it cannot lose a quasi this way.** Measured across `app`/`components`/`lib`/`workers`: **42 at-risk-SHAPED concatenations, ZERO constant-foldable** — the 8 whose interpolations are all bare identifiers were each opened and every one mixes in a runtime local. ✅ **Every user-facing site my filing worried about is specifically clear**: `alerts-send` outbound copy, the wallet-page `description` + JSON-LD, `stale-fmv-monitor`, `data-integrity`, `pinnacle-events-ingest`. **My INCONCLUSIVE wallet-page probe does not need re-running.** ⚠ **The zero means something only because of the POSITIVE CONTROL**: run against the pre-fix source at `e0f3186dc`, the same detector flags that site and names both constants. ✅ Guard `no-constant-foldable-joined-templates` bans the **PRECONDITION at population zero** — deliberately a ban, not a cleanup, since banning 42 sites to prevent a defect none can exhibit is the "cost stated with no number in it" shape. ⚠ What is NOT established is that turbopack's fold is correct in general — one fold, one dropped quasi | ✅ closed |
| R6 | 2026-08-15 | **P1 — ⚠ REFUTED IN PART. ⚠ 2026-08-23: re-measured IN-BAND and it is 1 of 5, not 4 of 5 — but the band was NOT saturated, so the owed measurement is STILL OPEN and its exit condition needs re-wording to name SATURATION, not the clock** | **`get_collection_stats` times out on 4 of 5 public collection landings** | ⚠ **Neither half reproduced 2026-08-22 ~22:00Z (healthy window).** Availability: **all 5 collections returned 200 with real data in 0.97–8.7 s** (`/api/collection-stats`), against the audit's "4 of 5 end at Couldn't load". ⚠ **This is a SAMPLE-TIME difference, not a fix** — the audit ran 16:20–18:05Z inside the degraded band; a degraded-band re-measure is still owed before closing. **The RENDER half is REFUTED outright**: the filing said the KPI band shows "empty labelled boxes with no value, no em-dash, no skeleton". Measured by blocking `/api/collection-stats` for 25 s in a real browser: **10 `rpc-skeleton` elements render**, and `KpiCard` already branches `loading \|\| value == null → skeleton`. **The loading state the handoff asked for already exists — do not build it.** The real fix (precomputed latest-FMV-per-edition) is unchanged, as is its trap: the naive `DISTINCT ON` over `fmv_snapshots` timed out at 55 s ⚠ **RE-MEASURED 2026-08-23 17:55–17:58Z, INSIDE the 16:20–18:05Z band — and the band was NOT SATURATED** (`4 active · 1 IO waiter · longest query 1.9 s`, vs the audit's *31 active / 30 IO waiters*). 🚨 **So this does NOT discharge the owed measurement, and the exit condition as written is under-specified: "re-measure in-band" and "re-measure during saturation" are different asks, and only the second tests what R6 is about.** Re-word it to name saturation before anyone closes this on a quiet reading. **Result at this sample: 1 of 5, not 4 of 5** — `nba-top-shot` 503 `{code:timeout,retryable:true}` with `Retry-After: 30` and `no-store`; **all four others 200 with full payloads**. ✅ The 503 is the HONEST degraded shape, so with the render half already refuted the user impact is "Top Shot's landing KPIs never fill", not a lie. ✅ **STRUCTURAL FINDING, no timing and so no confound: the same per-edition lateral is computed TWICE per request**, concurrently — inside `get_collection_stats` (for `fmv_pct`, confirmed from a live error CONTEXT naming `line 63`) and again in `route.ts::computeHighMediumPct` (for `fmv_high_medium_pct`), identical shape over identical rows, differing only in the FILTER. Scale: `fmv_snapshots` **1,230,231 rows**; editions **TS 19,838 · AD 6,190 · Golazos 575 · UFC 518** — the cost tracks edition count, which is why TS is the one that fails. TS's leg exceeded **25 s as `postgres` on the quiet DB above**, against `authenticator`'s **8 s** ceiling. ⛔ **The one-pass merge is NOT shipped:** it needs a migration (≈10–20 s of user-facing `PGRST002` 500s) and **would not fix TS** (one pass is already over the ceiling) — it belongs inside R52's standing decision, not ahead of it. ⚠ **A number deliberately DISCARDED:** a direct timing of the AllDay lateral read **40,229 ms**, which cannot be its cost — the AllDay HTTP call returned 200 with real data two minutes earlier. Cold-vs-warm; **a DB A/B must be WARM-vs-WARM**. Recorded so nobody re-derives it and believes it. [Filing](../overnight/inbox/2026-08-23T1805Z-r6-re-measured-in-band-one-of-five-not-four-and-the-lateral-runs-twice.md) 🚨 **UPDATED 2026-09-01 — THE ONE-PASS MERGE IS NOW SHIPPED, and the recorded reason NOT to ship it does not reproduce.** This row said the merge *"would not fix TS (one pass is already over the ceiling)"*, resting on TS's leg exceeding **25 s as `postgres` on a quiet DB** (2026-08-23). **Re-measured 2026-09-02 ~05:2xZ, also quiet: `get_collection_stats('nba_top_shot')` is 244,916 buffers / 3,863 ms COLD and 244,754 / 1,257 ms WARM** — an order of magnitude under the old reading and well inside `authenticator`'s 8 s. I am recording the delta, **not** claiming a cause: the two samples are ~10 days and several unrelated changes apart. ⚠ **The lesson is the standing one — a filed DECISION NOT TO ACT is a hypothesis nobody re-checks, and this one's premise had expired.** Shipped as `audit_20260902_collection_stats_folds_the_high_medium_pass_into_the_scan_it_already_makes` (migration `20260902054902_…`): the HIGH/MEDIUM aggregate now rides the pass the function already makes, the route's `computeHighMediumPct()` is gone, and **equivalence was proved for all five collections in a SINGLE STATEMENT** (one snapshot, so a concurrent `fmv-recalc` write cannot fake a mismatch). Removed per uncached request: **116,945 buffers / 2,875 ms / 19,942 lateral loops**. ⛔ **THIS DOES NOT CLOSE R6 AND DOES NOT DISCHARGE ITS OWED SATURATION MEASUREMENT** — halving the work is not the same as clearing the ceiling under load, and the exit condition still needs re-wording to name SATURATION rather than the clock. ⛔ **R52 is untouched**: this needed no new object. ✅ **A separate defect found while in there and fixed in the same commit:** the KPI fell back from `fmv_high_medium_pct` to `fmv_pct` — a DIFFERENT metric — so a failed read published **87.3% for LaLiga Golazos against a true 0.3%** and **73.6% for UFC Strike against 0.0%** under the label "Priced from Sales". It now renders nothing rather than something false. [Filing](../overnight/inbox/2026-09-02T0545Z-collection-stats-runs-the-same-19942-probe-fmv-scan-twice-per-request.md) | Claude Code — re-measure in-band |
| R19 | 2026-08-22 | **P1 — ✅ CLOSED 2026-08-23: mechanism verified, all five pages degrade PER SECTION, and the end-to-end is no longer pending (both directions, one deploy, rendered DOM)** | **Large public entity pages return an UNBRANDED Next.js 500 — and it is a real USER-facing outage, not just crawler noise** | ✅ **Both throwers identified from Vercel runtime errors, and both are catchable JS throws (real `Error` + digest), NOT wall-clock kills** — which was the open question, since no `try/catch` can catch a kill. ⚠ **They are DIFFERENT on the two pages and I initially generalised from one stack to the other:** `set/[slug]` throws `set editions unavailable: canceling statement due to statement timeout` from **`get_set_editions`, inside the `Promise.all`** (24 since 08-02); `team/[slug]` throws `team detail unavailable: …` from the **DETAIL read** (**34 occurrences across 8 DISTINCT USERS** since 07-14 — lakers, liberty, spurs, nuggets, sparks, timberwolves). Both call sites are now guarded on both pages. ⚠ **The register had this as an SEO/branding item; 8 real users say otherwise.** ✅ The section helper already degrades omittable sections (`team activity … degrading to empty`) and throws on structural ones (`set editions … STRUCTURAL — throwing`) — the throw was correct, what was missing was a caller that caught it. ⚠ **NOT established:** that a user has seen the branded page. The failure has not recurred on the new build and I did not force it; an end-to-end probe came back CONFOUNDED (500 on the old deploy, 200 on the new — deploy and load both changed) and is recorded as proving nothing. ⚠ **Root cause unchanged:** the RPCs exceed the statement timeout under load. Bounding the read stops the unbranded 500; **it does not make the page work.** ⚠ **UPDATED 2026-08-23: the 4-page half is CLOSED and the claim it closes on is NARROWER than this row implied.** All four entity pages carrying a `structural: true` read (`set`, `team`, `player`, `series`) now catch it and return their own branded `*Unavailable`. **`series` was the last, and it was NOT an unbranded 500 by then** — `app/(collections)/[collection]/error.tsx` (shipped `ca8dbde4`, same day) already caught it in brand. The remaining difference was what the READER KEEPS: the segment boundary replaces the whole page with a generic "couldn't render this page", while `SeriesUnavailable` — which that file already defined and already used for a failed DETAIL read — names the series and links to its sets. ⚠ So this was a three-plus-one consistency gap, not an outage, and saying otherwise would inherit a severity this row's own header warns about. Pinned by `__tests__/structural-entity-reads-degrade-in-brand.test.ts`, **derived from the tree** (every `[collection]/*/[slug]/page.tsx` containing `structural: true`) so a fifth entity page is inside it by construction. 🚨 **That guard's first draft was VACUOUS and only mutation found it:** it matched `catch … return <…Unavailable` anywhere, and every one of these pages has a SECOND catch around its DETAIL read returning the same component — so reverting the series fix still passed. It now locates the structural fetcher and works forward from its CALL SITE; mutation-proven on two pages, each naming the right one. ⚠ **UPDATED 2026-08-23 (second pass): the per-section half is now CLOSED too, and this row's LAST open claim goes with it.** All four pages hand their structural read to `structuralSection()` (`lib/entity-section-rpc.ts`), which absorbs the throw into `{rows, ok}` instead of letting it reach a page-level catch. The reader now keeps everything the DETAIL read paid for — on `/nba-top-shot/series/series-7` that is the hero, the season and all five stat cells, because `get_series_detail` answers in ~18 ms off `series_detail_rollup` while `get_series_editions` is the 6,615 ms / 32,484 buffer read that actually fails (R49). ⚠ **The TEAM page was the sharpest case and it is not the one this row described:** the roster shares a SIX-WAY `Promise.all`, and a rejected `Promise.all` discards its SETTLED siblings — so one roster timeout was costing five sections that had already come back. ⚠ **A second, machine-readable half had to ship with it:** `collectionEntityJsonLd` publishes `numberOfItems: items.length`, so `set` and `series` were handing a crawler a structured-data claim that a 4,895-edition series holds none. The `<script>` is now WITHHELD on a failed read — no claim beats a false one. 🚨 **The rewritten guard MISSED a real mutation on its first pass and only mutation testing said so:** swapping `{editionsOk ? (<grid/>) : (<SectionUnavailable/>)` for `{true ? …` leaves both the identifier (used elsewhere) and the literal `<SectionUnavailable` (now unreachable) in the source, so every presence check still passed while the page rendered an empty grid out of a failed read. **Presence of a component is not evidence it is reached.** Now pinned by a same-branch assertion; 7 page mutations + 4 helper mutations, all caught. ✅ **VERIFIED END-TO-END IN PRODUCTION, and this row's standing ⚠ "NOT established: that a user has seen the branded page" IS NOW ESTABLISHED.** One deploy, one load, both directions, read as RENDERED DOM (`dpl_6RsSY…` / `b601f6f4`) — so it is NOT the confounded probe this row records. **Positive control** `/nba-top-shot/series/series-7`, whose section reads DID fail during the render: hero (`Series 7`, `2025-26`) and all five stat cells survived; the grid rendered "Couldn't load the editions in this series … it does **not** mean there are none"; `cardsUnavailable` fired too; and the ItemList JSON-LD was WITHHELD — `numberOfItems` appears ZERO times, the only two `ld+json` blocks being the site `WebApplication` and the segment layout's `CollectionPage` (`@id` = the collection hub, not the series URL). **No-change control** `/nfl-all-day/series/series-4`: 75 `/edition/` links, 18 `/set/` links, all three sections, `ItemList` present, ZERO unavailable copy — the gating did not leak onto a working page. ⚠ **Direct egress to `www.rippackscity.com` is BLOCKED from the sandbox** (agent proxy answers 403 to CONNECT); the deployment URL is reachable via the Vercel MCP's server-side fetch. ⚠ **EXTENDED to a FIFTH page** (`edition/[slug]`, `b601f6f4`): its five-way fan-out had the same page-level catch, and `FmvHistoryChart`'s server-SEEDED 30-day view could not tell a failed read from a thin market — it rendered "too few sales to chart" out of `[]`. `entity-pages-bound-their-detail-read` now pins a tree-derived COUNT over all five pages: exactly one whole-page `*Unavailable` return, and it must be the `detailFailed` branch. | Claude Code — root cause + 4 pages |
| R20 | 2026-08-22 | ~~P1~~ **⚠ SUBSTANTIALLY REFUTED 2026-08-23 — the family is DONE, not throttled** | **The 9-pipeline `*-sales-history-backfill` family is throttled off by its own saturation breaker and is invisible to every failure instrument** | ⚠⚠ **THREE OF THE FOUR CLAIMS DO NOT SURVIVE RE-DERIVATION, and the first was refuted by the condition this row itself named.** (1) **The breaker DOES decay.** `recent_fails` oldest→newest tick over 48 h: allday-studio **91 → 20** · golazos-studio **154 → 21** · pinnacle-studio **163 → 22** · ufc **32 → 20** · ufc-studio **22 → 18**. Every arm fell materially. (2) **"The other eight wrote zero rows" is TRUE and the CAUSE is not throttling** — reading one raw `extra` payload per arm (cheap-check #2, which exists for exactly this) gives: `topshot`, `allday-studio`, `golazos-studio`, `pinnacle-studio` = **`{"note":"queue_empty"}`**; `allday` = **`{"note":"reached_spork_floor_hint","next":"deeper history (<2025-12-29) needs spork-proxy"}`**. Five arms are **FINISHED**. `pinnacle` writes (70 rows on its newest tick). `golazos` and `ufc` genuinely scan 40,000 blocks per tick and decode 0 — but **Golazos trades ≈2.4 sales/day and UFC has 0 sales in 30 days**, and 40k blocks ≈ 11 h, so a zero there is the EXPECTED yield, not a failure. (3) **"Invisible to every failure instrument" is false**: `extra` distinguishes all four states (`queue_empty` · `reached_spork_floor_hint` · `skipped:saturation` · a real scan payload). The information was present; the audit read `rows_written`. (4) `ufc-studio` at 1 tick/day against its siblings' 8 is **its schedule** — `vercel.json` `0 6 * * *`, not `*/3` — not starvation. ✅ **WHAT STANDS:** 131 of 196 ticks (66.8%) skip on saturation, which is real and is an R46 symptom; and `rows_written = 0` + `ok = true` IS a null instrument at the top level — the discrimination exists only inside `extra`. **No code change was made, and that is the finding** | ✅ closed as refuted |
| R21 | 2026-08-22 | ~~P1~~ **✅ SUBSTANTIALLY RESOLVED 2026-08-28: 29 → 11, and the residual 11 are withheld FOR CAUSE with a full credential census** | **29 of 67 deployed edge functions had NO committed source — 43% of the fleet outside every credential guard by construction** | ⚠ **18 of the 29 fetched + credential-scanned twice (zero shapes) and committed (`2e4bbb88`) — then MOVED OUT of `supabase/functions/` the same hour (`ba511f6c`): FOUR guards red-lit the bulk drop** (drift-checker's pinned fleet measurement, the `*KEY*`-literal ban firing on two benign `CHECKPOINT_KEY` consts, the reachable-tests ratchet at +18, and two missing inline-copy pins). **The verbatim sources are preserved in `docs/audits/edge-fleet-staging-2026-08-28/` with a README stating exactly what each guard demands to land them properly** — per-function integration work, not a bulk commit. Until then the credential-grep blind spot narrows only INFORMATIONALLY (the census below), not structurally. The 18 include 7 RETIRED 410 stubs and 11 live functions, all env-var-gated (`INGEST_SECRET_TOKEN` family). 🚨 **THE OTHER 11 CARRY HARDCODED CREDENTIALS IN THE DEPLOYED BUILDS and were deliberately NOT committed** (committing would publish the keys): **(a) 5 × unrotated `rpc_pls_` gate literals (21–24 chars, pre-rotation length per the LENGTH discriminator)** — backfill-allday-dist-opened, backfill-allday-pack-sales, backfill-topshot-pack-sales, resolve-allday-pack-dist, resolve-allday-pull-editions — ⚠ **three of these are ACTIVELY INVOKED 350–460×/day** (function_edge_logs 24h, positive control passed), so those literals are live production auth; **(b) 5 × ONE SHARED 16-char brand-shaped bearer literal (first4 `ripp` — guessable)** — backfill-golazos-series, scan-allday-wallet, scan-golazos-wallet, seed-allday-editions, seed-golazos-editions; **(c) 1 × 48-char hex literal** — ipfs-catalog-loader (compared via substring `includes()`, doubly weak). Remediation per function: env-var migration + dashboard secret + redeploy + caller repoint — the D2b/rpc-edge-fn-deploy workflow; the 5 sharing one bearer rotate together. ✅ **BONUS EXPOSURE CLOSED: `classify-acquisitions` v37 had `verify_jwt:false` and NO auth at all in front of service-role writes (arbitrary `?wallet=`)** — orphaned (0 invocations/24h with positive control; no cron, no repo caller; superseded by the authed hourly multicollection route). **Stubbed to 410 with verify_jwt:true (v38); anonymous probe now 401, was open.** Revert: redeploy the committed v37 source. ⚠ Watch 7 days for 410s in its function_edge_logs — a slow-cadence unseen caller would surface loudly there  🚨 **PRIORITY UPDATE 2026-08-29: THIS IS ALSO THE GATE ON THE PLATFORM'S LARGEST SINGLE ADDRESSABLE IO COST, which nobody had connected.** Two of the 5 `rpc_pls_` functions — **`backfill-topshot-pack-sales` and `backfill-allday-pack-sales`** — are the callers behind the two biggest TABLE reads on the instance: `topshot_pack_sales_history` **63.9M cold blocks / 3.6% of all instance cold reads / 19,306 per call**, `allday_pack_sales_history` **58.2M / 3.2%** — **6.8% together**, re-measured 2026-08-29 (was 6.4% on 08-22, so it is growing). Their query carries **no `WHERE` and PostgREST `count=exact`**, touching the whole 305 MB table on every call. ⭐ **The 08-22 filing recorded the blocker as "no source in the repo"; that is now stale — the source EXISTS and was deliberately withheld because of these very literals.** So the chain is: 6.8% of cold reads → two unfiltered `count=exact` reads → two edge functions → unrotated hardcoded keys → this row. **On an IO-bound instance this is no longer only a security item.** ⚠ A correctness item rides along: those reads are `SELECT * … LIMIT/OFFSET` with **no `ORDER BY`** (the unstable-pagination ban). ⛔ Not established that `count=exact` is removable without breaking a caller — read the source first, which is the point of unblocking. Detail: [inbox 2026-08-29T1359Z](../overnight/inbox/2026-08-29T1359Z-the-platforms-largest-IO-waste-is-blocked-behind-R21s-credential-rotation.md) | rotation of the 11 → Trevor (secrets) + Claude Code |
| R22 | 2026-08-22 | ~~P1~~ **✅ RESOLVED 2026-08-22 20:35–20:43Z** | **Cross-collection mats were 136.5 h stale under a "REBUILT DAILY" label** | **FIXED, verified live.** Applied `20260822013000_audit_20260821_cross_collection_refresh_lock_window` at 20:35Z — inside the 20:00–00:00Z healthy window its own header requires, which is the only reason run 3 deferred it (run 3 ran 16:20–18:05Z, inside the degraded band). Both functions now build into a TEMP TABLE and TRUNCATE immediately before the tiny insert; `prosrc ILIKE '%CREATE TEMP TABLE%'` true for both, ACLs unchanged. Schedules moved out of the degraded band: jobid **60** 04:10Z→**23:10Z**, jobid **4** 04:25Z→**23:25Z**. ⚠ **The migration's own `cron.alter_job` recipe does NOT work** (permission pincer — `postgres` does not own job 60; `cron_heavy` cannot EXECUTE `alter_job`); used `SET LOCAL ROLE cron_heavy; cron.schedule(<same name>,…)`, **both jobids preserved**, ownership + 600 s budget retained. Mats refreshed by hand: step1 ≈470–490 s → **221 rows** (was 179), step2 <60 s → **260 rows**; age **136.5 h → minutes**. ⚠ **Live proof the function-level `statement_timeout` is INERT**: step1 ran ~470 s under a declared 180 s. Binding budget for the 23:10Z run is `cron_heavy`'s **600 s** — only ~20% over today's runtime, so **if step1 fails again the cause is BUDGET, not the lock**. ⚠ Page metadata/share text still hardcode **"143 wallets"** (true: 221) — filed separately | ✅ closed |
| R23 | 2026-08-22 | ~~P1~~ **✅ RESOLVED 2026-08-23 ~02:00Z — migration APPLIED** | **`funnel_events` volume is ~100% machine traffic and there was no bot flag on the table** | **FULLY SHIPPED.** `user_agent` + **`bot_ua`** (classified SERVER-SIDE — a client-supplied flag is worthless when the population being labelled is the one that would not send it) plus the human-only partial index. ⚠ Named `bot_ua`, NOT `is_bot`, deliberately: it records what the UA CLAIMS. The route's self-healing fallback is now dormant and **kept** (it is also the branch that survives a rollback or a lagging branch DB), and its **"committed but NOT applied" comment was corrected in the same turn** — that sentence was true for about an hour, and a stale ordering note is exactly how the next person concludes the columns are missing when they are not. ⚠ **Pre-migration rows are NOT backfilled and `false` there means UNKNOWN, not HUMAN** — recorded in the column comment; any query spanning the boundary must filter on `created_at`. Controls both directions: 6/6 bot agents flagged, 0/3 real browsers | ✅ closed |
| D2b | 2026-08-09 | **P1 — ⚠ A SECOND EXPOSURE CHANNEL FOUND 2026-09-02, and it changes the ORDER of the fix** | **Gate-key rotation: six de-hardcoded functions never had that code DEPLOYED, so their secrets are INERT and rotating their crons would 403 every tick.** ⚠ The leak vector is the rotation procedure ITSELF. ⚠ The `rpc_pls_` prefix does NOT discriminate — LENGTH does (40 = rotated, 21–28 = original) | VERIFIED, re-derived against the `e66884f79` boundary rather than relayed; every figure in the 08-18 blocker filing reproduces to the minute. Staleness now: `compute-golazos-pack-ev` **35.0d** · `ingest-pinnacle-mints` **23.7d** · `ingest-topshot-pack-opens-history` **15.0d** · `ingest-allday-pack-opens` **14.9d** · `backfill-pack-opens-api` **11.7d** · `backfill-allday-pack-supply` **11.7d**. Timestamp-free corroboration: two members are deployed `import_map:false` while their origin/main sources import by bare specifier — they would boot-fail if the repo source were deployed. Operational control: `net._http_response` 10:34–16:34Z, n=663 → **0×403, 0×401**; all six pipelines healthy in 72h ⇒ the deployed code still accepts the un-rotated key. ⛔ Never delete an `_OLD` without re-pointing its cron first  ⚠ **NEW CHANNEL 2026-09-02: all 14 HTTP-dispatching pg_cron jobs send their gate key IN THE URL (`?key=…`), 13 active, 0 using a header — and Supabase edge-function logs record full request URLs, so every tick re-publishes the key into the log store.** ⭐ **This reorders the remedy:** relocating the key to a header must land BEFORE rotation, or the new value is immediately re-logged; and relocation alone closes nothing, since these values are already burned in public git history (#22). The edge half is deploy-gated (#23 / R64), the cron half is `postgres`-owned and reachable, and the cron rewrite can be done with `regexp_replace` inside `cron.alter_job` so the value never leaves the database. Filing: [inbox 2026-09-02T1100Z](../overnight/inbox/2026-09-02T1100Z-all-14-pg-cron-http-jobs-put-their-gate-key-in-the-URL-and-the-credential-probe-cannot-see-them.md). | **Trevor (secrets)** |
| R24 | 2026-08-22 | ~~P2~~ **✅ RESOLVED 2026-08-22** | **`/api/pack-ev` POST let an unauthenticated caller set the persisted pack price, the EV denominator and the +EV verdict** | **FIXED.** Gated on **PRICE PROVENANCE, not on the method**: `priceIsCallerInfluenced(priceSource)` — extracted into `lib/pack-ev-pricing.ts` so the SECURITY rule is a tested unit rather than an inline expression in a route whose happy path is upstream-GQL-driven and effectively untestable — is true for `"primary"`/`"min"` (packPrice IS the body's requestedPrice) and false for `"secondary"`/`"none"` (derived from data we hold). The `pack_ev_history` insert now requires `persistAuthorized(req)` (CRON_SECRET/INGEST_SECRET_TOKEN, **fails closed when unset**) only in the caller-influenced case, so a secondary-priced pack keeps recording history anonymously. **The READ/compute path is untouched and stays anonymous.** ⚠ `check_anon_write_surface()` is blind to this by construction — it tests the anon DB ROLE and the route holds the service-role key. 9 new tests + 2 added to `api-pack-ev-deep`, and the pre-existing "writes a clamped row" test was **UPDATED, not deleted** — it had pinned the defect by posting anonymously with a caller price | ✅ closed |
| R25 | 2026-08-22 | ~~P2~~ **✅ HEADLINE RESOLVED 2026-09-01; structural residual only** | **8 `rpc_thp_leg_*` pg_cron jobs have zero `pipeline_runs` observability** | Run-4 re-measure (2026-08-27, 7d, 28 runs/leg): observability unchanged (0 legs call `log_pipeline_run`, 0 rows 48h). Failure rates IMPROVED: impossible-parallel **25.0%** (was 33–35%; p50 ok 390 s, still brushing the 600 s ceiling) · serial-supply 17.9% · pinnacle-fmv-share 14.3% · fmv-coverage 10.7% · the other 4 at 0%; every leg's last_ok > last_fail. ⚠ **Run-4 measured the impossible-parallel leg's COST MECHANISM: its `editions JOIN sales` carries no collection scoping — 202,680 ms observed; a scoped variant returns instantly.** NOT shipped: the equivalence proof costs a 4.8M-row join, and scoping would hide a miscategorized sale's impossible serial — the fix needs both halves argued, see run-4 report §C-4 ✅ **THE HEADLINE IS RESOLVED — re-derived 2026-09-01 and the observability shipped since run 4.** All eight legs now dispatch through `public.run_thp_leg_logged(<leg>::regprocedure, '<name>')`, and over a **72-hour window measured on BOTH instruments, 95 of 96 dispatches carry a `pipeline_runs` row** (the one gap is `board-liveness`, whose cron run FAILED — consistent with a killed run being unable to log itself). Each leg also writes a `-heartbeat` row. ⚠ **Measure this over 72 h, not 7 d: `pipeline_runs` retains ~73 h**, so a 7-day comparison reads 12 logged against 28 dispatched and looks like a 57% logging gap that does not exist — a retention artifact, not a defect. 🚨 **The STRUCTURAL residual stands and is now the whole of this row:** every logged run reads `ok = true` while `cron.job_run_details` records real failures, because **a wall-killed leg writes NO terminal row at all** — the failure is ABSENT, not failed, exactly like the `after()` kill class, and the only detector is the CORRELATION (dispatch present, terminal row missing). ⛔ The run-4 cost note about the unscoped `editions JOIN sales` is unchanged and still correctly unshipped — **scoping it would hide a miscategorized sale's impossible serial**, which is the thing the leg exists to find | Claude Code |
| R26 | 2026-08-22 | ~~P2~~ **✅ RESOLVED 2026-08-23 — and the guard had a SECOND blind spot bigger than the one filed** | **`scripts/` is outside the `.range()`-requires-`.order()` ban by construction**; 10 unordered pagination sites | **FIXED: all 10 sites now carry a deterministic `.order()` on a UNIQUE key, and `scripts` is in ROOTS.** ⚠ **R26's count was exactly right (10) but only because it was grepped by hand — the guard itself could not have found six of them.** 🚨 **Its walk matched `/.(ts\|tsx)$/`, so it could not see `.mjs` or `.js` in ANY root, including the four it already declared.** Six of the ten are `.mjs`, so adding `scripts` alone would have "cleared" the debt while leaving most of it invisible. Both widened. ⚠ **Measured BEFORE widening so the ban was not loosened to fit: with `.mjs`/`.js` admitted, the four ORIGINAL roots still report 0** — the existing ban was honest about the files it could see. ⚠ **My first re-derivation used a hand-written detector and reported 2 false positives in `app/` plus one on its own `console.log`; re-running the guard's OWN exported `findUnorderedRangeSites()` gave `[]`.** The register's "positive control: 0 on its declared ROOTS" was correct and I nearly filed a false correction against it — **reuse the guard's detector, do not re-implement it.** Order keys chosen from PKs (`editions`/`wmc`/`unmapped_sales`/`moment_acquisitions`/`pinnacle_editions` = `id`; `fmv_snapshots` = `(id, computed_at)`); ⚠ **`flowty_funded_loans` has NO PK and NO unique index**, so `funding_resource_id` is used as **MEASURED-unique (3,388 distinct of 3,388), not constraint-enforced** — recorded at the call site. New scope test pins BOTH widenings, **proven against two mutations**: reverting the extension filter reddens only the scope test (**the ban stays green — the exact blind spot**), reverting one fixed site reddens the ban | ✅ closed |
| R27 | 2026-08-22 | **P2 — ⚠ HALF DONE BY DESIGN; the destructive half is Trevor's call** | **`docs/overnight/inbox/` has not been archived since 2026-08-13; drained and open are indistinguishable** | Now **188 live files across 14 days** (was 168 at audit time — still growing). **Shipped: `docs/overnight/inbox/INDEX.md`**, generated, newest-first, every filing's H1 in one scannable read, so a session can honour cheap-check (1) without opening 188 documents. ⚠ **Archiving by date was considered and REJECTED, and the reason is load-bearing: `rpc-nightly-autonomous-pass` DRAINS this directory.** Moving a filing that was never acted on would silently remove it from that queue and nothing would resurface it. A date is not a drained-determination and no per-item drained state exists to read, so the safe half shipped and the destructive half did not. ⚠ The INDEX is a **listing, not a status** — and it will rot exactly like the map it replaces unless regenerated when filings are added | **Trevor — archive/no-archive decision** |
| R28 | 2026-08-22 | ~~P2~~ **✅ RESOLVED 2026-08-22** | **The homepage promised "moment ID" three times; the submit path could not resolve one and blamed the user** | **FIXED by making the promise TRUE, not by deleting it** — `app/moment/[id]` already exists. `WalletSearch` now routes a numeric input to `/moment/<id>`, placed BEFORE the resolver so it costs nothing and an upstream outage cannot break it. Pattern is bounded `/^[1-9][0-9]{0,17}$/`: a looser `\d+` would swallow pasted serial and jersey numbers into a guaranteed 404. The two homepage hints that read "Try a wallet address or username." — contradicting the placeholder in an adjacent element — now name all three. 5 tests incl. a **no-change control** (a username starting with a digit must still hit the resolver) and a **join pin** tying the copy to the branch, because every behavioural test would pass if someone reworded the hint back while the branch stayed | ✅ closed |
| R29 | **2026-09-02** | P2 | **`job startup timeout` is 67–80% of all pg_cron failures, and it writes NOTHING to `pipeline_runs`** — pure invisible tick loss | VERIFIED over 11 days (the 08-22T1600Z filing named the cause independently: `max_worker_processes = 6` vs `cron.max_running_jobs = 32`). Daily 08-12→08-22: 11/42/154/123/67/151/332/77/268/224/162(partial) against ~4,000 dispatches/day = **1.7–8.3%**. Spread across 25+ distinct jobs at 2.8–9.5% of their own ticks — broad, not concentrated. **This is why missing-tick detection, not `duration_ms`, is the only valid kill detector here** 🔁 **RE-DERIVED 2026-09-02, 7 d: 384 startup timeouts = 76.6% of all pg_cron failures — inside this row's 67–80% band, unchanged.** ⭐ **NEW SHAPE, and it changes what to do about it: expressed as a share of EACH JOB'S OWN runs the rate is FLAT at ~1.1–1.9% across the fleet** (pinnacle-mints-backfill 1.4%, topshot-pack-sales 1.1%, allday-pack-sales 1.1%, wmc-fmv-confidence 1.4%, roll-pack-ask 1.9%, refresh-wmc-fmv-changed 1.2%), **regardless of schedule, owner or workload** — the absolute leaders rank by RUN COUNT, not by any property of the job. That independently corroborates the `max_worker_processes = 6` pool mechanism (a shared pool gives every arriving job the same chance of finding it full) and **means there is no single job to fix**: cutting overlap helps everything proportionally, and picking off "the worst job" would move ~1.4% of one job's ticks. ⛔ **Do not read the absolute ranking as a culprit list.** Detail: [cron-and-schedulers.md](../reference/cron-and-schedulers.md). 🔁 **RE-MEASURED 2026-09-03, 7 days — and THE THREE OBVIOUS LEVERS ARE ALL MEASURED DEAD.** Distribution: **13Z 117/1,171 = 10.0%**, 9Z 5.3%, 18Z 4.1%, 14Z 3.7%, 8Z 2.1%, **ZERO in 13 of 24 hours**; 13Z was also the 08-29 peak, two weeks apart. ⭐ **The launcher's tick volume is FLAT across all 24 hours (1,166–1,345)** — the band is not "more jobs firing". ⛔ **Re-stagger the minutes: real but far too small.** 1 job/min 0.64% → 6 jobs/min 1.46% is **2.3× against a 15× hour effect**, and within the hot hours the timeouts do not cluster on any minute, so there is no heavy hourly job to move. ⛔ **Move the victims: dead by their schedules.** The top five run every 2–5 minutes (`*/2`, `*/3`, `1-58/3`, `2-58/4`, `2-59/5`) and are inside the band by construction — **the 08-29 workaround worked BECAUSE its three jobs were low-frequency, and cannot generalise.** That is the honest reason this class stands. ⚠ **"We are overloading the instance then": DOES NOT SURVIVE ITS OWN CONTROL.** The app-side proxy said independence outright (hour 0 heaviest at 66,638 pipeline-seconds with **10** timeouts; hours 1 and 20 heavy with **ZERO**; hour 14 the lightest hot hour with **45**) — but the DB-side control refused it: the hot hours are generally heavier, **yet 12Z runs 35,955 cron-seconds, second-heaviest on the instance, with TWO timeouts, and 9Z runs a mid-range 13,661 with SIXTY-FIVE.** ⭐ **DB-side load is neither SUFFICIENT nor NECESSARY** — correlated, not the mechanism — so *"cut the work in those hours"* is NOT a supported prescription. **Remaining candidates are exogenous (a platform window on the same clock) or a limit around the launcher itself (background-worker slots), neither reachable from a cron expression — both Supabase-side questions, not repo changes.** ⚠ **The discriminating measurement could not be taken here**: worker-slot occupancy at the moment of a timeout, which `cron.job_run_details` does not record. [Filing](../overnight/inbox/2026-09-03T0400Z-r29-the-three-obvious-levers-for-pgcron-startup-timeouts-are-all-measured-dead.md) | Claude Code|
| R30 | 2026-08-22 | P2 — **experiment SHIPPED run 4** | **`wallet-backfill*` drops ~100k wmc rows/day to upsert-chunk failures; the GHA backstop was 4.3× worse than the primary it protects** | ⛔ **2026-09-02 — DO NOT CLOSE THIS ON `rows_lost = 0`. The experiment's split is UNANSWERABLE and the zero is a DENOMINATOR ARTEFACT.** (1) The falsifier this row set — *split the backstop's kill share at this commit* — **can no longer be run**: `pipeline_runs` retains ~73 h and its oldest `wallet-backfill%` row is 2026-08-30 00:45Z, while the experiment landed 2026-08-28 06:09Z (`1de79b1a8`). The pre-side is gone, and `pipeline_runs_daily` keeps only `extra_key_counts` — key PRESENCE, not values — so `chunk_rows_lost` was never made durable. ⭐ **An experiment whose measurement window is shorter than the time it takes anyone to read it has no falsifier at all**; if this is re-run, make the counter durable first. (2) Measured over the retained 48 h: **7,065 runs, 3,251 of them carrying `chunk_rows_lost`, and every one ZERO** — `chunk_errors` 0 as well, and the key is genuinely present (this is not a vanished field read as 0). **But `rows_to_write` over those same runs totals 236**, against 7,334,914 moments seen and 7,616,009 skipped as already-cached. The upsert path is where rows were lost, so with 236 rows to upsert in two days **a zero is nearly guaranteed whether or not the mechanism was fixed.** 0 of 236 is not evidence against ~110k lost per 48 h; the exposure collapsed because the cache caught up, not necessarily the defect. 👉 **Settle it in the next HIGH-WRITE window** (a seeded-wallet refresh wave or new wallet onboarding) and judge on loss PER ROW ATTEMPTED, never on the absolute count. — Run-4: 48h rows_lost **109,657** (was 152k — same class, mildly better). The 08-28T0420Z filing's split: per-attempt kill 41.5%, per-wallet completion 88.2%; primaries 17.0% killed vs GHA backstop **73.1%** (cohorts 60 s apart at the worst hours). Kill dimension has since read RECOVERED (341 clean ticks, p=1.4e-34); the residual loss is the per-chunk statement-timeout/pool mechanism. ✅ **Run 4 shipped the specified experiment**: `wallet-backfill-backstop.yml` `sleep 60→300` + `timeout-minutes 10→25` in one commit, falsifier in the workflow comment (if the backstop's kill share does not fall at 300 s spacing, spacing is not the lever — the hour band is). Next pass: split the backstop's kill share at this commit ⭐ **2026-09-01 — THE FALSIFIER IS NOW DURABLE, and one high-write day is already captured.** This row's own instruction was *"if this is re-run, make the counter durable first"*; that shipped hours earlier the same night as `pipeline_runs_daily.extra_num_sums` (`audit_20260902_pipeline_runs_daily_keeps_numeric_extra_values_not_just_key_presence`), which sums every NUMERIC `extra` value per pipeline per day — indefinitely, against `pipeline_runs`' ~73 h. `chunk_rows_lost`, `chunk_rows_recovered`, `chunk_errors` and `rows_to_write` are all in it now. **Ready-to-run:** `SELECT day, pipeline, (extra_num_sums->>'rows_to_write')::numeric AS attempted, (extra_num_sums->>'chunk_rows_lost')::numeric AS lost FROM pipeline_runs_daily WHERE pipeline LIKE 'wallet-backfill%' AND extra_num_sums ? 'chunk_rows_lost' ORDER BY day DESC;` 🚨 **AND IT ALREADY CONTRADICTS THE ZERO.** **2026-08-30 was a high-write day and it is retained**: `wallet-backfill-allday` **167,970 attempted · 15,425 lost · 90 chunk_errors (9.2%)**, `pinnacle` 1,833 · 2,196 · 27, `ufc` 813 · 456 · 7, `golazos` 106 · 106 · 4. **That is AFTER the 2026-08-28 backstop experiment**, so the mechanism was still live on 08-30; 08-31 → 09-02 read 0 lost on 159 / 15 / 9 attempted, which is the denominator collapse this row already warned about, not evidence. ⚠ **DO NOT COMPUTE `lost / attempted` NAIVELY — the two keys are not guaranteed to come from the same RUNS.** Pinnacle reads `lost 2,196 > attempted 1,833`, which is arithmetically impossible within one run: some code paths emit `chunk_rows_lost` without `rows_to_write`. Treat the ratio as an order of magnitude until the emitters are aligned. ⓘ **Semantics, read from `wmc-chunk-upsert.ts` rather than assumed:** `chunkRowsLost += chunk.length` on a FAILED upsert (rows genuinely unwritten), while `chunkRowsRecovered += chunk.length` counts rows in a chunk that succeeded AFTER a retry — **the two are disjoint sets, so `recovered` is NOT a subtraction from `lost`.** ✅ **One emitter gap CLOSED in the same pass:** the Top Shot `wallet-backfill` lane — the biggest one — emitted no `rows_to_write` at all (it buffers and flushes its own chunks, so there was no `rows` array to measure), and its three non-happy exit paths emitted no chunk fields either, so **a run that lost chunks and then errored reported neither the loss nor the attempt.** Both fixed; `rows_skipped` on those paths now includes the lost rows as the success path already did | Claude Code |
| R31 | 2026-08-22 | ~~P3~~ **✅ RESOLVED 2026-08-23 — and it was HALF the defect** | **~70 indexable page titles lost the brand suffix — and ~30 others printed it TWICE, from the same line** | **FIXED, verified by rendering.** A plain-string `title` is formatted by the NEAREST ANCESTOR template and contributes NO template of its own, so `collectionLayoutMetadata()` and `app/insights/layout.tsx` did two opposite things at once. Measured live: `/insights/first-mint` and `/nba-top-shot/collection` rendered with **no brand**; `/about`, `/blog`, `/privacy`, `/legal/fmv-methodology`, every blog post, `/disney-pinnacle/collection`, `/nba-top-shot/pack-sniper` and `/nba-top-shot/challenges` rendered **`… — Rip Packs City \| Rip Packs City`** — the D24 class, still live. ⚠ **It survived because the pages a human spot-checks are the two the bug cannot reach**: `/` and `/insights` are one level down and formatted by the ROOT template, so they look perfect. Fix is `title: { absolute: meta.title, template: BRAND_TITLE_TEMPLATE }` — **both halves load-bearing**, dropping either ships half the bug. ⚠⚠ **Restoring the template ALONE would have been worse than the bug**: the ~33k-URL entity corpus bakes `\| Rip Packs City` in precisely BECAUSE no template reached it, so `buildMeta()` + 5 entity fallbacks + both pack pages went `absolute` and **20 static titles were stripped** instead (suffix-only — a mid-sentence brand mention is not this defect). ✅ **14 URLs read out of a real dev server**, including both controls byte-identical to prod. 🚨 **The D24 guard was blind THREE ways** — pipe spelling only (every offender used an em dash), double-quoted literals only, and `app/`-only while the collection title lives in `lib/seo.ts`. Rewritten, walks all quote styles, skips og/twitter by NESTING, two-level positive control, **proven against 3 separate mutations**. 18 assertions **updated, not deleted** | ✅ closed |
| R32 | 2026-08-22 | ~~P3~~ **✅ RESOLVED 2026-08-22** | **Scheduled workflows carried the `bash -e` fallible-assignment shape and died before the line that names the failure** | **FIXED in the 2 places it is actually a defect:** `offer-fill-backfill.yml` (4 `jq` assignments in the drain loop; a 200-with-HTML killed the step before its `::warning::`) and **`allow-list-reconcile.yml` — NOT named in the filing** — where the parse sits ABOVE the `HTTP_CODE != 200` check, so a 502 aborted the step and **nothing reported the 502 at all**. Both now `X=$(…) \|\| X=""` with an explicit unparseable-body branch that names the fault and prints the first 200 bytes. ⚠ **Scope was wrong in BOTH directions.** The filing said 2 workflows; the sweep finds **30** unguarded fallible assignments across ~14 files — **but 30 is NOT a defect count.** Most are `STATUS=$(curl -s -o … -w "%{http_code}")`, which fails only at the transport layer and is arguably correct to abort on, with the HTTP status handled on the next line. ⚠ **My first sweep said 33 and flagged `ops-monitor.yml:118`, the file the audit called the correct reference impl — a SCANNER ARTIFACT**: its `\|\| echo ''` is on a continuation line. **The real rule the filing never stated: the shape is dangerous only when a fallible parse sits UPSTREAM of the branch that would report the failure.** Blanket-fixing the other 28 would prevent nothing | ✅ closed |
| R33 | 2026-08-22 | ~~P3~~ **✅ RESOLVED 2026-08-23** | **`/api/top-sales` fabricates `"Unknown"`, serial `#0` and circulation `0`, and returns 200-on-error for Pinnacle ONLY** | **FIXED — four defects, and the fourth is the one R33 rated highest.** (1) `?? "Unknown"` on the name, (2) `Number(serial ?? 0)` → an impossible **#0**, (3) **`circulationCount: 0` was HARDCODED**, never read from anything, and (4) 🚨 the Pinnacle branch returned `[]` on a read error, so a DB failure rendered as "no top sales" at **HTTP 200** and was then cached `s-maxage=300` — **five minutes of a false claim**, while the non-Pinnacle branch returned 500 for the identical failure. `pinnacleTopSales` now returns a DISCRIMINATED result (its old signature could not express "the read failed", so the caller could not tell it from "no sales" and chose the reassuring one); every unknown is `null`; the error path is 500 + **`no-store`**. ⚠ **R33's edition_id half has SINCE RESOLVED and is not re-asserted** — it recorded "4 of 5 have NULL `edition_id`"; measured 2026-08-23 that is **0 of 5**. The serial half stands and is worse than filed: **5 of 5** top Pinnacle sales by price in 7 d carry a NULL serial. ⚠ **Zero callers re-confirmed** by grepping the literal path across `app/ components/ lib/ workers/ scripts/ .github/` and `vercel.json` — **not even `vercel.json`**. Fixed rather than deleted: a template-literal or external caller cannot be ruled out by grep, and a wrong answer is worse than a removed one | ✅ closed |
| R34 | 2026-08-22 | ~~P3~~ **✅ RESOLVED 2026-08-23 — and it was TWO lines, not one** | **Golazos badge rows all carry `circulation_count = 0` and one render branch does not guard it** | **CONFIRMED and FIXED.** R34's own refutation condition was "refuted if `badgeInfo` is never populated for Golazos" — measured: **all 218 rows are keyable by `(player_name, series_number)`**, so it is populated. ⚠ **And the population is wider than filed: 218/218 carry `circulation_count = 0` AND `effective_supply = 0`, with ZERO nulls** — the neighbouring line at +1 had the identical `!= null` shape, so the tooltip published **two** fabricated supply figures per Golazos row, not one. ⚠ **The correct guard already existed 350 lines up in the SAME FILE** (the row-level "N minted" line tests `!= null && > 0`) — one branch guarded, its neighbour not, which is the per-PANEL-not-per-page rule inside a single component. ⚠ **R34's own one-DOM-read plan does NOT settle it** — the tooltip is behind a wallet-loaded row and a hover, and the no-wallet page is a 1,931-char shell; settled from source + DB instead. 🚨 **The guard took THREE tries and each failure is worth more than the fix.** (1) line-scoped → flagged the already-CORRECT `{…} minted` span whose `> 0` sits two lines up; (2) window-scoped on raw text → **my own explanatory comment contains "`> 0`", so the COMMENT satisfied the guard and the mutation stopped reddening — it failed OPEN**, the more dangerous direction than the recorded mirror; (3) comments blanked. ⚠ **And that mutation appeared to PASS until I checked it had APPLIED — it had not** (shell escaping ate the replace target), i.e. a result read off an unchanged baseline. 6 tests pinning both control directions plus a no-change control (`circulation_count === 1` still reaches the 1/1 Ultimate branch) | ✅ closed |
| R35 | 2026-08-22 | ~~P3~~ **✅ RESOLVED 2026-08-22** | **`sitemap.xml` `lastmod` was generation time, not modification time** | **FIXED by REMOVING `<lastmod>`, not by correcting it.** It was `new Date().toISOString()`, so all five children carried an identical timestamp equal to the moment the index was generated, moving every 6 h on the revalidate whether anything changed or not — Google discounts a lastmod that always reads "now", so the tag was both false and useless. `<lastmod>` is OPTIONAL in the protocol: omitting it asserts nothing. ⚠ Do NOT "fix" this by bucketing to the revalidate window — still a claim the content changed at T, only coarser. The existing test **"gives every child a lastmod" was INVERTED, not deleted** — it had been holding the defect in place | ✅ closed |
| R36 | 2026-08-22 | ~~P3~~ **✅ RESOLVED 2026-08-29 — and the filed repair was correctly declined; the fix was in a different layer** | **Mobile bottom nav's leftmost tab sends an anon first-run visitor to the login wall** | ✅ **Redirect chain measured 2026-08-23: `/profile` → **308** → `/dashboard` → **307** → `/login?next=%2Fdashboard`.** ⭐⭐ **THIS ROW'S REFUSAL TO SHIP THE OBVIOUS FIX WAS RIGHT, AND IT IS WHY THE REAL ONE IS SAFE.** Hiding or re-pointing the tab needs CLIENT session state — three states where "unknown" must not render as "signed out", plus a 5-tabs-then-4 layout shift — which this row correctly identified as the recorded **React #418** shape that no gate here can see. **It was framed as a NAV problem and it is a DESTINATION problem.** **FIXED** `e4b4e41f5`: `app/profile/page.tsx` is now a SERVER component that `redirect("/dashboard")`s a signed-in visitor and otherwise renders a value-first public page — the product's real hook (`WalletSearch`, `surface="profile_tab_anon"` so pastes are attributable to this placement), an honest three-item account pitch drawn from the auth gate's own *walls PERSONALIZATION, not CONTENT* block, and three no-account exits so it is never a dead end. **The nav is byte-identical for everyone and NO client session state was added** — a test bans `"use client"`, `useState` and `useEffect` in that page. 🚨 **It touched `proxy.ts`, which CLAUDE.md names off-limits for unattended change** — one line, an **exact** match (`pathname === "/profile"`), never a prefix, so it cannot reach `/profile/edit` or `/dashboard`; `__tests__/proxy-profile-entry-is-public-but-nothing-else.test.ts` pins both what opened and what must not have, and folding it into the neighbouring prefix reds three arms. ⚠ **The production smoke check asserted the OLD 308 and would have gone permanently red** — found by grepping for what READS the path before changing it; it now asserts an anonymous 200 that is not a `/login` redirect AND carries the page's own copy. ⚠ The old redirect was `permanent: true`, so browser/crawler caches may skip the new page until they expire. **Falsifier:** `/profile` anonymously must be 200 with "No account needed" and no `location: /login`; `/profile/edit` must still 302. | Claude Code (shipped) |
| R37 | 2026-08-22 | ~~P3~~ **✅ RESOLVED 2026-08-22** | **`check-brand-tokens.mjs`'s LITERAL check iterated a curated 54-file list while its own second half walked the tree and said why** | **FIXED — the guard now walks the tree: coverage 54 → 399 web surfaces**, with PROTECTED kept only as a rename tripwire. ⚠ **The audit's "population outside the list is 2" was nearly right and my first re-measure said 52** — a scanner artifact from not applying the guard's own `FALLBACK` (`var(--rpc-red, #E03A2F)`) and `brand-exception` filters. Applied properly: **3**, of which **one was ConsoleGreeting's own comment DESCRIBING the sanctioned hardcode** — so the guard now **strips line comments first** (at least six guards here have fired on the comment documenting the thing they check). The email-accent half strips them too: **it fired on the comment I was writing to explain it**, live. Also: `brand-exception` window widened 3 → 6 lines (a multi-arg `console.log` puts the literal 4+ lines below its justification, so the escape hatch was impractical and got worked around), and the summary now reports **what it inspected** (399) instead of what was listed (54). ⚠ ConsoleGreeting's stale "only sanctioned hardcode" claim corrected — state the REASON, which stays true, not the COUNT, which does not | ✅ closed |
| R38 | 2026-08-22 | ~~P3~~ **✅ RESOLVED 2026-08-27 (run 4)** | **`support_conversations.session_id` minimum-length `WITH CHECK` was vacuous (≥ 1)** | **FIXED by `20260828055741_audit_20260828_r38_session_id_floor_and_dune_search_path`: floor raised 1 → 20.** ⚠ **The floor was chosen from the measured WRITER population, not a guess** — 30d length distribution read first: smoke suite = 28 chars (449 rows, newest 08-27), real clients 31–35, server default 41, the 25-char historical cohort ended 07-21; the ONLY sub-20 writers are inactive (13-char fixture ×34 newest 08-16, one 16-char row 08-17), and the reused 13-char id was itself the exposure. Same migration pinned `search_path` on `dune_budget_status(text)` + `dune_spend_report()` (advisor WARNs) — **the R14 outage class was checked first: both are prokind='f' with no COMMIT in body**, so the attached SET is safe. Revert in the migration header | ✅ closed |
| D8 | 2026-08-09 | P2 | wmc metadata denorm has no self-heal; the blocker is **ROW-LOCK CONTENTION, not the scan** — `EXPLAIN` gives a Bitmap Index Scan on `idx_wmc_lock_wallet_coll`; Postgres named it (`57014 … while locking tuple … in relation "wallet_moments_cache"`), colliding with wmc FMV writers jobids 302/303. **Fix = CHUNK IT** (50 instant · 400 reliable · 800 collides); `FOR UPDATE SKIP LOCKED` was tried and did NOT help. **No operator index is needed** | ✅ TS healable 1,956 → 0; UFC `set_name` 329 → 2 (honest gap). AllDay remainder **284**, of which a 50-row sample found only **1** healable — the bulk is the R8 class one layer up (editions with no name), which no wmc backfill can fix. Run 3 did not re-measure the 284 | Claude Code |
| D37 | 2026-08-09 | P2 — ⚠ **SHRANK, and the growth STOPPED** | AllDay `unmapped_sales` backlog | Run 4: unresolved **104,953** (total 105,791; drifting DOWN ~1k over 5d; newest unmapped sold_at 08-26 — inflow continues, resolution outpaces it). Run 3: **106,069 → 105,172** (price-0 parked 68,020 = 64.7%, correctly held; promotable 37,152). ⚠ **Only 9 rows added in 24h vs the filed +1,376/day.** Control run confirms this is NOT an ingest outage: AllDay `sales` took **247 rows in 24h / 10,476 in 30d**, last sale 2026-08-22. Downgrade from "GREW" | Claude Code |
| R8 | 2026-08-15 | P2 — ⚠ **THE PRESCRIBED HEAL IS CORRUPTING. DO NOT RUN IT.** | The 512/481 canonical TS editions with no player are **TEAM MOMENTS and correctly have no player**; the real defects are the NAME field and `wmc.player_name` itself. **GENERAL RULE, worth more than this row: reading a denormalized cache back into its own source is CIRCULAR whenever that cache was populated with a fallback.** `backfill_wmc_metadata_from_editions` fills `player_name = COALESCE(wmc.player_name, e.player_name, e.team_name)`, so wmc "has a name where `editions` does not" **because a backfill put the TEAM name there** | **548** editions render `name = set_name` (was 480 — **GREW +68**). ⚠ **RUN 4 RE-DERIVED THE GROWTH MECHANISM AND IT IS NOT A NAME-CLOBBERING WRITER**: all 548 have NULL `player_name`, 485 are Reel-shaped (`\d+:\d+` parallel keys, Fit Check 134 · Season Rewind 82 · Clamps 71 · Champion's Path 64), and 485 were "updated" in the last 7 days — that churn is the **stub-resolver's rotating no-op upserts** (520-edition queue, whole population touched every ~6h; the 08-27 ledger's Reels entry is the same root). The +68 is the Reel catalogue growing plus the display fallback landing on new rows, not corruption of existing names. Repair unchanged: a better upstream TITLE for multi-player Reels (their on-chain metadata has no `FullName` by design); the stub-resolver "give up" marker migration is the decision item. NULL `player_name` populations ~flat (AllDay 15,311/502,920 = 3.0%, of which 15,252 resolve at render-time join — enrichment lag, not loss) | Claude Code |
| D25 | 2026-08-09 | P3 — ⚠ **grain changed AGAIN 2026-09-03: the row's 62 is INTACT, and a SECOND population 37x larger was never counted** | wmc rows rendering an impossible serial | Run 4: **66** (TS 62 · Pinnacle 4 · AllDay 0, full scan, no timeout). Run 3: **67** at the wmc render grain (`w.serial_number > w.mint_count`): TS 63 · Pinnacle 4 · **AllDay 0**. AllDay stays at zero, so the 08-15 targeted UPDATE's trailing `<= circulation_count` predicate held. ⚠ The register's 62 was `editions.circulation_count` grain and **the fixable-vs-upstream split could not be re-obtained** — the 2.29M-row join hit `57014` twice and was not retried. The two numbers are NOT interchangeable 🔁 **RE-DERIVED 2026-09-03 at this row's own grain (`w.serial_number > w.mint_count`): 2,353 rows. ⛔ NOT a 35x regression** — split by key it is **62 on BASE editions (which IS this row's number, measured over that population) and 2,291 on `::N` PARALLELS, which were never in it.** A THIRD grain; the row was never wrong. ⭐ **THE PARALLELS ARE A UNIT MISMATCH AND THE EVIDENCE IS TOTAL: for 895 of 895 offending parallel keys the largest serial we hold fits inside the BASE edition's circulation — ZERO exceptions.** The parallel row carries the BASE moment's serial while the denominator beside it is the PARALLEL's own run (`90:3410::1` 7,585 vs base 16,000 · `224:8241::17` 5,244 vs 5,373 · `124:4344::1` 7,962 vs 8,500). **The data is not corrupt; the RENDER pairs two different units.** ⛔ **THE OBVIOUS REMEDY IS MEASURED DEAD:** copying `editions.circulation_count` over `wmc.mint_count` fixes **2 of 899 keys** — for **897** the editions value is ALSO below our largest serial, because both stores describe the parallel run while the serial describes the base one. **That afternoon moves 0.2% and changes nothing on screen.** ⚠ **A separate, LARGER integrity problem found on the way: 1,095 of 2,853 Top Shot parallel `edition_key`s (38%) carry MORE THAN ONE distinct `mint_count` in wmc — 28,164 rows, up to FOUR values for one key.** The impossible-serial rows are only the subset where the wrong denominator landed below the serial. ⚠ **The disagreement runs BOTH ways and no side is picked** — `51:1885` has `editions` at 4,000 against wmc's 8,500 with real serials to 7,944, so there **`editions` is the odd one out.** **What remains is a PRODUCT CALL:** what `#N / M` should mean on a parallel — base serial with base run, or parallel with parallel — which changes a number on every parallel moment card. ⚠ Two measurements NOT taken: the full-table conflict count for BASE keys (60 s timeout) and which writer writes which value. ⓘ Pinnacle's 4 are gone; the population is entirely Top Shot. [Filing](../overnight/inbox/2026-09-03T0430Z-d25-is-two-populations-and-the-big-one-is-a-unit-mismatch-895-of-895.md) | Claude Code |
| D26 | 2026-08-09 | P3 — **grew 5 → 14** | Duplicate TS player rows sharing one slug | **14 slugs held by 2 rows each, 28 rows**, slugified with the exact expression `get_player_detail` uses (read from `prosrc`). ⚠ **Impact re-TESTED rather than inherited and is unchanged:** all 14 pairs have exactly **1 distinct `name`**, and the RPC counts editions by `(player_id = p.id OR player_name = p.name)`, so the tie-break winner claims both halves — verified on `steph-curry` (16/82 by `player_id`, **144 by name**). The finding is the PRODUCER: three competing `external_id` conventions keep minting twins. **Do not "fix" the data** 🔁 **RE-DERIVED 2026-09-02 with the same slug expression: 12 slugs / 24 rows, down from 14 / 28 — it SHRANK, and all 12 still carry exactly 1 distinct `name`; no other collection is affected.** ⭐ **AND THE PRODUCER DIAGNOSIS IS ONE LEVEL TOO LOW.** The three `external_id` conventions describe what the rows LOOK like, not what makes them. `ensure_players_from_edition_names` mints `<collection_slug>-<name_slug>` for any `editions.player_name` slug that resolves to nothing, `DISTINCT ON (collection_id, name_slug)`, with a `NOT EXISTS` byte-for-byte identical to `get_player_detail`'s — **it is working exactly as written.** The real cause is that `editions.player_name` carries **TWO SPELLINGS of one name** and the slug expression maps every non-ASCII letter to a hyphen, so “Alperen Şengün” and “Alperen Sengun” are two slugs and therefore two players. The DUPLICATE appears **afterwards**, when one row is renamed (`upsert_player_canonical` does `name = COALESCE(v_name, p.name)`) and the two slugs collapse onto each other — corroborated on **12 of 12** pairs, whose numeric row's `updated_at` postdates its twin's `created_at`. ⚠ Corroboration, not proof: `updated_at` moves on any column and `players` keeps no name history. 🚨 **The user-facing half is bigger than this row and is NOT about duplicates:** `get_player_detail` counts editions by the winner's EXACT name, so **Alperen Şengün's page reports 43 editions when he has 74** — 145 editions across 10 players are split, **54 of them on a second, near-empty page**, and today's canonical URLs include `alperen-eng-n`, `temi-f-gb-nl-` and `v-t-krej-`. ⛔ **Not fixed: both remedies are product calls** (normalise the names — which spelling wins? — or slugify on `unaccent`, which changes the canonical URL of every diacritic player on a public site). Filing: [inbox 2026-09-02T1045Z](../overnight/inbox/2026-09-02T1045Z-ten-topshot-players-are-split-across-two-spellings-and-half-their-editions-sit-on-a-second-page.md) ⭐ **Swept across all five collections the same session, and the obvious prediction was WRONG:** Top Shot **10** players / 145 editions / **54 stranded**, LaLiga Golazos **1** / 3 / **1**, AllDay + Pinnacle + UFC **zero**. Golazos is full of diacritics and has almost none of this, because its names are spelled CONSISTENTLY. 👉 **The defect is not “names contain diacritics”, it is “two ingests disagree about one name” — and Top Shot is the only collection with two producers writing `editions.player_name`.** That narrows the remedy: unaccenting the slug would change every diacritic player's URL in five collections to repair eleven players in two | Claude Code |
| D30 | 2026-08-09 | ~~P3~~ **✅ RESOLVED 2026-08-27 (`a44bef5c0`); row updated 2026-09-02** | 3 production-dead components (test-only importers): `components/InsiderSignals.tsx`, `profile/TierBreakdownCard.tsx`, `profile/PortfolioSparkline.tsx` | Re-verified run 3: all 3 exist, only importers are their own `__tests__` files. ⚠ **REGISTER CORRECTION:** the note "deleting any requires removing its `check-brand-tokens.mjs` PROTECTED entry in the same commit" is true for **only 1 of 3** — `PortfolioSparkline.tsx` is listed; the line-174 `InsiderSignals` entry is the **LIVE** `components/analytics/` file; `components/InsiderSignals.tsx` and `TierBreakdownCard.tsx` are not in the list at all. Deleting still mechanically lowers the component ratchet. ✅ **RE-VERIFIED 2026-08-24 (interactive) and D30 IS STILL CORRECT for both profile components** — `grep -rn "from ['\"].*(TierBreakdownCard\|PortfolioSparkline)" app components lib` returns **zero**, and `app/profile/[username]/ProfileClient.tsx` (the page they would sit on) imports `CostBasisCard`, `TopMoversCard`, `CollectionBreakdownCard`, `PublicAchievements` and neither of these. ⚠ **This row EARNED ITS KEEP the hard way: I shipped honesty fixes to both the same day and wrote user-impact claims for them, then had to retract both** (ledger, same date) — the register said it and I had not read it. ⚠ **The tell to record: a `components/profile/*` file with a plausible name AND a healthy test suite reads as shipped. A test-only importer is exactly that shape.** ⓘ Consequence worth carrying into the disposition: `/api/profile/tier-breakdown`'s ONLY consumer is the dead card, so that route is effectively dead too; and `app/api/profile/portfolio-history/route.ts:7` still DOCUMENTS `PortfolioSparkline` as its `/profile/[username]` consumer, which is false (the route is live via `CollectionProfileClient` + `ProfileClient`). Both components are now HONEST as of 2026-08-24, so a revival would not re-ship the defect ✅ **RESOLVED — and it was resolved on 2026-08-27 by `a44bef5c0`; this row simply never said so.** Re-derived 2026-09-02: all three files are GONE from the tree, the brand-token guard's `PortfolioSparkline` entry went with them in the same atomic commit (the guard names protected files individually, so a deletion without the list edit reds CI on its own), and `app/api/profile/portfolio-history/route.ts`'s header no longer names the deleted component — it now names the two real callers, `ProfileClient.tsx` and `CollectionProfileClient.tsx`. ⚠ **Re-deriving a row costs minutes; inheriting a stale one costs a session.** This one read OPEN for six days after it was done. ⓘ **Residual, OBSERVED not actioned:** `/api/profile/tier-breakdown` still exists with its route test, and its only consumer was the deleted card — re-checked 2026-09-02, **zero in-repo fetches** (the `tierBreakdown` hits in `allday-pack-ev` are an unrelated local variable) and no dynamic `/api/profile/${…}` construction anywhere. **Deliberately NOT deleted**: it is a reachable public endpoint, so removing it is a product call, not a cleanup | ✅ closed |
| R39 | 2026-08-22 | ~~P3~~ **⚠ PREMISE SELF-RESOLVED 2026-08-23 — the arms answer now, and the answer is bad (see R50)** | **Two trust-board arms are INCONCLUSIVE, not clean** | The 999s are gone. Re-read 03:34Z: `budget_exhausted: false`, **45 of 45 active boards probed**, `empty_or_error: 0`, **`slow: 12`**. The instrument was never broken — it was reporting that it could not finish a sweep. Now that it finishes, it says 12 of 45 public boards missed their latency budget on that tick, and a 12-day distribution turns that into **11 boards persistently over** (R50). ⚠ **The one-day membership and the 12-day membership are NOT the same set** — `topshot_serial_premiums_board` is in the snapshot and is 3.8% over across 12 days. This row is closed as a metric problem and reopened as R50, a data problem | ✅ closed → R50 |
| R40 | 2026-08-22 | ~~P3~~ **✅ RESOLVED 2026-08-22** | **Two routes logged the SERVICE-ROLE key's LENGTH** | **FIXED.** `app/api/fmv-recalc/route.ts` and `app/api/ingest/route.ts` now log only the presence boolean. No value was ever emitted and the register's "0 leaking hits" stood, but a length is still a property of a credential and it narrows a guess — it was the only near-miss left in the class. The presence check was the genuinely useful half for diagnosing an unset env var, so that is what was kept | ✅ closed |
| R14 | 2026-08-15 | 🚨 **NOT RESOLVED — SHIPPED AS A REGRESSION 2026-08-22, REVERTED 2026-08-23 08:10Z. CLOSED AS WONTFIX.** | 2 `function_search_path_mutable` advisor WARNs on `reconcile_all_saved_wallet_stats` and `rpc_trust_health_precompute_refresh_p` | 🚨 **I BROKE PRODUCTION WITH THIS AND ANOTHER AGENT CAUGHT IT.** `ALTER PROCEDURE … SET search_path = public` on both. **PostgreSQL forbids `COMMIT`/`ROLLBACK` inside a routine carrying an ATTACHED `SET` clause**, and both are PROCEDURES that COMMIT per wallet — so both began raising **`2D000 invalid transaction termination` at their first COMMIT**. `rpc-reconcile-saved-wallet-stats` (jobid 259, hourly at :44) failed **every tick from ~02:15Z to 08:10Z**, freezing the saved-wallet cards on `/dashboard`, `/profile` and `/share`. ⚠⚠ **THE ROW ABOVE IS WHY IT SHIPPED: it said "verified harmless — both are PROCEDURES (`prokind='p'`)", treating procedure-ness as the SAFETY argument when it is the exact thing that makes the ALTER unsafe.** I checked `prosecdef`, `proconfig` and anon EXECUTE, and never checked whether the bodies COMMIT — one `prosrc ILIKE '%COMMIT%'` would have caught it. ⚠ **And I verified the wrong thing afterwards**: I confirmed the ALTER APPLIED (`proconfig = {search_path=public}`), not that the procedures still WORK. ⚠ **It was also a REPEAT** — the 2026-08-10 ledger work had already established that these transaction-control procedures must carry NO `SET` clause. ✅ Reverted by `20260823080934` (RESET, config-only, bodies + `prosecdef=false` preserved); recovery confirmed by OUTCOME: 7 failures all ≤ 07:44Z, 7 successes after, newest 14:44Z. ✅ **Class closed, not just these two: procedures in `public` with BOTH a `proconfig` AND a COMMIT in the body = 0.** ⛔ **WONTFIX — do NOT re-attempt.** The hardening buys **zero** exposure reduction (both INVOKER, anon and authenticated EXECUTE false), and it has now caused one production outage. If it is ever revisited, the attached clause is the wrong tool: set `search_path` as a STATEMENT inside the body, which does not block transaction control | ⛔ closed WONTFIX |
| D39 | 2026-08-12 | ~~P3~~ **✅ RESOLVED 2026-09-01** | `check_unmapped_backlog_growth()` has no cache-age guard — a persistent writer outage would serve the last-good payload forever as current, and a missing row returns `[]`, a clean bill of health manufactured from absence | Latent, NOT live. Cache age re-measured run 3 at **3.7 min** (jobid 261, `29 * * * *`), far inside the 120-min worst case. Still deliberately not auto-fixed: this is the live Telegram/email alert path and a naive "raise when stale" risks a permanently-red arm ✅ **RESOLVED 2026-09-01 — and the register's own caution is what shaped the fix.** Migration `audit_20260902_alerts_see_the_unmapped_backlog_cache_going_stale_instead_of_reading_its_last_good_payload_as_current` (file `20260902064112_…`). ⛔ **The reader was NOT the place to fix it, and that is the finding worth carrying:** `get_pipeline_alerts_core()`'s consumer arm builds its `detail` by concatenating `e->>'open_actionable_rows'`, `e->>'collection'`, `e->>'open_rows'` … , so returning a differently-shaped element (a `{"status":"stale"}` marker) would make every `->>` NULL and `\|\|` over NULL yields NULL — **the "honest" reader would have emitted an alert with a NULL detail.** The array cannot express *unknown* to a consumer expecting backlog rows. **The staleness went on the CALLER instead**, as one extra row in the existing `data_stale` arm's `freshness` subquery, inheriting its severity ladder, its wording and its `active_suppressions` join (suppressible as `pipeline = 'unmapped_backlog_growth_cache'`). ⚠ **The permanently-red risk this row warned about is answered with a measurement, not a promise:** jobid 261 over 30 days is **453 successful runs, gaps min 55 / avg 74 / MAX 422 min (7.0 h)** against the arm's inherited **24 h** — **3.4× the observed worst case**, 24 missed hourly ticks before it speaks, on a job whose own failure rate is 9.1% (15/165 in 7 d). ⭐ **The MISSING-row half needed a `COALESCE` or the fix would have reproduced its own second defect:** `max()` over zero rows is NULL, `now() - NULL` is NULL, and `NULL > interval '24 hours'` is **not true** — so an absent row would have stayed invisible in the new arm exactly as in the reader. `COALESCE(…, interval '99 days')` makes absence the loudest state, and the `metric` label names which case fired. **Post-state proves the arm is SILENT on the live 10.8-min-old cache AND that it CAN fire** (the missing-row predicate is evaluated against `id = -999` and must raise) — a silent arm and a blind arm otherwise look identical. ⛔ **Still true and deliberate:** the reader serves last-good while stale; with this arm alongside, an operator sees the numbers AND that they are old | Claude Code |
| D21 | 2026-08-09 | P3 — **IMPROVED 2.25×** | AllDay `edition_offers` bids stale | Run 4: median **6.06d** (mild backslide from 5.11, within the lane's oscillation; population 2,294 → 2,155). Run 3: median **11.5d → 5.11d**; newest row 0.04h. P1 escalation condition (cross-collection contamination) re-confirmed **NOT met**: AllDay's lane carries **0 of 2,294** `low_ask`, so it cannot contaminate an ask. ⚠ Side-finding, second direction: CLAUDE.md's "TopShot 1,929 low_ask of 6,930 ≈ 28%" is stale — live **12,259 of 12,464 = 98.4%** | Claude Code |
| R41 | 2026-08-22 | P3 | **The roadmap headline metric has TWO live figures in circulation with no stated denominator** | ⚠ **RESOLVED AS A DISAGREEMENT — both are honest, neither is fabricated, and 100% of the gap is one deliberate predicate.** The instruments agree on 4 of 5 collections (All Day 22.8/22.7 · Golazos 0.0/0.0 · UFC 0.0/0.0 · Pinnacle 43.2/42.4). Top Shot is **49.7% vs 34.2%** because `rpc_thp_leg_fmv_coverage` filters TS to canonical `setID:playID` keys, dated in its own comment **2026-08-04, "the TopShot leg is CANONICAL-ONLY"**. Arithmetic closes: 13,241 canonical × 0.497 + 163 = 6,744 / 19,667 = **34.3%**. **Publish it as `49.7% (canonical Top Shot) / 34.3% (all rows)` or fix the producer.** ⚠ Non-canonical rows are STILL BEING MINTED (newest `created_at` 2026-08-20) — a live population, not legacy residue. **Run-4 re-measure (2026-08-27): BOTH figures ROSE — canonical TS precompute 55.0, all-rows 39.21.** Other precompute shares: candy 63.2 · pinnacle 44.0 · allday 23.4 · golazos 0.2 · ufc 0.0. ✅ **DECIDED BY TREVOR 2026-08-28: the gate metric is ALL-ROWS (39.2)** — and ✅ **the leg re-point SHIPPED the same day** (`20260828225605_audit_20260828_r41_fmv_coverage_leg_all_rows_denominator`): the canonical filter and its editions join are gone; ran once live — TS share **38.4**, TS stale **31.7**, other collections untouched; ACLs preserved, secdef drift 0. ⚠ **BOTH TS metrics changed denominator** — the stale arm now carries a ~32.6% structural floor (6,426 dead non-canonical rows) under its breach_at 50, which makes it MORE sensitive to canonical drift (fires at canonical_stale ~26%). Consumer sweep: the share metric appears in NO view or function (⚠ found only after realising `_` is a LIKE wildcard — grep DB objects with strpos, not ILIKE); doc quotes of 49.7/55.0 are all in labelled/superseded historical blocks. Pin file rewritten same commit — its 999 test had to change its error-injection target (dropping `editions` no longer errors the leg, BY DESIGN). Residual: any Cowork artifact labelling the number "canonical" (folder not reachable this session) and the trust-board view's family-comment mentioning "TopShot canonical" — text-only staleness, view deliberately not rewritten (reloptions-wipe risk ≫ comment value) | ✅ closed (decision + re-point shipped) |
| E5 | 2026-08-22 | P2 — **open, correctly instrumented, no action requested** | ~~49~~ ~~37~~ **33** `after()` routes write a terminal `pipeline_runs` row with no invocation heartbeat | `__tests__/after-route-heartbeat-ratchet.test.ts`, asserted `.toBe()` in both directions so a conversion must lower it. Walk is non-vacuous (>400 routes, >50 qualifying). Named as the largest open coded item. 🔁 **RE-DERIVED 2026-09-02: BUDGET 49 → 40 → 37**, and the last three were chosen on MEASURED MARGIN against each route's own wall, read from `pipeline_runs` (73 h retention): `cron/allday-lock-refresh-batch` is the tightest in the fleet — **71 of 73 ticks finish between 270,077 ms and 292,225 ms against a 300,000 ms wall**, worst 97.4%, which is structural rather than a spike (SOFT_DEADLINE_MS is 270,000, so every tick stops with 30 s left and the measured tail already eats 22.2 of them); `wallet-backfill` is one of the few watchlist rows at severity **high** and its longest tick is 261,273 ms, 87% of the wall and one second past its own soft deadline — the risk is the whale, not the p90 of 48,222 ms; `cron/populate-pinnacle-wmc-fmv` peaks at 230,416 ms (77%) and is ONE RPC call, so there is nothing to break out of. ⚠ `duration_ms` on an `after()` route absorbs the terminal write's latency, so these are UPPER bounds — the direction that matters, since the question is whether a tick can reach the wall. ⭐ **The ratchet is a STATIC read and is blind to the property that makes a marker work**: it sees the call, not that the call comes FIRST. New `__tests__/heartbeat-marker-precedes-the-terminal-row.test.ts` plus a pin in `api-wallet-backfill-deep` assert the ordering off ONE interleaved event log (two recorders cannot see an order between them), with a positive control that an unauthorized call writes no marker. Both mutations verified: moving the call after the work reddens the ordering tests while the ratchet stays GREEN, and a hand-rolled marker under the REAL pipeline name reddens both 🔁 **37 → 33 the same day, and THE SELECTION RULE CHANGED — record that, not the number.** Every prior batch picked from `pipeline_cadence_watchlist WHERE is_active`, on the argument that a kill there is *actively misread* rather than merely unlogged. Re-derived 2026-09-02: **the tightest margin left in that tier is `topshot-listing-cache` at 4% of a 300 s wall — the watchlisted tier is done and the risk has moved.** The four converted on wall margin instead, each `max(duration_ms)` over the 73 h retained: `cron/resolve-topshot-stubs` **29,313 ms of 30,000 (97.7%)**, the smallest wall in the fleet and **not watchlisted at all**; `check-alerts` 37,415 of 60,000 (**62%**); `cron/refresh-conflated-editions` 79,059 of 120,000 (**66%**); `cron/alerts-send` 23,067 of 60,000 (38%). ⭐ **`resolve-topshot-stubs`'s maximum is CENSORED AT THE WALL BY CONSTRUCTION** — a tick that crossed 30 s wrote nothing, so it is ABSENT from the distribution rather than at the top of it, and the recorded max can never exceed the ceiling however often the ceiling is hit. **The observed distribution cannot answer the question being asked of it.** 🚨 `check-alerts` and `alerts-send` are the worst cases on the fleet for a reason that is not their number: their OUTPUT is mail and Telegram, so **a killed tick fails by sending nothing** — unfalsifiable from outside. ⚠ Two of the four build their OWN supabase client, so the helper's `db` argument is explicit; **mutation-verified that dropping it makes the marker vanish from the client the route uses**. ⚠ Three of the four ordering pins failed first for reasons unrelated to the marker — a **200 not a 202**, a **GET-only export**, and a handler reading `req.nextUrl` that a plain `Request` cannot satisfy (it throws BEFORE the marker is written, which reads exactly like a missing call). Each is named in the test | Claude Code |
| R11 | 2026-08-15 | ~~P3~~ **✅ RESOLVED 2026-09-02 — the structural fix shipped, and the first draft of it manufactured three false kills** | `detect_stalled_pipelines()` is terminal-row-keyed, so a heartbeating-but-killed pipeline oscillates in and out of "stalled" | **Run 4 (2026-08-27): `candy-listings-indexer` RECOVERED across the 08-27 03:48Z deploy** (pre 87.5% killed → post 0%, kills-script verdict RECOVERED p=2.7e-4), so the cry-wolf instance is gone — the detector currently fires only 2 arms, both documented transitions. The STRUCTURAL fix (accept a `-heartbeat` row as liveness while treating a missing terminal row as incomplete) is still unlanded and will matter again the next time any heartbeated pipeline runs killed for a stretch ✅ **SHIPPED 2026-09-02** (`20260903024204_audit_20260902_detect_stalled_pipelines_says_whether_the_schedule_is_firing`). Three ADDED fields — `heartbeat_last_run`, `uncorrelated_heartbeats`, `classification` (`no_marker` / `invoked_but_never_logged` / `not_invoked`) — read from the `<pipeline>-heartbeat` marker `lib/pipeline/heartbeat.ts` has written since 2026-08-20 and **21 of the 87 active watchlist rows now carry**. ⛔ **The `WHERE` is byte-identical and the row set CANNOT move**: both added LATERALs are unGROUPed aggregates, so each returns exactly one row and a LEFT JOIN to them can neither add nor remove a caller-visible row. This is an ENRICHMENT, never a silencer — a marker must never refresh the real pipeline's silence clock. Proved over the whole population in ONE statement with the threshold forced so every row qualifies: **87 vs 87, 0 only-in-old, 0 only-in-new**. 🚨 **THE OBVIOUS SPELLING WAS WRONG AND ONLY LIVE DATA SAID SO.** Counting a marker as orphaned when `h.started_at > last_run` reported **3 pipelines with orphaned markers — `allday-listings-retry`, `snapshot-pack-asks`, `candy-editions-ingest` — ALL THREE HEALTHY**: their marker is written **2–14 ms AFTER** the terminal row's `started_at` (two clocks inside one route), so a tick counted its own heartbeat as evidence of its own death. With the ±5 s window the helper already documents: **3 → 0**. ⭐ *A plausible mechanism is not a measurement.* **POSITIVE CONTROL** so the counter is not trusted at zero: the same correlation over the full 73 h retention finds exactly ONE uncorrelated marker fleet-wide — `fmv-recalc` at 2026-09-01 00:15:52Z — a real killed tick no alert on this platform could see. COST warm: 1,200 → 1,490 buffers, 5.6 → 6.8 ms against an 8 s ceiling. Grants unchanged; `check_secdef_anon_exec_drift()` 0; scratch dropped. Pinned by `supabase/tests/detect_stalled_pipelines.sql` (7 fixtures, all three classes, both directions of the ±5 s window) and registered in `db-invariants-drift-guard`; **three mutations each redden exactly their own arm**, and live `prosrc` md5 equals the migration body's | ✅ closed |
| R44 | 2026-08-22 | ~~P1~~ **✅ RESOLVED 2026-08-23 ~02:00Z — and the filed remedy really was inverted** | **`/api/ready` had 500'd for 8 days; the obvious fix would have republished an anon leak** | **FIXED, and the diagnosis needed a SECOND cause the filing did not have.** ⚠⚠ The revoke stays — `health_check()` is SECURITY DEFINER and returns user counts, saved-wallet counts, allow-list size, telemetry volume, distinct wallets and `db_size_mb`, and the route spread the WHOLE payload to an anon-reachable surface. **But restoring the grant would not have fixed the route either:** `health_check()` returns `collections` as a `json_object_agg` KEYED BY SLUG and the route called `.map()` on it — TypeError, caught, 500 — and there is no `fmv_pipeline`, `data_integrity`, `sales_pipeline` or `listing_cache` key in the deployed function at all. **The route was written against a shape the DB does not return, and `__tests__/api-ready.test.ts` was green throughout because it MOCKED that invented payload.** ✅ Fix: `public.readiness_collection_stats()` — per-collection `sales_24h` + `last_sale_at` only, **SECURITY INVOKER not DEFINER** (both source tables are RLS-readable by PUBLIC with exactly this filter, so it publishes nothing new AND stays out of `check_secdef_anon_exec_drift()`; re-checked = **0**). Warm-vs-warm cost **10.9 ms / 3,907 buffers, all hits** — a large net reduction against `health_check()`. Unmeasurable fields DROPPED, not nulled. 🚨 **Reading the payload instead of assuming it surfaced a live false claim: Disney Pinnacle sales live in `pinnacle_sales`, so `sales_24h` read 0 against 343 real sales in 24 h, and /disney-pinnacle/market + /analytics told every visitor the market is thin.** Fixed in `20260823021500`; ⚠ UFC's 0 is genuine and was left alone as the no-change control. Shape-break test **proven to fail** against `(data ?? []).map` — ⚠ its first version was vacuous (an object 500s either way; only `null` discriminates). ⚠⚠ **BAND CAVEAT 04:08Z — THE ROUTE FLAPS AND "VERIFIED LIVE" MEANT VERIFIED CALM.** 200 in 557 ms at 04:03Z, **500 at 04:05Z and 04:07Z**, publishing its honest `heavy load … retryable` body each time. It is bound by `authenticator`'s 8 s on the anon path, and `readiness_collection_stats()` measured **as `anon` under an 8 s timeout** takes **2,309 ms calm vs 53,957 ms in a spell** for the same 8,064 buffers. Before this work it 500'd **100%** of the time, so this is a strict improvement — but R46 is the binding constraint and the claim has to carry its window  🚨 **THE BAND CAVEAT ABOVE UNDERSTATES IT — RE-MEASURED 06:45Z WITH THE INSTANCE QUIET (5 active backends, 6 IO waiters, NOT a spell): `readiness_collection_stats()` took 4,863 ms as `postgres` and 24,523 ms as `anon`.** ~8,000 buffers of which **~340 are DISK READS** this instance serves at 10–40 ms each. ⚠ **The 24,523 ms run BEAT a `SET LOCAL statement_timeout = '8s'`** — the recorded overshoot-under-IO-throttle, best-effort not a cap. **So the 500s are NOT only the band, and my "10.9 ms warm" was the fully-cached case and should never have been quoted as the cost.** ✅ **Mitigated**: the SUCCESS path now carries `s-maxage=60, stale-while-revalidate=120` (~60× fewer origin DB hits; the only real consumers are two `sales_24h < 10` caveats for which minute-old data is indistinguishable from live, and an uptime probe reads the STATUS CODE), while the FAILURE path is explicitly `no-store` — **caching a failure is what turned a blip into five minutes on /api/top-sales (R33)**. Pinned by a test proven against BOTH mutations. ⚠ **This is a MITIGATION, not the fix.** The durable fix is to stop counting: the consumers need only `sales_24h < 10`, which a probe bounded to 10 rows answers EXACTLY while reading ~10 index rows instead of 3,844 — deferred because an unknown count must not reach the clients' `?? 0` and read as thin  🚨 **THE MITIGATION DID NOT WORK AND THE BOUNDED PROBE IS NOW SHIPPED.** Caching the SUCCESS path only helps if a request ever succeeds; none did, so `/api/ready` still 500'd **3 of 3**. A 500 is never cached — a cache is not a fix for a read that always fails. ✅ **Fixed by stopping the count**: a probe bounded to 11 rows answers `< 10` EXACTLY at **24 buffers / 3 reads / 63 ms** (vs ~8,000 buffers / ~340 reads / 4.9–24.5 s), ~330× fewer buffers. ⚠ **`sales_24h` is now a BOUNDED PROBE — exact when ≤ 10, NULL above**; `least(n, 11)` would have been a fabricated count, so `thin_volume` carries the answer. 🚨 **The client change was NOT optional and shipped with it**: the old `(sales_24h ?? 0) < 10` coerces the new NULL to 0 and renders **"THIN-VOLUME ECOSYSTEM" on Top Shot** — a false claim about the busiest market, produced by a performance fix. Both clients read `thin_volume === true` (`=== true` because null is UNKNOWN and unknown must not assert thin), with regression controls **proven to redden against the old expression** | ✅ closed (bounded probe shipped) |
| R45 | 2026-08-22 | ⚠ **PREMISE NO LONGER HOLDS 2026-08-22 22:2x PT — the phantom rows are GONE, and their removal is UNRECORDED** (live: `collection_series` holds **1** Golazos row, `1 = Series 1`, against the 3 this row measured hours earlier; no ledger entry and no migration mentions the deletion, so I can attribute it to nobody — re-measure before closing, and if you removed them, ledger it) | **The LaLiga Golazos Collection tab offers a Series 2 and a Series 3 filter that can only ever return nothing** — `/api/collection-series` reads `collection_series` directly to build the dropdown | VERIFIED live: `collection_series` holds 3 Golazos rows and the edition counts are **Series 1 → 575 · Series 2 → 0 · Series 3 → 0**. Corroborated by two independent instruments in the 2026-08-23T0020Z filing (`wallet_moments_cache` 23,351 moments / max edition id **575**; `nft_edition_map` 286 rows / max **572**; `unmapped_sales` 9 rows all-time) with a **positive control** that the predicate can see high ids (2,427 rows above edition 500). ⚠⚠ **DO NOT DELETE THE TWO ROWS ON THIS EVIDENCE.** The filing states its own blind spot: **both instruments read the same Golazos contract and would be blind by construction to a second one**, so the evidence is strong for "the contract we index has 575 editions" and only suggestive for "Golazos has 575 editions". Egress denied the sandbox any upstream check. **Settle the contract question first; the honest interim fix is to suppress a series option with zero editions at RENDER time, which is correct either way** ⚠ **RUN 4 (2026-08-27): CLOSED AS MOOT** — `collection_series` still holds exactly **1** Golazos row (`1 = Series 1`), so the dropdown can no longer offer a phantom option; the defect has no substrate. The ATTRIBUTION GAP stands unresolved (no ledger entry, no migration ever claimed the deletion) and is recorded here as history, not re-opened | ✅ closed (moot; deletion still unattributed) |
| R46 | 2026-08-22 | P2 — **context row: this is the denominator behind R6, R20, R22, R25, R29 and R30** | **The disk-IO saturation is STRUCTURAL, not a spell** | VERIFIED, `pg_stat_statements` since its 2026-08-12 01:34Z reset = 10d 18h with **`dealloc = 0`** (nothing evicted, so the percentages are over the complete population): `sum(shared_blks_read)` **8,227 GB = 765 GB/day ≈ 8.9 MB/s sustained** against a 22 MB/s throttled floor; `sum(total_exec_time)` **1,171 hours in 258 hours of wall clock ⇒ ≈4.5 backends busy at all times on a 2-core instance**; buffer miss rate **1.89%** — the cache is working, the read VOLUME is the problem. **Nothing has to go wrong for this instance to be saturated; the steady state already is.** ⚠ Consequence for every other row: chasing an individual slow query will not move it, and **a fix must not be credited with ending the band without measuring the band before and after**. ⚠ Do NOT re-derive the refuted hypothesis that warming Panini/Candy is waste — both are LIVE public boards (`CANDY_MLB_PUBLIC` / `PANINI_PUBLIC` are true); `collections.is_active` is not the launch switch and the "STAGED: gated pre-launch" route headers are stale. ⛔ **DECIDED 2026-08-23 20:48 PT — Trevor: stay on Small, no spend.** $96/mo (Large) declined = $1,152/yr. **The cost accepted is stated in numbers** in the filing so this no-action is re-checkable: 22.4 MB/s pinned AT the tier baseline · 2,067 IOPS vs 1,000 · `fmv-recalc` 72.7% wall-kills · 66.8% of backfill ticks skipping · 11 boards over budget with **6 of 11 above the 60 s prerender budget at p90 (build-failure class, not latency class)** · `readiness_collection_stats()` 24,523 ms as `anon` on a QUIET instance · `get_series_editions` 97,443 ms. 🚨 **The budget is now permanently at 100% by choice, so every new cron/refresh/index must state its steady-state IO cost and what it displaces.** ⛔ **Do not re-suggest the upgrade** except on: a production build failing on a board read · first revenue or 50+ WAU · a public page serving degraded copy to anon visitors for a sustained window · the hot set passing ~8 GB (which argues Large is ALSO insufficient, a different decision). Conditions + full cost table: `docs/overnight/inbox/2026-08-23T1610Z-R46-…md` | ✅ closed (decided: no action) |
| R47 | 2026-08-22 | ~~P1~~ **✅ RESOLVED 2026-08-23 — both defects fixed, verified in production against the DB** | **`/sitemap/3.xml` truncates on a Postgres statement timeout and serves the partial set under a 200 — and it pages on a key that is 72% ties** | VERIFIED from a production runtime log (`[sitemap] editions page 24000 error: canceling statement due to statement timeout`, 02:19:54Z, response **200**): `fetchAllByCollection` logs and **`break`s**, so segment 3 built its set / player / team universe from **24,000 of 27,246** rows (`editions` re-counted live 2026-08-22 19:44 PT = **27,246**). ⚠ Second defect the first one hides: the paged read orders by `editions.updated_at`, **72% ties, largest group 1,084 against a page of 1,000**. ⚠ `paginated-range-requires-order-ratchet` is GREEN on it — it asserts `.order()` **presence, not key uniqueness**, i.e. the spelling not the property. ⚠ **The symptom is silent by construction:** the four entity-monitor arms that consume segment 3 fail-soft to SKIP and a skip is a green job. ⚠ **The 'four arms are skipping' half of the original filing was RETRACTED by the next dispatch** (all four resolved on `6fce088b`) — a single observation of a failure is not a standing state; the structural half above is what stands  ✅ **FIXED 2026-08-23 (`9b862dc8`), BOTH halves, plus four more instances of the second.** (1) `fetchAllByCollection` throws `SitemapReadIncomplete` instead of `break`ing, and `/sitemap/<id>.xml` serves **503 + `Retry-After` + `no-store`** — not 404 and not an empty `<urlset>`, both of which are affirmative statements; a 5xx makes a crawler retry and KEEP the sitemap it has. ⚠ **The `break` was only half: every enumerator ended `catch { return [] }` and a missing env key returned `[]` too**, so the throw would have been converted straight back into the same empty list. All six read arms rethrow, incl. the `maxRows` exhaustion (a third silent truncation). 🚨 **EIGHT tests pinned the defect, two with it in their NAME**; a block header stated it as a design goal. All inverted, not deleted, each with a no-change control. (2) Re-measured live: `editions.updated_at` = **8,927 distinct over 27,121 rows, 68.4% tied, largest group 1,084 — WIDER than the 1,000-row page** (this row's 27,246 / 72% were dated samples; both moved). New **ban at zero**, population measured first: 6 sites — the sitemap, four backfills, and **`/api/badges`, USER-FACING**, sorting by a query-string column where every allowed value is non-unique. 🚨 **The guard written for R47 could not see the site R47 found** — `fetchAllByCollection` takes its order column as a PARAMETER, so a literal-only detector reported ZERO on it and on `/api/badges`; a sole NON-LITERAL key now counts. And a static check can never see the tiebreaker's VALUE (swapping `'id'` back to `'updated_at'` left the widened ban green), so `assertUsableTiebreak()` checks it where it exists — and caught a live one immediately: `getPackRows` passed `'dist_id'` as BOTH keys. ⛔ **An `if (error) … break` detector was deliberately NOT shipped**: its only hit is `app/api/market/route.ts`, which is already correct (carries `complete`, caches only when complete) — a guard whose sole hit is correct code reddens correct code. ✅ **VERIFIED IN PRODUCTION against the DB, by two instruments**: seg 1 **13,241 = 13,241**, seg 2 **7,283 = 7,283**, seg 4 **5,514 + 2,564** exact, all five segments 200 and well-formed, and — the only check that can see tie-paging loss — **zero duplicate URLs** (distinct == count in both edition segments). ⚠ The 503 path is NOT claimed as verified live; the DB was calm, so only the complete-read half was exercised. | Claude Code |
| R48 | 2026-08-22 | P2 — ⚠ **downgraded on re-derivation; the outage self-resolved, the RATE is the finding** | **`allday-pack-opens-backfill` writes on ~1 tick in 10 while pg_cron reports every dispatch a success** | ⚠ **The filed 'silent for 12.6 h' state had ENDED before the filing was acted on** — re-measured 2026-08-22 19:42 PT: **482 rows written at 02:16:55Z, `ok`**. What survives: **47 `pipeline_runs` rows across 79.0 h against a 6/h schedule (~474 expected) = 9.9%**, **13 of 47 `ok`**; errors are upstream (`events … status 0` / `503`) plus `Timed out acquiring connection from connection pool`. **`succeeded` in `cron.job_run_details` means DISPATCHED**, which is why 141/144 read healthy over a pipeline writing nothing — the documented dispatched/killed/answered split. Not auth (771 × 200) | Claude Code |
| R49 | 2026-08-23 | **P1 — ✅ THE 500 IS FIXED; MODE 2 AND THE SECTION RPCs ARE OPEN** | **26 indexable `/[collection]/series/[slug]` pages were served by a query 3–5× over its own ceiling, deterministically — and a second, unrelated failure hangs a page the server rendered correctly** | ✅ **`get_series_detail` FIXED: 21,229 ms warm at 3,600 editions → 18 ms / 504 buffers at 4,895.** It recomputed six aggregates from 1.16M `fmv_snapshots` rows per request via a per-edition `LEFT JOIN LATERAL`. ⚠ Its own `statement_timeout = '8s'` is INERT under psql/pg_cron but **BINDS on the PostgREST path production uses**, so the measured costs were real and production simply got 57014 — five distinct series URLs, all HTTP 500 in 24 h; R19 counts **259 failures across 38 users in 7 days**. ⚠ **Both obvious rewrites are measured dead ends**: `JOIN fmv_current` / `IN (subquery)` is the recorded 1.05M-buffer shape, and the `= ANY(<array from a CTE>)` variant re-tested 2026-08-23 **TIMED OUT**. Fix = `series_detail_rollup` + hourly `refresh_series_detail_rollup()` (jobid 357, cron_heavy, watchlisted at 180 min). ✅ **Equivalence proved by reading BOTH bodies with the SAME instrument in one query** — all six aggregates exact on 4 series covering every code path, ⚠ **including Pinnacle's floor > fmv quirk, reproduced rather than quietly corrected**. 🚨 **`cron_heavy` lacked EXECUTE and the job would have 42501'd hourly leaving NO `pipeline_runs` row** — silence, not failure, because the log line is inside the body. ⚠ The page's `isEmpty` `?? 0` had to go in the same commit: against a rollup a missing row is UNKNOWN, and `?? 0` published "No editions in this series yet" AND short-circuited the two fetches that would have contradicted it. ⚠ **CORRECTED 04:05Z — "all six return 200" DOES NOT HOLD.** With the DB calm, `/nba-top-shot/series/series-7` and `series-5` **500 consistently at ~8.6 s**; All Day series-1/-4 and Golazos are 200. The detail read is NOT the cause — **`get_series_editions(TS, series-7, 100, 0)` = 6,615 ms / 32,484 buffers** for 100 rows, against the 8 s ceiling, and the page treats that section as STRUCTURAL and throws. The six-for-six 200s were a calmer window. **Honest claim: the DETAIL-read 500 is closed; the page still 500s on its SECTION read for the two largest Top Shot series.** ⚠ **UPDATED 2026-08-23 (R19 per-section, `f2076b31`): that is NO LONGER A 500 — and the query is NOT fixed.** `series-7` now serves 200 with its real name, season and all five totals plus an honest "couldn't load the editions in this series", verified as rendered DOM against a no-change control on the same deploy. **`get_series_editions` is unchanged at 6,615 ms / 32,484 buffers and still loses to the 8 s ceiling; its fix is still the missing precomputed latest-FMV-per-edition object** (shared with R6 and R50). What changed is what the reader is told when it loses. Do not read this as "series-7 is fixed". A real Chromium did show 5 of 5 visible with correct counts and 25 rendered `/edition/` links in that window. ⛔ **STILL OPEN — Mode 2**: `/nfl-all-day/series/series-4` (54 editions, detail 10 ms) was reported rendering all 71,982 chars into `<div hidden id="S:0">` with the Suspense boundary **never completing**. ⚠ **It did NOT reproduce here — a negative result on a DIFFERENT CLIENT, not a closure**: that page rendered visibly in 2.0 s with its grid, but the filing measured from a Cowork cloud browser and client/network/build all differ. It is client-side; no query tuning touches it. **Run 4 (2026-08-27): a SECOND independent negative** — real Chrome on Trevor's box, anonymous, rendered hero + KPIs + populated grid visible ~28 s post-load, not stuck in S:0. Two clients now fail to reproduce; the original single observation is the outlier. Downgrade Mode 2 to watch-only; close it if the weekly rpc-surface-qa never sights it again. ⛔ **Also open and now MEASURED at the top end: `get_series_editions` is 6,615 ms / 32,484 buffers at 4,895 editions** (1,440 ms at 54) — it computes FMV for every edition then takes 100. ⚠ **Its fix is the same missing object as R6 and part of R50: a precomputed latest-FMV-PER-EDITION (~26k rows).** R6 re-measured in-band 04:04Z: `/api/collection-stats?collection=nba-top-shot` **30.6 s → 503**, so R6 does still reproduce under load. `get_series_rollups` untouched | Claude Code — Mode 2 |
| R50 | 2026-08-23 | ~~P2 — measured as a DISTRIBUTION; user impact deliberately NOT claimed~~ **✅ THE SINGLE HIGHEST-VALUE TARGET SHIPPED 2026-09-02 — `allday_scarcity_board` reads `edition_fmv_current`; the other ten boards stay OPEN (P2)** | **11 public board views run persistently over their own latency budgets, one with a MEDIAN 2.8× budget and a worst read of twelve minutes** | 12 days of `public_board_liveness_history`, n = 26–36 per board. Worst by share of samples over budget: `allday_scarcity_board` **86.1%** (budget 8,300 ms · p50 **23,429** · p90 207,092 · max **725,799**) · `candy_offer_spread_board` 85.7% (p50 8,871) · `candy_pack_market` 80.0% (p50 16,064) · `candy_scarcity_board` 73.5% (p50 14,837) · `candy_player_board` 58.8% · `candy_special_serials_board` 57.6% · `topshot_2025_rookie_cohort_stats` 46.4% · `candy_secondary_board` 45.5% · `panini_sale_feed_status` 40.0% · `pack_table_rows` 35.5% · `topshot_set_squeeze_board` 30.8%. ⚠ **`topshot_serial_premiums_board` is in the day's SNAPSHOT and NOT in this list** — p50 **64 ms**, 3.8% over; today's 8,822 ms was its outlier. Diffing the SET against history is what separated it. 🚨 **USER-FACING LATENCY IS NOT ESTABLISHED AND I WENT LOOKING TO KNOCK IT DOWN**: all three backing pages returned **200 in 359–654 ms with no degraded copy**; they are ISR (`revalidate = 1800`), so a visitor gets a cached render and the cost lands on REGENERATION. A "12 boards are slow for users" claim would have been false. Where the cost DOES land: the instance's IO budget (R46's structural saturation, every 30 min per board) and the **production BUILD** — prerendered `/insights` pages get 60 s each and a slow board can fail the whole build, and **6 of the 11 have a p90 above 60 s**. ⚠ **Do NOT raise the budgets to make the arm green** — six are red on a majority of samples, and the fix is the query, not the threshold. **Not shipped: a concurrent session is actively working board MVs and an 11-board materialisation would collide.** Highest-value single target: `allday_scarcity_board` → **SHIPPED 2026-09-02 (Cowork), migration `20260903055218_audit_20260902_allday_scarcity_board_latest_fmv_from_edition_fmv_current`:** the `latest_fmv` CTE no longer `DISTINCT ON`s all 417,577 AllDay snapshot rows; it reads `edition_fmv_current` (hourly materialised latest-per-edition, cast to `numeric(12,4)` so the view column type is unchanged — `42P16` refused the bare `numeric`). Warm A/B on the page`s REAL SELECT (13 cols, family≥3, scarcity>0, ORDER BY scarcity, LIMIT 50): **22,742 → 8,888 buffers (−60%)**, and the snapshot scan (19,969 buffers, 15,528 heap fetches — the part that balloons under saturation) is gone. Same-snapshot SET diff over all 6,190 editions: 6,190 priced both ways, 11 fmv values differ (max \|Δ\| $47.70), 6 confidences — the hourly lag, nothing dropped. `security_invoker=on` re-asserted and verified; live `/api/public/insights/allday-scarcity` answered in 166 ms. **Exit condition:** next 7 days of `public_board_liveness_history` for this board — share over 8,300 ms falls from 86.1% to under 20% (⚠ the probe times `count(*)`, which prunes the LEFT JOIN either way, so read it as a floor, not the page). **Falsifier:** a user-visible fmv on the board that differs from `/nfl-all-day/edition/<id>` by more than one hourly refresh. **Owed:** the other ten boards, one at a time, same recipe where they only DISPLAY the lagging columns. | Claude Code / whoever owns board MVs |
| R51 | 2026-08-22 | ~~P2~~ **✅ RESOLVED 2026-08-23 — RLS enabled on the table; re-verified live, `check_public_security_invariants()` returns 0 rows** (the filing was acted on by another session between 05:10Z and the overnight pass, and the overnight pass reports the same 3/3 security-clean independently — two instruments, one conclusion). ⚠ Original severity stands as filed: instrument, not exposure | **`check_public_security_invariants()` returns 1 row for the first time — `series_detail_rollup` is an RLS-off base table** | ⚠ **The exposure question was ANSWERED, not assumed: there is none.** Its migration ran `REVOKE ALL … FROM PUBLIC, anon, authenticated` and `has_table_privilege` reads **false** for anon SELECT/INSERT and authenticated SELECT/UPDATE; the only reader, `get_series_detail`, is SECURITY DEFINER and bypasses RLS anyway. **The defect is that a clean-for-life security check is now standing red**, which is the documented 'permanently-red instrument' class — leave it and the next reader skims the row that matters. **Two one-line resolutions, neither mine to pick:** `ALTER TABLE … ENABLE ROW LEVEL SECURITY` (no behaviour change given the grants, restores the invariant as written), or teach the check that zero anon/authenticated privileges is compliant (more honest, but a check that accepts more catches less). ⛔ **Do not suppress the row** — suppression is for arms, not instances. ⚠ Read the shape: a SETOF check is **0-rows-clean**, so `count(*) = 1` here is a finding, not health | Claude Code + whoever owns the rollup |
| R52 | 2026-08-23 | **P2 — ⛔ DECISION MADE: DO NOT BUILD IT YET. The binding constraint is R46 (disk), not the query.** | **R6, the series-page reads and part of R50 all want ONE missing object: a precomputed latest-FMV-PER-EDITION** | ✅ Full rebuild measured at **61,076 ms / 65,000 buffers for 27,075 rows**. Piggybacking on `refresh_series_detail_rollup()` (hourly, ~99 s) would cost near-zero extra IO because that pass already computes latest-FMV per edition. **I was going to build it and did not.** 🚨 **THE MEASUREMENT THAT CHANGED THE DECISION, 2026-08-23 15:1xZ: `get_series_editions(allday, series-9, 100, 0)` — 1,889 editions — took 97,443 ms on 14,212 buffers with 1,320 reads (~74 ms PER DISK READ), and the Top Shot series-7 call TIMED OUT even as `postgres`.** ⚠ **Taken INSIDE a spell (31 active backends / 30 IO waiters), so it is NOT like-for-like against the earlier 7,708 ms / 24,700-buffer reading — the timing is confounded and I am not claiming a 12× regression.** The comparable figure is BUFFERS, and **the buffer count went DOWN while the wall time went up**: the cost is the disk, not the plan. **A rollup cuts buffers ~10× and cannot fix ~74 ms/read.** ⚠ And a benchmark taken in a spell cannot verify a performance fix, so "ship it and confirm" was not available. **Four other reasons, stated so the decision can be re-litigated on its merits:** the pages are NOT down (ISR serves 200; the harm is latency on 26 SEO pages, not an outage) · three agents were concurrently editing these files and several of my pushes hit conflicts, one needing reflog recovery · four things I called done this session needed correcting, one a production regression in this exact class (R14) · and adding a rollup+cron per symptom, while others do the same, is how an IO-bound instance acquires a dozen refresh jobs. ➡ **The lever is R46: Trevor's capacity decision.** Build this AFTER that, and measure it in a 20:00–00:00Z window | ⚠ **THE GATE HAS OPENED AND THE ANSWER WAS NO — re-litigate on its own merits, do not leave it parked.** R46 decided 2026-08-23: stay on Small, permanently. So "build this AFTER the capacity decision" now resolves to **build it or drop it, deciding on the piggyback cost alone** — and the piggyback on `refresh_series_detail_rollup()` was measured at near-zero extra IO *because that pass already computes latest-FMV per edition*, which is the one shape E does not argue against. ⚠ The counter-argument also survives untouched: a rollup cuts buffers ~10× and **cannot fix ~74 ms per disk read**, and adding a refresh job to an instance now pinned at 100% of its IO budget is exactly what the R46 decision says must be justified against what it displaces. **Owner: whoever picks it up next — with a measurement taken in a 20:00–00:00Z window, not in a spell.** 🚨 **RE-LITIGATION INPUT 2026-08-23 22:35 PT — R52's object has a SECOND consumer, and it is a 100%-red pipeline, not a latency complaint.** `topshot_serial_board_candidates` (the whole GET body behind `topshot-active-listings-ingest`, red 40/40 for 5 days — #30) opens with an unbounded `DISTINCT ON (edition_id)` over Top Shot's `fmv_snapshots`: measured plan **857,293 rows scanned to return 13,230**, a **~65:1 amplification**, as an `Index Scan` **not** Index Only because `confidence` is uncovered — ⚠ **the 118 MB `INCLUDE (fmv_usd)` index is maintained for this shape and the planner DECLINES it.** ⚠ **R52's "a rollup cuts buffers ~10× and cannot fix ~74 ms per disk read" was reasoned against ISR pages that still serve 200; it does NOT transfer to a caller that dies at a 30 s ceiling**, where a 10× buffer cut is the difference between finishing and not. ⛔ **And do NOT substitute `fmv_current` here** — recorded as ~335 buffers on a literal `IN` but **~1.05M on a JOIN**, and this call site is a JOIN, so the idiomatic fix is the pessimal one at this shape. ⓘ Steady-state-IO argument for the rollup, per the R46 decision: it would **displace** that unused 118 MB index |
| R53 | 2026-08-23 | **P2 — the COST is the finding; R25's "no observability" framing understates it** | **The 8 `rpc_thp_leg_*` trust-health jobs burn 437 minutes of query time a week to refresh 8 numbers, and the worst fails 34.6% of the time producing nothing** | Measured 7 d from `cron.job_run_details` (real durations, not self-reports): **`impossible-parallel` p50 489 s / max 617 s / 9 of 26 runs FAILED (34.6%) / 192 min total** · `fmv-coverage` p50 233 s / 3 failed / 125 min · `serial-supply` p50 80 s / 6 failed / 66 min · `pack-ev` 24 min · `pinnacle-fmv-share` 22 min · `panini` 4 · `fmv-sanity` 4 · `board-liveness` 0. ⚠ **`impossible-parallel`'s p50 is 489 s against a 600 s budget — 1.2× headroom, so ordinary variance kills it**, and a killed run burns up to 10 min and writes nothing: **≈90 min/week of IO for zero output.** 🚨 **THE 999 SENTINEL CANNOT BE WRITTEN WHEN THE FAILURE IS THE TIMEOUT** — the handler's own INSERT is cancelled along with the statement, so the metric goes **STALE rather than reading 999**: `topshot_impossible_parallel_serials` measured **550 min old against a 360 min cadence**. **A sentinel that cannot fire on its most common failure is not a sentinel**, and this is the mirror of R39, where the sentinel COULD fire and was read as a broken instrument. ⚠ Observed 2026-08-23 03:48–03:58Z: `serial-supply` ran its full 600 s while the instance hit **30 IO waiters / 26 queued PostgREST calls**, `/api/ready` 504'd, and an 11 ms function took 54 s — then active backends fell **32 → 12 within two minutes of the leg being killed**. ⚠ **ONE correlated observation and the DIRECTION IS NOT SETTLED** — the spell could equally have made a p50-80 s leg run 600 s. ⚠ In aggregate the family is only ~0.6% of total exec time, so this is a CONCENTRATION problem, not a volume one: **do not quote it as a top-line load source** 🔁 **RE-DERIVED 2026-09-01, same instrument, same 7-day window — THE COST FIGURE HAS FALLEN 2.9× AND MUST NOT BE RE-QUOTED FROM ABOVE.** `cron.job_run_details`, 28 runs per leg: family total **437 → 149 min/week**; `impossible-parallel` **p50 489 → 187 s**, fails **9 of 26 (34.6%) → 3 of 28 (10.7%)**, total 192 → 111 min; `fmv-coverage` p50 **233 → 2 s** (125 → 4 min); `serial-supply` p50 **80 → 12 s** (66 → 15 min). ⚠ **The delta is RECORDED, not ATTRIBUTED** — several unrelated changes landed in between and no cause was measured. **What is still true:** `impossible-parallel` still reaches `max 600 s`, i.e. it still hits the wall, but its p50 now sits at **3.2× headroom** rather than the 1.2× that made ordinary variance fatal. ✅ **And the actionable half of the sentinel finding has SHIPPED: `v_rpc_trust_health_freshness` now publishes `computed_at` / `age_hours` / `is_stale` per metric** (all 19 metrics `is_stale = false` at re-derivation, oldest 5.94 h), so *"the metric goes STALE rather than reading 999"* is no longer invisible — the staleness is a column. ⭐ **The general lesson survives intact and is the part to carry: a sentinel that cannot fire on its most common failure is not a sentinel — measure the metric's AGE, never only its value** | Claude Code |
| R54 | 2026-08-27 | ~~P2~~ **✅ RESOLVED 2026-09-02 — 4 clean ticks, falsifier did not fire** | **`apply-fmv-haircut` failed 6 of its last 8 daily runs — the Top Shot leg specifically had not applied for 3+ consecutive days** | VERIFIED (pipeline_runs_daily 08-19→08-27): `1/7 legs failed: nba_top_shot: upstream request timeout` every failure; it ran ~06:30Z inside the degraded band and the TS leg alone breached the ~120 s gateway cap. ✅ **2026-08-28: cron-job.org job 7593344 moved 06:30 → 22:35 UTC** via the console recipe (Common tab only; save confirmed by POST + the jobs-list next-execution column reading 22:35Z — server truth, not client state). 22:35 verified free against every pg_cron minute at hour 22 (free set was 19/31/35/49/55) and the stagger ban-list. `docs/operations/cron-schedule.md` updated same commit. **Exit condition: the first 22:35Z run writes `ok` with 7/7 legs.** Falsifier: if the TS leg still times out at 22:35Z the band was not the cause — the residual fix is scoping/splitting the TS leg ✅ **EXIT CONDITION MET — CLOSED 2026-09-02.** Four consecutive clean daily ticks at the new time: **08-29, 08-30 22:35:21Z, 08-31 22:35:20Z, 09-01 22:35:19Z, every one `ok = true` with `legs_total 7 / legs_failed 0`** (105–144 rows, $10.3–11.0 k removed per run). **The falsifier did not fire** — no Top Shot leg timeout at 22:35Z — so **the degraded band WAS the cause** and the residual "scope or split the TS leg" fix is not needed. The 6-of-8 failure rate was a SCHEDULE collision, not a query defect | ✅ closed |
| R55 | 2026-08-27 | ~~P2~~ **✅ RESOLVED 2026-09-02 — 4 clean ticks, truncation contract engaged** | **`drain-conflated-subeditions` failed 5 consecutive daily ticks since 08-24 — a TIMER RACE, not slow work** | **MECHANISM (from `extra.step_ms`, which the route was built to record): four step RPCs hit the ~120 s Supabase GATEWAY cap before their own DB `statement_timeout` fires** — `split`/`realign` declared **300 s (structurally unreachable via PostgREST — and the server can keep burning up to 300 s after the gateway abandons the client)**, `seed_miskeyed`/`conflation_guard` declared exactly 120 s, a race the gateway has won every night since 08-24 (`step_ms` 120,3xx). The route's own design (57014 → `truncated_steps`, non-fatal, retried) never engaged because it deliberately does NOT reclassify a gateway timeout — correct, and the runs were red while STILL doing real work (knots 47–91/day, wmc_split 1,007). ✅ **Fix: `20260828231031_audit_20260828_r55_conflated_step_timeouts_beat_the_gateway` — all four declare 110 s**, safely under the gateway, restoring the truncation contract. Bodies re-created VERBATIM from the pins (whitespace-collapsed md5 proven identical to live BEFORE applying — no clobber); pins + drift-guard pointers updated same commit; pg_cron caller (jobid 62) unaffected (attached SET is inert on that path). **Exit: next 20:30Z tick reports `ok:true` (with `truncated_steps` naming any overrunner). Falsifier: another `upstream request timeout` means 10 s of margin is not enough — the step is slow before first byte, and the residual fix is work-scoping** ✅ **EXIT CONDITION MET — CLOSED 2026-09-02, and the truncation contract is DEMONSTRABLY engaged.** Four consecutive clean ticks (08-29, 08-30, 08-31, 09-01), all `ok = true`. ⭐ **The proof is not just the green: 08-31's run carries `truncated_steps: ["seed_recent"]` with `step_ms.seed_recent = 120,285`** — i.e. the step DID overrun, the 110 s statement timeout fired first, the 57014 path reclassified it as a named truncation, and the run stayed green. That is exactly the contract the gateway was stealing. **The falsifier did not fire** — no `upstream request timeout` on any of the four. 09-01's `seed_recent` came in at 79,073 ms untruncated, so the step is variable rather than permanently over. 🆕 **A SEPARATE OBSERVATION, recorded rather than claimed:** `conflated_editions_remaining` reads **1,001 → 959 → 963** across 08-30/31 and 09-01 while the job writes ~1,000 rows a night. **The tick is restored; whether the POPULATION is draining is a different question and three points is not a trend** (a delta between two stocks is neither a rate nor a sign). ⓘ Note `rows_written ≈ wmc_split`, a DIFFERENT population from `conflated_editions_remaining`, so the 1,000/night is not evidence either way. **EXIT: re-read `conflated_editions_remaining` in a week — it is now durable in `pipeline_runs_daily.extra_num_sums`, so the series will exist.** If it is still ~960, open a new row for the drain RATE; do not re-open this one, which was about the timer race | ✅ closed |
| R56 | 2026-08-27 | ~~P2~~ **✅ KNOWN-SET HALF SHIPPED 2026-08-28** | **`topshot-pack-pool-backfill` burned ~74% of ticks re-drawing permanently-empty bundle dists** | ✅ **`20260828231417_audit_20260828_r56_known_empty_dists_get_one_daily_slot`: a dist listed in `topshot_atlas_no_pool_dists` is now eligible in exactly ONE hash-spread 5-min bucket/day** (simulated first: 17 known-empty + 81 fresh in the backlog, each empty gets 1..1 of 288 daily buckets; live draw post-apply returned 3 fresh, 0 empties). ⚠ **This deliberately departs from 20260828025307's "WHERE byte-identical" property and says so in its header** — over a day the eligible population is unchanged, within a tick only known-futile probes are excluded; still scheduling, not pricing; self-heals via the pool-row NOT EXISTS if Atlas ever answers. ⛔ **STILL OWED (the real failure-memory): nothing writes `topshot_atlas_no_pool_dists` since 07-17**, so a NEW permanently-empty dist gets full-rate retries — needs the edge-function/column half (a deploy). Watch: wasted-tick share on the backfill should collapse once the 81 fresh drain; if `0/3 … no editions` failures persist at high rate with the fresh set drained, the 17 are not the whole empty population and the dead writer is the finding. ⚠ **WATCH CONFOUNDED FROM 21:28Z 2026-08-28: a Top Shot Atlas upstream outage (HTTP 530 / Cloudflare 1033, also hitting topshot-moments-hydrator ×41 and offers-sweep ×20) has every tick failing on the FETCH, not the sampler** — first 530 predates the R56 migration by ~1h46m, so R56 is exonerated by timeline; the sampler verifiably draws 3 fresh/0 empty per tick throughout. Do not read any failure rate pooled across this outage | edge-fn half → Claude Code |
| R57 | 2026-08-27 | **P2 — ✅ CLOSED 2026-08-29: root-caused, fixed and VERIFIED** | **`deadlock detected` is a NEW error class on jobid 303 (`rpc-refresh-wmc-fmv-changed`)** — 4 occurrences 08-23→08-26, plus 91 lock timeouts / 557 runs 48h on `refresh_wmc_fmv_changed` | ⭐ **The contention is `refresh_wmc_fmv_changed` FIGHTING ITSELF: it has TWO callers.** pg_cron jobid 303 (`7-57/10`) and `app/api/wmc-fmv-populate/route.ts` (every 5 min). **jobid 303 runs a MEDIAN of 240.8 s** (mean 210.6, max 463.4, n=284 succeeded over 48 h from `cron.job_run_details`), so the route's tick ONE MINUTE later blocks on `wallet_moments_cache` row locks and dies. **The signal is exact: 83 of 84 lock timeouts in 48 h land on :08/:18/:28/:38/:48/:58 — the six minutes following a 303 firing — and the six even-decade minutes are clean** (:58 28/47 · :38 27/45 · :18 22/45 vs :03 0/42 · :13 0/46 · :23 0/44). ⭐ **The asymmetry is built in**: the function derives its budget from `statement_timeout × 0.6`, which is ~18 s over PostgREST but hits the `ELSE interval '300 seconds'` branch under pg_cron, so the 300 s caller starves the 18 s one by construction. ⛔ **Shifting the schedule was rejected on the numbers** — 303 occupies ~4 min of every 10 against a 5-min tick, so exactly one collision per cycle at ANY offset. ✅ **THIS ROW'S OWN CAUTION DISCHARGED WITH A MEASUREMENT, not ignored: split at the 08-28 05:30Z fast-path revert, the lock-timeout rate is 17.0% (53/311) before and 17.9% (45/252) after — unchanged, so pooling across it was safe.** **FIXED** `20260829030000_audit_20260828_rwfc_two_callers_collide_every_ten_minutes` + `c56dd86c9`: `pg_try_advisory_xact_lock` returns **NULL** (not 0 — a skip must not become a fourth meaning of `rows_written = 0`) and the route records `ok=true, rows_* NULL, extra.note='skipped_concurrent_refresh'`. ✅ **VERIFIED 2026-08-29 13:45Z, 123 post-fix runs: ZERO lock timeouts and 37 `skipped_concurrent_refresh` rows.** Against the 17.0–17.9% pre-fix rate, P(0 of 123) ≈ **1.2 × 10⁻¹⁰**, and the 37 skips are the guard observed firing rather than an absence inferred. ⚠ Three residual failures are `statement timeout` (a different error), all inside the 12–13Z wave with 30 of 36 backends in IO wait — saturation, not reentrancy. ⭐ **The registered falsifier (jobid 302) is therefore NOT needed and should not be chased.** ⛔ **Deliberately NOT decided: whether TWO schedules for one drain is right at all**, and 🚨 **jobid 303 writes NOTHING to `pipeline_runs`, so this pipeline's telemetry describes only the minority caller** | Claude Code |
| R58 | 2026-08-27 | **P3 — ⛔ PREMISE REFUTED 2026-08-29; a small residual and a data-hygiene item remain** | ~~13 TS `sets` rows have zero backing editions — their set pages render empty~~ → **they render NOTHING: every setless slug 404s** | **The sample this row asked for was taken, and it overturned the row.** ⛔ **There are no empty pages.** `get_set_detail` resolves a slug against `sets_summary`, a MATERIALIZED VIEW built entirely from `editions_unified` that **never reads `sets`** — so a setless row yields no summary row, the RPC returns NULL, and `layout.tsx:40` `notFound()`s before the first flush. **Verified by the real caller with controls both directions: `/nba-top-shot/set/archive-set-1986-` → 404 + `robots: noindex`; `/nba-top-shot/set/wnba-skyline` → 200.** ✅ **Also unreachable three other ways, each measured not assumed:** the sitemap's `/set/` list is built from `slugifyName(editions.set_name)` (`lib/sitemap-data.ts:545-603`), so a setless row cannot produce a URL; `rpc_search_catalog` filters `n > 0` over `set_id OR set_name`, and all 13 score 0 on both legs; and the ONLY FK to `public.sets` in the whole schema is `editions.set_id`. 🚨 **The finding this row MISSED: the names are corrupt.** 8 of 13 are truncation artifacts with the season range cut mid-string (`Archive Set 1986-`, `Run It Back 2005-`, `Run It Back: Legacies 2014-`) matching **zero** rows in `editions.set_name`; the other 5 duplicate canonical sets carrying 9–1,218 editions (`Rookie Revelation` / ext `185` is a pre-canonical stub keyed by the on-chain id as a string). ⚠ **RESIDUAL, small:** `get_set_detail`'s D20 counts underlying sets without requiring editions, so `wnba-skyline` reads `underlying_set_count = 2` where 1 has editions — ⛔ **whether that renders a visible banner is NOT established** (a keyword scan of the live 200 found no "merged"/"underlying" text). ⚠ **A hypothesis of mine was refuted by its own control:** `the-champion-s-path` is NOT inflated by its trailing-space twin, because D20 matches `set_name_variants`, which derive from `editions.set_name`. ⛔ **No code shipped, for cause:** the fix is one predicate in a **pinned** function = three-file discipline + a ~10–20 s user-facing `PGRST002` window, a bad trade for one cosmetic banner — **batch it into the next migration that must happen anyway.** 🚨 **TWO NEW items found in passing, neither is R58:** (a) the set page's JSON-LD omits the block on a FAILED editions read but a **successful empty** read still publishes `numberOfItems: 0` — the third state, latent on any set that legitimately empties; (b) **`sets_summary` has no `CREATE MATERIALIZED VIEW` anywhere in `supabase/migrations/`** — only `refresh_sets_summary()`; a load-bearing object for every set page exists only in the live DB. **Exit:** `underlying_set_count` counts only sets with editions. **Falsifier for the refutation:** any setless slug returning a 200. 👉 **Trevor's call: the 13 rows** (destructive SQL). ⭐ Cleanup is **durable, measured**: no `auto_*` set row created in 60 days while ordinary set creation continues to 2026-08-28. ⚠ Also 3 TS `sets` rows have `name <> btrim(name)` — the ingest does not trim, which is what made the duplicate. Filing: `docs/overnight/inbox/2026-08-30T0130Z-R58-REFUTED-…` → **2026-09-02 (Cowork): residual (b) DONE — `sets_summary` DDL recorded as migration `20260903064122_audit_20260902_sets_summary_ddl_recorded_from_prod` (IF NOT EXISTS throughout; verified a no-op on prod: 833 rows, ACL and 3 indexes unchanged). Still owed: the D20 predicate in pinned `get_set_detail` (batch into the next three-file migration on that function) and residual (a), the set page`s JSON-LD `numberOfItems: 0` on a successful-empty read — both cosmetic, neither reachable today (a setless slug 404s).** | Trevor (row cleanup) / Claude Code (batched predicate) |
| R59 | 2026-08-27 | P3 — instrumentation caveat, partially addressed run 4 | **`bot_ua` under-catches: session-level funnel counts are still ~99% machine** | VERIFIED (7d): of 4,332 `NOT bot_ua` events, 2,242 have NULL user_agent, 1,535 are one Chrome UA at exactly 1 event/"session" (crawler signature), 250 were literal `Lightpanda/1.0`. ✅ Run 4 added `lightpanda` to the classifier + test. The rest is the heuristic's documented limit (the column records what the UA CLAIMS) — **the durable rule: only wallet-bearing events are a human proxy; never quote `NOT bot_ua` session counts as visitors** | — (rule, not a fix) |
| R60 | 2026-08-27 | P3 — ⛔ **NOT REPRODUCIBLE FROM A CLOUD SESSION; needs a Chrome-capable session** | `/insights/pack-reality` has a 3px horizontal overflow (scrollWidth 726 vs clientWidth 723) — visible horizontal scrollbar | VERIFIED in real Chrome run 4. Nothing in CI measures layout; only `e2e/mobile-layout.spec.ts` could pin it. ⛔ **2026-08-29: attempted and BLOCKED, measured not assumed.** Chromium is preinstalled here and launches, but the egress gateway **403s `www.rippackscity.com:443` on CONNECT** (`recentRelayFailures` names it), so a browser in this sandbox cannot load the page at all; the Vercel MCP fetch returns HTML but no layout. ⚠ **And the obvious "pin it now" is WRONG:** `e2e/mobile-layout.spec.ts` states its own contract — *only properties that are CURRENTLY TRUE are pinned, so red here always means a regression* — so adding this route while it overflows would make the monitor permanently red, which this repo already records as indistinguishable from broken at a glance. 👉 **Order matters: fix first, THEN add `/insights/pack-reality` to that spec's `ROUTES`.** The spec's own `evaluate` already reports the culprit (widest box whose ancestors do not scroll or clip) — reuse it rather than writing a new probe. **Exit:** overflow 0 at 390px and 320px, and the route present in `ROUTES`. | Claude Code (Chrome-capable session) |
| R61 | 2026-08-29 | **P1 — Trevor's call, no code fix exists** | **GitHub honours only ~5 scheduled runs per workflow per day, so every GHA cron above ~5/day is a FALSE DOCUMENT** | Cross-section, 17 workflows, one 24 h window: observed ≈ `min(expected, 5)`. Eight asking 24–96/day all got **4–6 (mean 5.0)** with no relation to what they asked; 8/day→4,6; 4/day→3,3; all three 1/day→**1 of 1**. `offer-fill-backfill` asks 96, gets 5. ⛔ **Not a constant fraction, so "~92% shed" is the wrong statistic** (95% for a 96/day cron, **0%** for a daily one). ⛔ Cause NOT established — per-workflow cap vs per-repo budget (561 runs/day requested) are indistinguishable in one window. ✅ Shipped: `scheduler-liveness.yml` (daily) which catches TOTAL silence only, and prints observed/expected every run. **Exit:** high-frequency GHA schedules moved to cron-job.org or their crons rewritten to state what they get. **Falsifier:** disable a few high-frequency workflows — if the others RISE it is a repo budget, not a per-workflow cap, and the remedy changes. | Trevor (cron-job.org) |
| R62 | 2026-08-29 | **P1 — one credential away** | **The ops EMAIL channel is not configured in production; all alerting rests on a single Telegram bot token** | The sentinel's first durable `pipeline_runs` row (18:42Z) carries `notifications: ["telegram","github-actions-native"]` — **neither `email` nor `email-FAILED`**. Both are pushed from inside `if (RESEND_API_KEY && ALERT_EMAIL)`, so the branch never executed and one of the two is unset. The same consts gate `sendEmail` in `lib/ops-alert.ts`, so `stale-fmv-monitor` and `data-integrity` are almost certainly the same. ⛔ Which var is missing was NOT investigated — reading them is a secret-safety hazard. **Exit:** a sentinel row carrying `email`. **Falsifier:** if a row shows `email-FAILED` instead, the vars are set and the Resend call is failing — a different fix. | Trevor (secrets) |
| R63 | 2026-08-29 | P2 | **`edge-fn-drift` TIER 2 has never run — the authoritative census is dark, and was masked by the run being red for another reason** | 38 of 38 body reads fail `Unexpected token 'E', "ESZIP2.3"` on every run since 2026-08-09. Root cause named: `api()` calls `r.json()` unconditionally and the Management API now serves an eszip bundle. So a function whose IMPORTS match but whose BODY drifted is invisible; tier 1's count is a lower bound. ✅ Shipped: the exit code no longer returns 0 when the census did not run (it did — latent until tier 1 reaches zero). ⛔ Fix NOT attempted: both candidates (parse the eszip / negotiate a files-JSON response) are unverifiable without the Management PAT, and `get_edge_function` is a secret-safety hazard on a fleet where 5 functions carry unrotated `rpc_pls_` literals. **Exit:** a run reporting `content census: N read, 0 failed`. **Falsifier:** the workflow's own pinned baseline says debug the DETECTOR if the count is not 31; it reads **19** and nobody has reconciled that. | Claude Code (needs PAT)  ⚙ **HALF-FIXED 2026-08-30 (`3006520` + `fe9559b`)**: the READER is fixed (`apiBody()` handles the eszip body; old shapes still work) and the calibration run answered: containment matched **0 of 38** — Supabase's bundler TRANSPILES before bundling, so byte-containment can never census this fleet. The script now reports tier-2-did-not-run honestly (positive-control rule; 28/28 tests) instead of crashing on JSON.parse. **STILL OPEN — the census itself:** parse the bundle with `@deno/eszip` (`Parser.parseBytes`→`load`→`getModuleSource`, verified in Node 2026-08-30; plain `parse()` crashes in Node) and compare against `typescript.transpileModule` of the repo side — needs the devDependency added where `npm ci` runs natively.  ✅ **CLOSED 2026-08-30 (`7743d01`→`c0f27da`)** — PARSE MODE IS THE CENSUS, and it ran: `@deno/eszip` devDependency added; the entrypoint's stored module source is extracted (production bundles reference the function's OWN modules by RELATIVE specifier — `source/index.ts`, 0 `file://` among 545+ specifiers/bundle; learned from the run's own log-safe failed-pick diagnostic, two dispatch iterations) and compared through the canonicalising build→parse roundtrip (`scripts/lib/eszip-source.mjs`), with a calibration rule: mismatches are findings only once ≥ 1 function matches. `typescript.transpileModule` was NOT needed — same-machine canonicalisation replaces it. Run 33338453882: **38/38 parsed, 0 failed, 13 matched (all `canonical` — full calibration), 25 content-drifted.** Tier 1's 19 ALL corroborated (zero tier-1 false positives) **plus 6 drifts tier 1 could never see**: `backfill-allday-pack-supply`, `backfill-pack-opens-api`, `ingest-allday-pack-opens`, `ingest-topshot-pack-opens-history` (all four gate-key-blocked — correctly in the DO-NOT-REDEPLOY list), `snapshot-institutional-wallets`, `sync-nba-projections`. **Exit met verbatim:** `content census: 38 body/bodies read, 0 failed`. The 25 redeploys are R64/R21 operator work; the falsifier's 31-vs-19 baseline note is now moot (the baseline predates six 08-15–08-30 redeploys). |
| R64 | 2026-08-29 | P2 | **19 edge functions are deployed without their import map** — the drift check's tier-1 proof | Named in every `edge-fn-drift` run: `compute-*-pack-ev` ×3, `ingest-pinnacle-mints`, `ingest-topshot-atlas-pool`, `pinnacle-owner-discovery{,-forward}`, `sales-serial-backfill`, `scan-{pinnacle,ufc}-wallet`, `seed-{allday,topshot}-pack-distributions`, +7. Omitting the map turns a stale function into a hard-down one on the next deploy. ⚠ **This is a LOWER BOUND** — see R63; the body census that would find the rest has never run. **Exit:** each redeployed with `deno.json` in `files` AND `import_map_path`, verified by the next cron tick's `pipeline_runs` row. **Falsifier:** if tier 1 drops to 0 while R63 is still dark, the run goes GREEN having measured nothing — which is why R63's exit-code fix shipped first. | Trevor (deploys) |
| R65 | 2026-08-29 | P2 | **10,483 lines of edge-function code cannot enter the JS coverage gates, and extraction is the only lever** | 38 functions, median 246 code lines, and **all 38 use `Deno.*` globals or `serve()`** — none is importable by vitest, so a coverage `include` over `supabase/functions/*/index.ts` is not achievable without a second (Deno) coverage toolchain. ⚠ **Only 6 of 38 import from `_shared/`**, which IS in the primary gate (29 modules) — so the path exists and six take it. `compute-topshot-pack-ev` alone is **1,273 un-extracted lines** computing the product's headline number. ⛔ **Two wrong readings recorded so nobody repeats them:** "38 files in no gate" frames a toolchain limit as an oversight; and inferring from ONE `_shared/`-importing test that the logic is extracted by design understates it by 32 functions. **Exit:** the un-extracted line count falls. **Falsifier:** if a Deno coverage toolchain is stood up instead, this row's premise (extraction is the ONLY lever) is wrong. | Claude Code / design |
| R70 | 2026-08-29 | ~~P2~~ **✅ RESOLVED 2026-09-02 — and the stated blocker was FALSE: the picker is a DB function, so the entire fix was a migration and needed no deploy at all** | **`sales-counterparty-backfill` walked past the mainnet19 spork, and the access node answers `200` WITH NOTHING — so no instrument can tell** | **FIXED by `audit_20260902_sales_counterparty_backfill_gets_a_spork_floor_and_a_second_pass`.** ⭐ **This row said the fix was worker code needing `wrangler deploy`. It is not** — candidates come from `claim_sales_counterparty_batch`, a DB function, which now carries a `floor_sold_at` bound; **re-read a 'blocked' item's blocker before inheriting it.** ⛔ **AND THIS ROW'S BOUNDARY NUMBER IS WRONG.** It puts the last productive point at `2023-11-08T14:39Z`, read from the CURSOR — but the cursor advances past rows that were MISSED, so it is not a decodability timestamp. Read from the rows actually written instead: the worker's last productive hour (2026-08-28 17:00Z) recovered down to **sold_at 2023-11-08 19:41:13Z** and no further. 24 fresh `pg_net` probes agree and bracket it tighter: `Pending` at 14:01:30 / 14:30:16 / 15:00:11 / **15:58:12**, `Success` at **18:51:39** and everything after — and `sales` holds NO row between those two, so the shipped floor `2023-11-08T17:00:00Z` partitions the population exactly. Sizing re-measured: 2,308,045 outstanding, **450,987 (19.5%) above the floor** and back in front of the walk (cursor reset in the same migration, because resetting without the floor reproduces this state), 1,857,058 (80.5%) below it. ⛔ **CORRECTED SAME NIGHT — 450,987 IS REACHABLE, NOT RECOVERABLE, and I published it as recoverable.** The tick after the one I quoted recovered 0, and the next five did too (0 of 720, ~82 s each), all `allday_studio_history_v1`. Probed: **21 of 21 hash-bucket-sampled studio-history txs** (both `allday_` and `ufc_`, two bucket moduli) are `NFTStorefrontV2.ListingAvailable` with **zero Withdraw events**, against a 3-of-3 positive control on `onchain` Top Shot rows — a listing moves no NFT, so no seller exists in it. **DECODABLE ≈ 37,603 (8.3%)**; 408,309 (90.6%) are studio-history listing rows and a further 4,959 `topshot_marketplace` rows measured **0 recovered of 480 attempted** over four consecutive ticks. ⭐ That last figure corrects my own PROBE METHOD: *has a Withdraw event* is NECESSARY, not SUFFICIENT — 1 of 8 sampled topshot_marketplace txs had one and none converted, so **size a backlog by what the pipeline WRITES.** topshot_marketplace is deliberately left in scope: 4,959 rows is ~3.4 h of ticks and BOUNDED, unlike the 408,309. Fixed by `20260902050149_…_excludes_studio_history_rows_whose_tx_is_a_listing_not_a_sale`, the same exclusion `lock-check-batch` already documents for Golazos. ⭐ Reachable and convertible are different populations, and a floor tells you only the first — **size a backlog by sampling the CONVERSION, and watch the SECOND tick.** ⭐ **The floor is a property of `rest-mainnet.onflow.org`, NOT of Flow history** — 2,339 rows below it, back to 2021-07-31, were recovered on 2026-07-19 by another path, so this row's `SPORK_PROXY_URL` observation stands as the route to the remaining 1.86M. ⚠ RESIDUALS, both operator: the worker still reads `res.ok` not `execution`, so it cannot see a FUTURE prune horizon (needs `wrangler deploy`, deliberately not committed — repo ahead of what runs is its own defect), and the Cloudflare cron should be slowed once the 450,987 drain in ~13 days. — A step change, not a decay: 6-hourly applied counts run 4,619 → 4,616 → **0**, then **311 more runs at zero** — ~383 runs, ~36,600 rows found, **33 h, nothing applied** — while `rows_found` never dips, the cursor keeps advancing, and `ok: true` is reported on 562 of 571 runs. ⛔ **THE OBVIOUS ATTRIBUTION IS WRONG AND I NEARLY FILED IT:** the Top Shot 530 began 08-28 ~17Z, one hour earlier the same evening, but this worker never touches Top Shot — it decodes from `rest-mainnet.onflow.org`. ✅ **Cause bracketed to 22 MINUTES by the pipeline's own cursor** — last productive `2023-11-08T14:39Z`, first dead bucket ends `T14:17Z` — the **mainnet19 boundary, which CLAUDE.md records only as “~2023-11-08”**; confirmed by a controlled probe pair (pre at 13:59, post-control at 2023-11-09 00:02). 🚨🚨 **The mechanism is worse than a 404, and it is this platform's own defect class arriving from OUTSIDE: a pre-spork tx returns `200 OK` with `execution: Pending`, an empty `block_id` and `events: []`.** Success with nothing in it — so the decoder's miss is indistinguishable from a genuinely undecodable row, **no error is ever raised**, the hardcoded `ok: true` at `index.ts:186` never flips, and `rows_written` is *honestly* 0, which makes every fleet instrument read **idle** rather than **blind**. ⚠ **Two overclaims removed before filing, both mine:** it is **NOT permanent loss** (rows stay NULL for a later pass), and the 846 min per 48 h is **worker WALL time dominated by Flow REST round-trips — the DB share is UNMEASURED**, so it must not be quoted as a database cost. ⭐ **The repo already owns the fix and this worker does not use it:** `backfill-topshot-buyers` decodes via `SPORK_PROXY_URL` back to the mainnet17 root floor `2022-04-06T18:20Z` and already classifies this exact condition as **`spork_floor`** rather than a fault; this worker's `wrangler.toml` asserts the opposite premise as verified fact. 👉 **Owed:** classify the 100%-miss case, then bound the queue at the reachable floor or route decodes through `spork-proxy`; a cursor reset only once the decode path can serve that corridor. **Exit:** a run with `rows_found > 0` and `recovered = 0` that reports `ok: false` or an explicit `spork_floor`. **Falsifier:** probe a tx just after `2023-11-08T14:39Z` (expect a populated `block_id`) and one just before `T14:17Z` (expect `Pending`, no events). Filing: `docs/overnight/inbox/2026-08-30T0300Z-the-counterparty-backfill-walked-past-the-mainnet19-spork-…` | Trevor / Cloudflare — it self-schedules on CF Cron Triggers every 5 min, with no pg_cron job, no GHA workflow and no cron-job.org entry (all three checked), so this session can neither stop nor deploy it |

---

## RESOLVED

| id | resolved | finding | fix | revert path |
|---|---|---|---|---|
| R79 | 2026-09-02 | **CI estate audit: the push-time smoke test measured the PREVIOUS deploy (`sleep 45` against a 110–124 s build); `workers/**` was type-checked by nothing (root tsconfig excludes it, vitest strips types); `run-db-tests.sh` passed at zero files; ten ops workflows did a bare `cat` of curl's output under `bash -e` (a DNS failure killed the step before its own `::warning::`); three GHA-only callers read a 401 as a transient warning; `allday-ingest.yml` asked 72×/day for a route that returns `skipped` unconditionally; `topshot-listing-cache.yml` + `cron-schedule.md` claimed an rpc-pipeline backstop step removed 2026-06-25** | **Shipped in one commit (2026-09-02):** `smoke-tests.yml` now triggers on `deployment_status` (Production + success) instead of `push`, concierge armed only for schedule/dispatch (tests updated); new `workers-typecheck` CI job over `workers/*/tsconfig.json` with a count floor of 3 (`@cloudflare/workers-types` added to root devDeps); `run-db-tests.sh` fails under 90 files (183 today); `cat … 2>/dev/null \|\| echo "<no body>"` in 7 workflows; `401\|403 → exit 1` in badge-sync ×2, pinnacle-owner-discovery (backward), topshot-sales-history-backfill; `allday-ingest.yml` deleted; listing-cache claim corrected in both files; `rpc-pipeline` lost `--retry 2` on its six unlocked synchronous POSTs (`--max-time 200`); `migration-autorecover` fails on a stopped rebase and asserts `ls-remote == HEAD` after push; `timeout-minutes` on all 14 ci.yml jobs + 8 sweeps; `permissions: contents: read` on ci.yml + 16 workflows; dispatch inputs and the smoke secret moved to `env:`; five duplicate 9-line comments in ci.yml collapsed to pointers. ⚠ **Watch (falsifier):** a `Smoke Tests` run with event `deployment_status` must appear within a day of a code push; if none does, Vercel is not posting GitHub deployment statuses and the push trigger must return WITH a wait sized from the measured build. **Not shipped, deliberate:** docs-only path filter for CI (M1), pre-push hook (M2), unit-test sharding (M3), composite `rpc-call` action (M11), SHA-pinning (L2), retiring one of three `/api/smoke-test` concierge callers (L4, needs the cron-job.org console). Full report: the session's `RPC CI Estate Audit` artifact | `git revert` the commit (message `ci: audit drain …`); each item is independent inside it |
| R67 | 2026-08-29 | **Wall-clock-dependent tests had no suite-level detector, and a grep cannot be one** | **FIXED — and deliberately NOT the way this row proposed.** `scripts/clock-sweep.mjs` runs the suite once per sampled UTC hour and **diffs the failing SETS**; a test failing at some offsets and passing at others is the finding. Daily `clock-sweep.yml`; the clock is shifted by an opt-in `RPC_CLOCK_OFFSET_MS` shim in `vitest.setup.ts` that moves only the ZERO-ARGUMENT `Date`. ⛔ **R67 filed the `sudo date -s` version and declined to ship it**, with the falsifier *"if it reds for runner reasons rather than test reasons, revert"*. Shifting IN-PROCESS removes that failure mode **by construction**: no privileges, nothing outside vitest, and a failure caused by the runner or by genuinely broken code fails at EVERY offset, so it is classified ALWAYS-FAILING and does not trip the sweep. **The instrument cannot cry wolf about its own environment.** ⚠ **The sampled hours are chosen against the residue classes, not spread evenly** — an even spread is exactly what a `% 6` predicate survives. `0, 5, 13, 20`: no two agree under mod 6, 3 or 2, and hour 0 is load-bearing twice (the `% 6 === 0` notify branch AND the early-UTC window where a date parsed as a datetime reads as elapsed). Asserted, not merely chosen. ✅ **Positive control before trusting it**: a temporary file carrying all four classes — the `% 6` shape, the date-as-datetime shape, a genuine pass, a genuine always-failure. Both clock-dependent shapes flagged with their hours; the genuine failure classified as NOT clock-dependent; the pass ignored. 🚨 **THE FIRST REAL RUN CAUGHT ME TWICE, and both are recorded because the second is a standing limitation.** (1) My own `expect(RPC_CLOCK_OFFSET_MS).toBe("0")` is false BY CONSTRUCTION while the sweep runs — **I shipped a permanently-noisy instrument into the replacement for one declined for being permanently noisy**; now asserts the property that holds under both conditions, verified at three offsets. (2) ⭐ **The shim cannot reach a CHILD PROCESS.** `find-future-dated-ledger-headings.test.ts` computes "today in Pacific" in the vitest process and runs the detector via `execFileSync`; a child starts with the real clock, so parent and child disagree under the sweep in a way they never do in CI. That difference gets a **third bucket — UNMEASURED**: printed every run, explicitly *not* a clean bill, and not a failure. The predicate is a PROPERTY of the file (does it spawn a child), not a name list, so a new shelling-out test is covered with no edit. **25 tests, 11 mutations red across two rounds** — and **two survived the first round**, both real gaps: reporting an always-failing test, and an exemption widened to everything while the other bucket kept the same entry (the buckets are now asserted to be a PARTITION). ⛔ **What is still NOT covered, stated rather than glossed:** anything a child process decides, and any clock-dependence that needs more than a 24 h span (a month- or year-boundary shape). **Exit for those: unmet.** | see the 2026-08-29 ledger entries; `git revert` removes `scripts/clock-sweep.mjs`, `__tests__/clock-sweep.test.ts`, `.github/workflows/clock-sweep.yml` and the `vitest.setup.ts` shim, which is inert when the env var is unset |
| R66 | 2026-08-29 | **`next/og` reached a third-party CDN for glyphs at RENDER time, on the path a social crawler waits on** — filed as 6 of 44 OG routes rendering a hardcoded emoji (`cdn.jsdelivr.net/.../twemoji`), owner `Trevor / design` because removing a load-bearing emoji would be a regression, not a fix | **FIXED, and the re-measurement found a SECOND remote fallback nobody had recorded.** `next/og` goes remote for an emoji **and** for any glyph the supplied fonts miss (`fonts.googleapis.com/css2?family=Noto+Sans+Symbols`). One character per `ImageResponse`, network closed: jsdelivr for `1f3af 2b50 1f4e6 1f389 1f3b4 1f48e 1f3c6 26a1 1f4da 1f4b0 1f392 26a0`; **Google Fonts for `2605 25c8 25a3 25a6 2192 2191 2193 2190 25b2 25bc 2713 2715 2153 2116 203e`**; local for every accented Latin name that matters here (Jokić, Dončić, Porziņģis, Şengün, Bogdanović) and `· – — • … × −`. ⭐ **The first probe got it BACKWARDS** — rendered with no `fonts` option it showed `→` fetching nothing, and I nearly filed *"the register wrongly flagged `insights/serial-premiums`"*. Satori's BUNDLED default covers arrows and production never uses it; re-run with `brandFonts()` supplied, `→` fetches. **A probe whose harness differs from production in the one dimension the answer depends on is not a measurement of production.** **The design call:** `lib/og/marks.tsx`, 11 original monoline marks in pure inline SVG (0 fetches, no font lookup), drawn in each card's own accent — Twemoji's rounded cartoons were the one element on a brutalist typographic card drawn in someone else's language, and at 20–26px their detail was not legible anyway. `deal` 🎯→reticle, ⭐→star; `pack`/`pack/lifecycle` 📦/🎉→sealed pack/burst (**the one place the glyph carried STATE**); `profile`'s 7-entry map + its `★` fallback (not an emoji — the *other* remote path), with the mark now CARRYING the tier so the 6px dot is gone; `serial-premiums` `→`→drawn arrow (**a relation between two numbers, so it could not be dropped**); `collection`'s **140px hero**, where every registry icon is an emoji so the card's largest element reached the CDN **through DATA** — replaced by an accent rule with the wordmark raised 76→96px, registry untouched. All five cards re-rendered and reviewed as PNGs. ⚠ **Instrument change is the durable half:** `api-og-cards-render-sweep` STUBBED `twemoji`, so it rendered every card, stayed green, and the CDN dependency shipped anyway — stub deleted, and verified a throwing fetch makes the render REJECT rather than degrade. ⭐ **Two instruments covering each other's blind spot, proved by a separating mutation:** `→` back in `serial-premiums` reds the source scan and leaves the sweep GREEN (that card takes its fallback branch under the sweep's fixtures). New guard `og-cards-render-no-glyph-they-must-fetch` replaces `og-cards-do-not-silently-acquire-a-cdn-dependency` (deleted) — a **ban at zero**, and a SUPPRESSION list not a detection list: `COVERED` enumerates what is *proven local*, so the next pasted emoji is flagged by default. 4 mutations red; `og-marks.test.tsx` adds 5 more incl. a positive control that a glyph in the same position still escapes. ⛔ **Still open:** none of these routes sets an explicit `maxDuration` | see the 2026-08-29 ledger entry |
| R72 | 2026-08-29 | **Every OG card read was an UNBOUNDED `fetch` on the path a social crawler holds open** — 30 bare calls across 28 files under `app/api/og/**` + `lib/og/**`, **zero** carrying an `AbortSignal`, against CLAUDE.md's standing *"Bound every `fetch` — no default timeout"*. Found by finishing R66: after the two third-party fetches came off the render path, this is what was left on it. Not hypothetical — the unbounded brand-font fetch stalled a card that renders in **83 ms** out to a **60,000 ms** CI timeout (run 4202) | **FIXED.** `lib/og/og-fetch.ts` → `ogFetch()`, a drop-in with a deadline; a caller's own `signal` WINS (the helper adds a bound where there was none, never overrides a deliberate one — composing wants `AbortSignal.any`, absent on some runtimes these routes deploy to). ⚠ **10 s is a PRODUCT BUDGET, not a latency percentile, and that is the whole decision** — not *how long do these APIs take* but *after how long is a degraded card better than no card*, since a crawler still waiting has already shown a bare URL. **Cost stated, not hidden:** `/api/public/insights/pack-sniper`, production runtime logs, 6 h to 2026-08-30T04:33Z, **n=40** — min 385 ms, median ~1.9 s, tail 6.1/6.7/7.8/7.8/**10.9** s; at 10 s **one of forty** aborts into the honest fallback, at the tempting 5 s it is **four**. ⚠ One board, one window, and it is the PAGE's `limit=50` shape while a card asks `limit=3` (or `limit=200` for its count) — **a sample, not a census; re-measure, do not quote**. ⭐ **The safety check that had to come first: what an abort RENDERS.** A bound that turned a slow read into a confident *"0 sales this week"* would be worse than the hang. Every call site sits in a `try` whose `catch` leaves `fetched` **false** → `boardEmptyCopy`'s honest branch; verified mechanically (18 of 19 `fetched = true` gated on `r.ok`, the 19th a Supabase read behind `if (error) throw error` that makes no `fetch`). ✅ Guard `og-fetches-are-bounded` — ban at zero on bare `fetch(` in both trees. ⚠ **Deliberately a SOURCE ban:** `api-og-cards-render-sweep` cannot see this (its stub answers instantly, so bounded and unbounded are identical to it) and mocking a HANG would need a 10 s wall-clock wait per route — the wall-clock-dependent shape this suite has been burned by twice. ⭐ The exemption list is checked **both ways**: an exempted file losing its bound reds, and one that stops calling `fetch` reds. **9 cases, 6 mutations all red**, incl. `AbortSignal.timeout(2_147_483_647)` — the unbounded case wearing a signal. ⚠ Trap worth recording: **`spy.mockRestore()` CLEARS `mock.calls`**, so asserting after the `finally` fails on a correct helper | see the 2026-08-29 ledger entry 🚨 **THE CLASS RECURRED 2026-09-03, OUTSIDE THIS BAN'S GLOB.** `og-fetches-are-bounded` walks `app/api/og/**` + `lib/og/**`; **`app/api/badge-image` and `app/api/moment-thumbnail` are in neither**, and both called `fetch` with no `AbortSignal` AND no `try/catch` on the edge runtime — so a slow CDN threw a 500 instead of a status `<img onError>` could act on. Found in the LIVE error table, not by a grep: **463 `TimeoutError: The operation was aborted due to timeout` across 69 users in 24 h.** ⭐ **This ban did everything right except choose its population from the property instead of from the incident** — third instance of that shape in this repo (see `docs/reference/testing-and-ci.md`). Both routes now bound at 8 s and answer 502; the new `__tests__/image-proxy-routes-bound-their-upstream.test.ts` bans at zero on the three user-facing image proxies and RATCHETS the remaining `app/api/**` at 26. |
| R68 | 2026-08-29 | **Nobody could tell whether four production ingest endpoints ran or worked** — `rpc-pipeline.yml` could not fail, four of its six routes wrote NO run record, and it fires ~3×/day | **FIXED, both halves.** ⚠ **The routes first, because that is the durable record**: `fmv-backfill`, `backfill`, `cron/price-snapshots` and `backfill-player-names` now write a terminal `pipeline_runs` row on **every exit path past auth**, and `ingest` — which logged two SUB-steps and never its own outcome, on the fleet's highest-volume endpoint — got the heartbeat + terminal pair under its own name (`after-route-heartbeat-ratchet` budget **41 → 40**). New helper `lib/pipeline/terminal-run.ts`, sibling to the heartbeat helper and written for the reason its header records: five routes once hand-rolled that contract and no two agreed. ⚠ **Its counters default to `null`, never `0`** — a route that counted nothing must publish *not measured*, and `log_pipeline_run` stopped COALESCEing an explicit NULL to 0 in migration 20260829040000, so the NULL now survives. **Then the workflow**: per-step tolerance is KEPT (one bad endpoint must not starve the rest — the 2026-06-25 restructure), and a new non-tolerant gate fails the job when **every** endpoint failed. That threshold is deliberate: partial failure is normal and self-heals next tick; total failure is an outage and had no signal at all. Two steps that captured a status and never tested one (`backfill-player-names`) or captured none at all (`price-snapshots`) now do both. ⚠ **A missing status file counts as a FAILURE, not an unknown** — a step killed by its own `timeout-minutes` writes nothing, which is exactly the case that must not read as healthy. ⚠ **A `?? 0` note that was NOT a blanket ban**: the first draft banned `p_rows_*: 0` across these routes and flagged 13 sites in `fmv-recalc`; opening all 13, **twelve are genuine measured zeros** (empty sweep window, a delete that wrote nothing, a Step-1a failure before any work) and **exactly one was the fabrication** — the `fatal_after_throw` row, where the run aborted at an UNKNOWN point. That one is fixed and pinned; banning the other twelve would have punished accurate code. What is ratcheted instead is the structural cause: routes still hand-rolling the RPC, budget **1**, no slack. ✅ **THE TWO LAYERS WERE OBSERVED DISAGREEING, WHICH IS THE DESIGN WORKING.** On the 02:20Z verification run the gate step ran, inspected **6 of 6** (its non-vacuity assertion satisfied against a real population, `EXPECTED_STEPS` matched) and printed **`6 of 6 endpoints returned 200`** — while `pipeline_runs` recorded `ingest` **`ok:false`** on the same tick (Top Shot GraphQL 530). ⚠ **So the job's green is honest about what it measures — HTTP reachability — and is NOT evidence the pipeline worked.** `/api/ingest` returns 202 from `after()`, so a 200 is returned before the work is attempted; the durable row is the only place the outcome lives. **Anyone reading this workflow's badge must go to `pipeline_runs` for health, and that is now possible for all six.** **16 tests**, including the gate's own `run:` body **executed under bash against fixtures** (a YAML assertion cannot tell you whether the script works), and **nine mutations, all red** — gate never fails · a step made intolerant · a dropped status write · `EXPECTED_STEPS` drift · the gate's list shrinking · a missing file read as 200 · a route's log calls removed · the fabrication restored · `ingest` losing its heartbeat. ⛔ **Still open and stated rather than glossed:** a `maxDuration` kill cannot write a terminal row, so an absent row on `backfill` (300 s cap) still has two readings; and R61 — the workflow gets ~3 of its 72 scheduled ticks — is untouched by this and remains Trevor's | see the 2026-08-29 ledger entry |
| R69 | 2026-08-29 | **The canonical open list was the one coordination file with NO guard — and four of its rows had been rendering into the wrong columns** | **FIXED.** `scripts/check-register-integrity.mjs` + 18 tests + a `register-guard` CI job (fetch-depth 2, push-only, mirroring `ledger-guard`). Three arms: no id vanishes between HEAD~1 and HEAD (ids are tracked as a SET, so an OPEN→RESOLVED move is not a loss), every row matches the width **its own section header declares**, ids are unique. ⚠ **The four data defects are one shape**: GFM splits a row on `\|` BEFORE parsing inline code, so a raw pipe inside backticks opens a column (R26 `/(ts\|tsx)$/`, R31 two brand-title strings, D30 `(TierBreakdownCard\|PortfolioSparkline)`), and cells past the header width are **discarded by the renderer** — R46's `owner` column had vanished outright. **Every character is still in the file, so a grep finds it all**, which is why four audit runs missed it. ⚠ **Two corrections to the guard, both from running it against the real file rather than a fixture**: hardcoding `{OPEN:6, RESOLVED:5}` flagged all 48 resolved rows (the width is now header-derived and cannot drift), and matching `^\| R\d+ \|` silently skipped `D12b`/`D30`/`D2b`/`E5` — widening took the population **67 → 111 and found D30 immediately**. Non-vacuity at two granularities: exit **2** on zero rows AND on fewer than two contributing sections. The prose sections are excluded **by the property** (first column `area`/`item`, not `id`), asserted, so a new id-keyed section needs no edit. Positive control: exit 1 naming exactly the four rows against pre-fix `HEAD`. **Six mutations, two survived the first pass** — the two-section floor was masked by a second agreeing signal, and restricting the parent set to OPEN made deleting **resolved** rows invisible (where the revert paths live). Both closed | see the 2026-08-29 ledger entry |
| R1 | 2026-08-22 | Two overview panels stated "No sales in the last 24h" during a DB timeout (`?? 0` on a NULL `stats`) — sixth instance of the honesty class, on the page D11 was fixed on | Zero `?? 0` remains in the overview page; header cites "R1 and R4 each fixed one layer". ⚠ **Verified under a REAL `get_collection_stats` outage** — the strong form of the test — both named panels render "Couldn't load this right now" | `git log --grep` the overview honesty commit; revert the page change |
| R2 | 2026-08-22 | `/api/edition-floor` let an UNAUTHENTICATED caller trigger a service-role DELETE on `fmv_snapshots` via `?persist=1` | `persistAuthorized(req)` requires `Bearer $CRON_SECRET` or `$INGEST_SECRET_TOKEN` on both GET and POST and fails closed when unset (`!!cronSecret &&`); the false "No writes, no user data" comment in `proxy.ts` corrected in place. ⚠ **The CLASS is not closed — see R24** | revert `persistAuthorized` + the proxy comment |
| R3 | 2026-08-22 | `/api/fmv` carried the D27 raw-`fmv_snapshots`-DESC + JS-dedup anti-pattern on the documented product API | `lookupEditions` reads `fmv_current` chunked; the residual `fmv_snapshots` read is a single-edition `.limit(21)` sparkline | revert the `lookupEditions` change |
| R4 | 2026-08-22 | Pinnacle `edition_id` resolution "stopped and is ESCALATING to 86.4%"; `/disney-pinnacle` RECENT TOP SALES rendered a blank box with no empty-state copy | ⚠ **The PIPELINE half was a MEASUREMENT ARTIFACT.** 86.4% is the trailing edge of a **daily** bridge (`bridge_pinnacle_sales_editions`, pg_cron jobid 184, `41 5 * * *`) read at one instant — exactly the signature a batch resolver with a 24h period produces. Settle curve: 0–6h **87.31%** null → 6–24h 30.49% → 24–48h **3.61%** → >7d **0.98%**; all-time 546/194,027 = 0.28%; jobid 184 7/7 succeeded. **The RENDER half is genuinely fixed** with the full three-state treatment incl. "N more sales in this window not yet matched to a moment." ⚠ **Refutation condition for this overturn: if the >7d bucket exceeds ~2%, it IS a break** | n/a — render fix stands; pipeline half was never broken |
| R5 | 2026-08-22 | `compute-pinnacle-pack-ev` 100% failing since 08-11; fix `bd53bb3a` "confirmed NEVER DEPLOYED" | ✅ Confirmed with **R5's own prescribed positive control**: newest `extra` contains `dist_dupe_count: 2` ⇒ the fix IS deployed. 12/12 ok in 72h; Pinnacle `pack_ev_latest` 91 packs, newest **4.2h**, not 98h frozen; 9,323 rows in 30d | n/a — verification only |
| R7 | 2026-08-22 | `drain-conflated-subeditions` 100% dark-killed; `seed_recent` the residual 120 s ceiling-hitter | ✅ Newest tick `ok: true`, `rows_written: 1005`, knots_resolved **92**, wmc_split 996, total 271,224 ms of 600,000, `skipped_steps: []`. ⚠ **The register's "split/realign are chronic 120 s rollbacks costing 42% of budget" is REFUTED for the current window — `split` ran 17,725 ms, not 125,250.** `seed_recent` still hits 120 s but is now non-fatal (`truncated_steps: ["seed_recent"]`) | revert the step reorder |
| R10 | 2026-08-22 | `openGraph`/`twitter` shallow-merge trap live in three shared helpers; ~30 insights layouts unfurling with no X byline | `lib/seo.ts` exports `OG_INHERITED` / `TWITTER_INHERITED`, spread by all three helpers; **30 of 30** insights layouts reference it; `__tests__/metadata-inline-blocks-inherit-root-fields.test.ts` WALKS THE TREE rather than naming helpers. Live probes returned `og:site_name`, `og:locale`, `og:type`, `twitter:site=@RipPacksCity` on all three sampled URLs | revert `lib/seo.ts` + the layout sweep |
| R12 | 2026-08-22 | `/share/<wallet>` rendered a failed read as "your wallet isn't indexed yet"; no canonical | `fetchSnapshot` returns `{data, ok}` and renders a distinct `ShareUnavailable`; `alternates.canonical` emitted lowercased. ⚠ Sweep D could not force the failure path live and correctly declined to call it fixed from the success path alone — the code read settles it | revert the share page change |
| R13 | 2026-08-22 | `/api/profile/achievements` POST was a confused deputy (owner key from the BODY, server's `INGEST_SECRET_TOKEN`) | `const gate = await requireOwnedKey(ownerKey); if (gate instanceof Response) return gate;` precedes the edge-fn call. Fails closed. ⚠ Its callee `compute-achievements` is in the **uncommitted edge-function set (R21)**, so the edge side remains unauditable | revert the route change |
| R15 | 2026-08-22 | The ledger's `## Queued` section dead since 2026-07-01, and the audit brief pointed readers at it | Retitled `## Queued — ARCHIVE (frozen 2026-07-01; do NOT read this as the live queue)` with a pointer to the dated entries | revert the heading edit |
| R16 | 2026-08-22 | `scripts/local-cost-basis-backfill.mjs` invited pasting live Dapper session cookies into a tracked file — the exact leak the 2026-08-03 `filter-repo` purge existed for | `COOKIE = process.env.DAPPER_COOKIE ?? ""` / `DAPPER_X_ID_TOKEN` | revert the script change |
| D31 | 2026-08-22 | Migration parity backlog (prod 2,544 vs 539 committed files) | `227c94a2` recovered 5 from prod; `migration-parity.yml` now exits on RC and handles RC=2 ("a check that cannot answer is not a pass") | `git revert 227c94a2` |
| R17 | 2026-08-22 | "Two history backfills scan 40,000 blocks and decode zero" | ⚠ **CLOSED, but NOT by the refutation condition it named.** Every recent tick (n=8 golazos, n=8 ufc, 12h) returns `{"skipped":"saturation"}` in 0.7–29.7 s **before any block scan**. Neither the "genuine defect" nor the "pre-collection-filter counts" branch describes production — they are members of **R20**. Do not action R17's recommendation | — |
| D18 | 2026-08-22 | "10 inert schedules that run on time and write nothing" | ⚠ **CLOSED AS UNVERIFIABLE.** The list was never enumerated anywhere in the repo — `docs/handoff-2026-08-09d-deep-audit.md` names exactly one member and no others. That member (`pinnacle-sales-history-backfill`) is refuted a **third** time: 48h = 14 runs, `rows_found` 315, `rows_written` 315 — the only member of the 9-pipeline family still writing anything. There is nothing left to re-verify | — |
| R-M1 | 2026-08-15 | `get_collection_stats` probed 2 provably-EMPTY `fmv_snapshots` partitions on every LATERAL loop | `audit_20260815_collection_stats_prune_future_fmv_partitions` — `AND computed_at <= now()` on both LATERALs. `Subplans Removed: 1`; leg **7,763 → 6,220 buffers**. Equivalence PROVEN not sampled. ⚠ **NOT a rescue — see R6** | Re-apply the previous definition, which differs ONLY by that predicate. Guarded inverse `DO` block in the migration header. Pre-image `md5(prosrc) = 8086744dce041410f8e62610a3bdc9aa` |
| R-M2 | 2026-08-15 | `candy-listings-indexer` held `info` severity on the premise "candy_mlb is unpublished", invalidated by the 2026-07-31 go-live | `UPDATE pipeline_cadence_watchlist SET severity='medium'` + note rewritten. Swept the whole class; exactly one live instance | `UPDATE pipeline_cadence_watchlist SET severity='info' WHERE pipeline='candy-listings-indexer';` |
| D15 | 2026-08-12 | `check_unmapped_backlog_growth()` blew the alert path under saturation (14/71 = 19.7% on 08-09) | 19.7% → 2.9% → 4.3% → 0.0%; p95 46,216 ms → 9,946 ms. ⚠ "Resolved" means BACK TO BASELINE, not zero — pre-incident days were already 1.4–4.3% of the same timeout class, so **2.9–4.3% must not be re-opened as a regression** | n/a — verification only |
| D33 | 2026-08-09 | A fabricated bid-ask spread on the PUBLIC `/insights/candy-mlb` board — `floor_usd − best_offer_usd` subtracted a MINT-grain bid from an EDITION-grain ask | 94% of two-legged rows priced one NFT against another; 7 (21.2%) NEGATIVE. Dropped `spread_usd`/`spread_pct`; added `floor_mint`/`best_offer_mint`/`same_copy` so the grain is visible, plus a genuine `exec_spread_*` on the floor copy only. ⚠ **The clamp was the tell — a figure that needs clamping to stay plausible should be doubted, not clamped** | `git revert <code sha>` + re-create the view from `20260724160400_…sql` |
| D12 | 2026-08-09 | `/nba-top-shot/analytics`: "ORDER BOOK DEPTH 1 listings" and "$0.00 / 0 sales" | (a) percentiles over ONE 86-day-old `ts_listings` row under a banner claiming "typically 100-200 listings" — **a stale disclosure is worse than none**; (b) `$0.00 / 0 sales` was a soft-failed fetch through `?? 0`. ⚠⚠ **RE-OPENED AS D12b 2026-08-22 — this was fixed on `ListingsDashboard.tsx` ONLY; the identical panel in `CollectionAnalyticsClient.tsx` was never touched and still renders the row, now 99.1 days old** | `git revert ffb513a2` |
| D13 | 2026-08-09 | Pinnacle overview reported ASK-derived numbers under FMV labels | ⚠ The original filing ("the FMV recompute appears dead") was WRONG — every Pinnacle pipeline was healthy. Real defect: `get_collection_stats` filled `fmv_*` from `ask_price`/`ask_updated_at`. Page said 28% / 23D; truth 69.4% / 4.7h | migration `20260810031639` |
| D19 | 2026-08-09 | CLAUDE.md's "~200 historical rows/24h" for the UFC revival arm | ⚠ The "premise is falsified" filing was WRONG and would have invited simplifying the arm. 485 UFC rows in 7d, all `sold_at` ≤ 2026-04-17, all in a SINGLE burst — a burst is exactly what an ingest-time predicate misreads as a revival. **`sold_at` keying validated; do not simplify** | `git revert ffb513a2` (docs) |
| D-R1 | 2026-08-09 | 56,898 wmc rows rendered a nameless, mintless moment while the name sat in `editions` | Ran the pinned `backfill_wmc_metadata_from_editions` per wallet. 56,898 → 197 | None needed — fill-only |
| D1 | 2026-08-09 | Unauthenticated service-role write IDOR on `/api/support-chat/feedback` | Scoped by `session_id`; `.select("id")` makes a zero-row update observable. **Also: feedback-with-a-comment had NEVER persisted** (violated a CHECK). Tests 3 → 11, mutation-proven | `git revert 791d7e8c` |
| D2 | 2026-08-09 | 8 cron gate keys hardcoded in a PUBLIC repo (CODE half) | All 8 read fail-closed edge secrets. ⚠ An env read alone is NOT fail-closed — an unset secret is `""` and a bare `?key=` would match, so each carries an explicit unset-guard. **Rotation still owed → D2b** | `git revert b4e46435` |
| D3 | 2026-08-09 | Set Tracker dead on 3 collections + raw Postgres error shown to users | Root cause was NOT slowness: `lower(wmc.wallet_address)` is not sargable, so the planner probed 2.2M rows per edition — cost 124,243 → 2,551 | `git revert 6693a126` |
| D3b | 2026-08-09 | 11 more prod fns carried the non-sargable `lower(wmc.wallet_address)` | 6 that genuinely seq-scan wmc fixed. ⚠ The obvious rewrite `col = lower(param)` SILENTLY BREAKS CANDY (25,375 case-sensitive base58 rows) — only `col IN (raw, lower(raw))` is safe there | `git revert <sha>` |
| D4 | 2026-08-09 | Sniper showed `0 deals` while 200 existed | ⚠ Did NOT flip the default — the framing was wrong. 54% of TS listed editions are HIGH/MED so the gate is not broken; the hidden rows are ask-derived, so defaulting them visible would headline fabricated signal. Fixed the empty state instead | `git revert f610ea33` |
| D5 + D35 | 2026-08-09 | 8 user-facing claims the product no longer honours, incl. the legal page | `/privacy`, homepage buy/skip, `/pricing` selling a 404ing `/rewards`, concierge contradictions, 4 "connect a wallet" strings. Re-verified live run 3: `/privacy` now states it affirmatively | `git revert 3946753e` |
| D6 + D7 | 2026-08-09 | Two crons failing blind | `stale-fmv-monitor` had no `log_pipeline_run` while failing 40.8%. ⚠ `ok=true` means the CHECK RAN, not that FMV is fresh | `git revert cb5bcfd7` |
| D9 | 2026-08-09 | Net-of-fees figure had an INVERTED SIGN on ~199 of 200 rows | Branched on `flipsNegative` as if it were a sign flag. ⚠ The old test passed on an **impossible fixture** | `git revert f610ea33` |
| D10 | 2026-08-09 | `/nba-top-shot/series/series-4` appeared to 404 | Not routing — the page collapsed *error* and *absent* into `return null`, defeating the layout gate that fails open precisely to avoid soft-404ing real pages to Google | `git revert 418a1ce7` |
| D11 | 2026-08-09 | Overview rendered `TOTAL EDITIONS 0 / PRICED 0% / $0` for a collection with 6,190 editions | A failed read served as HTTP 200; the error object became truthy `stats` and every KPI fell through `?? 0`. ⚠ Fixed the KPI band only — the two panels below were R1, now also fixed | `git revert <sha>` |
| D14 | 2026-08-09 | 3 prod migrations with no committed file, 2 redefining a public board MV | Recovered byte-identical from `schema_migrations.statements[1]` (**recover, never retype**) | `git revert 90d5a3cf` |
| D16 | 2026-08-09 | `candy-offers-indexer` fails 5/13 (38%) | ⚠ Downgraded: the 72h window **straddled a fix**, so 38% describes neither era. Post-fix max duration 78 s, zero deadline hits. User impact ZERO | — |
| D17 | 2026-08-09 | "4 Vercel cron routes at/over their Lambda ceiling" | ⚠ CLOSED — measurement artifact. **Do not compare `pipeline_runs.duration_ms` to `export const maxDuration`** — `after()` routes self-measure across the background continuation; 8 of 8 over-cap runs were `ok=true`. The right kill-detector is MISSING TICKS | — |
| D20 | 2026-08-09 | 299 sets collapse into merged entity pages; the "Variants merged" banner fired only on different SPELLINGS, so it was anti-correlated with the problem | `get_set_detail` returns `underlying_set_count` from the 914-row `sets` table — complete by construction. Catches all 116 real merges vs the 2 the spelling key caught | `git revert <sha>` |
| D22 | 2026-08-09 | 4 URL-reachable prototype-key lookups | Already fixed by a concurrent session (`46b0fb9f`). Re-probed rather than assumed | n/a |
| D23 | 2026-08-09 | 4 drifted `TIER_ORDER` copies | ⚠ Filed as duplication hygiene; was **two LIVE data defects**. The FMV dashboard's TitleCase list never matched the RPC's UPPERCASE enum so every row bucketed into one "Other" bar — and its test passed because the fixtures were TitleCase too | `git revert 4885d3ba` |
| D24 | 2026-08-09 | 29 layouts double-suffixed the brand in `<title>` | Stripped; directory-driven guard still allows the sanctioned `absolute` opt-out. ⚠ Run 3 found the **counterpart** error — ~70 titles now lost the suffix entirely (R31) | `git revert 4885d3ba` |
| D27 | 2026-08-09 | `/api/alerts` raw-`fmv_snapshots` + JS-dedupe | Repointed to `fmv_current`, chunked at 500. The `/api/fmv` sibling is now also fixed (R3) | `git revert 418a1ce7` |
| D28 | 2026-08-09 | "the 08-08 v9 split may be mis-classifying" | ⚠ NOT A FINDING — the answer predated the audit in an **unreferenced** handoff. ⚠ **Third instance found run 3**: two handoffs carry unticked verification boxes for work that shipped. An unticked box is not open work | — |
| D29 | 2026-08-09 | `purge-stale-listings` 401'd every Vercel cron tick | Accepts `CRON_SECRET` or `INGEST_SECRET_TOKEN`. ⚠ Same footgun that got `pinnacle-sync` deleted as "dead" when it had only ever been mis-authed | `git revert 4885d3ba` |
| D32 | 2026-08-09 | 5 pipelines find rows and write none | ✅ `topshot-onchain-art-backfill` FIXED. The other 4 confirmed not-defects. **"Finds rows, writes none" is NOT a defect signature on its own — read the `extra` payload** | — |
| D34 | 2026-08-09 | Pinnacle had no FMV confidence-share arm | Prerequisite cleared by splitting `rpc_trust_health_precompute_refresh` into 7 per-leg fns + a COMMIT-ing orchestrator; then shipped as the 8th leg. TRACK-only | ledger 2026-08-10/11 |
| D36 | 2026-08-09 | AllDay 217 editions where `highest_offer > low_ask` (50× the TS rate) | ⚠ PROVEN benign, and NOT timing skew — AllDay's cheap-market structure (median `low_ask` **$1.00**, 84% under $2) | — |
| D38 | 2026-08-09 | Doc drift in CLAUDE.md | Mostly drained. ⚠ There are **three** sanctioned hardcoded chart strokes, not one | `git revert 418a1ce7` |

---

## VERIFIED-CLEAN (re-probe cheaply, do not re-audit)

| area | last verified | one-line probe | expected |
|---|---|---|---|
| RLS coverage | 2026-08-22 | `select count(*) from pg_tables where schemaname='public' and rowsecurity=false` | `0` |
| Policies on RLS-off tables | 2026-08-22 | `pg_policies` joined to `not relrowsecurity` | `0` |
| Public security invariants | 2026-08-22 | `select count(*) from check_public_security_invariants()` | `0` rows |
| Anon write surface | 2026-08-22 | `select count(*) from check_anon_write_surface()` | `0` rows — ⚠ tests the anon DB ROLE; blind to a service-role-keyed HTTP route (R24) |
| SECDEF anon-exec drift | 2026-08-22 | `select jsonb_array_length((select check_secdef_anon_exec_drift()))` ⚠ **not** `count(*)` | `0`. Underlying set SHRANK 4 grants/3 fns → **3 grants / 2 fns**, none write, both pinned |
| No anon-readable view bypasses RLS | 2026-08-22 | anon-readable `relkind='v'` whose `reloptions !~ 'security_invoker=(on\|true)'` | `0` |
| No anon/auth-readable materialized views | 2026-08-22 | `relkind='m'` with anon or authenticated SELECT | `0` — sharpest silent-leak class |
| No foreign tables in the API | 2026-08-22 | `relkind='f'` in `public` | `0` |
| `auth.users` not exposed via a view | 2026-08-22 | view/matview depending on `auth.users` with anon/auth SELECT | `0` |
| No extensions in `public` | 2026-08-22 | `pg_extension` in nspname `public` | `0` |
| No trivially-true write policy reachable by anon/authenticated/public | 2026-08-22 | permissive `cmd in (INSERT,UPDATE,DELETE,ALL)` where `qual` and `with_check` are both true/null and roles ∋ anon\|authenticated\|public | `0` — wider than `check_anon_write_surface` |
| Staged Panini/Candy data is genuinely data-gated | 2026-08-22 | anon SELECT over `relname like 'panini%' or 'candy%'` | **0** anon-readable |
| Anon write grants all RLS-neutralised or shape-bounded | 2026-08-22 | resolve each object holding an anon write GRANT against its policies | 20 objects; 4 genuinely anon-insertable (`email_subscribers`, `funnel_events`, `outbound_clicks`, `support_conversations`), all length/range `with_check`; 16 neutralised. ⚠ The 20th is a matview grant that Postgres rejects DML on regardless |
| Anon EXECUTE surface | 2026-08-22 | `has_function_privilege('anon', …)` over public | **81 of 659**; every one carries a pinned `search_path`; the 1 writer is a trigger fn, INVOKER |
| No PII in anon-readable surfaces | 2026-08-22 | PII regex over anon-SELECT objects, **each resolved against its SELECT policy** | none reachable. `chat_sessions.user_email` / `fmv_alerts.notification_email` carry a grant but only a service-role policy ⇒ 0 rows; `email_subscribers` / `outbound_clicks` have no SELECT policy ⇒ 0 rows; `editions.player_birthdate/_birthplace` are public athlete facts |
| No hardcoded credentials | 2026-08-22 | grep `eyJhbGciOiJ`, `sb_secret_`, `sk-ant-`, `AKIA`, `ghp_`, `github_pat_`, `re_`, Telegram shape, `rpc_pls_` over **origin/main** | **0 real hits** — ⚠ **AND THE CORPUS IS THE LIMIT, NOT THE PATTERN LIST. Measured 2026-09-02: all 14 HTTP-dispatching pg_cron jobs carry a `rpc_pls_` gate key IN THE URL** (`?key=…`), 13 of them active, **0 using a header** — the exact prefix this row greps for, sitting in `cron.job.command`, which lives only in the database and is outside `origin/main` by construction. ⭐ **A credential probe is bounded by its CORPUS, not its regex**, and "0 real hits" reads as an estate-wide all-clear. Same blindness covers Vercel env and edge-function secrets. Filing: [inbox 2026-09-02T1100Z](../overnight/inbox/2026-09-02T1100Z-all-14-pg-cron-http-jobs-put-their-gate-key-in-the-URL-and-the-credential-probe-cannot-see-them.md). ⚠ **Scope-bounded by R21 — 29 deployed edge functions have no repo source and are outside this probe** |
| No secret leaked to logs or response bodies | 2026-08-22 | grep `Object.fromEntries(req.headers)`, `JSON.stringify(process.env)`, echoed `Authorization` | **0** value leaks. One near-miss: two routes log the key's LENGTH (R40) |
| GitHub Actions secret handling | 2026-08-22 | 33 `secrets.*` refs across 20 workflows | all `Bearer ${{ secrets.X }}` or `env:`; never in a URL, never echoed |
| IDOR — `/api/profile/**` writers | 2026-08-22 | grep each route for `requireUser`/`requireOwnedKey`/`user.id` | all gated, **including** the former R13 POST |
| pg_cron has no disabled/orphan jobs | **2026-09-02** | `select count(*), count(*) filter (where active) from cron.job` | **104 / 103 — ⚠ THE ROW AS PREVIOUSLY WRITTEN MAKES A CORRECT PAUSE READ AS A FAILURE.** The one inactive job is **jobid 16 `rpc-backfill-pack-pool`**, PAUSED deliberately during the `public-api.nbatopshot.com` 530/`1033` outage (ledger, alongside `topshot-moments-hydrator` STOPPED and `offers-sweep` breaker-throttled) — and **that outage is still live**, `error code: 1033` still accruing at +245 per 6 h on 2026-09-02, so the pause is still correct. 👉 **The invariant is: no inactive rows OTHER than those with a ledger-recorded pause and a stated revival condition — currently exactly one, revived when the Top Shot legacy endpoint returns.** Prior stamp: **93 / 93**. ⚠ The invariant is "no inactive rows", not the count. ⚠ **Diff the SET** — 85→93 is +10 added, −2 removed; jobids 109 and 287 are gone and unnameable |
| pg_cron non-timeout error class | **2026-09-02** | `cron.job_run_details` grouped by `return_message` class, 7d | every non-`startup timeout` failure is `canceling statement due to statement timeout`. **Any other error class is a real finding** 🔁 **RE-DERIVED 2026-09-02, 7 d: `job startup timeout` 384 (76.6%)** — inside R29's 67–80% band, unchanged; `statement timeout` 112 (22.4%); **`other` 5 (1.0%), so the invariant IS violated.** ⭐ **All five are benign, and that is the finding about the PROBE.** Four sit on jobids **373 / 406 / 410 / 430, which no longer exist in `cron.job`** — deleted scratch jobs (`job canceled` ×2, `invalid transaction termination` in `reconcile_all_seeded_wallet_stats` at its `COMMIT`, `permission denied for function populate_pinnacle_wmc_fmv`), each failing once before being dropped. **`cron.job_run_details` OUTLIVES `cron.job`, so a one-off probe job someone created and removed reads as a new error class forever.** 👉 **Sharpen the probe: LEFT JOIN `cron.job` and treat only rows with a LIVE job as findings.** The one live-job hit is `rpc-refresh-wmc-fmv-changed` (jobid 303), a single **`deadlock detected`** on 08-26, not recurring — same function as known-issues #36 (40.2% of dirtied blocks). |
| Scheduler inventory | 2026-08-22 | `vercel.json`, Glob `.github/workflows`, `cron.job` | **36** Vercel crons (register previously said 38 — corrected) · 20 GHA files, 17 scheduled, 19 `- cron:` lines · 93 pg_cron |
| `pipeline_runs_daily` rollup is running | 2026-08-22 | `pipeline-runs-daily-rollup` in `pipeline_runs` | 4 runs/24h (jobid 233, `11 */6`). ⚠ Use it for VOLUME and TREND, **never for RECENCY** |
| CI job set | 2026-08-22 | read `.github/workflows/ci.yml` + the three vitest configs | **10** jobs (was 8; `tree-corruption` + `edge-deno` added), all blocking, zero `continue-on-error`. Gates ROSE: primary **91.8/79.4/93.6/93.85**, component **90.85/81.95/89.3/93.75**, workers **88.15/76.15/89.6/91.1** |
| In-code TODO/FIXME backlog genuinely drained | 2026-08-22 | `grep -rn '\bTODO\b\|\bFIXME\b\|HACK:'` excl. node_modules/.next/docs | 0 actionable — **fourth** independent confirmation. 6 hits, all prose describing a resolved TODO |
| Root handoffs carry no open work | 2026-08-22 | read **all 111** root `docs/handoff-*.md` | 0 open. Only 2 have unticked boxes, both stale *verification checklists* for shipped work — **third instance of the D28 trap; an unticked box is not open work** |
| Repo hygiene / no tracked capture or patch files | 2026-08-22 | `git ls-tree` + `git check-ignore -v` + credential-shape grep over every loose file | **CLEAN.** 12 loose `.patch` + 4 capture files (159 MB) in the working tree, **0 tracked**; `.gitignore` `*.patch` and `panini*capture*.jsonl*` both fire; no credential shape in any patch. Largest tracked blob is `docs/overnight/ledger.md` at **5.4 MB / 1,763 `### ` headings** |
| `.ilike` on an enum column | 2026-08-22 | `\.ilike\(\s*["'](confidence\|tier\|fmv_confidence\|liquidity_rating)["']` | 3 hits, all `cached_listings.tier` (plain text, not the enum) |
| Batch `.insert()` swallowing 23505 | 2026-08-22 | grep `23505` in non-test code | all sites are row-by-row fallbacks after an all-or-nothing batch; two indexers document the rule inline |
| `rows.length` used as a PostgREST total | 2026-08-22 | `rows\.length\|total:\s*\w+\.length` over `app/api` | every hit is the length of an in-memory array, not a DB total |
| `\|\| 1` / `?? 1` fabricated divisor | 2026-08-22 | walked `scripts`, `supabase/functions`, `e2e` (the roots the ratchet excludes) **plus** the hoisted-into-a-variable shape across all 4 ratchet roots | **0** both |
| `.range()` without `.order()` — **inside the ban's declared scope** | 2026-08-22 | the ratchet's own `findUnorderedRangeSites` re-implemented and run on `app,lib,workers,supabase` × `.ts,.tsx` | **0** — this is the positive control for R26; `components/` = 0; extension blind spot empty |
| `error.message` in a response body | 2026-08-22 | the 5 spellings in `__tests__/helpers/driver-message-leak.ts` | clean-by-guard; both anon and authed guards blocking |
| Brand tokens | 2026-08-22 | replicated the guard's LITERAL + var-fallback + `brand-exception` window across **all 456** `.tsx`, not the 54-file list | 2 hits, both benign (R37) |
| Canonical tags | 2026-08-22 | live read of `/insights/squeeze`, `/nba-top-shot/overview`, `/nba-top-shot/edition/2:16` | present and self-referential on all three; `/share/[wallet]` now emits one (R12 resolved) |
| `robots: {index:false}` inventory | 2026-08-22 | `git grep "index: *false" app/ lib/` | 18 hits, all justified — authed surfaces, user-scoped detail, `notFound` branches, + the **2** sanctioned flag-conditional lines (inert while both flags true) |
| `robots.txt` + sitemap index | 2026-08-22 | live fetch vs `app/robots.ts` / `app/sitemap.xml/route.ts` | byte-consistent; valid `<sitemapindex>` with 5 children. ⚠ `lastmod` is generation time — R35 |
| No cart / gifting / trading / wallet-connect in rendered copy | 2026-08-22 | grep those strings over `app`, `components` + a 30-URL rendered sweep | **0 hits**; `/privacy` states it affirmatively |
| FMV coverage per collection | 2026-08-22 | `editions LEFT JOIN fmv_current`, partition-pruned per collection | Candy 100% · Golazos 100% · AllDay 100% · **UFC 100% (was 89.77 — a real gain)** · TS 99.14% (canonical leg 100.00%) |
| Pack-EV honesty | 2026-08-22 | `pack_ev_latest` TS grouped on `edition_count=0` | 291 rows: 291 NULL `gross_ev`, 291 NULL `pack_ev`, **0** `is_positive_ev`. Nothing fabricated |
| A stale pack EV is never published as live | 2026-08-22 | rows older than 30d with `primary_available` | TS 409 / AllDay 397 / Golazos 2 stale rows, **`primary_available = true` on 0 of them in every collection** |
| TS parallels / subeditions | 2026-08-22 | `editions` TS by key shape | 3,805 parallel keys; **0** missing `subedition_id`, **0** missing `subedition_name`, **0** subedition ids on a base key |
| Zero/negative FMV, `circulation_count=0`, `fmv_sanity_flags`, `topshot_impossible_parallel_serials`, `sales_serial_supply_worst_pct` | 2026-08-22 | combined probe / precompute | **0 / 0 / 0 / 0 / 0.00** |
| MV / catalog freshness | 2026-08-22 | `pg_stat_all_tables` + each object's OWN cron cadence | `pack_grail_metrics_mv` 0.1h · `mv_topshot_set_play_catalog` 0.7h · `pinnacle_catalog` 2.8h · `sets_summary` 8.6h ⚠ **correct-by-cadence, not stale** (jobid 37 is `50 7 * * *`, 3/3 succeeded; the register's 0.5h reading was a sample-time artifact) |
| AllDay badge writer alive | 2026-08-22 | `badge_editions` AllDay `updated_at` + zero-ask count | median 0.08d, 4,143/5,607 with `low_ask`, **0 zero-asks** — the clear-phase is working, not publishing 0 as a price |
| `candy_offers` | 2026-08-15 | full table | 257 rows / 45 active / 0 null edition_id / **0** active-unseen-12h / 0 active-but-expired |
| `funnel_events` DB CHECK vs route allowlist | 2026-08-15 | `pg_get_constraintdef` vs `ALLOWED_EVENT_TYPES` | identical, 10 values — no silently-rejected event type |
| Set-name merges consistent with D20's fix | 2026-08-15 | `sets` vs distinct slug | AllDay 363→143, TS 272→192 (= the 299 D20 identified) |
| **A failed read served as HTTP 200** (the D11 class) | 2026-08-22 | `rg -U 'error[^)]{0,120}\},\s*\{\s*status:\s*200' app/api` — then ask: *does a caller reading the success field see a plausible value?* | ⚠ **The tell is not the 200 — it is whether the failure shape is INDISTINGUISHABLE from a legitimate success.** Several 200s are deliberate and must NOT be "fixed" (cron routes returning `{ok:false, skipped:…}`; `bundle_not_supported`; `track-funnel` invalid-event). New instance run 3 → R33 |
| **A live surface reads a retired table** (the D12/D13 class) | 2026-08-22 | for each table a public surface reads, compare `max(<write ts>)` against the freshness the surface implies | ⚠ **THIS PROBE FIRED RUN 3 → D12b.** `ts_listings` = 1 row, 99.1 days old, still rendered as market depth. **Signature: a table whose newest row predates the last deploy by months but is still joined into a rendered KPI** |

---

## NOT-A-FINDING (healthy by design — do not re-raise)

| item | why | probe |
|---|---|---|
| UFC 1,654h FMV staleness / 0% HIGH+MED | Market closed May 2026. ⚠ Re-derived run 3 with the decisive control: monthly volume **265 → 165 → 48 → 57 → 5 → 0** while average price fell **$3.86 → $0.68**. **Volume and price decaying TOGETHER is a dying market; an ingestion break is a cliff at full price.** Last sale 2026-05-13 | — |
| Golazos 0.7% HIGH+MED | Genuinely market-limited — only **14 of 575** editions have ≥3 sales in 30d; the busiest traded 6 times in a month. Ceiling ~2.4–2.8% | — |
| 1,221 editions with `jersey_number > circulation_count` | A player wearing #23 on a 10-mint edition has no reachable jersey-match serial; the chip correctly never fires | — |
| Pinnacle 72.3% NULL serial in wmc | Pinnacle has unserialized editions by design | — |
| Pinnacle has **0 rows in `pack_drop_pool`** | Table-scope fact — Pinnacle EV comes from the edge fn's own path. A grain artifact | — |
| `special_serial_holders` holding only 25 rows | Legacy table; the live board is wmc-backed | — |
| `ufc-sales-indexer` writing 0 rows | Correct — market closed. The revival detector is the right instrument | — |
| UFC's 1,069 parked priced `unmapped_sales` | ⚠ Ran the control before filing: monthly `sold_at` distribution is **2025-12 through 2026-04, nothing after April** — the recent `ingested_at` is a history-backfill walker reading old blocks, not live trade. Under-reports historical UFC volume on charts; **cannot move FMV confidence and does not revive the dead market** | monthly `sold_at` histogram |
| `ownership-sync-dune` weekly | By design (free Dune credit tier) | — |
| **The AllDay `unmapped_sales` open backlog is ~102,000 rows and reads alarming** | ⚠ **Measured 2026-09-02: it is a PRICE backlog, not an edition-resolution backlog, and it is DRAINING.** Composition: **93,438 `onchain_dapper_v1` / `nflallday`** rows (58,779 of them **unpriced**), 8,417 flowty (all priced), 47 v2. `promote_unmapped_sales` requires `COALESCE(price_usd,0) > 0` on purpose — a V1 Dapper sale whose tx-decode budget was exhausted lands at `price_usd = 0`, and promoting it would pollute FMV. ⭐ **A 1-in-50 sample splits it decisively: of the PRICED rows only 0.2% have a resolvable edition — matching the function's own recorded '~0.6 newly resolvable per call' — while of the UNPRICED rows 39.7% do.** So ~23,000 rows are one price-decode away from promoting, and the bottleneck is decode throughput, not the edition maps. 🚨 **A first pass that omitted the price guard measured '22.9% resolvable' and would have filed a resolver defect that does not exist** — the guard is the whole discriminator. ✅ The drain exists and works: `allday-price-recover` wrote **8,710 rows in 7 d** (~1,244/day), and the backlog moved **104,908 (08-30) → 101,902 (09-02)**, ≈1,000/day NET of continuing history-backfill arrivals. ⚠ **Do not quote a clearance date from the gross rate** — the producer is still writing | ⭐ Split the backlog by `coalesce(price_usd,0) > 0` BEFORE testing edition resolvability; the two populations behave oppositely: `SELECT source, count(*) FILTER (WHERE coalesce(price_usd,0)>0), count(*) FILTER (WHERE coalesce(price_usd,0)<=0) FROM unmapped_sales WHERE resolved_at IS NULL GROUP BY 1` |
| **86,847 Pinnacle `cached_listings_v2` rows carry a NULL `edition_id` — 100% of them — against only 154 rows in `listing_resolution_failures`** | ⚠ **Raised as a possible coverage gap by the 2026-09-02 zero-yield sweep (*"either the failures table tracks one narrow event path or there is a coverage gap — do not read that as a defect without establishing which"*). Established: NEITHER.** `edition_id` is a UUID FK to `editions`, and **Pinnacle editions live in `pinnacle_editions`, keyed by `edition_key`, not in `editions`** — so for Pinnacle a NULL `edition_id` is the normal state and **is not the resolution criterion**. The indexer's own `resolved` gate is `editionKey && knownEditionKeys.has(editionKey)`, where `knownEditionKeys` is seeded from BOTH tables, and only genuinely unknown edition_keys are queued as failures — hence 154, not 86,847. 🚨 **This exact misreading has already caused an incident:** the code comment records that an earlier `!editionUuid` gate *"treated every pinnacle_editions-only edition as a failure and re-queued it every tick"*, which is what fired the per-tick Sentry noise. **I nearly re-filed it.** ✅ **And the harm test is negative:** every consumer of `cached_listings_v2.edition_id` is AllDay or Golazos (`allday_edition_floor_ask`, `golazos_edition_floor_ask`, `get_allday_market_*`, `get_allday_sniper_deals`, `resolve_golazos_listing_edition_ids`) — **no Pinnacle surface reads it**, and the one generic-looking consumer, `get_edition_market_bundle`, gates its `cached_listings_v2` branch on an explicit `ed.collection_id IN (AllDay, Golazos)` and sends Pinnacle down a different path. The contrast makes the point: AllDay's 47,900 `direct` rows are **0%** NULL, Pinnacle's 86,847 are **100%** | ⭐ Contrast the NULL rate BY COLLECTION before reading it as a gap: `SELECT c.slug, count(*) FILTER (WHERE edition_id IS NULL), count(*) FROM cached_listings_v2 l JOIN collections c ON c.id=l.collection_id WHERE l.source='direct' GROUP BY 1` |
| `sales-seller-recovery-dune` inert | Documented — missing `DUNE_SALES_SELLER_QUERY_ID`. ⚠ Its 3 `idx_sales_*_nullseller_soldat` indexes read as "unused" but back this job — do not drop | — |
| `/api/cron/warm` not writing `pipeline_runs` | Deliberate — "a warmer is not a data pipeline" | — |
| `compute-laliga-pack-ev` logging `{"phase":"invoked"}` with 0 rows | ⚠ **NOT inert** — it is the fire-and-forget invoker for `compute-golazos-pack-ev`. Observability gap only | latest `extra` |
| `topshot-pack-opens-history-backfill` 496 runs/7d, 0 rows | `extra` = `{"done":true,"spork_available":true}` — finished, not stalled | latest `extra` |
| `pack-events-ingest-backfill` finding rows, writing none | `caught_up:true` with `from_block == to_block` on all three cursors | latest `extra` |
| `topshot-active-listings-ingest` ~67–69% fail | `egress_blocked` — the documented Atlas WAF issue; `atlas-proxy` INERT pending an operator `wrangler deploy` | — |
| `sync-nba-projections` 100% fail | NBA offseason + all three upstreams WAF-blocked. Self-resolves ~Oct. ⚠ Restoring ESPN alone will NOT refill `nba_players` — the roster fetch is SLATE-GATED | — |
| `upstream request timeout` appearing "new" in a 72h view | Present continuously since 2026-07-31; `pipeline_runs` retention (~73h) makes chronic saturation look new | `pipeline_runs_daily` over 20 days |
| A `mean_gap_min` "coordinated stall" across `wallet-backfill*` / `panini-ingest` | ⚠ **REFUTED run 3, and the refutation is the lesson.** Both are **burst-scheduled**: `panini-ingest` fires in dense bursts at hours 1, 5, 9, 13, 17, 21 with identical shape in the prior 24h window. **A mean gap is not a cadence, and on a bursty pipeline it manufactures a stall that is not there** | hourly bucketing over two 24h windows |
| Six ephemeral pg_cron jobids appearing and vanishing | The documented one-off `CREATE INDEX CONCURRENTLY` / `VACUUM` pattern — not a leak | `cron.job_run_details` with no `cron.job` row |
| `fmtUsd` 9 variants, serial-multiplier 2 copies, `computeDualPrice` lib↔Deno | Duplication done **right** — documented decision / pinned by cross-agreement tests | — |
| `refresh_wmc_fmv_changed` at 112 GB / mean 330 s | **Not a bad plan** — 112 GB is the price of the `wmc.fmv_usd` denormalization (UPDATE fan-out). The lever is the denormalization. ⚠ A committed-but-unapplied migration trims ~13% of its reads and says so; it is NOT a fix for the saturation band | — |
| Golazos `highest_offer` 0/218; AllDay+Golazos pack pools never decremented; TS 6,561 UUID-keyed NULL-thumbnail residue | **HONEST GAPS** — no upstream source exists. Golazos re-confirmed 0 of 218 run 3 | — |
| Golazos badge `updated_at` median 27.85d | `refresh_golazos_badge_low_ask()` uses `IS DISTINCT FROM`, so it is a last-**changed** stamp. Never render as "updated N days ago" | — |
| Candy MLB wmc | Reference implementation — 0 NULLs on every column, 0 fossils, 25,375 rows = supply exactly | — |
| TS headline confidence share appearing to fall 54.9 → 46.6 | **Measurement artifact** — the per-day repriced cohort oscillates with no trend. Quote a 7-day mean, never a single reading | — |
| Two figures for the accuracy gate (49.7% vs 34.3%) | ⚠ **Not a disagreement** — one deliberate canonical-only Top Shot filter dated 2026-08-04 accounts for 100% of the gap; the instruments agree on the other four collections. **But state the denominator** — see R41 (decided + producer re-pointed 2026-08-28: all-rows) | §1 of the run-3 report |
| Email capture: 0 subscribers all-time (run-4 sweep F2) | **The path is verified functional by source, end to end**: `DealWatchCapture` renders UNCONDITIONALLY on every `/share/<wallet>` result, POSTs to `/api/subscribe` which IS in `proxy.ts` `isPublicPath` (the 307→login trap does NOT apply), and fires `email_capture_submitted` only on a confirmed-ok response. Zero conversions is an INPUT fact — ~14 human share_views/7d ≈ 2/day — not a defect. ⚠ The one untested link is the live Resend send; a 2-minute manual check (submit your own email on /share) settles it if ever doubted | source read of component + proxy, 2026-08-28 |

---

## DECLINED — do not re-suggest
*(Trevor's section — only he edits this.)*
