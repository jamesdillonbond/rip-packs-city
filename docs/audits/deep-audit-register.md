# RPC deep-audit findings register

Persistent across monthly deep audits. **Purpose: never re-derive a settled question.**

How to use it: at the start of each deep audit, run only the one-line probe for each `VERIFIED-CLEAN` row. If it still returns the expected value, stamp the date and move on — do NOT re-audit that area. If a probe fails, that becomes a finding and earns a full investigation. Re-measure each `OPEN` item's evidence number and record whether it grew, shrank, or resolved.

Created 2026-08-09 (first deep-audit run).

⚠ **Probe hygiene, learned on run 1:** `select count(*) from check_secdef_anon_exec_drift()` returns **1** when the surface is CLEAN — the function returns a single row containing a JSON array, so `count(*)` counts the row, not the findings. Use `jsonb_array_length((select check_secdef_anon_exec_drift()))` and expect **0**. The count-based probe produces a false positive every time.

---

## OPEN

> **Drained 2026-08-09 (Claude Code, interactive):** D1 D2\* D3 D4 D5 D6 D7 D9 D10 D14\* D22† D23 D24 D27 D29 D33 D35 D38\* — see RESOLVED. \*partial (see notes) · †already fixed by a concurrent session.
> ⚠ **Re-probe before acting.** Concurrent sessions ship to this repo constantly; D22 was already fixed and most of D38 already drained by the time this pass reached them.

| id | first seen | sev | finding | current evidence | owner |
|---|---|---|---|---|---|
| D2b | 2026-08-09 | **P0** | **Rotate the 8 pack-pipeline cron gate keys.** Code is de-hardcoded to fail-closed edge secrets and pushed, but the LIVE keys are still the burned ones and the deployed functions still hold them | ⚠ Confirmed **reachable in git history** (`git log -S` hits ~20 commits) — a second `filter-repo` is not worth it, so rotation is the only remedy. Ordering matters (9 pg_cron callers, jobids 15/16/20/42/44/55/56/83/84). Runbook: `docs/handoff-2026-08-09e-edge-gate-key-rotation.md` | **Trevor (secrets)** |
| D8 | 2026-08-09 | P1 | wmc metadata denorm has **no self-heal** — the post-pass enrichment failure is only `console.warn`'d (`wallet-backfill-helpers.ts:1009`), never logged, and `skipCached` prevents recovery on the next walk | backlog repaired 08-09 (56,898 → 197) but **will regenerate**: 47,305 of 47,498 AllDay rows were created within 7 days. ⚠ Plain `EXPLAIN` on the unscoped fill = cost 131,420, **seq-scan-bound on wmc**; the obvious partial index on `player_name IS NULL` is the WRONG fix (partial-index predicates block HOT) | Claude Code |
| D3b | 2026-08-09 | **✅ RESOLVED 2026-08-10 (Claude Code) — the 6 fns that actually seq-scan wmc are fixed + verified live; the other 7 are negligible small-table/false-positive, documented below** | **11 more prod functions carry the same non-sargable `lower(wmc.wallet_address)` predicate** that made the Set Trackers unusable (D3) — incl. `get_wallet_portfolio`, `get_active_challenges`, `get_challenge_plan`, `holdings_summary`, `mcp_find_set_completion` | **Definitive remaining list (live `pg_proc` scan 2026-08-10, 13 fns by any alias):** `get_wallet_portfolio(text)` **×3 hits** · `update_fully_enriched_flags(numeric)` ×3 · `mcp_find_set_completion(text,text,text)` ×2 · `analytics_resolve_usernames(text[])` · `backfill_pinnacle_mint_acquisitions(int)` · `discover_and_seed_active_wallets(int,int,bool)` · `get_active_challenges(text,uuid)` · `get_challenge_plan(text,uuid)` · `get_user_profile(text)` · `holdings_summary(text)` · `pick_verification_target(text,int)` · `sync_seeded_wallet_to_username_cache()` · `tg_capture_topshot_insider_marketplace_buyback()`. ⚠ **The obvious rewrite `lower(col)=lower(param)` → `col=lower(param)` SILENTLY BREAKS CANDY.** Case audit: `wallet_moments_cache` 2,211,030 rows / **25,375 non-lowercase** (all Candy — Solana base58 is case-sensitive by design), vs `seeded_wallets` 274/**0**, `saved_wallets` 99/**0**, `wallet_usernames` 8,388/**0**. So on wmc, forcing the param lowercase makes every Candy wallet match zero rows; only `col = param` (exact) is safe there. On the other three tables the column is provably all-lowercase, so dropping `lower()` is unconditionally equivalence-preserving. ⚠ **`holdings_summary` is DB-pinned** — its `supabase/tests/` copy must move in the same commit. **FIX shipped (migration `20260810062131`):** rewrote the 6 fns that genuinely seq-scan the 2.2M-row wmc — `get_wallet_portfolio` (4 preds incl. the un-aliased `lower(wallet_address)` the aliased-regex missed), `get_active_challenges`, `get_challenge_plan`, `mcp_find_set_completion` (2), `pick_verification_target`, `update_fully_enriched_flags`. Cross-collection/user-facing reads use `col IN (raw, lower(raw))` (keeps Flow case-insensitivity AND matches the exact Candy base58; Bitmap/Index Cond, not a Filter); Flow-only-scoped preds keep the lowered RHS and bare the wmc column (`col = v_wallet`); the seeded-wallets JOIN uses `wmc.wallet_address = LOWER(sw.wallet_address)`. **Equivalence proven: 0 colliding lowercase groups over 2,211,030 rows** → identical row set for any correctly-cased input; `get_wallet_portfolio(0xbd94…)` returns the exact same 19,260 moments / $79,746.91, and a normal wallet went from >60s (timeout) to instant (25× cost drop, 41,253→1,662). **NOT touched (measured negligible — no perf, no churn):** `get_user_profile`/`holdings_summary`/`analytics_resolve_usernames`/`discover_and_seed_active_wallets`/`tg_capture_topshot_insider_marketplace_buyback` (all predicate on <10k-row seeded/username/profile tables), `backfill_pinnacle_mint_acquisitions` (wmc join driven by `moment_id`, not wallet → wallet-sargability is not the access path), and `sync_seeded_wallet_to_username_cache` (FALSE POSITIVE — `lower(NEW.wallet_address)` normalizes a scalar for INSERT, not a column scan). So `holdings_summary` stays untouched → the DB-pin move was avoided. Revert: `git revert <sha>` + replay each predicate back | Claude Code |
| D15 | 2026-08-09 | P1 — **fix landed, NOT yet confirmed** | `check_unmapped_backlog_growth()` blows the alert path under saturation — `check-alerts` failed **14/71 on 2026-08-09**, so alerting is down exactly when it matters | ⚠ A concurrent session applied `audit_20260809_unmapped_backlog_growth_precompute_cache` late on 08-09. **Do not mark this resolved on the current evidence: only ONE `check-alerts` run has executed since (1 ok / 0 fail).** One sample inside the old range proves nothing — the same trap as the jobid-236 post-swap tick. Re-probe after ~24h: `select count(*) filter (where not ok) from pipeline_runs where pipeline=char(39)||…` i.e. fail rate over a full day; expect << 18%. ⚠ **Re-probed 2026-08-10 06:35Z: 24h aggregate still 21.1% (15/71) — BUT the fails are entirely a pre-fix/saturation cluster in the 08-09 20:00–00:00Z window; the most recent 6.5h are 18 consecutive runs / 0 fails** (hourly: 01:00–06:00Z all clean). Trending resolved, not yet a full clean day — mark resolved only after a 24h window with no 08-09-style cluster | Claude Code |
| D16 | 2026-08-09 | **P2 — DOWNGRADED 2026-08-10. Both the cause and the user impact were wrong** | `candy-offers-indexer` fails 5/13 runs (38%, 72h) | ⚠ **NOT workload growth, and do NOT raise the 700s budget** — that treats the symptom. Healthy runs finish in **39–78s, ~10× under budget**. The discriminator is perfectly bimodal: `bidder_fetch_errors` is **0 on all 8 healthy runs and 24–26 on the failures**. Upstream Magic Eden fetch failures drive retries → long duration → the occasional deadline hit, so **deadline hits are a SYMPTOM, not a capacity problem** (2 of 13). Real fix is bounding/backing off per-bidder retries. ⚠ **User-visible impact is ZERO — the "stale public board" claim is false:** 71 active offers, **all** last seen 4.0h ago, `active_but_unseen_12h = 0`, 1 expired. The guard suppresses deactivation on a degraded sweep, so a failed run is a clean no-op and the next good run refreshes everything — cost is wasted compute + a red row, not corruption. ⚠ **CORRECTED TWICE against live `pipeline_runs` 2026-08-10 — the 72h window STRADDLES A FIX, so its "38%" describes neither era.** Six fixes landed 2026-08-07 (`acb5ff24` → `27609e15`, last deploy ~08-08 03:05Z). Split at that boundary: **pre-fix** runs hit 249s/710s/760s with 2 deadline hits and 1 watchdog hang; **post-fix** (9 runs, 08-08 03:15Z →) max duration is **78s**, with **zero** deadline hits and **zero** watchdog hangs. So `dur_max 760,223ms` and "abandons sweeps on its 700s deadline" are **PRE-FIX ARTIFACTS — do not quote them as current**, and the "bounding/backing off per-bidder retries" remedy is already shipped. ⚠ **The "minor loose end — deadline enforcement may be leaky" is CLOSED and was never a defect**: the route's own comment documents that exact 08-08 00:50Z run, and `deadline_hit:false` was **correct** — the *watchdog* fired, not the deadline, because the sweep was blocked in an un-timed-out Supabase await that no loop check can reach (a deadline only fires where the code looks at it). Fixed by batching mint resolution out of the walk + `abortSignal(AbortSignal.timeout())` on every DB read. ⚠ **"The last ~24h are clean" is also wrong** — **08-09 12:50Z failed** (`fetch_err=24`, degraded). Residual is **1 of 9 post-fix runs ≈ 11%**, purely short (17–32s) degraded sweeps from upstream ME error bursts. **The bimodal discriminator is CONFIRMED live: `bidder_fetch_errors` = 0 on every success, 24–26 on every failure.** Zero user impact re-verified 2026-08-10: 219 offers / 71 active / **all last seen 4.1h** / `active_but_unseen_12h = 0` | Claude Code |
| D17 | 2026-08-09 | **CLOSED 2026-08-10 — measurement artifact, both halves falsified** | Filed as "4 Vercel cron routes at/over their Lambda ceiling: `allday-lock-refresh` max 310s vs 300 cap (47.5% fail), `candy-listings-indexer` 344s vs 300", inferring "those runs are being killed, so the recorded max is a lower bound" | ⚠ **(a) The failure rate is a pre-fix artifact** — same deploy-boundary error as D16. Measured 24h: `allday-lock-refresh` **4.3%** (1/23, was 47.5%), `candy-listings-indexer` **0%** (0/6). The 08-08 picker fixes (`babdc1a1` + `e9d08618`) landed; the overnight handoff already said so ("the 54.7% 2-day rate is pre-fix runs still inside the window"). ⚠ **(b) "At/over the ceiling" compares two different clocks.** Both routes are genuinely `maxDuration = 300`, yet over 72h **8 of 8 runs exceeding 300s completed `ok=true`, 0 failures** (`candy-listings-indexer` now *averages* 310s). Both use `after()` and return early (`allday-lock-refresh-batch/route.ts:20` — "returns 202 immediately"), so `pipeline_runs.duration_ms` is self-measured across the background continuation and is **not** the interval the Lambda cap enforces. **Comparing `duration_ms` to `export const maxDuration` generates false ceiling alarms — do not re-file this.** ⚠ Honest caveat: this does not prove kills never happen, since a killed route logs NOTHING and would be *absent*, not `ok=false`. The right kill-detector is **missing ticks**, not long durations — and sweep B's own tick-delivery check found 108% / 95%, i.e. no loss. ⚠ **INDEPENDENTLY RE-VERIFIED 2026-08-10 (Claude Code), every figure exact:** `allday-lock-refresh` 24h **1/23 = 4.3%** with **27** of its 28 72h-fails older than 24h (the pre-fix concentration, confirming the deploy-boundary reading); `candy-listings-indexer` 24h **0/6**, 24h mean **310.1s** — i.e. its *average* run sits above the 300s cap while never failing. Over 72h **8 runs exceeded 300s and all 8 are `ok=true`**. Both routes confirmed `export const maxDuration = 300` with `duration_ms` computed INSIDE the `after()` continuation. ⚠ Precision on the mechanism: the *observation* (over-cap runs succeed) is measured and is what makes the comparison invalid; the *explanation* (that `duration_ms` spans an interval the cap does not bound) is inferred, not proven from Vercel internals — so rely on the observation, not the theory. Residual worth knowing rather than re-filing: `allday-lock-refresh` averages **271.7s** against that 300s cap, but it self-bounds via its own `SOFT_DEADLINE_MS` break (`route.ts:98`), which is why it does not get killed | — |
| D18 | 2026-08-09 | P2 — **LIST IS CONTAMINATED; the headline example was FALSE** | "10 inert schedules that run on time and write nothing for 7 days" | ⚠ **`pinnacle-sales-history-backfill` is NOT inert and must not be retired.** Measured direct from `pipeline_runs`: **233 rows found AND written in 48h** (latest run `rows_written=131`, `extra` shows a live block scan `143932740-143972739`, `below_floor:false`); the rollup's own 7d view says **64 written**, not 0. Sweep B's "0 rows / cheapest saturation win" was simply wrong, and acting on it would have deleted a working pipeline — the `pinnacle-sync` mistake again, one step upstream. **Re-verify every remaining entry against raw `pipeline_runs` + its `extra` payload before touching any of them.** Confirmed genuinely 0/0 in BOTH raw and rollup over 48h: `golazos-`, `ufc-`, `topshot-sales-history-backfill`. ⚠ Note `pipeline_runs_daily` is written `11 */6`, so it lags raw by up to 6h — a raw-vs-rollup gap on a recent window is LAG, not corruption; don't over-read it | Claude Code |
| D20 | 2026-08-09 | **✅ RESOLVED 2026-08-10 (Claude Code) — banner re-keyed on the complete underlying-set-count, computed in-RPC from the 914-row `sets` table (no MV change needed)** | 299 sets collapse into merged entity pages (AllDay 220/363, TS 79/271). `set/[slug]/page.tsx:189` renders "Variants merged: …" — but **only when `set_name_variants.length > 1`**, i.e. only when the merged sets have *different spellings*. The big merges are name-IDENTICAL seasonal repeats, so they produce one variant and the banner never fires | ⚠ **The disclosure is anti-correlated with the problem.** Top 8 AllDay merges, every one `name_variants = 1`: `draw-it-up` **10** underlying sets/117 eds · `divisional-round` 8/69 · `playoff` 7/107 · `gridiron` 7/160 · `conference-championships` 7/30 · `super-wild-card-weekend` 7/73 · `against-the-clock` 6/15 · `rivalries` 6/16. Merge-by-name is deliberate (`fetchFullTierMix` intentionally queries across variants), so the fix is **not** to split the sets — it is to key the banner on the underlying **set count / season span**, not on name variants. Set-completion %, edition count and FMV totals remain computed on the merged denominator. ⚠ **MEASURED 2026-08-10 — the two candidate keys are NOT equivalent, and season span alone is not enough.** Coverage over all 117 real merges (TS 36 + AllDay 81): **name variants (current) catches 2 — TS 2/36, AllDay 0/81.** **Season span (`max_series > min_series`) catches 75 — TS 34/36 (94%), but AllDay only 41/81 (51%)**, with 2 false positives across 148 TS single-set slugs and 0 on AllDay. The AllDay half it misses are merges *within one season* (several same-named sets in one series), which no series comparison can see. **Underlying set count is the only key that is complete by construction.** ⚠ Cost differs sharply: `min_series`/`max_series` are **already columns on `sets_summary`**, so the span key is an RPC+page change with no MV touch; a set count is **not** a column, so it needs `refresh_sets_summary()` + the MV + the RPC + the page — 4 layers on a load-bearing cron-refreshed MV. **Do not ship the span key alone and call D20 closed** — it would disclose 64% of merges while reading as handled, and would leave half of AllDay (the worse-affected collection) silently merged. **FIX shipped (migration `20260810143454` + `set/[slug]/page.tsx`):** `get_set_detail` now returns `underlying_set_count = count(*) FROM sets WHERE collection_id AND name = ANY(set_name_variants)` — the complete-by-construction key. ⚠ **The register's "needs the MV → 4 layers" prediction was WRONG:** the count reads the 914-row `sets` table DIRECTLY in the RPC, so `refresh_sets_summary()`/the MV are untouched — it was **RPC + page only (2 layers)**. Banner now fires on `underlying_set_count > 1`, catching **all 116 real merges (81 AllDay + 35 TS)** vs the 2 the old spelling key caught; wording → "Combined view: N sets share this name (Series span) … Edition count, completion % and FMV totals are combined across all of them", still listing distinct spellings when present. Verified live: draw-it-up→10 (eds 117 unchanged), single set→1, Pinnacle→0 (no `sets` rows → banner off). Additive field only, all other outputs byte-identical, `tsc` clean, null-safe fallback to the old banner during the deploy window. Revert: `git revert <sha>` + drop the count block from the RPC | Claude Code |
| D21 | 2026-08-09 | P2 | AllDay `edition_offers` bids are a median **12.8 days** old while fresh AllDay offers live in `badge_editions` (0.8h) | ⚠ **P1 escalation condition CHECKED and NOT met (2026-08-09)** — all four consumers are collection-gated: `/api/best-offers` uses `.eq("collection_id", …)`, the edition page keys on `p_edition_id`, and both board views (`topshot_deals_vs_fmv`, `topshot_offer_ask_spread`) carry a `collection_id` predicate. Staleness only; no cross-collection contamination | Claude Code |
| D25 | 2026-08-09 | P2 — **characterised 2026-08-10; two populations needing different remedies** | 128 wmc rows render an impossible serial (e.g. "#500 / 499"). Rate 128 / 2,209,817 = **0.006%**, 0 rows lack a matching edition | **TS 71** (9 stale denorm / 62 upstream-wrong) across **20 wallets**; **AllDay 57** (25 / 32) across **1 wallet**. (a) **34 stale-denorm rows** — `wmc.mint_count` disagrees with `editions.circulation_count`, which IS the authoritative source (wmc is its cache), so these render a visibly wrong fraction and are correctable. ⚠ **`backfill_wmc_metadata_from_editions` will NOT fix them** — it is `COALESCE` fill-only by design and never overwrites a non-null value, so this needs a deliberate targeted UPDATE, and that design choice should be checked first in case a value was corrected on purpose. (b) **94 upstream-wrong rows** — both sources agree the serial exceeds circulation, so the chain data itself is wrong and there is no local fix. **Not repaired this pass:** 34 cosmetic rows is low value against a non-fill-only mutation with no git to record it | Claude Code |
| D28 | 2026-08-09 | **NOT A FINDING — closed 2026-08-10; the answer predated the audit** | Filed as "the 08-08 v9 split may be mis-classifying". **It is not.** [`docs/handoff-2026-08-09-atlas-proxy-and-projections-egress.md`](../handoff-2026-08-09-atlas-proxy-and-projections-egress.md) §2 already documented this: NBA offseason **and** all three upstreams Akamai/WAF-blocked incl. the `rpc-sports-proxy` worker (DK 403/502, ESPN 403, scoreboard 502). **Even the scoreboard that would prove "no slate" is blocked**, so `all_upstreams_failed` is the literally correct classification and "no code fix remains" | ⚠ Do NOT retire — sole writer for `nba_games`, read by a live public team page. Self-resolves at season start (~Oct) if egress recovers. If the red arm becomes noise, the sanctioned reversible lever is `INSERT INTO pipeline_alert_suppression (pipeline, reason, expires_at) VALUES ('sync-nba-projections', …, '2026-10-14')` — operator call, auto-expires. ⚠ **Method gap this exposes: the sweeps never cross-referenced root-level `docs/handoff-*.md` before filing** (sweep E explicitly read only those referenced by the recent ledger, not all 81). Grep the handoffs per finding next pass | — |
| D31 | 2026-08-09 | P2 | Migration parity backlog: 14-day window **223** prod rows with no committed file; total prod 2,482 vs 402 committed files | ⚠ **The 3-day window is clean again (verified 2026-08-09 — all 25 recent prod migrations have files)**, but that was always the easy part. The 14-day backlog is the real blocker for making `migration-parity.yml` enforcing | Claude Code |
| D32 | 2026-08-09 | P2 — **2 of 5 resolved 2026-08-09; they are OPPOSITE cases** | 5 pipelines find rows and write none for 7 days: `topshot-onchain-art-backfill` (4,430/0), `topshot-subedition-circulation-backfill` (22,020/0), `match-topshot-players` (11,320/0), `pinnacle-listings-retry` (3,045/0), `pack-events-ingest-backfill` (45,237/0) | ⚠ **`topshot-onchain-art-backfill` is a GREEN PIPELINE THAT ACCOMPLISHES NOTHING** — its `extra` payload is decisive and identical every run: `scanned:60, thumbs_filled:0, videos_filled:0, resolver_misses:60, terminated_reason:"no_more_editions"`. **100% resolver-miss rate, `ok=true`, 8×/day, indefinitely.** Not idle — actively failing and reporting success. ⚠ **`topshot-subedition-circulation-backfill` is the OPPOSITE: finished work.** Only **53 of 3,694** subeditions still lack `circulation_count`, so its 22,020 "found" is re-enumeration of already-complete rows — a drain nobody turned off. **Do not apply one remedy to both.** Note the 6,598 TS NULL-thumbnail editions are ~entirely the known 6,561 UUID-keyed non-canonical residue (honest gap), NOT this pipeline's failure. **Other 3 also resolved and NONE is a defect** — `pack-events-ingest-backfill` reports `caught_up:true` with `from_block == to_block` (idle by design); `pinnacle-listings-retry` reports `cadence_resolved:6, resolved:2, still_unresolved:4` (it IS resolving — `rows_written` just doesn't count resolutions); `match-topshot-players` is a **review-queue generator** (`auto_aliased:0, needs_review_count:1233`) — writing 0 is correct, but ⚠ it reports `candidate_count:0` and `best_sim:0.13` for names as common as LaMelo Ball, so the *matcher* may be broken and 1,233 players sit in a queue nobody reviews. **Verdict: "finds rows, writes none" is not a defect signature on its own — read the `extra` payload, which is decisive in all 5 cases**. ⚠ **`match-topshot-players` matcher REFUTED as broken 2026-08-10 (Claude Code): it is CORRECT.** `nba_players` holds only **174 rows and ZERO "Ball" players** — LaMelo Ball genuinely isn't in it, so `similarity()` over the whole table maxes at **0.13** and the name correctly queues (`candidate_count 0`, no exact `full_name_normalized` match either). Root cause is an **incomplete 174-row reference roster**, populated by the NBA roster/projections feed which is **egress-blocked + offseason-dormant (D28)**, and consumed only by the Fast Break game (`app/api/fast-break/*`, also offseason-dormant) + `sync-nba-projections`. So the 1,233-name queue is mostly REAL players absent from the stub roster — **not a matcher bug, blocked on the same upstream as D28**, not independently fixable. Self-resolves if/when the NBA feed recovers at season start | Claude Code |
| D34 | 2026-08-09 | P2 | Pinnacle has no FMV confidence-share arm (29.7% HIGH+MED, untracked) because its FMV lives in `pinnacle_fmv_history` | ⚠ **Assessed 2026-08-09 — NOT safely doable as a 6th arm; the prerequisite is now named.** `rpc_trust_health_precompute_refresh` runs at ~569s of its 600s budget and has **only 3 EXCEPTION handlers across 7 legs** (verified `regexp_count(prosrc,'EXCEPTION')`=3), so an unprotected leg's timeout rolls back all 18 metrics. Pinnacle FMV is in a DIFFERENT table, so there is no existing leg to piggyback on and CLAUDE.md forbids adding a new scan. **Prerequisite: split the fn into per-leg functions with their own cron entries first** | Claude Code |
| D36 | 2026-08-09 | P2 | AllDay: 217 editions where `highest_offer > low_ask` (5.34%) vs TS 0.11% — a 50× ratio on the same code points at the AllDay offers lane. Median gap $1.00, max $115 | INFERRED benign timing skew, not proven | Claude Code |
| D37 | 2026-08-09 | P2 | AllDay `unmapped_sales` backlog growing: **94,852** unresolved (was 90,865 on 08-08, +3,987) | resolver never reaches the tail | Claude Code |
| D13b | 2026-08-09 | P2 | **Pinnacle grain question + the stale ask feed.** `get_collection_stats` still reads legacy `pinnacle_editions` for `edition_count` (527, EDITION-grain) while `pinnacle_catalog` is RENDER-grain (2,457); `sniper_deals` / `top_sales` / `tier_breakdown` also still read the legacy table | ⚠ The FMV half was fixed 2026-08-09 (D13). What remains: the page's 5 "cheapest asks" are the **23-day-old** legacy ask feed of which **140 of 328 are exactly $1** (the documented uniform-$1 Flowty artifact), and `edition_count` may be understated 4.7×. ⚠ **Do NOT swap 527→2,457 casually** — that moves a public headline and is the D20 merged-denominator trap. Memory `pinnacle-render-id-rekey` says `render_id` is the true per-pin key and the legacy `edition_key` is 91.6% wrong, which argues FOR the catalog grain, but this needs a deliberate product decision and should repoint all four consumers as ONE coherent change | Claude Code |
| D4b | 2026-08-09 | P2 | `/[collection]/overview`'s "TOP 5 SNIPER DEALS" does not apply `isVerifiedDeal`, while the Sniper tab does — the other half of the same-collection contradiction in D4 | Left deliberately: it is a product call whether Overview should gate, flag, or keep showing ask-priced rows | Trevor / Claude Code |
| D26 | 2026-08-09 | P3 | 4 Top Shot players have duplicate rows sharing one slug (`alexandre-sarr`, `alperen-eng-n`, `carlton-carrington`, `maya-caldwell`) | ⚠ **IMPACT CLAIM CORRECTED 2026-08-09 — "splits each player's catalogue" is NOT observable at the surface.** 3 of the 4 twins hold **0 editions** (harmless fossils). For the only real one (Sengun 33 vs 6), `get_player_detail` resolves deterministically via a designed tie-break ladder (`team_edition_count DESC, is_active, headshot_url, id LIMIT 1`) and its `edition_count` does not key on `player_id` at all — it returns **59**, more than either row or both combined (39). Repointing would change nothing rendered. **Downgraded P2 to P3; do not "fix" the data** | Claude Code |
| D30 | 2026-08-09 | P3 | 3 components are production-dead (test-only importers): `components/InsiderSignals.tsx`, `profile/TierBreakdownCard.tsx`, `profile/PortfolioSparkline.tsx` | ⚠ Deleting any requires removing its `check-brand-tokens.mjs` PROTECTED entry **in the same commit** (`PortfolioSparkline` has one; the live `analytics/InsiderSignals.tsx` is a DIFFERENT file and keeps its entry). ⚠ It also deletes 3 well-covered component suites, mechanically LOWERING the component ratchet — the one legitimate reason to move a ratchet down, but it must be deliberate. **Downgraded P2 to P3:** pure cleanup, no user impact | Claude Code |

---

## RESOLVED

| id | resolved | finding | fix | revert path |
|---|---|---|---|---|
| D33 | 2026-08-09 | **A fabricated bid-ask spread on the PUBLIC `/insights/candy-mlb` board.** `candy_offer_spread_board` computed `floor_usd − best_offer_usd`, where `best_offer_usd` is a **mint-grain** max bid (a bid on one specific NFT) and `floor_usd` is an **edition-grain** min ask over a **different copy**. ⚠ Filed as "offers look mis-attributed" — the offers data is CORRECT (`candy_best_offers` is a clean `GROUP BY edition_id / max(price_usd)`); only the derived metric was wrong | **Measured before the fix: of 33 two-legged rows only 2 had the top offer on the same mint as the floor listing and only 5 had the offered mint listed at all — so 94% priced one NFT against another; 7 (21.2%) were NEGATIVE (worst −91.9%, avg −54.4%), which reads as "buy below the standing bid". In all 7 the bid target was NOT LISTED.** ⚠ **On a like-for-like basis ZERO books are crossed** — every negative was grain artifact, not market condition. **Fix (migration `20260810062100`):** dropped `spread_usd`/`spread_pct`; added `floor_mint`/`best_offer_mint`/`same_copy` so the grain is visible in the data and no consumer can silently rebuild the bad subtraction, plus `floor_copy_bid_usd` + `exec_spread_usd`/`exec_spread_pct` — a genuine executable spread on the **floor copy only**, ask-denominated (bounded, no clamp needed), NULL on the 122 of 125 rows where the cheapest copy carries no bid. Surface renders the executable spread only, marks cross-copy rows "≠ copy", and the tab prose now states the two floors are quotes on different copies. ⚠ **The clamp was the tell:** a 2026-08-01 pass saw the crossed rows, judged a bid-above-ask a *rendering* problem and pinned the display at 0% — hiding the symptom and leaving the cause. **A figure that needs clamping to stay plausible should be doubted, not clamped.** Row count (125) and both-legs count (33) unchanged; view re-verified anon/authenticated SELECT-revoked, `check_public_security_invariants()` 0 | `git revert <code sha>` + re-create the view from `20260724160400_audit_20260724_candy_deals_spread_market.sql` (full SQL in the new migration's header) |
| D12 | 2026-08-09 | `/nba-top-shot/analytics`: `ORDER BOOK DEPTH 1 listings` and `$0.00 / 0 sales` for 30d | **Two separate defects, both fixed.** (a) The orderbook panel rendered count/min/**median**/**p90**/max ask from `ts_listings` — a table retired 2026-05-26 holding **1 row from 2026-05-15**, so every figure was a percentile over one 86-day-old row. ⚠ Its banner claimed the table is "**periodically-refreshed**… typically **100-200 listings**", so a visitor seeing `1` concluded the market had one listing — **a stale disclosure is worse than none**. The app already contradicted itself (`SniperFilterBar.tsx:157` calls it "a dead table"; edition/moment pages already render an em-dash). Section retired with an honest note. (b) `$0.00 / 0 sales` was NOT a data gap — `get_daily_marketplace_volume` returns **89,831 sales / $583,208** for that window; the page's fetch soft-failed to null and every KPI fell through `?? 0`, so a timeout rendered as a measurement. Now an em-dash | `git revert ffb513a2` |
| D13 | 2026-08-09 | Pinnacle overview reported ASK-derived numbers under FMV labels and flagged itself broken | ⚠ **The original filing ("the Pinnacle FMV recompute appears dead") was WRONG** — every Pinnacle pipeline is healthy (7d: fmv-recalc 27,620 rows, listings-indexer 3,806, catalog-floor-refresh 63,499; `pinnacle_fmv_history` 4.6h old). The real defect: `get_collection_stats`'s Pinnacle arm filled `fmv_covered`/`fmv_pct`/`fmv_last_at` from **`ask_price`/`ask_updated_at`** on the superseded `pinnacle_editions`. Page said **28% coverage / FMV AGE 23D / PIPELINE OUTDATED**; truth is **69.4% / 4.7h**. ⚠ Numerator bridged onto the EXISTING 527 denominator via `pinnacle_catalog.legacy_edition_key` — **the grain question was deliberately NOT resolved here** (D13b). Other collections verified unchanged | migration `20260810031639`; DB revert: replay substituting the ask block back |
| D19 | 2026-08-09 | CLAUDE.md's "~200 historical rows/24h" justification for the UFC revival arm | ⚠ **My original "premise is falsified" filing was WRONG and would have invited simplifying the arm.** Re-verified direct from `sales_2026`: **485 UFC rows ingested in 7 days, every one with `sold_at` ≤ 2026-04-17** (a month before the 2026-05-13 close), **zero** post-close sales. Sharper still — over 30 days **all 485 landed in a SINGLE burst on 2026-08-05, zero on the other 29 days**, and a burst is exactly what an ingest-time predicate misreads as a revival spike. **The `sold_at` keying is validated; do not simplify it.** Only the stale rate was corrected. ⚠ Side finding → D32: `pipeline_runs_daily` says `rows_written=0` for both UFC backfills that day and **no pipeline accounts for the 485 rows** | `git revert ffb513a2` (docs) |
| D-R1 | 2026-08-09 | **56,898 wmc rows rendered a nameless, mintless moment while the name sat in `editions`** — 47,498 AllDay across 49 wallets + 9,400 Golazos in 1 wallet | Ran the pinned SECDEF `backfill_wmc_metadata_from_editions` per wallet (COALESCE fill-only, idempotent). 56,898 → **197**, and the 197 are fully explained (59 NULL `edition_key`, 138 point at an edition with no name of its own). Golazos `mint_count` NULLs 9,400 → 0 | None needed — fill-only, nothing deleted, no prior value overwritten |
| D1 | 2026-08-09 | Unauthenticated service-role write IDOR on `/api/support-chat/feedback` | Every write scoped by `session_id` (already the crypto-random capability token RLS keys on); `sessionId` now required; `.select("id")` makes a zero-row update observable → a 404 that does not distinguish "no such id" from "not your session". **Also fixed: feedback-with-a-comment had NEVER persisted** (`"up: <text>"` violates `CHECK (feedback = ANY('up','down'))`) — comments now go to `feedback_details`. Tests 3 → 11, mutation-proven | `git revert 791d7e8c` |
| D2 | 2026-08-09 | 8 cron gate keys hardcoded in a PUBLIC repo (CODE half only) | All 8 read fail-closed edge secrets. ⚠ An env read alone is NOT fail-closed — an unset secret is `""` and a bare `?key=` would match it, so every gate carries an explicit unset-guard. Per-function secrets preserve the isolation the 8 distinct values gave. 5 docs redacted. New directory-driven guard test (6 cases). **Rotation still owed → D2b** | `git revert b4e46435` |
| D3 | 2026-08-09 | Set Tracker dead on 3 collections + raw Postgres error shown to end users | ⚠ Root cause was **not** slowness on big wallets: `lower(wmc.wallet_address) = lower(p_wallet)` is not sargable, so the planner drove from `editions` (8,579 rows) and probed the 2.2M-row wmc once per edition — **cost 124,243 → 2,551** — and it blew the 25s cap even for a 1-moment wallet. 5 functions fixed (TS/AllDay/UFC); **AllDay's tracker was equally dead and the audit never covered it**. Verified TS 254 sets, AllDay 363/95, UFC 256 — the last two match `analytics_sets_summary` exactly. Leak fixed via new `lib/api-error.ts` (503 + Retry-After, never the driver message); `/api/sets-db` had the same leak | `git revert 6693a126`; DB: re-apply each fn substituting the predicate back |
| D4 | 2026-08-09 | Sniper showed `0 deals` while 200 existed | ⚠ **Did NOT flip the default — the audit's framing was wrong.** Measured 54% of TS listed editions are HIGH/MED, so the gate is not broken; the board's rows are ask-derived (FMV *is* the ask, hence the 0% spread), so defaulting them visible would headline fabricated signal. Fixed the empty state instead — it blamed "your filters" for a default the user never set. New tested `countHiddenByVerifiedGate()` | `git revert f610ea33` |
| D5 + D35 | 2026-08-09 | 8 user-facing claims the product no longer honours, incl. the legal page | `/privacy` (wallet connect + Dapper/Flowty purchases), homepage "buy / skip recommendations", `/pricing` selling the 404ing `/rewards`, the concierge denying two boards public for 9–10 days + omitting them from its board list + an invite-vs-open-beta contradiction + twice pointing at `/rewards`, and 4 "connect a wallet" strings. Copy guard added to the wallet-connect invariant test (scoped to `.tsx` so the prompt's NEGATIONS don't trip it) | `git revert 3946753e` |
| D6 + D7 | 2026-08-09 | Two crons failing blind | `stale-fmv-monitor` had no `log_pipeline_run` at all while failing 40.8%; now logs both exit paths, `maxDuration` 30→60. ⚠ `ok=true` means the CHECK RAN, not that FMV is fresh. `drain-conflated-subeditions` was dark 9 days because it logged only at the end; it now writes a **start marker** and updates it in place, so a killed tick is left `ok=false / phase=started`. Budget 300→600 | `git revert cb5bcfd7` |
| D9 | 2026-08-09 | Net-of-fees figure had an INVERTED SIGN on the dominant row shape | `NetOfFeesNote` branched on `flipsNegative` (`f > a && margin <= 0`) as if it were a sign flag, so at ASK == FMV a −$0.25 margin printed `net +$0.25` — on ~199 of 200 rows. Now branches on `netMarginUsd < 0`. ⚠ The old test passed on an **impossible fixture** (`flipsNegative` true with a positive margin) | `git revert f610ea33` |
| D10 | 2026-08-09 | `/nba-top-shot/series/series-4` appeared to 404 | ⚠ Not a routing bug — `get_series_detail` returns a **populated** row (3,596 editions, $121k FMV). The page collapsed *error* and *absent* into `return null`, feeding `notFound()`, so a timeout rendered "does not exist" — and **defeated the layout gate, which fails open precisely to avoid soft-404ing real pages to Google**. Already fixed on set/player/team 2026-07-14; `edition` (the largest route) and `series` were missed. New 15-test guard, mutation-proven | `git revert 418a1ce7` |
| D14 | 2026-08-09 | 3 prod migrations with no committed file, 2 redefining a public board MV | Recovered byte-identical from `schema_migrations.statements[1]` (not retyped). ⚠ Two are NOT re-runnable and say so in their own headers. The ledger half was fixed by splicing the lost 5-prod-DB-changes entry. 3-day parity verified clean. **14-day backlog untouched → D31** | `git revert 90d5a3cf` (removes files only; unwinds nothing in prod) |
| D11 | 2026-08-09 | Overview rendered `TOTAL EDITIONS 0 / PRICED 0% / $0` for a collection with 6,190 editions | ⚠ **A failed read was served as HTTP 200.** `/api/collection-stats` returned `{error:"stats_unavailable"}` with status **200**; the page's idiomatic `if (!res.ok) throw` passed it, the error object was stored as `stats`, `stats` became **truthy**, and every KPI fell through its `?? 0`. `catch` never ran, so the page's own error banner — which already exists and reads "Couldn't load collection stats right now" — never fired, and the `CollectionStats` type already declared `error?: string` that nothing checked. Not AllDay-specific: any collection whose stats RPC times out showed it. Fixed at the source (503 + Retry-After via `lib/api-error.ts`, the D3 precedent — driver message logged, never published) **and** at the consumer (an `error`-body guard, so no future 200-with-error-body can resurrect it). ⚠ **`/api/platform-stats` carried a byte-identical copy of the trap** and was fixed in the same pass; it has no runtime consumer, so it was a landmine waiting for its first caller rather than a live defect. ⚠ **My own "zero consumers" claim about it was wrong** — a route test existed that my grep missed, and the suite caught it | `git revert <sha>` |
| D22 | 2026-08-09 | 4 URL-reachable prototype-key lookups, one a hard crash | **Already fixed by a concurrent session** (`46b0fb9f`, `ownLookup` on all 4 sites). Re-probed rather than assumed | n/a |
| D23 | 2026-08-09 | 4 drifted `TIER_ORDER` copies | ⚠ **Filed as duplication hygiene; was two LIVE data defects.** The FMV dashboard's TitleCase list never matched the RPC's UPPERCASE enum, so every row bucketed into one gray "Other" bar — and its test passed because the fixtures were TitleCase too. The sets dashboard's `tierMixTotal` sums only over the list, which omitted `uncommon` + the UFC ladder: AllDay's mix totalled 5,560 of 6,190 editions, UFC's 2 of 446. New `lib/tier-order.ts` exposes **RANKS, not an array**, so collection-disjoint tiers can share a rank | `git revert 4885d3ba` |
| D24 | 2026-08-09 | 29 layouts double-suffixed the brand in `<title>` | Stripped the baked-in suffix (output-identical). New directory-driven guard that explicitly still ALLOWS the sanctioned `absolute` opt-out | `git revert 4885d3ba` |
| D27 | 2026-08-09 | `/api/alerts` raw-`fmv_snapshots` + JS-dedupe anti-pattern | Repointed to `fmv_current`, chunked at 500. ~87 rows/edition against a 1000-row cap meant the window covered ~11 editions. The test fixture keyed on the old table and was updated — it would otherwise have kept validating the old shape | `git revert 418a1ce7` |
| D29 | 2026-08-09 | `purge-stale-listings` 401'd every Vercel cron tick | Accepts `CRON_SECRET` or `INGEST_SECRET_TOKEN`. ⚠ Same footgun that got `pinnacle-sync` deleted as "dead" when it had only ever been mis-authed | `git revert 4885d3ba` |
| D38 | 2026-08-09 | Doc drift in CLAUDE.md | **Mostly already drained** by concurrent sessions (37 crons ✓, 20/17/3 workflows ✓, UFC arm retirement ✓); the stale figures the audit cited live in **frozen dated session entries**, which were correct as of their tip and must not be rewritten. One live claim was genuinely wrong and is fixed: §17's "the lone exception is `FmvHistoryChart`" — there are **three** hardcoded chart strokes, all sanctioned | `git revert 418a1ce7` |

## VERIFIED-CLEAN (re-probe cheaply, do not re-audit)

| area | last verified | one-line probe | expected |
|---|---|---|---|
| RLS coverage | 2026-08-09 | `select count(*) from pg_tables where schemaname='public' and rowsecurity=false` | `0` |
| Public security invariants | 2026-08-09 | `select count(*) from check_public_security_invariants()` | `0` |
| Anon write surface | 2026-08-09 | `select count(*) from check_anon_write_surface()` | `0` |
| SECDEF anon-exec drift | 2026-08-09 | `select jsonb_array_length((select check_secdef_anon_exec_drift()))` ⚠ **not** `count(*)` | `0` |
| Security advisors | 2026-08-09 | `get_advisors(security)` | 0 ERROR (7 WARN are the benign `*_security_definer_function_executable` class on 3 known fns) |
| Performance advisors | 2026-08-09 | `get_advisors(performance)` | 0 ERROR, 0 WARN |
| Staged Panini/Candy data is genuinely data-gated | 2026-08-09 | `count(*) filter (where has_table_privilege('anon',c.oid,'SELECT'))` over `relname like 'panini%' or 'candy%'` | 37 objects, **0** anon-readable |
| No anon-readable view bypasses RLS | 2026-08-09 | anon-readable `relkind='v'` whose `reloptions !~ 'security_invoker=(on\|true)'` | `[]` |
| No anon/auth-readable materialized views | 2026-08-09 | `relkind='m'` with anon or authenticated SELECT | `[]` — sharpest silent-leak class, empty |
| No anon-executable SECDEF function writes | 2026-08-09 | `prosecdef and has_function_privilege('anon',oid,'EXECUTE')`, grep `prosrc` for INSERT/UPDATE/DELETE | 4 grants / 3 fns, **none write** |
| No hardcoded credentials (JWT, `sb_secret_`, `sk-ant-`, `AKIA`, `ghp_`, `re_`, Telegram) | 2026-08-09 | grep those prefixes outside `node_modules` | **0 hits** (the 8 `rpc_pls_` gate keys are tracked separately as D2) |
| No secret leaked to logs or response bodies | 2026-08-09 | grep `NextResponse.json(…process.env…)`, `Object.fromEntries(req.headers)`, echoed `Authorization` | **0 hits**; only boolean-presence + length are logged |
| GitHub Actions secret handling | 2026-08-09 | 20 workflow files | all via `Bearer ${{ secrets.X }}` or `env:`; never in a URL, never echoed, no `set -x` |
| IDOR — the guarded majority | 2026-08-09 | ~60 routes | ownership enforced via session id / `.eq("user_id", user.id)` / `requireOwnedKey`; all 47 `admin/**` Bearer-gated; all 4 `breaks/[id]/**` token-gated |
| No PII in anon-readable surfaces | 2026-08-09 | scan the 77 anon-readable tables for email/phone/IP/token columns | **none**; exposure is deliberate public-profile + public-market data only |
| No stalled pipelines | 2026-08-09 | `select * from detect_stalled_pipelines()` | `[]` |
| Every pg_cron failure is the known self-recovering saturation class | 2026-08-09 | `check_pgcron_recent_failures()` | all errors are bare `statement timeout` on REFRESH MV / big INSERT…SELECT. **Any non-timeout error is a real finding** |
| Overall pipeline failure rate | 2026-08-09 | 24h agg on `pipeline_runs` | 2.20% (320/14,569); ~80% of fails are saturation-class |
| pg_cron has no disabled/orphan jobs | 2026-08-09 | `select count(*), count(*) filter (where active) from cron.job` | 80 / 80 |
| Vercel cron tick delivery (no cron-job.org-style dropout) | 2026-08-09 | ratio of `pipeline_runs_daily` runs to schedule-implied count | 95–112% across 15 jobs; only the 3 broken jobs show real loss |
| `evm-transfers-ingest` confirmed dead as documented | 2026-08-09 | `select max(day) from pipeline_runs_daily where pipeline='evm-transfers-ingest'` | `2026-08-02` |
| In-code TODO/FIXME backlog genuinely drained | 2026-08-09 | `grep -rn '\bTODO\b\|\bFIXME\b\|HACK:'` excl. node_modules/.next/docs | 6 hits, **0 actionable** (4 retrospective prose, 2 test names) |
| `.ilike` on an enum column | 2026-08-09 | `\.ilike\(\s*["'](confidence\|tier\|fmv_confidence\|liquidity_rating\|status)["']` | 3 hits, all on `cached_listings.tier` (plain text, not the enum) — clean |
| Batch `.insert()` swallowing 23505 | 2026-08-09 | grep `23505` in non-test code | every live batch site retries row-by-row; `backfill/route.ts:265` is single-row (N/A) |
| `rows.length` used as a PostgREST total | 2026-08-09 | `rows\.length\|data\.length\|total:\s*\w+\.length` over `app/api` | ~70 hits, all loop bounds / pagination sentinels — **0** substituted for a count |
| Unchunked `.in()` > 500 | 2026-08-09 | `\.in\("(edition_id\|external_id\|id)",\s*<ident>\)` | ~55 sites, substantially all chunked; 2 exceptions tracked in D27 |
| Brand tokens | 2026-08-09 | `node scripts/check-brand-tokens.mjs` | exit 0; every `#E03A2F` in a protected file is the sanctioned `var(--rpc-red, #E03A2F)` fallback or carries a `brand-exception` comment |
| `check-brand-tokens.mjs` references only existing files | 2026-08-09 | same script (exits 1 naming any missing file) | exit 0 — the 2026-08-08 5-commit CI landmine has not recurred |
| CI job set | 2026-08-09 | read `.github/workflows/ci.yml` | 8 jobs, **all blocking**, zero `continue-on-error`; thresholds match CLAUDE.md exactly (primary 89.3/75.1/91.5/91.6, component 78.6/66.4/78.2/82.6) |
| Both launched boards are indexable + route-open + sitemapped | 2026-08-09 | `grep -n "CANDY_MLB_PUBLIC\|PANINI_PUBLIC" proxy.ts lib/sitemap-data.ts` | flags `true`, gates are `!FLAG && …`, both slugs in sitemap, neither noindex |
| Canonical tag on every insights board | 2026-08-09 | `grep -rLn canonical app/insights --include=layout.tsx` | empty (31/31 have it) |
| Anon first-run CTA is identifier-only | 2026-08-09 | `grep -n "placeholder =" components/WalletSearch.tsx` | "Top Shot username, 0x wallet, or moment ID"; no connect surface anywhere |
| No cart / gifting / trading in rendered copy | 2026-08-09 | `grep -rn "add to cart\|trade-hub\|/api/gift" app components --include=*.tsx` | 0 hits |
| Zero/negative FMV | 2026-08-09 | `count(*) filter (where fmv_usd<=0)` on `fmv_current` + `pinnacle_fmv_history` | `0` everywhere |
| `circulation_count = 0`, impossible jersey, `drop_weight > orig_drop_weight`, negative pack EV, `fmv_sanity_flags`, `sales_serial_supply_worst_pct`, `topshot_impossible_parallel_serials`, subedition without a name | 2026-08-09 | see report §C-10 | **0** on every one |
| TS parallel conflation rate stable | 2026-08-09 | parallel share of canonical TS editions | 3,670/13,106 = **28.0%** (CLAUDE.md says ~27.9% — current) |
| Golazos `edition_offers` still absent (honest gap, not fabricated) | 2026-08-09 | `select count(*) from edition_offers where collection_id='06248cc4-…'` | `0` |
| AllDay + Golazos pack pools never decremented — **honest gap, not a defect** | 2026-08-09 | `count(*) filter (where drop_weight = orig_drop_weight)` by collection | AllDay 89,783/89,783, Golazos 1,957/1,957 — no upstream feed exists |
| Golazos badge `updated_at` median 363h is **not** staleness | 2026-08-09 | — | `refresh_golazos_badge_low_ask()` uses `IS DISTINCT FROM`, so it is a last-**changed** stamp. Do not "fix"; never render it as "updated N days ago" |
| Candy MLB wmc is the reference implementation | 2026-08-09 | NULL rates by column | 0 NULLs on every column, 0 fossils, 25,375 rows = supply exactly |
| **A failed read served as HTTP 200** (the D11 class) | 2026-08-09 | `rg -U 'error[^)]{0,120}\},\s*\{\s*status:\s*200' app/api` — then for each hit ask: *does a caller reading the success field see a plausible value?* | 3 live instances fixed (`collection-stats`, `platform-stats`, `support-chat/search-deals`); the rest are **deliberate and must not be "fixed"**: cron routes returning `{ok:false, skipped:"throttle_error"}` at 200 (the caller is a scheduler and the real status goes to `pipeline_runs`), `bundle_not_supported` on the two pack-EV routes (a successful determination, not a failure), `track-funnel` invalid-event (fire-and-forget telemetry must not fail the client), and `wallet-cost-basis`/`wallet-hold-time` returning `rows:[]` **with an explicit `reason`** for a non-TopShot collection (an honest "we don't have this here"). ⚠ **The tell is not the 200 — it is whether the failure shape is INDISTINGUISHABLE from a legitimate success.** `search-deals` was the sharpest: its failure body `{deals:[]}` was byte-identical to its own "no deals matched" response ten lines below |
| **A live surface reads a retired table** (the D12/D13 class) | 2026-08-09 | For each table a public surface reads, compare `max(<write ts>)` against the freshness the surface implies. Residue swept 2026-08-09: `ts_listings` 1 row/86.5d **(was live-read — fixed)**, `pinnacle_editions.ask_*` 23.5d **(was live-read — fixed)**, `pinnacle_cached_listings` 141 rows/62d (no live reader), `flowty_transactions` 78d (frozen history, documented), `special_serial_holders` 35d (documented legacy), `evm_nft_transfers` 0 rows (documented dead), `cached_listings` **fresh** | A retired table does not throw — it returns stale rows or one row, and the surface renders that as current market data. **Signature: a table whose newest row predates the last deploy by months but is still joined into a rendered KPI** |

---

## NOT-A-FINDING (healthy by design — do not re-raise)

| item | why | probe |
|---|---|---|
| UFC 1,654h FMV staleness / 0% HIGH+MED | Market closed May 2026. `ufc_fmv_pct_stale_30d` correctly loud (96.3) | — |
| Golazos 0.35% HIGH+MED | Thin market, matches CLAUDE.md | — |
| 1,221 editions with `jersey_number > circulation_count` | A player wearing #23 on a 10-mint edition has no reachable jersey-match serial; the chip correctly never fires | — |
| Pinnacle 72.3% NULL serial in wmc | Pinnacle has unserialized editions by design (`edition_type`) | — |
| `special_serial_holders` holding only 25 rows | Legacy table; the live board is wmc-backed per CLAUDE.md. Not a collapsed pipeline | — |
| `ufc-sales-indexer` writing 0 rows | Correct — market closed. The revival detector is the right instrument | — |
| `ownership-sync-dune` running once a week | By design (free Dune credit tier) | — |
| `sales-seller-recovery-dune` inert | Documented — missing `DUNE_SALES_SELLER_QUERY_ID` | — |
| `/api/cron/warm` not writing `pipeline_runs` | Deliberate (`route.ts:16`) — "a warmer is not a data pipeline". Only the 33× 504 rate is the finding (D-adjacent) | — |
| `fmtUsd` 9 variants, serial-multiplier 2 copies, `computeDualPrice` lib↔Deno | Duplication done **right** — documented consolidation decision / pinned by cross-agreement tests | — |
| `acquisition-stats` `SLUG_TO_DB_SLUG[input] ?? input` prototype key | Value only reaches `.eq("slug", …)`, matches nothing, falls back to the TS collection id. Identical to any unknown slug | — |
| TS headline confidence share appearing to fall 54.9 → 46.6 | **Measurement artifact.** Per-day repriced cohort oscillates 55.8–71.0 over 7 days with no trend. Quote a 7-day mean, never a single reading | per-day cohort query in report §C-1 |

---

## DECLINED — do not re-suggest
*(Trevor's section — only he edits this.)*
