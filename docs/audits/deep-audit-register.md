# RPC deep-audit findings register

Persistent across monthly deep audits. **Purpose: never re-derive a settled question.**

How to use it: at the start of each deep audit, run only the one-line probe for each `VERIFIED-CLEAN` row. If it still returns the expected value, stamp the date and move on — do NOT re-audit that area. If a probe fails, that becomes a finding and earns a full investigation. Re-measure each `OPEN` item's evidence number and record whether it grew, shrank, or resolved.

Created 2026-08-09 (run 1). Last pass: **2026-08-15 (run 2)**.

⚠ **Probe hygiene, learned on run 1:** `select count(*) from check_secdef_anon_exec_drift()` returns **1** when the surface is CLEAN — the function returns a single row containing a JSON array, so `count(*)` counts the row, not the findings. Use `jsonb_array_length((select check_secdef_anon_exec_drift()))` and expect **0**. The count-based probe produces a false positive every time.

⚠ **MECHANISMS MAY BE INHERITED FROM THIS FILE. SEVERITIES, RATES AND LIVENESS CLAIMS MUST BE RE-DERIVED EVERY PASS.** Run 1 had six findings overturned and every one was a severity/rate/liveness claim; not one was a mechanism claim. Run 2 then found **D2b's own ✅/❌ lists were inverted** and **D32's headline example had silently resolved** — both inherited, both wrong. Re-measure before acting.

⚠ **Run-2 additions to the four cheap checks:** (5) **A watchlist/suppression justification is a claim with an expiry date** — `candy-listings-indexer` kept `info` severity on the premise "candy_mlb is unpublished", invalidated by the 2026-07-31 go-live; two siblings were re-derived and this one was missed. Sweep the class, not the row. (6) **`check_anon_write_surface()` is blind to a route holding the SERVICE-ROLE key** — it tests the `anon` DB role, so an unauthenticated HTTP route that deletes via `supabaseAdmin` passes it (run-2 finding R2). (7) **A cost estimate is not a measurement and one warm timing is not a cost** — quote buffers, which are load-independent.

---

## OPEN

| id | first seen | sev | finding | current evidence (run 2, 2026-08-15) | owner |
|---|---|---|---|---|---|
| R1 | 2026-08-15 | **P0** | **Two overview pages state "No sales in the last 24h" during a DB timeout.** `overview/page.tsx:331` and `:444` apply `?? 0` to a NULL `stats`, converting an honest 503 into a market assertion. Sixth instance of the honesty-table class, on the very page D11 was fixed on — D11 hardened the KPI band directly above and did not reach these two panels | VERIFIED: Top Shot **8,332** sales / All Day **240** in the same 24h the pages said none. `/api/collection-stats` correctly 503s (`code:"timeout"`) and the KPI band correctly shows em-dashes, so the D11 fix works; the page contradicts itself on screen (its Insider Signals panel lists 269- and 171-moment sweeps from 1–2h earlier). Fix = branch on error/null BEFORE the length check, plus a directory-driven both-directions guard | Claude Code — handoff §1 |
| R2 | 2026-08-15 | **P0 (latent)** | **`/api/edition-floor` lets an UNAUTHENTICATED caller trigger a service-role DELETE on `fmv_snapshots`.** `?persist=1` / `{persist:true}` are caller-controlled; `proxy.ts:665-673` opens the route to anon GET/POST under a comment claiming "No writes, no user data" | VERIFIED: no auth check in GET or POST; `persistFloorToSnapshot` builds a SERVICE_ROLE client and runs `.delete().in("edition_id", ids).gte("computed_at", todayStart)` for ≤50 editions. INFERRED worst case: on a truncated read (R3) the re-insert fails wholesale after the DELETE committed (`confidence` is NOT NULL), inside a catch logging "persist failed (non-fatal)". NOT firing today — 0 rows at `algo_version='1.2.1'` in 30d, only caller is a single-edition GET with no persist. ⚠ `check_anon_write_surface()` cannot see it (tests the anon DB ROLE, not a service-role-keyed route) | Claude Code — handoff §2 |
| R3 | 2026-08-15 | P1 | **`/api/fmv` reports "No FMV data yet" for editions we have priced** — the D27 raw-`fmv_snapshots`-DESC + JS-dedup anti-pattern, unfixed on the documented product API | VERIFIED: 40.7 snapshots/edition mean (max 209) on TS; a realistic 100-edition batch yields 3,702 rows so the 1000-row cap covers **50 of 100**. Anon-reachable via `proxy.ts:666`. `alerts/route.ts:64-76` and `allday-pack-ev/route.ts:315-317` already cite D27 and read `fmv_current` — this route was missed. Same shape P3 in `scan-ufc-wallet`/`enrich-ufc-wallet`. ⚠ Update the `fmv_snapshots`-keyed test fixtures in the same commit | Claude Code — handoff §3 |
| R4 | 2026-08-15 | P1 | **Pinnacle sales stopped resolving `edition_id` ~2026-08-14 16:00Z and it is ESCALATING** | VERIFIED hourly: 0% through 15:00Z → 9.3% at 16:00Z → 29.4% at 22:00Z → **86.4% at 08-15 07:00Z**. 193 nulls in 24h and **all 193 of the 30-day nulls are inside that window** — clean onset, not a standing gap. Causes `/disney-pinnacle/overview` RECENT TOP SALES to render a **blank box with no empty-state copy**: all 5 top sales have NULL `edition_id` so the name join cannot match, and the guard runs on the unfiltered array (len 5) before the name filter strips all 5. ⚠ The join's INPUT is null — do not chase the name join (`pinnacle_catalog.legacy_edition_key` also 0 matches) | Claude Code / ingest — handoff §4 |
| D2b | 2026-08-09 | **P0 — ⚠ THE ✅/❌ LISTS IN THE RUN-1 ENTRY WERE INVERTED** | Rotate the pack-pipeline cron gate keys. Run 1 recorded `backfill-topshot-pack-supply` (cron 15+16) as "✅ rotated + verified" and 20/42/44/55/56/83/84 as the failing remainder | ⚠ **THE OPPOSITE IS TRUE (re-derived run 2, both directions).** Over the full 6h `net._http_response` window (707 rows): **71 are 403, all body `{"error":"forbidden"}`; 71 of 71 on jobid 16's cron minutes, 0 off them.** Every OTHER gate-keyed job is healthy in 24h `pipeline_runs`. Blast radius smaller than P0 implies — jobid 16 writes only `pool_source='gql_historical'`, frozen 2026-08-12 03:33Z, while the live `gql` lane is minutes fresh. ⚠ The **secret-exposure** half is untouched and is the real P0; setting the key to the value cron already sends is **service restoration, not rotation** (value is in public git history). Deploy needs BOTH `--no-verify-jwt` AND `--import-map supabase/functions/deno.json` | **Trevor (secrets)** |
| R5 | 2026-08-15 | P1 | **`compute-pinnacle-pack-ev` 100% failing since 08-11; fix `bd53bb3a` confirmed NEVER DEPLOYED** | VERIFIED via the fix's own telemetry: `extra ? 'dist_dupe_count'` is **false** on all recent runs ⇒ never deployed, "ineffective" excluded. `extra = {"elapsed_ms":1496,"function_version":2}`; error byte-identical every run (`ON CONFLICT DO UPDATE cannot affect row a second time`). Clean 4/4/day 07-30→08-10, 0 ok since 08-12. **Impact (new): `pack_ev_history` for Pinnacle frozen at 08-11 06:17:03Z ≈ 98h stale vs TS/AllDay 0.1h, and 22 of 81 (27.2%) frozen dists still carry a `+EV` flag** | Trevor (secret) + Claude Code |
| R6 | 2026-08-15 | P1 | **`get_collection_stats` still times out; the run-2 mitigation is NOT a rescue** | Shipped `AND computed_at <= now()` on both LATERALs: `Subplans Removed: 1`, that leg **7,763 → 6,220 buffers (−20%)**, deterministic. ⚠ Post-patch `('nba-top-shot')` returned CORRECT data in **84 s** and `('nfl-all-day')` still threw `57014` at **line 214** with the new predicate visible in the running code. All Day drives **2,230 LATERAL loops vs TS 769 (2.9×)** — why it fails first. Real fix = the filed precomputed latest-FMV-per-edition materialization; ⚠ its trap: the naive `DISTINCT ON` over `fmv_snapshots` **timed out at 55 s** when tested | Claude Code — handoff §7 |
| R7 | 2026-08-15 | P1 | **`drain-conflated-subeditions` is 100% dark-killed and nothing watches it** | VERIFIED: 5/5 runs 08-10→08-14 fail with `{"phase":"started"}` and `duration_ms` **147–176 ms** — only the start marker lands. Last success 2026-07-31. The D6 fix made it VISIBLE, not working; raising `maxDuration` 300→600 did not help and `route.ts:44-51` predicts otherwise. Absent from `pipeline_cadence_watchlist`; its step-5 `refresh_topshot_conflated_editions_detector_only` is inside the killed tick, so **the conflation guard has been stale by construction ~15 days**. Fix = split the 6 steps with per-step COMMIT + own row, NOT another ceiling raise | Claude Code |
| R8 | 2026-08-15 | P2 | **512 canonical TS editions have NO player; 482 render the SET NAME in the name slot** | VERIFIED 512/13,208 = **3.88%**; `name = set_name` on 482 (94.1%); `player_id` NULL on all 512; e.g. `229:7668` "Skyline", `98:3150::6` literally `"Unknown — Clamps"`. **Not a coverage gap** — all 512 `updated_at` within 3 days, 311 carry a `description`, only 18 distinct setIDs. Consequences: unreachable by player-name search, and the same moment shows a player in the wmc-backed collection table vs a set name on the editions-backed entity page. **357 of 512 healable** from modal non-null `wmc.player_name` (COALESCE fill-only) | Claude Code |
| D8 | 2026-08-09 | P1 — **REGENERATED 37×, in the collection nobody scoped** | wmc metadata denorm has no self-heal; post-pass enrichment failure is only `console.warn`'d and `skipCached` prevents recovery | ⚠ **197 → 7,369.** Run 1 predicted All Day would regenerate; **Top Shot did (7,087)** and All Day held (208). Honest-gap split matters: only **1,725 (23.4%) are healable defects**; 5,567 have no `edition_key` at all. UFC regenerated 306 `set_name` NULLs (304 healable) despite a closed market. Tool exists (`rpc_wmc_metadata_selfheal(uuid)`, fill-only, pinned) but automation is still blocked on the operator `CREATE INDEX CONCURRENTLY` in `docs/handoff-2026-08-10-d8-wmc-selfheal-index.md` — **now 5 days old while the backlog grew 37×**. ⚠ Run 2 deliberately did NOT run it: the instance was in an active saturation episode (7 long queries, 8 `DataFileRead` waiters) and CLAUDE.md records the automated sweep being removed after 3/3 saturation failures | Claude Code + operator |
| D37 | 2026-08-09 | P2 — **GREW** | AllDay `unmapped_sales` backlog | **97,812 → 106,069 (+1,376/day)**. Price-0 parked 61,373 → **68,515 (64.6%)**, correctly held (`promote_unmapped_sales` excludes `price_usd<=0`) — honest gap. Promotable 36,439 → 37,554. Both levers are ingest-capacity calls; no autonomous fix | Claude Code |
| D25 | 2026-08-09 | P2 — **AllDay half now 100% correctable** | 128 wmc rows render an impossible serial (e.g. "#500 / 499") | Unchanged at **128 / 2,228,087 = 0.0057%**, but the split MOVED: AllDay is now **57 stale-denorm / 0 upstream-wrong** (was 25/32), i.e. fully correctable; TS 71 (26 stale / 62 upstream-wrong) across 20 wallets. ⚠ `backfill_wmc_metadata_from_editions` will NEVER fix these (COALESCE fill-only by design) — needs a targeted UPDATE with a trailing `serial_number <= e.circulation_count` predicate so it cannot touch genuinely-upstream-wrong rows | Claude Code |
| D31 | 2026-08-09 | P2 — **regressed** | Migration parity backlog | prod **2,544** vs **539** committed files (was 2,482/402); 14-day window **196** (was 223). ⚠ **Method correction: `check-migration-parity.mjs` keys on NAME, and a VERSION-keyed comparison overstates the 3-day gap by a third** (9 → 1 real + 5 self-cancelling prototypes). The one real fileless migration is **`20260814045645 audit_20260814_editions_player_bio_columns`** — a live schema change with no git revert path. Recover byte-exact from `schema_migrations.statements[1]` (D14 precedent: recover, never retype) | Claude Code |
| R9 | 2026-08-15 | ~~P2~~ → **RE-POINTED** | ~~Homepage publishes three serial-premium multipliers the live model does not produce~~ **— THIS FINDING WAS WRONG; DO NOT ACT ON IT.** The real finding is: **two serial-multiplier models disagree ~3× and both are called "serial premium multipliers"** | ⚠ **`HomePageMarketing.tsx:145` is ACCURATE.** It states `lib/fmv/serial-multiplier.ts` verbatim (`serial===1 → 12.0`, `<=10 → 4.5`, `serial===circ → 3.0`) — the model the PUBLIC `/api/fmv` actually calls. The 9.89/1.50/5.00 are `serial_fmv_multipliers` ALL/ALL roll-ups, a **different subsystem** (`serial_fmv_estimate` → portfolio + underpriced-serials board). **Acting on the original recommendation would have replaced accurate API documentation with numbers from a model the API never calls**, and would have published one scalar for a matrix spanning 1.98–60× (first) / 1.00–16.25× (low) / 1.17–48× (perfect). Real open questions: (a) which model is authoritative for the product API; (b) `/api/fmv` hardcodes `serialMultiplier(serial, 1000)` and never reads `circulation_count`, so the documented `lastMint: 3x` is unreachable except on /1000 editions and the tail uses a fabricated denominator — a pricing change, filed not taken. **Shipped alongside:** `/api/fmv/demo`'s forked multiplier (tail drifted to `max(1,(circ/2/serial)^0.4)`, overstating serial-100 premium 1.90× vs the real 1.07×) deleted + pinned. Detail: `docs/overnight/inbox/2026-08-15T1507Z-…` | Trevor (which model is authoritative + the circ fix) |
| R10 | 2026-08-15 | P2 | **`openGraph`/`twitter` shallow-merge trap is LIVE in three shared helpers** | VERIFIED `lib/seo.ts`: `pageMetadata()` (~40 tab URLs) drops og `siteName`/`type`/`locale` + twitter `site`/`creator`; `buildMeta()` (whole entity corpus incl. ~23.5k editions) drops og `locale` + twitter `site`/`creator`; `collectionLayoutMetadata()` (5 roots) drops og `locale` + twitter `creator`. Plus ~30 of 31 insights layouts define `twitter` with no `site` — so the public boards unfurl with **no X byline**. `app/profile/[username]/layout.tsx` is the correct reference impl and documents the trap in its own header; the fix was never generalised | Claude Code |
| R11 | 2026-08-15 | P2 | **`candy-listings-indexer` runs and writes but does not log** | VERIFIED: `candy_listings.last_seen_at` fresh 06:40Z with 1,690 active listings, while the last `pipeline_runs` row is 08-14 21:35Z (3 ticks missing). `detect_stalled_pipelines()` fires at 644 min silent vs a 400 ceiling. INFERRED kill-before-log (self-measured `duration_ms` 314→404 s against `maxDuration=300`); refuted if a future tick logs ok at >404 s. ⚠ Per D17 do NOT treat duration-vs-`maxDuration` as the evidence — the MISSING ROWS are. Severity half FIXED in-DB run 2 | Claude Code |
| R12 | 2026-08-15 | P2 | **`/share/<wallet>` renders a failed read as "your wallet isn't indexed yet"**, and emits no canonical | VERIFIED `page.tsx:60-68` + `98-118` HTTP **self-fetch** returning `null` on any non-ok or throw, then `!data` treated identically to "0 moments" → `ShareEmptyState` claims the wallet is being indexed. Lowered from P1 only because the client re-polls every 8 s so a transient failure self-heals. Same self-fetch anti-pattern was removed from `/profile` in `dd13a03e` and the guard enumerated files by hand, so this sibling was outside it. Also: no `alternates.canonical`, so `/share/0xABC` and `/share/0xabc` are indexable duplicates | Claude Code |
| R13 | 2026-08-15 | P2 | **`/api/profile/achievements`: POST is a confused deputy; GET is the D11 class** | VERIFIED POST takes `ownerKey` from the BODY and calls the edge fn with the server's `INGEST_SECRET_TOKEN`, with no `requireOwnedKey` — every sibling `/api/profile/**` writer has one. Signed-in→any-user (not anon; route is not in `isPublicPath`). GET returns `{achievements:[]}` at **200** on a Postgres error, invisible to both leak guards because it swallows the message entirely. ⚠ The GET's missing ownership check is **NOT** a finding — `profile_achievements` carries a deliberate `"public read achievements"` policy (`roles={public}, qual=true`), so the rows are already anon-readable | Claude Code |
| R14 | 2026-08-15 | P2 | **2 new `function_search_path_mutable` advisor WARNs** on `reconcile_all_saved_wallet_stats` and `rpc_trust_health_precompute_refresh_p` (both from the 08-10 D34 split) | Only P2, and the reason is load-bearing: both are `prosecdef = false` (INVOKER procedures) with `has_function_privilege` **false** for anon AND authenticated, so a mutable `search_path` is style, not a privilege-escalation vector. Fix = `ALTER PROCEDURE … SET search_path = public, pg_temp`. ⚠ Deliberately NOT bundled into the run-2 migration — `apply_migration` costs a ~10–20 s user-facing `PGRST002` burst and the instance was already saturated | Claude Code |
| D39 | 2026-08-12 | P3 | `check_unmapped_backlog_growth()` has no cache-age guard — a persistent writer outage would serve the last-good payload forever as current, and a missing row returns `[]`, i.e. a clean bill of health manufactured from absence | Latent, NOT live. Cache age re-measured run 2 at **53.3 min**, well inside the 120-min worst case; the arm measures multi-day growth so a 2h-old read is immaterial. ⚠ Still deliberately NOT auto-fixed: this is the live Telegram/email alert path and a naive "raise when stale" risks a permanently-red arm, which this repo has repeatedly shown trains operators to skim past | Claude Code |
| D21 | 2026-08-09 | P2 | AllDay `edition_offers` bids stale while fresh AllDay offers live in `badge_editions` | Improved **12.8d → 11.5d** median. Newest row 0.11h proves the writer is alive — a long tail, not a dead pipeline. P1 escalation condition (cross-collection contamination) re-confirmed **NOT met**: AllDay's lane carries **no `low_ask` at all** (0 of 2,351), so it cannot contaminate an ask. ⚠ Side-finding: CLAUDE.md's "TopShot 1,929 low_ask of 6,930 ≈ 28%" is STALE — live **94.0%** | Claude Code |
| D26 | 2026-08-09 | P3 — **grew 4 → 5 AND the set CHURNED** | Duplicate TS player rows sharing one slug | 2 healed (`alexandre-sarr`, `carlton-carrington`), **3 new**. Only `alperen-eng-n` is a genuine split (35 vs 5 editions); the other 4 are 0-edition fossil twins. ⚠ **The finding is the PRODUCER, not the rows:** twins carry a THIRD `external_id` convention (`1630578` NBA-stats-id vs `flow:2991` vs `nba_top_shot-lonnie-walker-iv`), so something keeps minting a second row. Impact unchanged — `get_player_detail` tie-breaks deterministically and `edition_count` does not key on `player_id`. **Do not "fix" the data** | Claude Code |
| D30 | 2026-08-09 | P3 | 3 production-dead components (test-only importers): `components/InsiderSignals.tsx`, `profile/TierBreakdownCard.tsx`, `profile/PortfolioSparkline.tsx` | Re-verified run 2: still zero non-test importers. ⚠ The LIVE `components/analytics/InsiderSignals.tsx` is a DIFFERENT file and is imported by `app/(analytics)/analytics/page.tsx:17`. Deleting any requires removing its `check-brand-tokens.mjs` PROTECTED entry in the same commit, and mechanically lowers the component ratchet | Claude Code |
| R15 | 2026-08-15 | P3 | **The ledger's `## Queued` section has been dead since 2026-07-01** | VERIFIED `ledger.md:14159` — newest entry under that heading is 2026-07-01, and items beneath it are marked RESOLVED elsewhere in the same file (`UFC-EDITIONS-SEED-GAP` at 13808, `REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT` at 14213). Six weeks of queued work lives in the top-of-file dated entries instead. A reader following the documented "skim the ledger first" instruction lands on a stale, mostly-closed list — this audit's own brief pointed there | Claude Code |
| R16 | 2026-08-15 | P3 | **`scripts/local-cost-basis-backfill.mjs:137-138` invites the paste that caused the 08-03 history rewrite** | Committed placeholders `PASTE_FRESH_COOKIES_HERE` / `PASTE_FRESH_X_ID_TOKEN_HERE`, correctly no live value at HEAD — but the 2026-08-03 `git filter-repo` force-push existed specifically to purge leaked live Dapper session cookies, and this is a tracked file whose workflow is "edit these two constants in place". Fix: read from `process.env` + add to `.env.example` | Claude Code |
| D18 | 2026-08-09 | P2 — **list still CONTAMINATED, do not act on it** | "10 inert schedules that run on time and write nothing" | Untouched since filing. `pinnacle-sales-history-backfill` re-confirmed **NOT inert** run 2 (`rows_found 45 / rows_written 45`, live block scan). ⚠ Re-verify EVERY remaining entry against raw `pipeline_runs` + its `extra` payload before touching any of them | Claude Code |
| D32 | 2026-08-09 | P2 — **headline example RESOLVED; 4 of 5 were never defects** | 5 pipelines find rows and write none | ✅ **`topshot-onchain-art-backfill` is FIXED** — was 100% resolver-miss (`scanned 60 / thumbs_filled 0 / resolver_misses 60`), now `batch 300 / thumbs_filled 66 / videos_filled 65 / rows_written 131`. The other 4 confirmed not-defects (`caught_up:true`; resolving-but-not-counting; finished work; a review-queue generator blocked on the same egress-dead 174-row NBA roster as D28). **"Finds rows, writes none" is NOT a defect signature on its own — read the `extra` payload** | — |
| R17 | 2026-08-15 | P2 | **Two history backfills scan 40,000 blocks and decode zero** | `golazos-sales-history-backfill` `{rawV1:368, rawV2Dapper:460, rawV2Flowty:931, v1_decoded:0, editions_resolved:0, blocks:40000}`; `ufc-` similar with rawV1 2,539. ~49 runs each per 7d ≈ 35 min/week apiece producing nothing. ⚠ **Not the same as the two genuinely drained** — `topshot-sales-history-backfill` and `allday-studio-…` return `{"note":"queue_empty"}` in 0.7 s, a correct cheap no-op. INFERRED defect; **refuted if** `rawV1/rawV2*` are pre-collection-filter counts and those ranges genuinely hold no Golazos/UFC sales | Claude Code |

---

## RESOLVED

| id | resolved | finding | fix | revert path |
|---|---|---|---|---|
| R-M1 | 2026-08-15 | `get_collection_stats` probed 2 provably-EMPTY `fmv_snapshots` partitions on every LATERAL loop (~20% of that leg) on the TopShot + All Day branches — the two collections observed timing out | `audit_20260815_collection_stats_prune_future_fmv_partitions` — `AND computed_at <= now()` on both LATERALs. `Subplans Removed: 1`; leg **7,763 → 6,220 buffers**. Equivalence PROVEN not sampled (0 future-dated rows table-wide). Guarded migration refusing unless the pattern appears EXACTLY twice and refusing if already patched; nothing hand-transcribed. ⚠ **NOT a rescue — see R6** | Re-apply the previous definition, which differs ONLY by the absence of ` AND computed_at <= now()` in the two LATERAL WHERE clauses. Guarded inverse `DO` block in the migration header. Pre-image `md5(prosrc) = 8086744dce041410f8e62610a3bdc9aa` |
| R-M2 | 2026-08-15 | `candy-listings-indexer` held `info` severity on the premise "candy_mlb is unpublished … so a stall is not user-facing" — invalidated by the 2026-07-31 go-live. Its second claim ("clean no-op, ME listedCount 0") was also false: 1,690 active listings, 1,455–1,534 rows/run | `UPDATE pipeline_cadence_watchlist SET severity='medium'` + note rewritten with both corrections. **Decisive corroboration: both sibling Candy pipelines were already re-derived** (sales medium→high 07-31; offers info→medium 08-07 *for this exact reason*) — this row was simply missed by that sweep. Swept the whole class; exactly one live instance | `UPDATE pipeline_cadence_watchlist SET severity='info' WHERE pipeline='candy-listings-indexer';` (original note text preserved in `docs/audits/deep-audit-2026-08-15.md`) |
| D15 | 2026-08-12 | `check_unmapped_backlog_growth()` blew the alert path under saturation — `check-alerts` failed 14/71 (19.7%) on 2026-08-09 | Confirmed on 3 full post-fix days: 19.7% → 2.9% → 4.3% → 0.0%; p95 duration 46,216 ms → 9,946 ms. ⚠ "Resolved" means BACK TO BASELINE, not zero — pre-incident days were already 1.4–4.3% of the same timeout class, so **2.9–4.3% must not be re-opened as a regression** | n/a — verification only |
| D33 | 2026-08-09 | A fabricated bid-ask spread on the PUBLIC `/insights/candy-mlb` board — `floor_usd − best_offer_usd` subtracted a MINT-grain bid from an EDITION-grain ask | 94% of two-legged rows priced one NFT against another; 7 (21.2%) were NEGATIVE (worst −91.9%), reading as "buy below the standing bid" — all 7 grain artifact, ZERO crossed books like-for-like. Dropped `spread_usd`/`spread_pct`; added `floor_mint`/`best_offer_mint`/`same_copy` so the grain is visible and no consumer can silently rebuild it, plus a genuine `exec_spread_*` on the floor copy only. ⚠ **The clamp was the tell — a figure that needs clamping to stay plausible should be doubted, not clamped** | `git revert <code sha>` + re-create the view from `20260724160400_…sql` |
| D12 | 2026-08-09 | `/nba-top-shot/analytics`: "ORDER BOOK DEPTH 1 listings" and "$0.00 / 0 sales" | Two defects. (a) percentiles over ONE 86-day-old row from `ts_listings`, retired 2026-05-26, under a banner claiming "periodically-refreshed… typically 100-200 listings" — **a stale disclosure is worse than none**. (b) `$0.00 / 0 sales` was a soft-failed fetch falling through `?? 0`; the RPC returns 89,831 sales / $583,208 | `git revert ffb513a2` |
| D13 | 2026-08-09 | Pinnacle overview reported ASK-derived numbers under FMV labels | ⚠ The original filing ("the FMV recompute appears dead") was WRONG — every Pinnacle pipeline was healthy. Real defect: `get_collection_stats` filled `fmv_*` from `ask_price`/`ask_updated_at`. Page said 28% / 23D; truth 69.4% / 4.7h | migration `20260810031639` |
| D19 | 2026-08-09 | CLAUDE.md's "~200 historical rows/24h" for the UFC revival arm | ⚠ My "premise is falsified" filing was WRONG and would have invited simplifying the arm. **485 UFC rows in 7d, all `sold_at` ≤ 2026-04-17, all in a SINGLE burst on 08-05** — a burst is exactly what an ingest-time predicate misreads as a revival. **`sold_at` keying validated; do not simplify** | `git revert ffb513a2` (docs) |
| D-R1 | 2026-08-09 | 56,898 wmc rows rendered a nameless, mintless moment while the name sat in `editions` | Ran the pinned `backfill_wmc_metadata_from_editions` per wallet. 56,898 → 197 | None needed — fill-only |
| D1 | 2026-08-09 | Unauthenticated service-role write IDOR on `/api/support-chat/feedback` | Scoped by `session_id`; `.select("id")` makes a zero-row update observable. **Also: feedback-with-a-comment had NEVER persisted** (violated a CHECK). Tests 3 → 11, mutation-proven | `git revert 791d7e8c` |
| D2 | 2026-08-09 | 8 cron gate keys hardcoded in a PUBLIC repo (CODE half) | All 8 read fail-closed edge secrets. ⚠ An env read alone is NOT fail-closed — an unset secret is `""` and a bare `?key=` would match, so each carries an explicit unset-guard. **Rotation still owed → D2b** | `git revert b4e46435` |
| D3 | 2026-08-09 | Set Tracker dead on 3 collections + raw Postgres error shown to users | Root cause was NOT slowness: `lower(wmc.wallet_address)` is not sargable, so the planner probed 2.2M rows per edition — cost 124,243 → 2,551, and it blew the cap even for a 1-moment wallet | `git revert 6693a126` |
| D4 | 2026-08-09 | Sniper showed `0 deals` while 200 existed | ⚠ Did NOT flip the default — the framing was wrong. 54% of TS listed editions are HIGH/MED so the gate is not broken; the hidden rows are ask-derived, so defaulting them visible would headline fabricated signal. Fixed the empty state instead | `git revert f610ea33` |
| D5 + D35 | 2026-08-09 | 8 user-facing claims the product no longer honours, incl. the legal page | `/privacy`, homepage buy/skip, `/pricing` selling a 404ing `/rewards`, concierge contradictions, 4 "connect a wallet" strings. Verified live run 2 | `git revert 3946753e` |
| D6 + D7 | 2026-08-09 | Two crons failing blind | `stale-fmv-monitor` had no `log_pipeline_run` while failing 40.8%. ⚠ `ok=true` means the CHECK RAN, not that FMV is fresh. `drain-conflated-subeditions` now writes a start marker — ⚠ which **revealed** it is 100% killed; see R7 | `git revert cb5bcfd7` |
| D9 | 2026-08-09 | Net-of-fees figure had an INVERTED SIGN on ~199 of 200 rows | Branched on `flipsNegative` as if it were a sign flag. ⚠ The old test passed on an **impossible fixture** | `git revert f610ea33` |
| D10 | 2026-08-09 | `/nba-top-shot/series/series-4` appeared to 404 | Not routing — the page collapsed *error* and *absent* into `return null`, defeating the layout gate that fails open precisely to avoid soft-404ing real pages to Google | `git revert 418a1ce7` |
| D11 | 2026-08-09 | Overview rendered `TOTAL EDITIONS 0 / PRICED 0% / $0` for a collection with 6,190 editions | A failed read served as HTTP 200; the error object became truthy `stats` and every KPI fell through `?? 0`. ⚠ **Fixed the KPI band only — the two panels below it are R1** | `git revert <sha>` |
| D14 | 2026-08-09 | 3 prod migrations with no committed file, 2 redefining a public board MV | Recovered byte-identical from `schema_migrations.statements[1]` (**recover, never retype**) | `git revert 90d5a3cf` |
| D20 | 2026-08-09 | 299 sets collapse into merged entity pages; the "Variants merged" banner fired only on different SPELLINGS, so it was anti-correlated with the problem | `get_set_detail` now returns `underlying_set_count` from the 914-row `sets` table — complete by construction. Catches **all 116 real merges** vs the 2 the spelling key caught. ⚠ The register's "needs the MV → 4 layers" prediction was WRONG: RPC + page only. Re-verified consistent run 2 | `git revert <sha>` |
| D3b | 2026-08-09 | 11 more prod fns carried the non-sargable `lower(wmc.wallet_address)` | 6 that genuinely seq-scan wmc fixed. ⚠ The obvious rewrite `col = lower(param)` SILENTLY BREAKS CANDY (25,375 case-sensitive base58 rows) — only `col IN (raw, lower(raw))` is safe there. Equivalence proven: 0 colliding lowercase groups over 2.2M rows | `git revert <sha>` |
| D13b + D4b | 2026-08-09 | Pinnacle grain + the stale ask feed; Overview showed ungated ask-priced "deals" | Repointed to render-grain `pinnacle_catalog`: edition_count 527→2,457, FMV 69%→95.2%. ⚠ `top_sales` deliberately KEPT on `pinnacle_editions` (legacy join is 26:1). ⚠ **This migration added the LATERAL that now times out — see R1/R6** | `git revert` + CREATE OR REPLACE the pre-change body |
| D34 | 2026-08-09 | Pinnacle had no FMV confidence-share arm | Prerequisite cleared by splitting `rpc_trust_health_precompute_refresh` into 7 per-leg fns + a COMMIT-ing orchestrator; then shipped as the 8th leg. TRACK-only (a thin market is a fact, not an alert) | ledger 2026-08-10/11 |
| D16 | 2026-08-09 | `candy-offers-indexer` fails 5/13 (38%) | ⚠ Downgraded: the 72h window **straddled a fix**, so 38% describes neither era. Post-fix max duration 78 s, zero deadline hits. Discriminator is bimodal `bidder_fetch_errors` (0 on every success, 24–26 on every failure). User impact ZERO — the guard suppresses deactivation on a degraded sweep | — |
| D17 | 2026-08-09 | "4 Vercel cron routes at/over their Lambda ceiling" | ⚠ CLOSED — measurement artifact, both halves falsified. **Do not compare `pipeline_runs.duration_ms` to `export const maxDuration`** — `after()` routes self-measure across the background continuation; 8 of 8 over-cap runs were `ok=true`. The right kill-detector is MISSING TICKS (which is how R11 was found) | — |
| D22 | 2026-08-09 | 4 URL-reachable prototype-key lookups | Already fixed by a concurrent session (`46b0fb9f`). Re-probed rather than assumed | n/a |
| D23 | 2026-08-09 | 4 drifted `TIER_ORDER` copies | ⚠ Filed as duplication hygiene; was **two LIVE data defects**. The FMV dashboard's TitleCase list never matched the RPC's UPPERCASE enum so every row bucketed into one "Other" bar — and its test passed because the fixtures were TitleCase too | `git revert 4885d3ba` |
| D24 | 2026-08-09 | 29 layouts double-suffixed the brand in `<title>` | Stripped; directory-driven guard still allows the sanctioned `absolute` opt-out | `git revert 4885d3ba` |
| D27 | 2026-08-09 | `/api/alerts` raw-`fmv_snapshots` + JS-dedupe | Repointed to `fmv_current`, chunked at 500. ⚠ **The same anti-pattern survives on `/api/fmv` — see R3** | `git revert 418a1ce7` |
| D28 | 2026-08-09 | "the 08-08 v9 split may be mis-classifying" | ⚠ NOT A FINDING — the answer predated the audit in an **unreferenced** handoff. Method gap: sweeps read only ledger-referenced handoffs. Run 2 read all 101 and closed a second instance of the same trap (Phase F's handoff still shows an unticked box for work that shipped 2026-06-01) | — |
| D29 | 2026-08-09 | `purge-stale-listings` 401'd every Vercel cron tick | Accepts `CRON_SECRET` or `INGEST_SECRET_TOKEN`. ⚠ Same footgun that got `pinnacle-sync` deleted as "dead" when it had only ever been mis-authed | `git revert 4885d3ba` |
| D36 | 2026-08-09 | AllDay 217 editions where `highest_offer > low_ask` (50× the TS rate) | ⚠ PROVEN benign, and NOT timing skew — it is AllDay's cheap-market structure (median `low_ask` **$1.00**, 84% under $2, asks spread continuously below $1). A bid $1–2 over a $0.20 ask is ordinary noise | — |
| D38 | 2026-08-09 | Doc drift in CLAUDE.md | Mostly drained by concurrent sessions. ⚠ There are **three** sanctioned hardcoded chart strokes, not one | `git revert 418a1ce7` |

---

## VERIFIED-CLEAN (re-probe cheaply, do not re-audit)

| area | last verified | one-line probe | expected |
|---|---|---|---|
| RLS coverage | 2026-08-15 | `select count(*) from pg_tables where schemaname='public' and rowsecurity=false` | `0` |
| Public security invariants | 2026-08-15 | `select count(*) from check_public_security_invariants()` | `0` |
| Anon write surface | 2026-08-15 | `select count(*) from check_anon_write_surface()` | `0` — ⚠ but see R2: this tests the anon DB ROLE and is blind to a service-role-keyed HTTP route |
| SECDEF anon-exec drift | 2026-08-15 | `select jsonb_array_length((select check_secdef_anon_exec_drift()))` ⚠ **not** `count(*)` | `0` |
| Security advisors | 2026-08-15 | `get_advisors(security)` | 0 ERROR; 9 WARN = 7 known-benign `*_security_definer_function_executable` + 2 new `function_search_path_mutable` (R14); ~197 INFO `rls_enabled_no_policy` (deny-all, expected) |
| Performance advisors | 2026-08-15 | `get_advisors(performance)` | 0 ERROR, 0 WARN (all INFO) |
| Staged Panini/Candy data is genuinely data-gated | 2026-08-15 | anon SELECT over `relname like 'panini%' or 'candy%'` | 37 objects, **0** anon-readable |
| No anon-readable view bypasses RLS | 2026-08-15 | anon-readable `relkind='v'` whose `reloptions !~ 'security_invoker=(on\|true)'` | `0` |
| No anon/auth-readable materialized views | 2026-08-15 | `relkind='m'` with anon or authenticated SELECT | `0` — sharpest silent-leak class |
| No trivially-true write policy reachable by anon/authenticated/public | 2026-08-15 | permissive `cmd in (INSERT,UPDATE,DELETE,ALL)` policies where `qual` and `with_check` are both true/null and roles ∋ anon\|authenticated\|public | `[]` — wider than `check_anon_write_surface` |
| Anon write grants are all RLS-neutralised or shape-bounded | 2026-08-15 | resolve each of the 20 objects holding an anon write GRANT against its policies | 4 genuinely anon-insertable (`email_subscribers`, `funnel_events`, `outbound_clicks`, `support_conversations`), all with length/range `with_check` |
| No anon-executable SECDEF function writes | 2026-08-15 | `prosecdef and anon/auth EXECUTE`, grep `prosrc` for INSERT/UPDATE/DELETE | 4 grants / 3 fns, **none write**, all carry a pinned `search_path` |
| No PII in anon-readable surfaces | 2026-08-15 | PII regex over the 89 effectively-anon-readable tables | none. ⚠ `support_conversations.user_email` is capability-gated by `session_id`; `editions.player_birthdate/_birthplace` are public athlete catalog facts (regex false positive) |
| No hardcoded credentials | 2026-08-15 | grep `eyJhbGciOiJ`, `sb_secret_`, `sk-ant-`, `AKIA`, `ghp_`, `re_`, Telegram shape, `rpc_pls_` | **0 real hits** (the only match is this register describing the probe) |
| No secret leaked to logs or response bodies | 2026-08-15 | grep `Object.fromEntries(req.headers)`, `JSON.stringify(process.env)`, echoed `Authorization` | **0** leaking hits |
| GitHub Actions secret handling | 2026-08-15 | 33 `secrets.*` refs across 20 workflows | all `Bearer ${{ secrets.X }}` or `env:`; never in a URL, never echoed |
| IDOR — `/api/profile/**` writers | 2026-08-15 | grep each route for `requireUser`/`requireOwnedKey`/`user.id` | all gated **except** the POST in R13 |
| IDOR — service-role `.delete()` in `app/api` | 2026-08-15 | grep `\.delete\(\)` + 3 lines | every user-scoped delete carries `owner_key`/`user_id`; rest are pipeline deletes scoped by `collection_id`/cursor. ⚠ R2 is the exception and is a DELETE-then-reinsert, not a user-scoped delete |
| `/api/profile/follows` proxy carve-out is safe | 2026-08-15 | the exact-path GET/HEAD bypass | POST and DELETE both `requireUser()` — the route self-gates |
| pg_cron failure class | 2026-08-15 | `check_pgcron_recent_failures()` | 8 rows, **all** bare `statement timeout` on REFRESH MV / big INSERT…SELECT. **Any non-timeout error is a real finding** |
| pg_cron has no disabled/orphan jobs | 2026-08-15 | `select count(*), count(*) filter (where active) from cron.job` | **85 / 85**. ⚠ The invariant is "no inactive rows", not the count — it moved 80 → 85 |
| Scheduler inventory | 2026-08-15 | `vercel.json`, Glob `.github/workflows`, `cron.job` | 38 Vercel crons · 20 GHA files (17 scheduled) · 85 pg_cron — all match CLAUDE.md |
| CI job set | 2026-08-15 | read `.github/workflows/ci.yml` + both vitest configs | 8 jobs, all blocking, zero `continue-on-error`; thresholds match CLAUDE.md **in both directions** — primary **91.2/78.3/93.0/93.35**, component **88.5/79.4/88.2/91.6** |
| In-code TODO/FIXME backlog genuinely drained | 2026-08-15 | `grep -rn '\bTODO\b\|\bFIXME\b\|HACK:'` excl. node_modules/.next/docs | 0 actionable — third independent confirmation. `docs/code-todos.md` both items closed |
| `.ilike` on an enum column | 2026-08-15 | `\.ilike\(\s*["'](confidence\|tier\|fmv_confidence\|liquidity_rating)["']` | 3 hits, all `cached_listings.tier` (plain text, not the enum) |
| Batch `.insert()` swallowing 23505 | 2026-08-15 | grep `23505` in non-test code | all 5 forward indexers retry row-by-row; guard is directory-driven and blocking |
| `rows.length` used as a PostgREST total | 2026-08-15 | `rows\.length\|total:\s*\w+\.length` over `app/api` | every `count:"exact"` site reads `count` |
| Unchunked `.in()` > 500 | 2026-08-15 | `\.in\("(edition_id\|external_id\|id)",\s*<ident>\)` | ~60 sites, all bounded by chunk/slice or an upstream cap. ⚠ R3 is a **missing `.limit()`**, a different defect |
| `error.message` in a response body | 2026-08-15 | the 5 spellings in `__tests__/helpers/driver-message-leak.ts` | clean-by-guard; both anon and authed guards blocking, and the authed one asserts anon's set is a subset |
| Brand tokens | 2026-08-15 | every `#E03A2F` / `'Barlow Condensed'` in a PROTECTED file must be the `var(--rpc-red, #E03A2F)` fallback or carry `brand-exception` | clean; all **3** sanctioned chart strokes present and annotated |
| Both launched boards indexable + route-open + sitemapped | 2026-08-15 | `grep CANDY_MLB_PUBLIC\|PANINI_PUBLIC lib/launch-flags.ts proxy.ts` | both `true`; the only `robots:{index:false}` in `app/insights` are the two flag-conditional lines, inert while true |
| Canonical tag on every insights board | 2026-08-15 | `grep -c canonical app/insights/**/layout.tsx` | 31/31. ⚠ `/share/[wallet]` has NONE — R12 |
| No cart / gifting / trading / wallet-connect in rendered copy | 2026-08-15 | grep those strings over `app`, `components` | **0 hits** |
| Anon first-run CTA is identifier-only | 2026-08-15 | `WalletSearch` placeholder + submit path | no connect surface. ⚠ It advertises "moment ID" which the submit path cannot resolve — filed separately |
| `generateMetadata` false-figure trap | 2026-08-15 | read `app/profile/[username]/layout.tsx` | FIXED and correct — every figure gated on `> 0`; it is the reference impl for R10 |
| Zero/negative FMV | 2026-08-15 | `count(*) filter (where fmv_usd<=0)` on `fmv_current` + `pinnacle_fmv_history` | `0` everywhere |
| `circulation_count=0`, `drop_weight > orig`, `fmv_sanity_flags`, `topshot_impossible_parallel_serials`, `sales_serial_supply_worst_pct` | 2026-08-15 | combined probe / precompute | **0 / 0 / 0 / 0 / 0.00** |
| FMV coverage per collection | 2026-08-15 | `editions LEFT JOIN fmv_current` | Candy 100% · Golazos 100% · AllDay 100% · TS 99.32% · UFC 89.77% (edition grain) |
| FMV confidence shares | 2026-08-15 | `rpc_trust_health_precompute` metric `<coll>_fmv_high_med_share_pct` ⚠ never call `rpc_fmv_confidence_share()`; **999 is a failure sentinel, not a percentage** | TS 54.9 · Candy 60.0 · Pinnacle 30.8 · AllDay 27.8 · Golazos 0.7 · UFC 0.0 |
| TS pack-EV `pool_empty` sentinels are HONEST | 2026-08-15 | `pack_ev_latest` TS grouped on `edition_count=0` | 291 rows: 291 NULL `gross_ev`, 291 NULL `pack_ev`, 0 `is_positive_ev` — nothing fabricated |
| TS parallel/subedition conflation | 2026-08-15 | `editions` TS by key shape | 3,757/13,208 = 28.4%; **0** parallels missing `subedition_id` or `_name`; **0** subedition ids on a base key |
| `candy_offers` | 2026-08-15 | full table | 257 rows / 45 active / 0 null edition_id / newest 1.5h / **0** active-unseen-12h / 0 active-but-expired |
| Set-name merges consistent with D20's fix | 2026-08-15 | `sets` vs distinct slug | AllDay 363→143, TS 272→192 (= the 299 D20 identified) |
| MV / catalog freshness | 2026-08-15 | `pg_stat_all_tables` | `sets_summary` 0.5h · `pack_grail_metrics_mv` 0.9h · `mv_topshot_set_play_catalog` 1.5h · `pinnacle_catalog` 0.6h |
| `funnel_events` DB CHECK vs route allowlist | 2026-08-15 | `pg_get_constraintdef` vs `ALLOWED_EVENT_TYPES` | identical, 10 values — no silently-rejected event type |
| `evm-transfers-ingest` confirmed dead as documented | 2026-08-09 | `select max(day) from pipeline_runs_daily where pipeline='evm-transfers-ingest'` | `2026-08-02` |
| **A failed read served as HTTP 200** (the D11 class) | 2026-08-15 | `rg -U 'error[^)]{0,120}\},\s*\{\s*status:\s*200' app/api` — then ask: *does a caller reading the success field see a plausible value?* | ⚠ **The tell is not the 200 — it is whether the failure shape is INDISTINGUISHABLE from a legitimate success.** Several 200s are deliberate and must NOT be "fixed" (cron routes returning `{ok:false, skipped:…}` to a scheduler; `bundle_not_supported`; `track-funnel` invalid-event; `wallet-cost-basis` returning `rows:[]` with an explicit `reason`). New instance found run 2 → R13 |
| **A live surface reads a retired table** (the D12/D13 class) | 2026-08-09 | for each table a public surface reads, compare `max(<write ts>)` against the freshness the surface implies | **Signature: a table whose newest row predates the last deploy by months but is still joined into a rendered KPI** |

---

## NOT-A-FINDING (healthy by design — do not re-raise)

| item | why | probe |
|---|---|---|
| UFC 1,654h FMV staleness / 0% HIGH+MED | Market closed May 2026 | — |
| Golazos 0.7% HIGH+MED | Genuinely market-limited — re-tested 2026-08-13: only **14 of 575** editions have ≥3 sales in 30d; the busiest traded 6 times in a month. Ceiling ~2.4–2.8%; **there is no All-Day-shaped win here** | — |
| 1,221 editions with `jersey_number > circulation_count` | A player wearing #23 on a 10-mint edition has no reachable jersey-match serial; the chip correctly never fires | — |
| Pinnacle 72.3% NULL serial in wmc | Pinnacle has unserialized editions by design | — |
| Pinnacle has **0 rows in `pack_drop_pool`** | Table-scope fact — Pinnacle EV comes from the edge fn's own path. A grain artifact, not "EV computed with no pool" | — |
| `special_serial_holders` holding only 25 rows | Legacy table; the live board is wmc-backed | — |
| `ufc-sales-indexer` writing 0 rows | Correct — market closed. The revival detector is the right instrument | — |
| `ownership-sync-dune` weekly | By design (free Dune credit tier) | — |
| `sales-seller-recovery-dune` inert | Documented — missing `DUNE_SALES_SELLER_QUERY_ID`. ⚠ Its 3 `idx_sales_*_nullseller_soldat` indexes read as "unused" but back this job — do not drop | — |
| `/api/cron/warm` not writing `pipeline_runs` | Deliberate — "a warmer is not a data pipeline" | — |
| `compute-laliga-pack-ev` logging `{"phase":"invoked"}` with 0 rows | ⚠ **NOT inert** — it is the fire-and-forget invoker for `compute-golazos-pack-ev`, and `pack_ev_history` for Golazos is 1.5h fresh. The rollup alone would have condemned a working trigger. Observability gap only | latest `extra` |
| `topshot-pack-opens-history-backfill` 496 runs/7d, 0 rows | `extra` = `{"done":true,"spork_available":true}` — finished, not stalled | latest `extra` |
| `pack-events-ingest-backfill` finding rows, writing none | `caught_up:true` with `from_block == to_block` on all three cursors | latest `extra` |
| `topshot-active-listings-ingest` ~69% fail | `egress_blocked` — the documented Atlas WAF issue; `atlas-proxy` INERT pending an operator `wrangler deploy` | — |
| `sync-nba-projections` 100% fail | D28 — NBA offseason + all three upstreams WAF-blocked. Self-resolves ~Oct | — |
| `upstream request timeout` appearing "new" in a 72h view | Present continuously since 2026-07-31; `pipeline_runs` retention (~73h) makes chronic saturation look new | `pipeline_runs_daily` `last_error` over 20 days |
| `fmtUsd` 9 variants, serial-multiplier 2 copies, `computeDualPrice` lib↔Deno | Duplication done **right** — documented decision / pinned by cross-agreement tests | — |
| `refresh_wmc_fmv_changed` at 112 GB / mean 330 s | **Not a defect** — 112 GB is the price of the `wmc.fmv_usd` denormalization (UPDATE fan-out), not a bad plan. The lever is the denormalization | — |
| Golazos `highest_offer` 0/218; AllDay+Golazos pack pools never decremented; TS 6,561 UUID-keyed NULL-thumbnail residue | **HONEST GAPS** — no upstream source exists | — |
| Golazos badge `updated_at` median 363h | `refresh_golazos_badge_low_ask()` uses `IS DISTINCT FROM`, so it is a last-**changed** stamp. Never render as "updated N days ago" | — |
| Candy MLB wmc | Reference implementation — 0 NULLs on every column, 0 fossils, 25,375 rows = supply exactly | — |
| TS headline confidence share appearing to fall 54.9 → 46.6 | **Measurement artifact** — the per-day repriced cohort oscillates 55.8–71.0 with no trend. Quote a 7-day mean, never a single reading | — |

---

## DECLINED — do not re-suggest
*(Trevor's section — only he edits this.)*
