# Nightly autonomous pass — 2026-07-06 (RUN 2, GENUINE OVERNIGHT ~01:03 PDT / 08:03Z; no clock skew; shipped 0 correct; post-ship watch of the 07-05 evening P1–P8 CC wave ALL PASS; CLOSED 1 self-healed; queued 1 new + carried)

## Mode & timing (READ FIRST — this is the SECOND 07-06 pass)
- This is the real scheduled 01:02-PDT overnight run, firing **~8h after** the earlier off-hours `run-3983f38a` (which fired 00:08Z / 17:08 PDT against a 16h-stale sandbox and correctly shipped 0). Its handoff is `docs/handoff-2026-07-06-overnight-pass.md`; this run's handoff is this `-2` file so neither clobbers the other.
- **No clock skew this run.** shell `date -u` 08:02:41Z ≈ DB `now()` 08:03:15Z ≈ newest `sales.ingested_at` 08:03:05Z ≈ newest `fmv.computed_at` 07:58Z — all within ~35s (production rows can't be future-stamped, so they bound real time from below). Real time = ~08:03Z / **01:03 PDT = GENUINE OVERNIGHT** → normal shipping allowed.
- Gates: lock was RELEASED (prior run), no `docs/FREEZE.md`, **push AVAILABLE** (`git push --dry-run` = "Everything up-to-date"). Clone `$HOME/rpcwork`; origin/main `c2918b66` unchanged start→end (latest commit 22:56Z 07-05, ~9h old — origin NOT advancing, no concurrent session).
- **Shipped 0** (correct — see Candidates), reverted 0, repaired 0, **closed 1**. Drained 1 inbox file (`2026-07-06T00-06Z.md`).

## Post-ship watch — 07-05 EVENING P1–P8 CC wave (the prior off-hours run watched the EARLIER subedition wave, NOT this one): ALL PASS, 0 reverts
Prod has advanced `c09f9693` → **`12bf57ce` (dpl_5Zs53) READY** = the full P1–P8 wave is live. Commits `a49f4c3`(P7a)→`abe1b27`(P6a)→`7223bd6`(P3)→`7d216ec`+`12bf57c`(P4), 22:12–22:56Z 07-05.
- **P3 `get_pack_detail_bundle` (pack-detail perf) — HOLDING.** Fn present, SECDEF, `statement_timeout=30s`, not anon-exposed (`secdef_anon_violations []`). **Every `[pack-detail] …statement timeout` runtime-error group is attributed to the SUPERSEDED `dpl_6mMQJbzU` (pre-P3 prod), last seen ~02:49Z 07-06; ZERO on current prod `dpl_5Zs53`.** The 10-way fan-out → 1 bundle RPC + Suspense-stream change is quieting the class. Target (pack-detail statement-timeout → ~0 over 48h) trending correctly — continue watching.
- **P4 Pinnacle render-keyed serial-premium FMV — clean.** `compute_pinnacle_serial_fmv_multipliers()` + `pinnacle_serial_fmv_estimate()` present + SECDEF + secured; new table RLS-on (`rls_off_base_tables []`); kept separate from `pinnacle_catalog.fmv_usd`; mint≥25 UI guard (`12bf57c`) is current prod. No new Sentry issue / no new runtime-error class.
- **P6a Pinnacle moment-page site chrome — clean.** Only signal on `/pinnacle/moment/[id]` is the benign `DEP0169 url.parse` deprecation (present platform-wide since 2026-03-22). No new error class.
- **P7a `sales-serial-backfill` pipeline_runs logging — CONFIRMED.** The edge fn (console-only before) now writes `pipeline_runs`: 06:40:27Z ok=true 5.4s `extra={"resolved":2,"failed":0,…}`. Working as shipped.
- **Env-fix `c09f9693` (drain-orchestrator NEXT_PUBLIC_SUPABASE_URL) — VERIFIED.** `drain-conflated-subeditions` ran 20:30Z 07-05 **ok=true** 95.8s, `extra.split.wmc_split=2283` — the orchestrator executes real subedition splits post-fix (this was the prior run's open P8 verification).
- Security **0/0/0/0** after all wave migrations.

## Health-drift triage (rpc_ops_snapshot @ 08:06Z + drill-downs) — GREEN
- security **0/0/0/0** (invariants / anon_write_holes / rls_off_base / secdef_anon all []).
- trust **16/16 ok, breaches []** — incl. `topshot_impossible_parallel_serials` 1/3, `unmapped_resolution_backlog_max` 30/100, `edition_integrity_flags` 4/50, `fmv_sanity_flags` 0, `pinnacle_fmv_stale_hours` 22/30 (nearing but ok), all per-collection FMV-stale ok.
- `stalled_pipelines []`; `check_pgcron_recent_failures('24h') []` (no pg_cron job's LATEST run is currently failing).
- sentinel **TS-UUID editions 48h = 0**; `ts_uuid_dupes_created_24h` 0 → no writer leak.
- `pipeline_alerts`: 2 INFO (golazos_sales 3/24h, ufc_sales 42/24h resolving_editions — benign edition-resolution bridge).
- **Sentry 0 unresolved / firstSeen 24h.** Vercel prod `12bf57ce` (dpl_5Zs53) READY; no ERROR-state deploy (CANCELED entries = docs-only ignoreCommand + superseded-concurrent). No new runtime-error class — all 47 groups are known families (DEP0169, 300s backfill lambda timeouts, wmc idempotent dup-key, DBSAT connection-pool contention, IPFS ×2, Flow/QuickNode-429).
- **FMV improving:** TS HIGH+MED **4934** (1318+3616; up from 4913 @00:06Z / 4785 @07-04). AllDay 825 / UFC 15 / Golazos 4.
- **Editions:** TS **18,151** (+7 vs 18,144 = continued `::subID` cataloging, sentinel 0 = not a leak) / AllDay 6190 / Golazos 575 / UFC 518. (AllDay −1, Golazos −6 vs prior nightly = the 07-05 ghost-edition deletions `b707a60b`, benign/attributable.)
- **DB 8205 MB** (+46 vs 8159 @00:08Z = benign backfill drain; Pro-Micro headroom passive watch — this DB-growth-at-8GB is the root of the carried DAYTIME-CONTENTION statement-timeout family).
- `pipeline_fails_24h`: all transient (latest run ok=true) EXCEPT `pinnacle-studio-sales-history-backfill` (06:58Z ok=false, "pick: Timed out acquiring connection from connection pool") = a DRAINED no-op backfill failing to acquire a connection under overnight contention; watchlisted, `detect_stalled []`, recovers next tick (09:58Z) — DAYTIME-CONTENTION family, not a new class.
- **Conflation guard `topshot_conflated_editions` = 623** (last detected 06:23Z). This is the working set of the IN-FLIGHT daytime subedition de-conflation program (drains daily at 20:30Z via drain-conflated-subeditions, which split 2283 wmc last run). NOT trust-breaching (edition_integrity_flags 4/50, impossible_parallel 1/3, sentinel 0) → deferred to the daytime CC program (do not interfere with in-flight de-conflation). WATCH, not an incident.

## Candidates
- **CLOSED — ULTIMATE-FMV-RECALC-V1-MISSED-TICK (self-healed).** The 07-06 **06:35:22Z** daily tick fired **ok=true** (2552ms, no_data 601, sales_only 75). Clean cadence resumed (06-28→07-04 ok, 07-05 one-off overnight non-fire, 07-06 ok) — exactly the predicted self-heal (same class as the 07-04 TOPSHOT-FMV-POPULATE self-heal). No operator action.
- **QUEUED (NEW, night-count 1) — CROSS-SOURCE-DEDUP-STATEMENT-TIMEOUT (LOW-MED; DB fn; CC/operator, NOT a safe unattended ship).** `rpc-allday-cross-source-sales-dedup` (pg_cron jobid 32, `40 * * * *`) FLAPS at its 120s cap in the evening-contention window: overnight ticks 8–12s, but 00:40Z 95s / 22:40Z 91s / **21:40Z + 23:40Z FAILED at 120s**. It is **currently recovered** (last 8 ticks ok, 8–12s). Root: `dedup_allday_cross_source_sales()` does TWO full-table AllDay scans (an enrich `UPDATE` pass + a `DELETE`-to-`audit_20260702_allday_dedup_collapsed_sales` pass), each a `GROUP BY nft_id, round(price,2), date_trunc('day',sold_at)` over all growing AllDay `sales`. **Why NOT shipped (correcting the monitor's suggested fix):** the monitor proposed `WHERE sold_at > now() - interval '30 days'`, but that is NOT result-identical — the AllDay studio/on-chain deep-history backfills insert sales with OLD `sold_at`, so a `sold_at` window would miss backfill-created historical cross-source dups; and windowing the group-detection CTE risks SPLITTING dup groups (a dup needs both members visible in `g`). It is on the **`sales` DELETE path** (the repo's most invisible-failure-prone surface). No clean index fix either — a `sales(collection_id,nft_id,…)` composite taxes the hot ingest path (same reason CLASSIFY-ACQ rejected it). Blast radius is LOW (dedup BACKSTOP only; the writer-side insert trigger `trg_zzz_allday_cross_source_dedup` prevents most cross-source dups at insert; a missed sweep tick lets a handful linger ~1h to the next ok tick). **Correct fix = CC/daytime:** a recent-INGEST prefilter that preserves group completeness, OR the weaker `statement_timeout` raise (rejected historically for this contention family — holds a connection longer under evening pool saturation). Folds into DAYTIME-CONTENTION-CLUSTERS-BROADENING.
- **QUEUED (carried, night-count 2) — BADGE-CATALOG-STALE-429 (LOW/cosmetic; GHA/operator).** `topshot-badge-catalog` still last-logged 07-04 21:38Z ok=false (TS-GQL 429); no run since (~34h). SHARPENED: the `badge-sync.yml` GHA IS scheduled (catalog sweep `45 2,8,14,20`; tag sweep `15 */6`) so it should fire, but no SUCCESSFUL catalog run has logged since 07-02 16:26 (upserted 8986). Existing badge coverage (~9,136 canonical TS editions via `badge_editions` → `get_edition_badges_unified`) is intact; only new `::subID` editions may lack catalog entries until the next successful sweep. Can't trigger the GHA or read its run history from here (no workflow scope / GitHub connector needs auth). Operator: check the badge-sync Actions run history — are the `:45` catalog ticks firing, and are they repeatedly 429'ing?
- **QUEUED (NEW, future-night — gate UNMET) — SALES-SERIAL-BACKFILL-WATCHLIST.** P7a made `sales-serial-backfill` a `pipeline_runs` producer today (edge-fn v18, `a49f4c3`), so it's a candidate for a `pipeline_cadence_watchlist` row (natural completion of P7a). But it has only **1** logged tick (06:40Z ok) — the standard 2-consecutive-clean-tick gate is NOT met. Bank ≥2 clean ticks + confirm the `40 */2` cadence, then a future night ships: `INSERT INTO public.pipeline_cadence_watchlist (pipeline, max_silent_minutes, severity, notes, is_active) VALUES ('sales-serial-backfill', 400, 'medium', 'per-collection serial resolver, 40 */2 cadence; no-ops (resolved:0) when drained but keeps firing', true) ON CONFLICT (pipeline) DO NOTHING;` (revert: DELETE the row).

## Carried standing (unchanged — owned/operator/gated): DAYTIME-CONTENTION-CLUSTERS-BROADENING, CLASSIFY-ACQ-ALLDAY-STATEMENT-TIMEOUT (nc5, latest tick ok 08:06Z 14.8s), FMV-RECALC-EDITION-FETCH-TIMEOUT-CREEP (latest fmv-recalc 08:08Z ok=true but 122s — near budget), REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT, BUYERBF-PERINVOCATION-WORK, ALLDAY-V1-UNMAPPED-DRIFT (~held out of sales), WEEKLY-SURFACE-QA-PROSE, THIN-FMV-GUARD-CONTENTION, refresh-conflated-editions cron (operator), topshot-sales-history-backfill watchlist, VERCEL cost family, A1-WORKER-PASSTHROUGH-CLEANUP, PIN-FMV-REKEY-WAVES 2/3, PIN-SYNC-CRON, P3-BUYERS, DUPE1 (gated/CC), Q2/Q5/Q6, N1, ANALYTICS-SMOKE-RESIDUAL, IPFS ×2, SMOKE-SECURITY-GUARD-TRANSIENT-API-PROBE-DEBUG.

## Shipped: NONE (correct). Reverted: NONE (no regression attributable to any recent ship). Repaired: NONE.
## Metrics: docs/overnight/metrics-latest.json (live 08:06Z 07-06).
