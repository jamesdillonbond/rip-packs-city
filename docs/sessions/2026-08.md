# 2026-08 session archive

August 2026 session entries archived from `CLAUDE.md` (newest-first). The current `CLAUDE.md` keeps roughly the last 3 days inline.

---

### August 13, 2026 (Claude Code, interactive — "implement a TODO" → "keep going" ×8 → "don't stop until you've exhausted everything") — **started on a drained TODO backlog and ended by finding that the platform's biggest user-facing error is something WE cause every time we ship a migration.** ~10 pushes to `main`, every one CI-green, each ledgered with a revert path. No DB, migration, cron, auth/`proxy.ts`, hot-wallet, or FMV/pricing-MATH change all session — the DB lane was a concurrent session's and I stayed out of it deliberately.

- **The in-code TODO backlog really is drained** (re-verified exhaustively, 0 markers outside `docs/`), so every item below came from measuring something instead: a guard's blind spot, a disk-read ranking, or the Sentry board.
- ⚠ **THE HEADLINE, and it corrects my own earlier claim in the same session.** `PGRST002 "Could not query the database for the schema cache"` was the largest real user-facing error — `NEXTJS-1Z`, **81 users / 84 events since 2026-07-18**, plus edition + player. I classified it transient (`4f303102`) and reported it fixed. **Then I went back to verify and it wasn't that simple**: every schema-cache event in 24 h fell inside ONE 11-second window, 10-21 s after `schema_migrations` shows a migration applied. **We cause these 500s ourselves, one ~20 s burst per `apply_migration`.** And `rpcWithRetry` is 3 attempts at 50 ms + 200 ms ≈ **250 ms** — entirely inside the first quarter-second of a twenty-second outage, so the classification is right in kind and **does not close NEXTJS-1Z**. Canonical rule + the filed options now live under the `apply_migration` bullet.
- ⚠ **The leak guard's greatest strength was exactly its blind spot.** `anon-api-no-driver-message-leak-guard` EXECUTES `isPublicPath` so it can never miss an anon route — and that same derivation **fixes its scope to the anon wall**. Sweeping the routes `proxy.ts` GATES found **120 more leaking sites across 64 files**: the collector's own profile / wallet / alerts / watchlist / portfolio. One (`/api/profile/trophy/reorder`) is rendered **straight into a toast**, so a statement timeout was shown to a collector as the reason their trophy case did not save. Fixed by codemod on exact `(file, line)` matches (never a broad regex — this repo has a 351-file incident), with 13 contract-key sites done by hand. **Two codemod regressions were caught by existing tests**: a 502→500 flattening in `breaks/*` and a silently dropped `rows: []`.
- ⚠ **`safeApiError` only knows Postgres, so blanket-applying it DESTROYS an actionable error** — four routes throw their own "Could not resolve `<username>`", and classifying that as `internal` replaced the one thing the user needed with "Something went wrong." at a **500 instead of a 400**. Hence `isUnresolvedIdentifierError` / `unresolvedIdentifierResponse`.
- **59 existing tests asserted the leak**, two titled *"500s and **surfaces the message**"* — a title that makes a defect read as the contract.
- ⚠ **Two guards were fabricating verdicts out of their own failed reads.** The Pinnacle FMV drift guard (`NEXTJS-14`, 54 events since May) built its comparison set from `pinnacle_catalog` and never destructured that read's `error` — a failed fetch emptied the set, so **every priced row "leaked"** and it hard-paged. Verified false against the DB: every named row was present with a matching FMV. Fixed to `couldNotRun`, with an empty-but-successful read deliberately still a real leak.
- ⚠ **A stale canonical claim nearly made me DELETE a live tripwire.** CLAUDE.md said the sentinel's `Sales Ingest (2h)` check was "superseded by the per-collection arm"; reading the live RPC showed it keys on **`sold_at`** while the sentinel keys on **`ingested_at`** — different questions. Kept it, and made it cheaper instead: bounding the **partition key** prunes 6 of 8 partitions (`EXPLAIN` **212,454 → 107,025**) and makes it STRICTER, since a lone history backfill used to satisfy it while every forward indexer was dead.
- **Perf/cost, code-only:** `count_insider_detector_candidates` was **44.7 GB / 27 GB-per-day of pure telemetry** with no production consumer (12 calls per hourly tick, 4× re-scanning the same 24 h window) — sampled to every 6th UTC hour, ~22 GB/day recovered. ⚠ It needed a new `candidates_status` field rather than reusing `null`, which already meant "the RPC errored" — otherwise a broken instrument becomes indistinguishable from a deliberate skip.
- **Bounded the edition page's last two unbounded table reads** (`withQueryDeadline`), picked up as a clean handoff from a concurrent session's filing — and **corrected its spec**: it suggested a second primitive, but `withDeadline` is already shape-agnostic, and extracting those two does NOT satisfy the server-page ratchet (it keys on the `@/lib/supabase` import, still present for two `rpcWithRetry` sites).
- **Measurements that closed flagged items:** the roadmap's **headline metric** was re-measured post-sweep — **Top Shot 17.3 → 54.5%**, **All Day 6.3 → 27.7%**, Pinnacle 30.3% (never measured before) — with the correction that §6's target MOVED, since All Day quadrupled while Top Shot tripled. And **Golazos was re-tested rather than accepted**: it is genuinely market-limited (only **14 of 575** editions have ≥3 sales in 30 d; the busiest traded **6 times in a month**), so its ceiling is ~2.4-2.8% and **there is no All-Day-shaped win there**.
- ⚠ **Sentry triage method:** my own first sweep filtered `firstSeen:-3d` and **hid the biggest issue on the platform for being a month old**. Sort by frequency and users; verify every "stale" verdict against the live DB. Two verified-stale reds resolved; `NEXTJS-28` deliberately left open (fired 2 h ago).
- ⚠ **A mutation test that stops the suite PARSING proves nothing** — my first attempt at one spliced a file by string index, broke it, and reported `no tests`; redone as a clean one-token change. Every new assertion this session is mutation-proven, several in both directions (the naive `"schema cache"` substring reds the PGRST205 cases, i.e. the obvious implementation is demonstrably wrong).
- **Concurrent-session discipline:** rebased onto their commits five times, took one clean handoff, corrected one of their specs, and stayed out of the DB lane entirely — every DB-side fix I found (`get_series_detail` at **20 s** on 12 public pages, the one-pass insider RPC, the `STALE`-share arm) was FILED with measurements rather than taken.

### August 13, 2026 (Claude Code, interactive, cont. — Trevor: "Game winner isn't just for the win set moments… think about the Damian Lillard Archive and Run It Back Legacy Moments") — **a domain expert reading the OUTPUT caught in one sentence what three green gates, 11k tests and my own live query could not: "narrative search" returns a SET ROSTER, and I had verified the defect and reported it as a success.**

- ⚠ **THE CORRECTION.** I shipped `search_catalog` as "the only tool that can answer a NARRATIVE query", cited `lillard game winner` → "Damian Lillard — **For the Win**" as live proof, and built a **"Find a game winner" pill** on top of it. **"For the Win" is a SET NAME containing the query words.** I confirmed set-name matching and called it prose matching — three times, in a tool description, a system prompt, and the canonical docs.
- **The measurement that settles it:** `game winner` (Top Shot, limit 30) returns **0** actual game winners — a For-The-Win set roster, **`Block` variants included**, which is exactly how the defect announces itself to anyone who knows the catalog. `buzzer beater`, `series winner`, `game winning shot`, `walk off winner`, `lillard game winner`: all **0**. ⚠ The two most famous Blazers game winners are missed entirely — the 2014 0.9-second series winner (**Archive Set `48:1652`**) and the 2019 37-footer (**Run It Back: Legacies `121:4255`**) — **though BOTH carry descriptions**, one containing "game-winning" verbatim, the other "buzzer". Single-token `buzzer` returns *Buddy Hield* on a fuzzy NAME match instead.
- ⚠ **Root cause established by CONTROL, not inference — and it is not what it looks like.** The prose arm is **not broken**: `weber state`, a phrase present only in description text, resolves to exactly the right row. Matches are scored with trigram **`similarity()`, which is LENGTH-NORMALIZED**, so a 200-character paragraph containing the phrase verbatim scores BELOW the short set name "For The Win". **Prose only wins on a distinctive proper noun; every generic narrative phrase loses to a set name.** `similarity()` is the wrong scorer for a long text field — fix is a containment boost or full-text ranking, **NOT** raising the `via_prose` 0.12 constant (the gap is structural, not an offset).
- ⚠ **THE METHOD LESSON, and it is the one I keep paying for.** My verification asked *"did it return rows?"* instead of *"are the rows RIGHT?"* — the identical failure mode I had filed an inbox note about earlier the same session (a probe that passes during the outage it exists to catch). **For a relevance or ranking feature, the check must be a known-correct ANSWER, not a non-empty result** — and the cheapest source of a known-correct answer is someone who knows the domain.
- **Shipped:** pill REMOVED (live ~40 min demoing set-name matching as narrative search) + a test blocking re-add until the ranking is fixed **and verified against those two slugs**; tool description and prompt section rewritten to stop promising narrative search, name the concrete failure, and require the bot to READ results before presenting — if hits all share one set name and don't match what was described, say so rather than pass a roster off as an answer. Ranking defect FILED (`inbox/2026-08-14T0310Z-…`), not taken: `rpc_search_catalog` is a concurrent session's function shipped the same day.
- **Verified:** `tsc` clean; primary **11,563 green**; component **1,614 green**.

### August 13, 2026 (Claude Code, interactive — "CX and UX improvements? Also expand the concierge's capabilities and feed it additional intent abilities") — **the two new intents came from asking what the PLATFORM can answer that the BOT cannot; both UX finds were things that had never worked rather than things that broke.** 4 waves to `main`, each ledgered + deployed READY (checked per commit); both gates green; `tsc` clean.

- **Concierge 29 → 31 tools.** `search_catalog` (the catalog INDEX, searching `editions.description` as well as names — ⚠ **but see the correction entry above: it does NOT deliver narrative search**, and I shipped it claiming otherwise) and `get_price_history` (ACTUAL SALE PRINTS back to 2020, the only path past the ~4.5 months `fmv_snapshots` covers). Canonical detail + the honesty constraints now live in the AI Concierge section, not here. The gap existed because the site shipped narrative search EARLIER THE SAME DAY and nobody wired the assistant to it — worth remembering that a new capability does not reach the bot for free.
- ⚠ **A capability with no route to discovery is half-shipped** — but the route matters. Narrative search is the least guessable thing the concierge does (nobody thinks to ask a support bot to find a moment by what HAPPENS in it), so it needed surfacing. I first put it in the **welcome copy** and then reversed that in the same session: it is the first thing every user reads, the prompt's own rule is "one tight, human line, not a menu dump", and most users are on mobile — a clause there is read by everyone and acted on by almost nobody. It is now a **pill** ("Find a game winner"): no reading cost, one tap, and it DEMONSTRATES the capability by running it. ⚠ **Top Shot pages ONLY** — prose is ~45% there and **0%** on every other collection, and `search_catalog` scopes to the active collection, so the same pill on an All Day page would demo a coverage gap instead of the feature. Pinned + mutation-proven; do not copy it to other collections until their coverage is non-zero.
- ⚠ **The chat panel ignored the light theme, and had since the theme shipped.** Hardcoded `#0d0d0d`/`#141414`/`#ccc` while `ThemeToggle` flips ~20 tokens: a light-mode collector got a black slab over a white page. **Not a regression — it never worked**, which is why no test and no monitor ever said anything.
- ⚠ **Every `/insights/*` link the bot handed out was inert text**, for the same reason: the bubble rendered the message as one raw string and the repo has no markdown renderer. The prompt tells the bot these boards are "the most shareable thing RPC has" and the UI made them un-clickable.
- ⚠ **THE MOST TRANSFERABLE FIND — a regex I shipped would have killed the whole chat on older Safari, and every gate was green.** `rich-text.ts` used an ES2018 lookbehind. **`tsc` does not transpile regex literals**, so the pattern ships verbatim and is a `SyntaxError` at **PARSE** time on Safari < 16.4 — the module never evaluates, `SupportChat` never imports, the concierge vanishes. CI passed because the entire toolchain runs on Node (lookbehind since v8.3). **A Node test suite is structurally incapable of catching a browser-support defect**; only a source guard can. Fixed in a follow-up (`a0944b6c`) with that guard.
- ⚠ **TWO of my own tests were vacuous, both caught by mutation, both for the same underlying reason — the environment could not produce the failure.** (1) The focus-restore assertion passed with the fix REMOVED, because jsdom happily focuses a `display:none` element; rewritten to assert the MECHANISM (the launcher must not be hidden after close). (2) The pill test asserted two states in sequential `waitFor`s, which a plain overwrite satisfies at DIFFERENT TIMES — page pill before the fetch lands, server pill after. **Two assertions individually true at different moments do not prove they are ever true together; when state arrives asynchronously, pin the ORDER of the waits.**
- ⚠ **My own source guard tripped on its own comment** (the file explains why it refuses to render HTML, naming the banned API) — third instance in this repo, after `pack-dist-contents-not-streamed` and `collection-analytics-failed-vs-empty-guard`. **Any guard that searches source for a string must strip comments first.**
- **Also fixed:** the header ✕ and the launcher shared the accessible name "Close chat"; focus restoration silently no-op'd because the launcher hides itself while an input is focused; the chat was the only overlay of six with no `role="dialog"`/Escape/`aria-live`; and `PAGE_DEFAULTS`' 35 per-page pill sets were **dead code on every session**, overwritten ~200ms after open by the context route's generic list.
- ⚠ **Could NOT verify by HTTP round-trip:** the sandbox proxy blocks `rippackscity.com` (403 CONNECT), so production evidence is the direct Supabase RPC checks (`rpc_search_catalog`, `get_edition_sale_history`, `edition_description_coverage` all queried live) plus the suites — not a real request through the deployed routes.
- **Verified:** primary gate **11,397 tests green, 91.57 st / 78.91 br**; component gate **1,590 green, 90.03 / 81.42**; brand-token guard clean. Commits `9d383d6e` (tools) · `7114dc9e` (UX) · `a0944b6c` (Safari) · `43c14350` (pills + discovery copy).

### August 13, 2026 (Claude Code, interactive — "analyze test coverage → propose" → "do all you can that makes sense, don't stop until you've exhausted your options") — **measured first, and the measurement said breadth was already closed: 452/452 API routes and 250/254 `lib/` files are referenced by a test. So every find below came from the STRUCTURAL blind spots — and there turned out to be TWO of them, together ~75k LOC.** 6 waves to `main`, each ledgered; both gates green; `tsc` clean throughout. Primary gate 91.18→**91.54** st / 78.02→**78.90** br, ~360 tests added.

- ⚠ **The blind spot: `app/**/page.tsx` is 48,325 LOC measured by NEITHER gate** — comparable to the entire component gate's surface (45,875 LOC) — and **79 of those pages query Supabase directly**. The repo's existing answer was `server-pages-error-vs-absent-guard.test.ts`: hand-written source assertions covering **2** pages, i.e. a per-file allowlist for a class with ~79 members. Now also held by **`server-page-data-access-ratchet.test.ts`**, which freezes the count at **18** (36 when first landed → 35 `/moment/[id]` → 34 edition page → **26** when EIGHT `/insights` board pages moved to one shared `lib/insights/board-page-fetch.ts` → 25 when a DEAD import was deleted → 24 the analytics sets-detail page → 23 the allday-scarcity page and its API route on ONE shared query → **18** when FIVE more `/insights` board pages (squeeze, trophies, set-squeeze, offer-spread, pinnacle-scarcity) each moved onto a shared `lib/insights/<board>-board.ts` with its own route — a ratchet is lowered by the change that earns it, in the same commit). **Down to 8 as of 2026-08-16** (18 → 14 → 13 → 12 → 11 → **8**, each lowered in the commit that earned it). ⚠ **AND THE HONESTY SWEEP BEHIND IT IS NOW COMPLETE: all 8 remaining pages already distinguish a failed read from an absence** — verified page by page (`insights/cross-collection` carries `ok` on all three legs, `insights/market` a `loadError`, `insights` and `admin/flowty-errors` degrade by OMISSION, `challenges`/`hot-floors` carry an `errored` flag consulted *before* their empty state, and `pack/[id]` + `analytics/wallets` are pinned by the source guard). **What remains on the ratchet is structural debt with no known defect**, which is a materially different thing from where this started; the next conversion buys coverage, not a fix. ⚠ Extraction remained the way to REACH the defects right up to the end — every one of the 2026-08-16 finds below was invisible until the code moved somewhere a test could drive it, and each was already accompanied by a comment asserting the right behaviour. **The comment is not the check.** ⚠ When a page duplicates its route's query, **share the QUERY, not the POLICY**: return supabase-js's `{ data, error }` untouched so the route keeps `boardUnavailable()` and the page keeps `ok:false`, and let their `limit` defaults stay different. ⚠ And copy the route's ordering EXACTLY, **including secondary sorts** — a first draft dropped the squeeze route's `squeeze_pct` tiebreaks, which would have silently reordered equal-valued rows between the server HTML and the client's first refetch. A ban would have shipped a 35-entry allowlist (theatre); a ratchet forces NEW pages through `lib/` where a gate watches them. It carries a not-vacuous check and a **no-slack** assertion, because a ratchet with headroom silently licenses the next N additions. ⚠ **A NOT-VACUOUS CHECK MUST BE SATISFIABLE AT A POPULATION OF ZERO — ours was not, and it went RED on its own success (2026-08-16).** It asserted `pages.length > 10`; the population reached 8 and the guard failed. That is precisely the failure its own next comment warns about for naming a specific page — *"the guard would punish its own success"* — expressed as a threshold instead of a name, which is why nobody spotted it while writing the warning two lines below. **Assert that the ENUMERATOR still sees the tree** (the walk finds >50 `page.tsx` files), not that many of them are still dirty; the dirty count is the thing you are driving to zero, so it can never also be the evidence that the check works.
- **Extraction as the remedy** (`lib/trophy-case/pdf-image.ts` precedent): the pack-dist page's 11 fetchers → `lib/pack-dist/fetchers.ts`, injectable `db`, uniform `{ data, ok }`. ⚠ **It immediately exposed a live defect**: `fetchTopPulls` returned `[]` on a query ERROR and the caller renders an empty list as *"Drop-pool contents aren't indexed for this distribution yet"* — **a claim about OUR INDEX manufactured from OUR outage**, on packs whose pool is fully indexed. `fetchExhaustedCount` returned a literal **`0`** that a section header printed as a measured count. ⚠ **"Not applicable" is NOT a failure**: a Top-Shot-only panel on an All Day pack reports `ok: true`, else every All Day pack page carries a permanent degraded banner — the cry-wolf outcome `board-status.ts` warns about.
- ⚠ **The OG cards were the sharpest find, and the copy could not possibly have been true** — 15 of them printed `Loading the live board…` into a **static, edge-cached PNG**. Full detail in the honesty-layer table under "Key files"; it is now the FOURTH row of that table.
- **The three worst-covered files in the primary gate were all OG cards** (og/moment 21.4% br, og/edition 25.5%, og/pack 36.0%) because the existing sweep is deliberately breadth-only. New `__tests__/helpers/og-capture.ts` stubs `next/og` and asserts the rendered element tree, so the tests pin **honesty logic, not layout**: the closed-market FMV suppression, and the pack card's **survivor-bias guard** (a depleted TS pool prices at 40–86×, and a green **+EV** badge on an unfurl is a buy signal reaching people who never open the page). ⚠ Complementary to the sweep, not a replacement — the stub cannot see a satori render failure.
- ⚠ **My own push ERRORed the production deploy, and only a per-commit check caught it.** See the second bullet in "Vercel tool behavior" — same class as first-mint, different page, and the build failure and the honesty defect turned out to be the same bug.
- ⚠ **The SECOND blind spot, found only because I asked what a passing guard could not see.** `insights-gate-include-completeness` requires every `"use client"` `page.tsx` to be named `*Client.tsx` so the component gate catches it — a good guard, and it **walks `INSIGHTS_DIR`**. So it is silent about client pages anywhere else *by construction*, however often it runs green: **33 of them, 27,016 LOC** (`dashboard` 2,299 · `[collection]/sniper` 1,748 · `[collection]/analytics` 1,706). Held now by `client-page-gate-ratchet.test.ts`. **Third time in one session that a guard's own derivation fixed its blast radius** — the others being the anon driver-message guard and the server-page guard's hand-written 2-of-79 list.
- ⚠ **The same error-vs-absent defect turned up a FOURTH and FIFTH time, and the fourth is the worst of them.** `/moment/[id]` — the platform's most-shared URL, where every link posted into Discord or a DM lands — returned a bare `null` for both "no such moment" and "the RPC failed" and answered `notFound()`, so a timeout told a collector who had *just shared the link* that their moment does not exist, and handed a crawler a hard 404. ⚠ **It has TWO `ok`s that must never be merged**: the RPC's own `ok` means "I looked and there is none" (an ANSWER, still 404s); the envelope's means the read worked. **Both merge directions are mutation-proven**, because collapsing them either way is a defect — one makes a timeout 404, the other makes every genuinely-missing moment render "unavailable". Its metadata failure branch is `noindex, follow` so an outage cannot de-index a real moment. Fifth was `/analytics/sets/[set_id]`, above.
- ⚠ **Two OG cards could 500 into a BLANK unfurl.** `og/pack` and `og/pack/lifecycle` had no try/catch: **supabase-js RETURNS a Postgrest error but THROWS on a transport failure**, and an uncaught throw escapes `GET`. Both files' headers already promised "never 500" — it held only for the error-return path. Swept the family: `og/deal`/`og/collection` also lack a catch but are param-only and do no I/O.
- ⚠ **I inferred instead of reading, twice more, and the tests caught both.** Writing the `og/pack/lifecycle` suite I took `fmtUsd`'s threshold and the stat labels from the SIBLING `og/pack` card — but this file rounds at ≥100 (`$120`, not `$120.00`) and its ripped path labels the anchor `PAID`, not `LAST BOUGHT`/`RETAIL`. **Reading the sibling is not reading the file.** Same class as the `ufc-strike` slug earlier the same day.
- ⚠ **Four things I did NOT do, recorded so nobody re-derives them.** (0) `app/api/cron/pinnacle-listings-reconcile` is the worst-covered file in the gate at 28.6% **because `ASK_UNIFY_RETIRED = true` short-circuits it** and everything after is unreachable rollback code — testing it inflates coverage for nothing. (0b) `og/{set,team,series}` lack the closed-market FMV suppression `og/moment`/`og/edition` have, but **their own pages don't suppress either**, so aggregate FMV on a dead market is a product decision, not a test fix. (1) Extracting the 5 forward sales indexers' batch-insert retry (~529 uncovered branches, the largest remaining cluster): `sales-batch-insert-23505-guard.test.ts` is source-level **by design** and says so, extraction would break it, and **rewriting a guard to accommodate a refactor of production ingest with a permanent-data-loss failure mode is a bad trade for branch percentage**. (2) The `/analytics` overview loaders — they gate every total on `> 0`, so a failed read renders a status line, not a manufactured `$0`; I had estimated a defect there and the code was already correct.
- ⚠ **A test I deleted rather than tuned to green.** An OG "with rows" case needed a fixture per card's real row shape; one generic fixture satisfied 10 of 15. Passing would only have meant **my invented shape happened to fit** — the impossible-fixture trap. Replaced with a spelling-independent structural assertion, and ⚠ **my first structural attempt was also too narrow** (`.length === 0`; `market` uses `heads.every(h => h.median == null)` and correctly red it).
- ⚠ **A pre-existing guard had to be REPOINTED, which is a distinct and legitimate move from weakening one.** `pack-dist-contents-not-streamed.test.ts` asserted the literal `Promise<EditionTile[] | null>` — a TYPE SIGNATURE that legitimately moved to `lib/`. It now pins the **property** (the shell mapping `ok` back to the `null` the panel branches on). **A guard that pins an implementation detail reds on a correct refactor and teaches people to edit guards to make builds pass.**
- **Every new assertion mutation-proven** (removed the market-closed suppression → 1 red; disabled the survivor-bias guard → 3; reverted the two pack-dist swallows → 2; flipped one card's `fetched` → 2; dropped the Data-Saver skip and the packs-surface gate → 4). Ratchet raised 90.4/77.0/92.2/92.6 → **91.2/78.3/93.0/93.35**; see the CI-ratchet bullet for why that raise was ~0.9 rather than an increment. ⚠ Also: **UFC's registry slug is `ufc`, not `ufc-strike`** — I inferred it, and an unrecognised segment falls back to the last-visited collection, so the wrong slug silently tests Top Shot; the paired positive assertion caught it.

### August 13, 2026 (Claude Code, docs, SECOND pass of the day — "analyze repo → update CLAUDE.md to current state") — docs-only maintenance on tip `48369a45`: re-verified all 13 canonical-reference facts (**every one CURRENT — the earlier pass today had just corrected them**), so the work was entirely in the four CODE commits that landed since `df0779fb`. Three carried durable lessons documented nowhere canonical, and one of them **invalidated a claim in the canonical sections rather than merely extending it**. No product/DB/prod change; direct to `main`. Ledger entry 2026-08-13.

- **The headline: a canonical bullet was asserting a scope that was wrong, and the wrongness is the transferable part.** `lib/api-error.ts` was documented as "the REQUIRED failure path for every route an **ANONYMOUS** visitor can reach". `7e6a4a2c` swept the routes `proxy.ts` **gates** and found **120 more leaking sites across 64 files** — the collector's own `/profile`, `/wallet`, `/alerts`, `/watchlist`, `/portfolio`. ⚠ **The anon guard's greatest strength is exactly why it could never have found them:** it keeps no route list *because* it executes `isPublicPath`, which is what makes it impossible to miss an anon route — and that same derivation **fixes its scope to that wall**, putting everything behind sign-in outside it by construction. The bullet now leads with the correction rather than quietly swapping the word, because a future reader who trusts a scope claim will not re-derive it. Fifth mis-filing of this one defect.
- **Recorded because the remedy nearly caused its own harm:** blanket-applying `safeApiError` (which only knows Postgres) to four routes that throw their own "Could not resolve `<username>`" replaced an actionable **400** with "Something went wrong." at a **500** — deleting the one thing the user needed. Hence `isUnresolvedIdentifierError` / `unresolvedIdentifierResponse`. Also `aafd014d`: `/api/pack-listings` was publishing **Dapper Studio's** wording — the leaked text is not always ours or Postgres's — and two codemod regressions worth knowing (a 502 flattened to 500, losing the "did WE break?" signal; `rows: []` dropped beside a 500, which is correct but must be deliberate).
- **Added the fourth commit's lesson to the conflation family** (`48369a45`): sampling the insider-detector telemetry needed a **new field**, not a reused `null`, because `candidates_evaluated: null` already meant "the RPC errored" — overloading it makes a broken instrument indistinguishable from a deliberate skip. Its operational half is worth the space too: that pure telemetry was **~27 GB/day, ~3% of all disk reads** on the IO-throttled tier, with no production consumer.
- **No correction due on `788a5605`** (the sentinel `sold_at` bound) — the concurrent session's own `bee9e7c3` had already landed it in the `sales` bullet accurately, including the part that matters most: the check was **not** deleted as redundant, and the two probes answer different questions.
- **No tail roll due.** Zone read before converting (`date '+%Z'` → **PDT**; 12:59 PT / 19:59Z) — same PT calendar day as the earlier pass, so Recent sessions still spans Aug 13 · Aug 12 · Aug 11 and the archive pointer stays "August 10 → August 1". Two docs passes on one day is not duplication: the first was **facts drifting under stable claims**, this one is **a claim invalidated by new work**.

### August 13, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only maintenance on tip `c2d72683`: re-verified every checkable canonical-reference fact against the repo and found **five had drifted since the last docs pass**, all in the same direction — the shipping sessions moved a number and the canonical section still carried the old one. Corrected in place, plus two genuine ADDITIONS for state that had landed and was documented nowhere canonical. No product/DB/prod change; direct to `main`. Ledger entry 2026-08-13.

- **Five drifted figures corrected.** Vercel crons **37 → 38** (`7a8d7f2a` added `/api/cron/topshot-catalog-backfill` `12 2 * * *`); DB-invariant pins **122 → 125** (over 124 distinct fns); primary coverage thresholds **89.3/75.1/91.5/91.6 → 90.4/77.0/92.2/92.6**; component thresholds **79.0/67.0/78.8/83.2 → 88.5/79.4/88.2/91.6**, in both places they appear. ⚠ **The pin count needs the PARSER, not a grep** — `grep -cE '\bfn:'` reads **126** because it catches the `it.each(PINS)("$fn: …")` title too, and `allday_sales_cross_source_dedup` legitimately carries two pins, so *pins ≠ distinct fns ≠ grep*. The prior note's "raw grep = 123, one two-fn pin counted twice" was a correct arithmetic for a different tree and does not survive re-derivation; the durable instruction is to count the array.
- **⚠ A stale line here was actively misdirecting, which is the failure mode this pass exists to catch.** The `editions.description` bullet — a CANONICAL section, not a dated one — still said the catalog walker is "**on no cron** … coverage only grows when someone runs it by hand." It has been on a Vercel cron since `7a8d7f2a`. A future session reading that would have concluded description coverage was frozen and either hand-run a redundant sweep or built the wrapper that already exists. Corrected there, with a `⚠ SUPERSEDED` pointer left on the dated Aug-12/13 entry that originated the claim (the diagnosis in it was right; only its "until then" went stale).
- **Recorded the wrapper's reasoning, not just its existence,** because the trap it dodges has now bitten this repo three times: `verifyAdminRequest` accepts ONLY `RPC_ADMIN_TOKEN` while Vercel cron injects `CRON_SECRET`, so a direct `vercel.json` line 401s every tick — and **a 401 writes no `pipeline_runs` row, so it is indistinguishable from "never scheduled"** (same shape as the 08-11 gate-key outage; same trap that killed the old `pinnacle-sync` entry). Also recorded why it calls the handler **in-process** rather than self-fetching (absolute-base-URL fragility, a second lambda, fails closed behind preview SSO) and why it logs nothing itself (the walker already writes the row; two would double-count).
- **Addition 1 — `wmc.fmv_confidence`** (`6bd15560`), undocumented in the wmc schema section. wmc is the portfolio store and ~34 DB functions sum `wmc.fmv_usd`, but the table carried the VALUE with no LABEL, so confidence was structurally unavailable at the point a portfolio total is computed and `/share/[wallet]` emitted FMV with no confidence field at all. Captured the two non-obvious decisions a future editor would otherwise re-litigate: **deliberately unindexed** (the cohort index already INCLUDEs `fmv_usd`; HOT ratio 1.8%), and the force-path change-detection **widened to fire on a differing label** — without which already-correct rows keep a NULL label forever.
- **Addition 2 — the FMV-propagation blind spot** (`cd1018f0`): two RPCs failed `57014` on every 5-minute tick for 10+ hours while `pipeline_runs` showed **988 runs and ZERO failures**, because the per-collection rows come from the wrapper (which succeeds), the route returns **202 by design**, and the RPCs' only failure signal was `console.log`. It ran long enough to reach Top Shot 7% exact-match / AllDay p95 14.2× against `fmv_current`. Generalized into the sections rather than left in a commit message: **a 202 route and a succeeding wrapper do not make the work inside observable.**
- **Also recorded, in the `editions.description` bullet: the catalog walker's "empty vs refused" fix** (`9909f27d`) — `fetchEditionsPage` returned `… | null`, collapsing "this set has no editions" with "we could not ask", which is *why* the 422 regression logged `ok: true`. Now a discriminated `PageFetch` carrying the upstream fault (whose body NAMES the offending field), a `sets_faulted` counter, and — deliberately — `ok` does NOT redden on a single fault, because a chronically-red pipeline trains the operator to skim past it, the exact cost already paid for with `ufc_fmv_stale_hours`.
- **No tail roll due.** Zone read before converting (`date '+%Z'` → **PDT**; `date -u` 16:21Z, PT 09:21) — this sandbox is PDT, so the "subtract 7h" reflex would land a day early here. Recent sessions still spans exactly Aug 13 · Aug 12 · Aug 11 and the archive pointer stays "August 10 → August 1".
- **Relationship to the entries below:** those are shipping logs; this is the docs-maintenance log. The distinction is load-bearing — **dated entries are frozen history and roll off to `docs/sessions/` within ~3 days**, so a fact left only in a session log stops being found by the canonical sections a future session actually reads. That is exactly how all five figures above drifted. Two open items in the dated Aug-13 overnight-pass entry have since been fixed by other sessions and are noted here rather than by editing that frozen entry: `compute-pinnacle-pack-ev`'s deterministic `ON CONFLICT` self-collision (`bd53bb3a`) and the `allday-pack-opens` unreachable-tip early return that sat outside the `try` and so wrote no row (`033c0c15`).

### August 13, 2026 (Claude Code, interactive — continuation of the Aug-12 honesty thread: "keep going" ×3 → wrap-up) — **the turn's most useful finds were a defect I shipped 40 minutes earlier and two errors in my own ledger.** 3 pushes to `main` (`7f48e928` → `b32c7363` → `6db740b3`), both gates green on each, all deployed READY. No DB, migration, cron, auth/`proxy.ts`, hot-wallet, or FMV/pricing-MATH change.

- **⚠ DURABLE — a disk-IO saturation spell can FAIL THE WHOLE PRODUCTION BUILD, and an ERRORed deploy is easy to miss.** Found by sweeping deploys PER COMMIT: `dpl_FwbnxURHqSbbYRqCQus44Cxxgyhc` (`6960281c`) was state **ERROR** — `/insights/first-mint` exceeded Next's 60s per-page export budget ×3 → `npm run build` exited 1. The next push superseded it and went READY, so the tip looked healthy. **Check the deploy state of the commit you shipped, not the state of the tip.** Fixed by `BOARD_LIVE_TIMEOUT_MS` (8s) — full detail in the Vercel bullet under "Vercel tool behavior".
- **⚠ DURABLE — the stale rung existed and could not be reached.** first-mint's snapshot was ~85 min old at build time, present and usable. `readBoardOrLive` only fell back when the live query **ERRORED**, and a query that is merely SLOW errors nowhere. **Slow and broken are equally unservable; only broken was modelled.** That exact shape then turned up twice more in the same subsystem (panini's `partial` flag covering only the error exit, and `summarizeDegraded` reading `partial` only inside its `!ok` branch), which is why the third fix went into the PRIMITIVE rather than a third call site.
- **⚠ I SHIPPED A DEFECT AND CAUGHT IT BY ASKING A DIFFERENT QUESTION.** `f02362c7` made panini's page-cap exit return `partial: true` — correct — but the status it passed to `summarizeDegraded` was `{ ok: rows.ok, partial }`, and a capped read is `ok: true`, which that function skipped. Result: an EMPTY board with NO notice, strictly worse than the silent truncation it replaced. **The root cause was the test:** it asserted `res.ok` and `res.error` — what the FUNCTION RETURNS — and never `payload.degraded`, what the READER SEES.
- **⚠ DURABLE — a mutation test caught a test of mine that asserted nothing.** The obvious "does not time out a fast query" case passes **even with the budget set to 0 ms**, because `Promise.race` settles an immediately-resolving microtask before the `setTimeout` macrotask runs. **When you test a timeout, the happy-path case must take REAL TIME or the assertion is vacuous.**
- **The board-warm telemetry shipped 08-12 paid off within hours.** The failures now name their views: `deals` → `cross_collection_deals_board` timing out, `first-mint` → BOTH its views. With `service_role`'s `statement_timeout=30s` and `topshot_first_mint_trophy_stats` measured at **2,047 ms** on a quiet instance (its covering index alive and used, 591 scans), that is a **~15× throttling multiplier, not a plan defect** — so the lever is the shared materialize-latest-FMV item, NOT another index. Filed: [inbox 2026-08-12T2330Z](docs/overnight/inbox/2026-08-12T2330Z-board-view-timeouts-now-named.md).
- **Two tidy stories died on measurement, both recorded so nobody re-derives them.** (1) The warm cron's ~32s-per-5-minutes duty cycle looked like reclaimable waste — but the pages are ISR `revalidate=300` with `s-maxage=300`, so the cadence already matches what a consumer can observe. (2) Sweeping the estate for more blind instruments found exactly one candidate, `promote_unmapped_sales`, which on inspection is a *working* silent-no-op detector whose condition IS its reason.
- **⚠ DURABLE — TWO ledger errors of my own, both corrected in `6db740b3`.** (a) The session resumed across a day boundary and I carried the previous turn's date forward: two headings said 08-12 for commits stamped **2026-08-13 07:30 / 07:48 PDT**. A concurrent session hit the identical class the same day. **Read the zone, never assume it** — `date '+%Z'`; this sandbox is **PDT**, so the reflexive "subtract 7h from `date -u`" lands a day EARLY. (b) **`cont. N` in the ledger is NOT session-unique** — two sessions increment the same counter and 08-12 alone carries duplicate 16/19/20/21, so a `cont. N` cross-reference is ambiguous by construction. **Cite the sha.** I corrected only my own headings and references; renumbering another session's entries would be the whole-file rewrite the ledger's own rules forbid.
- **⚠ Concurrent-session collision stayed cheap by staying in different files.** The other session worked `components/**` (Candy client truncation disclosure, CollectionMomentTable P&L parity, the anon-guard's missing fifth spelling) while this one worked `lib/insights/**` — and the two halves of the truncation story COMPOSED: their client-side "Showing N of M" is only honest while the fetch layer isn't silently capping, which is what the `fetchView` change guarantees. One BLOCKED-deploy backlog (four commits stranded ~40 min after ~10 deploys in 30 min from two sessions) cleared on its own.

### August 13, 2026 (nightly autonomous pass, OFF-HOURS 07:49 PT — ran late) — MONITOR-MODE + NO-PUSH (shell down, ~6th night, `/sessions` no-space). Shipped 0 / reverted 0 / repaired 0. Two NEW findings, both already caught by the live instruments, both QUEUED (non-shippable from a monitor/NO-PUSH pass). Full log: `docs/handoff-2026-08-13-overnight-pass.md`.

- **`pg_net_http_403` CRITICAL 24/2h — RECURRENCE.** The gate-key half-rotation outage CC fixed + verified `[]` on 08-12 03:38Z is firing again. The 08-11 real-loss ingest jobs (allday-pack-opens-forward, pinnacle-mints fwd/backfill, topshot-pack-opens-history-backfill) are confirmed HEALTHY now (writing `pipeline_runs`), so a *different* subset of the 14 gate-keyed jobs (15/16/20/22/25/26/27/29/42/44/55/56/83/84) re-403'd — the "partial rotation reproduces the outage" the `2026-08-11T0300Z` runbook warned of. Can't attribute the exact jobs (`net._http_response` won't join to URL; dispatch shows `succeeded`). Operator: complete the rotation as ONE window (8 `*_GATE_KEY` secrets + deploy env-var fns + repoint cron `?key=`).
- **`compute-pinnacle-pack-ev` HIGH ~100% fail since 08-12 — deterministic ON CONFLICT** batch self-collision (`upsert pack_distributions: ON CONFLICT DO UPDATE cannot affect row a second time`). Clean 08-04→08-10, onset 08-11 (2/4), 08-12 4/4, 08-13 2/2; fails identically in quiet + saturated windows → not the disk-IO class. Bounded to Pinnacle pack-EV surface staleness. Already daytime-monitor `2026-08-13T1453Z` Candidate 1. Fix = dedup batch by conflict key before upsert (keep last) OR per-row upsert w/ 21000/23505 fallback; verify byte-identical EV. Pack-EV code + edge deploy → CC/operator.
- **Health green-with-known-saturation-noise:** security invariants `[]`; precompute fresh 7.95h (breach 13 — so trust breaches are real); Sentry 0 new/48h; DB 12,718 MB. Trust breaches 4, all known-class: panini_fmv_stale_hours 37.4 / panini dry 15 (home-box runner outage, operator); public_board_slow_count 3 (was 16 — oscillating down, snapshots fresh, no user impact); unmapped_resolution_backlog_max 225 (AllDay floor, net-draining). **Post-ship watch (08-11 pass):** jobid 259 reconcile now intermittent — quiet ticks succeed (08-13 82.3s, 08-11 110.8s), saturated ticks timeout (08-12, 08-10); display-only self-healing, CARRY. Precompute jobid 287/D34 healthy.
- **Inbox 25 files un-archived** (archival is a git mv; no git). **ESCALATION:** `/sessions` no-space has blocked the overnight push ~6 nights and the 08-12 pass silently did not write — operator must delete old Cowork sessions to free `/sessions` (`docs/handoff-2026-08-09-cowork-shell-recovery.md`).

### August 12–13, 2026 (Claude Code, interactive — "analyze test coverage → propose" → "do everything" → "keep going" ×8) — **a long coverage program that kept turning into a DEFECT program: every real bug below was found by writing the test for code that already looked fine.** ~14 pushes to `main`, all CI-green; both gates green on every push; each change ledgered with a revert path. No DB, migration, cron, auth/`proxy.ts`, hot-wallet, or FMV/pricing-MATH change.

- **Gates moved, but that was the by-product.** Primary 89.3→91.2 st / 75.1→77.9 br (1,150 files / 10,966 tests); component 79.0→~90 st / 67.0→81.3 br (185 files / 1,560 tests). Closed the `.tsx` blind spot (44 `route.tsx` + 2 libs, ~8k LOC measured by NEITHER gate), the `app/**` vs `app/api/**` sitemap gap, and a coverage glob that was **valid and matched nothing** (`app/(collections)/**` — route-group parens are picomatch extglob syntax), now rot-guarded.
- **User-facing defects found and shipped** (each mutation-proven, each with a positive mirror so the fix cannot be "always say couldn't load"): the mobile moment table had **no empty state at all**; a public profile could report **"↑ 50000.0% / 30D"** from a `|| 1` divide-by-zero guard; the same moment showed **a different P&L on a phone than a desktop**; the Candy board **silently sliced a ranking**; `TeamChecklist` claimed an empty catalogue **and disabled its own retry**; and ~10 client components reported a failed fetch as a market finding ("No recent whale trades.", "No Flowty marketplace activity in this window.", and — sharpest — *"try a longer time range or lower min FMV floor"*, advice to fix a filter that was never the problem). All four durable lessons are recorded in **Key files** above.
- **⚠ Tests that ASSERTED the defect were a recurring find, not a one-off.** ~35 existing tests pinned a leak or a false claim across the session, two titled *"500s **with** the message"*, and one ran on a `costBasisLabel` that **does not exist in the vocabulary** (`"Pack pull"`; the map emits `"Pack"`), so it exercised a path real data never reaches — the D9 impossible-fixture shape, hit independently twice. **A green test proves nothing if its fixture is unreachable; check a literal against the map that produces it.**
- **⚠ My own errors, all caught before CI except one.** I pushed once with a failing `tsc` because commit/push were chained without `&&` — reddened `main` ~4 min. A mechanical test-repair regex hit **351 files instead of 18** (reverted, redone by exact `(file, line, string)` targeting). I twice INFERRED a shape instead of reading it (a sort key, a prop envelope). And `echo "TSC=$?"` after a pipe into `head` reports HEAD's exit — it printed a green `0` over a real `TS2698`; only `TSC=$?; [ "$TSC" -ne 0 ] && exit 1` works.
- **⚠ Concurrent-session collision is now a real cost on this repo.** A parallel session shipped the anon-API driver-message sweep while I had an equivalent one ready; **theirs was strictly better** (43 route sites *plus* 33 concierge tool-result sites no HTTP-body grep can see, a guard that EXECUTES `isPublicPath`, `Cache-Control: no-store`), so I deleted mine unpushed — the same call as dropping my `edge-fn-no-hardcoded-gate-keys` fix earlier. What survived was the cross-check: their guard had **four** leak patterns where the older one had five, and the missing fifth was still live on 12 sites. Later rounds deliberately stayed in the component layer while they worked `lib/insights`/API/DB, and the two halves of the truncation fix (my client `DataTable`, their fetch layer) **composed** rather than collided. Cost across the session: one full duplication and ~6 rebase conflicts.
- **Also:** a GitHub **500** on push (not a fast-forward conflict) — succeeded on retry with backoff. Five ledger headings mis-stamped a day early and corrected; see the clock note above, which this session had to amend.

### August 12, 2026 (Claude Code, interactive — "implement a TODO" → "fix the error.message leak in those six routes too, along with anything else unresolved" → "keep going" ×N → "do what you think is best") — **the driver-message leak has now been filed FIVE times, each at the size of wherever the last person happened to be looking; this pass measured the boundary instead of guessing it.** Multiple pushes to `main`; `tsc` clean, both coverage gates green, each wave ledgered. No DB, migration, cron, auth/`proxy.ts`, hot-wallet, or FMV/pricing-MATH change.

- **⚠ DURABLE — the scope history IS the finding, and it is the reason the remedy is not another list.** D3 filed it as `/api/sets`. 2026-08-11 filed it as six `/api/public/insights` routes, then found all **29**. This pass swept the routes **`proxy.ts` actually lets anonymous visitors reach** and found **43 more sites across 33 files** — plus **33 concierge tool-result sites** invisible to any HTTP-body grep. Five filings, five under-counts, every one honest at the time it was written. A hand-maintained route list cannot catch the route nobody thought to add.
- **The guard EXECUTES the security wall rather than describing it.** `__tests__/anon-api-no-driver-message-leak-guard.test.ts` imports the real `isPublicPath` from `proxy.ts` and runs it over every route file on disk, so widening the public surface widens the guard **in the same commit**. Mutation-proven (re-introduced the leak in `/api/market`; it reds). It excludes `/api/admin/**` + `/api/cron/**` — proxy-bypassed *precisely so* their own bearer-secret check can BE the gate — and then **asserts each excluded route really carries that gate**, so the exclusion can never quietly come to cover an ungated admin route. That assertion immediately flagged 5 routes my first detector called ungated; all 5 delegate to shared helpers (`verifyAdminRequest`, `runStudioHistoryDrain`), each verified to 401 before the predicate was widened.
- **One primitive, not a fourth peer helper.** `apiErrorResponse(err, tag, fallback?)` moved into `lib/api-error.ts` and `boardUnavailable` is now a thin alias of it — so the CLAUDE.md three-layer map (route / server page / client dashboard) still has exactly three entries. Behaviour-preserving for the boards (its 8 tests pass untouched).
- **⚠ The concierge surface is structurally invisible to an HTTP-body sweep.** 33 sites in `support-chat/route.ts` returned `JSON.stringify({ status: "error", message: err.message })` as **tool results** — the model reads them and can quote them, so Postgres's own wording reaches a visitor *through the assistant* with no response body ever carrying it. Now `safeApiError(err, "<tool> failed").error`, which also gives the model actionable copy ("the database is under heavy load") instead of driver text it has to paraphrase.
- **⚠ 29 existing tests asserted the LEAK** — `expect(body.error).toBe("db down")` — across 21 files, plus 2 more the route→test name heuristic missed (`api-moment-id`, `api-public-media-and-detail-routes`; one was even titled *"500s (with the RPC message)"*). **A test that pins a defect makes the defect look like the contract**, and a rename does not remove that: each was rewritten to `not.toContain(<driver text>)`, which asserts the property that actually matters.
- **The status half matters as much as the body.** These all returned a bare **500**; a statement timeout is now **503 + `Retry-After`**, keeping transient capacity out of the hard-5xx budget that pages on genuine breakage, with `Cache-Control: no-store` so a momentary blip is not edge-cached into a sustained outage alongside these routes' `s-maxage` success responses.
- **Two routes kept their shapes deliberately**, spreading the classified error in rather than being converted: `/api/ready` (`status: "error"` is its contract with the monitoring consumers named in its own header) and `/api/subscribe` (`success: false` is its contract with the two email-capture components). `/api/moment/[id]` went the other way — its `{ ok: false }` **is** the RPC's "no such moment" verdict, so reusing it for a database failure made an outage indistinguishable from a genuine miss.
- **Also fixed at the client layer:** `TrophyPickerModal` reported ANY non-ok moment lookup as *"Couldn't find a moment with that ID"*, so a 503 told the user their moment does not exist — a claim about the catalogue manufactured from a database outage. Now branches on `res.status >= 500`, with a companion test that fails if the branch is removed.
- **Tail roll:** this Aug-12 entry pushed Recent sessions to 4 calendar days, so the **18 Aug-9 entries** moved to [docs/sessions/2026-08.md](docs/sessions/2026-08.md) verbatim, newest-first (archive pointer now reads "August 9 → August 1"). Recent sessions now holds Aug 12 · Aug 11 · Aug 10. Date check: `date -u` 15:07Z − 7h PDT = **08:07 PDT Aug 12**, a genuine Aug-12 PT day.

### August 12–13, 2026 (Claude Code, interactive, cont. — "Do everything you can" → descriptor discovery → "keep going") — **NARRATIVE SEARCH IS LIVE: "Damian Lillard game winner" now returns the For the Win moments.** The thread's goal, reached — and the two things that nearly stopped it were both MY OWN diagnostics lying. Multiple pushes to `main`, 1 column + 1 view + 2 CONCURRENTLY indexes, each verified live and ledgered.

- **⚠ DURABLE — Top Shot `description` is on `Play`, NOT `PlayStats`, and getting that wrong FAILS SILENTLY WITH `ok: true`.** An invalid field invalidates the WHOLE GraphQL query; Top Shot answers **HTTP 422**; `fetchPage` returns null; `backfill-topshot-catalog` walks every set and upserts **ZERO** editions while logging success. **The only tell was `gql_calls` EXACTLY equalling `sets_processed`** (257 == 257 — one call per set means every call died on its first page; a healthy walk needs multiple pages, and the fixed run showed 327 > 258). Two live runs wrote nothing before this was caught. Blast radius was limited only because that route is on **no cron**.
- **⚠ DURABLE — a diagnostic must have a CONTROL, or a total failure is indistinguishable from a total absence.** The descriptor probe's v1 reported `exists:false` for EVERY field on both leagues — including `classification` and `dateOfMoment`, which our live ingest queries daily. Two transport failures (wrong AllDay endpoint → 404; `setID: null` → Top Shot 422) had been rendered as schema facts. v2 made `status` `yes|no|unknown` (a transport failure is NEVER `no`), probed CONTROL fields we already use in production, and reported the arm INCONCLUSIVE with `absent: []` when they fail. **v2 then immediately paid for itself**: it refused to answer and its captured error BODIES named both causes exactly. v3 fixed the causes; **v4 added `on: "PlayStats"|"Play"` to every finding** because the flat `found` list was what let the `Play`-vs-`PlayStats` mix-up happen at all.
- **⚠ DURABLE — the live Top Shot `searchEditions` contract** (copy it from `backfill-topshot-catalog`, do not improvise): `filters` is **REQUIRED** (`bySetIDs`, **UUID-format set ids only**) plus `searchInput.pagination`, and the response path is the **double-`data` inline-fragment** `searchSummary.data.data[]` (`... on Editions { data { ... on Edition } }`). The flat `searchEditions { data { … } }` shape does not exist. Also: `headline` exists but is just the player name — **NOT** the moment page's editorial title ("…stuffs stat sheet in historic 5x5"), whose source is still unlocated — and sibling fields return **SENTINELS not nulls** (`draftYear: 0`, `draftRound: "N/A"`, `quarter: "NA"`), so `lib/topshot/play-description.ts` exports `isSentinel()` to encode that.
- **⚠ DURABLE — the sandbox cannot reach the collectible upstreams AT ALL** (measured: `curl` to both returns `status=000`) and holds no secrets, so GraphQL discovery MUST run as a prod route. That is why `/api/admin/discover-moment-descriptors` exists.
- **Shipped:** `editions.description` + capture in the Top Shot catalog walker (normalized via `lib/topshot/play-description.ts`: null never empty-string, whitespace collapsed for trigram consistency, sentinel-only → null); `idx_editions_description_trgm`; `rpc_search_catalog` searching prose with a `via_prose` boost (**33ms → 23ms**, faster because the extra selective index narrows earlier); the `edition_description_coverage` view; `/api/search` `meta.coverage`/`meta.note` read LIVE; `GlobalSearch` rendering it. Coverage at ship: **5,885 / 13,197 canonical Top Shot (44.6%)**, 0 elsewhere; **76 Top Shot moments describe a game-winner**.
- **⚠ Two tests asserted copy that had become FALSE** ("Moment descriptions aren't in the catalog") and were rewritten, not deleted — a test pinning outdated copy makes the copy look like the contract. Also fixed `meta` missing from `GlobalSearch`'s `useMemo` deps, which would have stopped the panel re-rendering when coverage arrived.
- **⚠ `/api/admin/backfill-topshot-catalog` is on NO cron and a naive Vercel cron would 401 every tick** — `verifyAdminRequest` accepts ONLY `RPC_ADMIN_TOKEN`, while Vercel cron sends `CRON_SECRET`. Same trap that killed the `pinnacle-sync` entry. Scheduling it needs a `/api/cron/*` wrapper that checks `CRON_SECRET`, not a `vercel.json` line. Until then description coverage only grows when someone runs it by hand. ⚠ **SUPERSEDED 2026-08-13 (`7a8d7f2a`): the wrapper was built and it IS on a cron now** — `/api/cron/topshot-catalog-backfill` at `12 2 * * *`. The diagnosis above was right; only the "until then" is stale. Current detail lives in the `editions.description` bullet under the editions-table section.
---

### August 11, 2026 (Claude Code, interactive — "implement a TODO" → "fix the error.message leak in those six routes too, along with anything else unresolved") — **the in-code TODO backlog is drained, so the real find was that TWO honesty defects on the public `/insights` surface were each ~5x wider than filed.** 2 pushes to `main`; both coverage gates green; each ledgered. No DB, migration, cron, auth/`proxy.ts`, hot-wallet, or FMV/pricing-MATH change.

- **In-code TODOs genuinely DRAINED** (exhaustive sweep, all file types): every literal marker is a RESOLVED note, a `TODO_`-prefixed operator-gated env sentinel (Candy secrets), or a historical reference. Did NOT manufacture work from them — pivoted to natural-language equivalents, which is where both real items were.
- **Ship 1 — honest degradation on the 6 remaining single-view boards** (squeeze · trophies · offer-spread · pinnacle-scarcity · allday-scarcity · set-squeeze). `lib/insights/board-status.ts` was built 2026-08-09 for one named failure and wired into only **2** of 8 boards; the other six still had the exact `if (error) return []` the module's own header names, so a 57014 rendered as an EMPTY board at HTTP 200. ⚠ **Invisible to both existing instruments**: it is a 200 (5xx metrics blind) and the board-liveness probe times `SELECT count(*)`, which the planner prunes.
- **Ship 2 — the D3 driver-message leak was SURFACE-WIDE: all 29 `app/api/public/insights/**` routes, in THREE spellings** (see the `lib/insights/board-error.ts` bullet under "Key files"). The filed follow-up had scoped it to six. `pack-reality` additionally published a per-source `errors[]` of driver text into `meta.errors` on its **200** — its only consumer reads `e.source` and never the message, so the leak bought nothing.
- **⚠ DURABLE — a pre-authorized deletion whose PREMISE WAS FALSE.** `app/api/topshot-listing-cache/route.ts` carried a 2026-06-07 note: the wallet-verification matcher "is effectively a no-op — cached_listings is frozen (Flowty shut 2026-05-13)… safe to delete." Measured live: `cached_listings` holds **309 rows, newest `cached_at` minutes old** — the Flowty *frontend* shut, `api2.flowty.io` is alive, and **this very route writes that table**. Deleting on that note would have dropped a working backstop. It has never fired for a *different* reason (all 9 challenges resolved `expired`/`gql_on_demand` — the on-demand GQL route gets there first). Comment corrected to KEEP. **Re-measure a "this is dead" annotation before acting on it; the annotation is not the evidence.**
- **⚠ DURABLE — `workers/rpc-mcp-proxy` was telling every LLM agent that a WORKING tool is broken.** `get_badge_data`'s description claimed the backing RPC "currently raises an `unaccent` error… returns badges:{}". Verified live: `get_edition_badges_unified` carries `search_path=public, extensions, pg_temp` and a real call returns `gaps: []` with actual badges. Tool descriptions are agent-facing config — stale text there actively routes agents away from working tools. ⚠ Committed but **INERT until an operator `wrangler deploy`**.
- **Smaller corrections, each verified not assumed:** `backfill-badges-from-sets` reported `matchedMissing: wanted.size` (keys *sought*) while the real count was dropped with `void matched`, so a dry run could never show a set matched **nothing**; `/api/fmv` carried a provably-unreachable `if (fallbackTier !== "rpc_fmv")` in both handlers (API contract left untouched, 87 tests green); `RTRClient`'s "stub until the Odds API is wired" was stale; one genuinely-unused import kept alive by `void`.
- **⚠ Method notes.** `main` was **CI-red on arrival** (`edge-fn-no-hardcoded-gate-keys` failing on clean HEAD — proven by stashing my diff); I fixed it by widening the regex, then a concurrent session landed a **strictly better behavioural fix** (it EXECUTES the real `gateKeyOk` under set/unset secrets and pins `executed === 8`) and I **dropped mine and took theirs**. Every new test here is **mutation-proven** — reverted the fix and confirmed the test reds — because the repo's own lesson is that a test which cannot fail asserts nothing. ⚠ **`io.open(p,"w")` in Python truncates the file BEFORE the argument expression is evaluated**, so a `write(transform(...))` whose transform raises leaves an EMPTY file (it silently emptied a route; restored from git). Compute every transform first, then write. ⚠ Run the two coverage gates into **separate `--coverage.reportsDirectory` dirs** — a stray concurrent run collided on `coverage/` and failed with a bogus `ENOENT coverage/.tmp/*.json` that reads like a real break.


### August 11, 2026 (Claude Code, interactive — assessed the filed candy-treasury finding → "work through the rest of the tree, leave it clean") — **found and fixed a 24h silent ingest outage that five consecutive monitor runs had misdiagnosed.** 7 pg_cron jobs repointed + 2 prod migrations, each verified live and ledgered. One regression introduced and closed in the same session. No hot-wallet, auth/`proxy.ts`, FMV/pricing-MATH, or destructive-SQL change.

- **⚠ DURABLE — `cron.job_run_details` "succeeded" on a `net.http_get` means DISPATCHED, not executed; and a 403'd edge fn writes NO `pipeline_runs` row, making it INDISTINGUISHABLE from a completed no-op walk.** The 2026-08-09 D2 change de-hardcoded 8 edge-fn gate keys to env vars and warned in its own ledger entry: *"Do NOT clear it by redeploying without first setting the secrets, or 9 pg_cron jobs (15, 16, 20, 42, 44, 55, 56, 83, 84) start 403ing."* On 08-11 ~03:09–03:22Z the pg_cron `?key=` values were rotated but the fns were never redeployed and the secrets never set — **half a rotation**. `ingest-allday-pack-opens`, `ingest-pinnacle-mints` and `ingest-topshot-pack-opens-history` all 403'd for 24h. **Real loss:** AllDay pack opens (the forward walk wrote 8 rows on its first repaired run) and Pinnacle mints (~17 rows/tick forward, 8–35 per 2 min backfill).
- **⚠ DURABLE — the tell was a dispatch-layer success plus a silent pipeline; that is an AUTH hypothesis BEFORE it is an upstream hypothesis.** Settled with two `mode=probe` (read-only) calls to the SAME live function: the cron's rotated hex → **403**, the deployed gate literal → **200 OK**, scanning to tip `161083198` and resolving a live pack open. **Flow/spork reachability was perfect the whole time** — the spork-proxy / mainnet24 theory in the `0610Z`/`1512Z`/`1809Z`/`2112Z`/`0309Z` monitor files is wrong; do not re-open that lane.
- **⚠ DURABLE — a real alarm was suppressed by a stale "known false positive" annotation.** `detect_stalled_pipelines()` DID fire and was dismissed five times as *"documented no-op-walk false positive."* That annotation is real but belongs to the **TopShot** walk (cursor 61,808,846, genuinely below the 65,264,619 spork floor → `done:true`); it was **misapplied to AllDay**, whose cursor at 85,940,403 sits ~20.7M blocks ABOVE the floor, mid-walk. The 0309Z run even cited `cron.job_run_details` as its verification — the one instrument structurally blind to this. **A benign annotation on a shared symptom is a diagnostic hazard: scope it to the pipeline it was measured on.**
- **Shipped the missing instrument.** Nothing in the estate read `net._http_response` — 100 of 194 responses were 403 and no check looked. New `check_edge_fn_http_failures()` flags 4xx on pg_net-dispatched edge fns, wired into `get_pipeline_alerts()` (the live Telegram/email path) by **RENAME + thin wrapper** (`get_pipeline_alerts_core`) so its load-bearing 11,383-char body was never retyped. **Scoped to 4xx only** — 5xx and pg_net's 55s timeouts are routine noise and would have made it permanently red. It fired on the live outage (109 × 403, `critical`) and reads `[]` now that all 7 jobs are fixed. ⚠ `net._http_response` retains only ~1.6h and `net.http_request_queue` is **pruned on completion**, so a response cannot be joined back to its URL — status code + body are the only attribution available.
- **⚠ DURABLE — `REVOKE … FROM PUBLIC` is NOT enough, and it bit me.** `CREATE OR REPLACE` on the name freed by the RENAME picked up Supabase's default `anon`/`authenticated` EXECUTE grants, widening `get_pipeline_alerts()` from its original postgres+service_role-only ACL. Caught on the same-session verification pass and closed in a second migration with `FROM PUBLIC, anon, authenticated`. **Always re-check `has_function_privilege('anon', …)` after a RENAME + recreate — the new object does not inherit the old ACL.**
- **Candy treasury finding closed, with its key number corrected.** Both argmaxes re-verified in agreement (`BhA2Bfd8…APe2`; wmc 2,610 vs 1,367 = 1.9×, packs 2,243 vs 21 = 107×). ⚠ **The filed "76.8 ms" is wrong — `EXPLAIN (ANALYZE, BUFFERS)` cold is 2,854 ms**, which inverts the doc's own conclusion and rules out a live trust arm. `check_candy_treasury_divergence()` is therefore **standalone**, deliberately NOT in `get_pipeline_alerts()` (45s budget, already taken down once by one heavy arm). **Board stays at 38 arms.**
- **Also corrected:** the `1512Z` claim that the D34 precompute needs "per-leg budgets below the outer 600s" — **every per-leg budget is already below 600s** (60/90/120/180/180/240/300/480). The `CALL` is one statement and `statement_timeout` does not re-arm per `COMMIT`, so `cron_heavy`'s role-level 600s bounds the TOTAL; the split worked (committed legs persisted, only the two expensive tail legs were lost — which IS the separately-reported `public_board_slow_count` freeze). Not a live defect (18:58Z 494.7s, 00:58Z 73.6s both succeeded); nothing changed.
- **⚠ STILL OWED (operator).** The 7 jobs now use the PRE-rotation literals, which are **public** (in this public repo's git history — exactly why D2 rotated them). No NEW exposure (the deployed fns already accept them), but the rotation must be completed as ONE window: set the 8 `*_GATE_KEY` secrets → deploy the repo (env-var) fns → repoint the pg_cron `?key=` together. Any subset reproduces this outage. **Not fixed, different cause:** jobid 44 `rpc-compute-golazos-pack-ev` has a literal key yet `compute-laliga-pack-ev` is silent ~22h.


### August 11, 2026 (nightly autonomous pass, GENUINE OVERNIGHT ~01:04 PDT) — NO-PUSH (4th consecutive night: `/sessions` no-space kills the workspace shell → no git). Shipped 0 / reverted 0 / repaired 0. Outputs written to the mount, UNPUSHED (Trevor/Claude Code commit as usual via the pushurl PAT). Full log: `docs/handoff-2026-08-11-overnight-pass.md`.

- **Post-ship watch of the big 08-10 CC structural wave — ALL PASS, 0 reverts.** The precompute per-leg split + D34 (jobid 287) had its first natural post-M3b tick at **06:58Z SUCCEEDED 233.3s** (the 00:58Z permission-denied tick was PRE-M3b, which landed 01:03Z) — confirms the M3b `cron_heavy` grant fix AND the D34 8th leg work in prod; `trust_precompute_max_age_hours=1.11` fresh. This is the explicit "left for the automated cadence" item from the 08-10 entry, now RESOLVED. ccm-step2 (jobid 4) clean 20.6s @04:25Z (was 300.1s). reconcile (jobid 259) post-fix procedure deployed but 13:33Z 08-10 still hit the hard 300s cap under saturation (known limitation; display-only self-healing; next test 13:33Z 08-11).
- **Health green-with-known-saturation-noise.** Security 4/4 clean. 3 trust breaches all known-class: panini dry 14 (operator runner outage), `public_board_slow_count=16` (saturation collateral — last liveness sweep in a heavy-IO window, all 16 views returned rows; **all 5 public-board snapshots FRESH <16 min so the caching layer is serving fresh data, no user impact**), unmapped 215 (net-draining backfill inflow). 0 new recurring Sentry (2 entity-page saturation-collateral, decaying). pg_cron scheduler healthy (pack-opens `cron_silent` = documented no-op-walk false positive; jobs 55/56 fire every tick ~0.1s). DB 12,372→12,470 MB.
- **Nothing ship-eligible** (NO-PUSH; every DB lever is CC-owned/operator-gated/needs a code half). **Queued new:** `/api/public/ipfs-media/[cid]` 5xx elevated (103/6h, upstream IPFS pass-through, fails soft) → daytime monitor to characterize. Inbox empty (CC archived the deferred files). **ESCALATION (4th night):** operator must delete old Cowork sessions to free `/sessions` and restore overnight push (`docs/handoff-2026-08-09-cowork-shell-recovery.md`).


### August 11, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only maintenance on tip `d410be5e`: independently re-verified every checkable canonical-reference fact against the repo (all CURRENT, no factual correction due) and made **three genuine ADDITIONS** — a whole new public product surface that had landed from a concurrent session and was documented nowhere in the canonical sections. No product/DB/prod change; direct to `main`. Ledger entry 2026-08-11.

- **Added the global catalog search to Route structure + Selected API endpoints.** `/api/search` + `rpc_search_catalog` + `components/search/GlobalSearch.tsx` (in `GlobalSiteHeader`) shipped in `98a9d4a2` — **the site's first real search**; everything prior searched ONE column with an unindexed `ilike`. Recorded the three durable constraints a future editor would otherwise re-break: it MUST apply the **canonical-edition predicate** (TS moments are stored under two key conventions, so without it every TS hit doubles — Lillard 95 vs the canonical 65), it MUST join `AND c.is_active` (unpublished collections have no routes → hits 404 on click), and it MUST **503 rather than 200-with-`results:[]`** (an empty array is byte-identical to "nothing matched", so an outage would render as "we have no such moment"). Also recorded that the catalog holds **no descriptive prose**, so narrative queries correctly return zero and the copy says so — do not "fix" that by implying coverage the data lacks.
- **Added both new `proxy.ts` public-path carve-outs** (the security-relevant one) — `/api/search` and GET `/api/profile/follows`, both GET/HEAD-only EXACT-path (no subtree). The prose now carries the ⚠ that made the follows carve-out safe: **`isPublicPath` never sees a query string**, so BOTH the `?username=` probe and the bare listing form bypass the proxy, and the route's own `requireUser()` is what protects the listing — keep that guard when editing the route. POST/DELETE writers stay gated.
- **Added the Follow button** to the `/profile/[username]` bullet (`c91a5aea`) — the follows table, CRUD route and dashboard friend-activity feed had existed since Phase 4 with **zero callers**, so follows sat at 0 rows and the feed showed its empty state permanently.
- **All 13 canonical reference facts re-verified current on this tip, no edit due:** 37 Vercel crons · 122 DB-invariant pins (raw `grep -cE 'fn:'` = 123, one two-fn pin counted twice) · both launch flags (`CANDY_MLB_PUBLIC` / `PANINI_PUBLIC`) `true` · 8 collections defined / 5 published (the interface's own field decls inflate a naïve grep to 9/6) · 17 workers · 20 GHA workflows (17 scheduled / 3 unscheduled) · 8 CI jobs · concierge **29 tools** on `claude-sonnet-4-6` · primary coverage thresholds 89.3/75.1/91.5/91.6 · component thresholds 79.0/67.0/78.8/83.2 (the search commit added `components/search/**` to the gate's `include` allowlist but did NOT move the thresholds).
- **No tail-roll due:** `date -u` 02:40Z − 7h PDT = **19:40 PT Aug 11**, so this entry introduces no new calendar day — Recent sessions still spans exactly Aug 11 · Aug 10 · Aug 9 and the archive pointer stays "August 8 → August 1".
- **Relationship to the shipping session's entry directly below.** All four ships (`98a9d4a2` search, `c91a5aea` follow button, `7c454ce6` proxy carve-out, `9d6e2408` sale-print charts) landed from a concurrent session that authored its own entry — it pushed twice mid-session, forcing two rebases. The two entries are complementary, not duplicates: **theirs is the shipping log, this one is the docs-maintenance log**, and the durable value here is that the facts were moved into the CANONICAL REFERENCE sections (Route structure, Selected API endpoints, the `proxy.ts` bypass set, the `fmv_snapshots` schema bullet). That distinction is load-bearing: **dated entries are frozen history and roll off to `docs/sessions/` within ~3 days**, so a fact left only in a session log stops being found by the sections a future session actually reads.


### August 11, 2026 (Claude Code, interactive, cont. — Trevor sent Top Shot moment-page screenshots: "this is the type of additional descriptors I'm hoping we can pull") — **the encyclopedia gap is REAL but SMALLER than this session first concluded, and one instance of it was a defect, not a gap.** 2 commits (`f3480a43` region), 1 prod migration. No hot-wallet, auth, or destructive-SQL change.

- **⚠ DURABLE — we have been FETCHING descriptive prose and throwing it away.** `ALLDAY_RELAY_QUERY` in [lib/editions-hydrate.ts](lib/editions-hydrate.ts) has selected **`play { description }` since the query was written**, but `AllDayEditionMeta` never carried the field, so every AllDay hydrate discarded the only descriptive text any of our ingests receives. Now captured through `hydrateAllDayEditions` → `toUpsertRow`. ⚠ **Clobber-safety is the subtle part:** `toUpsertRow` spreads the row straight into the upsert, so a present-but-`undefined` `description` key would write NULL over a good value on every Top Shot hydrate and on every AllDay row with no prose — the key must be **omitted entirely**, and `HydratedEditionRow.description` is OPTIONAL so `emptyRow` and the Top Shot path never emit it. Same class as the `save_user_wallet` COALESCE-never-null-out rule.
- **⚠ `editions.description` now exists (nullable, catalog-only ALTER) but is DELIBERATELY NOT SEARCHED.** `rpc_search_catalog`'s edition arm is index-bound; an un-indexed `description ILIKE '%q%'` OR forces a seq scan of all ~27k editions on EVERY query and would undo the **162ms → 33ms** the `idx_editions_team_name_trgm` index bought. Correct order: **populate → measure coverage → trigram index → then extend the arm.** Wiring it at 100% NULL buys zero recall for real latency. Do not "finish the job" by adding the predicate before the data is there.
- **⚠ DURABLE — the sandbox CANNOT reach the collectible upstreams at all.** Measured 2026-08-11: `curl` to `public-api.nbatopshot.com` and `public-api.nflallday.com` both return **`status=000`** (connection refused by the agent proxy), and `TS_PROXY_SECRET` / `INGEST_SECRET_TOKEN` / `SUPABASE_SERVICE_ROLE_KEY` are all **absent** from the sandbox env. So GraphQL schema discovery CANNOT be done from a dev session — it has to run where the secret lives. Same class as the documented `supabase.co` (403 CONNECT) and `jsr.io` blocks.
- **Shipped the probe rather than guessing: `/api/admin/discover-moment-descriptors`** (admin-gated POST, read-only, writes nothing). Introspection first; falls back to **FIELD PROBING** — ask for one candidate field and read the error, since GraphQL answers `Cannot query field X on type Y` for an unknown field, so the error text IS the schema. Walks Top Shot `PlayStats`/`Play` (headline / prose / box-score / bio candidates drawn from what the moment page visibly renders) and AllDay `Play`. It returns **sample values, not just field names**, because "field missing" and "field exists but empty" imply opposite next steps. **Operator action — one call:** `curl -X POST .../api/admin/discover-moment-descriptors -H "Authorization: Bearer $RPC_ADMIN_TOKEN"`.
- **What the screenshots establish:** the Top Shot moment page renders a **headline** ("Victor Wembanyama stuffs stat sheet in historic 5x5 vs Lakers"), a **prose paragraph**, and a **full per-moment box score** (MIN/PTS/REB/AST/STL/BLK/TOV/+-, FG, 3P, FT + quarter scores) plus player bio (height/weight/hometown/draft). Our Top Shot ingest selects only `play { stats { playerName, playCategory, playType, dateOfMoment, teamAtMoment, … } }` — no descriptive field at all. So the earlier session conclusion "the catalog holds no descriptive prose" is correct about OUR DB but must NOT be read as "the data is unobtainable upstream".


### August 11, 2026 (Claude Code, interactive — "some ideas I've been thinking of" → assessment → "Do all you can") — **shipped 3 of the 7 ideas end-to-end: the follow button, the site's first catalog SEARCH, and long-horizon price charts.** 8 commits to `main` (`84710248` → `9d6e2408`), 5 prod migrations + 1 CONCURRENTLY index, each verified live + ledgered with a revert path. No hot-wallet, FMV/pricing-MATH, or destructive-SQL change.

- **The encyclopedia idea is DATA-blocked, not search-blocked — measure before building.** `editions` has **no `description` column**; `editions.name` is literally `"Damian Lillard — Base Set"`; `badges`/`reward_indicators` are **empty on every row of all 5 collections**; `play_type`/`play_category` are shot mechanics (Rim, 3 Pointer, Dunk). So **"Damian Lillard Game Winners" is unanswerable** — the concept exists nowhere in the data. The richest untapped corpus is `badge_editions.play_tags` (Tomahawk Dunk, Alley-oop, Hot Streak, MVP Year, Hall of Fame) which `app/api/badges/route.ts:138-152` deliberately DISCARDS — but it covers only **3,839 of 19,735** Top Shot editions (**19%**), so a search built on it would silently answer for a fifth of the catalog. Catalog scale is tiny (27,143 editions / 3,841 players / 914 sets / 171 teams) — search was never an infra problem.
- **⚠ DURABLE — `editions` stores Top Shot moments under TWO key conventions for the SAME moment** (int `setID:playID` **9,436** rows vs UUID-pair **10,299**), and the UUID twin carries no thumbnail and **no sales**. Any new surface reading `editions` un-filtered returns every Top Shot result TWICE with ~doubled counts (Lillard read 95; canonical **65**). Use the platform's existing predicate — Top Shot rows must match `'^[0-9]+:[0-9]+(::[0-9]+)?$'` — the same one `rpc_fmv_confidence_share` and the trust-health precompute legs use. Sales attach ONLY to the canonical rows, so sale-derived history needs no such filter.
- **⚠ DURABLE — `fmv_snapshots` begins 2026-03-31 (~4.5 months); `sales` begins 2020-07-28 (3.11M TS rows).** So the edition chart's "365d" chip had NEVER shown a year. Any 1Y/all-time price view must be sale-derived. New `get_edition_sale_history` returns median (not mean — the Sept-2020 `5:145` bucket is median $50 vs high $800) with **grain returned per row** (day/week/month) so the axis label is measured, not assumed.
- **⚠ DURABLE — a plpgsql `RETURN QUERY` type-checks on EXECUTION, not at CREATE.** `rpc_search_catalog` applied cleanly and only failed when first CALLED (`42804`, `collections.slug` is `varchar(50)` not `text`). **Applying a migration is not verification — you must call the function.** Also: `count(*) FROM check_secdef_anon_exec_drift()` reads **1 when clean** (it returns one row containing an empty array) — read the contents, not the count.
- **Shipped.** (1) **Follow button** — the `follows` table, its full CRUD route, and the dashboard friend-activity feed had existed since Phase 4 with **zero callers** (grep returned only the route file), so it sat at 0 rows behind a permanent empty state. Added `FollowButton` + a single-edge `?username=` probe; then found by probing PROD that `/api/profile/follows` was proxy-gated, so anon got 50KB of login HTML instead of the `{authed:false}` the route was written to return (the button survived via its catch — a waste/honesty fix, not a broken UI). (2) **`/api/search` + `rpc_search_catalog` + a header search bar** — player/set/team/edition, multi-token (longest token anchors an index-backed ILIKE, rest via `LIKE ALL`), entity arms resolve from the table the PAGE resolves from so a hit always has a working route, zero-edition rows dropped, `is_active` gate (candy/panini links would 404). **One new index** `idx_editions_team_name_trgm` (CONCURRENTLY, out of band): **162ms → 33ms**. (3) **1Y/ALL sale-print charts** + fixed a pre-existing bug where a FAILED fetch rendered "too few sales to chart" — a claim about the market, from a network blip.
- **Honesty constraints are structural, not decoration:** `/api/search` returns **503 on failure, never 200-with-empty** (indistinguishable from "nothing matched"); `meta.note` and the empty state both state that narrative queries can't match; the FMV and sale-print series are **never merged into one line** (different quantities). Verified: primary gate **1117 files / 10496 tests**, component gate **170 / 1342**, `tsc` clean, security invariants 0, secdef drift 0, anon-write 0; search confirmed live in prod anonymously.
- **Left for Trevor (exposure decisions, deliberately not taken):** there is still no collector DIRECTORY, so following requires already knowing a username — a public `/collectors` index is a new public surface. And `GlobalSiteHeader` is only rendered by the `(collections)` layout, so the search bar is absent on `/dashboard`, `/insights`, `/analytics` and `/`; unifying those headers is a separate layout change.


### August 11, 2026 (Claude Code, interactive — "implement a TODO" → inbox drain → "keep going"×N → user green-lit a prod index build → wrap-up) — **3 ships to `main` + a prod covering index built under a strict quiet-window gate.** Commits `708145e7` (smoke-test) · `95c521f7` (finding) · `ed487dde` (index). Each verified; no hot-wallet, auth/`proxy.ts`, FMV/pricing-MATH, or destructive-SQL change.

- **In-code TODO backlog is (still) genuinely DRAINED** — exhaustive sweep across all source types + `docs/code-todos.md`; every literal TODO is resolved / test-fixture / product-gate / operator-gated (Panini captures, Candy secrets). Pivoted to the inbox on request rather than manufacturing work.
- **Ship 1 — smoke-test `couldNotRun` (`708145e7`, deployed READY):** extended the JAVASCRIPT-NEXTJS-25 fix to its last un-covered guard family. `softIfTransientRpc` (the `detect_stalled_pipelines`/`analytics_pipeline_health` health guards) never set `couldNotRun`, so a NON-transient RPC error hard-fired Sentry as `smoke test failed: sales indexers running…` — restating an assertion the errored check never evaluated. Now set on the helper (truthful in both branches; only the non-transient hard-fail path reaches the Sentry-title logic) + a covering test. The two highest-stakes peers (`check-alerts`, `sentinel`) were swept and confirmed already hardened.
- **Ship 3 — trophy-stats covering index (`ed487dde`, user-greenlit):** `idx_sales_2026_ts_otherserial_cover` on `sales_2026 (collection_id, sold_at DESC) INCLUDE (edition_id, price_usd) WHERE serial_number>1 AND price_usd>0` fixed `topshot_first_mint_trophy_stats` (public `/insights/first-mint` KPIs) — **`EXPLAIN ANALYZE` 17,308ms → 2,047ms** (under its 5,400ms budget), seq-scan → Parallel Index-Only Scan. Interim win only; the durable fix is **precompute** (inbox `1930Z`). Parity file `20260812022915_audit_20260812_…`; revert `DROP INDEX CONCURRENTLY IF EXISTS public.idx_sales_2026_ts_otherserial_cover`.
- **⚠ DURABLE — one-off pg_cron `CREATE INDEX CONCURRENTLY` recipe (now 7 indexes built this way).** The cron command MUST be a **SINGLE statement** — a `SET statement_timeout='0'; CREATE INDEX CONCURRENTLY …` multi-statement command runs in an implicit txn where CIC fails (the inbox 1941Z "prefix SET" note is WRONG on this). On an **idle** DB the inherited 120s cap is ample: a 270MB / 955k-row partial covering index built in **35.8s**. Schedule at a **specific minute** (`24 2 * * *`), not `* * * * *`, so it fires ONCE (next occurrence 24h out — no double-fire); `cron.unschedule` by JOBNAME only AFTER the build completes (unscheduling a RUNNING pg_cron job KILLS it).
- **⚠ DURABLE — the Small-compute instance can RESTART under sustained IO saturation.** Mid-session the DB returned `57P03 "not accepting connections / hot standby disabled"` for ~1-2 min after a multi-hour IO-depletion spell (a 256s stuck autovacuum on `wallet_moments_cache` + rolling `fmv_snapshots` reads). **Build prod DDL ONLY in a genuinely quiet window** — gated on `datafileread_waiters<=2 AND long_running_gt10s<=1 AND io_waiters<=4 AND 0 idle-in-txn` and polled via `send_later` across ~40 min until it cleared. `hypopg` is available (not installed) for hypothetical-index proofs, but its hypothetical index + the `EXPLAIN` must run in ONE MCP call (each MCP `execute_sql` is a fresh session; hypothetical indexes are session-scoped).
- **⚠ DURABLE — `idx_sales_2026_ts_edition_median` (the `ed_med` index) does NOT serve `collection_id`-uuid TS aggregates.** It keys on `collection` (text long-form) with a `price_usd > 0.50` partial, so a `collection_id=<uuid> AND price_usd>0` query cannot use it — that mismatch is exactly why the trophy-stats CTE seq-scanned. Check this before assuming ed_med covers a new TS `sales_2026` aggregate.


### August 11, 2026 (Claude Code, interactive — "analyze test coverage → propose → proceed with all → keep going ×N → do all you can → do it → proceed") — **a large test-coverage program + an edge-fn de-dup, and PROVED the edge-deploy mechanism.** ~20 green commits to `main` (`d9a6fa46` region → `0df9b86e`), every one CI-green (incl. the blocking `edge-deno` job) + each ledgered. No hot-wallet, auth/`proxy.ts`, FMV/pricing-MATH, or destructive-SQL change.

- **Test coverage — both gates moved, genuine-safe targets now EXHAUSTED.** Primary gate branch ~75.5%→~78% (stmts→91.2%): trophy-case PDF `route.tsx` pure logic extracted to gate-measured `lib/trophy-case/pdf-image.ts` (+23 tests — a `route.tsx` is measured by NEITHER gate, so extraction to `lib/` is the pattern), **29 API routes + 2 pure libs deepened (~313 cases)** across 7 waves, all reusing the existing route-harness. Component gate branch ~72.7%→78.9% (full gate 1268 tests green): **31 components deepened (3 previously ZERO-test)** across 3 waves. Thresholds LEFT as-is (additive; kept the concurrent-churn buffer). ⚠ **The floor is real, not laziness:** what remains is structurally undriveable (Supabase `.rpc()` reject-only `.catch`, recharts-internal tooltip/tickFormatter callbacks jsdom's 0-width container never invokes, `Date.now`/timer-mock edges), the deep-inline Flow/Cadence giants (`fmv-recalc`/`wallet-search`/`sniper-feed`/`smoke-test`/`ingest`) the codebase flags closed-for-cause (pure logic already tested in `_shared`), the async-server `PopularOnCollection` (jsdom can't render), or dead/retired routes. Subagents did the route/component waves; each batch independently re-verified (targeted vitest + `tsc`, full suite where a shared helper changed) before commit.
- **DB-pin live-drift check weekly→daily** (`.github/workflows/db-pin-staleness.yml`) — it's the ONLY check that catches a pinned fn whose LIVE definition drifted (MCP-applied, no committed file); weekly left ≤7d silent, daily ≤24h. Read-only, soft-skips without secrets.
- **Edge de-dup (`030b9fa6`):** `sync-nba-projections/index.ts` now imports its tested `_shared/nba-projections-parse.ts` (8 inline duplicate fns + `GameMatch` deleted, −113 lines) instead of running parallel inline copies — the silent-drift hazard the drift-guard exists for. Also hardened the `_shared` combining-mark regex raw-literal→escape. ⚠ **NOT redeployed** (byte-identical behavior verified via local `deno check` parity vs backup; the whole edge fleet is already repo-refactored/deployed-inline drift). Verified type-safe by installing deno locally (see below).
- **⚠ DURABLE — the MCP `deploy_edge_function` multi-file `../_shared/` bundling WORKS (proven 2026-08-11).** No deployed edge fn had ever used it (fleet runs pre-`_shared` inline code). A throwaway `shared-deploy-probe` deployed with `[{name:"index.ts"},{name:"../_shared/x.ts"}]` landed the files at `<root>/source/index.ts` + `<root>/_shared/x.ts` (exactly where `../_shared/…` resolves) with a valid eszip → the relative dep bundled. **⚠ CLEANUP OWED: delete `shared-deploy-probe` from the Supabase dashboard** (neutered to an inert `410` stub; MCP has no delete verb). **⚠ DURABLE FOOTGUN — there is NO `supabase/config.toml`, so a naïve `supabase functions deploy` defaults `verify_jwt=true`, which BREAKS every custom-auth cron fn** (they run `verify_jwt=false` + in-body Bearer/gate auth; the gateway would reject the `INGEST_SECRET_TOKEN` callers). Any fleet de-drift MUST pass `--no-verify-jwt` per fn. Full procedure + the 6-fn inventory: [docs/handoff-2026-08-11-edge-fleet-dedrift.md](docs/handoff-2026-08-11-edge-fleet-dedrift.md).
- **⚠ DURABLE — sandbox network facts:** the agent proxy **blocks `supabase.co` (403 CONNECT)** — deployed edge fns can't be runtime-boot-probed from here — and **`jsr.io` returns 403**, so `deno check` can't fetch `jsr:@supabase/supabase-js` (check a `_shared` module in isolation, or stub the import + diff the error signature vs a backup). **`deno` itself is installable** when `install.sh` is proxy-blocked: `curl -fsSL -o deno.zip https://github.com/denoland/deno/releases/latest/download/deno-x86_64-unknown-linux-gnu.zip && unzip`.
- **Health sweep + 1 filed finding:** CI green on all pushes; the 3 trust breaches (panini-dry / public_board_slow / unmapped-backlog) are all known/documented; nothing broke. Filed [inbox 2026-08-11T1746Z](docs/overnight/inbox/2026-08-11T1746Z-precompute-split-silent-stale-under-role-timeout.md): today's per-leg precompute split can **silently-stale a late leg** (no 999) under a saturated tick because `cron_heavy`'s role `statement_timeout=600s` caps the whole `CALL` cumulatively (doesn't re-arm per COMMIT) → the cap fires outside the leg's `EXCEPTION→999` path. Read-only; NOT auto-fixed (sentinel-critical, concurrently owned). Fix direction = lift the cumulative cap in the orchestrator, NOT widen the role timeout.


### August 11, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only maintenance on tip `030b9fa6`: independently re-verified every checkable canonical-reference fact against the repo, found all CURRENT, made no factual edit, and rolled the tail (12 Aug-8 entries → archive). No product/DB/prod change; direct to `main`. Ledger entry 2026-08-11.

- **All canonical reference facts re-verified current on this tip (`030b9fa6`), no edit due:** 37 Vercel crons (`vercel.json` schedule count) · 122 DB-invariant pins (raw `grep -cE 'fn:'` = 123, one two-fn pin counted twice) · both launch flags (`CANDY_MLB_PUBLIC` / `PANINI_PUBLIC`) `true` · 8 collections defined / 5 published (the interface's own `id:`/`published:` field decls inflate a naïve grep to 9/6) · 17 workers · 20 GHA workflows (17 scheduled / 3 unscheduled: `ci.yml` + the `allow-list-reconcile`/`topshot-listing-cache` Vercel-migrated backstops) · 8 CI jobs (typecheck · cadence-lint · cadence-escrow-tests · unit-tests · component-tests · db-tests · ledger-guard · edge-deno) · concierge model `claude-sonnet-4-6` · primary coverage thresholds 89.3/75.1/91.5/91.6 · component thresholds 79.0/67.0/78.8/83.2.
- **Tail roll:** this Aug-11 entry pushed Recent sessions to 4 calendar days, so the **12 Aug-8 entries** moved to [docs/sessions/2026-08.md](docs/sessions/2026-08.md) verbatim, newest-first (archive pointer now reads "August 8 → August 1"). Recent sessions now holds Aug 11 · Aug 10 · Aug 9. Date check: `date -u` 13:41Z − 7h PDT = **06:41 PDT Aug 11**, a genuine Aug-11 PT day.
- **Landed-since, already ledgered (no separate Recent-sessions entry authored here — that's the shipping session's job):** the interactive component-gate + API-route + pure-lib branch-coverage waves (`699e3dfc` → `030b9fa6` region), the trophy-case PDF pure-logic extraction (`d9a6fa46`), the DB-pin live-drift check moved weekly→daily (`a9038e2c`), the D34 Pinnacle FMV-confidence-share 8th precompute leg (`61bb92aa`) + its cron_heavy EXECUTE grant hotfix (`a579f5b8`), the board-liveness honest-probe decoupling (`4113cab8`), the candy pack-EV FMV-fetch collection scoping (`861d5673`), the migration-parity CI fixes + 68-migration parity restore (`03d3d8fa` → `f2b4030d`), and the `sync-nba-projections` `_shared` parse-core de-dup (`030b9fa6`) all carry their own ledger entries.

### August 10, 2026 (Claude Code, interactive — "implement a TODO" → drained the deep-audit register → "keep going"×N → shipped the D34 prerequisite → wrap-up) — **SPLIT the load-bearing precompute + shipped the D34 arm it unblocked.** ~9 DB/docs commits to `main` (`0a0458da` region → `61bb92aa`), each verified live + ledgered with a revert path. One defect introduced and caught from its own first tick. No hot-wallet, auth/`proxy.ts`, FMV/pricing-MATH, or destructive-SQL change.

- **⚠ DURABLE — `rpc_trust_health_precompute_refresh` is now SPLIT; this SUPERSEDES the frozen "single 569s function / only 3 EXCEPTION handlers / a leg timeout rolls back all 18 metrics" claims in the Aug-8/9 dated entries below (and register D34).** It is now **8 per-leg SECDEF functions `rpc_thp_leg_*()`** (each own `statement_timeout` + `EXCEPTION→999`) driven by a thin **INVOKER procedure `rpc_trust_health_precompute_refresh_p()`** that `COMMIT`s between legs; **pg_cron jobid 287** (`58 */6`) `CALL`s it. A saturated-tick kill now loses ONE leg (a loud 999 for its own arm), never the whole board. Monolith `rpc_trust_health_precompute_refresh()` left defined (unscheduled) as the one-line revert. Migrations M1 `20260810225549` (legs) → M2 `20260810230106` (orchestrator) → M3 `20260810230704`+cron cutover → M3b `20260811010305` (grant fix) → D34 `20260811012334` (8th leg). Plan+§5 in [docs/audits/precompute-split-plan-2026-08-10.md](docs/audits/precompute-split-plan-2026-08-10.md).
- **⚠ DURABLE — a `cron_heavy`-owned pg_cron job needs EXECUTE granted to `cron_heavy`, and an INVOKER orchestrator needs it on EVERY function it PERFORMs (EXECUTE is required to CALL a fn even when it is `SECURITY DEFINER`).** The split's first natural tick (00:58Z, jobid 287) **FAILED `permission denied for procedure`** because M1/M2 revoked the new objects to postgres+service_role only — but job 287 runs as `cron_heavy` (its owner), and the retired monolith carried an explicit `cron_heavy=X` grant (that is why it ran for months). Fixed in M3b. **Check `has_function_privilege('cron_heavy', oid, 'EXECUTE')` before cutting a cron over to new objects** — the M1/M2 revoke-to-service_role pattern (correct for SECDEF fns only reached by `supabaseAdmin`) is WRONG for a fn a `cron_heavy` job calls.
- **⚠ DURABLE — cron cutover privilege recipe.** `apply_migration` can't touch `cron.job` (`42501`) — do DDL via migration, the cron via `execute_sql`. And even as `postgres`, **`cron.unschedule(bigint jobid)` and `cron.alter_job(...)` both `42501` permission-denied**; only **`cron.unschedule(text jobname)` works, and only when run as the job's OWNER role** (the jobname overload silently scopes to the current role's jobs). For a `cron_heavy`-owned job: `SET ROLE cron_heavy; SELECT cron.unschedule('<name>'); SELECT cron.schedule('<name>', '<sched>', $$CALL …$$);` in `execute_sql`. `CREATE OR REPLACE PROCEDURE/FUNCTION` PRESERVES existing grants (the M3b cron_heavy grant survived the D34 orchestrator replace).
- **⚠ DURABLE — a procedure that does `COMMIT` must be INVOKER-rights, NO `SET` clause, single-statement `CALL` (schema-qualify everything); a SECDEF proc or a SET clause → `2D000 invalid transaction termination`. `statement_timeout` does NOT re-arm per `COMMIT`, so the per-leg budget lives on the SECDEF leg FUNCTIONS' `SET` clauses, never the procedure.** (Same family as the `reconcile_all_saved_wallet_stats` finding.) Validated the whole mechanism with throwaway one-off pg_cron jobs (schedule → observe `cron.job_run_details` → unschedule).
- **⚠ DURABLE — set a per-leg budget ABOVE the leg's SATURATED cost, not its healthy cost.** M3a raised the `impossible_parallel` leg 300s→480s because in the cheapest-first order it runs LAST (most connection-contended). Vindicated live: a full 8-leg one-off run under heavy saturation took **319s total with the impossible_parallel leg at 289s** — a 300s cap would have written a false 999 and PAGED (breach_at 3). EXPLAIN-characterized §5: that leg is index-optimal; its cost is ~16k cross-partition `edition_id` probes + a heap fetch per matching sale to test `serial_number > circulation_count` (no sales index carries `serial_number`) — the two real fixes (covering `(edition_id, serial_number)` index / recent-`ingested_at` window) are both gated (operator / owner-semantic).
- **D34 RESOLVED (`20260811012334`):** `rpc_thp_leg_pinnacle_fmv_share()` computes `pinnacle_fmv_high_med_share_pct` (**28.7** live) from `pinnacle_fmv_history` (DISTINCT ON render_id over the `(render_id, computed_at)` PK, ~0.7–1.5s) as the 8th leg. **TRACK-only like the other 5 `*_fmv_high_med_share_pct` metrics — NOT a `v_rpc_trust_health` arm (board stays 38); a thin market is a fact, not an alert.** Read via `rpc_fmv_confidence_share()`/dashboards straight from `rpc_trust_health_precompute`.
- **Also shipped this thread:** pruned 2 orphaned `public_board_liveness_state` rows (47→45, `20260810190101`); characterized D37 (AllDay `unmapped_sales` backlog — 63% is correctly-parked price-0 rows, no cheap fix). **Verified benign:** a concurrent session's board-liveness decouple (`public_board_liveness_probe()` is now a cheap state read + a separate `public_board_liveness_sweep()` on jobid 288 `28 */6`) makes the split's board-liveness leg ~86s→~0ms — de-risked before the tick.
- **⚠ DATE FOOTGUN, bit me: I mis-stamped the M3b + D34 ledger headings `2026-08-11` (UTC) — corrected to `2026-08-10` PT** here and in the ledger (with `[ledger-roll]`, since a heading-date edit changes the guard's heading set). Migration NAMES stay UTC-stamped (`audit_20260811_…`) — that PT-heading/UTC-migration split is the correct convention. It was 20:02 PT (03:02Z) at wrap-up.
- **Left for the automated cadence:** the split's first NATURAL 06:58Z tick (the two verifying runs were manual one-offs) — the nightly pass's post-ship watch + daytime monitor will observe it; the split is proven (permission fixed, 8-leg run green, zero 999s). Operator/owner-gated remainder unchanged: D2b (secrets), D8/trophy-stats CONCURRENTLY indexes, the board-liveness `count(*)`-pruning fix (estate-wide arm-flip decision), `pinnacle-sync` cron-job.org re-enable (`pinnacle_fmv_stale_hours` climbing ~22.5 toward breach 30).

### August 10, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only maintenance on tip `879f2dc1`: independently re-verified every checkable canonical-reference fact against the repo, found all CURRENT, made no factual edit and no tail-roll. No product/DB/prod change; direct to `main`. Ledger entry 2026-08-10.

- **All canonical reference facts re-verified current on this tip (`879f2dc1`), no edit due:** 37 Vercel crons (`vercel.json` schedule count) · 122 DB-invariant pins (raw `grep -cE 'fn:'` = 123, one two-fn pin counted twice) · both launch flags (`CANDY_MLB_PUBLIC` / `PANINI_PUBLIC`) `true` · 8 collections defined / 5 published (the interface's own `id:`/`published:` field decls inflate a naïve grep to 9/6 — the real entries are 8/5) · 17 workers · 20 GHA workflows (17 scheduled / 3 unscheduled: `ci.yml` + the `allow-list-reconcile`/`topshot-listing-cache` Vercel-migrated backstops) · 8 CI jobs (typecheck · cadence-lint · cadence-escrow-tests · unit-tests · component-tests · db-tests · ledger-guard · edge-deno) · concierge model `claude-sonnet-4-6` · primary coverage thresholds 89.3/75.1/91.5/91.6 · component thresholds 79.0/67.0/78.8/83.2.
- **No tail-roll due:** Recent sessions spans exactly 3 calendar days (Aug 10 · Aug 9 · Aug 8), within the "keep ~3 days" rule — archive pointer under "Older sessions" unchanged ("August 7 → August 1"). Today is Aug 10 PT (`date -u` 23:16Z − 7h PDT = 16:16 PDT Aug 10), so this Aug-10 entry introduces no new calendar day.
- **Landed-since, already ledgered (no separate Recent-sessions entry authored here — that's the shipping session's job):** the **D34-prerequisite precompute per-leg split SHIPPED** (`68fb505c` → `879f2dc1`, ledger 2026-08-10): `rpc_trust_health_precompute_refresh` is now split into 7 per-leg SECDEF functions (M1) driven by a per-leg-COMMIT INVOKER orchestrator (M2), with pg_cron 222 cut over to the orchestrator (M3) and the impossible_parallel leg budget widened 300s→480s (M3a). ⚠ **This means the "the real fix is to split the fn into per-leg functions" prescription — and the "runs at ~569s of a 600s budget / a timeout rolls back all 18 metrics" framing — in the frozen Aug-8/Aug-9 dated entries below (and the register D34 item) is now DONE**; those entries are left verbatim (correct as of their tip), but a per-leg kill now loses one metric, not the whole board.

### August 10, 2026 (Claude Code, interactive — assessed a filed Cowork finding, then "keep going / do all you can / clean tree") — **refuted the finding's conclusion with a live re-measure, then shipped the fix it said was unnecessary.** 1 prod migration + 3 docs commits, all pushed, ledgered with a revert path. No hot-wallet, auth/`proxy.ts`, FMV/pricing-MATH, or destructive-SQL change.

- **⚠ DURABLE — you CANNOT time a query by wrapping it in `clock_timestamp()` CTEs. The planner evaluates them independently of the work.** A filed finding declared `public_board_slow_count` a false alarm on the strength of "I re-measured `allday_scarcity_board` live: **4,044 ms**, 49% of budget." I reproduced that method (`MATERIALIZED` CTEs bracketing a `count(*)`) and it returned **0 ms** for the same 6,190-row board — impossible, and the same class of artifact that produced the 4,044. **`EXPLAIN (ANALYZE)` is the only trustworthy timing here.** By that measure the board was **32,809 ms cold / 38,574 ms warm** against an 8,300 ms budget, and `topshot_first_mint_trophy_stats` **17,308 ms** vs 5,400 — **both WORSE than the "stale" snapshot the finding was dismissing.** Warm being *slower* than cold also rules out cold-cache. The arm was a **TRUE positive**; acting on the finding would have dismissed a real breach on a public board and its ⛔ "don't re-tune `max_ms`" would have been kept for exactly the wrong reason.
- **⚠ DURABLE — a `count(*)` health probe can be silently PRUNED into meaninglessness, and my own fix did it.** `public_board_liveness_probe` times `SELECT count(*) FROM <view>`. The board's old `LEFT JOIN LATERAL (… ORDER BY computed_at DESC LIMIT 1)` was **not provably unique, so it survived into the plan and `count(*)` measured the FMV work by accident.** Replacing it with `DISTINCT ON (edition_id)` **proves** `edition_id` unique → PG **removes the LEFT JOIN entirely** for `count(*)`. Net: probe reads **2,377 ms** while the page pays **15,172 ms** — the arm will read **GREEN for a board still ~183% over budget**. Users got a 2.4× faster board and the instrument watching it went blind in the same commit. This is the recorded *"the probe times `count(*)`, which the planner prunes"* caveat graduating into an active false-negative. **Do not treat this board's arm as authoritative until the probe is fixed** (queued, `docs/overnight/inbox/2026-08-10T1900Z-*.md`, with the candidate one-line fix AND the slow-vs-empty conflation that must be fixed alongside it — an errored probe counts as EMPTY, and at 15.2s this board would exceed its `clamp(max_ms*1.5)` = 12,450 ms cap and mislabel as "renders blank").
- **The ship (`931a52db`, migration `20260810185031`):** the LATERAL ran once PER EDITION — 6,190 random probes into partitioned `fmv_snapshots` (~5.2 ms each, ~2,950 heap fetches). One `DISTINCT ON` CTE filtered on `collection_id` rides the existing `fmv_snapshots_<year>_collection_id_edition_id_computed_at_idx` (Merge Append → Unique, no sort). **33–38 s → 15.2 s.** Verified **output-identical BEFORE applying**: `EXCEPT` diff in BOTH directions = 0 rows over all 6,190 editions, and **0 editions tie at `max(computed_at)`** so neither form is order-dependent. Post-apply 6,190 rows / 5,210 priced, `security_invoker=on` and all three grants unchanged, `check_public_security_invariants()` 0, route test 3/3, live page 200 with **142,881 bytes of real DOM**. ⚠ **Deliberately reported as PARTIAL** — still 183% of budget; the durable fix is the shared materialize-latest-FMV item, which would also close `/api/market` and `cross_collection_deals_board`.
- **⚠ The finding was wrong on two more points, both checked rather than assumed.** (1) *"It does not use the existing 999 sentinel"* is **FALSE** — the board arms are precomputed rows and `v_rpc_trust_health`'s `pre` CTE **already** maps `computed_at < now()-24h` → 999. The guard exists at 24 h; the snapshot was 11.2 h old, so it simply had not tripped. The proposed fix is a **tightening 24h→8h**, not a missing guard. (2) `trust_precompute_max_age_hours` (breach 13) fires **earlier** than that 999 mapping, so precompute staleness was already attributable — it tracked correctly all session (9.98 → 11.97).
- **Missed by the original sweep: 2 orphaned rows.** `public_board_liveness_state` holds **47** rows against **45** active watchlist entries — `candy_deals_board` and `topshot_underpriced_serials_board` are `is_active=false` with rows frozen at **2026-08-02 (208 h)**. They do **NOT** inflate any arm (the probe derives `n_slow`/`n_empty` from its own loop over active rows, never from the table), so it is a hand-reading hazard only — and it is what made the original table read confusing. Left in place deliberately; cleanup buys no signal.
- **Method:** the mechanism half of the finding was fully CONFIRMED (single caller `rpc_trust_health_precompute_refresh`, jobid 222 killed at 600.0 s on the 12:58Z tick two days running, probe writes rolled back with it) — **a correct mechanism does not make the conclusion correct.** Corrected the filed inbox artifact in place rather than writing a new one, keeping the original text struck-through so the record shows what was claimed. Ledger discipline held: re-read fresh, `grep -c '^### '` 1185 → 1186 (+1 exactly), line 9 intact — and the first splice landed my entry BELOW its same-day sibling, so it was redone against `head -9` to keep newest-first.

### August 10, 2026 (Claude Code, interactive — "work through everything pending in the tree to ensure we have a clean tree afterwards. Address any issues you encounter") — landed **three nights of NO-PUSH overnight output** and fixed the one job in the estate that had *never once succeeded*: `rpc-reconcile-saved-wallet-stats` (pg_cron jobid 259). 2 prod migrations, both verified live end-to-end, each ledgered with a revert path. No hot-wallet, auth/`proxy.ts`, FMV/pricing-MATH, or destructive-SQL change.

- **⚠ DURABLE — a PL/pgSQL procedure that does transaction control can be NEITHER `SECURITY DEFINER` NOR carry a `SET` clause.** Both put the call in an atomic context and `COMMIT` fails `2D000 invalid transaction termination`. Measured with three one-minute throwaway pg_cron probes: SECDEF-without-SET **FAILED**, invoker-with-`SET search_path` **FAILED**, plain invoker **SUCCEEDED**. This matters because the queued profile doc explicitly prescribed keeping both ("FUNCTION→PROCEDURE is an object change: grants reset — re-`REVOKE`/`GRANT` in the same migration"), which would have shipped a job that `2D000`s on its FIRST tick — strictly worse than the timeout it replaces. So the procedure is invoker-rights with no `SET` clause and **schema-qualifies every reference** instead; a runtime `SET search_path` was also rejected because it would persist on a pooled connection after the call returns. Privilege is unchanged in practice (pg_cron runs it as `postgres`, only other grantee is `service_role`) and the actual column writes still go through the SECDEF `aggregate_saved_wallet_stats` / `log_pipeline_run`.
- **⚠ DURABLE — `statement_timeout` does NOT re-arm per `COMMIT`.** The doc's stated mechanism — "the timer re-arms after each COMMIT, so it effectively gets the budget *per wallet*" — is false. A probe set the session GUC to 2500 ms then ran three 3.0 s legs; **all three survived**, each reporting `statement_timeout = 2500ms` at its own commit boundary. Only the timer armed at top-level statement start counts (the same reason a function-level `SET statement_timeout` is inert here). A per-wallet `COMMIT` therefore buys **durable partial progress**, not a per-wallet budget — the soft deadline is what bounds the run. Related: the cron command **must be a single statement**, since `SET ...; CALL ...` is an implicit transaction block and the COMMIT fails 2D000; the `SET statement_timeout='300s';` prefix added on 08-09 is removed.
- **⚠ DURABLE — `saved_wallets.cache_updated_at` is a LAST-CHANGED stamp, not a last-verified stamp, and every staleness figure filed against this job has been measuring the guard.** Live: 21 of 99 rows read older than 12 h — and **all 21 are (wallet, collection) pairs holding nothing whose three `cached_*` columns are already correctly `(0, NULL, NULL)`. Zero rows are wrong on screen.** Pass 2 deliberately skips a row that already holds the right values, so its stamp can never advance. The inbox doc's "p50 age 9.4 h / max 33.5 h / 21 rows past 12 h" is that artifact — do NOT re-file it as a data-quality defect.
- **⚠ The fix's own new telemetry caught a defect I had just introduced, on its first run.** It committed **4 of 21 wallets / 13 rows in 106.5 s** and logged `truncated: true` — the first progress this job has ever made (2 of 2 prior scheduled attempts committed *nothing*: killed at 120.0 s, then at 300.4 s after the budget raise). That immediately exposed two things: (a) stalest-first was ordering on those *perpetually-old zero-holding rows*, so it spent the whole budget on wallets chosen by a timestamp that can never move and would pick the same ones on every future run — **a livelock dressed as a priority queue**; and (b) `IF NOT v_truncated` gated the cheap, always-correct pass 2 behind completion of the expensive pass 1, so the one genuinely-wrong-on-screen case (a wallet that sold out of a collection keeps showing its old count forever) was skipped in exactly the saturated conditions that cause it. Second migration runs **pass 2 first and ungated**, restricts pass 1's list *and* ordering to pairs that actually hold moments, and adds a 6 h freshness skip.
- **Also measured:** the soft deadline is checked BETWEEN wallets only and **cannot preempt a single long wallet** — one wallet exceeded 100 s in the 16:00Z saturation window (the same set takes 37 s for all 21 on a quiet instance). A kill then costs only that wallet. And `cron.unschedule` **cancels a RUNNING job** — it killed one of my verification runs mid-flight at 29.0 s.
- **NOT done, deliberately:** no third budget raise (refuted twice); no schedule move off 13:33Z (that slot is not structurally congested — jobid 215 exceeds 3 min on only 13% of runs); no `tier` covering index on `wallet_moments_cache` (INCLUDE columns block HOT exactly like key columns on a 2.2 M-row table the wallet walks write constantly — not worth it for display-only data); `aggregate_saved_wallet_stats` untouched (still the single definition of these three columns, shared with the route); the array_agg fold declined (the doc measured it at only −21%, because removing the subplan knocks the aggregate off its covering index).
- **Tree landed:** the 08-10 nightly pass's ledger entry + `metrics-latest.json` + `docs/handoff-2026-08-10-overnight-pass.md`, the weekly `docs/health/PROJECT_HEALTH_2026-08-10.md`, and the two 08-10 inbox files, all of which had been written to the mount UNPUSHED across three consecutive shell-down nights. **Daytime-monitor `1509Z` dispositions:** its item 1 (Sentry `JAVASCRIPT-NEXTJS-26`, edition-page pool-acquire timeouts, 8 events/1 user) is saturation collateral with no new code lever, as filed; item 2 (`pinnacle-sync` missed its 10:07Z tick, last ran 08-09 10:07Z) is confirmed real but LOW — `pinnacle-catalog-backfill` ran today 09:37Z ok/2,457 rows, so catalog freshness is covered by the 08-08 Vercel backstop and Pinnacle FMV is on separate pg_cron jobs. It is an external cron-job.org tick, operator-side; not escalated on one miss.
- **Tail roll:** this Aug-10 entry pushed Recent sessions to 4 calendar days, so the 2 Aug-7 entries moved to [docs/sessions/2026-08.md](docs/sessions/2026-08.md) verbatim, newest-first (archive pointer now reads "August 7 → August 1"). Recent sessions now holds Aug 10 · Aug 9 · Aug 8. ⓘ Unlike the 08-09 entries below, the date check passes here: `date -u` 16:00Z − 7 h = **09:00 PDT Aug 10**, a genuine Aug-10 PT day.

### August 10, 2026 (nightly autonomous pass, GENUINE OVERNIGHT ~01:03 PDT) — NO-PUSH (3rd consecutive night: `/sessions` no-space kills the shell → no git). Shipped 0 / reverted 0 / repaired 0. Post-ship watch of the 08-09 CC wave ALL PASS (check-alerts recovered via the unmapped-backlog precompute; candy spread board 0 neg; D3b wallet reads instant). 3 trust breaches all known (panini dry 13, unmapped 194 backfill-inflow, public_board_slow 5 = deals board ~3h stale). Artifact estate verified healthy vs the D33 column drop. Full log: `docs/handoff-2026-08-10-overnight-pass.md`. Outputs landed to `main` by the Aug-10 Claude Code session above.


### August 9, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only maintenance on tip `f871c76c`: independently re-verified every checkable canonical-reference fact against the repo, found all CURRENT, made no factual edit and no tail-roll. No product/DB/prod change; direct to `main`. Ledger entry 2026-08-09.

- **All canonical reference facts re-verified current on this tip (`f871c76c`), no edit due:** 37 Vercel crons (crons array) · 122 DB-invariant pins (raw `grep -cE 'fn:'` = 123, one two-fn pin counted twice) · both launch flags (`CANDY_MLB_PUBLIC` / `PANINI_PUBLIC`) `true` · 8 collections defined / 5 published · 17 workers · 20 GHA workflows (17 scheduled / 3 unscheduled) · 8 CI jobs (typecheck · cadence-lint · cadence-escrow-tests · unit-tests · component-tests · db-tests · ledger-guard · edge-deno) · concierge model `claude-sonnet-4-6` · primary coverage thresholds 89.3/75.1/91.5/91.6 · component thresholds 79.0/67.0/78.8/83.2.
- **⚠ DATE FOOTGUN, live again: the harness stamped "today = 2026-08-10" but that is UTC.** `date -u` = Aug 10 06:32Z; −7h PDT = **Aug 9 23:32 PT**, so this is an **Aug-9** entry and **NO tail-roll is due** — Recent sessions correctly holds Aug 9 · 8 · 7 (exactly 3 calendar days), archive pointer under "Older sessions" unchanged. Always convert to PT before stamping or rolling.
- **D33 (`f871c76c`) falsifies no canonical claim:** the Candy Spread-tab fix repointed `candy_offer_spread_board` to same-copy grain instead of publishing a bid-ask spread between two different NFTs — it changed the board's computation, not its existence, so the chain-strategy prose that names it stays accurate. Its own Recent-sessions + ledger entries are the shipping session's to author.
- **Landed-since, already ledgered (no separate Recent-sessions entry authored here — that's the shipping session's job):** D33 (`f871c76c` + register/ledger `06d597bd`), the D3b wallet-read sargability fix (`39a081f9`), and the unmapped-backlog-growth precompute (migration `20260810030734`, parity file `04b01196`, ledger tip `e62bcd00`) all carry their own ledger entries dated 2026-08-09.

### August 9, 2026 (Claude Code, interactive — drained deep-audit addendum-2 → "clean tree afterwards + explore how to re-enable shell in Cowork") — **2 green ships to `main` (`b83435c4` docs → `ae78fb84` code)** closing D11 plus two siblings of the same class it uncovered, and a root-cause diagnosis of the wedged Cowork sandbox. Clean tree, `tsc` clean. No DB, hot-wallet, auth/`proxy.ts`, or FMV/pricing-MATH change.

- **⚠ DURABLE — a new named defect class with a standing probe: "a failed read served as HTTP 200".** A route returns `{error:"…"}` with status **200**; the caller's idiomatic `if (!res.ok) throw` passes it; the error object is stored as data; it is **truthy**, so every field falls through its `?? 0`. `catch` never runs, so the page's error banner never fires either. **A database timeout renders to users as a measurement.** Same family as D12/D3 ("failure renders as data") but a distinct mechanism — the lie is in the *status code*, not a swallowed fetch.
- **⚠ D11 was NOT AllDay-specific and NOT a data gap.** `/api/collection-stats` showed `TOTAL EDITIONS 0 / PRICED 0% / $0` for a collection with **6,190** editions; any collection whose stats RPC times out did the same, AllDay was just the one saturated during the sweep. **The page was already correct** — its null path renders an em-dash, it already had a written error banner ("Couldn't load collection stats right now") that had never once fired, and its `CollectionStats` type already declared `error?: string` that nothing checked. Only the honest status was ever missing. Fixed at the source (503 + `Retry-After` via `lib/api-error.ts`, the D3 precedent — driver message logged, never published) **and** at the consumer, so no future 200-with-error-body can resurrect it.
- **⚠ The sweep found two more, and the second is the sharpest instance yet.** `/api/platform-stats` is a byte-identical copy with no runtime consumer — a landmine awaiting its first caller, not a live defect. `/api/support-chat/search-deals` returned `{deals: []}` on failure — **byte-identical to its own genuine "no deals matched" response ten lines below** — so a caller reports "no deals found" for a database outage, a fabricated answer manufactured from a failure; it also published `error.message` (the D3 leak). It now **omits `deals` entirely** on failure, since an absent field cannot be misread as "none".
- **⚠ The tell is NOT the 200 — it is whether the failure shape is INDISTINGUISHABLE from a legitimate success.** Most 200-with-error returns here are deliberate and must not be "fixed": cron routes' `{ok:false, skipped:"throttle_error"}` (the caller is a scheduler and the real status goes to `pipeline_runs`), `bundle_not_supported` on the two pack-EV routes (a successful determination), `track-funnel` invalid-event (telemetry must not fail the client), and `wallet-cost-basis`/`wallet-hold-time` returning `rows:[]` **with an explicit `reason`** for a non-TopShot collection (an honest gap). Recorded with its probe in the register's VERIFIED-CLEAN section.
- **⚠ I claimed `/api/platform-stats` had "zero consumers" and was WRONG** — a route test existed that my grep missed, and the full suite caught the break. Corrected the in-code comment, and applied the lesson immediately by checking for a test *before* touching `search-deals`. A grep proving "no callers" does not prove "no tests".
- **Cowork shell — root-caused, and it is not fixable from this repo** ([docs/handoff-2026-08-09-cowork-shell-recovery.md](docs/handoff-2026-08-09-cowork-shell-recovery.md)). It is a disk-capacity failure on the hosted `/sessions` volume: `useradd` runs during provisioning **before any user code** and must create `/sessions/<session>/`, so at zero free bytes the shell never starts. ⚠ **The proven `/tmp` fallback (which restored full git on 08-05 and 08-07) is now structurally unreachable** — using `/tmp` requires a shell, and the failure moved upstream of the shell, which is why 08-09 failed identically on resume, create and re-resume. ⚠ **The "repo is too big" theory is disproven by measurement: a fresh clone is ~82 MB** (33.4 MB tracked per `git ls-files` + 48 MB history) — it is **`node_modules` at 811 MB**, ~10× the clone. ⚠ **Do NOT measure this with `du -sh` on a working tree — it overstates a clone by ~18×** and appears to confirm the wrong theory: plain `du` reports 1.1–1.5 GB here, but **1 GB of that is a single gitignored file** (`panini-capture.jsonl`, the residential Panini runner's local capture, `.gitignore:122`) plus 362 MB of per-worker `node_modules`, none of it tracked. Use `git ls-files`, not `du`. Inferred: each session makes a fresh clone under its own `/sessions/<name>/` home (the deploy-split rule) and any session running tests adds ~811 MB, so the checkout is ~9% of a session's footprint and the dependency install ~91% — ≈0.9 GB per session, apparently never garbage-collected. **Recovery: operator deletes old Cowork sessions** (the only step that can break the deadlock, since nothing in-sandbox can run until space exists), then verify with `df -h /sessions` + `du -sh /sessions/* | sort -h` — nobody has yet seen the actual breakdown.
- **⚠ Local-suite tally correction: the documented "3 known local-env files / 7 tests" is now stale.** `worker-test-completeness` joined the class and is **INTERMITTENT** — it passes in **3.66s alone** but times out at vitest's 5,000 ms default under full-suite contention, and it both passed and failed across runs in this one session; its walk grows with the worker count (17 dirs since `atlas-proxy`). Proven decomposition: `INGEST_SECRET_TOKEN=""` clears `api-ingest-backfill`, `--testTimeout=120000` clears `worker-test-completeness`, and the irreducible remainder is exactly `worker-moments-hydrator-handler` (2) + `worker-pack-events-ingest` (3) = 5 tests. Count failing FILES against the list; do not trust a fixed total.
- **⚠ METHOD — vitest SKIPS the coverage report and threshold check entirely when any test fails**, so a red local suite yields *no* threshold signal at all rather than a passing one. To get a real ratchet reading locally you must first neutralize the local-env failures (the two env/timeout levers above, then `--exclude` the two irreducible worker files).

### August 9, 2026 (Claude Code, interactive — "implement a TODO" → in-code backlog drained → "drain a deferred item + address any bugs + look through the roadmap" → "keep going" ×3 → wrap-up) — **1 green DB ship to `main` (migration `20260810030734` + parity file `04b01196`, ledger tip `e62bcd00`)**: precomputed the unmapped-backlog-growth checker so the alert path survives DB saturation. Verified live end-to-end. No hot-wallet, auth/`proxy.ts`, FMV/pricing-MATH, or destructive-SQL change.

- **In-code TODO backlog is (still) genuinely DRAINED** — swept `TODO/FIXME/HACK/XXX` + a wide natural-language net across `.ts/.tsx/.mjs/.js/.sql/.py/.sh/.yml/.cdc`. Every hit is resolved-and-documented, a design/limitation note, or operator-gated (the Panini `docs/drafts/` go-live captures need `pack_id`/`psku`/SPA-header data unobtainable from the sandbox). Did NOT manufacture work; pivoted to the inbox on request.
- **⚠ DURABLE — the alerting path could silently NOT FIRE during saturation, and the fix is the precompute pattern.** `get_pipeline_alerts()` (45s cap) and `rpc_ops_snapshot()` both call `check_unmapped_backlog_growth()`, which ran **two full-table scans of `unmapped_sales`** (~130k rows / 144 MB, planner cost **37,959**) on every call — so under disk-IO saturation that one arm blew the 45s timeout and took the WHOLE alert aggregation down (measured: check-alerts 4×timeout/6h). Fix: relocated the VERBATIM heavy query into a service-role-only `refresh_unmapped_backlog_growth()` writing a singleton cache `unmapped_backlog_growth_cache` (RLS on, anon/auth revoked), hourly pg_cron `rpc-refresh-unmapped-backlog-growth` at `:29` (a minute clear of every existing fixed+interval job); reader rewritten to an O(1) last-good lookup (**cost 37,959 → 0.26**, identical 14-key jsonb shape, ACL unchanged, fail-open). Same single-writer idiom as the nc1 `public_board_snapshots` cache. **Verified three ways:** the `:29` cron fired `succeeded`; `get_pipeline_alerts()` now runs **1.1s** (all other arms index-bounded); and the real `/api/check-alerts` cron logged `ok=true` at 03:15/03:35Z (post-deploy). Security clean (`check_public_security_invariants()` 0, secdef drift 0). Revert path in the ledger + migration header.
- **⚠ Re-measuring beat the stale premise (roadmap §7 in action).** Roadmap-2026-08-03 Gate-1 top item "Candy wallet totals $0 — denormalise Candy FMV into wmc (worst/cheapest defect)" is **already DONE** live (25,375 rows, 0 NULL); Golazos shells **drained** (0 shells, 0.0% FMV-null); dust-floor CLOSED. The Aug-3 figures were stale — do not act on them without a live re-measure.
- **⚠ `rpc_ops_snapshot()` is 57s but has ZERO automated callers** (not in DB, not in any route) → operator-convenience latency, not a production failure. Its cost is `v_rpc_trust_health` (read twice, **4.5s warm** each — dominated by *live-computed* metrics: InitPlan 14 Top Shot offer-freshness **2.3s**/22.8k heap fetches, InitPlan 13 Pinnacle rollup 0.8s — NOT the precompute). Folding those into the precompute is **register D34** territory (actively owned by a concurrent session, sentinel-critical) — left alone to avoid collision. This is the measured pointer for whoever does D34: TS offer-freshness is the #1 live-computed arm to move.
- **Swept the rest, declined with evidence (no churn):** security advisors = 196 `rls_enabled_no_policy` (the intended service-role-only posture, incl. my new table) + 7 SECDEF WARNs already allowlisted (drift=0); performance advisors = 182 `unused_index` + 50 `no_primary_key` (blind index-drops on live prod are risky — idle ≠ safe), 0 unindexed FKs. Remaining real work is register-owned (D34), UI needing rendered-DOM verification the cloud sandbox can't do (UFC dead-market labels on analytics+sets tabs; pack-EV actionable-vs-retired styling), pricing-owner FMV pipeline, or operator-gated (`1941Z` `CONCURRENTLY` rookie indexes). Inbox `2110Z` Candidate 1 drained + archived; `1941Z` left (operator-gated).

### August 9, 2026 (Claude Code, interactive — drained the deep-audit ADDENDUM: "keep going and do all you can, clean tree afterwards") — **6 green ships to `main` (`3bab914c` → `588518d7`)** closing all three addendum findings plus 1 prod migration. Register now **22 open / 19 resolved**. Clean tree, both gates green. No hot-wallet or destructive-SQL change.

- **⚠ DURABLE — a new defect class, now named and given a standing probe: "a live surface reads a RETIRED table."** A retired table does not throw; it returns stale rows or one row, and the surface renders that as current market data. Two live instances, both fixed. The register's VERIFIED-CLEAN section gained the sweep that catches the whole class: *for each table a public surface reads, compare `max(<write ts>)` against the freshness the surface implies* — signature is a table whose newest row predates the last deploy by months but is still joined into a rendered KPI.
- **⚠ D12a — the Top Shot orderbook panel was serving PERCENTILES over ONE 86-day-old row.** `ts_listings` retired 2026-05-26, holds **1 row from 2026-05-15**, and the panel rendered count/min/**median**/**p90**/max ask off it. **The banner made it worse than no disclosure** — it claimed the table is "periodically-refreshed… typically 100-200 listings", so a visitor seeing `1` concluded the market had one listing rather than that the feed died three months ago. ⚠ **The app already contradicted itself**: `SniperFilterBar.tsx:157` calls it "a dead table: 1 row, frozen 2026-05-15", and edition/moment pages were already migrated to an em-dash. Section retired with an honest note; `summary.topshot_orderbook` deliberately LEFT in the API response so removing it stays separately reviewable.
- **⚠ D12b — ROOT-CAUSED as the same family as D3/D10/D11, not a data gap.** `/nba-top-shot/analytics` showed `$0.00 / 0 sales` for 30d; `get_daily_marketplace_volume` returns **89,831 sales / $583,208** for that window. The page's `r.ok ? r.json() : null` + `.catch(() => {})` left `marketData` null and **every KPI fell through `?? 0`** — a failure rendered as a measurement, which is exactly why the same collection read `$32,584` in 24h on Overview. ⚠ **Half my patch silently no-op'd on the first pass** (the `setMarketFailed` calls didn't apply), which would have shipped a `kpi()` helper wired to a flag nothing ever set — verified every setter present before committing.
- **⚠ D13 — the addendum corrected its own filing and I re-verified rather than trusting either version.** "Pinnacle FMV 23 days stale, the recompute appears dead" is **false**: 7d writes were `pinnacle-fmv-recalc` **27,620**, `listings-indexer` 3,806, `catalog-floor-refresh` 63,499, `pinnacle_fmv_history` 4.6h old. The real defect is that `get_collection_stats(text)`'s Pinnacle arm reads the superseded `pinnacle_editions` and fills the **FMV** outputs from **`ask_price` / `ask_updated_at`** — an ask feed's age labelled FMV. Page said **28% coverage / FMV AGE 23D / PIPELINE OUTDATED**; truth is **69.4% / 4.7h**. So the page was **understating our own data quality and flagging itself broken** — the *pessimistic* direction of the "instrument lies about its own state" class, which still corrodes trust in a healthy pipeline.
- **⚠ The restraint on D13 is the point.** `pinnacle_editions` is EDITION-grain (527), `pinnacle_catalog` RENDER-grain (2,457) — swapping `edition_count` moves a public headline **4.7×** and is the D20 merged-denominator trap. The numerator was instead **bridged onto the existing denominator** via `pinnacle_catalog.legacy_edition_key`, keeping both on one basis. `edition_count` / `sniper_deals` / `top_sales` / `tier_breakdown` still read legacy — including the five "cheapest asks" that are the 23-day-old feed with **140 of 328 exactly $1** (the uniform-$1 Flowty artifact) — queued as **D13b** to be repointed as ONE coherent change.
- **⚠ D19 — my own original filing was wrong and would have invited simplifying a load-bearing guard.** Re-measured direct from `sales_2026` (not `pipeline_runs_daily`, which **disagrees with the table**): **485 UFC rows ingested in 7 days, every one `sold_at` ≤ 2026-04-17**, zero post-close sales. Sharper than the addendum: over 30 days **all 485 landed in a SINGLE burst on 2026-08-05, zero on the other 29** — and a burst is precisely what an ingest-time predicate misreads as a revival spike. **The `sold_at` keying is validated; only the stale "~200 rows/24h" number was corrected.** ⚠ Side finding → D32: no pipeline's `rows_written` accounts for those 485 rows; read the `sales` table, not the rollup, before calling a backfill inert.
- **⚠ Did NOT mark D15 resolved** even though a concurrent session's `unmapped_backlog_growth_precompute_cache` landed for it — only **one** `check-alerts` run had executed since (1 ok / 0 fail). One sample inside the old range proves nothing (the jobid-236 trap); flagged "fix landed, NOT yet confirmed" with a 24h re-probe instead.
- **Verification.** `tsc` clean; component gate **164 files / 1099 tests, exit 0**; unit suite fails only the same **3 known local-env files / 7 tests** (`api-ingest-backfill`, `worker-moments-hydrator-handler`, `worker-pack-events-ingest`) proven earlier to be byte-identical to the pre-session commit and green in CI. 3-day migration parity re-verified clean after adding the new migration.

### August 9, 2026 (Claude Code, interactive — "analyze test coverage → propose → do all that make sense → keep going → do what you think is best → wrap up") — **9 green test-only ships to `main` (`c6fd6ead` → `ec6a149c`)**, both coverage gates green + `tsc` clean throughout, each round ledgered. One byte-identical refactor; no runtime/DB/prod-state change (except that one page now imports a lib).

- **Coverage moved (session totals): component gate branch 67.74→72.76% / stmts 79.92→83.05%; primary gate branch 77.06→77.21% / stmts 90.61→90.76%.** The component gate's historically-thin ~0.7-pt ratchet buffer (a flagged concurrent-push risk) is now ~5.8 pts. Component thresholds were NOT raised this session — left additive so concurrent sessions' merges don't red on a tight margin (lesson `47f901a1`); a future session can ratchet them up to lock the floor.
- **Structural gaps closed:** the ONE logic-bearing lib module with no importing test — `lib/active-collection.ts` — is now covered by a **jsdom-tagged `.test.ts`** (runs under the PRIMARY config, so the localStorage/SSR branches the node-env gate can't reach are exercised). A structural sweep confirmed **0 of 449 API routes untested** and **0 un-gated component subtrees** — no other structural gaps remain.
- **Indexer dark paths (ufc + golazos, the two lowest-branch indexers):** the Cadence-borrow-SUCCESS path (resolves a wmc-miss edition on-chain, writes `nft_edition_map` — the self-healing production path) + the V1 `cached_listings_v2` price/seller hit. ⚠ **DURABLE: golazos `runScript` decodes `JSON.parse(res.text())` (bare base64 string) while ufc reads `res.json()`** — a shared borrow fixture must return the RAW base64 string to satisfy both; a `{value:…}` object only satisfies ufc.
- **Concierge `check_wallet`:** the live-walk fallback (`source: live_walk_first_page`, asserted NOWHERE before) + username recovery via the live resolver on a cache-RPC miss. Deliberately stopped there — a status-string sweep confirmed all arm-level statuses are already covered; the rest is the ~450 deep per-tool fixtures the config flags as low-value to force.
- **Pack-EV engine extracted to a tested lib (the one deferred item, later done on request):** `computeTopPulls()` in `lib/pack-dist-odds.ts` (now in the primary gate), lifted byte-identical out of the 2,760-line, gate-unmeasured `pack/dist/[distId]/page.tsx` — owns the denominator choice (cached `totalUnopened`→full-pool weight→null, never the inflated top-50 sum, Pack B2), the `FMV × drop_weight/denom × slots` EV (reconciles with the Gross EV KPI, Pack D3), and the sort. +7 unit tests; the page now fetches rows and calls it. Byte-identical → no rendered-DOM check needed.
- **Component gate — the 8 biggest gaps driven** (via subagents, each output verified green + `tsc`-clean before commit): CollectionMomentTable (FMV-alert popover + acquisition chips), WalletProfile (position-transfers subtree), TrophyPickerModal (manual-entry tab + pin), PackSniperClient (filters/sorters/mobile — `window.matchMedia` must be stubbed in jsdom), SetsDashboard, ListingsDashboard, FastBreakClient (save() toast matrix), SqueezeBoardClient (refetch/drill-down effect), PaniniSqueezeClient (the whole coverage-banner disclosure chain — dark because `renderBoard` passes `coverage=null`), CandyBoardClient (Spread `belowAskPct` sort legs). PopularOnCollection is an async server component (can't render in jsdom) → only its `distinctSlugLinks` helper is testable.
- **Method reconfirmed:** run the two gates into SEPARATE `--coverage.reportsDirectory` dirs (they collide on `coverage/`); a jsdom-tagged `.test.ts` (not `.tsx`) lands a browser-branch test in the PRIMARY gate; `git push | tail` masks the exit code; ledger is concurrent-write-heavy (rebase-on-reject, splice the freshly-read top, `grep -c '^### '` up by exactly the delta — held across ~4 rebases here); for a test-only push commit tests first + ledger as the docs-only TIP to suppress the Vercel deploy, but for a real source change (the extraction) commit ledger FIRST so the code commit is the tip and deploys. **Left as the deliberate not-forced tail:** deep per-tool support-chat fixtures + the live Flow-GraphQL/Cadence inline route bodies (base topshot/allday indexers, fmv-recalc, ingest).

### August 9, 2026 (Claude Code, interactive — "do everything in the deep-audit register you can possibly handle, don't stop until you've exhausted your capabilities") — **10 green ships to `main` (`55db0ce9` → `1744ebe5`)** draining **17 of the 38 register findings** plus 2 prod migrations, each ledgered with a revert path. The register (`docs/audits/deep-audit-register.md`) is the durable artifact: **24 open / 16 resolved**, with 3 impact claims corrected and 3 new follow-ups. No hot-wallet or destructive-SQL change.

- **⚠ The handoff's NO-PUSH blocker did not apply** — it was the *Cowork sandbox's* git proxy, and Trevor's box carries the PAT in `remote.origin.pushurl`. The 7 untracked audit docs landed first (`55db0ce9`) so nothing was lost, and the lost **5-prod-DB-changes ledger entry** from `docs/wrapup-2026-08-09-ledger-and-claudemd.md` was spliced in — those migrations had **no revert path in either place**.
- **⚠ DURABLE — D3 was NOT "the Set Tracker is slow", it was "the Set Tracker is dead on three collections, for every wallet".** `get_topshot_set_progress` blew its own 25s cap **for a wallet holding ONE moment**, so the cost was fixed and catalogue-wide. Root cause: **`lower(wmc.wallet_address) = lower(p_wallet)` is not sargable** — wrapping the COLUMN defeats every `wallet_address` index, so the planner inverted the join and probed the 2.2M-row wmc once per catalogue edition (cost **124,243 → 2,551**). `lower()` on the column was superstition: across all 2,211,030 wmc rows the **only** non-lowercase values are the 25,375 candy_mlb rows (Solana base58 is case-sensitive by design); every Flow collection is 0. **All Day's tracker was equally dead** and the audit never covered it (its rendered-DOM pass skipped the AllDay tabs). Verified TS 254 sets / AllDay 363 / UFC 256 — the last two match `analytics_sets_summary` exactly. ⚠ The intended `lower()` functional index from migration `20260704000200` **does not exist in prod** — repo↔DB drift — which is why the predicate had nothing to use. **11 more prod functions still carry it (D3b).**
- **⚠ DURABLE — "four drifted copies of a constant" (D23) was filed as hygiene and was two LIVE data defects.** The FMV dashboard's TitleCase `TIER_ORDER` never matched the RPC's UPPERCASE enum, so **every row bucketed into one gray "Other" bar** — and its unit test passed because **the fixtures were TitleCase too**, a fixture that cannot fail. The sets dashboard's `tierMixTotal` sums *only* over its list, which omitted `uncommon` + the UFC ladder: **AllDay's tier mix totalled 5,560 of 6,190 editions; UFC's showed 2 of 446.** New `lib/tier-order.ts` exposes **RANKS, not an array**, so collection-disjoint tiers (FANDOM vs UNCOMMON) share a rank instead of being given an invented order.
- **⚠ DURABLE — the "failure renders as data" family claimed three more victims, all fixed.** D3: `/api/sets` returned `err.message` and the page renders `body.error` verbatim, so `canceling statement due to statement timeout` was shown to anonymous visitors — **and that leak was introduced by an earlier fix** that replaced `[object Object]` with the real message without asking whether the real message was publishable (new `lib/api-error.ts`, 503 + Retry-After). D10: `get_series_detail` returns a **populated** row for `series-4` (3,596 editions) — the page collapsed *error* and *absent* into `return null` → `notFound()`, **defeating the layout gate that fails open precisely to avoid soft-404ing real pages to Google**; already fixed on set/player/team 2026-07-14, `edition` (largest route) and `series` were missed. D9: `NetOfFeesNote` used `flipsNegative` as a *sign flag*, printing `net +$0.25` where the buyer loses $0.25 — on ~199 of 200 rows; **its old test passed on an impossible fixture**.
- **⚠ Three audit claims were CORRECTED against evidence rather than implemented.** **D4** — did NOT flip the Verified-FMV default: 54% of TS listed editions are HIGH/MED, so the gate is not broken; the board's rows are ask-derived (FMV *is* the ask, hence D9's zero spread), and defaulting them visible would headline fabricated signal. Fixed the empty state instead, which blamed "your filters" for a default the user never set. **D26** — "splits each player's catalogue" is not observable: 3 of 4 twins hold **0 editions**, and `get_player_detail` dedupes via a designed tie-break ladder and doesn't count by `player_id` at all (returns 59, more than both rows combined). **D21** — the P1 escalation condition was *checked* and not met: all four `edition_offers` consumers are collection-gated.
- **⚠ D34 was declined with its prerequisite named, not left as an open ask.** `rpc_trust_health_precompute_refresh` runs at ~569s of a 600s budget with **only 3 EXCEPTION handlers across 7 legs** (verified `regexp_count(prosrc,'EXCEPTION')`=3), and Pinnacle FMV lives in a different table so there is no leg to piggyback on — the fn must be split per-leg first.
- **Method notes.** ⚠ **`npm run test:coverage | tail` masks the exit code** — it reported success while 8 tests failed; re-run unpiped. Of those 8, **7 pre-exist the diff** (proven: `git log` shows this session touched neither the 3 test files nor their subjects, and CI shows `1 failed | 1105 passed`, so they are the documented local-env class and green in CI) and **1 was my own D27 fix correctly tripping the `RAW_FMV_DESC_ALLOWLIST` no-stale-entries guard** — the allowlist working as designed. ⚠ **Re-probe every register item before working it**: D22 was already fixed by a concurrent session (`46b0fb9f`) and most of D38 already drained. Ledger discipline held across 10 rebases (splice before line 11, never onto line 9; `grep -c '^### '` up by exactly the delta each time).

### August 9, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only maintenance on tip `3946753e`: independently re-verified every checkable canonical-reference fact against the repo and made ONE genuine correction — the worker count/table. No product/DB/prod change; direct to `main`. Ledger entry 2026-08-09.

- **Corrected the worker count: `16` → `17` and added the `atlas-proxy` row to the Cloudflare Workers table.** `workers/atlas-proxy/` shipped 2026-08-09 (the Sentinel-triage entry below records it) — a Dapper Atlas marketplace pass-through so `topshot-active-listings-ingest` can reach Atlas from a non-WAF-blocked IP (the GHA runner is `egress_blocked` ~83%). Marked **INERT**: not yet `wrangler deploy`'d, and Cloudflare-egress-to-Atlas is UNVERIFIED (different WAF than the GQL host topshot-proxy unblocks) — its README says probe before wiring the runner. Auth `X-Proxy-Secret` (reuses `TS_PROXY_SECRET`'s value). The "16 workers" mentions inside the frozen dated session entries were left verbatim (correct as of their tip).
- **⚠ DATE FOOTGUN, live: the harness stamped "today = 2026-08-10" but that is UTC — it is still 2026-08-09 18:20 PDT.** Confirmed by hand (`date -u` = Aug 10 01:20Z; −7h PDT = Aug 9 18:20). So this is an **Aug-9** entry and **NO tail-roll is due** — Recent sessions correctly holds Aug 9 · 8 · 7 (exactly 3 calendar days). I initially rolled the Aug-7 block on the false Aug-10 reading and reverted it; the archive pointer stays "August 5 → August 1". This is exactly the trap line 108 warns about — always convert to PT before stamping or rolling.
- **Everything else re-verified CURRENT on this tip (`3946753e`), no edit due:** 37 Vercel crons (crons array) · 122 DB-invariant pins (raw `grep -cE 'fn:'` = 123, one two-fn pin counted twice) · both launch flags (`CANDY_MLB_PUBLIC` / `PANINI_PUBLIC`) `true` · 8 collections defined / 5 published · 20 GHA workflows (17 scheduled / 3 unscheduled) · 8 CI jobs (typecheck · cadence-lint · cadence-escrow-tests · unit-tests · component-tests · db-tests · ledger-guard · edge-deno) · concierge model `claude-sonnet-4-6` · primary coverage thresholds 89.3/75.1/91.5/91.6 · component thresholds 79.0/67.0/78.8/83.2.
- **Landed-since, already ledgered (no separate Recent-sessions entry authored here — that's the shipping session's job):** the honesty-claim corrections (`3946753e`), the Sentinel-triage D22 crash guard (`46b0fb9f`) below, and the other 2026-08-09 waves all carry their own Recent-sessions entries and ledger entries.

### August 9, 2026 (Claude Code, interactive — Sentinel-alert triage + even-hour MV-cron stagger, resumed from compaction → "keep doing all you can, don't work the deep-audit register — a concurrent session owns it" → wrap-up) — verified a prod cron change from earlier in the thread, shipped **1 green correctness fix (`46b0fb9f`, D22)**, and confirmed the remaining Sentinel items are operator-gated. Ledgered. No FMV/pricing MATH, auth/`proxy.ts`, or hot-wallet change.

- **⚠ DURABLE — `rpc-refresh-market-index-daily` (pg_cron jobid 235) runs every 2h *on purpose* despite the "daily" name; do NOT "fix" the cadence to daily.** Its `mv_topshot_market_index_daily` is watched by `board_mv_refresh_stale_hours` which **breaches at 8h**, so daily (24h) would breach on the first missed tick. Migration `20260809145945` already tabulated the options and chose 2h deliberately (3 consecutive 2h failures reach the 8h edge at p≈0.46%/cycle; 3h/6h breach). I had earlier flagged the name/cadence mismatch as a follow-up — **that flag is wrong and is retired here.** The real lever the migration itself names is the `ed_med` split (removes work, not frequency), which a concurrent session **already shipped** (`20260809200134` + `20260809200600`, jobid 236 now ~9s vs 300–460s; migration files present in-repo, no parity gap).
- **Cron-stagger post-ship verification (the 6-job even-hour MV pileup I staggered earlier in the thread to :00/:07/:30/:34/:42/:52).** Held across multiple cycles: the 4 de-pileup'd jobs (236/240/241/237) = **0 failures over 6h** (former-worst 236 now completes ~50s, was 9 fails/3d). The residual failures are **saturation, not stagger collisions** — verified by timing: 235 (kept @:07) hits its 600s ceiling colliding the uncontrolled :10–:15 cluster, and 36's one fail was collateral in a broad 21:13–21:20 global IO-depletion storm that took out ~9 unrelated jobs. Both are the SMALL-tier disk-IO over-subscription the stagger can't fix; more minute-shuffling just whack-a-moles 235. Not re-armed.
- **D22 prototype-key crash guard (shipped `46b0fb9f`).** 4 URL-reachable `MAP[urlParam]` reads on plain-object literals resolved a crafted `constructor`/`__proto__` slug to a truthy `Object.prototype` member, defeating the `!entry`/`notFound()` guard — `/analytics/methodology/constructor` **hard-crashed** (`.paragraphs.map` on the Object ctor); sales/loans `[collection]` soft-broke to "undefined …" pages; `/api/alerts/suggest` leaked a garbage-column query. All routed through the existing `ownLookup()` primitive (`lib/safe-lookup.ts`) + a route regression test. This was a deep-audit-register (D22) item; I landed it before being told the register is a concurrent session's — I did NOT touch the register file itself or pull further D-items.
- **Sentinel dispositions (no more code lever):** `wallet-username-resolver` FIXED earlier this thread (single-sales-scan rewrite, `855ab7a7`). `sync-nba-projections` (NBA offseason + Akamai-blocked upstreams incl. the `rpc-sports-proxy` worker) and `topshot-active-listings-ingest` (`egress_blocked`) are infra-gated — the `atlas-proxy` worker is shipped but **inert until an operator `wrangler deploy` + probe**; nothing further ships from code.
- **Method:** `EXPLAIN`-only profiling is safe under IO throttling (planning does no IO); `cron.job_run_details` timing distinguishes a stagger collision from a global saturation storm (check what ELSE failed in the same minute). Ledger is concurrent-write-heavy — a rejected push → `git pull --rebase`, resolve the top-of-file conflict keeping BOTH entries newest-first, `grep -c '^### '` must go UP by exactly the delta (1152→1153 here after a concurrent D2 entry landed).

### August 9, 2026 (Claude Code, interactive — drained a Cowork wrap-up doc: "commit these two doc gaps you couldn't safely write from the cloud" → wrap-up) — **1 green ship to `main` (`c3e1ce4c`)**: closed the last uncommitted 2026-08-09 DB change + fixed two now-false CLAUDE.md claims. No runtime/code change (one prod-parity `.sql` recording already-live state).

- **⚠ VERIFY-BEFORE-WRITING paid off: the wrap-up doc flagged "5 prod DB changes with no ledger revert path" but 4 were ALREADY ledgered.** A concurrent same-day Claude Code session had drained most of them between the Cowork wrap-up being written and this session. Checked each migration name against `ledger.md` + prod first (`grep -c` per migration; `cron.job` schedules live) — items 1–4 (`20260809145547` ufc-arm-retire/precompute-freshness, `20260809145945` halve-four-hourly-MV, `20260809200134`+`200600` ed_med split) were covered with revert paths. **Pasting the doc's 5-in-1 entry verbatim would have DUPLICATED four entries.** Only **item 5** was a genuine gap.
- **Item 5 = the "held back" fifth cadence cut, later shipped.** `rpc-refresh-pack-reality-top-ev` (pg_cron jobid 241) `15 * * * *` → `15 */2 * * *`, applied to prod as tracked migration `20260809203055` but with **no committed file** — so the new daily `migration-parity` check (07:40 UTC, matches prod migrations to committed files by NAME) would flag it, and the `20260809145945` ledger entry still recorded it as "Not touched, deliberately" (correct as of that tip, superseded here). The object-identity question that held it back (`topshot_pack_reality_top_ev` view vs `mv_topshot_pack_reality_top_ev`) was resolved: they're different objects but the MV is itself watchlisted, so the same `board_mv_refresh_stale_hours` breach-8 arm gates it.
- **⚠ DURABLE — byte-exact migration reconstruction from prod is the parity pattern (reused from the sibling ed_med session).** For a prod migration with no committed file: pull `array_to_string(statements, E'\n')` + its `md5` from `supabase_migrations.schema_migrations`, write the file, and confirm `md5sum` of the local file == the prod `body_md5` (`3001233d…` here) — zero transcription risk, and it documents already-live state without running any new prod change. The Write tool produced no trailing newline, so as-is md5 matched.
- **Two now-false CLAUDE.md claims corrected (both load-bearing):** (a) the precompute bullet's "each leg is isolated (a throw marks only its metric)" — live `prosrc` has **3 of 7** `EXCEPTION` blocks (`fmv_sanity_flags`, `pack_ev_publish_shortfall_pct`, board-liveness); Legs 1/2–5/6/7 have none, so the 08-09 12:58Z Leg-7 `statement_timeout` rolled back **all 18 metrics** (verified via `regexp_count(prosrc,'EXCEPTION')`=3). (b) the `ufc_fmv_stale_hours` bullet's "`ufc_fmv_pct_stale_30d` is unaffected and still live" — RETIRED 2026-08-09, now noted as TRACK-only-still-written.
- **Method:** ledger heading count 1138→1139 (+1, verified), line 9 intact (486 chars — a `rev` on that very long line hung a check; use `awk` substr, not `rev`). No tail-roll (Recent sessions spans Aug 9·8·7). The `.sql` under `supabase/` is NOT docs-excluded by `ignoreCommand`, so this commit does trigger a Vercel build — harmless (no code changed).

### August 9, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only maintenance on tip `1291356b`: independently re-verified every checkable canonical-reference fact against the repo and made ONE genuine correction — the component-coverage thresholds. No product/DB/prod change; direct to `main`. Ledger entry 2026-08-09.

- **Corrected the component-coverage thresholds: `78.6/66.4/78.2/82.6` → `79.0/67.0/78.8/83.2` (the actual on-disk value in `vitest.components.config.ts`).** They were raised by the 08-09 board-client coverage pass (PackSniper/FirstMint/PackDrops/UnderpricedSerials clients — the top Recent-sessions entry, tip's own dated log at the raise); the two canonical-reference lines (the "Testing & CI coverage" React-components bullet + the CI-jobs list) still cited the pre-raise value. Fixed both; the frozen dated session entries that cite the old figure were left verbatim (correct as of their tip). Primary thresholds unchanged (`89.3/75.1/91.5/91.6`).
- **Everything else re-verified CURRENT on this tip (`1291356b`), no edit due:** 37 Vercel crons (crons array) · 122 DB-invariant pins (raw `grep -cE 'fn:'` = 123, one two-fn pin counted twice) · both launch flags (`CANDY_MLB_PUBLIC` / `PANINI_PUBLIC`) `true` · 8 collections defined / 5 published · 16 workers · 20 GHA workflows (17 scheduled / 3 unscheduled — the cron-surfaces section + key-files list already carry `migration-parity.yml`) · 8 CI jobs (typecheck · cadence-lint · cadence-escrow-tests · unit-tests · component-tests · db-tests · ledger-guard · edge-deno) · concierge model `claude-sonnet-4-6`.
- **No tail-roll due:** Recent sessions spans exactly 3 calendar days (Aug 9 · Aug 8 · Aug 7), within the "keep ~3 days" rule — archive pointer under "Older sessions" unchanged. Adding this Aug-9 entry introduces no new calendar day.
- **Landed-since, already ledgered (no separate Recent-sessions entry authored here — that's the shipping session's job):** the PUBLIC-BOARD-CACHING (nc1) roll-out across all 5 hot `/insights` boards (`105deb95` → `c35306e8`), the two 08-09 test-coverage passes (`c88eb32c` → `d364bbf4` and the earlier daytime-handoff drain `ac36cc5a` → `9f93a221`) all carry their own Recent-sessions entries below and their own ledger entries.

### August 9, 2026 (Claude Code, interactive — "analyze test coverage → propose → proceed with all you can, don't stop until exhausted" → wrap-up) — **4 green test-only ships to `main` (`c88eb32c` → `15a6af09` → `11d308d4` → `d364bbf4`)**, ~72 additive tests, each `tsc`-clean + file-green + ledgered with a revert path. Primary gate **90.2→90.45 st / 76.4→76.68 br**; component gate **78.83→79.91 st / 66.65→67.74 br**. No runtime/DB/prod change (one test-config threshold raise).

- **The concierge `support-chat/route.ts` was the single biggest uncovered-branch file in the primary gate (563 unc @ 60% br) and is now the target of THREE waves** (`api-support-chat-tools-populated-2/-3.test.ts` + `api-support-chat-lifecycle.test.ts`): the log helpers, search_catalog/serial/across-collections filters, watchlist/alerts CRUD, check_wallet live-walk, get_collection_snapshot, escalate_to_human dual-channel paging + both-failed telemetry, get_challenges, and the fetchPublicInsight board readers; plus the request lifecycle (greeting variants + streaming, quota 429, trusted-bot DM history, model_error telemetry). ⚠ **DURABLE — `ctx.userWallet` is SERVER-derived (`deriveIdentity` → `allow_list`), NEVER the client-passed field**, so owner-scoped tools (manage_watchlist/manage_alerts) need a signed-in (`authedEmail`) + `allow_list` fixture, not a body `walletAddress`. The proven harness for all of this is `helpers/route-harness.ts` (`makeInstrumentedSupabaseFixture` + `installFetchMock`) + `helpers/anthropic-fixture.ts` (scripted tool-use turns incl. `{error:{status,type}}`); assert on the `tool_result` JSON the handler feeds back to iteration 2.
- **The component gate had a razor-thin 0.25 buffer over its ratchet — a flagged operational risk on this concurrent-push repo — now widened to ~1.3** by covering the **four biggest UNTESTED `/insights` board clients** (`PackSniperClient` 949 LOC/44.7% br, `FirstMintBoardClient`, `PackDropsBoardClient`, `UnderpricedSerialsBoardClient`). Thresholds raised **78.6/66.4/78.2/82.6 → 79.0/67.0/78.8/83.2** to lock in a floor while keeping churn headroom. ⚠ **DURABLE — these board clients ALWAYS refetch on mount** (`showHighVariance`/drill-down/collection defaults), so the jsdom fetch stub must return the row set the test asserts on, not the initial props; and `PackSniperClient` reads `window.matchMedia`, absent in jsdom — stub it per-test. Component-gate test PRESENCE is now near-complete: a live cross-reference found only ONE untested logic-bearing gate component left (`components/auth/SignOutButton.tsx`, low-value); remaining component gaps are DEPTH in already-tested files.
- **`allday-pack-ev/route.ts` (pure DB+compute, no Flow calls, 56.8% br):** `api-allday-pack-ev-nodes.test.ts` makes the edition node MUTABLE (the sibling test used one fixed node) to drive the previously-dark pure helpers — the full `bestPrice` price-source ladder (pack_wap→market_wap→ask×0.95→last_sale×0.80→none, asserted via `topPulls[].priceSource` + grossEV), `serialPremiumLabel` combos, `normalizeTier` breakdown keys. ⚠ **DURABLE — the route caches EV by `packListingId`**, so each case MUST use a unique id or it silently reads the first test's cached node; grossEV is rounded to 2dp (assert `toBeCloseTo(x, 2)`).
- **Where I stopped (deliberate, not arbitrary):** the remaining gaps are lower-value DEPTH work — deepening already-well-tested files (`badge-sync`, `CollectionMomentTable`, `WalletProfile`, `TrophyPickerModal`) for marginal branches — or driving Flow/Cadence-inline indexer code that this file documents as "no clean transferable win" (the ufc/golazos/base sales indexers don't share AllDay's concentrated mapper). Padding those adds risk/noise without signal. **Method reconfirmed:** run the two coverage gates into SEPARATE `--coverage.reportsDirectory` dirs (they collide on the shared `coverage/` if run concurrently); rank targets by UNCOVERED-BRANCH COUNT (not %); `tsc --noEmit` before every push (vitest doesn't run it); ledger is concurrent-write-heavy (re-read fresh before splicing, `grep -c '^### '` up by exactly the delta) and the code/tests commit first with the docs-only ledger tip last so a test-only push suppresses the Vercel deploy.
### August 9, 2026 (Claude Code, interactive — "implement a TODO" → in-code backlog drained → "tackle a queued deferred item" → PUBLIC-BOARD-CACHING → "now do panini-squeeze too" → wrap-up) — **shipped the nc1 PUBLIC-BOARD-CACHING item end-to-end across all 5 hot `/insights` boards** (`105deb95` → `c35306e8`, ~7 green commits), each CI-green + `tsc`-clean + full-ratchet-green + ledgered. One additive migration (`public_board_snapshots`, service-role-only). No auth/`proxy.ts`, hot-wallet, or FMV/pricing MATH change.

- **The in-code TODO backlog is (still) genuinely DRAINED** — every remaining `TODO` is resolved, a test fixture, or operator-gated (Panini captures / Candy secrets). Confirmed via full sweep; did NOT manufacture work from comments. Pivoted (Trevor-directed) to the top queued item instead.
- **The feature.** New `public.public_board_snapshots(board_key pk, payload jsonb, row_count, refreshed_at)` (RLS on, anon/auth SELECT-REVOKED, service_role only — verified `has_table_privilege` false + `check_public_security_invariants()` 0). `lib/insights/board-cache.ts` is the core: **single-writer model** — consumers call `readBoardOrLive(key, live)` (fresh-snapshot → live → **stale-fallback** ladder, never writes); only the cron calls `warmBoard(key, live)` (writes on success). Every op FAIL-OPEN, so worst case = pre-cache behavior. Per-board default builders in `lib/insights/{boards,candy-board,panini-board}.ts` (also ended the page↔route query duplication). Writer: `/api/cron/refresh-insights-cache` (`*/5`, vercel.json 36→37). ISR pages read the snapshot so the heavy backing-view query moves off the user-facing render path entirely.
- **⚠ DURABLE — the Vercel-cron auth footgun bit AGAIN (verified live):** a new cron route accepting ONLY `INGEST_SECRET_TOKEN` 401s every tick — **Vercel injects `Authorization: Bearer $CRON_SECRET`**. Accept EITHER secret, mirroring `/api/cron/warm` (the canonical pattern: `(cronSecret && auth===Bearer cronSecret) || (ingest && auth===Bearer ingest)`). My snapshot cache stayed empty for 11 min until this was fixed.
- **⚠ DURABLE — multi-section board warm gates differ by failure semantics, and the choice is load-bearing.** `candy-mlb` (10 views via `Promise.all`) gates the warm on the **PRIMARY Market section only**, NOT all-10 — live evidence showed that under sustained saturation even single-view boards warm ~1/tick, so an all-10-healthy gate would almost never fire exactly when the board most needs a cached copy; a degraded peripheral section rides honestly in `payload.degraded`. `panini-squeeze` (PAGINATED `.range()` over `panini_squeeze_board ORDER BY fmv_usd DESC`) gates on a **COMPLETE page-set** (`ok = rows.ok && !partial`) — STRICTER — because a truncated fetch is a *corrupted ranking* (top-N rendered as the whole board), not merely short, so a partial is never cached. Both verified warming live (candy 125, panini 4,472).
- **⚠ DURABLE — `deals` + `rookies` public boards are I/O-BOUND, not bad plans; they 500/time-out first under disk-IO saturation.** Profiled (safe read-only `EXPLAIN`): `topshot_2025_rookie_index` cost **~79,595**, ~76k in ONE node — the max-mint-#1 lookup scans **all 8 `sales_20xx` partitions** filtering `serial_number=1`, and `sales_2020–2025` LACK the `idx_sales_YYYY_serial1` partial index that `sales_2026/2027` have (cost 52–248/partition vs 15). `cross_collection_deals_board` cost **~20,149** (per-edition `fmv_snapshots` probes + 8,449-row `cached_listings_v2` scan). **Ready fix queued in `docs/overnight/inbox/2026-08-09T1941Z.md`**: `CREATE INDEX CONCURRENTLY … serial1` on 2020–2025 (mirrors the proven 2026 index) — but ⚠ **CONCURRENTLY only** (historical partitions still take daily history-backfill writes), **SQL-editor/quiet-window only** (can't via MCP; and building under active saturation worsens the very throttling). deals needs a materialized latest-FMV-per-edition (the standing `/api/market` precompute item). Do NOT bump statement_timeout / upgrade tier / serialize the cron (per-view cold I/O, not contention — panini's 4,472-row fetch succeeds while deals' 37-row fails in the same tick).
- **The cache demonstrably did its job under a multi-HOUR saturation spell:** `deals`/`rookies` sat ~60 min stale but their pages served honest last-good data (`fetchedAt`-stamped, `meta.cache_stale`) instead of live-timing-out or rendering a truncated ranking. Non-regressive by construction. Cron run status reads `ok` when ≥1 board warms (per-board detail in `extra`) so routine single-board saturation doesn't log a chronically-red `pipeline_runs` row. **Deliberately NOT built:** API-route/concierge snapshot fallback — `fetchPublicInsight` already fails safe (returns `{status:"error"}`, no crash) and route default-params mostly don't match the cached page-defaults, so it's bespoke risk for modest gain.

### August 9, 2026 (Claude Code, interactive — drained the Cowork daytime handoff `docs/handoff-2026-08-09b-daytime-pass.md` → "work through any issues you find and leave a clean tree") — **3 green ships to `main` (`ac36cc5a` → `18d40be7` → `9f93a221`)** plus the inbox drained to empty. Closed a 2-migration prod/repo drift window, killed the #1 public-page 5xx, and made the public boards stop rendering timeouts as data. No auth/`proxy.ts`, hot-wallet, FMV/pricing MATH, or destructive-SQL change.

- **⚠ The handoff's NO-PUSH blocker did NOT apply here.** It was a *cloud-sandbox* git-proxy 403 ("repo not in this session's authorized set"). Trevor's Windows box carries the PAT in `remote.origin.pushurl`, so pushes worked normally. **Check the actual remote before believing an inherited "cannot push" claim** — the two migrations had been applied to prod ~18h earlier and were sitting uncommitted purely because of that.
- **⚠ DURABLE — the "compute-for-all-then-filter" anti-pattern claimed a THIRD victim, and it was the top public-page 5xx.** `/[collection]/pack/dist/[distId]` threw **81 hard 500s/24h, every one `/nfl-all-day/`**. `v_allday_pack_info` LEFT JOINs a `DISTINCT ON` subquery over `pack_ev_latest`; the `dist_id` predicate lands **above** that `Unique` so it cannot push down, and PG cannot prune the join (LEFT JOIN removal needs a base relation with a unique index, not a subquery). Every AllDay pack page materialized the latest EV for **all 4,245 packs from 119,591 `pack_ev_history` rows** and discarded all but one. **The tell: not one column any caller reads came from that node** — all four consumers read only `mv_allday_pack_ev_corrected` + `allday_pack_supply`. New lean `v_allday_pack_detail_ev`: planner cost **1,195,280 → 7.54**, verified output-identical by a 0-row `EXCEPT` diff over all 3,052 AllDay dists, and the 8 previously-500ing dists now return in **36–883 ms** (live probe: 200 with 132–262 KB of real DOM). When a view is slow, check whether the expensive node feeds any column the caller actually selects before rewriting anything.
- **⚠ DURABLE — a fail-soft `if (error) return []` renders a TIMEOUT as an empty section at HTTP 200.** Measured live: one `/insights/candy-mlb` render logged **six simultaneous 57014s** and still served 200. It is **triply invisible** — the 5xx metrics can't see a 200; the board-liveness probe measures `SELECT count(*)` on the view (a query the planner prunes) so `public_board_slow_count`/`empty_count` both read **0**; and the user just sees a blank section. This is the 2026-08-06 "probe understates by up to 8,300×" finding **confirmed in production for the first time**. New `lib/insights/board-status.ts` + `DegradedDataNotice` make the boards say so; `null` when healthy, so a good render is byte-identical. **A board that SUCCEEDS with zero rows is deliberately NOT flagged** — an empty board is an honest answer.
- **⚠ CORRECTED the handoff on two points.** (1) It said "panini-squeeze and first-mint do not appear in any failure log" — **`panini-squeeze` fails repeatedly** (`backing view error: canceling statement…`), and its variant is the sharper one: it pages 10×600 `ORDER BY fmv_usd DESC`, so a page-3 failure renders the top ~1,800 rows **as though they were the whole ranking**. (2) The daytime monitor's prescribed build fix targets code that is **already fail-soft** (`loadSet` try/catches to `null`; `generateStaticParams` try/catches to `[]`) — so the throw that reds the build is elsewhere, and implementing its suggestion would close the ticket without fixing anything.
- **`/api/market` profiled, NOT rewritten (queued).** Worst timeout route (51×504/24h); shares `get_topshot_sniper_deals` with `/api/sniper-feed`. Measured **13,989 ms cold / 9,768 ms warm** against a 30 s statement timeout AND `maxDuration = 30`. ⚠ **Its plan is already index-optimal** (all index scans, cost 8,029, partition pruning working) — this is NOT the compute-for-all shape, so a rewrite would find nothing to remove; the cost is ~1,645 random probes into partitioned `fmv_snapshots` that 512 MB `shared_buffers` can't hold. Also note the fn has **no `listed_desc` arm**, so the route's default sort falls through to `discount_pct DESC` — **fmv-dependent**, which is why the 08-08 LIMIT-pushdown trick does not transfer. Lever is precompute/caching, not SQL.
- **⚠ DURABLE — the trust board is COLD-CACHE-bound, and this CORRECTS a claim I put in the ledger earlier the same day.** I had recorded as "reusable" that `SELECT … FROM v_rpc_trust_health WHERE metric IN (…)` "returns instantly — Postgres prunes the UNION ALL branches". The pruning half is true; **"instantly" is not** — I had happened to test two cheap arms. Measured properly: the SAME 10 arms cost **19,002 ms cold and 80 ms warm (237×)**. So per-arm cost is near-zero once resident and the whole cost is physical I/O (12 GB DB, 512 MB `shared_buffers`, depleted IO budget) — corroborating the standing "trust-health cost is not time, it's I/O" note. **Consequence: chunking `/api/sentinel`'s whole-board read would NOT bound its time** (a cold chunk still costs seconds), so I measured that idea and discarded it rather than shipping it. Precompute remains the only lever — but still cannot be done naively, since `rpc_trust_health_precompute_refresh` already runs at ~569 s of its 600 s budget and rolled back all 18 metrics on hitting the ceiling at 12:58Z that day.
- **Shipped the containment for the push-outage class: `migration-parity` (daily 07:40 UTC, reporting-only).** Prod migrations that never got a committed file — the exact harm behind this session's opening state. ⚠ **It matches on NAME, not version**: `apply_migration` stamps its own version, so my own migration landed in prod as `20260809155541` while the committed file is `20260809170000_…` — a version-keyed check would flag it missing forever. ⚠ **The window is bounded on purpose**: prod holds **2,478** migration rows vs **402** committed versioned files, so an unbounded check reports ~2,000 findings and becomes noise; at 14 days it found **114** (a real pre-existing backlog, now queued), at 3 days exactly **2**. It reports rather than fails for the same reason a permanently-red trust arm is harmful. ⚠ Windows footgun: `process.exit()` mid-stdout-flush trips a libuv assertion and the shell sees **127** — use `process.exitCode`.
- **Method notes.** `git push | tail` masks the exit code — and so does `npm run test:coverage | tail`: the first ratchet run reported "exit 0" while 7 tests were failing (it was `tail`'s status). Re-ran unpiped → real exit 1, then **proved the 3 files / 7 failures pre-exist my diff by stashing and re-running** (5× 5 s timeouts + 2× `expected 401` = the documented local-env class, green in CI). Both coverage gates green otherwise (component ratchet exit 0, 982 tests). Inbox archival: **check `archive/` for a same-named file before `git mv`** — two inbox entries were truncated "PROCESSED" stubs whose full copies were already archived, so moving them would have clobbered the good ones; deleted instead, as their own bodies instructed. Live verification beat inference throughout: the insights banner was confirmed by fetching the rendered page and reading the actual copy ("3 of 10 sections could not be loaded (Pack market, Serials, Scarcity)"), which is also how the "1 of 1 **sections**" plural bug surfaced.

### August 9, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only maintenance on tip `325d6f68`: independently re-verified every checkable current-state fact against the repo, found all CURRENT, made no factual edit and no tail-roll (the file was already at the most recent state — the current tip *is* today's coding wrap-up). No product/DB/prod change; direct to `main`. Ledger entry 2026-08-09.

- **All canonical reference facts re-verified current on this tip (`325d6f68`), no edit due:** 36 Vercel crons (crons array) · 122 DB-invariant pins (raw `grep -cE 'fn:'` = 123, one two-fn pin counted twice) · both launch flags (`CANDY_MLB_PUBLIC` / `PANINI_PUBLIC`) `true` · 8 collections defined / 5 published · 16 workers · 19 GHA workflows (16 scheduled / 3 unscheduled) · 8 CI jobs (typecheck · cadence-lint · cadence-escrow-tests · unit-tests · component-tests · db-tests · ledger-guard · edge-deno) · concierge model `claude-sonnet-4-6` · primary coverage thresholds 89.3/75.1/91.5/91.6 · component thresholds 78.6/66.4/78.2/82.6.
- **No tail-roll due:** Recent sessions spans exactly 3 calendar days (Aug 9 · Aug 8 · Aug 7), within the "keep ~3 days" rule — archive pointer under "Older sessions" unchanged.
- **Landed-since, already ledgered (no separate Recent-sessions entry authored here — that's the shipping session's job):** the Aug 9 acquisition-method `mint`-drop fix across all three map layers (`595dce9c` → `a4a7237e` → `f7d52185`), the test-coverage + server-page-extraction pass (`e35b71e9` → `0fffb2ae`), and the Golazos wallet-search + odds-proxy `/scores` wave (`01f98bb5` → `8788a59e`) all carry their own Recent-sessions entries below and their own ledger entries.

### August 9, 2026 (Claude Code, interactive — "implement a TODO" → "keep going / make sure green / fix any issues" → wrap-up) — **3 green ships to `main` (`595dce9c` → `a4a7237e` → `f7d52185`)**, all 8 CI jobs green + `tsc`-clean + local full-coverage-ratchet green (9,542 tests) + each ledgered with a revert path + all 3 deployed READY (tip `dpl_J8QN…`). One user-visible data-accuracy bug fixed at three layers. No FMV/pricing MATH, auth/`proxy.ts`, hot-wallet, or DB/prod-state change.

- **The in-code TODO backlog is genuinely DRAINED** (dual-verified: my own sweep + an independent Explore agent). Every remaining `TODO`/`FIXME`/stub is resolved, an intentional guard, operator-secret / captured-data gated (Candy `CANDY_MLB_*`, Panini `docs/drafts/` captures), or inert-by-decision (`golazos-offers-indexer`). Do NOT manufacture work from the remaining comments. Instead closed the real gap the analytics "Acquisition history — coming soon" copy hinted at.
- **⚠ DURABLE — `moment_acquisitions.acquisition_method` is CHECK-constrained to 10 values (`marketplace, pack_pull, loan_default, gift, challenge_reward, airdrop, unknown, flowty_purchase, offer_accepted, mint`), and THREE duplicated inline maps all silently DROPPED `mint`** — Pinnacle's primary-acquisition method (5,622 live rows) — plus `flowty_purchase`/`offer_accepted`. So a Pinnacle wallet's minted Pins vanished from the "Portfolio Origin Story" (rendered an all-zero breakdown + empty bar for a wallet holding 511 Pins — verified live on `0xa9435b88c3798c19`, `get_acquisition_stats` → `[{method:"mint",count:511}], total_moments:511`) AND showed no per-row acquisition badge. The three copies: (1) the aggregate `{pack_pull,marketplace,challenge_reward,gift}` bucketing in `app/api/analytics/route.ts` + `app/api/wallet-search/route.ts` + `app/api/collection-moments/route.ts`; (2) the per-row label maps `ACQUISITION_LABELS` (wallet-search) + `LABEL_MAP` (`collection/page.tsx`); (3) `ACQUISITION_LABEL_MAP` in `lib/collection/server-moment.ts` (the shared `serverMomentToRow` Collection-tab / wallet-row mapper).
- **Fix — ONE source of truth in [lib/analytics/shape.ts](lib/analytics/shape.ts):** `bucketAcquisitionCounts()` (aggregate) + `acquisitionMethodLabel()` (per-row badge), both `ownLookup`-guarded. `mint`/`pack_pull` → pack bucket + "Pack"; `flowty_purchase`/`offer_accepted` → marketplace + "Bought"; `loan_default`/`airdrop`/`unknown` deliberately left uncategorized. All 3 map copies deleted and repointed. **New code with an acquisition-method lookup MUST use these helpers — never a fourth inline copy.** Byte-identical output for TS/AllDay/Golazos/UFC (they carry none of the folded methods); Pinnacle-only behavior change. +15 unit tests.
- **Method notes:** the aggregate loops used `counts[b.method] = …` (ASSIGN); once two methods fold into one bucket you MUST accumulate (`+=`) — the helper does. `get_acquisition_stats` is already collection-agnostic (reads `moment_acquisitions` by `collection_id`), so the gap was purely code-side bucketing — no DB change. `/api/acquisition-stats` passes the raw breakdown through and was unaffected. CLAUDE.md/ledger are concurrent-write-heavy — the ledger needed a mid-session rebase (kept both entries; `grep -c '^### '` up by exactly the delta each time); the huge `actions_list` MCP result overflows the token cap → parse the saved JSON with python.

### August 9, 2026 (Claude Code, interactive — "analyze test coverage → propose → do all that make sense → Proceed → Do that" → wrap-up) — **4 green ships to `main` (`e35b71e9` → `762c4c3b`/`4e14ec9a` → `0fffb2ae`)**, all CI-green + `tsc`-clean + each ledgered with a revert path. Additive tests + one behavior-preserving refactor; primary-gate branches **75.68% → 76.34%** (~290 covered branches). No FMV/pricing MATH, auth/`proxy.ts`, hot-wallet, or DB/prod-state change.

- **Concierge (`support-chat/route.ts` 47.0% → 60.0% br, ~180 branches):** new `__tests__/api-support-chat-tools-populated.test.ts` (33 tests) supplies POPULATED per-tool Supabase fixtures so the data-SHAPING success paths run — the "~25 bespoke per-tool fixtures" gap `vitest.config.ts` had deferred. The harness is the sibling `api-support-chat-tools.test.ts` pattern: `install(fixtures)` → `script(tool, input)` → assert on `toolResult()` (the JSON the handler feeds back to the model). The existing `-tools.test.ts` only hit the guard/empty branches; populating the fixtures lights check_wallet (indexed-cache portfolio + collection-detail + best-offer), search_serial_deals (board + listedOnly), compare_pack_value (EV math), the ENTIRELY-dark manage_deal_subscriptions (~50 br: list/pause/resume/delete/create with team-resolution + rookie-badge expansion + default-channel + preview), et al. **This is the single biggest uncovered-branch lever in the primary gate — 745 uncovered branches, 8% of the whole gate's total — and it's cheap: pure fixture work, no route change.**
- **AllDay indexer (`allday-sales-indexer/route.ts` 52.2% → 65.5% br):** +2 deep-test cases driving `buildOnChainEditionRow` (the on-chain edition-metadata mapper, previously entirely dark). ⚠ **DURABLE: `editionsToHydrate` fills ONLY via the Cadence BORROW path (`resolvedEditionID`), NOT the wmc edition_key path** — so to reach the on-chain GET_EDITION_DATA fallback the case needs a wmc-MISS sale + a decoded buyer + TWO ordered `/v1/scripts` results (borrow → editionID, then GET_EDITION_DATA → the `{String:String}` blob). `scriptResult(dict)` in `helpers/flow-cdc-fixture.ts` produces exactly that `{String:String}?` shape.
- **⚠ DURABLE — the ufc/golazos/base sales indexers do NOT share AllDay's concentrated mapper.** They have a borrow path but no `buildOnChainEditionRow` equivalent; their remaining dark branches are scattered low-level `unwrapCdc`/`extractNftTypeId`/venue/price-parse paths, so the AllDay technique yields no clean transferable win there — don't chase it.
- **⚠ DURABLE — `app/**/page.tsx` (117 async server pages) is measured by NEITHER coverage gate** (primary = `lib/** + app/api/**/route.ts + proxy.ts`; the component gate can't render async server pages in jsdom). The fix is the 2026-07-31 extract-to-`lib/` pattern: this session peeled BYTE-IDENTICAL pure logic out of four pages into tested libs — `lib/insights-hub-format.ts` (liveStat), `lib/player-page-view.ts` (buildPlayerSetCards), `lib/fan-teams-format.ts` (teamLogoUrl), `lib/share-card-view.ts` (buildSeriesBars/closedMarketNote), 16 tests. **The big entity pages (pack/dist, moment, edition) are ALREADY well-extracted** — don't redo them; the remaining pages hold mostly JSX-coupled or trivial logic, so further page extraction is low-priority.
- **Method reconfirmed:** the concurrent-write ledger discipline held (re-read fresh before each splice, `grep -c '^### '` up by exactly the delta, `git pull --rebase` between commits); `git push | tail` masks the exit code (checked `${PIPESTATUS[0]}`); commit ledger BEFORE code so the code commit is the tip (the refactor touches pages → Vercel builds; the test-only pushes correctly did not need a deploy).

### August 9, 2026 (Claude Code, interactive — "implement a TODO" → "keep going / everything you can" ×N → wrap-up) — **3 green ships to `main` (`01f98bb5` → `f4a996ac` → `8788a59e`)**, each CI-success + `tsc`-clean + ledgered with a revert path. One worker route, one coverage close, one real feature. No FMV/pricing MATH, auth/`proxy.ts`, hot-wallet, or DB/prod-state change.

- **Golazos wallet analysis is now LIVE via `/api/wallet-search` (`8788a59e`) — the "coming soon" stub was STALE.** Verified live: `wallet_moments_cache` already holds **21,512 Golazos rows / 130 wallets** and `get_wallet_moments_with_fmv` already serves rich Golazos data; the collection "Collection" tab already renders it via `/api/collection-moments`. The gap was only in the standalone `wallet-search` API + its consumers (the wallet tool, `/share`, and the collection page's fire-and-forget **wmc-warm** call — which for Golazos warmed nothing, so fresh Golazos wallets never populated on-demand). Fix: the `laliga-golazos` branch reads wmc via the RPC and maps with the **shared `serverMomentToRow`** (identical shape to collection-moments — safe without rendered-DOM verification, since AllDay already renders the same envelope), and on a wmc miss fires the **existing** `/api/wallet-backfill-golazos` (fire-and-forget) so a never-seen wallet self-warms like TS/AllDay do. **No new Cadence** (reuses the verified backfill pipeline — so the Cadence-MCP-not-connected constraint this session did not apply), no `proxy.ts` change. New `__tests__/api-wallet-search-golazos.test.ts` (7 cases) + updated 2 stale "coming soon" assertions; 37/37 wallet-search + 6/6 mapper green.
- **`odds-proxy` `/scores` route (`01f98bb5`)** — implemented the one genuinely-open documented-but-unimplemented placeholder (shared `proxyToOddsApi` core, no `/odds` behavior change; `daysFrom`/`dateFormat`/`eventIds` allowlist, NO `daysFrom` default so a no-param call stays at 1 the-odds-api credit, 60s cache). ⚠ **INERT until operator `wrangler deploy --name odds-proxy`** — Cloudflare workers don't auto-deploy from `main`.
- **`/api/profile/me` coverage (`f4a996ac`)** — closed the untested open-door `saved_wallets` fallback (was 42.9% br), the load-bearing path for post-2026-07-20 self-serve signups (no `allow_list` row → `wallet_addr` from `saved_wallets`, the field the Pro badge/concierge key on). The prior test's mock returned one result for every `from()`; made it table-aware. Query schema-verified live.
- **Method reconfirmed:** the `actions_list` MCP result overflows the token cap → parse the saved JSON with python; a safe-zone `lib/**` bug-hunt via an Explore subagent found NOTHING genuine (the suite is hardened) — the one borderline `ASK_LABEL[collection]` prototype-key candidate is DEFENDED by the edition page's upstream `if (!coll) notFound()` gate (unlike the old `/[collection]/sniper` page that WAS the `/constructor/sniper` crash), so correctly left alone. CLAUDE.md/ledger are concurrent-write-heavy — re-read fresh before splicing; `grep -c '^### '` must move by exactly your delta.
### August 9, 2026 (nightly autonomous pass, GENUINE OVERNIGHT ~04:28 PDT) — but NO-PUSH (2nd consecutive night): the workspace shell won't start (`useradd … /sessions no space left on device`), so no git at all. Shipped 0 / reverted 0 / repaired 0; post-ship watch of the 08-08 CC wave ALL PASS; outputs written to the mount, UNPUSHED. Full log: `docs/handoff-2026-08-09-overnight-pass.md`.

- **Environment (escalated, unchanged from 08-08):** `mcp__workspace__bash` failed identically on resume/create/re-resume → no clone AND no `GIT_INDEX_FILE` fallback. DB (Supabase MCP), Vercel + Sentry MCP, and file tools (Read/Write/Glob against the mount) all worked; git did not. Provisioning fix needed to restore overnight push capability.
- **Health green-with-known-saturation-noise; trust health IMPROVED 4→2 breaches.** Security 4/4 clean; `detect_stalled_pipelines()` []; 0 new Sentry/24h; DB 12,227 MB (down from 12,444). Fails 360/13,861 = 2.6% in 24h (a saturation window; every top-faller a known lock/statement-timeout class). The 4 pg_cron failures are all the documented disk-IO-saturation MV-refresh cluster (`allday-serial-fmv-jersey`, `ccm-step1`, `misattrib-candidates`, `thin-sale-ask-disclosure`). Two trust breaches, both documented/carried: `panini_sale_price_capture_dry_days`=12 (upstream home-box outage), `unmapped_resolution_backlog_max`=162 (permanent AllDay floor, DRAINING 175→162). Cleared since 08-08: `ufc_fmv_stale_hours` red-by-design (arm re-pointed to `ufc_flow_revival_sales_30d`=0/ok), `public_board_slow_count`→0.
- **Post-ship watch — 08-08 CC wave ALL PASS, 0 reverts.** allday-lock picker fix (`babdc1a1`+`e9d08618`) holding (~69 ms; the 54.7% 2-day rate is pre-fix runs aging out). saved_wallets reconcile (`991291be`) clean. The AllDay locked-moment studio-custody fix (`4b354e0e`) is NOT implicated in the `wallet-backfill-allday` 60 fails — those are `wmc_upsert … lock timeout` on the WRITE side (saturation), distinct from the studio-holdings READ union. UFC arm re-point clean; candy-offers healthy (`unverified_pct`=0, oldest 4.7h); FMV freshness green.
- **Nothing SHIP-eligible.** NO-PUSH blocks all code/deploys; the MV-cluster indexing needs CONCURRENTLY (unavailable via MCP) + competes for the depleted IO budget + is actively CC-owned; the `rpc-ccm-step1` cron stagger stayed declined (speculative + collision risk). **New queue (nc1):** PUBLIC-BOARD-CACHING — back the hottest public `/insights` boards (candy-mlb, panini-squeeze, deals, rookies, first-mint) with a cached/materialized payload so they survive disk-IO throttling; read-path/caching only (LOW) but a CODE design change → CC. Artifacts: 11 enumerated by the monitor, all present, no schema break 08-08 → no repair due; deep validation skipped to avoid piling read load on a saturated DB. Inbox drained (10 files 08-07T2108Z→08-09T0607Z); archival deferred (no shell for `mv`).

### August 8, 2026 (Claude Code, interactive — drained the Cowork handoff `docs/handoff-2026-08-08-stale-saved-wallet-cached-counts.md`) — **2 green ships to `main` (`bd0eb975` ledger, `991291be` code)** plus a prod data reconcile: every user's displayed collection size was stale. CI-green + ledgered with revert paths. No auth/`proxy.ts`, hot-wallet, FMV/pricing math, or destructive-SQL change.

- **The bug: `saved_wallets.cached_moment_count` / `cached_fmv_usd` / `cached_top_tier` — what `/dashboard`, `/profile/<username>`, `/share` and the profile OG card render — had exactly ONE writer, the signup/wallet-association path.** Nothing refreshed them afterwards while `wallet_moments_cache` kept growing via the scheduled walks. Measured live: **99 rows, 21 of 21 users affected, 45 NULL, 41 drifting >5** (Edogg1976 Top Shot NULL vs 12,552; Rigged 33,374 vs 38,097; ThunderHour 50 vs 1,474 — its shallow first-paint value frozen since signup). Reconciled to **0 NULL / 0 drift at any tolerance**.
- **⚠ DURABLE — `aggregate_saved_wallet_stats` structurally CANNOT zero a collection the wallet no longer holds, and that is the sharper failure than the NULLs.** It runs `UPDATE … FROM agg` where `agg` is a `GROUP BY` over that wallet's wmc rows, so a `(wallet, collection)` pair with **zero** wmc rows yields no `agg` row, the join matches nothing, and the stored value survives **forever** — a wallet that sells out of a collection displays its old count indefinitely. Any future "recompute the cards" work must carry an explicit zero pass. NULL→0 is display-neutral (every consumer already coalesces: `?? 0` in ProfileClient / the collection profile page, `Number(…) || 0` in the OG route + `CrossCollectionHoldingsCard`), but it makes `cache_updated_at` a real freshness stamp.
- **Two durable writers (migration `20260809050000_audit_20260809_reconcile_saved_wallet_cached_stats.sql`).** (1) SECDEF `reconcile_all_saved_wallet_stats()` → pg_cron **jobid 259 `rpc-reconcile-saved-wallet-stats`, `33 13 * * *`** (06:33 PT; hour 13 UTC is the only hour carrying no other pg_cron job, deliberately off the `:17`-past and `*/6` pileups that contend for the depleted disk-IO budget). It **loops the existing per-wallet RPC instead of re-implementing the aggregate** — that RPC *is* the definition of these three columns (tier ladder, "0 fmv → NULL"), and a second set-based copy would be a second source of truth; the loop is over the **user** count (21), not the moment count. Verified live: `{"wallets":21,"rows_refreshed":78,"rows_zeroed":0,"elapsed_ms":37241}`. (2) `wallet-backfill-multicollection` re-stamps the wallet's cards at the end of its `after()`, **after** the COMPLETE telemetry row (that row is the load-bearing dispatch-gap signal — ordering is mutation-proven by a test), **gated on the wallet belonging to a signed-up user** because the route also runs for hundreds of unclaimed seeded wallets per 12h sweep and the aggregate is a full per-wallet wmc scan.
- **⚠ FOUND EN ROUTE — `docs/overnight/ledger.md` had been silently corrupting its own newest heading since `af3a30d4`.** A splice welded a `### ` heading onto the **tail of the line-9 "Dates are Pacific" note** rather than its own line, truncating that note mid-sentence (486→344 chars) and leaving the heading not matching `^### ` — so `grep -c '^### '` undercounted by one and the ledger guard's heading-set comparison could not protect that entry. Every later session spliced onto the same broken line, making it self-perpetuating. Line 9 restored verbatim from `HEAD~3`; count went 1106 → **1108** (+1 mine, +1 recovered), no heading lost. **When splicing the ledger, insert BEFORE line 9's newline — never onto it.**
- **Also corrected a FALSE comment** in `resolve-and-associate` claiming "the hourly wmc-fmv-populate sweep and the next dashboard load reconcile it upward" — neither did, and that comment is precisely why nobody looked for ~3 months. A comment asserting a backstop that does not exist is worse than no comment.
- **NOT done (deliberate):** no SQL invariant pin for `reconcile_all_saved_wallet_stats()` — pinning needs a fixture copy of `aggregate_saved_wallet_stats`, whose body calls `auth.uid()`, which is non-trivial stubbing for a display-only function whose worst failure a nightly re-run self-corrects. The handoff's optional "add the drift check to `rpc-pending-signups-watch`" was skipped as moot once the nightly reconcile landed (the handoff says so itself).

### August 8, 2026 (Claude Code, interactive — drained the Cowork handoff `docs/handoff-2026-08-08-allday-locked-moments-invisible.md`) — **2 green ships to `main` (`4b354e0e`, `e0805f0c`)**: the AllDay LOCKED-moment fix, plus a CI unblock found along the way. Both CI-green + ledgered + verified live in prod. No auth/`proxy.ts`, hot-wallet, migration, or DB-schema change.

- **⚠ DURABLE — NFL All Day has NO on-chain locking contract, so a "locked" moment is INVISIBLE to every wallet walk we own.** Only `AllDay` + `PackNFT` are deployed at `0xe4cf4bdc1751c65d` (verified live) — unlike `TopShotLocking`, which flags the NFT *in place* so `getIDs()` still returns it. On All Day, Dapper custodies the moment and it is **absent from the holder's account entirely**. `GET_UNLOCKED_MOMENT_DETAILS` borrows `/public/AllDayNFTCollection` → `getIDs()`, so it structurally cannot see one, and a wallet whose AllDay moments are all locked scans **`ok=true / rows_found=0`** — indistinguishable from an empty wallet. That was the report: `0xdcd41c74d2dd0a66` (ThunderHour) held 5, RPC showed 0.
- **⚠ DURABLE — the handoff's prescribed fix (`searchMomentNFTsV2`) is IMPOSSIBLE; do not re-attempt that lane.** Both AllDay GQL surfaces are dead, re-verified 2026-08-08 **from Trevor's residential IP** (so it is upstream, not a worker-egress block): `public-api.nflallday.com/graphql` → **nginx 404** (path gone), `nflallday.com/consumer/graphql` → **403** block page. Confirms [[allday-consumer-gql-cf1009-blocked]] still holds 45 days on.
- **The fix (`4b354e0e`):** new `lib/chains/flow/allday-studio-holdings.ts` walks the **Dapper studio-platform** `searchAllDayNft` — the same unauthenticated, Origin-header, Vercel-egress-OK endpoint `allday-studio-sales-history-backfill` already uses — and `runAllDayDetailsBackfill` **UNIONs** it with the chain result, opted in per-collection via `config.studioCustodyHoldings` (AllDay only, so Golazos/Pinnacle are byte-identical). Fired in parallel with the chain read (≈no added wall-clock) and handed down to the paginated mega-wallet path, written once at `resumeFrom === 0`.
- **⚠ DURABLE — `owner_address` on studio is BARE HEX.** A `0x`-prefixed query returns `totalCount 0` — a **silent empty, not an error**, so it fails exactly like "this wallet owns nothing". `stripFlowPrefix()` is the single chokepoint; mutation-tested.
- **⚠ DURABLE — studio's `owner_address` CAN BE STALE, so this AUGMENTS and never REPLACES the chain, and never DELETEs a wmc row.** Verified counter-example: AllDay moment `1557801` is held **on-chain** by `0x11859edcf2f53edd` while studio still names a 2022 owner. Chain wins on nftId conflict. Live proof it holds: Levy re-backfilled to **33, not 32**.
- **⚠ The blast radius is platform-wide, not one user: Trevor's own wallet reads 24 on-chain vs 3,707 in studio.** wmc already held 3,706 for him, so **wmc has long been right and the chain walk is the undercount** — it is *new* wallets, whose only writer is that walk, that render empty. Verified before wiring that the two sources agree on keys: across the 32 moments present in BOTH for `0x11859edcf2f53edd`, studio `edition.id`/`serial_number` matched on-chain exactly (32/0).
- **Honesty preserved:** `on_chain_count` still means chain truth, union total is the new `holdings_count`, `studio_*` fields alongside; `rows_found` keeps its "raw on-chain scan count" meaning (the union passes malformed chain tuples through verbatim — filtering them silently redefined that field and the existing pinned test caught it). A degraded walk logs `studio_custody_walk_degraded` rather than passing as "no locked moments". `fetchAllDayStudioHoldings` never throws, so a studio outage leaves the on-chain backfill exactly as it was.
- **Live verification (prod):** ThunderHour `rows_processed 0 → 5`, all 5 wmc rows enriched, and the three moments match the report exactly incl. mint counts (`#1/4999`, `#59/59`, `#355/499`); telemetry read `on_chain=0 / holdings=5 / studio_added=5`. Regression: Levy 33→33, RipPacksCity 1→1, zero NULL `edition_key`/`player_name`, 0 new Sentry.
- **⚠ FOUND EN ROUTE — `main`'s CI had been RED for 5 straight commits and it was nobody's diff (`e0805f0c`).** `17738436` deleted `components/SignInWithDapper.tsx` + `components/auth/ConnectButton.tsx` but left both in the protected lists in `scripts/check-brand-tokens.mjs`; the guard fails on a missing protected file, so every push after it failed. **The step lives inside the `TypeScript` job, so the red job name pointed at `tsc`, which was passing the whole time.** Deleting a protected component means deleting its guard entry in the same commit. All 8 jobs green after.
- **NOT done (deliberate):** no bulk re-backfill was triggered — existing wallets self-heal on their next scheduled scan. Whether Golazos/UFC/Pinnacle have the same custody gap is **unverified**; the flag is AllDay-only.

### August 8, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only maintenance on tip `8beaa67e`: re-verified every checkable current-state fact against the repo and made ONE genuine correction — the component-coverage thresholds. No product/DB/prod change; direct to `main`. Ledger entry 2026-08-08.

- **Corrected the component-coverage thresholds: `79.0/66.5/78.6/83.0` → `78.6/66.4/78.2/82.6` (the actual on-disk value in `vitest.components.config.ts`).** They were re-baselined DOWN in `17738436` (the wallet-sign-in removal) — deleting the well-covered `SignInWithDapper`/`ConnectButton` components with their suites mechanically lowered the aggregate; the config's own comment documents it as the ONE legitimate reason to move a ratchet down ("files left the measured set"). Fixed both canonical-reference occurrences (the "Testing & CI coverage" React-components bullet + the CI-jobs list); the two frozen dated session entries that cite the old figure were left verbatim (they were correct as of their tip). Primary thresholds unchanged (`89.3/75.1/91.5/91.6`).
- **Everything else re-verified CURRENT on this tip (`8beaa67e`), no edit due:** 36 Vercel crons · 122 DB-invariant pins (raw `grep -cE 'fn:'` = 123, one two-fn pin counted twice) · both launch flags `true` · 8 collections defined / 5 published · 19 GHA workflows (16 scheduled / 3 unscheduled) · 16 workers · 8 CI jobs (typecheck · cadence-lint · cadence-escrow-tests · unit-tests · component-tests · db-tests · ledger-guard · edge-deno) · concierge model `claude-sonnet-4-6`.
- **No tail-roll due:** Recent sessions spans exactly 3 calendar days (Aug 8 · Aug 7 · Aug 5), within the "keep ~3 days" rule — archive pointer unchanged.
- **Landed-since, already ledgered (no separate Recent-sessions entry authored here — that's the shipping session's job):** the new-user deep-warm-on-signin fix (`c6b55735`, `lib/profile/warm-wallet.ts` + both profile entry points), the `sales_2027` twin index + 2026-index verification (`7075d9f9`), and the `sync-nba-projections` v9 no-slate/all-upstreams-failed split (`8beaa67e`) all carry their own ledger entries.

### August 8, 2026 (Claude Code, interactive — "work on the Sentinel WARN" → observability → "keep going / do what you think is best" → tier decision → session-close) — **7 green ships to `main` (`2fd756e4` → `ece298d0`)**: a Sentinel-WARN fix, a multi-collection observability build, three saturation-resilience fixes, and the platform's #1 disk-IO query fix (index built by Trevor via SQL editor). Each CI-green + ledgered with a revert path. No auth/`proxy.ts`, hot-wallet, or destructive-SQL change.

- **⚠ DURABLE — the DB is DISK-IO-BUDGET-bound, NOT compute-bound. Do NOT upgrade the compute tier to fix saturation.** The intermittent "everything 504s" spikes (`/api/market`, `/api/sentinel`, price-snapshots, entity pages, 6 pg_cron MV refreshes all timing out at once) are Supabase's **disk-IO burst-credit model** depleting → throughput throttles to a **22 MB/s baseline** (confirmed on-screen by Supabase's own "about to deplete its Disk IO Budget" banner). Diagnosis signature: `pg_stat_activity` shows the DB **idle** (2–3 of 90 connections active, CPU idle, 0 lock waits) with backends in **`DataFileRead`** I/O-wait; 12 GB DB vs 512 MB `shared_buffers` on the Small tier. **We are on SMALL** (2 GB RAM / 2-core, `max_connections`=90 — CLAUDE.md's old "Micro" was stale). **Medium is the SAME 2 cores** (only +RAM) for **4× the cost** — you don't get more cores until XL. So a bigger box would run the *same* wasteful queries with more cache; the fix is **killing the expensive queries**, which is free + permanent. Cheapest infra lever if ever needed: provision disk throughput/IOPS independently (not a compute bump). **EXPLAIN (no ANALYZE) is safe to profile even a saturated/IO-throttled DB** (planning only, no execution/IO) — used it throughout.
- **⚠ DURABLE — the "compute-for-all-then-LIMIT" anti-pattern + the heap-fetch-storm MV pattern, both fixed, both reusable:** (1) `get_allday_market_editions` ran a per-edition fmv LATERAL over ALL ~4,300 listed editions then `LIMIT 60` → 7s warm/12s cold, 504ing ~19% of the **public** Market tab. Fix (`abbf7fb5`, migration): for fmv-INDEPENDENT sorts (`price_asc`/`price_desc`, the default) sort+LIMIT on the aggregate FIRST, fmv-lookup only the page → **72ms** (byte-identical output, 0-row EXCEPT diff). (2) `mv_topshot_perfect_mint_premiums_board` refresh burned **2,417 s/day** because its `ed_med` median CTE used `(edition_id, price_usd)` then **HEAP-FETCHED ~406k rows** to filter `collection`/`sold_at`. Fix (`ece298d0`): partial covering index `idx_sales_2026_ts_edition_median (edition_id, sold_at DESC) INCLUDE (price_usd) WHERE collection='nba_top_shot' AND price_usd>0.50` → validated live **`Index Only Scan`, zero heap fetches**. The other 5 timing-out MV refreshes are the SAME class (queued, inbox `2026-08-08T1945Z.md`; `topshot-edition-median` likely shares the exact `ed_med` shape).
- **⚠ DURABLE — `CREATE INDEX CONCURRENTLY` cannot be run from the Supabase MCP** (can't run in a txn → not `apply_migration`; and a full-partition build exceeds the ~60s MCP tool cap, worse when disk-throttled — a plain `COUNT` already timed out at 25s). It aborts on client disconnect leaving an **INVALID index** on the hot partition. Path: the **Supabase SQL editor** (no cap) or a quiet-window psql. This one Trevor ran himself in the SQL editor; committed a reference migration (`20260808200000_*`) for repo↔DB parity (re-run is a no-op via `IF NOT EXISTS`).
- **⚠ DURABLE — the Sentinel itself was 504ing ~88% of runs (failing BLIND — no report, no digest, no alerting).** Its ~8 sequential health-check DB reads blew `maxDuration=60` under saturation. Bumped `60→180` (`f3ebcd8e`; GHA caller waits 5 min, Pro cap 800s). Same class fixed on `price-snapshots` `maxDuration 15→60` (`e2ff80cd`) — it was silently dropping hourly OHLC buckets (7 of 24 missing); safe because its RPC only writes the prior hour + is idempotent (`ON CONFLICT DO NOTHING`). A cron/health route near its cap under saturation is a candidate for this whenever the work is idempotent.
- **Multi-collection Sentinel (`e47fefef`, migration):** the aggregate `Sales Ingest (2h)` check was ~92% Top Shot, masking a per-collection ingest death, and Pinnacle sales (`pinnacle_sales`) weren't even watched. New `sentinel_ingest_watch` config + service-role `sentinel_sales_ingest_health()` (per-collection + per-source lane health, **index-bounded per collection** because **there is NO index on `sales.ingested_at`** — every sales index is `sold_at`/`collection_id`, so query recent sales by `sold_at` + `collection_id`). Two new checks page TS/AllDay, warn the rest, off for UFC (market closed). +9 tests. Also the `pinnacle-catalog-backfill` WARN that opened the thread → Vercel-cron backstop (`2fd756e4`, vercel.json 35→36) for the daily catalog whose sole cron-job.org tick had dropped.
- **Method:** 504-triage via Vercel `get_runtime_logs` `group_by=requestPath`/`statusCode`; separate transient-saturation from chronic by pre/post-fix timing + `deploymentId`. Ledger is concurrent-write-heavy (several rebases this session; splice the freshly-read top keeping BOTH entries, `grep -c '^### '` must go UP). Scheduled self-check-ins via `send_later` (self-bind) for live verification; cancelled the pending one at session close since the thread is being archived.

### August 8, 2026 (Claude Code, interactive — "implement a TODO" → "keep going / everything mentioned" → close-up + commit to memory) — **3 green ships to `main` (`55ab7b9c` → `b8235e0c` → `8375d2fe`)**, all CI-success + `tsc`-clean + each ledgered with a revert path. One worker wiring, one operator handoff doc, one bug-class continuation. No FMV/pricing/ingest MATH, auth/`proxy.ts`, hot-wallet, or DB/prod-state change.

- **Wired the last genuinely-open in-code TODO (`55ab7b9c`):** `workers/sports-proxy/index.ts` `handleOdds()` was a reserved `501 odds_route_pending_api_key` stub. Now a real **key-gated pass-through** to the-odds-api.com, mirroring the dedicated `odds-proxy` worker (whitelisted POST-body params over defaults, `apiKey` from a worker-only `ODDS_API_KEY` secret, quota headers surfaced, 502 on upstream fail, 5-min cache). ⚠ **It STILL returns the exact `501 odds_route_pending_api_key` while `ODDS_API_KEY` is unset** — so the two existing stub tests stay green and the route is inert until bound. ⚠ **This overlaps the dedicated `odds-proxy` GET worker on purpose** (same-surface convenience for `SPORTS_PROXY_URL` consumers; the live `sync-nba-odds` pipeline reads `odds-proxy`, NOT this) — do NOT "de-duplicate" it as a bug. Live only after operator `wrangler deploy` + `wrangler secret put ODDS_API_KEY --name rpc-sports-proxy` — procedure in [docs/handoff-2026-08-08-sports-proxy-odds-route.md](docs/handoff-2026-08-08-sports-proxy-odds-route.md) (`b8235e0c`).
- **⚠ DURABLE — the prototype-key lookup class had 3 MORE uncaught sites (`8375d2fe`), months after it was "closed across lib/**".** A safe-zone sweep (display/format/util libs, `_shared` parsers, worker transforms — no FMV/auth/ingest) found genuine `MAP[externalKey] ?? fallback` leaks the earlier capped sweeps missed: `lib/collection/series-param.ts` `resolveSeriesParam` (label = a localStorage/URL filter value; leak violated its `string|null` return and stamped a stringified fn into the `series` query param — strongest), `lib/moment-special-serials.ts` `mapNotableTagsToSpecialSerials` (a prototype-key tag slipped past the allowlist's `if (!badgeType)` drop-guard, fabricating a function `badge_type`), and `lib/laliga-clubs.ts` `getClubAbbrev` (`team_name` = free-text DB column rendered as a club badge). All fixed with `ownLookup(MAP,key) ?? fallback` + a prototype-key regression test each. **The controlled-vocab tail was correctly LEFT** (tier/variant/confidence enums, numeric-keyed maps, and `market-closed.ts` which is already `hasOwnProperty`-guarded inline — a bracket-`??` grep hit is not a bug if a guard precedes it, and a prototype-name key provably can't reach an enum/numeric key). Method: verify each candidate is externally-keyed AND genuinely reachable before touching — most `MAP[key]` sites are controlled-vocab and NOT defects.
- **Method reconfirmed:** the safe-zone sweep is best delegated to an Explore subagent capped at top-N with "nothing found is an acceptable answer," then each finding re-verified by reading the source + call site before fixing; ledger is concurrent-write-heavy (splice the freshly-read top, `grep -c '^### '` must go UP by exactly your additions); `npm install` then `git checkout package-lock.json` before vitest/tsc (Node 22 local vs CI 24); a docs-only tip suppresses the Vercel deploy (`ignoreCommand`) so the code commit must be the tip.

### August 8, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only maintenance: every checkable current-state fact re-verified against the repo on this tip (`e2ff80cd`) and found CURRENT; no factual edit was due and no tail-roll was due. No product/DB/prod change; direct to `main`. Ledger entry 2026-08-08.

- **All canonical reference facts re-verified current on this tip (`e2ff80cd`), no edit due:** 36 Vercel crons · 122 DB-invariant pins · both launch flags (`CANDY_MLB_PUBLIC` / `PANINI_PUBLIC`) `true` · 8 collections defined / 5 published · 19 GHA workflows (16 scheduled / 3 unscheduled) · 16 workers · 8 CI jobs (typecheck · cadence-lint · cadence-escrow-tests · unit-tests · component-tests · db-tests · ledger-guard · edge-deno) · concierge model `claude-sonnet-4-6` · primary coverage thresholds 89.3/75.1/91.5/91.6 · component thresholds 79.0/66.5/78.6/83.0. (The raw `grep -cE 'fn:'` over the drift-guard test now reads 123, but that counts one two-function pin entry twice — the highest numbered pin is still #122, added Aug 5 in `0404aa77`; 122 is current.)
- **No tail-roll due:** Recent sessions spans exactly 3 calendar days (Aug 8 · Aug 7 · Aug 5) — within the file's "keep ~3 days" rule — so the archive pointer under "Older sessions" is unchanged.
- **Landed-since, already ledgered (no separate Recent-sessions entry authored here — that's the shipping session's job):** the Panini realized-sales re-point from the dead `brought_at_price` to op `nftSalesData` + the "op never fires unprompted, only on Sales-History-tab activation" finding (`521ee89f`, `c4ff8500`), the `get_allday_market_editions` LIMIT-pushdown that fixed the ~19% AllDay-Market-tab 504s (`abbf7fb5`), the `price-snapshots` cron `maxDuration` 15→60 (`e2ff80cd`) and sentinel `maxDuration` 60→180 (`f3ebcd8e`) fixes, and the `ownLookup()` prototype-key primitive (`a233310c`, already covered by this day's earlier CC entry) all carry their own ledger entries dated 2026-08-08.

### August 8, 2026 (Claude Code, interactive — "implement a TODO" → "keep going / address any issue" → "do all you can / proceed through anything remaining" → session-close) — **6 green ships to `main` (`cfe36151` → `a233310c`)** + one read-only handoff, all CI-success + full-ratchet-green + each ledgered with a revert path. One runtime pricing change (verified live), the rest a bug-class cleanup + coverage. No auth/`proxy.ts`, hot-wallet, or destructive-SQL change.

- **Candy trade-day pricing (`cfe36151`, runtime — the "implement a TODO" item, VERIFIED LIVE).** Resolved the one genuinely-open code TODO in the tree (`lib/chains/solana/das.ts`: "revisit with a per-day historical SOL/USD series"). New `solUsdOn(atMs)` prices a realized Candy `sales` row on the SOL/USD close for its OWN UTC day (CoinGecko `/coins/solana/history`), wired into `candy-sales-indexer` `buildSaleRow`. **Safe by construction:** today/future/any-failure falls back to the existing spot `solUsd()`, so it can never price worse than before; per-UTC-day cache incl. negatives. Listings/offers indexers keep spot (an ASK/BID is a live snapshot). ⚠ **It is a near-no-op for the live forward book** — sales are same-day, so `solUsdOn` short-circuits to spot with ZERO added latency + no CoinGecko call; the historical branch only fires on a genuine cross-midnight-UTC parked-then-drained sale. **Post-deploy verification (11:20 AM PT tick):** pipeline `ok=true`, 8090 ms (on baseline), `error=null`, 0 new Sentry; all 16 sales that landed were same-day (`cross_day=false`), implied rate ≈ spot 76.2 — confirmed working + dormant-by-design.
- **⚠ DURABLE — the prototype-key lookup bug class, now closed across `lib/**` + a shared primitive.** A bare `MAP[key]` read on a **plain-object literal** (`Record<string,T>`) whose key is externally controlled resolves a crafted `"constructor"`/`"toString"`/`"hasOwnProperty"`/`"valueOf"`/`"__proto__"` to a **truthy `Object.prototype` member**, which defeats the `?? fallback` and surfaces a function where a value is expected (a wrong render, or a THROW downstream). The repo already guarded it inline in `lib/market-closed.ts`. Fixed the rest in four rounds: (1) `collection-tiers.ts` — a real **URL-reachable crash** (`/constructor/sniper` → `sniperTierTabs` maps over a function → `TypeError` mid-render; the `[collection]/sniper` page has NO `notFound()` gate); (2) `cosmetics.ts` (user-set `equipped_border`) + `analytics/format.ts`; (3) `seo.ts` — the ~16 URL-keyed metadata reads (a crafted slug put a function into `<title>`/meta/JSON-LD); (4) new **`lib/safe-lookup.ts` `ownLookup(map,key)`** primitive routing `collections.ts` slug resolvers + `pack-availability.ts` + `analytics/window.ts` + `slab-style.ts badgeColor`. **New code with an externally-keyed plain-object lookup should use `ownLookup(MAP,key) ?? fallback`.** The **controlled-vocab tail was deliberately left** (pinnacle variant maps, tier styles, numeric series labels, DB-enum keys — a prototype-name key can't reach those; guarding is churn without value). Each round CI-green + regression-tested (prototype keys → fallback).
- **⚠ METHOD — a `grep | head -40` sweep is NOT proof of exhaustiveness.** I claimed the class "closed across lib/**" after round 3, but that sweep was capped at 40 hits and missed the tail; round 4 re-ran it uncapped (31 sites) and found the rest. Corrected the overstatement in-ledger. Also: verify each candidate is a GENUINE bug before touching it — most `MAP[key]` sites are controlled-vocab and NOT defects (same discipline that kept me from over-guarding `PAGE_META[page]`, whose key is a hardcoded literal at every call site, and `marketplace-fees SLUG_ALIASES`, whose prototype value flows harmlessly into `Map.get`).
- **Coverage (test-only, rode the same passes):** closed the branch gap on `lib/user/resolveDisplayName.ts` (60%→~95% br — the dashboard/header identity resolver with a profanity guard on every rung; ladder rungs + wallet fallbacks + `shortAddress` edges + email-absent allow_list skip), plus `collection/export-csv.ts` all-nullish-row (export-honesty: never write "undefined") and full `trophy/slab-style.ts` tierBorder/tierGlow branches. New `__tests__/safe-lookup.test.ts` pins the primitive.
- **MV-refresh timeout cluster — read-only profiled handoff → `docs/overnight/inbox/2026-08-08T1717Z.md` (NOT fixed).** Live `check_pgcron_recent_failures()` showed **8 jobs timing out**. Finding: every MV output is TINY (128 kB–4.3 MB / 163–19k rows) yet the defining queries cost up to 600 s — a query-cost + scheduling-contention problem (a `:17`-past-the-hour + 6-hourly pileup on a saturated pooler), not data volume. Flagged the untracked ones (`topshot-edition-median` feeds TS FMV median) + config-only stagger and per-query index levers with the do-not-bump-timeout guardrail. Left for the night pass — profiling/indexing a saturated prod DB is out of the safe interactive lane.
- **Method reconfirmed:** ledger is concurrent-write-heavy (5+ rejected pushes this session → `git pull --rebase`, resolve the top-of-ledger conflict keeping BOTH entries, strip markers, `grep -c '^### '` must go UP by exactly your additions); commit ledger+code together so the tip carries code and Vercel deploys (a docs-only tip is skipped by `ignoreCommand`); `npm install` then `git checkout package-lock.json` before tsc/vitest; the giant `actions_list` MCP result overflows the token cap → parse the saved JSON with python; **show Trevor PT times only, never UTC** (his explicit ask this session).

### August 8, 2026 (Claude Code, interactive — "analyze test coverage → propose → do all that make sense → keep going ×N → do what you think is best") — the STRUCTURAL-gap test-coverage pass: **10 green test-only commits to `main` (`64182f78` → `0f857dcb`)**, all `tsc`-clean + each ledgered, each rebased cleanly past heavy same-day concurrent churn. No runtime product code, no DB/prod change.

- **The headline structural fix: `proxy.ts` (the site-wide auth + allow-list security wall) is now GATED.** It sits at the repo root, so until now NEITHER coverage gate measured it — a new anon-reachable branch in the lockdown could land with nothing to catch the drop. Added it to `vitest.config.ts` `coverage.include` and wrote `proxy-dispatch.test.ts` driving the async `proxy()` flow for the first time (bypass-token header+`?token=`, CORS preflight, API+page 429 caps, unauth→`/login?next=`, and the allow-list ladder — cookie-cache hit skips the RPC, RPC error fails **closed**, revoked→signOut+`access_revoked`, cross-session/expired/malformed cookie handling). proxy.ts 92.6% st / 90.6% br; the sibling `isPublicPath`/page-rate suites already existed.
- **Covered 7 dynamic routes that had NO test importing their module** (were ~0% in the primary gate): `moment/[id]`, `public/profile/[username]`, the `ipfs-media`/`pinnacle-image` proxies (SSRF regex + the 8 MB Fast-Data-Transfer size gate), the 3 `/analytics` detail routes, `admin/allow-list/[id]` + `feedback/[id]`, and `mcp/keys/[keyId]` (the ownership 403). Primary gate 89.57/75.34/92.16/91.94 → **89.63/75.54/92.07/91.99**; thresholds bumped to 89.3/75.1/91.5/91.6.
- **Component gate: seven batches on the lowest-branch USER-FACING clients** — the interaction layer (filter/sort/tab refetch, formatter bands, per-row conditional cells) the smoke/populated passes leave dark: 12 `/insights` board clients (Pinnacle/Serial/Rookie/SetCompleters/OfferSpread/Parallel/Trophies/Underpriced), `PlayersGridPaginated` (no prior test), `PackTable` DualPriceCell/PackThumb, `MomentDetailModal` financial cells + loan-default provenance, `AnonSignInPill`, and `WalletProfile` (copy/status-badge/expand/limbo). Component gate **63.99→66.91 br / 76.3→79.43 st** across seven threshold ratchets → **79.0/66.5/78.6/83.0**.
- **Primary-route error paths (pivot back off the saturated component gate):** deepened three routes whose tests stopped at auth/happy — `auth/fcl-nonce` (HTML-error-page defense + never echo it + 8s timeout race), `wallet/pack-lifecycle` (400/403 guards + lookup/RPC 500s), `cron/purge-stale-listings` (delete-error + fatal-catch 500s, non-fatal log-run, 1000-row count_capped). Primary gate → **89.71/75.65/92.1/92.06**; thresholds left at 89.3/75.1/91.5/91.6 (buffer absorbs it; no concurrent-churn risk).
- **Deliberately NOT forced** (would add risk/padding, not value): async server components (`PopularOnCollection`, `app/**/page.tsx`) can't render in the jsdom harness; the Deno edge-fn orchestration is already covered via the `_shared` + inline-copy-drift-guard pattern (completeness allowlists empty); `support-chat`'s ~25 bespoke tool fixtures the config says not to chase; the DB-pin campaign is actively owned by concurrent sessions (migration-authoring stakes — don't collide). The suite is mature — the value was structural (unmeasured security file, zero-test routes) not raw %.

### August 8, 2026 (Claude Code, interactive — "analyze test coverage → propose → keep going ×N → exhaust everything that makes sense for RPC data quality → hand off the MV work + commit to memory") — a long escalating thread: ~87 additive tests across the lib/route + component gates, then a LIVE data-quality audit (all 122 integrity pins vs prod = 0 stale) + a fix to the staleness check's OWN blind spot, then the MV-optimization handoff. 10 green commits to `main` (`a45062d0` → `b9dc2088`), all CI-green, each ledgered 2026-08-07/08. No FMV/pricing/ingest MATH, auth/`proxy.ts`, hot-wallet, or prod-DB mutation.

- **Coverage push (7 commits).** lib+route branch 75.02→75.34%, component branch 62.36→63.99% — thresholds HELD (additive, never raised to force). Closed REAL gaps, not padding: verified each candidate against live first and SKIPPED the already-saturated files (alerts/format, seo, editions-hydrate, wallet-backfill-helpers, pinnacle-router, ingest, sniper-feed helpers — all thoroughly tested, residual is defensive fallbacks). Landed: all 5 forward sales indexers' buyer-recovery + venue-tagging (the silent-misattribution class — V1-Dapper decode, V2-Flowty `flowty` tag, price-uncertain→unmapped park, base `determineMarketplace` flowty/other); wallet-search pre-walk dispatch (EVM/unknown-collection/pinnacle-redirect); the `/api/market` read route (Pinnacle catalog path + the full in-memory sort/filter/pagination ladder); and 5 component commits (FmvDashboard 34→68 br, CollectionMomentTable 49→60 br, WalletsHubOverview 24→78 br, PopularOnCollection `distinctSlugLinks`, PackPageClient `toPackRow` via a JSON-exposing PackTable stub — the pattern for asserting a component's mapped output when the child is stubbed).
- **⚠ DURABLE — the DB-pin STALENESS CHECK had a silent blind spot of its OWN (fixed `a5e98fb3`).** `scripts/check-db-pin-staleness.mjs` re-derives the pin list with a regex that required `fn:`/`test:`/`migration:` on strictly ADJACENT lines — so any PINS entry carrying a comment between its fields was SILENTLY DROPPED from the only check that can catch live drift. Two data-critical pins (`get_wallet_moments_with_fmv`, `get_team_detail`, both re-pointed with a "re-pointed 2026-…" note) were invisible to it. Loosened the field separators to `[\s\S]*?` (now captures all 122, no over-capture) + added `__tests__/db-pin-staleness-parser-coverage.test.ts`, which reads the script's ACTUAL regex and asserts it covers every pin the guard defines — so a future tightening can't re-open the hole.
- **Live DB data-quality sweep (read-only, MCP).** All **122 integrity pins CLEAN / 0 stale** vs prod (replicated the staleness check by md5-ing `pg_proc.prosrc` under both normalizations and comparing to committed bodies). Security fully clean (RLS 0-off, all 3 invariant checks `[]`). FMV honesty north-star among editions recalced <3d: **TS 65.5% / AllDay 36.7% / Candy 57.6% HIGH+MED** (Golazos 0.6% = known listing-gated) — well up on the 08-03 roadmap baseline (17.3% / 6.3%).
- **MV-optimization handoff (QUEUED ledger + inbox `docs/overnight/inbox/2026-08-08T1443Z.md`).** Two DATA-QUALITY refreshes chronically time out and are stale: `mv_topshot_misattrib_candidates` (46.8h; `refresh_topshot_misattrib_candidates` ALREADY carries 600s and still exceeds it → query weight NOT config, so a timeout-bump is the WRONG fix) + `fmv_thin_sale_ask_disclosure_cache` (29.1h). Handed to the night pass with the view/MV defs, profiled bottleneck hypotheses, candidate `CREATE INDEX CONCURRENTLY` levers, and do-not-bump-timeout / do-not-touch-`remap_topshot_*`-logic guardrails — blindly tuning prod pg_cron without profiling is out of the autonomous lane. Scheduled a self-check-in (`trig_015sCbtqmqMxbvZpLPYguC2S`, fires 2026-08-09 16:00Z) to confirm the pass drained the inbox + whether it shipped/re-queued.

### August 8, 2026 (Claude Code, interactive — "implement a TODO" → FMV accuracy program, Trevor-directed → "keep going / best judgment") — **8 green ships to `main`**, all CI-success + each independently verified + each ledgered 2026-08-07/08. The complete FMV confidence-accuracy program across the two major Flow collections. No auth/`proxy.ts`, hot-wallet, or destructive-SQL change.

- **The program (all forward-only — the daily recalc sweep re-baselines over 1-5 days; no data mutation):** (1) **ask-ceiling** — non-special-serial base FMV must never exceed the cheapest live ask; `capFmvAtCheapestAsk` in `lib/fmv-phantom-guard.ts`, applied in `fmv-recalc`. TS uses `edition_offers.low_ask` (`4145fc0f`), AllDay uses `allday_edition_floor_ask.floor_ask` (`bb7ee654`) — kept in a **separate ceiling map** from the ask-corroboration map, because the ceiling only ever LOWERS and corroboration only ever RAISES. Measured: 547 TS + 1,549 AllDay editions were reading above buy-it-now. (2) **volume-tier HIGH gate** — `gateHighToRecentVolume` (`lib/fmv-confidence.ts`): HIGH requires ≥7 sales in the RECENT 30d window, so the 90d-widening can't grant top-tier off stale spread (`8dd83571`). (3) **90d catch-up** — editions that trade in 90d but not the recent 30d were never enumerated (the edition-page is a 30d GROUP BY) → fell to ASK_ONLY/NO_DATA/stale. New SECDEF `fmv_recalc_90d_catchup_editions` + offset-0-only seeding into the pricing loop; TS (`9253744c`, 878 eds) + AllDay (`2f62a9cd`, 230 eds incl. 13 stale false-HIGH). (4) **Step-6 stale-touch gate fix** (`a4ec34ee`) — the liveness re-stamp preserved HIGH on cold editions (77 TS false-HIGH); now runs the gate too.
- **⚠ DURABLE — `fmv-recalc` writes confidence on THREE paths, and any confidence change must cover all three:** the main-loop recompute, the offset-0 90d catch-up seed, and the Step-6 `?force_stale` stale-touch re-stamp. The volume gate initially covered only the first; the catch-up and stale-touch were separate holes found only by post-ship verification. All three now apply `gateHighToRecentVolume`.
- **⚠ DURABLE — the roadmap headline metric (HIGH/MEDIUM confidence share) is now measurable + tracked** (it had NO cheap read path — the latest-per-edition DISTINCT-ON over ~680k TS `fmv_snapshots` is **>60s cold / ~13-18s warm**, blowing the service_role 30s / MCP 60s budget cold). On-demand: `rpc_fmv_confidence_share()` (SECDEF, 120s local timeout, service_role-only, `20260808143508`). Auto-tracked: 5 `{coll}_fmv_high_med_share_pct` metrics **piggybacked on Legs 2-5's existing scan** in `rpc_trust_health_precompute_refresh` (`20260808144413`, jobid 222 `58 */6`). **Measured live: TS 54.9% · AllDay 28.2% · Candy 57.6% · Golazos 0.5% · UFC 0.0%** — vs the 08-03 roadmap baseline TS 17.3% / AllDay 6.3%, the cumulative accuracy program (this session included) is dramatically ahead.
- **⚠ DURABLE — `rpc_trust_health_precompute_refresh` runs at ~569s of its 600s budget and is LOAD-BEARING (the sentinel fails BLIND on a timeout).** Add metrics by piggybacking an existing leg's scan, NEVER a new scan. ⚠ **Only 3 of 7 legs are isolated** (`fmv_sanity_flags`, `pack_ev_publish_shortfall_pct`, board-liveness — verified live: 3 `EXCEPTION` blocks in `prosrc`); Legs 1, 2–5 (fmv coverage), 6 (panini) and 7 (serial supply) have **NO exception handler**, so a `statement_timeout` in any of them rolls back **all 18 metrics**, not one (observed 2026-08-09 12:58Z in Leg 7 — table left 8h stale, presenting as ~13 unrelated red arms because the view's `pre` CTE maps any row >24h old to 999). The claim this line used to make — "each leg is isolated, a throw marks only its metric" — is FALSE and was load-bearing (it made a partial failure look survivable). `trust_precompute_max_age_hours` (breach 13, added `20260809145547`) now detects it. The real fix is to split the fn into per-leg functions with their own cron entries — each gets its own 600s budget, a kill loses one metric instead of 18, and it makes headroom for the 25 live arms. New per-collection health metrics that reflect market liquidity are TRACK-only — no breach arm (a thin/dead market is a fact, not an alert).
- **Pending verification (thread archived before it could run):** the precompute live-write lands on the **18:58 UTC** cron tick — `docs/overnight/ledger.md` (2026-08-08 entry) carries the exact verify query (5 share metrics fresh @ 18:58 + the last/most-expensive board leg also fresh = fn stayed under budget). **QUEUED for the resolver owner:** AllDay has **90,865 unresolved `unmapped_sales` at avg 0.1 on-chain attempts** (the resolver never reaches the backlog; TS is fully caught up at 0, Golazos 7) — determine resolvability (pre-catalog vs a query that never selects the tail) before building. Golazos/UFC confidence is fundamental thin/dead-market, not code-fixable.
- **Method reconfirmed:** ledger + CLAUDE.md are concurrent-write-heavy (rebase-on-reject, `git stash` for untracked-safe pulls doesn't stash new files — they survive rebase; resolve top-of-file conflicts keeping BOTH entries newest-first; `grep -c '^### '` must go UP); `| tail` after `git push` MASKS the push exit code (`if git push | tail` always "succeeds" — check `${PIPESTATUS[0]}` or don't pipe); the giant `actions_list`/`list_migrations` MCP results overflow the token cap → parse the saved JSON with python; new SECDEF fns need an explicit `REVOKE EXECUTE FROM anon, authenticated` (Supabase default-privilege grants them EXPLICITLY, so `FROM PUBLIC` alone leaves `has_function_privilege('anon',…)` true).

### August 8, 2026 (nightly autonomous pass, GENUINE OVERNIGHT ~01:03 PDT) — but NO-PUSH: the workspace shell was fully down (`/sessions` "no space left on device" now kills the sandbox itself, not just the clone → no git at all). shipped 0 / reverted 0 / repaired 0; post-ship watch of the 08-07→08 CC wave ALL PASS; outputs written directly to the mount, UNPUSHED. Full log: `docs/handoff-2026-08-08-overnight-pass.md`.

- **Escalated environment failure:** `mcp__workspace__bash` failed 3× identically with `mkdir /sessions/... no space left on device` — so no clone AND no `GIT_INDEX_FILE` mount fallback. DB/Sentry/Vercel MCP + the file tools (Read/Glob/Write with explicit Windows paths) still worked; git did not. Prior nights degraded the clone to `/tmp`; this night the whole shell wouldn't start. Provisioning look would restore overnight push capability.
- **Post-ship watch — 08-07→08 CC/interactive wave ALL PASS, 0 reverts.** candy-offers reliability fix (concurrency→1 + throttle + batched DB reads) HOLDING (last two ticks 06:50Z + 03:15Z ok=true/degraded=false, deactivation 7/9; coverage arms `candy_offers_unverified_pct` 0.0 / `oldest_active_hours` 1.3). FMV 90d catch-up climbing (TS HIGH+MED 7112→7359, AllDay 1713→1746). Panini home-box RESOLVED (`panini_fmv_stale_hours` 2.4). proxy.ts page-metering `acb5ff24` holding (fails 209/13,997 = 1.5%, intermittent not saturation). Nothing to revert.
- **Health green-with-known-noise.** Security 4/4 clean; `detect_stalled_pipelines()` [] (candy-offers + panini stalls both cleared); 0 new Sentry/24h; DB 12,444 MB. pg_cron 2 intermittent MV-refresh timeouts (self-recovering). 4 trust breaches all known/by-design (panini price-capture 11, public_board_slow 5 crawler-class, ufc 97.6 red-by-design, unmapped 175 — HELD FLAT, stopped climbing).
- **Queued (new, nc1):** `rpc-refresh-misattrib-candidates` daily MV refresh timed out (08-07 15:35Z); LOW/internal (parallel↔base misattrib helper, self-retries daily). Not shipped — NO-PUSH would drift repo and no obviously-idle slot exists. Drained both inbox files (2026-08-07T2108Z page-metering-verify + 2026-08-08T0306Z misattrib) → archive (file-tool copy + stub, no `mv` without a shell).

### August 8, 2026 (Claude Code, interactive — "analyze test coverage → propose → implement #1 → proceed with all → close up + commit to memory") — two green test-only batches to `main` (`c808d150` + `a006f9db`), both CI-success, both ledgered 2026-08-08. One tiny pure-lib hardening rode along. No FMV/pricing/ingest MATH, auth/`proxy.ts`, hot-wallet, or prod-DB change.

- **Coverage state (measured live this session): 88.78% stmts / 91.78% funcs / 91.16% lines / 74.41% branch.** Lines are saturated; **branch is the systematic gap** (14pts below stmts) — the error/retry/fallback paths. That's where both batches aimed. `lib/**` + `app/api/**/route.ts` is the measured layer; components + DB pins + edge-Deno + e2e are separate gates.
- **Batch 1 — sales-indexer 23505 runtime retry (`c808d150`).** `sales-batch-insert-23505-guard.test.ts` only STATIC-pattern-pins the row-by-row retry idiom; the behavior was unexercised. Added runtime tests that drive a batch MIXING one dupe + one new row and assert only the dupe drops (`rows_written`=1, not 0/2) — the permanent-data-loss footgun. `allday-sales-indexer` + base topshot `sales-indexer` had NO runtime 23505 test; golazos/ufc's used a single-row batch that can't prove the invariant. **Mutation-proven load-bearing** (swallow the 23505 in the route → `rows_written` 1→0 → tests red). Files: `api-{allday-,}sales-indexer-deep.test.ts` + `api-golazos-ufc-sales-indexer-deep.test.ts`.
- **Batch 2 — the four remaining coverage-analysis proposals (`a006f9db`).** (1) `lib/market-closed.ts` — the new test CAUGHT A REAL BUG: `isMarketClosed`/`closedMarket` used `slug in`/bracket access → matched `Object.prototype` keys (`closedMarket("toString")` returned the fn, not null); hardened to `Object.prototype.hasOwnProperty.call` (zero real-slug behavior change). (2) Concierge — drove the 6 untested `executeTool` arms (wallet-analysis + deal-search) through the real loop via the anthropic-fixture harness. (3) Confidence-share guard — pinned `escalateConfidence` boundaries (the classifier `fmv-recalc/route.ts:855` writes to `fmv_snapshots.confidence`, = the roadmap's HIGH/MEDIUM-share headline metric) + a threshold-constant tripwire; chose the lib guard over a DB pin (lower risk, and this lib IS the live classifier). (4) e2e entity/detail pages (edition/moment/set/player/team/series/pack — the pooler-saturation Sentry-incident pages the monitor omitted): URLs **discovered live from the sitemap** (`e2e/entity-urls.ts`, fail-soft skip), discovery self-checked offline (+5 tests), wired into `e2e-smoke.yml`.
- **⚠ Skipped `pinnacle-listings-reconcile` on purpose** (lowest-coverage route, 40%): it's `ASK_UNIFY_RETIRED = true`, so its live path is parked-for-rollback dead code — testing it would be theater. Low coverage on a retired route is EXPECTED; don't force it.
- **⚠ DURABLE — the sandbox CAN run Playwright against localhost** (verified: 17/17 self-check green): `PW_CHROMIUM_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome`, `PW_PROXY_SERVER=http://127.0.0.1:1` + `PW_PROXY_BYPASS=localhost,127.0.0.1`. It CANNOT reach the live site (proxy-blocked) — so `entity-smoke.spec.ts` first runs in the scheduled `e2e-smoke.yml` CI job, offline-validated only via the self-check. Also: the e2e-smoke workflow names spec files explicitly (`smoke.spec.ts entity-smoke.spec.ts smoke-selfcheck.spec.ts`) — a NEW e2e spec must be ADDED to that line or it never runs.
- **Method reconfirmed:** ledger is concurrent-write-heavy (three separate pushes landed mid-session; each rejected push → `git pull --rebase` + splice the freshly-read top keeping BOTH entries newest-first, `grep -c '^### '` must go UP by exactly your additions); the giant `actions_list` MCP result overflows the token cap → parse the saved JSON with python; `test:coverage` runs the full ratchet (~200s, 1061 files / 9131 tests) and only cares about DROPS, so additive tests are always safe.

- **The single factual drift since the 08-07 pass: a new scheduled GitHub Actions workflow.** `edge-fn-drift.yml` (repo↔deployed edge-fn drift comparator, daily 06:40 UTC) landed 2026-08-07 in `5e17a755` — the tooling half of the "26–31 of 37 edge fns run code older than `main`, and no repo↔deployed check exists" finding from that day's ledger. Corrected the two canonical reference lines (the cron-surfaces GitHub-Actions bullet and the CI/CD key-files line); measured live: **19 workflows / 16 scheduled / 3 unscheduled** (`ci.yml` + the two GHA→Vercel-migrated backstops `allow-list-reconcile` + `topshot-listing-cache`).
- **Everything else re-verified CURRENT on this tip (`f0d89c2a`), no edit due:** 35 Vercel crons · 122 DB-invariant pins · both launch flags `true` · 8 collections defined / 5 published · 16 workers · **8 CI jobs** (typecheck · cadence-lint · cadence-escrow-tests · unit-tests · component-tests · db-tests · ledger-guard · edge-deno) · concierge model `claude-sonnet-4-6` · primary coverage thresholds 87.85/73.35/90.7/90.35 · component thresholds 74.6/61.75/73.5/78.65. The new `edge-fn-drift` workflow is NOT a 9th CI job — it's a scheduled monitor, not a `ci.yml` pull-request gate.
- **Tail roll:** adding this Aug-8 entry pushed Recent sessions to 4 calendar days, so the 4 Aug-4 entries moved to [docs/sessions/2026-08.md](docs/sessions/2026-08.md) verbatim, newest-first (archive pointer now reads "August 4 → August 1"). Recent sessions now holds Aug 8 · Aug 7 · Aug 5.
- **Landed-since, already ledgered (not re-entered here — that's the shipping session's job):** the 08-07 edge-fn-drift tooling + worklist re-prioritisation (`5e17a755`, `f0d89c2a`), the PostgREST `.or()` filter-injection hardening on 4 read routes (`05dbea8d`), the candy-offers budget/watchdog fixes (`03e21a1d`, `793f5294`), and the dashboard stale-closure fix (`a0e6362b`) all carry their own ledger entries.

### August 7, 2026 (nightly autonomous pass, OFF-HOURS ~07:57 PDT) — fired late (outside 00:00–06:00) → monitor-mode: full review + health triage + post-ship watch, QUEUED everything, shipped 0 / reverted 0 / repaired 0. Full log: `docs/handoff-2026-08-07-overnight-pass.md`.

- **Post-ship watch — 08-06→07 CC/interactive wave ALL PASS.** The 08-06 HIGH inbox item (`rollup_pipeline_runs()` `jsonb_object_keys`-on-array failure) is CLOSED — fixed by migration `20260806031249` (`38d91194`); confirmed cleared (`check_pgcron_recent_failures()` [], `pipeline_runs_daily` current to 08-07). statement_timeout caps holding (entity Sentry 6→3, board-999 pair→0). All 4 recent 08-06 migrations live. Nothing to revert.
- **Health green-with-known-noise.** Security fully clean. FMV sweep improving (TS HIGH+MED 6800→7112, AllDay 1586→1713; sweep-stall 4.2). DB 12,368 MB. 4 trust breaches all covered (ufc red-by-design; panini_sale_capture upstream; unmapped_backlog climbing 105→127→175; panini_fmv_stale NEW).
- **NEW operator finding: Panini residential home-box runner dark ~37.7h** (`panini-ingest` last ran 08-06 01:21Z; `panini_fmv_stale_hours`=37.7 breach). Board public since 08-01 → user-facing stale FMV. Trevor: wake the box. Also QUEUED a config-only watchlist severity bump (`panini-ingest` info→medium, missed at go-live) and a candy-offers-indexer 62h-silent-Vercel-cron operator probe. Concurrent Daytime Monitor + CC pushed to main mid-run → rebased onto `84cba78b`; independently queue-only.

### August 7, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only maintenance: every checkable current-state fact re-verified against the repo and found CURRENT, rolled the tail, and folded ONE durable convention clarification into the Series map (tied to the shipped `audit_20260806_get_wallet_moments_series_topshot_convention`). No product/DB/prod change; direct to `main`. Ledger entry 2026-08-07.

- **All canonical reference facts re-verified current on this tip (`38d91194`), no edit due:** 35 Vercel crons · 122 DB-invariant pins · both launch flags `true` · 8 collections defined / 5 published · 18 GHA workflows (15 scheduled / 3 manual) · 16 workers · 8 CI jobs · concierge model `claude-sonnet-4-6` · ~29 concierge tools · primary coverage thresholds 87.85/73.35/90.7/90.35 · component thresholds 74.6/61.75/73.5/78.65.
- **Series-map clarification (durable, tied to `audit_20260806_get_wallet_moments_series_topshot_convention`):** the long-standing "there is NO series 1 on-chain / series 0 IS series 1" rule is **Top-Shot-specific and was NOT labeled as such**. Trevor reported two same-edition Lillard moments rendering different series ("Series 1" vs a bare "1"); root cause was `get_wallet_moments_with_fmv` unioning two incompatible encodings (`wmc.series_number` = on-chain, `editions.series` = display) — and the same column silently DROPPED 385,734 TS rows when a "Series 1" filter sent the on-chain `p_series=0`. The fix scopes the `1 → 0` fallback remap to the Top Shot uuid only; a blanket remap corrupts four collections (AllDay/Golazos/Pinnacle use 1 legitimately, **UFC has BOTH 0 and 1** — per `collection_series`). Added a scoped note to the Series map section.
- **Landed-since, already ledgered (no separate Recent-sessions entry authored — that's the shipping session's job):** the tip `38d91194` "honest stats errors + wallet-cap/form gating + sealed-pack links" (Cowork-handoff drain) plus the Aug-6 migrations `audit_20260806_get_wallet_moments_series_topshot_convention` + `audit_20260806_rollup_pipeline_runs_shape_defensive_extra` all carry their own ledger entries dated 2026-08-05.
- **Tail roll:** adding this Aug-7 entry pushed Recent sessions to 4 calendar days, so the 7 Aug-3 entries moved to [docs/sessions/2026-08.md](docs/sessions/2026-08.md) verbatim, newest-first. Recent sessions now holds Aug 7 · Aug 5 · Aug 4.

### August 5, 2026 (Claude Code, docs/hooks — "update CLAUDE.md to current state" → "how do we stop the stale-main divergence" → "do what else makes sense") — verified the file was already fully current (no factual edit due), then FIXED the SessionStart hook's mirror-lineage self-heal, which the 08-03 filter-repo root re-hash had silently defeated. `main` @ `950cdcf4` (hook + ledger, pushed clean). Hook-only; no product/DB/prod change.

- **⚠ DURABLE — the "stale local `main`" self-heal was BROKEN and nothing caught it, because the guard is repo-vs-repo.** `.claude/hooks/session-start.sh`'s realign fallback gated on `merge-base(main, origin/main) == origin's ROOT commit`. The 2026-08-03 `git filter-repo` purge re-hashed origin's root (`55f5fe4f`), so a frozen pre-purge provisioning mirror now shares only a DEEPER non-root ancestor (`90c508a4`) with origin/main → the gate mismatched → the hook printed "left as-is" and stranded local `main` days behind (the July-29 lineage this thread opened on). **A gate that hard-codes any historical SHA is defeated by the next history rewrite — key on a CONTENT property instead.**
- **The fix (`950cdcf4`): purge-proof `is_mirror_lineage()`** — realign when local `main` carries no commit unique by patch-id (`git cherry origin/main main` shows no `+` — provably lossless) OR is ≥100 commits ahead (mirror-shaped magnitude; genuine unpushed work on a commit-directly-to-`main` repo is never hundreds, and this catches the purge-orphan case where a stripped commit leaves a lone `+` so the lossless signal alone misses). Kept every prior guard: ff-only tried FIRST, clean tree required before `reset --hard`, genuine small branch-off left untouched. Validated: `bash -n` clean; the real July-29 mirror `d7fda5a0` (ahead 4023) → realign, a synthetic 3-commit branch-off → left alone.
- **Root cause is UPSTREAM PROVISIONING (out of repo), now handed to Cowork:** the web/Cowork container seeds each session from a frozen, days-old, re-hashed mirror of `main` rather than a live clone. The hook only heals it after the fact — and only once provisioning ships a snapshot containing the fixed hook (the same chicken-and-egg that delayed the first 08-05 attempt `aa453cc6`). Permanent cure = seed from live `origin/main` at provision time. **Manual instant heal any session (clean tree): `git fetch origin main && git reset --hard origin/main`.**
- **⚠ METHOD TRAP (cost me my first hook Write): a `git reset --hard origin/main` run to clean test pollution ALSO discards uncommitted working-tree edits.** I made synthetic test commits ON `main` (wrong — use a throwaway branch) then `reset --hard` to undo them, which reverted my un-committed hook edit back to origin's version. Commit hook/code changes BEFORE any reset; never make test commits on `main`.
- **Current-state verification (all still current on the `950cdcf4` tip, no edit needed):** 35 Vercel crons · 122 DB-invariant pins · both launch flags `true` · 16 workers · 18 GHA workflows · 8 CI jobs · 5 published / 8 defined collections · 29 concierge tools · model `claude-sonnet-4-6` · RLS 361 tables + 130 views. It is still Aug 5 PT, so Recent sessions correctly holds Aug 5/4/3 — no tail-roll due.

### August 5, 2026 (Claude Code, interactive — long thread: "implement a TODO" → timezone work → "do it all" ×N → live incident response) — 11 green ships to `main` (bug fixes · viewer-timezone · dead-code · a11y · a DB incident-hardening migration), each CI-green + deploy-READY + regression-tested + ledgered (2026-08-05). No FMV/pricing/ingest MATH, auth/`proxy.ts`, or hot-wallet code touched.

- **Shipped set (each its own ledger entry + `git revert` path):** FMV history chart dates off-by-one for US viewers (`fmtDay` formatted a date-only bucket in local time) + the same class on `PinnacleFmvChart` (`5abc9c6f`, `0d89aabf`); 4 public `/insights` routes 500'd/blanked on `?limit=<non-numeric>` (NaN → `.limit(NaN)`), guarded with `Number.isFinite` preserving the `limit=0→1` clamp (`5abc9c6f`); `CollectionMomentTable` desktop row keyboard-nav (`5abc9c6f`); `fmtRelDate` "0y ago" for moments 360–364 days old (`515c4f7c`); front-door "Sales Indexed" 280K+→4M+ (`d6763754`); `FirstRunTour` real dialog a11y via `useModalA11y` (`e6e8a97a`); deleted **10** verified-zero-importer orphan modules across two commits (`84b3a9f3`, `5b33f54a` — held nothing; both coverage ratchets re-verified green after).
- **⚠ DURABLE — TIMEZONE MODEL (settled this session).** The daily **FMV/portfolio charts are UTC date-BUCKETS** (`DATE(computed_at)` / daily GROUP BY), so their axis MUST be formatted `timeZone:"UTC"` — local formatting mislabels the bucket a day earlier west of UTC (that WAS the bug). All three chart `fmtDay`s (FmvHistoryChart/PortfolioChart/PinnacleFmvChart) are now UTC. Real **time-of-day** values render in the viewer's zone — and the app ALREADY does this in client-fetched components (bare `toLocaleTimeString()` = viewer-local in the browser). `FreshnessStamp` (18 boards) was deliberately UTC since 2026-08-01 (SSR/crawler-safe: server+client render byte-identical, no `—`); reworked TWO-PHASE (`8ed46786`) — UTC on SSR/first-paint, swap to viewer-local after mount (post-hydration state update, no React #418). Server date-only stamps (wallet first-seen month, pinnacle sale/fmv dates) left UTC on purpose (a bare calendar date is correct in UTC; localizing only risks an off-by-one).
- **⚠ DURABLE — "Edge Requests Nx spike + external API failures" Vercel alert = SCRAPER → Supabase POOLER exhaustion, not a code regression and not raw-Postgres saturation.** Diagnose: `get_runtime_logs group_by=requestPath` (finds the flooded public page — here `/nba-top-shot/collection`, all `cache=MISS`/200, ~5 req/s bursts, bare URLs) + `get_runtime_errors` (the "external API failures" are PGRST003 "Timed out acquiring connection from connection pool" + "canceling statement due to statement timeout" across every DB route). `pg_stat_activity` can read CALM (raw PG fine, e.g. 4 active/90) while the **pooler** pool is what tips over — it's bursty/self-limiting (recovers between bursts; entity pages "degrade to empty"). **The fix is edge rate-limiting the scraper — Vercel Firewall per-IP rate limit (operator dashboard; the Vercel MCP has NO firewall/WAF write tool) — NOT a code change.** Entity pages are already ISR-cached (`revalidate=600`), so the load is volume-driven (thousands of distinct catalog URLs each a first-render cache-MISS); no caching win exists in code.
- **⚠ DURABLE — statement_timeout convention (role defaults + per-function caps).** Role defaults: **anon 3s / authenticated 8s / service_role 30s** (postgres unlimited). Public **entity pages query under `service_role` via `supabaseAdmin`** — so an anon-role timeout change is a NO-OP for them. The team deliberately per-function-caps the entity read RPCs so a slow read fail-fasts and frees its pooler connection: `get_edition_detail/_recent_sales/_fmv_history`, `get_player_*`, `get_team_activity`, `get_set_detail`, `get_series_detail` = **8s**; `get_team_detail` **25s**; `get_pack_detail_bundle` **30s** (pack EV is genuinely heavy — do NOT cap it low). This session closed a gap: `get_edition_market_bundle` / `get_edition_insight_links` / `get_edition_special_serials` inherited the 30s default while their same-page siblings freed at 8s → capped them at 8s (migration `audit_20260805_cap_edition_read_fn_statement_timeout`, warm 132/41/<1000ms so ~60× headroom, only bites under saturation). `ALTER FUNCTION … SET statement_timeout` is config-only (preserves body + search_path). **Revert:** `RESET statement_timeout` on the three fns.
- **Method reconfirmed:** ledger is concurrent-write-heavy (re-read fresh before every splice; rejected push → `git pull --rebase`, resolve the top-of-ledger conflict by keeping BOTH entries newest-first; `grep -c '^### '` must go UP); a11y toggle/nav pattern = `role="button"`+`tabIndex={0}`+`onKeyDown` guarded by `e.target===e.currentTarget`; TZ-forced tests bite where a UTC CI runner can't (`process.env.TZ` is honored at runtime for `toLocaleString`); dead-code deletion re-verifies zero-importer live (only refs were `check-brand-tokens.mjs` guard lines) and re-runs BOTH coverage ratchets before push; `actions_list` MCP result overflows the token cap → parse the saved JSON file with python.

### August 5, 2026 (Claude Code, interactive — "analyze test coverage → propose → proceed with all → keep going") — coverage suite is SATURATED, so the real value was verifying the ONE layer CI can't: DB pins vs LIVE prod. Three green ships to `main`; ledger entries 2026-08-05 each. Test/migration/hook only — no product/FMV/ingest/prod-DB mutation.

- **🔒 Audited all 121 DB-invariant pins against LIVE production and fixed 2 genuinely STALE ones (`e8e410ed`).** The in-CI drift guard is repo-vs-repo and structurally CANNOT catch a pin whose live definition drifted; only a live check can (`scripts/check-db-pin-staleness.mjs`, which needs `SUPABASE_SERVICE_ROLE_KEY` the sandbox lacks). **DURABLE METHOD — replicate it from the cloud sandbox via the Supabase MCP:** `execute_sql` runs elevated, so pull `pg_proc.prosrc` (or `pg_get_functiondef`, base64-encode big bodies with `encode(convert_to(...,'UTF8'),'base64')` to dodge JSON-escaping) for the pinned fn set, then compare to each pin's committed-migration body under the script's own normalization (collapse `\s+`, plus a comment-stripped variant — live prosrc is often comment-stripped vs the file). Found `get_wallet_collection_snapshot` + `holdings_summary` STALE: both wallet-total RPCs were redefined 2026-08-03 for UFC market-closure (`collections.market_closed_at` excludes closed collections from grand totals) and **applied via MCP with NO committed migration**, so pin+test+guard all stayed green while validating a body that no longer runs. Fix = author snapshot migrations byte-identical to live (no-op if applied), repoint PINS, update the embedded DDL, AND (per CLAUDE.md) re-check the ASSERTIONS — added a `market_closed_at` fixture column + a closed-market assertion to each test (moments still counted, value excluded). Validated end-to-end: drift guard 122/122, both now match live, both SQL tests pass on a throwaway `postgres:16`, new assertions mutation-proven to bite. Other 119 clean. **⚠ The cloud sandbox CAN stand up `postgres:16`** for `run-db-tests.sh`: bins at `/usr/lib/postgresql/16/bin`, `useradd pgtest` + `su pgtest -c "initdb -A trust -U postgres"` + `pg_ctl -o '-k /tmp/pgrun -p <port>'` + `CREATE EXTENSION unaccent` — but on RE-init in the same session the server can fail to start (`global/pg_filenode.map` missing); use a FRESH datadir/port and `select 1` to confirm connectivity before trusting a "test failed" (a down server fails on connection, not assertion — a false mutation-test pass).
- **🔒 Pinned the worst-incident edge fn's cursor safety (`8ad588c6`).** `ingest-allday-pack-opens` (the pack-opens walker behind the 2026-07-25 cursor-reset that re-walked ~31.4M blocks + the 2026-08-01 budget-stop drop) had NONE of its inline cursor safety pinned, unlike its topshot twin. New `__tests__/edge-ingest-allday-pack-opens-cursor.test.ts` source-drift-pins the getCursor discriminated-union (read error ≠ reseed), the forward `after = err||rerr ? start-1 : end` hold, and the descending-backfill arithmetic vs the tested `nextBackfillCursor` mirror; each mutation-proven. **Edge/worker/component layers are otherwise saturated on current `main`** (only `sync-nba-games`, a retired 410 stub, is genuinely untested — a path-reference scan over-counts because tests reference edge fns by NAME in arrays like `THROW_PATTERN_WALKERS`, not by import path).
- **🔧 Session-start hook now self-heals the mirror-lineage nag (`aa453cc6`).** ⚠ **DURABLE — web sessions here are seeded with a re-hashed MIRROR lineage of `main`**: local `main` shares ONLY the root commit with `origin/main` (merge-base == root, ~4000 commits each side, identical commit MESSAGES under different SHAs), frozen days behind. The Stop hook counts `origin/main..HEAD` and nagged "N unpushed commits" every turn; `.claude/hooks/session-start.sh` used `git merge --ff-only` which fails on this (non-ff) and treated the mirror as precious unpushed work. Added a realign arm to both hook branches fenced by *non-fast-forwardable AND merge-base == root AND (on-main) clean tree* → `git reset --hard`/`git branch -f`; genuine branch-offs (recent merge-base) or dirty trees fall through untouched. **Manual one-liner if it ever recurs before the hook runs: `git fetch origin main && git reset --hard origin/main` (clean tree only).** Root cause is upstream provisioning (out of repo); the hook makes it harmless. **Editing that hook is classifier-gated** (it adds `git reset --hard` to an auto-run file) — needs explicit user approval.
- **Method reconfirmed:** `npm install` then `git checkout package-lock.json` (Node 22 vs CI 24); ledger is concurrent-write-heavy (rejected push → `git fetch` + `merge --ff-only` + re-splice the freshly-read top, `grep -c '^### '` must go UP); large MCP/Actions tool results auto-save to a file — parse with python (`json.load(...)['result']`, slice the `<untrusted-data>` array), never trust an unauthenticated `curl` to the GitHub API (proxy-blocked → `None`).

### August 5, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only maintenance: rolled the tail (the 6 Aug-2 entries → `docs/sessions/2026-08.md`, verbatim) so Recent sessions holds ~3 days again; the rest of the file was already current through the 08-05 overnight pass + FMV sweep-wedge work. No product/DB/prod change; direct to `main`. Ledger entry 2026-08-05.

The working checkout arrived stale (local `main` at 07-30 with a diverged, rewritten history — 4023 local vs 4513 origin commits); reset the working tree to authoritative `origin/main` (`aa6fc440`) before touching anything. The file is a heavily-maintained living doc — the only maintenance actually *due* was the session roll (the file's own "keep ~3 calendar days" rule): Recent sessions had grown to 4 days (Aug 5/4/3/2, ~18 entries), so the 6 Aug-2 entries moved to the archive newest-first and the `2026-08.md` pointer now reads "August 2 → August 1". Did NOT fabricate factual "corrections": current-state facts were refreshed by the 08-04 docs pass and maintained by the 08-05 overnight pass, and the 08-05 daytime FMV sweep-wedge/ASK-disclosure work (`aa6fc440`, Cowork) carries its own ledger + handoff. Recent sessions now = Aug 5 (this + overnight) · Aug 4 · Aug 3.

**Follow-up same session (post-roll refresh + stale-branch cleanup):** spot-checked the checkable current-state facts against the repo + live DB and made TWO verified surgical corrections to the canonical reference lines (NOT the frozen dated session entries): **Vercel crons 34 → 35** (`abf47065` added `/api/cron/golazos-discover-buyers` `18 8 * * *`) and **DB-invariant pins 117 → 122** (the `PINS` array in `__tests__/db-invariants-drift-guard.test.ts` measured 122; the campaign continued 08-04→05). Everything else re-verified current: both launch flags `true`, 5 published / 8 defined collections, 18 GHA workflows, 16 workers, model `claude-sonnet-4-6`. **Stale `claude/*` branch cleanup:** deleted `claude/claude-md-docs-roq9yy` (local + remote); `todo-implementation-q5rrov` (fully merged) + `kp3f6m` (content already in `main` via a rewritten sha) are safe to delete but **branch deletion is blocked by a persistent proxy 403 policy** — Trevor must clear them from the GitHub UI. `todo-implementation-qi4350` is **kept**: its 3 commits look unmerged but its DB migration (`retire_deposit_scanner_ownership_scaffold`) is ALREADY committed to `main` under a different timestamp (`20260805015722`) and applied to prod (verified: all dropped objects gone, migration recorded) — merging it would duplicate an applied migration; its only unique content (2 operator handoff docs + a `code-todos.md` #2 wording update) belongs to the active session that authored it.

### August 5, 2026 (overnight autonomous pass, ~01:06 PDT) — GENUINE OVERNIGHT, git restored; shipped 0; post-ship watch of the 08-04 CC wave ALL PASS; one new profiled QUEUE

Quiet honest night. Git worked (the "no space" clone class recurred on `/sessions` so the clone went to `/tmp`; otherwise fully functional, push available). No clock skew. Shipped 0 / reverted 0 / repaired 0. Full log: `docs/handoff-2026-08-05-overnight-pass.md`; ledger + metrics updated.

- **Post-ship watch — 08-04 daytime CC wave ALL PASS, 0 reverts.** `bc99369c`'s only knock-on (the `fmv_clamp_disconnected_ask` ACL reset) was already re-granted by CC `a9960e16` — verified live `proacl` includes `cron_heavy=X`. Corroborating deltas all improving: **`fmv_sweep_stall_pct_24h` 45.2→4.4** (the queued FMV-RECALC-MAXDURATION item de-prioritizes), `candy-editions` no longer stalled, `allday-unmapped-resolver-tail` 45.5%→27.8%.
- **Health green-with-known-noise.** Security fully clean (all 4 `[]`); `stalled_pipelines` `[]`. Three trust breaches all documented non-defects: `panini_sale_price_capture_dry_days`=8 (upstream outage), `public_board_slow_count`=7 (4→7, contention/do-not-re-budget), `unmapped_resolution_backlog_max`=105 (draining). Sentry 4 unresolved = entity-page saturation class, no new. DB 12,108 MB.
- **NEW QUEUE (profiled) — `wallet-username-resolver` 33.9% statement timeout.** `wallet_usernames_unresolved()` re-aggregates the whole 21-day counterparty universe every ~20 min (buyer+seller of `sales` + `pack_purchases`) to return ~4 rows; two warm EXPLAINs = 81.9s/85.3s, ~630 MB working set, ~104k Heap Fetches from stale VM on hot append tables. No low-risk additive fix (covering index already exists; VACUUM decays hourly). Levers: operator cadence-cut (~20min→hourly, negligible freshness cost on a 14-day-negative-cached @handle nicety) or a code RPC watermark rewrite (live-function replacement, needs tests). Not shipped.
- Also queued LOW/cosmetic: `topshot_flowty_backfill` `cursor_stalled` false positive pinned at `SPORK_FLOOR_HINT`.


### August 4, 2026 (Claude Code, interactive — drained the "Golazos shell drain stalled" handoff → "keep going" ×3 → verified) — 🩹 **ROOT-CAUSED + FIXED the Golazos wallet-scan silent-failure that stranded 3,822 wmc shells, then VERIFIED it worked end-to-end (shells 3,822 → 0, sales undercount 134 → 87).** Commits `922fd2c1` (borrow fix + honesty guards) · `2c18f7ff` (pagination gate) · `9cb057ca` (verification); each its own ledger entry (2026-08-04) + revert path. Code+test only; no migration, no prod-DB mutation.

- **The bug — a nil Cadence borrow masquerading as an empty wallet.** `wallet-backfill-golazos`'s details scan (`runAllDayDetailsBackfill` + `GET_GOLAZOS_MOMENT_DETAILS`, live since 07-31) borrowed the NARROW `&{NonFungibleToken.Collection}`, which returns nil for wallets whose capability is published only as `CollectionPublic` → the script returned a genuine `[]` → logged `on_chain_count:0 / no_more_moments / ok:true`, indistinguishable from a real empty. **Root-caused from telemetry, NOT a guess:** 7d of golazos runs showed ONLY `no_more_moments` + `pagination_failed` (never `error`/`timeout`), so `fcl.query` RESOLVES `[]` (it rejects on script failure) → a nil borrow, not a throw. Provenance clinched it: canonical wallet `0x4ba45c2312086820` had `max_onchain_ever=0` across every scan yet held 1,832 wmc rows all written in one 07-25 18:16 bulk pass — the live route had NEVER successfully read it.
- **The fix — borrow the BROADER `&{NonFungibleToken.CollectionPublic}`** (both public-path attempts). Verified against `onflow/flow-nft` `NonFungibleToken.cdc` via WebFetch: `CollectionPublic` declares BOTH `getIDs()` and `borrowNFT()`, and `Collection` inherits it, so CollectionPublic resolves for the currently-working wallets PLUS the nil-borrow ones (the sibling ID-only `CADENCE_GOLAZOS` already proved that borrow resolves for these wallets). ⚠ **DURABLE — Flow REST is 403 from the cloud sandbox and no Cadence MCP was connected, so on-chain testing was impossible; the safe path was (1) verify the contract INTERFACE from the public standard source, (2) ship self-verifying (a failing script → visible ok:false, never silent/corrupting), (3) scheduled self-check to confirm.**
- **Honesty guards (so `on_chain_count:0` can never again silently mean "the scan failed"):** a non-array `fcl.query` result → `ok:false non_array_scan_result` (universal); an empty scan for a wallet that STILL has cached wmc rows → `ok:false empty_scan_but_cached_holdings` (+ `cached_row_count`), gated by new config flag `flagEmptyWithCachedHoldings` set ONLY on the Golazos route so AllDay's verified-healthy empty path is untouched. Both skip the clean-refresh stamp so the wallet is retried. Plus a pagination-misroute gate: the comp-limit/access-500 catch branches routed EVERY caller into the AllDay paginated path (`mode:"allday"`) — there's no Golazos range script — so gated on `detailsMode==="details_allday"` (AllDay byte-for-byte unchanged; Golazos surfaces an honest failure).
- **✅ VERIFIED ~2.5h post-deploy via a scheduled self-check:** canonical wallet `0 → 1,832`; **shells 3,822 → 0**; runs returning moments ~23% → **66%**; **0** `empty_scan_but_cached_holdings` (nothing masked). Downstream fired AUTOMATICALLY as traced — `promote_unmapped_sales` **Path 4** (resolves via `wmc.edition_key` → `editions.external_id`, called in `golazos-sales-indexer`'s finally block every run): **shell-blocked unmapped sales 45 → 0, unmapped_open 134 → 87.** Closes Gate 1 item 3's Golazos shell drain. DURABLE: enriching a shell is SUFFICIENT to unblock its sale — no map rebuild — because Path 4 reads wmc directly.
- **⚠ ENVIRONMENT — local `main` was a stale DIVERGENT line (4023 vs 4469 commits, merge-base = initial commit), not just behind.** Prod deploys `origin/main`; the recent Golazos work (`b60e2833`, `925b54f5`) was only on origin. `git checkout -B main origin/main` FIRST or you fix the wrong code and push non-fast-forward. Method notes reconfirmed: `npm install` then `git checkout package-lock.json` (Node 22 vs CI 24); ledger is concurrent-write-heavy (rejected push → fetch, re-splice the freshly-read file, `grep -c '^### '` must go UP).

### August 4, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only refresh; the file was already current thanks to today's concurrent maintenance (the 08-04 bug-sweep wrap-up landed at 12:44Z), so only the roadmap summary had drifted behind one commit. No product/DB/prod change; direct to `main`. Ledger entry 2026-08-04.

- **The single post-CLAUDE.md-touch change was `cbe019fb` (13:08Z, docs-only):** roadmap §5.1 "dust-floor post-ship verification" CLOSED — **Gate 1's only blocking item cleared.** The `fmv-recalc` sweep is now confirmed complete (13,605 eds recalced/24h > 11,606 traded), full-cohort TS ratios landed on the unfloored `cold-tail` control (median 1.000 / p90 1.176 / >2× **0.37%**, down from 1.110 / 2.576 / 461 floored), the `fmv_apply_thin_sale_haircut` cohorts converged (symptom, not a second defect), and §5.3's "9.2% >2×" resolved to 0.37%. Folded these into the "Prioritized next actions" roadmap summary.
- **Kept honest:** `cbe019fb` re-measured the FMV-vs-median accuracy RATIOS, NOT the HIGH/MEDIUM confidence-SHARE headline metric — so that caveat ("shares due a re-measure") stays in place rather than being marked resolved.
- **⚠ ENVIRONMENT (same as the 08-04 bug-sweep note below):** the sandbox's local `main` was a stale pre-2026-08-03-purge shadow (diverged history, tip 07-29); worked off the true `origin/main` tip. A healthy clone follows the normal "commit direct to `main`" rule.

### August 4, 2026 (Claude Code, interactive — "implement a TODO → work through bugs → keep going → do everything mentioned → land on main") — 14 SAFE self-contained fixes (a11y · display-format · route-robustness · a stateful-UX bug · workers/edge), found via four parallel Explore-agent sweeps and **merged to `main`** via branch `claude/todo-implementation-q5rrov`. Ledger entry (2026-08-04) carries per-fix `git revert <sha>` paths. Each fix is its own commit + a regression test proven-to-bite (the 2 Deno edge fns excepted). No FMV/pricing MATH, ingest-write, cursor, auth, or hot-wallet code touched.

- **⚠ ENVIRONMENT — the session's local `main` was a stale pre-2026-08-03-purge SHADOW** (shared only the initial commit with the real `origin/main`; ~4023 vs ~4442 commits), so work was built on the harness branch off the real `origin/main` tip, then landed via a `--no-ff` merge — SHAs preserved (revert paths stay valid) and the merge tip carries code so the Vercel deploy fires (no docs-only-tip suppression). A healthy clone follows the normal "commit direct to `main`" rule.
- **✅ The 2 Deno edge fns (`flowty-proxy`, `special-serial-delta`) were DEPLOYED later the same day** (v23→v24, v4→v5) via the Supabase MCP — see the 2026-08-04 PROD EDGE DEPLOY ledger entry. ⚠ **That deploy corrected this file's long-standing repo↔deployed divergence note, which pointed the WRONG WAY:** deployed carried inline `jsr:` URLs while the repo imports by **bare specifier** via `supabase/functions/deno.json`, so the documented "minimal diff, keep the inline import" recipe would have boot-failed `special-serial-delta`. The right move is to pass `deno.json` in `files` **and** set `import_map_path: "deno.json"` (what `supabase functions deploy` does) — which also *closes* the divergence instead of perpetuating it. Deployed bodies are now byte-identical to repo.
- **A11y (4):** wired `PaywallModal` + the `MobileNav` collections sheet to shared `lib/hooks/useModalA11y` (were Escape-only / no keyboard+focus); made `WalletProfile` loan rows + `CollectionMomentTable` mobile cards keyboard-operable (clickable `<tr>`/`<div>`, chevron a plain `<span>` so click was the only expand path). **DURABLE toggle-card pattern:** `role="button"` + `tabIndex={0}` + `aria-expanded` + `onKeyDown` guarded by `e.target === e.currentTarget`. **Navigation cards with an inner `<Link>` (PackTable) were LEFT ALONE** — a card-level tab stop just duplicates the link.
- **Display correctness (2):** `lib/analytics/format.ts::fmt()` tested the raw value against its M/k thresholds → negatives rendered `$-1500.00` not `-$1.5k` (losing-wallet P&L); `components/profile/_shared.ts::fmtDate()` read LOCAL month/day off a date-only string → PortfolioSparkline axis off-by-one west of UTC. **DURABLE: `new Date("YYYY-MM-DD")` is UTC-midnight — use `getUTC*` for display; branch money formatters on `Math.abs(n)` with the sign re-attached.**
- **`sales_2026` year-boundary class (2, incl. 1 edge fn):** `/api/profile/activity` + the `special-serial-delta` fallback queried the hardcoded `sales_2026` partition on a rolling now-Xh window → returns empty once the date rolls into 2027. Now read parent `sales` (Postgres prunes by `sold_at`). **⚠ FOLLOW-UP: grep for other `.from("sales_2026")` + rolling-window readers — there are likely more.**
- **Route robustness (2):** `/api/badges` NaN limit/offset → `.range(0,NaN)` → 500 (siblings guard it); `/api/analytics/packs/fresh` uncapped `parseLimit`. **Stateful UX (1):** `PackTable` Sort dropdown inert for the 4 sort-only keys (mount-only state, no prop-sync) → syncing `useEffect`. **Alert honesty (1):** FMV-alert "In-app" was stored as a nonexistent in-app channel (→ telegram, never fired) → relabeled "Telegram". **RTR (1):** lock-ROI worst-5 accent overlapped below 10 rows → gated on `≥10`. **Workers (2):** `dune-proxy` limit NaN (mirrors offset guard); `reddit-proxy` marked error responses cacheable → starved announcement retries.
- **Verification:** full primary suite **8942 tests** + component suite **894** green; `tsc --noEmit` clean; eslint clean on touched files (residual `no-explicit-any` only; no eslint CI gate).

### August 4, 2026 (overnight autonomous pass, ~01:03 PDT) — GENUINE OVERNIGHT, NO-GIT / queue-only (sandbox bash VM down 2nd night running); shipped 0; post-ship watch of the 08-03 CC wave ALL PASS; health green with 2 known non-defect breaches

Fired in-window, no skew. Bash/`useradd` sandbox-disk failure (same class as 08-03) → all git unavailable; plus an **active concurrent Claude Code session** on `claude/todo-implementation-q5rrov` (preview deploys every few min) → queue-only regardless. Shipped 0 / reverted 0 / repaired 0. Outputs were mount-only and unpushed; **committed later the same day by the tree-cleanup session** (handoff `docs/handoff-2026-08-04-overnight-pass.md`; ledger 2026-08-04).

- **Post-ship watch — 08-03 CC wave ALL PASS, 0 reverts, and the fmv-recalc accuracy payoff LANDED.** Cursor fix `484d08d7` is paging (`page_size=500`/`has_more=true`/ok=true) and TS HIGH+MED FMV jumped **3,416→6,855**, AllDay **394→1,618** (the sweep now reaches the full catalogue — the roadmap headline metric ~doubling for TS). allday-resolver-tail partial index dropped the fail rate ~39%→~25% (residual UNION split deferred); `fmv_clamp_disconnected_ask` runs per-collection with a healthy 1–2-row trickle; candy-editions maxDuration 300→800 (`98f12079`) is deployed READY (08:40Z tick is its unexercised first real test).
- **Health.** Security fully clean. Two breaches, both known non-defects: `public_board_slow_count`=4 (the exact set CC analyzed 08-03 — topshot_first_mint_* "plans optimal, do not fix", candy board fixed on buffers, cross_collection_deals trivially 1.05×; load/threshold under contention, do NOT re-budget autonomously) and `unmapped_resolution_backlog_max`=105 (draining by design). `fmv_sweep_stall_pct_24h`=45.2 climbing toward 50 = the queued maxDuration item. **`sales_serial_supply_worst_pct` breach cleared 5.53→0.14; pinnacle-sync recovered.** Sentry 4 unresolved, all known saturation/smoke-flap classes (RLS smoke flap is a verified false alarm). DB 11,852→11,987 MB.
- **DURABLE — the bash/`useradd` sandbox-disk failure has now blocked the overnight pass 2 nights running (08-03, 08-04).** When it recurs the pass can still do the full health triage + post-ship watch via the Supabase/Vercel/Sentry/Cowork connectors and write continuity outputs to the mount (Read/Write file tools) — only git (clone/fetch/collision-gate/commit/push) and DB migration *shipping discipline* are lost. Ship 0 is the correct output; nothing was revertable or blind-shippable.

### August 3, 2026 (Claude Code, interactive — drained the "UFC market closure is presentation-only" handoff) — recorded market closure as a **DB fact** and made the pricing + wallet pipelines respect it, so Gate 1 item 4 is genuinely closed rather than closed at the banner layer. `main` @ `20cef621` (CI #2476 green, 8/8); 5 MCP migrations + a ledger entry (2026-08-03) with per-item revert paths. No FMV pricing MATH changed — only confidence LABELS + total INCLUSION.

- **THE DURABLE PATTERN — market closure now lives at THREE layers, each with its own job (do not collapse them):** (1) presentation — `lib/market-closed.ts`, a static map keyed by URL slug for pure/sync surfaces (seo, banners), shipped earlier in `53ff1fa5`; (2) the DB fact — new `collections.market_closed_at timestamptz` (UFC = 2026-05-13), the source of truth for "may a pipeline publish this as a current price / fold it into a wallet total?"; (3) enforcement (below). ⚠ **`market_closed_at` is deliberately NOT `is_active`/`published`** — those are nav flags and the standing rule is nav flags must never gate data (the Candy `published`-flag $0-wallet incident). When a market closes, set `market_closed_at` AND the `lib/market-closed.ts` entry (same date).
- **Pricing enforcement = ONE BEFORE INSERT trigger, not N route patches.** `fmv_snapshots_cap_closed_market_confidence` (migration `audit_20260804_fmv_cap_confidence_closed_market`) caps confidence HIGH/MEDIUM/LOW/SALES_ONLY/ASK_ONLY → STALE for any collection with `market_closed_at` set (STALE keeps the last value under an honest label; NO_DATA untouched). A trigger was correct because fmv-recalc has **8 insert paths** + fmv-backfill + future writers — the defect source was Step-6's carry-forward re-stamp that copies prior HIGH/MEDIUM forward as fresh `1.7.0`, which no route-level patch on the "main" path would have caught. Mirrors the existing `fmv_snapshots_block_*` guard triggers. Result: UFC HIGH/MEDIUM **14→0**; scope-checked that TS/AllDay/Candy/Golazos are byte-unchanged (only UFC carries the fact). A one-off UPDATE relabeled the current latest snapshot per edition so it's honest immediately; the trigger makes that durable (a bare UPDATE would drift back to MEDIUM within 24h — the daily snapshot age proves it).
- **Wallet totals have THREE independent grand-total computations — there is no single chokepoint.** All three now exclude `market_closed_at` collections from the grand $ total while KEEPING their moment count + adding `market_closed_at` per per-collection entry: `get_cross_collection_portfolio` (→ `/api/portfolio` → CrossCollectionPortfolio), `get_wallet_collection_snapshot` (→ `/api/collection-snapshot` → `/share` + support-chat), and `holdings_summary` (→ `refresh_seeded_wallet_stats` → `seeded_wallets.cached_fmv_usd` → profile/wallets-hub/OG). Fixing the RPC is the only layer that also corrects the server-rendered OG images + support-chat text. After editing `holdings_summary` you MUST re-run `refresh_seeded_wallet_stats` for affected wallets (did all 108 seeded UFC-holders) or the cached total lags. UFC value stays in `wmc.fmv_usd` (for the count/display); it is simply no longer summed.
- **⚠ MCP `apply_migration` GOTCHA (cost me a near-double-apply):** a migration whose body includes a slow statement (a partitioned-`fmv_snapshots` UPDATE here) can blow the **60s MCP client timeout while the server transaction keeps running and COMMITS afterward.** The tool returns "timed out"; the change may still land. **Before retrying, verify actual state** (`pg_trigger`/`pg_proc`/a data probe) — a blind retry hit `23505 pg_proc duplicate`. Keep slow one-off backfills OUT of the DDL migration, or expect this.
- **Method notes:** session-start hook left local `main` on stale **pre-2026-08-03-purge** history (diverged 4023/4434, merge-base = initial commit) — `git checkout -B main origin/main` first, always. Deps aren't installed in a fresh sandbox (`npm install` then `git checkout package-lock.json`; `npm ci` fails on Node-version skew). `tsc` clean, component gate green (+4 tests pinning the closed-market UI: WalletStatRow closed branch, CrossCollectionPortfolio card+disclosure, PortfolioSummary cost-basis suppression), no `lib/**` or `app/api/**/route.ts` touched. Rakic carve-out (the one edition with an 82-day sale) NOT taken — UFC publishes no confident price at all. **Revert:** `git revert 20cef621` (RPC bodies + UI) + `ALTER TABLE collections DROP COLUMN market_closed_at` + `DROP TRIGGER/FUNCTION fmv_snapshots_cap_closed_market_confidence` (full SQL in the ledger).

### August 3, 2026 (Claude Code, interactive — "implement a TODO" → "keep an eye out for issues" → "keep going" → "wrap up for memory") — re-confirmed NO safe live TODO, then a full health sweep found ONE real bug and shipped it: the smoke gate's retry helper was a silent no-op on timeouts. `main` @ `c741e338` (deploy READY, CI green); ledger entry 2026-08-03 + revert path. Smoke-route only; no product/FMV/ingest/DB.

- **SHIPPED — `smokeFetchRetry` reused the caller's one-shot `AbortSignal.timeout(N)` on its retry, so the retry aborted INSTANTLY on any timeout — a silent no-op on the exact class it exists to tolerate** (`app/api/smoke-test/route.ts`, `c741e338`). `isTimeoutOrTransient` caught the abort and fired the retry, but the retry reused the same already-fired signal → immediate re-abort. This weakened EVERY caller (sniper-feed, og, wallet-search, public-profile), and the two `resolve-and-associate` probes had never been migrated onto the helper at all (bare `smokeFetch` = zero retry) — which is why Sentry `NEXTJS-K` flapped **43× over 3 months** on "operation was aborted due to timeout". Fix: `smokeFetchRetry` now builds a **fresh** `AbortSignal.timeout(SMOKE_RETRY_TIMEOUT_MS=15000)` on the retry when `init.signal?.aborted` (reuses `init` otherwise, so a non-timeout transient keeps its live budget); + converted the flapping Phase-4.1 correctness probe onto it (left the 4.2 latency probe on bare `smokeFetch` — a retry would muddy its `<3s` assertion and it wasn't flapping). +2 regression tests (`__tests__/api-smoke-test-html-contains.test.ts`); the fresh-signal one was **proven to fail against the pre-fix helper**. **DURABLE: the existing unit tests passed the whole time because they mock `fetch` with no real signal — a mocked-fetch test can't catch an AbortSignal-lifecycle bug; when a helper's contract is about signal reuse, assert the signal identity per attempt.** `checkUrl` was always correct (it builds a fresh `AbortSignal.timeout` inside each of its 3 fetch blocks); the inline `smokeFetchRetry` was the odd one out.
- **DURABLE — a Sentry statement-timeout cluster (57014) on entity pages is almost always SATURATION collateral, not a slow query; profile WARM before "optimizing".** Four issues (`NEXTJS-1Z` pack /16 users, `1Y` team, `23` player, `24` set) all first-seen Aug 2 = the fmv-recalc sweep-cursor stall (`484d08d7`) that re-priced ~1,000 editions every 20 min and saturated the pool. Proof it was collateral, not code: `get_pack_detail_bundle` warm = **16.8ms** (a timeout needs ~8000ms), the team/player RPCs already carry their slug expression indexes, and every event predated the fix's 20:42Z deploy with zero recurrences since. Resolved all four (Sentry auto-reopens as a regression if they recur — so resolving a confirmed-collateral cluster is correct, it makes a genuine recurrence stand out instead of hiding in the pile). CLAUDE.md already says this ("A pool-acquire timeout is a SATURATION symptom… profile warm vs cold first") — this extends it to *statement* timeouts on the read RPCs.
- **The health sweep itself (all green on the Aug-4 tip):** typecheck + 8923/8923 tests · the Aug-3 4-item queued ledger list all drained same-day · monitor inbox empty · security advisors 0 ERROR (the 7 SECDEF WARNs are allowlisted — `check_secdef_anon_exec_drift()` = `[]`) · `check_pgcron_recent_failures()` = `[]`. Two trust-health BREACHES were both already-fixed-and-decaying (`fmv_sweep_stall_pct_24h` 62.2 → cursor now marches `0→11500→wrap`, `edition_limit:500` under the 1000 cap; `candy-editions-ingest` stall → `maxDuration` already 300→800, awaiting its 08:40Z tick) and one (`unmapped_resolution_backlog_max` 105) is a deliberate honest-open finding the metric itself says "DO NOT raise breach_at". **The `git filter-repo` stale-local-`main` gotcha recurred — reset to `origin/main` FIRST** (session-start hook said diverged 4023↔4434; `origin/main` is the live canonical, local was a day-behind fossil).

### August 3, 2026 (Claude Code, interactive — "analyze test coverage → propose improvements → do all that makes sense → wrap up for memory") — the coverage suite is SATURATED; found + closed the one real gap, reverted one theater add, mapped where the frontier actually is. Test-only; `main` @ `74855950`; ledger entry 2026-08-03 + revert path.

- **⚠ THE STALE-LOCAL-`main` GOTCHA RECURS WITH A SECOND SYMPTOM — always realign before ANY analysis.** The session-start hook said "diverged, 4023 and 4430 different commits," but the real tell was a **711-file / +52k−14k content diff** between local `HEAD` and `origin/main` — local `main` was a day-behind, author-rewritten fossil (the 2026-08-03 `filter-repo` force-push rewrote every sha). My first coverage pass was run against it and was **almost entirely stale** (it "found" gaps that were already closed). Fix is the documented one: `git checkout -B main origin/main` FIRST, then analyze. **Do not trust a coverage/gap finding taken before you've confirmed `git diff --stat HEAD origin/main` is small.**
- **DURABLE — the coverage FRONTIER MAP (so the next "improve coverage" session doesn't re-derive it): every structural gate is saturated or blocking.** Primary gate (`lib/**` + `app/api/**/route.ts`, thresholds ~87.85/73.35/90.7/90.35, live 88.74/74.37/91.68/91.13) = **0 untested routes, 0 truly-untested lib modules** (the ~8 my import-grep flagged all have real test refs — the grep misses `@/`-alias imports, so confirm a "gap" by `grep -rl <basename> __tests__` before believing it). Component gate saturated + rot-guarded (`component-gate-include-completeness.test.ts`), now covering auth/onboarding/pricing/filters/marketplace-status + `app/insights/**/*Client.tsx`. `edge-deno` is **BLOCKING** (no `continue-on-error`). `proxy.ts` pinned. Every weak measured file is documented-deliberate (`support-chat`), dead code (`pinnacle-listings-reconcile`'s `ASK_UNIFY_RETIRED` rollback), or un-driveable inline Flow-REST scan bodies. **The ONLY unmeasured surface left is the stateful client monoliths** (`app/dashboard/page.tsx` 2,326 lines, the collection/sniper/analytics/market bodies) — NEITHER gate touches `app/**/page.tsx`, and covering them needs product-code extraction + rendered-DOM validation (medium-risk, not a safe blind-sandbox push). So point a future coverage session at a SPECIFIC feared surface, never the aggregate — chasing the % here is the theater CLAUDE.md warns against.
- **SHIPPED (the one real gap) — pinned the fatal-`ok:false` "green while blind" contract on the golazos + ufc sales indexers** (`__tests__/api-golazos-ufc-sales-indexer-deep.test.ts`, commit `74855950`). No test drove the leg where a throw in the enrichment/write body must still log `pipeline_runs.ok=false` (vs a silent `ok:true` tick). The lever: `makeInstrumentedSupabaseFixture(fixtures, { failWrites: ["sales"] })` makes the sales insert THROW → drives the fatal catch (distinct from a resolved 23505). **All 6 forward sales indexers now pin this**: candy (ME-error path), topshot `sales-indexer` (sealed-height-down, logs via a `pipeline_runs` table write not the `p_ok` RPC arg — so grep both), pinnacle (observability test), allday (cursor-read-failure, line 534), + golazos/ufc (this thread). **⚠ the two siblings surface a fatal differently** — golazos defers the scan via `after()` (returns 200, fatal logged in the deferred body) while ufc scans inline (returns **500**); assert the `log_pipeline_run` p_ok:false, NOT the HTTP status.
- **DURABLE — MEASURE a coverage add before keeping it; "different trigger, same catch" adds nothing.** I wrote the same fatal pin for allday too, then measured the allday route branch **stayed 47.08** (allday already covers that catch via its cursor-read test; the genuinely-uncovered allday `1167-1172` is a *different* resolver-logging catch my test didn't reach) and **reverted it rather than ship theater**. golazos's gain was real and measured (route branch 43.33 → 43.7, the fatal catch newly covered). Coverage attribution is per-LINE — always `--coverage.include` the one route file and confirm the number moved before committing.
- **Method notes (unchanged, reconfirmed):** `npm install` then `git checkout package-lock.json` (Node 22 vs required 24); vitest via `npx vitest run` (the `--reporter=basic` flag errors in this version — use default); the ledger is concurrent-write-heavy (my push rejected once, rebased, re-spliced my entry BELOW the concurrent session's two new entries, `grep -c '^### '` verified up); commit author must be set to `noreply@anthropic.com` per-commit for the verified-signature stop-hook.

### August 3, 2026 (Claude Code, interactive — "find a TODO → implement one" → "look for issues" → "do as much as you can" → "keep going" → "make your own judgements" → "wrap up for memory") — long thread: re-confirmed NO safe live TODO, then shipped **15 fixes + 1 reasoned decline** across security, correctness, and the FMV-ingest cursor-safety class, driving `main` green each time. Every item its own `docs/overnight/ledger.md` entry (2026-08-03) + revert path. Code/test/DB-grant only; no FMV pricing MATH touched.

- **⚠ SESSION-START GOTCHA (will recur): the fresh clone's local `main` was a STALE PARALLEL HISTORY vs `origin/main`** — `git merge-base` was the *initial commit* (diverged 4023↔4405), an artifact of today's `git filter-repo` force-push. `git checkout -B main origin/main` to realign to the authoritative remote BEFORE any work; the local commits are a sandbox fossil, not lost data. Symptom: session-start hook says "diverged, N and M different commits."
- **THE DURABLE FIND — a silent-data-loss cursor class across 8 event indexers.** Block-chunked indexers advanced the cursor to `targetHeight` even when a chunk's fetch **THREW** (network/timeout/parse), permanently skipping those blocks' listing/sale events. Fixed in all 4 listing + all 4 sales indexers, in **three structural variants**: (a) cursor-written-after-loop → cap to `firstFailedChunkStart-1` (allday/topshot/golazos/ufc-listings, allday/golazos-sales); (b) **ufc-sales advances the cursor PER-CHUNK inside the loop** → a mid-range failure lets a later chunk LEAPFROG the gap, so it `break`s on first failure; (c) **topshot `sales-indexer` uses `fcl.send` (THROWS on failure, vs the REST helpers returning `[]`)** with two per-fetch catches + two cursor sites → track across both, cap both via a shared `cursorTarget`. Re-scan is safe (all writes idempotent — sales dedup on `transaction_hash`, listings upsert). +2 regression tests proven to bite. ⚠ **CRUCIAL NUANCE: `fetchEventRange` DEGRADES an HTTP 500/429 to `[]` ON PURPOSE** (the dormant Flowty-fork legs 500 by design; the "survives a 500" tests encode it) — only the THROW path is the bug; do NOT "fix" the intentional HTTP-degrade.
- **DECLINED with reasoning (own judgement): do NOT make a live-leg HTTP-500 hold the cursor.** It would wedge every FMV indexer at once during a broad Flow-REST 500 storm; distinguishing live vs dormant legs is fiddly; the safer bounded-retry needs prod 500-rate + run-duration metrics the sandbox lacks. Measure-first, not a blind fix. (Logged as a DECIDED ledger entry.)
- **⚠ CI-GUARD-TRIPS-ON-REFACTOR (cost 2 red jobs; verify CI after pushing — local `tsc`+vitest does NOT run the brand or coverage gates).** Two source-grep guards broke on *correct* refactors: (1) `check-brand-tokens.mjs` forbids raw `zinc-N`/white/black Tailwind neutrals in PROTECTED surfaces (must be `var(--rpc-*)` tokens; **semantic colours like emerald/amber/rose/sky/orange are always allowed** — that's why the new SALES_ONLY=sky passed but STALE=zinc failed → switched STALE to orange); (2) `invariants-postgrest-cap.test.ts` greps watchlist source for `.from("fmv_current")`, which the chunking refactor moved into `selectInChunks(client,"fmv_current",…)` → widened the regex to accept the helper arg. When you refactor a pinned pattern, the grep guard breaks even though intent holds — update the guard, don't revert the fix.
- **The shipped set (all in the ledger):** best-offers empty-editionKeys leg; two read-IDORs (`portfolio/history` + `profile/export-csv`, the latter also had `wallet_address`→`wallet_addr` schema drift) closed via `requireOwnedKey`; `FmvConfidence` type was narrower than the DB `fmv_confidence` enum → SALES_ONLY/STALE/NO_DATA badges mislabeled (widened + styled); **watchlist uncapped `.in()`** → a >1000-edition watchlist silently drops FMV/floor so `below_target` alerts stop firing (new shared `lib/supabase/chunked-in.ts`); **`mv_topshot_pack_realized_ev` was anon-selectable at `/rest/v1/`** (materializing a view re-adds the default anon grant — same class as the 08-01 `mv_topshot_market_index_daily` revoke; revoked via MCP, verified service_role retained); 2 Candy-MLB label gaps (dead "ROOT NETWORK" OG pill — use `dbChain` not roadmap `chain`; missing `candy_mlb` tc-report label); candy-sales-indexer parking non-dup insert failures instead of dropping; dead `/api/pack-roi` deleted (zero callers, hardcoded null ROI). **Sandbox note:** `npm ci` fails (Node 22 vs CI 24) → `npm install` then `git checkout package-lock.json`; `get_advisors`/`actions_list` MCP results overflow the token cap → parse the saved file with python.

### August 3, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only refresh; the file was already current thanks to today's concurrent maintenance, so only four current-state facts had drifted. No product/DB/prod change; direct to `main`. Ledger entry 2026-08-03.

- The session opened on a **stale local `main` fork** — today's `git filter-repo` force-push (leaked-cookie purge, ledger top note) rewrote history, so the fresh clone read as diverged 4023↔4403. Aligned local `main` to `origin/main` (authoritative, current through today) before touching anything; the old local commits were superseded history, no data lost.
- Verified CLAUDE.md's checkable facts against the repo + live DB. Four had drifted: (1) **Vercel crons 33 → 34** (`grep -c '"schedule"' vercel.json` = 34 — the `allow-list-reconcile` + `topshot-listing-cache` GHA→Vercel-cron migration, net of the dead `pinnacle-sync` removal `e719e5e5`); (2) **GitHub Actions "17 scheduled / ci.yml is the one non-scheduled" → "15 scheduled / 3 manual"** (`allow-list-reconcile.yml` + `topshot-listing-cache.yml` are `workflow_dispatch`-only after their Vercel-cron migration); (3) **pg_cron ~54 → 78 active jobs** (live-measured); (4) the dated **public tables/views snapshot 340/122 → 361/130** (example only — the actual invariant, 0 rows `rowsecurity=false`, re-verified = 0).
- Re-verified still-current, no edit needed: **117** DB-invariant pins (`PINS` array), **8** CI jobs, **16** workers, both launch flags `true` (Candy 07-31 + Panini 08-01 live), **8** collections / **5** published, **29** concierge tools, primary+component coverage thresholds, concierge model `claude-sonnet-4-6`.

### August 3, 2026 (Claude Code, interactive — drained the FMV-sweep-cursor-stall handoff) — 🩹 **UNSTUCK THE FMV SWEEP: it had been re-pricing the same ~1,000 editions on every run, leaving 74% of the traded catalogue never recomputed** — plus a 126× fix to a slow PUBLIC board. Commits `a989aebc` (ledger) · `484d08d7` (fix) · `3655cf44` (board); each its own ledger entry (2026-08-03) + revert path. CI-green, deploy `dpl_4K43mhkkCbseaJTqgJautqoJaLSh` READY.

- **🩹 SHIPPED — `/api/fmv-recalc` cursor never advanced (`484d08d7`).** It asked `fmv_recalc_edition_page` for a **2,500**-edition page over PostgREST, which clamps RPC results to `db-max-rows`=**1000** and returned 1,000; `hasMore = pageEditionIds.length === limit` (1000 !== 2500) → false → `cursor_after=NULL` → **every run restarted at offset 0, `ok=true` throughout**. Verified live before shipping: 20h of runs all `cursor_before='0'`/`cursor_after=NULL`; the page fn returns **2,500** called direct in SQL vs **1,000** through PostgREST; population is **11,606** editions with a sale in the 30d window. Fix: `DEFAULT_LIMIT` 2500→**500**, new `MAX_PAGE_LIMIT=900` ceiling on `body.limit` (was 5000 — a manual call could re-trigger the truncation), `hasMore` `===`→`>=`, a cap-hit tripwire, `page_size`/`has_more` telemetry, and a `Number.isFinite` guard on the limit parse (`Math.min(NaN,x)` is NaN — the same class already swept out of `/api/edition-history` and `/insights`). **VERIFIED IN PROD: first run on the new code went `cursor_before=0 → cursor_after=500`, `has_more=true`** — the first cursor advance since the cursor was introduced. **✅ AND THE ACCURACY PAYOFF LANDED: by sweep position 6500/11606 (56% of one pass), editions publishing FMV >2× their own 30d realised median fell from the roadmap's 264 TS / 194 AllDay to `18` / `12` — 463 → 30, −94% — with p90 ratios 1.889→1.167 (TS) and 3.000→1.214 (AllDay), collapsing toward the never-floored `drain_fmv_cold_tail` control (1.060), median holding 1.000.** This closes the 08-02 dust-floor question: the removal (`3809425b`) was right all along and simply could not REACH the catalogue while every run restarted at page 0. **Revert:** `git revert 484d08d7` (no DB unwind; the sweep just returns to page 0).
- **⚠ DURABLE — chose 500, NOT the handoff's 900, because of a risk the handoff never measured: fmv-recalc runs average 181s against `maxDuration=300` and *95 of ~402 invocations in 72h (23.6%) are KILLED at the wall* before writing a terminal row.** A killed run logs no `cursor_after`, so the next run retries the SAME offset — harmless while the cursor was pinned at 0, but once it advances a systematically-slow page could re-stall the sweep. 900 sits inside the band where a quarter of runs already die. **QUEUED (not shipped — cost decision): `maxDuration` is 300 but the Vercel Pro cap is 800, so the route's own comment calling 300 "the Vercel Pro maximum" is WRONG.** Raising it would likely eliminate the kills.
- **⚠ DURABLE — the guard for this class already existed one layer away and nobody noticed.** `__tests__/invariants-fmv-recalc-chunking.test.ts` enforces the identical 1000-row cap on every `.in()` chunk site *and its header comment states the cap outright* — it just never covered the page fetch. Extended it there (+5 assertions), mutation-tested: restoring `2500` and `===` reds exactly the two intended assertions. **When you find a silent-truncation bug, grep for an existing guard on the same constant — the fix is usually to widen its scope, not to write a new file.**
- **SHIPPED — `candy_special_serials_board`, a PUBLIC board: buffer touches ~397,000 → ~42,000 (**~9×**) (`3655cf44`, `audit_20260803_candy_special_serials_board_union_arms`).** ⚠ **DURABLE — do NOT quote wall-clock speedups on this Micro instance without a like-for-like load comparison.** I first recorded "7,388ms → 58ms (~126×)"; re-measuring the SAME fixed view under load returned **28,174ms** with an identical plan and identical buffer counts. Wall-clock here swings ~500× with I/O contention, so a quiet "after" against a contended "before" measures the load, not the fix. Like-for-like under heavy load: 82,137ms → 28,174ms (~2.9×). **Buffer accesses are the load-independent metric — use them.** It was **2.73×** over its liveness budget and one of the 3 boards holding `public_board_slow_count` in BREACH. ⚠ **The 3 breaching boards are NOT the two that arm's own `catches` text names** (`topshot_perfect_mint_premiums_board`, `topshot_pack_reality_dist` — both since re-budgeted); the live three are `candy_special_serials_board` (2.73×) plus `topshot_first_mint_trophy_stats` (1.04×) and `topshot_first_mint_trophies` (1.02×). Cause: a four-way OR on `serial_number` that Postgres can't satisfy from an index, so each of 125 editions scanned ~203 serials and filtered 198 away (25,375 index entries / 6,623 heap fetches for 607 rows), with the correlated FMV join re-running **607 times (327,554 buffers)**. Split into three `UNION` arms inside a LATERAL → index probes, and the FMV join collapsed to one Hash Right Join (1,101 buffers). **`UNION` not `UNION ALL` is load-bearing** (a serial can satisfy two arms). Equality proven BEFORE applying: 607 rows both ways, **both directions of `EXCEPT ALL` = 0**.
- **⚠ DURABLE — two hypotheses tested and REJECTED before shipping; measure, don't pattern-match.** (1) The candy slowness looked like the 08-01 VACUUM/visibility-map win, but wmc measured **98.4% all-visible**, 4.5% dead, autovacuum 40 min prior — it was query shape, and a VACUUM would have decayed within hours on a table that hot. (2) For `topshot_first_mint_trophies` I tried the *same* restructure (restrict the `other_serial_avg` aggregate to the ~662 editions the inner join keeps, instead of all 11,164) — provably equivalent, and **5× SLOWER (39,071ms vs 8,216ms)**: at 73% selectivity one parallel seq scan beats 672 per-edition index probes. **Its existing plan is already optimal — do not "fix" it.** The two marginal boards are 2–4% over a budget set at 3× warm; that is a threshold question, not a query defect.
- **SHIPPED — closed-market disclosure finally reached UFC's two densest priced tabs (`53ff1fa5`), closing Gate-1 item 4.** ⚠ **The 2026-08-02 honesty pass shipped only the SEO half: `lib/market-closed.ts` is imported by `lib/seo.ts` and NOTHING else**, so titles/JSON-LD disclosed the closure while the rendered tabs did not. `MarketplaceStatusBanner` covered overview/collection/sniper/edition but **not `sets` or `analytics`** — and those are the two densest priced surfaces UFC registers (`pages: overview, collection, sniper, sets, analytics`): analytics renders `total_fmv` + per-tier/per-series FMV + a locked/unlocked split (a whole portfolio valued on a market whose last Flow sale was **2026-05-13**), and sets renders `lowestAsk`/`fmv` per missing piece (a cost-to-complete nobody can transact at). Per-row staleness can't catch this — UFC snapshots keep re-stamping `computed_at`, so freshness reads green on year-old evidence; the collection-level fact is the only honest signal. Additive (the banner returns `null` for healthy collections; verified `/nba-top-shot/analytics` unchanged). Guard `__tests__/invariants-closed-market-disclosure.test.ts` pins the mount on all 5 UFC tabs, **cross-checks the registry so a NEW UFC tab fails the guard rather than silently escaping it**, and asserts *behaviourally* that `isMarketClosed()` resolves both `ufc` and the `ufc-strike` alias (the alias is registered by assignment, not a literal map key — a source regex would pin syntax, not the guarantee). Mutation-tested.
- **⚠ DURABLE — measured, then declined: `topshot_fmv_pct_stale_30d` must NOT be re-baselined until a full sweep completes**, and the Gate-1 **pack-EV "actionable vs retired" item is a product decision, not an honesty defect** (the 08-02 pass already labels the 4,488 non-actionable rows; how a public board *defaults* its presentation is Trevor's call). Accuracy checkpoint recorded in the ledger for the next session: at sweep position 1500/11606, editions publishing >2× their own 30d realised median are **TS 263 / AllDay 200 (463)** vs the roadmap's 458 — **unchanged by design**, since the sweep pages most-recent-first while that cohort sits in the stale tail.
- ⚠ **Don't hand-trigger `fmv-recalc` to verify — it races the cron.** My manual POST landed 35s after a cron tick; both concurrent runs died at the 300s wall. The heartbeat row (`pipeline='fmv-recalc-heartbeat'`, written synchronously at `after()` entry) carries `edition_limit`, so it proves which code is live **without** triggering anything.

### August 3, 2026 (Claude Code, docs — "analyze repo → update CLAUDE.md to current state") — docs-only refresh of this file's stale current-state facts for the work that landed AFTER the 08-02 CLAUDE.md refresh (`c1d25139`). No product/DB/prod change; direct to `main`. Ledger entry 2026-08-03. The landed-since work these corrections capture (each shipped by other sessions, mostly Trevor/Cowork, with its own ledger entry + revert path):

- **🩹 THE $0.50 FMV DUST FLOOR WAS REMOVED AND SHIPPED — `3809425b` (Trevor/Cowork), supersedes the 08-02 "QUEUED, Trevor's call" note below.** The decision was delegated to Claude and made on a **production control group, not a model**: `drain_fmv_cold_tail()` already prices Top Shot with a plain 30d median and NO dust floor, so the same collection/window gave a clean A/B — the unfloored writer landed at median 1.000× / p90 1.060× vs the floored `fmv-recalc` at 1.110× / **2.576× p90** (>2× ratio: 1 edition vs 461). The floor discarded **46% of Top Shot / 76% of All Day** 30d sales before any pricing math, and because survivors are the top of the distribution, published FMV sat above market. Removed as step 1 of `dampenGrailSpike` in [lib/fmv-recalc-math.ts](lib/fmv-recalc-math.ts); **KEPT** the real downside protection — low-serial grail removal, outlier band, cap, and the `0.2×`-median band in `wapWithoutOutliers`. Simulated on 26,802 baselined editions: moved closer/further 2,643/455 (5.8:1); >2×-own-median 1,814→276; p90 ratio 3.135→1.459. Pack-EV decreases are the correction landing (EV/truth 1.325→0.996 TS, 1.750→1.065 AllDay), not a regression. **Revert:** `git revert 3809425b`.
- **🩹 `pinnacle-sync` false-stall fixed + a dead Vercel cron removed — `e719e5e5` (Trevor/Cowork) + test pin `8d1b9827`.** The premise "never calls `log_pipeline_run`" was FALSE (it has logged ok=true daily at 10:07Z since PIN-SYNC-OBS, visible in the indefinite `pipeline_runs_daily` rollup, not the 73h `pipeline_runs`); the real defect was narrower — it logged ONLY from inside `after()`, so a dropped deferred run left no row while the work still completed and `detect_stalled_pipelines` false-paged. Added a synchronous `phase:"invoked"` marker + fatal-catch. Also REMOVED the `/api/cron/pinnacle-sync` `0 6 * * *` Vercel entry: it has **never once executed the route body** (Vercel Cron only sends `CRON_SECRET`; the route accepts only `INGEST_SECRET_TOKEN`, so every 06:00 tick 401'd). vercel.json now carries **34 crons**. Owners are now one HTTP (cron-job.org 10:07Z) + one pg_cron backstop (jobid 200 `rpc-pinnacle-fmv-recalc-backstop`, `37 22 * * *`). Pinned by `__tests__/api-cron-pinnacle-sync.test.ts` (4→8 tests, proven to bite).
- **DB-invariant pin layer 115 → 117** — added `remap_topshot_parallel_to_base_misattributed` (`abe40f79`) + `remap_topshot_base_keyed_parallel_sales` (`aede2afd`), the parallel↔base sales-misattribution remappers, plus `compute_pinnacle_serial_fmv_multipliers` (`6b87f982`) and `roll_pack_ask_hourly_low` (`e1ff6f93`) — each byte-identical snapshot migration + SQL invariant test, and the whole layer re-audited against live prod (`6b87f982`).
- **⚠ NEW ROADMAP + NEW GATE — [docs/strategy/roadmap-2026-08-03.md](docs/strategy/roadmap-2026-08-03.md) (`e719e5e5`) supersedes roadmap-2026-07-18: accuracy is the GATE, not a phase.** "Zero users is the correct output of the current input" — every growth tactic is removed rather than demoted until the data beats the sites collectors already use. New headline metric: **share of prices at HIGH/MEDIUM confidence** — Golazos 0.7%, AllDay 6.3%, UFC 3.2%, Top Shot 17.3%, Candy 60.8%. **NEW GATE-1 DEFECT found — ✅ NOW RESOLVED (re-measured live 2026-08-03 evening):** Candy `wmc.fmv_usd` was NULL on all 25,375 rows, so a Candy wallet totalled **$0** despite Candy being 100% priced in `fmv_snapshots`. The `wmc-fmv-populate` arm has since drained it — **25,375 rows, 0 NULL `fmv_usd`, 0 NULL `edition_key`, $137,607.85 total**. Do not re-open this item.
- **🩹 `classify-acquisitions-multicollection` bounded-window fix (08-03 handoff Items 2+3, `c571e9f3` All Day + `8e62cf26` UFC follow-up; not a cron dropout).** The candidate scan drove off partitioned `sales` oldest-first via bitmap heap scans that can't stream, so it burned its 90s `statement_timeout` proving the ~612k already-classified 2022–2025 rows empty and died before reaching the 2026 partition where every real candidate lives — a killed `after()` then left no `pipeline_runs` row, indistinguishable from a cron that never fired. Fix added `p_since timestamptz DEFAULT NULL`; the route passes a 14d window for the two costly-but-empty legs (All Day, then UFC — whose market has been CLOSED since 2026-05-13, so it was scanning 813k sales hourly to prove nothing at **68.3s/tick**), Golazos stays unbounded at 0.2s. All Day leg unbounded-TIMEOUT → 3.5s; drained 166 starved acquisitions; added a synchronous `phase:"invoked"` marker + fatal-catch. **⚠ DURABLE — the window predicate MUST be `s.sold_at >= COALESCE(p_since,'-infinity')`, NEVER `(p_since IS NULL OR s.sold_at >= p_since)`.** The OR-form is non-sargable — the planner can't prune partitions or use `sales_YYYY_collection_id_sold_at_idx` through it, so the window buys nothing (measured identical 14d window: OR-form **42.9s** vs COALESCE-form **3.5s**; only COALESCE reports `Subplans Removed: 6`). Ledger 2026-08-03. **Revert:** `git revert 8e62cf26 c571e9f3` + the three `audit_20260803_*acquisitions*`/`*serial_supply*` migrations.

### August 2, 2026 (Claude Code, interactive — "analyze test coverage → do all you can", long multi-session thread) — the DB-invariant pin layer grew **89 → 115 pins** (+26 in the 08-02 wave) + 2 live-board/CI fixes. Almost all test/migration-only (each snapshot migration byte-identical to live = no-op vs prod); the 2 code fixes are CI-green with a regression test proven to bite. Each item its own `docs/overnight/ledger.md` entry (2026-08-02) + `git revert <sha>` path.

- **DB-INVARIANT PIN CAMPAIGN — the biggest lever both vitest gates structurally can't reach; now 114 pins.** The 08-02 wave pinned the FULL insider-signal detector family (`detect_floor_drops`, `detect_concentration_buys`, `detect_unusual_edition_volume`, `detect_new_edition_early_buyers`, `detect_topshot_sweeps` — the **fabricated-market-signal** class: a threshold/dedup regression INVENTS or drops an alert), the two sales serial-number write guards (`update_sale_serial` + `update_topshot_sale_serial` — a regression silently corrupts every serial-keyed FMV multiplier / special-serial / #1-premium), the canonical player-write path `upsert_player_canonical`, `record_serial_backfill_failure`, `draw_raffle`, `admin_verify_wallet`, `bump_concierge_ip_rate`, `close_expired_cached_listings`, `pinnacle_upsert_nft_map`, `mark_signal_wallets_fully_enriched`, `claim_pipeline_lock`, `check_feature_quota`, `apply_topshot_supply`, `resolve_golazos_listing_edition_ids`, `check_set_completion`, `stub_editions_from_wmc`, and `compute_pinnacle_serial_fmv_multipliers` (money-critical Pinnacle serial-FMV). All MCP-applied with no committed migration → previously UNPINNABLE; each got a byte-identical snapshot migration + SQL invariant test + drift-guard PIN, validated on local `postgres:16` + **mutation-tested** (every distinctive guard proven to bite). The whole 112-pin layer (111 distinct fns) then audited **0 STALE / 0 NOT_IN_LIVE** vs live prod (MCP-replicated `check-db-pin-staleness.mjs`, control-proven to bite).
- **⚠ DURABLE method notes recorded in the pins:** (1) a `public.random()` override does NOT shadow unqualified `random()` inside a `SET search_path TO 'public'` function (pg_catalog resolves first) → pin `draw_raffle`'s deterministic SAFETY properties (winner always a real entrant across 40 draws), not band-level determinism; (2) `ON CONFLICT DO NOTHING` can MASK a dropped `NOT EXISTS` pre-filter mutation → pin the observable `stubs_created` contract + behavior-CHANGING mutations (collection-scope, unknown-slug); (3) isolating a "new-edition guard" needs a fixture that enters the candidate set AND has an old first-sale AND a burst within that old window, or the mutation is masked. Deliberately SKIPPED theater pins (`bridge_integer_fmv` post-dedup no-op; `resolve_special_serials_from_ownership` dormant/uncalled).
- **SHIPPED — `/insights/candy-mlb` Holders tab was rendering only 250 of 407 holders (`49b92983`).** `CandyBoardClient`'s `DataTable` silently slices to its `cap` with no "showing N of M"; the Holders table's explicit `cap={250}` sat below the live count while the tab badge showed the full `holders.length` — so the UI said "Holders 407" while dropping 157 lowest-serial wallets. Same class already fixed for Serials. Raised holders `cap` 250→800 + fetch ceiling 600→800; +1 regression test proven to bite. The other candy tabs are safe (deals/spread on default cap 400, scarcity 130, players/market have no count badge).
- **SHIPPED — `components/packs/WalletPacksView.tsx` no longer paints a stale pack-history response (`2c5c9ad2`).** The `load()` fetcher had NO stale-response guard while sibling `TeamChecklist.tsx` uses `reqIdRef`. Two ways it bit: a filter change resets `page` in a SEPARATE effect, so switching Opened/Unopened/Sold while on page ≥1 issues two overlapping fetches (stale `offset=page*25`, then `offset=0`) with no ordering guard → a slow stale response paints page-2 rows under a "Showing 1–25 of N" footer; and rapid filter switches race. Fixed with the same monotonic `reqIdRef` guard (bump on entry, drop response if `myReq !== reqIdRef.current`, gates catch/finally). +1 regression test proven to bite. A bug-hunt over the rest of `components/**`/`*Client.tsx` found every other fetcher already guarded.
- **⚠ DURABLE — CI-unblock: unbroke `main`'s red `unit-tests` job left by the concurrent Cowork `c61a9d8e` inventory fix (`1f225226`).** That fix changed `loadCachedMomentIds`→`loadCachedMomentIdsAndKeys` (presence-only Set → enrichment-aware Map) and updated the Pinnacle + paginated fixtures for the new shape but MISSED the AllDay one (a cached row lacking `edition_key` mapped to `false` → not skipped → assertion flipped). The lib was correct and untouched; fixed the fixture + added the missing test for the behaviour `c61a9d8e` shipped (a cached row with `edition_key: null` must be RE-WALKED). **When a fix changes a shared helper's RETURN SHAPE, grep every fixture that feeds it — the miss was invisible locally because the widened type (`edition_key?: string | null`) already type-checked.**

### August 2, 2026 (Claude Code, interactive — "find a TODO → implement one", multiple threads) — re-confirmed NO safe live TODO, then swept two real defect classes: the UFC canonical-slug link class (4 more emitters) and the ingest-clobbers-player-team class. Code+test only; direct to `main`, CI-green; each fix a regression test proven to bite. Each its own ledger entry (2026-08-02) + revert path.

- **SHIPPED — 4 more sites in the UFC canonical-slug-vs-alias link class (`4b456ffa`)**, in the insights board clients + dashboard formatter the 08-02 morning sweep didn't reach. Each built a collection URL via a naive `slug.replace(/_/g,"-")`, turning the sales-table long-form `ufc_strike` into the **`ufc-strike` alias** — a self-canonicalizing duplicate that RENDERS instead of canonical `ufc`. Fixed by routing through `fromDbSlug(c) ?? c.replace(/_/g,"-")` (byte-identical for the other 4 collections): `top-sales`/`market-pulse`/`trophies` board clients + `lib/dashboard-format.ts` `collectionMetaBySlug` (there a UFC holding lost its name/color on `/dashboard` since `ufc_strike` isn't a registry id). **STILL OPEN (deliberately not bundled — routing change, larger blast radius): a 301 `/ufc-strike/*`→`/ufc/*` redirect for full canonical dedup.**
- **SHIPPED — ingest stopped clobbering current team + minting duplicate players (`2777296f`), + a daily re-derive so the baked snapshot can't rot (`3275c58f`).** The blind `.upsert()` overwrote `players.team` and minted one row per `flow:<playID>`; a new SECDEF `upsert_player_canonical` COALESCE-fills team (never overwrites) and resolves-or-creates on canonical identity, and a pg_cron job re-derives current team daily. `upsert_player_canonical` was pinned as a DB invariant the same day (see the pin-campaign entry above).
- **SHIPPED — suppressed the flapping `ufc_sales` CRITICAL alert arm + corrected UFC market causation/coverage in monitoring (`c2ad305c`).** The Flow UFC market is closed (0 sales in 30d, last sale 2026-05-13), so an alert keyed on UFC sales silence pages forever on a by-design-dead market; suppressed with the causation documented, not the arm weakened blindly.

### August 2, 2026 (Cowork — INVENTORY ACCURACY + HONESTY passes) — recovered/enriched empty wallet rows, caught an ARMED data-destruction bug, and stopped the site publishing numbers it KNOWS are wrong. Trevor's gate is now explicit: **expect zero users until the data beats the sites collectors already use — so publishing a knowingly-wrong number is worse than publishing nothing.** Code + 3 prod migrations (EV-neutral / disclosure-only); NO FMV pricing math touched. Commits `c61a9d8e` (inventory) · `bb09ea6a` (honesty); ledger 2026-08-02 + per-migration revert SQL.

- **⚠ ARMED DATA-DESTRUCTION BUG caught + disarmed — `scan-pinnacle-wallet` (`c61a9d8e`).** Its upsert payload carried `serial_number: null`/`series_number: null`/null metadata for unmapped ids, and a PostgREST upsert overwrites EVERY payload column — harmless only because it threw on every call since 2026-06-10, until a same-day repair made it FUNCTIONAL (destructive). Verified NO damage (0 of 14,077 serialed-type rows missing a serial), deployed v26 omitting serial/series from the payload. **DURABLE: repairing a function that's been failing silently can convert a latent bug into a live one — audit the WHOLE write payload before re-enabling.**
- **Inventory recoveries.** Golazos: 4,796 of 9,494 empty wallet shells recovered via `sales` (edition_key NULL 9,494→4,698; the rest never transacted on an indexed marketplace); root cause was `runAllDayDetailsBackfill`/`runPinnacleDetailsBackfill` loading cached ids as a PRESENCE-ONLY set + `seed-wallet-refresh` dispatching `skip_cached:true`, so pre-existing empty shells were skipped every tick forever — fixed enrichment-aware (closed the class). Candy: 18,932 un-enriched rows → 0 on a PUBLICLY-LIVE board (Candy writers upsert directly and never called the metadata post-pass). **Pinnacle 72.2% NULL serials is an HONEST GAP — do NOT schedule backfill:** serials are a per-edition-TYPE property (84 serialed types at 100%, 294 never-serialed; zero mixed), an upstream Disney design fact; suppress the serial column conditionally on edition type.
- **HONESTY PASS (`bb09ea6a`) — stopped 3 knowingly-wrong publications.** (1) **UFC was publishing a dead 62-day-old price to Google as a transactable schema.org `Offer`** — the `lib/seo.ts` Offer was guarded only by `confidence !== 'STALE'`, which a stale MEDIUM passes, and the snapshot `computed_at` itself LIES (15 editions carry TODAY at MEDIUM/HIGH off sales 470–524 days old). New static dated `lib/market-closed.ts` (keyed by canonical slug AND the `ufc-strike` alias): closed markets emit NO Offer, titles read "Last Value … (Flow market closed)". UFC in cross-collection totals MEASURED (0.49% of all wallet FMV) and deliberately NOT re-valued — Trevor's call. (2) **Pack EV** now labels the 4,488/4,596 rows for packs nobody can buy (`On sale`/`Secondary only`/`Retired`, fails CLOSED) and discloses that AllDay/Golazos EV is an ORIGINAL-supply model (pool rows never decremented), and `edition_count` was overstating the pullable pool up to 27× (fixed to `weight > 0` under the basis in use). (3) **Pinnacle serials** — `is_serialized` is unusable (Open-Edition rows carry `is_serialized=true` over null-serial holdings); use `edition_type`, helper returns true/false/**null='cannot say'**. **Panini banner was NOT a defect** — it reads `panini_coverage_summary` LIVE (38.8% now vs the 47% doc snapshot), so the DOC was stale; the real gap closed was surfacing `oldest_family_refresh_h` (a 16-day-stale parallel sat invisible behind a uniform-freshness impression).

### August 2, 2026 (Claude Code, interactive — FMV dust-filter decision doc) — ANALYSIS ONLY on this thread: measured the blast radius of the `DUST_PRICE_USD = 0.5` sale floor and recommended deleting it. Docs-only commit; **no code, no migration, no prod-DB change**. Ledger entry 2026-08-02. **✅ SUPERSEDED — the recommendation was ACCEPTED and SHIPPED 2026-08-02 (`3809425b`, Trevor/Cowork); see the Aug-3 entry above. The QUEUED status below is historical.**

- **The finding:** the absolute $0.50 floor at [lib/fmv-recalc-math.ts:8](lib/fmv-recalc-math.ts) / `:116` (step 1 of `dampenGrailSpike`, before any pricing math) discards **46.0% of Top Shot** and **76.3% of All Day** 30d sales, and `sales_count_30d` is the POST-filter count — so RPC under-reports volume as well as over-reporting price. Attribution is a clean dose-response: published FMV ÷ the edition's own raw 30d median is **1.03×** where the filter touches nothing, **1.55×** where it cuts some, **2.28×** where it empties the edition (All Day 1.00× / 1.97× / 3.38×). Mark-to-market, the affected editions transacted **$24,464** in 30d that RPC values at **$35,503 (+45%)**. Full option analysis, R1–R9 risks, exact diff, and verification checklist: [docs/fmv-dust-filter-decision-2026-08-02.md](docs/fmv-dust-filter-decision-2026-08-02.md).
- **DURABLE — `fmv_current` for actively-traded editions has TWO writers, and a platform-wide FMV scan HIDES this class of defect unless you split by `algo_version`.** Besides `/api/fmv-recalc` (`1.7.0*`), `drain_fmv_cold_tail()` (`cold-tail-1.0*` — 4,504 TS + 2,099 AllDay editions) writes a plain `PERCENTILE_CONT(0.5)` over ALL 30d sales with **no dust floor, no outlier removal, no grail guard**. Editions the dust filter empties fall silent, age 7 days, then get rescued at an honest price (ratio 1.000) — which is why the platform-wide average looks fine while the `1.7.0` rows are 1.55–2.28× wrong. Two side effects: that rescue is **7+ days stale by construction** (mean snapshot age 73.6h vs 41.7h), and `purge_fmv_snapshots_today()` deletes a same-day cold-tail row for a dust-emptied edition without fmv-recalc replacing it.
- **DURABLE — the constant was never derived from data.** `e3aee286` (2026-06-10) introduced it as step 1 of a 4-step guard aimed at the HIGH side (the "$9,000 Serial #1 Jokić" class); steps 2–4 are what the commit, the audit and all 40 unit tests actually describe. Its assumed downside protection already exists twice over — the `0.2 × median` band inside `wapWithoutOutliers` and steps 2–4 for the high side — so removing it does not remove a guard (with it gone, min published FMV is **$0.19** TS; only 72/8,481 editions land below half their own median). It is also **not** wash-trade defense: **zero** self-trades platform-wide in 30d, 1,094 distinct TS buyers below $0.50, and the price histogram is continuous through the cut (modal TS price = **$0.25 × 5,354 sales**, the marketplace minimum).
- **`fmv_apply_thin_sale_haircut` needs no fix** — a symptom, not a second defect (misfires on 691 editions but removes $127 total, and they stay ~2–2.8× their own median afterward); it stops selecting them once counts are honest. **Pinnacle is out of scope on both counts** (render-keyed in `pinnacle_fmv_history`, and fmv-recalc explicitly `.neq`s it); Candy 0 price changes; UFC 0 sales.

### August 2, 2026 (Claude Code, interactive — "find a TODO" → "do bug-hunt + fix, coverage, implement anyway" → "keep going"×4) — re-confirmed NO safe live TODO, then swept a real defect CLASS across 4 batches: 6 UI/SEO-correctness fixes, all CI-green, each a regression test proven to bite. Commits `71f51c61` · `dcf32622` · `38366de6` · `08a7c594`; each its own ledger entry (2026-08-02) + `git revert <sha>` path. Pure code+test, no DB/prod-data.

- **TODO sweep re-confirmed the standing finding** (exhaustive over code + `supabase/functions/**` + `workers/**` + `docs/code-todos.md`): every live marker is RESOLVED-reference, launch-flag-gated defensive placeholder (the two `candy-*` `note` branches — keep), or shelved operator-blocked/superseded drafts. Did NOT manufacture one; pivoted to a bug-hunt (three parallel `Explore` agents: lib+routes, components+workers+edge, app-page-layer).
- **THE DURABLE FINDING — the UFC canonical-slug-vs-alias class, now SWEPT.** `"ufc"` is canonical everywhere (`lib/collections.ts` `id` + sitemap `urlSlug` + `fromDbSlug` + UUID map); `"ufc-strike"` is accepted only on INPUT (`collection-slug.ts` alias, for old bookmarks) — and `getCollectionByUrlSlug` resolving it means alias URLs RENDER (pages don't `notFound()`). Multiple per-collection maps keyed only one form → wrong output. Fixed all live emitters/consumers: (1) `ASK_LABEL` (edition + `moment/[id]`) rendered "Floor ask" on canonical `/ufc/edition/*` because it keyed only `ufc-strike`; (2) `urlSlugForCollection` + alerts `COLLECTION_URL_SLUG` EMITTED `/ufc-strike/*` links (duplicate-canonical); (3) `lib/seo.ts`'s 3 label maps (read by ~16 metadata/JSON-LD helpers) lacked the alias so `/ufc-strike/*` pages branded themselves **"Flow"**. All fixed additively + registry cross-check tests (`urlSlugForCollection`==`fromDbSlug`, `COLLECTION_URL_SLUG[uuid]`==`getCollectionByUuid().urlSlug`). Confirmed all 5 published collections have `id`==canonical `urlSlug` (UFC is the sole alias). **STILL OPEN (deliberately not bundled — routing change, larger blast radius):** a 301 `/ufc-strike/*`→`/ufc/*` redirect for full canonical dedup; the alias URL still self-canonicalizes.
- **ALSO SHIPPED — TeamChecklist stuck "Indexing" banner** (`components/entity/TeamChecklist.tsx`): the poll-budget-exhausted branch `return`ed without `setIndexing(false)` — a no-exit terminal state that showed the false banner forever until reload. One-line fix + first-ever test of the polling path (fake timers; ⚠ RTL `waitFor` deadlocks under fake timers — drive with `act`+`advanceTimersByTimeAsync`, and the progress mock must return a FRESH object per poll or `setProgress(sameRef)` bails React out and stalls polling at one tick). And **`/analytics/sets/[set_id]` rendered the raw `candy_mlb` slug** as its collection name (title/meta/JSON-LD/badge) — a LOCAL `COLLECTION_LABEL` copy drifted behind the canonical `lib/analytics-sets-dashboard-compute` map; fixed by importing the canonical + adding `candy_mlb`.
- **⚠ DURABLE METHOD — DB-verify a page-layer finding before shipping.** The app-page-layer agent flagged *Pinnacle* as the unlabeled set collection; querying `analytics_sets_directory(NULL)` against prod showed **zero** pinnacle sets (Pinnacle has no analytics "sets") — the real live leak was `candy_mlb` (1 set, in the pre-rendered top-100, public since 07-31). Checking the live RPC flipped the finding. Same discipline caught that the "Pinnacle set page" was theoretical.
- **Untouched on purpose:** ~20 other duplicated `COLLECTION_LABEL` maps (riskier consolidation; most analytics RPCs exclude Candy via `is_active`, unverified) and the live **IOPS-saturation alert** (MV refreshes failing / entity 500s — operator/night-pass territory, FMV/ingest/wmc off-limits for autonomous shipping). **Method notes (this cloud sandbox):** working push credential present; `npm install` then `git checkout package-lock.json` (Node 22 vs CI 24); vitest via `node_modules/.bin/vitest`; ledger + main are concurrent-heavy — stash→ff→pop→splice the freshly-read ledger (never whole-file rewrite); the GitHub-Actions MCP `list_workflow_runs` result overflows the token cap and saves to a file (parse with python).

### August 2, 2026 (Claude Code, interactive — "analyze the repo → update CLAUDE.md to the most recent state") — docs-only refresh of this file's stale current-state facts + a summary of the Aug 1-evening→Aug 2 landed work not yet captured in Recent sessions. No product/DB/prod change; direct to `main`. Ledger entry 2026-08-02.

- **Corrected two stale CI current-state facts.** (1) `edge-deno` is now **BLOCKING at 0 errors** (`33b207e3`), NOT the "NON-BLOCKING, 16 errors, toolchain conflict" this file described — the root cause was an INVOCATION bug (missing `--config supabase/functions/deno.json` so the import map never applied); the 2026-07-31 diagnosis was wrong on all three claims and the `jsr:→npm:` remap it prescribed must not be resurrected. (2) `db-pin-staleness.yml` now **ENFORCES** (`f322aadf` ledger) — only `NEXT_PUBLIC_SUPABASE_URL` was missing (the service-role key existed since 04-02), added via `gh secret set`; first real dispatch checked 90 pins, 90 clean; weekly Mon 07:20 UTC. Both were previously logged as environment-blocked handoffs; both are now closed. Fixed the CI-jobs bullet + the DB-invariant-tests bullet accordingly (dated historical session entries left frozen as written).
- **The landed Aug 1-evening→Aug 2 work these corrections reflect (shipped by other sessions, each with its own ledger entry + revert path):** players dedupe (`09c55e75`) — wallet-search keyed players by `flow:<playID>`, minting one row per play, so a new SECDEF `resolve_canonical_player()` resolves-or-creates on `(collection_id, name-slug)`; TS players 4,433→1,359, 1,401 editions repointed, 0 orphaned, current-team baked into `players.team` (148 rows, since the 08-01 tie-break fix was load-bearing on the duplicates); `/insights/pack-reality` un-broken (`358b6850`) — materialized the 4th slow leg `v_topshot_pack_realized_ev` (7,722 ms → 0.18 ms; the route was a guaranteed 500 rendering "NO QUALIFYING PACKS YET" while 184 dists qualified, then Vercel cached the lie) + finished the tier-token migration; the e2e rendered-DOM monitor stopped failing on 4 auth-gated pages (`01392e4f`); `?specialSerials` un-broken on the legacy market path (`9871dcc2`); the 2026-08-02 overnight pass (`ef119040`) shipped 0 (IOPS-saturation breach QUEUED not blind-shipped), post-ship watch ALL PASS, 4 inbox drained; and coverage-extraction batches A–F peeled the last clean byte-identical pure-logic clusters out of the `app/**` page monoliths into tested `lib/` (analytics/shape, four page clusters, grail-premium EV, special-serial badge allowlist, sets/display) — the safe blind-sandbox extraction lever is now noted exhausted.

### August 1, 2026 (Claude Code, interactive — "find a TODO → do all you can" → "do the lanes you mentioned") — re-confirmed NO safe live TODO, ran a two-lane bug-hunt, shipped ONE anon-reachable 500 fix. Commit `b4328b17` (+ ledger `a0d301b8`); direct to `main`, CI-green, deploy `dpl_8qKmTEoHKDbghbi8UY7bV1jrZN57` READY.

- **The TODO sweep re-confirmed the standing finding** (exhaustive `\bTODO\b`/`FIXME`/`HACK`/`XXX` over code + `supabase/functions/**` + `workers/**` + `docs/code-todos.md`): every live hit is a resolved-`RESOLVED`-reference, a launch-flag-gated defensive guard (the two `candy-*` `note` branches — constants filled, unreachable, keep per CLAUDE.md), or shelved operator-blocked `docs/drafts/panini/*`. `lib/format.ts` was an `XXX`-in-`$X,XXX.XX` false positive. Did NOT manufacture one — instead pivoted to the two lanes prior sessions take at this wall (bug-hunt + coverage), on Trevor's "do the lanes you mentioned".
- **SHIPPED — `GET /api/profile/portfolio-history?ownerKey=…&days=<non-numeric>` no longer 500s (`b4328b17`).** Anon-public route (explicit `proxy.ts` GET/HEAD carve-out + "GET is DELIBERATELY PUBLIC" header), NO try/catch. `days = Math.min(parseInt(?days ?? "30",10), 90)` — `parseInt("abc")`/`parseInt("")`→NaN, `Math.min` doesn't sanitize NaN → `since.setDate(getDate()−NaN)`→Invalid Date → `since.toISOString()` threw `RangeError` on the ownerKey branch → anon 500. (Wallet branch didn't crash — NaN→JSON `null`→RPC, which returns `{data,error}`.) Same NaN-clamp class as the `/api/edition-history` + `/insights` fixes; missed because it uses a bare `parseInt`+`Math` clamp not `Number(x)||default`. Guarded `Number.isFinite(rawDays) ? Math.min(Math.max(rawDays,1),90) : 30` (also clamps a negative `?days` up to 1 instead of a future window). +7-case regression test PROVEN TO BITE; suite 22/22, `tsc` exit 0. **Revert:** `git revert b4328b17`.
- **DURABLE — the anon-reachable-crash class is now essentially CLOSED; do NOT re-chase.** A parallel agent bug-hunt over the WHOLE public surface (`PUBLIC_READ_APIS` + `/api/public/*` + `/api/analytics/*` + `/api/entity/*` + anon POST computes) found `portfolio-history` as the SOLE outlier — `/api/entity/*` all use a `Number.isFinite` `clamp`, `/api/analytics/*` + `pulse/activity` are `Date.parse`+finite-guarded in try/catch, `/insights/*` `new Date()` are current-time, every anon `req.json()` is try/catch-wrapped. A second agent over `lib/**` FMV/pack-EV/serial/market/wallet/slug math came back CLEAN (the `fmtUsd` `$-50.00` vs `-$50.00` split is the test-pinned house convention).
- **Test-coverage lane NOT padded (correctly).** Primary gate saturated (~88% st / ~74% br), component gate ~74.5% — a blanket batch is theater; the only coverage added was the regression test tied to the real fix, which is where new coverage earns its keep. No unresolved follow-ups from this session.
### August 1, 2026 (Claude Code, interactive — "analyze test coverage → do all you can" → "keep going") — swept the high-value pure logic out of the `app/**` page-monolith layer (the 65k-LOC layer NEITHER coverage gate measures) into tested `lib/` modules, and fixed a pre-existing red that was blocking `main`'s CI for every session. 3 commits (`54260b4f` · `62301cf9` · `7d510670`), each its own `docs/overnight/ledger.md` (2026-08-01) entry + revert path; test/refactor-only, byte-identical, CI green on every push.

- **The durable coverage finding (re-confirmed, matches the prior test-coverage passes):** both vitest gates are saturated (~87.9% primary / ~74.6% component), top-level `lib/**` is fully tested (only pure-type `lib/types.ts` left), `workers/**` (23 test files + a completeness rot-guard) and the edge `_shared/**` modules are covered — so **new coverage value now lives ONLY in the layers neither gate sees**, chiefly the `app/**` non-API page layer. This session swept the reachable part of that layer; the remaining gaps are all environment-blocked (below).
- **SHIPPED — extracted the last clean pure-logic clusters from all six major page monoliths into tested `lib/` (99 new tests total, every extraction a byte-for-byte lift the page then imports):** `lib/moment-detail-seo.ts` (Product JSON-LD — STALE-FMV omits the Offer so a wrong price is never indexed; canonical path; hero-image candidates = the ~30% blank-hero regression fix; price-band gate; serial badges), `lib/pack-dist-verdict.ts` (+4: realized-vs-modeled + sealed-resale verdicts, calibrated-EV gate, low-conf EV share), `lib/collection/filter-options.ts` (filter-option builders, owned/locked edition rollups, pack-count lookup, near-complete sets), `lib/edition-detail-format.ts` (+2: `fmvDayDelta`, `sortNotableSerials`), `lib/sets/display.ts` (+2: `tierStripeColor`/`TIER_STRIPE`), and new `lib/pinnacle/catalog-format.ts` (`fmtList` jsonb-array parse+dedupe). Agent scans confirmed `market/page.tsx`, `app/dashboard/page.tsx`, and the `/analytics/*` dashboards are ALREADY fully factored — do not re-scan them for extraction.
- **SHIPPED — unblocked `main`'s CI (`62301cf9`).** The blocking `unit-tests` job was **red before my push**: `api-allday-sets-deep.test.ts` asserted the stale `tier:"incomplete"` where the unified `lib/set-completion-tier.ts` (Cowork `fa1d356a`, "5 ladders → 1") deliberately returns `"unpriced"` for an enriched-but-unpriced gap — a behavior its own unit tests bless. A prior commit even logged it as a known "pre-existing red" without fixing it. (A concurrent Cowork session then expanded the same fix with +45 branch tests — `9e123a44`.) **DURABLE: verify a push's CI even for a pure test/refactor change — `main` can already be red from a concurrent session, and the drive-to-green includes the red you inherit.**
- **DELIBERATELY did NOT manufacture low-value batches (coverage theater):** skipped the trivial `fmtUsd`/`fmt$`/`fmtInt` formatter copies (already pinned by `usd-format.test.ts`) and the JSX/localStorage-bound admin pages. **The safe blind-sandbox extraction lever is now largely exhausted.** The genuinely valuable remaining coverage gaps ALL need tools this environment lacks, each with an existing handoff: (1) **DB-pin-staleness CI enforcement** — `db-pin-staleness.yml` soft-skips until an operator adds the `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` repo secrets; (2) **edge-fn ORCHESTRATION-body testing** (the cursor-advance/silent-data-loss class) — `docs/handoff-2026-08-01-edge-orchestration-testing.md`; (3) **`edge-deno`→blocking** (16 `deno check` errors, root-caused) — `docs/handoff-2026-07-30-deno-edge-ci.md`. This cloud sandbox HAS a working push credential; `npm ci` fails (Node 22 vs CI 24) so use `npm install` then `git checkout package-lock.json`; run vitest via `node_modules/.bin/vitest`; the ledger is concurrent-heavy — splice your entry into the freshly-read file and resolve rebase conflicts by keeping BOTH sides (hit twice this session, resolved cleanly).

### August 1, 2026 (Cowork, evening) — DEFECT DRAIN + DEAD-CODE REMOVAL: instrumented 2 blind ingests, unbroke the freshness stamp on all 18 boards, stopped the sniper filters lying, fixed `/api/market`'s fake total + the Candy spread sort/display mismatch, then deleted 16 zero-importer modules. 2 code commits (`fa1d356a` defect drain · `fc68e057` dead-code) + 2 migrations; each its own `docs/overnight/ledger.md` (2026-08-01) entry + revert path.

- **⚠ ACTIONABLE FOLLOW-UP — ARM THE TWO NEW WATCHLIST ROWS AFTER THE DEPLOY LANDS.** `pinnacle-sales-indexer` + `allday-pack-listings` are both LIVE ingests that had **zero** `log_pipeline_run` — invisible to `pipeline_runs`, `detect_stalled_pipelines()`, and the cadence watchlist (a silent stop would never page). Both proven healthy only from destination tables (`pinnacle_sales` +240/24h; `pack_listings_cache` 281 AllDay rows). Logging added on every terminal path (`allday-pack-listings` is `after()`-based → also got the synchronous `phase:"invoked"` marker + a fatal-catch). Watchlist rows were added by `audit_20260801_watchlist_pinnacle_sales_and_allday_pack_listings` **staged `is_active=false` on purpose** (arming before deploy would manufacture two false `last_run IS NULL` stalls). Once a real `pipeline_runs` row exists for each: `UPDATE public.pipeline_cadence_watchlist SET is_active=true WHERE pipeline IN ('pinnacle-sales-indexer','allday-pack-listings');`
- **SHIPPED — the freshness stamp read a bare "—" on ALL 18 public boards (not the 2 reported).** `FreshnessStamp` deliberately rendered `"—"` server-side + first client render to dodge React #418, so the SERVED HTML (every crawler, every no-JS reader) permanently said "—". Rewritten to format deterministically from `getUTC*` parts (pure fn of the ISO string → no hydration drift → safe server-side), wrapped in `<time dateTime>`. **DURABLE: a hydration-dodge that renders a placeholder on the server ships that placeholder to everyone who doesn't run the effect.**
- **SHIPPED — sniper filters that lied are fixed or removed.** "Badges only" was doubly broken (`l.tags` never assigned + RPC rows bypassed the filter loop) — now joins `badge_editions` by `external_id`. "Special serials" was UNSATISFIABLE (`get_topshot_sniper_deals` is edition-level, `serial_number` NULL) so the control is REMOVED rather than left lying (the param is still honoured server-side, honestly returning nothing).
- **SHIPPED — `/api/market` returned a fabricated `total`** (it was the WINDOW SIZE — `offset+limit+100` — not a total). Now resolves three explicit cases and exposes `totalIsExact` + `matchedBeforeFilters`. **Candy Spread tab** sorted on `(ask−bid)/bid` while displaying `(ask−bid)/ask` (6 crossed rows showed "0.0%" and sorted last) — both now use the rendered fn. **5 divergent set-completion ladders → `lib/set-completion-tier.ts`** (restored a `completionPct===0 → incomplete` guard the consolidation had dropped). **`lib/chains/solana/normalize.ts` was re-NULLing Candy `set_name` every 24h** — fixed + backfilled (`audit_20260801_candy_editions_set_name_denorm`; 125/125 populated). Also revoked anon on `mv_topshot_market_index_daily` (materializing it earlier that day made it anon-readable at `/rest/v1/`; both real consumers use the service-role client).
- **SHIPPED — deleted 16 zero-importer modules + 13 tests (`fc68e057`).** 6 top-level components (`BadgePills`, `BadgeRow`, `HotEditions24h`, `MarketSummary`, `UserLabel`, `WhaleWatch7d`), `ui/EmptyState`, `pinnacle/PinnacleListingCard`, and 8 `lib/` modules (`edition-utils`, `flowscan-market-truth`, `local-market-files`, `local-storage`, `market-scope`, `serial-utils`, `topshot-market-truth`, `tracked-collections`). Each re-verified zero-importer at HEAD. ⚠ **ISOLATED IN ITS OWN COMMIT ON PURPOSE — if `component-tests` dips, REVERT THIS COMMIT, do NOT lower the threshold** (it removes 6 ratchet-measured components + their tests, and neither a full `tsc` nor a full coverage run fits the sandbox's 45s command cap, so CI is the first place the net effect is measured; the observability/honesty fixes in `fa1d356a` stay). The audit's premise for the largest group was WRONG and was checked, not trusted — the "Market Overview" backing routes DO exist (27 passing tests); the components were still genuinely unimported, so the deletion stands on its own evidence.
- **Revert:** `git revert <sha>` (per-commit; per-migration revert SQL in each migration comment).

### August 1, 2026 (Claude Code, interactive — "find a TODO → do all you can → work through any issues → wind-down") — re-confirmed NO safe live TODO, pivoted to a bug-hunt and eliminated a wrong-collection defect class, then swept health and fixed a degrading MV-refresh. 2 code commits (CI-green, deploys READY) + 1 prod-maintenance op; each its own `docs/overnight/ledger.md` (2026-08-01) entry.

- **The TODO sweep re-confirmed the standing finding** (exhaustive `\bTODO\b`/`FIXME`/`HACK` over code + `docs/code-todos.md`): every live-code hit is a resolved-reference, a launch-flag-gated defensive guard (the two `candy-*` `note` branches — both constants filled, unreachable, keep per CLAUDE.md), shelved operator-blocked `docs/drafts/panini/*`, or historical. Did NOT manufacture one.
- **SHIPPED — the UFC-slug→wrong-collection class, eliminated across all 3 sites.** `/api/acquisition-stats` (commit `4e6f84e1`), `wallet-search`, `wallet-cache` (`67a69f34`) each hand-rolled a slug→DB-slug map with `"ufc":"ufc"`, but the `collections` row slug is **`ufc_strike`** (no `ufc` row) → `resolveCollectionId` missed and fell back to the **Top Shot** collection (acquisition-stats) or `null` (wallet-cache). Fixed by importing the canonical `lib/collections.ts` `SLUG_TO_DB_SLUG` (acquisition-stats + wallet-cache) / a minimal one-line value fix (wallet-search, a hot route — importing there would add candy/panini resolution at line 1410). acquisition-stats was latent (no caller); wallet-search/wallet-cache are provably unreachable for UFC today (guard-redirect at line 1081 / collection-page guard before POST) → **zero behavioral change now**, corrected for any future refactor dropping those guards. Strengthened `api-acquisition-stats.test.ts` 3→6 with a slug-aware mock, **proven to bite**. **DURABLE — audited the whole class:** grep on the `"nfl-all-day":"nfl_all_day"` signature — those 3 were the ONLY buggy files; `insider-signals`/`support-chat`/`marketplace-fees` already map `ufc→ufc_strike`. `lib/analytics/format.ts` `"ufc":"ufc"` is a *short-form* map (correct). No source-regex guard added (it false-positives on wallet-search's legit URL-alias map); canonical map is the durable protection.
- **SHIPPED (prod maintenance, no revert) — VACUUMed the 7 `sales` partitions**, fixing a genuinely-slow, worsening MV refresh. `check_pgcron_recent_failures()` flagged one `mv_topshot_misattrib_candidates` refresh timeout; profiling showed it's **43.5s** (not saturation), dominated by `count(DISTINCT nft_id)` over ~3M TS sales whose `idx_sales_YYYY_ts_edserial_collide` index-only scans were **degraded to massive heap fetches** (visibility map unset). `VACUUM (ANALYZE)` on `sales_2020..2026` → heap fetches **0** on all static partitions (durable — 2020–2025 get no new writes) → **43.5s → 30.4s**. The refresh fn already has `statement_timeout=600s`, so the timeout was a rare extreme-contention balloon, not a too-tight limit. **QUEUED (ingest/FMV-adjacent — don't autonomously ship):** the ~30s+ refresh can still time out cold; durable options = autovacuum tuning for the current partition / incremental collision detection / `REFRESH … CONCURRENTLY` (feasible — MV is one `nft_id` column). See ledger.
- **DURABLE — "one-off transient" is a hypothesis, not a verdict; profile before dismissing.** I first read the MV timeout as a benign 6/7-success transient; profiling proved it a real 43.5s slow query trending worse. Conversely the entity-page Sentry `statement timeout` issues (pack/player/team) ARE saturation, confirmed fresh: `get_pack_detail_bundle` 52ms warm / 560ms cold with **no indexable amplification** (the 12k buffers are the legitimate pack-bundle working set) — the intentional throw-and-retry is correct, leave it. The distinguishing test is warm-vs-cold + whether one hot scan dominates.
- **Health otherwise green:** trust 0/25 breaching; advisors 174 INFO rls-no-policy + 7 WARN secdef (known posture), 0 ERROR; the `ufc-studio-sales-history-backfill` "stall" is an external cron-job.org tick dropout (pipeline healthy, `walk_complete` no-ops, no data loss — already triaged by the Daytime Monitor); the 55-file cart/trade/gift deletion (`2eceb4da`) left zero dangling refs; two parallel bug-hunt agents (API routes + lib/`_shared`) came back clean on live paths.

### August 1, 2026 (Claude Code, interactive — "analyze test coverage → do all you can, don't stop" → "keep going") — extracted the one substantial remaining monolith pure-logic cluster + shipped 7 high-stakes DB-invariant pins + verified the WHOLE 89-pin layer against live prod. All test/refactor/snapshot-only (byte-identical to live), direct to `main`, CI green, each its own `docs/overnight/ledger.md` (2026-08-01) entry + revert path.

- **The durable finding on WHERE coverage value is:** the two vitest gates are saturated (~88% primary / ~74.6% component) and the top-level `lib/**` is fully tested (only pure-type `lib/types.ts` untested), so new coverage value lives ONLY in the layers neither gate measures. This session worked the two reachable ones — pure logic still trapped in `app/**` page monoliths, and the **DB-invariant pin layer** (the single biggest lever, per the Testing section). Edge orchestration + `edge-deno` remain Deno-blocked (handoffs exist); component Phase-2 needs rendered-DOM the sandbox can't reach.
- **Monolith extraction (`2dacf18d`):** pulled the pack-EV verdict honesty gates (holding-pack/escrow-sentinel detection, −10000 clamp, secondary-ask anchor, net/ratio/margin, ≥90%-depleted / >3×-ask survivor bias) out of the 126 KB `pack/dist/[distId]/page.tsx` into tested **`lib/pack-dist-verdict.ts`** (29 tests) and wired BOTH the SEO `generateMetadata` and the page body to it — killing a drift hazard the code itself flagged ("Mirrors the page's evSurvivorBiased gate"). Byte-identical (`hasDropPool` defaults true so the SEO path is unchanged).
- **7 DB-invariant pins, 85→91**, each authored as a byte-identical snapshot migration for a previously-UNPINNABLE MCP-applied function, validated on a throwaway `postgres:16` AND mutation-tested to prove the assertions bite: `redeem_shop_item` (rewards spend / double-spend guard), `fulfill_redemption` (rewards grant / **idempotency = no double-Pro**), `record_link_state` (account-linking / monotonic-by-block), `upsert_wallet_moments` (wmc ownership / sync-reap + COALESCE-never-null), `save_fast_break_lineup` (game integrity / cross-user authz + use-budget), `ensure_topshot_edition_stub` (new-set self-heal / UUID→set_id_onchain bridge). The rewards trilogy (mint `award_points` was already pinned → spend → grant) is now complete.
- **DURABLE — the cloud (web) sandbox is a FULL DB-pin authoring environment.** `postgres:16` binaries are at `/usr/lib/postgresql/16/bin`; `initdb`/`pg_ctl` need an unprivileged user (`useradd pgtest; su pgtest -c '…' -o '-k /tmp/pgrun -p 5433 -h ""'`) + `CREATE SCHEMA extensions; CREATE EXTENSION unaccent SCHEMA extensions`; then `DATABASE_URL='host=/tmp/pgrun port=5433 dbname=postgres user=postgres' bash scripts/run-db-tests.sh`. Stub Supabase runtime deps in-fixture when a fn needs them (`auth.uid()` → a `CREATE FUNCTION auth.uid()` reading a session GUC; enums like `tier_type`/`edition_kind` → `CREATE TYPE`). Always **mutation-test** (break the guard, confirm the assert reddens) before pushing — a test that can't fail is theater.
- **DURABLE — pin-vs-live staleness IS runnable from any session with only the Supabase MCP (no service-role key).** `check-db-pin-staleness.mjs` needs `SUPABASE_SERVICE_ROLE_KEY`, but you can replicate it: compute `md5(trim(regexp_replace(prosrc,'\s+',' ','g')))` AND the comment-stripped variant (`regexp_replace(prosrc,'--[^'||chr(10)||']*','','g')`) IN SQL per pinned fn, then the identical `collapse`/`stripComments` normalization on the committed migration bodies in JS, and compare the md5 sets. **Ran it over all 89 prior pins → 89/89 clean, 0 stale.** ⚠ It only covers PINNED fns — `ensure_topshot_edition_stub` proved an UNPINNED fn with a committed migration (`audit_20260523`) had silently drifted from live; nothing detects that class until the fn is pinned to a fresh snapshot.
- **Method notes (this sandbox):** `npm ci` fails (Node 22 vs CI 24) → `npm install` then `git checkout package-lock.json`; run vitest via `node_modules/.bin/vitest`; working push credential present; git is concurrent-heavy (rebase on rejection, splice the ledger — never whole-file rewrite). The GitHub Actions MCP `list_workflow_runs` result exceeds the token cap → it saves to a file; parse with `python3 -c "import json; …"`.

### August 1, 2026 (Claude Code, interactive — "find a TODO → work through any issues → keep going") — re-confirmed NO safe live TODO exists, then swept the health surfaces and shipped ONE real anon-reachable-500 fix + closed a documented cron follow-up. Commits `fe798cb0` (docs) · `1e45a1b9` (ledger) · `6876813a` (fix); direct to `main`, CI green, fix deploy `dpl_9xscxreh…` READY.

- **The TODO sweep re-confirmed the standing finding** (exhaustive `\bTODO\b`/`FIXME` over `**/*.{ts,tsx,js,mjs,sql,cdc}` + workers + edge fns + `docs/code-todos.md`): every live-code TODO is RESOLVED, launch-flag-GATED (the two `candy-*` route placeholder notes are conditionally-accurate defensive code, NOT stale), or in shelved `docs/drafts/panini/` scaffolding blocked on operator-captured data. Did NOT manufacture one.
- **SHIPPED — `GET /api/edition-history?days=<non-numeric>` no longer 500s (`6876813a`, deployed READY).** The route is in `proxy.ts` `PUBLIC_READ_APIS` (anon) with NO try/catch; `parseInt(?days)→Math.min(Math.max(daysParam,1),90)` and `Math.min/Math.max` do NOT sanitize NaN, so `?days=abc` gave `days=NaN` → `since.setUTCDate(x−NaN)`=Invalid Date → `since.toISOString()` threw `RangeError` → anon 500. Same NaN-clamp class the 08-01 `/insights`+`/api/recent-sales` sweeps fixed, missed because it's outside `/insights` and uses a bare `parseInt`+`Math` clamp not `Number(x)||default`. Guarded `Number.isFinite(daysParam) ? clamp : 21`; +6-case regression test PROVEN TO BITE (reverting reddens exactly the 3 NaN inputs with the RangeError). `tsc` + suite green. **Revert:** `git revert 6876813a`.
- **DURABLE — audited the WHOLE anon-reachable crash surface, `edition-history` was the SOLE outlier; class now CLOSED, do NOT re-chase.** Numeric params across `PUBLIC_READ_APIS`+`/api/analytics/*`+`/api/public/*`+`/api/entity/*` are all already guarded (`Number(x)||default` — NaN falsy → default, covered by `invariants-public-insights-limit-nan-guard.test.ts` — or explicit `Number.isFinite` helpers). The `new Date(<user-string>)` class: the one public instance (`pulse/activity`) is guarded. The 20 public routes with no try/catch: only `edition-history` did a synchronous throw on user input; the rest just call RPCs (supabase-js returns `{data,error}`, never throws).
- **Closed the documented cron-auth OPEN FOLLOW-UP (`fe798cb0`, docs).** Verified via live `pipeline_runs` (NOT the rollup) that the two schedules moved to Vercel in `2fefdad9` authenticate post-deploy: `topshot-listing-cache` `ok=true` at the new `:55`/`:15` slots + `allow-list-reconcile` `ok=true` at the new `:14` slot. The ZERO→`CRON_SECRET`-mismatch→revert trigger was never met; NO revert of `2fefdad9`. ⚠ **DURABLE: do NOT verify this class via `pipeline_runs_daily`** — that rollup runs `11 */6 * * *`, so right after a cron move its today-row still reflects the PRE-deploy GHA rate and reads like a failure; check live `pipeline_runs` for a tick whose minute matches the NEW Vercel `schedule`.
- **Everything else swept = healthy or documented-deliberate** (nothing shipped): CI green; `v_rpc_trust_health` 0 breaches; security advisors 8 WARN + perf 227 all the known posture; the 3 entity-page `statement timeout` Sentry issues are SATURATION not slow queries (`get_pack_detail_bundle` 25ms warm; the retryable-throw is intentional); the 24h pipeline failures were a self-resolved ~4-min connection-pool-saturation burst. Stopped shipping when a concurrent Cowork session landed `2eceb4da` (cart/trade/gift frontend deletion) to avoid a collision.

### August 1, 2026 (Claude Code, interactive — "analyze test coverage → do all you can, don't stop") — closed a real drift hole in the edge layer with ONE surgical test batch, then correctly STOPPED rather than manufacture volume. Commit `533ac09b` (+ ledger `219d5665`); test/CI-only, no product-runtime / migration / edge-fn SOURCE / prod-DB change; direct to `main`.

- **SHIPPED — `__tests__/edge-inline-copy-drift-guard.test.ts` (13 pins).** Several Deno edge fns carry a hand-copy of a pure helper already unit-tested in `supabase/functions/_shared/*.ts`. Edge fns run outside BOTH vitest gates AND are redeployed by inlining the whole body via MCP, so an edit to a deployed copy leaves the mirror's tests GREEN while the running code drifts — the "mirror passes, deployed copy wrong" hole the per-module guards (`edge-nba-odds-parse`, `edge-flow-address`, `edge-cdc-reduced`) already close for OTHER fns. Closed it for the remaining VERIFIED-VERBATIM, previously-UNGUARDED copies: `cdc::unwrapCdc` (owner attribution, both pinnacle-owner-discovery fns), `hybrid-custody-parse::{parseAccountUpdatedPayload,unwrap}` (account-link ownership, hybrid-custody-events), `pinnacle-mint-parse::extractDeposit` (mint recipient, both owner-discovery fns), `spork-cursor::isTransient` (retry-vs-abort, ingest-allday-pack-opens), `topshot-stub-parse::flattenCadenceDict`, `cdc-reduced::toSerial`, `topshot-subedition-parse::{clampInt×3,decodeDict}`. Same `norm`/`extractFn`/`importsShared||inline===shared` shape as the siblings; **proven to bite** (temp `Math.floor→round` drift reddened the exact named case, restored green); `tsc` clean, all 30 `edge-*.test.ts` green. **Revert:** `git revert 533ac09b`.
- **DURABLE — the method + the discipline.** To find this class: for each `_shared` exported fn, find edge fns that DON'T import the module but define a same-named `function`, then compare NORMALIZED bodies — a **verbatim** match is an unguarded maintained copy to pin. A **DIFFER is NOT automatically a bug**; three were correctly EXCLUDED: the reduced-`cdc` copies (already guarded to STAY reduced by `edge-cdc-reduced`), the `spork-cursor` floor helpers (behaviourally identical but parameterized `SporkConfig`-arg vs closed-over-constants, so byte-equality is the wrong assertion), and `ufc-wallet-enrich` vs `seed-ufc-editions` (two independently-authored same-named fns). Did NOT build an auto-discovery rot-guard — `b64ToUtf8` is exported identically by 4 different `_shared` modules (one inline copy matches all 4), so there's no unambiguous canonical source to name; such a guard would be noisy/brittle for concurrent sessions. **And: both coverage gates are saturated and every vitest-reachable layer is covered, so I shipped the one real gap and stopped** rather than pad with trivial-helper pins or re-chase the gate % (coverage theater). The remaining gaps are all environment-blocked with existing handoffs: edge-orchestration testing (`docs/handoff-2026-08-01-edge-orchestration-testing.md`), `edge-deno`→blocking (`docs/handoff-2026-07-30-deno-edge-ci.md`), and DB-pin-staleness CI (needs operator secrets on `db-pin-staleness.yml`).
- **FINDING (documented, NOT fixed — editing edge source is a deploy-risk change out of a test pass's scope):** `seed-ufc-editions`'s inline `inferTier` maps circulation `0` → `"ULTIMATE"` (guard `circulation === null`) while canonical `ufc-wallet-enrich.inferTier` maps `0` → `"FANDOM"` (`!max || max === 0`). A 0-supply UFC edition would be seed-tagged the rarest tier. Low severity (seed rows, correctable); flagged in the test comment so it isn't mistaken for a copy to align.

### August 1, 2026 (Claude Code, interactive — drained the `RPC-Claude-Code-Handoff-2026-08-01.md` stack) — shipped 8 of the 12 Claude-Code items and **disproved or materially corrected 4 of them**. Commits `418ed607` (player teams) · `3a24ba05` (phantom editions) · `44e97c34` (fail-fast + cron observability) · `2fefdad9` (GHA→Vercel crons) · `7ba54184` (homepage h1 + insights #1); each its own `docs/overnight/ledger.md` (2026-08-01) entry + revert path. CI green on every push; all 5 deploys READY; **verified by rendered DOM, not HTTP 200**.

**⚠ FOUR handoff items did not survive verification. Re-verify a handoff's premise before acting on it — half of this session's value was in NOT doing what was asked.**
- **Item 3 (player teams)** — it claimed the tie-break was *arbitrary* and that SGA/Mitchell/Brunson render Clippers/Jazz/Mavericks. All three were **already correct** (fixed same-day by `cd153d2e`). The real defect was narrower: `team_edition_count DESC` pins a **traded** player to a former team forever (295/949 dup slugs stale, **201 still-active**). Its proposed fix — order by newest `first_minted_at` — is **unimplementable**: that column is **0 of 19,583 populated** on Top Shot. Used `game_date` (92%) instead. **DURABLE: "latest moment wins" is wrong alone** — unqualified it shows a retired player's final-season team (Pierce ⇒ Nets, not Celtics); it needs an activity gate, and I made that gate **data-relative** (`collection max(game_date) − 18mo`) so it can't go stale. Net *faster* too (45ms → 19.5ms) via a partial index, because the horizon needed a per-collection `max(game_date)` scan.
- **Item 7 (2 misfiled AllDay editions)** — its "2-row `UPDATE` of `collection_id`" **would have failed**: AllDay already holds both `external_id`s and `editions` has `UNIQUE(external_id, collection_id)`. They were duplicate **stubs**, so removal was correct, and they'd also dragged in **2 phantom `sets` rows** the handoff never mentioned. Swept all **51 FKs** first; only `fmv_snapshots` (24) and one `cached_listings_v2` row had data.
- **Item 19 (wallet box "441 views → 0 pastes")** — **not a conversion signal.** The forked inputs emitted no `wallet_paste` at all until `f7b665ea` (07-26), and since then only **7 of 235 view events had a referrer**, with **175 of 183 sessions firing exactly one event**. Real human denominator ≈ **8**, not 441; the single paste is Trevor's own wallet in a 36-event session. **Do not redesign the box** — the input is traffic.
- **Item 10 (409 "overstated" FMV editions)** — reproduced (432/4,412 = 9.8%) but the dollars kill the headline: **median sale price $0.30, median overstatement $0.55, total across all 432 = $339**, only 4 above $5 FMV and **zero above $20**. A ratio artifact on penny moments. **Do NOT move the 3× clamp to 2×.** If revisited, gate on **absolute gap**, not ratio.

**Durable operational lessons:**
- **A cadence watchlist keyed on SILENCE cannot see PARTIAL tick loss.** GitHub Actions was dropping **~83%** of `topshot-listing-cache` ticks (10–13/day vs 72) — measured from **GHA run history, not the DB**, which matters because the route is `after()`-based and a DB-only count would have been ambiguous. It *was* watchlisted (360 min) and never breached, because at 17% delivery it still fired ~every 2h. Moved it + `allow-list-reconcile` (~60% loss, **no watchlist row at all**) to Vercel crons at identical minutes. **Vercel Cron can only ever send `CRON_SECRET`** — both routes accepted only `INGEST_SECRET_TOKEN`, so a `vercel.json`-only move would have 401'd every tick while still looking scheduled.
- **`after()` swallows observability, not just work.** `compute-laliga-pack-ev` has a daily cron and **zero `pipeline_runs` rows ever** because its `log_pipeline_run` lives inside `after()`. Added a synchronous invoked-marker so "never invoked" and "`after()` dropped" are distinguishable. Its header comment claiming the Golazos pool is empty is **false** — the pool now holds **1,957** pullable rows, so it has real work and its silence is a genuine gap.
- **Entity-page `statement timeout` Sentry issues (pack/player/team) are SATURATION, not slow queries** — `get_player_editions` is **161ms warm** on the heaviest player. Left alone deliberately; the structural-throw is an intentional honesty choice.
- Deliberately kept `exit 1` on the Atlas egress block rather than the handoff's `exit 0`: the exit code doesn't feed `v_pipeline_failure_rates` (`postRoute` does), so exiting 0 would only have turned a real outage green in the GHA UI. The win was cutting **24.2 min → ~1 min** per blocked run.
- **✅ FOLLOW-UP CLOSED (Claude Code, 2026-08-01 ~23:16Z): both new Vercel crons authenticate — NO revert of `2fefdad9` needed.** Confirmed by live `pipeline_runs` (not the rollup): `topshot-listing-cache` ticked `ok=true` at 22:55:10Z + 23:15:25Z (the new `15,35,55 * * * *` `:55`/`:15` slots) and `allow-list-reconcile` at 23:14:28Z (the new `14 * * * *` `:14` slot) — both post-deploy, no error. ⚠ **DURABLE — do NOT verify this class via `pipeline_runs_daily`:** that rollup runs pg_cron `11 */6 * * *`, so right after a cron move its today-row still reflects the PRE-deploy GHA rate and reads like a failure; check live `pipeline_runs` for a tick whose minute matches the NEW Vercel `schedule`. The original ZERO→`CRON_SECRET`-mismatch→revert trigger was never met.
- Item 13 (Panini) correctly **not** shipped: feed out 4 days vs the handoff's own >7-day gate, and Panini FMV is fresh (68 min), so no rendered number is stale. Re-check from 2026-08-04. Items 1/2/6/12 remain **operator-only**; 11/14/15/16/17 untouched.

### August 1, 2026 (Claude Code, interactive — "analyze test coverage → proceed with all → keep going") — closed the serial-FMV DB-pin cluster + Pro-grant (batches 17–20, **66→85 pins**), ran a live 84-pin staleness audit (**all clean**), then fixed a real anon-reachable CRASH class across 16 public routes and unbroke 2 CI reds. Commits `46b17281`·`d4275569`·`acbbaa81`·`5241f4f2` (pins), `d0f562d2`·`9a7630a9` (crash fix + ledger correction), `79ebfd92`·`38b7e345` (CI fixes); each its own `docs/overnight/ledger.md` (2026-08-01) bullet + revert path. Main verified GREEN (`79ebfd92`).

- **DB-PIN CLUSTER CLOSED (batches 17–20).** Pinned the entire serial-FMV compute cluster — `compute_serial_fmv_multipliers`, `compute_ultimate_non_special_fmv`, and the two regression models `compute_serial_fmv_power_model`/`_jersey_model` — plus `grant_pro_grandfather` (Pro-entitlement mint). All byte-identical to LIVE (md5 match, homoglyph-repaired base64 decode), validated on local `postgres:16`. **DURABLE — how to pin a regression/percentile model without theater:** put the qualifying rows on a PERFECT curve (power: price=2·fmv→k=2/β=1/r=1; jersey: price=10·√fmv→k=10/β=0.5/r=1) and seed fit-wrecking outliers the input gates must reject (LOW-confidence FMV, wrong-serial, jersey=1, serial=circ) — the fit landing EXACTLY on the clean coefficients IS the proof every gate held. Jersey's reliability ceiling is the tighter `β<1.0` vs power's `<1.25`. **DURABLE — `percentile_cont` returns double**, so a numeric-column store normalizes `1.0`→`1` in text; assert multipliers via boolean numeric equality (`(multiplier=1.0)::text='true'`), not `::text`.
- **DURABLE — I can run `db:pins:check` via MCP without the service-role key** (the check `db-pin-staleness.yml` is still soft-gated on operator secrets). Pull each pinned fn's live `prosrc`, md5 it under BOTH `collapse` and `collapse(stripComments)` normalizations IN SQL (matching `scripts/check-db-pin-staleness.mjs`), and compare to the committed migration bodies extracted with the guard's `bodiesOf` parser. Ran it over all 84 pins → **0 stale, 0 not-deployed** — the one check CI structurally can't do (its drift guard is repo-vs-repo). Enabling that workflow (operator adds `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`) is the top handoff — it makes this audit automatic.
- **REAL CRASH FIXED — `?limit=abc`/`?days=abc` 500'd 16 public `/insights` routes** (same class as the earlier `/api/recent-sales` fix). `Number(sp.get("limit") ?? "50")` looks guarded but `??` only catches an ABSENT param; a present non-numeric → NaN → `.limit(NaN)`→PostgREST 400→500 (and `market` threw `new Date(NaN).toISOString()` RangeError pre-DB). Fixed all to `Number(sp.get("limit")) || 50` + a directory-driven guard `__tests__/invariants-public-insights-limit-nan-guard.test.ts`. **DURABLE — the OPTIONAL filter params (`min_ask`/`max_mint`/`min_premium`/`series`/…) are ALREADY `Number.isFinite`-guarded across all 13 DB-filter sites** (a bug-hunt agent claimed otherwise; audited and disproven) — only the limit/days clamp hid its missing guard inside `Math.max/min`. A second bug-hunt over `lib/**` FMV/pack-EV/wallet math came back CLEAN (no false positives).
- **DURABLE — a new `supabase/tests/*.sql` invariant that references `extensions.*` MUST `CREATE SCHEMA IF NOT EXISTS extensions;` ITSELF.** My batch-15 `resolve_challenge_slots.sql` did `CREATE EXTENSION … SCHEMA extensions` without it — passed on the scratch PG (which already had the schema) but reddened the CI `db-tests` job (vanilla postgres, no `extensions` schema). The scratch PG HIDES this; validate a new pin against a fresh `TEMPLATE template0` DB (or CI) to catch it. Also hardened a CI-only flake: `component-PortfolioSparkline.test.tsx` asserted `onChange` synchronously right after a text `waitFor`; under CI timing the text paints before the `useEffect` flushes → wrap the `onChange` assertions in `waitFor` too.

### August 1, 2026 (Claude Code, interactive — "analyze test coverage → do all you can → keep going") — closed the ONE unmeasured, security-sensitive layer both coverage gates miss: `workers/**`. 5 test/CI/docs-only batches to `main` (`b4324c9b`, `c61bd983`, `13c5394e`, `47d374d0`, `7ae8f46c`), each its own `docs/overnight/ledger.md` (2026-08-01) bullet + revert path. NO product-runtime, migration, worker/edge-SOURCE, or prod-DB change.

- **THE DURABLE FINDING: the measured gates are saturated, and so is everything a test can currently reach — so new coverage value now lives ONLY in the unmeasured layers, and those are nearly closed too.** Verified saturated (do NOT re-chase): primary vitest gate ~88% stmts, component gate ~74.5%, all 26 `_shared/*.ts` edge modules (behaviorally tested + drift-guarded), DB invariants (66 pins), `proxy.ts` (117-case `isPublicPath` table). ⚠ My opening analysis's "15 untested `_shared` modules" was a **grep false-negative** (test imports are extensionless; my `grep "$base.ts"` missed them) — ALL `_shared` modules are tested. Re-verify with `grep "_shared/<base>\"` before ever claiming a `_shared` gap.
- **SHIPPED — all 16 Cloudflare workers now drive their `fetch()`/`scheduled()` entry handler (was: 4 had only pure-submodule tests).** `workers/**` is in NEITHER coverage `include`, so a dropped auth check on a paid-egress proxy / the public MCP API was invisible. New handler suites: `sports-proxy` (18.6%→70.6% stmts), `rpc-mcp-proxy` (10.8%→74.7%), `sales-counterparty-backfill` (`scheduled()` cron + Bearer `fetch()`), `topshot-moments-hydrator` (gate + candidate-read legs). Pattern: `worker.fetch(req, env[, ctx])` with a stubbed global fetch + `vi.mock("@supabase/supabase-js")`. **Locked in by a strengthened `worker-test-completeness` rot-guard** — it now requires each worker's entry to be *drive-tested* (a test that both imports `index` AND calls `.fetch(`/`.scheduled(`), not merely referenced; `KNOWN_ENTRY_UNDRIVEN` allowlist is empty. Runs in the blocking `unit-tests` CI job.
- **DURABLE — testing a worker's `index.ts` drags it into the app tsc program** (tsconfig `exclude`s `workers/**`, but an imported file is still checked), surfacing Cloudflare-global type refs the app tsconfig lacks. Fixed with a **type-only ambient shim** `__tests__/cloudflare-worker-shims.d.ts` (`ExportedHandler`, `ScheduledEvent`, `Fetcher`; `ExecutionContext` already resolves — do NOT re-declare it or you get a duplicate). Zero runtime effect, no worker-source edit. Also: import worker sources with `// @ts-expect-error — plain worker module, no type declarations` (the established pattern) to suppress the `.ts`-extension `TS5097`.
- **SHIPPED — extracted `sync-nba-odds` betting math to `_shared/nba-odds-parse.ts` + 25 tests + source-drift guard.** It was the LAST edge fn with real pure logic and zero extraction (452 LOC: `americanToImplied`/`devigPair`/`parseEvents`/`pickBookmaker` — the odds→de-vigged win-probability that feeds Fast Break / RTR; a bug there is the fabricated-signal class). Verified the other big edge fns are NOT gaps: `sync-nba-projections`→`nba-projections-parse`, `compute-topshot-pack-ev`→`pack-ev-edition`/`topshot-pack-ev-pricing` (incl. `editionExtKey`/`normalizeTier`/`computeDualPrice`), stub-resolver→`topshot-stub-parse`, ufc→`ufc-wallet-enrich` all already extracted+tested.
- **SHIPPED — broadened `e2e/smoke-selfcheck.spec.ts` 5→12 tests** so every branch of `assertHealthyPage` (the "HTTP 200 but broken/blank DOM" guard) is pinned — all 5 `ERROR_SIGNS` regexes individually, error-detection-over-content-length, `minContentChars` both directions, status-vs-content isolation. Verified locally against the sandbox Chromium (`/opt/pw-browsers/chromium-1194/...`, `PW_CHROMIUM_PATH` + `PW_PROXY_SERVER=http://127.0.0.1:1` bypass localhost) — 12/12.
- **UNRESOLVED / handed off (needs tools this sandbox lacks — NOT skipped):** (1) **Behaviorally testing edge-fn ORCHESTRATION bodies** (the cursored ingest/backfill loops — the "advance past a failed fetch = silent data loss" class) needs a **Deno toolchain + edge deploy access**; full scope, 5 ranked targets, DI-seam approach, and guardrails in [docs/handoff-2026-08-01-edge-orchestration-testing.md](docs/handoff-2026-08-01-edge-orchestration-testing.md). (2) `edge-deno`→blocking (still 16 `deno check` errors) per [docs/handoff-2026-07-30-deno-edge-ci.md](docs/handoff-2026-07-30-deno-edge-ci.md). Both are Deno-session work. **Sandbox notes:** `npm ci` fails (Node 22 vs CI 24) — use `npm install` then `git checkout package-lock.json`; run vitest via `node_modules/.bin/vitest`; this cloud sandbox HAS a working push credential.

### August 1, 2026 (Claude Code, interactive — picked up the Cowork "trust-health view takes 49s" handoff) — DISPROVED the handoff's diagnosis, then fixed the REAL hot spot with one DB-only covering index; `v_rpc_trust_health` 7,999ms → 1,006ms, board byte-identical. Commit `90abbef4`; `docs/overnight/ledger.md` (2026-08-01) + revert path; direct to `main`.

- **DURABLE — the reusable lesson: `packev` is 92% of the planner COST ESTIMATE (522,256 of 567,668) but only ~8.5% of RUNTIME (676ms of 7,999ms). Cost is not time.** The handoff proposed moving the `packev` CTE into `rpc_trust_health_precompute` for a claimed ~12×; measured per-arm with `EXPLAIN (ANALYZE, TIMING ON)` it would have bought **~8%** (8.0s→7.3s) at the cost of 6h staleness on two arms — declined on its merits. The real hot spot was `pinnacle_fmv_impossible_flags` (`v_pinnacle_fmv_sanity_flags`) at **3,911–4,639ms, ~50–58%** of the view. (Fair to the instinct: `packev` is the most VARIABLE leg — 676ms quiet → 5,267ms contended, an 8× swing — so under contention it can dominate; but the cited evidence was wrong.)
- **SHIPPED — `audit_20260801_pinnacle_sales_render_rollup_covering_index` (DB-only, applied to prod via Supabase MCP, `90abbef4` is the idempotent repo record).** The pinnacle per-render 90d rollup planned as a Bitmap Heap Scan touching 3,831 blocks and returned `shared hit=6 read=3825` — **near-zero cache hits, re-reading ~30MB from disk every call** on Micro. The existing `idx_pinnacle_sales_pulse_window` covers `(sold_at DESC) INCLUDE (sale_price_usd, …)` but **omits `render_id`**, so it couldn't go index-only. New `idx_pinnacle_sales_render_rollup` = `(sold_at DESC) INCLUDE (render_id, sale_price_usd)` (~9MB, 13th index on `pinnacle_sales`). **Arm 3,911ms → 31ms; disk reads on that leg 3,742 → 240; whole view 7,999ms → 1,006ms.** Board identical: 25 arms, 0 breaching, `pinnacle_fmv_impossible_flags` still 0. **Revert:** `DROP INDEX IF EXISTS public.idx_pinnacle_sales_render_rollup;`
- **DURABLE — validated the index-only PRECONDITION before building** (the exact error just caught in the handoff): a covering INCLUDE index only helps if index-only actually engages. Checked `relallvisible = relpages = 6474` → **100% all-visible** → it engages (`Index Only Scan`; residual `Heap Fetches` are post-autovacuum rows = cache hits, not disk reads). The documented counter-case (hot not-all-visible pages, per the `fmv_current` note) did not apply.
- **DURABLE — the "monitoring that times out looks healthy" alarm did NOT apply here.** `v_rpc_trust_health`'s consumer is **`/api/sentinel`, NOT `/api/check-alerts`** — `app/api/sentinel/route.ts` catches the error, classifies via `isSaturationError()`, and reports `status:"warn"` prefixed `INCONCLUSIVE (db saturated) — `, never `ok`; `get_pipeline_alerts()` doesn't read the view. So a cold timeout made the board **UNREAD, not falsely green** (a real visibility gap — service_role budget is 30s — but not silent-healthy).

### August 1, 2026 (Claude Code, interactive — handoff drain, cont.) — SHIPPED the fix for the queued OPENS-HISTORY-BACKFILL-500 (edge fn `ingest-topshot-pack-opens-history`, repo + DEPLOYED v4 via MCP); found + fixed the same-class defect in the AllDay twin (repo-only, NOT deployed). Commits `a3817e14` (TS) + `8cbd1271` (AllDay); `docs/overnight/ledger.md` (2026-08-01) + revert path; direct to `main`.

- **The queued diagnosis was wrong on three counts (each evidence-backed).** (1) **`rows_written=0` across the green days is correct DEDUP, not silent loss** — the 03:56Z run found `rows_found=81`, `pack_rips` holds exactly 81 in that window; the miss was that **`rows_skipped` was never instrumented** on this route (a null instrument for a *re-walking* backfill). (2) The apparent "cursor-skip" was a **fast-forward to `MIN(pack_rips.block_height)`=61,930,346**, and coverage above it is dense/gapless — nothing stepped over. (3) The correlated `c4c4d3c9` is a **docs-only ledger commit** — spurious. (The ~272k low-range TS rips came from the one-day 2026-07-11 Dapper `searchPackNft` registry bulk load, not this scanner — which is why the fn re-verifies and writes nothing.)
- **THE ACTUAL DEFECT + FIX.** The fn scans a 25,000-block window as ~100 sequential 250-block `/events` queries and abandoned the **entire** window on the first failure (`after = anyTransient ? cur : start`). The deep-history spork is **flaky, not down** (the 41 failing runs died at query 1–5 in rotation), so a 100-query all-or-nothing window essentially never completes → cursor never moved, 96 runs/day burned. Shipped: `scanOpens` now walks **DESCENDING** (matching the cursor's direction) and returns a `scannedFloor` checkpoint; the cursor advances to `min(scannedFloor-or-resolvedFloor, cur)`, so progress is **monotonic under any upstream failure rate**. `rows_skipped` now populated; `ok` = progressed-OR-no-error. `SPORK_FLOOR = 27,341,470` (mainnet17 root) → ~34.6M blocks of reachable, un-indexed TS pack-open history below the registry floor; the fn should keep walking to it (TS genesis ~block 7M is permanently unreachable, documented gap).
- **DURABLE — scan direction MUST match the caller's cursor direction; getting it backwards is SILENT loss, not an error** (the unscanned hole lands on the side the cursor is about to claim). The AllDay twin `ingest-allday-pack-opens` carries the identical defect AND is **actively losing data today** — on a `MAX_TX=180` budget stop it advances the full 25,000-block window over opens it never resolved (measured 16/143 runs/24h). Fixed properly with `scanOpens(start, end, dir)` — `"desc"` for backfill, default `"asc"` for the forward/probe walker (which goes the opposite way), so **forward-mode behaviour is byte-identical**. **AllDay is repo-only, NOT deployed** (healthy high-volume pipeline; the MCP deploy inlines ~460 lines by hand = transcription risk — deserves its own deploy + tick verification). **Forward-mode tx-budget loss left OPEN (queued).**
- **Post-deploy verification (`3e0806c0`) CONFIRMED the fix and PROVED flaky-not-dead:** a chunk that 500'd at 14:57:19Z SUCCEEDED at 15:11:01Z (`ok=true`, `partial=true`, cursor advanced) — under the old code that run would have been zero-progress. ⚠ **NEW RISK the fix introduces — do not read it as a clean win.** At ~250–750 blocks/tick against 34.6M remaining this region is ~YEARS, and because the cursor now moves with `ok=true`, **neither `cursor_stalled` NOR the fail-rate alarm can ever fire for it again** — a silent crawl ([[green-pipeline-blind-to-its-own-work]]). Still strictly better than a paging hard-wedge, but the pipeline now needs a **rate** signal. **Queued:** raise the per-chunk retry budget in the spork events path (`j()` gives up after 3 tries — the same chunk succeeds minutes later), and add a backfill-rate/ETA arm to `v_rpc_trust_health`. **Deployed as a MINIMAL DIFF** keeping the inline `esm.sh` import (the documented repo↔deployed divergence). **Revert:** `git revert <sha>` for the repo files; to unwind prod, redeploy the v3 body (recoverable via `get_edge_function` history) — no DB/cursor unwind (forward-down + idempotent on `tx_hash`).

### August 1, 2026 (Claude Code, interactive — "find a TODO → do all of them" → wrap-up) — 🚀 SHIPPED PANINI WC PRIZM SQUEEZE GO-LIVE (public `/insights/panini-squeeze`, chain-two-adjacent) + a public `/api/recent-sales` robustness fix. Direct to `main`, each its own `docs/overnight/ledger.md` entry (2026-08-01) + revert path.

Thread arc: the TODO sweep re-confirmed the standing finding (no safe live code TODO — all gated/resolved/shelved) → offered 3 lanes (test coverage / bug-fix / gated go-live), Trevor said "do all of them"; the Panini go-live was NOT inferred from that blanket yes — it was confirmed separately via AskUserQuestion (public-exposure = Trevor's call).

- **🚀 PANINI GO-LIVE (`f1c20d0c`, deploy `dpl_2bdxuzLyez2fhYQjrW12C4XQTJ3j` READY + aliased to www).** Flipped `PANINI_PUBLIC` `false`→`true` in [lib/launch-flags.ts](lib/launch-flags.ts) after Trevor confirmed; Candy's 07-31 go-live had cleared the Candy-first ordering gate. The single compile-time boolean atomically un-gated all 5 consumers (proxy route wall, sitemap slug, `/insights` hub card, `panini-squeeze/layout.tsx` robots noindex→index, smoke public-page probe) — `/insights/panini-squeeze` is now PUBLIC + indexable; the mandatory listing-gated coverage disclosure (`meta.coverage` + the "floor, not census" banner) travels with it. Inverted the 5 STAGED-state test assertions the flip changes: panini + candy launch-flag contracts (sitemap skeleton 43→44), `proxy-is-public-path` (3 panini rows staged→live), `api-smoke-test-deep` (total 55→56 / hardTotal 43→44 / concierge 58→59), `sitemap-data` (skeleton 43→44). **Post-ship watch GREEN:** CI + Smoke GHA green (incl. the live `/insights/panini-squeeze` HARD public-page probe — the rendered-DOM proof, since the cloud sandbox is proxy-blocked from prod), full local suite 8504 + `tsc` clean, board data populated (4,051 `panini_editions`, squeeze-totals + coverage-summary rows, panini FMV fresh 13:29Z from the Windows-box runner), 0 new Sentry. **Rollback:** set `PANINI_PUBLIC` back to `false` + push (~3 min, no DB unwind). The surface flag is INDEPENDENT of the `panini-blockchain` collection's own nav/tab routes (unaffected) and of any `is_active`/`published` switch — the board reads Panini data directly via `supabaseAdmin`. Candy stays live; the "Candy ships first" ordering is now satisfied for both.
- **BUG FIX (`2382925f`) — `/api/recent-sales` 500 on a malformed `?limit` (PUBLIC route).** `parseInt('abc')`→NaN→`Math.min(NaN,50)`→`.limit(NaN)`→PostgREST 400→500 instead of degrading. NaN/non-positive-guarded to the default 15 (+radix 10); +2 regression tests. Found by a dedicated bug-hunt agent.
- **Bug hunt confirmed a well-swept codebase.** The agent's only other flag — `lib/dashboard-format.ts` `fmtUsd` rendering negatives `$-50.00` — is VERIFIED NOT A BUG: it's the deliberate house convention (pinned by tests across `grail-format`/`moment-detail-format`/`pack-dist-format`/`CrossCollectionPortfolio`), and the dashboard only ever passes non-negative FMV to it. The two admin backfill routes (`tier-backfill`, `backfill-onchain-ids`) share the `parseInt` pattern but are token-gated operator-only with well-formed params → left (defensive-only, no real trigger).
- **Test-coverage lane genuinely saturated** (0 untested top-level `lib/**` except pure-type `lib/types.ts` = theater); did NOT manufacture a batch. **Standing finding intact — no safe live code TODO exists**, so "do all of them" landed as a real public launch + a real public-route bug fix, not a manufactured TODO.

### August 1, 2026 (Claude Code, interactive — "analyze test coverage → do all of them until exhausted") — shipped 4 test/CI-only additions closing the STRUCTURAL gaps the two saturated gates (~88% primary / 74.5% component) can't see; commit `84aa94a7`, `docs/overnight/ledger.md` (2026-08-01) + revert path; direct to `main`. Test/CI-only — no product-runtime, migration, edge-fn/worker SOURCE, or prod-DB change.

- **The durable finding: the two big gates are saturated, so new coverage value is entirely in the OTHER layers.** Shipped: (1) `__tests__/collections-chain-dispatch.test.ts` — chain-two URL dispatch (Candy→Magic Eden/Solana, Panini→OpenSea/Ethereum) + the INVARIANT that no `dbChain !== 'flow'` collection can resolve to a Flow-only `dapper.market` URL (guards future chains); the existing `collections-urls.test.ts` covered only Flow. (2) `__tests__/edge-cursor-throw-on-fetch-failure.test.ts` — structural regression guard pinning the 07-29 "cursor advances past a failed fetch → silent data loss" fix across the 4 throw-and-hold Flow-REST walkers (Deno source vitest/tsc exclude), with an anti-pattern self-test. (3) `supabase/functions/_shared/pack-opens-cursor.ts` + test — extracted the bounded-advance "scanning ≠ writing" backfill cursor decision to a mirrored, unit-tested module + source-drift guard (the zero-edge-edit `_shared` pattern). (4) `e2e/smoke.spec.ts` broadened 12→35 public routes (per-collection tabs incl. Pinnacle's separate data plane, core `/insights` boards, `/analytics` siblings). **A future test-coverage session should NOT re-chase the aggregate — these structural guards now exist; point it at a specific feared surface.**
- **DB pins: spot-audited live via Supabase MCP, CLEAN** (4 high-stakes money/FMV pins present with exact signatures; `award_points` body matches its `20260801160600` snapshot migration predicate-for-predicate). Consistent with the full 53/53-clean 08-01 audit.
- **TWO handoffs remain (can't be done in-sandbox):** (1) **DB-pin-staleness is NOT enforced in CI** — `db-pin-staleness.yml` soft-skips until the operator adds the `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` repo secrets; only that catches a pin matching its migration while LIVE prod diverged (the repo-vs-repo drift guard structurally can't). (2) **Promote `edge-deno` to blocking** (16 `deno check` errors, toolchain-root-caused) — needs a Deno + deploy session per `docs/handoff-2026-07-30-deno-edge-ci.md`.

### August 1, 2026 (Claude Code, interactive — "find a TODO → also fix any bugs → keep going" → wrap up) — shipped 2 prod DB fixes clearing BOTH open trust/Sentry issues, then swept every monitoring surface clean. Each ship its own `docs/overnight/ledger.md` entry (2026-08-01) + revert path; direct to `main`.

Thread arc: the TODO sweep re-confirmed the standing finding (every live TODO is RESOLVED or launch-flag-GATED — nothing safe to implement) → pivoted to the "also fix bugs" directive → fixed the two open issues → swept Sentry/advisors/pipeline-fails, found nothing else actionable, wrapped.

- **SHIPPED — get_set_detail graceful FMV-timeout** (`audit_20260801_get_set_detail_graceful_fmv_timeout`, commit `6d0cc7e0`, DB-only). Killed Sentry **NEXTJS-22** "set detail unavailable: canceling statement due to statement timeout" on `/[collection]/set/[slug]`. Root cause (profiled): the per-edition latest-FMV rollup on TS **"Base Set"** (3,606 thumbnail editions) fans out to thousands of correlated `LEFT JOIN LATERAL … ORDER BY computed_at DESC LIMIT 1` probes across the `fmv_snapshots` partitions (~2s warm) and, cold under contention, blew the request statement budget and THREW — erroring the whole public page. Fix: wrap ONLY the rollup in `BEGIN … EXCEPTION WHEN query_canceled` → degrade the header stats to NULL (page renders "—", the separately-fetched editions grid still loads) instead of throwing. Also dropped the RETURN's `COALESCE(edition_count,0)` so a degraded read shows "—" not a misleading "0" beside a populated grid (happy path byte-identical; `COUNT(*)` never NULL). DB-invariant test re-pinned to the new migration (drift guard 57/57); Sentry resolved; overnight post-ship watch confirmed 0/24h.
- **DURABLE (cost a live break) — `statement_timeout` is armed at the TOP-LEVEL command boundary and CANNOT be re-armed mid-function.** A runtime `SET LOCAL statement_timeout` inside a STABLE fn THROWS *"SET is not allowed in a non-volatile function"* (my first apply broke `get_set_detail` on EVERY call for ~2 min until fixed); and even under VOLATILE the inner SET is a NO-OP for sub-statements (verified: `pg_sleep(1)` under `SET LOCAL '50ms'` completes; a function-level `SET statement_timeout` clause also does NOT enforce within the call). So you CANNOT give a slow sub-query its own inner budget — instead **catch the REQUEST/role timeout that's already firing** with `EXCEPTION WHEN query_canceled` and degrade; the `RETURN` still runs (the one-shot timer already fired, won't re-fire). Final ship is the clean STABLE + `BEGIN/EXCEPTION` wrapper, no SET. Proven deterministically on a local `postgres:16`.
- **DURABLE — for latest-per-edition FMV IN a DB query, KEEP the per-edition `LATERAL … LIMIT 1`; do NOT swap to a `fmv_current` DISTINCT-ON join.** Profiled on Base Set: the `fmv_current` join is **11× WORSE (22.5s vs 2s)** — it materializes the DISTINCT ON over all ~942k snapshot rows before filtering. The CLAUDE.md "use `fmv_current`" rule is ONLY for the JS-1000-row-cap correctness case (raw DESC + JS dedup), NOT in-DB per-edition reads. A covering `INCLUDE(fmv_usd,floor_price_usd)` index doesn't help either — the wanted row is always the newest = a hot, not-all-visible page → index-only still heap-fetches.
- **SHIPPED — v_fmv_sanity_flags own-sales corroboration** (`audit_20260801_fmv_sanity_flags_own_sales_corroboration`, commit `68aa236d`, DB-only view) — resolves the star-set FALSE-POSITIVE the overnight pass queued (§4). The view flagged any TS edition with FMV <12% of its **SET** median — a "cheap vs peers" heuristic that can't tell a genuinely-cheap role player (Champagnie `261:8714`, FMV $97 = **118% of his own $82 raw 30d median**) from a mispricing. Added corroboration against the edition's OWN RAW sales (`public.sales`, independent of the FMV pipeline): fire only when `fmv < 0.6 × own 30d sales median` on ≥4 sales with a >$50 gap. **STRICT SUBSET of the prior output** (only ADDS predicates) → can only remove the FP class, never introduce one. Appended `own_sales_median_30d`/`own_sales_n`; `fmv_sanity_flags` metric **1→0 ok**; view runs 358ms; sole consumer `v_rpc_trust_health` reads `count(*)` so appended cols are safe; `security_invoker=true` + service-role-only grants re-asserted. Both trust breaches now cleared — board green.
- **Sweep — nothing else actionable (healthy platform):** Sentry — the other 2 unresolved are stale one-offs (9–13d, non-recurring). Perf advisors — 227, all INFO (unused-index / no-PK hygiene; acting on them is net-negative risk). Security advisors — 7 WARN are known SECDEF-executable fns backing real features (`serial_fmv_estimate` MUST stay anon per CLAUDE.md), 167 INFO the deliberate service-role-only RLS posture, `secdef_anon_exec_drift` `[]`. Pipeline fails/24h — all upstream Flow `400/429/500`, transient saturation-class statement timeouts (each with healthy neighbors), by-design guards, or the queued OPENS-HISTORY-BACKFILL-UPSTREAM-500 (off-limits ingest edge-fn + no Deno toolchain here).

### August 1, 2026 (overnight autonomous pass) — GENUINE OVERNIGHT (01:03 PDT, no skew); shipped 0 (correct); post-ship watch of the busy 07-31→08-01 CC wave ALL PASS; health GREEN except one verified-benign FMV-sanity false positive; 2 queued

Fired in-window, no clock skew, push available, no FREEZE, inbox empty, `origin/main` `6d0cc7e0` unchanged start→end. Shipped 0 / reverted 0 / repaired 0 — no low-risk shippable candidate existed. Full handoff: [docs/handoff-2026-08-01-overnight-pass.md](docs/handoff-2026-08-01-overnight-pass.md); ledger entry 2026-08-01.

- **The one trust breach — `fmv_sanity_flags`=1 — is a VERIFIED BENIGN FALSE POSITIVE; FMV was NOT touched.** `v_fmv_sanity_flags` flags TS `261:8714` Julian Champagnie "2026 NBA Finals" LEGENDARY: FMV $97.08 (MEDIUM) = 11.9% of the set median $815 (view fires <12%). The FMV is CORRECT — the edition's own 17 sales/30d run $60–$115 — so this is a role player in a star-inflated set, legitimate intra-set dispersion, not a mispricing. `breach_at=1` is very sensitive so the single persistent FP holds the metric red. Queued a sanity-view refinement (require the edition's own sales to also exceed FMV before flagging); did NOT weaken the guard autonomously.
- **QUEUED (new): OPENS-HISTORY-BACKFILL-UPSTREAM-500.** `topshot-pack-opens-history-backfill` failing on `events … status 500` (upstream) at a fixed range since ~04:11Z, correlating with the `c4c4d3c9` cursor-skip. Backfill-only (no live loss; TS forward pack-opens is the `pack-events-ingest` worker). NOT auto-reverted — `c4c4d3c9` is Trevor's legitimate pack-EV fix (verified good: bad positive-EV 23→0) and the failure is upstream, not a code defect. Operator/CC: confirm transient vs permanently-bad range, then a route-level skip if persistent.
- **Post-ship watch ALL PASS:** pack-EV suppression 23→0, unmapped_sales serial 0→NULL (96,374), AllDay serial-supply 1.5 ok, set-detail timeout fix cleared Sentry NEXTJS-22 (0/24h), Candy go-live still green. Health: security 0/0/0/0, pg_cron [], stalled [], sentinel 0, Sentry 0/24h, prod READY, DB 11,582 MB.
