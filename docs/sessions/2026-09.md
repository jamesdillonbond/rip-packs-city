## 2026-09-10 (close-out PT) · Claude Code (cloud) — I set out to backstop a second lane, the measurement said don't, and the thread ended on a 13-day user-facing staleness nobody's instrument could see

**Trevor:** "Do what you think is best… based upon what's best for RPC long term and for our users" → "Keep going" → "work through anything unresolved… then commit what you need to memory"

Everything durable from this session is promoted — nothing here is the only copy. Homes: **#81** in [known-issues.md](../reference/known-issues.md) · the 4th `*_at` case in [database.md](../reference/database.md) · the docs-only-badge recurrence in [testing-and-ci.md](../reference/testing-and-ci.md) · the `*_at` rule rewritten in `CLAUDE.md` · three ledger entries.

### What actually happened, in the order the answers arrived

1. **Finished the ten-lane egress audit.** Exactly two of ten reach no external network: `wmc-fmv-populate` (already backstopped) and, new, `cron/alerts-dispatch`.
2. **Measured `alerts-dispatch` and did NOT backstop it** — 230 invocations/72 h, p50 1.4 s, p95 32.3 s, max 97 s, producing **3 deal enqueues and 0 FMV** for **2 subscriptions and 1 owner**. Its zero is **correct**, checked rather than assumed: against live boards of 196 and 29 rows, the Blazers sub matches 1 serial (under its own 25% bar) and the Lillard sub matches 0. The June-834-vs-July-0 shape that looked like an outage was a since-deleted subscription.
3. **Shipped the audit as a guard instead** — `scripts/check-lane-egress.mjs`, 18 mutation tests, wired into `ci.yml`. The obvious check (grep the route file) returns 0 for four of ten lanes and is **wrong on two**.
4. **Then it caught its own first new lane**, hours later, exactly as designed — see 6.
5. **Fixed a red `main` that wasn't mine** and found it had been hiding behind **seven green badges** (5112–5118 were docs-only, so the code jobs were *skipped*). ⭐ The documented class inverted: I had a true local red, saw CI green, and nearly retracted an accurate finding.
6. **#81 — the real finding.** `offers-sweep` is Top Shot's only writer of `edition_offers.highest_offer`; last write **2026-08-28**. `edition_offers.updated_at` reads **minutes old** anyway because `topshot-deal-floor-serials` stamps it while writing only `low_ask`. Added `offers-sweep` to the backstop as its eleventh lane — which made the new guard fail as *unpinned*, which is the design working in production.

### What I got wrong, kept for the next session

Five hypotheses of mine died on data this session, which is the honest headline: *alerts-dispatch is broken* · *alerts-dispatch is worth backstopping* · *All Day's `edition_offers` is stale* (it is **correct** — that indexer deletes on offer-gone) · *Top Shot is fine because `updated_at` is fresh* · *`low_ask_serial IS NULL` is a clean control* (contaminated — that cron writes a null serial). ⭐ **Two of them would have shipped work that made things worse**, and the only thing that stopped either was running one more query.

Two mechanical lessons already promoted: a **hand-rolled comment stripper** put my guard's evidence 14 lines off (`:219` for a call at `:233`) — the ban-at-zero caught it; and **mutation tests against an injected filesystem proved nothing** until `resolveSpec` stopped closing over the real `fs`.

### Left open, named rather than dropped

- **#81's first failure is the live one.** `offers-sweep` stopped WRITING on 08-29 (72 runs/day, `rows_written` 0) ten days before it stopped RUNNING. Restoring the caller addresses only the second.
- `sales-counterparty-backfill`: **130 of 863 runs** in 72 h fail `canceling statement due to statement timeout` — the **Postgres** one, on the claim. 0 rows found across all 863.
- `golazos-listings-indexer` is no longer zero-yield (25 found / 8 written) — take it off the #79 candidate list.
- Operator-only, unchanged: re-enable the cron-job.org entries (the real fix for all eleven lanes) · add `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` as GitHub secrets.

## 2026-09-10 (close-out PT) · Claude Code (cloud) — the sweep metrics I shipped at the start of the night answered the question the night started with, and the answer is "not with code"

**Trevor:** "Keep going and work through anything unresolved... then commit what you need to memory"

### The measurement that closes the thread

The thread opened with *"how do we keep driving the KPIs that gate the roadmap"*. With the sweep-completeness metrics live (shipped earlier tonight, migration `20260910230812`), the gated collections read for the first time **with their position attached** — 20:42 PT leg:

| | HIGH/MED (all) | swept 24 h | HIGH/MED among swept |
|---|---|---|---|
| Top Shot | 52.5% | 77.0% | **68.1%** |
| All Day | 25.8% | 55.2% | **46.7%** |

⭐ **In both, the unswept cohort is ~0% HIGH/MED**, so the headline is arithmetically `sweep% × fresh-rate`. **Which makes "finish the sweep" look like a free +21 points on All Day.**

🚨 **It is not, and I checked before writing it down.** Cohorted against the LIVE `sales` table (not the recorded `sales_count_30d`, which is as stale as the snapshot carrying it): **the stale cohort is 2,806 editions with 2 HIGH/MED and only 6 holding ≥5 sales in 30 days.** The tail is unswept *because it is illiquid*; sweeping it moves ~6 editions, **+0.1 pt**. And the other lever is bounded too — **91** fresh editions carry ≥5 sales/30d without being HIGH/MED, so a perfect rule-side lift is **~+1.5 pt → ~29%**, short of the bar. ⚠ 91 is a proxy (the real rule re-fetches thin editions over 90 d and corroborates asks at 3), so it is an order of magnitude, not a target.

⛔ **So M2 is gated on All Day sales volume, not on our code — both code levers together are ~+1.6 pt.** That agrees with the 09-09 sizing (173 eligible → 54 real → +0.9 pt) reached by a completely different route, which is the only reason I trust it.

⚠ **And M1 should be stated as a RANGE: 48–55%.** The same day produced 47.9 twice and 52.5–54.6 tonight on cursor position alone. **A single leg is neither a pass nor a fail**, and 68.1% is the ceiling the pricing logic can actually reach.

### What went into memory, and what it cost

CLAUDE.md is at **39,975 / 40,000**. One rule earned its place — the honesty **MIRROR**: *an `unknown` that is actually KNOWN is the same defect*. Tonight's watchdog said "cannot tell" seven minutes after the proof it needed had landed; the canon is almost always invoked against a failed read rendering as a fact, and the reflex that follows is to widen `unknown`. **Paid for by three trims, none of them a rule:** a stale TS reading (which also carried a UTC stamp in memory, against the PT rule), the Candy/Panini tail compressed to register numbers, and one over-long pointer.

⚠ **The link guard caught me immediately** — quoting a CLAUDE.md line verbatim into `docs/reference/` carries a repo-root path that resolves wrong from there. **That is the exact bug the guard was written for, and it has now bitten the same way twice in one session.**

### Also closed tonight

- **21 UTC stamps in `go-live-2026-09.md`** converted to PT-first with the UTC original in parentheses; the bare `HH:MMZ` forms that remain are **sweep-leg identifiers** (database row names, not reports of a time) and the file now states that convention explicitly.
- The daytime monitor's carried-forward watch — *does the GHA backstop self-fire?* — **closed with the bad answer** (dispatch-only; 0 of 27 slots), recorded in the inbox index and register #80.

## 2026-09-10 (late night PT) · Claude Code (cloud) — GitHub's scheduler went to ZERO for 2.8 hours, and the reason nobody knew is that every watcher in the estate runs on the thing that broke

**Trevor:** "Keep going"

### What shipped

- **`public.rpc_gha_schedule_watchdog()` on pg_cron `8,38`** (migration `20260911045500`) — the first watcher in this estate that does **not** depend on GitHub. Plus a `gha-schedule-watchdog` watchlist row so the GHA sentinel alarms if *pg_cron* stops: two independent schedulers, each watching the other.
- **`extra.event` on both heartbeat writers** (`pipeline-sentinel.yml`, `dead-lane-backstop.yml`) carrying `github.event_name`. ⭐ **This is the whole measurement:** a heartbeat recorded that a workflow RAN but never WHICH TRIGGER ran it, so a `schedule` tick and a hand-fired dispatch wrote **identical rows** — and on the night the only runs that existed were dispatches.
- **A tree-walk guard** (`__tests__/gha-schedule-watchdog-covers-every-tagged-heartbeat.test.ts`, 13 tests) asserting the watchdog's lane list and the workflow tree agree **in both directions**, and executing both shipped `run:` bodies rather than grepping them. Two mutations run, two caught.
- Docs: known-issues **#80**, the stall + watchdog design in `cron-and-schedulers.md`, one rule promoted into CLAUDE.md by displacement.

### What I measured

**Scheduled delivery was not degraded — it was OFF.** Baseline over the 7 hours to 18:30 PT: **~4.3 scheduled runs/hour**, hourly range 1–6. From 18:30 to 21:11 PT, ~27 slots across four high-frequency workflows: **zero**. Three controls, because "GitHub is broken" is the easy story: Actions itself was healthy (three dispatches and every push CI run succeeded in the window); `status=queued` held exactly **one** run and it was created **2026-08-19**, so ticks were dropped and not delayed; and **cron-job.org was alive** — the three `*-sales-indexer` lanes logged 8–9 runs each. ⭐ That pins the blast radius to exactly *lanes whose only live caller is a GHA schedule*.

A concurrent session on Trevor's box measured the long-run rate the same night — **27% of an hourly cron over 191.9 hours, median gap 4.06h** — which I used rather than the older 16-of-48 figure, and which **changed a decision**: see the threshold below.

### Three things worth keeping

1. **An alarm sharing a scheduler with its subject is not an alarm.** `pipeline-sentinel`, `ops-monitor`, `e2e-smoke` and `scheduler-liveness` are all GHA `schedule`s, so the lanes and every instrument watching them stopped together. A 2.8-hour outage of 13+ lanes was found by a session happening to look. **This is the rule that went into CLAUDE.md**, and it is the second time in one day a whole detection layer turned out to be downstream of the thing it watches (the first was #76, where a Vercel pause took out the HTTP detectors).
2. **A threshold is a false-alarm budget, so it is computed, not chosen — and mine was wrong first.** I wrote 3 hours. At 27% delivery and 4 slots/hour that is 12 slots, p = 0.73¹² ≈ 2.3% for an all-miss run, and with 96 starting positions a day **~2 false alarms every day**. 6 hours is 24 slots, p ≈ 5e-4. ⛔ The cost is stated rather than hidden: **a stall publishes ~6h in.** The tick counts are written every tick regardless — the flag is the alarm, the counts are the instrument.
3. 🚨 **The first tick rebuilt this repo's own honesty defect inside the instrument meant to prevent it.** A **total** stall writes no tagged heartbeat at all — nothing runs, so nothing writes one — which classified as `unknown`, and with `ok` keyed on `stalled` alone **the single worst case would have written `ok = true` forever** while the estate's only scan (`ok = false`) saw nothing. ⭐ **I found that by RUNNING it and reading the row, not by reasoning about the code I had just written.** Fixed: an unknown has a shelf life — deploy artifact for 12h, BROKEN PROBE after that.

### Two measurement notes

- ⭐ **The obvious predicate was the expensive one.** `WHERE pipeline LIKE '%-heartbeat'` cannot use `pipeline_runs_pipeline_started_idx` and seq-scans the whole table: **3,618 buffers / 147 ms against 11 buffers / 2.8 ms** for an `IN (...)` list — **329×** — and ⚠ **narrowing the time window does not help, because the seq scan reads every page either way.** Measured before scheduling, because this instance's saturation is IO-bound.
- ✅ **Proven it can see a failure rather than assumed:** a synthetic `schedule`-tagged heartbeat at −7h produced `stalled` / `ok = false` / an error naming 420 min / `rows_*` NULL, with the no-tagged-rows case as the negative control in the same pass. All five test rows deleted afterwards. ⚠ The 3-arg `log_pipeline_run` **COALESCEs `rows_*` to 0** — a fabricated zero for anything that measures nothing — so the 11-arg form with explicit NULLs is the only correct call for a heartbeat or a watchdog.

### Still open, and it is Trevor's

🚨 **Re-enabling the cron-job.org entries for the ten lanes is the only lever that restores a 5-minute cadence** — the watchdog **records**, it does not **deliver** (no Telegram/email: those tokens are not reachable from a sandbox) and it does not restore cadence. ⚠ And the mechanism behind the stall is **not established**: `githubstatus.com` is egress-blocked here and the repo is public (free standard runners), so a GitHub-side incident and repo-level deprioritisation under the night's heavy push volume are indistinguishable from this side. The defence is a caller GitHub does not throttle, which is what shipped.

## 2026-09-10 (PT) · Claude Code (cloud) — I shipped the one lever M2 had, then spent the rest of the session finding out that most of what I measured about it was a sampling artifact

**Trevor:** "How can you continue to work on driving and increasing our key metrics and KPIs" → "make your own decisions" → "Keep going"

### What shipped

- **Ask-corroboration reaches All Day** (`app/api/fmv-recalc/route.ts`, Step 2a-ter(c)). It was fed only by Top Shot's `edition_offers`, so 461 TS editions held MEDIUM on that path and All Day held **zero** — while the same All Day floor ask was *already* trusted in the same route to CAP a sales-derived FMV and to be the SOLE price signal for 1,560 ASK_ONLY editions. **The state was internally inconsistent; that was the finding, not the missing feature.**
- **The trust-health leg publishes its own sweep completeness** (migration `20260910230812`): `<c>_fmv_sweep_pct_24h` + `<c>_fmv_high_med_fresh24h_pct`, folded into the `DISTINCT ON` pass the leg already makes so it costs no extra scan.
- Docs: #76 (P0), #70/#78 notes, go-live M1+M2 rows, and three rules promoted into memory.

### The thing worth remembering: I was wrong three times, in three different ways

1. **I reported "M2 moved +4.9 points, read at the bar."** The next leg read 23.0 and the series went **22.59 → 24.07 → 28.3 → 23.0 in nine hours**. ⭐ The metric reads the LATEST snapshot per edition, and recalc walks the liquid head before the illiquid tail, so the whole-estate share swings 5–7 points on **cursor position**. 28.3 was a favourable sampling point. **The M1 row in go-live already documented this mechanism and I read a single leg as a settled gain anyway.**
2. **I over-corrected my own sizing.** Having claimed 449, I downgraded it to "somewhere between 223 and 449" on a mechanism I had not measured. Measured, the 30-day-map condition costs **~13 %**, not the ~50 % the 223 implied. **The pessimistic correction was as unmeasured as the optimistic estimate it replaced.**
3. **I re-made a misreading the ledger had corrected an hour earlier.** `get_project` → `live: false` plus both apex hosts missing from `domains` **reads identically on a healthy estate**; I reported the site down while it had been up 2.4 hours. **Verify platform state by making a request, not by reading a status field.**

### What actually measured the change

The **matched control** — treated (ask inside the ±25 % band) vs control (outside it), two groups differing only by the predicate the fix touches. It is immune to sweep position because both face the same cursor: **83.2 % vs 13.0 %**. ⭐ **The mechanism's effect was established; the gate's level was not, and I conflated them.** The honest decomposition: +317 editions lifted (+5.1 pts) against ~−4.5 pts of tail-correction = **net +0.6 points on M2**.

### Two things I declined, with the number that would reopen each

- **The 30-day-window second lever.** 173 editions looked eligible (+2.79 pts, clearing the bar) — **119 were already MEDIUM**, so the real gain is 54 and +0.87 pts. ⭐ **An eligibility count is not a gain count.** Reopen if the already-in-target share falls well below ~69 %.
- **A sentinel arm for the new metrics.** No non-noisy threshold exists (a low sweep % is information, not a fault); the one alertable shape needs a curated per-collection list; and the outage it would catch is already covered by the pipeline-silence arms. Reopen if a collection ever stops sweeping *without* those arms firing.

### And one P0 found by accident

A hard smoke failure appeared on my commit. It was not mine — smoke had been red for 7 hours — and reading the failing job found `DEPLOYMENT_PAUSED`. ⭐ **Three detectors, three blind spots, composing into silence:** the sentinel is itself an HTTP route so it 503s *before* `log_pipeline_run` and leaves no row (looking "silent", not "failing"); `alerts-dispatch`/`alerts-send` sit behind the same paused deployment; and `Scheduler liveness` is **daily**, last ran an hour before the event, blind for ~23 hours. **I did not unpause it** — the pause may have been the intent, against a $412/cycle bill and a "no infra spend pre-revenue" rule, and I could read neither billing nor prod. It recovered on its own; all three listing lanes are ingesting again, Golazos for the first time in 7.4 days.

### Promoted to memory

`verify platform STATE by a REQUEST, never a status field` · `an ELIGIBILITY count is not a GAIN count` · and into `tooling-gotchas.md`, **a run's START, not its row timestamps, decides which side of a deploy it is on** (a run that began 3 s before the alias switched wrote old-code rows into my "post-deploy" window and produced a 1-of-12 false refutation).

## 2026-09-10 (night PT) · Claude Code (cloud) — the alarm was mute, and every finding tonight came from RUNNING something rather than reading it

**Trevor:** "Keep going and plan on working autonomously for the next 3 hours"

### The one habit that produced everything else

I dispatched `Pipeline Sentinel` to verify an unrelated change end-to-end. That single real invocation produced **four** findings that no amount of reading the code would have surfaced: the Telegram 400, the fact that email has never delivered, the proof that the site was already back, and the lane split. ⭐ **Reading tells you what the code intends; running tells you what it does.** I keep re-learning this and it keeps paying.

### The alarm's delivery probability falls as the incident gets worse

The sentinel ran 18 checks, concluded **CRITICAL**, and got back `telegram-FAILED:http_400 … "message is too long"` with `email-FAILED:not_configured` beside it. **Both out-of-band channels failed on the same run.** The mechanism is the nastiest shape an alarm can have: the message is one line per check, `Pipeline Silence` names every silent lane, Telegram caps text at 4096 characters and **rejects rather than truncates** — so the message grows with the incident and the alarm is least likely to arrive on exactly the runs that matter.

Fixed as a class (three unbounded senders, bound applied **inside** each sender so no call site can be copied without it). The contract I held myself to was not *"it fits"* but *"it fits AND the reader can tell what was removed"* — dropped criticals are named, because a bare count lets someone believe they saw everything.

Then I built the arm that reads the delivery record the route has written **since 08-29 with nobody ever looking at it**. Its first finding: `email-FAILED:not_configured` on **18 of 18** runs. The estate has been single-channel this whole time. Setting the env is Trevor's — it is off-limits here — and until it is set, one Telegram failure is a total alerting outage.

### The arm shipped wrong once and the deep battery caught it

All 8 of my new cases passed. The full suite did not: 7 cases in `api-sentinel-deep` went red because the arm warned on an all-green fixture. I had **collapsed two different zeroes** — *no sentinel runs at all* (a real defect) and *runs with nothing to say* (a healthy estate). Warning on the second is the "must be satisfiable at a population of zero" shape; the arm was punishing its own success. ⚠ **`npx vitest run <file>` would have shipped it.**

### Two of #76's three P0 readings read identically on a healthy estate

The site came back on its own between 21:46Z and 00:01Z. What is worth keeping is not that, but this: `get_project` **still reads `live: false`** and its `domains` list **still omits both apex hosts** — right now, with `www` aliased to a READY deployment and the route serving 200s. Those were two of the three readings the P0 rested on. The only one that was ever evidence is the sentinel's **503 `DEPLOYMENT_PAUSED` body — a direct observation of a request.** The repo's "verify by rendered DOM, never HTTP 200" rule, one level up: **verify platform state by making the request, not by reading a status field.**

### Testing the stated exit condition instead of re-reading it

#76 left a test — *do the lanes resume on their next fire?* Two did, exactly on schedule. Eleven did not, and they are the ones on **1–15 minute** cadences: 43 to 641 missed ticks each. But the count was also wrong in the *other* direction — `refresh_wmc_fmv_changed` is pg_cron job 303, which **ran 70 times, all succeeded, and never stopped**; it contains no `log_pipeline_run` at all, so its `pipeline_runs` rows came from a Vercel-side caller that died. **Its silence is a logging gap, not a work gap.**

So I went to the outcome tables, and four lanes came out four different ways: Pinnacle **alive** (0 min old) while reading silent · All Day **genuinely dead**, confirmed independently of its own log · Golazos **7.3 days stale** — a different problem entirely. ⛔ **"N lanes are dead" cannot be said off `pipeline_runs` silence alone.**

### #78: a lane reported 96 clean runs a day for a week and moved nothing

`laliga_golazos` listings: 908 rows, newest `listed_at` **2026-09-03**, **539 still marked open**, against 91,350 rows at 0 minutes old for Pinnacle on the same instrument and the same query. Roughly **670 clean-reported runs that wrote nothing.** I named the alternative I could not exclude — Golazos is much the smallest marketplace — and said plainly that the discriminator is one live read of the feed, which I did not run. **The cause is not claimed.** Every Golazos floor ask and deal score has been serving week-old data while reporting nothing wrong.

### The one he had to tell me again: report times in PT

For the whole session I wrote `03:12Z`, `02:27:34Z`, `00:01:09Z` into every summary I gave Trevor. It was **8:12pm** where he sits. *"I've told you so many times."*

⭐ **The rule was already in CLAUDE.md and I still broke it, which is the part worth understanding.** It said *"DATES ARE PACIFIC"* and *"Convert to PT before stamping any `### <date>`"* — **both about STAMPING**. So I converted every ledger heading correctly (the future-dated guard passed all night) and quoted raw UTC in every sentence I wrote to him. **The rule covered the artefact and said nothing about the conversation, and I read it exactly that narrowly.**

It is now explicit — *chat, summaries, headings, all of it* — and it went in by **displacement**, not by spending the 33 characters of headroom left: the replacement is 24 chars shorter than the line it replaces, because the clock recipes moved to `tooling-gotchas.md`. Headroom 33 → 57.

⚠ And the thing I should have checked months ago: **this sandbox has working zone data.** `TZ=America/Los_Angeles date` returns `2026-09-10 08:14PM PDT`, verified against `2026-09-11 03:14:54 UTC`. I had been carrying the *Git Bash on Windows lies both ways* trap as if it meant no conversion could be trusted anywhere, and stayed in UTC by default. It is a Windows problem, not this one.

### The one I got wrong: three code commits with red CI

I pushed three code commits tonight and **every one of them had a failing CI run**. I verified each with `npm test` (16,599 → 16,610 → 16,619 tests, green) and `npx tsc --noEmit` (clean), wrote "full suite green" in the commit message, and pushed. The docs-only commits in between passed, so the badge history looked alternating rather than broken, and I did not look until the end of the window.

**One job of nineteen was red: `ESLint ratchet`** — the one gate neither local command covers. `npm test` runs vitest and nothing else; `tsc --noEmit` is types only. The offender was a single `let` that is never reassigned, in a harness I wrote. And the ratchet is **per-rule, not per-total**: `prefer-const grew 15 -> 16` while the total read **717 against a baseline of 717**, so a total-only check would have waved the broken tree through.

⚠ CLAUDE.md already says *"`npx vitest run <file>` proves the FILE, not the tree — run the full suite before pushing."* I did run the full suite. **The full suite is not the full gate**: here it is `npm test` AND `npx tsc --noEmit` AND `npm run lint:ratchet`. The third existed the whole time and I never ran it once. ⭐ **And the deeper habit error is the one I want to keep: I read my own local green as CI's verdict instead of reading CI** — which is the same mistake, in miniature, as the `live: false` correction I wrote three hours earlier. **Verify by making the request, not by reading the status you expect.**

### Also closed tonight

#75's open question came back **FROZEN, not climbing** — `n_dead_tup` sat at exactly the value my own ANALYZE set while ~600 rows aged out — so the scheduled job was required rather than optional. Job 482 is live and **verified on a real fire** (23:41:00Z, succeeded, 320 ms, stat 238 → 977), because "can a `postgres` job ANALYZE a `supabase_admin` table" was worth testing rather than assuming.

And #76's blind spot (1) is fixed: the runner writes a `sentinel-heartbeat` **before** calling the route, so "the tick fired and the route died" no longer looks identical to "the tick never fired". That one also had a vacuous case — 18 tests passed first time and the status digit-strip survived deletion, because my "no response" fixture left the variable empty and the `:-0` default rendered it `0` either way. The real vector is that the status is `tail -1` of the response, so a transfer dying before curl's `-w` runs puts **body text** in the status variable.


## 2026-09-10 (late evening PT) · Claude Code (cloud) — a statistic was lying, autovacuum had never once run, and the second fix I tried had to be cancelled

**Trevor:** "Keep going and plan on working autonomously for the next 3 hours"

### Catching it live was the whole difference

I checked the instance mid-sweep and it was saturated — **34 active, 20 IO waiters**. Instead of reading job history afterwards I took two `pg_statio_user_tables` snapshots 225 seconds apart, and the top reader was a table I had never looked at: **`net._http_response`, 589 blocks/s, 2.8× the next one** — while sitting only **4th** by cumulative reads, which is exactly why every previous sweep missed it.

⭐ The live wait breakdown also answered a question the scheduler tables cannot: **12 sessions on `IO/DataFileRead`, 9 on `IPC/BufferIo`, zero on any lock.** It is IO. And `authenticator` held 9 of 12 — REST-side, which corroborates yesterday's wallet-backfill conclusion on a third instrument.

### The mechanism was a statistic, not a query

pg_net's TTL pruning works fine. But `n_dead_tup` read **0** on a table that actually held **161,390** dead tuples, and `autovacuum_count` was **0 for the table's entire life**. Autovacuum's threshold is computed *from* that number, so a stat pinned at zero makes the trigger unreachable **by construction** — the deletes happen forever and the vacuum never does. 8.4 GB of TOAST holding 443 MB of live data.

✅ One `ANALYZE`, **581 ms**, and autovacuum fired **43 seconds later** and cleared all 161,390. The satisfying part is that the fix doesn't substitute for the estate's maintenance — it unblocks it.

### Then I tried the second lever and had to cancel it

A manual `VACUUM (ANALYZE)` is **unthrottled**. It blew past the 60-second timeout, was still in `DataFileRead` at 66 s, and pushed `io_waiters` from 0–2 to **6** — on an instance with a documented saturation problem, in a wallet-backfill hour. I cancelled it and then **checked rather than assumed**: no vacuum running, **zero cron failures** in the window.

⭐ **That failure is worth more than the fix.** The obvious remediation here is a scheduled maintenance vacuum, and there is even a precedent job to copy. **It would reproduce exactly the IO spike I just had to kill.** Autovacuum is throttled; a manual vacuum is not. Measured: 581 ms versus over 60 seconds.

### What I did not settle

Whether the stat keeps tracking on its own. It read 238 after my ANALYZE — **but ANALYZE sets that value directly, so that number may be my own measurement rather than the collector recovering.** The test is a re-read with no manual ANALYZE, and it decides whether a scheduled job is needed at all.

And the 8.4 GB itself stays. Plain vacuum only marks space reusable; reclaiming it needs `VACUUM FULL` and an ACCESS EXCLUSIVE lock across every pg_net lane. That is a maintenance window and Trevor's call, not something to do autonomously at midnight.

## 2026-09-10 (morning PT) · Claude Code (cloud) — the new arm caught the recurrence on day one, and the data killed the explanation we had already accepted

**Trevor:** "Keep going"

### The arm earned itself immediately

`check_pgcron_failure_rate('6 hours')` read **158 fails across 38 jobs** on its first real day. Yesterday nothing watched this surface at all; today it is the thing that said "look here."

### The bursts recur on a clock

`job startup timeout` lands at **09-09 12Z, 13Z, 18Z** and again at **09-10 12Z, 13Z** — and at **zero of the other 43 hours** in the window. #73 is written as a *nine-hour spell*. It is a **daily pattern at fixed hours**, and that reframing matters more than any number in it.

### Then the part I did not expect

#73 records the amplifier as the six-hourly heavies clustering at 0/6/12/18 and overlapping. ⛔ **The scheduled population is identical across all four windows, and at 0Z and 6Z the identical cluster finishes in seconds.** Same job, by hour: 51 s · 27 s · **780 s** · **530 s**. Another: 9 s · 7 s · **600 s** · **688 s**.

⭐ **The variable is the hour, not the schedule.** So the reflex fix — stagger the `*/6` jobs — cannot work, and I would have reached for it. The explanation was already on the page, already believed, and 48 hours of hourly data disposed of it.

### And `job startup timeout` turned out to be the symptom

At **09-09 12:36Z, 29 runs across 21 distinct jobs ended inside one minute, with durations spanning 0 to 788 seconds.** ⭐ **A shared duration is a timeout; a shared instant across heterogeneous durations is a global unblock** — that one distinction is what turned a pile of failures into a mechanism.

Several of those jobs returned `1 row` after 300–780 seconds. They were **waiting, not computing**. The startup timeouts fire eleven seconds after a launch attempt because the six slots are still held by the stalled jobs; then everything releases at once and a backlog flushes at 0.1 s a job.

I also ruled out the obvious lock explanation for the price of one query — all 11 bare-refresh jobs already use `CONCURRENTLY` — and wrote that down so nobody re-runs the hunt.

### What I did not do, and why

`pipeline_runs` says hourly load varies **3.1×** while pg_cron's population is flat, so the extra load belongs to the other schedulers. That is a real narrowing. ⛔ But my interaction hypothesis has a live counterexample in it — 0Z carries *more* runs than 12Z and stays clean — so I stated the counterexample rather than omitting it, and changed nothing. **The levers here are capacity and other people's schedules, and neither should move on a hypothesis I can already falsify myself.**


### Postscript, an hour later: I resolved my own counterexample, and the fix was a better denominator

I ended the entry above with an honest gap — 0Z carries *more* `pipeline_runs` than 12Z and stays clean, so run count could not separate them. **The count was the wrong denominator.**

Split by lane and it falls apart immediately: the load is the **`wallet-backfill` family**, and it runs **1,728 times at 13Z, 1,636 at 18Z, 1,456 at 07Z — and exactly zero at fourteen of the twenty-four hours.**

✅ **Both single-factor controls were sitting in the data.** 06Z: the six-hourly cluster with **zero** wallet runs → clean. 07Z: 1,456 wallet runs with **no** cluster → clean. 13Z and 18Z: both → bursts. That is an interaction with each factor independently exonerated, which is a much stronger thing than the correlation I had an hour ago.

⭐ And the overlap explains itself from a schedule already written down in #48: the 0Z and 6Z wallet cohorts land their work in the *following* hour and miss the heavies; the 12Z heavies run 600–869 s and are still holding slots when 13Z's flood arrives; at 18Z the cohort and the heavies overlap head-on.

⛔ I changed nothing, again. A plausible mechanism is not a measurement. But it gives #48's untested experiment a second and independent reason to run, a cheaper probe than the one it proposes, and a falsifier I wrote down before anyone runs it.

⚠ And the caveat stays in view rather than being quietly dropped: burst-hour run count may be partly an *effect*, since wallets re-dispatch ~9× and die more often when the instance is busy. **The 06Z/07Z pair is what carries the argument, because there the 0-versus-1,456 split is schedule, not retry.**
## 2026-09-09 (late evening PT) · Claude Code (cloud) — I went to widen a guard's scope, and measurement said the scope was right and my fix would have made it worse

**Trevor:** "Keep going"

### The gap was real and the fix was wrong

`check_cron_heavy_job_exec_drift` filters `WHERE j.username = 'cron_heavy'`. It inspects **58** job/function pairs while **55 active jobs run as `postgres`** — half the scheduled population, invisible by construction. That is textbook *"ask what a passing guard is structurally silent about"*, and I went to widen the filter.

⛔ **Then I checked whether the widened assertion could ever fail, and it could not.** `postgres` **owns** all 54 public functions its own cron jobs call, and an owner's EXECUTE cannot be revoked. Widening the `WHERE` would have added **55 assertions that cannot fail** — coverage on paper, nothing underneath. ⭐ **A guard I'd have shipped feeling good about, that would have made the next reader trust a number that meant less than the one it replaced.**

ⓘ Worth separating: it is not the superuser rule. `postgres` has `rolsuper = false` here — only `supabase_admin` is super. **Ownership** is what makes those pairs immune, and I only know that because I checked instead of reasoning from the role's name.

### So what was actually broken

The filter was **correct** — for a reason nobody wrote down and nothing enforced. ⭐ **The guard's blast radius was pinned to a role NAME rather than to the PROPERTY that makes a pair testable.** The day a job is scheduled under a role that doesn't own its target function, the name filter silently fails to cover it and nobody finds out — which is the same failure the guard itself exists to catch, one level up.

It now walks every active job and splits by that property: `inspected` **113** · `immune_owner` **55** (tested by nothing, and it says so) · `revocable` **58** (what it actually tests) · `offenders` **0**. **Reading `inspected` as coverage is now impossible.**

### The controls, because a zero without one is worthless

Across the 51 functions `cron_heavy` is scheduled to call, `has_function_privilege` reads **true for `cron_heavy` 51/51** and **false for `anon` 0/51** — so the predicate is not a constant. And `cron_heavy` owns **0** of the 51, so its EXECUTE is grant-derived and genuinely revocable. Nothing was mutated to establish either.

The smoke arm now floors `revocable` rather than `inspected`, and hard-fails a run that inspected 113 while testing 2 as **vacuous, not clean**. ⚠ Mutation-proven both ways — and the second mutation is the one worth remembering: replacing the three-state read with `?? 0` reds **14** tests. **The shape this codebase is organised around, caught in the arm I was writing to catch it.**

### Two things re-derived and deliberately not acted on

**#43's population has not drained** — 50 inert `statement_timeout` declarations today against the filing's 48, the working form still used by exactly 7 jobs, unchanged in 14 days. ⚠ But my role split (27/23) does not reproduce the filing's (36/12), so **I did not claim the split moved**: a method that cannot reproduce the recorded value cannot measure a change in it. And #43's own worked example, jobid 256, no longer exists.

**`rpc-ts-listings-atlas-sync`** — the saturation spell's top offender, 55 failures in the 6 h window — is **not** chronically broken: 09-07 **0.0%** · 09-08 **0.4%** · 09-09 **26.5%** (191) · 09-10 **3.3%**. The failures were the spell. Checking cost one query and stopped a hunt.


### Postscript: the same question, a second guard, and the answer was "don't"

`sales-batch-insert-23505-guard` is scoped to one directory and excludes the backfill routes on a **claim about their code**. Nothing enforced that claim, so I checked it: all 30 files referencing `23505`, every handler outside the scope read by hand. **Population zero, exclusion still true.**

⛔ **Then I did not widen it, and the reason is the useful part.** The property is *batch insert + 23505 swallowed with no row-by-row fallthrough*, and **the fallthrough half is not reliably visible to a source scan.** My prototype produced twelve candidates and all twelve were false positives — mostly the row-by-row retry itself, whose `.insert(row)` argument is an identifier and therefore looks batch-shaped. ⭐ **A guard with that false-positive rate is worse than none: it teaches readers to skim.** Same instinct as the cron guard, opposite conclusion, and both times it was measurement that decided.

### And a small piece of #70

The upstream probe is **confirmed impossible from here** rather than assumed — the proxy answers `403 CONNECT`, `connect_rejected — policy denial`. From our own tables the second All Day lane moves in lockstep with the sales lane, which **disfavours** a defect specific to the sales query. ⚠ It is **not** an independent control and I said so: both routes read the same Flow REST host.

🚨 The part I'd have got wrong by reading the obvious number: raw weekly totals say *the slide is accelerating*. The current week is **partial** — 234 runs against 551–725 — and **per run it recovered on both lanes.** A delta between two stocks is neither a rate nor a sign, and this one would have shipped as a headline.
## 2026-09-09 (evening PT) · Claude Code (cloud) — I went to unblock someone else's filing and ended up refuting the two facts it rested on

**Trevor:** "Keep going and looking for additional tasks to complete for the next 3 hours autonomously"

### The question was narrow and the answer was not

The 09-08 filing left exactly one blocker: *name the live path keeping `pack_distributions` fresh, then jobid 15 can be paused.* Its author had done the honest thing — enumerated six caller sources, found nothing, and **declined to guess.**

⭐ **The path exists and the enumeration cannot reach it: a Supabase Edge Function invoked by an external scheduler.** `pg_stat_statements` gave it away in one query — the RPC arrives wrapped in `pgrst_source`, which means it came over HTTP, so pg_cron, triggers and every in-database caller are excluded at a stroke. ⚠ **That wrapper is a free discriminator and I want it in the standing rule: check it before concluding "no caller."** The timestamps then placed the schedule (minute :13, four hours apart) and a GitHub Actions comment written for an unrelated reason corroborated it.

### And then the safety argument dissolved

That filing's case for pausing jobid 15 was *"the supply data is provably fresh from elsewhere"*, resting on two observations. ⛔ **Both are artifacts.** `total_sealed` is a GENERATED column — `GREATEST(total_minted - total_opened, 0)` — so "populated on 2,099 of 2,099" is a tautology, not a reading. And `pack_distributions.updated_at` is bumped every four hours by the catalog lane, which **says in its own source that it deliberately does not write the supply columns.**

🚨 The supply behind it: **median as-of 2026-06-28**, 2,068 of 2,085 rows older than 30 days, newest successful fetch two days before the upstream died. `pack_distributions` mirrors it on **2,083 of 2,083 rows, zero divergence in either direction** — with a positive control, because a zero is worthless without one: the same comparison **does** report a difference on the two rows whose fetch failed.

### The part worth carrying

⭐ **Four layers, each individually defensible, whose net effect is that a 73-day-old number is indistinguishable from a fresh one.** A lane dies. Its failure is recorded honestly — `topshot_pack_supply` has `supply_ok`, `supply_err`, and stores NULL rather than zero, exactly the shape this repo keeps asking for. **Nothing reads those columns; a grep across `app`, `components` and `lib` returns zero matches.** A sibling lane keeps bumping the row's `updated_at`. A generated column keeps returning a value. And the one alert named for this table keys on *"has a new distribution appeared"* while labelled `type: data_stale, pipeline: pack_distributions` — silent about the four columns every pack surface renders, and it would not have fired at any point in the drift.

⚠ **I did not claim a user is seeing a wrong number, and the reasons are in the filing rather than implied.** The highest-stakes surface is already drained to zero behind an honest branch, the pack page is careful about NULL-vs-0, and pack supply is genuinely slow-moving. **What is established is that nothing can tell the difference.**

### Why I shipped nothing

⛔ Pausing jobid 15 costs less than it saves in the wrong direction: 1 run/day, the only lane that resumes automatically if the upstream returns, and the last daily record that it is still dead. ⛔ A supply-age alert arm would fire immediately and stay red indefinitely — this estate's own permanently-red-instrument trap, and the fifth layer of noise rather than the first of signal. **The useful version needs a decision that is Trevor's.** So: a filing, a register entry, and a stated recommendation that the cheapest real win is not an alert at all — render the as-of date next to the number.

## 2026-09-09 (evening PT) · Claude Code (cloud) — the pg_cron failure surface was watched by a function nobody calls, and I corrected my own "recovered" call

**Trevor:** "Keep going and looking for additional tasks to complete for the next 3 hours autonomously"

### The gap, and why nobody had seen it

**Every sentinel arm reads `pipeline_runs`. A pg_cron job that fails to START never writes one** — `log_pipeline_run` sits inside the body, which never runs. I verified it by grep rather than by reasoning: `app/api/sentinel/route.ts` had **zero references to `cron.job_run_details`**.

⭐ And the one instrument that does read it — `check_pgcron_recent_failures()` — **has no caller anywhere in the repo.** It appears only inside *other migrations' comments*, thoughtfully discussing what it can and cannot catch. So on 09-09 the estate logged **399 cron failures against a 0–3-per-day baseline** and five sentinel sweeps saw none of them. **A function nobody calls is not an instrument.**

### Where I was wrong, twice, and how I found out

⚠ **My first framing of that function was "it sees nothing." Wrong.** Its `where l.status = 'failed'` filter means it reports the *currently* broken. Replayed at four probe times: the worst job **would** have surfaced (fails 47 → 174); two others with 23 and 17–19 failures were invisible at every probe because their latest tick happened to succeed. The real gap is narrower and more specific than the one I set out to describe — and only a replay showed it.

🚨 **Then two defects in my own arm, both caught by tests rather than by reading it.** The first is the exact error this whole codebase is organised around: `Number(r.runs ?? 0) || 0` **collapses "the RPC returned no payload" into "the RPC answered zero runs"** — a failed read published as a measurement, **inside an arm whose entire subject is measurement honesty.** Seven existing sentinel tests went red; that is how I learned. Fixing it left the later field reads unguarded, so a null payload threw a raw `TypeError` into the catch and the report said *"Exception: Cannot read properties of null"* — a driver message leaked into a health report, with the honest branch never reached.

⭐ **Both times the tests knew before I did.** I had read that code three times.

### And I corrected my own conclusion from a few hours earlier

I reported the spell **recovered**. The spell had. But **statement-timeout cancellations never reached zero** — 2, then 0, then 11, then 1 across successive hours, against a baseline of 0–3 per *day*. ⭐ **My call rested on the indicators I happened to check — startup timeouts, IO waiters, median duration — and not on the one still firing.** The new arm's own "negative control" window is what surfaced it, on its first use.

⚠ **Reattributed rather than re-filed:** every one dies at ~120 s, not `cron_heavy`'s 600 s — `username = postgres`, no `SET statement_timeout` prefix, and that role carries none in `rolconfig`. It is the 120 s-clipping class filed on 08-31, not spell residue.

### The finding that changes a nine-day-old recommendation

That filing is excellent and its cases are drained. But **my five jobs are different from its five**, because its method finds jobs clipped **at rest** and **load recruits others** from the 36 prefix-less jobs. So the class cannot be closed by draining a list.

⛔ **And there is now a second reason not to apply its fix, independent of the one it gives.** Its argument was **waste** (a 120 s kill becomes a 600 s kill). Mine is **contention**: with `max_worker_processes = 6`, raising these jobs 120 s → 600 s multiplies worker-slot occupancy ~5× **on the exact constraint that produced this incident's 135 startup timeouts.** Applying the prefix would have made today worse. It is a trade-off across two budgets now, not a one-liner — so nothing was applied, and the filing says why.

### Three more of my own defects, and the one that was a defect in a FIX

🚨 **The arm cost 18,313 physical buffer reads (~143 MB) per call** — an arm about IO saturation that was itself a 143 MB read every sweep. I found it with a pre-flight `EXPLAIN (ANALYZE, BUFFERS)`, not by it hurting. `cron.job_run_details` has no index on `start_time`, so a 6 h filter seq-scans 249,731 rows to find 2,174. ⛔ **The obvious fix was unavailable and I tested that rather than assuming it:** the table is `supabase_admin`-owned and `postgres` is not a member. Fenced the scan on the runid PK instead — **628 reads, identical answers.**

⭐ **Then the fence needed its own honesty branch, and that branch needed to be reachable.** A fence that cut the window short would publish an under-count as a measurement — the same failed-read-as-fact shape, one level down — so the payload carries `complete`. 🚨 **But my control for it would not bind: the fence scales with the window, so no window binds it, and no synthetic row can be inserted to force one. `complete` was unfalsifiable in production — indistinguishable from a flag hard-wired true, feeding a dead branch.** That is this file's own "prove a watcher can see a FAILURE" rule, and I had shipped the opposite. Fixed with a test-only fence-span override and proven live in both directions, with a no-change control the fix cannot move: span 100 → false/100, span 50000 → true/2174, default → true/2174, identical to before.

🚨 **And I retracted my own baseline.** I sampled 7 days, called it "the baseline", and it was the 7 days *after* a change point. Re-measured over 30: **08-11..08-30 at 50–496 failures/DAY, 08-31..09-08 at 0–3/day, 09-09 at 399.** ⚠ The inverse of the pooled-rate rule already in CLAUDE.md — a rate pooled *across* a fix measures the fix's absence; a rate sampled only *after* one mistakes the fix for the norm. ⭐ The 08-31 top-consumer IO drain is the plausible cause (sharp change point, sensible mechanism) but several things shipped that day, so it is recorded as correlation. **Nobody claimed that benefit at the time: the drain's own entry talks only about IO, so ~200 cron failures/day were removed by a change measured on a different axis.**

### What shipped

`check_pgcron_failure_rate(p_window)` and a `pg_cron Failures (6h)` arm. ⭐ Its detail splits startup-timeout from statement-timeout counts deliberately: **that split is the discriminator between worker-slot contention and a clipped tail — same failure count, opposite causes, opposite fixes** — and it is the thing that took longest to find by hand. It also covers the 11 of 134 cron jobs that are bare `REFRESH MATERIALIZED VIEW` and write no pipeline row at all.

ⓘ It does **not** supersede `Pipeline Silence`, which answered correctly that day. Saying so matters: an arm that oversells its own coverage is how the next reader stops checking the other one.

## 2026-09-09 (midday PT) · Claude Code (cloud) — I used a control to exonerate my own change, then didn't ask what the control meant. It was a live incident.

**Trevor:** "Dig deeper"

### The one sentence worth keeping

**A signal strong enough to exonerate a change is strong enough to be a finding.** Three hours earlier I proved a deploy of mine innocent with a no-change control — *the whole fleet got slower, so my probe moved with its siblings, not against them.* That reasoning was right. And then the control sat unexamined, **because it had already done its job.** Everything below came from finally turning around and asking what it was saying.

### What it was saying

The instance stepped into a saturation spell at **10:00Z** and was still in it nine hours later, worsening: fleet median pipeline duration 2,988 ms at 08Z → **30,921 ms at 18Z**, against a **flat 09-07 and 09-08 at the same hours**. That last comparison is the whole reason this is a finding rather than "the known daytime band" — two prior days refute it.

⭐ **The part I would not have predicted: `max_worker_processes = 6`.** Connections were fine (45 of 90); the binding resource is worker slots. pg_cron runs every job as a background worker, and the heavy six-hourly MV refreshes are clustered inside a 30-minute window of hours 0/6/12/18. Yesterday they took 12–93 s and never overlapped. Today they take 250–600 s and **collide** — I caught three running at once. The result is **270+ `job startup timeout` failures**, bursting at exactly hours 12, 13 and 18.

⛔ **And a job that fails to START writes no `pipeline_runs` row**, so every pipeline monitor sees silence rather than failure. This is the same shape CLAUDE.md recorded *yesterday* for the REVOKE/grant case, arrived at from a completely different direction.

### Three hypotheses I killed, which is most of the value

- **Autovacuum.** A 48-minute `VACUUM ANALYZE wallet_moments_cache` was at the head of the activity list — a beautiful culprit. It finished, and the instance stayed at 12 active / 11 IO waiters. Symptom, not driver.
- **An invalid index.** Zero in `public`.
- **An FMV propagation wave.** Writes did step up at 10Z — but **yesterday hit a higher rate with only a mild slowdown**, so volume alone is not sufficient.

⭐ Root cause is **not settled**, and I wrote that down instead of picking the most narratable candidate. `wallet_moments_cache` is the common denominator and is explicitly labelled UNPROVEN — including the caveat that its `autovacuum_count` of 963 has **no window** (`stats_reset` is NULL), so it is not a rate and I must not quote it as one.

### The instrument finding, where nothing was broken and nothing lied

The sentinel saw it. It ran mid-incident, named the right lane twice, and reported **six of sixteen checks INCONCLUSIVE — including `Trust Health`, the master arm.** Overall status: **WARN**. GHA: green. `ops_alert_dedup`: empty for fourteen hours.

The per-check rule that produces that — *a statement timeout is not data loss, so warn, don't page* — is **correct and was earned by two documented false pages**. I nearly filed it as the defect. It isn't.

⭐⭐ **The gap is one level up: overall status is `checks.some(status === "warn")` — a `some()`, not a count.** One warn and thirteen warns are identical. **Every check reasons correctly in isolation, and no check can see the population.** "My query timed out" is a weak statement; "six of us timed out at once" is a strong one, and nothing in the system could make it.

So: a `Measurement Blackout` arm that **issues no query** — the one check that sharpens as the database degrades — capped structurally at `warn`, and `ok`-but-visible below threshold per the route's own anti-chronic-warn convention.

### The part I got wrong, caught before shipping

I keyed the arm on the `INCONCLUSIVE` label and replayed the **real** 18:47Z payload through it rather than a fixture I'd written. It counted **four**, below its own threshold. **It would have stayed silent on the exact incident that motivated it.** Two checks time out with the identical error and are never labelled, because they build their detail by hand.

⭐ The fix is not to go label those two — it's to stop trusting the label. The arm keys on the **condition** now, and the real payload is a verbatim fixture, so label-only detection fails with *"expected 4 to be 6"*. **A fixture I write tests what I believe; the production payload tests what is true.**

### And what I didn't do

⛔ Nothing operational. Capacity is Trevor's call, and every lever I had — unschedule a heavy job, stagger the `*/6` block — treats a symptom on an unproven mechanism. CLAUDE.md already records a stagger that was REFUTED. Shipping the instrument is safe; shipping a guess at 13 active / 13 IO waiters is how last night's jobid-481 mistake happens in reverse.

---

## 2026-09-09 (morning PT) · Claude Code (cloud) — a top-3 database trap had no tree-wide instrument, and the guard that "covered" it was a list of six files

**Trevor:** "Work on anything you can find on our to do lists, Claude.md, roadmaps, pipelines, sentinel issues, known issues, or anything else… Also look for CI improvements."

### The pass in one line

I went looking for CI improvements and found that the most-repeated database trap in CLAUDE.md — PostgREST silently clamping a `.limit()` above 1,000 — was guarded by a **curated list of six already-fixed routes**. A tree walk found **21 sites carrying the shape, none of them on that list**, and two of them truncating today.

### Why the existing guard could not see them

`__tests__/invariants-postgrest-cap.test.ts` is a good regression pin: it names the six routes that were fixed after the class bit production five times, and asserts each still reads `fmv_current` or pages with `.range()`. It cannot fail for a seventh site, because it never looks for one. ⭐ **CLAUDE.md already says this in as many words — *prefer a tree walk over a curated list and a ban at zero over an allowlist; make suppression the curated list* — and the rule simply had not been applied to the class its own DB section names.**

⭐ **The tell here is a NUMBER, not a copy.** This repo's usual instruction is *"grep for the EXPRESSION, not the file"*, and it does not work on this class: every instance spells the number differently (2000, 3000, 5000, 8000, 10000, 20000, 100000, 200000). That is exactly what a tree walk with a numeric predicate is for.

### What `.limit(20000)` actually is

Not a bound. PostgREST returns 1,000 rows and says nothing, so the number is an **unstated claim that the population is smaller than it** — written in the one syntax that reads as an enforced limit. Anything computed underneath (a total, a map, `rows.length`) is then built on a truncation no caller can see.

### The measurement point: why I did NOT just rewrite every number to 1,000

It looks free. It is not. **Lowering an over-population bound to `.limit(1000)` is behaviour-preserving only if the server cap really is 1,000** — and that value is a PostgREST setting, not readable from Postgres. ⭐ **I tested the egress rather than assuming it: the agent proxy rejects CONNECT to the REST host, so I could not measure the cap from here.** Cutting a bound over a population of 838,392 on an unverified premise would have introduced the very truncation I was there to remove.

So the rule became: **measure the population first.** Sixteen sites measured under the cap were lowered to a bound that is TRUE and now carry the measured count and the date. The four at or over it keep their numbers and carry a marker stating the measurement instead.

### The two that truncate today

- **`pinnacle-metadata-backfill`'s Q3/Q4 discovery pools** — `.limit(5000)`/`.limit(8000)` over ≥9,000 rows, **with no `.order()`**, so which 1,000 it sees is undefined and the candidate filter runs client-side over the physical head. Classic queue-walk-from-the-top. ⚠ **But I measured the blast radius instead of leading with the scary number: only 419 distinct composite `edition_key`s exist across the pool and Q4's target set was 12 rows.** "Some keys may be unreachable" is the honest claim; "the tail is never repaired" would have been the exciting one.
- **`compute-topshot-pack-ev`'s ask-anchor fallback** — 1,995 rows read at `.limit(3000)`, so about half, unordered. ⭐ **`lib/packs/pack-deals.ts` hit the same wall on the same table and fixed it by calling `get_pack_ask_state_map()` — same defect, same table, sibling file, fixed in one and not the other.** ⛔ Left alone because the function is **dormant**, verified rather than assumed: no `cron.job` row, no `pipeline_runs` row in 24h, and `refresh_atlas_pack_ev()` (jobid 217) does the work.

### The endpoint I withdrew, and the test that was holding the defect in place

`/api/pack-listings/historical-pulls` read 838,392 rows at `.limit(20000)` (→1,000, unordered) and published the result as a pack's `total` pulls; a failed read returned `{ total: 0 }`. ⭐ **And a green test asserted that**, titled `returns total 0 when the acquisitions query errors` — a passing test stating the promise is what holds it in place, so it is **inverted, never deleted**.

⛔ **The reason it cannot simply be repaired is the third defect, and I only found it by reading the data rather than the code:** `moment_acquisitions.pack_dist_id` — the column that would tie a pull to a pack — is **NULL on all 838,392 rows**. That is why the route reached for a loose token match on `set_name` in the first place. **Caller named before touching it: zero.** Withdrawn to a 501 rather than deleted, so the diagnosis stays where the next reader meets it.

### Two method notes

- ⭐ **I extracted the suppression-window helper instead of copying it.** The divisor ratchet had bought three properties painfully (a same-line window is unreachable because these expressions wrap; a fixed 3-line lookback silently ignored two real suppressions on 09-07; the block walk must stop at code). A second copy would have re-earned all three. `scripts/lib/marker-suppression.mjs` now holds one implementation, and the divisor guard's **own** four controls still drive it — 9/9 after the migration, so the extraction is proved by tests that already existed.
- ⭐ **Mutation-checked in both directions.** A synthetic offender injected into `lib/seo.ts` reds the guard at the right line; neutering all four real markers reds it with exactly those four. "It passes" was never the evidence.

### And what I did NOT change

ⓘ **CLAUDE.md is untouched on purpose.** Every rule this pass needed was already in it. **Nothing new was learned — a documented trap simply had no instrument** — and adding a line would have spent scarce headroom (39,895/40,000) restating text that was already there and unapplied.

ⓘ **Two things checked and left, because a sweep that cannot return "not a defect" is a closing spree:** `sniper-feed`'s `fetchRetiredMomentIds` fails OPEN on a paged read, but `moments.retired` has 0 true rows (re-verified today) so it is latent and its own comment says so; and `lib/market-sources`' `return out`-on-error partial map serves `/api/market-truth`, which has **zero callers** and is already recorded as dead in that file's header.

---

## 2026-09-09 (early morning PT) · Claude Code (cloud) — I retracted my own #69 a day after filing it, and shipped the two instrument fixes it exposed

**Trevor:** "Do it all"

### The retraction is the session

Yesterday I filed #69 as *"the client-error beacon's first REAL finding — a production defect nothing else would have seen"*: 17 unhandled `SyntaxError: failed to parse` rejections on `/nba-top-shot/collection`, across six deployment ids, in one day. I put it in the register, in CLAUDE.md and in the go-live doc.

Today, one `GROUP BY metadata->>'ua'` returned **one value for all 17: `Lightpanda/1.0`** — a headless automation client. No `Mozilla/` prefix, which every mainstream consumer browser has; constant `width: 1280`, which fits a headless default and not 17 screens. **It is almost certainly that engine's own incomplete JS support choking on our bundle, and there is no evidence a human ever hit it.**

⭐⭐ **The rule this cost: when an instrument's first finding is SURPRISING, establish WHO generated it before believing WHAT it says.** I grouped by message, path, stack and deployment id — four fields — and shipped a conclusion, **while the one field that falsifies it sat in the same JSON object I was already querying.** I had even listed `ua` in a `jsonb_object_keys` result and moved past it. ⚠ A `count(*)` over an open endpoint counts *requests*, not *readers*, and `/api/telemetry` is anon-public by design.

### Both fixes, and an honest note about the first one

**`sid`** — a random per-tab id in `sessionStorage`, so same-`sid` rows are repeat loads in one tab and distinct `sid`s are distinct tabs. Not localStorage, not a cookie: per-tab and cleared on close is a discriminator, a visitor id is a different thing with different obligations. Blocked storage *throws* rather than returning null, so it falls back to a module id instead of raising inside the error handler.

⚠ **But `sid` does not solve the problem that actually bit.** A crawler gets a `sid` like anything else. It answers *how many tabs* when the question that mattered was *is there a human here at all*. **A correct fix to the question I asked, and the wrong fix for the question I should have asked.**

**The alert arm** — this is the one that could have paged someone. `client_error_burst` counted every row with no UA predicate, firing at `paths >= 5 OR count(*) >= 25`; the crawler produced **17 of the 25 needed** to raise an alert on traffic with no human in it. Now filtered on a *structural* property (`ua NOT LIKE 'Mozilla/%'`) rather than a bot list, because a guard that names its instances dies on a rename — three have here.

⚠ **The polarity was the real design decision: a MISSING `ua` counts as HUMAN.** Unknown is not automation, and the other polarity fails *silent* — a future beacon change that stopped sending `ua` would switch the alarm off with every gate green. ⛔ And excluded rows are not hidden: the detail carries `(+N from non-browser UAs, excluded)`.

### The control that mattered was the negative one

Four post-flight controls run inside the migration's transaction, so a failure rolls the splice back. The one worth naming is **(2): 30 rows from `Lightpanda/1.0` must NOT fire.** Without it, "the arm stopped firing" is equally consistent with a correct fix and with an arm that can never fire again — and I would have had no way to tell which I had shipped.

### Two smaller method notes

- ⭐ **Mutation testing caught my test, not just my code.** The first version of the `sid` test asserted tab-discrimination via `pageSessionId()` directly, so hardcoding `sid` in the *payload* was caught only by a sibling assertion. The test whose **title** claims the property has to be the one that fails when the property breaks; rewritten to drive the posted body.
- ⭐ **I tested egress instead of asserting it.** #70 needs an upstream read, and rather than repeat "sandbox egress is blocked" from memory I ran it: `curl` to the Flowty and Atlas hosts returns `000` through the agent proxy. The claim now rests on a measurement.

### CLAUDE.md is structurally full

This turn's one new rule took the file **38 characters over** the 40,000 limit. I funded it by trimming the *reference index* — navigational descriptions, the least rule-dense content in the file — rather than shaving the rule itself to fit. **Two sessions in one day have now had to cut something to add anything.** 39,895 / 40,000. The next substantive addition needs a deliberate restructure, not another squeeze.

## 2026-09-09 (early morning PT) · Claude Code (cloud) — part 2 of the doc refresh: All Day gets its register row, and I correct my own measurement from part 1

**Trevor:** "Keep going"

### The gap I left was the finding

Part 1 identified All Day's confidence slide (M2, **27.7 % → 20.2 %** against a ≥ 30 % bar, a real numerator loss on a fixed denominator) and wrote it into two strategy docs — but **never gave it a register row.** This register's own rule says an item that matters and is still open should have one, and that its absence is itself the finding. **#70 filed.** It is worth stating why the gap happened: M1 has a Routine, a ledger entry and a retraction attached to it, so it gets re-read constantly; M2 had nothing, and slid seven points across four readings unattended. **Instrumentation attracts attention, and the un-instrumented metric is where the drift hides.**

### I violated a CLAUDE.md rule in the same pass that re-derived everything else properly

Part 1 stated the All Day decline as *"~333/day (08-19..24) → ~153/day (09-01..08)"*. That is **a delta between two hand-picked windows** — precisely what CLAUDE.md forbids: *a directional claim needs a DISTRIBUTION, not a snapshot*. I had re-derived every other number in that pass from the live system and still shipped the one shape the rules name.

The weekly series changes the reading. All Day: **2,578 · 6,772 · 2,984 · 2,626 · 1,609 · 2,479 · 3,012 → 1,291 (wk 08-24) · 938 (wk 08-31)**. That is an already-volatile base with a **STEP**, not a smooth halving — and the volatility matters, because two windows drawn from a series that swings 1,609–6,772 can be made to show almost anything. Corrected in both documents.

⭐ **The cross-collection control I then reached for was ambiguous, and recording it that way was the point.** On the two-window comparison *everything* is down (All Day −55.5 %, Top Shot −40.7 %, Candy −37.5 %), and Candy is genuinely independent — Solana, a separate ingest path sharing nothing with All Day's. That looks like corroboration for "market-wide". But Candy's weekly shape is a **monotonic decay from a late-July launch peak**, not a step, so **it does not corroborate**; it only shows a market-wide decline cannot be excluded. And Top Shot is unusable as a control at all here, because the #68 dedupe moved its baseline for an unrelated reason. **A control that fell for a different reason is not a control.**

### The instrument that actually settled something

`pipeline_runs` retains ~73 h, so it cannot see a change point two weeks back. **`pipeline_runs_daily` is indefinite**, and it carries `rows_found` alongside `rows_written`: for `allday-sales-indexer` the two **fall in lockstep** (~300–460/day through 08-24, then 51–264), with `rows_skipped` small and 0–2 failures a day. ⭐ **That splits the two hypotheses cleanly — the lane is finding less, not dropping what it finds.** ⛔ It does *not* reach the market: `rows_found` is what our query of the upstream returns, so an upstream coverage narrowing looks identical. That question needs a prod-side probe; sandbox egress is blocked.

⚠ Named rather than buried: the indexer's **run count fell ~100–110 → ~78/day around 08-26/27** and stayed there. Stated as a coincidence, not a cause — but it sits at the change point, nothing explains it, and if a per-run cap is being hit it becomes the cause.

### Displacement, done the safe way

The new CLAUDE.md rule (`rows_found` vs `rows_written`) was funded by carrying the #22 GC/rotate status and the #55 Routine detail down into the register. ⭐ **The load-bearing step was checking they were not already there** — they were not, so removing them from CLAUDE.md without adding them first would have destroyed both. **Displacement is only lossless if you verify the destination before you cut the source.** Headroom **39,805 / 40,000 (195 chars)**, against the 40 this pass inherited.

## 2026-09-08 (late evening PT) · Claude Code (cloud) — documentation refresh: M1 is at its bar for the wrong reason, All Day is sliding unwatched, and the beacon caught its first real defect

**Trevor:** "Can you refresh and update Claude.md, the roadmap, known issues, and any other stale documentation?"

A documentation pass, so the discipline was the point: **re-derive every claim the docs make, never re-quote it.** Three of the four things worth recording came out of that rule rather than out of the editing.

### M1 is right and misleading at the same time — the denominator moved, not the pricing

`topshot_fmv_high_med_share_pct` reads **50.7 %** against its 50 % bar, up from the 37.3 % the go-live doc still carried. Before writing that down I asked what moved it, and the answer was the 09-08 retirement of the non-canonical editions, not accuracy. **Measured against the retirement's own backup tables** — `audit_20260908_ts_noncanonical_fmv_snapshots` holds **6,426 priced editions, only 163 of them HIGH/MEDIUM** — so it took 6,426 off the denominator and 163 off the numerator. Rebasing 09-01 onto today's population (19,771 → 13,345 priced, 7,885 → 7,722 HIGH/MED) against today's 14,015 / 7,186: ⭐ **the count of Top Shot editions with a trustworthy price FELL by ~536 in the week the published share rose 11.4 points.**

The retirement was the right call and the denominator is now honest. But a **launch gate reading at its bar because dead rows left the denominator** is exactly the reassuring-direction rot this repo keeps paying for, and no surface said so. It now does — in the M1 row, in CLAUDE.md's go-live line, and in a new roadmap-status headline block. ⚠ Stated as a reading, not a trend: the post-drain sweep is 63 % complete and the +425 editions from the recovered Atlas sales are still propagating.

### All Day is the regression nobody was watching

M2 is **20.2 %** against a ≥ 30 % bar — 27.7 → 25.4 → 24.1 → **20.2** across four readings, and unlike M1 it is a **real numerator loss** (1,574 → 1,252 on an unchanged 6,190 denominator). ⭐ I measured the cause instead of asserting one: All Day **sales volume roughly halved** (~333/day 08-19..24 → ~153/day 09-01..08) while **every All Day lane is green** — `allday-sales-indexer` 156 runs / 156 ok / 348 rows in 48 h, tracking the observed volume. ⛔ That rules out a broken lane. It does **not** establish that the market halved: an upstream coverage change looks identical from inside, and it is the wrong direction for the start of the NFL season. **The next probe is upstream, not another pipeline read.**

⭐ **This is the finding the pass would have missed had it only re-read the number everyone watches.** Top Shot's M1 is the metric with a Routine, a ledger entry and a retraction attached to it; All Day slid five points across three documented readings with nothing naming it.

### The beacon's first real catch — and it cannot size its own finding (#69)

The client-error beacon shipped 09-07 as B2's answer. Its entire history is **19 rows**: 2 synthetic probes and **17 real `unhandledrejection` / `SyntaxError: failed to parse` on `/nba-top-shot/collection`**, 01:17Z → 22:31Z on 09-08, across **six distinct deployment ids** — so it survives redeploys and is not a bad build. Nothing had read the table; I found it re-deriving a go-live row.

⚠ **Then the instrument defect, which matters more than the bug:** every row carries the literal `wallet_address = 'anon'`, with no session or visitor key. **17 events is consistent with one browser retrying seventeen times and with seventeen readers hitting it once** — a ~17× range on the only number that decides urgency. ⭐ **A detector that cannot attribute its events to distinct actors reports a COUNT that reads like an INCIDENCE.** M7's bar ("a detector exists and has caught a synthetic error") was met by an instrument carrying that hole, and it only became visible the first time the detector caught something real. Fix the discriminator first, or the parse-error fix's own falsifier — does the rate fall? — is unreadable for the same reason.

### #67 rotted in four hours, which is the fastest recorded here

Its item (2) told the reader the ~74,297 historical Atlas sales were "paced on jobid **481**" and to "retire 481 when `extra.remaining` reads 0". **481 was unscheduled the same night** after its first working tick caused an IO spell. Live: no matching `cron.job` row, 6,589 `atlas_backfill` rows and no more, `remaining` frozen at 74,297. ⭐ **Both halves were written by sessions that were correct when they wrote — the register carried the first and never the second**, leaving a reader waiting on a job that no longer exists and watching a number that can no longer move.

### Method notes worth keeping

- ⭐ **A share moving is not a numerator moving.** Both M1 and M2 changed this week; only one of them was about pricing. **Reconcile a rate against its own denominator's change before reporting a direction** — the backup tables the retirement wrote made that a one-query check.
- ⭐ **A sweep must be able to return "still open".** #8 (`sync-nba-projections`, 16 runs / 0 ok in 48 h) and #55 (both Routines `enabled: false`, last fire 09-01) were re-read and left open with evidence, not tidied away.
- ⚠ **CLAUDE.md's headroom is real.** The three corrections cost 191 chars against 67 available; they were paid for by **displacing** the ISR and `/sitemap/3.xml` instance details verbatim into `key-files-and-honesty.md`, keeping the rules in place. Final: **39,950 / 40,000**, measured with `node -e` + `.length`.

## 2026-09-08 (evening PT) · Claude Code on Trevor's box — 81,337 Top Shot sales were already in our own database, the gap was rarity-biased, and the drain I scheduled for the historical half had to be pulled

**Trevor:** "Do what you think is best. Exhaust all of your capabilities and leave nothing unresolved that you can handle yourself. Make decisions yourself based upon your best judgement along with what's best for RPC long term and for our user base."

### The decision that mattered: check the free sources before buying reads

#67(2) proposed a per-edition Atlas history walk — ~14 K reads, ~2 days, competing with live lanes already running a 16–23 % Cloudflare 403 rate. ⭐ **Before spending any of it I asked what we already held, and `topshot_atlas_market_events` had 81,337 completed nba listing sales with no `sales` row**, every one already mapped to an `rpc_edition_id`. The firehose had paid for them; nobody had converted them.

🚨 **The gap was RARITY-BIASED, which is the finding — not the row count.** Share of each tier's sales that had been missing: **COMMON 19.1 % → FANDOM 28.5 % → RARE 34.5 % → LEGENDARY 46.3 % → ULTIMATE 55.9 %.** The mechanism is plain: the on-chain indexer resolves editions through the catalogue, and the rarest and newest editions are the least catalogued, so their sales fell through to `unresolved` and were dropped. ⭐ **A collector pricing a Legendary or Ultimate was seeing an FMV computed on barely half its evidence — on exactly the Moments where a pricing error costs the most real money.** It also retroactively raises the value of the parking fix shipped earlier: the sales that indexer was discarding were disproportionately the expensive ones.

**Result: 6,589 rows recovered into the live FMV window; editions at MEDIUM-or-better 4,301 → 4,726 (+425, +9.9 %)**, computed both ways from one query by excluding `source='atlas_backfill'`.

### Three things that would have done damage, all caught before they did

⛔ **The obvious dedup would have undone another session's work.** The live lane's ±10 min guard let **4,644 candidates through that matched rows the #68 dedup had DELETED hours earlier** — Atlas's `purchased_at` aligns with the deleted `topshot_gql` timestamp while the surviving on-chain row sits further away, so a ±10 min probe finds nothing live and the candidate looks free when it *is* the same sale. ⭐ **"No matching row exists" is not "this row belongs" — a row can be absent because someone deliberately removed it.** Key re-derived by measurement on 2,000 of exactly those: `(nft_id, price)` **77 %**, `(nft_id, sold_at::date)` **100 %**. The day key ships, deliberately conservative — **under-counting our own accuracy is safer than inflating it.**

⛔ **A units error would have looked identical to the rarity skew.** `atlas_backfill` median 2.76 vs on-chain 0.30 could have been `price_cents` holding dollars. Verified: median backfill/other ratio **1.429** (not ~100), 3,777 of 3,929 within 5x, and the tail checks row-by-row (**$25,000 ← 2,500,000 cents, ULTIMATE, serial #1**).

⛔ **My `REVOKE … FROM PUBLIC` orphaned the cron job that calls the function.** First tick: `permission denied`. `SECURITY DEFINER` governs what a function RUNS AS, not who may CALL it. 🚨 And it presented as **silence** where anyone would look — the error fires before `log_pipeline_run`, so `cron.job_run_details` showed `failed` while **`pipeline_runs` showed nothing**. Rule now in `database.md`: pair the REVOKE with a GRANT to the job's role in the same migration, and for any "scheduled job wrote no row" the discriminator is `cron.job_run_details`, never the pipeline table.

### And one I did NOT catch until it was live

🚨🚨 **I scheduled the historical drain, it caused an IO spell, and I pulled it.** jobid 481's first working tick ran **246 s in `IO / DataFileRead`** and took the instance **1 active / 0 IO-waiters → 10 / 8**. Cancelled and unscheduled at ~00:52Z; back to **1 / 1 / 0** within three minutes.

⭐⭐ **Root cause is a rule this repo already has, and my own verification is what hid it: a `LIMIT` bounds a query's OUTPUT, not its COST.** The candidate CTE evaluates `NOT EXISTS` against `sales` for all ~209 K events *before* `LIMIT 1500` applies. My manual runs measured **38 s** — but only because I passed `p_since = now() - 30 days`, which bounded the scan. **The scheduled call passes no `p_since`. The one argument that made my verification cheap was absent from the thing I scheduled.** ⚠ **A verification that does not use the SAME ARGUMENTS as the scheduled caller is not a verification of it.** (Its `SET statement_timeout` did not save it either — function-level `SET` is inert on pg_cron, so it ran under `cron_heavy`'s 600 s.)

⛔ **Nothing was lost and nothing rolled back.** The FMV-window half is banked; only the ~74,297 historical rows are deferred, and those affect edition-page completeness rather than FMV — **not worth an IO spell.** The correct bounded design (a date-slice cursor, or a `p_until`) is specified in the unschedule migration's header so the next pass is a pickup, not a re-investigation. ⭐ Draining the FMV window *first and separately* is what made this recoverable rather than a rollback.

### Also this stretch

Declined to loosen the `pack_table_rows` slow-arm after refuting its candidate by measurement (the whole sweep was slow at that tick: 44-board average **662 ms vs 232–283**; an isolated re-probe of the exact query returned **166 ms**). **The arm did its job; tuning a safety threshold from one benign event is how instruments erode.** Documented the one-query discriminator in `trust-board-and-safety.md` instead, with the principled fixes named in order should it ever become genuinely noisy.

**Closing state:** job gone, 6,589 rows kept, instance 3 active / 1 IO-wait, security invariants `[]`, secdef drift 0, the only stalled pipeline the pre-existing `topshot-catalog-backfill` info item. Suite green (1,491 files / 16,528 tests), tree clean.

## 2026-09-08 (07:0x–12:0x PT) · Claude Code on Trevor's box — a register staleness sweep, the two indexes a monitor never named, and a wrap-up that found a live CLAUDE.md fact had quietly stopped being true

**Trevor:** "Keep going" ×3, then "Wrap up and handle anything unresolved, then commit what you need to memory and refresh relevant documentation."

### The register was rotting in the REASSURING direction

Went looking for a permanently-red instrument to fix and found there was nothing left to fix — **four times**. ⭐ The durable finding is the *shape*: a stale CLOSED entry gets caught the moment someone needs the thing, but **a stale OPEN entry looks like diligence**, so nobody re-checks it and every reader inherits the wrong priority. Three of the four had been fixed by *other* sessions whose work never came back to the register.

| item | claimed | live read | disposition |
|---|---|---|---|
| **#38** | fails 99.6 % of ticks | **zero runs** in the whole retention window; jobid 16 `active = false` for 10 days | closed |
| **#31** | authoritative arm "never once produced a result" | reads **38 of 38** bundles, 0 failed, since 08-30 | closed |
| **#23** | 25 edge fns running non-`main` code | **6**, and all six deliberate (2 deferred-by-decision, 4 do-not-redeploy) | closed |
| **#21** | codify the hydrator's cron | worker logs **nothing**; pg_cron does the work (339/339, 99/99 ok) | **updated, not closed** |
| **#11** | named sub-item is a ghost | ghost confirmed — but it carries a REAL remainder | ⛔ **checked and LEFT OPEN** |

**#21 was deliberately not closed:** Cloudflare is unreadable from a session, so *retired* and *running-but-dying-before-its-first-log-write* are indistinguishable — the same ambiguity that item's own "24–49 of ~144 ticks log" finding described, now at 0 of ~144. Both states need the same operator action, so the action could move without resolving it. ⛔ **#11 is the half of a sweep that usually goes unrecorded** — an old item is not a stale item, and **a sweep that cannot return "still open" is a closing spree.** Both facts are now in the register's stamp and as a standing rule near the top of the Open section.

### The five-lane finding that fell out of closing #38

The 2026-08-29 dead-host mitigation was applied to **one** job and never swept across its siblings. **Five lanes still fire into `public-api.nbatopshot.com`** — `ingest`, `topshot-subedition-circulation-backfill`, `ingest-topshot-challenges`, `topshot-pack-supply-backfill`, `topshot-misattrib-drain` — at **0 ok and 0 rows written each** over the FULL retention window, `last_success` NULL throughout. ⛔ **Paused nothing:** `pack_distributions` is provably fresh (1,995 of 2,099 in 24 h), so nothing is going stale, but **the path keeping it fresh could not be identified** (ruled out: pg_cron, the DB writers' in-DB callers, this box's Task Scheduler) — and pausing something whose live path you have not mapped is how a retry arm disappears. Filed with per-lane disposition. ⭐ **Transferable: a mitigation applied to ONE job is a claim about every sibling sharing its upstream — sweep by UPSTREAM** (`error ILIKE '%530%'` over full retention, one query).

⚠⚠ **A method trap that nearly became a false finding of mine:** grouping `net._http_response` by `status_code` and sampling with **`max(content)`** returns the **lexicographically largest** body. It surfaced `product is required` (**2 of 1,635**) and buried the real **1,633** — benign Flow REST `Error Code: 1101` moved-Moment responses, already attributed. I burned several queries chasing a phantom Atlas bug. **Group by `left(content, N)` and count.**

### #56 — the two indexes the monitor never named

Its two headline claims were stale (four reindex jobs exist; the verify ran green 09-06) but **its rate was right** — and I nearly filed it as falsified. `idx_wmc_cohort_cover` read 326.4 MB on 09-02 and 269.9 MB today, which reads as *"the bloat stopped"*; **a REINDEX ran in between.** Split on that change point: **148.4 → 269.9 MB ≈ 60 MB/day**, matching the item. ⭐ **A rate measured across its own fix is not a rate**, and it lied in the reassuring direction.

🚨 **The real gap was one neither claim named:** the verify hardcoded a **four-name** `v_targets` array and was structurally silent about every other index on the table — and **the two it never named are the two that grew largest**: `idx_wmc_lock_wallet_coll_cover` **433.5 MB / 44.48 %** and `idx_wmc_wallet_coll_ek_fmv` **401.6 MB / 37.18 %**, both under the verify's *own* 60 % threshold, against 83.7–90.5 % on the four covered. ≈**430 MB** recoverable; the second is the table's second-hottest index (18.4 M scans) on an IO-bound instance. ⭐ **The unnamed objects grow worst BECAUSE they are unnamed** — everything in the list is maintained on a schedule, everything outside it accumulates without limit, so a curated list actively selects for its blind spot being the worst. Checked first that all 19 wmc indexes are USED, so there was no drop to make instead.

**Shipped** jobs 477/478 on the existing one-statement `REINDEX INDEX CONCURRENTLY` / `cron_heavy` pattern, `v_targets` extended to six, verify moved 03:23 → 04:03 (load-bearing: it would otherwise collide *and* measure the new targets before their rebuild, reporting a false red weekly). **#56 marked 🟠 partial, not closed** — the new targets first run **2026-09-13** and the change was verified structurally only, because running the verify now costs ~1.9 GB of reads, the exact cost that item declined to re-spend. **Watch: 09-13 ~04:03Z, six entries in `extra.indexes`, `invalid_left` empty.**

### Two self-inflicted problems, both caught

🔁 **I created repo/prod drift and it would have survived every gate.** `apply_migration` takes SQL inline, so the applied text is hand-assembled while the file is written separately; I trimmed an in-body comment out of the MCP call (`5c45be6d` deployed vs `540afbb7` committed). Nothing misbehaved — **the committed file simply stopped describing production**, which is a migration's only job. ⛔ `migration-parity` matches on NAME and this function is not in `PINS`, so **green parity + green drift guard is compatible with a file that does not match prod.** Now a rule in `tooling-gotchas.md`: md5 `pg_proc.prosrc` against the committed body after every `apply_migration`.

⚠⚠ **And my md5 extractor reported a FALSE drift** because the new file names the dollar-quote tag *in its own prose*, so splitting on it returned the wrong slice (1390 vs the real 3034). Anchor on the LAST marker pair. ⭐ **A verifier that can be confused by the text it verifies is not a verifier.**

### Wrap-up: the documentation refresh found live drift

Committed the weekly data-quality sweep left untracked by a NO-PUSH session (read in full first; its offer-reconciliation item independently reaches the same conclusion as my 0530Z filing — the offer-sanity set is ~100 % sub-serial, which is why it cannot drain). Its one documentation ask was **regenerate `schema-truth.md`, 17 days stale** — and doing so found **real drift in the row that file exists to protect**: `public.collections` is no longer 5 active / 2 inactive but **6 / 1**, because `candy_mlb` flipped to `is_active = true` when Candy went live (#63). **CLAUDE.md still read "both are the `is_active=false` rows".** Corrected — and the replacement is **27 chars shorter**, so it reclaimed headroom (39,960 → 39,933 of 40,000). Other counts re-read: public tables 376 → **423**, views 136 → **137**, functions 668 → **731**, active cron jobs 99 → **133**; invariants all hold (0 RLS-off, 0 rows from both security checks), all four enums byte-identical. ⚠ Stamped as **NOT a full regeneration** — column inventories and RLS detail are still 08-22 vintage, and the file's *"this file wins"* precedence is only as strong as **that** stamp. ⭐ **The drift found today is the argument for a real regeneration rather than a sixth spot-check: a spot-check only re-reads the facts someone already thought to list.**

**Left for Trevor, stated rather than dropped:** the five dead-upstream lanes (operator/cron-job.org, whose console is a secret-leak hazard) · #56's 09-13 verify row · `MEMORY.md` still ~800 bytes over with no trimmable slack, so getting under it means retiring memories, which is a judgement call about which facts matter.

## 2026-09-07 (late PT) → 09-08 (07:0x) · Claude Code (cloud) — the final free Dune cycle: 84,868 sellers recovered, and five of my own load-bearing claims died to measurement

**Trevor:** "What would be good use of our final monthly dune credits before they close off their free query accounts?" → "start consuming credits responsibly to move towards our goals and positively impact key metrics" → "do what you think is best for RPC long term and for our users"

**Outcome.** 84,868 `nba_top_shot` seller addresses recovered below the 2023-11-08 spork wall, where nothing free reaches. Below-wall gap 909,970 → 825,102; 2023-08 filled 19,565 → 98,639. 1,034,812 datapoints of a 1,000,000 cap, 30 executions, ~300 est. credits of 2,500. Audit clean: 0 malformed addresses, 0 self-trades, 8,976 distinct sellers, `sales_counterparty_recovered` matching exactly. The lane stopped by its own gate — five consecutive ticks `ok: true` / `budget_stopped` / ~400 ms.

⭐ **The session's real output was the measurement, not the rows. My opening recommendation would have bought ZERO.** I proposed spending the cycle on 210,801 pre-2021 counterparties as an "irreplaceable archive". Dune returns **nothing** for 2020 — ten clean windows over 2020-11-27..2020-12-17, every one `rows_returned: 0`. Four more claims died the same way: 699,169 of the rows I called Dune-only are served **free** by Atlas (floor bracketed at ~2021 across 9 probed Moments, 7 carrying 2020 sales, zero 2020 records); credits were never the binding meter (~0.34/window actual vs the 10/execution our state table estimates); #67's ten-day sales hole was **self-healing** at a measured ~150–190 rows/hour; and the UFC "prize" I reserved 450k datapoints for is a market that **closed 2026-05-13**.

⚠ **The one repeated error, and it is the transferable lesson: VALUE ASSERTED FROM VOLUME.** Twice in one night I sized a prize by row count without reading who consumes it — 778,032 UFC/Golazos rows (market closed), then the backfill itself. The consumer check afterwards: **of 109 `saved_wallets`, exactly ONE gained sell-side history — 10 rows, $120.** With ~1 WAU the near-term user impact is ~nil; the value is archival and latent. **A row count is not a benefit. Measure the consumer overlap FIRST, then size the spend.** The rule worked when finally applied: on the Golazos question I ran the consumer check first (1 of 2,586 rows touches a saved wallet) and it reached the right verdict before any feasibility work.

⚠ **Four execution-level corrections, each recorded.** A single `QUERY_STATE_FAILED` read as an era/schema fault — it was a transient, and the same era ran clean an hour later (**one failed execution is not a diagnosis**). A stranded 100k datapoints blamed on the wrong lane's cap before reading `dune_budget_status` — the real term was `day_datapoint_cap`, and **there are TWO day caps** (`day_row_cap` AND `day_datapoint_cap`); raising only the first leaves the second silently binding. A sunset job scheduled 09-11 that was **both redundant and harmful** — redundant because the spent cycle cap already stops the lane in ~400 ms without reaching Dune, harmful because it would have killed the 14-day Plus trial window; moved to 09-23, ahead of the real risk at the 09-24 cycle reset. And `ownership-sync-dune` described as "fires weekly, next Monday 11:40Z" when it has **no caller at all** (absent from `vercel.json` and `.github/`, last run 08-24) — a live cadence inferred from four historical runs.

⭐ **The durable artifact is the counterparty source-floor map, now in `docs/reference/apis-and-cadence.md`.** Three of the four sources hide their floor behind a SUCCESS: Flow REST answers **200 with an empty body** below the spork wall, Dune returns **`rows_returned: 0`** below ~2021, Atlas simply holds nothing pre-2021 (and hard-rejects `product: 'ufc'`/`'golazos'` with 400 — not the silent-firehose fallback an unknown *key* produces). Flowty carries no counterparties at all. **Consequence: 2020 Top Shot counterparties — 79,160 sales — sit below every floor and are permanently unrecoverable.** Nobody should fund that chase again, and no surface should imply provenance for that era.

⭐ **Also resolved, with a controlled probe: the `*_studio_history_v1` hash question — and the answer DIFFERS BY COLLECTION.** Randomised sample, all **above** the wall so a pruned empty-200 could not be misread as "not a purchase", with a positive-control arm of already-filled rows. All Day filled **3/3** real purchases, All Day null **2/3**, UFC null **0/3**, Golazos null **0/3**. The reusable discriminator: a **purchase** carries `ListingCompleted` + an NFT `.Withdraw` at **62–81 events**; a **listing creation** carries only `ListingAvailable` at **4–6**. Both answer `Success`/`Sealed` on a 200 — the event set is the only signal. ⇒ UFC/Golazos studio rows point at LISTING transactions and are structurally undecodable; All Day's are mixed and owned by a dedicated lane (`allday-buyer-backfill`, 16/16 ok, 1,920 rows/48 h). **This RETRACTED my own filing that Golazos's absence from `claim_sales_counterparty_batch` was "a real gap" — it is deliberate and correct.**

⚠ **Two guards read `docs/sessions/**` (`check-retired-rules.mjs`, `check-memory-doc-links.mjs`) and BOTH exclude it as frozen history — which is also why session entries carry no markdown links.** Grepping for the guards that read a file before editing it is what surfaced that.

**Left running:** nothing. pg_cron **jobid 475 `rpc-dune-free-tier-sunset`** (`0 12 23 9 *`) pauses the budget ahead of the 09-24 cycle reset. `ownership-sync-dune` sits at `cap_datapoints = 0` with its reservation freed (it has no caller either way). Reverts for every DB change are in the 09-07/09-08 ledger entries.

## 2026-09-07 (late PT, 19:0x–20:2x) · Claude Code on Trevor's box — the indexer was dropping ~740 sales/day, and fixing it opened two problems I had to go find

**Trevor:** "Keep going"

**Took #67 item (1), the one open item blocked on nobody.** `app/api/sales-indexer/route.ts` hit `unresolvedIds.push(nftId); continue` when it could not resolve an edition — the sale was never written to `sales`, never parked in `unmapped_sales`, and Step 7 advanced the cursor past its block anyway. **Nothing revisits a block below the cursor.** The route's own comment had described this for weeks. ⭐ **Re-derived live rather than trusted: six consecutive ticks dropped 41 sales in ~80 minutes (~740/day), every tick `ok: true`.** Every FMV reads `sales`.

**Shipped:** Step 6b parks unresolvable events **before** the cursor advances, with a per-tick dedup guard (`unmapped_sales` has no natural-key constraint and 1,985 duplicate groups already exist there from other collections, so the guard lives in the route). Serial parks as **NULL, not 0** — the edition lookup is what failed, so the serial is genuinely unknown. Drain is pg_cron jobid 474, **not** an inline call: this route is `maxDuration = 120` inside an `after()` body where a kill cannot be caught, against a promoter measured at p95 196 s.

⭐ **Not speculative, which is what made it a safe pickup:** `unmapped_sales` already held **24,583 nba_top_shot rows, all 24,583 resolved**, and the promoter is collection-agnostic (resolves via `nft_edition_map` / `resolution_hint` / `wallet_moments_cache`, all continuously hydrated by jobids 468/469).

✅ **VERIFIED IN PRODUCTION, split on the change point.** Three pre-fix ticks carry NULL for the new fields and dropped **112 / 8 / 3**; the first post-deploy tick reports **25 unresolved → 25 parked, `unresolved_unparked` 0** — and I checked the **outcome table**, not the self-report: 25 rows with serial NULL, price > 0, tx hash and hint present, covering exactly the window the prior tick would have discarded.

⚠ **THE HONEST RATIO, which is not the headline one: only 4 of the 25 are resolvable today.** The other 21 wait on the hydration queue. **The claim this earns is "no longer LOST, and recoverable" — not "recovered".** Stated in the ledger that way on purpose.

🚨 **TWO THINGS WENT WRONG, AND BOTH WERE FOUND BY LOOKING RATHER THAN BY BEING TOLD.**

**1. My change fired a destructive branch retroactively.** jobid 474's *first* tick reported **`archived: 24583`**. `promote_unmapped_sales` carries a 7-day retention `DELETE` alongside the promote, scoped to its collection argument — and **nothing had ever called it for Top Shot**, so eight months of resolved rows were reaped the instant a drain existed. ✅ Safe, and checked rather than assumed: every deleted row was already promoted, and `sales` holds **73,660** rows across exactly that window. ⭐ **Rule now in `database.md`: a collection-scoped shared function is not dormant for an unused scope — it is a LOADED ARM whose predicate has simply never been evaluated there, and the blast radius is that scope's entire history.**

**2. My change opened a duplicate-write race, and another session caught it within minutes.** Parked rows carry a `transaction_hash`; the Atlas lane writes the same sale with a **NULL** one; the promoter's only guard is `ON CONFLICT DO NOTHING` on a **PARTIAL** index (`WHERE transaction_hash IS NOT NULL`), so the two **can never collide** and a second row would have landed. Duplicated sales are fabricated FMV volume — *the same class as the defect I was fixing*. I verified dedup WITHIN `unmapped_sales` and never asked who else writes that row. ✅ Zero escaped (control run: the single ±10 min twin found is a **real** flip — same nft, 94 s apart, $0.30 then $1.00). ⭐ **Rule now in `database.md`: when a change creates a NEW writer of an existing row, enumerate every OTHER writer and read each dedup index's `WHERE` — a partial unique index guarantees uniqueness only across rows satisfying its predicate.** Recorded in **my** ledger entry, not left in theirs.

⭐ **Then closed the instrument I had just created.** `unresolved_unparked` exists only to alarm at `> 0`, so I checked what reads it: **nothing** — no `v_rpc_trust_health` arm among its 37, no `get_pipeline_alerts_core` rule, no detector keys on a `pipeline_runs.extra` field. That is the *instrument nobody keys on* trap, which my own code comment had named two paragraphs earlier. **Rather than build a watcher, the route now THROWS** — the outer catch writes `ok:false` with `cursorAfter: null`, the cursor holds, the next tick re-reads the range. ⚠ The direction is a cost argument and the stall vector was **checked**: no NOT NULL column on `unmapped_sales` can fail permanently (`toIsoTimestamp` falls back to `new Date()`), so a park failure is transient infrastructure — exactly what a cursor hold repairs. **Negative control run: with the throw disabled EXACTLY ONE test reds, while the `CONTROL: a successful park still advances the cursor` case stays green**, so the guard is not the degenerate "never advance".

⭐ **Sibling sweep, so the class is bounded rather than assumed fixed** (CLAUDE.md: grep the EXPRESSION, not the file): of the **six** sales indexers, **Top Shot was the only one that dropped.** All Day / Golazos / UFC park in `unmapped_sales`; Candy parks via its own `park()`/`closeParked()`; Pinnacle writes the sale with `edition_id = null` for later backfill. Three different correct answers — and this route had a fourth that was not one.

**Context for whoever takes #67 (2):** Top Shot sales/day ran **4,426** on 08-24, troughed at **954** on 08-30, and is back to **2,220** on 09-07 — the Atlas lane alone went 35 → 881/day. Still ~1,700/day short of baseline; ~740 of that is what was being dropped (now recoverable), and the rest is the 08-28 → 09-06 history gap, which needs Trevor's Atlas-budget call.

**Suite 16,521 green; CI green; tree clean.**

## 2026-09-07 (evening PT, 17:2x–18:0x) · Claude Code on Trevor's box — a fabricated zero that vetoed the flag it sat beside, and a clean tree

**Trevor:** "Look through the roadmap, to do list, known issues, pipelines, etc and find things to work on" · then, mid-turn: "also, make sure you work through everything in the tree, and that we have a clean tree when you're done"

**Swept the queues first and picked the one item that was blocked on nobody.** `/api/best-offers` is half-refuted by its own author and its surviving half is a product call; the ~19 anon-public soft-404s need Trevor; the `offsan` candidate filed at 00:10Z is explicitly a *trend* question with no baseline. The pickup was inbox `2026-09-07T2135Z` — `refresh_atlas_pack_ev()`'s fabricated `total_unopened = 0` — which had deferred itself for coordination with #65's live lane. **That lane shipped overnight, no `.lock`, no `FREEZE.md`, so the deferral had lapsed.**

⭐ **Re-derived before touching it, and the filing was stale by three hours in the direction that matters: 333 → 573 rows.** Still zero of them positive-EV, against 12 positive among the 100 rows other writers leave NULL.

**The defect is CLAUDE.md's top class one table apart.** The success branch hardcoded `total_unopened = 0, depletion_pct = NULL`; `pack_ev_latest` overrides `is_positive_ev` when `total_unopened IS NOT NULL AND <= 0`. The view's rule is right — a sold-out pack cannot be +EV — and it was being fed **a fabricated `0` meaning "I did not compute this"**. The eight documented honesty properties did not cover it: property 4's `0/100` is the *failure* branch and is deliberate. Real supply was already in the joined `pd` row (`total_sealed > 0` on 57/57), so the `0` was **wrong on every row**, not merely unknown.

**Shipped `20260908003056`** — cursor selects `pd.total_sealed, pd.depletion_pct`, success INSERT writes them, NULL passing through as *unknown* rather than being coalesced. Two safety claims made **structurally rather than off today's data**: the `SELECT DISTINCT` cannot multiply rows because `pack_distributions_dist_collection_unique` is a UNIQUE index on (dist_id, collection_id), and the types match with no cast.

⭐ **Verified by SPLITTING ON THE CHANGE POINT, not by re-reading a total.** Same 57-dist sweep: the 23:25Z and 00:25Z ticks each wrote **42 fabricated `(0, NULL)` pairs**; the post-fix run wrote **0**, with 42 rows carrying real supply. Repo body and live `prosrc` byte-identical (md5 both sides). ACLs by `has_function_privilege`, not acl text; secdef drift array **length 0**.

🚨 **AND THE PIPELINE'S SELF-REPORT NEVER MOVED.** `pipeline_runs` reads `ok=true, rows_written=57, extra.rows=57` on **every** tick — the broken ones and the fixed one are byte-identical in telemetry. A textbook [[green-pipeline-blind-to-its-own-work]]: the only instrument that could see this was a query against the *outcome* columns.

⚠ **Chased an apparent survival rather than reporting around it, and it was the view.** `pack_ev_latest` still shows 15 atlas rows at `total_unopened = 0` with a NULL `depletion_pct` — which reads exactly like the defect persisting. It is not: the view NULLs `gross_ev`/`depletion_pct`/`fmv_coverage_pct` on the sentinel shape (`gross_ev = 0 AND edition_count = 0`) while passing `total_unopened` through RAW. **The defect signature was an artifact of reading a masked column beside an unmasked one.**

⭐⭐ **THE REAL FINDING IS THE PIN, WHICH WAS GREEN FOR THREE WEEKS.** A 455-line honesty pin asserted `total_unopened` **nowhere** and `depletion_pct` only on the failure branch — structurally silent about the two columns that can **veto its own subject**. Repointed with a column-level fixture audit and four assertions in *opposite* directions: real supply published · a genuinely sold-out pack still writing a **measured** `0/100` · unknown withheld as NULL · the fabricated pair banned at population zero. Two new fixtures (`D-SOLDOUT`, `D-NOSUPPLY`) make the second and third non-vacuous, and `D-FAIL` now carries real supply so the sentinel is proved to *ignore* it.

⛔⛔ **The verification harness lied in BOTH directions first, and that is the session's transferable lesson.** No Postgres on this box, so the pin was replayed against prod in a `pg_temp` harness. Calling the sweep inside the **same statement** as the assertions meant every sibling sub-select read the pre-call snapshot: **15 FAIL / 4 PASS, every cell meaningless** — the failures because the reads were empty, and the "passes" because they were `count(*) = 0` checks *passing on invisibility*. **A mixed scoreboard is more convincing than a uniform one, which is exactly why it is dangerous.** Sweeping in its own statement: **19/19, including a positive control that rows were written** — the one cell that can separate "clean" from "cannot see". Then the negative control on the OLD body: **all four new assertions FAIL, both must-not-move controls PASS.** Recorded in [[probe-null-results-need-a-positive-control]] as its fourth shape.

⚠ **A guard I had not grepped for reddened the full suite** (1 failed / 16,513 passed): `migration-new-function-states-its-anon-exec-decision`. Its own message prescribes the fix — for a `CREATE OR REPLACE`, which does **not** reset a function ACL, use the marker, because a REVOKE there would *change* production rather than describe it. CLAUDE.md's "grep for the guards that READ a file before you EDIT it", paid again. Final: **1490 files / 16,514 tests green**, gated on the exit line. CI's **DB invariants (SQL)** job — the pin against a real Postgres — passed.

**Clean tree, on evidence rather than tidying.** Three parked stashes resolved and **dropped**, every patch preserved: one had **landed** (its ledger entry greps 1 live / 0 archived), two were superseded and would have **regressed** main (removing the 09-04 arweave CSP fix; dropping an import used at two call sites). The one comment worth salvaging is already pinned by a *test* asserting both directions. Disposition + evidence in `docs/archive/stash-rescue-2026-09-07-disposition.md`, with a forward pointer on the frozen 08-23 README whose "still in `git stash list`" line is now false. ⚠ Method: **diff a stash against its OWN base** — `git diff HEAD stash@{0}` gave a 42 KB whole-tree diff with the sign inverted. Also committed the cloud monitor's `2026-09-08T0010Z` filing on its behalf (NO-PUSH) with INDEX counts re-derived at apply time, and removed a stray untracked `.beacon-probe.mjs` from the repo root (preserved to scratch; telemetry has real test coverage and nothing referenced it).

**Left for Trevor, stated rather than quietly dropped:** `MEMORY.md` is still ~800 bytes over its limit with **zero** index lines above 200 chars, so the mechanical trim is exhausted — getting under it means **retiring or consolidating memories, which is Trevor's call**. Both lessons from this session were therefore folded into *existing* memory files rather than adding index entries.

## 2026-09-07 (late PT) · Claude Code (cloud) — the deferred item had no caller, and the reason I had written down was the weak one

**Trevor:** "Wrap up and complete anything still unresolved."

Went to close the one item the fabricated-divisor pass left open — the `pack-supply-parse` `|| 1`, deferred because *"the fix needs both hand-copies plus an edge deploy, and bundling that into a display fix destroys the attribution."* ⚠ **That reason dissolves the moment the deploy is its own commit**, which is exactly what I was about to make it. It would have licensed the deploy on the next read.

**Named the caller first, and the answer is nobody.** The divisor lives in `backfillPool`, reachable only via `mode=pool`, whose sole caller is pg_cron **jobid 16 — `active = false`**. The one active lane (jobid 15, `mode=supply`) has returned **HTTP 530 every day since ≥ 09-05** on the dead `public-api.nbatopshot.com` host already registered as **#50/#65**. Zero rows were ever fabricated (no degenerate distribution exists), and deployed-vs-repo parity is **unverifiable** — the edge-fn content census has never produced a result (**#31**). 🚨 **And this is the function whose half-done deploy caused the documented ~40 h silent outage** (2026-08-12; a 403'd edge fn writes no `pipeline_runs` row, so nothing noticed). **Nothing deployed.** Both markers and the known-issues row now carry the measured reason, with the exit re-pointed to "as part of the change that revives the pool lane (#65)".

⭐ **Lesson, sharpening a rule this repo already had:** CLAUDE.md says a filed decision NOT to act is a hypothesis nobody re-checks and *"the tell is a cost stated with no number in it"* — my own deferral, written six hours earlier, was exactly that shape. **A WEAK reason crowds out the strong one and becomes PERMISSION once it dissolves.** Promoted to the measurement bullet; room reclaimed from three redundant `database.md` pointers rather than spent (39,943 → 39,960, 40 headroom). ⚠ **The limit guard rejected two over-budget drafts before either reached disk** (40,039, then 40,011) — the pre-flight `if (s.length > 40000) exit(1)` in the edit script, not the CI test, is what kept an over-limit file from ever existing.

## 2026-09-07 (evening PT, 16:51) · Claude Code on Trevor's box — session wrap: the heartbeat closed on production evidence, and one rule promoted into a full CLAUDE.md

**Trevor:** "Wrap up and complete anything still unresolved, then update your CLAUDE.md and any other memories and documentation."

**Closed the one open verification, on the production caller.** Since `20260907155956` landed: **16 pg_cron ticks / 16 `pipeline_runs` rows (1:1), 6 of them naturally `no_op: true`, max gap 30 min** against a pre-fix 180; `detect_stalled_pipelines()` `[]`. ⚠ While writing that evidence I read `count(*) FROM detect_stalled_pipelines()` as **1** and nearly reported a stall — it is a **jsonb-array** function where one row holding `[]` IS clean. The mixed-return-shape rule, hit inside the closing check for a fix about instrument honesty.

**CLAUDE.md at 39,958/40,000 (42 chars).** Read candidates instead of ranking by size, per the guard's own warning that size is anti-correlated with displaceability. `### Enums` turned out to be **pure duplication** — every fact already verbatim in `database.md` (:71 values + the `.ilike` rule, :73 the 3-letter `MED`, :758 `tier_type`) and `chain-strategy.md` (:17 `chain_type`, and why `chain` lives on `collections` only). Condensed to a pointer keeping the `.ilike` trap inline; **nothing needed moving because it was already there.** Added the new rule — *a `*_at` name is not its contract; read `col_description()` before treating a timestamp as freshness.* Net **+18 chars**, guard green.

**Docs:** `database.md` gains the full `last_checked_at` retraction including ⚠ the false corroboration (the oldest stamp sat one day from the Top Shot host death I already had a story about — it is the migration's own apply time). `tooling-gotchas.md` gains the `innerText`-is-CSS-transformed trap and the positive/negative control rule, which matters here because the E2E DOM smoke badge is the entire client-side detection surface.

**Memory:** new `read-the-column-comment-before-calling-a-timestamp-freshness`; `pack-availability-flags-are-snapshot-columns` corrected in place (it is right about the EV-snapshot columns; I misapplied it to a table re-verified every 5 min). Index all under ~200 chars, **net −55 bytes despite a new entry**. ⚠ Still ~1.4 KB over its limit — **17.2 KB of 25.8 KB is mandatory link/title across 193 entries**, so hooks are not the cause and a 45-char cap only reaches 24.8 KB. Retiring/consolidating memories is Trevor's call; left undone deliberately.
## 2026-09-06 → 09-07 (evening → late PT) · Cowork (cloud + device VM) — the audit drain: the sniper's serial feed, the pack-pull queue hydrated on-chain from the database, and the sales ledger's ten-day hole

**Trevor:** "Keep doing all you can" → "Keep going" ×5 → "make sure you address [the pg_net_http_400 alert]" → "Wrap up… re-audit everything like you did at the beginning… update your Claude.md and any other memories." Every ship on `main`, CI-green per code tip; ledger entries for all of it (twenty-nine this session), register #67 opened, #21/#38 annotated; Project handoff `claude/handoff-2026-09-06-audit-drain-sniper-serial-feed-live-asks-and-seo-close.md` (30 items).

**Shipped (the shape, not the list — the ledger has the list):** the Top Shot sniper's serial feed, `cached_listings` and `edition_offers.low_ask` rebuilt every 2 min from the Atlas firehose with `{nftId}` verify probes; an edition verify lane; `offers-sweep` Inactive; SEO parts two and three (the five client-shell tabs carry the catalog fan-out); the wallet-backfill orchestrator's 120 s of paid sleep per wallet cut to 40 s (12Z wave verdict: stands); **the dead-host pack-pull hydrator replaced on pg_cron from four sources** — the wallet cache, the Atlas events, our own sales ledger, and for the rest a Cadence `borrowMoment` script per pull POSTed from pg_net to the public Flow REST API (jobid 469; 212K → 155K in the queue by the close); the sentinel's 4xx arm attributes that lane; **and, found by the re-audit's failure sweep, the Top Shot sales ledger had been missing ~70 % of listing sales since the GraphQL host died** (the on-chain indexer drops what it cannot resolve; `sales` fell from ~4,200/day to ~1,000–1,600) — `sync_sales_from_atlas()` (jobid 471) writes them from the Atlas events now, deduped against every other writer.

### The lesson worth the file: three compounding queue walks in one day, all mine, all caught by `duration_ms`

The Atlas edition dispatcher's per-candidate seq scan (morning), the chain hydrator's newest-first re-scan of everything it had itself named (69K buffers after six hours), and the wmc hydrator's "N unresolved rows" page (would have walked the whole 640K-row index every tick at convergence). Same shape each time: a walk that starts at the top of a queue whose top is what the walk resolves. Fixed the same way each time — a bounded slice of the INDEX behind a cursor, filters applied to the page — and then the second-order trap: a temp-table page has no statistics, so a set-form query probes every exclusion for every row before its LIMIT (5.5 s → 1.0 s walked row by row). Promoted to CLAUDE.md by displacing the timeouts bullet into `database.md`; the cases are in `cron-and-schedulers.md`.

### Also learned, and recorded where the next session will read it

- A new pg_net lane must be wired into `check_edge_fn_http_failures()` by request_id in the migration that creates it — mine paged as an unknown edge-function 400 two hours later. Session probes get `__probe__` rows the moment they are sent.
- Atlas addresses carry no `0x` (40,438 of 40,438) — `flow_addr_0x()` normalises; 1,773 owner rows my Atlas arm wrote unprefixed were repaired.
- The Atlas tick's ~8 MB of WAL per tick is full-page images of ~1,400 index pages — 0.5 % of the IO budget, not a saturation risk; two never-read `cached_listings` indexes dropped. Two `pg_stat_statements` snapshots joined on `queryid` alone double-count.
- The seed-wallet-refresh wave (:59 of 13Z/19Z/23Z) is the cause of the Atlas tick's 50–110 s outliers — every step slows together; nothing fails. Known band (#48/#61), recorded rather than chased.
- The cloud shell's clock runs behind the DB; `select now()` only, and read the tick you waited for by its own row.

**Open for the next session:** #67's route half (park unresolved Top Shot sales in `unmapped_sales`; the 08-28 → 09-06 gap for editions without an open listing; retire the `/api/ingest` GHA step); the moments hydrator worker's console entry when the queue reads ~0 (#21); the still-Trevor items (gate-key rotation, `e4tib3`, Sentry, WNBA Gold paragraphs, #50's pool source, the scorecard regex).

## 2026-09-07 (afternoon PT) · Claude Code (cloud) — asking a green guard what it cannot see, and finding three live sites inside its own roots

**Trevor:** "Look for things to work on… Fix anything you encounter." → "Keep going" ×5 → "We should just get rid of the sniper section for ufc since there is no market currently." → "Do what you think is best."

Earlier stretches of this session shipped the UFC sniper retirement (registry + a **307** in `proxy.ts` — not 308, because Trevor said "currently" and a 308 is cached hard), the closed-market copy layer (`lib/sniper/header-copy.ts`, `lib/collection/closed-market-chrome.ts`, `CLOSED_MARKET_PAGE_META` in `lib/seo.ts`), and the verification of another session's `wmc-metadata-reconcile` heartbeat fix against the pre-fix baseline only this session held (**12/26 → 11/11** ticks logged, max gap **180 → 30 min**). ⚠ That session's own forced-no-op-with-rollback proof is **stronger** evidence than my observational window — `P(observation | fix did nothing) = 0` vs merely improbable — and I said so rather than letting my table stand as the better instrument.

### The last stretch: what a green guard cannot see

Started from CLAUDE.md's standing question rather than a symptom. `no-fabricated-divisor-ratchet` has read green since it was written; it anchors on the `/`, which is exactly what keeps a legitimate `parseInt(x) || 1` legal — and therefore it matches nothing once the denominator is **named on a previous line**. Three live sites were inside its own roots:

- `components/analytics/WalletProfile.tsx` published a measured **"0%"** collection-mix share. `flowty_analytics_wallet_detail` does `COALESCE(ROUND(SUM(principal_usd),2), 0)`, so on a wallet whose funded loans all carry NULL the total *and every part* go to zero together and the substituted $1 denominator turns an undefined share into a measurement. **2 of 16** borrower wallets (`0x005ec64f25575b48`, `0x9f86ab4a834904e7`).
- Both `Sparkline` copies used `max - min || 1`, sending every point of a FLAT series to `(v - min) / 1 === 0` — drawn hard along the **bottom** of the box, visually identical to a portfolio that fell to the low of its window and stayed. **The same copy-paste pair this ban was written for, one expression down**, so both are fixed and both pinned.

⭐ **The rule promoted to CLAUDE.md: a guard anchored on an OPERATOR is blind to the hoisted spelling of its own class.** Ask it of every operator-anchored guard here. Full case in `testing-and-ci.md` ("A THIRD AXIS") and `key-files-and-honesty.md`.

**Guard widened on three axes**, each proved by injection into real files and reverted: the clamp spelling `x / Math.max(y, 1)` (added at population zero), the hoisted spelling, and the roots — curated four → a walk that also covers `scripts` (with `.mjs`, where one instance lived) and `supabase/functions`, whose exclusion rested on nothing since edge fns sit outside the coverage gates. ⚠ Hoisted enrolls only a **non-zero** literal fallback (`?? 0` divides by an explicit zero → `Infinity`/`NaN`, loud not plausible), and its first cut had two false positives that were **both the identifier inside a URL path** (`"/api/fmv/demo"`, `"/serial-premiums"`).

⚠ **The opt-out's fixed 3-line window swallowed BOTH suppressions I wrote** — a justification naming the measured population, why the fix is elsewhere and the exit condition does not fit in three lines. Now those 3 lines UNIONed with the contiguous comment block above, which stops at the first non-comment line. *An escape hatch that quietly does nothing is worse than none: the author believes it took.*

### Two honest limits, recorded rather than implied

1. ⛔ **The production half of the verification was not available.** `/analytics/wallets/[address]` is behind the sign-in funnel, and my anon fetch returned **HTTP 200 carrying the `/login` shell** — the "rendered DOM, not HTTP 200" trap, live. Evidence is the component pins (each re-run with the defect restored, and each fails there) plus CI 4983, not a production DOM. It also *lowers* the severity a bare "2 of 16 wallets" implies.
2. ⛔ **The `pack-supply-parse` pair is suppressed, not fixed.** At `totalCount === 0` it writes `drop_weight 0` for a whole distribution — a "never drops" claim. Measured **latent** (0 degenerate `gql_historical` dists), and left out because the fix needs both hand-copies **plus an edge deploy**, which would destroy the attribution if bundled into a display fix. Marker carries the exit condition.

ⓘ **One dead end recorded so it is not swept a third time:** the sibling shape — `?? 0` on a supabase count — is **drained**. 39 sites across `app`/`lib`/`components`/`workers`; opened one by one, every one branches on `error` first or fails closed. A tree-wide ban would over-report where the work is done.

## 2026-09-07 (morning PT) · Claude Code on Trevor's box — catching the tree up from three no-push sessions, then the one ship they measured and could not land

**Trevor:** "get us caught up, address everything pending in the tree, and work through them until we have a clean tree."

The mount held output from three sessions that all failed to push (nightly pass, two daytime-monitor ticks, a Cowork laptop session whose device flow was declined) while **33 commits landed upstream**. Spliced into the current tree rather than writing back their stale copies: 2 ledger entries, 09-07 metrics, a session entry, 3 inbox filings, INDEX 404 → 407 (counts re-derived by the fixer, never hand-edited). Guards held: swallowed 3, future-dated 0, 5/5 INDEX assertions. Annotated rather than edited the nightly's `/api/best-offers` item, which its own author refuted six hours later — left alone it would have sent the next reader to re-queue settled work.

**Shipped** `20260907155956`: `reconcile_wmc_metadata_from_editions()` now logs every tick. It only logged `IF v_n > 0`, so no-op ticks wrote no `pipeline_runs` row and the cadence arm measured silence-of-*writes* — while the watchlist row shipped 14 h earlier says in its own notes "Health is SILENCE, not rows_written". Measured: **24 pg_cron runs all succeeded, 10 pipeline_runs rows** in 12 h; 7-day gaps avg 18.8 min, max 180, 3 over threshold with 2 in the last 24 h. Blast radius enumerated (2 objects, no app code); the `ok=true` heartbeat cannot trip `check_pipelines_running_but_not_succeeding`.

**Measured, queued** (inbox `2026-09-07T1603Z`): `/insights/pack-reality` **is** honest — verified by rendered DOM with controls — but the 0311Z "genuinely empty" diagnosis is refuted. `pack_drop_pool` froze on 08-28 with the GQL host and the `atlas` pool is a **57-distribution July seed**; packs priced/day collapsed **308–836 → flat 145–152** and has sat there 10 days under `ok` 24/24. Do not widen the gates (all 12 fresh positive-EV packs are 92–99 % depleted); the lever is the pack pool, which is **#65** and another session's live workstream. ⚠ Method: `innerText` is CSS-uppercased, so my first case-sensitive DOM probe called the honest copy absent — controls caught it.

## 2026-09-07 01:0x PT · Nightly autonomous pass (Cowork cloud, NO-PUSH) — quiet honest night

Health green (security all `[]`, trust breaches `[]`, no stalls, public 5xx ~19/24h none on boards, TS HIGH+MED 37.4%). Post-ship watch on the 09-06 ships all holding (atlas edition-verify dispatch opt 0.6–1.7 s; moments-hydrate-wmc draining; refresh_wmc_fmv_changed clean). Three fresh candidates all closed/queued: `allday-lock-refresh` trypdub false-alarm was **already fixed by another session** (per-wallet non-fatal, `ok=true` since 09-06 17:23Z); `/api/best-offers` break-on-error warn fired **0× in 24h** (queue, close after a week of zeros); ~19 anon-public soft-404 tabs (product call + `proxy.ts`, queued); `sync-nba-projections` dry ≥72h (likely NBA offseason, queued/watch). **Nothing shipped** — no additive/reversible DB lever. Ops: 405 un-archived inbox files need a push run to clear. Handoff: `docs/handoff-2026-09-07-overnight-pass.md`.

## 2026-09-06 (evening) · Cowork (cloud + device VM), Trevor present — the Top Shot feed is back, from Atlas, read through the database

**Trevor's brief:** scrape hardcourt.io, Dune `flow/d23-tradeboard`, `pbc-challenge-tracker` for intel; look for Diamond's dashboards; "continue to look for ways to get our top shot feed back live… dig deeper into Atlas." Ledger entry: *THE TOP SHOT FEED IS BACK*. Known-issues **#65**; method catalogue in `apis-and-cadence.md` (new Atlas section).

### What was found

- Watching what nbatopshot.com itself calls (real Chromium on an edition page) then re-probing FROM THE DATABASE with pg_net: `MarketplaceService/SearchMarketplaceTransactions` unfiltered is a platform-wide firehose (listings, sales with buyer/seller/fee/proceeds, offers; 200/page); by `editionId` it is the open book price-ascending; by `nftId` one Moment's history (a verification-by-listing check with no dead host); by `sellerAddress` a wallet's listings; `ProfileService/SearchUserProfiles` is the username resolver. Unknown keys are silently ignored.
- **pg_net sends only after the enqueuing transaction commits.** The first `apply_migration` failed its own in-transaction positive control (`atlas_timeout` on a call that answers in 4 s from `execute_sql`); the probe's queue row was gone after rollback. So a synchronous post-and-poll SQL function is impossible; every live read is two RPCs (`*_begin`, `*_collect`) — `lib/chains/flow/atlas.ts`.
- **Cloudflare's challenge on the Supabase egress is burst-sensitive.** ~5–15 % base `403 Just a moment` on the badge lane; my 60-request header A/B drove it to 100 % for ~4 min, every header variant alike. Do not A/B in bursts; read `topshot_atlas_market_requests.error` before blaming code.

### What shipped

- Migration `20260906203504`: `topshot_atlas_market_events` + `_requests`, `atlas_market_dispatch()/drain()` on pg_cron every 2 min (pipeline `atlas-market-feed`; first drain 400 rows), `atlas_resolve_username_{begin,collect}`, `atlas_verify_listing_{begin,collect}` (service_role). Drift check clean.
- Code: `lib/chains/flow/atlas.ts`; `resolveTopShotUsername` Atlas-first (dead-GQL ladder only on a read failure); `lib/verify-wallet-gql.ts` over Atlas; `verify-challenge/check` seller-matched — the verification badge works again (#59 option b) with the gate still dropped.

### Competitive intel (Project doc `claude/intel-2026-09-06-hardcourt-dune-pbc-tracker.md`)

hardcourt.io (Supabase-backed; edge fns `hardcourt-verify` / `topshot-collection`; still verification-by-listing; fantasy contests + referral codes), Dune `flow/d23-tradeboard` (the @flow team's Disney Pinnacle D23 Trade Board, event concluded, queries private; the @flow team has ~63 dashboards), `pbc-challenge-tracker` (Panini WC Prizm challenge tracker — per-challenge cards required / max completions / cost to complete; maps onto our WC Prizm plane, #64). "Diamond" was not found by name on Dune — asked Trevor for the handle.

### The sentinel sweep that followed (same evening)

Trevor forwarded the sentinel: a `pg_net_http_403` CRITICAL (our own market lane — the arm now attributes it, plus a rule to RECORD session probes), a retired pipeline's watchlist row still reading "silent 2769m" (retired; `atlas-market-feed` armed), the rookie cohort board at 4.9 s (one missing `collection` predicate on the serial-1 join: 4.6 s → 0.7 s, equivalence proven over 6,379/6,379), the All Day unmapped-sales floor (Atlas `{nftId}` is the ownerless read it never had — `allday_resolve_unmapped_via_atlas` every 5 min), and five stale DB pins (a snapshot migration of the live bodies, five tests' assertions re-derived on a local Postgres 16 — `refresh_unmapped_backlog_growth`'s stall semantics had changed on 09-05 and the pinned test still described the ratio test). Ledger: *SENTINEL SWEEP after the feed went live*.

### The Search Console read (same evening)

Trevor: "get a feel for improvements that can be made for google SEO." Read from his logged-in GSC in Chrome (a background tab — `document.body.innerText` via `javascript_tool`, not screenshots). The headline is not technical: **External links: 0**, 45 clicks lifetime against 31.3K indexed pages. Three technical findings shipped as one code commit (robots carve-out for `/_next/static` + `/_next/image`; `/moment/<edition uuid>` 301 + dropped from the sitemap; wallet cells off the non-existent `/profile/<address>`). The rest — links, query-matched pages, sitemap pruning, internal linking, titles — is in `docs/strategy/seo-2026-09-06-search-console-read.md` and known-issues #66. Lesson worth keeping: **a "Not found" bucket in GSC is a list of links WE emit** — 302 404s were four components building an href from a wallet address to a route that resolves usernames only, and every reader clicking a buyer name got a 404 too.

### SEO part two (same evening, ~18:00 PT)

Trevor: "Take /pricing out of the footer and keep going." `/pricing` left the footer, the sitemap and the index. Then the internal-linking finding: the edition page, now the 308 target for every `/moment/<edition uuid>`, linked to zero other editions — and the *Same Play · Other Sets* and *Parallels* cards were themselves the emitters of the `/moment/<uuid>` duplicates. `get_edition_related` (pinned) + `editionHref()` fix both. Two GSC query intents were traced to pack-dist pages and those titles now say what the page answers. **Two lessons:** (1) a redirect decided in a page behind `loading.tsx` is a 200 + meta refresh — decide it in the layout (the segment's own 07-25 comment already said so for 404s; I re-learned it live); (2) the "Not found" and "Alternate canonical" buckets in GSC are lists of links WE emit — grep the card `href`s before blaming the crawler.

### The audit drain (late evening) — "keep doing all you can"

Five ledger entries. The DB's #1 physical reader (`reconcile_wmc_metadata_from_editions`, converged 09-04) went from `*/10` to every 30 min after its filing's three falsifiers were re-run, and got the watchlist row it never had. 2,065 tiny pack distributions left the sitemap. Then the big one: `ts_listings` — the Top Shot sniper's "primary source" — held ONE row from May; it is rebuilt every 2 min from the Atlas firehose with a `{nftId}` verify probe (cancellations are not in the firehose), and the route now keys deals off the on-chain ids because the name-tuple lookup priced 12 of 604 rows and keyed them on inert uuid editions. `cached_listings` and `edition_offers.low_ask` followed from the same verified set — the `offers-sweep` behind the latter has been dead since ~08-28 under `ok=true skipped` (395 of 464 compared editions showed a HIGHER stale ask than the live floor). **Three lessons:** (1) Atlas `{editionId}` is the edition's FULL history, not an open book — `{nftId}` is the verify read; (2) a "sweep ok" that means "the breaker skipped it" is the sweep-ok-means-completed class one more time — measure the OUTCOME table (`edition_offers.updated_at`), never the run row; (3) "editions with no sales ever" is 2,233 of 21,423 canonical (measured) — a sitemap prune that needs a materialized flag, not worth the build cost tonight.

### Not done (at the time of writing — see the addendum below)

The sniper / `cached_listings` off `topshot_atlas_market_events` (needs a per-edition open-book refresher — delistings are not in the firehose); re-pointing the paused dead-host pipelines (`offers-sweep`, `resolve-wallet-usernames`, `deal-floor-serials`, fmv-populate asks, `moments-hydrator`, `backfill-pack-pool`).

### Addendum — the rest of the night ("keep going… don't stop until there's nothing left")

Four more ledger entries. **The sniper's `cached_listings` and `edition_offers.low_ask` were done** in the same Atlas tick (migrations `021459`, `022120`), and then the open-book refresher the "Not done" paragraph asked for: an **edition verify lane** (`024130` + RLS `024159` + index `024433` + `024754`) reads two editions' full Atlas history per tick, NULLs a stale `low_ask` on evidence and fills `highest_offer` from the open EDITION/PARALLEL offers — the first live "best offer" since the sweep died. **`offers-sweep` (cron-job.org 7712610) set Inactive from Chrome.** An hour was then spent on a "Loading…" hang on `/pack-sniper`, `/market`, `/collection` that turned out to be an artefact of BOTH automation browsers (a VM Chromium renders them; pinned in `tooling-gotchas.md`); the E2E smoke gained a `<main>` floor so a never-resolving Suspense fallback cannot read as healthy (`dbf4999`). The register's **R49 and R50 closed** on re-measurement (series pages 509 ms / 188 ms; every board in budget since 09-04; "Mode 2" was the same browser artefact). **SEO part three:** the five indexable client-shell tabs served Googlebot 95–178 chars and zero links — they carry the overview's catalog fan-out now, Data-Cached per collection (`e618065`, `deeb1ba` for Pinnacle's bespoke tabs); verified 18 edition + 35–44 hub links on every tab after the deploy. Measured and NOT shipped: 2,233 canonical editions with no sale ever all carry art, circulation and (886 of 972 TS) descriptions — not thin enough to prune.

Still not done: `resolve-wallet-usernames`, `deal-floor-serials`, fmv-populate asks, `moments-hydrator`, `backfill-pack-pool` — the remaining dead-host pipelines, each needing its own Atlas mapping; the WNBA Gold / premium-chance explainer (blocked on Trevor's slot paragraphs); R6's in-band saturation measurement.

## 2026-09-06 · Cowork (cloud + device VM), Trevor present — the deep-audit + 670-page QA + new-user walk, and the first TRUE-mobile instrument

**Four code batches and five migrations to `main` in one day, every one of them found by looking at a real 390 px Chromium rather than the extension window.** Full detail per batch is in the ledger (four 2026-09-06 entries); this is what a future session should carry.

### What the instrument change found that nothing else could

- The Cowork/Chrome-extension window cannot go below ~738 px. **Every "mobile" claim before today was a desktop layout squinted at.** `scripts/qa/mobile-sweep.mjs` (real Chromium, iPhone 13 descriptor, run from the device VM because the cloud sandbox's proxy resets the TLS tunnel to prod) + staging the PNGs and reading them with `Read` is the method now, documented in `scripts/qa/README.md` and testing-and-ci.md.
- 510 entity pages + 32 surfaces × 5 collections + the 12-path signed-in onboarding walk: 57 sitemap player URLs that 404'd (three classes — team Moments, legacy players with no `players` row, diacritics), "Unknown · Squad Goals" pack subjects, 394 dead AllDay thumbnails and 45 dead Golazos tiles rendering as broken glyphs (an `onError` that fired BEFORE hydration), a 46-second `get_collection_stats`, $0 tiles on a 15,284-Moment wallet (username sent to an address-only RPC, zeros under HTTP 200), All Day tiles wearing Top Shot art (id collision in `getThumbnailUrl`), an anon 307→405 class at the proxy, and **the same wallet headlined at four different numbers on four surfaces** (raw vs total − stale).

### The new-user walk (account `qa0906`, created and deleted the same day)

Create account → trophy case → save wallet → favourite teams → Collection / Market / Sniper × 5 collections → toggle. Found and fixed: dashboard tiles read **$0 / 0** for a brand-new account with no wallet (now "—" + "Add a wallet above"); profile saved-wallet rows linked EVERY collection to the Top Shot tab and printed $0.00 for market-closed ones; **wallet verification is dead platform-wide** (the listing host is gone since ~08-28) and the route said *"No matching listing found"* — a false claim about the reader's own account. That last one is a go-live blocker that needs Trevor's decision (#59); the route is honest now.

### Later the same day — Trevor delegated the needs-Trevor list ("decide what's best for RPC long term and our users")

Decided and shipped in one push (`092f886`): the verification gate is gone (history reads every SAVED wallet — the check had no data source, so "verified" meant "nobody"); a free `window.onerror` beacon with its own alert arm closes the client-error blind spot; **Candy MLB is live, thin** (overview only — the tabs are Flow-dispatched and a dead tab is worse than none); Panini is the WC Prizm plane (bridge not started). The trap the Candy flip exposed: `published` had been standing in for "Flow" in five fan-outs — each now filters on `dbChain`, and the guard that used to say "every published collection is Flow" now says "a published non-Flow collection is overview-only until its tabs have a dispatch". Left for Trevor because an agent structurally cannot: the two gate-key rotations, the #22 branch, cron-job.org cadence, the Sentry bill.

### Strategy

`docs/strategy/go-live-2026-09.md` is new: the measured state (24 accounts / 1 WAU; TS HIGH/MED 37.3%, down from 54.5% while the Atlas migration is incomplete), five blockers, eleven go-live bars with the instrument that reads each, the Candy checklist (~1 week, data plane complete, product plane missing — thin tabs recommended), and Panini's P0 decision (WC Prizm plane recommended). The roadmap carries a 09-06 stamp pointing at it. Vercel: 75% of $412 was Build CPU on a Turbo 30 vCPU machine — switched to Standard + queued with Trevor's approval (#61).

### Lessons promoted

- **A component that server-renders an `<img>` misses its own `onError`** when the load fails before hydration — replay the event from a mount effect, never a second state path. Now in `PackThumb` and site-wide in `DeadImageGuard`.
- **Headline = total − stale, disclosed, on EVERY surface that prints a wallet total** — there were four surfaces and two RPCs, and each had been "fixed" once. Fix per PANEL.
- **A test account is a data mutation** — enumerate every `user_id` table (19 + `follows` + `user_profiles` + `auth.users`; `public.*` has no FK to `auth.users`) and delete in one transaction; verify with a 404 on the public profile AND an orphan count.
- ⚠ **`left(cron.job.command, N)` prints gate keys** — two `?key=` values were echoed into this session's transcript while enumerating pack-opens jobs. Select `jobname` only; the keys are listed for rotation in the needs-Trevor doc.
- Two hourly pipeline-alert emails were a 40-minute Flow-host outage on 09-04 (rolled off by the 2-day window) and a single bad wallet failing a whole run (fixed in code): both suppressed with self-expiring `pipeline_alert_suppression` rows — the predicate lives in the `reason` column so the next reader does not re-derive it.

## 2026-09-05 → 09-06 · Claude Code on Trevor's box — a long autonomous stretch whose main output was four corrections to its own published claims

**Three code ships, two owed watches closed, and — the part worth reading — four numbers I published and then had to correct.** Every correction was caught by running a control, never by review.

### The corrections, because the pattern is the finding

1. 🚨 **A "90% reduction" that was window-selected.** I discharged the `ipfs-media` gateway-race watch on a matched-hours comparison (17.6/h → 1.71/h). Running the filing's *own* falsifier on a **second** window of the **same two days** gave **223% WORSE**. The series is burst-dominated — 26 events in ~3 minutes, silence for hours, then 32 in 45 — and **154 of 09-04's 194 daily events sat inside the one window I picked**. ⚠ **A matched-hours control corrects for traffic and does nothing about bursts.** Full-day, the only window-proof summary: **194 → 84, ≈51%.** ⭐ **The MECHANISM survived** — a positive control shows 100% of served requests moved to a different, faster upstream. **Magnitudes move; mechanisms tend to hold.**
2. 🚨 **A p-value computed against one day's baseline.** Step 6's "p ≈ 0.032" used the **highest** of four daily rates (3.25–8.67%). Corrected *weaker* to a 0.02–0.24 range. Only n≈200 made it baseline-insensitive.
3. 🚨 **An attribution read off a smeared field.** "The DEP0169 group's `routes` lists exactly the two importers" — one of the two imports **no Flow SDK at all**. Root-caused properly later (§below).
4. 🚨 **A units error that nearly became an alarm.** `get_edition_market_bundle` looked like ~3× the platform's top reader at 34.5M "blocks" — that was **read + hit**; by **physical reads** it is 1.93M and **11th**, 94% cache-served. It also nearly became a *fifth* correction: I wrote that a `get_runtime_errors` window bug "affects every number in this repo", then checked whether it generalised — **it does not**, and narrowing it confirmed an earlier filing was right.

### Shipped

- **`resolve-topshot-stubs` was losing 2.5% of ticks** to a 30 s wall while its own code declared a 120 s fetch bound. 3 kills, each matched 1:1 to an edge run over 30 s, **work succeeding every time**. Wall → 120, plus a tree-walk guard (455 routes, 181 with walls) banning "declared budget > wall"; mutation-proven, md5-restored.
- **A ban at zero on sitemap discovery** in `entity-smoke`. All eight arms are fail-soft, and `fetchSitemapLocs` returns `[]` on any non-200 — so **a broken sitemap made the ENTIRE client-side detection surface report success having rendered nothing** (Sentry has been dead since 08-18). Proven green (8/8) *and* red (0/8) against the real site.
- **The suite's most frequent flake** (red in 2 of 3 full runs, pre-existing) stabilised, with both dropped hypotheses kept in the file rather than inventing a cause.

### Root-caused: DEP0169, the platform's #1 error group

Not the Flow SDK. **`@onflow/fcl → cross-fetch@4 → node-fetch@2`**, which calls `url.parse()` on **every request** — so it fires on **use, not import** (importing fcl is silent, which is why "every cold start logs one" was the wrong model). Two static greps had failed at this; **patching `url.parse` to name its own caller settled it in three lines.** A version bump is foreclosed (latest fcl still pins `cross-fetch: ^4.0.0`), and the native-`fetch` shim was **probed locally with a matched control** — real Flow read OK, 0 `url.parse` calls, vs 2 and the warning in the control. ⛔ Not shipped: it sits under every Flow read, and the probe covered one plain GET, not the body/redirect/streaming paths where undici and node-fetch differ.

### Watches closed

- **Step 6**: 198 runs, 0 timeouts, 961 editions touched. Null excluded at *every* plausible baseline.
- **pack-reality / api/packs**: 0 events over 26.3 h. ⚠ Its positive control was re-run **at close** — because the same query gave a **false all-clear** the day before, when `routes: "/insights/pack-reality"` matched nothing (the real path is `/api/public/insights/pack-reality`) and *"No runtime errors found"* read as good news.
- **The stubs wall** closed in **hours, not the ~5 days a rate would have needed**, because the watch was built around **the one observation the broken version could not produce** — a run exceeding the old wall that still writes its terminal row. ⭐ At a 2.5% base rate, waiting for the *absence* of kills would have died of `pipeline_runs`' ~73 h retention before it could conclude.

### Housekeeping worth knowing

**Four orphaned filings committed on other sessions' behalf** — `rpc-daytime-monitor` and the night pass write to the mount and cannot push, and *"could not commit"* is indistinguishable from *"has not committed yet"* in `git status`. Each was read in full first. One had a **wrong supporting sentence** (`badge-sync.yml`'s schedule "was stopped" — it is still scheduled and ran 4× that day); its *conclusion* held, because the pipeline it names is written by a **different route**. ⚠ A **0-byte, 76-second-old** `index.lock` blocked commits — the recorded recipe says "0 bytes and hours old is stale", so age alone would have said *leave it*; the discriminator that worked was **no git process + a frozen mtime across two samples**.

## 2026-09-04 (midday PT) · Claude Code on Trevor's box — closing the night's reads, and paying for two new CLAUDE.md rules

Every read the 09-03 evening stretch left owed came back clean, and three of them were only readable because eight hours had passed: the **circulation sweep** (triggered manually rather than waiting for 09-05: 9,523 pairs, 239 script calls, **0 script errors**, 25 rows changed, 89.7 s, and 0 parallels touched), **#54's gate** (08:00Z tick `gated: true` in 990 ms against 16,687 ms the day before), and the **drift parity control** (neither function I deployed appears among the 6 drifted). The Top Shot `unknown` bucket has taken no new failure since the on-chain port.

⭐ **The lesson promoted to CLAUDE.md is the ninth honesty shape: a sweep's `ok` means it COMPLETED, not that its LANES worked.** Three correct local decisions composed into a month-long blind spot, and the tell was a `GROUP BY failure_reason` nobody runs — not a red badge. The second addition is the pipe-exit trap in a new costume, which cost me a red push this session: `grep <log> && git push` gates on grep FINDING a line, never on the run passing.

⚠ **Both additions were PAID FOR, not spent** — the file had 5 characters of headroom. Five passages moved out verbatim to their reference docs (the `candy-editions-ingest` kill figures, the unshipped-DB-fix numbers, the sports-proxy detail, the resolved #27 note) or were rewritten to carry a newer fact (`fmv-recalc` now names the `query_sql` sizing). 39,995 → 39,989.

Also committed the no-push night pass's own artifacts — its ledger entry, metrics and handoff sat uncommitted in this clone because that run was cloud-only. **Handing off the artifact, not the promise, applies to somebody else's artifact too.**

## 2026-09-03 (late evening PT, into 09-04 UTC) · Claude Code on Trevor's box — "keep going" stretch: alert ask-age, five dialogs, and a Top Shot lane that had been dead for a month under ok=true

Shipped, each with a ledger entry: **deal alerts say how old the ask is** on all three channels using the boards' own 12 h marker (`1067665b6`); **five `role="dialog"` containers gain Escape / Tab trap / focus restore** via the shared hook — the #17 remainder, with the four new Escape cases proven failing against the unfixed components (`55b3e1762`); **`sales-serial-backfill`'s Top Shot lane moved on-chain** and the function's auth became strict header-only Bearer (register steps (a)+(c)), edge fn v38, plus a cadence-watchlist row for a pipeline that had none (`5fbc95ea6`, migration `20260904021206`).

⭐ **The finding worth keeping: a lane can fail 100 % for a month with every instrument green.** The sweep logged `ok=true` 36/36 because per-target failures are not sweep failures (correct for escrowed moments), the pipeline was on no watchlist arm, and the failure bucket was named `unknown` — three layers of "not my job". The tell was not a red anything; it was `count(*)` on a failures table grouped by reason. **Follow-on (v39, this session):** a lane whose every target (≥20) fails on a TRANSPORT reason now flips the sweep to `ok=false`, so the new no-success arm can see the next dead host. **The 02:40Z tick read (v38): Top Shot 131 processed → 128 resolved, 3 `onchain_nil`, 7.4 s — against 184 / 0 / 184 `unknown` two hours earlier; no new `unknown` failures after it.** Also shipped: `npm run pipelines:kills` reads durations against each route's own wall (`wallClipped`, UNMAPPABLE at 1.2×, `killed` untouched), which needed `allowImportingTsExtensions` and retired twelve `@ts-expect-error` directives.

⭐ **`test:cadence` does not walk `supabase/functions/**`** — the new Top Shot borrow script was verified by executing it on mainnet through the cadence MCP with two real rows (serials 41 and 15), which is stronger than lint and is now a memory.

**Later, same stretch:** `pipeline_gap_hourly` + `rollup_pipeline_gaps()` on pg_cron 443 (`1a822f68d`, migrations `20260904024810` + `..921`) — the CALIBRATION series the 45-skipped-ticks arm needs, not an alarm (first hours: 99 scheduled, ~320 ticks/h, 1 skipped/h; first scheduled tick 03:07:00Z succeeded, 6 rows); `wallet-preflight` body-read timeout → 502 not 500 (`9ecae40dd`), found by re-running the 09-03 body-outside-catch sweep with a user-facing split (54 files → 1 real); the memory index trimmed from 26.4 KB to 23.7 KB so it loads whole again (it had been silently truncated every session).

**Then the port:** Top Shot BASE-edition circulation now comes from the chain daily (`d6c7b3572` + `4cf6967f1`, migrations `20260904031944`/`..601`, Vercel cron 04:05Z) — measured first on a hashed sample (every base row matched the chain; every mismatch was a parallel row), the series-8 normaliser lifted into one function with two callers, an apply RPC that compares against the normalised value, rolled-back live controls both directions. ⚠ Pushed RED once: the suite had one failure and my push command gated on grep, not on the suite's EXIT line — the pipe trap, in a new costume. Fixed in the next commit. ⭐ **The first tick failed exactly as designed to fail:** 250 pairs per script tripped the execution node's computation limit (Flow 1110), the run logged `ok=false` with `script_errors 38`, and a chunk of 40 (probed: 100 fails, 50 passes) shipped within the hour (`7df740cae`). The real first read is the 09-05 04:05Z tick.

**Delegated decisions (Trevor: "use your best judgement, considering RPC long term and our users"):** #54 — `match-topshot-players` keeps its pipeline and gates its 17–126 s walk behind its inputs (weekly ceiling; full run 15.9 s, gate 10 ms; `aa8cb8eab`, migration `20260904043939`, edge fn v30); **40 `info` watchlist arms** seeded for every unwatched pipeline from each one's own 73 h profile — visibility before pages (`20260904044417`); four Cowork follow-ups on my own work — `%5C` paths 404 in the proxy, `/trophy-case/<u>` aliases to the public route, the no-success arm gains grace + a lower bound (it had fired on my own new pipeline's first failed run), `search_path` pinned on the two new circulation functions and deliberately NOT on the two flagged PROCEDURES (`fc20c2f06`, migration `20260904044756`). Two rebases over Cowork's concurrent pushes, both clean with the ledger resolver. Then two more dashboard third-states from Cowork's new-user walk — a failed hero read shows a loading-problem panel, a just-added wallet's tiles say INDEXING (`1f23b3e00`, 3 tests proven failing) — and a measurement filed, not shipped: `query_sql` is the DB's #1 reader (12.1 M blocks / 24 h) and `fmv-recalc`'s seven ad-hoc scans own it (inbox 2026-09-04T0500Z).

⚠ **Owed reads:** the 02:40Z tick (`pipeline_runs` `sales-serial-backfill`, `extra.per_collection.topshot.resolved > 0`, no `http_530/429` detail; 131 Top Shot targets were eligible at 02:14Z); the sentinel's first run carrying the "Pipeline Success" arm (the 02:34Z schedule; last run 00:16Z predates the deploy); the daily edge-fn drift check at ~11:39Z must list `sales-serial-backfill` as in-parity.

## 2026-09-03 (afternoon PT, Trevor's box) · catch-up, then two instruments that were reading their own echo

Local was 153 commits behind origin with the night pass's continuity uncommitted behind two stale
git locks; fast-forwarded and re-spliced (ledger, session, metrics, handoff, two filings, INDEX 377→379).
The 09-03 `!docs/**` gitignore negation had un-ignored `docs/overnight/.lock` — a RELEASED lock was
one `git add -A` from being committed as HELD; re-asserted after the negation and pinned by order.

Both daytime-monitor filings were then re-derived rather than drained. **The "NEW HIGH" parallel-serial
breach was the self-healer's routine inflow**, read at :48 and healed at :52 on the same cadence, so
every raise was published as a six-hour breach on a healed condition; the filing's own reconstruction
read 0 because its regex was stricter than the leg's. Fix was ORDER, not threshold: jobid 219 now heals
at :43. The **"wedged" pack-opens backfill walks DOWN**, and its falling floor was progress; `status 0` is
its own documented abort budget on a function nobody can deploy until a gate secret is set. And the
**"recalibrate `max_ms`" ask for `candy_scarcity_board` would have hidden a 1,000× regression** — the
"2 ms" calibration had been a 2.4–4 s p50 for three weeks with a 390 s tail; materialised it (16,662
buffers → 4) instead of widening the alarm. ⭐ **Lesson promoted to the reference docs: a consumer on
the same cadence as its producer still has an ORDER relative to it, and "raise the budget" on an arm
that flickers is only right if the board's history says the calibration was wrong, not the board.**

Second block (15:00–15:40 PT): three tree-walking guards were red only on this box (backslash paths vs
forward-slash prefixes — fixed at the walker, lesson in tooling-gotchas + memory); the wmc REINDEX became weekly
maintenance (jobids 438–442, sized off the 08-31 second wave, regrowth measured decelerating); #36's week-old
decision written into the register with a fresh T2 baseline; the spork-wall filing annotated as fixed on 09-02; the
wallet-username-resolver "stop" traced to the deliberate dead-host pause; the overnight handoff's queued concierge
item found already done. ⭐ Two of today's six "findings" were instruments reading their own echo and two more were
work already shipped — re-derive before draining.

Third block (15:40–16:10 PT): the 09-01 pack-EV staleness handoff turned out to be a surface question, not a
job question — `secondary_available`/`secondary_ask` are columns on the EV snapshot, and THREE readers
(the pack OG card, `/api/packs/grails?buyableOnly`, the concierge's `compare_pack_value`) called a months-old
row "buyable" with no age check while the page and the deals surface already refused past 72 h; all three
now gate (22 Top Shot packs were unfurling as green +EV off 7–135-day-old snapshots). And `/api/sniper-feed`
bounds its six deal-bearing reads: a hanging read resolves into the branch that already says COULDN'T LOAD
THE FLOOR instead of a 504 — proven by stashing the fix and watching five never-settling reads hang to
vitest's 30 s wall. Eight pushes today, all CI-green; tree clean.

Close (17:52 PT): every verification the ships owed inside the session landed — the hourly candy refresh fired
twice (1.6 s / 0.6 s), the heal-before-read order was confirmed by the real cron callers (219 healed the one
inflow at 00:43Z, 324 read 0 at 00:48Z, board down to its single structural breach), and the two-hour rwfc
flow off the fresh T2 baseline is on #36 as orientation for tomorrow's ≥ 24 h read.

## 2026-09-03 (04:10Z → 19:40Z) · the CI estate audit, drained in five passes, and the fleet alarm acknowledged instead of silenced

A single cloud session (`session_016PmC2SrSHtZuyZ9XpaPwyx`), started from "audit our CI and similar practices" and run autonomously overnight and through the day on "keep going". Everything shipped to `main` directly; every ship has a ledger entry with a watch and a revert. Report: the *RPC CI Estate Audit* artifact (status paragraphs cover passes 1–4).

**Pass 1 (c3f65841c) — every High and nine Mediums.** The push-time smoke measured the previous deploy (`sleep 45` against a 110–124 s build) and now fires on `deployment_status`; the worker tree gained a typecheck job (`workers/rpc-mcp-proxy` had no tsconfig at all); `run-db-tests.sh` asserts it found files; ten workflows had a bare `cat` that killed the step before its own warning on a connection failure; 401/403 became fatal where the workflow is a route's only caller; `allday-ingest.yml` (72 asks/day into a stub) deleted; timeouts, `permissions: contents: read`, inputs through `env:` everywhere.

**Pass 2 (198a89adc + 79c8ab453) — the docs-only path filter.** 58% of the last 500 commits touched only docs and ran 13 jobs each. A fail-safe `changes` job classifies the pushed RANGE (`github.event.before..HEAD`, never `HEAD~1`; a zero or unresolvable base is `code=true`), ten code jobs gate on it, every guard job runs unconditionally, and a docs push still runs the 107 tests that read docs. ⭐ The classifier is pinned by a test that EXECUTES the step body in a throwaway git repo (ten cases) — a regex would pin the spelling. Also: the Flow CLI install as a composite, the Playwright browser cache, the scheduler-liveness false positive on a brand-new workflow, and 1,600 lines of case history moved out of the three vitest configs verbatim.

**Pass 3 (e52a2ba01, 3776a3206, 06bdc8acd) — sharding and the composite.** The primary coverage gate runs as two shards plus a merge that applies the ratchet once to the merged report; shards zero their thresholds on purpose (a half-suite's 55% is not a number about the codebase). Merged coverage byte-identical to a single run, wall clock 6:12 / 4:51 / 4:55 against a 476 s median. One `rpc-call` composite replaced ten hand-copied curl blocks across six ops workflows, verified by a nine-case stub-server harness and two dispatches. ⚠ Two self-inflicted reds: the curl-guard test's floor did not know the composite existed (fixed by walking both roots and asserting the second root CONTRIBUTES), and the sha-pin test asserted something a composite cannot satisfy — shipped red for four minutes because the push chain echoed test output instead of gating on it. Both in the ledger as written.

**Pass 4 (00f38211e) — SHA pins, then current majors.** Every `uses:` pinned to a 40-char sha with a `# vX.Y.Z` comment under a ban-at-zero test, then all five actions moved off the deprecated Node 20 majors (checkout v7, setup-node v7, upload-artifact v7, download-artifact v8, cache v6) after reading each repo's breaking-change notes over anonymous `git clone --depth 1` (the releases page is API-gated from the sandbox; the repo is not). Verified by CI, three dispatches, a Playwright cache hit under cache v6, and a job log read end to end with no deprecation line; a scheduled ops-monitor run at 18:36Z on the new pins succeeded.

**Pass 5 — the fleet alarm, by decision.** The Pipeline Sentinel had paged CRITICAL hourly since 08-30 for `edge-fn-drift`'s 12-run streak, a red engineering cannot clear (4 of 6 drifted functions wait on `*_GATE_KEY` secrets, 2 are `DEPLOY_DEFERRED`). Trevor chose "build and ack, 30 days" via `AskUserQuestion`: `sentinel_threshold_config` gained `ack_reason` + `ack_expires_at` (pair CHECK), the route downgrades a critical with an unexpired ack to **warn — never ok** — and renders the reason and date ahead of the finding; an expired ack pages again with `[ACK EXPIRED …]`. ⚠ Migration applied BEFORE the code push, because the route's wider select against the narrower schema would have failed the config read — and that read was itself silent: `const { data } = …` dropped every override and every ack on a returned error with a report identical to a healthy one. Now a visible `Threshold Config` warn arm (R86). Verified on the real caller twice: WARN, banner intact, no config arm.

**Lessons promoted** (to `testing-and-ci.md`, `tooling-gotchas.md`, `cron-and-schedulers.md`): a test that waits for the fetch CALL and asserts the render synchronously is a race that passes locally 3/3 and reds a loaded runner; two concurrent sessions claimed the same register id twice in one day (R83, R84) — re-fetch and re-check the register in the same command as the push; the GitHub MCP run-list output exceeds the tool limit at any page size and job logs 404 until the job completes; `send_later` check-ins are bound to the session and die with it, so a pending read must be written down, not scheduled.

**Owed, written down rather than scheduled (this session archives):** the 09-04 ~12:5xZ scheduler-liveness report is the first fully post-deletion window for R61's shedding experiment (baseline 39 / 48 / 48 fires for the seven remaining high-frequency workflows; per-repo budget predicts a rise toward ~54+, per-workflow cap predicts ~48; one day is one sample). The positive half of R86 (the config arm appearing during a real `PGRST002` window) shows up in the sentinel's own `pipeline_runs.extra` when it happens. Detector Health's ack re-surfaces 2026-10-03; set the secrets or re-ack with a fresh reason.

**Still Trevor's:** the `*_GATE_KEY` secrets (R64/R21), the email alert channel (R62), retiring one of three `/api/smoke-test` concierge callers (cron-job.org console), and the pre-purge public branch (known-issues #22).

## 2026-09-03 (morning PT) · re-QA of the onboarding + trophy-case path — every 09-02 fix held, and walking it as a fresh user surfaced seven more ships

Cowork, the day after the 09-02 walkthrough drained. Trevor's ask was to **redo the QA on the shipped
code**, so the whole campaign path was walked again in Chrome twice: as `qa0903b` (existing account →
tour 6/6 → claim handle → edit → public profile → trophy-case page) and as a brand-new `+qa0903c`
(branded signup email → dashboard → add wallet → pin → caption → remove). Nothing from the night
regressed. What the re-walk found was the **empty-state user** — a fresh handle with nothing pinned —
whom no prior pass had been:

- **The public profile's Collection Breakdown summed to ~2× its own headline** (raw wmc sum vs the
  09-02 total − stale). `get_collection_breakdown` now appends `stale_fmv`/`stale_count`
  (`20260903142035`); the card shows total − stale with a caption. ⭐ *A fix to a headline is a fix to
  one panel; the panels below it were still on the old number.*
- **The empty trophy case offered SHARE ON X and told its owner to "build your own"**; the profile
  share copy said "post your trophy case" with nothing pinned. Share only when there is something to
  share; the owner is sent to pin.
- **Tour steps 2 and 3 spotlit the same box** — the 09-02 fix moved step 2 off an absent anchor onto
  step 3's. New guard: every anchored step exists on the dashboard tree, no consecutive repeats.
- **`TopMoversCard` concluded "FMV history building — check back in a few days" in the SERVER RENDER**
  before any read — the not-yet-read state is a third state, and only `renderToString` can see it.
- **The public-profile bundle dropped the trophies and wallets read ERRORS** — a DB error rendered
  "No trophies pinned yet" on the profile page, the trophy-case page and both OG cards. Only the bio
  read's `error` was destructured; supabase-js RETURNS errors.
- **"ANALYZE <handle>'S WALLET →" searched Top Shot for the RPC handle** — it only ever worked because
  Trevor's handle equals his Top Shot username.
- **The parked `/api/profile/top-movers` budget patch was never in git** — `*.patch` was gitignored and
  the parking commit carried only the README pointing at it. Re-derived from the ledger spec, shipped
  with two mutation checks. (The concurrent session then fixed `.gitignore` at the mechanism.)

⭐ **Method notes.** A `git clone --depth 1` of the public repo works from the cloud container and is
a far better test tree than the tar recipe; changes still cross to the push clone as `git diff`
patches applied with `git apply`, never whole files. The device push clone needs
`GIT_AUTHOR_NAME='Claude (Cowork)'` per call, and the credential file is a full URL —
`"${U}/owner/repo.git"`, never spliced. Mobile layout is still unmeasurable from here: Chrome's
`resize_window` reports success and `innerWidth` stays 1536.

Left alone, stated: the `saved-wallets` quota check fails OPEN on a DB error (a documented decision;
cap = 1 free wallet, monetization tabled); ~200 `const { data } = await …` sites without `error`
remain under the existing ratchet; `edge-fn-drift` red 12× is by design (0 safe to redeploy).
## 2026-09-03 (daytime PT) · two rate limiters that could not fire, and an ETA computed from a rate that had stopped

Continuation of the overnight run. The through-line this time is **guards and alerts that were
structurally incapable of doing their job while every instrument read healthy** — and, twice, the
discovery that *my own* first diagnosis of the fix was wrong.

**The MCP daily cap could not fire, for two independent reasons.** `check_feature_quota` gates every
authed request to the MCP worker and the README advertised a `429`. (a) The quota rows were keyed to
plans **`pro`** and **`partner`**, which `pro_users_plan_check` FORBIDS — so the two rows carrying the
real caps were unreachable **by construction, provable from the schema rather than argued**, and all 20
active `pro_grandfather` wallets took the `no_quota_configured_failing_open` branch. (b) The counted key
`'mcp_query'` was **written by nothing** — `mcp_log_tool_call` writes `'mcp_' || tool_name` — so
`used_today` was pinned at 0 for every plan, `free` included. ⭐ **My first diagnosis of (b) said it
needed a `wrangler deploy` no session can run. It did not** — the Worker already CALLS that function, so
the missing write belonged in the function it was already calling. *Check what the caller already calls
before concluding a fix is out of reach.* ⚠ Both halves were **unexercised** (zero `mcp%` rows all-time),
and the first attempt to measure that used the WRONG KEY.

**The backlog alert published a three-week ETA off a rate that had stopped.** `days_to_drain` divides
the pile by `outflow_24h`, a **trailing** count. 🚨 **The published number went UP (25.1 → 32.6 days)
while the true rate went DOWN**, because the numerator barely moves and the stale burst ages out slowly
— so on a decaying series it reads plausible at every refresh **and errs toward reassurance**. Against
the same table and column over a shorter window: `outflow_3h` = **10** against 1,263/24h where steady
state gives ~158, i.e. an honest ETA nearer **526 days**. The resolver was HEALTHY and out of tractable
work (candidates/hour 1,031 → 17, nearly all `onchain_nil`) — **not a broken pipeline, an alert making a
claim its data cannot support.**

⭐ **The half that nearly got missed, and the most transferable thing here.** Nulling the ETA made the
alert honest and it *looked done* — the existing `COALESCE` dropped the sentence cleanly. What remained
was *"Live inflow 1/24h vs outflow 1263/24h"*: every number TRUE, and read alone it says the pile is
draining briskly. **The reader reaches the same false conclusion from facts instead of from a claim.**
So "we stopped saying the wrong thing" is not the exit condition; **"a reader cannot conclude the wrong
thing" is.** Three states — draining · stalled · never-flowed — and the middle one has to be SAID.
Promoted to the honesty canon as the inverse of the empty-state rule: **silence is not neutrality when
the surrounding context is directional.**

**Two reds on `main` were mine, and both are worth naming.** A duplicate register ID (`R84`) that two
sessions took independently — git merged both cleanly because the rows are different lines in different
sections, so **a rebase reporting no conflict is not evidence there is no collision**, and the red
surfaced on the OTHER session's commit. And a migration whose `REVOKE` named only `public` — the rule is
in CLAUDE.md, enforced by a guard I never looked for because I had grepped for guards reading the ledger
and the register but not `supabase/migrations/`. **Not a live exposure** (`CREATE OR REPLACE` does not
reset an ACL; anon read false before and after), and the `anon-exec: intentional` marker was deliberately
NOT used — it would have satisfied the guard with a false claim.

**A near-clobber avoided by diffing rather than choosing.** A stash-pop offered a version of `R78`
**667 characters shorter** than upstream's, on a row I had not touched. ⚠ The comparison itself nearly
misled me: `awk length()` counts BYTES, python `len()` counts CODE POINTS — the same line measured 3,672
and 3,620.

**Verification discipline that paid.** Every guard was made to FAIL before being trusted (dropping the
stall guard, `<`→`<=`, widening the window; un-suppressing `mcp_query`, emptying the walk). The backlog
watch was closed by the **real caller** — the pg_cron tick at 18:29:00.064Z, not a manual run. And
`check_cursor_stall_threshold_drift()` returning **one row** was nearly reported as a finding: it is the
jsonb-array shape holding `[]`, i.e. clean. **Read the return TYPE before interpreting the count.**

Shipped: register **R83** and **R85** both closed; four migrations; two new CI guards; a re-pin
(pin + migration + guard registration). Left open on purpose: whether a stalled 42k backlog deserves
severity above `info` — a product call, Trevor's.


## 2026-09-03 (early hours PT) · five ships, and three of them came out of verifying the previous one

An autonomous overnight run. The through-line is that **each fix was found by checking the one before
it**, not by a fresh sweep — which is the cheapest source of real defects this session has found.

**The pack-detail header was quoting a fourteen-day-old severity as if it were today's.** 124/86/26
users became **6/13/1** on the same grouping — a 7–20× drop, and the remainder is the honest-degradation
path *firing* rather than the hang it replaced. ⛔ And the obvious follow-up is measured dead:
`get_pack_lifecycle` runs **5–32 ms** warm over a deterministic hash sample against a **5,000 ms**
budget, so an overrun is instance contention (R46), not the query. An afternoon avoided.

**The intermittently-red `edge-deno` job was wall-clock dependent, and the fix overturns a filed
decision.** supabase-js 2.113.0 aged past Deno's 24 h minimum-dependency-age at 06:04:34Z; its own
auth-js at **06:05:01Z**. For those **27 seconds no resolvable graph existed**, and the run inside them
was a hard error. ⚠ **Tuning the threshold cannot fix it** — any non-zero age has a boundary and the
sub-package skew straddles it wherever it sits. `.gitignore` said a lockfile was *"deliberately NOT
committed … introduces a lock-mismatch failure mode"* — **a cost stated with no number in it**, and the
job does not run `--frozen`, so a drifted lock is rewritten rather than rejected. ⭐ Measured both
directions on a re-runnable control: no lock + an 11-h-old version required exactly is a hard error;
the same version reached through a **floating `^3`** range but present in a lock resolves under the
default policy. The lock had to be generated **by CI itself** — jsr.io, deno.land and esm.sh are all
403 from the sandbox — and was byte-verified against that output.

**E5 pass ten — two markers, and the third candidate was rejected on re-derivation.**
`panini-ingest` at **67% of a 60,000 ms wall** (3,501 runs) and `evm-transfers-ingest` at 44% with a
p90 of 25,536 ms. ⛔ `admin/backfill-offer-fill-sales` ranked next at 27% — but its `after()` path is
the LEGACY one, so that maximum belongs to the path that *cannot* be killed at the wall. **Rank by
margin, then check which path produced the number.** Below it everything is ≤20%, so the wall-margin
tier is exhausted.

**⭐ The one that matters most: the IPFS proxy was killing transfers it had already won.** One
production request carries the whole diagnosis — a **200**, `[ipfs-media] ok … elapsedMs=6037`, and then
**four uncaught `TimeoutError`s**. `AbortSignal.timeout(8_000)` starts at fetch time and stays live for
the response body, so a transfer whose headers arrived at 6 s was aborted mid-flight at 8 s, after the
success log had gone out. **426 of them across 60 users in 24 h.** ⚠ And it was invisible at the one
place that looks: the `ok` line records the DECISION to stream, not the outcome. Fixed as two phases on
one controller, plus a counting `TransformStream` so `ok` **with no** `streamed` is the signal — read by
correlation, exactly like the pipeline heartbeat marker.

**Then verifying THAT produced the fifth.** A probe returned `reason=transport name=Error` at
**7,800 ms** — which is either a real fault 200 ms inside the deadline or **our own abort with its
reason discarded by the runtime**, and those need opposite responses. The timing says the first (every
real abort in the logs sits at 7,982–7,999 ms), but ⚠ **the name stopped being a safe discriminator the
moment the code left `AbortSignal.timeout` behind**: a manual `controller.abort(reason)` preserves the
reason only in some runtimes, and this route ships to the edge. Now classified off
`controller.signal.aborted` — the route's own state, which no runtime can reinterpret.

⚠ **Two tests were pinning spellings rather than properties, and both were rewritten rather than
deleted.** The ipfs stubs used `body: "binarydata"` — a string, where `Response.body` is a stream in
every runtime this route ships to, so they passed while exercising a shape production never sees. And
"names an ABORT distinctly" rejected with a `TimeoutError`-named error and **never aborted anything**,
so it would have gone green on a route that called every failure an abort.

### The second half — and the pattern held: every one of these came out of checking the last

**The whole class, swept rather than the two files that failed.** 146 files bound a `fetch`; the subset
where a signal can kill a *response body* is a route that returns an upstream `.body` as a Response, and
a comment-stripped walk finds **exactly two**. The second, `avatar-media`, had the same shape unfixed —
plus a defect of its own: **every failure branch returned 502 with no log at all**, so *"our bound
fired"* and *"the host answered"* were spelled identically. That is the ambiguity that left ipfs-media's
soft-fail path unreachable dead code for months, and this route had inherited that file's TIMEOUT lesson
(its constant cites it) and not its OBSERVABILITY one. Population now zero, so it is a **ban**.

⛔ **And two sibling proxies had the half-bound version:** `badge-image` and `moment-thumbnail` read
their bodies with `await upstream.arrayBuffer()` **after** the try/catch closes. No live instance
claimed — they buffer, and the objects are small — and the code says so rather than implying a count.

**⭐ THE BIGGEST FIND CAME FROM VERIFYING A HEARTBEAT I HAD JUST ADDED.** The Vercel error table showed
`Task timed out after 60 seconds` on `cron/evm-transfers-ingest`, and the DB showed that tick with a
marker, a terminal row, `ok = true` and `duration_ms = 60,464` against a **60,000 ms** wall. **The
platform killed it and the correlation is clean** — the terminal write raced the wall and won. So
`heartbeat + terminal row -> ran to completion`, in this repo's doctrine since 08-20, is too strong: the
test answers *"did a row LAND"*, never *"did the invocation SURVIVE"*.

**Following that thread produced the night's best single fix.** Grouping Vercel's timeouts by
`requestPath`: **`wallet-backfill-golazos` 6 in 24 h — more than every other route on the platform
combined.** It uses `after()`, so a kill writes nothing; and it was invisible to the heartbeat ratchet
**by construction**, because that guard reads each ROUTE FILE for a `pipeline_runs` write and this
route's terminal write is one delegation away in `lib/chains/flow/wallet-backfill-helpers.ts`. Five
routes were outside it.

🚨 **Fourth instance of "a guard scoped to where the bug was found", and the instructive one — it had no
glob at all.** That ratchet went out of its way NOT to use a path convention; its header argues that
*"routes under `app/api/cron/` is a curated list wearing a glob"*. It was still scoped to the past,
because the structural predicate it chose asked about **the text it was reading** rather than the set
where the property is meaningful.

⭐ **And the wall was inherited, not chosen.** `1791e9083` created all four wallet enrichers at
`maxDuration = 60`; `d57349c5a` raised AllDay to **600** two days later and the others followed — this
one did not, then took on AllDay's own runner. Siblings today: 600 / 600 / 300, **zero kills between
them**. The pre-change distribution settles it: **max 59,801 ms of a 60,000 ms wall — 0.997 — over 1,621
runs**, p99 already at 78%. A maximum at 99.7% of the ceiling is the censored maximum in its purest
form.

**Then the instrument the doctrine correction asked for, step 1 of 3.** `lib/pipeline/route-walls.ts`
maps each route to its own `maxDuration` — 116 pairs — because the kill classifier had no per-route wall
to compare a duration against. ⛔ The cheap substitute (guess walls from round numbers) is **refuted
three of three**: the clustering above a round number is each route's INTERNAL budget *working*.
⭐ **The first sweep diagnoses its own errors:** a route cannot record a run much longer than its wall,
so `refresh-pack-grail-metrics-mv` at **2.7×** is the map failing — and migration `20260829235752` says
by name that the refresh **moved to pg_cron**, which the sweep spotted without being told.

⏳ **Owed, all three traffic-gated rather than broken** — the site was quiet from 00:40 PT:
`[ipfs-media] streamed` (no organic requests since deploy), `wallet-backfill-golazos-heartbeat` (its
last burst was 07:39Z, before the deploy) and `panini-ingest-heartbeat` (its caller is the residential
runner, idle). ✅ `evm-transfers-ingest-heartbeat` **is** verified live — five markers, all correlating.

## 2026-09-03 (nightly pass, cloud NO-PUSH) · a quiet honest night

Genuine overnight (01:02 PT, no clock skew). Push unavailable in the cloud runner (harvested pushurl
has no usable cred — Trevor's box + Claude Code push normally). Health GREEN: security clean ×4,
`detect_stalled_pipelines` [], the only trust breach the known declining structural
`unmapped_resolution_backlog_max`=209. The one new `medium` alert (`allday-pack-opens-backfill`) was a
90-min-window artifact over transient upstream Flow status-0 scan errors — interleaved ok ticks write
rows; benign. Accuracy gate checked and stable (per-day recompute HIGH/MED ratio 70–77% over 5 days;
the −194 standing HIGH+MED move is MEDIUM→LOW churn, not staleness). No artifacts flagged broken. Every
fresh inbox candidate is code/route/measurement work (off-limits under NO-PUSH) or already handled by
today's Claude Code session. NOTHING SHIPPED — continuity written to the mount, uncommitted.

## 2026-09-02 (cont.) · four re-derivations, and every one changed what the register said

A pass over the open register and the instrument fleet. **Nothing in this batch was a code fix — all
four were measurements that moved a filed conclusion**, which is the register's own stated purpose and
the thing that decays fastest.

**R29 — the three obvious levers for `job startup timeout` are all dead.** 13Z runs 117 timeouts in
1,171 ticks (10.0%) while 13 of 24 hours run zero, and ⭐ **the launcher's tick volume is flat across
all 24 hours**, so the band is not "more jobs firing". Re-staggering minutes is real but **2.3× against
a 15× hour effect**. Moving the victims is impossible — the top five run every 2–5 minutes. And the
load story **did not survive its own control**: the app-side proxy said independence outright, then the
DB-side control refused the tidy version, because **12Z is the second-heaviest hour on the instance
with two timeouts while 9Z is mid-range with sixty-five**. Load is neither sufficient nor necessary.

**D25 — the row was right and a population 37× larger had never been counted.** At the row's own grain
the count is 2,353 against its 66; split by key it is **62 on base editions — which IS the register's
number — and 2,291 on `::N` parallels**. ⭐ **For 895 of 895 offending parallel keys the largest serial
fits inside the BASE edition's circulation, zero exceptions**: the row carries the base moment's serial
and the denominator beside it is the parallel's run. Not corrupt data — a unit mismatch in the render.
⛔ And the obvious remedy is dead: copying `editions.circulation_count` over `wmc.mint_count` **fixes 2
of 899 keys.**

**R78 (new) — the two permanently-red workflows are one problem.** `edge-fn-drift` is red 30/30 **by
design** (5 drifted functions, 0 safe to redeploy, each with a dated reason), and the sentinel's
CRITICAL comes from **exactly one check** — Detector Health, counting that streak. ⭐ **The arm for
this hazard already existed and was firing correctly; nobody had joined the two badges.**

⛔ **And I corrected myself on it twenty minutes later.** I wrote that the two warns underneath "both
look like real work" — an inference from alert text. Measured: `allday-pack-opens-backfill` had
**self-cleared** (71 runs / 65 ok / 63 writing rows) and the unmapped backlog is `info`, draining at
**outflow 4,672 vs inflow 41 per day**. The finding survives; the colour I put on it did not.

⭐ **The pattern across all four: the mechanism was right every time and the ARITHMETIC or the GRAIN
was wrong.** ±5 s vs `>` earlier in the session; hour vs minute in R29; base vs parallel in D25; alert
text vs the numbers in R78. **None of these were caught by re-reading the filing — only by re-running
it with a control.**

ⓘ **A guard written this morning caught a real regression this evening.**
`api-error-copy-reaches-the-user-not-the-code` went **5 → 0** on `87a173c40`, because a concurrent
session wrote back a stale copy of `DashboardClient.tsx` and silently reverted the `apiErrorMessage`
fix. Restored in `4c6353c08`. ⚠ **That is the THIRD stale-copy clobber in one day** — the inbox
correction, the ledger, and now product source — and the only one caught by a machine was the one with
a count assertion pointed at it.

## 2026-09-02 (cont.) · the obvious spelling of a correlation manufactured three kills, and only live data said so

Closed R11's structural residual: `detect_stalled_pipelines()` keyed on `max(started_at)` over a
pipeline's own terminal rows, so it could report silence and could not report **why** — and "the
schedule stopped" and "the route is killed at its wall before it can log" produce an identical
signature there while needing opposite responses. The evidence had existed since 2026-08-20 (the
`<pipeline>-heartbeat` marker, now on 21 of 87 watchlist rows) and nothing read it.

⭐ **The lesson is not the fix, it is the first draft.** Counting a marker as orphaned when
`started_at > last_run` is the obvious spelling and it is wrong: run against production it named
three pipelines as having orphaned markers, **all three healthy**. Their marker lands **2–14 ms
after** the terminal row's `started_at` — the two timestamps come from different clocks inside the
route — so a tick was counting its own heartbeat as evidence of its own death. With the ±5 s window
`lib/pipeline/heartbeat.ts` already documents: **3 → 0**.

Nothing about the mechanism was wrong. The arithmetic was, by 14 milliseconds, and a fixture would
never have contained that skew — it only exists because a real route writes two timestamps from two
places.

⚠ **A counter that reads zero has to be shown reading nonzero.** The same correlation over the full
73 h retention finds exactly one uncorrelated marker fleet-wide: `fmv-recalc`, 2026-09-01 00:15:52Z.
A real killed tick, invisible to every alert on this platform, and the counter sees it.

ⓘ The equivalence was proved the way this repo requires — one statement, one snapshot, the whole
population with the silence threshold forced so all 87 rows qualify: 87 vs 87, zero either way. Both
added LATERALs are unGROUPed aggregates, so the row set *cannot* move; the enrichment can never
become a silencer.

ⓘ And the sandbox CAN run the DB-invariant suite: `/usr/lib/postgresql/16/bin` is present, `initdb`
refuses to run as root, and `su postgres -c ...` into a scratch datadir works. 183 files green.

## 2026-09-02 (cont.) · the ratchet that drove a 15-route workstream is blind to the property it exists to enforce

Advanced register E5 (BUDGET 40 → 37) by converting the three `after()` routes with the tightest
measured margin against their own walls — `cron/allday-lock-refresh-batch` (**71 of 73 ticks between
270,077 ms and 292,225 ms against a 300,000 ms wall**, worst 97.4%), `wallet-backfill` (watchlist
severity HIGH, max 87%) and `cron/populate-pinnacle-wmc-fmv` (max 77%, one RPC call).

⭐ **The finding is not the conversions.** `after-route-heartbeat-ratchet` has driven this workstream
since 2026-08-20 and is a STATIC read: it asserts the route CALLS `writeInvocationHeartbeat`. That is
satisfied by a call placed anywhere in the callback — **including after the work, where the marker
cannot survive the kill it exists to record.** The whole contract is the ORDERING, and nothing in the
suite was asserting it.

Proved rather than argued: moving the call after `runBatch` reddens the new ordering tests and **the
ratchet stays green**. It is the shape this repo keeps paying for — *a guard that asserts the PRESENCE
of a thing rather than the PROPERTY that makes it work*.

⚠ **Two separate recorders cannot see an order between them.** The shared route harness records
`rpcCalls` and `writes` in different arrays, so it could not express "the marker came first" at all;
the pins wrap `from().insert` and `rpc()` into ONE interleaved log instead.

⚠ And an arm written `if (firstWalk >= 0) expect(...)` was corrected before commit: a conditional
assertion goes vacuous the moment the fixture stops reaching that branch, while still reading as
coverage.

## 2026-09-02 (cont.) · the inbox's content had no watcher, and the detector's two failed designs were both this repo's own lessons

Two sessions edited one inbox filing twenty minutes apart. The second rewrote it in place from a copy
read before the first landed, deleting an 84-line `## ⛔ CORRECTION` block — the block refuting the
filing's headline number. A human reading the diff caught it (`4ed563071` destroyed it, `2f1263240`
restored it). **Nothing in CI moved, and the response was a memory note.**

🚨 **All three existing inbox guards passed and none COULD have failed.** They ask which filings exist
and how many; a rewrite that keeps the file keeps the count. That is the ledger's own *diff the SET,
not the count* lesson (`2966c0a`, 356 → 356) sitting unlearned one directory over.

⭐ **The part worth keeping is the two designs that failed, each refuted by the REAL commits rather
than by invented markdown.**

| design | on the incident | on the repair |
|---|---|---|
| **v1** — every `## ` heading before must exist after | caught | **false-positive** — the repair legitimately RENAMED two sections while merging |
| **v2** — the after-file must still hold SOME correction heading | **missed** — the clobbering commit added its own correction, 1 → 1 | clean |
| **v3** — content fingerprint | 1 | 0 |

v1 is the 2026-08-22/23 shape where the ledger guard **punished compliance** with its own future-date
arm. v2 is count-blindness reproducing *inside the guard written about count-blindness*. Neither
would have been visible against synthetic fixtures: both look correct on paper, and both were killed
by three commits that already existed.

⚠ **Validating a CI guard by reading it is not validating it.** The whole step body was extracted from
`ci.yml` and executed in a throwaway clone at each real commit — the incident exits 1, the repair
exits 0, an unrelated push exits 0 with "nothing to compare", a deleted filing exits 1, the
`[inbox-correction-retracted]` escape exits 0, a missing detector exits 1. Two bugs only that found:
a `… | while read` loop runs in a **subshell**, so the counters were discarded at the `done` and the
step would have reported `inspected: 0` whatever it found; and the removal case printed the CLOBBER
remedy, which cannot work for a deletion — **an error whose remedy fails costs the guard its
authority**, so the remedy is now gated on the failure it fits.

ⓘ Corrections only, deliberately: highest value, lowest churn, and *"the correction vanished"* has no
legitimate cause. Everything else in a filing stays free to be rewritten, which is what keeps the
guard from being one people switch off.

## 2026-09-02 (cont.) · two "raw slug reaches the user" findings, two DIFFERENT correct answers

Drained the last code item of the QA handoff's #9: `DashboardClient` threw `data.error`, so a user who
hit the wallet cap saw the literal string `plan_limit_reached`.

The route answers that rejection with BOTH a machine code and human copy — *"Free plan supports 3
saved wallets. Remove the wallet you have saved, or upgrade to RPC Pro."* — plus an `upgrade_url`.
⭐ **The sentence someone wrote for exactly this moment sat unused in the same response**, and the
actionable half never appeared at the one point it was relevant. The copy existed, was correct, and
reached nobody.

⚠ **The handoff named two line numbers; the expression was at five.** Fixing only what was reported
is how a class survives its own fix, so the guard asserts the count is **5** rather than asserting
that "the reported one" changed.

⭐ **The part worth keeping is that this and the `/login?error=` fix an hour earlier LOOK like the
same defect and are not.** Both present as "a raw slug reaches the user". But:

| | source of the value | correct fix |
|---|---|---|
| `/login?error=` | the QUERY STRING — attacker-controlled | allowlist; never render an unknown value |
| `DashboardClient` | our own API's RESPONSE BODY | restore the DISCARDED field |

An allowlist on the dashboard would have been wrong (most routes legitimately put human text in
`error`); preferring `message` on the login page would have been wrong (there is no `message`, and
the value is hostile). **Matching on the symptom would have produced one right answer and one wrong
one.** The discriminator is where the string came from, and it is invisible from the rendered output
— which is the only place the two findings were ever compared.

ⓘ The no-change control mattered here more than usual: without it, "prefer `message`" could have
silently downgraded every route that sends only `error` — a regression dressed as a fix, in the same
commit that claims to improve error copy.

## 2026-09-02 (cont.) · the same disguise, twice in one session, in unrelated code — which is the finding

Took another P2 from the QA handoff: *"`/auth/confirm` ignores its `?redirect=`"*. The functional half
is exactly as filed — `/api/auth/request-magic-link` already carries the login page's `next` into the
emailed callback, and `AuthConfirmClient` never read it, hard-coding `router.replace("/")`. It only
LOOKED fine because `/` bounces a signed-in user to `/dashboard`, so the common case worked and every
deep link lost its destination silently.

🚨 **The half that was not filed:** the only sanitiser in the chain was `redirect.startsWith("/")`.
That is not a same-origin test — `//evil.example/x` starts with `/` and resolves to
`https://evil.example/x`. The shape was already baked into the emailed link, so honouring
`?redirect=` without fixing it would have **completed** the exploit: a link that genuinely is ours,
genuinely signs the victim in, and then lands them on someone else's page already authenticated and
primed to trust it.

⭐ **This is the SECOND time tonight I have found `startsWith("/")` used as a security check** — the
first was trophy-slab art a few hours earlier, in unrelated code, reached by a completely different
route (an OG-layer sweep, not the handoff). Two independent discoveries of one shape in one session
is not a coincidence to note in passing; it is the finding. **The lesson is not "remember to check
for `//`" — that is the fix. The lesson is that `startsWith("/")` READS like a same-origin check and
is not one**, which is precisely why it survives review, and why both instances sat in code that had
otherwise been carefully written.

⚠ **The fix had its own trap, and a test caught it rather than production:** `params` inside that
effect is the HASH — Supabase's implicit flow puts the token in the fragment — while `?redirect=` is
a QUERY param. Reading it from `params` would have been silently always-null and the whole fix inert
while every test still passed. It reads `window.location.search` instead, which also sidesteps the
Suspense boundary `useSearchParams` would have required.

ⓘ **Twice tonight my test asserted something the code got right, and I kept the correction in the
test.** A trailing CRLF is `.trim()`ed to a valid path, and a space is not a control character — it
cannot split a header and a browser percent-encodes it. Banning either would reject a legitimate
redirect for no security gain. Recorded in place so the next reader does not "fix" them back.

## 2026-09-02 (cont.) · the handoff filed it as polish; the mechanism was a phishing surface on the login page

`docs/handoff-2026-09-02-onboarding-trophy-case-qa.md` #9 lists, among a batch of P2 polish, *"raw
error slugs reach users: `LoginClient.tsx:36-42` shows `auth_failed` / `missing_token` /
`session_failed` verbatim"*. The slug half IS polish. The mechanism underneath it is not, and reading
the code rather than the finding is what surfaced it.

`LoginClient` mapped two known values and **fell through to the raw query-string value for everything
else**. That value is attacker-supplied. So
`/login?error=Your+account+was+locked.+Call+555-0100+to+restore+it` rendered that sentence inside our
own error banner, in our voice, on the login page. React escapes it — this is not XSS — which in this
particular spot makes it worse, not better: no browser warning, no broken markup, just Rip Packs City
appearing to tell someone their account is locked, on the one page where they expect exactly that
kind of message.

⭐ **The defect was never the ugly slug. It was that an UNKNOWN value was rendered at all** — which is
why the fix is an allowlist with a generic fallback and not a prettifier. A prettifier would have
satisfied the filing and left the vector open.

⛔ **AND AN EXISTING TEST WAS PINNING THE DEFECT, with its own counter-example one case above it.**
`component-LoginClient` had:

- *"allowlist_unavailable is translated, never echoed as a raw code"* — asserts the right property
- *"forwards an /auth/confirm failure code into the inline error"* — asserts
  `getByText("session_failed")`, i.e. **requires** the raw slug to reach the user

Two adjacent cases asserting opposite things about the same banner, and the wrong one was written to
sound like a feature. Inverted rather than deleted, per the standing rule: the property it is named
for (a confirm failure reaches the inline error) is unchanged; only the spelling moved.

⚠ The guard includes a source walk over `AuthConfirmClient` and `api/auth/callback` that extracts
every `?error=` value they set and fails if one is missing from the map — asserting the walk found ≥3
so it cannot pass vacuously. A slug added later without copy degrades to the generic fallback, which
is safe but vaguer than it needs to be; this makes that visible instead of silent.

## 2026-09-02 (cont.) · took the handoff's #7 and the re-derivation was worth more than the fix

`docs/handoff-2026-09-02-onboarding-trophy-case-qa.md` #7: *"Trophy pins are entirely
client-asserted"* — `POST /api/profile/trophy` upserts `playerName`, `fmv`, `serialNumber`, `tier`,
`thumbnailUrl` straight from the body, so "a campaign that says 'show off your case' invites a
$1,000,000 LeBron #1/1 on someone's public page within a day." Recommendation: resolve every display
field server-side.

⛔ **True of the WRITE path, and ~80% false of the product.** `get_trophy_slab_data` renders public
slabs with `COALESCE(e.<field>, tm.<field>)` and `COALESCE(f.fmv_usd, tm.fmv)` — the live `editions`
and `fmv_snapshots` values WIN, so a forged player, set, tier, circulation, video, FMV or badge list
is overridden the moment the edition resolves. ⭐ **Reading the handler says twelve fields are
forgeable. Reading the RENDERER says two are.** Implementing the filing as written would have been
mostly redundant work on the most-shared surface in the product.

**The two that are real**, because they are not COALESCEd: `thumbnail_url` — rendered publicly AND
fetched server-side by `/api/og/profile/[username]`, which inlines trophy art as data URIs, so an
arbitrary value is an arbitrary image on a public page *plus* a server-side fetch of a chosen host —
and `serial_number`, because "#1 of 15,000" is the entire claim a trophy makes.

⭐ **The allowlist is derived, not guessed.** Seven hosts straight out of `editions.thumbnail_url`
with their row counts, plus the same-origin `/api/public/pinnacle-image/…` proxy that a host-only
list would have rejected and that one live row uses. A guessed list silently blanks the art of
whichever host it forgot, and a blank slab explains nothing.

⚠ **Said plainly rather than implied: this is not an incident.** 19 rows, 7 users, every thumbnail
legitimate. ⭐ **And the no-change control is the part I would want a reviewer to check:** all 19 pins
resolve in `wallet_moments_cache` and every stored serial ALREADY agrees with the indexed one — so
the change alters no live value, *and* the new read is demonstrably not a dead path. Both halves
matter; either alone would leave the fix unproven in a different direction.

**The mutant worth remembering** is the protocol-relative URL: `//evil.example/x.png` resolves to
`https://evil.example/x.png` in a browser, so the obvious "starts with `/` means same-origin" check
waves it straight through. That is why the same-origin branch is a PREFIX test on
`/api/public/`, not a slash test — and why the case is in the suite rather than in a comment.

## 2026-09-02 (cont.) · drained the QA handoff's P0 — and the fix was already in the tree, one route over, for the fourth time today

`docs/handoff-2026-09-02-onboarding-trophy-case-qa.md` is a real new-user walkthrough that shipped
nothing and handed off findings in campaign-priority order. Took **#1 (P0)**, re-deriving it first per
the standing rule: both of its numbers hold (`wallet_usernames` = **9,370**, exactly as filed).

`POST /api/profile/resolve-and-associate` called `resolveTopShotUsername()` — Top Shot's public GQL
and nothing else — so during an outage every username signup 502'd, including for the 9,370 handles we
already had. The filing's 72 h counts of `Top Shot GraphQL failed with 530` were `/api/wallet-search`
243, this route 43, `offers-sweep` 108: intermittent for days.

⭐ **`resolveTopShotUsernameCacheAware` — four DB cache layers, live GQL last, write-back on a live
hit — was already in the tree, used by the ANONYMOUS home analyzer**, and the filing confirmed it
resolving `jamesdillonbond` during the same outage this route was failing on. The signed-in path, the
one the hero copy leads with, had the worse implementation. **Fourth "a correct sibling is not a
guard" today**, after `fetchFmvBatch`, `slabsError` and `og/profile`. Four in one session is not four
coincidences — it is what happens when a fix is applied where the bug was reported instead of where
the SHAPE lives.

**The handoff didn't name the second half, and it is the worse one.** The live resolver returned
`null` for both "Top Shot says no such user" and "we couldn't reach Top Shot", so a cache miss during
an outage rendered as **"Couldn't find that Dapper username"** — sending a real person away believing
their own handle was wrong. The outcome union carries the reason, so those are now 404 and 502. The
honesty canon's *a failed read is not an empty result*, on a signup form.

⚠ **And I added the mirror image rather than inheriting it:** `empty_username` now returns the route's
existing 400. Reporting the user's bad input as OUR outage is the same error pointed the other way,
and a whitespace-only handle reaches that branch because the guard above only rejects a missing field.

⚠ **The source-level pin is what makes the fix testable at all.** The resolver is mocked in these
tests, so a route reverted to the live-only function passes every behavioural case. Without an
assertion on WHICH resolver is called, the guard would have been green over the bug.

## 2026-09-02 (cont.) · a social card was 16.4x low on a dollar figure, and the two halves of the bug were individually invisible

Swept the OG layer — CLAUDE.md's fourth honesty layer, and the one I had not looked at. It came back
almost clean: `og/profile` and `og/share` both carry an explicit `ok`/`fetched` flag with comments
explaining why, and the `?? 0` sites elsewhere are per-row field defaults inside successful reads, not
population zeros. One card was not clean.

`/api/og/insights/panini-squeeze` published **"$143,849 still sealed in packs" beside "4,813
editions"**. The true sum is **$2,358,840**. It read
`.select("sealed_fmv_exposure_usd", { count: "exact" })` with no bound and summed in JS: PostgREST
caps an unbounded `.select()` at 1,000 rows and reports no error, while `count: "exact"` is a real
`COUNT(*)` over the whole view.

⛔ **Neither half is visible on its own.** The count is right. The sum is a correct sum of the rows it
was given. Nothing errors — supabase-js resolves rather than throwing, and the cap does not even set
`error`, so the read SUCCEEDS and returns a fifth of the table. **It is the PAIRING that lies:** an
exact count next to a truncated sum reads as one measurement, and on a social card the number travels
without the page around it.

⭐ **The lesson of the evening arrived again in the guard, twice.** A mutant that deleted the
deterministic `.order("id")` SURVIVED my first test, then survived the fix to that test:

1. First pass: the stub sliced its array positionally regardless of ordering, so the missing order
   was unobservable.
2. Second pass: I modelled physical order by rotating the table between calls — but the route calls
   `.from()` once **per page**, so a counter scoped inside `from()` reset every page and the table
   never appeared to move.

**Both times the guard looked right and measured nothing.** And the case only works with DISTINCT
per-row values: with a constant value the overlaps and omissions cancel exactly, so a wrong row SET
produces the right SUM. That cancellation is the whole reason unordered `.range()` paging survives
review — it is invisible to every count-based check, and it was nearly invisible to a test written
specifically to catch it.

⚠ **And `og/profile` is the third "correct sibling" today.** Its own comment records this defect —
*"a failed `saved_wallets` read returned `[]`, `totalFmv` reduced to 0, and the card published $0 as
that person's portfolio"* — fixed there 2026-08-13, while the profile PAGE rendering the same two
numbers kept the bug until tonight. Someone fixed the share card and never checked the page it
shares.

**Sweep closed with a number:** 59 files use `count: "exact"`; **0** now pair an unbounded `.select()`
with a JS `reduce`.

## 2026-09-02 (cont.) · the fifth honesty layer, found by sweeping SEEDS rather than FILES — and the file-level sweep was worthless, which is the transferable part

Swept `app/` for server-seeded props (`initial*={…}`) looking for the class CLAUDE.md describes as the
one its four-helper table does not cover: a seed that arrives as `[]` on a failed read carrying no
provenance.

⛔ **My first sweep flagged six files and was wrong about all six.** It asked "does this FILE contain
failure vocabulary" — and `player/[slug]`, `set/[slug]` and `series/[slug]` all gate their seeds with
`editionsOk` + `SectionUnavailable`, a spelling my regex didn't know. Widening the vocabulary then
produced **zero** offenders across all 32 seeded pages, which is the real finding: **every server page
that seeds a client contains some failure vocabulary, so a file-scoped grep cannot discriminate at
all.** The property is per-SEED. Both defects I eventually found are in files that handle failure
correctly a few lines away. ⭐ **"Fix per PANEL, not per page" has a sweep-side twin: SEARCH per
panel, not per page** — and my own guard nearly repeated the exact "assert the property, not the
spelling" mistake it exists to catch.

**What that turned up.**

1. **`/profile/<username>`** — `page.tsx` computed `result.ok` and **discarded it**, seeding
   `initialWallets={[]}` on failure. The KPI tiles render `PORTFOLIO FMV —` / `MOMENTS —` and the
   saved-wallet list does not render at all when empty, so a collector who added six wallets sees a
   page with none. ⚠ It is the rendering **this page's own header records as the 2026-06-12 audit
   finding**, re-created for the failure path once the SSR seed fixed it for the loading path. ⭐ And
   `slabsError` — the identical distinction, with a comment explaining why it was necessary — sits
   **twenty lines below** the wallet fetch that lacked it. A correct sibling is not a guard, at
   twenty lines.

2. **Seven public `/insights` boards** stamped `Updated <render clock>` unconditionally, each one
   directly above a degraded notice it already passed correctly. "Updated just now" over a board that
   failed to load is the fabricated-number family applied to a timestamp.

⚠ **The test had to be `renderToString`, and mutation proved it:** ProfileClient refetches on mount,
so in jsdom both `useState(true)` and `useState(false)` leave every client assertion green. Only the
SSR HTML — what crawlers, unfurls and the pre-hydration paint get, and what ISR caches for 300 s —
carries the difference.

⭐ **Mutation found a defect in my own fix that no test I had written would have.** Removing
`setProfileError(false)` from the success path survived everything: the notice would have persisted
on a profile that had loaded perfectly, because I had only tested that failures show it, never that
successes clear it. **Over-correcting is its own defect and needs its own case.**

**And a third instance came from reading the logs for the fix I had just shipped.** Production at
00:12Z: `AD FMV map size: 6190` — my new read, healthy — immediately followed by
`get_allday_sniper_deals error: canceling statement due to statement timeout`, a different read
reached only because the All Day GraphQL is 403ing. That path already named its source (I had added
that hours earlier), but its return stamped `lastRefreshed: new Date()`, and `SniperStatsBar` renders
"updated <time>" from exactly that field — so the degraded, empty board claimed it had just been
refreshed. ⭐ **Verifying a fix is a better finder of the next defect than looking for one**, and the
same freshness shape turned up in three unrelated places in one evening once I knew to look for it.

## 2026-09-02 (cont.) · two sessions measured the same swap and disagreed by 15× — both harnesses were wrong, and the real number is the one that justifies finishing the job

A concurrent session shipped `get_editions_latest_fmv()` for the concierge with a headline of **249×
fewer buffers**. I had, hours earlier, shipped the sniper-feed read that CHUNKS AT EXACTLY 500 — the
size their "before" arm measured as catastrophic. Two possibilities: my fix was a regression, or one
of the measurements was not measuring production. Testing the curve was not optional.

**Neither number was production.** Cost is linear in the id count in every pushed-down form (40 /
200 / 500 ids → 2,894 / 14,624 / 35,852 buffers, ~72 each), so **chunk size is a round-trip decision,
not a cost decision** and my fix was not a regression. But their 1.33M was reproducible — from a
`WITH ids AS (… ORDER BY external_id LIMIT 500)` + `IN (SELECT id FROM ids)`, which the planner
serves with a **hash** semi-join over the fully materialised view. Change that one ORDER BY to `id`
and the same query is a **merge** semi-join at 152,869 buffers. 8.7×, one word. And PostgREST sends
neither: `pg_stat_statements` (`query LIKE 'WITH pgrst_source%'`) shows `edition_id = ANY($1)`, a
bound array that becomes an index condition with no join to pick a strategy for.

⭐ **The lesson is not "they were wrong."** My own half was the mirror image: I had written
"`IN (<literal uuids>)` ← **what PostgREST sends**" into `database.md` as a table row, and never
checked. It happens to plan identically, so the conclusion survived — but **a claim that is right by
luck is indistinguishable from one that is right by measurement until someone tests it, and the cost
of the difference lands on whoever inherits it.** The instrument for this took one query, against a
table that was already recording the answer.

**The correction that mattered more than the ratio.** Both of us framed this as pushdown/no-pushdown.
The real mechanism: `edition_id = ANY(…)` DOES reach the partitioned index and STILL costs ~70
buffers per edition, because the view has **no per-group LIMIT** — it reads every snapshot row for
each edition (~35 and growing) and `Unique` discards all but the newest. The LATERAL's `LIMIT 1`
costs ~4. 👉 **"Reaches the index" is not "is cheap."** Under the pushdown framing, a read keyed on
the DISTINCT ON column is *done*; under the right one it is 17× from done. That framing is why my
own sweep bullet the same morning called every keyed app caller **"Clean"** — a word that closed a
question the measurement had not answered.

**Shipped on the back of it.** `/api/sniper-feed`'s All Day leg moved to the RPC: 424,475 → ~26,000
buffers per request. The earlier fix took it from ~1.84M to 424k and looked finished. ⭐ **A fix that
is correct, measured, documented and 4× can read as complete when it captured under a quarter of the
win.**

**And the guards were vacuous in the way this repo keeps re-learning.** Both pins for that read
asserted `.from("fmv_current")` and `.range(` against the WHOLE FILE. The All Day leg stopped using
the view — and both still matched, on the Top Shot leg and the jersey read. A case *titled* for All
Day would have passed with the All Day read deleted. ⚠ **A file-scoped assertion about a function is
a vacuous assertion waiting for the function to move.** Both now slice `computeAllDaySniperFeed`; the
chunk pin asserts the VALUE is ≤1000 rather than the constant's spelling, because raising it to 6,190
would satisfy every other pattern and silently truncate.

**Then the correction paid for itself twice.** Getting the mechanism right — *a qual on the DISTINCT
ON key becomes an index condition; anything else builds the whole view* — gave a property to sweep
for that the pushdown framing did not. Two live instances, in code a sweep the same morning had
cleared:

- **`/api/overview-stats`** counted HIGH-confidence editions with `.eq(collection_id).eq(confidence)`
  on the view: **1,331,923 buffers / 14,085 ms**, against 909 / 39 ms on `edition_fmv_current`, the
  materialised table that exists for exactly this. ⚠ **It has no caller** — nothing in the repo
  fetches it, production logged zero requests in 72h — so it is a landmine, not a wait. Fixed rather
  than deleted because the next person to wire an overview panel would inherit the worst
  `fmv_current` shape in the repo, already written and looking reviewed.
- **`/api/support-chat`'s `get_edition_listings`** filtered the view through an embedded
  `editions!inner(external_id)`: **933,871 buffers / 1,390 ms** inside the 60 s concierge lambda, on
  every "what is this worth" turn. The edition UUID was in hand two statements above. Keyed on it:
  **6 buffers / 0.06 ms**. It was also less CORRECT — the embed matched `external_id` with no
  collection scope, and Top Shot stores every moment twice.

⭐ **The morning sweep missed both because it enumerated the columns that are WRONG.** `collection_id`
beside `fmv_current` is greppable; an embedded-resource filter is not — the offending column is not
in the read's own table. The new guard asserts the RIGHT column is PRESENT instead
(`__tests__/fmv-current-reads-are-keyed-on-edition-id.test.ts`, a tree walk, ban at zero, asserting
the count it inspected). **A ban list is only ever as long as the last incident.**

⚠ **One instrument caveat worth keeping:** `pg_stat_statements` is at **4,905 of 5,000** entries, so
it is evicting. It was the right tool for reading what PostgREST actually sends, and it is NOT a
tool for concluding a query never ran.

**And a third measurement of the same view landed mid-flight, refuted the same way.** An inbox filing
reported *"THREE ids cost the same 1.33M buffers as five hundred — the cost is FIXED, not
proportional"*. The curve, both forms: 1 / 3 / 10 / 100 / 500 ids → **6 / 23 / 311 / 5,312 / 25,330**
buffers. A straight line. ⛔ Its one-line discriminator — *"`.eq` is fine, `.in` is not"* — cleared
`/api/overview-stats` and `/api/support-chat` as "safe, need nothing"; those were the two at 1.33M and
934k. **A wrong axis does not merely mis-rank: it hands the next session a reason to skip the actual
defects.** Corrected in the filing and INDEX.md rather than archived — the site list, the drop-in RPC
and "enumerate consumers by the OBJECT, not by the language the first instances were written in" all
survive, and that last rule is exactly what would have caught the two it cleared.

⭐ **Three sessions measured one view in one day and produced 249×, "fixed cost", and 17×.** The two
wrong answers came from harnesses, and the tell was identical in both: **an arm far worse than the
same query issued the production way.** The cheapest defence is one query against
`pg_stat_statements` for what the client actually sends — which none of the three ran before
publishing, this session included.

**Last thing measured, and it inverted the framing I had been working inside all evening.** Having
fixed three sites, the obvious next move was a wider helper so the remaining thirteen `.in()` sites
could convert too. Ranked them by what they actually cost first (`pg_stat_statements` since its
2026-08-12 reset): the ONE collection-scoped shape cost **31,052,866 blocks in 43 calls**; **every
id-list call in the product combined** cost **8,691,506 over 4,443**. ⭐ **3.6× the buffers from 1% of
the calls, out of a single file. A file count is not a cost model — and "sixteen routes" was a file
count.** No migration; the remaining sites convert when a wider helper exists for another reason.

⚠ **And I had made the same mistake I spent the evening correcting in others.** I wrote
"`wallet-search` on a 5,000-moment wallet is ~350k buffers where ~20k would do" into `database.md`,
the ledger and the inbox as though it were sized. It was a projection. Observed: **2,042 blocks/call,
~29 editions.** The tail exists — over 1,165 wallets, distinct editions run p50 50 · p90 3,196 · p99
7,674 · max 11,349 — so a p90 wallet does cost ~224k. But "the tail is real" and "this is what the
route costs" are different claims, and I published the second while holding evidence for only the
first. **Writing a projected number in the same paragraph as measured ones launders it into a
measurement.** Corrected in all three places.

⚠ One instrument note that cuts both ways: `pg_stat_statements` is at **4,905 of 5,000 entries** and
therefore evicting. It was the right tool for reading what PostgREST actually sends — the thing none
of the three sessions checked before publishing — and every total it gives here is a LOWER BOUND, so
it is not a tool for concluding a query never ran.


## 2026-09-02 (cont.) · confirmed the daytime monitor's two pg_cron items — one classification was wrong, both were already fixed

The `rpc-daytime-monitor` filing (15:10Z) asked the night pass to confirm two failures on the next
tick. Both settle now, because both are things this session shipped hours before the filing was
written.

**1. `rpc-topshot-onchain-rekey` permission denial — resolved, and the filing's reasoning was right.**
The grant migration is named `20260902113501`; the failed run was 11:33:00Z. ⚠ But
`supabase_migrations.schema_migrations` has **no applied-at column** — `version, statements, name,
created_by, idempotency_key, rollback` — so the filename is the only clock and that is corroboration,
not proof. ⭐ **The better evidence is the instrument, not the timestamp:**
`check_cron_heavy_job_exec_drift()` returns `{"inspected": 56, "offenders": []}`. That function
exists for exactly the recurrence the filing asks someone to watch for, walks all 56 cron_heavy
callables rather than the one that failed, and is a hard smoke arm. **A reset would now red the smoke
suite within the hour instead of surfacing as one failed job a day later.**

**2. `rpc-refresh-challenge-costs` — the classification is wrong.** The filing called it "a single
statement-timeout after two clean days → saturation collateral, self-clearing". `cron.job_run_details`
over eleven consecutive daily runs: **four failures (08-23, 08-24, 08-26, 09-02) — 36%**, with the
successes trending 57 → 74 → 81 → **101** s against a 120 s ceiling. ⚠ **The filing looked back two
days, and the two days it sampled (40.1 s, 40.5 s) were the two fastest runs in the window.** A rate
needs a distribution, not a snapshot. Already fixed: migration `20260902120329` hoists arm 1 out of
the per-row COALESCE, verified live in `pg_proc`, and measured **1.208 s**. ⭐ Credit to the filing —
it named the correct lever ("the same DISTINCT-ON-filtered-on-non-key-column shape") without knowing
it had landed.

## …which prompted sweeping every DISTINCT ON view, and the negative result is the interesting part

21 views use `DISTINCT ON`. **Every app/lib caller filters on the view's own key. Of the 8 SQL
functions reading `fmv_current`, every one is either keyed on `edition_id` or has ALREADY been
converted to a LATERAL — with the reason written into the source:** *"the planner cannot push a join
key into DISTINCT ON, so every call walked all 1.31M fmv_snapshots rows"*.

⭐ **So the SQL layer learned this lesson thoroughly and the TypeScript layer never heard it.** Three
functions carry explicit comments about this exact pushdown; the route-side read I fixed today was
making the identical mistake with no comment anywhere in `app/`. **A lesson can be fully absorbed in
one layer of a codebase and completely absent from another** — "a correct sibling is not a guard",
one level up. When you fix a query-shape bug, ask which other LANGUAGE in the repo issues that query.

⭐ **And there was already a materialised answer I did not know about:** `edition_fmv_current`, a real
table refreshed hourly, carrying `collection_id` — so the collection-scoped read has a supported home.
Five functions already use it. My chunked `.in()` fix is still right for the sniper (it wants live
FMV, not hourly), but "all FMVs for collection X" should read that table.

⚠ **My screen was wrong twice before it was right.** `'%fmv_current%'` also matches
`edition_fmv_current`, so 8 real view-readers looked like 13 and two functions came back as confident
false positives. Word boundaries (`prosrc ~ '\mfmv_current\M'`) fixed it. Same family as the
substring-vs-line-start rule the ledger already has.

## 2026-09-02 (cont.) · 🚨 I fixed a route that does not run — and post-deploy verification is what caught it

Correcting my own work from earlier today. The Golazos half of the pack-EV fix is **overstated: the
route it fixes has not run since 2026-08-27.**

`app/api/cron/compute-laliga-pack-ev/route.ts` has **zero rows in `pipeline_runs`** over the whole
~73 h retention window — not even a heartbeat — and `pipeline_runs_daily` (indefinite) puts its last
run at 2026-08-27. It was superseded by a Supabase **edge function**: pg_cron jobid 44
`rpc-compute-golazos-pack-ev`, `37 */6 * * *`, active, calling
`/functions/v1/compute-golazos-pack-ev`. ⛔ **And the live edge function does not have the defect at
all** — it *writes* `pack_drop_pool` per dist and computes EV by iterating the ~40 distributions it
already holds from the upstream GraphQL (`rows_found: 40`, never ~1,000). No unbounded pool scan
exists on the live path.

⭐ **"Name the caller before you touch the function" — and I did not.** I read the code, measured the
table (1,958 rows / 211 dists / 134 reachable — all true), wrote four behavioural tests,
mutation-tested them 4/4, and never asked whether the route executes. CLAUDE.md's own example for
this rule is an afternoon spent on a function with zero callers. **The check is one query** —
`SELECT count(*) FROM pipeline_runs WHERE pipeline = '<name>'` — **and it belongs before the fix.**
I am adding it to my own pre-flight for anything under `app/api/cron/`.

⭐ **What caught it was VERIFYING the fix, not reviewing it.** I went to confirm a before/after in
`pack_ev_history` and the post-fix run had written *fewer* dists (23 vs 36–39). Chasing that
disagreement is what surfaced `rows_found: 40` and the absence of my own new `extra.pool_rows_read` —
i.e. the runs were never mine. **A post-deploy check that can only confirm is not a check.** Had the
number gone up I would have written "verified" and been wrong.

⚠ **The measurement I first reached for was also the wrong subject.** `pack_ev_history` rows grouped
by minute looked like "runs of my pipeline"; they were another producer's. Same family as pairing a
count from one table with a property from another — I compared MY code against SOMEONE ELSE'S output
and read the delta as a regression.

What stands: the ownership-walk half of that entry is real and live. The Golazos change is harmless
and strictly more correct — correct dead code now. ⚠ **Not deleted:** no pg_cron job and no in-repo
caller reference it, but cron-job.org and Trevor's Task Scheduler are invisible from this sandbox and
CLAUDE.md names both as real caller sources, so a five-of-eight sweep is not grounds for deletion.
Filed as a dead-code candidate instead.

**The #57 sweep's true live-defect count is 3, not 4.** Corrected in the ledger and in the register
entry, at the point a reader actually lands.

## 2026-09-02 (cont.) · re-derived an inbox filing and RECOMMENDED AGAINST one of its own fixes

Picked up the `sales-serial-backfill` filing from this morning's cloud session — it names two halves,
an edge-side one (operator-gated, can't touch) and a DB-side one it explicitly gated on a measurement
it hadn't taken. Took the measurement. **Nothing shipped, and that is the result.**

⭐ **The DB-side escalating cooldown must not be built: its population is ZERO.** Joining every
failure row back to its sale, **all 2,069 AllDay `not_in` rows (and all 296 `no_holder` rows) have
since had their serial resolved by another path**, so the picker's `WHERE serial_number IS NULL`
already excludes every one of them. The proposed change would act on an empty set.

⭐ **And the filing's *premise* is refuted, not just its urgency.** It reasoned that "`not_in` means
the moment is not in the recorded holder's collection, which for a sold-on moment is PERMANENT."
100% of them recovered. `not_in` is a statement about one stale holder snapshot, not about the moment.

⭐ **This is the good side of "a filed DECISION NOT TO ACT is also a hypothesis."** That filing
declined to ship pending a measurement and wrote its own falsifier — *"an escalating backoff on a
recoverable row just delays the recovery."* Shipping it would have delayed recovery on 2,365 rows
that were all going to resolve. The conservative call was right, and now there is evidence rather
than caution.

⚠ **Two of the filing's numbers do not reproduce, both from mixing populations.** "About 14 attempts
per row" — `max(retry_count)` across the whole lane is **6**, mean **2.07**. The 14 came from
dividing an attempt SUM over the failures table by a row count that had already shrunk as rows
resolved: **a count from one population against a property of another**, which is a rule this repo
already has. And the `http_429` "since" date is 2026-08-03 live, not the 2026-08-29 filed.

The falsifier itself FIRED in the other direction: the Top Shot lane is still failing (682 → 1,288
rows on `http_530` in eleven hours, newest failure an hour before I looked), so that half is open,
edge-side, and stays queued. Worth noting it is *not* hopeless — 38% of the 530-failed rows and 55%
of the 429-failed rows have resolved since, which argues for the breaker (stop burning the batch
while the origin is down) rather than for abandoning the retry.

Wrote all of it back into the filing so the next session — or the nightly pass, which drains this
directory — cannot pick up the withdrawn fix.

## 2026-09-02 (cont.) · the 45-second sniper timeout was a DISTINCT ON pushdown, found by correlating two errors

The one open item from this morning's runtime-error triage, now root-caused and fixed.

⭐ **The diagnosis came from a CORRELATION, not from either error.** `AD fmv_current error @0:
canceling statement due to statement timeout` and `Vercel Runtime Timeout Error: Task timed out after
45 seconds` share a **deployment and a second** — same request, both ceilings hit. Neither line alone
says anything about the other; the pairing is the whole finding. Worth doing routinely: group runtime
errors by `lastDeployment` + timestamp before treating them as separate problems.

`fmv_current` is `DISTINCT ON (edition_id)`. The read filtered on `collection_id`, and **a qual on the
DISTINCT ON key pushes down while a qual on any other column does not**. One page measured
**274,519 rows walked, 263,392 buffers, 19.5 s** — times six pages, against a 45 s budget. ~136 s of
DB work for one user request.

Fixed by inverting the order: editions first (keyset on the indexed `id`), then `.in("edition_id", …)`
on `fmv_current`.

⚠ **I nearly banked the wrong number.** My first probe used `IN (SELECT id FROM …)` and got a merge
semi-join: 114,446 buffers / 287 ms. That is 68× better and it *looks* like the fix — but PostgREST
sends a **literal array**, not a subquery, and that is a different plan. Measuring the real shape (a
40-uuid literal list, small enough to paste) gave `Index Cond: (edition_id = ANY (...))` on all three
partitions at **2,894 buffers / 21 ms**, ~72 buffers per edition. **A control must use the production
caller** — I know that rule and still reached for the convenient form first.

⭐ **The right pattern was one function away the whole time.** `fetchFmvBatch` in the same file has
always chunked `fmv_current` by `.in("edition_id", …)` for the Top Shot leg. **A correct sibling is
not a guard.** Third time today a fix applied per-file left a sibling behind (after the `support-report`
NaN guard and the badge/jersey caps).

⚠ **A control I wrote THIS MORNING had to be inverted, and the rule is the point.** It pinned
`if (page.length < FMV_PAGE) break;` — the *spelling* of the loop I then deliberately deleted. The
property it was protecting (the map is bounded, never one unbounded read) still holds and holds
better, so the case is inverted rather than deleted and now pins the property, with `collection_id`
banned from that read. **Pin the property, not the spelling** — broken within hours of applying it.

⚠ **The no-progress cursor guard survived the first mutation sweep for the third time today.** No
fixture produces "the same full page forever" unless you build one; it took a mock flag that ignores
the cursor. **A guard against a state your fixtures cannot reach is untested by construction** — that
is now three separate paging loops where I had to add the flag or the case deliberately.

## 2026-09-02 (cont.) · 🚨 I had main red for four commits, and the instrument said green

Worth writing up plainly: **CI failed on every code commit I pushed today** — `24aed0c69`,
`cbef9d016`, `1b8f36065`, `bd7ab8dad` — and I only found out because I went to check at the end.
Last green before mine was `6d3763bd6`. Fixed and verified green.

One job, the same one each time: **ESLint ratchet**, `no-unused-vars grew 353 -> 355`, from two
unused mock parameters in a test file I created this morning. Every other job passed on all four.

🚨 **The root cause is an instrument, not the lint.** CI's ratchet job is two lines — generate the
eslint JSON, then compare it. **`npm run lint:ratchet` was only the second line.** It reads whatever
sits at a fixed `/tmp` path, so all day it read a report written before any of today's work existed
and printed `2889 files, 717 violations (baseline 717)`. CI regenerated and saw `2893 files, 719`.
⭐ **A green local instrument and a red CI job out of the same script.**

⚠ **The denominator was in the output the entire time — 2889 vs 2893 — and I read past it.** When
two runs of "the same" check disagree, the file count is where it shows first.

⭐ **The lesson that generalises: "did the instrument produce output?" and "did the instrument
measure THIS?" are different questions.** The script already refused a MISSING, unparseable or EMPTY
report — three carefully-handled ways of measuring nothing, each with its own exit code. It had no
defence against a report that is complete, parseable, and describes a *different tree*. I was
applying exactly that rule to DB A/Bs the same afternoon ("a reading taken while its subject changed
is not a reading") and never turned it on my own toolchain.

**Any check that consumes an artifact it did not produce can be handed a stale one.** The shape to
look for: a fixed path, a `--report`/`--input` flag, a build output compared to a baseline.

Fixed three ways: the npm script now generates the report itself (this is the part that removes the
class); `findStaleness()` refuses a report older than any file it claims to have linted, and names
the worst offender; and a test pins that CI's own job still generates before comparing, because a
guard's blast radius is fixed by what runs it.

⚠ **Also corrected a false claim in that guard's test header** — *"This repo does not run eslint in
CI and ci.yml says so."* Raw eslint is not a gate; the **ratchet job is blocking and runs eslint**.
That sentence is part of why I trusted the local number. **A comment that mis-describes what runs a
guard is a defect in the guard.**

⚠ Process note for the next session: I ran `npm test`, `tsc` and the ratchet before every push and
never once opened CI. **Local gates green is not CI green** — and this is the second time this
session that shape has cost something (the first was the docs-only fast path that never existed).
Check the run, not the local echo of it.

## 2026-09-02 (cont.) · the cap sweep is closed: 19 candidates, 4 real, and the HIT RATE is the finding

Last confirmation and the close-out of #57, opened this morning.

**`/api/support-report`** computed `totalMessages`, `uniqueSessions`, per-category counts,
`dailyVolume` and **`deflectionRate`** from one unbounded `select("*")`. Live: **666 conversations in
the default 7-day window**, and `days` was **unclamped**, so `?days=11` already crossed the cap.
⭐ **A rate over a truncated denominator is wrong in an unpredictable DIRECTION, not merely low** —
1,000 deflected + 1,000 escalated is 50%, and truncated it reads **100%**, a plausible number and
therefore the dangerous kind. Same three lines also had `parseInt("abc")` → NaN →
`new Date(NaN).toISOString()` **throwing a RangeError**; both sibling routes already guard it with a
comment saying exactly this. **A fix applied per-file rather than per-pattern leaves siblings
behind** — the same lesson as "grep for the expression, not the file", one layer up.

⭐ **THE HIT RATE IS THE REAL RESULT: 19 candidates, 4 defects, 15 not — and 14 of the 15 for the
identical reason.** The bound is real but lives in a *different statement* from the `.from()`, so a
single-chain regex window ends before it. **A static scan of query chains has roughly a 20% precision
ceiling on this codebase.** That is precisely why I filed the sweep as candidates and refused to turn
the 197-site superset into a ban-at-zero guard this morning: it would have been ~90% false alarms and
suppressed within a week. Filing a scan's output as findings would have burned a day and a guard.

⚠ **The 15th refutation is the one I'd have got wrong at a glance.** `lib/studio-sales-history.ts`
reads `sales` by `edition_id` and **367 editions have over 1,000 sales** (max 20,339) — so it IS
truncated. But that set only pre-filters duplicates ahead of an insert whose `23505` path falls back
to per-row inserts: **the unique constraint is the real guard and the read is an optimisation.** A
performance cost, not a wrong answer, and deliberately not fixed because it writes to `sales`.
**"Is it truncated?" and "does the truncation change an answer?" are two questions.**

⚠ **Two mutants survived again and both were worth chasing.** The no-progress cursor guard (second
time today — no fixture produces "the same full page forever" unless you write one), and the
in-memory re-sort, which `dailyVolume` and `topCategories` cannot see because they sort themselves.
⭐ **When a mutant survives, ask which OUTPUT could observe it** — here the answer was "one of six",
and `escalatedDetails` now pins it.

Left alone with the reason recorded: `ufc-enrichment-drain` and `ufc-studio-sales-history-backfill`
read all UFC editions unbounded, and there are 518. Correct today, one collection-expansion from
being this morning's badge bug.

## 2026-09-02 (cont.) · an AVERAGE doing a bound's job, and an unordered LIMIT that churned

Two more confirmed out of #57, and between them they cover both ways this silent-truncation class
gets justified in a comment.

**`compute-laliga-pack-ev`** read the Golazos `pack_drop_pool` unbounded **and unordered**. The pool
holds **1,958 rows across 211 dist_ids**; the first 1,000 in physical order carry **134 of 211**, so
77 distributions could not get an EV row on any tick. ⭐ **And which 77 changed between runs** —
physical order shifts as the table is rewritten, so `pack_ev_history` has eventually seen all 211
while any single run covers a fraction. That churn is the tell I would have missed if I had only
compared the two counts. ⚠ **I nearly over-claimed here:** the latest run wrote 39 dists, far under
the 134 the cap allows, so the EV RPC's own `ok !== true` filter drops most of the rest. The cap is
one of two causes and the fix removes the non-deterministic one; the ledger says so rather than
banking the whole gap.

**`ownership-onchain-walk`** justified its unbounded read with *"avg ~23/wallet, well under
PostgREST's 1000-row cap"*. ⭐ **An average says nothing about the tail, and the tail is exactly who a
verification walk exists for.** Measured: 270,424 rows over 7,903 owners (avg 34), **max 26,737, and
21 owners over 1,000**. For those the walk confirmed a fraction of their holdings and reported the
rest as not-confirmed — on the pipeline whose only job is confirming ownership. This is the third
comment today whose factual claim had expired (after "a small table (hundreds of rows)" and "dormant
since the shutdown"), and the first where the number was never a bound in the first place.

⛔ Three more refuted: `market/route.ts:248`, `analytics/insider/signals:39`, `edition-history:69`.
⚠ **All five refutations so far are multi-statement builders** — `let q = …; if (x) q = q.eq(…);
await q.limit(50)` — so that is a limitation of the scan, not a coincidence, and it is recorded in
#57. **8 of 19 triaged in total: 3 confirmed and fixed, 5 refuted, 11 left.**

⚠ **A mutant survived and was worth the extra test.** The no-progress cursor guard (stop when the
next cursor equals the current one) killed nothing, because no fixture produced the pathological
"same full page forever" case. Added a termination test: without the guard the loop runs to its page
ceiling and the confirmed count becomes nonsense rather than merely incomplete. **A safety belt with
no test is indistinguishable from dead code.**

## 2026-09-02 (cont.) · the Pinnacle sniper couldn't price 30% of its editions, and a miss DROPS the listing

First item triaged out of the #57 list I'd just filed, and the worst landing of the three found today.

`lib/sniper/pinnacle.ts`'s `loadFmvMap()` read `pinnacle_catalog` unbounded. **2,470 rows carry an
`fmv_usd`; the first 1,000 under the old ordering cover 290 of the 416 distinct legacy edition keys —
69.7%.** And `flowtyNftToSniperDeals` does `if (!fmvData || fmvData.fmv <= 0) return []`: **a map miss
DROPS the listing.** So ~30% of Pinnacle editions could not appear on the sniper board however they
were priced, and the board read as honestly quiet because nothing errored.

⚠ **The module's own comment says the Flowty leg is "dormant since the 2026-05-13 shutdown".** It
isn't — production returned `flowtyCount: 42` with real deals while I was measuring. Second time
today that a comment's factual claim had expired: **a comment's claim about LIVENESS is a dated
sample, exactly like a row count.** I nearly wrote this off as a dead path on the strength of it.

⭐ **The fix forced a design change I would not have made on my own.** Keyset paging alone would have
been wrong: the representative render per key was picked by a global `ORDER BY fmv_sales_count_30d
DESC, fmv_usd DESC` plus first-wins, which needs the read to be COMPLETE *and* TOTALLY ORDERED.
Neither held — there was no unique tiebreak, so two identical requests could disagree about which
render represents a key, and under paging first-wins over a page order that is not the ranking order
is simply wrong. **Paging a read whose semantics depend on its ORDER means moving that decision out
of the ORDER BY and into an explicit comparison.**

⭐ **Two of the 19 candidates were REFUTED, which is why they were filed as candidates.**
`lib/concierge/fmv-distribution.ts:202` and `app/api/support-chat/route.ts:2172` both carry a
deliberate `.limit(500)` / `.limit(50)`; my scanner's chain window ends before it because those
queries are built across several statements (`let q = …; if (x) q = q.eq(…); await q.limit(50)`).
**A multi-statement builder defeats a single-chain regex** — recorded in #57, and the reason every
remaining candidate has to be read rather than counted. 3 of 19 done: 1 confirmed, 2 refuted.

Seven behavioural cases; the mock records the cursors the loop passed, so "it paged" is asserted as
*the cursor advanced to page 1's last render_id* rather than "more than one read happened".
Mutation-tested 6/6 (single-page, no-short-break, first-wins, last-wins, no-tiebreak,
drop-all-on-error).

## 2026-09-02 (cont.) · the sniper's badge map saw 10.6% of the table, and the COMMENT was why

Triaging the sniper-feed timeouts from the entry above turned up a second, quieter defect in the same
route: two reads had silently outgrown PostgREST's 1,000-row cap.

| read | rows (measured today) | returned | coverage |
|---|---|---|---|
| `badge_editions`, Top Shot scope | **9,471** | 1,000 | **10.6%** |
| `players`, `nba_top_shot` with a jersey | **1,317** | 1,000 | 76% |

⭐ **The comment was the defect.** The badge read carried an explicit justification — *"Safe because
badge_editions is a small table (hundreds of rows)"* — plus a measured *"4,981 rows / 4,981
distinct"*. Both were true when written. **A row count in a comment is a dated sample, never a
bound**, and a reader who checks the justification finds a number and stops looking. This repo
already applies that rule to its own memory files and had not applied it to code.

⛔ **The worse consequence is a control claiming a filter it applied to a truncated map.** With the
cap in place, ticking **Badges only** hid ~89% of the badged listings that exist and the board read
as nearly empty — *honestly* empty, because nothing errored. The degraded-source signal shipped
hours earlier cannot see this: it is the third state (read ok + truncated), not the first.

⚠ **The existing cap guard could not see it, and the reason generalises.**
`invariants-postgrest-cap.test.ts` is scoped to ONE idiom — a raw `fmv_snapshots` read ordered
`computed_at DESC`. It is structurally silent about every other unbounded read, **including two in a
file its own allowlist already names.** Being listed in a guard's allowlist is not coverage.

Fixed by keyset paging (`external_id` unique within the collection, so `.gt(cursor).limit(1000)` is
deterministic and O(n)): measured 1,935 buffers / 118 ms on the last page vs 7,698 / 187 ms for the
equivalent `.range()` page, because an OFFSET page re-walks everything before it.

**A sweep came out of it and is filed rather than shipped** (known-issues #57): 339 `.select()` sites
→ 197 on tables over 1,000 rows → **19 with no row-bounding filter at all**. ⚠ Deliberately NOT
turned into a ban-at-zero guard: most of the 197 are legitimately `.in()`-chunked, and a guard that
cries wolf gets suppressed. Equally deliberately NOT called 19 findings — a static heuristic is a
candidate list, and each needs its own live count under its own filter first.

⚠ **Two testing traps, both worth remembering.** (1) A `/\.range\(/` assertion **survived** a mutant
that deleted the call — the source *comment* beside it says ".range()" too. Fixed by pinning the
paging EXPRESSION, which prose does not carry, rather than reaching for the comment stripper this
repo has been blinded by three times. (2) `makeSupabaseFixture` was built ONCE in the badge suite's
mock, and a SEQUENCE fixture's cursor lives inside that instance and is not reset by clearing
`fx.tables` — so the third test to use a sequence starts mid-array and reds correct code. Rebuilt per
`seed()`.

⚠ **And a scripted replace silently matched nothing** while I was mutation-testing (my pattern said
`// listing itself`, the source says `// the listing itself`), so a mutant reported as SURVIVED had
never been applied. Asserting the occurrence count is what caught it — this file already carries that
rule.

## 2026-09-02 (cont.) · the Pack Sniper told four users to widen their filters while All Day 403'd it

Same defect class as the boards above, one layer lower — and the first instance in this file found by
a **victim count** rather than an audit. `get_runtime_errors` over 24h:
`[sniper-feed] AD GQL FAILED: HTTP 403 <title>block</title>` — **4 occurrences, 4 users**. That route
is on-demand and user-facing, not a pipeline (`pipeline_runs_daily` returns `[]` for `%sniper%`), so
those are four collectors who got a wrong answer on the surface that tells them what is underpriced.

`fetchAlldayGqlPage` returned an empty page from a 403, from a GQL `errors[]` payload, from a missing
`searchMarketplaceEditions`, and from a thrown fetch. The route answered **200 with `deals: []`** and
the board concluded *"THE FLOOR IS QUIET — No deals match your filters. Try widening your search."*

⭐ **The finding I did not expect: the BUTTON is part of the claim.** Underneath that copy sat
**CLEAR FILTERS**, which asserts the same false diagnosis in the one place a frustrated reader is
most likely to click — and in a form that cannot be read as hedging. Fixing the sentence and leaving
the call to action would have half-fixed it. It now offers RETRY.

Nine deal-bearing reads collapsed the same way in this one route. Two of them are only deal-bearing
on a second look: a failed `get_editions_for_sniper` costs the edition key, and without a key a
listing has no FMV to price against, so it is dropped downstream; a failed All Day `fmv_current` map
empties the board because a listing with no FMV is EXCLUDED rather than priced off its own ask.
Enrichment reads (badges, jersey numbers, pack EV) are deliberately NOT noted — noting them would
fire the degraded copy on a board that is honestly empty.

⚠ **The receiver was an allowlist, so the instrumentation half nearly shipped dead.**
`/api/public/log/empty-sniper` builds its logged payload by enumerating fields. Adding `degraded` /
`sourcesFailed` to the client beacon alone would have recorded **nothing**, with no error anywhere,
while reading as instrumentation in the diff AND in the client test. Caught by opening the receiver;
now pinned as a round trip. Generalised into the honesty doc: adding a field to any beacon means
opening the thing that logs it.

⚠ **Mutation testing found the two branches my fixtures could not reach.** 11 `sink.note` sites; the
first sweep killed 9. Both survivors were `catch` arms — supabase-js RETURNS errors rather than
throwing, so a suite that only sets `{ error }` never enters them. Two fixtures added, 11/11 killed.
Three client mutants each red 2 cases.

Also fixed a cache amplifier the 25 s TTL made easy to wave off: without evicting a degraded build,
one reader's 403 is served to every reader of the same filter combination for the rest of the window
— the ISR-caches-a-failed-read shape at lambda scale.

Gates: 1,426 files / 15,777 tests green (+2 files, +40 tests), `tsc` clean, eslint ratchet 717 =
baseline over 2,889 files.

## 2026-09-02 (cont.) · four more boards concluded from a failed read, and three blamed the reader's filters

Ran the honesty canon's own fifth-layer predicate — *"server pages that KNOW `ok` and seed a client
component without passing it"* — which the 2026-08-24 sweep wrote down, applied to five boards, and
stopped. **10 candidates, 4 real, 6 cleared.**

⭐ **Three of the four blame the FILTERS**, which is a worse sub-class than "there are none": "No
sales match those filters" sends the reader to widen filters that were never the problem. That is the
canon's actionable-harm test aimed at the reader's query instead of their account.

⚠ **My first pass used the wrong predicate and would have missed all four.** I checked whether the
FILE carried any provenance signal — and every one of these renders `<DegradedDataNotice>` from the
very `ok` it then drops, so all four passed. The 08-24 write-up says explicitly that a banner is not
a fix and that the predicate is about the PAGE→CLIENT handoff. I had read that section only after
running my own sweep.

⚠ **`parallel-premiums` is the instructive one: it already had the right sentence.** Its client
renders "Couldn't load these filters just now — this says nothing about which parallels match." for
its own refetch failure, and initialised that state to `false` unconditionally. The concept was
present; only the seed was missing. Any grep for "does this board handle a failed read?" answers yes.

Swept two sibling layers clean in the same pass and recorded the method, because a negative result is
only reusable if you can see what it covered: layer 1 (37 public API routes) and layer 4 (29 insights
OG cards). Both fine — the routes without the helper are media proxies and deliberate hand-rolled
error bodies; the OG cards without it fall back to product copy or `—`, never a claim.

All six new cases are SSR-asserted and mutation-tested; each failure case is paired with a no-change
control so that deleting an empty state cannot pass it.

🚨 **Then verifying the deploy found a panel the diff review had not.** I fetched the live SSR HTML
to confirm the healthy path still rendered (132 `<tr>` on cross-collection, zero degraded copy) — and
in doing so noticed `rookie-board` has a **burn view** over the same `initialRows`, still saying "No
burned rookie editions match those filters." ⭐ **"Fix per PANEL, not per page" applies inside one
client, not only across pages** — and the thing that caught it was checking the deployed output,
which is the step that is easiest to skip once the tests are green.

## 2026-09-02 (cont.) · the cron fleet is clean, and the one watcher that cannot go green

Swept every post-boundary pg_cron failure: **6 in 67 hours, all explained, none live** — jobid 87
(fixed today), jobid 434 (mine, fixed today), one timeout at the boundary itself, one in-flight row my
`status <> 'succeeded'` filter miscounted, one `job canceled` on a job since unscheduled, and one
`invalid transaction termination` on jobid 430 that another session already diagnosed and replaced
with jobid 432 (succeeding, 52 s, `CALL`). Also checked for INVALID or leftover `*_ccnew` indexes:
**zero**.

**What the sweep did surface is a rate.** `wmc-reindex-verify` is red, and the reason is real:
`idx_wmc_cohort_cover` went 165.7 MB / 81.14% density (post-reindex, 08-31) → 299.2 MB / 45.13%
(09-01) → 326.4 MB (09-02). **~64 MB/day, so a REINDEX buys about a week**, and its pre-campaign size
was 614.4 MB. That turns "should the reindex be scheduled?" from a judgement into a number.

🚨 **And the watcher cannot clear itself.** `wmc-reindex-verify` has run 3 times ever, fails while any
target is under 60% density, and **no reindex job is scheduled** — a permanently-red instrument, which
is the shape this repo warns is indistinguishable from a broken one. Filed as known-issues #56.

⛔ **I did not book the job**, and the reason is coordination rather than technical: the campaign
belongs to another session's thread that deliberately did not re-book it, and a concurrent session was
pushing to `main` throughout this measurement (its concierge commit landed mid-rebase). What is needed
is a cadence decision, not a diagnosis.

⚠ Two care-points recorded with it: density was **not** re-probed today because `pgstatindex` reads the
whole index (~900 MB across the three targets), so the 45.13% is labelled as 09-01's reading rather
than passed off as current; and a pg_cron `failed` on a REINDEX job does **not** mean the work did not
happen, because `REINDEX INDEX CONCURRENTLY` commits its phases.

## 2026-09-02 (cont.) · a sixth control for the fleet finding, and one lead that looked dead and was not

**The corroboration.** The `wallet-backfill*` pipelines are HTTP routes logging to `pipeline_runs`,
not pg_cron jobs — a different instrument over a different population, so nothing in the
`cron.job_run_details` measurement can leak into them. Their failures collapse on the same boundary
with DB-contention errors: `wallet-backfill-pinnacle` **225/1,125 → 0/375 → 0/571**,
`wallet-backfill-ufc` 25/494 → 0/375 → 0/571. ⚠ And those failures were **losing rows** —
`wmc_upsert_chunk_failures=N rows_lost=200…800` on most pre-boundary days, none after. The drain
bought `wallet_moments_cache` completeness, not just time.

⛔ **The lead I nearly filed as a defect.** `wallet-backfill-golazos` writes `rows_written = 0` on 30
of the last 35 days, and Golazos is the only live collection whose `wallet_moments_cache` rows have
not been re-stamped in three days — while every other collection is minutes fresh. That reads exactly
like a pipeline that reports success and does nothing, which is this repo's most productive defect
class.

**It is not one.** `rows_found` is **8,000–25,000 per day** and `rows_skipped` ≈ `rows_found`: it
reads every Golazos moment every day and finds nothing changed. `rows_written` counts CHANGES, so 0
is the honest value for a quiet 133-wallet collection, and `last_seen_at` is only re-stamped on a
write. ⭐ **The discriminator was `rows_found`, one column over from the one that looked alarming** —
and it is the third of the three incompatible meanings CLAUDE.md already assigns to a zero there.
Recorded as a NOT-A-FINDING so the next sweep does not re-open it.

## 2026-09-02 (cont.) · re-read the one "blocked" DB fix, and its premise is a shape nothing calls

CLAUDE.md's prioritized-actions list carried one measured-but-unshipped DB fix —
`compute_pack_ev_per_edition_weighted`'s `fmv_current` leg at **18,766 vs 1,046,192 buffers**, held on
a fixture re-seed that is Trevor's call — with an explicit instruction attached: *"re-read a 'blocked'
item's blocker before inheriting it."*

Taking that literally: **the 1,046,192 figure measured a `LEFT JOIN fmv_current` over 3,097 edition ids
at once, and no caller does that.** All four call sites are per-dist. Measured on the exact 57-dist
atlas set jobid 217 drives: **1,746 ms / 152,542 buffers for the whole pass**, ~2,625 buffers per call.
Corroborated from the fleet side — jobid 217 completes a tick in ~2 s, which a 1M-buffer-per-call
function cannot do.

⭐ **The filed figure was never wrong. It measured a shape that was never run** — and neither the
ledger entry nor the CLAUDE.md pointer recorded which call site it came from. **A cost figure is only
actionable together with its call site.** The item is not closed (the fixture question is still
Trevor's, and the leg is still the more correct shape); what changed is the stakes, so nobody spends a
session unblocking a 55× win that is worth ~1.7 s.

The CLAUDE.md bullet was DISPLACED rather than extended — 400 → 396 chars, file at 39,972 of 40,000 —
with the full write-up in `roadmap-status.md`, which is what that file's header prescribes.

## 2026-09-02 (cont.) · the fleet quietly got 8× cheaper three days ago and three filed items are measuring history

I went looking for the next expensive cron job using a 14-day pooled ranking, and it pointed at
jobids 217, 71, 303 with tens of thousands of wasted seconds each. **All of it was history.**

⭐ **The committed triage query had already told me so and I stopped reading it.**
`supabase/analysis/cron-waste-triage.sql` classified every one of those as RECOVERED or UNPROVEN and
put only two jobs in LIVE — jobid 87 (which I then fixed) and my own jobid 434. Its whole design is to
prevent the pooled-ranking mistake, and I made the mistake anyway by going back to a hand-rolled
`sum(duration) WHERE failed` afterwards. **The instrument was right; I overrode it.**

**What is actually true.** `job startup timeout` stopped at **2026-08-30 18:26:00Z** and has not
recurred: **270 → 0** across matched 66.6 h windows, 52 jobs → 0, at 93% of the run volume. The cause
is that the work got cheaper, not that anything was re-staggered: successful-run database time went
**201,812 s → 25,682 s (7.9×)** at 96% of the run count, mean concurrency 1.02 → 0.11. Per job,
210 s → 15 s, 195 s → 2 s, 178 s → 1 s at unchanged run counts. That is the fleet-scale confirmation
of the 08-30/31 top-consumer drain, which had only been verified per query.

⚠ **The subsection in `cron-and-schedulers.md` re-derived EARLIER TODAY — "the rate is FLAT PER JOB …
unchanged" — is itself pooled across the change point.** Its reasoning about the mechanism is right
and I kept it verbatim; only its currency was wrong. Two sessions in one day walking into the same
pooled-window trap, in the file that documents that trap, is the thing worth remembering.

I ran four controls before believing an 8× improvement: the instrument still records failures (it
caught two of mine on 09-02), the job mix is unchanged (118 → 117), Postgres has not restarted in 81
days and the worker-pool config is untouched, and the Top Shot outage is refuted as the cause because
sales ingest continued across the boundary.

🚨 **And then I got the outcome control wrong, in the way this repo documents.** I read
`pipeline_runs` over the same 66.6 h matched windows and reported "3 completed runs before vs 66
after" — a 22× claim I had already written into two docs before checking it. `pipeline_runs` retains
~73 h and that window sits 66.6–133 h back, so the rows were PRUNED, not missing.
⭐ **The window that is correct for one instrument is silently wrong for another: `cron.job_run_details`
holds 55 days, `pipeline_runs` holds three.** `pipeline_runs_daily` is indefinite and gives the true
answer — completed runs/day 17–23 of 24 → 24/24, with 57.0 rows per completed run on both sides, so
the work did not shrink. Caught before pushing, by asking what else could produce a missing row.

## 2026-09-02 (cont.) · a daily job that failed 15% of days on a lookup that could never match

Found by sweeping `cron.job_run_details` — which retains ~55 days here (201k rows), far
longer than `pipeline_runs`' ~73 h — for jobs whose failures cluster at ONE round
duration. `rpc-refresh-challenge-costs` died at **exactly 120.0 s** on 8 of 52 runs.
⭐ **A job that fails at exactly the same number every time is at a ceiling, not flaky**,
and that is a cheap sweep worth repeating.

The ceiling was this cluster's DEFAULT `statement_timeout` (120 s) — what a
`postgres`-owned pg_cron job runs under. Known-issues #43 said those jobs "run at the
cluster default" without ever naming the number.

**99.7% of the cost was one COALESCE arm**: a correlated scalar subquery against
`pack_ev_latest`, a `DISTINCT ON` view. The predicate cannot push through the DISTINCT,
so the whole view is re-materialised **once per outer row** — 40,716 ms / 21,094,324
buffers for 31 challenges, with the view's own `pack_ask_state` subplan running 3,849,487
times. Hoisted into one temp-table build: 1,220 ms / 681,430 buffers; the function now
runs in **1.208 s** live.

⚠ **And the arm returns NULL for every row** — all 29 reward dists are in
`pack_distributions`, zero are in `pack_ev_history`. I ruled out the two obvious
alternative explanations before believing it: the view's filters (the base table has no
rows for them either) and an id-vocabulary mismatch (same numeric-string space, and the
history's max dist id is *higher* than the challenges'). ⛔ Deleted nothing anyway — the
cost was the 31 evaluations, not the arm.

🚨 **My first post-state control was wrong, and the migration caught me.** I asserted that
no `cached_reward_value` changed; 18 of 31 changed and it rolled back. The stored values
were computed by the last successful run a day earlier, and arms 2–4 read tables that
move daily — **a before/after comparison spanning a refresh window measures the data
moving, not the code.** The correct control was narrower and exact, and 33× cheaper: only
arm 1 changed, and the two forms differ only where a reward dist appears in the view, so
`overlap = 0` proves equivalence outright.

## 2026-09-02 · red `main` fixed, and the Top Shot on-chain re-key was moved off PostgREST

**1. `main` was red for five commits and I caused it.** `ce85c45c6` added a line of prose
to `tooling-gotchas.md` quoting a Postgres regex — `'[?&](key|token|secret)='` — whose
character class abuts an alternation group, so the line contains the byte sequence `](`
and `scripts/check-memory-doc-links.mjs` read it as a pointer to a file named
`key|token|secret`. Two jobs failed (the standalone CI step and the vitest case that runs
the guard live) on `ce85c45c6`, `b5b713b4e`, `a744f11a4`, `e794ecf9c`, `180f9ee7f`.
Everything else was green throughout. The guard now blanks fenced blocks and inline code
spans; three fixtures pin it, including the positive control that a dead link OUTSIDE the
backticks on the same line still fails. Mutation-tested by reverting the strip.

⚠ **The lesson is the SWEEP, not the regex.** Every one of those five commits was
docs-only, and I ran the register-integrity, inbox-index and ledger guards after each
edit. I never ran the link guard, because "I only touched docs" reads as *below* the CI
surface — when it is exactly the surface a docs-only commit is gated by. **There is no
docs-only fast path.**

**2. `topshot-misattrib-drain`'s re-key was being rolled back on half its runs.** Found by
asking why 10 Top Shot pipelines read 100% failed over 72 h; nine of those are the known
upstream 530 outage (already filed, known-issues #50), but this one's error string was
different: `rekey: upstream request timeout`, a **gateway** timeout, on a WRITE RPC.

⭐ **The control settles what the error string cannot.** Day-by-day, `pipeline_runs_daily`
against the function's own audit table: on all five `upstream request timeout` days
(08-23 → 08-28) `audit_topshot_sale_drain_remap_20260621` gained **zero** rows, against
673 on the one clean day. **The gateway giving up is a ROLLBACK.** The existing docs said
the statement "keeps running server-side", which is true about cost and silent about
commit — and the two compound: full IO cost, no rows.

⚠ **Why it hid.** The re-key is idempotent, so the next successful run catches up, no
backlog grows, and no downstream metric moves. Re-derived live: 0 sales / 10 moments
currently need a re-key, i.e. the work is current. This was waste plus a lying `ok=false`,
not data loss — which is exactly why nothing found it.

⛔ **"Make the query faster" was measured and rejected.** The sales leg scans 3,198,302
Top Shot rows (174,820 buffers / 9.0 s warm) and the planner's hash join is the RIGHT
plan: forcing the nested loop over the per-partition `nft_id` indexes costs 1,315,991
buffers / 33.3 s. Shipped instead: pg_cron **jobid 434 `rpc-topshot-onchain-rekey`**
under `cron_heavy`, whose role `statement_timeout` is 600 s, with `?rekey=1` dropped from
`vercel.json`.

**3. 🚨 And the fix broke on its own first tick, which is the most useful thing that
happened all session.** Jobid 434 fired at 11:33 UTC and died in **0.0 s**:
`permission denied for function run_topshot_onchain_rekey`. The mandated anon hardening
(`REVOKE EXECUTE … FROM PUBLIC, anon, authenticated`) removes the PUBLIC grant that is
`cron_heavy`'s ONLY path to execute a new public function — `ALTER DEFAULT PRIVILEGES` on
this database covers anon/authenticated/service_role and not cron_heavy. **Two standing
policies in direct conflict — and I walked into a trap this repo had already documented three
times** (`database.md`, 2026-08-23: two Pinnacle trade jobs and a series rollup). My migration's
header says nothing said so; that is wrong, and the correction is recorded in `database.md`,
`cron-and-schedulers.md` and the ledger. ⭐ **Three prose records did not prevent a fourth
instance, because prose is read after a session knows its topic and this trap fires while you
are thinking about anon safety. What was missing was a CHECK, not knowledge.**

⚠ **The failure shape is worse than the error.** 0.0 s, no `pipeline_runs` row (the
function never runs, so it never logs), message only in `cron.job_run_details` — which no
fleet sweep here reads. A job in that state is indistinguishable from one never
scheduled: silent, free, green everywhere. It was caught only because I watched the first
tick on purpose. Fixed with an explicit `GRANT … TO cron_heavy` (the convention: 48 of
the other 49 cron_heavy-called functions already carry `cron_heavy=X/postgres`), plus
`check_cron_heavy_job_exec_drift()` — population derived from `cron.job` by walk, returns
`{inspected, offenders}` so a run that inspected nothing cannot read as clean. Live:
inspected 56, offenders 0.

**4. Verified end-to-end, and one claim of mine was wrong for an hour.** The first
successful run took **75,803 ms** (11:35:48→11:37:04Z, ok, 0 sales / 0 moments re-keyed,
10 moments deferred on genuine collisions). **63% of the gateway's ~120 s, 13% of
`cron_heavy`'s 600 s** — which is the whole argument for the move in one number.

⚠ That run was issued through the Supabase **MCP** `execute_sql` tool, which gave up at
its own 60 s budget. I checked `pipeline_runs` at ~11:36, saw zero rows, and recorded it
as a second replication of the rollback finding. **It committed at 11:37:04.** I had taken
a reading while its subject was still changing — the exact trap this repo's measurement
rules name. The gateway-504 rollback finding stands on its own control (the audit table,
5 for 5); the MCP tool timeout is a DIFFERENT layer and does **not** abort the statement.
Both are now written down, separately, so nobody carries one across to the other.

## 2026-09-02 (early hours) — a failed READ that lands on a retirement, and four register rows that were no longer true (Claude Code, cloud session, push-enabled)

Continuation of the 09-01 night pass. Two threads: one new defect **class**, and a batch of filed
findings re-derived rather than inherited. Durable lessons are promoted to
`docs/reference/key-files-and-honesty.md`, the register and the ledger — not left here.

### The class: `const { data } = await …` where `not found` is RECORDED, not re-tried

The shape appears **25 times** across `app/`+`lib/`+`workers/`. **Most are harmless**, so a ban is the
wrong instrument. 👉 **The discriminator is not the expression, it is WHERE `not found` LANDS.** In an
enrichment loop a miss costs one un-enriched row and the next tick fixes it. In these seven routes it
landed on:

- **`retry_count + 1`**, and ten bumps RETIRE the row permanently — at `*/15` a sustained read failure
  would have retired every queued row in **two and a half hours**, with no trace but a rising
  `rows_skipped`, which reads as the pipeline working;
- **a cursor that advanced anyway** (`topshot-offers-indexer`) — zero rows written, `ok: true`, and
  nothing revisits a block below the cursor, so the offers are **gone**;
- **a Cadence storm** (`allday-resolve-unmapped`) — `existing` empty ⇒ every edition "missing" ⇒ one
  on-chain call per id, then a mass upsert back over rows already there;
- **a flooded retry queue** (`allday-listings-indexer`), worked with real Cadence borrows and a finite
  budget, for rows that were never unresolvable;
- **`edition_id: null` forever** (`pinnacle-trades-indexer`) — `ignoreDuplicates: true` means no later
  tick corrects it.

**Two siblings found in the same files.** `allday-listings-retry` marked rows `resolved_at` whose v2
upsert had **failed** — the listing is then lost and nothing re-derives it. And all three retry routes
ended their resolved-mark with a log-only error branch followed by `resolved = list.length`; the admin
route answered its operator `{ok: true, resolved: true}`, the one signal that stops a human looking.

⭐ **And the exception is now written down.** `wallet-backfill`'s `seeded_wallets` read discards its
error too, and SHOULD: a NaN there sends the wallet down the full-walk path — more work, never a wrong
answer. It carries a comment saying so. **A rule that cannot name its own exceptions gets applied
mechanically until someone breaks something with it.**

Fifteen tests, every one pinning an **absence** (the retry row at `retry_count: 9` receives zero
UPDATEs; the cursor write does not happen; 87% never appears) and every one mutation-checked.

### Also shipped

- **`/api/collection-stats` ran the same 19,942-probe FMV scan twice per request** — the route
  recomputed, through `query_sql`, a scan `get_collection_stats` had already made. **116,945 buffers /
  2,875 ms / 19,942 lateral loops** off every uncached request. Folded, with **equivalence proved for
  all five collections in a SINGLE STATEMENT** so a concurrent write cannot fake a mismatch.
  🚨 **And a 291× false claim found on the way:** the "Priced from Sales" KPI fell back to `fmv_pct`, a
  different metric — **Golazos 87.3% against a true 0.3%**, UFC 73.6% against 0.0%.
- **deep-audit D39 closed.** The alert path could not see its own cache going stale. ⛔ **The reader was
  the wrong place to fix it:** its consumer builds `detail` by concatenating `->>` reads, so a
  differently-shaped "stale" marker would have emitted an alert with a **NULL detail**. The staleness
  went on the CALLER. Threshold measured (30 d: max gap 422 min vs the arm's 24 h), and the post-state
  proves the arm is **silent** on a fresh cache AND that it **can** fire.
- **`allday-price-recover` watchlisted** at a threshold from 228 measured gaps, not the nominal cadence.
- **Top Shot `wallet-backfill` had no denominator** — every sibling lane emits `rows_to_write`; the
  biggest one did not, and its three non-happy exit paths emitted no chunk fields at all, so **the run
  most likely to have lost rows was the one that hid it.**

- **The null-serial sale backfill was reading two sources and a third had the answer.**
  `backfill_null_serial_sales_from_moments` (hourly) resolved a missing `sales.serial_number` from
  `moments` then `wallet_moments_cache`. Adding `nft_edition_map` as a **strictly third** leg and
  running one unbounded pass wrote **2,307 serials**: AllDay's null-serial population with a real
  `nft_id` went **2,295 → 0**, UFC **10 → 0**, Golazos **2 → 0**. Top Shot's 1,094 stay — they belong to
  the separately-dead edge lane, and `nft_edition_map` gained **2** of them.
  ⭐ **Two independent gaps lived in one function and I nearly shipped the wrong one.** My first sizing
  said "824 AllDay rows are one COALESCE away"; measuring *inside* the 45-day window found 1,096
  candidates, **all Top Shot**, of which the new leg gained **2**. The AllDay rows were all OLDER than 45
  days — the **age window**, not the missing leg, was what excluded them. Both were real; the drain pass
  closes the age one for the backlog and the leg keeps it closed inside 45 days.
  ⭐ **And it retired a work item without doing that work item.** The 08-55Z filing had proposed an
  escalating cooldown for `nfl_all_day / onchain_nil / not_in` — **700 rows, 9,959 attempts since
  08-07** — gated on measuring how many were permanently unrecoverable. That measurement is moot:
  `get_serial_backfill_targets` keys on `serial_number IS NULL OR = 0`, so all **2,144** AllDay failure
  rows now point at resolved sales and the picker returns **0** AllDay targets. 👉 **Before tuning how
  often you re-ask a failing upstream, check whether anything local already knows the answer — a retry
  policy fixes a *rate*, never a *gap*.**
  Pinned with 4 new assertions (the new leg wins alone, loses to `moments`, loses to `wmc`, and its
  `> 0` + collection scoping both bind), each mutation-checked; the drift guard was repointed in the
  same commit.

- **`resolve-topshot-stubs`: 37 rows written in 74,800 chain lookups over 36 days, and our own
  database had the answer for two-thirds of them.** `wallet_moments_cache` carries a `player_name`
  for 346 of the 515 nameless Top Shot stub editions — and those stubs were created *from* wmc rows
  by `stub_editions_from_wmc`, which writes `player_name` as `NULL, -- player to be resolved later`
  while reading a row that has it. 267 filled; the stub queue went **520 → 253** and the resolver's
  ~2,300 daily chain lookups roughly halve, so the change **displaces** work instead of adding it.
  🚨 **The control did not come back clean, and that is the interesting part.** Against editions that
  already carry a name, 46 of 11,866 disagree — mostly Steph/Stephen — but three name a **different
  person** (Trae Young recorded as Alex Sarr, John Collins as Matas Buzelis, Julius Randle as Andre
  Drummond), and all three sit on edition_keys where wmc holds more than one distinct name. Split on
  that, over the subedition keys where the risk lives: **n=1 → 2,750 checked, 0 wrong players; n>1 →
  17 checked, 10 wrong (58.8%)**. ⭐ **A source that is 99.6% right overall and 58.8% wrong about
  *which person* on an identifiable subset is not a 99.6% source — it is two sources, and finding the
  discriminator is what makes it usable.** The 79 ambiguous stubs stay nameless on purpose: a Dynamic
  Duos play really has two players.
  ⚠ **Two of my own vacuous assertions, both caught and both recorded rather than quietly fixed.**
  The first migration's "positive control the change cannot move" was
  `player_name IS NOT NULL AND (player_name IS NULL OR = '')` — a contradiction, zero in every
  database in every state; a second migration supersedes it with a row-by-row check against what was
  actually written, recorded *before* the run so it cannot merely re-derive the function's own
  predicate. The second was in the pinned test: it claimed the UPDATE's never-clobber re-check was
  covered, and deleting that re-check leaves the file green (both halves read one snapshot; only a
  concurrent writer separates them). Fixed in the COMMENT, not by weakening the assertion. 6 of 7
  mutations red, the 7th named as uncovered.

- **Five backward history backfills could rewind their own walk on one failed read.** The
  `*-sales-history-backfill` family holds the LOWEST block already scanned in `event_cursor`, and
  every one of them discarded the error on that read — so a blip left `ceiling` at `CEILING_INIT`,
  the TOP of the walk, and the tick then **wrote that high block back over the real cursor**. Nothing
  re-walks a range above a cursor. A sibling defect in the same block assigned `cursorWritten`
  without checking the upsert, so a **failed advance logged as a movement** — and `p_cursor_after` is
  the only instrument anyone would notice this on. Fourteen chunked id lookups in the same files read
  as "unmapped" on failure and sent whole batches into the Cadence fallback. Ratchet **47 → 28**.
  ⚠ **No evidence it fired, and the caveat is the point:** `pipeline_runs` retains ~73 h, and over
  that window no run shows `cursor_after > cursor_before` nor a `cursor_before` above the previous
  run's `cursor_after`. **A clean 73 h is not a clean history.**
  ⭐ **And two apparently-dead walkers turned out to be correctly retired.** `ufc-` (stopped 08-27,
  10.25 M blocks short of the floor) and `topshot-flowty-` (stopped 08-17, exactly at it) both look
  like silent deaths; both carry `is_active = false` in the cadence watchlist with the reason — UFC
  migrated to Aptos in May, flowty finished. **Absence of runs is not a defect when the instrument
  that would alert on it was deliberately switched off and says why.**

- **Twenty-one indexers logged a cursor advance they had not made.** `cursorAfter` was assigned
  straight after an `await …from("event_cursor").update(…)` whose result was thrown away, so a write
  supabase-js refused still logged the new block and the next tick re-scanned the same range while
  the run log showed progress. ⭐ **The read ratchet could never have found these:** it matches
  `const { … } = await supabaseAdmin`, and a bare `await …update(…)` has nothing destructured to
  inspect. **Two guards, disjoint syntax — neither finds the other's population.** The new one is a
  **ban at zero** (cursor writes are few, uniform and consequential by construction), with a detector
  positive control on synthetic snippets and a behavioural test on `topshot-offers-indexer`.
  ⛔ **Whether it ever fired is NOT established, and the tempting evidence is a false positive.** The
  signature (run N claims `cursor_after = X`, run N+1 opens below X) shows `pinnacle-trades-indexer`
  at **436 of 874 runs**, which is innocent: that route runs two modes under one pipeline name against
  two cursors, one ascending and one descending. ⭐ **A cursor-continuity check that ignores WHICH
  cursor a run used reads a dual-mode indexer as 50% broken.**

- **The error-discarding-read population reached ZERO — and the ratchet went red for succeeding.**
  61 → 51 → 47 → 28 → 0 across the sales indexers, the five backward history walkers, ten forward
  indexers, `topshot-flowty-unmapped-drain` (where a failed local read routes an nft into an on-chain
  `getMinted` whose not-found **retires the row permanently**), `candy-sales-indexer` and two admin
  routes.
  🚨 **The last fix broke the guard.** Its not-vacuous floor asserted `>= 20` occurrences still matched
  **in the tree** — a floor over VIOLATIONS FOUND, wearing the name of a detector control. ⭐ **The
  tell is what the floor counts: over "files walked" or "fixtures classified correctly" it survives
  success; over "violations found" it cannot, and the two read identically.** The rule it broke is
  already in this repo, and the file's own header cited it. Replaced with nine assertions against
  synthetic source — including `{ data: cursorRow, error: cursorErr }`, the exact false positive this
  detector once had — and the ratchet converted to a **ban at 0**.
  ⭐ **`candy-sales-indexer`'s four reads each landed differently and got four different decisions**,
  including one that deliberately does NOT throw: `unresolved_open: unresolvedOpen ?? 0` is the
  fabricated-number shape verbatim, publishing a **measured zero** for the park-queue backlog out of a
  failed count — but it runs after every write of the sweep, so failing there would discard real work
  to report a statistic. It reports `null` plus a discriminator instead.

- **The null-serial recovery job could never see the rows the history backfills produce.** Eighty
  minutes after the drain took AllDay to zero it was back at **499**, none inside the hourly job's
  45-day window, **100% resolvable**. Not a backlog — a structural leak: the `*-sales-history-backfill`
  walkers write HISTORICAL sales, so their `sold_at` is older than 45 days **by construction**, and
  jobid 76 calls the function with no argument. Default widened 45 → 3650 (the cron COMMAND is
  unreachable — jobid 76 is `cron_heavy`-owned and no session-reachable role may `cron.alter_job` it).
  ⭐ **A bounded window is a claim about where the work comes from.** This one was written for a
  FORWARD indexer's recent rows and became wrong the day a BACKWARD walker started writing into the
  same table. **When you add a producer, re-read every consumer's window.**
  ⚠ **And the cost comparison nearly lied to me:** a bare `count(*)` over the candidate subquery lets
  the planner DROP the COALESCE subplans and reads 745 buffers where the real shape reads 10,593.
  Caught on the plan, not on the number. Like-for-like the change is **+5,302 buffers / +1.8 s per
  hourly run** — and the 45-day version was not a saving anyway, since all 1,094 rows it scanned
  resolve from none of the three legs.

- **The 102,000-row AllDay unmapped backlog is a PRICE backlog, and my first measurement of it was
  wrong in a way that would have filed a defect that does not exist.** A 1-in-50 sample said **22.9%
  of the open rows have a resolvable edition** — which reads as a broken resolver, since
  `promote_unmapped_sales` runs constantly and its own comment records finding "~0.6 newly resolvable
  per call". ⭐ **The sample omitted the function's price guard.** Split on it: of the **priced** rows
  **0.2%** are resolvable — exactly matching the function's own figure — while of the **unpriced**
  rows **39.7%** are. The composition is 93,438 `onchain_dapper_v1` rows, 58,779 of them unpriced,
  because a V1 Dapper sale whose tx-decode budget ran out lands at `price_usd = 0` and
  `COALESCE(price_usd,0) > 0` parks it on purpose — promoting it would pollute FMV.
  👉 **The bottleneck is decode throughput, not the edition maps**, and the drain exists and works:
  `allday-price-recover` wrote 8,710 rows in 7 days and the backlog moved 104,908 → 101,902.
  ⭐ **The lesson is the one this session kept re-learning from the other side: reproduce the
  consumer's OWN predicate before concluding it is failing.** Three times tonight a population looked
  broken until it was measured through the filter its consumer actually applies — here the price
  guard, on the null-serial job the age window, and on Pinnacle's `edition_id` the fact that it is not
  the resolution criterion at all.

### The register's own probes, re-run — and two of them lie in opposite directions

Thirteen `VERIFIED-CLEAN` rows re-run. Twelve returned their expected value. The two that did not are
the interesting ones, and they fail in opposite ways.

- 🚨 **`No hardcoded credentials` PASSED while the invariant was broken.** It greps `rpc_pls_` — the
  exact gate-key prefix — over `origin/main`, and reports `0 real hits`. Meanwhile **all 14
  HTTP-dispatching pg_cron jobs send that key IN THE URL**, 13 active, **0 using a header**, and
  Supabase edge-function logs record full request URLs. ⭐ **A credential probe is bounded by its
  CORPUS, not its regex** — a cron command lives only in the database, so it was never in scope, and
  "0 real hits" reads as an estate-wide all-clear. Same shape as the `?token=` leak fixed on
  `/api/cron/sales-serial-backfill` hours earlier, at fourteen times the count, on the leg nobody
  looked at. ⛔ Not fixed: the edge half is deploy-gated, and relocation must precede rotation or the
  new value is re-logged on the next tick — while relocation alone closes nothing, because these
  values are already burned in public git history.
- ⓘ **`pg_cron has no disabled/orphan jobs` FAILED while everything was fine.** 104/103; the one
  inactive job is a ledger-recorded pause for an outage that is still live. The row now says so, since
  the register's own rule is that a failed probe earns a full investigation — and this one would have
  sent the next auditor down the same path.
- **`pg_cron non-timeout error class` also failed benignly**, and the reason is durable: four of the
  five offending rows sit on jobids that no longer exist. **`cron.job_run_details` outlives
  `cron.job`**, so a one-off scratch job someone created and dropped reads as a new error class
  forever. The probe needs a LEFT JOIN.

⭐ **R29 re-derived, and the new shape changes the advice.** 384 startup timeouts = 76.6% of pg_cron
failures, unchanged — but per job it is **flat at ~1.1–1.9%** regardless of schedule, owner or
workload. The absolute leaders rank by run count alone. That corroborates the `max_worker_processes=6`
pool mechanism independently and means **there is no worst job to fix**: the ranking is not a culprit
list.

### Re-derived, and four of them had moved

⚠ **A filed finding is a hypothesis; so is a filed number.**

- **R25** resolved — 95 of 96 dispatches now log. ⚠ **Measure it over 72 h, not 7 d:** `pipeline_runs`
  retains ~73 h and `cron.job_run_details` does not, so the 7-day version reads 12-of-28 and invents a
  57% logging gap. **Two instruments with different retentions cannot be differenced over a window
  longer than the shorter one.**
- **R53** — 437 → **149 min/week**; `impossible-parallel` p50 489 → 187 s, fails 34.6% → 10.7%.
  Recorded, **not attributed**.
- **R54 / R55** closed on four clean ticks each. R55 does better than reading green: one run carries
  `truncated_steps: ["seed_recent"]` at 120,285 ms — the contract engaging, not just an absence of red.
- **R30's falsifier is durable now** (`extra_num_sums`, shipped hours earlier), and **08-30 already
  contradicts the zero**: AllDay 167,970 attempted / 15,425 lost, *after* the 08-28 experiment.
  ⚠ Do not divide those two keys naively — Pinnacle reads `lost > attempted`, so they are not over the
  same population.
- **pack-EV**: both measurements the 08-18 filing owed are done. **Zero editions carry snapshots under
  two collections** (so the feared pre-existing defect does not exist), and the **"cost crossover" is
  refuted** — the plan never flips from 3 to 3,097 ids. The `= ANY` route dies anyway, for a *measured*
  reason: its cost is **linear in the id count**, because it prunes which rows are read and not which
  **versions**. ⭐ **A refutation whose stated mechanism is false is not safe to inherit.**
- **wmc-metadata's exit condition, run 4 h early, answered the branch nobody expected**: `fillable`
  climbed 0 → 29, and the regeneration is on the **editions** side — an edition gaining a value flips a
  months-old wmc row with nothing touching `wallet_moments_cache` at all.

## 2026-09-01 (night) — six pipelines were green and doing nothing; found by one census, fixed, and two of my own fixes corrected within the hour (Claude Code, cloud session, push-enabled)

Started by draining the newest inbox filing and ended up with a class rather than a list. Every defect
below reported `ok`, and most reported a healthy row count while converting nothing. Durable lessons are
promoted to `docs/reference/key-files-and-honesty.md`, the register and the ledger — not left here.

**The instrument that found them.** A 30-day yield census over `pipeline_runs_daily` (indefinite,
unlike `pipeline_runs`' ~73 h). ⛔ **It is a triage list a human reads, never an arm** — of nine
pipelines it lists, seven are false positives, including the top row by a factor of twelve
(`snapshot-pack-asks`, whose `rows_found` is the size of the live listing set). **`rows_found` does not
mean the same thing twice across the fleet.**

**Shipped**

1. **`lock-check-batch`** — 9,590 checks/24 h to 12 SEEDED wallets, **0 to any saved or linked wallet**,
   at 48/48 ok. 1.47 M NULL `lock_checked_at` values make the ordering one huge tie, and mass decides.
   Added a wallet-class tier — in **two** places, because the priority leg's own `LIMIT` discards user
   rows before `ranked` sees them. Verified: 200/200 to user wallets.
   ⚠ A concurrent session shipped the same fix 3.5 min earlier under the same migration name; both
   applied rows are kept, because parity keys on NAME and one file would have hidden the other.
2. **`sales-counterparty-backfill`** — 288 runs/day, **10.25 h**, zero recovered. It had walked past
   Flow's prune horizon, which answers **HTTP 200** with `execution: "Pending"` and no events.
   24 `pg_net` probes bracket the wall inside 2023-11-08 to a window containing **no `sales` row at
   all**, so the floor partitions exactly. Verified: **0/120 → 109/120, 3× faster.**
3. **A race 42 s later** — an in-flight tick wrote its stale cursor back over the migration's reset, and
   the next tick returned 0 rows looking perfectly healthy. The claim now self-heals a cursor stranded
   below the floor.
4. **`rpc_ops_snapshot.pipeline_fails_24h`** — 51 of 82 failures were ONE Top Shot 530 across seven
   pipelines, so the list ranked a pipeline with fresh data above three that were losing rows. Now
   carries an `upstream` count and orders on `(fails - upstream)`, with a drift guard on the signature.
5. **`topshot-buyer-backfill-historical`** — re-decoded the **same 45 rows** 47×/day forever, with
   `decode_404`, `decode_failed` and `decode_other_status` all zero. Verified: 45 → 0 found, 12.0 s →
   5.2 s, and the 45 exhausted rows are now reported rather than silently absent.
6. **`pipeline_runs_daily.extra_num_sums`** — the daily rollup kept only which `extra` keys existed,
   never their values, so every per-step counter died at ~73 h. Three measurements this week ran into
   that, one of them fatally (R30's falsifier). Now durable — and it preserved tonight's own before/after.
7. **`allday-price-recover`** — a **9,859-row** recovery moving at ONE row per tick: no `ORDER BY`, so
   the same physical page every time, 999 of 1,000 discarded as multi-NFT. Its `CANDIDATE_LIMIT = 2000`
   was silently clamped to 1,000 by PostgREST, which is why `candidates` read exactly 1000 forever.
   Verified on the first tick: **`tx_decode_ok` 1 → 500, `skipped_multi_nft_rows` 999 → 0** — the
   backlog goes from ~137 days to about seven hours.
8. **`backfill-topshot-subedition-circulation`** — logged `ok: true` with `errors_sample: []` on every
   daily run while reading NOTHING, because `ok` came from per-edition WRITE errors and the failure was
   the upstream READ. With `ok` true it could not even trip the arm built for this shape, whose
   predicate is `ok_runs = 0`.
9. **`check_secdef_anon_execute_violations`** — the guard watching for anon-executable SECURITY DEFINER
   functions iterated **nine hardcoded names**; the schema has **559**. Found by asking Supabase's own
   advisor the same question and getting a different answer. Nothing is exposed — all four reachable
   rows are deliberate — but the zero it published meant almost nothing. Now a tree walk, 9 → 559.

**The two corrections, both of my own work, both within the hour**

- **"450,987 recoverable" was wrong by 10×.** It counted rows whose TRANSACTION IS REACHABLE, not rows
  whose SELLER IS DECODABLE — the same circular sizing the fmv-recalc filing described the day before,
  which I had read. It surfaced by watching the **second** tick: the first recovered 109/120 and the next
  six recovered **0 of 720**. Probed: 21 of 21 hash-bucket-sampled studio-history txs are
  `NFTStorefrontV2.ListingAvailable` with zero Withdraw events, against a 3-of-3 positive control. A
  listing moves no NFT. **Reachable 450,987 · decodable ≈ 38,200.**
- **The exclusion that fixed it had the NULL trap.** `NOT IN` yields NULL for a NULL source, so an
  unlabelled row would have been dropped silently — the shape of everything else fixed tonight,
  reintroduced by the fix for one of them. `IS DISTINCT FROM` attempts it instead.

**What I would tell the next session**

- ⭐ **Watch the SECOND tick.** The first tick after a fix is the one most likely to flatter it. Both
  corrections above came from the tick after the one I would have quoted.
- ⭐ **Reachable ≠ convertible.** A floor or a predicate tells you the first; only sampling the
  conversion tells you the second.
- ⭐ **Build a suppression list from the WALK'S OWN OUTPUT.** Mine came from another instrument's
  summary and the post-state caught the function I had skipped.
- ⭐ **A cursored backfill needs a FLOOR, not just a cursor** — a newest-first walk terminates only by
  running out of data.
- ⭐ **`res.ok` is not a liveness check** when the upstream answers 200 for data it cannot serve.

**Not shipped, deliberately.** `resolve-topshot-stubs` (37 rows in 74,800 attempts over 36 days) is
filed rather than fixed: its rotation works as designed and the cheap fix uses the same column for
"attempted" and "changed". The 408,309 undecodable and 37,832 unsplittable rows still inside their
predicates want re-classification, which is a large data mutation and deserves its own change.

**Verification still owed:** `backfill-topshot-subedition-circulation` runs daily at 21:10Z, so its
`ok:false` + `gql_fault_reason` land tomorrow, not tonight.


---

## 2026-09-01 (evening→night) — drained two cloud handoffs and both monitors, then found four defects by verifying my own work (Claude Code, Trevor's box, push-enabled)

Started as a drain of the 09-01 cloud handoffs and the two daytime monitors. Most of the value came
afterwards, from checking whether the things I had just shipped actually ran. Durable lessons are
promoted to CLAUDE.md, `docs/reference/*.md`, the ledger and memory — not left here.

**Shipped**
1. **20 fileless migrations recovered and committed** across the session (9 + 3 + 4 + 4), all byte-exact
   via `recover-fileless-migrations.mjs`, never retyped. Two sessions independently recovered the same
   nine and got byte-identical files — which is what makes the new CI auto-recovery safe to run
   unattended.
2. **`enrich-ufc-wallet`** reads FMV through `get_fmv_snapshot_for_editions` instead of 200-wide
   PostgREST slices. Confirmed live: **~185,457 → 3,268 buffers/call, ~29,400 → 97.5 ms**, and the old
   queryid left the top-12 of a 7.5 h `ops_pgss_delta`. Both halves of its EXIT met.
3. **`cache-refresh`** batches the `lock_checked_at` stamps (ten single-row UPDATEs → at most two).
4. **fmv-recalc Steps 5c/5d/5e** use a per-edition LATERAL instead of a whole-history `DISTINCT ON`.
5. **fmv-recalc historical fallback** made finishable, then its admission predicate corrected.
6. **`/insights/pack-reality`** stopped claiming the market is empty when our own prices are stale.
7. **`scripts/resolve-ledger-rebase-conflict.mjs`** + test — the rebase recipe as an executable.
8. **`backfill_wmc_metadata_from_editions` drained** (152 rows; `fillable` now 0).

**Closed by re-derivation, not by new work — #41 and #47.** Both recorded numbers that described regimes
which no longer existed. #41's `7 */6` schedule had been reverted; split at the change point, running the
job **3.2× MORE OFTEN** cut failures **23.1% → 4.0%** and waste 41% at unchanged total cost, because a
`REFRESH … CONCURRENTLY` costs what changed since last time. #47's falsifier is answered and not met
(6/12 → 0/5 kills), **but the duration collapse lands a day after the deploy**, so "the kills stopped" is
established and "the schedule change stopped them" is not — recorded as confounded rather than banked.

**The four defects, all found by verifying my own work rather than by looking for them**

1. **fmv-recalc's historical fallback had failed on 350 of 350 runs** and reported
   `historicalFallback=0` — indistinguishable from "nothing to do" on every instrument anyone reads.
   Found by asking whether the change I had *just* deployed actually executed, which needed the runtime
   logs because `pipeline_runs` cannot see it and pgss cannot see through `query_sql`.
2. **My own fix then failed its exit condition** — 23,800 editions "covered" for ~895 net progress. The
   admission predicate tested `algo_version NOT LIKE '1.7.%'`, a staleness proxy from when 1.7.x was the
   only writer; there are now eight and seven do not match, so this step and the thin-sales guard were
   overwriting each other every tick. **99.6% of admissions were false.**
3. **`/insights/pack-reality` was telling the public "No +EV packs right now"** while `pack_ev_latest`
   held 4,640 fresh rows. The 48 h freshness clause alone emptied the board; the three survivors were
   107–130 h stale behind the dead Top Shot endpoint.
4. **`sales-counterparty-backfill` has walked past Flow's spork wall** and burns ~9.2 h/day for zero —
   because Flow REST answers pruned history with **HTTP 200 + `execution:"Pending"` + zero events**, so
   an unreachable era is indistinguishable from a throttle miss. Filed; the fix needs `wrangler`.

**Corrections I made to my own work, in order**
- Bracketed a constant `171` with the fallback finding because the log lines were adjacent. All 171 have
  zero paid sales, so that step could never reach them. Two numbers on adjacent lines are not a
  relationship.
- Sized that backlog at "4,277 convertible" using the very predicate that was defining it wrongly. The
  real figure was ~13–31. **Circular, and the more useful lesson than the SQL.**
- Claimed the redundant writes would "disappear from `fmv_snapshots`" — unmeasurable that way, because
  the table is delete-then-insert and row counts by hour measure survivors, not writes.
- Read `[exited with code 0]` from a piped `npm test` over a run whose own summary said two tests failed.
- Queried Vercel with a `since` timestamp in the **future**, got zero deployments, and nearly read it as
  "no deploy fired".
- Hit **instance seven** of the unanchored conflict-marker false positive, an hour after reading the
  paragraph that documents it. That is what prompted turning the recipe into a script.

**Refuted before acting**
- The queued `edition_fmv_current` swap would have **corrected 41 editions' confidence wrongly and
  corrupted 4** — membership agreed perfectly (27,170 = 27,170) while the column being filtered on did
  not. Third independent time that table has been measured non-substitutable.
- A filed fix for `offers-sweep`'s 530 alternation would have **disarmed the upstream breaker**. The
  alternation is arithmetic: 30 min window ÷ 20 min tick = one real attempt per two ticks, a period-2
  square wave with no jitter. The `ok=false` is load-bearing state.
- The `paginateGroup` chunking #47 names as its durable fix is **not indicated** by the evidence.

**Guards that caught me, and were right**
- `insights-client-dates-are-hydration-safe-guard` reddened on a `Date.now()`. My reasoning was sound but
  the justification sat in a block comment above the `useMemo`; the guard requires it **inline within 4
  lines of the call**, because an escape placed anywhere in the file is an allowlist with extra steps.
- The inbox INDEX count guard caught a stale header twice, both times from concurrent sessions.
