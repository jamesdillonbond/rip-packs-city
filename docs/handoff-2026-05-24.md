# Claude Code Handoff — 2026-05-24 (post-Cowork pass)

Companion to `docs/audits/cowork-platform-pass-2026-05-24.md`. The Cowork session shipped 3 DB migrations live but could not push code (git index corrupt in the sandbox). This handoff covers the code-side follow-ups.

**Do step 0 first, then review `git status` before anything else — the working tree state is uncertain.**

## Step 0 — git state

The "corrupt index" reported by the Cowork sandbox was a cross-OS mount artifact — git is healthy on the Windows side (`git reset` ran fine). Just confirm state:

```
cd /c/Users/TDill/rip-packs-city
git status
```

Only if git reports a real index error, repair with Git Bash syntax (`rm`, not `del` — `del` is CMD-only):

```
rm -f .git/index.lock .git/index && git reset
```

## The Claude Code prompt (paste into Claude Code, plain text)

Work directly on main, commit and push to main, no branches no PRs per CLAUDE.md. Do these in order and stop to show me git status after step 1.

1. Repo hygiene. These files are tracked scratch/fixture junk per the May 20 audit (F20): the 11 livetoken-portfolio JSON files at repo root (livetoken-portfolio.json and livetoken-portfolio (1).json through (10).json), test-gql.json, sniper.json, nftlocker-contract.json, nftlocker-full.json, flowty-locker-test.json, and the two zero-byte junk files named exactly "main" and "Invoke-RestMethod" at repo root. Run git rm --cached on all of them, confirm they are covered by .gitignore (add patterns if not), and delete them from disk. Also check whether scripts/cleanup-wmc-int-orphans.mjs still exists — the wmc edition-key drain it belonged to is fully retired, so delete it if present.

2. Dead Flowty ingest routes. The Flowty marketplace shut down and every Flowty cron is deleted. Per docs/audits/flowty-teardown-plan-2026-05.md these API routes are orphaned. Delete the route directory for each that still exists: app/api/flowty-enrich, app/api/flowty-harvester, app/api/flowty-listings, app/api/flowty-loans, app/api/flowty-monitor, app/api/flowty-offers, app/api/flowty-sales, app/api/flowty-tx-scanner, app/api/sync-flowty-listings, app/api/wallet-enrich-flowty, app/api/cron/extract-flowty-purchases, app/api/cron/extract-flowty-offers, app/api/cron/prune-flowty-archive. Do NOT delete app/admin/flowty-analytics or app/admin/flowty-errors or anything under lib/ — those are kept as the frozen historical archive per the retention decision. After deleting, grep the repo for imports of each deleted route to confirm nothing references them, and run npx tsc --noEmit to confirm a clean build.

3. fmv-recalc acceleration. Open app/api/fmv-recalc/route.ts. It is mid its first full sweep of the 262,733-row sales table and ~9 days from completing it, which is why ~4,168 recently-traded editions still have no canonical 1.7.0 FMV snapshot. Two changes: (a) if the sales scan is ordered oldest-first, change it to scan recent sales first (ORDER BY sold_at DESC) so live editions get priced within one cycle instead of last; (b) raise DEFAULT_LIMIT from 1000 to 2500 (the route already has maxDuration 300, confirm 2500 fits the time budget — if a run starts timing out, fall back to 1500). Run npx tsc --noEmit.

4. Commit. If the May 20 audit fixes are still uncommitted in the working tree (analytics crash guards in app/(collections)/[collection]/analytics/page.tsx and components/analytics/ListingsDashboard.tsx, app/global-error.tsx red accent, .github/workflows/ci.yml, .gitattributes, next.config.ts redirects, ops-monitor.yml schedule, the Pinnacle round-trip cap), commit them as their own commit first using the sequence in docs/audits/audit-2026-05-20-full-platform.md section 5. Then commit steps 1-3 as separate logical commits. Push to main. Run the smoke test after deploy.

## After Claude Code — cron-job.org (manual)

- Increase the fmv-recalc cron frequency (e.g. every 10-15 min) to speed the first full sweep.
- Delete the "RPC Listing Divergence AllDay" cron entry if it still exists (last ran 2026-05-23 18:07).

## Notes

- The 3 DB migrations are already live (audit_20260524_block_stale_ingest_fmv_algo_haircut, audit_20260524_purge_fmv_1_1_0_clobber_residue, audit_20260524_pack_ev_targets_unpoison_via_history). Do not re-apply.
- Pack EV queue is unpoisoned and will self-drain over ~2 days — no action needed.
- If the working tree still shows the 269-file CRLF churn, run `git add --renormalize .` as its own commit after .gitattributes is committed, per the May 20 audit.
