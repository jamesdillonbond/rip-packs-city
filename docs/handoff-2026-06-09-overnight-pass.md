# Handoff — 2026-06-09 overnight autonomous pass

**Mode: GENUINE OVERNIGHT (~01:02 PDT / 08:02Z, in-window) + NO-PUSH (no GitHub creds — `git push --dry-run` → "could not read Username").**
Shipped 0, reverted 0, repaired 0. Platform GREEN except one active trust-health BREACH (Pinnacle ASK staleness, saturation-driven, self-healing). All doc outputs on this run are **on disk, uncommitted/unpushed** (a future push-capable session commits them, as `493fe8e` did for the 06-08 batch).

Lock: took over the stale/released `docs/overnight/.lock` (prior run released it 2026-06-08T08:17Z, ~24h old; unlink not permitted on this mount). Re-created LOCKED, will overwrite with RELEASED marker at end.

Baseline `origin/main` = local HEAD = `b7211fb` (0 ahead / 0 behind; tree clean — the historical phantom-CRLF divergence stayed resolved).

---

## What was reviewed
- **CLAUDE.md** in full; **ledger.md** (Queued + Shipped; no populated "Declined" section exists yet — only the intro reference + the per-item PACKEV "stays DECLINED" note).
- **7 inbox files drained** (06-08T15:05Z scheduled-runs-review, 06-08 15:10/18:12/22:21Z + 06-09 00:14/03:15/06:18Z daytime-monitor) — all archived to `inbox/archive/`.
- Most recent overnight handoff (`docs/handoff-2026-06-08-overnight-pass.md`) + `metrics-latest.json` (06-08T08:15Z baseline) + `focus.md` (06-07 → 08, stale but its DUPE1-gated / "don't touch Pinnacle FMV in-flight" rules still honored).
- Live: `git log -30`, `pipeline_runs`, Supabase security catalog SQL, Vercel deploys (20/20 READY), Sentry (21 unresolved), `v_rpc_trust_health`, 17 Cowork artifacts.

## Post-ship regression watch (last ~24–48h) — nothing reverted
- **`b7211fb` "resolve Top Shot buyer + capture execution accounts on every sale" — the freshest ship, deployed 06:19:19Z, AFTER the 06:18Z monitor (so unseen by any monitor) → my priority check. PASS.** Sales-indexer healthy (last 08:03Z, 68 runs/24h, **0 fails**) despite the new per-sale `/v1/transactions?expand=result` decode (budgeted 60/tick). Buyer/payer/proposer resolution is working at **100% on every post-deploy sale** (06Z 21/21/21/21, 07Z 4/4/4/4, 08Z 18/18/18/18) vs **0%** pre-deploy — the flagship went from 100% buyer-blind to fully buyer-resolved. New `sales.payer_address`/`proposer_address` columns + `v_sale_execution_accounts` monitor view present and populating. Minted **0** Sentry issues. (The "27% in 3h" I first measured was just pre-deploy 05Z sales diluting the window.)
  - **WATCH (folded to monitor, not a regression):** TS sale *volume* in the first ~2 post-deploy hours (06–08Z: 4–21/hr) sits at the low end of yesterday's same hours (44–98/hr). Attributable to the normal evening→late-night diurnal dip + TS's inherently spiky volume (32h range 45–254/hr), and **corroborated as NOT throttling by the 100% buyer coverage** (if the indexer were the bottleneck, the 60/tick decode budget would exhaust and buyer coverage would drop first) + 0 fails + normal ~20-min cadence. Re-confirm volume recovers to the diurnal norm at the next US-daytime peak (~16–23Z 06-09); if it stays depressed, revisit whether the per-sale decode is throttling ingestion.
- **PINBACKUP-RLS — RESOLVED 06-09 03:15Z** (migration `audit_20260609_lock_pinnacle_backup_and_rewards_tier_search_path`, by an earlier Cowork/operator session). Verified tonight: `check_public_security_invariants()` = `[]`; 0 RLS-off base tables; 0 anon/auth-write-on-RLS-off base tables; Sentry `NEXTJS-1C` last-seen 9h ago (pre-fix), quiet since. No recurrence.
- **`f9210cf` dapper.market second buy link** — additive UI, monitor-confirmed no regression; no backing pipeline.
- **Candy chain-two (`52072be`/`d079e5b`/`638e54c`)** — inert (writes only for `collection_id 209ade70…`, `is_active=false`); no live pipeline; editions flat.
- **Brand Phase-2 tokenization (`4be97c5`→`57b3def`, Batches 1–5)** — UI only, all READY, no regression.
- **Legacy-FMV retirement (`ab2e6f4`/`b367e76`)** — its same-day backup table caused PINBACKUP-RLS (now resolved) and made `pinnacle-listings-reconcile` the load-bearing Pinnacle ASK writer (see PINNACLE-RECONCILE-TIMEOUT below). Artifact backing relations validated healthy post-drop by the monitors.
- **`04011b3` concierge ship** — caused PINFMV-DRIFT-14 (still live; off-limits, carried — see Queued).
- **`5880eeb` pinnacle-sync render-FMV `statement_timeout` fix** — validated only by the daily ~10:07Z tick, which fires ~1.5h AFTER this run. `pinnacle-sync` last ran 06-08 10:07Z (the failed pre-fix tick). Render FMV currently fresh (`pinnacle_catalog.fmv_computed_at` 06-08 17:38Z from the post-migration manual run). → PIN-SYNC-FMV-WATCH (carried).
- **No auto-revert performed:** no DB-revertable regression exists. PINFMV-DRIFT-14's revert (`git revert 04011b3`) would reintroduce the worse dead-Flowty fabricated-discount bug and is off-limits route logic. PINNACLE-RECONCILE-TIMEOUT is saturation-driven, not a revertable code bug.

## Section 2 health-drift findings + deltas (vs 06-08T08:15Z metrics baseline)
- **Security: CLEAN (0/0 all four checks).** `pg_tables` RLS-off `[]`; anon/auth-write-on-RLS-off base tables `[]` (the ~51-row stock-query result is the documented all-views false-positive — needs `relkind IN ('r','p')`); `check_secdef_anon_execute_violations()` `[]`; `check_public_security_invariants()` `[]`.
- **`v_rpc_trust_health`: 1 BREACH — `pinnacle_ask_stale_hours` = 3.8h (breach_at 3), climbing.** Other 5 metrics ok (`edition_integrity_flags` 4 / `fmv_sanity_flags` 0 / `pack_ev_board_max_stale_days` 1.1 / `pack_ev_board_pct_depleted` 0 / `ts_uuid_dupes_created_24h` 0). → PINNACLE-RECONCILE-TIMEOUT below.
- **`detect_stalled_pipelines()` / `get_pipeline_alerts()`: 5 entries — all the CRON-DROP-WAVE cluster.** `wallet-backfill-multicollection-complete` (HIGH, 442m) — but this is a **telemetry artifact**: the constituent backfills all ran at 06:45–06:58Z (`wallet-backfill`/`-allday`/`-golazos`/`-pinnacle`/`-ufc`/`-dispatch` all 81–94m), only the completion-rollup row didn't log for the late/staggered 06:00 wave. `ultimate-fmv-recalc-v1` (info, ~daily, 1532m), `backfill-pack-rip-metadata` (info, 194m), `populate-pinnacle-wmc-fmv` (medium, 184m, just over its 180m), `refresh-pack-grail-metrics-mv` (info, 104m). **`fmv-recalc` is NOT in the list** — it dropped ticks 04:48→06:28Z, fired once at 06:28Z, now 101m silent (under its 120m/high watchlist threshold). All external-cron (cron-job.org) tick-drops, 0 route crashes.
- **Sustained overnight DB-saturation window ~05:00–08:30Z (I1/DBSAT class, self-recovering).** In this window: `pinnacle-listings-reconcile` failed every tick 05:09→08:09Z (recovered 08:24Z), `analytics-smoke` failed every tick 05:13→08:13Z (`analytics_smoke_run: statement timeout`, was ok 04:13/04:43Z), the FMV-confidence `DISTINCT ON` queries (`sentinel_fmv_confidence_rows`, `fmv_current`) timed out on 4 attempts, and fmv-recalc's cron dropped. Notable because it's an *overnight* window (DBSAT was previously daytime 09–11Z + evening 18–20Z) — a data point for the 06-09 DBSAT re-baseline the monitors called for: there IS a residual saturation component beyond the 14:55Z cross-collection-refresh fix.
- **Sentinel TS-UUID-keyed-48h = 0** (total 6,404 historical fossils, flat); `ts_uuid_dupes_created_24h` = 0. DUPE1 re-mint fully stopped + 48h window rolled off (was 542 at the 06-08 baseline).
- **Editions flat:** TS 15,542 / AllDay 6,191 / Golazos 581 / UFC 446 (identical to baseline).
- **DB size 6,687 MB** (+205 since the 06-08T08:15Z baseline of 6,482; the monitors tracked the creep 6,633→6,679→6,682→6,687). Watch-only — dupe-fossil roll-off + index churn + the one-time 14:55Z `idx_wmc_cohort_cover` build.
- **unmapped_sales open = 183** (flat).
- **FMV: writers fresh, histogram unavailable this run.** `allday-fmv-populate` last 08:02Z (0 fails/24h), `topshot-sales-indexer` 08:03Z (0 fails), `offers-sweep` 08:02Z (0 fails); fmv-recalc 57/24h, 3 fails (CRON-DROP). `fmv_sanity_flags`=0. The latest-per-edition confidence histogram could not be measured — `sentinel_fmv_confidence_rows` + raw `DISTINCT ON` + `fmv_current` all timed out (the 05–08:30Z saturation). Last-known (06-09T06:18Z monitor): **TS HIGH+MED ≈ 2,917 / NO_DATA ≈ 5,029 (improving); AllDay HIGH+MED ≈ 488**. FMV health confirmed via fresh writers + 0 sanity flags; exact histogram is non-critical.
- **Vercel: 20/20 recent prod deploys READY, 0 ERROR.** Current prod = `b7211fb` (`dpl_39L6z31zJ8uD2zy59JUypd6iKE7u`, rollback candidate).
- **Sentry: 21 unresolved, no net-new from any recent ship.** NEXTJS-14 (PINFMV-DRIFT-14, carried HIGH); the smoke-saturation cluster (W/4/E/1E/1H/1J/A, last seen ~2h ago = the 06–08Z saturation/storm, super_low actionability); NEXTJS-15 (Q4 `cadence_capped`, carried); NEXTJS-1C (PINBACKUP-RLS echo, quiet 9h → markable resolved 24h).
- **Artifacts: 17, all healthy.** Backing relations validated by the monitors post-Pinnacle-legacy-drop (06:18Z: 17/17 resolve). None flagged broken, none drifted, none repaired (per "don't regenerate working artifacts").

## Shipped this run
**None.** NO-PUSH blocks all code commits + Vercel deploys (Vercel builds from GitHub `main`); the only DB-eligible candidate (PIN-SYNC-FMV-WATCH's render-FMV-staleness companion) was QUEUED, not shipped — reasons under Queued. An honest quiet night.

## Auto-reverted / failed this run
**None.** No verification failures (nothing shipped → no subagent verification step needed). No regression met the auto-revert bar.

## Queued — needs operator / Claude Code / Trevor

### PINNACLE-RECONCILE-TIMEOUT (NEW · MED-HIGH · CC/operator) — active `v_rpc_trust_health` BREACH on the now-load-bearing Pinnacle ASK path
- **Symptom:** `pinnacle_ask_stale_hours` = 3.8h (breach_at 3) and climbing; freshest `pinnacle_editions.ask_updated_at WHERE ask_source='pinnacle_direct'` = **04:39:08Z** (frozen ~3.8h). `pinnacle-listings-reconcile` failed **every** tick 05:09→08:09Z (`canceling statement due to statement timeout`, 8–23s, hitting the ~8s service_role statement cap), self-recovered at 08:24Z but its rare successes haven't advanced the freshest ask past 04:39Z. 12h: 30 ok / 17 fail; 24h: 92 runs / 34 fail.
- **Why it matters now:** the 23:00Z Flowty-cache retirement (`04011b3`/`ab2e6f4`) consolidated Pinnacle ASK onto this on-chain reconcile path, so its rush-fragility now directly degrades displayed Pinnacle ASK freshness (an intelligence-product surface) and trips the `pinnacle_ask_stale_hours` metric (itself added today via the `4abfc36`-logged Cowork migration). The `rpc-trust-health-watch` task will surface this BREACH.
- **Root cause:** saturation-driven, not a code regression — it times out during the sustained 05–08:30Z I1/DBSAT window (same window that took down analytics-smoke + the FMV-count queries + fmv-recalc's cron). Self-heals when saturation lifts.
- **NOT auto-shipped / NOT auto-reverted:** (a) it's Pinnacle pricing route/function logic = off-limits class + focus.md item 2; (b) no clean revert (reverting the Flowty-cache retirement reintroduces the dead-Flowty fabricated-discount concierge bug and still wouldn't refresh asks — Flowty is frozen); (c) NO-PUSH blocks the route side regardless.
- **Ready options for CC/Trevor (do NOT auto-apply):**
  1. *Mirror `5880eeb`* — give the reconcile RPC a longer `statement_timeout` (CREATE OR REPLACE with `SET statement_timeout TO '120s'`, body byte-identical, preserve grants per the rpc-migration checklist). **CAVEAT (important):** reconcile fires every ~15 min (vs pinnacle-sync's daily), so a 120s timeout would hold a connection up to 15× more often during rush windows — it could *worsen* the I1/DBSAT contention rather than fix it. Validate against connection-pool pressure across several rush cycles before trusting it.
  2. *Make reconcile incremental / lighter* (only reconcile listings changed since last run) — addresses the root (the query is too heavy under contention) without holding long connections.
  3. *If the reconcile cadence genuinely can't hold the 3h ASK-freshness SLA under rush*, widen the `pinnacle_ask_stale_hours` breach threshold from 3h to ~4–5h — but only after confirming option 1/2 isn't the better fix (do NOT just widen to silence a real degradation).
- **Re-check:** once the saturation window passes, confirm reconcile resumes writing fresh asks and `pinnacle_ask_stale_hours` falls back under 3.

### CRON-DROP-WAVE (carry/escalate · operator · 2nd consecutive overnight) — cron-job.org selective tick-drops; `fmv-recalc` worst
- Generalizes the 06-08 LISTCACHE-CRON-DROP. Overnight ~04:23–06:28Z a :08–:53-anchored cluster of cron-job.org-triggered jobs dropped ticks: **`fmv-recalc`** (load-bearing sales-FMV confidence sweep, watchlisted @120m/high — fired once 06:28Z then 101m silent; closest to tripping `detect_stalled` HIGH + the smoke `fmv pipeline healthy` check), `refresh-pack-grail-metrics-mv`, `populate-pinnacle-wmc-fmv`, `backfill-pack-rip-metadata`, `ultimate-fmv-recalc-v1`, and the 06:00 `wallet-backfill` wave (ran late ~06:45–06:58Z; only its completion-telemetry row didn't log). All clean external-trigger dropouts (0 route crashes); bounded + self-healing (sales/ASK/offers data stays fresh via the indexers; fmv-recalc down only delays re-confidence of newly-traded editions). **Not auto-touchable** (external cron + NO-PUSH).
- **Action (operator):** since this is the 2nd overnight cron-drop symptom in 24h, do a single consolidated look at the cron-job.org execution history for ~04:20–08:30Z (likely a cron-job.org account/dispatch dip, not 5 independent flakes) rather than per-job re-fires; `fmv-recalc` ("RPC FMV Recalc Force Stale", :08/:28/:48) is the one to re-fire first. The `-v2` listing-cache residual cron-entry removal is folded in here.

### PINFMV-DRIFT-14 (carry · HIGH · CC/Trevor-owned · OFF-LIMITS to the night pass — do NOT auto-fix/auto-revert)
- Sentry `JAVASCRIPT-NEXTJS-14` "Pinnacle FMV not borrowed across characters (drift guard)" — still `unresolved`/`regressed`, **22 events, last ~2h ago**. Cross-character FMV leak reintroduced by the ~23:00Z `04011b3` concierge ship; deterministic, re-trips each smoke run while the leaked row persists (repro: Goofy / Mickey & Friends Vol.1 / Standard fmv=$1.247). Narrow but real (1-row wrong FMV on the concierge deals surface).
- **Fix is concierge / Pinnacle-FMV route logic (off-limits + focus.md item 2):** in `searchPinnacleDeals`/`searchPinnacleByName`, join FMV to `pinnacle_catalog` on the SAME `render_id` as the ask, not a collapsible `(character,set,variant)` triple. Probe: `SELECT render_id, fmv_usd, floor_ask FROM pinnacle_catalog WHERE character_name='Goofy' AND set_name ILIKE '%Mickey & Friends Vol.1%' AND variant ILIKE 'Standard'` — >1 render sharing the trimmed triple = the collision. Then re-resolve NEXTJS-14 with regression arming. Last-resort `git revert 04011b3` reintroduces the dead-Flowty fabricated-discount bug — prefer the keying fix.

### PIN-SYNC-FMV-WATCH (carry/refine · LOW-MED · CC/operator) — refines PIN-SYNC-CRON
- **(a) Verify the ~10:07Z 06-09 `pinnacle-sync` tick logs `ok=true`** now that `5880eeb` (render-FMV `statement_timeout` 120s) is live — fires ~1.5h after this run. `pinnacle-sync` last ran 06-08 10:07Z (the failed pre-fix tick); that success is the gate PIN-SYNC-CRON's watchlist INSERT waits on. Until then a watchlist row would immediately false-positive (only 1 run/24h).
- **(b) Add a render-FMV-staleness companion to `v_rpc_trust_health`** (the ASK companion `pinnacle_ask_stale_hours` was added today; the render-FMV side is still unmonitored, so a `pinnacle-sync` freeze would go uncaught the way the ASK freeze now is caught). **Ready migration** (verified view shape — append one `UNION ALL` branch matching `(metric text, value numeric, breach_at numeric, catches text)`; the outer SELECT computes status):
  ```sql
  -- CREATE OR REPLACE VIEW public.v_rpc_trust_health AS ( ... existing branches ...,
  --   add inside the `raw` CTE UNION:
  SELECT 'pinnacle_fmv_stale_hours'::text AS metric,
         (SELECT COALESCE(round(EXTRACT(epoch FROM now() - max(pc.fmv_computed_at))/3600::numeric,1),999::numeric)
            FROM pinnacle_catalog pc WHERE pc.fmv_usd IS NOT NULL) AS value,
         30::numeric AS breach_at,   -- daily cadence + ~6h grace
         'render-FMV recompute (pinnacle-sync / pinnacle_fmv_recalc_render_all) frozen'::text AS catches
  ```
  Revert: re-CREATE the prior view body (capture it first via `pg_get_viewdef`).
- **NOT auto-shipped tonight:** it's a CREATE OR REPLACE on a live monitoring view the `rpc-trust-health-watch` task reads, it's Pinnacle-FMV-adjacent (the suggesting monitor leaned CC/operator), it pairs with the (a) tick-verify gate I can't complete this run, and shipping a trust-health-view change *during* an active saturation + active breach window is poor timing/hard to cleanly verify. Cleanest sequencing: confirm (a) first, then add (b).

### b7211fb-VOLUME-WATCH (NEW · watch · monitor) — re-confirm TS sale volume at the next peak
- Post-`b7211fb` TS sale volume in the first ~2 post-deploy hours (06–08Z, 4–21/hr) was low vs same hours yesterday (44–98/hr). Almost certainly the diurnal dip + spiky volume (100% buyer coverage proves the indexer isn't dropping sales), but worth one confirmation: the daytime monitor should check TS sale volume during the next US-daytime peak (~16–23Z 06-09) recovers to the normal 100–250/hr band. If it stays depressed, investigate whether the per-sale `/v1/transactions` decode is rate-limiting/throttling the indexer.

### Carried (unchanged, with night-count)
- **SMOKE-EDITION-TIMEOUT** (NEXTJS-1H + 1J) — recurred (1H 6 events, 1J 4 events, both ~2h ago = saturation tail); assertion-class → bypasses SMOKE-RETRY; touches `app/api/smoke-test/route.ts` (hot file + NO-PUSH). Bump the smoke per-fetch timeout for the heavier edition/pack-dist endpoints or route the abort through SMOKE-RETRY. (2 nights)
- **DBSAT re-baseline** — tonight's overnight 05–08:30Z saturation is a data point that a residual saturation component survives the 14:55Z cross-collection fix; the monitors' 06-09 clean-day re-baseline should include this window. Do NOT blindly raise statement_timeout (I1 lesson). (ongoing)
- **NEXTJS-15 / Q4** — `cadence_capped` counted toward the listing-resolution spike gate (benign false-positive); exclude `cadence_capped` from the gate whenever Q4 is next touched. (carry)
- **N1** (`snapshot-institutional-wallets` irregular cadence — operator re-fire/move slot), **P3-BUYERS** (`pinnacle-resolve-buyers` flaky external trigger — watch for a 3rd dropout), **CRON-30S items 3/4 + token hygiene**, **PIN-FMV-REKEY-WAVES 2/3** (Trevor), **PACKVIZ-GRID** (CC review), **Q5/Q6/Q8** — all unchanged. **Q7** (NO-PUSH root cause: scheduled sandbox has no GitHub creds / Windows-mounted `.git` fragility) — re-confirmed this run.

## metrics + continuity
`docs/overnight/metrics-latest.json` overwritten with tonight's values. Ledger upserted (PINBACKUP-RLS reconciled resolved; LISTCACHE-CRON-DROP → CRON-DROP-WAVE; PINNACLE-RECONCILE-TIMEOUT / b7211fb-VOLUME-WATCH / PINFMV-DRIFT-14 / PIN-SYNC-FMV-WATCH added/carried). 7 inbox files archived. CLAUDE.md "Recent sessions" prepended. Lock released. **All on disk, uncommitted/unpushed (NO-PUSH).**
