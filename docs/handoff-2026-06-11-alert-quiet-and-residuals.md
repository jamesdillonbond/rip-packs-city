# Claude Code handoff — 2026-06-11 (afternoon): alert-quiet root fixes shipped by Cowork + the four residuals

Context — Trevor's directive: "get all pipelines healthy so Telegram/email alerts stop." Cowork root-caused the alert storm to ONE mechanism (server-side queries needing 9-30s dying at the 8s cap service_role inherited from authenticator during IO-contention windows, then retrying + paging) and shipped the platform-level cures live. What's left for you is four bounded residuals. Claude Code's direct file inspection wins over this doc on any disagreement.

SHIPPED LIVE BY COWORK TODAY (verify, don't redo — full reverts in each migration header):
1. audit_20260611_fix_upsert_topshot_marketplace_fmv_round_cast — TFP's flush fn died on ROUND(float8,2) ("function round(double precision, integer) does not exist", latent, masked for days); guarded in-place cast patch. AllDay twin verified clean.
2. audit_20260611_service_role_statement_timeout_30s — service_role now has its own 30s statement_timeout (was: inherited 8s). anon stays 3s, authenticated 8s — public surface unchanged. This single change retires the recurring marginal-basket failure class (hydrator candidate_read, pack-ev targets, nft-resolver load-batch, buyer-backfill, stub-targets, refresh-error-triage, etc. — they're all service_role reads). Watch-the-watcher note: slow-query ROT is now easier to miss; the weekly check's pg_stat sweep is the backstop.
3. audit_20260611_pgcron_cross_collection_refresh — pg_cron ENABLED; the daily cross-collection refresh now runs IN-DATABASE at 04:10Z (no MCP connector, no role cap; fns carry their own 180s timeouts) + a self-cleaning one-shot at 16:40Z today to clear the current staleness. The Cowork scheduled task was repurposed to verify-only. CONSEQUENCE FOR YOU: the previously-queued "native /api/cron/refresh-cross-collection route" plan is CANCELLED — do not build it.
4. (Earlier today) the TFP cron entry was confirmed NOT broken — its 13:15Z tick fired (ack 886ms); the missing pipeline_runs rows are the ack-green/body-dies class on TFP's own after().

Residual 1 (decided by tonight's 19:15Z tick) — TFP completion. With the round-cast fixed, the 19:15Z tick is the first chance in days for a full sweep+flush. IF it logs ok=true with upserted>0: run UPDATE pipeline_cadence_watchlist SET max_silent_minutes=480 WHERE pipeline='topshot-fmv-populate'; and TFP is closed. IF it acks on the cron dashboard but writes NO pipeline_runs row again: TFP's after() body is being killed (deploy-swap recycling or budget) — give it the fmv-recalc treatment: a run-started heartbeat marker (the e386542 correlation pattern) and/or shrink per-tick work so the body finishes well inside maxDuration 300. File: app/api/topshot-fmv-populate/route.ts.

Residual 2 — analytics-smoke per-leg optimization (carried): leg timing table is in the ledger; optimize the remaining slow legs, then restore the fn cap: ALTER FUNCTION public.analytics_smoke_run() SET statement_timeout='60s'; (currently 110s stopgap). Note the new service_role 30s default does NOT govern this fn (its own 110s setting wins) — the restore matters.

Residual 3 — tshb drain cadence: GHA throttles the 15-min cron hard (2 scheduled fires in ~8h). The self-throttle is proven (it skipped itself at 44 recent fails — working as designed). Once the Supabase IO-budget banner clears (check the dashboard), bump editions/tick 15 -> ~30 in the route config so the sparse GHA ticks each do more; do NOT move it to cron-job.org (ticks run up to ~10 min synchronous — the 30s client cap would kill them). Then run the LT acceptance gate per docs/handoff-2026-06-11-askonly-phase2-greenlight.md once pending approaches 0.

Residual 4 (OPTIONAL, only if pages persist after a week of the 30s default) — saturation-storm consolidation in the pipeline-failure-alerts edge function: when >=4 pipelines fail with the timeout class in one sweep, send ONE "saturation window" digest message instead of N per-pipeline pings. Supabase edge fn — Cowork can also ship this; coordinate via ledger so it's not built twice.

Acceptance for the whole effort: 48h with zero Telegram pipeline pings outside genuine breakage; pipeline_runs fail-rate <2%/hr through the 4 daily wave windows; detect_stalled [] sustained; TFP logging every 6h tick.

Guardrails: direct-to-main, no branches/PRs; PowerShell git; exact-path staging; tsc clean; deploy READY + smoke green; maxDuration cap 800; never echo the pushurl PAT; workflow-file pushes need gh's token (PAT lacks workflow scope).
