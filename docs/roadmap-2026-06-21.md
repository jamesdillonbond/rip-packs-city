# Rip Packs City — Product Roadmap (updated 2026-06-21)

Supersedes [docs/roadmap-2026-06.md](roadmap-2026-06.md). Companion: [handoff-2026-06-21-platform-audit.md](handoff-2026-06-21-platform-audit.md) (this audit). Standing constraints unchanged: **intelligence-first** (cart / Trade Hub shelved), **no paywall / promo until 50+ WAU**, cost-flat infra, chains added one at a time.

---

## Where we are (post-audit, 2026-06-21)

A full platform audit + page-by-page QA ran today (security, DB, pipelines/crons/GHA, FMV, Sentry/Telegram, artifacts/scheduled-tasks/skills, every main page + 10 each of editions/packs/sets/teams, mobile, concierge). **The platform is GREEN and the data is, with two now-fixed exceptions, complete and accurate end-to-end.**

Since the 06-18 roadmap the build shipped a dense, healthy wave: the **TS sales mis-attribution program closed out** (writer leaks fixed `6b9e89a`, on-chain drain + daily Vercel cron, conflation guard converged to **17**), the **thin-data fake-deal flag** on the deal board, **SEO internal-linking** fan-out from overview hubs, the **special-serial-owners MV moved to pg_cron**, the **Next.js 16.2.9 security bump** (clears the proxy-bypass CVE), and a whole **Pack Sniper feature** (live sealed-pack deal feed + Sniper controls + 24h-low signal + mobile cards + hover-enlarge).

FMV quality keeps climbing and holding: **TS HIGH+MED 4,316** (24.9%; was 1,062 on 06-05), AllDay 889, Pinnacle 759 (33.5%), `fmv_sanity_flags` 0, reconciles to edition counts.

### Audit headline: one critical regression, caught and fixed
The single most important finding: **the AI concierge had been dead for ~7 days.** Anthropic retired its model (`claude-sonnet-4-20250514`) on 2026-06-15, so every concierge message returned a generic error. **Fixed live this session** (migrated to `claude-sonnet-4-6`, `f6ee7d47`) and verified working (accurate, current, tool-calling). Also fixed: the **Pack Reality** page showed two different medians (hardcoded `$0.00` lede vs `$1.66` KPI) — `f27bb70f`.

Everything else the audit surfaced is cosmetic / low-impact and queued in the handoff (broken legacy IPFS thumbnails on ~106 old editions, image-fallback gaps, a timing-out self-healer cron, smoke-test false-alarm noise).

Three facts still frame the month: the **capacity ceiling is resolved** (Micro→Small, clean for 2+ weeks); **the first organic users arrived** (retention, not acquisition, is the gap); **chain two has a date** (Candy/Solana tripwire 07-08, everything buildable-in-advance already live).

---

## Now — through ~June 28 (land the audit fixes, finish retention plumbing)

1. **Restore image integrity on the flagship browse pages (handoff A+B).** Refresh the 106 dead `ipfs.dapperlabs.com` thumbnails (operator-gated art backfill) — these are the oldest, highest-value Series-1 editions (LeBron/Steph) and they currently render broken on set/team/player grids. Ship the `onError` tile fallback so any dead asset degrades gracefully. **This is the most visible data-quality gap on the site.**
2. **Quiet the false alarms (handoff C+D).** Window the timing-out `remap_misattributed_topshot_sales` pg_cron so it stops 100%-failing, and add pg_cron-failure visibility to monitoring. Harden the smoke RLS / FMV-tier checks so they stop firing false Telegram alerts + Sentry NEXTJS-1C. Alert hygiene matters now that alerts are live.
3. **Turn alerts on for the allow-list.** Alerts are live and proven (go-live test: 31/31 delivered across email/Telegram/Discord). The cheapest retention experiment available is to open them to the 25 allow-listed users and watch whether weekly deal/FMV pings bring people back. (Trevor's call — see Open decisions.)
4. **Cosmetic cleanup (handoff E–H).** AllDay pack-thumb fallback, Pack Reality stale meta/share copy, per-edition sub-cent EV display, the `/badges` 404 stale route + CLAUDE.md route-doc refresh.

Exit criteria: base-set/grid thumbnails render; remap cron green; smoke false-alarms stopped; alerts decision made.

## Next — through ~mid-July (FMV depth, retention proof, chain-two gate)

- **FMV quality march continues.** Keep converting ASK_ONLY / zero-history TS editions to sales-backed prices (tshb GHA drain); TS HIGH+MED toward ~5,000. Monitor the serial-FMV power model's estimate quality on the underpriced board.
- **Retention is the lever, not features.** Onboarding converts ~100%; only ~1/10 of the first organic wave returned. The return-hooks are built (serial-premium boards, omni-channel alerts, Pack Sniper). The move is to **prove retention** with alerts-on-for-a-week before inviting the next cohort. 50-WAU stays the monetization tripwire.
- **Extend deal coverage.** The cross-collection deal board + alerts cover TS + Pinnacle + AllDay; deepen AllDay (its `unmapped_sales` drift ~57 is the known V1-budget residual) and keep the Pack Sniper's `snapshot-pack-asks` recency feed healthy (watchlist it once cadence banks).
- **Candy / Solana chain-two gate.** 06-22 interim audit + 07-08 firm tripwire (both scheduled). Pass → begin chain-two code (DAS editions → ME sales → wallet backfill → FMV), one surface at a time, Flow quality bar unchanged. Fail → Beezie/Base is the documented pivot.
- **Keep the autonomous loop healthy.** Nightly pass + daytime monitor + weekly checks carry real load; the GitHub PAT expires Sep 7 (reminder set).

## Later (Q3)

- Chain-two build-out to published status (if gated in).
- Pro paywall + Stripe flip — only after 50+ WAU and a deliberate launch.
- **Model-version monitoring** — a smoke check / reminder so the next Anthropic model retirement is caught before it silently kills the concierge again (this audit's lesson).
- Full per-special-serial owner indexing (today ~30% via wmc + last-sale; revisit with chain-two indexing).
- Monolith page refactors (collection / sniper / analytics), brand-token Phase 2, `/dashboard` token migration.
- True 390px mobile sign-off (device emulation), and retire dead-Flowty edge functions (dormant, zero cost).

## Open decisions for Trevor

1. **Open alerts to the broader allow-list (25 users) now?** The feature is live + proven; this is the cheapest retention experiment available. Go, or wait for the audit-fix wave to land first?
2. **Image refresh priority.** The 106 broken Series-1 thumbnails include your flagship LeBron/Steph editions. Confirm you want the art backfill run over them (operator-gated) — it's the highest-visibility fix from the audit.
3. **Launch-readiness bar.** The platform survived its first organic wave + an infra incident, and today's audit found it complete, accurate, and (now) concierge-healthy. What's the bar for actively inviting the next 50 users — does retention need the alerts-live-for-a-week proof point first?
