# Open Claude Code items — consolidated (2026-06-24)

Single list of everything still needing CC after this session's Cowork work. Everything else from the thread is shipped + verified. Health green throughout (security 0/0/0/0, trust 9/9). Each item has the precise action + revert.

## Shipped LIVE by Cowork this session — repo-sync these migration files (parity only, no behavior change)
The `supabase/migrations/` dir is live-only since 2026-05-17; add faithful parity copies + ledger entries:
- `audit_20260624_allday_sales_history_backfill_targets` — monitoring queue (view), AllDay backfill (2,295 zero-sale editions at ship).
- `audit_20260624_pinnacle_sales_history_backfill_targets` — monitoring queue (view), Pinnacle backfill (264 zero-sale renders).
- `audit_20260624_recover_allday_video_url_media_cdn_v2_bare` — **the live AllDay video recovery** (6,176 editions → `media.nflallday.com/editions/<id>/media/video`). Revert: `UPDATE editions SET video_url=old from audit_20260624_allday_video_backfill_v2 ...` (backup table retained).
- `audit_20260624_recover_golazos_video_url_from_thumbnail_key` — **the live Golazos video recovery** (575 editions → `assets.laligagolazos.com/editions/<editionKey>/play_<editionKey>__capture_Animated_Video_Popout_Black_1080_1080_default.mp4`, editionKey parsed from `thumbnail_url`). Backup table `audit_20260624_golazos_video_backfill`. Revert: `UPDATE editions e SET video_url=b.old_video_url FROM audit_20260624_golazos_video_backfill b WHERE e.id=b.edition_id;`
- Also record (already self-contained, net no-op): `audit_20260624_recover_allday_video_url_to_media_cdn` (v1, wrong form) + `audit_20260624_revert_allday_video_recovery_stale_form` (its revert).
- `audit_20260624_default_privileges_revoke_anon_table_dml` — **the durable anon-grant hardening** (item #4 below; future `postgres`-created tables are anon read-only by default). Revert in item #4.
- All four `audit_20260623_*` migrations (thumbnails, videos, pinnacle ASK_ONLY, anon-DML) were already repo-synced earlier — skip if done.

## 1. Fix `lib/media/momentVideoUrl.ts` AllDay video form (1-line — HIGH, trivial)
The helper's AllDay form is **stale**: `https://media.nflallday.com/editions/${editionId}/media/video?width=512&format=mp4` → live returns *"ERROR 9401: 'mp4' is not a supported output format"* (that `?format=` endpoint is an image resizer). The **correct live form** (read from `app.nflallday.com`'s own `<video>` elements + confirmed via direct nav returning `Content-Type: video/mp4`) is the **bare path, no query params**:
```
https://media.nflallday.com/editions/${editionId}/media/video
```
Change line ~51 to drop the `?width=512&format=mp4`. This fixes the **Trophy modal's** AllDay video (the only consumer of the helper; the main grids/edition pages already read `editions.video_url`, which Cowork already repointed to the bare form). Verify: the trophy modal plays AllDay video; `npx tsc --noEmit` clean.

## 2. Historical sales-capture program — verification + deep tails
- **First-tick verification is autonomous:** the scheduled task `verify-allday-backfill-first-tick` (07:05 UTC) checks decode quality on all 5 backfill routes' first ticks + auto-reverts clearly-broken with the ledger predicates. Read its output before assuming the backfills are clean. **FYI — all 5 fired their first ticks OK already** (06:08-06:48 UTC): topshot-flowty wrote 470 / Pinnacle 64 / AllDay 69 historical sales, golazos 0 / ufc 0 (current-spork window thin for those). Capturing history as designed.
- **`v_rpc_trust_health.unmapped_resolution_backlog_max` will breach while backfills run (EXPECTED).** Its threshold (breach_at 100) was set for the forward indexer's tiny backlog; the history backfills write historical sales that can't always edition-resolve (the holder moved / no cache), so they land in `unmapped_sales` pending the chained resolver — first tick already pushed it to ~384 (TS-Flowty skipped 384 of 858). The chained `*-resolve-unmapped` drains the resolvable portion; a residual is inherent to backfilling old sales. **Once the resolvers reach steady state (watch over ~a day), raise this leg's threshold** (in `v_rpc_trust_health`) to the new normal so it stops false-alarming + can still catch a real resolver stall — don't raise blindly now; let the steady-state level reveal itself first. Confirm the TS-Flowty route actually fires a TS unmapped-resolver (AllDay/others chain theirs); if not, its 384 won't drain.
- **AllDay drain cadence:** at 30k blocks/tick × 3h that's ~47 days for the 11.3M-block window. If you want history sooner, bump the `vercel.json` cron (e.g. `7,37 * * * *`, off-rush) — that's the throughput lever. The 07:05 verification reports the real blocks/tick + an ETA.
- **DEEP-TAIL SOURCE FOUND + VERIFIED (CC, 2026-06-24) — candidate (b) below is the answer. See [handoff-2026-06-24-studio-platform-gql-deep-history.md](handoff-2026-06-24-studio-platform-gql-deep-history.md).** The Dapper **studio-platform GQL** (`api.production.studio-platform.dapperlabs.com/graphql`) is alive + unauthenticated and exposes `search{AllDay,Golazos,Pinnacle,TopShot,UFC}MarketplaceHistory` — full per-edition sale history (real `transaction_hash`, `block_time`/`block_height`, `price` (/1e8), `serial_number`, `purchased`), Relay-paginated, filterable by `edition_id` (== AllDay `editions.external_id`). Verified live: edition 3206 → 8 sales back to 2025-08. **This needs NO spork-proxy and NO Trevor CF creds** — it overturns the "AllDay GQL dead" conclusion (that only tested the unrelated dead `public-api.nflallday.com`). The Flowty-venue-only slice (#2 below) remains the lone chain-scan case. Build plan + exact query in the new doc; one open question = Vercel→studio-platform egress (add a `topshot-proxy` `/studio` route if blocked). The spork-proxy finding below stays true but is now mostly moot for the native tail:
- **Deep pre-2025-12-29 tails (AllDay 2021→, Pinnacle 2024→, TS-Flowty 2022→) — pursue via GQL/dapper.market API, NOT spork-chain-scan.** **CRITICAL finding (Cowork-verified 2026-06-24): the deployed `spork-proxy` worker is a non-functional STUB** — `spork-proxy.tdillonbond.workers.dev` returns `{"ok":true,"worker":"spork-proxy"}` to *every* path (health, `/mainnet26/v1/blocks`, unknown sporks alike); it never deployed the repo's `infrastructure/spork-proxy-worker/index.ts` proxy logic (classic worker-deploy-drift). So the chain-scan deep-tail path is a from-scratch worker build (write+`wrangler deploy` the real proxy with the full spork node list mainnet1–27 + auth, **needs Trevor's Cloudflare creds**), and the `SPORK_PROXY_URL`/`SPORK_PROXY_SECRET` Vercel env vars stay **unset until that worker is real** (this is why they were correctly NOT created — they'd point at a stub). **Preferred path instead — the GQL/API mechanism that already gave TS 618k sales back to 2020 with zero chain-scanning:**
  1. **Native deep history (AllDay / Pinnacle / Golazos) → Dapper GQL or dapper.market's own public API.** dapper.market is accessible (no WAF, no auth for public data — Trevor's steer), serves full per-moment **Activity** (sale history) for TS/AllDay/LaLiga, and fetches it server-side through its own `dapper.market/api/...` BFF. CC: probe whether (a) the AllDay consumer GQL via the **functional** `topshot-proxy` `/allday-consumer` route, (b) the Dapper studio-platform GQL (`api.production.studio-platform.dapperlabs.com/graphql`, reachable no-WAF), or (c) dapper.market's `/api/` activity endpoint exposes per-edition/moment historical sales — then mirror `topshot-sales-history-backfill` (the proven GQL pattern) for each collection. **No spork worker, no Vercel env vars, runs on already-functional infra.**
  2. **Flowty-venue-only deep tail (TS-Flowty 2022→25, AllDay-Flowty pre-2026)** is the *only* genuinely chain-only slice (Flowty API dead + archive deleted). Lower value (the Flowty venue is a fraction of volume). Pursue *only after* the native GQL backfill lands, and only if worth standing up the real spork-proxy worker (Trevor CF creds → then Cowork can set the env vars via the Vercel API).
  - Full gap map + block ceilings: `handoff-2026-06-24-historical-sales-capture-program.md` (note: its "spork-proxy" feasibility assumed a working worker — supersede with the stub finding above).

## 3. Golazos / UFC hover-play video

### Golazos — DATA DONE (Cowork, 2026-06-24); needs the frontend video gate widened (1 line)
The live Golazos video form was found via **dapper.market/laliga** (Trevor's steer — the collection sites are WAF-gated): `https://assets.laligagolazos.com/editions/<editionKey>/play_<editionKey>__capture_Animated_Video_Popout_Black_1080_1080_default.mp4`. The `editionKey` is already embedded in each edition's `thumbnail_url` path, so Cowork populated `editions.video_url` for **575 of 581** Golazos editions (the other 6 have NULL thumbnails — nothing to construct) via `audit_20260624_recover_golazos_video_url_from_thumbnail_key` (backup table `audit_20260624_golazos_video_backfill`, exact revert). Both editionKey formats (`2301593_..._recXXX` and the g-prefixed `g1009531_...`) verified to serve `Content-Type: video/mp4` via browser direct-nav.

**CC — surface it (1 line):** `components/entity/EditionsGridPaginated.tsx` line ~107 gates hover-play to TS/AllDay only:
```
const videoEnabled = collectionUrlSlug === "nba-top-shot" || collectionUrlSlug === "nfl-all-day"
```
Add `|| collectionUrlSlug === "laliga-golazos"`, and update the now-stale comment above it ("All Day video_url is null in our data today, so this is effectively Top Shot" — both AllDay **and** Golazos `video_url` are populated as of this session). Verify a Golazos grid tile hover-plays.

**CC — also fix `lib/media/momentVideoUrl.ts` (Trophy modal):** its `laliga-golazos` case still uses the **stale** `...__capture_Animated_Hero_Black_2880_2880_default.mp4` form (same class of staleness as the AllDay `?format=mp4` bug). Replace with `...__capture_Animated_Video_Popout_Black_1080_1080_default.mp4` (the live form above; same `editionKey` input). `npx tsc --noEmit` clean.

### UFC — CC to attempt the on-chain pull (Trevor, 2026-06-24: "see if CC can pull the UFC videos")
`editions.video_url` is NULL for all 518 UFC editions (grids show thumbnail — graceful). Cowork ruled out every *non-chain* source: dapper.market does NOT carry UFC (only NFL/NBA/LaLiga); UFC thumbnails are bare **single-file** IPFS CIDs on `ipfs.io/ipfs/<cid>` (no sibling video to derive); **no UFC edition flowID in our schema** (`set_id_onchain`/`play_id_onchain` NULL; the slug trailing number is `circulation_count` — verified 23970/750/5000 == circulation); `media.ufcstrike.com` is a locked S3 bucket (`AccessDenied`); `ufcstrike.com` is WAF-gated. **So the only path is on-chain — CC build:**
1. **Probe the UFC media shape via the Cadence MCP** against the live `UFC_NFT` contract (`0x329feb3ab062d289`). We have per-NFT `moment_id`s in `wallet_moments_cache` (e.g. `205106`, edition_key `ALEX-MORONO-...-23970`). Per the CLAUDE.md UFC gotcha: borrow as generic `NonFungibleToken.CollectionPublic` + `borrowNFT(id)!` (NOT a `UFC_NFT.NFT` cast; `Traits` FAILS via AnyStruct `.toString()`). Resolve `MetadataViews.Medias` / `Display` on a sample NFT to find the **video** media file (URL or IPFS CID) — the thumbnail IPFS CID is image-only, so there should be a distinct video media entry.
2. If a video CID/URL is exposed: build a small admin/worker route (mirror `backfill-topshot-onchain-art`) that walks UFC editions (one representative `moment_id` each from wmc), reads the video media on-chain via the `topshot-proxy`/Flow REST path, and populates `editions.video_url` via the **backup-table pattern** (see `audit_20260624_golazos_video_backfill`). Then add `laliga-golazos`-style — widen the `EditionsGridPaginated.tsx` `videoEnabled` gate to include `ufc` (note the slug used there for UFC) + fix the `momentVideoUrl.ts` `ufc` case (its current `.png→.mp4` swap can't work on a single-file IPFS image CID — replace with whatever the on-chain probe reveals).
3. If the on-chain metadata exposes **no** video media (some very old UFC plays may genuinely be image-only), document that and leave NULL — graceful. Lowest priority (smallest collection).

## 4. Durable anon-grant default — SHIPPED LIVE (Cowork, 2026-06-24)
Resolved. Root cause confirmed via `pg_default_acl`: the `public`-schema default ACL granted anon `arwdDxtm` (full DML) on every new table created by `postgres` — that's why each new table this thread needed a manual `REVOKE anon`. Shipped `audit_20260624_default_privileges_revoke_anon_table_dml`: `ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public REVOKE INSERT, UPDATE, DELETE, TRUNCATE, TRIGGER ON TABLES FROM anon`. Verified: default ACL now `anon=rxm` (SELECT/REFERENCES/MAINTAIN only — read-only; PostgREST read API intact). `authenticated`/`service_role`/`postgres` untouched; **not retroactive** (existing tables/grants unchanged); RLS still gates all. Future anon-writable tables must now `GRANT anon` explicitly + carry an RLS policy (correct opt-in pattern). **Repo-sync** this migration. **Revert:** `ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public GRANT INSERT, UPDATE, DELETE, TRUNCATE, TRIGGER ON TABLES TO anon;`

## Guardrails
Direct to `main`, no branches/PRs. PowerShell `git`; re-verify push `git rev-list --count origin/main..HEAD` → 0. Vercel Pro `maxDuration` cap 800s. `npx tsc --noEmit` before deploy; confirm READY + smoke.

## End state
Shipped + verified live: 6 sales-history backfill routes capturing (TS-Flowty ~600/tick, AllDay ~190/tick, Pinnacle ~45/tick), media recovered (8,255 TS thumbs / 8,235 TS videos / 6,176 AllDay videos / **575 Golazos videos**), Pinnacle + AllDay FMV ASK_ONLY parity, scarcity board, two monitoring queues, **durable anon-grant hardening (#4 done)**. Item #1 helper AllDay fix + Golazos frontend gate + Golazos helper form already shipped by CC (`1e5300f`).

**CC's remaining work:**
- **#2 deep historical tails (Trevor priority "asap")** — pursue via the **GQL / dapper.market public API** path (native AllDay/Pinnacle/Golazos history; functional infra, **no Vercel env vars, no spork worker**). The spork chain-scan path is *not* viable as-is — the deployed `spork-proxy` is a dead stub; only the lower-value Flowty-venue tail needs a real worker built+deployed (Trevor CF creds) and is deferrable.
- **#3 UFC video** — CC on-chain Cadence-MCP probe of the `UFC_NFT` contract for video media, then populate + widen the grid gate.
- **repo-sync** the migration parity copies.

**Nothing requires Trevor in Vercel right now.** The only future Trevor dependency is *if* the Flowty-venue deep tail is pursued (deploy the real spork-proxy worker via wrangler+CF creds; Cowork then sets the env vars via the Vercel API).
