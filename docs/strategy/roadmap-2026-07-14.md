# RPC roadmap — refreshed 2026-07-14 (post-full-audit)

Supersedes [roadmap-2026-07-10.md](roadmap-2026-07-10.md). Framing unchanged: **intelligence-first**, cost-flat pre-revenue, no monetization until 50+ WAU, chain-two gated. Grounded in the 2026-07-14 full audit ([docs/audits/full-audit-2026-07-14.md](../audits/full-audit-2026-07-14.md)).

## Now (days → 2 weeks)

1. **IOPS recovery watch.** The contention family (smoke fails, timeouts, slow pages) traces to disk-IOPS burst exhaustion from maintenance churn. No more REINDEX waves or bulk backfills for ≥48h; verify analytics-smoke + the pack-dist smoke go green and pack pages render <10s. If still throttled after quiet time: the only hard fix is a compute bump (cost-gated — Trevor decision) or a further read-volume diet (biggest readers: fmv-recalc fan-out, wmc denorm sweeps).
2. **Restore the underpriced-#1s ingest** (Atlas datacenter block; GHA lane dead, CF-worker lane disproven). Operator: revive the home-machine Task Scheduler lane (`scripts/register-active-listings-task.ps1`) — also revives the AllDay-badges home ingest, down since 07-07.
3. **Surface the AllDay 5% rebate window (ends 2026-09-09)** on AllDay deal/market surfaces + concierge: "effective price after rebate" on deal boards is a free, time-boxed buy-side hook aimed exactly at RPC's collector cohort.
4. **Team-page 30d volume precompute.** `get_team_detail`'s live 30d sales aggregate dies under throttle (soft-404'd Blazers/Chiefs pages). Move the 30d volume/count into a small hourly-refreshed rollup (teams_master or a tiny MV) so team pages never run live aggregates. (The July "fine as-is" disposition predates the IOPS regime change.)
5. **Finish error-path hardening on remaining entity pages** (set/player/edition) — same throw-on-RPC-error pattern shipped for pack/team this session, so contention can never soft-404 a real page.
6. **TS thumbnail one-shot** for the ~82 actively-trading art-less editions (GQL via topshot-proxy; the on-chain backfill is exhausted).
7. **Badge residual drain (55 editions)** — operator script ready (`backfill-badges-from-moments.mjs`, residential egress).
8. **Pinnacle render cache fill** via browser-harvest (in motion 07-14) → trophy PDF + OG art complete for Pinnacle.

## Next (2–8 weeks)

1. **Mobile pass.** Competitor apps are mobile-first; RPC's dense mono tables need a deliberate small-viewport audit (nav ticker, tables → cards, tap targets). Ship a viewport-sized QA loop into weekly surface-qa.
2. **Autographs + Redeem awareness (TS v2 parity).** Catalog/flag autograph editions and redemption-eligible items; they're first-class filters on v2.nbatopshot.com and invisible in RPC today.
3. **Pinnacle set-completion tracker.** Pinnacle's own site centers "Your Set Cards in Progress"; RPC has the catalog + wallet data to do completion cost/EV (mirror of the TS set-completion planner shipped 07-12).
4. **AllDay Top Owners.** Dune ownership index exists for TS only; extend (or wmc-approximate) for AllDay so its edition pages get the Top Owners module.
5. **Insights pack-market boards → MVs** if board latency degrades (full-table aggregate views kept for boards; per-dist path already RPC'd).
6. **Founding-Collector angle:** AllDay finite-catalog thesis content + tools (holdings valuation vs rebate window, sealed-pack reality boards) while attention is on the sunset.
7. **wmc index rationalization** (unique-idx decision + idx_wmc_lower_wallet_coll_edkey) in one deliberate CC pass — ~440 MB and write-amplification on the hottest table.

## Later (gated)

1. **Chain two — Candy/Solana.** Tripwire: ≥30 days Candy sales history (earliest ~2026-08-07) + defined edition/serial schema. Phases A–F complete; helius-proxy gets its own auth surface. No public multi-chain messaging pre-launch.
2. **"Next evolution of NFL digital collectibles"** (Dapper's words in the sunset note): watch for the successor product; RPC should be indexing it day-one — same playbook as Candy.
3. **UFC-Aptos disposition:** retire/archive Flow UFC UI (CC-owned decision item).
4. **Monetization** (Pro paywall, Stripe): untouched until 50+ WAU. Traction levers first: SEO surfaces (soft-404 fix helps), share/OG loops, concierge + alerts, trophy-case/gifting social loop (Phase 1 shipped 07-13).
5. **Wallet verification GA:** listing-challenge flow is canonical; Dapper developer-access request pending for "Sign in with Dapper".

## Standing constraints

Cost-flat infra (levers before spend); no promo until launch-ready; quality bar on chain one does not drop while chain two builds; autonomous passes coordinate via the ledger; FREEZE.md halts all autonomous shipping.
