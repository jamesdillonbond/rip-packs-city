# RPC Roadmap — refreshed 2026-07-11 (evening full-audit session)

Supersedes [roadmap-2026-07-10.md](roadmap-2026-07-10.md). Read with CLAUDE.md "Prioritized next actions" (intelligence-first; monetization tabled until 50+ WAU). Companion: [full-audit-2026-07-11](../audits/full-audit-2026-07-11.md).

## Done since the 07-10 roadmap (don't re-plan)

AllDay serial/jersey FMV port (parity complete); sitemap 5-way split (`f001113`); UFC teardown (listings retired, sales watchlist relaxed, cursor suppressed); badge v2-parity fixes (5 classes); complete pack-opens history via Dapper `searchPackNft` (TS done 788k, AllDay ~2.81M finishing); WAP→ASP rename end-to-end; concierge combo deal-alert subscriptions; special-serial official badge art; fmv-recalc Step-1a 120s fn + covering index; CI vitest gate + 93 tests.

## Strategic reframe: Dapper is consolidating Flow to TopShot

AllDay is officially sunset (no new Moments ever; marketplace continues; 5% Dapper-Balance rebate through Sep 9 2026; "Founding Collector" designation; "next evolution of NFL digital collectibles" teased). UFC already left for Aptos. Implications:

- **TopShot is the Flow franchise** — keep it the flagship quality bar.
- **AllDay becomes a finite, fully-indexable catalog** — RPC can own the definitive AllDay dataset (we now have ~2.8M pack opens to genesis, complete editions, serial FMV). "The AllDay record of record" is a durable moat nobody will rebuild.
- **The teased NFL "next evolution" joins Candy/Solana as a chain-two-adjacent watch item** — whichever ships a real market first.

## Now (0–4 weeks) — activation + honest states + audit follow-ups

1. **Traction instrumentation + activation loop (unchanged #1).** ~13 users vs a product that now beats the native sites on depth. Weekly funnel snapshot; 2–3 evergreen intelligence pieces/wk off existing boards; no paid spend.
2. **AllDay sunset surfacing (NEW, small, timely):** honest banner/state on AllDay surfaces (mirror the UFC treatment but market-alive); surface the **5% rebate window (ends Sep 9)** on AllDay market/sniper/pack pages — it's a live, expiring buyer signal only RPC will quantify (rebate-adjusted deal pricing). Founding-Collector angle for content.
3. **Audit follow-ups (CC handoff ready):** kill the pack-page LOW/ASK_ONLY confidence chips + footnote (directive violation); reward-pack hero placeholder; team-montage blank tiles; stale "since Jun 2026" caption; retire the AllDay opens cron on done; post-storm re-check of rollup-rip-value / classify-acq.
4. **Offers coverage completeness:** after the per-printing offers sweep completes a full catalog cycle, diff 10 v2-visible open offers vs `offers`/`edition_offers`; if dapper.market runs an off-chain offer book, scope indexing it (small — one GQL surface).
5. **Restart the two home-machine ingests** (Deal Board / AllDay badges — operator, 5 min) — still down since ~07-07.
6. **Operator quickies:** `wrangler deploy` topshot-moments-hydrator (activates the c1ba51e partial-data fix); optionally deploy the backfill-pack-opens-api concurrency guard.
7. **Concierge soak** (unchanged) — weekly QA against real Telegram/Discord usage.

## Next (1–3 months) — depth that compounds

1. **AllDay offers indexing** (DapperOffersV2 template exists from TS) → offer columns on AllDay edition pages + concierge. Pairs with the offers-completeness check above.
2. **Ownership parity: Top Owners beyond TS.** AllDay/Golazos top-owners + top-stacker callout from wmc/Dune-style data (v2 does per-edition top-stacker; RPC has it TS-only). Also "% of circulation listed" per edition (listing counts from the GQL sweep).
3. **Pinnacle pin page v2:** migrate `/pinnacle/moment/*` onto the (collections) template with sales history (pinnacle_sales), special serials (multipliers exist), owners (pinnacle-owner-discovery), and pinbook/set-completion intelligence (their gamified core loop is indexable data).
4. **Pack-rips-at-scale hardening:** pack_rips is now 3.5M rows; make its consumers set-oriented/indexed (rollup-rip-value first), then build on the new asset: pull-history intelligence (realized EV by pack/wallet/era, "what did this wallet rip and what was it worth").
5. **Pre-2023 AllDay/Golazos history via spork worker** — 3 operator steps (deploy + secret + env). Completes "full sales history" to the public-infra floor (2022-04).
6. **Chain two — Candy/Solana re-audit ~Sep 2026** (unchanged; tripwire deferred to ~60d from 07-08). Add the NFL "next evolution" to the same watch.
7. **Monolith refactors** (sniper, collection, analytics pages) before more features land on them.
8. **DB cost watch:** 9.1 GB and +0.5 GB/day during backfills; when the opens/history drains settle, prune worktables + evaluate partition pruning before the next PRO tier cliff.

## Later (3+ months) — expansion

1. **Chain two build** (Candy/Solana via Helius DAS + Magic Eden) when the 30-day gate passes — chain-abstraction A–F done and waiting.
2. **Aptos evaluation** only if UFC-on-Aptos develops a real indexable market.
3. **Monetization** (Pro tier, Stripe) strictly after 50+ WAU — price around concierge + alerts + serial-FMV depth + the AllDay record-of-record.
4. **Beezie/Base promotion** only on a real product consumer or if Candy slips again.
5. **Panini** monitor-only.

## Standing invariants (do not regress)

- Quality bar on Flow stays up during any chain-two work; never parallel chain builds.
- Cost-flat infra until revenue; no paid acquisition.
- No promo pre-launch-ready; no Team-Captain/name on user-facing surfaces.
- No confidence/staleness labels anywhere on public UI (build-time signal only).
- Trust-health + security invariants stay green; every migration logs a revert path.
