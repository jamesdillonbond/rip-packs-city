<!-- Extracted from CLAUDE.md on 2026-08-17 to bring that file under the memory-file
char limit. Content is VERBATIM; CLAUDE.md carries a one-line pointer to this file.
Same rules apply: every number here is a dated sample - re-measure before quoting. -->

## AI Concierge

Claude Sonnet chat on every page via SupportChatConnected component.
Routes: `/api/support-chat` (**33 tools — see the 2026-08-13/16 additions below**; the 29 listed here are the 2026-07-20 set — incl. `get_fmv`, `check_wallet`, `analyze_wallet_holdings`, `search_live_deals`, `search_catalog_deals`, `search_across_collections`, `compare_pack_value`, `search_serial_deals`, `get_hot_floors`, `get_edition_sweep`, `get_set_completion_cost`, `get_challenges`, `get_collection_snapshot`, `explain_fmv`, `check_wallet_squeeze`, `manage_deal_subscriptions`, `manage_alerts`, `manage_watchlist`, `get_special_serial_owners`, `escalate_to_human`, `log_bug`/`log_feature_request`/`log_feedback`; plus the 2026-07-20 read-only market/ecosystem reads `get_top_sales`, `get_market_movers`, `get_rookies`, `get_premiums`, `get_ecosystem_stat`, and the generic `get_insight_board` (reads the remaining shareable boards by enum — squeeze/set_squeeze, set_completers, trophies, pinnacle_scarcity, allday_scarcity, topshot/allday pack_market, pack_reality/allday_pack_reality, market) — each wired to an anon-public `/api/public/insights/*` board via the shared `fetchPublicInsight` helper; the 2026-07-20 ship also added the never-disclose security block to the system prompt), `/api/support-chat/feedback`, `/api/support-chat/context`, `/api/support-report`.

**38 tools as of 2026-09-13** (35 on 09-02, 33 on 08-16 — counted from the tool array — ⚠ **the count has now drifted THREE times in three days**: this file said 31 while the code carried 32, then 32 while the code carried 33. **A tool is one array entry and a growing product adds them, so treat this number as a dated sample and re-count it** — `[...src.matchAll(/^\s+name: "([a-z_]+)",/gm)]` over the route is exact, and all 33 matches are real tools). The 2026-08-13 pass added the two intents the PLATFORM could answer but the BOT could not, 2026-08-14 added a third, and 2026-08-16 added a fourth:
- **`get_edition_listings`** (added 2026-08-16 in `0d10cbcb`, refined by `b0f6db43`) — answers *"is this one listed right now?"*, the question a collector asks immediately after any FMV or deal answer, which the bot previously could not take. `b0f6db43` is the part worth remembering: it let a follow-up **chain onto a deal** already on screen and **stopped dropping the serial**, i.e. the tool was answering about the wrong copy of a Moment — on a serial-keyed market, an answer about the edition when the user asked about *their* serial is a wrong answer wearing a right one's clothes.
- **`search_catalog`** — wraps the public `/api/search`. It searches names AND `editions.description`. ⚠ **It was shipped described as "the only tool that can answer a NARRATIVE query (the Lillard game winner)" and that is FALSE — do not restore that claim.** Measured 2026-08-13: `game winner` returns a roster of the **"For The Win" SET** (Block variants included) and returns NEITHER of the two most famous Blazers game winners — Lillard's 2014 series winner (**Archive Set `48:1652`**) or the 2019 37-footer (**Run It Back: Legacies `121:4255`**) — though both carry descriptions, one containing "game-winning" verbatim. ⚠ **The "outranked / length-normalized trigram" explanation that replaced it was ALSO wrong** — see the `/api/search` bullet under "Selected API endpoints" for the full correction. Real cause: `rpc_search_catalog` required EVERY query token, fixed 2026-08-14 so a 3+-token query may miss one. **What the tool description and prompt must say now: it matches WORDS, not concepts — there is no stemming**, so prose saying "game-winning" is unreachable by "game winner", and the reliable shape is a NAME plus a distinctive word (`lillard buzzer`, `lillard game-winning`). A narrative miss therefore usually means *we did not use that exact word*, not that the moment does not exist. The prompt also requires the bot to **read the rows before presenting them** — a non-empty result is not a correct result, which is precisely how the original false claim survived review. Full repro: `docs/overnight/inbox/2026-08-14T0310Z-narrative-search-is-outranked-by-set-names.md`. Also the identity lookup that supplies an edition's route slug to `get_price_history`. ⚠ **Its coverage disclosure is load-bearing.** Prose exists for part of Top Shot and **0% of every other collection**, so an empty narrative result is ambiguous between *"we hold no description for that"* and *"no such moment"* — completely different things to tell a collector. The tool forwards the LIVE `coverage`/`coverage_note` from the route (never hardcoded — the backfill moves it every run, the Panini lesson) and **deliberately keeps them attached on the `no_results` branch**, which is where the model needs them; the system prompt makes stating which case applies mandatory. ⚠ Do NOT confuse it with **`search_catalog_deals`** — that one is a PRICE tool; this one is the catalog INDEX and carries no prices. The prompt disambiguates them explicitly because the names are one word apart.
- **`get_price_history`** — wraps `/api/entity/edition?part=sale-history` → `get_edition_sale_history`: ACTUAL SALE PRINTS (low/median/high + count per bucket), the only path to a multi-year answer since `fmv_snapshots` begin 2026-03-31 while `sales` run to 2020. ⚠ Labelled `basis: "actual_sale_prints"` with an explicit `not_fmv` note, because a sale median is what buyers PAID and an FMV is a model ESTIMATE — the conflation class this repo keeps paying for. `grain` (day/week/month) is returned and the prompt requires stating it, so the bot cannot imply daily resolution on a six-year window; a bucket with `sales_count` 1 must be called a single trade, not "the price".
- **`find_quirky_serials`** (added 2026-08-14) — scans a wallet for NOVELTY serials via `lib/serials/fun-patterns.ts` (palindromes, repdigits, meme numbers, #1 / last mint, and — once the bio lookup lands — jersey / birthday / draft-year matches). ⚠ **Its honesty constraint is the opposite of the other tools': it must never imply VALUE.** The result carries a `not_a_price_signal` disclaimer and the prompt forbids framing a quirk as a premium, because `applySerialPremium` really does pay 1.35× for a #1 and these deliberately do not participate — see the `lib/serials/fun-patterns.ts` bullet under "Key files". ⚠ It **paginates rather than `.limit()`** and reports `count_is_lower_bound` when the page budget is exhausted: PostgREST silently CLAMPS a limit above 1,000, so a 3,000-moment wallet would otherwise get a confidently wrong "you have N quirky serials" computed from the first third — the silently-sliced-ranking class. An empty result is a real answer ("an ordinary serial is the normal case"), never a failure.
Both fail LOUD — a 503 stays `status: "error"` and never degrades to `no_results`, which would render an outage as "we have no such moment". `get_price_history` is also added to the prompt's "FMV numbers must come from a tool call this turn" allow-list, since it is a legitimate price source. Tests: `__tests__/api-support-chat-catalog-history-tools.test.ts`.
Supabase table: `support_conversations` (with feedback col).
Escalations: Telegram + Resend. Rate limit: 25/hr.
Env vars needed: `ANTHROPIC_API_KEY`, `RESEND_API_KEY`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `ALERT_EMAIL`.
Telegram sentinel bot: `@rpc_sentinel_bot`, chat_id `1755958876`.

⚠ **THE CONCIERGE CAN BE DOWN FOR TWO WEEKS WITH THE SMOKE TEST REPORTING "ALL PASSED" — fixed 2026-08-16 (`08b16613`), and the reason it was possible is a rule worth generalizing.** Measured at the time: **150 `concierge_unavailable` conversations against 3 `general` in 24 h**, and **~780 degraded conversations since 2026-08-02**, with nothing paging. The cause was not a missing probe but the SHAPE of the ones that existed — **the only concierge probes were soft AND opt-in** (`?concierge=1`), and they were both, deliberately, because each live-LLM probe spends real tokens. **A check that costs money per run will be made optional, and an optional check is not a monitor.** The fix measures the **OUTCOME instead of the dependency**: the share of *real* conversations that got a fallback rather than an answer — one indexed read, zero credits, and it observes the thing users actually experienced rather than simulating it. Three details are load-bearing: it keys on the **category**, not the fallback COPY (prose gets reworded, and a check keyed to copy silently stops matching); it carries a **5-conversation sample floor** so one failure on a quiet night does not page (the `ufc_fmv_stale_hours` cry-wolf cost again); and it is a **hard** check, so it counts. Smoke totals **54 → 55**, hard **42 → 43** (assertions in `__tests__/api-smoke-test-deep.test.ts` — read them there, they move most times anyone touches the suite). **When a dependency is expensive to probe directly, probe its footprint in the data it already writes.** ⚠ **AND THE FIRST VERSION OF THAT FIX PROBED ITS OWN FOOTPRINT — corrected hours later the same day (`63bfc654`).** The check had **no `is_smoke_test` filter**, while its sibling `support-chat graceful-degradation (synthetic Anthropic 4xx)` POSTs `x-rpc-test-error-mode: credit_balance` on **every** smoke tick, deliberately, to prove the degradation path works — writing exactly the `concierge_unavailable` row this check then counts. **A good test doing its job, and manufactured evidence for the other one.** Measured live 2026-08-16: of **905** conversations since 08-02, **902 were smoke tests and 3 were real**, and **ALL 863 degraded rows were smoke rows — real degraded conversations ZERO**, with real traffic **0 on nine of the last ten days** and the 3 on 08-16 all SUCCEEDING. It fired as Sentry `JAVASCRIPT-NEXTJS-2E` (27 users, *escalating*) reporting an outage that was not happening, and — one synthetic degraded row per tick against ~0 real traffic — **it could never have gone green on its own**. ⚠ **SO THE "150 vs 3" AND "~780 degraded conversations" FIGURES ABOVE ARE SMOKE TRAFFIC, NOT USER IMPACT — do not requote them as user impact.** (This does NOT prove the concierge was healthy in that window — there was independent evidence of an Anthropic 403 `credit_balance` and Trevor found it by using the product — only that the *number* came from the wrong rows.) ⚠ **This is the guard-scope class in its sharpest form: a monitor whose input set includes the OUTPUT OF ANOTHER MONITOR.** Every other instance in this file is a guard blind to something outside its derived scope; this one is a guard that can see itself. **Look for it wherever a check reads a table the test suite also writes.** ⚠ **Consequence, and it is deliberately not smoothed over:** with smoke rows excluded, real concierge traffic is ~0–3/day, so the check now normally reports `low_sample: true` and **no verdict** — which is honest, and must NOT be "fixed" by lowering `MIN_SAMPLE` or by counting smoke rows again. Guarded by `__tests__/smoke-concierge-degraded-share-excludes-smoke-rows.test.ts`, which **strips comments first** (the fix's own comment names `is_smoke_test` repeatedly, so a raw grep passes on prose alone with the filter deleted) and pins that the sibling still manufactures those rows.

### 2026-09-13 pass — the bot now sees badges, supply, the page's entity, and its own pricing rules (38 tools)

**Input was the real traffic, not a hunch.** Every non-smoke `support_conversations` row from the last 90 days (≈60 turns, 22 in the last 60 days — real concierge traffic is still thin) plus every `feedback_type` row was read before anything was changed. The record is good: of the six concierge complaints Trevor logged through the bot in June–July (no team filter, no badge filter, no combo alerts, no squeeze FMV totals, no cheapest-set ranking, no pack EV tool), **all six are `shipped`**. Three defects were still live and shipped in this pass; one open item is not the concierge's (feedback #4067, the /alerts filter UI, still `new` since 06-25).

- **The prompt's FMV methodology disagreed with the pricing code.** It told users *HIGH (5+ sales), MEDIUM (2+), LOW (1)* and *recalculated every 20 minutes*; `lib/fmv-confidence.ts` gates HIGH at **≥7 sales/30d AND serial-residual dispersion < 0.20**, MEDIUM at **≥5** (or ≥7 with spread < 0.35, or LOW rescued by a <7-day-old live ask within ±25% at ≥3 sales), and `fmv-recalc` sweeps the catalog in 500-edition pages (~119 runs/day, a full sweep in hours). Two real users were given the wrong definition (rows 6925, 8397). The block is now `FMV_METHODOLOGY_BLOCK`, a module constant that **interpolates the constants from `lib/fmv-confidence.ts`** — a threshold change reaches the bot with no prompt edit — and it names the other enum values a row can carry (ASK_ONLY / SALES_ONLY / STALE / NO_DATA). The cache-breakpoint guard allows exactly this one extra deploy-constant interpolation.
- **Coverage percentages were hardcoded and stale** ("Golazos 12.9% (75/581)", "Pinnacle 86%"). Replaced by `lib/concierge/fmv-coverage.ts`: per-collection head counts on `edition_fmv_current` (the per-edition snapshot table, 909 buffers for the whole aggregate; `editions` is deliberately not read so the coverage read shares no fixture table with the price tools) plus `pinnacle_catalog`, module-cached 30 min with a 2.5 s budget, rendered into the DYNAMIC prompt as "as of N min ago". A failed count is `null` → that collection's line reads "coverage read failed — do not quote a figure". Measured 2026-09-13 (a dated sample): TS 52.8% HIGH/MED of 14,015 · All Day 29.4% · Golazos 0.7% · UFC 0%.
- **Badge blindness on the price tools (Trevor, 06-25, #4064).** The rule that shipped for it could only use tags a tool row already carried, and `get_fmv` / `get_edition_listings` / `explain_fmv` carried none. `lib/concierge/edition-metadata.ts` reads `badge_editions` (play_tags → `badges`, team, series, parallel, circulation / burned / locked / `squeeze_pct`, rookie flags, `metadata_as_of`) for every edition those tools hand back, plus `get_fmv`'s sample editions and `search_live_deals` rows (the sniper feed always carried `badgeLabels`; it never reached the model). ⚠ `badges_status` has THREE values — `ok` (an empty list is a real "none"), `unavailable` (the read FAILED), `not_tracked` (no moment tags for that collection / no row) — and the prompt forbids "this has no badges" on anything but `ok`. Coverage: play_tags exist for Top Shot only (5,395 of 13,915 rows tagged on 09-13); All Day / Golazos rows carry supply columns and no tags; Pinnacle / UFC have no rows, so the helper returns `skipped` rather than an honest-looking empty.
- **Three new tools.** `get_badge_info` — taxonomy definition + a LIVE per-tag head count (`play_tags @> '[{"title":…}]'`, 1,576 buffers / 17 ms warm; a per-tier breakdown only when ONE tag matched; module-cached an hour) and, with `player`, that player's tagged editions. ⚠ The first draft PAGED a census of all tagged rows — 2,672 buffers and **13.3 s per page cold** under the saturated instance, six pages inside a 12 s budget; the probe would have been the load. It deliberately does NOT price a badge (a premium is a comparison of priced editions, which the price tools own). `get_player_editions` — the `get_player_editions` RPC the public player page uses, ranked by FMV, tier-filterable, each row badge- and supply-enriched, with `floor_indexed_usd` labelled as a snapshot and `total_is_lower_bound` at the 200-row RPC page. `get_team_intel` — `get_team_players` / `get_team_squeeze` / `get_team_activity` behind a partial-name resolver ("Blazers" → Portland Trail Blazers via `editions.team_name`; ambiguous → candidates), with `rookiesOnly` for the exact question that was asked live on 09-12 ("good Blazers rookie cards").
- **The page's entity now reaches the prompt.** `SupportChatConnected` reported an edition page as `edition (nba-top-shot)` and nothing more, so "what's this one worth?" needed the user to re-type the moment (rows 3987/3990). It now sends `pageEntity: {kind, slug}` for edition / player / team / set / series / moment pages and `/pinnacle/moment/<render_id>`; the POST validates the kind against `PAGE_ENTITY_KINDS`, strips line breaks and backticks, bounds the slug at 120 chars, and the dynamic prompt says which tool parameter it maps to. `pageContext`'s shape is unchanged (the quick-suggestion pills key on it).
- Also: the canned "why is the sniper feed empty" answer narrated proxies and Cloudflare — the one thing the prompt elsewhere forbids — and the security section's own example of a safe answer was the stale "every 20 minutes". Both rewritten.

Guards: `__tests__/concierge-2026-09-13-metadata-badges-context.test.ts` pins the derivation (the block must interpolate the constants; no `\d+% (\d+/\d+` literal anywhere in the prompt; neither "Recalculated|refreshes every 20 minutes"), the three-state `badges_status`, null-not-zero coverage counts, the entity block landing BELOW the cache breakpoint with unknown kinds dropped, and each new tool's error-vs-empty exits.

**Second pass, same day (09-13):** the remaining edition-returning tools carry the same block — `search_live_deals`'s catalog fallback (`badge_slugs`), `search_serial_deals` (both paths), `get_special_serial_owners`, and `check_wallet`'s per-collection top-5 (now with `editionKey`). Entity pages got pills that exercise the new context ("What's this one worth right now?" on an edition page; `PAGE_DEFAULTS` keys `edition` / `player` / `team` / `set` / `series` / `moment`). Deploy verified per commit (`get_deployment` READY with `ready` > `buildingAt` and the production aliases attached); the sandbox's egress proxy 403s `rippackscity.com`, so the live check was the OUTCOME table — the synthetic-4xx smoke probes wrote their `concierge_unavailable` rows against the new bundle at 17:16Z / 17:20Z / 17:26Z and the deployment's runtime logs for the route show 200s only.

**Production verification, 09-13 11:0x PT (real turns, spent deliberately):** two questions fired at the deployed route from Postgres (`net.http_post` — the sandbox's egress proxy 403s the apex; tooling-gotchas.md "point it at our own domain"), sessions `probe-claude-2026-09-13-a/b`, rows kept in `support_conversations` as real (not smoke) traffic. (a) On an edition page with `pageEntity` `48:1652` and the message "what is this one worth right now, and what badges does it carry?" the bot resolved the key WITHOUT being told the moment, called `get_fmv` + `get_edition_listings` (650 ms + 1,051 ms), answered FMV $0.58 LOW with the computed_at date, "Badges: None" from `badges_status: ok` + `[]` (correct — `48:1652` carries no tag), used the supply block (33.4% squeeze), and reported the live-floor check as FAILED with the edition link rather than a number — 22.5 s end to end. (b) "How rare is the Top Shot Debut badge, and how is FMV confidence decided?" → `get_badge_info` (2,568 ms): **2,282** editions, by tier Common 1,504 / Rare 558 / Legendary 150 / Fandom 34 / Ultimate 36 (live counts, dated samples), then the confidence tiers recited exactly from the constants — 31.7 s. ⭐ **`cache_read` 23,322 on iteration 2 of both turns** — the prompt-cache breakpoint survived the methodology block sitting above it, which is the point of making it a module constant. ⚠ **One defect found and fixed the same hour:** the badge answer added that a debut "typically commands a premium" — the exact phrase the FMV rule bans as a soft directional claim — because the badge section itself said a badged moment "is reasonably worth more … and you should say so". The two rules now agree: the badge is context and a ranking reason; the premium's SIZE is a price and needs two priced rows this turn. Pinned in the 09-13 guard file.

**Engagement review — closed and open:** feedback #4067 (alerts filter UI: "toggleable + type-to-fill") is **already built** — `app/alerts/AlertsClient.tsx` has `ChipTypeahead` for players / sets / teams and `Chip` toggles for channels, badges, jersey / last-mint / never-sold — but its row still reads `new`. ✅ Flipped to `shipped` 10:41 PT 09-13 once Trevor authorised the write ("handle these yourself"); the trigger stamped `shipped_at`. Real concierge traffic is ~0–3 real turns/day, so the degraded-share check stays `low_sample` and the honest instrument for "is it answering well" remains reading the rows.

### 2026-09-25 pass — names are not people (the full model: [player-identity.md](player-identity.md)): `resolve_player_name` and the `player_identity` block (39 tools)

**Input was the crosswalk work of the same day (#139, batches 44–54).** Until this pass every player tool took the typed label as the person: `get_player_editions` slugged it and called the RPC (an alias — "Joseph Flacco", "Stephen Curry", "Kenny Gainwell", "Mike Vick" — was a catalog miss), a suffix-less namesake resolved to the row spelt that way (the FATHER: "Marvin Harrison", "Tim Hardaway", "Gary Payton") with no word about the son, `get_fmv`'s `ILIKE '%name%'` pooled father and son into one distribution, and a name change (Robby Anderson → Robbie Chosen 2022; the Jaguars' Josh Allen → Josh Hines-Allen 2024) was invisible.

- **`public.resolve_player_name(collection, name)`** (`20260926011020`, pinned by `supabase/tests/resolve_player_name.sql`) — exact → alias → the league's spelling (`player_identities.name_slug`) → a recorded former name → the base name with the suffix dropped ("X Sr." names the unsuffixed row) → a partial. `one` returns the row, its aliases, the crosswalk identity (league id, ESPN id, position, seasons, latest team, `stats_seasons`), every recorded relation and every NAMESAKE in the collection (same base name, or a row whose former name is the query) described the same way; `ambiguous` returns the candidates; `none` invents nothing. service_role only.
- **`public.player_relations`** — hand-curated, public record only: `parent_of` (Gary Payton → II, Glenn Robinson → III, Larry Nance → Jr., Ron Harper → Jr., Tim Hardaway → Jr.; Antoine Winfield → Jr., Asante Samuel → Jr., Joey Porter → Jr., Marvin Harrison → Jr.), `namesake` = UNRELATED (Byron Murphy Jr. the Vikings CB vs Byron Murphy II the Seahawks DT), `name_change` (Robby Anderson ↔ Robbie Chosen; Josh Hines-Allen ↔ Josh Allen). A same-name pair with no row is reported as "kinship NOT recorded" — the bot must not assert one. ⚠ **When a new father/son or renamed player enters the catalog, add the row here**; the query in the migration header lists every same-base-name pair.
- **`lib/concierge/player-identity.ts`** — the wrapper adds the FOURTH state, `unavailable` (a failed read is never "no such player"), and `identityContextFor` builds the `player_identity` block: what the name resolved to and how, the league's spelling when it differs, names the person has used, each namesake with its relation as a sentence, and the warnings ("the answer below is for Marvin Harrison ONLY … never pool them").
- **Wiring.** `get_player_editions` resolves first (alias → the resolved slug; `ambiguous` → candidates and NO editions call; `unavailable` → the typed spelling plus the note). `get_fmv` and `search_catalog_deals` resolve and, on `one`, filter the distribution on `editions.player_id` (`fetchUnifiedFmvDistribution.playerId`; the population count and the fetch share the predicate) — on `ambiguous` they return the candidates and no distribution. New tool **`resolve_player_name`** answers "who is X / is X the same as Y / are they related". Prompt: the **Names are not people** section under the player-filter rule. ⚠ `search_live_deals`, `search_serial_deals` and the alert `players` array still match deal rows by label — a subscriber names the label they watch — unchanged on purpose.
- **Batch 59 (same evening):** `resolve_player_name` carries `player.labels` (every spelling the person's editions use) and `executeTool` resolves the typed name ONCE for the six label-keyed tools (`search_live_deals`, `search_catalog_deals`, `get_edition_listings`, `search_serial_deals`, `get_special_serial_owners`, `get_badge_info`): `player_name IN (labels)` or `player_id` where the table has it, candidates on `ambiguous`, the tool's own ILIKE when unresolved. Pinnacle and `search_across_collections` are not scoped.
- **Batch 61 (same evening) — teams:** `resolve_team_name(collection, name)` groups the collection's team labels into FRANCHISES through `league_team_abbr` (historic names; WNBA arm), `get_team_intel` reads the primary (current) name and attaches `franchise` (historic labels with edition counts, and a note that the reads cover every era — **batch 62** folded the historic labels into every team read, so the note no longer sends those moments elsewhere); different franchises are `ambiguous`; a failed franchise read falls back to the label ILIKE and says so. Prompt: **Teams are franchises**. ⚠ Add a new historic label to `league_team_abbr` when a relocation/rename appears in `editions.team_name` (the unmapped-label query is in the 20260926021808 migration header).
- **Decision (delegated, 09-25):** no renames to the league's spelling; the alias carries the URL and search, and the concierge names both.
- Tests: `__tests__/concierge-player-identity.test.ts` (route + wrapper; two planted defects caught), the `playerId` case in `concierge-fmv-distribution.test.ts`.

### The concierge on Telegram vs Discord — the asymmetry is PERMANENT, and it reads as a dead bot

Both bots reach the same concierge through `lib/alerts/concierge-bridge.ts`, but they receive user input in fundamentally different ways, and this has already cost a real "the Discord bot didn't respond" report (Trevor, 2026-08-15).

- **Telegram** (`app/api/bots/telegram/route.ts`) — its webhook delivers **plain text**, so any non-command message routes straight to the concierge. Just typing a question works.
- **Discord** (`app/api/bots/discord/route.ts`) — this is an **Interactions webhook**. Discord sends it `PING` and `APPLICATION_COMMAND` and **nothing else**. Ordinary DM messages are **Gateway** events (`MESSAGE_CREATE`, behind the privileged `MESSAGE_CONTENT` intent) delivered over a persistent websocket that serverless cannot hold. ⚠ **A plain Discord DM therefore produces NO REQUEST TO THIS APP AT ALL** — the bot does not fail to answer, it never hears it. `/ask` is the only concierge path on Discord, and no code change to this repo can alter that; it needs an always-on gateway process (separate host, real cost), which is a product decision, not a bug fix.

⚠ **DIAGNOSING "the Discord bot is dead" — check registration BEFORE assuming the above, and do not guess.** A command defined in `lib/alerts/discord-commands.ts` does nothing until it is REGISTERED with Discord by a one-time `POST /api/bots/discord/register`. If `ask` was added after the last registration run, it is absent from every user's client — **indistinguishable from a dead bot**. `GET` on that same route (added 2026-08-15) reports `registered` / `missing` / `dm_capable` using the server's own bot token, so the token never leaves Vercel. ⚠ **Measured 2026-08-15: registration is COMPLETE** — all four of `link` / `soldpacks` / `alerts` / `ask` are registered AND DM-capable, so the plain-DM architecture above was the entire cause. **Re-run the GET rather than trusting this line**, and do not run the POST speculatively.

⚠ **A WRONG BEARER ON THAT ROUTE RETURNS THE LOGIN PAGE AT HTTP 200, NOT A 401.** The route is reachable only via `proxy.ts`'s token bypass; an invalid token falls through to the auth wall, which **302s to `/login`**, and the caller follows the redirect and gets HTML at status 200. This is the same trap documented for `/fonts/*.ttf` — every naive "did it respond?" check passes. **When hand-calling any bearer-gated route, assert the body is JSON**; HTML means the token was wrong (or a placeholder was pasted literally), not that the endpoint is broken.

Mitigation shipped 2026-08-15: the alert DM in `/api/cron/alerts-send` now tells the user to reply with `/ask` and that plain messages don't reach the bot — that DM is the message a user is most likely to reply to. Guarded by `__tests__/discord-alert-dm-tells-users-how-to-reply.test.ts`, which also pins the Telegram side as deliberately carrying no such caveat.



---

## Preserved from the 2026-08-17 CLAUDE.md restructure

> These lines were condensed or dropped in CLAUDE.md when it was cut to fit the memory-file
> char limit. They are kept here verbatim so nothing is lost.

### Concierge non-negotiable rules

1. **Pinnacle FMV**: NEVER join by `edition_key` alone — always triple (`character_name`, `set_name`, `variant_type`) per `92aab30`. Cadence uses `Int` not `UInt64`.
2. Memory-FMV banned (`a910745`) — must tool-call same turn.
3. `get_fmv` reads `editions + fmv_snapshots` primary; returns `p10/p50/p90` + sample shape.
4. Tier filter: `.eq` not `.ilike` per `f55e022 + e9c90e5`.
5. `trg_support_conv_updated_at` OWNS `shipped_at / updated_at` — never set manually.
6. `/api/admin/feedback` GET MUST filter `feedback_type IS NOT NULL`.

(CLAUDE.md keeps items 1 and 2 plus the read-only and error-vs-empty rules inline, and points here for the rest.)

### The three rules CLAUDE.md held inline until 2026-08-17 (moved verbatim; CLAUDE.md now keeps only the READ-ONLY one)

⚠ These are numbered 3–5 in CLAUDE.md's old inline list and are **not** duplicates of items 1–6 above — they are the honesty rules, and they generalize past the concierge.

3. **RPC is READ-ONLY** — no cart, no gifting, no trading. Never offer an action the product lacks.
4. **An errored tool is NOT an empty result** — `status:"error"` and `status:"no_results"` are distinct claims; their prompt rules must stay distinct.
5. **A tool cannot observe its own health** — it can only report how old its data is. Never let copy reassure that a feed is fine.
