<!-- Extracted from CLAUDE.md on 2026-08-17 to bring that file under the memory-file
char limit. Content is VERBATIM; CLAUDE.md carries a one-line pointer to this file.
Same rules apply: every number here is a dated sample - re-measure before quoting. -->

## Deferred hardening

Tracked but intentionally unfixed — revisit when adding a real consumer or a per-row write API.

- `email_subscribers`, `outbound_clicks`, `portfolio_snapshots`, `support_conversations` each have an INSERT policy with `qual=true`/`with_check=true` for `roles=public`. Hardening to add when revisited: per-row size caps via CHECK constraints, `created_at`-based rate-limit column or trigger, `bot_score` column populated from BotID, possibly an unauthenticated rate-limiter at the edge.
- `user_achievements` + `watchlist_items` migrated 2026-04-27 to service-role-only writes. Both still use `owner_key` (text) instead of user_id UUID. Neither table is referenced by any /api route today. When a real consumer arrives, do the user_id+RLS migration like saved_wallets / trophy_moments / profile_bio.
- `badge_editions.low_ask` coverage gap — **AllDay RESOLVED (verified 2026-07-17), the cron this note asked for is LIVE.** `allday-badge-low-ask-refresh` (pipeline_runs, `nfl_all_day`, every 30 min, ok=true, ~3,800 rows/refresh) now keeps AllDay at **3,897/5,607 (69.5%), fresh same-day** — the old "0/1572 always NULL" is stale; do NOT build a second AllDay cron. TopShot healthy and fresh (1,929 low_ask + 1,929 offers; the pct fell to ~28% only because the badge-set backfill grew the row count 2,987→6,930, not a regression). **Golazos low_ask RESOLVED (verified live 2026-07-27, supersedes the "111/218, no pipeline, UNIDENTIFIED" note this bullet used to carry).** The `golazos-badge-low-ask-refresh` cron IS live and healthy (pg_cron `10,40 * * * *`, last run `succeeded`; SECDEF `refresh_golazos_badge_low_ask()` self-heals `edition_id` via `resolve_golazos_listing_edition_ids()` then reads `golazos_edition_floor_ask`), holding Golazos at **81/218 low_ask, fresh same-hour**. The 37% ceiling is **listing-gated** (only editions with a live Flowty floor get a `low_ask`), not a bug — do NOT build a second Golazos low_ask cron. **The one genuinely-open Golazos gap is `highest_offer` 0/218:** unlike `low_ask` (which the refresh fills), `highest_offer` is fed by the offers pipeline (`edition_offers`), and `edition_offers` holds **ZERO Golazos rows** — there is no Golazos offer source at all. Filling it therefore requires a NEW Golazos offers indexer (a DapperOffersV2 sweep), a real ingest project, not a refresh tweak — operator/ingest call, not autonomous. **STAGED 2026-07-28 (inert, pending one on-chain recon):** `app/api/golazos-offers-indexer/route.ts` (mirror of the live `allday-offers-indexer`) + `golazos_open_offers` table are committed but **uncronned** — the gate is whether Golazos OffersV2 offers are EDITION-type (unverifiable from the cloud sandbox: no Flow REST egress). One authenticated POST from prod settles it; see [docs/handoff-2026-07-28-golazos-offers-indexer.md](../../docs/handoff-2026-07-28-golazos-offers-indexer.md).
  ✅ **THAT POST WAS RUN ON 2026-07-28 AND THE GATE IS SETTLED — the answer just never made it back to this bullet.** It is recorded in the route's own header (`app/api/golazos-offers-indexer/route.ts`) and nowhere else, which is why this line still reads as open. **The result is NOT the "NFT-type offers" case this bullet hypothesizes: Golazos has NO DapperOffersV2 offers at all, of EITHER type.** The gated POST returned `ok:true / pages 60 / offersSeen 0`, and an independent Flow REST sweep found **zero Golazos `OfferAvailable` events in a contiguous 400,000-block window** (~4 days, 14,495 offers total — TopShot 9,460, MFLPlayer 3,986, AllDay 775, MFLClub 274, **Golazos 0**, UFC 0), plus zero across six spot samples spanning 2026-02 → 2026-07. Positive control on the same contract and code path in the same 24 h: allday-offers-indexer 142 offers, topshot-offers-indexer 1,979. **The cause is demand, not plumbing.** ⚠ **Re-derived live 2026-08-23** (numbers move, the conclusion does not): `golazos_open_offers` **0 rows**, `badge_editions` Golazos `highest_offer` **0/218**, `low_ask` **75/218** (this bullet says 81 — a dated sample), and 30-day sales **Golazos 70 vs Top Shot 110,873**, a 1:1,584 ratio — *more* extreme than the 123-vs-90,612 at recon time. **So building the offers indexer would index nothing. Do not schedule it, and do not re-run the recon** — re-test only by POSTing the existing inert route and reading `offersSeen`.

---

## Known issues / active work

Main branch is the canonical clean branch.

**Status reconciled 2026-05-23** against the codebase + production DB. Full verification table: `docs/health/PROJECT_HEALTH_2026-05-22.md` §9. Item numbers below are stable (they match the report); resolved items are listed at the end under their original numbers.

### Platform changes (May 2026) — these make several sections of this file stale

- **Flowty shut down its NFT marketplace FRONTEND (~2026-05-13) — but its API is ALIVE (re-verified 2026-07-07: api2.flowty.io serves current Series-8 listings; the infra now backs dapper.market).** The loan-book (`flowty_loans` / `flowty_loan_events`) ingest and analytics MVs are frozen history, but the `flowty-proxy` edge fn + listing-cache routes are LIVE ingest feeding cached_listings/ASK-FMV today — do not treat them as dead. The "Flowty API", sniper-feed, and worker sections of this file describe what is now legacy/dead infrastructure pending a deliberate teardown. `flowty_loan_events` going cold on 2026-05-11 is expected behaviour, not a regression.
- **NFL All Day ended primary pack sales.** AllDay `PackNFT.Mint` ingestion and AllDay pack-EV are historical-only; AllDay is a secondary-market collection going forward.

### Open

> ⭐ **REGISTER SWEEP 2026-08-26 (PT) — every item below whose status could be settled by a live read WAS re-read, not re-quoted.** Touched this pass: **#8** (still regressed, 0/8 ok), **#27** (unchanged, and 15 of 99 active jobs share the 600 s default), **#29** (ratio moved; the two failures are upstream 503s, not kills), **#30** (index shipped, hypothesis MEASURED, residual is now egress), **#34** (Trevor decided: no Sentry spend — mitigations shipped instead), **#35** (cadence cut REVERTED and the revert confirmed), **#36** (shipped, correctness proven, cost claim NOT), plus two new items **#37** (React #418 live in production) and **#38** (a 99.6%-failed pipeline that was filed but never registered).
> ⚠ **Not swept, and named rather than left implied:** #10, #11, #14, #17, #18, #20, #21, #22 (re-verified separately — still live), #23, #25, #31, #32, #33. **An item this stamp does not name carries its own older date; read that date, not this one.**

0. **Wallet verification — RESOLVED BY REMOVAL 2026-08-08 (Trevor directive): RPC offers NO wallet sign-in on any surface.** Users cannot sign into Dapper Wallet without Dapper developer approval RPC does not have, so every connect path was deleted rather than left as a promise the product kept making and never keeping. **The evidence it never once worked:** `saved_wallets.verification_method` = `listing_challenge` 5 / `owner_attested` 4 / NULL 90, with **ZERO** `fcl_dapper`/`fcl_blocto`/`fcl_other`/`hybrid_custody_link`; **0** synthetic `@flow.rip-packs-city.local` users out of 21 `auth.users`; `fcl_auth_nonces` **1 row ever minted, 0 consumed**. Deleted: `components/SignInWithDapper.tsx`, `components/auth/ConnectButton.tsx` (a verified orphan, mounted nowhere), `lib/hooks/useFlowUser.ts`, `lib/chains/flow/fcl-config.ts`, the dashboard's inline `verifyViaLink` "Fastest: verify with a linked wallet (read-only)" card (read-only framing, still `fcl.authenticate()`), and the server half `app/api/auth/fcl-{verify,nonce}` + `app/api/profile/verify-link`. **RPC now asks only for a public IDENTIFIER** — a Dapper address or Top Shot username (Panini username / Candy address once those publish) — and reads it view-only; `/dashboard`'s `SignInBanner` is a single chain-detected field. **The working verification path is unchanged and is now the ONLY self-serve one: the listing challenge** — RPC picks one of the wallet's cheap Moments, the user lists it at a unique ~100×/$10-floor price, and `/api/profile/verify-challenge/check` confirms it live via the topshot-proxy GQL then calls `resolve_wallet_challenge_match` (+500 credits). `admin_verify_wallet` (owner-attested) remains the manual fallback; those two produced all 9 verified wallets. ⚠ **The landmine this removal had to dodge:** `ProBadge` keyed `useProStatus` on `fcl.currentUser`, so with no connect surface it would have rendered `null` for every Pro/Founding member site-wide (GlobalSiteHeader, `/my-teams`, `/analytics`) with `tsc` perfectly green — re-pointed onto the new `lib/hooks/useSessionOwner.ts` (cookie session via `/api/profile/me`). `__tests__/no-client-wallet-connect.test.ts` pins the invariant (no client `@onflow/fcl` import, no `fcl.authenticate()`, no `discovery.*` config anywhere). **`@onflow/fcl` STAYS in `package.json`** — every remaining use is a server-side READ (`fcl.query`/`fcl.send`) in wallet-search, owned-flow-ids, sales-indexer, allday-sets, cost-basis-backfill, cron/ownership-onchain-walk, and the breaks routes; `lib/breaks/server-authz.ts` exports its OWN separate `configureFcl()` — do not confuse it with the deleted one. **DB follow-up NOT done (deliberately deferred to a separate migration, after the deploy is READY):** `fcl_auth_nonces`, the `purge_old_fcl_auth_nonces` job, and the `verify_wallet_via_fcl` RPC are now dead; dropping them in the same window as the code would 500 anyone mid-flow. `saved_wallets.verification_method`'s CHECK still allows the `fcl_*` values — left alone (zero rows use them; narrowing buys nothing).
   🚨 **2026-08-23 — THE DROP LIST IN THE SENTENCE ABOVE IS INCOMPLETE, AND FOLLOWING IT LITERALLY BREAKS A LIVE JOB THAT PURGES TEN OTHER TABLES.** It names three objects and omits the caller that makes the ORDER matter: pg_cron `rpc-weekly-log-purges` → `run_weekly_log_purges()` → **`purge_old_fcl_auth_nonces(7)`** → `DELETE FROM fcl_auth_nonces`. `run_weekly_log_purges()` also purges `pipeline_runs`, `debug_logs`, `fmv_phantom_attempts`, three failure tables, `smoke_test_results`, `usage_events`, `wallet_holdings_snapshots` and `support_conversations`. Drop the table first and that function raises `42P01`, which propagates **before** the `log_pipeline_run` call at the end — so ⚠ **the whole weekly purge stops AND WRITES NO ROW. It presents as SILENCE, not failure**, and `pipeline_runs` retention is one of the things that stops. ⚠ **Third missed dependency:** `supabase/tests/purge_old_fcl_auth_nonces.sql` is a registered DB-invariant pin, so dropping the function while that pin stands reddens CI. ✅ **The correct, ordered version is committed as `supabase/migrations/20260823070000_audit_20260823_retire_dead_fcl_wallet_auth_objects.sql`** — it re-creates `run_weekly_log_purges()` without the nonce leg FIRST, then drops the two functions, then the table, and it carries the full six-source caller enumeration and revert path. ⚠ **The repo half (deleting the pin + its PINS entry) must land BEFORE the DDL** — that direction is green in both intermediate states; the reverse reddens main for the length of the gap. ⚠ **Correction to the stated motive:** "dropping them in the same window as the code would 500 anyone mid-flow" no longer applies — the table holds **0 rows** and both functions are **service_role-only** (anon and authenticated EXECUTE are false on each), so this is dead weight, not a live surface.

1. **Cart execution — SHELVED 2026-05-24, FRONTEND + API DELETED 2026-08-01 (see the Update at the end of this bullet).** RPC is an intelligence product; in-app live-buy is not a goal. The Cadence code in `lib/cadence/purchase-moment.ts` stays in the repo, dormant and revivable, but off the critical path — do NOT pursue H1/H2 or the external deps (`NEXT_PUBLIC_WALLETCONNECT_ID`, Dapper co-signer registration). Market/Sniper were reframed 2026-05-23 (commit `b19d8f2`) to FMV + discount intelligence with outbound "View Listing" links. `docs/audits/purchase-moment-2026-05.md` retains the historical Cadence detail. **Update 2026-08-01 (READ-ONLY directive, Trevor): the entire Cart FRONTEND + API is DELETED, not merely shelved.** Trevor's call — "get rid of anything gifting, cart, or trading from any front end surfaces, expect everything to be purely read-only… low friction and low risk." Removed: `components/cart/` (CartButton + the ~500-line CartDrawer), `lib/cart/` (CartContext / eligibility / usePurchaseQueue — the browser `fcl.mutate` purchase executor), `lib/cart-drawer-compute.ts`, and both API routes `app/api/cart/{validate,record}` (⚠ `record` was an **unauthenticated anon POST → service-role INSERT** into `cart_purchase_log`). Verified before deletion: **the cart had ZERO mounts** — `CartProvider` was in no layout and `CartButton` had no importer, so nothing rendered in prod (any CartDrawer seen on a live page was stale CDN HTML). Also dropped: the `/api/cart/validate` smoke probe (suite 56→55, hardTotal 44→43), the `cart` entry in `component-gate-include-completeness`'s `KNOWN_UNMEASURED`, and the stale "cart purchases" claim in `/privacy`. **KEPT deliberately:** the inert Cadence template strings `lib/chains/flow/cadence/{purchase-moment,purchase-moment-flow-wallet,make-offer-topshot,make-offer-flowty}.ts` + `__tests__/cadence-transaction-templates.test.ts` — they are data, not an executable path, and this bullet's original retention decision still stands; the DB table `cart_purchase_log` is untouched (service-role-only since `audit_20260731_revoke_log_cart_purchase_and_prune_stale_secdef_allowlist`). **Revert:** `git revert <sha>` restores every file; nothing to unwind in the DB.

3. **Trade Hub — SHELVED 2026-06-01, DELETED FROM THE TREE 2026-08-01 (see the final Update in this bullet), same class as Cart #1.** On-chain trade escrow (`RPCTradeEscrow`) is not deployed; the 5 submitters in `lib/trade-escrow/fcl-submit.ts` (deleted 2026-08-01, see the final Update) were returning fake `0xstub_` tx ids, implying swaps that never happened. Guarded 2026-06-01: each submitter calls `ensureLive()` (throws unless `RPC_TRADE_ESCROW_ADDRESS` is set); the live routes `/api/trade-chain/{propose,execute,deposit-callback,cancel-callback}` + `/api/admin/reclaim-expired-trades` return 503 "Trade Hub is not available yet."; `/dashboard/trade-hub` `notFound()`s via a server gate (split into `TradeHubClient.tsx`). The wishlist/offers/matches CRUD (`/api/trade-hub/*`) is untouched. To re-enable: deploy the contract, set `RPC_TRADE_ESCROW_ADDRESS`, and replace each stub body with the real `fcl.send` per the file's NEXT_STEPS + `RPCTradeEscrow_DEPLOYMENT.md`. Revert the guard: `git revert`. **Update 2026-07-17:** the contract now has a green `flow test` suite (`cadence/tests/RPCTradeEscrow_test.cdc`, 16/16, all 12 audit scenarios + 4 bonus properties), and two latent compile blockers were fixed in the still-undeployed contract (`Trade.execute()` → `settle()` — `execute` is a hard keyword in Cadence ≥1.0 — and the NonFungibleToken import switched to string form). Shelve status unchanged. **Update 2026-07-25 (Claude Code):** completed the panel's Cancel TODO — new `/api/trade-chain/cancel-callback` route (mirrors `deposit-callback`: 503 while shelved, else party-check + status→`cancelled` + `cancel_tx_id`) + client stub `lib/trade-escrow/sign-cancel.ts` (mirror of `sign-deposit.ts`) + `TradeChainPanel.onCancel` now calls it (was console-log-only). Shelve status STILL unchanged — route 503s and the page `notFound()`s in prod. **Update 2026-07-31 (Claude Code, Trevor-directed):** the 5 `fcl-submit.ts` submitters AND the 2 client sign helpers (`sign-deposit.ts`/`sign-cancel.ts`) are now WIRED FOR REAL — the "replace each stub body with real fcl.send" instruction above is DONE. New `lib/trade-escrow/cadence.ts` holds 5 address-injected FCL transaction templates (deposit is ONE universal template over the generic `NonFungibleToken.Provider`/`Receiver` since the contract validates NFT type/ids on-chain); server verbs `fcl.mutate`+`onceSealed` (throw on sealed-with-error) signed by the RPC hot wallet via the verified `lib/breaks/server-authz` authz; client verbs user-wallet `fcl.mutate`. **Shelve status STILL unchanged — INERT in prod:** `ensureLive()` still throws unless `RPC_TRADE_ESCROW_ADDRESS` is set, the routes still 503, the page still `notFound()`s. ⚠ **UNVERIFIED + UNTESTED ON-CHAIN** — `RPCTradeEscrow` is not on Flow mainnet, so the mandated Cadence-MCP-against-mainnet verification could not run; templates are correct-by-construction against the in-repo contract source + the 16/16-green `flow test` templates + the production NFT-standard Cadence in `break-transactions.ts`, but **MUST be testnet-dry-run for all 5 verbs before go-live**. Go-live now needs TWO envs (the server-only `RPC_TRADE_ESCROW_ADDRESS` is not visible in the browser): `RPC_TRADE_ESCROW_ADDRESS` (server) + `NEXT_PUBLIC_RPC_TRADE_ESCROW_ADDRESS` (client, same value). `RPCTradeEscrow_DEPLOYMENT.md` referenced above does NOT exist in the repo — the grounding sources are the ones just listed. Tests: `__tests__/trade-escrow-{cadence,fcl-submit,sign-deposit,sign-cancel}.test.ts`. Revert: `git revert <sha>` restores the honest stubs. **Update 2026-08-01 (READ-ONLY directive, Trevor) — the Trade Hub is DELETED from the tree, not shelved.** Same directive as Cart #1. The 07-31 update above is what forced this: live-fire mainnet-signing code (`fcl.mutate` against the RPC hot wallet) that was **UNVERIFIED ON-CHAIN against a contract that is not deployed** is a strictly worse state than no code, and a 503 route guard does not remove it from the repo. Deleted: `lib/trade-escrow/` in full (`cadence.ts`, `fcl-submit.ts`, `sign-deposit.ts`, `sign-cancel.ts`, `types.ts`), the page `app/dashboard/trade-hub/` (page + `TradeHubClient` + `TradeChainPanel`), the on-chain routes `app/api/trade-chain/{propose,execute,deposit-callback,cancel-callback}`, the off-chain CRUD `app/api/trade-hub/{wishlist,offers,matches}` (its only consumer was `TradeHubClient`), the cron route `app/api/admin/reclaim-expired-trades`, and all 15 associated tests. Also removed the two `trade-hub` entries from `scripts/check-brand-tokens.mjs`. **KEPT deliberately:** the Cadence contract source + its green 16/16 `flow test` suite (`cadence/contracts/RPCTradeEscrow.cdc`, `cadence/tests/**`, still gated by the `cadence-escrow-tests` CI job) and the `trade_chain_state` / wishlist / offers / matches DB tables — no migration was run. **To revive:** `git revert <sha>` restores the whole feature (page, routes, submitters, tests); the contract still needs deploying + `RPC_TRADE_ESCROW_ADDRESS` / `NEXT_PUBLIC_RPC_TRADE_ESCROW_ADDRESS`, and the ⚠ testnet-dry-run-all-5-verbs requirement above still binds.

3b. **Gifting — REMOVED from the frontend 2026-08-01 (READ-ONLY directive, Trevor).** `/dashboard/gift` (parent-signed Hybrid-Custody Top Shot gifting, shipped 2026-07-13) was the one LIVE write surface of the three; it is now deleted, not gated. Removed: `app/dashboard/gift/` (page + `GiftClient`, which did the user-wallet `fcl.mutate`), the routes `app/api/gift/{children,quote,record}`, the server read module `lib/chains/flow/gift.ts` (hybrid-custody-proxy Cadence reads), both tests, the "Gift a Moment" CTA on `/dashboard`, and the concierge's `/dashboard/gift` product-surface bullet (its shelved-features rule now states plainly that RPC is read-only — no cart, no gifting, no trading — so the assistant can never offer an action the product lacks). **KEPT deliberately:** the inert template `lib/chains/flow/cadence/gift-moment.ts` (data only, no signer, same treatment as `purchase-moment.ts`), `app/api/wallet/capability` (a pure read of the wallet-link graph), and the `moment_gifts` table + its migration — no DB change. **Revert:** `git revert <sha>`.

4. **Pinnacle FMV — RESOLVED (verified 2026-05-24; table renamed 2026-06-08, re-verified 2026-06-28).** The "0 FMV editions" claim was stale. Pinnacle FMV is RENDER-keyed and lives in its own `pinnacle_fmv_history` table (cols: `render_id, fmv_usd, fmv_confidence, fmv_sales_count_30d, computed_at`; ~13.4k history rows, ~1.8k renders priced in 2d), recomputed daily by engine `pinnacle-2.0.0-render` and propagated to `wmc` hourly by `populate-pinnacle-wmc-fmv`. Pinnacle ASK comes from `pinnacle-listings-indexer` (direct-chain), not Flowty. CRITICAL: Pinnacle FMV lives in `pinnacle_fmv_history`, NOT the uuid-keyed `fmv_snapshots`; the old `pinnacle_fmv_snapshots` table was DROPPED 2026-06-08 (survives only as `pinnacle_fmv_snapshots_backup_20260608`) so a query against `pinnacle_fmv_snapshots` now 42P01-errors.
   ⚠ **`pinnacle_fmv_history` is NOT an independent source — it is written by an `AFTER INSERT/UPDATE` TRIGGER on `pinnacle_catalog`** (`pinnacle_catalog_fmv_history_capture`). So it is a derivative copy, and reconciling the two is a tautology, **not** a cross-source check.
   ✅ **The ASK_ONLY drop is FIXED, and this bullet described it as open until 2026-08-16.** It silently discarded the ASK_ONLY revision for **776 renders** because `NOW()` is transaction-stable — `pinnacle_fmv_recalc_render_all()` writes each render twice in ONE transaction (the sales loop, then the ASK_ONLY pass), both stamping the same `fmv_computed_at`, and the trigger's `ON CONFLICT (render_id, computed_at)` was `DO NOTHING`. Migration `20260815172945` changed it to `DO UPDATE` (keep the LAST write per timestamp) and it is applied; **measured live 2026-08-16, renders whose catalog price disagrees with their newest history point on an ASK_ONLY value = ZERO**. The fix is **forward-only by design** — the pre-existing wrong rows were not rewritten, because each render's latest point becomes correct on its next recalc. Pinned in both directions by `supabase/tests/pinnacle_fmv_recalc_render_all.sql`, which reproduces the exact double-write and asserts the SECOND (published) revision survives, so a revert to `DO NOTHING` reds CI.
   ⚠ **A SMALLER, DIFFERENT CASE IS STILL OPEN — pinned as CURRENT BEHAVIOUR, not fixed.** The capture trigger's WHEN clause is `new.fmv_usd IS NOT NULL`, so the sweep's self-correct step (an ASK_ONLY render whose floor disappeared → `fmv_usd = NULL`, `NO_DATA`) **writes no history row at all**. The chart therefore keeps that render's last PRICE as its newest point, forever, while the page shows it unpriced — measured 2026-08-16: **20 renders, oldest 54 days**. Same page-contradicts-chart shape as the bug above, reached by a different route. Whether a price SERIES should record a de-pricing is a product decision, not a bug fix, so it is asserted rather than changed. **Do NOT "fix" the tautology by asserting catalog = history in a guard** — that fires on every one of these while saying nothing about drift.

7. **AllDay `unmapped_sales` backlog — RESOLVED 2026-05-25.** The earlier "historical spork scan" framing was wrong: the backlog is not spork-era data. All 2,550 NFL All Day unmapped rows are under 6 weeks old and were starved by the resolver running at `batch_size: 5` against a Flowty-only lookup. Fixed by the GQL-primary edge-function rewrite + `batch_size 5→200` bump in the 2026-05-25 (latest) session above. The Pinnacle side is separately covered by the direct ASK pipeline (Phase 2C, 2026-05-11). The `spork-proxy` worker remains live for any genuinely spork-era investigation but no longer blocks the unmapped-sales backlog.

9. **Storefront audit pipeline — RETIRED (verified 2026-05-24).** It is a manual script (`scripts/scan-historical-storefront.mjs`), not a deployed cron or route — not monitored, not read by any frontend code. Cold since 2026-04-28 simply because nobody runs the script. De facto retired; no operational action. `storefront_audit_wallets` (5,365 rows, tiny) is harmless — optional drop candidate.

   **Storefront-cleanup machinery removed (2026-06-03).** The manual listing-cleanup chore (`scripts/cleanup-storefront-wallets.mjs` → root `cleanup.cdc`, which shelled `flow transactions send cleanup.cdc <addr> --signer my-account` gas-paid by the Cadence payer wallet `0x73f55c4450b8d466`) was the sole FLOW drain on that wallet, and it cleaned only dead storefronts (tied to the TS listings-indexer retired 2026-05-26 + Flowty shut 2026-05-13) — zero product value, since no live surface (FMV / analytics / insights / concierge / pack-EV — all reads + Supabase) draws on it. Both files were deleted to make the drain impossible to restart from the repo. **The payer wallet `0x73f55c4450b8d466` is intentionally empty and its `cadence-payer-balance-check` cron is paused** while all Cadence-write features (breaks / Cart / Trade Hub) are shelved (the `breaks` schema `20260509120000_breaks_schema.sql` is UNAPPLIED in prod). The `/api/cron/cadence-payer-balance-check` route is left dormant (not deleted) for easy revival. **To revive:** fund the wallet >0.05 FLOW + un-pause the cron-job.org entry. If `cleanup`/`flow` txns keep appearing on Flowscan from this wallet with no human at the keyboard, an out-of-repo scheduler (OS cron / Task Scheduler / launchd) is still alive — kill it as part of the Flowty teardown. Ledger cross-ref: N3.

8. **NBA stats / projections — REGRESSED, re-opened 2026-08-16.** ⚠ **Filed under `### Resolved` until 2026-08-17, which is why it kept being read as closed** — the entry was loudly marked REGRESSED in place, but it sat under a Resolved heading, so anyone enumerating the Open list never saw it. Moved here. **The original text said "syncing again (no longer 0 rows/day)" and that has been false since 2026-08-04.** `sync-nba-projections` has failed **100% of its runs for 13 days** — measured live: 08-02/03 all `ok`, then 5 of 8 failing on 08-04 and **every run since**, error `all_upstreams_failed`. `nba_player_projections` holds 485 rows, newest `last_synced_at` **2026-07-20**, i.e. **27.4 days stale**. It is on **no `pipeline_cadence_watchlist` arm**. ⚠ **RE-DERIVED 2026-08-22 (do not quote the 08-16 numbers above — they were a dated sample and the item got WORSE, not better):** the 100%-failure run is now **18 consecutive days** — 08-04 was the mixed day (5 ok / 3 fail), and **every day 08-05 through 08-22 is 0 ok / all fail**, `all_upstreams_failed` throughout (132 failed runs over the trailing 20 days). `nba_player_projections` still holds **485 rows**, newest `last_synced_at` unchanged at **2026-07-20 18:07Z — now 32.8 days stale**. `nba_players` is **174 rows, newest `created_at` 2026-05-07** (the starved catalogue of item #8's sports-proxy 403). 🚨 **AND THE `ok` DAYS WROTE NOTHING EITHER — this is the `rows_written = 0` null instrument, live.** 08-02 and 08-03 report **8 runs, 8 ok, `rows_written` = 0**: the pipeline was already producing zero rows while every instrument called it healthy, so **restoring the upstream will NOT by itself refill the table** and a fix must not be declared on `ok` turning true. That is the same slate-gated mechanism CLAUDE.md records for `nba_players` (the season ended 08-04, which is also when `ok` flipped — ⚠ a correlation worth testing, NOT a cause established here). **Re-confirmed 2026-08-22: still 0 of 102 `pipeline_cadence_watchlist` rows match, and `detect_stalled_pipelines()` returns nothing for it** — so 18 dark days produced no alert by construction, not by oversight. **Cheapest real fix is an arm, not a repair:** the pipeline cannot be fixed here (it is downstream of the sports-proxy 403, operator-gated), but its silence can be made visible. Filed: [inbox/2026-08-22T1450Z-nba-projections-18-dark-days-and-the-concierge-probe-check-nobody-ran.md](../overnight/inbox/2026-08-22T1450Z-nba-projections-18-dark-days-and-the-concierge-probe-check-nobody-ran.md). ✅ **RE-VERIFIED 2026-08-26 (PT) — still regressed, and the pipeline is honest about it:** `sync-nba-projections` ran **8 times in the trailing 24 h, 0 ok**, every tick `error = all_upstreams_failed`, `rows_written = 0`. ⚠ **That is the OPPOSITE of a null instrument** — it reports its own failure on every dispatch; what is missing is a reader. Nothing here re-opens the ⛔ measured-dead ESPN finding below.
  ✅ **THE DECISIVE TEST HAS BEEN RUN (2026-08-17 18:01 PT, from Trevor's residential box) AND IT SPLITS THE TWO LANES — the "three providers tightening bot-blocking" reading is REFUTED as a single cause.** `site.api.espn.com` returns **HTTP 200 / 9,448 bytes** residentially while 403ing from Supabase edge ⇒ that lane really is **egress-network blocking**, fixable by routing it through a Cloudflare Worker (no secret, no rotation). `cdn.nba.com` returns **403 residentially too** — on **every path including the bare host root** — with an **Akamai `errors.edgesuite.net` "Access Denied"** body, and **403s identically with a current Chrome UA and with a full browser header set**. ⚠ So that lane is **not our egress and not a User-Agent**, which means ⛔ **the UA refresh and the 403-retry are now MEASURED to be useless on it** — the two things the warning below pre-emptively forbade. ⚠ It also falsifies the design assumption the code rests on: `workers/sports-proxy/index.ts:564` calls that host *"open to CF Workers, no WAF"* and gives it the file's only fetch with no retry and no fingerprint rotation **because** of that belief. Remaining unknown: TLS/JA3 fingerprinting vs a retired path — **a real-browser fetch settles it and has NOT been run** (a Playwright attempt exceeded the tool budget), so do not assume JA3. **Two causes ⇒ two fixes; check whether restoring ESPN alone feeds `nba_players` before touching the NBA lane at all.** Full measurement: [inbox 2026-08-18T0100Z](../overnight/inbox/2026-08-18T0100Z-the-sports-proxy-403-is-TWO-causes-not-one.md).
  ✅ **The instrument is NOT the problem — it was fixed on 2026-08-08 and the fix is DEPLOYED and WORKING.** The edge function's own comment records the earlier incident verbatim: it used to collapse "every upstream broke" and "an upstream answered and there is no slate today" into one `all_upstreams_failed`, and *"the offseason supplied a plausible story for it, and the actual cause (a sports-proxy 401, visible in this very payload) went unread"*. The repaired version emits `reason: "no_slate"` for the offseason case. Live payloads carry `function_version: 9`, `reason: all_upstreams_failed`, `rolling_ok: false`, `espn_ok: false` — **so this is the REAL-failure branch, not the offseason branch**, and the discriminator that exists to prevent exactly this misreading is doing its job.
  ⚠ **The cause is in the payload, unread for 13 days: `rolling_upstream_status: 403`.** ⚠ **BUT THE READING OF IT WAS WRONG, AND THAT WRONG READING IS WHY THE HIGHEST-VALUE ITEM SAT PARKED FOR SESSIONS — REFUTED 2026-08-17.** This bullet used to say the proxy was "refusing", that it was "the same shape as the 401 the 08-08 fix was written about", and that ⛔ *"Secret/credential work is OPERATOR-ONLY (reading it would leak it into a transcript), so this is a handoff, not an autonomous fix."* **No secret is involved at any point.** Three independent grounds, all from data already on hand:
  1. **The proxy returned 502, not 401.** `workers/sports-proxy/index.ts:970-972` returns `{"error":"unauthorized"}` **401** on a bad or missing `X-Proxy-Secret`, *before any upstream is touched*. The live payload carries `dk_status: 502` / `rolling_status: 502` **with `dk_upstream_status: 403` and `rolling_upstream_status: 403` populated** — and recording an upstream status requires having already passed auth.
  2. **The edge function's own env guard did not fire.** `sync-nba-projections/index.ts:642` short-circuits on `!SPORTS_PROXY_URL || !SPORTS_PROXY_SECRET` with `has_url`/`has_secret` diagnostics; that branch is absent from every failing payload, so both are set.
  3. ⚠ **The ESPN lane does not use the proxy or the secret AT ALL and is ALSO 403ing.** The edge function says so at lines 16 and 368 — *"site.api.espn.com scoreboard (direct, no proxy)"*, *"open to Supabase egress"* — so it fails from **Supabase edge egress, a different network entirely**.

  **What is actually happening: three providers, two independent egress networks, all 403** — DraftKings via Cloudflare Worker (an Akamai `errors.edgesuite.net` "Access Denied" page), `cdn.nba.com` via Cloudflare Worker, and `site.api.espn.com` direct from Supabase. That pattern points at the providers tightening bot-blocking, **not** at one stale fingerprint or one blocked IP range of ours. ⚠ **The `cdn.nba.com` failure specifically contradicts the design's own assumption** — its route comment calls that host *"static S3 mirror … Open to CF Workers (no WAF)"* and therefore gives it **the only fetch in the file with no retry, no fingerprint rotation, and a deliberately minimal 2-header request**; it was built thin *because* it was assumed WAF-free. ⚠ A latent sibling: `fetchWithStatsRetry` returns immediately on any status `< 500` (*"4xx that won't change"*) — true when written, false for a bot-detection 403, but **not** the currently-failing lane. ⚠ `BROWSER_POOL` is **~2 years stale** (Chrome 126/127/128, Safari 17.5, Firefox 128 — mid-2024 captures presented in August 2026), which is real but is **not obviously the fix**, since a worker-side fingerprint change cannot touch the ESPN lane. ⛔ **Do NOT ship a UA refresh or a cdn 403-retry before running the decisive test** — one `curl` of that cdn URL from any ordinary non-datacenter network: **200 ⇒ the block is IP/ASN-based and a fingerprint refresh will not help** (the fix is a different ingress or a paid feed); **403 ⇒ header-based and the refresh is worth doing.** The two outcomes point at opposite fixes. ⚠ The sandbox cannot run it — it answers `CONNECT tunnel failed, response 403`, which is the **SANDBOX's** 403 and not NBA's; do not use it as evidence either way. Filed: [inbox 2026-08-17T1845Z](../../docs/overnight/inbox/2026-08-17T1845Z-the-sports-proxy-403-is-not-a-secret-it-is-three-upstreams-across-two-egress-networks.md).
  ⚠ **Impact is currently NIL for PROJECTIONS and becomes real on a clock — which is exactly why it will keep being ignored.** There are no NBA games in August (newest `game_date` is 2026-07-20, the end of Summer League), so nothing downstream is visibly wrong today. It starts mattering at preseason (~early October). **Do not close this on "no user impact" — the impact is deferred, not absent.**
  ⚠ **AND THE PROJECTIONS ARE NOT THE ONLY CASUALTY — this pipeline also writes `nba_players`, which makes it the ROOT CAUSE of two things being triaged as separate items.** That catalogue sits at **174 players across 19 of 30 teams, last synced 2026-05-07 (101 days stale)**. Downstream: **Fast Break** reads it (`app/api/fast-break/{today,uses,optimize}`) and is therefore running on a 19-team roster *now*, not in October; and **`match-topshot-players`** matches ~1,417 distinct Top Shot names against it, which is why that pipeline has produced **zero auto-aliases in its entire existence** (`nba_player_aliases` = 7 rows, all `source = 'manual'`). **Fixing the 403 is the lever for all three; optimizing the matcher's query plan is not.**
  ⚠ **RESTORING ESPN ALONE WILL NOT REFILL `nba_players` — the roster fetch is SLATE-GATED** (`teamPlan` comes from the day's scoreboard) and the season ended 08-04, so a healthy lane yields **zero rows until ~October**, then only teams that play: **do not read a flat catalogue as the fix having failed.** Unbuilt cheap fix: a slate-independent sweep of the 30 team IDs, which `fetchEspnRoster` already supports. *(Moved verbatim out of CLAUDE.md 2026-08-18 when that bullet was compressed to a pointer; nothing was deleted.)* Filed: [inbox 2026-08-18T0120Z](../overnight/inbox/2026-08-18T0120Z-restoring-espn-alone-will-not-refill-nba_players-the-roster-fetch-is-slate-gated.md).

  🚨 **2026-08-22 23:06 PT — THREE CORRECTIONS, AND THE FIRST KILLS THE PRESCRIBED FIX. Re-measured, not re-read.**

  **(1) ⛔ THE ESPN PREMISE HAS EXPIRED, SO "ROUTE ESPN THROUGH A CLOUDFLARE WORKER" IS NO LONGER SUPPORTED BY EVIDENCE.** The whole lane split above rests on one measurement — *"`site.api.espn.com` returns **HTTP 200 / 9,448 bytes** residentially while 403ing from Supabase edge ⇒ that lane really is **egress-network blocking**, fixable by routing it through a Cloudflare Worker (no secret, no rotation)"* — taken **2026-08-17 18:01 PT from Trevor's box**. Re-run from the **same box, five days later**:

  | client | target | result |
  |---|---|---|
  | Playwright **Chromium** (real TLS, real header set, no UA override) | `site.api.espn.com/.../nba/scoreboard` | **403**, 444 B, Akamai `<TITLE>Access Denied</TITLE>` |
  | PowerShell / .NET, current Chrome 141 UA | same URL | **403** |
  | PowerShell / .NET, current Chrome 141 UA | `cdn.nba.com/.../todaysScoreboard_00.json` | **403** |
  | Playwright Chromium | `cdn.nba.com` (both the JSON and the host root) | **`ERR_HTTP2_PROTOCOL_ERROR`** — connection reset at the h2 layer, *not* a 403 |
  | Chromium `fetch()` from a real `nba.com` page (same origin, sec-fetch-\* headers) | cdn JSON | **`TypeError: Failed to fetch`** |

  ⚠ **CONTROLS, BOTH GREEN, because a bare 403 proves nothing on its own:** `api.github.com/zen` **200** and `www.rippackscity.com/api/health` **200** from the same box, same session, same clients. So the box, the network, the TLS stack and the probe all work — **the 403 is ESPN's answer, not our plumbing.**

  **What this changes:** a Cloudflare Worker is *another datacenter egress*. The 08-17 result justified proxying ESPN precisely because a residential IP got 200 where Supabase got 403; that asymmetry is gone. **Do not build the ESPN Worker on the strength of the paragraph above it — it would be a day spent on a lane that now 403s from every network we can reach from.**

  ⚠ **WHAT IS NOT SETTLED, and it matters because the two readings point at different fixes:** provider-wide tightening (ESPN/Akamai now blocking a class this box falls into) **vs. this specific IP having been listed** after a week of repeated automated probing from it. **The discriminator is one fetch of that ESPN URL from a DIFFERENT residential network** — a phone on cellular is enough. Until that is run, neither reading is established, and **"the providers tightened" must not be written down as fact.**

  ⚠ The Chromium `ERR_HTTP2_PROTOCOL_ERROR` on `cdn.nba.com` is *new information* and is a **different refusal from the .NET 403** on the identical URL from the identical box. Akamai is rejecting at different layers per client. Do not over-read it — it does not distinguish JA3 from a retired path either, and the JA3 question above remains open.

  **(2) ❌ "18 dark days produced no alert BY CONSTRUCTION" IS FALSE — THE ALERT EXISTS AND WAS DELIBERATELY MUTED.** The paragraph above reasons from `pipeline_cadence_watchlist` (0 of 102 rows) and `detect_stalled_pipelines()` (returns nothing). Both are true and both are the **wrong instrument**: this pipeline is not silent, it fires on schedule and *fails*. `get_pipeline_alerts_core()` has carried a **FAILURE-RATE arm since 2026-08-01**, whose own comment is *"a pipeline can fire on schedule and fail every single time. Cadence checks read that as healthy; this does not."* Measured live: `v_pipeline_failure_rates` holds `sync-nba-projections` at **14/14 runs failed, fail_pct 100, last_error `all_upstreams_failed`**. It produces no alert **because `pipeline_alert_suppression` mutes it until 2026-10-14** — a decision, not a construction gap.

  ✅ **And that suppression is sound — I re-checked its own predicate rather than trusting its text, which is exactly what it instructs.** Its `reason` states: *"PREDICATE THAT JUSTIFIES THIS ROW (re-check it rather than trusting this text): the run must be failing with error = all_upstreams_failed, i.e. every upstream unreachable, NOT a code fault."* Live 2026-08-22: `last_error = all_upstreams_failed` on 14/14. **The predicate holds; leave the row alone.** It also names its own accepted residual (while live, the arm cannot report a *different* failure mode on this pipeline either) and expires shortly after the season opens.

  **(3) ❌ "CHEAPEST REAL FIX IS AN ARM, NOT A REPAIR" IS WRONG, and acting on it would have shipped a no-op.** The arm already exists (see 2). Adding a `pipeline_cadence_watchlist` row would attach a **silence** detector to a pipeline that is never silent — it would fire never, while reading in every grep and review as coverage. That is the vacuous-guard shape this repo keeps paying for. **There is no cheap monitoring fix here. The monitoring is correct and is telling the truth: every upstream is unreachable.**

  **Net position after these three:** the item is *not* "highest-value, one Worker away". The code levers are exhausted (UA refresh ⛔, 403-retry ⛔, ESPN proxy ⛔ as of today), the monitoring is correct and deliberately muted with a dated re-look, and impact is still deferred to ~preseason. **The one cheap unrun experiment left is the different-residential-network fetch in (1); the one unbuilt code lever is still the slate-independent 30-team-ID sweep, and that only pays off once a lane is reachable again.**

10. **`/dashboard` token migration — ⚠ MIS-POINTED, NOT DONE. Re-derived 2026-08-24.** This read *"`app/dashboard/page.tsx` ~1,750 lines. Big lift, defer until stable."* **That file is 13 LINES.** It became a server shell on 2026-08-17 and the body moved to `app/dashboard/DashboardClient.tsx`, **which is 2,613 lines — 863 MORE than the number this entry deferred.** ➡ **The work is real and still open; only the pointer was wrong.** ⚠ **And the deferral has been costing:** the shell's own comment recorded ~2,545 lines at the 08-17 move, so it has grown since. **"Defer until stable" has no exit condition, and this is what that looks like after three months** — the recorded shape of a decision-not-to-act, on the largest client body in the repo. ✅ **The 08-17 move DID achieve its stated purpose** (verified against the coverage REPORT, not the config): the components gate carries a blanket `app/**/*Client.tsx` glob, so `DashboardClient.tsx` is measured. **Re-point this item at `DashboardClient.tsx` and re-derive its size before quoting 2,613.**

11. **Brand punch list — ⚠ ITS NAMED REMAINING ITEM IS A GHOST, and has been since 2026-06-01.** This entry read *"Still missing: the `/home-fmv-preview.png` home screenshot"* for **~3 months**, and the 2026-08-24 `PROJECT_HEALTH` repeated it — **while the home page has rendered a LIVE `components/HomeFmvPreview.tsx` since 06-01 and the PNG is referenced NOWHERE in code.** Re-derived 2026-08-24 over `*.{ts,tsx,js,jsx,json,css,html}`: **exactly one occurrence repo-wide, and it is a `data-testid` STRING inside a `vi.mock` in `component-HomePageMarketing.test.tsx`** — not an image reference. `public/` holds no `home-*` file and nothing asks for one. **The screenshot was made moot by the live component; three separate health reports (06-01, 06-03, 06-22) already said "moot" / "unreferenced" and the register never absorbed it.** ⚠ **Same shape as #12's Blazers trivia** — an entry citing an artefact that does not exist and is not wanted, which keeps getting re-reported because a register is read more often than it is re-derived. **Do not create the PNG.** ✅ **The gated half is GREEN, measured not assumed:** `scripts/check-brand-tokens.mjs` exits 0 over **399 web surfaces** scanned for brand literals (54 tracked for renames) plus **69 light-mode surfaces** clean. ⚠ **What actually remains is the ungated tail, and it is stated as tracked-not-gated on purpose:** the script's own output says *"Phase-2 debt across the rest of the repo is tracked separately — not gated here"*, and the long-tail surfaces (email HTML, Fast Break / RTR / admin) tokenize once stable. ➡ **This item is now "the ungated Phase-2 tail", not "a missing file".**

12. **Blazers trivia — CLOSED 2026-08-17: the file this slot cites does not exist.** ⚠ This entry read "`lib/blazers-trivia.ts` — 29 items shelved, still no UI" for months while the module was **absent from the tree** (verified: `find . -iname "*blazers*"` outside `node_modules`/`.git` returns **nothing**). So the slot was tracking a phantom, and three consecutive health reports flagged it as a stale reference rather than closing it. The 29 items survive in git history if they are ever wanted; there is no code to un-shelve. **Do not re-open this slot without first confirming the file exists** — that check is one `find` and it is the only thing that kept this open.

14. **Monolith page refactor — ⚠ ALL THREE TARGETS ARE MIS-POINTED THE SAME WAY, and one bigger file is untracked. Re-derived 2026-08-24.** The Phase-1 finding still holds, but **every filename and every number in this entry is now wrong**, because all three pages became thin shells and their bodies moved to `*Client.tsx` siblings — the same 2026-08-17 move as #10:

    | this entry said | that file today | where the body actually is |
    |---|---|---|
    | `collection/page.tsx` **~1,600** | **11 lines** | `CollectionTabClient.tsx` **1,347** |
    | `sniper/page.tsx` **~1,705** | **28 lines** | `SniperClient.tsx` **1,804** |
    | `analytics/page.tsx` **~1,754** | **11 lines** | `CollectionAnalyticsClient.tsx` **1,840** |

    ⚠ **Two of the three GREW** (+99, +86) while the entry recorded them shrinking — **a size claim that is only ever re-measured downward is not a measurement.** The **Phase 2** guidance is unchanged and still right: breaking the main interactive component into per-`useState`-cluster children is **medium-risk, requires rendered-DOM validation across all 5 collections, and must NOT be attempted in one commit** (plan: `docs/audits/refactor-plan-monolith-pages-2026-05.md`). Pure, unit-testable logic can still be peeled off safely.

    🚨 **AND THE ENTRY'S POPULATION IS A CURATED LIST, SO IT IS SILENT ABOUT THE BIGGEST ONE.** `app/(collections)/[collection]/pack/dist/[distId]/page.tsx` is **2,384 lines** — larger than any of the three above — is a **SERVER** page with no client sibling, and appears in no monolith entry. ⚠ **It is also unmeasured by BOTH coverage gates:** the primary gate includes `lib/**` + `app/**/route.ts(x)` and the components gate `components/**` + `app/**/*Client.tsx`, so **`app/**/page.tsx` is in neither** — there is exactly one hand-added exception (`app/insights/squeeze-check/page.tsx`). It is not unguarded (`server-page-data-access-ratchet` and `insights-server-pages-bound-their-reads` read it statically), but **no gate measures whether its branches run.** ➡ **Derive this population from the tree by size, not from a list — that is why the largest instance went unnamed.**

    ⓘ **One near-miss worth recording, because it nearly became a false finding.** Reading the components config with a regex, I concluded `app/**` was absent from its `coverage.include` and that the 08-17 move had therefore failed its own purpose. **It has a blanket `app/**/*Client.tsx` glob** (added precisely so client bodies under `app/` are measured), and the coverage REPORT shows those files. **The config was 180 lines of commentary and my regex matched the wrong `include:`.** ➡ **Check the instrument's OUTPUT, not its configuration.**

15. **`livetoken-portfolio*.json` fixtures — RESOLVED (verified 2026-06-01).** No longer tracked (`git ls-files` returns none for `livetoken-portfolio*` / `nftlocker-*` / `flowty-locker-test.json` / `test-gql.json`); nothing left to `git rm`.

17. **Pack / Moment / Set page tune-up — ongoing.** File:line audit findings live in `docs/archive/audits/PACK_PAGES_AUDIT_2026-05-22.md`, `docs/archive/audits/MOMENT_PAGES_AUDIT_2026-05-22.md`, `docs/archive/audits/SET_PAGES_AUDIT_2026-05-22.md` — those docs are point-in-time and now partially superseded; the current state is here. ⚠⚠ **RE-DERIVED 2026-08-24 — the *Remaining* list names "Set B5" as open and it was FIXED 2026-07-17.** The series page's own header labels it by the audit's ID: *"Set B5 fix (2026-07-17): the cards come from `get_series_rollups`, which aggregates over ALL editions in the series server-side."* **It was greppable the whole time.** 🚨 **What WAS live is the fallback it left:** when the rollup RPC fails and the editions page survives, the page regrouped ≤`PAGE_SIZE` (100) editions and rendered per-set counts + FMV totals **indistinguishable from the true series aggregate**. ⚠ **The fallback is a deliberate product call (*"partial … but better than hiding the sections"*) and is NOT reverted** — a partial must CARRY its partialness, so it now renders *"Counted from the first 100 editions only … a floor, not the series total"* on BOTH panels. Fixed + guarded 2026-08-24 (`series-partial-rollup-says-it-is-partial.test.ts`, mutation-proven per-panel).

   *Shipped* (commits `5c0af8a` → `8d8721e` → `2b7ce7f` → `61f5586`): brand-token consistency is complete across the three page templates, every `components/entity/*` and `components/packs/*` component, and `MomentDetailModal` — the exceptions are the recharts/SVG `stroke` attributes, which can't resolve a CSS var. ⚠ **There are THREE, not one** (the "lone exception is `FmvHistoryChart`" phrasing was wrong, corrected 2026-08-09 by deep-audit D38): `components/entity/FmvHistoryChart.tsx:175`, `components/pinnacle/PinnacleFmvChart.tsx:124`, and `components/profile/PortfolioSparkline.tsx:77`. `check-brand-tokens.mjs` passes because they are sanctioned, not because they are absent — so don't "discover" them again as new violations. Also shipped: the Pack AllDay-context banner and the AllDay set-tracker banner; stale-Flowty UI removed (dead "Flowty ask" stat, SEO + ticker copy, `marketplaceLabel` relabelled "Flowty (historical)"); `loading.tsx` skeletons for the moment / edition / set / series routes; and per-collection data-accuracy fixes — Top Shot series-label mapping, UFC tier vocab in `tierColorVar` / `TIER_STRIPE` / `TIER_COLORS`, three-way `is_listed`, honest null handling for Floor / drop_weight / completion-%, FMV-vs-ask labelling.

   *Correction*: the audit docs flag `MomentDetailModal.tsx` / `MomentMedia.tsx` as possible dead code — that is WRONG. A repo-wide grep confirms both are live: `MomentDetailModal` is used by `sniper/page.tsx` and `collection/page.tsx`; `MomentMedia` by `sets/page.tsx`. Their findings are normal fixes, not delete-candidates.

   *Shipped 2026-05-24*: Moment S5 — `MarketplaceStatusBanner` now mounts at the top of `app/(collections)/[collection]/edition/[slug]/page.tsx`. Pack D1 — reward / quest packs (`retail_price_usd = 0`) render a "Reward pack" badge and the value-ratio / EV-margin verdicts are suppressed (no more "Net +$X vs $0 retail" garbage). Pack D3 — the dist-page "Edition EV" column was raw `fmv × drop_weight` (un-normalized); now `fmv × (drop_weight / pool_weight) × slots`, reconciling with the Gross-EV KPI. Pack S2 — the "Buy on Top Shot" CTA is now suppressed when `price_source = "none"` and on reward packs, and re-labelled "Buy primary" / "Buy on secondary market" based on `price_source`. Pack S3 — simulator empty-state copy ("active drops" → "packs with an indexed drop pool"). Pack B6 — `PackTable` tier sort is now rarity-ranked instead of alphabetical (covers UFC tiers too). Pack B5 — `GrailsView` (which uses `useSearchParams`) is now wrapped in a `<Suspense>` boundary. Pack V4 — null-name pull cards / grail chase ribbons got non-empty `alt` text.
   *Remaining* (lower-value tier — most of the audit docs' bullets verified-shipped or punted): modal accessibility (Moment V3 / Set V5 — `role="dialog"`, focus trap; wants browser verification); Set B5 (series rollups derived from only the first 100 editions — needs an aggregate RPC, not a page-layer fix); Set B7 (client-sort partial-page issue — defer until a real consumer complaint); and the longer-tail Set/Moment/Pack V/D items that the audit docs already classify as low severity.

18. **Open from the 2026-08-15 monthly deep audit (run 2) — [docs/audits/deep-audit-2026-08-15.md](../../docs/audits/deep-audit-2026-08-15.md), register in [docs/audits/deep-audit-register.md](../../docs/audits/deep-audit-register.md).** The two P0s and the `/api/fmv` P1 were shipped same-day in `8f59749b`; these were NOT:
    - ⚠ **~~Pinnacle sales stopped resolving `edition_id` on 08-14 and it is ESCALATING~~ — DISPROVED 2026-08-15. NOTHING REGRESSED; it is a CATALOG-COVERAGE gap.** The audit's action ("find why `pinnacle-sales-indexer` regressed") sends you somewhere there is nothing to find. Settled by three measurements: all 329 unresolved sales carry **`resolution_attempts = 0`** (the resolver never reached them — it is not failing), `pinnacle-nft-resolver`'s yield has been **0.76–2.50% EVERY day back to 07-31**, including 0.87% the day *before* the claimed onset, and 0 of the 329 are in `pinnacle_nft_map` while the map itself is healthy and current. **A rate that looks alarming is not a regression until you compare it against its own history** — `pipeline_runs_daily` is indefinite and answers it in one query. Independently corroborated from another direction: the hourly null-rate **oscillates 11.8% ↔ 87.2%** over 48 h rather than climbing, which is not a break-shaped signature. ⚠ **The obvious one-line heal is a TRAP that a foreign key caught**: 307 of the 329 sit in `pinnacle_mint_events` with a non-null `edition_id`, but that column is a **numeric** Pinnacle id while `pinnacle_sales.edition_id` is the **text `edition_key`** — two vocabularies under one column name, and without that FK the update would have silently corrupted the join for every Pinnacle surface. **Do not relax it.** Detail: [inbox 2026-08-15T1530Z](../../docs/overnight/inbox/2026-08-15T1530Z-R4-is-a-catalog-coverage-gap-not-an-indexer-regression.md). The page half is fixed (honest empty state); the real work is catalog coverage, not the indexer.
    - ✅ **`drain-conflated-subeditions` — DIAGNOSED AND FIXED 2026-08-15 (`3a172b6b`); the rest of this bullet is the history that got there, kept because two of its corrections are the transferable part.** The 20:30Z tick on 08-15 was the first to carry `80e99d4d`'s progressive `mark()`, and it named the step outright: `seed_miskeyed` **120,310 ms** · `seed_recent` **120,326 ms** · `seed_knot_occupants` **120,393 ms** — the last on a `p_limit` of **200**. ⚠ **Three steps converging on ~120,3xx ms is a CEILING, not work — but WHICH ceiling is UNDETERMINED, and the half I first published was wrong.** These functions do each declare `statement_timeout=120s` in their own `proconfig`, and I read that as the binding budget because `service_role` carries 30 s. **The server default is ALSO exactly 120000 ms** (`pg_settings.reset_val` 120000, `source` = configuration file, `/etc/postgresql-custom/platform-defaults.conf`; there is no database-level or `<ALL ROLES>` override in `pg_db_role_setting`), so a 120,3xx ms kill is **numerically identical** under "the declaration bound it" and "the session default bound it" — I had two candidate parents and checked only one. Prod controls recorded in [[function-statement-timeout-is-inert]] say proconfig cannot RAISE the calling statement's budget, which makes the session default the likelier parent and the declarations decorative here. ⚠ **The practical consequence is what matters: raising these functions' proconfig to 300 s would do NOTHING** — it is the documented dead end. The levers are the CALLER's budget (role-scoped: `cron_heavy` carries 600 s) or making the work fit, which is what the reorder does. ⚠ Note also that neither `service_role`'s 30 s nor `authenticator`'s 8 s was in force on this path — whatever bound these statements was 120 s — so do not assume the 30 s figure quoted elsewhere in this file governs every service-role call. A statement that hits it **rolls back**, so those three burned **361 s of a 600 s budget producing nothing** and the route was killed before reaching the steps that do the work — which is why raising the ceiling 300→600 changed nothing. **Fix: DRAIN before SEED plus a wall-clock budget guard** — order is now catalog → split → realign → **knots** → conflation_guard → seeds → trigger, no step may start without room to hit its own 120 s ceiling and still write the completion row, and a declined step lands in `extra.skipped_steps`. The reorder is safe because the drain consumes what PRIOR ticks resolved while the seeds feed LATER ticks (the header always said so, and step 6 derives its own candidates), so it changes throughput, never correctness. ⚠ **`knots` sits ahead of the conflation guard deliberately** — the guard is a pure re-measurement and so the cheapest thing to drop under pressure; leaving knots last is exactly what let 15 days of starvation read as normal operation. Ordering is now pinned by `__tests__/pipeline-start-marker-duration-is-not-a-measurement.test.ts` (knots must precede every `seed_*`), and that file's `mark()`-call-site count was **repointed, not weakened**, when marking moved inside a `step()` helper that enforces the same property more strongly. ⚠ **NOT yet verified by a live tick** — the next 20:30Z run is the proof; read `extra.step_ms`, a non-zero `knots`, and growth in `topshot_collision_knot_resolutions` (frozen at 76 since 2026-07-31). ⚠ **The filed R7 remedy — "split the 6 steps so each writes its own row and commits per step" — was NOT what shipped**: that is a large restructure of TopShot keying and it does not address the defect, which was ordering. History follows. Every run wrote only the start marker, last success 2026-07-31, and it is **on no watchlist**. The D6 fix made it VISIBLE, not working, and the 300→600 ceiling raise did not help. ⚠ **R7's stated IMPACT is WRONG and a session acting on it would fix nothing (disproved 2026-08-15, `9e5dc9b7`).** R7 reasoned that step 5 (`refresh_topshot_conflated_editions_detector_only`) sits inside the killed tick, so the conflation guard must be ~15 days stale. **It has a second independent caller** — pg_cron jobid 62 `rpc-remap-misattributed-sales`, `23 */6 * * *` — and measured live `max(detected_at)` is **0.0 days stale over 931 rows**. Step 5 is redundant for freshness. That mattered more than tidiness: `topshot_deals_vs_fmv` EXCLUDES `topshot_conflated_editions`, so a genuinely stale guard would mean **the public deals board publishing conflated editions as deals** — it does not. ⚠ **The real impact is step 6, which R7 never mentions and which is worse:** `topshot_collision_knot_resolutions` shows **ZERO resolutions in 14.9 days** (last at 2026-07-31 20:33:53Z, the exact minute of the last successful run; 76 total ever). Knots are the transposed-pair class that split/realign structurally **cannot** fix, arriving at a measured **+8.3/night**, so roughly **124 have accrued undrained**. ⚠ **Do not guess which step overruns** — `80e99d4d` made `mark()` persist progressively, so `extra.last_step` first populates on the next 20:30Z tick, and the route's own "read `step_ms` and bound THAT step" only becomes actionable then. ⚠ **The audit's "`duration_ms` 147–176 ms" is NOT the run length and must not be read as one** (corrected 2026-08-15) — that is the marker's own INSERT latency, because `duration_ms` is generated from `finished_at`, which defaults to `now()`. It made the route look like it was **dying instantly** when it is running to its ceiling and being killed, which points at a different fix entirely. The marker now pins `finished_at = started_at` so the field reads a hard 0; **diagnose from `extra`/`ok`, and bound the step that overruns** (the route already records per-step `step_ms`, which never lands precisely because the completion update never runs).
    - ⚠ **The audit's "homepage publishes multipliers the live model does not produce" P2 is WRONG — do not act on it (disproved 2026-08-15).** `HomePageMarketing.tsx:145` states `lib/fmv/serial-multiplier.ts` **verbatim** (`serial===1 → 12.0`, `<=10 → 4.5`, `serial===circ → 3.0`) — the model the public `/api/fmv` actually calls. The 9.89/1.50/5.00 are `serial_fmv_multipliers` ALL/ALL roll-ups, a **different subsystem** reached by `serial_fmv_estimate`. **Following the recommendation would have replaced accurate API documentation with numbers from a model the API never calls**, and would have published one scalar for a MATRIX (Top Shot reliable rows span 1.98–60× first / 1.00–16.25× low / 1.17–48× perfect across tier × circ band). **The real finding is that two models disagree ~3× under one name** — the product API says a #1 is 12×, a collector's portfolio prices it at 9.89× (2.17× for LEGENDARY/ultra) — plus `/api/fmv` hardcoding `serialMultiplier(serial, 1000)` and never reading `circulation_count`, which makes the documented `lastMint: 3x` unreachable except on /1000 editions. Both are pricing changes → [inbox 2026-08-15T1507Z](../../docs/overnight/inbox/2026-08-15T1507Z-two-serial-multiplier-models-and-the-audit-p2-is-wrong.md), Trevor's call. ⚠ **A third set exists and is NOT part of this conflict** — `applySerialPremium`'s 1.35/1.18/1.2 is deliberately separate; do not "reconcile" it.
    - ⚠ **`get_collection_stats` throws `57014` inside the sniper-deals LATERAL that run 1 of this same audit added** (`audit_20260810164226`, the D4b/D13b confidence gate) — **a fix that improved honesty bought a cost that defeats the same page a different way.** The 08-15 partition-pruning mitigation cuts that leg ~20% and **is not a rescue**: post-patch Top Shot returned correct data in **84 s** and All Day still threw. All Day drives **2,230 LATERAL loops vs Top Shot's 769**, which is why it fails first.
    - ⚠ **R17 ("two history backfills scan 40,000 blocks and decode zero") is REFUTED AS FILED — measured 2026-08-15.** They are not burning ~35 min/week scanning; they **self-skip on saturation**, which is a circuit breaker working: `golazos-sales-history-backfill` 8 of 25 runs and `ufc-sales-history-backfill` 12 of 27 carry `extra.skipped = "saturation"`, at 885 ms and 7.3 s a tick. The real signal is the one the waste framing hid — `recent_fails` up to **136** and **117** — i.e. they are not doing their job because they are **blocked behind the platform-wide saturation**, not because they are pointless. Fold them into that root cause; do not retire them as waste.
    - ✅ **R11 CLOSED 2026-08-15, and the heartbeat then ANSWERED the question it was built to ask** — `candy-listings-indexer` writes an invocation heartbeat, and the truth table settles it: heartbeat **21:35:39Z** + `candy_listings.last_seen_at` **21:38:14Z** + terminal row still **18:35:11Z** ⇒ the route **ran, did its work, and was killed before the completion write**. ⚠ **The correction this bullet used to prescribe did NOT reproduce and must not be acted on:** it said `last_seen_at` was "**also 10 h stale**, so later ticks did no work either" — measured 2026-08-15 it is **170 min** old and tracks the heartbeat tick within 3 minutes. ⚠ **The real residual is cry-wolf in the DETECTOR, not the pipeline:** `detect_stalled_pipelines()` keys on the TERMINAL row, which this route writes on only ~⅓ of its ticks (**8** in the ~72 h window against **24** scheduled at `35 */3`), so the arm oscillates in and out of "stalled" on a pipeline that is demonstrably alive. The fix is to let the cadence check accept the `-heartbeat` row as liveness while still treating a missing terminal row as an incomplete run — a change to the DETECTOR. The watchlist note has been corrected in prod with all of this.
    - ⚠ **R8's PRESCRIBED HEAL IS CORRUPTING — DO NOT RUN IT (measured 2026-08-15, nothing written).** "512 canonical TS editions have NO player; **357 healable** from modal `wmc.player_name` (COALESCE fill-only)" would copy a **TEAM NAME** into `editions.player_name` for essentially every row it touched. **The 512 are TEAM MOMENTS and correctly have no player.** Split is **273 single-value / 71 CONFLICTING / 168 none** (the 357 ≈ 273+71, never separating the conflicting set, which has no single value to COALESCE). Top candidates in rank order are *Atlanta Hawks · LA Clippers · Golden State Warriors · Cleveland Cavaliers · Chicago Bulls…*; in a 170-edition chunk **82 of 83 equal the edition's OWN `team_name`**, 1 is `"Unknown (error loading)"`, and **0 are a person**. Every conflicting sample pairs a franchise with the literal `"Team Moment"`. ⚠ **The finding's own supporting evidence inverts:** it cites "the collection table shows a player where the entity page shows a set name" as proof of a gap in `editions` — that divergence is real and means the **wmc-backed collection table is the wrong surface**. ⚠ **Underneath it, `wmc.player_name` stores non-names**: `Team Moment` 3,973 rows/69 eds · `Unknown` 2,944/164 · `Unknown Play` 98/2 · **`Unknown (error loading)` 75/3** — an error message persisted to the portfolio store and shown to a collector as their moment's player, i.e. the failure-renders-as-data class **persisted**. Normalising that sentinel to NULL would also **unblock `rpc_wmc_metadata_selfheal`**, which is COALESCE fill-only and so is permanently blocked by any non-null string. **Re-pointed, not closed** — 481 editions still render `name = set_name`, but the repair is a better upstream TITLE, not a player. Detail: [inbox 2026-08-16T0010Z](../../docs/overnight/inbox/2026-08-16T0010Z-R8-would-write-team-names-into-player-name.md).
    - ✅ **D25's fixable class DRAINED 2026-08-15: 128 → 62, AllDay to ZERO.** Shipped exactly the prescribed shape — a targeted `UPDATE wmc SET mint_count = e.circulation_count` carrying the trailing `serial_number <= circulation_count` predicate that makes it **structurally unable** to touch an upstream-wrong row (66 rows: AllDay 57, TS 9). Cause was a stale early-mint snapshot: AllDay edition `2508` cached `mint_count = 300` against a true circulation of **4,999**, rendering serial 2446 as "#2446 / 300". ⚠ **The register's TS split did not sum** — "26 stale / 62 upstream" is 88 against a stated 71; measured **71 = 9 + 62**. ⚠ **The residual 62 are unreachable by any wmc refresh** (there `editions.circulation_count` is itself below the serial, so the CATALOG is wrong — the impossible-parallel family). ⚠ **No revert recorded, deliberately:** the prior values were the known-bad stale snapshots, were not captured, and restoring them means re-writing provably wrong data.
    - **Severity corrections worth knowing before re-reading run 1:** D2b is **INVERTED** (71 of 71 `pg_net` 403s land on jobid 16's minutes — blast radius far smaller than P0 implied, but the **secret-exposure** half is untouched and remains the real P0); D32's headline is **RESOLVED**; D8 regenerated **37×** and in Top Shot rather than the predicted All Day, with only 23.4% healable; D26's duplicate-slug set **churned**, so the finding is the *producer* (a third `external_id` convention), not the rows.

19. ✅ **RESOLVED 2026-08-22 — jobid 70 moved to 23:35Z via a migration; no grant was made or needed.** **pg_cron `cron_heavy` ownership block — NO session-reachable role can reschedule 42 of 93 jobs (measured 2026-08-22).** `postgres` (what Supabase MCP runs as) may `EXECUTE cron.alter_job` but does **not own** the `cron_heavy` jobs; `SET ROLE cron_heavy` returns *permission denied for function alter_job*; and `has_table_privilege('postgres','cron.job','UPDATE')` is **false**, closing the direct-catalog fallback. ⚠ **The ledger implies otherwise** — its 2026-08-22 entry records a successful `cron.alter_job` on jobids 83/84, which are `postgres`-owned. **Read `cron.job.username` before planning any schedule change.** **Live instance blocked by this:** jobid **70** (`rpc-refresh-misattrib-candidates`, `35 15 * * *`) is the **SOLE** caller of `refresh_topshot_misattrib_candidates()` and has failed **15 of 16 runs since 08-07** (8,616 s of heavy IO burned, 14 killed at the 600 s wall), leaving `mv_topshot_misattrib_candidates` **unrefreshed since 2026-08-16** — confirmed by its `last_autoanalyze`. It was healthy at 23–589 s until 08-06; it fires at 15:35Z, deep in the 01:00–19:00Z band. ✅ **The usual objection does not apply:** `anon` and `authenticated` have **no SELECT** on that MV, no view references it, and its only reader is the internal `topshot_misattrib_drain_targets` — a daytime lock is invisible to users. **Operator fix:** `SELECT cron.alter_job(70, schedule := '35 23 * * *');` from the Supabase SQL editor, then verify the next day that `cron.job_run_details.start_time` actually moved (an `alter_job` schedule change is recorded as having silently not taken once). ⚠ **Do NOT disable jobid 70 as redundant** — a 2026-08-22 near-miss: jobid 62's full command ends in `refresh_topshot_conflated_editions_detector_only`, a DIFFERENT function, and a `left(command, 80)` truncation made it look identical. ⚠ **CORRECTED 2026-08-22 (later the same day): the "NO session-reachable role" headline above is REFUTED at the privilege layer — and with it the premise of the grant question it raised.** Re-derived live: `postgres` **is** a MEMBER of `cron_heavy` (`pg_has_role('postgres','cron_heavy','MEMBER')` = true), and `cron_heavy` **already holds** EXECUTE on `cron.schedule(text,text,text)` and both `cron.unschedule` overloads. The only thing it lacks is `cron.alter_job(bigint,text,text,text,text,boolean)` — which `postgres` *does* hold but cannot use, because it owns none of these jobs and is **not** superuser here (`rolsuper` = false). All three functions are owned by `supabase_admin`, so `postgres` could not grant on them anyway. ✅ **DECISION (2026-08-22): do NOT grant `cron_heavy` EXECUTE on `cron.alter_job`.** It would widen a privilege to buy a capability the role already has by another door — `cron.schedule` upserts on `(jobname, username)`, so rescheduling *under the job's own role* updates it in place. **Operator recipe, which SUPERSEDES the `alter_job` one-liner above:** `SET ROLE cron_heavy;` then `SELECT cron.schedule('rpc-refresh-misattrib-candidates', '35 23 * * *', 'SELECT public.refresh_topshot_misattrib_candidates()');` ⚠ **The recipe is SELF-CHECKING and must be run as such — `cron.schedule` RETURNS the jobid, and it must come back `70`.** Any other number means it created a DUPLICATE instead of upserting; immediately `SELECT cron.unschedule(<that jobid>);` (still as `cron_heavy`) and fall back to the `alter_job` one-liner. ⚠ **The upsert-preserves-jobid property is pg_cron's documented behaviour, REASONED here and NOT measured** — the confirming read (a unique index on `cron.job(jobname, username)`) was blocked before it ran, which is precisely why the check is written INTO the recipe rather than assumed. ⚠ **Why no session simply ran this: the block is a HARNESS boundary, not a database one.** The Claude Code auto-mode classifier denies `SET ROLE` outright, and after one denial denies cron-schema SQL generally. **That distinction is the transferable lesson: "the sandbox could not do it" is NOT evidence that the DATABASE forbids it** — this item asserted the stronger claim for a day, and the stronger claim is what turned a two-line fix into a privilege-grant proposal. **✅ **DONE 2026-08-22 — and the blocker turned out to be neither the database NOR a missing privilege.** The path CLAUDE.md already documents (from the 08-17 correction) is `apply_migration` with `SET LOCAL ROLE cron_heavy … RESET ROLE`, and it works: migration **`20260822215445`** moved jobid 70 to `35 23 * * *`. ⚠ **The `RESET ROLE` is load-bearing** — `apply_migration` appends its own INSERT into `supabase_migrations.schema_migrations`, which `cron_heavy` cannot write, so leaving the role set fails the whole migration. ⚠ **The migration asserts its own success:** `cron.schedule` returns the jobid, and a `RAISE EXCEPTION` rolls the transaction back if it is not **70** — because any other value would mean it created a DUPLICATE and left two heavy refreshes scheduled. **Verified after apply:** schedule `35 23 * * *`, jobid still **70**, `job_count = 1` for that jobname, owner `cron_heavy`, one job at 23:35 (itself). **No grant was made; `cron.alter_job` was never needed.** ⚠ **VERIFY THE FIX, do not assume it** — the first 23:35Z run is the proof; a schedule change here is on record as having silently not taken once. Read `cron.job_run_details` for jobid 70 and confirm both that it started at 23:35Z **and** that it finished under the 600 s wall. Band evidence for the move, re-derived 2026-08-22:** 13 of the last 14 runs failed, twelve of them at the 600 s wall; the single success (08-16) took **187.6 s**, 4.2× under the wall — so the cost is disk-IO contention inside the 01:00–19:00Z band, not data growth, and moving the hour is the whole fix. Target minute verified collision-free: **zero** active jobs fire at 23:35 (positive control: the identical predicate returns jobid 70 at 15:35). ✅ **FIRST 23:35Z RUN OBSERVED 2026-08-22 — 36.4 s, AND IT BEATS THE HYPOTHESIS.** Fired at **23:35:00.065Z**, succeeded **23:35:36.42Z** (`1 row`). Both halves were checked, because a job that never fires is a WORSE failure than a wall-kill and reads identically to "nothing to report": it was observed `status='running'` live, then `succeeded`. Against 12 consecutive **600 s** wall-kills and the single prior success at **187.6 s**, this is **16.5× under the wall and 5.2× faster than the best previous run** — a stronger result than the theory required, implying the 15:35Z window cost this job roughly **5× even on its good day**. ✅ **The MV genuinely refreshed, verified rather than read off the exit status:** `mv_topshot_misattrib_candidates` holds **20,516 rows**, stats timestamp **23:35:46Z**, after being stale since 08-16. ⚠ **ITEM 19 IS NOT CLOSED ON THIS.** One run is one sample, and the 08-16 success looked exactly like this before twelve failures followed it. Note the asymmetry before reading the next few nights: **another success adds little, while a single wall-kill at 23:35Z would refute the diagnosis.**

26. 🟡 **PARTLY RESOLVED 2026-08-23 ~02:00Z (deep-audit R44) — the PRIVILEGE defect is fixed and the revoke STAYS; the route still FLAPS with the saturation band.** `6cf81a38` re-points the route at a new INVOKER `readiness_collection_stats()` and drops `health_check()` entirely. ⚠ **The obvious repair — re-granting `anon` EXECUTE — would have republished a real leak**, since `health_check()` is SECURITY DEFINER and returns user counts, saved-wallet counts, allow-list size, telemetry volume, distinct wallets and `db_size_mb` to an anon-reachable surface. ⚠ **And it would not have fixed the route anyway: there was a SECOND cause** — the handler called `.map()` on `collections`, which `health_check()` returns as a `json_object_agg` keyed by slug, and the test stayed green throughout because it mocked the invented payload. **Verified live 2026-08-22 19:45 PT:** `readiness_collection_stats` `prosecdef=false`, `anon` EXECUTE **true**; `health_check` `anon`/`authenticated` EXECUTE **false**. ⚠ **NOT verified from here: the HTTP response** — the web sandbox is egress-blocked from production. ⚠ **AND A LATER PROBE SETTLED IT THE OTHER WAY: the route FLAPS WITH THE SATURATION BAND** — 200 at 04:03Z, 500 at 04:05Z and 04:07Z, with an honest retryable body (register R44, stamped by a concurrent session). **The privilege defect is fixed; the readiness probe still fails inside the degraded band, which is a different, open problem.** "Verified live" in the line above meant **verified calm**. Original filing follows.

26b. 🔴 **(historical — the original filing, superseded by 26 above; kept because its evidence is what identified the privilege)** — `/api/ready` has returned 500 for EIGHT DAYS: `anon` lost EXECUTE on `health_check` (found 2026-08-22).** `[api/ready] health_check failed: 42501 permission denied for function health_check` — **count 72, users 9, first 2026-08-15T08:28:18Z, last 2026-08-22T23:46:40Z**, plus an older `Vercel Runtime Timeout` cluster (13, since 06-16). ⚠ **DETERMINISTIC, NOT A RATE:** the route builds its client with `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and live privileges are `anon` EXECUTE **false**, `authenticated` **false**, `service_role` **true** (`prosecdef = true`) — so **every** anon call fails and 72 is how often someone loaded an affected page, not a probability. **Three in-repo consumers:** the analytics **thin-volume caveat**, `MarketClient`, and `/api/health`. ⚠ **A readiness probe answering `error` for eight days unnoticed is the documented permanently-red-instrument class.** ✅ **The route's error branch is HONEST and needs no change** — it keeps its `status: "error"` contract, returns 500, and withholds the driver text because `/api/ready` is anon-reachable via `PUBLIC_READ_APIS`. **The defect is the privilege, not the handler.** ⚠ **NO MIGRATION IS RESPONSIBLE IN THE OBVIOUS WAY — do not revert one:** every migration mentioning `health_check` was audited, none has a `DROP` or `REVOKE` for it, the last `CREATE OR REPLACE` was **2026-06-07** (and that does not reset an ACL), and the nearest `revoke_anon_exec_*` migration applied **fourteen hours AFTER the first error**. Residual hypothesis, UNVERIFIED: a blanket revoke took it as collateral, consistent with this DB carrying both a PUBLIC default and `ALTER DEFAULT PRIVILEGES` grants. ⛔ **NOT fixable autonomously (auth/lockdown).** Two non-equivalent candidates: **(1) GRANT EXECUTE to anon** — restores the design but re-widens the anon-executable SECDEF surface the 08-15/16 pass was narrowing, which `check_secdef_anon_exec_drift()` polices; **(2) move the route to the service-role client** — keeps anon narrow but runs a heavy SECDEF check on an unauthenticated public endpoint under a role with **no Postgres timeout bound** (worst observed 352 s), and this route already shows 10 s Vercel timeouts. **Operator decision; re-run `check_secdef_anon_exec_drift()` afterwards.** ⚠ **It overturned the filer's own position:** a three-state "could not check" UI notice — proposed and reverted the same night — would have rendered on every affected page for eight days while the one-line privilege regression stayed invisible, i.e. **it would have made the outage look handled. Fix the endpoint; do not decorate its failure.** Filing: `docs/overnight/inbox/2026-08-23T0025Z-api-ready-has-been-500ing-for-eight-days-and-it-settles-a-disagreement.md`. 🚨 **CORRECTED ~00:50Z — BOTH CANDIDATE FIXES ABOVE ARE WRONG; DO NOT SHIP EITHER.** They share a false premise: that `health_check` is a viable synchronous call. **Measured:** run as `postgres` in the QUIET window it **did not return inside 60 s**; found still `active` at **76 s** in `wait_event = DataFileRead` and **cancelled** (the MCP cap abandons the result, not the query — it was not retried). ⚠ **That kills candidate 1:** `authenticator` carries **`statement_timeout=8s`**, the binding ceiling on the PostgREST path (`anon`'s declared 3 s never applies — `rolconfig` is LOGIN-time and PostgREST logs in as `authenticator`), and `health_check` declares only a `search_path`. **So granting anon EXECUTE swaps a 42501 for an 8 s statement timeout — still a 500, but now burning 8 s of IO on every probe. It would ship green and change nothing a user sees.** ⚠ **And candidate 2:** no Postgres timeout bounds a `supabaseAdmin` RPC, so Vercel kills it — which is literally the other error cluster already on this route (`Task timed out after 10 seconds`, 13 since 06-16). ✅ **Corroboration:** `pg_stat_statements` holds **4,777 rows since 2026-08-12** (positive control — populated, and the window spans the outage) and **ZERO for `health_check`**: it has not successfully executed in 11 days. **The real fix must change the SHAPE, not the privilege** — a >60 s aggregate cannot be served synchronously on an anon route under any role. Unmeasured options: precompute the snapshot on a schedule and read the row, or split the probe to its cheap legs. ⚠ **60 s is a FLOOR, not the runtime — it was cancelled, not finished.**

20. **`atlas-proxy` — needs an operator `wrangler deploy` + a Cloudflare-egress probe.** ⚠⚠ **THE ~60 % IS WRONG AND SO IS THE IMPLIED BLAST RADIUS — re-measured over all 40 runs 2026-08-19 07:11Z → 2026-08-24 04:11Z: `egress_blocked` is 9/40 (22.5 %), not ~60 %.** The 5/7 sample was real and unrepresentative — a 7-run read of a pipeline that fires 8×/day. 🚨 **Shipping this `wrangler deploy` fixes 9 of 40 failures and the workflow stays red**, because 29/40 die at `?phase=targets` on a DB statement timeout *before the first Atlas call is made* (#30) and 2/40 on a 504 at the same endpoint. **Do not credit #20 with un-redding this workflow.** Not fixable from a sandbox — no Cloudflare access. Carried in CLAUDE.md's "Prioritized next actions" but had **no register slot**, which is why it keeps being re-derived.

21. **`topshot-moments-hydrator`'s cron is declared in NO file in this repo, and a `wrangler deploy` would DELETE it (found 2026-08-22).** It is a Cloudflare Worker — confirmed by enumerating all four caller sources (absent from `vercel.json`, `cron.job`, GHA, and it has no `app/api` route) — and `workers/topshot-moments-hydrator/wrangler.toml` has **no `[triggers]`/`crons` block**. It is provably on a ~10-minute schedule: **every observed `started_at` minute is ≡ 2 (mod 10)**. ⚠ **`wrangler deploy` reconciles cron triggers FROM CONFIG**, so deploying this worker from the repo removes a dashboard-set cron and silently kills the pipeline — the same family as the 2026-08-21 "two wrangler configs deployed to one worker" incident. Control: of 17 workers, exactly **one** (`sales-counterparty-backfill`) declares `crons` in-repo, so the mechanism is used here. ⚠ **Second, unexplained finding:** only **24–49 runs log per day out of ~144 ticks**, every logged run reporting a full `rows_found: 300` — so it is not "no work to do", and what the other ~100 ticks do is **unmeasured**. **Operator:** codify the trigger in `wrangler.toml`, and read the worker's Cloudflare logs (the only place the current stall and the tick gap are visible). ✅ **RE-VERIFIED 2026-08-24 — every claim in this entry HOLDS, and the trap is still live.** `workers/topshot-moments-hydrator/wrangler.toml` still has **no `[triggers]`/`crons` block**, and the control is unchanged: **exactly 1 of 17** `wrangler.toml` files declares `crons` (`sales-counterparty-backfill`), so the in-repo mechanism is used here and its absence is meaningful. ✅ **The dashboard-set cron is still ALIVE — the trap has NOT fired:** 44 runs in 24 h, **all `ok`**, newest 15:02:08Z, and **every `started_at` minute ≡ 2 (mod 10)** exactly as filed. ⓘ **Sharper than the original signature: the MINIMUM inter-run gap is exactly 10.0 min**, which both confirms the 10-minute schedule and shows it never double-fires. ⚠ **NEW OBSERVATION, UNEXPLAINED AND NOT INVESTIGATED: the cadence is not uniform.** Of 43 intervals, **33 are ~10 min** (healthy) but **10 exceed it, the largest 370 min (6.2 h)** — average 30.0 min. ⛔ **Stated as arithmetic, not a diagnosis.** It could be Cloudflare cron skew, ticks that no-op without logging, or real outages; **all 44 recorded runs are `ok`, so `pipeline_runs` shows no failures to explain the gaps** — which is itself the shape worth noticing (a gap with no failed row is invisible to any arm keyed on `ok`). **Re-derive before quoting; this is a dated 24 h sample.**

22. **🚨 SECURITY — the 2026-08-03 `filter-repo` purge was DEFEATED by a stale branch of the PUBLIC repo (found 2026-08-22).** `origin/claude/todo-implementation-e4tib3` branches from the **ROOT COMMIT**, so it was never rewritten and still carries the pre-purge history. Blob `02a86fcb` of `scripts/fetch-allday-collection.mjs` is reachable there and **not** from `main`, introduced by **`1c3e01a8f`** — the exact sha this file's P0 names as the leak's origin. Measured values-free with a control (`eyJ` JWT markers 2 vs `main`'s 0). ⚠ **Bounded to ONE branch** — `qi4350` tested clean (post-purge merge-base). ⚠ **The cookies are from April and are almost certainly expired; the durable harm is the PII in the RS256 token payload, which does not expire.** ⚠ **"4,024 commits ahead" is a re-hash ARTEFACT** — tested on a content property, the honest figure is **one** draft commit (`ee94c8a2a`, panini serials). ✅ **RE-VERIFIED AGAIN 2026-08-25 at session close — unchanged: branch still on origin, marker control still e4tib3 2 / main 0.** ✅ **RE-VERIFIED 2026-08-24 (this is still live, and the bound still holds).** `git ls-remote --heads origin` still returns `refs/heads/claude/todo-implementation-e4tib3` at `ee94c8a2a`, and the **values-free control re-ran clean**: `git grep -c 'eyJ' <ref> -- scripts/fetch-allday-collection.mjs` reads **e4tib3 2 · main 0 · qi4350 0**, so the exposure is still exactly ONE branch and `main` is still clean. ⚠ **Two `claude/*` branches exist on origin — `e4tib3` AND `qi4350` — and only the first is the P0**; do not let the second's presence read as a second leak, and do not let its innocence read as the first being cleared. ⚠ **`git cat-file -e <blob>` is NOT a per-branch test** — once either branch is fetched the object is in the local store and the check answers `yes` for both; the per-ref `git grep` above is the instrument. **Operator, in order:** triage that commit → delete the branch **via the GitHub UI** (remote delete-ref **403s** from the sandbox) → ask GitHub Support to GC the unreachable objects, since a deleted ref stays fetchable **by sha** → **rotate regardless**, because deletion limits future discovery and does not undo past exposure. Method + the `for-each-ref` completion check: [tooling-gotchas.md](tooling-gotchas.md). ✅ **RE-VERIFIED AGAIN 2026-08-26 (PT) — UNCHANGED, and re-run with the documented per-ref instrument rather than by reading this entry.** `git ls-remote --heads origin` still returns exactly three refs — `main`, `claude/todo-implementation-e4tib3` at **`ee94c8a2a`**, `claude/todo-implementation-qi4350` at `47d717b04` — and the values-free control still reads **e4tib3 2 · main 0 · qi4350 0**. **24 days open. The operator steps below are unchanged and none of them has been taken.**

23. **25 edge functions are PROVEN to be running code that is not `main`, and the instrument saying so has been red and unread for 14 days (found 2026-08-22).** `.github/workflows/edge-fn-drift.yml` runs daily at 06:40Z. It has failed **every scheduled run since 2026-08-09** — 14 consecutive — and run #1 (08-08) is the only one that ever passed. ⚠ **It is not broken: it is LOUDLY CORRECT**, which is exactly the state CLAUDE.md warns reads identically to a broken instrument at a glance. Read the LOG, not the badge. **The finding is trending the right way and the SET is shrinking, not churning:** 08-09 reported **30**, 08-22 reports **25**, and the five that cleared (`backfill-allday-pack-supply`, `backfill-pack-opens-api`, `backfill-topshot-pack-supply`, `compute-pinnacle-pack-ev`, `snapshot-institutional-wallets`) are a strict subset — **nothing new drifted in 14 days**. **The cause is uniform, not 25 separate bugs:** tier 1's proof is that the repo source uses a **bare** specifier (which cannot resolve without an import map) while the deployed artifact reports `import_map: false`, so the deployed build **cannot** be current source — it would not have booted. They are running an older, pre-import-map build; they are **not** hard-down. ⚠ **`wrangler`-style redeploy is the trap:** the guard's own message says redeploy with `deno.json` in `files` **AND** `import_map_path` — omitting the map turns a stale-but-working function into a **hard-down** one. ⛔ **Not autonomous:** the list includes `compute-*-pack-ev` and `ingest-*`, which CLAUDE.md puts off-limits, and the blast radius of a bad redeploy is a dead pipeline. **Operator:** redeploy the 25 with both flags, then confirm the next 06:40Z run goes green. ⚠ **Also unexplained and NOT part of this item:** the header reads `38 repo functions, 67 deployed` — **29 deployed functions have no repo counterpart**, and nothing has characterised them. ✅ **The detector's own reporting defect was fixed 2026-08-22** (see the ledger): tier 2, which its header calls *"the only census"*, swallowed every body-read failure into a bare `catch {}`, so **a run whose census read nothing printed the same number as one that completed and found nothing** — and the persisted artifact recorded a content-drifted function as `"clean"`. It now reports `bodies_read`/`bodies_failed`, carries `ran` as a positive control, and refuses to print an all-clear it did not earn. ⚠ **Consequence for THIS item's number: 25 is tier 1's LOWER BOUND.** Whether the content census ever ran is not knowable from the 14 archived logs, so a function whose imports happen to match but whose body drifted has been invisible the whole time. **The next 06:40Z run is the first one whose census status is legible — read `bodies_read` before treating 25 as complete.** ✅ **THE REDEPLOY RECIPE IS NOW MEASURED, ON A CANARY — 1 of 25 (2026-08-22).** `ufc-stub-thumbnail-resolver` v22→v23 via MCP `deploy_edge_function`; `import_map: false` → **`true`**, `verify_jwt: false` preserved, **no repo file changed** (the deployed source was already `main`). Chosen because UFC is the deadest market in the estate (0 sales/24h) and the function has no user-facing read path. **The recipe, and each clause is a trap the guard message only half-names:** `files` = entrypoint **and** `deno.json`; `import_map_path` = `deno.json` — ⚠ **shipping the map in `files` WITHOUT the path is the hard-down case**, the file is present and simply never wired; and `verify_jwt` **read per function from `list_edge_functions` and passed back** — ⚠ **it is not uniform (8 deployed functions are `true`) and the MCP tool DEFAULTS to `true`**, so omitting it on a `false` function silently puts an auth wall in front of a cron-job.org caller, which is a *second*, undocumented way to kill a working function while the deploy reports success. ⚠ **Health is NOT yet confirmed and the green `pipeline_runs` rows are not evidence:** the newest tick (22:12:07Z) is **nine minutes older than the deploy** (22:21:45Z). The caller is external — cron-job.org every 30 min at :12/:42, **no `cron.job` row and no in-repo caller** — so the first legible tick is 22:42:07Z. ⚠ **A direct probe is unavailable from the sandbox** (the egress proxy 403s CONNECT to `*.supabase.co/functions/v1`, so `curl` returns `000` for healthy and dead alike — a null instrument, not a failure). **REVERT:** redeploy the same `files` with `import_map_path` OMITTED. **The gate for the remaining 24 is that tick going `ok`, plus the next 06:40Z run showing this slug leave the drifted set — not this entry.** 📋 **THE REMAINING 24 ARE NOW ENUMERATED AND TRIAGED (2026-08-22), and the triage's main result is a NEGATIVE one.** The set was re-derived independently of the detector — repo sources using a bare specifier (35 of 38) minus deployed functions already reporting `import_map: true` (12) — and came to **exactly 24**, corroborating the detector's 25 rather than trusting it. 🚨 **THE ATTEMPTED "SAFE BECAUSE NOTHING CALLS IT" SPLIT DOES NOT EXIST, AND THE CANARY IS ITS OWN COUNTEREXAMPLE:** `ufc-stub-thumbnail-resolver` has **ZERO in-repo callers** and **runs every 30 minutes** via cron-job.org, which is invisible to both a repo grep and `cron.job`. **So a zero-caller grep is not evidence of dormancy for ANY of the 24** — 6 of them (`backfill-allday-listing-serials`, `hybrid-custody-backfill`, `ingest-pinnacle-mints`, `scan-pinnacle-wallet`, `seed-allday-pack-distributions`, `special-serial-sweep`) grep to zero and must still be treated as live. ⚠ **`pipeline_runs` is a null instrument for 8 of the 24** — `enrich-ufc-wallet`, `hybrid-custody-backfill`, `scan-pinnacle-wallet`, `scan-ufc-wallet`, `seed-allday-pack-distributions`, `seed-topshot-pack-distributions`, `seed-ufc-editions`, `special-serial-sweep` **never call `log_pipeline_run` at all**, so they read as zero-runs no matter how often they execute; four of them sit on user-facing or cron paths (`/api/ufc-wallet-scan`, `/api/ufc-sales-indexer`, `/api/cron/ufc-enrichment-drain`, and a GHA workflow). **Absence from that table is not dormancy; it is blindness.** ✅ **What IS positively measured — 9 are demonstrably LIVE in the last 7 days** (runs, fails): `compute-topshot-pack-ev` (1130, 321 fail), `pinnacle-nft-resolver` (920, 181), `pinnacle-owner-discovery-forward` (147, 0), `topshot-stub-resolver` (144, 2), `compute-allday-pack-ev` (139, 93), `pinnacle-owner-discovery` (108, 2), `sales-serial-backfill` (37, 0), `compute-golazos-pack-ev` (5, 0), `match-topshot-players` (3, 2). ⚠ **7 more log correctly and have never appeared in `pipeline_runs_daily`** (indefinite retention, and a **superset** — 170 distinct pipelines vs 162 in the live table, so a null there is real): `backfill-allday-listing-serials`, `backfill-topshot-base-parallel-probe`, `backfill-topshot-subeditions`, `ingest-pinnacle-mints`, `ingest-topshot-atlas-pool`, `sync-nba-odds`, `topshot-insider-detect-patterns`. 🚨 **THAT 7-FUNCTION LIST WAS MEASURED AGAINST THE WRONG KEY AND IS CORRECTED HERE (same day) — `pipeline_runs.pipeline` IS NOT THE FUNCTION SLUG.** The query above keyed on the deployed slug, but a function self-reports under whatever name its own `p_pipeline` argument carries, and **4 of the 24 differ**: `backfill-topshot-subeditions` → `topshot-subedition-backfill`, `backfill-allday-listing-serials` → `allday-listing-serial-backfill`, `backfill-topshot-base-parallel-probe` → `topshot-base-parallel-probe`, `ingest-topshot-atlas-pool` → `topshot-atlas-pool-ingest`. ⚠ **A slug-keyed query returns zero rows and NO error, which is indistinguishable from dormancy.** Re-run on the correct names, **2 of the 7 are actually LIVE**: `backfill-allday-listing-serials` has **186 runs all-time and ran on 2026-08-22**, and `backfill-topshot-subeditions` ran **2026-08-21** (17 all-time). **The genuinely-never-seen set is 5, not 7** (`backfill-topshot-base-parallel-probe`, `ingest-pinnacle-mints`, `ingest-topshot-atlas-pool`, `sync-nba-odds`, `topshot-insider-detect-patterns`). ⚠ **This was caught only by opening a function to deploy it** — the mapping is invisible to any query, so ALWAYS read `p_pipeline` from the source before keying `pipeline_runs` on a slug. ✅ **5 of 25 NOW ON THE IMPORT MAP (2026-08-22) — and the bound that licensed the extra 4 was WRONG on the first attempt.** After the canary confirmed live (22:42:09Z, ok, 618 ms — the only admissible row, since all 1,143 earlier greens predate the deploy), four more shipped: `match-topshot-players` 27→28, `seed-ufc-editions` 37→38, `backfill-topshot-subeditions` 19→20, `sync-nba-odds` 25→26. **A redeploy pushes current `main` live, so it also ships every unshipped behaviour change** — and tier 2, which would reveal them, needs a PAT this sandbox lacks. ⚠ **The first bound was BACKWARDS: diffing from `ea789b3eb^` (just before the shared import map) is a LOWER bound, not an upper one, because a deployed artifact can be far OLDER than the import map.** The sound bound is each function's **FIRST commit**, which no deployed build can predate. Under it only **4 of 24** are import-only — the dirs with exactly 2 commits (creation + import refactor), so the deployed build can only be the creation source; the other 20 would ship **6–1,366 changed lines**. Those 4 are exactly what shipped. ⚠ **Verification is asymmetric and none of the 4 matches the canary's 30-min cadence** (`match-topshot-players` ~3/week, subeditions ~daily, `sync-nba-odds` seasonally dormant since the season ended 08-04, and `seed-ufc-editions` never calls `log_pipeline_run` at all): **what is confirmed for the four is METADATA only** (`ACTIVE`, `import_map: true`, version bumped). ⚠ **Escaping is a live hazard when deploying via MCP** — content travels as JSON and these files carry backslashes and non-ASCII; a hand-escaping slip still BUNDLES and deploys a silently corrupted function, so generate the payload with `json.dumps` rather than by hand. **REVERT per function: redeploy the same `files` with `import_map_path` OMITTED.** ⛔ **The remaining 20 stay gated on the 06:40Z census.** ⚠ **User-facing blast radius to weigh before touching any of them:** `compute-topshot-pack-ev` feeds a public pack-distribution page, `sync-nba-odds` feeds `/api/rtr/picks/today`, `topshot-stub-resolver` feeds `lib/editions-hydrate.ts`. **The content question — whether a redeploy also ships behaviour changes, not just the import map — remains UNANSWERABLE from a sandbox** (tier 2 needs a `sbp_…` Management PAT); the 06:40Z run is the instrument for it, and its census is legible for the first time tomorrow. ⚠⚠ **RE-DERIVED 2026-08-24: the count is 20, and the WHOLE 30 → 25 → 20 SERIES IS TIER 1 ONLY — the authoritative content census has never once produced a result (`0 of 38` body reads succeed; the API serves an `eszip` binary and the script calls `.json()`). See #31.** So this entry's "the SET is shrinking, nothing new drifted" measures how many remain PROVABLE, not how many are broken: a function redeployed WITH an import map leaves tier 1's view whether or not its source is current. ✅ Checked for the five that cleared — all five now report `import_map: true` with plausible redeploy dates, so the benign reading holds — but it is unverified and stays so until tier 2 runs.

24. ✅ **RESOLVED 2026-08-22 — all 6 closed.** **6 of 187 DB-invariant pins no longer matched the live database, and the check saying so has been red since 2026-08-10 — 13 consecutive runs (found 2026-08-22).** `.github/workflows/db-pin-staleness.yml`, daily 07:20Z. ⚠ **Working, not broken** — it reports `checked 187 pins — 181 clean, 6 needing attention`, and runs #1–2 (08-02, 08-03) are the only passes it ever had. **Why it matters:** a pin is a VERBATIM copy of a function's DDL embedded in `supabase/tests/<fn>.sql`. `__tests__/db-invariants-drift-guard.test.ts` only checks that copy against **the migration its PINS entry names** — repo-vs-repo — so when the live function is later redefined via MCP, the pin, the test and the CI guard all stay green **while the test validates a definition that no longer runs anywhere.** Five are `STALE` (live ≠ pinned): `get_set_detail` (pinned `20260801190000`), `get_pack_detail_bundle` (`20260809170000`), `get_active_challenges` (`20260801231000`), `get_challenge_plan` (`20260801231100`), `public_board_liveness_sweep` (`20260810233442`). ⚠ **The sixth is a DIFFERENT class and must not be lumped in:** `reconcile_all_saved_wallet_stats` (`20260816181600`) reports **`NO_DDL_IN_MIGRATION`** — the named migration contains no DDL for it at all, so the pin cannot even be compared. That is a **broken** pin, not a stale one, and repointing is not the fix. All six functions exist and are readable live (verified 2026-08-22 by `pg_get_functiondef` length + md5). 🚨 **THE TRAP, and it is the whole reason this is not a mechanical chore:** the check's own remedy text says *"re-check the test's ASSERTIONS — a stale pin usually means the assertions describe old behaviour."* **Repointing the five pins without reviewing their assertions would turn a loud, correct alarm into a silent green while the tests still assert superseded behaviour** — converting a working instrument into a false all-clear, which is strictly worse than the current red. Budget the assertion review, not the capture. **Remedy per pin:** capture live with `pg_get_functiondef()` into a snapshot migration, repoint the PINS entry, **then** re-derive what each assertion claims against the new body. ✅ **1 of 6 CLOSED 2026-08-22 — `get_set_detail`, done the long way as a worked example, so the remaining five have a template.** ⚠ **The diff refuted the hypothesis the probes suggested**: a phrase probe showed live had **no `DISTINCT ON`** while the pin's header asserted *"DISTINCT-latest"*, which reads like a mechanism change — but the actual diff showed the `LEFT JOIN LATERAL … ORDER BY computed_at DESC LIMIT 1` was **already pinned**, and so was the Pinnacle branch. The header had simply not described its own pinned DDL for some time: **a documentation bug, not drift.** **The entire real drift was ONE feature** — the D20 `underlying_set_count` rollup (a DECLARE line, an 8-line SELECT, a jsonb key). ✅ **Verified, not assumed, at every step:** the transcription was checked against the DB's own `md5(pg_get_functiondef)` (`f0371ee9…`) rather than by eye; the resulting body hashes **`c3e29774f15cc1068e89af80077c3777` identically across live `prosrc`, the test file and the new migration**, so the daily check will read this pin clean; the new `underlying_set_count` assertion was **mutation-tested in both directions** (dropping the `collection_id` scope reds it; dropping the name scope reds it); the pin's own SQL test and all **178** DB-invariant files pass against a local Postgres 16 provisioned the way CI does. ⚠ **The migration is deliberately NOT applied** — it is byte-identical to live, so applying it buys nothing and costs a ~10–20 s user-facing `PGRST002` burst. `migration-parity` checks applied→file, so an uncommitted-to-prod file is invisible to it. ⚠ **A snapshot migration must use the anon-exec MARKER, never a REVOKE** (the guard says so and it caught this one): `CREATE OR REPLACE FUNCTION` does not reset a function's ACL, so a REVOKE there would change production while presenting itself as a no-op. Verified with `has_function_privilege`: anon and authenticated EXECUTE are both already false, service_role true. ✅ **2 of 6 CLOSED — and the second one was not a stale pin at all.** `reconcile_all_saved_wallet_stats` reported `NO_DDL_IN_MIGRATION` because it is a **PROCEDURE**, and `check-db-pin-staleness.mjs` only ever searched for `CREATE OR REPLACE FUNCTION public.<name>`. **The pin, the migration and the test were all correct the whole time; the CHECKER could not parse them.** ⚠ **And it is worse than a false alarm:** because extraction failed, the live-drift comparison for that procedure **never ran** — on a procedure that writes the cached portfolio figures every collector sees on their saved wallets, while it sat in the PINS array looking covered. ⚠ **This was a MIRROR that broke:** `__tests__/db-invariants-drift-guard.test.ts` learned about `PROCEDURE` on **2026-08-16** (recording that a FUNCTION-only needle "made every PROCEDURE in this database UNPINNABLE"), and the checker — whose own comment says it *"mirrors the guard's own parser"* — did not get the fix, for six days, with the mirror claim sitting in the file. ⚠ **Grepping the EXPRESSION rather than the file then found a THIRD copy**, `scripts/verify-live-ddl.mjs`, also FUNCTION-only, whose header likewise claimed it extracts DDL *"exactly as"* the drift guard does. All three now handle both kinds. ✅ **Verified:** with the fix the extractor pulls exactly one body, hashing **`b276cb4eea98ba5dcb0d1923da9cf5b4`** identically across live `prosrc`, the migration and the test — so this pin reads clean without any content change. Negative control: pre-fix, `verify-live-ddl.mjs` printed *"not found"* for the same file. **A new test derives the extractor-copy set BY SCANNING** (not a curated list — that is how the third copy was missed) and fails if any copy stops accepting `PROCEDURE`; mutation-tested by reverting one copy, which it names precisely. ✅ **3 of 6 CLOSED — `public_board_liveness_sweep`, 2026-08-22.** ⚠ **Its drift is a coherent FEATURE, not rot**, and the slow-vs-empty split the instrument exists for is unchanged: least-recently-probed **rotation**, a 14-day per-board **median** cost estimate (p50, deliberately not p90), a **predictive skip** that declines to START a board whose estimate will not fit the remaining budget (a running board cannot be preempted), and a `skipped` counter. 🚨 **The property worth pinning is the LAST LINE of the skip branch, not the counter:** a skip *also* sets `budget_exhausted`. Without it a tick that quietly checked one board of four would report `probed=1, budget_exhausted=false` and read as a clean sweep of a short watchlist — the same false-green the budget path already guards, arriving by a different door. ✅ **Three new assertions, all mutation-tested and each killed by its intended case:** removing `v_bust := true` from the skip branch reds the inconclusive assertion; removing the `n_probed > 0` first-board exemption reds the skip-count assertion; replacing the rotation `ORDER BY` with plain alphabetical reds the rotation assertion. ⚠ **The rotation assertion needs its OWN isolated tick** — each sweep probes the least-recently-checked board and stamps it `now()`, so four assertion calls rotate through all four boards and destroy the ordering the check depends on. That is the feature working, and the first draft of the assertion failed because of exactly that. Body hashes **`e5fe398d0b1ac6d330ab1e096cf04c9d`** across live `prosrc`, the test and the migration. ✅ **4 of 6 CLOSED — `get_active_challenges`, 2026-08-22, and its one-line drift is worth reading.** The ENTIRE difference is the ownership join: `lower(o.wallet_address) = lower(p_wallet)` → `o.wallet_address IN (p_wallet, lower(p_wallet))`. That makes the predicate **sargable** (wrapping the column in `lower()` cannot use an index), but it is **also a semantic narrowing**: a stored address whose case differs from BOTH the argument and its lowercase form no longer matches, so its slots read as UNOWNED and `costToComplete` reads as full price — on the *"is this challenge worth it"* verdict. ⚠ **MEASURED and currently HARMLESS, but only a positive control established that:** `wallet_moments_cache` holds **25,447 mixed-case rows across 384 wallets**, none with a lowercase duplicate; **436** wallets own a challenge slot edition all-time; **the two sets are DISJOINT**. All 31 challenges are `ended` (latest 2026-07-16). 🚨 **The first pass read this as a live defect and was wrong** — an initial *"0 affected wallets"* was **vacuous**, because the active-challenge population is empty; the 436 figure is the control that makes the zero mean something. **A cheap positive control is what separated a real finding from a scare.** The pinned test now asserts the case semantics explicitly (with its own exact-form control) so the narrowing stops being silent; mutation-tested by restoring the old predicate, which reds it. ✅ **5 of 6 CLOSED — `get_challenge_plan`, 2026-08-22, the SIBLING of the above.** It drifted in the **same pass**: the `owned` CTE took the identical `lower(col) = lower(arg)` → `col IN (arg, lower(arg))` sargability rewrite, with the same narrowing. ⚠ **Its size delta is a trap worth recording: the body got 57 characters SHORTER while the predicate grew by 6**, because the other change is a **dropped comment** on the `pick` CTE. **A size delta is not a change summary** — only the diff is. Its test now asserts the case semantics *and* that the miss shows up in the PRICE (`costToComplete` reads the full 30+40 instead of 40), with an exact-form control; mutation-tested by restoring the old predicate. ✅ **6 of 6 CLOSED — `get_pack_detail_bundle`, 2026-08-22. THE ITEM IS DONE.** Three changes: `SECURITY DEFINER` → **`STABLE`** `SECURITY DEFINER` (accurate — it only reads); `SET statement_timeout TO '30s'` **removed**, which is a cleanup rather than a behaviour change because CLAUDE.md records a function-level `statement_timeout` as **INERT**; and `and fs.computed_at <= now()` added to the per-edition FMV lookup to hand the planner the partition key so it prunes the empty future partition. ⚠ **Its comment calls that predicate "a no-op on the RESULT" — which is a claim about the DATA, not the code.** Verified against prod: **zero** `fmv_snapshots` rows carry `computed_at > now()`, with a positive control (14,192 snapshots in the trailing 24h, max `computed_at` 37 s old) so the zero means *"none are future-dated"* and not *"the predicate matched nothing"*. The pinned test now asserts what the predicate actually DOES — a future-dated snapshot with a 5000 FMV must not win the hero slot, and its edition keeps its latest non-future value — so clock skew or a back-dated writer surfaces as a red test rather than a wrong hero image on a public pack page. Mutation-tested by deleting the predicate, which reds it. ⚠⚠ **RE-OPENED, DIFFERENT PIN, AND THE CAUSE IS BENIGN — re-derived 2026-08-24.** The daily check **FAILED again 08-24 08:07Z** (and 08-23 07:50Z): `checked 189 pins — 188 clean, 1 needing attention`, the one being **`refresh_wmc_fmv_changed`** — **none of the original six.** ✅ **Diagnosed, not guessed: this is a FIX LANDING, not drift.** `audit_20260822_rwfc_temp_build_materialized_cte` was the migration the 08-23 filing called *"UNAPPLIED, and silent about it"*; it has since been applied. Measured live today with the same method that filing prescribed — **`MATERIALIZED` occurrences 1 → 3**, and `CREATE TEMP TABLE _rwfc_recent … DISTINCT ON` (the bare build) now **absent**. **The live body changed, so its verbatim pin is legitimately stale.** ➡ **The remedy is a RE-PIN, and it is NOT a paste:** the check's own message warns *"a stale pin usually means the assertions describe old behaviour"*, so the pin's assertions must be re-read against the new body before the DDL is replaced. Live `md5(pg_get_functiondef(...))` = `7094783150faf1b39148dc3c213d1e18`, `prosrc` 3,648 chars — capture byte-exactly and verify against that digest rather than by eye. ⚠ **Deliberately not done in the 08-24 pass:** the rewrite is a performance change so the behavioural assertions probably still hold, but **"probably" is not a verification**, and a wrong re-pin is worse than an honest red — it would make the guard green over a definition nobody checked. ✅✅ **RE-PINNED AND CLOSED 2026-08-24, later the same day — the assertions were re-read FIRST, which was the actual job.** Every property this pin depends on was re-counted against the LIVE body before anything moved: `fmv_usd IS NOT NULL` **×3** (the pin's own *"appears THREE times"* note still holds), `IS DISTINCT FROM` **×1** (churn guard), `edition_key IS NOT NULL` **×1**, `rwfc_state` **×2**, `DISTINCT ON` **×2** — **all intact**, because the change is a planner-level CTE materialisation that moves no behaviour any assertion here depends on. **Then** the PINS entry was re-pointed 08-13 → `20260822213000_audit_20260822_rwfc_temp_build_materialized_cte` and the pin's embedded DDL replaced verbatim. ✅ **VERIFIED BY REPRODUCING THE CHECKER'S OWN COMPARISON rather than by running it** (no service-role key needed): the migration body matches live `prosrc` under **BOTH** normalisations the script accepts — collapsed `md5 e110b33f50747333f362699129ff3904` (3,238 chars) and stripped+collapsed `md5 ee96aade4d4f4db360ed70322a1088b5` (2,443) — **identical on both sides**. `db-invariants-drift-guard` green (194), full suite 1372/1372.

⚠ **VERIFY THE CLOSURE, do not assume it:** the next `db-pin-staleness` run (daily 07:20Z) is the proof. It should report **187 clean, 0 needing attention** — its first green since 2026-08-03. If it does not, read WHICH pin it names before reopening this item.

🚨 **THE VERIFICATION WAS RUN ON 2026-08-23 AND THE PREDICTION WAS FALSIFIED — but the item stays RESOLVED, because it named a DIFFERENT pair.** The 07:51Z run reported **`checked 189 pins — 187 clean, 2 needing attention`**: `refresh_cross_collection_cohort_step1` and `_step2`, neither of them among the six. The **08-22 ~20:35Z lock-window rewrite** (aggregate into a temp table, then TRUNCATE immediately before a tiny insert) shipped its own new output-equivalence pin file and **left the two existing pins pointing at the superseded 08-16 snapshot** — so the instrument re-opened within hours of this item closing, on work done the same evening. A **third** followed at 19:06:48Z: `log_pipeline_run`, from that day's `now()` → `clock_timestamp()` fix, whose migration was applied via MCP **with no committed file at all**, so `migration-parity` and this sweep were red on one change.

✅ **All three re-pinned 2026-08-23, and VERIFIED AGAINST THE LIVE DATABASE by `workflow_dispatch` at 20:31Z: `checked 189 pins — 189 clean, 0 needing attention`** — the sweep's first green since 2026-08-09.

⚠ **The durable lesson, and it is the reason this item should not be re-numbered as "the six" again: CLOSURE HERE IS A MOMENT, NOT A STATE.** Any migration that redefines a pinned function re-opens the sweep the same day, and repo-vs-repo CI stays green throughout — only the live check can see it. **Re-pointing the pin is part of shipping the change, not a follow-up chore.** ⚠ **And the assertion review keeps earning its budget:** on `log_pipeline_run` the existing assertion (`finished_at > started_at`, fixture calling with `now() - interval '5 seconds'`) **passed under both the broken and the fixed body** — a fixture that back-dates its input tests the happy direction of a defect that only appears in the other one. On `_step2` the assertion still passed while its stated REASON ("because the TRUNCATE happens first") had become false. Both are now written the way production behaves, and mutation-proven.

🔑 **The transferable finding, and it held for all six: NOT ONE was what this item's own summary implied.** "6 stale pins" reads as uniform rot; the six were a misleading header over a one-feature drift · a **correct** pin the checker could not parse (a PROCEDURE, see the mirror bug above) · a deliberate feature addition · a sargability rewrite that looked like a live pricing defect until a positive control refuted it · its sibling, whose body got **shorter** while gaining logic · and a partition-pruning predicate whose "no-op" rested on an unstated data assumption. **Each needed its own diff before it could be understood, and two needed a control before they could be trusted. A count of stale pins is a work-queue length, never a description of the work.**

25. 🟡 **ARM SHIPPED 2026-08-22, INACTIVE until one env var is set** — **Nothing reads the GitHub Actions instruments, which is why #23 and #24 both sat unnoticed for two weeks (found 2026-08-22).** Three credentialed detectors run daily and are the only things in the estate that can see their respective rot classes: `edge-fn-drift` (06:40Z), `db-pin-staleness` (07:20Z), `migration-parity` (07:40Z). Measured this day: **edge-fn-drift red 14 consecutive runs, db-pin-staleness red 13, migration-parity 14/14 green.** ⚠ **Both red ones are LOUDLY CORRECT** — the state CLAUDE.md warns is indistinguishable from a broken instrument at a glance, and the file already records this happening to `edge-fn-drift` once before. It has now happened to it again, and to a second detector, **simultaneously**. **The structural gap:** the hourly sentinel is what Trevor actually reads, and it has **no arm for GitHub Actions health** — it watches pipelines, FMV, coverage and trust, all via Supabase. So a detector can be red every day indefinitely and nothing surfaces it. ⚠ **The fix is not another workflow** — a watchdog workflow is itself something nobody reads, one level up. It belongs in the sentinel: an arm reading the last N conclusions for these three workflow files and warning on a failure STREAK (not a single red, which is normal for a detector that is doing its job). ⛔ **Blocked on a secrets decision, not a design one:** the sentinel runs on Vercel and would need a GitHub token with `actions: read` in Vercel env, and CLAUDE.md puts secrets/env off-limits for autonomous change. **Operator:** decide whether to add that token; the arm itself is small. ✅ **THE ARM IS BUILT AND MERGED (2026-08-22).** `Detector Health (GitHub Actions)` now lives in `app/api/sentinel/route.ts`: it reads the last 12 runs of the three watched workflows and keys on a **consecutive-failure STREAK** counted from the newest completed run backwards (warn ≥3, critical ≥7, both `thr()`-configurable). ⚠ **A streak, not a single red** — one red run is a detector doing its job; the defect is a red that PERSISTS unread. **To activate it, set `GITHUB_ACTIONS_READ_TOKEN` (a token with `actions:read`) in Vercel env — that is now the whole remaining task.** ⚠⚠ **ALL THREE NUMBERS RE-DERIVED 2026-08-24 — and one of them is now FALSE, which STRENGTHENS this entry rather than weakening it.** **`edge-fn-drift`: 14 → 16 consecutive SCHEDULED failures** (unbroken 08-09 → 08-24). ⓘ **And #31 now explains WHY in a way this entry could not: its authoritative arm has never once run** (0 of 38 body reads; the API serves an `eszip` binary and the script calls `.json()`), so it has been *loudly correct about the wrong half* the whole time. **`db-pin-staleness`: still red, but on a DIFFERENT pin** — the original six are closed and today's failure is `refresh_wmc_fmv_changed`, stale because `rwfc_temp_build_materialized_cte` was applied to prod. **Re-pinned 2026-08-24 (`fb533970`), so the 07:20Z run on 08-25 should be the first GREEN — a prediction with a named falsifier.** 🚨 **`migration-parity`: this entry says 14/14 GREEN and that is NO LONGER TRUE — it went RED on 2026-08-23** (07:58Z scheduled, and again on a 20:43Z dispatch) on the sixteen fileless migrations. ➡ **That is the entry's own thesis playing out a third time: a detector went red, was correct, and nothing surfaced it — it was found by a session reading logs.** ✅ **Partially remediated 2026-08-24: parity now runs 3×/day** (07:40 + 15:50 + 23:50 UTC, `039527d8`), cutting the blind window ~24 h → ~8 h. **Verified in production: the new 15:50 slot fired and passed** (`2026-08-24T16:16:56Z success schedule`). ⛔ **The sentinel arm itself remains blocked on the secrets decision** — cadence is a mitigation, not the fix.

⚠⚠ **ALL THREE RE-DERIVED AGAIN 2026-08-24 ~21:20 PT (2026-08-25 04:20Z) via `gh run list` — and the picture has MOVED in both directions, so do not quote the numbers above.** ⓘ Method note worth keeping: **`gh` is authenticated on Trevor's box** (`gh auth status` → `jamesdillonbond`), so the "manual step" this item mandates is three commands, not a blocked task — and it is filtered to `event=="schedule"` because dispatches interleave and make a naive streak count wrong.

    - **`edge-fn-drift`: 16 consecutive SCHEDULED failures, UNCHANGED** — an unbroken run 2026-08-09 → 2026-08-24, last success **2026-08-08T07:31Z**. Confirms the 08-24 figure exactly rather than restating it. Still loudly correct (see #31: its authoritative arm has never run).
    - 🚨 **`migration-parity` HAS RECOVERED, so this entry's "went RED and that is the thesis playing out" is now STALE IN THE OTHER DIRECTION.** Its only failure in the visible window is **08-23T07:58Z**; since then **three consecutive scheduled successes at three DISTINCT slots — 08-24T08:22Z, 08-24T16:16Z, 08-25T00:24Z.** ✅ **That is the 3×/day cadence change (`039527d8`) verified in production across a full daily cycle**, not just the single 15:50 slot this entry already recorded. The sixteen fileless migrations are cleared.
    - ⏳ **`db-pin-staleness`: the 08-25 prediction is PENDING, NOT FALSIFIED — and the run that looks like a falsification is a pre-fix run.** Latest scheduled: **08-24T08:07Z → `checked 189 pins — 188 clean, 1 needing attention`** (read from the log, not the badge). ⚠ **That run predates the `fb533970` re-pin by seven hours** (re-pin landed 08-24 08:27 PT = 15:27Z). CLAUDE.md's rule applies literally — *a failure timestamp that predates a fix is a STALE pre-fix run, not a finding.* The named falsifier is the **08-25 ~07:20Z** run, still ~3 h away at the time of writing. **Anyone reading "db-pin-staleness: failure" before then and concluding the re-pin failed has read the badge, not the log.**

    ✅✅ **THE 08-25 PREDICTION HELD — re-derived 2026-08-25 07:40 PT from the run LOG, not the badge.** `db-pin-staleness` scheduled run **2026-08-25T08:02:29Z → SUCCESS**, log line `checked 189 pins — 189 clean, 0 needing attention`. **That is its first green scheduled run after a 13-run red streak**, and it confirms the `fb533970` re-pin of `refresh_wmc_fmv_changed`. ⓘ The falsifier was named in advance and the run that would have falsified it is the one that fired — which is the only reason this counts as evidence rather than as a coincidence noticed afterwards.

    ⚠ **The OTHER two, same morning, so the numbers here stay honest in BOTH directions:** `migration-parity` is now **four consecutive scheduled greens** (08-24T08:22Z · 08-24T16:16Z · 08-25T00:24Z · 08-25T08:20Z) — the 3×/day cadence verified across two full daily cycles, not one slot. **`edge-fn-drift` went RED again at 08-25T07:27Z: 17 consecutive scheduled failures**, unbroken since 08-09. ➡ **So of the three detectors this item was written about, TWO are now green and one is still loudly correct** — and #31 explains why that one is loudly correct about only half its question.

    ⚠ **Two design points that were NOT obvious and are worth not re-litigating.** (1) **The unconfigured state reports `ok`, not `warn`** — visible and annotated `[NOT CONFIGURED]`, matching the convention this route already uses for a config-disabled check. A permanently-warn arm would drag the entire sentinel to WARN every hour until the token existed, and CLAUDE.md records that a permanently-red instrument is indistinguishable from a broken one — it would desensitise every *other* arm in the report. The property pinned by test is that the arm is **PRESENT and says why**, not that it pages. (2) ⚠ **It reads ONLY `GITHUB_ACTIONS_READ_TOKEN`, deliberately NOT falling back to `GITHUB_TOKEN`.** That name is set by GitHub Actions itself and exists in many environments for unrelated reasons — **it is set in this repo's own sandbox**, which is how the fallback was caught: it made four unrelated sentinel tests fail by firing live GitHub calls. A fallback would make the arm act wherever that variable happened to be defined, with whatever scopes it carries.

✅ **Five tests, two mutation-tested:** making the unconfigured branch silent reds it (that is the exact bug the arm exists to catch, so it must not commit it itself), and folding an unreadable workflow into "healthy" reds it. Also pinned: a single red run stays `ok`, a 3-run streak warns, and a 12-run streak — the shape `edge-fn-drift` actually had — pages critical.

27. 🟡 **OPERATOR ACTION, one statement — the three board-MV cron jobs carry a 600 s `statement_timeout` that is too generous, and one of them is burning it (found 2026-08-23 00:45Z).** The MVs shipped 2026-08-22 (deals / panini-squeeze / first-mint) were registered with `SET statement_timeout = '600s'`. During the spell that began ~23:40Z, `rpc-refresh-panini-squeeze` **burned the full 600 s and rolled back**, writing no MV rows and no `pipeline_runs` row, and repeated it on the next tick; the three jobs together took **911 s in hour 00 — 8.3% of all cron time on the instance**. **Fix:** re-run `cron.schedule` for jobs 352/353/354 with `'300s'` (it upserts on `(jobname, username)`, so no jobid churn); exact SQL in [the waste ledger §11.4](../overnight/inbox/2026-08-22T1956Z-the-saturation-is-structural-a-waste-ledger-from-pg-stat-statements.md) and in `docs/overnight/ledger.md`. **Revert:** the same three statements with `'600s'`. ⚠ **Why it is an operator action and not shipped:** the sandbox auto-mode classifier denies `cron.schedule` writes — this is a HARNESS refusal, not a database one, so the `cron_heavy` membership question in #19 does not apply. ⚠ **300 s clears every healthy run by 3–30× and cuts exactly one observed success** (panini's 379.9 s, which by the break-even test cost more than the reads it replaced). ✅ **Not urgent for correctness** — a failed refresh leaves the MV stale with an **honest** freshness stamp (`readMvAsOf()` returns the last *successful* refresh), and the 100-min liveness arm fires. It is a waste item, not a lying-surface item. 🟡 **STILL OPEN — re-read from live `cron.job` 2026-08-26 (PT), unchanged:** jobids **352 / 353 / 354** (`rpc-refresh-cross-collection-deals`, `rpc-refresh-panini-squeeze`, `rpc-refresh-topshot-first-mint`) all still carry `SET statement_timeout = '600s'`; the operator statement has not been run. ⓘ **Context that was not in the original filing: they are not outliers.** **15 of the 99 active jobs** declare a 600 s budget (18 declare any `statement_timeout` at all), so “tighten these three” is a spot fix on a house default — decide whether the number is wrong for the THREE or wrong for the FLEET before writing the statement.

28. ✅ **RESOLVED — code fix 2026-08-23 (`9b862dc8`), and VERIFIED IN PRODUCTION AGAINST THE DATABASE 2026-08-24 04:50 PT.** ⚠ **This entry read 🔴 OPEN for a day after the fix shipped, and CLAUDE.md's honesty bullet pointed at it as OPEN** — the register lagging the tree is the same stale-instrument class it exists to catch. **The verification is a SET comparison, not a status read:** every segment was fetched from `www.rippackscity.com` and its `<loc>` count compared against a `count(*)` over the live `editions` table computed with the route's own predicates. **Segment 2 = 7,283 `<loc>` vs 7,283 rows (All Day 6,190 + Golazos 575 + UFC 518, `external_id is not null`) — EXACT. Segment 1 = 13,241 vs 13,241 Top Shot rows surviving `dropTsFossils` (`external_id not like '%-%'`) — EXACT.** Segments 0 / 3 / 4 all 200 (117 / 4,607 / 8,078). ⓘ **An exact match on BOTH sides is the control the original defect could not have passed**: a truncating loop returns fewer rows than the table holds, and the 68.4%-ties paging bug drops and duplicates in ways only a set/DISTINCT comparison sees — a count alone would have been the wrong instrument had it not matched to the row. ⛔ **Do not read this as "the sitemap can no longer be incomplete."** It cannot be SILENTLY incomplete: `fetchAllByCollection` now throws `SitemapReadIncomplete` on a page error, on a non-unique tiebreak (`assertUsableTiebreak`, checked BY VALUE because the static ratchet can only see that a second `.order()` exists), and on exhausting `maxRows`; `app/sitemap/[id]/route.ts` turns that into a **503**, so a crawler retries and keeps the sitemap it already has. **A 503 here is the fix working.** ⚠ **The measurement above was taken on a healthy read** — it proves the complete path, not the throw path; that one is covered by `__tests__/sitemap-data.test.ts` and `__tests__/api-sitemap-routes.test.ts`. Original filing: [`inbox/2026-08-23T0228Z-…`](../overnight/inbox/2026-08-23T0228Z-the-sitemap-truncates-on-a-statement-timeout-and-pages-on-a-key-that-is-72pct-ties.md).

29. 🟡 **OPEN (downgraded on re-derivation) — `allday-pack-opens-backfill` writes on ~1 tick in 10, and pg_cron calls every dispatch a success.** The 2026-08-23 02:00Z filing reported it **silent for 12.6 h** while `cron.job_run_details` jobid 55 read **141 succeeded / 3 failed** in 24 h. ⚠ **The silence ENDED before the filing was read: re-measured 2026-08-22 19:42 PT (02:42Z), it wrote 482 rows at 02:16:55Z, `ok`.** What survives re-derivation is the RATE, not the outage: **47 `pipeline_runs` rows across a 79.0 h window against a 6-per-hour schedule (~474 expected) = 9.9%**, of which **13 are `ok`**; failures are upstream (`events 84699998-84700247 status 0` / `status 503`) plus `Timed out acquiring connection from connection pool`. ⚠ **`succeeded` in `cron.job_run_details` means the `net.http_get` was DISPATCHED and says nothing about the callee** — the documented split; read `net._http_response` and the outcome table, never the scheduler self-report. **Not an auth failure** (771 × 200 in the filing window). Filing: [`inbox/2026-08-23T0200Z-…`](../overnight/inbox/2026-08-23T0200Z-allday-pack-opens-backfill-silent-while-pg-cron-reports-141-successes.md). ⛔⛔ **RE-DERIVED 2026-08-24 15:45Z, AND A PROPOSED SUPPRESSION WAS REFUTED.** The daytime monitor filed that this pipeline's `cron_silent` arm is now a **FALSE POSITIVE** because *"the finite walk finished"*, and proposed suppressing the arm while jobid 55 is `active` and `succeeded`. ⚠ **Both conditions hold right now and the pipeline is still genuinely broken, so that suppression would have hidden a real fault behind a green board.** ✅ **The premise does not survive: `pipeline_runs` holds 25 rows for this pipeline and ZERO carry a `done` key** — the state the remedy rests on has never been emitted (the keys it writes are `partial` / `progress_blocks` / `scanned_floor` / `resolve_exhausted` / `skipped_permanent`). **18 of 25 rows are `ok:false`**, and the last two end on upstream `status 503` / `status 0` — **a finished walk does not end on a 503.** 🚨 **And it is ~19.4M blocks from its floor** (`scanned_floor` 84,662,756 vs target 65,264,619), having moved **40,492 blocks in ~2.3 days**. ✅ **What IS real:** the scheduler fires (**117 succeeded / 3 failed** dispatches in 20 h) while `pipeline_runs` has been **silent ~18.3 h** across ~110 of them — **so the arm is reporting a true anomaly.** ⛔ **NOT established: WHY the callee stops short of `log_pipeline_run`** — `net._http_response` carries no URL and `net.http_request_queue` drains, so responses cannot be attributed to this job from SQL; the *"returns before logging, like `topshot-active-listings-ingest`"* shape is an untested hypothesis. ➡ **Do NOT make the arm done-aware.** ⓘ **The generalisable lesson: "the instrument is a false positive" is the most expensive conclusion a monitor can reach, because the remedy is SUPPRESSION and a wrong suppression is silent by construction.** ✅✅ **MECHANISM FOUND 2026-08-24 15:50Z — and it breaks a rule CLAUDE.md already carries.** I had recorded *"why the callee stops short of `log_pipeline_run` is not established; it needs the function's own logs"* — **it does, and those logs are reachable** via the Supabase MCP unified log stream (`function_logs` / `function_edge_logs`); `net._http_response`'s missing URL was a dead end, not the only instrument. 🚨 **`function_logs` for `ingest-allday-pack-opens`: 186 Boot, 185 Shutdown `reason: EarlyDrop`, 2 `TerminationRequested`.** **185 of 186 invocations are dropped before they finish, so the function never reaches its logging call.** Execution is wildly variable — 51× HTTP 200 at **avg 12.7 s / max 124.4 s**, plus 2× 502 (~81 s) and 1× 504 (150 s) — against the caller's **`timeout_milliseconds = 90000`** (read from `cron.job` jobid 55), and `net._http_response` shows **76 `timed_out = true`** in one 3-hour window. ⚠ **~54 logged invocations against ~140 scheduled dispatches**, so most dispatches produce no invocation at all. ⛔ **Still inference, not measured: the trigger for EVERY `EarlyDrop`** — the 51 fast 200s did **not** time out, which hints at work continuing after the response and dying with the isolate; **do not record that as the mechanism without testing it.** ➡ **THE FIX IS THE RULE THIS REPO ALREADY WROTE:** *"any `after()` route needs an invocation heartbeat written BEFORE the work … `try/catch` CANNOT catch a `maxDuration` kill … read kills by CORRELATION (heartbeat, no terminal row)."* **Same defect, edge function instead of a Vercel route.** A heartbeat makes a killed tick visible as *heartbeat present, terminal row absent* instead of the indistinguishable silence the `cron_silent` arm reports today — and separately, a walk needing >90 s per tick should be chunked to fit its caller. ⛔⛔ **I RETRACT THE `EarlyDrop` MECHANISM, ~15 minutes after recording it (2026-08-24 16:00Z).** The claim was *"185 of 186 boots end in `EarlyDrop`, so the function never reaches its logging call."* **Wrong — `EarlyDrop` is the NORMAL shutdown reason for every edge function on this project and has ZERO discriminating power.** ✅ **The control, which I should have run before writing it:** `flowty-proxy` **3,109 boots / 3,119 EarlyDrops** (it serves the live app); `compute-topshot-pack-ev` **242 / 240** and writes **415 `pipeline_runs` rows in 24 h**; `pinnacle-nft-resolver` **297 / 296** and writes **289**. **Functions that EarlyDrop at ~99% log perfectly well.** ⚠ **This is the control CLAUDE.md names — *a POSITIVE needs a no-change control* — and the 99% figure is what made it feel conclusive, when 99% is exactly what a BASELINE looks like.** ⓘ **The meaningful kill reasons are `WallClockTime` / `CPUTime` / `Memory`, and `ingest-allday-pack-opens` has ZERO of all three** — it is not being killed by the runtime (`compute-allday-pack-ev` does show 5 `WallClockTime`, which is what a real kill looks like here). ✅ **UNAFFECTED: the `done:true` refutation above** (25 rows, zero `done` keys, 18 failures, ~19.4M blocks to go) — **still do not suppress the arm** — **and the timing facts** (51× 200 at avg 12.7 s / max 124.4 s vs a 90 s caller timeout; 76 `timed_out` in 3 h; ~54 invocations vs ~140 dispatches). ⛔ **OPEN AGAIN, in a sharper form: 51 invocations returned HTTP 200 and still wrote no run row. A 200 is not a kill** — something returns success without logging (early exit, lock, nothing-to-do path, or a failing `log_pipeline_run`). ➡ **Next step is a CODE READ of the edge function, not another log query.** ⓘ The heartbeat recommendation stands on its own merits but **no longer rests on an EarlyDrop argument.** ✅✅ **RESOLVED 2026-08-24 16:10Z — the two modes SHARE ONE EDGE FUNCTION, and only BACKFILL is silent. That asymmetry is the answer.** I had treated `ingest-allday-pack-opens` as one population; it serves two pipelines. **`allday-pack-opens-forward`: 46 rows / 24 h, ALL ok, newest 15:39:08Z (minutes ago). `allday-pack-opens-backfill`: 5 rows, newest 08-23 21:26Z.** ⛔ **This kills my own second hypothesis too:** I had flagged that `logRun` does `await supabase.from("pipeline_runs").insert({…})` **without checking the returned `error`** — a real instance of the repo's top defect (supabase-js RETURNS errors) — **but it cannot be the cause, because the identical call writes 46 forward rows a day.** ⚠ **A defect that is real and is NOT the cause of what you are investigating must not be reported as the cause** (fix it separately, as hygiene). ✅ **And the code confirms the `done:true` refutation was right for the right reason:** line 507 emits it under `if (cur <= floor)`, with `cur` = 84,662,756 vs `floor` = 65,264,619 — **the state is REAL and ~19.4M blocks away, not absent.** ➡ **The supported explanation:** forward scans a small tip window and finishes fast; **backfill scans large historical ranges — observed up to 124.4 s against the caller's 90 s `timeout_milliseconds`**, with 76 `timed_out` in one 3 h window and ~54 logged invocations against ~140 dispatches. **A request abandoned at 90 s never reaches `logRun`.** ⚠ **Measured: the asymmetry and the timing. NOT proven: that every missing row is a timeout** — individual dispatches cannot be matched to invocations (`net._http_response` has no URL). ➡ **Actions: (1) the heartbeat CLAUDE.md already prescribes — now supported by the timeout rather than by EarlyDrop; (2) chunk backfill to fit 90 s, or raise the caller's timeout deliberately; (3) fix `logRun`'s unchecked insert error as separate hygiene; (4) still DO NOT suppress the arm — it has been correct throughout.** ⚠ **RE-MEASURED 2026-08-26 (PT) — the RATIO IN THIS ITEM'S TITLE NO LONGER MATCHES, so re-derive before acting.** In the trailing 24 h `pipeline_runs` holds **6** `allday-pack-opens-backfill` rows (4 ok / 2 failed) against jobid 55's **144 dispatches** (`6,16,26,36,46,56 * * * *`) — a recorded rate near **1 in 24**, not 1 in 10. ⭐ **And the two failures name a DIFFERENT fault than the one this item diagnoses:** `events 84581756-84582005 status 503`, an UPSTREAM 503, not a wall-clock kill. ⚠ **A lower recorded count is not a lower work rate** — `rows_written` over the same window is **681**. The three ticks that write are still invisible to `cron.job_run_details`, which is the durable half of this item and is unchanged.

30. 🔴 **OPEN (headline CORRECTED same night — see the ⛔ below; it is DEGRADED, not dead) — `topshot-active-listings-ingest` refreshes ~once a day instead of 8×, and the DB timeout is breaking the only arm that works.** **40 of the last 40 runs failed**, unbroken 2026-08-19 07:11Z → 2026-08-24 04:11Z; zero successes in the window. Classified by reading every log, not by sampling: **29/40 (72.5 %) `GET targets failed: 500 {"error":"canceling statement due to statement timeout"}`** · **9/40 (22.5 %) Atlas WAF block** (the `egress_blocked` class of #20 — the runner's own probe trips after 5 consecutive non-JSON responses and exits 1) · **2/40 (5 %) `GET targets failed: 504 An error occurred with your deployment`** (Vercel gateway on the same endpoint). ⚠ **Two failure modes wearing one badge, and the register named the smaller one.** The dominant failure is `GET /api/cron/topshot-active-listings-ingest?phase=targets`, whose whole body is `supabaseAdmin.rpc("topshot_serial_board_targets", { p_min_no1_estimate: floor })` — **it never reaches Atlas**, so #20's egress fix cannot touch it. 🚨 **The RPC is marginal at rest, not merely in a spell** — `pg_stat_statements` over the complete population (reset 2026-08-12 01:34Z, `dealloc = 0`): **57 calls · mean 13,163 ms · max 29,949 ms** against `service_role`'s **30 s** `rolconfig` ceiling, **620,272 disk block reads and 46,184,123 buffer hits (~6.2 GB of buffer touches PER CALL)**. A mean at 44 % of its ceiling and a max at **99.8 %** of it is a query that fails on ordinary contention. ⚠ **This is an R46 SYMPTOM, not an R46 CAUSE** — at ~5 GB of disk reads over 12 days it is ~0.06 % of the 8,227 GB; it is a victim of the saturation Trevor decided on 2026-08-23 not to buy out of, and the honest consequence is that **this pipeline stays red until the query is cut down**, since capacity is not coming. ✅ **CONSUMERS NOW TRACED — it IS user-facing, two surfaces:** the public **Underpriced #1s / Perfect-mints board** (`lib/underpriced-serials-board.ts` → `/api/public/insights/underpriced-serials`, read by the API route, the server page **and** the OG card) and the **concierge**'s edition-listings tool (`app/api/support-chat/route.ts`, three reads). ✅ **BOTH CONSUMERS READ 2026-08-24 00:05 PT — THEY ARE HONEST, NOT ESCALATING.** The **view** gates on `l.active` and never filters `last_seen_at` (which it selects and passes through), and `active` is only cleared by `deactivate_stale_topshot_active_listings`, which runs **only on a successful ingest** — so the DB layer alone WOULD publish a sold listing as live. But the **page** computes `listingsAgeHours` from `max(last_seen_at)` and renders **"Listings last refreshed {N}h ago" at ≥4 h** (live right now at 7.1 h), and the **concierge** reads the same max, **tells the model the age on every call**, and sets a conservative `feed_stale` at **36 h** — its comment records the measured gap distribution (min 3 h / median 6 h / **p90 22 h / max 26.7 h**) and the `ufc_fmv_stale_hours` cry-wolf precedent that ruled out a 24 h ceiling. ⓘ The page's comment already knew *"the ingest runs ~every 3h from a residential runner"* — **the eighth-caller fact existed in a component comment and in no reference doc.** ⚠ **RESIDUAL (LOW–MED, unshipped): the OG card bounds the FAILED read and not the STALE one.** `app/api/og/insights/underpriced-serials/route.tsx` uses `boardEmptyCopy(fetched, …)` but renders `boardCountLabel(count, 'live deals')` and a `Live deals · buy on Dapper` footer with **no age signal at all** (verified: the only `age`/`hours` matches are substrings of *message*/*Image*/*page*). **Not a fabricated number** — the count is real `active` rows — **an unbounded liveness claim on the widest-reach surface** (Twitter/iMessage/Slack previews) while `active` is frozen. The documented *fix per PANEL, not per page* shape. Copy the page's ≥4 h threshold, but **`lib/og/board-count.ts` is shared by other boards — check the blast radius first.** ✅ **THIS RESIDUAL IS CLOSED AND THE ENTRY ABOVE IS NOW STALE — struck 2026-08-26.** The card was fixed in `d2101312` (*"the Underpriced #1s card must not claim 'Live deals' over a frozen board"*): it computes `boardMaxAgeHours(..., 'last_seen_at')` → `boardLivenessLabel(ageHours, 'Live deals')` and **renders it** — verified RENDERED, not merely computed, because a dropped honesty signal would be its own defect. The code carries the three-state note explicitly (*"null => age unknown => NO liveness claim at all"*), so an unknown age now yields **no claim**, which is exactly what this residual asked for. ⚠ **Re-derive a residual before acting on it** — this one would have sent someone to fix something already fixed. ⭐ A wider sweep of all 42 OG routes found the rest of the estate in better shape than a helper-based proxy suggests (15 cards flagged, ~all guard at the render site and show an em-dash rather than a measured zero), with ONE genuine survivor — `/api/og/insights/candy-mlb`, which asserts "Live secondary FMV" on a failed read and whose `catch` cannot fire because it destructures `{ data }` without `error`. That fix is blocked on a design call (the honesty guard's shape assumes a self-fetching card): [inbox 2026-08-26T1640Z](../overnight/inbox/2026-08-26T1640Z-the-og-honesty-convention-is-structurally-unavailable-to-supabase-backed-cards.md). ⚠ **And the concierge's COMMENT is now misdiagnostic** (it blames the Atlas WAF / GHA runner IP): behaviour right, attribution superseded — a future reader will chase `atlas-proxy` again. 🚨 **PLAN READ 2026-08-23 22:35 PT (plain `EXPLAIN`, nothing executed): the cost is an unbounded `DISTINCT ON (edition_id)` over Top Shot's `fmv_snapshots` inside `topshot_serial_board_candidates` — 857,293 rows scanned to return 13,230, a ~65:1 amplification, as an `Index Scan` NOT Index Only because `confidence` is uncovered.** ⚠ The 118 MB `INCLUDE (fmv_usd)` index is maintained for exactly this shape and **the planner declines it**, since `INCLUDE` buys nothing while `confidence` still forces a heap fetch on all ~857k entries. ⛔ **Do NOT "fix" it with `fmv_current`** — ~335 buffers on a literal `IN` but **~1.05M on a JOIN**, and this is a JOIN. ➡ **It wants R52's precomputed latest-FMV-per-edition, which gives R52 a second consumer that FAILS rather than merely lags.** ⚠ **Still unattributed:** `serial_fmv_estimate` runs **twice per surviving row** and is plpgsql that reads tables, but **`pg_stat_statements.track = 'top'` so nested statements are invisible** — the 6.2 GB/call includes it and cannot be split from it. **`EXPLAIN (ANALYZE, BUFFERS)` in a 13:00–17:00 PT window is the only instrument that can separate them**, so "the `DISTINCT ON` is the cost" is a well-supported hypothesis, not a measurement. ⛔ **CORRECTION 2026-08-23 23:05 PT — MY OWN HEADLINE WAS WRONG AND THE PIPELINE HAS A SECOND CALLER.** `topshot_active_listings` holds **624 rows / 224 active with `max(last_seen_at)` 7.1 h old**, and `pipeline_runs` shows **two `ok` runs writing 224 and 257 rows with 1,274 / 1,408 `atlas_calls` and ZERO skips** — at **22:13Z, which is not on the workflow's `29 */3` schedule**. 🚨 **The second caller is `RPC Deal Board Ingest`, a Windows Scheduled Task on TREVOR'S BOX (`PT3H` from 00:13 PT, running `scripts/run-active-listings-ingest.ps1`) — an EIGHTH caller source, invisible to all six CLAUDE.md requires, to cron-job.org, to `cron.job`, to `vercel.json` and to GHA.** Four production ingests live there (Deal Board 3 h · Pinnacle Render Cache Fill **15 min** · Panini 4 h · AllDay Badge daily). 💡 **This is where "residential Atlas ingest" physically happens** — Atlas WAF-blocks GitHub runners, not Trevor's home IP, so **the local task is the arm that FEEDS the board and GHA is the arm that never could.** ✅ **Corrected severity, worse-founded and pointing the same way:** the residential arm succeeded **1 of 8 runs on 08-23** (15:13 PT; the other seven logged `start` then died) **at the same `GET targets` DB timeout** — so the R46 symptom is throttling a WORKING production path from 8×/day to ~1×/day, and #20's `wrangler deploy` is even less of a remedy than stated above. ⚠⚠ **AND THE THREE DISAGREEING FAILURE RATES ARE THREE POPULATIONS, NOT A CONTRADICTION: `pipeline_runs` cannot see this failure BY CONSTRUCTION** — the route writes `log_pipeline_run` in the POST phase and the timeout kills the GET phase, so #20's ~60% and `metrics-latest.json`'s 80% are both computed over *runs that survived the timeout* (inside which `egress_blocked` really is ~80%), while my 22.5% was the GHA population. ⓘ ~48 runs should exist in the 73 h window; `pipeline_runs` holds **7**. **The pipeline's own telemetry is structurally blind to the thing that stops it.** ⚠ The local task's log records `start` and then nothing on failure — **and it DOES redirect every stream (`*>&1 | Tee-Object`), so "stdout only" is not the explanation**; the likely mechanism is PowerShell 5.1 wrapping a native exe's stderr in `NativeCommandError`, **corroborated by a recorded gotcha, NOT measured — do not repeat as fact.** Filing: [`inbox/2026-08-24T0430Z-…`](../overnight/inbox/2026-08-24T0430Z-topshot-active-listings-ingest-is-100pct-red-and-the-documented-cause-is-22pct-of-it.md). ✅ **HYPOTHESIS CONFIRMED BY THE NAMED INSTRUMENT 2026-08-26** — this entry said *"the `DISTINCT ON` is the cost" is a well-supported hypothesis, not a measurement* and named `EXPLAIN (ANALYZE, BUFFERS)` as the only thing that could separate it from `serial_fmv_estimate`. **Run warm-vs-warm: the `latest_fmv` `DISTINCT ON` plus its join, with NO estimate calls at all, is 841,312 of the query's 927,466 buffers — 90.7%**, so all 14,748 estimate calls are **~9.3%**. **The mechanism named here is confirmed verbatim by the plan**: `Index Scan` not Index Only, 871,797 rows read to return 19,678, because `confidence` is uncovered and the 120 MB `INCLUDE (fmv_usd)` sibling is therefore declined. 👉 **So the fix is one INCLUDE column, not R52**: adding `confidence` to `fmv_snapshots_2026_coll_ed_ct_fmv_idx` restores an Index Only Scan (~25x on 91% of the query), changes no semantics, and index DDL pays **no** `PGRST002` burst. ⛔ **Three cheaper-looking fixes FALSIFIED by measurement, so nobody re-spends the afternoon:** deferring the second `serial_fmv_estimate` is provably equivalent (`EXCEPT ALL` both directions at two floors; only **984 of 7,374** rows survive the $100 floor) but saves **1.7%**; swapping to `edition_fmv_current` is 111x faster on an identical row count but carries **233 `fmv_usd` + 79 `confidence` disagreements** and **admits 30 editions the board does not show**; a LATERAL is equivalent but **2.5x worse on buffers**. ⚠ **Production distribution re-read (69 calls): mean 13,513 ms, max 29,949 ms, 11,224 disk reads/call.** ⏳ **Index NOT yet built — the instance was in a saturation spell (9–11 active / 8–10 IO-wait) and the CIC recipe requires a proven-quiet window.** Filing: [`inbox/2026-08-26T1500Z-…`](../overnight/inbox/2026-08-26T1500Z-the-targets-rpc-is-50s-of-DISTINCT-ON-and-edition-fmv-current-is-NOT-a-drop-in.md). ✅✅ **RESOLVED 2026-08-26 — SHIPPED AND VERIFIED END-TO-END.** Built `fmv_snapshots_2026_coll_ed_ct_fmv_conf_idx` (same keys as the declined sibling, `INCLUDE (fmv_usd, confidence)`) via the one-off pg_cron CIC recipe: job `succeeded :: CREATE INDEX`, `indisvalid`, 94 MB, **0 invalid index debris**, role `statement_timeout` reset and the active job count back to its **99** baseline. **The plan flipped to `Index Only Scan`** (Heap Fetches 26,152): the CTE + join goes **841,312 → 56,859 buffers (14.8x)** and the whole function **927,466 → 142,625 (6.5x)** warm, on an identical 984-row result. ⭐ **The P0 is cleared, proven by running the thing that was broken**, not by a plan: `scripts/run-active-listings-ingest.ps1 -DryRun` from Trevor's box now **exits 0** and the targets phase returns **749 targets** where 30 minutes earlier it returned `500 canceling statement due to statement timeout` — and it reached Atlas (**6 calls, 0 skipped**), which no run had managed in a week, so the residential-egress arm is healthy too. ⏳ **Follow-up, not done in the same change:** the older `fmv_snapshots_2026_coll_ed_ct_fmv_idx` (120 MB, `INCLUDE (fmv_usd)`) is now a strict SUBSET of the new index and is a drop candidate worth 120 MB plus a hot table's 6th-index write amplification — it still shows 76,439 scans so it is live, and the new index can serve every one of them; **drop it CONCURRENTLY once production is observed using the new one.** ⭐ **THE HYPOTHESIS IS MEASURED AND THE INDEX SHIPPED — 2026-08-26.** `fmv_snapshots_2026_coll_ed_ct_fmv_conf_idx` (`INCLUDE (fmv_usd, confidence)`) restored the Index Only Scan: the CTE+join went **841,312 → 56,859 buffers (14.8×)** and the whole function **927,466 → 142,625 (6.5×)**, same 984 rows — full entry and the three falsified cheaper fixes are in the ledger (2026-08-26). ⚠ **AND THE RESIDUAL FAILURE MODE HAS MOVED, which changes who owns this item:** in the 24 h to 2026-08-27 02:20Z the pipeline recorded **9 runs — 5 ok / 4 `egress_blocked` / ZERO DB timeouts**, against the 29-of-40 DB timeouts that defined the item. **The DB half reads addressed; the binding half is now egress (#20).** ⓘ The GH workflow `TopShot Active Listings Ingest — underpriced` is correspondingly still red on `main` — that red is #20's, not this one's.

33. 🟡 **OPEN, TREVOR'S CALL — ISR bakes a failed read into the whole `revalidate` window, and `/insights/pack-drops` has no stale snapshot to fall back on (found 2026-08-24, registered 2026-08-25).** ⚠ **This was filed to `docs/overnight/inbox/` and left there for two days.** An inbox filing is a hypothesis; **the register is the tracked list**, so a decision item that never reaches it is one nobody re-reads — which is this file's own most-repeated failure mode applied to itself. **What is measured:** a COLD regeneration that exceeds the 8 s `BOARD_LIVE_TIMEOUT_MS` fails the page's read, and `export const revalidate = 900` then serves that failure for **up to 15 minutes** — observed live at `x-vercel-cache: HIT`, `age: 158`, while the API answered in **1.2 s** throughout. Warm the read is ~1.2 s (six samples: 11,529 cold, then 4,286 / 1,140 / 1,187 / 1,293 / 1,192). 🚨 **`pack-drops` passes `[]` as its fallback, not a snapshot** — so `BOARD_LIVE_TIMEOUT_MS`'s own comment, which justifies the budget as *"precisely when a stale-but-complete snapshot is the better answer"*, does not hold for this caller. **Enumerate which other boards pass an empty fallback before touching the constant.** ⚠ **It SELF-HEALS on the first warm revalidation, so it is easy to declare fixed by accident** — the honest test is *"does a COLD regeneration still exceed the budget"*, never *"is the page OK now"*. ⛔ **Three remedies are wrong and are named so nobody re-proposes them:** raising `BOARD_LIVE_TIMEOUT_MS` (shared by every board — trades every board's worst case for this board's rare one); retrying (the abandoned query keeps running server-side, adding load in exactly the window the budget protects); lowering `revalidate` (hits the cold path MORE, not less). ✅ **The two real options, both design decisions:** a per-caller budget (precedent: `SET_DETAIL_TIMEOUT_MS` on `/analytics/sets`), and/or giving the page a genuine stale snapshot. **Blast radius:** the page exists to put scored drops into the raw server HTML so the content is crawlable, so a cold-miss window serves a crawler a page with no drops on it. ⚠ **Every number here is a dated sample — re-run the six-sample distribution before quoting ~1.2 s.** The honesty half is FIXED and live (the page now says it could not load instead of claiming an empty market); **only the caching behaviour is open.** Filing: `docs/overnight/inbox/2026-08-24T1441Z-a-cold-isr-regeneration-can-bake-a-failed-read-into-15-minutes-of-cached-html.md`.

<!-- retired-rule:allow handoffs-are-iphone-pasteable -->
32. 🟡 **OPERATOR ACTION — the INSTALLED `rpc-cron-ops` skill is a pre-2026-06-19 export and is missing a secret-safety rule written after a real leak (found 2026-08-24).** `docs/cowork-skills/` keeps each skill twice: `<name>/SKILL.md` (the source) and `<name>.skill` (the zip that gets uploaded and installed). Both the repo source **and** the packaged bundle carry the **2026-06-19 HARD RULE** — *not opening the Advanced tab is NOT enough; the Authorization header is in the page DOM regardless, so never `querySelectorAll('input')`, never a full `read_page`, scope every DOM read to the crontab input and the minutes select* — written after `INGEST_SECRET_TOKEN` leaked exactly that way from Cowork. **The copy installed on the account does NOT have it** (4,232 bytes vs the repo's 4,823; measured by diffing the loaded skill against the repo). ⚠ **So a session that loads `rpc-cron-ops` to drive the cron-job.org console is running the pre-incident instructions.** ⛔ **Not fixable from the repo or the sandbox** — the installed copy lives in the account's synced skills. **Operator: re-upload `docs/cowork-skills/rpc-cron-ops.skill`** (verified 2026-08-24 to contain the rule), then confirm the loaded skill's byte size matches. ⚠ **Do NOT batch-re-upload every bundle as a shortcut** — until `9fb...`'s re-pack landed, `rpc-handoff.skill` was itself stale in the OPPOSITE direction and would have reinstalled the retired *"plain text, no code fences, copy-pasted from an iPhone"* rule; `npm run skills:bundles:check` now guards that direction and is a ban at population zero. ⚠ **The repo cannot see the installed copy at all**, so this item cannot be closed by a green guard — only by an operator check. ✅ **RE-VERIFIED 2026-08-25, STILL OPEN:** the installed copy is still **4,232 bytes** against the repo's **4,823**, and `grep` for the 2026-06-19 secret-safety HARD RULE in it still returns **0**. Two days on, a session loading `rpc-cron-ops` is still running the pre-incident instructions.

31. 🔴 **OPEN — the edge-fn drift detector's AUTHORITATIVE arm has never once produced a result, and every number it has ever published is Tier 1's lower bound (found 2026-08-24).** `scripts/check-edge-fn-drift.mjs` has two tiers. **Tier 1** proves drift by an argument that cannot false-positive (repo source uses a bare specifier, so it cannot resolve without an import map, while the deployed artifact reports `import_map: false` — that build could not have booted from current source). **Tier 2 is the CONTENT CENSUS** — fetch the deployed body, normalise, compare to repo source — and it is *"authoritative"* in the script's own words. ⚠ **Tier 2 has NEVER RUN. Measured on the 2026-08-24 07:41Z run: `38 body read(s) attempted, 0 succeeded`.** The cause is uniform across all 38 and is one line: `Unexpected token 'E', "ESZIP2.3"... is not valid JSON` — **the Management API's `GET /v1/projects/{ref}/functions/{slug}/body` returns an `eszip` v2.3 BINARY BUNDLE, and the script calls `res.json()` on it.** The docs (`v1-get-a-function-body`) document no response shape and offer no JSON alternative; `/body` is the only endpoint that serves source.

    ⚠⚠ **AND IT WAS SILENT ABOUT THIS UNTIL 2026-08-22.** `86163308` (*"tier 2 must report what it could not read"*) added the counters and the `TIER 2 DID NOT RUN` error. **Before that commit the failed body reads were swallowed and the census reported no content drift — a failed read rendered as an answer, committed by a detector against itself.** The two runs since (08-23, 08-24) carry the message; the twelve before it do not, **and that absence is the reporting's age, NOT evidence Tier 2 was working.** ⓘ The honesty fix worked exactly as designed — it made a two-week-old blindness visible on the first run after it landed. **Then nobody read the log for two days**, which is the same *"read the LOG, not the badge"* failure #23 already records.

    ✅ **WHAT THIS DOES AND DOES NOT CHANGE ABOUT #23.** Re-derived 2026-08-24: **DRIFT = 20** (was 25 on 08-22, 30 on 08-09). ⚠ **That series is Tier 1 ONLY, so it measures how many remain PROVABLE, not how many are broken.** A function redeployed *with* an import map leaves Tier 1's view **whether or not its source is current**, and the arm that would tell them apart is the dead one. **Checked, and the benign reading holds for the five that cleared 08-09 → 08-22** (`backfill-allday-pack-supply`, `backfill-pack-opens-api`, `backfill-topshot-pack-supply`, `compute-pinnacle-pack-ev`, `snapshot-institutional-wallets`): **all five now report `import_map: true` with plausible redeploy dates** (08-11, 08-11, 08-24, 08-15, 08-16), i.e. they were properly redeployed rather than merely slipping out of view. ➡ **So the decline is probably real remediation — but "probably" is the honest word, and it will stay that word until Tier 2 runs.** ⚠ **`backfill-topshot-pack-supply` was redeployed at 04:46Z on 08-24, three hours before that morning's run** — the count moves as redeploys land, so **re-derive it; never quote this 20.**

    🔑 **THE BLOCKER IS NOT WHAT THIS ITEM SAYS — RE-DERIVED 2026-08-24 (PT).** The bullet below reasons that *"a parser cannot be tested from Trevor's box without `SUPABASE_ACCESS_TOKEN`"*. ⚠ **`SUPABASE_ACCESS_TOKEN` IS SET in the shell on Trevor's box — and it is DEAD.** Correct shape (`sbp_…`, 44 chars), but it returns **HTTP 401 `{"message":"Unauthorized"}` on `GET /v1/projects`**, the cheapest Management API call there is. So the real blocker is **a token REFRESH, not the absence of one**, and that changes the item from "needs infrastructure we lack" to a **one-step operator action**: mint a fresh Management PAT and put it in the local environment, after which the eszip parser becomes testable against real bundles from this box and candidate (a) is shippable under the "do not ship untested" rule rather than barred by it.

    ⚠⚠ **AND THE PRESENT-BUT-DEAD STATE IS THE TRAP, not the absence.** A session that checks `[ -n "$SUPABASE_ACCESS_TOKEN" ]` — the obvious guard, and the one the workflow itself uses — concludes it is configured and proceeds, then meets a 401 in the middle of a script and is invited to misdiagnose it as a scope, endpoint or eszip problem. **Probe the token with a real request, never with a presence test.** ⓘ The CI secret of the same name is a DIFFERENT value and WORKS: the 08-24 07:41Z run reached 38 bodies and failed on the eszip parse, which is an HTTP 200 outcome. **Local-dead and CI-alive coexist under one variable name.** ✅ `scripts/check-edge-fn-drift.mjs:228` already prints *"The secret is configured but was refused. Rotate the PAT…"* on a 401, so the script handles this correctly — it is only this ITEM's stated blocker that was wrong.

    **Two candidate fixes, neither shipped:** (a) parse the eszip v2.3 bundle (the real fix, but it is a binary format with per-module compression, and a parser cannot be tested from Trevor's box without `SUPABASE_ACCESS_TOKEN`, so **do not ship an untested parser straight into a CI guard**); (b) use `ezbr_sha256` from the LIST endpoint — free, already returned, and enough to detect *when a deployed function changes*, but **it cannot compare deployed against REPO** because the bundle build is not reproducible locally, so it is a change detector and not a census. ⛔ **Do not "fix" this by deleting the Tier 2 call or defaulting it to clean** — the counters and the `ran` positive control are the only reason this was findable at all. ⚠ Reading the body **at all** is a secret-handling surface: a deployed function can carry a gate key, and this output goes to a CI log. The script already prints **messages only, never bodies** — keep it that way (same rule as the `get_edge_function` leak in `tooling-gotchas.md`).


34. 🔴 **OPEN — OPERATOR + a written patch. Sentry has accepted nothing since 2026-08-18T13:21:59Z because the ORG ERROR QUOTA IS EXHAUSTED, and ONE already-tracked signature burned it (settled 2026-08-25).** ⭐ **Proven by a single request, after three filings audited the emitters instead of the collector:** one POST of a minimal envelope at the production DSN answers `429` with `x-sentry-rate-limits: 60:default;error;security;attachment:organization:**error_usage_exceeded**`. ⛔ **The premise that kept this open for seven days is FALSE** — the filings recorded that the sandbox *"gets the agent proxy's 403, not Sentry's"*; ingest **is** reachable from the Cowork cloud container, and the empty-body control (`400 empty request body`) proves the key is live and accepted. Every app-side hypothesis (DSN, SDK, env var, bundler, egress) is eliminated, not merely unlikely. ⭐ **The burn is one signature:** `edition detail unavailable: rpc get_edition_detail timed out after 45000ms` threw **15,388 times across 2,963 distinct users in ONE week**, so a single known-broken RPC consumes a monthly error budget in ~2 days — **raising the quota alone changes nothing.** **Written, NOT pushed:** `lib/observability/sentry-quota-guard.ts` samples that signature 1-in-20, keeps **DEFAULT IS SEND** for everything unrecognised, stamps `sentry_sample_rate` on kept events so an issue count still reads back to an incidence, and leaves the drops visible in Sentry's own client-report stats; four mutations run against its test, all caught. It also sets `environment`/`release` on the three LIVE inits — previously set only in the dead `sentry.client.config.ts`, so **no event on any runtime has ever been attributable to a deploy**. ⛔ **Session replay deliberately NOT added** while quota is the binding constraint. **Operator half (Trevor):** Sentry → Settings → Subscription — raise the plan or enable an on-demand budget, *with* the guard, not instead of it. ⚠ **And nothing watches this:** a collector that has been dropping for a week looks exactly like a quiet week. An arm on "Sentry accepted an event recently" is now worth building, because the quota is a known recurring failure mode. Filing: [inbox 2026-08-26T0525Z](../overnight/inbox/2026-08-26T0525Z-SETTLED-sentry-is-not-broken-the-org-error-quota-is-exhausted-and-one-signature-burned-it.md). ✅ **DECIDED 2026-08-26 — TREVOR: DO NOT RAISE THE SENTRY SUBSCRIPTION**, consistent with the roadmap's *no infra spend pre-revenue*. **This item is no longer waiting on an operator payment**; what shipped in its place, same day, is (a) the quota guard extended to `pg-statement-timeout` (≈3,818 thrown events in 7 d from two signatures, ≈16,400/month — several times a 5,000/month quota on its own) and (b) the removal of `global-error`'s **“Our team has been notified”**, false since 2026-08-18. ⚠ **THE RESIDUAL GAP IS CLIENT-SIDE AND IT IS NOT THEORETICAL** — Vercel sees only server execution, there is no non-Sentry client capture in the repo, and on 2026-08-26 the scheduled `E2E DOM Smoke` monitor caught a **React #418 hydration mismatch in production**, which is exactly the browser-only class a server log cannot contain. **Registered as #37.** ⏳ Still unbuilt and still worth building: a watcher for *“did Sentry STORE anything recently”* — a dropping collector looks exactly like a quiet week, which is how this went 8 days unnoticed.

    ⭐ **AUDITED 2026-08-27 FROM A READ PATH THE FILINGS SAID DID NOT EXIST — and the guard checks out at 93.8%.** The standing conclusion was *"blocked on a read scope, not on money"*, from a correct probe showing `.env.sentry-build-plugin`'s token is upload-scoped (three real 403s). ⭐ **That proved the TOKEN was dead, not that the CAPABILITY was unreachable: a Cowork session has a Sentry MCP connector with org read access**, and it answered on the first call. ⚠ **Narrow claim** — it gives no token to production, so the in-app watchdog above is still unbuilt and a read-scoped token is still worth creating; and it may be absent in headless/scheduled runs. **What it gives is an on-demand audit path.** ✅ **First result: the blackout start is now confirmed by a SECOND independent instrument** — Sentry's newest stored error event is **`2026-08-18T13:21:59+00:00`**, agreeing to the second with the figure previously derived only from an ingest `x-sentry-rate-limits` header; and every one of the top 20 issues has `lastSeen` 8–11 days, so **the blackout is total, not partial**. ✅ **Second result: `sentry-quota-guard.ts` audited against the store it was NOT sized on.** Over 5,336 stored events (errors dataset, 30 d, ≈21 d of real data): `rpc-deadline` **3,427 (64.2%)**, `pg-statement-timeout` **1,578 (29.6%)**, **covered 5,005 = 93.8%**, uncovered 331 = 6.2%. At the shipped `rate: 0.05` that window becomes **~581 events, an 89.1% reduction**, with every unrecognised error still at full rate — and the `pg-statement-timeout` rule, sized entirely from Vercel runtime errors, lands in the same order as its Sentry share. ⛔ **The uncovered 6.2% should STAY uncovered:** 3.3% is the same DB degradation wearing generic messages (`TimeoutError: … aborted due to timeout`, `Could not query the database for the schema cache`, `Timed out acquiring connection from connection pool`) and **widening a substring test to catch it moves probability mass toward silently sampling a NOVEL error, which is the one property the guard exists to protect**; 2.8% is the smoke-test self-reports, i.e. the honesty layer working. ⚠ **The guard's effect has NEVER been observed** (nothing stored since 08-18). **Falsifier when the quota resets: stored events for these signatures MUST carry `sentry_sampled_signature` / `sentry_sample_rate`. If they do not, `beforeSend` is not wired into the runtime that produced them** — a live risk, since `sentry.client.config.ts` is already documented as never bundled by production's turbopack build. ⚠ **One figure in the guard's own header does not reconcile and must NOT be forced:** it cites *15,388 events in one week* for the edition signature where Sentry's store shows **2,738** across 30 days — different instruments over different windows (the 15,388 was measured 2026-08-23, after storage had already stopped, so it cannot be a Sentry count). The rule is a 5% sample either way. Filing: [inbox 2026-08-27T0400Z](../overnight/inbox/2026-08-27T0400Z-the-sentry-read-scope-is-NOT-blocked-here-and-the-quota-guard-checks-out-at-93.8pct.md).

35. 🟡 **PARTIALLY SHIPPED — two pack-sales backfills were 71.9 GB/day of disk reads (~9.2% of the instance) and 4.55 GB/day of WAL, to add ~165 rows a day (found 2026-08-25).** `topshot_pack_sales_history` reads **3,558 inserts against 14,903,768 updates**; `allday_pack_sales_history` **106 against 9,956,771**. Cause: PostgREST's default `ON CONFLICT (…) DO UPDATE SET <every column> = EXCLUDED.<column>` carries **no change-detection predicate**, so every re-walk of immutable historical sales rewrites every row identically. ⭐ **The bigger half is not the upserts but two `SELECT … LIMIT $1 OFFSET $2` statements at 19,183 and 16,587 disk blocks PER CALL** — "OFFSET does not paginate", re-reading whole tables ~200x/day each. ✅ **SHIPPED:** pg_cron jobids 25/29 cut from a 3-minute to a 15-minute cadence, offset by a minute (480 → 96 dispatches/day each); not a freshness regression, since the newest rows were already 3.4 h and 15.3 h old. ⛔ **The real fix — `suppress_redundant_updates_trigger()` — is BLOCKED, and the blocker is R21, not the trigger:** a suppressed row emits no `RETURNING`, PostgREST's `page_total` falls, and **both writers are edge functions with no committed source**, so a caller asserting on that count would break silently and invisibly for days. **Committing those two functions unblocks it.** ⚠ **Falsifier, to be RUN:** over 24 h `n_tup_ins` should hold (topshot ~160/day, allday ~5/day) while `n_tup_upd` falls ~5x — **if INSERTS fall too, the walk was covering ground and the cut is wrong.** Revert: `cron.alter_job(25, schedule := '*/3 * * * *')`, `(29, '1-58/3 * * * *')`. Filing: [inbox 2026-08-26T0635Z](../overnight/inbox/2026-08-26T0635Z-SHIPPED-two-pack-sales-backfills-were-72GB-a-day-of-disk-reads-for-165-new-rows.md). ⛔ **THE CADENCE CUT WAS REVERTED 2026-08-26 — this item's shipped half is OFF, and the revert is CONFIRMED.** The cut starved head freshness and the handoff's own falsifier caught it; `cron.alter_job(25, '*/3 * * * *')` / `(29, '1-58/3 * * * *')` are back. Verified ~11.6 h after the revert: **149 head rows ingested**, newest `block_time` ≈40 min old at ingest, walker actively cursoring. ⭐ **The mechanism is LAP TIME, not tick count** — the walk phase is ~4.2 h + a ~5.5 h park at `done = true` (a **phase, not a defect** — that was flagged as a second defect after 54 minutes of observation and falsified when it self-restarted on schedule), so a 5× cut would have pushed the walk alone to ~21 h and the cycle past a day. **The WRITE-AMPLIFICATION half of this item (the `DO UPDATE SET <every column>` shape) is untouched and still open.**

36. 🔴 **OPEN, PUSH-GATED — `refresh_wmc_fmv_changed` is 40.2% of every block this database dirties, and its lever is a 13 MB table that already exists (measured 2026-08-25).** Across its two callers: **10.0% of disk reads, 40.2% of blocks dirtied, 37.2% of WAL, 11.0 h/day of exec time** — larger by itself than the whole pack-sales cluster in #35. ⛔ **The obvious lever is FALSIFIED and is recorded so nobody spends an afternoon on it:** `v_chunk constant integer := 5` is conspicuous and commented, and is **not** the cost — the loop's working set is only **515 rows**. ⭐ **The cost is a correlated latest-FMV subquery carrying no partition key**, so it `Append`s across every `fmv_snapshots` partition (707 MB), reading ~64 rows to return 1, **~147,000 times a day**. **`public.edition_fmv_current` already exists** — a real 13 MB table keyed on `edition_id`, refreshed within minutes — so the fix is one CTE: `COALESCE(efc.fmv_usd, <the existing subquery>)`. 🚨 **AND THE FIRST FORM OF THAT FIX WAS WRONG — corrected in the same session, which is the part worth reading.** A random hash sample of 274 editions found **zero value disagreements**, which appeared to license a bare `COALESCE`. ⛔ **That is the wrong population.** Measured on the population the function actually serves — **697 editions whose latest snapshot landed inside the 30-minute cutoff** — `edition_fmv_current` **LAGS on 33 of them (4.7%), by up to 7.06 DAYS**: $74.00 published as $49.00 (−34%), $11.00 as $4.50 (−59%), $117.00 as $163.00 (+39%). Those would have been written straight into `wallet_moments_cache.fmv_usd`, **a displayed price**, and the function's own `IS DISTINCT FROM` guard would not have caught it — stale and correct are both "distinct from what is there". This vindicates the standing memory rule that `edition_fmv_current` is for **ordering and bulk aggregation only, never a displayed price**. ⭐ **The fix survives, FRESHNESS-guarded rather than NULL-guarded:** `_rwfc_recent` already carries the snapshot's `computed_at`, so `RETURNING edition_id, computed_at` plus `LEFT JOIN … AND efc.computed_at >= p.computed_at` takes the fast path **only** when the cached row is at least as fresh as the change being propagated — **95.3% fast path, zero staleness surface**, provably correct rather than merely better. ⭐ **Transferable: an equivalence proof is only as good as the population it was taken over — sample what the CODE touches, not what the TABLE holds.** And a recorded decision that contradicts your measurement is not automatically stale; measure what it was protecting. ⚠ **Expect the READ figure to fall sharply and dirtied/WAL much less** — the `wallet_moments_cache` UPDATE already has a change-detection guard and is doing real work; anyone quoting a 40% write saving after shipping this will be wrong. ⛔ **Not shipped because the function carries a DB-invariant pin** (`supabase/tests/refresh_wmc_fmv_changed.sql`) and re-pointing a pin requires a commit. ⭐ **Generalised, and now in CLAUDE.md: a PINNED SQL function is PUSH-GATED — a no-push session's real DB levers are pg_cron schedules, indexes, and brand-new objects.** Same filing carries three siblings: jobid 303 and `/api/wmc-fmv-populate` drain the SAME `rwfc_state` queue on two independent schedules, and four `ON CONFLICT DO UPDATE`s have no change-detection predicate (`upsert_pack_ask_state` spends **385 MB of WAL a day maintaining a 4 MB table**). Filing: [inbox 2026-08-26T0645Z](../overnight/inbox/2026-08-26T0645Z-refresh-wmc-fmv-changed-is-40pct-of-all-writes-and-the-lever-is-a-13MB-table-that-already-exists.md). ✅ **SHIPPED 2026-08-26 (prod `CREATE OR REPLACE` + committed migration + re-pinned invariant) — but read the next sentence before quoting a saving.** The fast path is live (`refresh_wmc_fmv_changed` reads `edition_fmv_current`, verified in live `prosrc` 2026-08-26) and **CORRECTNESS IS PROVEN over the population the function actually serves**: fast path taken **3,439 / 4,028 (85.4%)**, **0 disagreements with the incumbent**, **28 stale rows correctly REJECTED** by a FRESHNESS guard (`efc.computed_at >= p.computed_at`) — a bare `COALESCE` onto the cache would have written those 28 stale prices, off by as much as −59% / +39%, straight into a DISPLAYED portfolio total. ⛔ **THE COST CLAIM IS NOT ESTABLISHED AND MAY BE FALSE:** at 11.4 h, disk reads per call went **UP** for both callers (pg_cron 73,704 → 86,533; PostgREST 7,045 → 10,472). ⚠ **That comparison is CONFOUNDED — a 14.5-day lifetime average against 11.4 h of index builds, drops and 900k-buffer EXPLAINs during a saturation spell — so it is not evidence either way, and it is not being reverted on it.** ⏳ **A clean baseline is laid and the decision is scheduled, not open-ended:** rows `rwfc_cron_T1_CLEAN_20260827` / `rwfc_rest_T1_CLEAN_20260827` in `public._rpc_waste_baseline_20260825`, captured 2026-08-27 02:05Z after the index work settled — **re-read in a quiet window ≥24 h out; if reads are still not below 74,159 / 7,195 per call, revert** (`CREATE OR REPLACE` back to `20260822213000_audit_20260822_rwfc_temp_build_materialized_cte.sql`, pin + PINS entry move with it). **`_rpc_waste_baseline_20260825` therefore STAYS until that decision is made.**

37. 🔴 **OPEN, NEW 2026-08-26 — `/insights/underpriced-serials` throws React #418 (hydration mismatch) IN PRODUCTION, and the only instrument that can see it is a scheduled monitor nobody reads.** `E2E DOM Smoke` (`e2e/smoke.spec.ts:168`, via `assertHealthyPage`) failed on **two consecutive scheduled runs — 2026-08-26 13:37Z and 21:09Z — on all three retries each time**, 1 failed / 95 passed, so it is **deterministic within a run, intermittent across the day** (07:28Z, 04:03Z and 02:11Z the same day were green). ⭐ **THE MECHANISM IS IDENTIFIED IN THE CODE BUT NOT YET PROVEN BY AN INSTRUMENTED REPRODUCTION, and the distinction matters here.** `app/insights/underpriced-serials/UnderpricedSerialsBoardClient.tsx:341` computes `listingsAgeHours` from **`Date.now()` inside a `useMemo` during render**, and line 391 renders it two ways — a `>= 4` branch and a `Math.round(listingsAgeHours)h ago` caption. The page is a `"use client"` component SSR'd with `initialRows` under **`export const revalidate = 900`**, so the server HTML is baked up to 15 minutes before the browser hydrates: **whenever the rounded hour ticks over (or the ≥4 h branch flips) between bake and hydration, the two HTMLs disagree by construction.** That predicts exactly the observed pattern — same page, same signature, some runs green — but it is a mechanism, not a measurement. **Discriminator, cheap:** load the page twice inside one rounded-hour bucket and once across the boundary and watch the console; or pin the age at a constant and re-run the monitor. ⚠ **THIS IS #34's RESIDUAL GAP MADE CONCRETE:** a hydration error is client-only, Sentry has been dropping events since 2026-08-18, and Vercel's error board structurally cannot contain it — **the E2E monitor is the entire detection surface, and it is a scheduled workflow whose red badge nobody is alerted on.** ⛔ **Do not “fix” it with `suppressHydrationWarning`** — that silences the instrument and leaves the two DOMs disagreeing. The honest fixes are to compute the age in an effect after mount (render nothing for it server-side) or to pass a server-stamped age down as a prop so both sides render the same string.

    ✅ **FIXED IN CODE 2026-08-26 (same session), and the mechanism is now MEASURED rather than inferred.** ⭐ **The measurement that settled it:** the served production HTML carries its own server stamp, and at 2026-08-27 02:40Z that stamp read **00:12:06Z — 2.5 hours earlier**. **So the server/client clock gap is NOT the 900 s `revalidate` window** — Next serves the stale cached page while regenerating, and on a near-zero-traffic site that HTML can be hours old. Any threshold or rounding boundary between the two renders is a guaranteed mismatch, which is why the observed error was `args[]=HTML` (**structural** — the caption element exists on one side and not the other) rather than `args[]=text`. ⛔ **A first hypothesis was CHECKED AND FOUND INSUFFICIENT before shipping:** the 21:09Z failure could not be explained by the ingest cadence alone (last success 19:13Z ⇒ spine 1.9 h old ⇒ the `>= 4` branch is false at hydration), and it is the *staleness of the cached HTML* that closes the gap. **Do not re-cite the cadence as the cause.**

    **The fix is the house two-phase pattern, not a suppression:** `nowMs` is anchored to the `initialFetchedAt` PROP for the server render and the first client render, then swapped to the live clock in a post-mount effect — identical in shape to `TopSalesBoardClient` (fixed for the text flavour of this class 2026-08-16) and `components/insights/FreshnessStamp`. The caption therefore **stays in the served HTML** for crawlers and no-JS readers, which `suppressHydrationWarning` would have kept only by silencing the instrument.

    ⭐ **AND THE GUARD THAT SHOULD HAVE CAUGHT THIS WAS GREEN — that is the durable half.** `insights-client-dates-are-hydration-safe-guard` polices date *FORMATTING* (Rule A: runtime timezone, Rule B: runtime locale) and is structurally blind to *reading the wall clock*, which needs no formatting API at all. **Rule C added**: a ban on unmarked `Date.now()` / no-arg `new Date()` in insights client files (population **0 unmarked** after this pass), plus a site-wide ratchet at **57**. Measured while adding it: of the four clock reads in the insights roots, **exactly one was the defect** — the other three are correct (a mount-gated call site, a post-mount-only fetch, and the swap effect itself) and now carry an inline `hydration-safe: <reason>` marker. Both the guard and the new component test are **mutation-checked against the pre-fix source**: the ban + ratchet red, and 4 of the 6 behavioural cases red.

    ⏳ **NOT YET VERIFIED IN PRODUCTION, and it cannot be from a sandbox** — egress to `www.rippackscity.com` is blocked at the proxy (403 on CONNECT), so no browser here can hydrate the live page. **The verification is the monitor: `E2E DOM Smoke` must go green across several scheduled runs after this deploys**, and because the failure is intermittent, ONE green run proves nothing. Re-check the badge, not the page. ⏳ **STATUS 2026-08-27 03:10Z: deployed and serving** (the page carries the new deployment id and regenerated at 03:02:33Z), **CI green, and a dispatched monitor run is GREEN — the first after two failures.** ⚠ **Do not close the item on it:** the spine was ~1.5 h old at run time, so the `>= 4 h` branch was false on both sides and **the pre-fix code would have passed the same run.** It proves no regression. **Close this only after scheduled runs stay green through a window where the spine is stale.**

38. 🟡 **OPEN, PROMOTED FROM THE INBOX 2026-08-26 — `topshot-pack-pool-backfill` fails on ~99.6% of ticks and has since at least 2026-08-25; it was filed but never reached this register.** 24 h to 2026-08-27 02:00Z: **274 runs, 1 ok, 273 failed** — **258** with the synthesized `0/3 dists converted; 3 returned no editions` and **15** with `targets: canceling statement due to statement timeout`. ⭐ **The dominant error is the pipeline working as designed:** that string was deliberately synthesized (ledger 2026-08-23) so a wedged queue could never report as a silent success — **it is the correct report of a queue with nothing convertible in it, not a crash.** ⚠ **But two things make it a real item rather than a noisy log.** (1) The 15 statement timeouts are a *different* fault sharing the same badge, and a 94%-of-failures signature hides a 5% one — the same class as #29's two 503s. (2) A pipeline whose steady state is 99.6% failed is a **permanently-red instrument**, and this file's own standing rule is that those become unreadable and train people to skim (#25, `series_detail_rollup`). **Either the queue has a genuine upstream supply problem (jobid 16 dispatches every 5 min into a queue with 3 unconvertible dists) or the cadence is wrong for the work available.** ⛔ **Do not suppress the arm to clear the red** — measure why the 3 dists return no editions first. Filing: [inbox 2026-08-25T0312Z](../overnight/inbox/2026-08-25T0312Z-pack-pool-backfill-error-shifted-from-403-to-a-200-level-no-editions.md).

    ⭐ **PARTLY ANSWERED WITHIN THE HOUR by a concurrent session (`fe623dd1`, 2026-08-26), and the answer was a THIRD option this entry did not offer.** I framed it as *"either a genuine upstream supply problem or the wrong cadence"*. It was neither: **the sampler never rotated.** Targets are chosen by `NOT EXISTS (a row in pack_drop_pool)`, and a dist whose GQL walk SUCCEEDS with zero editions writes no pool row — so it stays eligible forever — while ~350 eligible rows share one `first_seen_at`, making the `ORDER BY` a tie broken by stable physical order. **The pipeline asked about the SAME three dists 808 times in 72 h against a 710-dist backlog.** ⛔ **So this entry's own headline number carried no information about the backlog**, and the daytime monitor's "3 permanently-unresolvable dists" was an artifact of the wedge, not a finding — the fix is `ORDER BY` only (eligible-set md5 identical before and after, 710 rows both times). ⭐ **The lesson I got wrong is worth more than the one I got right: a 99.6% failure rate over a sample that never moves is not a measurement of the population.**

    🟡 **STILL OPEN, and narrower:** the **15 `canceling statement due to statement timeout`** failures are a separate fault that the dominant signature still hides, and the rotation does not address them. **Re-measure the conversion rate after a full rotation cycle** — that number is now an actual measurement of the backlog, which it was not before.

39. 🟡 **OPEN, TREVOR'S CALL — the PUBLIC `/insights/underpriced-serials` API **503s**, its mean is **5,092 ms across 550 real production calls**, and the fix is a judgement about the cron budget, not a diagnosis (measured 2026-08-26, re-measured 2026-08-27).** `GET /api/public/insights/underpriced-serials` returned **503 after 62 s, reproduced twice ~10 min apart**; `?limit=5` returned 200 in 18 s (`x-vercel-cache: MISS`, `meta.elapsed_ms = 17267`). ⚠ **`limit` is a red herring** — `fetchUnderpricedSerials` always issues `.limit(500)` and applies `opts.limit` in JS afterwards, so both URLs run the *identical* query; the two outcomes are cache states, not a limit effect. ✅ **The honesty layer is correct**: the route returns `boardUnavailable()`, so a failed read surfaces as a 503 and not as an empty deals board. ⚠ The PAGE returns 200 from `PRERENDER` in ~1 s — the user gets a shell that then fails to populate, which is why this was invisible.

    ⭐ **The measurement that reframes it, from `pg_stat_statements` (window: reset 2026-08-12 01:34Z → 15.05 days; ⚠ a dated sample):** **550 calls / 36.5 per day, mean 5,092 ms, max 23,236 ms, 302 disk reads PER CALL** (166,636 total). Timed in one `DO` block, the same statement cold then warm: **19,895 ms → 32 ms, 622×**, on a working set of **3,069 buffers (~24 MB) / 230 disk reads**. At this instance's ~74 ms per cold random read, 230 × 74 ms ≈ 17 s — which is the observed number. **There is no plan defect and nothing to optimise.** At one call per 40 minutes the working set is evicted between every pair of requests, so essentially no call is ever warm. ⛔ **NOT caused by the 2026-08-26 index work — checked, not assumed:** the FMV leg uses the new index and is an `Index Only Scan`; it is 1,499 of the 3,069 buffers; the dropped index was a strict subset of a surviving one, so no plan lost an option; and the board's input held 280 active rows before that day's ingest and 272 after.

    ⭐ **THE REFRAME THAT DECIDES IT: this is a LATENCY problem, not a throughput one.** 302 × 36.5 ≈ 11,000 disk reads/day ≈ **86 MB against the instance's ~780 GB/day** — negligible. **The defect is not that the board is expensive; it is that its entire cost is paid by a user, one page load at a time, at a 5.1 s mean.** Every contention argument in the [2026-08-15 warm-cron filing](../overnight/inbox/2026-08-15T1200Z-the-insights-cache-warms-half-its-boards-and-reports-perfect-health.md) is about *work*, and this board contributes almost none — which is why that filing's conclusion does not automatically transfer here even though its warning does.

    ⛔ **TWO REMEDIES REFUTED — do not spend a night on either.** (1) **`VACUUM` cannot fix this.** The FMV leg reports `Heap Fetches: 289` of 289 rows, so the index-only scan delivers none of its benefit — but `fmv_snapshots_2026` is **96.2% all-visible** (`relallvisible` 26,059 / `relpages` 27,080), autovacuumed 8 h earlier. The board reads the **latest** snapshot per edition, and of the 273 tuples it lands on, **273 of 273 were written in the last 24 h** (oldest 12 h, newest 4 minutes). ⭐ **Transferable rule: an `Index Only Scan` that selects the NEWEST row per key can never stop hitting the heap, however well-vacuumed the table is — the visibility map lags the write head by definition, and `relallvisible` percentage is the wrong instrument for that query shape.** ⓘ Contrast the 2026-08-25 `sales_2026` case where `VACUUM (INDEX_CLEANUP OFF)` *did* pay — that scan was over a 90-day window, not the head. (2) **`edition_fmv_current` is not the escape either, by this repo's own rule** — that 13 MB table is for *ordering and bulk aggregation only, never a displayed price*, and on the recently-changed population it lags **4.7% of editions by up to 7.06 days** ($74.00 published as $49.00, $11.00 as $4.50). This board renders `edition_fmv_usd` and derives `discount_pct` from it: a displayed price on a *deals* board. ⚠ **And the freshness-guard that rescued `refresh_wmc_fmv_changed` does not transfer** — there the caller held the snapshot's `computed_at` to compare against; here there is nothing to compare to.

    👉 **THREE OPTIONS, and the third is the one the original filing did not consider.** (1) **The light buffer warmer** (`/api/cron/warm`, `*/10` business hours, `maxDuration = 30`) — lowest blast radius, but its first tick pays the ~20 s cold cost against a 30 s ceiling. (2) **The snapshot cache** (`WARM_BOARDS` + a `fetchUnderpricedSerialsDefault` builder + `readBoardOrLive`) — strictly better for users, but ⛔ **that cron already fails 59.5% / 54.2% / 51.0% of ticks on three of its five boards, its six board means sum to ~60.0 s = the route's entire `maxDuration`, and each failed warm burns a full 30 s of DB time producing nothing.** Adding a sixth board to a `Promise.all` losing three of five is a plausible way to make it lose four of six. (3) ⭐ **MATERIALIZE IT — options 1 and 2 both keep the board a view and argue about who pays to keep it warm; neither considered removing the recomputation.** The result is **7 rows** — an MV of it is a single page. **A refresh is 35.7 ms / 3,415 buffers / ZERO disk reads warm** (measured on a genuinely quiet instance: 1 active, 0 IO waiters), so the refresh *is* the buffer warmer rather than an addition to one. **And the freshness side of the trade is empty:** the board's input `topshot_active_listings` is fed by a residential Scheduled Task on a **PT3H** cadence (#30), so a 15-minute MV is *never* the binding staleness term. Precedent is exact — the `deals` board was materialised 2026-08-22, **12,905 ms → 1.98 ms**.

    ⚠ **Three things option 3 must get right, and they are why it was not shipped unilaterally:** an MV cannot carry `security_invoker`, so grants must be set explicitly and diffed (the `mv_panini_squeeze` rebuild came out `anon=rxm` on 2026-08-23); it adds a job to a scheduler already starved for workers (`max_worker_processes = 6` vs `cron.max_running_jobs = 32`); and it is a public **pricing** surface. ⚠ **The core prediction is labelled as one:** *"the refresh keeps the set warm so the 302 cold reads disappear"* is inference, not measurement. **Falsifier: after materialising, `disk_reads_per_call` on the board query should collapse toward single digits and the MV refresh's own `shared_blks_read` should be small and bounded. If the refresh itself keeps paying ~300 cold reads, the warm-loop assumption is wrong and option 3 is no better than option 1.**

    ⚠ **One confound recorded rather than reported as good news:** a live probe during the re-measurement returned 200 in ~1.0 s then 0.57 s. That was **my own `EXPLAIN (ANALYZE)` two minutes earlier warming exactly those buffers**, not a recovery. The 550-call distribution is the honest instrument. ⓘ The OG-card residual this filing originally flagged is **already closed in `d2101312`** — re-derived, not assumed. Filing: [inbox 2026-08-26T1630Z](../overnight/inbox/2026-08-26T1630Z-the-underpriced-serials-board-503s-in-public-and-it-is-32ms-warm.md). ⓘ Distinct from **#37**, which is the React #418 hydration error on the same page — same URL, unrelated fault.


40. 🟡 **OPEN, NEW 2026-08-26 — the candy listings sweep is FIXED and now visibly TRUNCATED: it spends its whole 240 s budget on listings and sees `activities_seen: 0` where it used to see 1,000.** `f11fe69` ended a 44.8-hour public outage on `/insights/candy-mlb` by time-bounding two unbounded `fetch()` calls, and it added `SWEEP_BUDGET_MS = 240_000` so a truncated sweep **reports itself** rather than being killed invisibly at `maxDuration = 300`. ✅ **Verified 2026-08-27 03:41Z:** first terminal `pipeline_runs` row in >24 h, `ok = true`, **1,100 found / 1,100 upserted**, `candy_listings` **44.8 h stale → 1.7 minutes**. ⚠ **But read the same row's other fields.** That tick: **252 s**, `budget_exhausted: true`, `sweep_complete: false`, `pages_walked: 11`, **`activities_seen: 0`**. The three pre-outage successes (08-24 ×2, 08-25) logged `sweep_complete: true` and **`activities_seen: 1000`** at durations of **376 / 389 / 391 s**. ⭐ **So the route can no longer do inside 300 s what it used to do in ~390 s** — the listings walk alone exhausts the budget and the activities loop breaks on the same clock (`route.ts:440`). ⛔ **This is NOT a reason to loosen or remove the timeout** — the unbounded fetch is what caused the 44-hour outage, and an invisible kill is strictly worse than a visible partial. ⛔ **And it is NOT a reason to merge the batched mint-resolution diff:** at the upstream's measured **1.39 ms** per `wallet_moments_cache` lookup, 1,100 listings is **~1.5 s of 240 s — 0.6%**, and the tempting rescue (*"1.39 ms is execution time, a real round trip is ~30 ms, so 1,100 × 30 ms ≈ 33 s"*) is the **identical** assumed-latency × operation-count error retracted the same night. 👉 **The question is capacity and it needs one measurement, not an argument: add per-phase timing (ME fetch vs. mint resolution vs. upsert) and read a single tick.** Only then is the choice between raising `maxDuration`, re-splitting the budget, or accepting a rolling partial sweep an informed one. ⚠ **One tick cannot size this** — re-read `sweep_complete` / `activities_seen` across a day before deciding anything. Filings: [inbox 2026-08-27T0330Z](../overnight/inbox/2026-08-27T0330Z-RETRACTION-i-fit-the-constant-to-the-answer-on-the-candy-indexer.md).


### Resolved (verified 2026-05-23)

- **fmv-recalc silent stall — RESOLVED 2026-05-25 (`dd84526`).** Stalled 2026-05-24 22:03 → 2026-05-25 14:53. Root cause: unchunked `.in("edition_id", …)` in `/api/fmv-recalc` Step 3 + the Step 2a-bis meta fetch blew past PostgREST's URL cap on a ~1,100-edition page; supabase-js surfaced it as a non-throwing `deleteError` and `if (deleteError) return` exited `after()` before `log_pipeline_run` — route crashed silently inside the lambda while `topshot-listing-cache` still logged `fmv_recalc_called: true`. Fix chunked both `.in()` sites at 500 (matching the file's other 3 delete sites) and added `log_pipeline_run` to the fatal-catch + Step 3 error paths so future silent stalls surface in `pipeline_runs`. Cron and chain were never at fault.
- **#2 Sentry error capture** — `NEXT_PUBLIC_SENTRY_DSN` confirmed set in Vercel env + redeployed. SDK wired (org `rip-packs-city`, project `javascript-nextjs`).
- **#3 Flowty event indexer "regression"** — not a bug; Flowty's marketplace shut down (see Platform changes above).
- **#5 AllDay/UFC mis-categorized editions** — only 8 stray editions remain under the TopShot collection_id (all `disney_pinnacle`), not ~454. Effectively resolved.
- **#6 WarmupContext key mismatch** — `WarmupContext.tsx` now prefetches `/api/packs` into the key `PackPageClient` reads.
- **#13 `flowty_archive` growth** — resolved (option-B prune + `VACUUM FULL`; total DB 13.8 → 6.5 GB).
- **#16 `flow test` CI gating** — `.github/workflows/ci.yml` gates `tsc` + the Cadence harness. The `cadence-lint` job's missing `flow dependencies install` step was fixed 2026-05-22; it runs `continue-on-error` (non-blocking) pending a confirmed green run.

---

### Displaced from CLAUDE.md 2026-08-23 (verbatim) — the sports-proxy 403 bullet

Moved here to pay for two new standing rules; nothing deleted. The one-line pointer in CLAUDE.md now
carries only the verdict.

> - **The sports-proxy `403` — NOT a secret, and ⛔ "PROXY ESPN" IS MEASURED DEAD.** ⚠ **"ESPN: 200 residential ⇒ proxy it" was a DATED SAMPLE and it EXPIRED — re-measured 08-22, ESPN 403s residentially too**, so a CF Worker is just another egress. ⛔ UA-refresh and 403-retry stay useless. ⚠ Restoring ESPN alone still would NOT refill `nba_players` (SLATE-GATED). ⚠ **The "no alert" gap is a MYTH** — the failure-rate arm reads 100%, deliberately SUPPRESSED to 2026-10-14.
