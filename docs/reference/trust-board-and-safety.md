<!-- Extracted from CLAUDE.md on 2026-08-17 to bring that file under the memory-file
char limit. Content is VERBATIM; CLAUDE.md carries a one-line pointer to this file.
Same rules apply: every number here is a dated sample - re-measure before quoting. -->

## ⭐ RE-DERIVED LIVE 2026-08-24 ~15:35Z (08:35 PT) — the precompute census, and the two 999s are STILL the inconclusive branch

⚠ **Every number below is a dated sample taken in one minute. Re-read `rpc_trust_health_precompute`; do not
quote this.** Read from the precompute table, deliberately **not** from `v_rpc_trust_health` — this file's own
rule is that the view blows the 60 s MCP budget under saturation, and there was no reason to spend that.

**Census: 19 metrics, 19 distinct**, newest 0.39 h old, **oldest 11.75 h**. The 19 matches the
`v_rpc_trust_health_freshness` row count recorded for 2026-08-16, so the precompute surface has not changed
shape in eight days.

- 🚨 **`public_board_empty_count` = 999 and `public_board_slow_count` = 999, at `duration_ms` 89 and 93.**
  ⭐ **This file's own discriminator settles it without a second query: 77/85 ms was called "far too fast to
  have probed anything" on 08-17, and 89/93 ms is the same order.** So this is the **deliberate
  `budget_exhausted` branch — "incomplete sweep is INCONCLUSIVE, not green" — and NOT the exception
  sentinel.** A reader who goes debugging an exception path will find one that did not fire. ⚠ **Do not read
  it as a zero, and do not read `cron.job_run_details` for the answer either** — the branch returns normally,
  so the leg reports `succeeded`.
- ⚠ **AND THAT IS A CHANGE SINCE THIS MORNING, WHICH IS THE PART WORTH RE-CHECKING RATHER THAN THE 999.**
  The 08-24 night pass recorded `public_board_liveness_sweep` running **45/45 twice** (00:28Z @34 s,
  06:28Z @85 s) after the panini-squeeze worker-second cut, versus 0/45 and 6/45 before it, and published
  `public_board_slow_count=3` at 08:12Z. By the **14:48Z** leg read it is back to inconclusive. ⛔ **One
  sample — do NOT conclude the cutover's gain was illusory from this.** The sweep (jobid **288**,
  `28 */6`, `postgres`-owned) and the leg that READS it (jobid **326**, `48 2,8,14,20`, `cron_heavy`) are
  different jobs on different schedules, so the discriminating checks are `public_board_liveness_probe()`
  called directly, and sweep 288's own 12:28Z/14:28Z run history — not this value.
- ⚠ **`trust_precompute_max_age_hours` was reading ~11.75 h against a breach at 13** — `sales_serial_supply_worst_pct`,
  written 03:48Z by leg **327** (`48 3,9,15,21`), which fires again at 15:48Z. **That is the split's designed
  sawtooth, not an incident**, and it is exactly the shape this file warns reads as a regression. The leg cost
  **480,061 ms**; the most expensive leg on the board remains `topshot_impossible_parallel_serials` at
  **582,287 ms** (leg 324) — both well inside the 600 s `cron_heavy` budget they each now own since the 8-way
  split, and both would have starved each other under the old monolith.
- ✅ **All eight leg jobs 324–331 re-verified live: same jobids, same schedules, all `cron_heavy`, all
  `active`.** The 8-way split is intact eight days on.
- ⚠ **`panini_sale_field_mapping_shortfall` reads `-16` — a NEGATIVE shortfall.** Not characterised here;
  noted because a shortfall arm reading below zero is a shape worth a look before anyone quotes it as 0.

**Accuracy-gate legs, same read (canonical-only denominator — NOT the all-keys one):** Top Shot **57.2** ·
All Day **27.0** · Candy **62.4** · Pinnacle **43.4** · Golazos **0.2** · UFC **0.0**. ⚠ **Name the
denominator or the number means nothing** — see the THREE DENOMINATORS warning in
[roadmap-status.md](roadmap-status.md).

---

## ⭐ 2026-09-03 (15:15 PT) — two arms were reading their own echo, and both daytime-monitor filings that day were the echo

- **`topshot_impossible_parallel_serials` BREACHED at 8 for six hours on a condition that had been healed four minutes after it was read.** Leg 324 reads at `:48 0,6,12,18`; the healer jobid 219 `raise_impossible_parallel_circ()` ran at `:52 */6` — the SAME four hours, four minutes LATER, over the IDENTICAL predicate. So every routine inflow (9 raises on 09-03, 8 on 08-31, 11–17/day mid-August, all in `impossible_parallel_circ_raises`) was counted, raised away, and then published as a breach until the next leg. The 21:10Z daytime filing called the 8 a NEW HIGH regression "the guard was built to page on"; a live re-run of the leg's own SQL at 22:00Z read **1** (a 21:29Z sale on `252:8919::21`, a genuine Galactic parallel per `topshot_moment_subeditions`, serial 5 against a declared circulation of 4 — the stale-circulation class the healer exists for). ✅ **Jobid 219 moved to `43 0,6,12,18`** (migration `20260903221540`, `SET LOCAL ROLE cron_heavy`, jobid + owner preserved, `cron.job` 106 → 106). **The arm now reads the post-heal RESIDUE** — a timed-out heal, a `circulation_count = 0` the healer skips, or a writer out-running four heals a day — so a persisting nonzero is a real signal and not a six-hour echo. ⚠ The in-view description text still says "pages a future regression"; the view was NOT re-created (drift-pinned, 60 s cost) — this paragraph is the correction. ⚠ **The healer's monotonic raise cannot distinguish a stale circulation from a mis-keyed sale**; the writer-side guard is `app/api/sales-indexer/route.ts` keying parallels off the on-chain subedition map, and that is where a real regression would show, not on this arm. ✅ **VERIFIED ON THE REAL CALLERS 2026-09-04 00:51Z:** 219 healed the one inflow at 00:43Z (`252:8919::21` 4→5), 324 read **0** at 00:48Z, board down to the single structural breach.
- **`public_board_slow_count` flickered on `candy_scarcity_board`, and the 18:07Z filing's remedy — raise its `max_ms` — would have hidden a 1,000× regression.** The watchlist note says the 3000 ms floor was calibrated from "125 rows / 2 ms"; `public_board_liveness_history` shows a p50 of 2.4–4 s on EVERY day since 08-11 with tails of 34 s / 46 s / 55 s / **390 s** (08-28). EXPLAIN ANALYZE 22:05Z: a 25,375-row index-only scan of `wallet_moments_cache`, **16,662 buffers, 2,230 heap fetches, 1,085 ms quiet** — on every page view and every liveness probe. ✅ **Materialised** as `mv_candy_scarcity_board` behind a byte-identical wrapper (migration `20260903221326`, the 08-02 holder-board shape): the read is now **4 buffers / 0.12 ms**; refresh pg_cron jobid 436 `rpc-refresh-candy-scarcity-board` `26 * * * *` at 300 s (the #27 convention), `REFRESH … CONCURRENTLY` proven by hand, watchlisted in `board_mv_refresh_watchlist` at 6 h. Grants unchanged (anon/auth false, service true) on both objects; `security_invoker=on` re-asserted after the replace. `max_ms` stays 3000 — a reading near 3 s again now means the wrapper stopped reading the MV.

## Cross-session safety + coordination (added 2026-06-27)

**Destructive-op circuit-breaker (LIVE).** A statement-level trigger (`rpc_guard_block_destructive`, thresholds in `rpc_delete_guard_config`) BLOCKS bulk/cross-cutting deletes on irreplaceable tables: `wallet_moments_cache` (DELETE spanning >3 distinct wallets), `editions` (>25 rows), `pinnacle_editions` (>25 rows), and any TRUNCATE on those. Routine scoped deletes (per-wallet wmc refresh, etc.) pass untouched. For a GENUINELY intentional bulk delete, opt in inside the txn: `SET LOCAL rpc.allow_bulk_delete = 'on';` — and only after confirming via the ledger it's intended. (This exists because a session blind-deleted 1,724 wmc rows on 2026-06-27.) `fmv_snapshots` + caches are deliberately NOT guarded (regenerable + hot delete paths).

**Handoff staleness / concurrent sessions.** Nightly pass, daytime monitor, Cowork, and Claude Code all run against this repo and share NO live state. Before ACTING on a dated handoff, re-read `docs/overnight/ledger.md` — a concurrent same-day session may have already drained it or recorded a deliberate decision (e.g. an intentionally-disabled cron). Before WRITING a handoff, re-measure each figure live (don't copy the lagging ledger). Never call a DB row "inert"/"safe to delete" without checking EVERY consumer including denormalized display paths (`/share`, wallet snapshots) — wmc UUID fossils render real moments.

**Per-collection FMV freshness (LIVE).** `v_rpc_trust_health` carries `topshot/allday/golazos_fmv_stale_hours` (breach 6/12/30h) alongside `pinnacle_fmv_stale_hours`, so a single-collection total-FMV outage pages directly (the global freshness check masked it). `rpc_ops_snapshot()` surfaces them. ⚠ **`ufc_fmv_stale_hours` was RETIRED 2026-08-08 and re-pointed, not merely re-thresholded** (`audit_20260808_ufc_arm_repoint_revival_detector`). UFC Strike's market has been closed since May 2026, so after the 08-03 confidence-cap trigger its value grew without bound — 103.6 against a breach at 30, **permanently red**, which trains the operator to skim past every arm on the board. It is now **`ufc_flow_revival_sales_30d`**: count of UFC-on-Flow sales with `sold_at` in the last 30 days, breach at 1, reading 0/ok and firing the moment UFC trades again. Re-pointed rather than retired because **nothing else detects a revival** — the `ufc_sales` and `unmapped-sales-ufc_strike` suppressions hide arms that fire on silence or inflow, and the cadence row is a total-stop signal, so all three are structurally incapable of firing on *resumption*. ⚠ It keys on **`sold_at`, not ingest time**: `ufc-sales-history-backfill` + `ufc-studio-sales-history-backfill` are both active and still land historical rows, which an ingest-time predicate would misread as a revival. ⚠ **The rate is BURSTY, not the steady "~200 rows/24h" this line used to claim** (re-measured 2026-08-09, deep-audit D19): over the last 30 days UFC took **485 rows in a single burst on 2026-08-05 and zero on the other 29 days**. Every one carried `sold_at` ≤ 2026-04-17 — a month before the 2026-05-13 close — and there are **zero** post-close UFC sales. So a burst is exactly the shape an ingest-time predicate would read as a revival spike, which makes the `sold_at` keying MORE load-bearing, not less. **Do not simplify this arm.** ⚠ Separately: `pipeline_runs_daily` reports `rows_written=0` for BOTH UFC backfills on 2026-08-05, and no pipeline's `rows_written` accounts for those 485 rows — an attribution gap tracked with the D32 "finds rows, writes none" cohort. Read the `sales` table, not the rollup, when checking whether a backfill is really inert. `ufc_fmv_pct_stale_30d` was **RETIRED 2026-08-09** (`20260809145547`) ahead of its 2026-09-03 permanent-red fuse (a percentage can't exceed its 99.5 breach, so no threshold remedy existed) and replaced on the board by `trust_precompute_max_age_hours` — the board stays 38 arms. ⚠ Its metric is still **WRITTEN (TRACK-only)** and must stay that way (the new arm reads `max(age)` over ALL precompute rows). No FMV coverage lost — Step 6 of `/api/fmv-recalc` is collection-agnostic.

⭐ **RE-READ LIVE 2026-08-27 20:45 PT (2026-08-28 03:45Z) — 38 arms, THREE breached, and the new member is one this file called clear three days ago.** Read from `rpc_ops_snapshot()`'s `trust_health` block, not from `v_rpc_trust_health` — the sentinel path this file already prefers, and it answered.
Breached set: **`board_mv_refresh_stale_hours` 9.57** (breach 8, **NEW since 08-26**) · **`public_board_slow_count` 5** (breach 1) · **`unmapped_resolution_backlog_max` 338** (breach 100).
⭐ **This is the "diff the SET, not the count" rule paying out in the direction that is easy to miss: the count went 2 → 3, so a reader diffing numbers sees "one more arm" — but the useful fact is WHICH, and `board_mv_refresh_stale_hours` is the arm the 08-26 entry below explicitly recorded as "all clear in this sample".**
⛔ **It is the documented cadence-vs-threshold mismatch re-firing, NOT a new incident, and it does not need a new investigation:** a **6-hourly** refresher against an **8-hour** threshold has 2 h of headroom, so ONE missed cycle guarantees a breach. That analysis is already written out at length below (2026-08-17) and its recommendation — either the threshold follows the cadence or the job stops dying — is unchanged and still unactioned.
⚠ **`unmapped_resolution_backlog_max` is the one arm still climbing: 258 → 291 → 338 across 08-16 → 08-27.** Its own text says do NOT raise `breach_at`; the fix is a permanent-failure reason to exclude on.
✅ **Three arms this file has spent pages on are GREEN in the same sample, and their history must not be re-quoted as current:** `fmv_sweep_wedge_hours` **0.16** (breach 3; it read 12.17 on 08-16 and this file describes it as "risen at every single reading, eight in a row") · `fmv_sweep_stall_pct_24h` **6.2** (breach 50; it read exactly 50 on 08-16) · `panini_sale_price_capture_dry_days` **0** (it read 19–20 and breaching in mid-August). ⚠ **`public_board_empty_count` is 0 and `public_board_slow_count` is a real 5 — neither is publishing the 999 inconclusive sentinel in this sample**, so the probe completed.

⭐ **RE-READ LIVE 2026-08-26 19:10 PT — 38 arms, exactly TWO breached, and the view ANSWERED.** Breached set:
**`public_board_slow_count`** and **`unmapped_resolution_backlog_max`**. Every other arm reads `ok`. ⚠ **Read
the set, not the count** — this file's own rule, earned when membership changed twice in a day at a constant
total of 5. Against the samples below, `fmv_sweep_wedge_hours`, `trust_precompute_max_age_hours` and the
`board_mv_refresh_stale_hours` family are **all clear in this sample**, and `public_board_slow_count` is the
one arm that appears in essentially every historical sample — it is the chronic one, not the news.

⚠ **AND ONE STANDING WARNING NEEDS QUALIFYING, NOT DELETING: `select * from v_rpc_trust_health` returned in a
few seconds in this sample, where CLAUDE.md warns it "still times out at 60 s".** One successful read in a
quiet window does **NOT** retire that warning — the timeout is a saturation-dependent property, and the
instance was quiet at 19:10 PT. **Keep reading the sentinel's `Trust Health` check by preference; treat a
direct read as a bonus that may not be there next time.** ⛔ Do not "fix" the warning on the strength of this
one read.

**Trust board — how to read it, and its state as of 2026-08-16.** `select * from v_rpc_trust_health` — **38 arms, status is UPPERCASE `BREACH`** (a `status='breach'` filter silently returns zero and reads as a clean board). Columns are `metric / value / breach_at / status / catches`; there is **no `threshold` column**. Every arm's `catches` text states what it detects and, for several, what it deliberately CANNOT — read it before acting on or re-pointing an arm. ⚠ **The view itself is expensive enough to blow the 60 s MCP budget under saturation** — a `count(*) filter (where status='BREACH')` over it timed out on 2026-08-16, and later the same day even the breached-rows-only query timed out, so *neither* form is reliably cheap. **When the view will not answer, read the sentinel's `Trust Health` check instead** — `/api/sentinel` publishes `33/38` plus every breached arm with its value and `breach_at`, and the hourly `pipeline-sentinel.yml` run already has it in its logs for free.

⚠ **RE-MEASURED 2026-08-17 ~14:56Z (07:56 PT) — 6 BREACHED, THREE OF THEM NEW, AND TWO ARE PUBLISHING `999`, A VALUE THIS FILE SAYS IS UNREACHABLE.** Current set: `board_mv_refresh_stale_hours` **8.65** (breach 8, NEW) · `fmv_sweep_wedge_hours` **7.10** (breach 3) · `panini_sale_price_capture_dry_days` **20** (breach 3, +1 as designed) · **`public_board_empty_count` 999** (breach 1, NEW) · **`public_board_slow_count` 999** (breach 1, NEW) · `unmapped_resolution_backlog_max` **291** (breach 100, flat). ⚠ **The two 999s are ONE root cause and are NOT the exception sentinel** — see the corrected sentinel bullet below before acting on them. ⚠ **`board_mv_refresh_stale_hours` is a SEPARATE subsystem — do not lump it in with the 999s just because all three landed together.** It reads `board_mv_refresh_max_stale_hours()` over `board_mv_refresh_watchlist`, and exactly one entry is stale: **`mv_topshot_market_index_daily` at 8.71 h**, against six siblings at **0.17–0.90 h**. Its refresher is **jobid 235 `rpc-refresh-market-index-daily` (`7 */6 * * *`), which FAILED at 12:07Z on the 600 s `cron_heavy` budget** after succeeding in 462.1 s at 06:07Z (history 118.9 / 357.7 / 438.3 / 462.1 s — **oscillating right at its budget**). ⚠ **The structural point is a cadence-vs-threshold mismatch, not a one-off failure: a 6-hourly job against an 8-hour breach threshold has only 2 h of headroom, so ONE missed cycle guarantees a breach.** This file records a deliberate "market-index cadence cut" for that job; **the arm's `breach_at` was not re-derived after it**, so the arm now reports a single miss of a job that regularly runs 2–8 minutes as a board-integrity incident. Either the threshold follows the cadence (≥14 h leaves one missed cycle of slack) or the job needs to stop dying — **but a threshold nobody re-derived after changing the cadence it measures is the same class as the self-silencing denominator recorded further down, pointing the other way.** ⚠ `fmv_sweep_wedge_hours` **7.10 is the documented FLICKER, not a new incident** — the 24 h profile re-measured the same hour is `19,176 rows written`, **identical to the recorded figure**, so throughput is flat; see the `fmv_snapshots` section. ✅ **CONFIRMED ONE HOUR LATER: it CLEARED unaided (7.10 → green), taking the board 6 → 5** — the FOURTH recorded instance of that arm self-clearing, so the "judge it by the gap distribution and 24 h `rows_written`, never by hours-since-advance at one instant" rule is now very well earned. ⚠ **In the same hour `board_mv_refresh_stale_hours` climbed 8.65 → 10.60**, exactly as the cadence-vs-threshold analysis predicts (jobid 235 cannot fire again until 18:07Z), and the two 999s persisted (the sweep cannot fire before 18:28Z). **So this whole paragraph is a dated sample that moved in three different directions within an hour — re-read the board, never quote this.** **The paragraph below is the 05:24Z sample; its reasoning about disagreeing sibling arms stands and its values are superseded.**

⚠ **[05:24Z SAMPLE — SUPERSEDED] BACK TO 4 BREACHED, AND THE RETURNING ARM IS THE ONE THE BULLET BELOW CELEBRATED AS RECOVERED.** `fmv_sweep_wedge_hours` **4.32** (breach 3) has re-entered after clearing to 0.09 six hours earlier — see the `fmv_snapshots` section for the wedge itself. The other three are `panini_sale_price_capture_dry_days` **19** (flat — a dry-day counter that did NOT tick, so it is not reliably +1/day either) · `public_board_slow_count` **5** (down from 9, and down from 12 the day before) · `unmapped_resolution_backlog_max` **291** (flat). ⚠ **`fmv_sweep_stall_pct_24h` is 40.4 and `ok`** — so the "treat the two as one signal" instruction below is **not** holding this time: the wedge arm breached while its sibling stayed green, exactly as the wedge arm's own `catches` text says it must (that one measures restarts at `cursor_before=0`, and a sweep stuck at an INTERIOR offset never restarts at 0). **Read both, and expect them to disagree.** ⚠ `public_board_slow_count` has now read 3 → 9 → 14 → 12 → 9 → 5 across four days, which is the arm's own "do not characterize its direction from fewer than several days" rule earning its place a third time — it has been called "climbing" and "oscillating down" on ~1-day windows and both were fair on their data. **The paragraph below is the 23:40Z sample; its reasoning stands and its values are superseded.**

✅ **[23:40Z SAMPLE — SUPERSEDED] DOWN TO 3 BREACHED, AND THE TWO THAT CLEARED ARE THE ONES THIS FILE DESCRIBED AS RUNAWAY.** `fmv_sweep_wedge_hours` **12.17 → 0.09** (breach 3) and `fmv_sweep_stall_pct_24h` **50 → 32.9** (breach 50), both **ok** — the sweep recovered *unaided*, so the "risen at every single reading, eight in a row" characterization below is **HISTORY, not current**, and the two-arms-one-signal reading was right: they cleared together. `trust_precompute_max_age_hours` is **5.87** (breach 13), which lands on the 8-way split's own predicted steady state of ~5.7 h — **the split is confirmed in production, not merely shipped**. The remaining three are `panini_sale_price_capture_dry_days` **19** · `public_board_slow_count` **9** (down from 12) · `unmapped_resolution_backlog_max` **291** (UP from 258 — **the one arm still climbing**, and the AllDay permanent-class floor whose own text says do NOT raise `breach_at`). ⚠ **The paragraph below is the 18:56Z sample, kept for its reasoning about how the MEMBERSHIP can change while the COUNT holds — that lesson stands and is the reason to diff the set, not the number. Its values are superseded.**

⚠ **[18:56Z SAMPLE — SUPERSEDED] STILL 5 BREACHED AT 18:56Z, AND THE COUNT IS THE MISLEADING PART — the MEMBERSHIP changed twice in one day while the total stayed at 5.** `trust_precompute_max_age_hours` **CLEARED** (11.84 ok, once the 8-way split's leg 324 fired — see the split bullet), and **`fmv_sweep_stall_pct_24h` newly BREACHED at exactly 50 against `breach_at` 50**. A reader diffing only the count sees "no change" across a fix landing and a new arm firing. The current five are `fmv_sweep_stall_pct_24h` **50** · `fmv_sweep_wedge_hours` **12.17** · `panini_sale_price_capture_dry_days` **19** · `public_board_slow_count` **12** · `unmapped_resolution_backlog_max` **258**. ⚠ `fmv_sweep_wedge_hours` has now risen at **every single reading, eight in a row** (4.30 → 4.68 → 6.03 → 8.04 → 9.39 → 9.44 → 12.17), and its new sibling `fmv_sweep_stall_pct_24h` is the same subsystem — the `fmv-recalc` kill rate — so treat the two as one signal, not two.

**The table below is the 09:15 PT sample and is kept for its per-arm reasoning, which still holds; the values are superseded by the 18:56Z figures above.**

| arm | value (08-16 09:15 PT) | breach_at | read |
|---|---|---|---|
| `public_board_slow_count` | **12** (was 14) | 1 | ⚠ **Now DOWN 14 → 12 over ~18 h, after 3 → 9 → 14 the day before.** This arm has now been characterized as "oscillating down" AND "climbing, not oscillating" on the strength of ~1-day windows, and **both readings were fair on their own data** — which is the finding. **Do not characterize its direction at all from fewer than several days**; the 4 worst are still Candy, `candy_pack_market` at ~128 s for a ONE-ROW result behind a board PUBLIC since 07-31 |
| `unmapped_resolution_backlog_max` | 258 (unchanged) | 100 | AllDay permanent-class floor, continuously replenished; the arm's own text says **do NOT raise `breach_at`** — the fix is a permanent-failure REASON to exclude on. **The one genuinely flat value on the board** |
| `panini_sale_price_capture_dry_days` | **19** (was 18) | 3 | ✅ **MECHANISM ESTABLISHED 2026-08-16 — it is WALLET SCOPING, and the arm's own "lighter per-serial shape" reading is REFUTED.** `brought_at_price` is wallet-scoped ("what did *you* pay"), and `is_owner` is true on **0** of 82,511 rows. Two measurements, both from `panini_card_serials.raw` — which the arm wrongly assumed had no pre-switch copy (it holds **5,876** pre-07-27 rows): (1) the top-level **key set is IDENTICAL across eras**, all 45 keys on 100% of rows both sides, so the payload shape never changed; (2) `my_public_wallet` set ⟺ `brought_at_price` null is a **perfect biconditional — identical COUNTS every day including the partial ramp days (708, then 2,026), and 0 exceptions in either direction across the 3,257 ramp-day rows** where both states coexist on the same box in the same walk. So the capture is not broken and upstream did not stop sending: **on 07-27 the runner began sending a wallet-scoped session and the field silently changed meaning.** ⚠ **BUT THE ARM IS NOW CRYING WOLF AND THAT IS THE BIGGER FINDING: it counts dry days on a field DELIBERATELY ABANDONED AND REPLACED on 2026-08-08 (`521ee89f`), while the replacement WORKS.** That commit's A/B — **the one this arm asks for, already run** — found `listType` **INERT** (a nonsense control returned the same 10 rows / 10 nulls as all four real values) and that **a fully signed request from Panini's OWN front end gets the same nulls we do**, so "no request-shape fix exists"; capture was re-pointed at **`nftSalesData`**. Measured live: sale-price capture went **5.0% (08-07) → 8.9% (08-08, ship day) → 22.7% (08-09)** and holds **21–39% since**, incl. **22.9% today on 17,809 rows** — **15,346** rows carry `last_sale_usd`, newest sale **2026-08-16 21:16Z**. **Panini sale capture is NOT dry; only the dead field is.** So this is the `ufc_fmv_stale_hours` shape again — an arm that can never go green, +1/day forever, training the operator to skim the board — and the fix is the same: **RE-POINT it** (at `max(last_sale_at)` freshness or the share of new serials carrying `last_sale_usd`), not re-threshold, not retire (the `nftSalesData` path has its own failure mode: it depends on the runner CLICKING the sales-history tab, so an upstream UI change silences it). ⚠ Do NOT re-run the `listType` A/B, and do NOT re-derive the three branches already disproved. ⚠ Also note the runner has **no Panini auth code at all** (its only `Authorization` is our own ingest bearer; the browser signs natively), so the 07-27 change was **upstream, not ours** — the only runner commits near it are a DOM psku-harvest enumeration fallback. Filed, with the correction: [inbox 2026-08-16T2355Z](../../docs/overnight/inbox/2026-08-16T2355Z-panini-sale-price-is-wallet-scoped-not-a-lighter-payload.md) |
| `fmv_sweep_wedge_hours` | **9.44** (was 7.40) | 3 | ⚠ **4.30 → 4.68 → 6.03 → 8.04 → 9.39 → 9.44 across ~26 h — the ONE arm that has risen monotonically at every single reading**, and a 07:48 PT note that called the climbers "eased" at 7.97 was contradicted within the hour. See the `fmv_snapshots` section; the sweep's completion rate, not its schedule, is what moved. **Un-diagnosed — trend observation only** |
| **`trust_precompute_max_age_hours`** | **15.25** (was 15.14) | 13 | ⚠ **DO NOT READ THIS AS THE 8-WAY SPLIT HAVING FAILED — it is the expected post-split transient, and it will get WORSE before it recovers.** See the bullet below |

⚠ **The movers still track the SAME root cause** — the disk-IO saturation that also kills `fmv-recalc` at `maxDuration` and fails insights board warms per tick — so **do not open separate investigations**; and no value here is a constant. The table is a 2026-08-16 09:15 PT sample, which is exactly what the `public_board_slow_count` row is about.

⚠ **`trust_precompute_max_age_hours` FIRED, AND WHAT IT CAUGHT IS THE BOARD PUBLISHING STALE NUMBERS AS CURRENT (measured live 2026-08-15).** ⚠ **THE MECHANISM BELOW IS THE PRE-SPLIT STATE AND IS KEPT BECAUSE IT IS WHY THE FIX LOOKS THE WAY IT DOES — but jobid 287 was UNSCHEDULED on 2026-08-16 and replaced by eight per-leg jobs (324–331); see the ✅ SHIPPED note two bullets down before acting on anything here.** At the time of measurement the refresh was pg_cron **jobid 287, `58 */6 * * *`**, and reading its source settles the mechanism: `rpc_trust_health_precompute_refresh_p()` is **eight `PERFORM public.rpc_thp_leg_*(); COMMIT;` lines with ZERO exception handlers**. So a statement timeout in leg N leaves legs 1..N-1 **committed** and legs N..8 **frozen at whatever tick last reached them** — a PARTIAL refresh, not an outage. That is exactly the live state: seven metrics are **3.2 h** fresh and **`topshot_impossible_parallel_serials` alone is 15.16 h**, because `rpc_thp_leg_impossible_parallel()` is the **LAST** line — ⚠ **it is stalest by POSITION, not because its own query is slowest**, so do not go optimize that query. The last four ticks read **succeeded (00:58Z) · succeeded (06:58Z) · failed (12:58Z) · failed (18:58Z)**, and the two failures died at **different statements** (`round(100.0 …` vs `count(*)::numeric FROM p…`) — the failure point moves with wherever saturation lands, the same "drew the short straw" shape as the prerendered-board build failures. ⚠ **The cost: `v_rpc_trust_health` shows no per-metric age**, so a frozen leg publishes a stale value indistinguishable from a current one, and the only thing standing between that and an operator acting on it is this one max-age arm. **It is doing precisely the job it was installed for — treat the breach as the instrument working, not as noise to threshold away.**

⚠ **WHY the leg neither writes its value NOR its 999 sentinel — and why the obvious fix is WRONG. This bullet's first version said the remedy was "wrap each leg in its own `EXCEPTION` block"; that was wrong twice over, and the correction is the durable part (measured 2026-08-15, shipped and reverted the same session).** Every leg **already has** an `EXCEPTION WHEN OTHERS` handler whose whole purpose is to write the 999 sentinel. **PostgreSQL excludes `QUERY_CANCELED` from `OTHERS`** — *"The special condition name OTHERS matches every error type except QUERY_CANCELED and ASSERT_FAILURE"* — and a `statement_timeout` raises exactly `query_canceled` (57014). Verified on the instance: `WHEN OTHERS` did **not** catch a raised 57014; `WHEN query_canceled OR OTHERS` did; and at the time of measurement `where value = 999` returned **zero rows, ever**. **So all eight handlers are structurally incapable of firing on a TIMEOUT** — which is the one failure the monolith produced.

  ⚠ **BUT "the sentinel has never once fired" IS NOW FALSE, AND THE WAY IT IS FALSE IS THE TRAP: THE 999 YOU WILL SEE TODAY IS NOT THE EXCEPTION SENTINEL AT ALL (measured live 2026-08-17 14:56Z).** `public_board_empty_count` and `public_board_slow_count` are both publishing **999** and both BREACHING. A reader following this bullet would conclude the leg is throwing and go debug an exception path that did not fire. **Read `rpc_thp_leg_board_liveness`'s source: it has an explicit, deliberate branch** — `IF COALESCE((v_board->>'budget_exhausted')::boolean, false) THEN v_empty := 999; v_slow := 999;` carrying the comment *"incomplete sweep is INCONCLUSIVE, not green"*. **So 999 carries TWO unrelated meanings in one leg — "the probe could not complete" and "the leg threw" — and they are indistinguishable in the published value.** ⚠ **Do not infer which from the leg's cron status either: `cron.job_run_details` says `succeeded` for every one of these runs**, because the deliberate branch returns normally. The discriminators that DO work are `rpc_trust_health_precompute.duration_ms` (**77 ms / 85 ms** here — far too fast to have probed anything) and calling `public_board_liveness_probe()` directly, which returns the state verbatim: `budget_exhausted: true, probed: 0, sweep_age_min: 508`. **It is the inconclusive branch, not the sentinel.**
  ⚠ **AND THE OTHER HALF OF THIS BULLET'S MECHANISM WAS OBSERVED IN PRODUCTION THE SAME DAY, SIDE BY SIDE — which is the strongest confirmation it has had.** In the same 24 h, **leg 324 `rpc-thp-leg-impossible-parallel` FAILED at 12:48Z with `canceling statement due to statement timeout` after exactly 600.0 s and wrote NO sentinel**, leaving `topshot_impossible_parallel_serials` frozen (8.11 h) — the `QUERY_CANCELED`-escapes-`OTHERS` case — while leg 326's non-cancel path was caught and published. **Both halves, one board, one day.** The analysis above is right; only its "never fires" closing claim was overtaken. **A never-happened claim about a sentinel is a hypothesis with a shelf life — re-run `where value = 999` rather than quoting this.** ⚠ **But changing the clause is NOT the fix, on three measurements:** (1) a **function-level `SET statement_timeout` does not bind statements inside that function** (probe: 300 ms declared, `pg_sleep(5)` ran to completion), so the legs' declared 60–480 s budgets are **all inert** — the **second instance of that trap on one day**, after `8918307c` on the drain seeders; (2) the real budget is **`cron_heavy`'s role-level `statement_timeout=600s`, shared by the whole `CALL`** — legs 1–7 burned **517 s** and leg 8, needing ~78 s, got ~83 s, so **it is stalest by POSITION and optimizing its query fixes nothing**; (3) decisively, **after a cancel is caught the timer is NOT re-armed** (probe at 500 ms: first `pg_sleep(3)` cancelled and caught, second **ran to completion**, `TAIL_UNBOUNDED ran_for=3.50s`). So catching the cancel buys a reachable sentinel at the price of running every remaining leg **with no bound at all**, holding a pooled connection on the 2 GB instance whose saturation caused the timeout — **a bounded failure traded for an unbounded one**, and plpgsql offers no way to re-arm mid-statement. **The real fix is structural: give each leg its own TOP-LEVEL statement (8 pg_cron entries instead of one orchestrator)**, so each gets a fresh budget, no leg's timeout can reach another, and `cron.job_run_details` names the failing leg. Original filing: [inbox 2026-08-15T2240Z](../../docs/overnight/inbox/2026-08-15T2240Z-the-999-sentinel-is-unreachable-on-a-timeout.md).

✅ **SHIPPED 2026-08-16 15:08Z — this bullet's "filed, not taken" is HISTORY; the monolith no longer exists.** jobid **287 unscheduled**; eight per-leg jobs **324–331** created, all owned by `cron_heavy` (so each inherits the 600 s role budget as its own), spread across hour offsets so only one leg runs per hour while each still refreshes every **6 h** — the freshness contract is unchanged. Record + one-block revert: `supabase/migrations/20260816150800_audit_20260816_trust_precompute_split_into_8_leg_jobs.sql`. ⚠ **That file is a committed RECORD and is deliberately ABSENT from `supabase_migrations.schema_migrations`** — the change was applied through `execute_sql` with `SET LOCAL ROLE cron_heavy` (see the pg_cron bullet), not through `apply_migration`. ⚠ **This bullet used to call that "the only path that can reschedule these jobs" — FALSE, corrected 2026-08-16.** jobid 215's cadence cut went in through **`apply_migration`** with `SET LOCAL ROLE cron_heavy` … `RESET ROLE` and is registered as `20260817015354`. The **`RESET ROLE` is load-bearing**: `apply_migration` appends its own `insert into supabase_migrations.schema_migrations`, which `cron_heavy` cannot write, so leaving the role set fails the whole migration. **Prefer `apply_migration` for future reschedules — it yields a REGISTERED migration instead of a committed-but-absent record**, i.e. it avoids the exact blind spot the next sentence describes. **So do not read its absence from `schema_migrations` as "never applied" — the eight jobs are live and verified.** Note the direction this defeats: `migration-parity.yml` reports prod-applied migrations with **no committed file**, and is structurally silent on the inverse, which is exactly the shape here. ⚠ **The decisive evidence is not the reasoning above but three consecutive ticks: 00:58 = 8/8 legs in 491 s ok · 06:58 = 7/8 at 600.0 s · 12:58 = 2/8 at 600.0 s.** If a `COMMIT` re-armed the timer the 06:58 run would have reached ~1,130 s; **both failures cap at exactly 600.0 s regardless of how many legs finished**, which is one governor, not a workload. **A `COMMIT` inside a procedure does NOT re-arm `statement_timeout` — the whole `CALL` is ONE statement, so per-leg COMMIT buys DURABILITY of finished legs, not a fresh budget.** Corroborating arithmetic: the legs' declared budgets sum to **1,650 s inside 600 s**, so it could never fit at any hour — ⚠ **which is why RESCHEDULING was rejected: it only chooses which legs starve.** A recommendation to move the job to a "6-for-6" slot was falsified by the next tick. Verified post-ship: two independent legs fired exactly on their cron minute and succeeded (132.1 s and 164.9 s), and `sales_serial_supply_worst_pct` went **8.83 h → 0.11 h**. ✅ **The per-metric-age gap this bullet names is CLOSED — `v_rpc_trust_health_freshness` is APPLIED AND LIVE, and this bullet described it as unapplied for hours after it landed.** It went in through `apply_migration` as **`20260816173845_audit_20260816_trust_health_freshness_companion_view`** (present in `supabase_migrations.schema_migrations`, so this is not the `execute_sql` case described two sentences up). Measured live 2026-08-16 ~23:57Z: **19 rows, per-metric age 0.20 h → 5.85 h**. ✅ **AND ON 2026-08-17 05:24Z IT CAUGHT THE THING IT WAS BUILT FOR, WHICH IS ALSO THE SPLIT'S ISOLATION DEMONSTRATED IN PRODUCTION.** Max age read **7.68 h** — above the ~5.7 h steady state — and the view named the single responsible metric: **`sales_serial_supply_worst_pct`**, because **jobid 327 `rpc-thp-leg-serial-supply` FAILED at 03:48:00Z** while all seven sibling legs succeeded on their own minutes (328 ok at 04:48Z, 331 ok at 03:09Z, 326 ok at 02:48Z, 325 ok at 01:48Z, 324 ok at 00:48Z, …). **Under the monolith that failure would have frozen every leg after it; here it froze exactly one metric and cost 2 h of extra age on that metric alone.** ⚠ It failed after **15.2 s**, nowhere near the 600 s budget, so it is an ERROR and not a timeout — **do not attribute it to saturation without reading `cron.job_run_details`**. ⚠ And `trust_precompute_max_age_hours` correctly did **NOT** breach (7.68 vs 13), which is the point: **the ARM cannot tell you WHICH leg is stale, and one failed leg does not move it far enough to notice — the freshness view is the only instrument that names the leg.** Check it, not the arm, after any leg failure. ⚠ **Leg 324 re-timed at 265.0 s this cycle against the 367 s recorded below** — so its runtime spans **265–367 s (44–61% of budget)** and the file's own "one sample; re-time it before acting" caution was right. ⚠ **That max of 5.85 h is a second, independent confirmation of the 8-way split** — it lands on the split's own predicted ~5.7 h steady state, derived from per-metric ages rather than from the max-age arm, so it is the instrument this bullet asked for actually answering. **Use it instead of inferring per-leg freshness from `rpc_trust_health_precompute` by hand.** ⚠ **The durable lesson is about the NOTE, not the view: a "committed UNAPPLIED" annotation has a shelf life and nothing announces its expiry** — the same shape as the disclosed-limitation-that-goes-stale class recorded under the alerts work. Worse here, a later pass cited the view's **absence from live as positive confirmation** that the migration was genuinely unapplied; that reasoning was sound when written and became a load-bearing false statement the moment someone applied it. **Check `schema_migrations` (or just query the object) before repeating any UNAPPLIED claim in this file, and never re-apply on the strength of the prose.** ⚠ **THE SPLIT HAS A ~6 h TRANSIENT IN WHICH THE ARM READS WORSE, AND NOTHING ABOUT IT LOOKS LIKE RECOVERY — this is the single most likely way a future session wrongly concludes the fix failed (measured 2026-08-16 09:15 PT / 16:15Z, ~1 h post-apply).** Because the eight jobs are spread one-per-hour on a 6 h cycle, **only the legs whose cron minute has come around since 15:08Z carry post-split data**; every other leg still holds whatever the dying monolith last wrote. Exactly **2 of 8 had fired** — jobid 331 at 15:09Z (132.1 s) and 327 at 15:48Z (164.9 s), both `succeeded`; **jobid 328 then fired on its own 16:48Z minute and also succeeded, taking it to 3 of 8, and `max_age` rose 15.25 → 15.95 h in the same window — the predicted transient behaving exactly as the schedule says it must** — and `rpc_trust_health_precompute` showed three distinct cohorts: the two fired legs at **0.45 h / 1.10 h**, panini at 3.29 h, a **9.13–9.28 h** block that has not had its turn, and `topshot_impossible_parallel_serials` alone at **15.27 h**. The schedules make the whole curve predictable, so check it against the clock rather than against a hunch: `324 impossible-parallel 48 0,6,12,18` · `325 fmv-coverage 48 1,7,13,19` · `326 board-liveness 48 2,8,14,20` · `327 serial-supply 48 3,9,15,21` · `328 fmv-sanity 48 4,10,16,22` · `329 pack-ev 48 5,11,17,23` · `330 panini 9 0,6,12,18` · `331 pinnacle-fmv-share 9 3,9,15,21`. **So the arm PEAKS at ~17.9 h just before 18:48Z** (when 324, the stalest leg, finally fires) and **the first full cycle only completes at 20:48Z** with jobid 326 — until then a rising `max_age` is the un-fired legs ageing, not the fix failing. ⚠ **AND THE CONVERGENCE IS NOT MONOTONIC — it clears, RE-BREACHES for ~40 minutes, then goes green, which is the shape most likely to be read as a regression** (independently measured by a concurrent Cowork pass the same hour, [inbox 2026-08-16T1615Z](../../docs/overnight/inbox/2026-08-16T1615Z-leg-split-will-re-breach-at-2007Z-and-runs-daily-is-a-6h-stale-rollup.md), which is sharper than the peak-then-settle reading this bullet first carried). **~18:48Z it CLEARS** (324 fires, the 15.24 h outlier drops to 0, max becomes leg 325 at ~11.7 h → GREEN); **~20:07Z it RE-BREACHES**, because leg 326's *pre-split* write at 07:07:21Z crosses 13 h at exactly 20:07:21Z and 326 does not fire until 20:48Z, so the arm reads 13.0 → 13.7 for ~40 min; **~20:48Z green for good**, steady state ≈5.7 h. **The re-breach is the last pre-split timestamp ageing out, not the split failing — do not revert it, re-point the threshold, or open an incident.** ⚠ **The check worth running is the FALSIFIER, not the 20:07Z number: if the arm has not cleared by ~18:55Z, leg 324 did not fire and the split has a real problem.** **Judge the split by `cron.job_run_details` for jobids 324–331 and by per-leg age, never by the max-age arm inside its first cycle.**

✅ **THE FALSIFIER WAS RUN AT 18:48–18:55Z AND THE SPLIT PASSED — the prediction above is now a MEASUREMENT, and it came with three corrections.** Leg 324 fired **exactly on its cron minute (18:48:00.6Z)**, `succeeded`, and the arm went **17.87 → 11.84 h, status `ok`** as `topshot_impossible_parallel_serials` dropped **17.87 h → 0.13 h**. The predicted ~17.9 h peak was measured at **17.87** — right to 0.03 h. **The 8-way split works; stop treating it as provisional.**
- ⚠ **CORRECTION — leg 324 takes 367 s, not the "~78 s" this bullet claims.** That 4.7× error changes the monolith's story from *"leg 8 needed ~78 s and got ~83 s"* (agonizingly close) to **never had a chance**: it was being handed ~23% of what it needs. Reinforces that the split, not a reschedule, was the only fix — and the "~78 s" was an inference, never a timing.
- ⚠ **MEASURED (not arithmetic) PROOF the monolith could never fit: six legs' real runtimes already sum to 748 s, against the 600 s the monolith had for all EIGHT** — `324` **367 s** · `327` 165 s · `331` 132 s · `329` 69 s · `330` 10 s · `328` 5 s (325 + 326 not yet timed post-split). The existing 1,650 s figure is the sum of *declared* budgets, which this file elsewhere proves are **inert**; this is the version that survives that objection.
- ⚠ **ALL EIGHT ARE NOW TIMED, AND THE SPREAD IS FAR WIDER THAN ANY SAMPLE ABOVE — re-measured 2026-08-17, this is the third time the "one sample; re-time it before acting" caution has paid.** Leg **324 ran 113.9 s at 06:48Z and then TIMED OUT at exactly 600.0 s at 12:48Z**, so its real span is **113.9 s → 600 s+**, not the "265–367 s (44–61% of budget)" recorded above — it is not comfortably inside its budget, it **exceeds it under load**, which is exactly what that bullet predicted would happen to this leg first. The two previously-untimed legs: **325 `fmv-coverage` 92.1–327.8 s** and **326 `board-liveness` 0.04–0.26 s** (326 is trivially cheap because it reads a state table rather than probing boards — see the sweep bullet under "Cron / scheduler surfaces"). ⚠ **Leg 324's timeout wrote NO sentinel and froze its metric at 8.11 h**, which is the `QUERY_CANCELED` case observed live; and leg **327 failed at 03:48Z with `job startup timeout`**, the connection-handshake class, not a budget kill. **Three different failure modes across the eight legs in 24 h — read `cron.job_run_details.return_message`, never just `status`.**
- ⚠ **Leg 324 is the ONE leg without comfortable headroom: 367 s of its own 600 s budget (61%).** Every other measured leg is under 28%. It is also the heaviest and the one that starved under the monolith, so **if any leg re-breaches under worse saturation it will be this one** — and it will now fail alone rather than taking the other seven with it, which is exactly what the split bought. One sample; re-time it before acting.
- ⚠ **THE FALSIFIER ITSELF IS FLAWED AND NEARLY CRIED WOLF — it conflates "did not fire" with "has not FINISHED".** Leg 324 started 18:48:00.6Z and ran **367 s**, finishing **18:54:07Z — 53 seconds inside the 18:55Z deadline.** A 15% slower leg (well within this instance's saturation swing) would have blown the deadline while being perfectly healthy, and the stated conclusion — *"leg 324 did not fire and the split has a real problem"* — would have been flatly wrong. **Check `cron.job_run_details` for the leg's START before concluding anything from the arm**, which is what the very next sentence already says and is the half that is right. ⚠ **And `trust_precompute_max_age_hours` itself needs NO change** — once every leg has fired, each is on a 6 h cadence so max age settles ~6.1 h against `breach_at = 13`, ~two missed cycles of headroom. "One perpetually-failing leg pins it red" is **correct behaviour, not the `ufc_fmv_stale_hours` cry-wolf fuse**: that arm could never go green because a closed market's staleness only grows, whereas this one recovers the moment the leg does. **Add visibility, don't move the threshold.**

⚠ **A NAME-TRAP COST ME A WRONG MECHANISM ON THE WAY TO THAT, and it is the kind that reads as authoritative.** Two objects sit one suffix apart: `rpc_trust_health_precompute_refresh` (a **FUNCTION**, 13,009 chars, monolithic, inlines every leg, and **`impossible_parallel` is leg 1** in it) and `rpc_trust_health_precompute_refresh_p` (the **PROCEDURE**, 492 chars, the one actually scheduled, where the same leg is **last**). I read the longer one first — it is the name you would guess and it is 26× the size, so it *looks* like the real implementation — and it yields the **opposite** conclusion about why that metric is stalest. The monolith has **zero callers** anywhere (0 DB functions, 0 cron rows, 0 views, and nothing in the repo outside migration history). **Read `cron.job.command` to learn what a schedule actually calls; never infer the callee from the name.**

⚠ **GATES WHOSE DENOMINATOR IS DRAWN FROM THE THING THEY WATCH SELF-SILENCE (2026-08-15, `2deeb8b4`).** The `panini-freshness-check` escalation gated on `pct_of_trailing7 < 55` — but a persistent collapse **drags the trailing-7 window down to meet itself**, so projected from a flat 154/day the gate stops firing on **PT 08-19 (four days)** and reads a clean **100% by 08-22 with the outage unchanged**. The fix is a denominator the outage cannot depress: `pct_of_catalogue` (yesterday's editions over the `panini_editions` count — a catalogue count cannot be reduced by an ingest stall, and it stays self-updating as the catalogue grows). ⚠ Note that `pct_of_baseline` (days 8–28 back) only **defers** the same failure to 09-06 — a longer window is not a fix for this class, it is a longer fuse. **Any ratio against a rolling window of the watched series will eventually call an outage healthy; the escape is a denominator from a different quantity, or the raw series.** The replaced prose had its own bug worth avoiding: it told the reader to compare against "the ~800 healthy-era figure", a hardcoded absolute the same prompt forbids nine lines earlier.

⚠ **This state lived only in a dated session entry until 2026-08-15, which is exactly how it went stale**: dated entries roll off to `docs/sessions/` within ~3 days, so "3 breached, all known-class" was still being read as current after a fourth arm had breached and one of the three had tripled. **A breach count belongs here, not in a session log** — and re-measure it rather than quoting this table, which is a dated sample like every other number in this file.



---

## 🚨 `pack_ev_board_max_stale_days` covers ONE collection, and its name says otherwise (measured 2026-08-18)

**The arm reads `topshot_pack_reality_top_ev` — a Top-Shot-only board of FOUR rows, which has no
`collection_id` column at all.** From `v_rpc_trust_health`:

```sql
WITH packev AS (
  SELECT max(EXTRACT(epoch FROM (now() - snapshotted_at)) / 86400) AS max_stale_days, …
  FROM topshot_pack_reality_top_ev          -- Top Shot only
)
```

Its name carries no collection qualifier and its `catches` text reads *"stale pack-EV board"*, so it
presents as covering every board. **It is structurally incapable of reporting staleness for
laliga_golazos, nfl_all_day, disney_pinnacle or candy_mlb.**

Measured side by side, same instant:

| | days |
|---|---|
| what the arm publishes (Top-Shot-only source) | **0.94** |
| Top Shot's own board staleness | 0.19 |
| **golazos board staleness — invisible to the arm** | **1.35** |

That blindness is why `compute-golazos-pack-ev` could stop for **31 hours** with the board green. ⚠
**Fixing the pipeline does NOT fix the arm** — a fresh feed makes the board current and leaves the arm
exactly as blind to the next collection that stops. Widen it to a per-collection max (or add a sibling
arm per collection). Full working: `docs/overnight/inbox/2026-08-18T1406Z-golazos-pack-ev-…md`.

⚠ **A refuted hypothesis worth not re-deriving:** the first explanation was that golazos packs are
excluded from the published board. **False.** `pack_ev_latest` is a plain view with **no recency
cutoff and no collection exclusion** — stale golazos packs ARE published, carrying stale
`snapshotted_at`. The blindness is in the arm's SOURCE, not in a publish filter.

⚠ **Aggregate mismatch that nearly produced a wrong number here.** `max(now() - snapshotted_at)` is
board staleness on a CURRENT board (one row per pack) and the age of the OLDEST row on a HISTORY table
— it read **42.38 days** against a true **1.35** when pointed at `pack_ev_history`. **Board staleness
is `now() - max(snapshotted_at)`.** Never carry an aggregate across from a board to a history table.

### ⚠ And the general lesson: a blanket root cause needs a per-item control

Both the 0013Z filing and the 2026-08-18 overnight handoff attributed this to *"likely saturation
collateral"* — the one root cause the focus doc tells sessions not to re-investigate. **A positive
control refutes it:** `pack_ev_history` newest-per-collection at one instant read golazos **31 h**,
nfl_all_day **8.4 h**, nba_top_shot **3.6 h**, disney_pinnacle **1.7 h**. Platform-wide IO does not
stop one collection and spare three. **The standing "it's the saturation" instruction is right in
general and is exactly what let a separate fault sit for 31 hours wearing a known-class label.**

---

## `Pipeline Success Coverage` — the arm, and the two things it CANNOT see (moved verbatim from CLAUDE.md 2026-08-18)

*CLAUDE.md carried this as its "Recently closed" paragraph until 2026-08-18, when it was compressed to a
one-line pointer to make room for newer rules. Nothing was deleted — this is the text verbatim.*

✅ **Recently closed — WITH its boundary, so nobody reads it as coverage:** the **success-coverage gap** (the `Pipeline Success Coverage` arm flags an active-watchlist pipeline with **runs > 0, zero successes, zero rows written**; the `rows_written` term is load-bearing — zero-successes alone gave 4 false positives in 20 days, all graceful degradation — and it reads the ≤6 h-lagged rollup, so **never use it to confirm a recovery**). ⚠ **2026-08-17: that term also makes it blind to "writes rows and never completes" — 4 of the 5 unwatched-and-failing pipelines do exactly that.** The boundary is NOT an argument to drop the term; it earned its place on measurement.

⚠ **And the membership half:** the arm only reads `pipeline_cadence_watchlist` rows with `is_active`, so
its blind spot is whatever the curated list omits — measured 2026-08-17 at **67 of 150** live pipelines,
not the 62 filed against all 102 watchlist rows. **Derive the watched set from `pipeline_runs`; make
*suppression* the curated list.**

⚠ **RE-MEASURED 2026-08-22 14:30Z — the gap GREW while nothing about the arm changed: 76 unwatched of
158 live pipelines** (same instrument: `distinct pipeline` over `pipeline_runs`, whose window was
08-19 12:41Z → 08-22 14:27Z, i.e. the documented ~73 h retention). The watchlist itself is
**unchanged at 102 rows / 83 active**, so every one of the 9 added blind pipelines came from the
*fleet* moving, not from anyone deactivating an arm. **That is the curated-list failure mode stated
as a rate rather than an anecdote: the list does not have to rot for its coverage to fall.** ⚠ Both
readings are dated samples of a moving population — re-derive, and note that the two numbers are
comparable ONLY because they were taken with the same instrument over the same retention window.
