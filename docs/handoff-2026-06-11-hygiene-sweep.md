# Claude Code handoff — 2026-06-11: telemetry-honesty hygiene sweep (the last open items)

Context: everything HIGH from 06-10 is shipped and verified (DBSAT fixed, drops -40% DB, pack-events p25+p26 cursor advancing, fmv-recalc early-exit logging 89c4891 monitor-verified, render-id index live via psql with the :37 ticks green, verify-challenge baf9802, sentinel softening 631b1de confirmed covering exactly the right checks). Cowork also moved the topshot-fmv-populate cron off the seed-wave shadow tonight (was 50 0,6,12,18 UTC = 5 min behind every wave -> now 15 1,7,13,19 UTC; server-verified next execution 07:15Z). What remains is FOUR small telemetry-honesty items — after these, the board is genuinely empty. Claude Code's direct file inspection wins over this doc on any disagreement.

Item 1 (MED) — every-exit-logs sweep across the c55d394-wrapped cron routes. The fmv-recalc disease (fixed in 89c4891) exists in siblings: an error thrown/returned inside after() exits before log_pipeline_run, so the cron dashboard acks green while pipeline_runs goes silent. Evidence from 06-10: backfill-pack-rip-metadata silent 15:53Z->01:53Z across ~10 acked-green ticks (recovered ok=true 4.8s at 01:53Z on the calm DB — so hygiene, not breakage); topshot-fmv-populate's 00:50Z tick acked 979ms and never logged. Sweep each route c55d394 wrapped (plus topshot-fmv-populate) and ensure EVERY exit path in the after() body — especially catch blocks around the first DB read — writes log_pipeline_run with the real error (mirror 89c4891's pattern: ok=false, the message, a stage extra). Verify: induce nothing; just confirm next-day pipeline_runs has no silent gaps while dashboards show green. Revert: git revert.

Item 2 (MED) — make smoke's "market API returns Top Shot listings" check honest (Sentry NEXTJS-4, 305 events since April, still firing on a CALM db at 02:50Z). The check asserts listings EXIST, which legitimately flaps at thin-market hours (the tsCount:0 green-but-empty class — known to bypass SMOKE-RETRY by design). Fix in app/api/smoke-test/route.ts: distinguish transport/HTTP failure (real FAIL) from 200-with-empty-listings (report soft INCONCLUSIVE, matching the checkHtmlContains pattern you added in d198e68). After one clean tick, resolve NEXTJS-4 with regression arming and it should finally STAY resolved. Verify: 48h with no NEXTJS-4 events. Revert: git revert.

Item 3 (LOW-MED, carried from addendum 4) — fmv-recalc maxDuration hard-kill blind spot: runs that blow the 300s cap die before the end-of-run log and are invisible (verified live 21:28/21:30Z 06-10: runs did mid-pipeline work per Vercel logs, no pipeline_runs row). Options: write a run-STARTED marker row at after() entry and finalize it at completion (kills become visibly-unfinished rows), or bound per-run work so 300s is unreachable, or both. Touches the fmv-recalc file — keep it telemetry-only like 89c4891, zero pricing-logic changes. Verify: count(started-markers without finalize) == 0 across a normal day. Revert: git revert.

Item 4 (LOW, verify-or-fix) — iOS WebKit "Load failed": 631b1de added the retry+toast to the dashboard refresh() (NEXTJS-1M). Confirm /share/[wallet] (NEXTJS-1K, 2 events) got the same treatment; if not, apply the identical pattern to the share page's client fetch. Verify: no new 1K events over a week of mobile traffic. Revert: git revert.

Status notes (no action): Sentry's reopened smoke issues (4/E/A/1E/W/14) are the evening saturation echo — regression-arming working as designed; re-resolve after 24h genuinely quiet (the monitor cadence handles this; don't churn them mid-window). NEXTJS-1N (auth-token lock steal, 1 event) watch-only. AF1 view closed (reads clean post-wmc-fixes; no rewrite). Champion's Path player_name closed working-as-intended (322 rows are genuinely playerless; display fallback covers). audit_lt_matches stays (hardened, holds the LT crosscheck data) until Trevor calls the drop. Focus-8b interim numbers: HIGH 2/890, MEDIUM 42/2,185 above 3x own median vs 63 pre-fix — cite these, don't re-measure the unfiltered population (sizing trap).

Guardrails: direct-to-main, no branches/PRs; PowerShell git; stage exact paths; npx tsc --noEmit clean; deploy READY + smoke green before closing; maxDuration cap 800; CRLF rules; never echo the pushurl PAT.

CLOSING: after Items 1-4, run the standard sweep (git status clean, rev-list 0, deploy READY, detect_stalled [], trust health 7/7, topshot-fmv-populate's 07:15Z tick ok=true on the new slot) and report end state as: zero open items anywhere, all watches time-gated and owned.
