# Claude Code handoff — 2026-06-10 (late): verify-challenge dead-end (TetrisLblock) — route-side residuals

Context: TetrisLblock (adam.rust@gmail.com, wallet 0xa9435b88c3798c19, allow_list active) is dead-ending on wallet verification with BOTH paths failing: FCL shows "Wallet did not return an address" (expected for Dapper-custodied wallets — copy problem, not a bug) and the listing challenge shows "Couldn't find a free Moment to list right now". Cowork diagnosed + shipped the DB half live; these are the route/UX residuals. Claude Code's direct file inspection wins over this doc on any disagreement.

What Cowork verified + shipped (do not redo):
- His wmc is healthy: 1,508 TS rows, 1,501 with fmv>0 + image, cheapest $0.21. 276 rows is_locked, 18 of them in the <=$0.30 tail. wmc lock data 2 days stale (lock_checked_at max 2026-06-09 01:38Z).
- SHIPPED live: migration audit_20260610_pick_verification_target_skip_known_locked — the strict picker RPC now excludes coalesce(is_locked,false)=true rows (NULL stays eligible; the route's live GQL check remains the authority). Verified: returns 8 unlocked-per-wmc candidates for his wallet.
- History: he DID get a challenge issued 2026-06-09 21:10Z ($10.90 on moment 26354516) which expired unresolved at 21:40Z (resolved_via=expired). Today's dead-end is the live GQL check rejecting all 8 candidates (found && !isLocked && !forSale) — most plausibly he locked or listed his dust commons between yesterday and today, and wmc can't see it yet.

Item 1 (HIGH, small) — widen the candidate pool + filter the relaxed picker. File: app/api/profile/verify-challenge/route.ts (pickCandidates, ~L64-82). (a) Bump the strict RPC call p_limit 8 -> 24 and the relaxed query .limit(8) -> 24 — with 1,225 unlocked-eligible rows in a wallet like his, 24 candidates makes an all-rejected outcome vanishingly unlikely; the per-candidate GQL loop already breaks on first success so cost is unchanged in the common case. (b) Add the same known-locked filter to the relaxed inline query (.or("is_locked.is.null,is_locked.eq.false")). Verify: tetrislblock retries and gets a challenge issued (wallet_verification_challenges gains an unresolved row). Revert: git revert.

Item 2 (MED, product+UX) — make the all-rejected outcome self-healing instead of a dead end. Same file, the no_listable_target branch (~L258): fire-and-forget a wallet-scoped lock-state refresh (mirror the existing backfill kick pattern in the same route — POST the lock-check path for this wallet if one exists; if only the batch cron exists, queue the wallet) and change the message to "Your cheapest Moments look locked or listed right now — we're refreshing your collection state; try again in ~10 minutes, or use owner attestation." Trevor product call embedded here: consider extending challenge expiry 30 -> 60 min (yesterday's challenge expired unresolved in the 30-min window; users listing from a phone are slow).

Item 3 (LOW, copy) — Dapper-wallet FCL error copy. The linked-wallet button surfaces raw "Wallet did not return an address" when a Dapper-custodied wallet can't FCL-auth (expected per CLAUDE.md known-issue 0). Find the verify modal component that renders that string and special-case it: "This wallet looks Dapper-custodied — Dapper wallets can't connect here yet. Use the listing method below instead." Verify: tsc clean + the modal renders the friendly copy. Revert: git revert.

Interim for Trevor: once Item 1 deploys (or even before — the RPC fix alone may suffice if his unlocked candidates pass live checks), have TetrisLblock retry the listing path; if he dead-ends once more, owner attestation (admin_verify_wallet) is the documented manual fallback so he isn't blocked on his +500 credits.
