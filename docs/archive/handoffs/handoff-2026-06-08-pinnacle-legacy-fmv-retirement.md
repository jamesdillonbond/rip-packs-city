Handoff 2026-06-08 — Pinnacle legacy FMV retirement (Item 2). Cowork-coordinated; do NOT drop until the CC route step below has deployed.

STATUS — Item 1 (reader wave) is COMPLETE and verified. There are ZERO live readers of pinnacle_fmv_snapshots left:
- Repo: no .from("pinnacle_fmv_snapshots") query anywhere (grep clean across app/lib/components and supabase/scripts/.github/workers — the only repo match is the frozen historical migration 20260511200000_*.sql, leave it).
- The last live readers were the AI concierge (lib/concierge/pinnacle-router.ts + fmv-distribution.ts); CC swapped them to per-render pinnacle_catalog on 2026-06-08 (commit a9f86af, deploy dpl_GrtjZpzSCnh1XeeDQenQS9vosiHd READY). Character-correct (legacy_edition_key spans up to 26 characters, so listing joins gate on character + edition_key with a representative collapse; (character,set,variant) is 1:1 with a render). Dead component components/pinnacle/PinnacleSniper.tsx deleted (980b6f1).
- DB functions: only 7 still reference the string. THREE are comment-only (already swapped, bodies read pinnacle_catalog/wmc): get_set_detail, get_wallet_moments_with_fmv, holdings_summary — NOT readers. FOUR are the legacy writer cluster (the retirement target): bridge_pinnacle_fmv_to_main, pinnacle_fmv_from_listings, pinnacle_fmv_from_sales, pinnacle_fmv_recalc_all.
- No views, matviews, or triggers reference it (verified). NOTE Cowork must still confirm its OWN live artifacts (not in repo) don't query pinnacle_fmv_snapshots before dropping — that's the one surface CC can't scan.

THE BLOCKER CC FOUND (handoff Item 2 understated this) — pinnacle_fmv_from_listings does TWO jobs, not one. Besides writing the dead pinnacle_fmv_snapshots table, its final UPDATE refreshes pinnacle_editions.ask_price / ask_source / ask_updated_at from pinnacle_cached_listings. That ask column IS still read live by get_edition_detail, get_moment_detail, get_collection_stats, get_platform_stats, pinnacle_listings_reconcile (+ the app/api/cron/pinnacle-listings-reconcile route). So you CANNOT just drop pinnacle_fmv_from_listings — that ask refresh has to be relocated first or those surfaces go stale. (pinnacle_fmv_recalc_all has no such side-effect — it only DELETEs+rebuilds the dead table from pinnacle_sales.)

SEQUENCE (must be in this order — dropping a fn that pinnacle-sync still calls = cron 500 every tick):

1. Cowork — ✅ DONE 2026-06-08 (migration audit_20260608_pinnacle_refresh_editions_ask). Fn signature for step 2: **public.pinnacle_refresh_editions_ask() RETURNS json** — SECDEF, service_role + postgres EXECUTE only; the ask UPDATE lifted verbatim; zero writes to pinnacle_fmv_snapshots. Verified live: {"editions_updated":30,"total_listings":142,"editions_with_asks":30}. ALSO done as step-3 prep: the rpc-fmv-watch Cowork artifact was the one live artifact reading pinnacle_fmv_snapshots — its Pinnacle panel is now swapped to per-render pinnacle_fmv_history (update_artifact applied; trend fills forward from 2026-06-08). Cowork will re-sweep the remaining artifacts + the trust-health/weekly-check scheduled prompts for the legacy string as the final pre-drop check when executing step 3.

2. CC — ✅ DONE 2026-06-08 (commit 0769091, deploy dpl_oLu5gx7MeDh4ekf863GUHh4T5pwJ READY, aliased to www.rippackscity.com). app/api/cron/pinnacle-sync/route.ts now calls pinnacle_refresh_editions_ask() + pinnacle_fmv_recalc_render_all() only; the two legacy writers are gone; log_pipeline_run observability kept. Verified: pinnacle_refresh_editions_ask() runs clean under service_role ({"editions_updated":30,"total_listings":142}); render engine unchanged (priced 1,794 on the prior tick). WATCH for Cowork's step-3 pre-flight: the NEXT scheduled pinnacle-sync run should log ok=true with extra.ask_refresh present and NO extra.fmv_from_listings / extra.fmv_recalc_all keys — confirm that one tick before dropping. The legacy table stops updating after the cron repoints (nothing reads it; rpc-fmv-watch already swapped to pinnacle_fmv_history).

3. Cowork — only after step 2 is live: drop the cluster as ONE migration with backups + count(*) capture for revert: pinnacle_fmv_from_listings(), pinnacle_fmv_from_sales(), pinnacle_fmv_recalc_all(), bridge_pinnacle_fmv_to_main(), then DROP TABLE pinnacle_fmv_snapshots (427 rows at 2026-06-08; capture the bodies + a row backup first). Verify get_edition_detail / get_moment_detail / collection-stats / platform-stats still return ask prices (fed by step-1 fn now) and that pinnacle-sync stays ok=true.

REVERT — re-create the four fn bodies (capture before drop) + restore the table from the row backup; revert the pinnacle-sync commit (git revert) to call the originals again. Keep pinnacle_fmv_recalc_render_all untouched throughout.

WHY NOW — the per-render engine (pinnacle_catalog.fmv_*, algo pinnacle-2.0.0-render) + pinnacle_fmv_history (trigger-fed) are the live home; the legacy table is fresh only because pinnacle-sync still calls the legacy writers, and nothing reads what they produce except that one ask side-effect. Closing this removes the last set-level FMV blend from the platform.
