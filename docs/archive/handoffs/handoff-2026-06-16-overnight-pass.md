# RPC nightly autonomous pass — 2026-06-16 (GENUINE OVERNIGHT, 01:02 PDT)

**Mode:** Genuine overnight (fired 08:02Z / 01:02 PDT, in-window). **Push AVAILABLE.** Shipped **0** / reverted **0** / repaired **0** / closed **0**. **4th consecutive clean overnight on the Supabase Small tier.** A quiet honest night — no candidate this run was both warranted and a fully-gated low-risk production change. The heavy 06-15 daytime wave (serial-FMV deepen + price-band + badge backfill, all Trevor) is additive / off-limits route-logic / fresh hot files, and was verified healthy via post-ship watch (nothing to ship or revert).

**Git environment note (NEW this run):** the documented `/tmp/rpc` clone path was unusable — this sandbox squashes new `/tmp` files to uid `nobody` (65534) while the shell runs as uid 1201, so reads worked but every git write (config/fetch/push pack temp files) hit `Permission denied`. Worked around by cloning to `$HOME/rpcwork` (owned by the session uid, fully writable + push-capable). A leftover `/tmp/rpc` from a prior run also could not be removed (same squash). Recommend the setup doc note `$HOME/<dir>` as the clone target on this mount, not `/tmp`. PAT harvested from `remote.origin.pushurl`, never printed. origin/main `ec6ecc5` at start AND end (no human push mid-session).

## Post-ship regression watch — ALL PASS, 0 reverts

Re-measured every change shipped by Trevor over the 06-15 daytime/evening wave (the night pass shipped nothing 06-15). All targets healthy; no regression attributable to any ship.

- **SERIAL-FMV family** (`0122f9c` additive #1/perfect premium line + migration `serial_fmv_estimate_helper...`; `a3db4235` deepen onto grid/trophy/sniper + migration `serial_fmv_onto_collection_grid...`; `30d2acaf` price_band_30d + migration `moment_detail_price_band_30d`; `dae7b0b`/`bfeab86` bake `SERIAL_FMV_PUBLIC=true`) — **PASS.** Independently exercised `get_moment_detail('857d3331-...')` (MEDIUM #1 serial, edition 249:8511 FMV $14.94): returns `serial_fmv` {basis tier_circ, label "estimated #1 premium", multiplier 5.45, sample_size 158, estimate_usd 81.49} AND `price_band_30d` {n 41, low 10, high 29} with no error — both additive fields populate correctly. Security invariants stay clean: `check_secdef_anon_execute_violations()` [] and `check_public_security_invariants()` [] confirm the `a3db4235` anon-grant on `serial_fmv_estimate` opened no hole (it is SECDEF but a pure public read estimate, intentionally exposed via get_moment_detail). FMV-by-collection unchanged (additive line, floored at edition FMV): TS HIGH+MED **3426** (HIGH 986 + MED 2440; up from 3364 baseline), AllDay **774** (up from 715). No new Sentry on the moment-page render path attributable to these. **Watch item carried (queue):** `refresh-serial-fmv-multipliers` still has 0 pipeline_runs — see SERIAL-FMV-MULT-CRON below.
- **BADGE-BACKFILL family** (`0df701c` set-driven backfill, `b8837bc` paginate + drop parallelName, `52b986c` moment-seeded fallback) — **PASS.** `topshot-badge-catalog` 3 runs / 12h, 0 fails, last 04:13Z. `badge_editions` grain holds: **0** non-int-pair TS rows / 9103 TS-keyed / 10893 total (the `badge_editions_block_topshot_uuid_key_trg` Q8 guard works); trust `ts_uuid_dupes_created_24h` 0/200. The 34 truly-unindexed residual editions are Trevor's tracking-only (no moment_id anywhere), not night-pass-shippable.
- **OFFER-SANITY-RAISE** (`60c1438`) — **PASS.** trust `offer_edition_gap_max_usd` 0/50 ok; security 0/0 (SECDEF anon hole stays closed).
- **SEED-REFRESH-WIDEN** (`0f3b8ca`, 6h->24h) — **PASS.** The 06:45-07:35Z cohort wave came in at **631 runs / 0 fails** — matching last night's 633/0, lighter than 06-14's 1955. Wave pacing decisively holding on the Small tier; the compute-add-on decision stays CLOSED.
- **N1 snapshot-institutional-wallets** — recovered during 06-15 (re-fired 10:07Z); `get_pipeline_alerts()` [] this run.
- **A1 owner-resolution** — concluded INEFFECTIVE by Trevor (`a126f44`); do NOT re-explore (disproven dead end).

## Health-drift triage — GREEN

- **Pipelines:** last 24h **8833 runs / 1 fail**; last 6h **1873 / 0**; cohort wave 06:45-07:35Z **631 / 0**. The lone 24h fail is `analytics-smoke` @16:13Z ("analytics smoke 1 fail(s)", the known DB-IO barometer transient, self-recovered). `detect_stalled_pipelines()` **[]**; `get_pipeline_alerts()` **[]**.
- **Security 0/0/0/0:** RLS-off base tables 0; anon/authenticated write on RLS-off base tables 0 (with `relkind IN ('r','p')` — the 54-row raw result is the documented all-views false-positive); `check_public_security_invariants()` []; `check_secdef_anon_execute_violations()` [].
- **Sentinel:** TS-UUID-keyed-editions-48h **0**; inert UUID fossils total **6406** (flat); editions flat (TS 15543 / AllDay 6191 / Golazos 581 / UFC 446 — no DUPE growth).
- **Trust health 8/8 ok:** edition_integrity 4/50, fmv_sanity 0/1, offer_edition_gap 0/50, pack_ev_board_stale 0.81d/2, pack_ev_depleted 0/30, pinnacle_ask 0.0h/3, pinnacle_fmv 22.1h/30, ts_uuid_dupes_24h 0/200.
- **FMV (improving):** TS HIGH+MED 3426 (986+2440), AllDay HIGH+MED 774 (204+570), Pinnacle per-render priced 1845. fmv_current TS NO_DATA 3818 includes the 6406 inert UUID fossils (canonical NO_DATA ~200, per the monitor's int-pair measure). Writers fresh.
- **UFC wmc null edition_key:** 2 / 4584 (fossil floor; UFC-WMC-NULLKEY stays closed).
- **unmapped_sales open:** 244 (208 AllDay non-budget V1 + 34 AllDay budget-exhausted + 1 AllDay v2 + 1 Golazos v2). See ALLDAY-V1-UNMAPPED-DRIFT below.
- **DB size:** 4681 MB (+41 vs 06-15 baseline 4640; benign creep; weekly-db-maintenance next ~06-20).
- **Sentry:** 1 unresolved — `JAVASCRIPT-NEXTJS-A` "smoke test failed: fmv pipeline healthy (analytics_pipeline_health)", culprit POST /api/smoke-test, 1 event, first=last ~04:06Z, Seer super_low. Single-event transient of the known smoke-noise class (momentary FMV-freshness blip under DB-IO); not a spike, not a pipeline_runs fail, not attributable to any 06-15 ship. Only ~4h old — left for resolution after the 24h-quiet mark (convention), not actioned.
- **Vercel:** 0 ERROR across the 20 most-recent deploys; prod HEAD `30d2acaf` READY (the latest commit `ec6ecc5` is a docs-only monitor-inbox commit, build-skipped via ignoreCommand = CANCELED, by design).
- **Artifacts:** 18 enumerated (13 active + 5 RETIRED tombstones). Monitor validated all 13 active GREEN at 06:15Z (~2h before this run); no schema change since (Trevor's last code ship 30d2acaf predates that validation; the night pass shipped nothing). None flagged broken, none repaired. Key backing objects were re-confirmed directly during triage (fmv_current, v_rpc_trust_health, badge_editions, serial_fmv_multipliers 37 cells, pinnacle_catalog 1845 priced).

## Shipped — none

No candidate was both warranted and a fully-gated low-risk production change. Everything actionable is off-limits route logic (FMV/ingest/resolver), a fresh hot file (<24-48h: all the SERIAL-FMV + BADGE commits), operator/Trevor cron/dashboard work, or destructive SQL. Correct outcome for a green platform after a heavy additive daytime wave.

## Queued — needs operator / Trevor / CC

- **SERIAL-FMV-MULT-CRON (NEW, refines SERIAL-FMV-WATCH) · LOW · Trevor/operator.** The `app/api/cron/refresh-serial-fmv-multipliers` route (shipped in `0122f9c`) has **0 pipeline_runs ever** — `serial_fmv_multipliers` (37 cells) was last computed **2026-06-15 23:06Z** by a one-off/manual run and is now ~9h stale and aging. Decide whether it needs a cron-job.org entry to stay fresh, or is manual-on-demand by design while the serial-FMV dial-in is mid-build. Impact LOW: estimates are labelled, floored at edition FMV, and the multipliers move slowly. NOT auto-shippable (FMV-pipeline route + external cron + mid-build feature = Trevor's call).
- **ALLDAY-V1-UNMAPPED-DRIFT (refined) · LOW · operator/CC.** Open unmapped held at **244** overnight (did NOT drain back to the ~90 baseline). Composition (measured 08:0xZ): **208** `nfl_all_day` / `onchain_dapper_v1` with **no** price_extraction hint (regular unresolved V1 — known $1-2 prices, e.g. nft 6818559/2917492, hint carries only `{nft_id, sale_source:v1_dapper}`; accumulated 06-12->06-15, **inflow stopped** — 0 in last 6h, newest 22:56Z 06-15); **34** budget-exhausted fossils (oldest 05-21, documented residual); 1 AllDay v2 + 1 Golazos v2 fossils. These are price-KNOWN but edition-UNMAPPED (nft_id->edition_key), likely old/burned moments the consumer GQL can't resolve — a class distinct from the budget-exhausted residual the recover-v1 cron targets. Resolver chain is HEALTHY (allday-unmapped-resolver/sales-indexer/promote_unmapped_sales all clean). NO data-integrity risk (rows correctly held OUT of `sales`; no FMV/analytics corruption). Decision needed: either (a) confirm the resolver is attempting these 208 and they're genuinely unmappable -> reclassify as permanent residual, or (b) if there's a resolver/recover-v1 coverage gap for the regular V1 class, raise its cadence. NOT auto-shippable (off-limits ingest/resolver logic + operator cron).
- **get_user_top_owned_moments 3-arg orphan (NOTE) · LOW · Trevor/CC.** `a3db4235` added a 4-arg overload (SECDEF, authenticated); the 3-arg overload still exists. If now orphaned (verify call sites), it's a `DROP FUNCTION` cleanup candidate — destructive SQL, off-limits to auto-ship, and the file is <24h fresh. Verify-then-drop on Trevor/CC's next pass.

## Carried (unchanged — in ledger, not re-raised)

N1 snapshot-institutional-wallets daily cron drop (operator; recovered 06-15), VERCEL cost family (Observability-sampling / Spend-pause / Fluid-concurrency — Trevor dashboard), A1-WORKER-PASSTHROUGH-CLEANUP (Trevor/wrangler), TFP-SLOT-WAVE-COLLISION (operator/gate), PIN-FMV-REKEY-WAVES 2/3, PIN-SYNC-CRON, PACKVIZ-GRID, P3-BUYERS, DUPE1, Q2/Q5/Q6, ANALYTICS-SMOKE-RESIDUAL, IPFS x2.

## Failed / reverted — none.
