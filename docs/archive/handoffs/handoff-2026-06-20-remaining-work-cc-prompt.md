RPC — remaining Top Shot parallel-conflation + data-completeness work (fresh Claude Code session, 2026-06-20)

GROUND RULES: work directly on main — no branches, no PRs. Commit via PowerShell git and re-verify git rev-list --count origin/main..HEAD = 0. tsc --noEmit clean on changed files. FMV writes stay delete-then-insert with collection_id NOT NULL. All Cadence / on-chain reads go through the proxy workers (topshot-proxy), never direct to a Flow node. After any DB change re-verify check_public_security_invariants() = 0 and check_secdef_anon_execute_violations() = []. Read the referenced handoffs before starting. Do the phases in order; verify each phase read-only before moving to the next; never leave a coordinated step half-done. Update CLAUDE.md Recent sessions + docs/overnight/ledger.md (with revert paths) as you ship each phase.

CURRENT STATE (verified 2026-06-20): Stage A (forward subedition keying, env TOPSHOT_SUBEDITION_KEYING=1) is LIVE and activated — buildEditionKey appends ::parallelID, the canonical predicate is widened to ^[0-9]+:[0-9]+(::[0-9]+)?$ across the code sites, the 6 public views, the sentinel fn, and the two set fns, and editions.subedition_id / subedition_name columns exist. There are 0 "::" editions so far — they appear gradually as sub>0 (Jukebox/Hexwave/etc.) sales ingest. Interim conflation guards are live on 4 public surfaces against topshot_conflated_editions (741 flagged): cross_collection_deals_board + topshot_underpriced_serials_board are SUPPRESSED (NOT EXISTS guard); topshot_serial_premiums_board + topshot_perfect_mint_premiums_board carry an additive is_conflated boolean (caveat — rows kept, frontend renders a "Parallel'd" badge/note). The refresh-conflated-editions cron is wired (cron-job.org job 7869080, daily 15:17 UTC). trust-health is 9/9. Ongoing autonomous backfills need NO action from you — the sales-history GHA backfill, the historical buyer lane (2023-24 via the spork lane), and the offer-fill backfill are draining on their own.

PHASE 1 — STAGE B: historical subedition remap (the big coordinated step). Follow the Stage B design in docs/handoff-2026-06-20-parallel-conflation-phase0-verified.md exactly. Summary: resolve each historical TS moment's subeditionID on-chain via the SubeditionAdmin resource (getMomentsSubedition / getSubeditionByNFTID, through topshot-proxy, batchable per nft_id — roughly 94K nfts per the handoff sizing); catalog ONE editions row per parallel (external_id setID:playID::subID, with subedition_id, subedition_name, the parallel's true per-parallel circulation_count, and — important for Phase 2 — its OWN thumbnail_url/video_url, because each parallel has distinct artwork); then re-map the historical sales + wallet_moments_cache rows to the correct "::" edition. ORDERING IS CRITICAL: catalog/backfill the parallel rows FIRST, then re-map, then verify — never a half-mapped state. Faster bulk remap source for SALES: the marketplace feed already exposes moment.parallelID (Top Shot GQL searchMarketplaceTransactions / dapper.market transactions — see the Phase 2 note in docs/handoff-2026-06-20-fmv-parallel-conflation.md); use it for bulk where present, with on-chain getMomentsSubedition as the authoritative per-nft fallback. After remap, recompute per-parallel FMV (the serial-aware base FMV from cd9d7b2 then applies cleanly within each parallel). VERIFY: the duplicate-serial signature is gone on remapped editions (same serial no longer maps to 2+ nft_ids within one edition), trust-health 9/9, per-parallel FMV sane (Traoré 233:8121 Standard about $23, Jukebox/Hexwave higher), security invariants 0.

PHASE 2 — PARALLEL MOMENT EDITION PAGES. Build per docs/spec-2026-06-20-parallel-moment-edition-pages.md. Page-per-parallel is the backbone (the "::" slug must round-trip through decodeURIComponent before get_edition_detail — same class as the single-colon fix bf3f4f6), each page rendering that parallel's own art / FMV / circulation / serials / OG / JSON-LD and self-canonical (do NOT canonicalize parallels to the Standard). Add the "Parallels" comparison module on every parallel page — a NEW siblings query keyed on the same setID:playID base (do NOT reuse get_edition_parallels, which is a different same-play/different-SET notion; this is same setID:playID, different subedition) — showing each sibling's own thumbnail + circulation + FMV, with the base/Standard page surfacing the full price ladder. Include the "::" URLs in the sitemap and apply the near-duplicate SEO guardrails (distinct titles/art/OG per parallel, self-canonical, the module as the internal-link graph). Requires Phase 1's cataloged "::" editions. QA via rpc-insights-qa.

PHASE 3 — REBUILD OWNERS + FIT THE SERIAL MODEL (both correctness-gated on Phase 1). Rebuild the topshot_special_serial_owners surface/MV per-parallel so a "#1" or perfect serial resolves to the correct parallel (about 14% of holdings were ambiguous pre-remap). Then fit the multi-factor special-serial FMV model PER-PARALLEL (player x badge x set x series x parallel x circulation, pooled/shrinkage regression) on the now-correctly-attributed history — it could not be fit on conflated editions because parallel price gaps masquerade as serial effects. Ship as reviewed pricing logic with per-prediction confidence and the fitted power-law (serial_fmv_power_model) as fallback, per the strategy doc spec (docs/strategy/topshot-sales-completeness-and-serial-fmv-2026-06-19.md).

PHASE 4 — DELETE THE INTERIM GUARDS. Once Phase 1 has re-keyed parallels and the boards compute honestly, remove the four topshot_conflated_editions interim guards: the NOT EXISTS suppression on cross_collection_deals_board + topshot_underpriced_serials_board, and the is_conflated caveat columns on topshot_serial_premiums_board + topshot_perfect_mint_premiums_board (plus the frontend "Parallel'd" badge/note in SerialPremiumsBoardClient.tsx and lib/serial-premiums-board.ts). Revert/teardown paths are in docs/overnight/ledger.md and the code comments. Re-verify the boards pass rpc-insights-qa after.

PHASE 5 (optional, low priority) — SERIAL=0 TAIL. About 480 TS sales on truly non-catalogued moments plus about 170 offer-fill rows from other sources still have serial_number 0/null; resolve via on-chain borrow if worthwhile, else skip.

OUT OF SCOPE / ACCEPTED — do NOT pursue: (1) the 2020-22 buyer/seller gap (160K sales at 0% buyer). There is no free recovery source as of 2026-06-20 — native Flow spork access is architecturally dead (nodes only serve the current spork), Bitquery deprecated Flow, and Flipside's free SQL shut down 2026-06-17 (data moved to the commercial SonarX). It is a documented, accepted, archival-only gap; recovery would require a paid commercial export joined on our tx_hashes. (2) The Vaultopolis re-packs feature.
