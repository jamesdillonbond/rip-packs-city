# Overnight autonomous pass — 2026-06-11 (~01:02 PDT / 08:02Z, in-window, PUSH OK)

Run id 2431929451. Genuine overnight window; lock taken over from the 06-10 RELEASED marker; no FREEZE. First full run on the sandbox-native clone flow (clone + pushurl harvest worked; prior run's stale /tmp/rpc was unremovable → used /tmp/rpc-<runid>). `git push --dry-run` OK at start. Shipped **3** DB migrations (subagent-verified / live-verified), reverted 0, repaired 0 artifacts (17/17 healthy). Ship budget 3/4 used.

## Reviewed
- Inbox drained (10 files, 06-10T15:15Z → 06-11T23:30Z incl. 3 Cowork interims + the Phase-2 green-light note) — all archived to `docs/overnight/inbox/archive/`.
- Post-ship watch on the 06-10/06-11 wave: p25/p26 pack-events, fmv-recalc telemetry wave (89c4891/553f850/abfe191/e386542), e3aee28 FMV-audit wave (focus 8b/c), tshb (cd77861/18fdf7e), TFP slot move (8956d44), render-id index, UX click-target ships.
- 20/20 prod deploys READY (head `f389035`). origin/main did not move during the run.

## Post-ship watch results
- **fmv-recalc heartbeat (e386542) VERIFIED WORKING:** 18 heartbeat markers, correlation query (from the code comment) returns **0 kill-orphans** — every heartbeat has a matching terminal row; zero maxDuration kills since deploy.
- **pack-events p25/p26 HOLDING:** all three cursors advanced well past the wedge block 154385136 (topshot_pack_purchases at 154442365, updated 07:54Z). 20 ok / 1 fail since 01:00Z — the single 07:54Z fail is `topshot_pack_opens: moment_acquisitions batch upsert (130 rows): statement timeout` during the 06:48–07:55Z saturation window, with the purchases cursor STILL advancing on that same run (chunking working as designed). One 75-min tick gap 06:39→07:54Z noted. Item 9 stays open until a full 24h clean (~01:16Z 06-12).
- **Focus 8c PASS:** 71 audit-repair editions, **0** re-spiked >3x repair value. 8b carried (02:25Z interim measurement stands: HIGH 2/890, MED 42/2,185, trending down).
- **tshb (topshot-sales-history-backfill):** the 3 manual runs (seed + 2 drains) are clean — 974 sales / 9 editions inserted (`source='ts_history_backfill_v1'`), pending 784→775, 0 gql_errors, **created 0 editions** (sentinel TS-UUID-48h = 0), fmv_sanity_flags = 0. **BUT the GHA schedule has never fired** — see TSHB-GHA-NOSCHED below.
- **TFP (topshot-fmv-populate): the decisive 07:15Z first-new-slot tick DID NOT LOG** — zero runs since 18:50Z 06-10 (last ok 00:50Z 06-09, ~55h). Per the 04:30Z addendum this is the ESCALATE branch: threshold restore (800→480) deliberately NOT done; the watchlist is now correctly flagging it (809m silent at 08:19Z). → TFP-SLOT-MOVE-FAILED, operator, HIGH.

## Health drift (Section 2)
- **Security 0/0 on all four checks** (RLS-off [], anon/auth-write-on-RLS-off base tables [] with relkind r/p, check_secdef_anon_execute_violations [], check_public_security_invariants []).
- **Trust health 7/7 ok** (closest: pinnacle_fmv_stale_hours 22.0/30 — resets at the ~10:07Z pinnacle-sync tick; pack_ev_board 1.32/2; pinnacle_ask 0.2h).
- **Sentinel TS-UUID-48h = 0. fmv_sanity_flags = 0. unmapped_sales 183 (flat). DB 4,251 MB** (post-drop baseline holding; +12–80 vs yesterday's post-drop readings, watch-only). Editions flat (TS 15,543 / AD 6,191 / Gol 581 / UFC 446).
- **FMV improving:** TS HIGH+MED **3,103** (904+2,199; was 2,852 at the 06-10 baseline), AllDay HIGH+MED **601** (141+460; was 481). TS SALES_ONLY 19→46 and ASK_ONLY 994→991 — the intended tshb effect beginning. Writers fresh (fmv-recalc ok 08:08Z-class cadence; TS sales ingest 08:03Z, AllDay 07:56Z).
- **Saturation:** calm 02–05Z (0.4–0.9%/hr). A 00Z midnight-anchor window (37 fails) and a **06:48–07:55Z post-wave window** (52 fails @07Z: wmc-fmv-populate 12, pinnacle-nft-resolver 8, pack-ev 8 — the known marginal 8s-cap basket). Root-cause signature matches the 00:50Z interim's vacuum-debt read: the 06:45Z seed-wallet wave (1,060 runs @06Z) → wmc autovacuum (completed 07:52:52Z, dead_tup now 15.8K) → basket tips over; DB fully idle + 08Z at 1 fail by 08:14Z. No single culprit chased, per the steer; re-baselined in metrics.
- **detect_stalled @08:19Z:** topshot-fmv-populate 809m/800m medium (TRUE positive — see TFP), backfill-pack-rip-metadata 146m/120m info + refresh-pack-grail-metrics-mv 116m/90m info (both last logged pre-window 05:53Z/06:23Z; both have the 553f850 every-exit-logs treatment, so silence = after() killed under saturation or dropped ticks — watch their 08:23/08:53Z ticks; if still silent on a calm DB, escalate the wrap gap).
- **Sentry:** 7 unresolved, zero new tonight — 1E/A/4/E/W smoke echoes (quiet 5–10h, resolve with regression arming after a quiet 06-11 daytime), NEXTJS-14 single event 13h ago (no recurrence — likely the saturation-window false-drift cousin; resolve-watch), NEXTJS-1N auth-lock steal (1 event, watch with 1M/1K).
- **Artifacts 17/17** enumerated; fragile backing queries validated live this run (AF1 view 22 rows fast — AF1 stays CLOSED; trust-health; sentinel_fmv_confidence_rows; liquidity now 162ms). None repaired.

## SHIPPED (2, both subagent PASS)
1. **`audit_20260611_analytics_liquidity_latest_per_edition_lateral`** — ANALYTICS-SMOKE-60S root fix. Diagnosis: `analytics_smoke_run` was timing out at its 60s cap nearly every tick for ~20h (statement-timeout cancels bypass the per-leg `WHEN OTHERS` handlers); pg_stat_statements showed the PostgREST call at mean 19.2s / max 58.7s, and `analytics_liquidity_distribution(NULL)` measured **66.8s standalone on a calm DB** — that ONE leg exceeds the whole budget. Its `shared` CTE did DISTINCT ON over all of partitioned fmv_snapshots — the exact AF1 anti-pattern. Rewrote it to the documented per-edition LATERAL (idx_fmv_edition_time seek). Output verified **byte-identical** on live data pre-ship (all 5 collection rows); new shape **162ms** (~400x); attributes preserved (SECDEF, STABLE, search_path, owner postgres, ACL postgres+service_role only); filtered path verified (ARRAY['ufc'] → 1 row). Subagent PASS on all legs + post-call 1.33s.
   **Revert:** re-CREATE with the prior body — identical except `shared` is: `SELECT DISTINCT ON (fs.edition_id) n.short_slug, fs.liquidity_rating, fs.confidence::text AS confidence, fs.fmv_usd FROM fmv_snapshots fs JOIN normalized n ON n.collection_id = fs.collection_id WHERE n.short_slug != 'pinnacle' AND (p_collections IS NULL OR n.short_slug = ANY(p_collections)) ORDER BY fs.edition_id, fs.computed_at DESC` (full prior source in pg_stat/this run's transcript and recoverable from the migration history).
   **Metric to re-check tomorrow:** `pipeline_runs` pipeline='analytics-smoke' ticks (:13/:43) log ok=true; the 18h fail streak ends at the first post-08:18Z tick. Sentry NEXTJS-A/1E stay quiet.
2. **`audit_20260611_watchlist_topshot_buyer_backfill`** — BUYERBF-WATCHLIST (queued 06-10, cadence gate met at ~34h). INSERT into pipeline_cadence_watchlist @**90m**/medium (AMENDED from the queued 40m: measured 194 runs/36h with max gap 50m + two >40m gaps — 40m would have false-positived twice; C-PIN lesson). Verified: row live, detect_stalled does NOT flag it (last run 08:14Z), watchlist count 62 with exactly 1 new row. Subagent PASS.
   **Revert:** `DELETE FROM public.pipeline_cadence_watchlist WHERE pipeline='topshot-buyer-backfill';`
   **Metric:** no false-positive stall alerts for topshot-buyer-backfill over 48h; a real ≥90m stall pages.

3. **`audit_20260611_analytics_smoke_run_timeout_110s_stopgap`** — the 08:43Z analytics-smoke tick STILL cancelled post-liquidity-fix, proving a residual: with the 66.8s leg gone the fn's calm-DB baseline is still ~40–50s vs its 60s proconfig cap (measured per-leg: `analytics_data_quality_overview` **10.1s standalone** — its serial-zero full-history counts ~0.8s/collection + 24h counts + mojibake regex sweeps; sales-freshness leg ~10s; fmv-freshness leg ~7s; pulse 6s; packs 5s; sales-summary 4.4s; liquidity now 2.3s ✓), so any load tips it over and the 57014 cancel (which bypasses the per-leg `WHEN OTHERS`) darkens the whole signal. STOPGAP: fn-level `statement_timeout` 60s→**110s** (`ALTER FUNCTION ... SET`) — empirically the governing timeout (authenticator default 8s; observed cancels ~58.7s = the fn's 60s); route lambda `maxDuration=300` holds it; ACL/search_path untouched. The real per-leg fix is queued (ANALYTICS-SMOKE-RESIDUAL).
   **Revert:** `ALTER FUNCTION public.analytics_smoke_run() SET statement_timeout = '60s';`
   **Live verification:** the 09:13Z tick was cancelled at EXACTLY 09:14:58.784Z = start+110.1s (postgres logs) — proof the fn-level SET governs; that tick ran during the :13 heavy-RPC collision window (the same `p_collection_*`/`p_deviation_pct` family caught at 17–85s of IO during the 08:39Z poll) and exceeded even 110s. **09:43Z also cancelled at ~110s** — under the 08:30Z+ morning load (steady ~6% fail-rate ambient contention) the fn exceeds even 110s on consecutive ticks, so the stopgap alone does NOT restore signal during loaded hours; calm-window ticks (02–05Z class, baseline ~50s) should pass at 110s where they failed at 60s. HONEST STATUS: root leg fixed (verified, necessary regardless), stopgap live + governing, signal restoration PARTIAL-at-best until CC lands ANALYTICS-SMOKE-RESIDUAL (per-leg optimization + consider a slot move off the heavy-RPC minutes). The stopgap is cheap to keep (one extra ~110s read-only connection per 30min) and trivially revertible if Trevor prefers 60s.

## QUEUED / escalations
- **ANALYTICS-SMOKE-RESIDUAL (MED, CC):** with liquidity fixed, `analytics_smoke_run`'s remaining weight is structural: `analytics_data_quality_overview` (10.1s calm — full-history `sales` serial-zero counts per collection (seq-scan all partitions, ~0.8s each), per-collection 24h sale counts (no `(collection_id, sold_at)` index), mojibake regex sweeps, and it scans ALL 8 collections rows incl. inert candy/panini — no `is_active` filter), the sales-freshness leg (~10s, same missing index), and the fmv-freshness GROUP BY (~7s; per-collection MAX is 0.6ms with the desc index — the grouped form doesn't use it). Options, in rising invasiveness: (a) add `(collection_id, sold_at)` index on sales partitions + restrict serial-zero counts to recent partitions; (b) add `WHERE c.is_active` to data-quality's per_coll (payload change — check readers); (c) rewrite the fmv-freshness leg as per-collection MAX subqueries; (d) split `analytics_smoke_run` into two fns called by the route. Then restore the 110s stopgap to 60s.
- **PACK-OPENS-WEDGE — SELF-CLEARED 08:39Z (downgraded to note):** the opens-leg 130-row `moment_acquisitions` upsert failed deterministically at 07:54Z + 08:24Z, freezing `topshot_pack_opens` at block 154437365 — then the 08:39Z tick succeeded (`ok=true`, 130 moments_linked) once IO contention eased. Live polling during the 08:39Z tick showed the contention source: concurrent heavy `p_collection_*`/`p_deviation_pct` RPCs in 17–85s of IO (one in BuffileRead — temp-file spill) sharing the minute with the worker tick. p25's idempotent chunk-retry worked as designed; no worker change needed now. Watch: if a 2+-consecutive-fail wedge recurs on a leg whose batch is already a single sub-cap chunk, CC options are smaller chunks (25–50) or a row-by-row 23505-style fallback on timeout. Also note: pack-events dropped ticks 06:54–07:39Z + 08:09Z (cron-side, watch).
- **TFP-SLOT-MOVE-FAILED (HIGH, operator — morning #1):** `topshot-fmv-populate` missed BOTH new-slot ticks (01:15Z + the decisive 07:15Z); zero runs since 18:50Z 06-10; primary TS marketplace ASK/LOW writer down ~2.5 days (core FMV unaffected — fmv-recalc healthy; tshb is independently chipping at ASK_ONLY). The slot move was server-verified recorded (8956d44) → the entry itself is broken/auto-disabled or the URL/auth got mangled in the edit. Operator: open cron-job.org, check job state + history per the 06-09 recovery recipe, re-enable/re-fire; watch 13:15Z. THEN restore the watchlist threshold: `UPDATE pipeline_cadence_watchlist SET max_silent_minutes = 480 WHERE pipeline = 'topshot-fmv-populate';` (deliberately left at 800 tonight so the alert that fired ~08:50Z is a true-positive page, not transition noise).
- **TSHB-GHA-NOSCHED (MED, operator/CC):** the tshb GHA workflow (id 293577673, state=active, YAML cron valid `7,22,37,52 * * * *`) has **zero `schedule` events ever** — only the 5 ship-time workflow_dispatch runs; 9+ consecutive scheduled ticks missed by 08:15Z. Likely GHA new-workflow cron registration lag (other repo schedules fire fine). Drain is safely paused (pending stays 775; no integrity risk). If still zero by mid-morning: re-save/touch the workflow file (a no-op commit re-registers the schedule) or fire workflow_dispatch drains until it wakes.
- **PACK-META-SILENCE — RESOLVED in-run:** refresh-pack-grail-metrics-mv 08:23Z ok=true AND backfill-pack-rip-metadata 08:53Z ok=true — both silences were saturation-kills of the after() during the 06:48–07:55Z window; logging fine on a calm DB. No wrap gap. detect_stalled clears as they log.
- Carried unchanged: USERNAME-CRON-UNWIRED, UFC-WMC-NULLKEY, OFFER-SANITY-RAISE, IPFS x2 (deferred), CRON-30S 3/4, PIN-FMV-REKEY-WAVES 2/3, PACKVIZ-GRID, NEXTJS-15/Q4, P3-BUYERS, Q5/Q6/Q8, N1, SMOKE-EDITION-TIMEOUT (CC, hot file), NEXTJS-1M/1K/1N watch. Item 9 (pack-events) closes after 24h clean ~01:16Z 06-12. Q7 infra: clone flow worked end-to-end this run — if the push lands, mark Q7 RESOLVED.

## Failed / reverted
None. No hard-stop triggered.
