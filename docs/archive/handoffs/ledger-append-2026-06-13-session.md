# Ledger entries to append — 2026-06-13 (interactive Cowork + CC session)

For CC: paste these into docs/overnight/ledger.md under the Shipped block. Produced from Cowork because I don't edit the ledger directly here — editing that large a doc over the Windows mount truncates it. Verify the one commit hash left as `<txn-history commit>` against `git log`, and dedupe against anything the 06-13 night pass (`71141ac`) already recorded under UFC-WMC-NULLKEY / buyer-backfill.

## Cowork-shipped live (DB / cron-job.org operator actions)

- **ANALYTICS-SMOKE-60S-RESTORE — migration `audit_20260613_analytics_smoke_restore_60s_timeout`** — reverted the 06-11 110s stopgap on `analytics_smoke_run()` to 60s after the Micro→Small upgrade brought its runtime to 3-6s (was ~40-50s calm). Closes ANALYTICS-SMOKE-RESIDUAL. **Revert:** `ALTER FUNCTION public.analytics_smoke_run() SET statement_timeout='110s';`
- **UFC-ENRICHMENT-DRAIN cron WIRED — cron-job.org job 7804392** (`POST https://www.rippackscity.com/api/cron/ufc-enrichment-drain`, `7,37 * * * *`, Bearer INGEST). Drained UFC null `edition_key` 1,252 → 2 (fossil floor). Closes UFC-WMC-NULLKEY. **Revert:** disable/delete job 7804392 (route is inert without the cron).
- **TFP-SLOT-WAVE-COLLISION fix — cron-job.org job 7658302** (topshot-fmv-populate): minutes [15]→[38], hours [1,7,13,19] UTC unchanged — off the cohort wave (:45/:59/:13/:27, tail ~:33). **Revert:** set minutes back to [15]. (Gate: restore watchlist 800→480 after 2 clean :38 ticks outside saturation.)
- **SMOKE-CONCIERGE-DAILY cron WIRED — cron-job.org job 7811731** (`GET https://www.rippackscity.com/api/smoke-test?concierge=1`, 09:08 UTC daily, Bearer INGEST). Reliable daily concierge LLM coverage after the per-tick concierge probes were gated off (the API-cost fix). **Revert:** disable/delete job 7811731.
- **VERCEL-SPEND-CAP SET $30 (Vercel dashboard, Cowork-driven via Chrome)** — Billing → Spend Management → On-Demand Budget **$30** + **Pause Projects ON** (was $1 / pause OFF = uncapped, the root of the $218.87 June / $168.29 May invoices). Set to $10 first, then raised to $30 per Trevor. Confirm dialog requires typing the team name "Rip Packs City". Verified persisted on reload ($0/$30, Pause Projects: On). **CAVEAT:** the pause halts ALL production deployments incl. the `/api/cron/*` routes → at the cap the whole pipeline platform goes dark, not just the website. Cycle Jun13–Jul13 burned $3.42/$20 credit on day 1; $30 on-demand likely trips ~mid-to-late cycle unless the Fluid cut lands first. **Revert/raise:** Billing → Spend Management → Configure.
- **REMAINING COST CUTS — consolidated into ONE post-clean-days pass (~06-15/16, Cowork+CC):** do NOT make piecemeal Vercel changes during the DBSAT recovery. After 2-3 clean Small-tier days, one measured pass covers: (1) Fluid ~$66/mo — reduce cron frequency where cadence allows + thin/stagger the seed-wallet-refresh ~1,260-child wave (CC gated this; it also reduces DB IO = double win; beware cursor-crons concentrate load if slowed); (2) Observability ~$24/mo — trim console.log volume in high-frequency cron routes + lower Vercel sampling; (3) maxDuration right-size on routes that finish well under their cap. Until then: cap $30 + the shipped Build-CPU skip + the concierge-API gate hold the line. FIXED already: Build CPU $125→docs-build skip (vercel.json ignoreCommand); Anthropic API $44→~$0 (smoke-concierge daily gate). Load-bearing/not cuttable: Supabase Pro $25 + Small compute (just upgraded to end the IO incident).
- **INSIGHTS-TROPHIES backing view SHIPPED — migration `audit_20260613_v_insights_trophies`** — new public `public.v_insights_trophies` (`security_invoker=on`, GRANT SELECT anon/authenticated/service_role; verified reloptions `{security_invoker=on}` + anon present). 1-of-1 + Ultimate-tier grail editions (TS + AllDay) with per-edition latest FMV via lateral on `idx_fmv_edition_time`. 683 rows / 94 priced (grails rarely trade → mostly ASK_ONLY/STALE/NULL — honest). Backs the new `/insights/trophies` surface; FRONTEND is a CC handoff (docs/handoff-2026-06-13-insights-trophies.md): route+page+OG+sitemap+canonical, mirror /insights/squeeze. **Revert:** `DROP VIEW public.v_insights_trophies;` (no other consumer).
- **LIGHT-MODE-BATCH-2 handoff written (CC) — docs/handoff-2026-06-13-light-mode-batch2.md** — finishes the deferred light-mode debt: dashboard (~60 literals, #080808/#fff wrapper cascade), collection/analytics/sniper monoliths (text-white classes + dark inputs), pack-dist client components. Token map + the 3 literal vocabularies + extend-the-CI-guard step included. Caveat flagged: VERIFY app/layout.tsx isn't already handled before touching it; audit's public/auth labels need a proxy.ts cross-check. No Cowork-shippable half (all .tsx).

## CC-shipped (commits on main)

- **`e26502e` — QR removed from trophy-case slab labels** (components/TrophySlab.tsx; narrowed the left column, kept the pinwheel). **Revert:** `git revert e26502e`.
- **`a1675fc` — UI cleanup batch:** slab collection-name removed; sniper "NATIVE" source chip removed; mobile theme toggle added to MobileNav (Collections sheet header); sniper thumbnails prefer canonical `editions.thumbnail_url` + a branded placeholder; Pack Sniper as a per-collection tab on TS + NFL All Day via new `/[collection]/pack-sniper` route (PackSniperClient with `lockedCollection`). **Revert:** `git revert a1675fc`.
- **`1d79539` + `83bb40f` — TS marketplace buyer/seller backfill widened** beyond `source='onchain'` (drops the `.eq("source","onchain")` filter so `topshot-buyer-backfill` drains ALL null-buyer TS sales) + `/api/ingest` stamps `source='topshot_gql'`. Null-buyer draining 105,385 → ~86K (~2.5d to clear, then auto-catches ~270/day). Keep cron 7776255 ENABLED — it's the drain engine. **Revert:** `git revert 83bb40f 1d79539` (re-adds the source filter / removes the label).
- **`<txn-history commit>` — Personal transaction history (`/dashboard/history`):** new SECDEF `get_wallet_transaction_history(p_wallet,p_limit,p_offset,p_kind)` (service_role-only, REVOKEd from anon/authenticated, wallet-agnostic for the future analytics any-wallet view) + `/api/wallet/transaction-history` (verified-ownership gated) + the dashboard page + a "History" header link. RPC verified live against `0xbd94cade097e50ac` (total 7,389 = packs 84 + buys 7,305; pulls 6,984 separate; titles/amounts/counterparties/thumbnails resolve). **Revert:** `git revert <commit>`; `DROP FUNCTION public.get_wallet_transaction_history(text,integer,integer,text);`
- **`f073ae0` — Smoke-test concierge API-cost fix:** the 3 live `/api/support-chat` probes gated behind `liveConcierge` (off on the per-tick run; on for `?concierge=1` + a ~09:00-09:24 UTC daily window); per-tick regression coverage preserved via direct `searchPinnacleDeals` lib tests + the synthetic-4xx probe. Cuts ~99.7% of the Anthropic API-Console spend — which was the smoke suite exercising the live concierge ~100-200×/day (real users ~2/week), measured at $43.94/mo, ~all Sonnet. **Revert:** `git revert f073ae0`.

## Already in focus.md (coordination only, no ledger action needed)

Fable-model outage fixed (all scheduled tasks moved off `claude-fable-5` → Opus); compute upgraded Micro→Small (DBSAT-IO-exhaustion class resolved, compute-add-on decision CLOSED); OFFER-SANITY-RAISE confirmed already-shipped (raise fn + monitor both live — ledger 212-213 is STALE, mark resolved); 8 stale DBSAT-0612 Sentry incident-echoes resolved (regression-reopen armed).
