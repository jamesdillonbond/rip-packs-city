# Handoff — F2 (revised) + F4 + DQ4, with validation baked in (2026-06-05, Claude Code)

Cowork did the read-only validation/groundwork for the three remaining FMV-thread items so they're de-risked. Develop on `main`, push to `main`, smoke after each. FMV-thread only.

Context: held TS HIGH+MED is now 24.4% (was 10%) after the fossil fix + dupe cleanup + A2. Canonical TS = 9,123 editions (0 bad circ, 37 honest-NULL tier). These three items finish the "accurate + honestly-labeled + maximal HIGH/MED" goal.

======================================================================
F2 — REVISED. Cohort estimation is DEAD; do the ASK fallback instead.
======================================================================
DO NOT build cohort/comparable estimation. Cowork validated it against live data and it fails:
- (set_id_onchain, tier) cohorts, >=5 priced members: 208 cohorts, only 22 (11%) tight (CV<0.40); **median cohort CV 0.977**.
- (player_name, tier) cohorts: 454 cohorts, only 65 (14%) tight; **median CV 0.906**.
Within-cohort price is driven by set-rarity × player × serial, which simple cohorts don't capture — a cohort estimate would be ~100% off for the median edition = confident-wrong. This is exactly what the whole engagement exists to prevent. Confirmed-dead; skip it.

WHAT TO DO INSTEAD (honest, high-value): wire the no-sales FMV fallback to `edition_offers.low_ask`.
- After the dupe cleanup the real NO_DATA tail is tiny: **705 canonical TS editions**. Of those, **535 (76%) HAVE a live ask** in `edition_offers.low_ask` but are still labeled NO_DATA — the ASK fallback isn't reaching them (fmv-recalc Step 5b reads `badge_editions.low_ask`, which doesn't cover them; `edition_offers` is the near-complete TS ask feed).
- Fix: in `/api/fmv-recalc` (the zero-sales branch / Step 5b), when an edition has no qualifying sales, look up `edition_offers.low_ask` (join `collection_id` + `external_id`) and write **ASK_ONLY** at the existing haircut (×0.90) — same pattern as the badge_editions ASK path, just add `edition_offers` as a source. This converts the 535 to honest ASK_ONLY.
- The remaining **170 (~1.9% of canonical) are truly no-market** (no sales, no ask) — leave them honest NO_DATA / "no market data yet". Do not estimate.
- Net: ~98% of canonical TS editions end with an accurate, honestly-labeled value (HIGH/MED/LOW/ASK_ONLY/STALE), ~1.9% honest NO_DATA. That is the real "toward 100%".
- Validate: NO_DATA-with-ask count -> ~0; ASK_ONLY rises by ~535; `v_fmv_sanity_flags` no regression; spot-check a few of the 535 now show ASK_ONLY at ~0.9×ask. Revert: git revert (snapshots self-heal).
- Apply the same to All Day (its ASK feed via the offers indexer); size it the same way first.

======================================================================
F4 — Basis on FMV tiles (pure presentation; consolidate, don't scatter)
======================================================================
Show the basis next to every FMV value so even a LOW/ASK_ONLY tile reads as trustworthy: "$X · N sales (30d)" / "ask-only $Y" / "no market data yet" / "$X · N sales, ask $Y". The inputs (confidence, sales_count_30d, ask) are already on the fmv payload.
Consolidate into ONE shared renderer rather than editing each site:
- Likely home: `components/entity/_shared.tsx` (+ `EditionsGridPaginated.tsx`) — build/extend a single `FmvBadge`/`FmvCell` that takes {fmv, confidence, sales_count_30d, ask} and renders value + confidence chip + basis line.
- Then use it across the user-facing tiles: `app/dashboard/page.tsx` (trophy case), `app/(collections)/[collection]/edition/[slug]/page.tsx` (KPIs), `app/(collections)/[collection]/collection/page.tsx` (grid), `app/moment/[id]/page.tsx`, `app/share/[wallet]/page.tsx`. (Sniper/market/insights already show deal context; align copy but lower priority.)
- Link the confidence chip to the existing `app/legal/fmv-methodology` page.
- No pricing logic — pure display. Verify on `0xbd94cade097e50ac` dashboard + a LOW edition + an ASK_ONLY edition. Revert: git revert.

======================================================================
DQ4 — Stop the dupe table re-filling (keep the drain drained)
======================================================================
CC drained the 7,374 UUID-dupe backlog cleanly, but the SOURCE is still live: **1,152 UUID-keyed TS editions remain and are still accumulating — newest created 2026-06-05 04:39 UTC**, from the `compute-topshot-pack-ev` -> `seed_topshot_editions` UUID-fallback writer (the deferred "Item B2" leak in CLAUDE.md, ~250 inert rows/6h). They're inert (the `editions_block_topshot_uuid_dupe_trg` trigger gates them) so no canonical corruption, but they refill the dupe table and slowly degrade `v_edition_integrity_flags`.
- Fix: make `seed_topshot_editions` prefer the on-chain integer pair (`setID:playID`) instead of the UUID pair — mirror the 2026-05-30 `/api/ingest` `buildEditionKey` fix (fetch `set.flowId`/`play.flowID` via the proxy and key on the integer pair; UUID only as a defensive fallback). After the writer is fixed, a one-time re-drain of the residual 1,152 leaves the canonical set permanently clean.
- Validate: `SELECT count(*) FROM editions WHERE collection_id='95f28a17-224a-4025-96ad-adf8a4c63bfd' AND external_id !~ '^[0-9]+:[0-9]+$' AND created_at > now() - interval '6 hours'` -> ~0 after deploy. Revert: git revert.

======================================================================
SEQUENCE
======================================================================
1. F2-revised ASK fallback (highest value, lowest risk — converts 535 NO_DATA -> ASK_ONLY honestly; same for AllDay).
2. DQ4 writer fix (stops re-accumulation) + one-time residual re-drain.
3. F4 basis-on-tiles (its own focused presentation pass via the shared component).
Validate each against `v_tracked_wallet_fmv_confidence` + `v_fmv_sanity_flags`; all independently `git revert`-able.
