# Handoff 2026-06-08 — Pack & Moment history parity (the TS-screenshot feature set) + the full remaining queue

CONTEXT (read first)

Trevor wants pack pages and moment pages to match what nbatopshot.com shows on its drop pages: a "Packs Content Remaining" module (donut of packs unopened + per-tier remaining bars), pack Sales History (Top Purchases + Recent Purchases with buyer/price/time), per-moment recent sales, and a Special Serials breakdown. ALL the data plumbing is LIVE — Cowork shipped it tonight (migration audit_20260608_pack_purchase_dist_bridge_and_history_rpcs + audit_20260608_pinnacle_fmv_history_capture). Your job is page-layer wiring plus the optional worker enrichment, then the gated DB queue when the sentinel opens. Direct file inspection wins over this doc on any disagreement.

What Cowork shipped live tonight (verified working — do NOT re-create):
- pack_purchases.pack_dist_id backfilled via the pack_rips bridge (15,187 TS rows stamped; 7,600 secondary sales now dist-linked) + trigger pack_rips_propagate_dist_trg keeps it accruing as sold packs get opened. Coverage is PARTIAL (~20% of TS secondary sales today, grows organically) — every UI use must carry honest framing.
- get_pack_sales_history(p_collection_id uuid, p_dist_id text, p_limit int DEFAULT 10) — returns kind-tagged rows ('top' = highest price, 'recent' = newest) with buyer_address, seller_address, sale_price, sale_currency, sealed_at, tx_hash. Verified on dist 7800: top $745/$707/$689 DUC, recent rows from tonight. Dist 901 returns ZERO rows (its sold packs haven't been re-opened) — that's the empty-state QA case.
- get_edition_recent_sales(p_edition_id uuid, p_limit int DEFAULT 12) — price_usd, serial_number, sold_at, marketplace, buyer_address, seller_address, transaction_hash. Verified on 124:4493.
- get_edition_special_serials(p_edition_id uuid) — notable serials with tags '#1' / 'jersey' (from players.jersey_number via editions.player_id) / 'low' (2-10) / 'last_mint' (= circulation), each with last_sale_usd + last_sold_at when one exists. Owner intentionally omitted v1 (partial ownership coverage).
- pinnacle_fmv_history (render_id-keyed change history; seeded 1,794 rows; trigger-fed from pinnacle_catalog) — for Item 5.
All four RPCs are SECURITY DEFINER, service_role + postgres only — call them server-side via supabaseAdmin like the other entity-page RPCs. If a client component needs them, add a thin /api/entity/* proxy route instead of widening grants.

ITEM 1 (P0) — Pack page: "Packs Content Remaining" module
File: app/(collections)/[collection]/pack/dist/[distId]/page.tsx. The data already on the page: packs remaining/minted (the existing KPI), and per-tier remaining_by_tier + original_counts_by_tier from pack_distributions.metadata (the v20 writer maintains them; the corrected tier-odds table already reads them). Build the TS-style visual: a donut/ring showing packs unopened (remaining / minted, with the % center label — e.g. "55 / 900 (6%) Packs Unopened") + horizontal per-tier bars ("Common 328 / 5,355 (6%)", one bar per tier present, tier-colored via the existing tierColorVar tokens). Keep the corrected odds columns (do not regress the denominator fix); this module can absorb/replace the plain tier table if it reads cleaner, as long as odds/pack stays visible. Render only when pool/tier metadata exists (the hasDropPool gate already on the page) — never fabricate bars on no-pool packs. Brand tokens only.

ITEM 2 (P0) — Pack page: "Sales History" module (Top Purchases + Recent Purchases)
Same page. Server-side call get_pack_sales_history(collectionUuid, distId, 10); split rows by kind into two tables styled like the TS screenshot: Buyer | Sale Price | Date/Time. Buyer renders as short wallet (0x1234…cdef) linking to /analytics/wallets/<address> — that's our differentiator over Top Shot (their buyer name is a dead end; ours opens the buyer's full wallet intelligence). sale_currency is DUC ≈ USD 1:1 — render "$745.00". Required honesty: a small caption "Traced via opened packs — partial coverage that grows over time" (the bridge only links sales whose pack was later opened). Empty state (dist 901): "No traced sales yet for this pack." — module stays mounted, no fake rows. QA: dist 7800 = rich case; dist 901 = empty case.

ITEM 3 (P1) — Edition + moment pages: Recent Sales + Special Serials
Files: app/(collections)/[collection]/edition/[slug]/page.tsx and app/moment/[id]/page.tsx (+ the pin page app/pinnacle/moment/[id]/page.tsx gets Recent Sales only if cheap — pinnacle_sales is render-keyed; optional, skip if it balloons scope).
3a. Recent Sales table on the edition page via get_edition_recent_sales(editionUuid, 12): Price | Serial | Date | Marketplace, buyer as short-wallet link as in Item 2. On the moment page (which is one serial of an edition), show the same edition-level list but HIGHLIGHT rows where serial_number == this moment's serial.
3b. Special Serials board via get_edition_special_serials(editionUuid): chips/rows for #1, jersey match (tag 'jersey' — players.jersey_number drove it), low serials, last mint, each showing its last sale (price + date) or "never sold". On the moment page, if the current serial matches a special tag, show a badge on the hero ("Jersey match #11", "Serial #1", "Last mint"). Note jersey can be NULL for many players — render only returned rows.
Verify: edition 124:4493 (Cam Reddish) renders both modules with real data; a moment page for a special serial shows its badge; JSON-LD untouched.

ITEM 4 (P2, optional but the real 100% lever) — pack-events-ingest worker: resolve dist at purchase time
The TS purchase/withdraw events carry NO distId (that's why the bridge exists). The durable fix for FUTURE rows: in workers/pack-events-ingest (deployed via wrangler, NOT git push — see worker-deploy-drift), when handling a TS PackNFT event, resolve the pack NFT's distribution via a Cadence script borrow of the PackNFT (its distId is on-chain metadata) through the existing proxy/REST pattern, budget-capped like the V1 tx-decode budget (~25/tick), writing pack_dist_id at insert. Backlog stays bridge-covered. Verify the Cadence field against the deployed PackNFT contract via the Cadence MCP BEFORE writing the script (per CLAUDE.md Cadence rules). If the borrow is awkward, an acceptable lighter variant: nightly batch that resolves dist for the newest N unresolved purchase rows via the same script. Coverage goal: new secondary sales ~100% dist-linked regardless of open status.

ITEM 5 (P1) — get_edition_fmv_history: swap the Pinnacle leg onto pinnacle_fmv_history, then start the retirement clock
The new table is live, seeded (1,794 renders), and trigger-fed on every engine change. Swap the fn's Pinnacle branch from legacy pinnacle_fmv_snapshots to pinnacle_fmv_history keyed by render_id (the 1:many edition→render caveat from the waves doc applies — the chart consumer must pass/resolve a render_id; follow the Wave-1b pin-page pattern). After the swap, grep repo + pg_proc for remaining pinnacle_fmv_snapshots readers; at ZERO, remove the legacy writer call from app/api/cron/pinnacle-sync/route.ts (keep the render engine), and tell Cowork to drop the legacy table+fns (it has the revert bodies).

ITEM 6 (GATED — run only after the gate notifier fires) — DUPE1 merge, then Tier-B
A scheduled check pings Trevor ~4 PM 2026-06-08 when the TS-UUID sentinel crosses below 250. Then, in order: (1) DUPE1 canonical merge per docs/migrations/dupe1-merge-plan-2026-06-07.md — re-verify the gate + every count yourself; build the map table; the pre-measured 666 dupe-vs-canonical tx collisions get DELETED before the repoint; backups before every destructive step. (2) Tier-B sales re-map per docs/handoff-2026-06-07-tier-b-sales-remap.md (incl. the 8:62 Giannis fix). Both docs are self-contained.

ITEM 7 (P3 polish, batch with any of the above)
- Pack hero chip renders "Fandompack" (tier chip + the word pack with no spacing) on slots-unknown packs — add the space/separator.
- After Items 1-3 ship, add one smoke assertion: pack dist 7800 page contains "Sales History" and edition 124:4493 page contains "Recent Sales".

GUARDRAILS (standard)
Direct-to-main, no branches/PRs. PowerShell git; verify push with git rev-list --count origin/main..HEAD (expect 0). tsc + corruption-guard before push; smoke after deploy READY. Full-file replacements; brand tokens only (no hardcoded hex); maxDuration cap 800s; workers deploy via wrangler with repo copy kept in sync.

END STATE: pack pages show contents-remaining + traced sales history with buyer intelligence links; moment pages show recent sales + special serials with hero badges; future pack sales self-link to their dist; Pinnacle history charts run per-render and the legacy table is on a countdown; DUPE1/Tier-B executed once the gate opens.
