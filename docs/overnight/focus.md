# Focus — 2026-08-17, steers appended through 2026-09-05 (accuracy-gate phase; the June studio-platform program is HISTORY)

⚠ **This file was 54 days stale until 2026-08-17** (it was still the 2026-06-24 studio-platform post-ship watch). That is not merely untidy: three of its steers had gone **actively wrong**, and a night pass following them would have been misdirected. The obsolete steers are listed at the bottom under "RETIRED STEERS" with the reason each died, so nobody re-adds them from an old copy. The June program's detail is **not lost** — it lives in `docs/overnight/ledger.md` and `docs/handoff-2026-06-24-studio-platform-gql-deep-history.md`.

**Rewrite rule for whoever edits this next: a focus file STEERS the next night, it is not an archive.** If a section is describing something that shipped more than ~a week ago and is not still a live trap, move it to the ledger and delete it here. A stale steer is worse than no steer.

## STEER — added 2026-09-05 ~07:45 PT (Trevor's box, interactive; a second block — 2 ships, 1 filing, and two artifacts of my own instruments)

1. ✅ **The pack-detail route's error count was DOUBLE its real one — one timeout logged twice.** `lib/pack-dist/fetchers.ts`'s local `bounded()` helper logged in its catch AND every call site logs in its `if (error)` branch, so one failure became two Vercel error groups with identical counts (`pack_lifecycle bound` 6 / `pack_lifecycle error` 6). ⛔ **Do not re-add a log to `bounded()`** — the caller's is the one to keep, and the reason is COVERAGE: the helper's catch fires only on the reject path, while a real PostgREST error resolves normally and reaches only the call site. Pinned by `pack-dist-bounded-reads-are-logged-exactly-once` (ban at zero on the helper + every site must log; a 14th site without a log would now fail *invisibly*, which is worse than the double-log). ⛔ **It fixes NO timeout** — the reads still exceed their 5 s bound at the same rate.

2. ✅ **70% of the runtime-error surface was ONE Node deprecation warning, imported by a single string constant.** `/api/cron/ufc-enrichment-drain` took `UFC_COLLECTION_UUID` from `wallet-backfill-helpers`, whose chain pulls `@onflow/fcl`; something in it calls `url.parse()`, and Node emits DEP0169 once per process, so every cold start of this `maxDuration = 300` cron logged one — **174 of ~250 events in 12 h**. Now takes `COLLECTION_UUID_BY_SLUG.ufc` from `@/lib/collections` (SDK-free, identical UUID).
    ⭐ **The attribution is a measurement, not an import-graph reading:** over 7 days the group's `routes` field lists **exactly the two routes importing that helper** and **not `/api/sniper-feed`**, which had 254 timeouts in the same window and so no shortage of cold starts. ⛔ A static `grep node_modules/@onflow` for `url.parse(` found only a **build-time** tailwind file — that is NOT evidence about the runtime chain, and I nearly shipped on it.
    ⏳ **OWED:** after deploy, `/api/cron/ufc-enrichment-drain` must **drop out of that group's `routes`** while `/api/wallet-backfill-ufc` remains (it genuinely runs Flow scripts and still needs the SDK). ⚠ The warning is once-per-process, so the test is whether NEW events attribute to this route — **not** whether the historical count falls, which it cannot.
    ✅ **FIRST READING, 2026-09-05 15:10Z — clean, and the timing is exact.** The group's last occurrence is **14:37:10Z**, which is precisely the last cron run BEFORE the ~14:40Z deploy; the next run at **15:07:10Z** produced **no event** (`get_runtime_errors` for that route over 30 min: none). ⚠ **n = 1 cold invocation.** The deploy invalidated every warm instance and this route has no other caller, so 15:07Z was necessarily cold — which is what makes one run worth reading — but it is one run, not a rate. ⚠ **The `routes` field still lists both routes** and will keep doing so: the window is cumulative and includes pre-deploy events. **The forward test is whether NEW events attribute to this route**, and the next few runs (15:37Z, 16:07Z…) settle it.

3. 📋 **The image-weight filing (`2026-09-05T1235Z`) is answered — all three falsifiers run — and the problem is NOT where it was filed.** F1 survives with a −36% magnitude correction (40-CID sample: **0 of 39 under 1 MB**, median 3.55 MB vs the filed ~5.1 MB mean). F2 discharged — byte-identical **warm** and cold, nothing resizes. 🚨 **F3 relocates it:** `/insights/trophies` moves **75 MB on load / 135 MB after three screens** at 390 px — but **90% is `assets.nbatopshot.com` (67.58 MB) and only 9.7% our proxy**, so the filed lever (resize in the proxy we own) addresses a tenth of it. A **7.59 MB PNG** lands in a **165 CSS px** tile.
    ⭐ **A free 190× lever exists** — the CDN's own `/media/<id>/image?width=400` (37,570 b vs 7,130,685), already used at 8 call sites, no metered Vercel transforms. ⛔ **But it flattens transparency:** the source is RGBA (IHDR colour-type 6), the CDN's webp is lossy VP8 with **no alpha chunk**, `?format=png` is ignored, and the decoded corner is **opaque black** — against `MomentMedia`'s theme-aware `rgba(255,255,255,0.05)` that is **a black slab in light mode**. ⛔ **Both alpha-preserving escapes are refuted:** `?width=` on the raw path is **IGNORED (200, full 6.8 MB)** — not rejected, so a status-code check would read as success — and no smaller variant exists at a predictable path (512/400/256/640/1080 all 404). **Losing transparency is a product call; nothing shipped.** Detail: inbox `2026-09-05T1400Z`.

4. ⚠ **TWO of my own instruments produced artifacts this block — both caught, both worth not repeating.**
    · **A hand-rolled `pg_stat_statements` delta invented a runaway.** Differencing two snapshots directly reported `refresh_mv_pack_ev_latest` at **1,080 calls / 12.1 M reads** in a 31-minute window — for a job scheduled `3,33 * * * *`, i.e. **one** call. It is a pgss **eviction/re-creation** artifact (the same query showed a *negative* `d_calls` on another row). ⛔ **Use `ops_pgss_delta`, which flags `counter_reset`; a hand-rolled difference cannot tell an eviction from traffic.** By the correct instrument that function is not in the top 8.
    · **`editions.thumbnail_url` does not predict what a page renders.** I picked the worst-case set page from the database — `Heat Check`, **60 of 60** rows carrying an IPFS thumbnail — and it requests **ZERO** proxy tiles and moves 0.51 MB. **Load the page before believing the query.**
    · **A computed-style CONTRAST probe is unreliable on this site — do not QA light mode with one.** Walking ancestors for the first non-transparent `background-color` is defeated by gradients, background images and overlays, so it reported **161 low-contrast elements** on pages that visibly work and **47 vs 9** light-vs-dark on `/insights/top-sales`. ⛔ Checked the three worst by SAMPLING PAINTED PIXELS instead — screenshot each box, measure luminance spread — and all three render normally (spreads **225 / 108 / 105**, 61–157 distinct colours). **Light mode is healthy on the 10 surfaces swept**; the probe was wrong. ⭐ **The reliable method is the rendered image, not the cascade** — it cannot be fooled by the three things that broke the first probe.

5. ⓘ **The Supabase security advisor was re-read (it is TRIAGED, not auto-fixable) and ONE count moved.** `rls_enabled_no_policy` **217 → 243** — expected, the new `audit_*` tables from the 09-04/09-05 migrations, and RLS-on-with-no-policy is the SECURE default (it denies everyone but `service_role`), so still zero action. The **7 SECDEF-executable warnings are UNCHANGED**, name for name, and still match `secdef_anon_exec_allowlist`. ⚠ **But `function_search_path_mutable` went 2 → 3**, and the recorded exemption does NOT cover the third. The two known ones are COMMITting **procedures** where adding `SET search_path` would silently stop them committing; the new one, **`series_chain_numbers(uuid, integer)`**, is a plain **function**, `SECURITY INVOKER`, and anon/authenticated-executable. ⭐ Still low risk — a mutable search_path on an INVOKER function grants nothing, since the body already runs with the caller's privileges — **but do not wave it away with the procedure sentence, because that reasoning does not apply.** Left unfixed deliberately (security DDL, and `apply_migration` costs a ~10–20 s PGRST002 burst); the triage memory now distinguishes the two cases.

    ⓘ **The PERFORMANCE advisor was re-read symmetrically and nothing new is there.** Counts went **down**, not up — `unused_index` **269** vs the 284 triaged on 08-30, **331** findings total vs 345 — so the 08-30 triage still covers the population and no fresh class appeared. Two INFO singletons are the only things not already dispositioned, and **both are non-actions**:

    · **`auth_db_connections_absolute`** — the Auth server is capped at 10 connections and will not use more if the instance is resized. It is a **project setting, not repo state**, and we are not resizing (the constraint here is IO, not connections). **Trevor's, if ever.**

    · 🚨 **`table_bloat` on `net._http_response` — MEASURED, and the advisor's label is technically true but operationally misleading.** 833 MB total = **57 MB heap + 1.3 MB index + 776 MB TOAST**, against **306 MB of live content in 2,042 rows**. ⭐ **Retention is working perfectly**: the retained window is **09:26Z → 15:25Z, exactly the stock `pg_net.ttl = 6 hours`** (pg_net 0.20.0), and no reloption disables autovacuum on either relation.
        ⛔ **DO NOT VACUUM FULL / TRUNCATE IT.** Three independent reasons, any one sufficient: (a) it is the **edge-function instrument** — 6 h of response bodies is the only place a `net.http_get` pipeline's dispatched/killed/answered split is visible with no deploy, so truncating destroys live diagnostic history; (b) `VACUUM FULL` takes **ACCESS EXCLUSIVE**, which would block every `pg_net` dispatch — **13 active `cron.job`s route through this table**; (c) the table is **UNLOGGED**, so the bloat carries **no WAL, PITR or replication weight** — it costs **disk**, and disk is not this instance's constraint.
        ⚠ **I tested a wrong explanation first and the data refuted it.** I expected the 2.5× ratio to be a **high-water mark from a traffic spike**; hourly volume is instead **flat — 340 responses and ~50 MB in every full hour**, so there is no peak to blame. The correct reading is stronger: **~2.4 GB of content has churned through since the 09-03 VACUUM and the file is still 776 MB**, so space *is* being reused and 2.5× a steady 306 MB working set is simply this table's **resting ratio**. It is bounded by the TTL, not by cumulative traffic.
        ⚠ **I tripped a known hazard getting here, and checking the inbox first would have avoided it.** To find what keeps this table bounded I ran a broad `SELECT … command FROM cron.job`, which printed ~13 live `?key=…` gate keys into the transcript. **This is already filed** — inbox `2026-09-02T1100Z`: all 14 pg_cron HTTP jobs carry their credential in the URL, none in a header, and the standing credential probe is blind to them because **it greps `origin/main` and a pg_cron command lives only in the database**. ⭐ **Severity is bounded by a fact that filing already establishes:** register **D2b** records these values as *already burned in public git history* (the defeated 08-03 purge), so a transcript print is a second channel on an already-public key, **not a fresh exposure** — the remedy remains **rotation**, still blocked behind operator-gated edge deploys. **The operational rule for next time: select `jobname`/`schedule`/`active` and filter server-side; read `command` only for a job you have already named.**

6. ✅ **Verified, not mine:** Cowork's `pack_ev_latest` rewrite holds — 6 cold cache-busted probes of the two affected routes return **200 in 1.8–2.4 s** against the 8 s bound, and both error groups' last occurrence **predates** the 13:46Z migration. ⚠ Not closeable: at the observed base rates the elapsed window predicts only ~2.7 events, so P(0 | no change) ≈ 0.07 — the filing's next-day exit condition stands. ⭐ Incidentally it confirms the 09-04 `RPC_READ_TIMEOUT` work end-to-end: the stored error carries that `code`, so a bound timeout is now classifiable in production instead of looking like a generic 500.


## STEER — added 2026-09-05 ~08:00 PT (Cowork cloud, the overnight autonomous pass; 2 DB ships, 4 filings, and SIX corrections to my own claims)

1. ⛔ **Do NOT re-investigate `pg_net_http_403`. It is attributed now and the arm says so itself** (migration `20260905110532`). Every 4xx is a Cloudflare bot-challenge against the **Atlas editions walk**, joined `net._http_response.id` → `atlas_edition_requests.request_id`. It reports as **`atlas-editions-upstream-403` at `info`**. ⭐ **The escalation criterion is NOT the challenge rate — it is whether the retry keeps up**: it flips to `high` the moment any Atlas set goes >6h without completing a walk. Validated live at **8× its ship-time rate** (53 of 480 = 11.0%, one hour peaked at 18.3%) and correctly stayed `info` at 0 of 266 sets stale. ⚠ The ledger shows 09-03 and 09-04 each opened by re-deriving "benign" on this row; that is what this closes. ⛔ Anything that does NOT join keeps the original **critical** text byte-for-byte, so a stale `?key=` gate still pages.

2. ⛔ **`pack_ev_latest` is rewritten — do not re-derive it** (migration `20260905134612`). A correlated `EXISTS` on `pack_ask_state` ran **128,911 times for 382,519 buffers, returning zero rows every loop**, before `DISTINCT ON` cut 314,130 rows to 4,642. Now a `LEFT JOIN` on the UNIQUE `(collection_slug, dist_id)` key. **707,048 → 10,898 buffers**; equivalence proven both directions (4,642/4,642, `EXCEPT` 0 each way).
   ⚠ **The `COALESCE` is load-bearing** — without it a NULL `gross_ev` is kept by the old form and dropped by the new one (0 such rows today).
   ⚠ **It trades the index-ordered scan for a ~28–49 MB external sort.** Sized at ~1.2 refreshes/hour, so not a concurrency hazard — **it would become one if a high-frequency caller were added.**
   ⏳ **The falsifier is the only thing that closes it:** the two Vercel error groups (`/api/public/insights/pack-reality`, `/api/packs`) must stop. **Read `get_runtime_errors` after 2026-09-06.** Interim: 0 events since, but P(0 | the fix did nothing) ≈ 0.07 — suggestive, not closed.
   ⛔ **The `mv_pack_ev_latest` lever from 2026-08-30 is UNTOUCHED and still operator-gated.** That MV does **not** read this view and has neither `pack_ask_state` nor `EXISTS` — different object, different disease.

3. 🚨 **`pg_stat_statements WHERE query ILIKE '%<name>%'` CANNOT distinguish an object from every object whose name CONTAINS it.** I sized item 2's concurrency risk on `refresh_mv_pack_ev_latest`, which **is not a caller at all** — the `%LIKE%` matched `mv_pack_ev_latest`. Use a word boundary (`query ~ '(^|[^_a-z])name'`) or, better, **resolve real consumers from `pg_depend`**. The conclusion survived; the citation was wrong, which is worse in a document someone audits.

4. 🚨 **POOLED-VS-RECENT bit FOUR TIMES in one night — treat any `pg_stat_statements` mean as suspect until split.** The worst near-miss: three MV refreshers show **55,463 s ≈ 15.4 hours** pooled and 707,481 blocks/call, which reads as "this rewrite reclaimed hours." Their recent runs are **1.5–9.7 seconds**. ⭐ **A pooled mean spanning a fix measures the fix's absence.** The honest instrument is a **single-call delta**: snapshot `pg_stat_statements`, wait for one run, difference it. Done for jobid 245 — **15,329 blocks / 1,734 ms**, with `temp_blks_written` **0 across 548 prior calls → 3,967 on the first post-change call**, which is the positive control that the new code path is the one running.

5. ⛔ **The headless QA sweep FAILS TOWARD FALSE POSITIVES. Every negative result needs a control before it becomes a filing.** Five raised and all five refuted in one night:
   · **`cdn.nba.com` blank on 4 of 4 NBA team pages** — sandboxed-Chromium HTTP/2 artifact; **real Chrome renders it at naturalWidth 150, 0 blank of 84**. The host is already in `proxy.ts` `img-src` and curl gets 200/31 KB. **Do not re-file this.**
   · **My isolation test could not have passed** — a `data:` page cannot load ANY cross-origin image; the control (`assets.nbatopshot.com`, which loads 105/105 on the real site) also failed it. **A test that cannot produce a passing result is not evidence.**
   · **`/packs` "renders nothing on mobile"** — settle artifact; **83,753 chars / 2,000 cards after three scroll passes.** One pass + 3.5 s is not enough for a lazy list.
   · **"36 over-wide elements on `/insights/pack-reality`"** — the table is correctly inside an `overflow-x: auto` parent. **An overflow check must walk the PARENT CHAIN.**
   · **"The trophy case renders no art"** — 204 images `pending`, **0 failed**, all `loading="lazy"`, 8 in viewport on a 21,918 px page. ⭐ **`naturalWidth === 0` is a failure ONLY when `complete` is true.**

6. 📋 **Filed, needing a DESIGN call rather than a tune — the unmapped-drain stall test.** `drain_stalled` is `outflow_3h * 16 < outflow_24h`, and its own comment states the premise: *"steady state puts an eighth of the 24h outflow in any 3h window."* **This drain is BATCHED** (5,581 rows / 157 h, busiest hour **542**, median inter-resolution gap **0.00 h**), so a burst inflates the 24h denominator for a day and **it fires 66 of 144 hours = 45.8%** on a working drain — and suppresses the ETA. ⛔ **The obvious retune is REFUTED:** `12h × 2` measures **60.4%, worse**. **No current-rate-vs-24h-average test works on a batch process.** Three designs listed in inbox `2026-09-05T1125Z`.

7. ⓘ **`public_board_slow_count` breached on `topshot_2025_rookie_cohort_stats` (5,133 ms at 11:28Z) and it is LOAD, not shape** — the same shape reads **10 ms** now. ⚠ Both readings are `count(*)`, which **prunes the view's expensive CTE** (`player_editions` → *never executed*), so neither is what a real caller pays; they are comparable to each other and nothing more. The probe's sweep can be hours stale — check `sweep_age_min` before acting on it.

## STEER — added 2026-09-05 ~04:30 PT (Trevor's box, interactive; 10 ships, and THREE of them correct earlier work — two in the steer below, one my own from the same night)

1. ⛔ **Do NOT touch `app/api/fmv-recalc/route.ts` Step 6 (the stale touch) — shipped 2026-09-05 08:38Z and under an owed watch.** Its `DISTINCT ON` walk over `fmv_snapshots` became a per-edition `CROSS JOIN LATERAL`: **1,390,030 buffers / 27,248 ms → 138,788 / 2,055 ms (−90%)**. The old shape walked 1,429,511 rows to emit 27,849 and then discarded 27,844 of them — a million buffers to return **zero rows** — and at 27.2 s against the 30 s `statement_timeout` it was **killed on 27 of the 460 runs that executed it (5.9%)**, silently, under `ok = true`.
    ⚠ **Equivalence was PROVEN over the population, not argued**: both forms return 27,114 rows, EXCEPT-identical both ways. The tie question was checked rather than assumed — this table has **12,121 tied `computed_at` groups whose rows genuinely differ**, but **0 of 27,849 editions have a tie at their LATEST `computed_at`**, so "the latest snapshot" is unambiguous.
    ⛔ **Do NOT push the freshness/confidence filters INSIDE the LATERAL to "let the index do the work".** It silently changes the meaning: it would take the newest *old* snapshot for an edition that also has a fresh one, reporting a freshly-priced edition as stale and re-stamping it.
    ⏳ **OWED, and it is a RATE:** `extra.stale_touch_error` at **0 over ≥100 executing runs**, split at 08:38Z. Pre-fix 5.9%, so P(0 | unchanged) = 0.941ᴺ — ~50 runs to get under 5%. **At n=38 executing runs (2026-09-05 14:45Z): 0 timeouts against a pre-fix 8.67% (13/150), p50 66,187 → 49,642 ms.** P(0 in 38 | the rate is unchanged) = 0.9133³⁸ ≈ **0.032** — past the point where the null explains it, though still short of the stated ≥100-run condition. ⭐ **The step is also PRODUCTIVE, which is the control that matters**: `extra.stale_touch` is non-zero after the deploy (35 editions across 38 runs), so it is not fast because it returns nothing. ⚠ Per-run touches fell from 6.05 to 0.92, and that is the EXPECTED direction rather than a warning: before the fix an 8.67% kill rate left stale editions unprocessed to accumulate, and a step that now completes every tick drains them promptly, so fewer qualify at each one. (Final-set equivalence was proven separately: 825 rows = 825 rows, zero differences both ways.) ⚠ An earlier reading here moved twice — 5 of 5 (p ≈ 0.031), then 5 of 6 (p ≈ 0.11), then 9 of 10 — and is recorded that way on purpose: **a small-n statistic is not a result, it is a number that has not finished moving.** ⚠ **This number moved twice while being watched and is recorded that way on purpose** — 5 of 5 (p ≈ 0.031) at n=5, then 5 of 6 (p ≈ 0.11) when the sixth landed high, then 9 of 10. **A small-n statistic is not a result; it is a number that has not finished moving.** ⛔ **The DURATION effect is now solid; the TIMEOUT RATE is still uninformative** — 0 in 10 against a pre-fix 8.7% is P ≈ 0.41, i.e. exactly what the null predicts. These are different questions and the exit condition is the rate over ≥100 executing runs.
    ✅ **CORROBORATED BY A SECOND, INDEPENDENT INSTRUMENT (`pg_stat_statements`, not `pipeline_runs`), 2026-09-05 10:36Z.** Both baselines placed so neither window straddles the 08:38Z deploy — the pre window is snapshot-to-snapshot (06:05Z→08:05Z, both BEFORE it), the post window is baselined on the 10:05Z snapshot (AFTER it). `query_sql`:

    | window | calls | buffers/call | ms/call |
    |---|---|---|---|
    | pre-deploy | 91 | **96,781** | 1,603.4 |
    | post-deploy | 20 | **38,335** | 835.7 |

    **−60% buffers per call, −48% ms per call.** ⚠ Read it for what it is: `query_sql` is the aggregate of ALL fmv-recalc inline SQL, so this is the route's whole inline profile rather than Step 6 alone; the post window is shorter (20 calls vs 91); and instance load differs between the two clocks. ⭐ **What makes it worth having is INDEPENDENCE** — it shares no input with the duration evidence, so the two agreeing is not one measurement counted twice.
    ✅ **CORRECTNESS is settled even though the RATE is not — do not conflate them.** Re-verified 2026-09-05 09:30Z against the FINAL result sets (not just the intermediate `latest` sets): at a threshold chosen to give a non-vacuous population, **old 825 rows, new 825 rows, 0 differences in BOTH directions**. ⭐ And the productivity control matters as much as the speed: post-deploy runs report `extra.stale_touch` of **2 and 11**, so the step is finding and touching editions — it is not fast because it returns nothing, which is the way this kind of rewrite usually goes wrong. Pipeline healthy throughout: `ok=true`, 497–500 `rows_written`, no `stale_touch_error` at all.

2. ✅ **New BAN AT ZERO — `fmv-recalc-has-no-distinct-on-snapshot-walk`.** No query reading `fmv_snapshots` in that route may use `DISTINCT ON`; every one must use a LATERAL. **This defect was written four times in one file** (5c/5d/5e converted 08-31, Step 6 left behind), which is why it is a guard and not a comment. ⚠ Two drafts of the guard were wrong in instructive ways: a naive backtick scan **desynchronises on a backtick inside a `//` comment** (the route has one) and reported prose as an offender; and requiring the `CROSS JOIN LATERAL` spelling reported three correct `LEFT JOIN LATERAL` queries as missing. It now strips JS comments first (**asserting the strip landed**) and pins the property, not a count.

3. 🚨 **ONE shared SQL stripper, and a collision resolved by measurement.** Two sessions independently built one on 2026-09-05; `scripts/lib/strip-sql-comments.mjs` survives and `strip-sql.mjs` was deleted. ⛔ **Do not re-add a second one** — the ratchet is at **0** and there is now a shared stripper for each language this tree greps. ⚠ **The comparison found TWO real defects in the survivor**, both fixed: an apostrophe inside a dollar-quoted body escaped the body and blanked live SQL after it; and `Array.from` (code points) indexed with UTF-16 units **drifted every offset by one per emoji — and 50 of the 909 migrations contain astral characters**, which breaks the anon-exec guard's bounded 200-char window. ⭐ **The existing offset test passed throughout because its fixture was ASCII.** An offset assertion over ASCII proves nothing about offsets.

4. ⛔ **CORRECTION to steer 09-04 §"the art is fine, it is a cold cache" — that reading was WRONG, and so was my replacement's SAMPLE.** `/api/public/ipfs-media` was single-sourced on `ipfs.io`, which answers **2 of 8** CIDs; the route's own header recorded **~76% of uncached loads failing** and two prior fixes had both treated the gateway as a given. It now **races** a gateway list (`Promise.any`, not `race` — a 504 must not win) and losers are aborted. ✅ Verified in production: `/nba-top-shot/market` went **0 of 14 serving → 16 of 16**, all `cache=HIT`, images painting at 2880×2880.
    🚨 **But my gateway ranking was measured on 8 CIDs off `/nba-top-shot/market` — all Dapper-pinned, so `ipfs.dapperlabs.com` COULD NOT LOSE** — and UFC Strike, the only collection served from `ipfs.io`, was absent from the sample entirely. Ranking on it **broke every UFC image** until Cowork added Pinata as a third gateway. ✅ Verified: 3 UFC CIDs serve 200 at `cache=MISS`, `/ufc/edition/…` paints at 1080×1080. ⛔ **Do NOT re-order the gateway list on a UFC-only sample** — that repeats the same mistake with the collections reversed. ⭐ A per-gateway rate is only as general as the CIDs it was measured on, and **the only reason this was recoverable is that the header wrote down HOW it was measured.**
    ⏳ **Trevor item, not a night-pass one:** `cloudflare-ipfs.com` is still in the `proxy.ts` CSP and the host is **decommissioned** (0/8, instant DNS failure). Inert today, but a CSP is read as a claim about which gateways are available. Filed on inbox `2026-09-05T0700Z`.

5. ✅ **The E2E smoke can now see a blank image, and there are THREE assertions on purpose — do not collapse them.** A ≤50% same-origin ratio (≥4 images), **and** a stricter check scoped to our own media-proxy routes that fails when ALL of a page's proxy art is blank. ⚠ The ratio alone was **structurally blind** to the UFC outage: it needs ≥4 same-origin images and a UFC edition page has three, and most content art here is *not* same-origin (CSP-allowed CDNs), so page chrome dilutes it — a page's only artwork can break while the ratio reads 33%. Measured across 19 surfaces, proxy-image counts are **1, 1, 3, 17**, so a ≥4 floor misses three of the four pages that carry any. ⛔ **Do not make the proxy check a flat ban** — "some blank, not all" must PASS, and there is a fixture pinning that.
    ✅ **THIRD ARM added later the same night, after Cowork found the gap the first two share** (inbox `2026-09-05T0940Z`): `/nfl-all-day/set/genesis` renders **47 of 52 images blank** and passed BOTH — the ratio arm because those images are not same-origin, the proxy arm because they are not proxy URLs. **A page could be 90% blank and green.** The third arm is **per third-party HOST**: >50% blank of ≥4 decided images fails. ⚠ Non-reflexive because it was measured — across 13 pages / ~400 images every healthy host reads **0%** (`assets.nbatopshot.com` 0/114, `media.nflallday.com` 0/105) against Genesis at **79%**; healthy and broken are separated by the whole range. ⚠ **PER HOST, not per page** — pooling would let 114 good Top Shot images hide 19 dead All Day ones, which is the shape on `/insights/trophies`. ⛔ **It does NOT catch Genesis and that is deliberate**: `entity-smoke` picks the first `/set/` URL deterministically (`/nba-top-shot/set/clamps`), so that page is never visited — **which is also why it was safe**, since reaching it would pin the badge red on an upstream nobody can fix.

6. ✅ **RESOLVED, and it was the harness: `entity-smoke`'s intermittent failures were FALSE.** `assertHealthyPage` navigated with `waitUntil: "domcontentloaded"` and read the body **immediately** — the load-wait and hydration settle ran *after* the content assertions — so on a streaming route it measured the shell. Reproduced: `chars@DCL` = 2061, **25**, 2061 against `chars@settle` = 2061 every time; **25 is the number the failing run reported.** The read now happens after load + settle. ⛔ **Not a loosening** — `EMPTY_SHELL` still fails, and a fixture whose content arrives 400 ms after DCL now passes.
    ⭐ **The transferable lesson is about the investigation, not the fix.** I probed this **37 times without reproducing it** and filed it as a transient — because every probe waited for `load` and a settle before reading, **differing from the harness in the ONE dimension that decides the answer**. `0 of 37` read as "cannot reproduce" when it meant "not measuring the same thing". ⛔ **I also twice argued in writing against the correct fix**, from an unchecked assumption about the code's ordering; that filing (`2026-09-05T1100Z`) is corrected in place rather than quietly superseded.

7. ⛔ **The `sales` 90d-extension fetch in `fmv-recalc` is the estate's #7 disk reader and it is NOT a defect — measured 2026-09-05, do not "optimise" it.** `SELECT edition_id, price_usd, sold_at, serial_number FROM sales WHERE sold_at >= 90d AND price_usd > 0 AND collection_id <> pinnacle AND edition_id = ANY(...) ORDER BY id LIMIT 1000` shows up at **823,990 blocks read / 170 calls / 2,853 ms** over 6 h, which invites a rewrite. `EXPLAIN (ANALYZE, BUFFERS)` says the plan is already right: it uses `sales_2026_edition_id_sold_at_idx` with both `edition_id = ANY` and the `sold_at` bound as index conditions, and the `ORDER BY id` — the part that looks suspicious — is a **top-N heapsort over ~4,210 rows, 272 kB**. ⚠ **The `ORDER BY id` is LOAD-BEARING and not decoration**: the route pages this with `.range()`, and `sold_at` alone ties, so removing it would silently skip and duplicate rows across page boundaries. ⭐ The per-call cost (~5,000 buffers) simply reflects real work — 5,098 thin editions carry 43,710 rows in the 90-day window. **An expensive-looking query is not a defect until its plan says so.**

8. 📋 **THREE RED INSTRUMENTS ARE NOW EXPLAINED AND FILED — none is a production defect, and the shared cost is that a red badge nobody can read hides the next real one.**
    · **`pipeline-sentinel` — CRITICAL for 8 days** on three Top Shot pipelines that died with `public-api.nbatopshot.com` on **08-29** (the alarm says "since 09-04", which is its ROLLUP WINDOW, not the onset). Measured: `topshot-catalog-backfill` is **SUPERSEDED** (Atlas keeps the catalog current — 699 editions created/7d, 7,046 updated/24h), `ingest-topshot-challenges` wrote **0 rows even when healthy**, and `topshot-misattrib-drain` worked a **STATIC** backlog (88% of its 20,128 candidates have no sale in 90 days; oldest 2021-01-08). ⛔ Its death is a paused cleanup, not an escalating defect. **Ack or retire all three.** Inbox `2026-09-05T1015Z`.
    · **`db-pin-staleness` — red since 09-04.** Two pins were not repointed when their functions changed: `detect_stalled_pipelines` (pinned `20260903024204`, changed by `20260904041635`) and `upsert_wmc_batch` (pinned `20260812033600`, changed by `20260904062632`). ⛔ **The repo is COMPLETE — production carries no un-migrated DDL**; the "MCP bypassed the repo" reading was checked and refuted. ⚠ Do NOT blind-repoint: `upsert_wmc_batch` now keys a resolved parallel at write time, so the pinned test's assertions may describe 08-12 behaviour. Inbox `2026-09-05T1040Z`.
    · **`edge-fn-drift` — correctly red and ACKED to 2026-10-03** (gate-key secrets, Trevor). No action.

9. ⛔ **`app/api/recent-sales` line ~55 is DONE — do not re-open it from inbox `2026-09-05T0555Z`.** That filing names it "the one worth doing next"; the edition lookup is already `boundedRead` + `apiErrorResponse` on error + an explicit empty on no-match, and its header documents the defect it replaced.

## STEER — added 2026-09-04 ~21:40 PT (Trevor's box, interactive; three ships into `fmv-recalc` and the OG cards — read this BEFORE the 09-05 pass drains anything)

1. ⛔ **Do NOT touch `app/api/fmv-recalc/route.ts` Steps 5 or 5b — both shipped tonight and both are under an owed watch.** Step 5's two `query_sql` scans became one `fmv_recalc_uncovered_editions` call (migration `20260905033723`, 208,005 → 101,725 buffers); Step 5b's became `fmv_recalc_historical_candidates` (`20260905040111`, 513,102 → 306,847). Both are ledgered 2026-09-04 with full equivalence proofs. ⚠ **`MATERIALIZED` in 5b's function is LOAD-BEARING, not decoration** — measured, removing it makes the planner re-inline and reproduce the old plan exactly (513,184 buffers, no change at all). Do not tidy it away.
    ✅ **MEASURED EFFECT, split at the change point rather than pooled** (2026-09-04 22:50 PT): **`query_sql` is no longer the database's #1 reader — it is #3.** Normalised per hour, pre-fix 24 h vs a post-deploy window: **495,555 → 186,622 blocks/hour (−62%)** and **88.8 → 42.1 calls/hour (−53%)**, the call drop consistent with removing 3 of ~7 per run. ⚠ Three caveats, so nobody over-reads it: the post window is only **1.7 h**, its baseline snapshot (04:05Z) sits **8 minutes before** the 04:13Z deploy so a little pre-fix load is included, and **`ms_per_call` barely moved** (2,440 → 2,326) — which is expected, since what was removed was calls, not per-call cost. ⛔ A 6-hour `ops_pgss_delta` still STRADDLES the deploys and will show the fix's absence; use a window that starts after 04:13Z.
2. ⏳ **THE ONE THING OWED, and it is a RATE not a green tick.** Step 5b's candidate query was killed by `canceling statement due to statement timeout` on **38 of 459 runs (8.3%)** over 73 h. The falsifier is `extra.historical_fallback_error` reaching **0 over ≥100 runs**, **split at the 2026-09-05 ~04:13Z deploy**. ⚠ **A dozen consecutive clean runs is what the NULL already predicts** (0.917¹² ≈ 36%) — do not close this early, and do not pool across the deploy (a pooled rate measures the fix's absence). First post-fix run 04:28:05Z was `ok=true` with `historical_fallback: 48` and no error, which confirms the new function is CALLED and productive — it is not yet evidence about the timeout rate.
    📊 **INTERIM at 2026-09-05 09:55Z — 35 runs since the deploy, 0 timeouts**, against **36 of 427 (8.4%)** before it. P(0 in 35 | the rate is unchanged) = 0.916³⁵ ≈ **0.046**, so this has crossed from "what the null predicts anyway" into real evidence. ⛔ **It is still NOT the stated exit condition (≥100 runs) and should not be closed on this** — but a reader at ~50 runs with 0 timeouts can reasonably call it. ⚠ Read it off `extra.historical_fallback_error`, NOT `pipeline_runs.error`: these failures are recorded in `extra` under `ok = true`, and a first attempt at this watch read the `error` column and returned **0 timeouts in BOTH eras**, including a window already measured at 38. **A zero that contradicts a known non-zero is a broken instrument, not a result.**
3. ⛔ **The `get_pack_lifecycle` MV proposal (inbox `2026-09-04T1430Z` §6) is ANSWERED — do not build it.** The census it asked for: `pack_rips` holds **3,682,066 rows / 3,682,066 distinct `pack_nft_id`**, 1,988 MB, growing 1,226/day, against **~445 lifecycle calls a day**. That is the write-amplification branch the filing itself named. ⚠ Also: that filing attributes the bound-exceeded events to `/[collection]/pack/dist/[distId]`, but **that page calls `get_pack_lifecycle_row(p_dist_id)`, a DIFFERENT function** — the pack-keyed one has three callers (the per-pack page, `/api/wallet/pack-lifecycle`, the OG card), all long-tail. Re-group on `requestPath` before re-opening.
4. ⛔ **Do NOT "finish the job" on the remaining three fmv-recalc inline scans (5c/5e/5d).** Measured tonight at 76,125 / 31,550 / 43,331 buffers — together 17% of the original total, and none shows the pattern that made 5 and 5b worth fixing. ⚠ **5c's route header already records a FALSIFIED rewrite** (a `COALESCE` hoist measured at 120,508 buffers, i.e. WORSE). Full census: inbox `2026-09-05T0430Z`.
5. 📏 **A hypothesis filed and deliberately NOT actioned — treat it as a hypothesis.** `fmv_snapshots_2026` shows an ~85–90% **heap-fetch rate** on index-only scans in every plan measured tonight (17,294 of 26,722 probes; 16,516 of 18,946; 11,030 of 12,148). The obvious lever is a more aggressive autovacuum, and it is **not obviously right on an IO-bound instance — vacuum is itself IO**. The honest next step is a measurement (warm-vs-warm, what a manual `VACUUM` of that partition buys), never a settings change.
6. ✅ **The OG cards' DB reads are now a BAN AT ZERO** (`og-cards-bound-their-database-reads`): 10 files / 16 sites, all on `boundedRead(…, OG_FETCH_TIMEOUT_MS)`. ⭐ **The lesson generalises and is worth applying elsewhere:** nothing caught these because they fell between two guards that each looked complete — `og-fetches-are-bounded` walks the right directories but matches `fetch`, not `.rpc`; the read-bound ratchet matches the right calls but its POPULATION is *routes calling `apiErrorResponse()`*, and an OG card returns `null`, so all ten were outside it **by construction, not by exemption**. When you inherit a guard's exclusion, check the other instrument can actually SEE the property. ✅ **VERIFIED IN PRODUCTION 2026-09-04 ~23:40 PT: all TEN cards return real PNG bytes** (200, `image/png`, 24KB-311KB, 598-3451 ms — well inside the 10 s budget). A broken card is invisible: the unfurl is merely ugly and nobody reports it, so this was checked rather than trusted to the unit tests.

6b. 🚨 **THE ONE CHANGE THAT WOULD LOOK LOW-RISK TONIGHT AND WOULD SHIP A DEFECT: do NOT bulk-wrap the 21 routes named by `read-only-api-routes-outside-the-honest-error-population-are-counted`.** That new ratchet is the complement of the one above — 360 of 500 route files read Supabase, only 131 call `apiErrorResponse`, so 229 were never exempted, they were never LOOKED AT; 28 read-only ones bound nothing and **8 were converted, leaving 20 COUNTED** (the 8th, `/api/profile/me`, is the worked example of doing it RIGHT — it needed no honesty fix, only a bound, because it already set `identity_degraded` on every read error and simply could not REACH that branch on a hang; proven behaviourally, its new cases TIME OUT against the unbounded route). ⛔ **The remaining 21 are counted rather than wrapped on purpose.** Bounding a read only helps when the route's error path is HONEST; several of these degrade with `?? []` / `?? 0` and **no `if (error)` branch**, so a bound there turns a HANG into a **confident empty list** — strictly worse, because a timeout at least tells the caller something failed. **Fix the error path first, THEN bound, one route at a time.** A mechanical sweep over the list would ship that at all of them at once and read as a hardening commit. ⚠ Same reason `/api/acquisition-stats`'s collection-id helper is left unbounded and its header says so: both exits fall back to `TOPSHOT_COLLECTION_ID`, so a bound makes one collection's stats answer under another's label MORE often. ⓘ **20 is a CEILING on the client-side gap, not a census of unbounded reads** — checked against `pg_proc.proconfig` hours after the guard was written: `BOUNDED` matches only bounds written in TypeScript, and **`/api/profile/collection-stats` and `/api/sets` are already fully bounded DB-side** (`get_wallet_collection_stats` 20s; `get_topshot_set_detail`/`_progress` 25s each). ⛔ **Do NOT wrap those two** — a function-local `statement_timeout` CANCELS the statement, while `boundedRead` only abandons the wait, so adding a shorter client timer is strictly worse. They are left counted rather than exempted because an exemption list in a source-level test would be an unverifiable claim about `pg_proc`. Separately, the write/cron exclusion is a regex and may over-exclude.
7. ⚠ **Two UNRELATED flakes surfaced in full-suite runs tonight and neither is a regression** — `api-allday-listings-indexer` and `component-DashboardClient` (the latter `expected 2 to be less than or equal to 1` on a `waitFor`-counted fetch). Both pass in isolation (the second 3/3) and both were green on re-run; the suite finished **1,466 files / 16,226 tests, exit 0**. ⛔ Do not adopt either as your own failure — *a red run is not automatically yours, read the failing job first* — but if one recurs, it is a real flake worth pinning rather than re-diagnosing from scratch.
8. 🚨 **A GREEN PIPELINE DOING NOTHING, newly measured and FILED not fixed — `sales-counterparty-backfill`.** It is `ok=true` on every run and absent from every failure list, yet `claim_sales_counterparty_batch` is the database's **#3 reader** (3,089,242 blocks / 301 calls / 9,818 ms per call, 24 h). Split at its cursor's last advance (2026-09-03 12:57:37Z): **98 runs before it, 98 productive and 0 drained; 470 after it, 470 drained and 0 productive** — a step change, ~40 min of DB time a day returning zero rows. It cannot recover on its own: the claim's range is provably empty (221,183 index entries walked, all rejected, 195,564 buffers to return 0) and the worker only advances the cursor past rows it PROCESSED. ⚠ **Its `extra.note` says `"drained"`, which means "the claim returned nothing", NOT "the work is done" — 146 rows in the last 30 days still match but sit ABOVE the cursor, and the walk only looks below it.** ⛔ **Do NOT fix this tonight.** The durable fix is worker-side and `workers/**` **never auto-deploys** (needs a manual wrangler deploy — a Trevor item); the DB-side alternative mutates pipeline state and would strand rows a historical backfill later writes into that range. ⛔ **And do NOT add a partial index to make the dead scan cheap — that optimises a no-op.** Full filing + falsifiers: inbox `2026-09-05T0510Z`.
9. ⓘ **`pg_stat_statements` is at 99.3% of its 5,000-entry cap with `dealloc = 36`** since the 2026-08-12 reset, and **972 entries (19.6%) carry the MCP source tag, 817 of them with `calls = 1`** — one-shot query SHAPES, each consuming a slot.

   ⛔ **CORRECTION to my own claim, made the same evening: I wrote that each MCP call creates a unique entry "carrying a unique `-- date:` comment and so a unique queryid". That mechanism is REFUTED.** `reconcile_wmc_metadata_from_editions`'s entry carries a `-- date:` comment **and accumulated 10 calls in a 1.7 h window** — and it is driven by **pg_cron jobid 456** (`*/10 * * * *`), not by MCP at all. If a date comment split the queryid, that entry could never accumulate calls. pgss keys on the **parse tree**, so comments do not split entries; the stored TEXT is merely whichever caller registered the queryid **first**.

   🚨 **The consequence is the useful part, and it is a trap for the next reader: `-- source: POST /mcp` in a pgss row does NOT mean an MCP caller is driving that load.** It means an MCP call happened to be the first to register that query shape. **Attribution by the comment in `pg_stat_statements.query` is unsound** — I nearly used it to attribute the estate's #1 and #2 readers to a runaway session. Read `cron.job.command` instead, which is what settled it. ⛔ **Filed as context, NOT as an action item, and one hypothesis about it is already REFUTED:** `ops_pgss_delta` handles eviction correctly — it flags `counter_reset` when live calls fall below the baseline and reports live counters whole rather than emitting a negative. Raising `pg_stat_statements.max` needs a restart and is Trevor's call, not a night-pass change.
10. ✅ **SHIPPED and under no watch — the collection Analytics tab's eight panels no longer render "No data" for a query that FAILED.** `/api/market-analytics` published twelve detail RPCs as `res.data ?? []` with the error going only to a lambda `console.log`; it now returns `degraded: string[]` and the client renders "Couldn't load this panel" per panel. ⛔ **Do not "simplify" `PanelEmpty` back to a plain div, and do not drop the `degraded` key** — an errored panel becomes an empty one again, and nothing goes red except the two guards that exist for it. ⚠ The route is still UNBOUNDED (16 RPCs, only 2 with a `statement_timeout`) and stays on the complement ratchet — that is deliberate: the honesty fix had to land first, because a bound on a route that renders failure as "No data" only makes that claim MORE frequent.
    ✅ **VERIFIED IN PRODUCTION by rendered DOM, not HTTP 200** (Playwright, `/nba-top-shot/analytics?tab=market`, 2026-09-04 ~23:30 PT): `/api/market-analytics` answers **200 with `degraded: []`** — the positive statement that every panel was read — all seven panels carry real rows (10/5/10/146/4/9/278), and the page renders with **0 pageerrors and 0 console errors**. That last number is the one that mattered: this was a client-component edit, Sentry has stored nothing since 08-18 (#34), so a scheduled badge is otherwise the entire detection surface.
    ⓘ **Two side observations from that probe, neither chased:** (1) the page fires `/api/profile/me` **three times** in one load — small, but it is a signed-in read now doing 3× the DB work per page; worth a look before anyone optimises that route. (2) The Sentry envelope endpoint answered **200 three times**, which does NOT contradict #34 — *an accepted event is not a stored event*, and that 200 is plausibly why the drop went unnoticed for weeks.
13. ✅ **FIXED AND VERIFIED IN PRODUCTION 2026-09-04 23:15 PT — do NOT re-open it, and do NOT re-add `arweave.net` to `PROXYABLE_AVATAR_HOSTS`.** (Filing `2026-09-05T0700Z` describes the pre-fix state.) The host MOVED into the CSP `img-src` and OUT of the proxy allowlist; the browser now follows the 302 itself and the art paints at 2160×3240 with **zero CSP violations**, and `/api/public/avatar-media` answers **400** for an Arweave URL — refused before any fetch, which is the intended new behaviour rather than the old 502. ⚠ **Scope, after I refuted my own widening of it:** 125 Candy MLB editions carry an `arweave.net` thumbnail in the DB, but the only surface OBSERVED rendering one is `/insights/top-sales` — `/candy-mlb/edition/<slug>` requests no Arweave image at all. **"2 cards on one board" was the honest number**; do not restate this as a site-wide unblanking. 🚨 **NEW follow-up this fix CREATES:** those assets are **6.9–7.4 MB PNGs now hotlinked to every visitor** of a public board — better than blank, still bad; the answer is a resizing/CDN step for chain-two art, ⛔ NOT putting the host back in the avatar proxy (its 4 MB cap is why it rejected them). ⚠ A test written that morning asserting the OLD behaviour was INVERTED, not deleted. Original framing: Candy MLB sale art on `/insights/top-sales` renders BLANK — verified in production, two `<img>` at `naturalWidth = 0`. Today's change routed `arweave.net` through the same-origin avatar proxy because the CSP does not carry that host; the proxy **refuses redirects by design as its SSRF guard**, and `arweave.net` ALWAYS 302s. The failure moved from a CSP refusal to a 502. 🚨 **And the obvious fix is measurably wrong:** following the redirect still 502s, because the asset is **6,872,443 bytes against `MAX_AVATAR_BYTES` = 4,194,304** — full-size NFT art borrowing a proxy built for 80-pixel avatars. ⛔ **Do NOT raise `MAX_AVATAR_BYTES`** (its 4 MB is the measured edge-cache ceiling; raising it makes every anon load of a public board uncacheable). The likely fix is to MOVE the host into the CSP `img-src` and OUT of `PROXYABLE_AVATAR_HOSTS` — `avatar-proxy-hosts.test.ts` already forces those two to stay disjoint, so it is one action across three files. ⚠ Scope honestly: 2 cards on one board, **zero user avatars affected**.
12. ⓘ **`reconcile_wmc_metadata_from_editions` is now the estate's #1 reader, and it is NOT a defect — checked, so nobody re-investigates it.** After tonight's `fmv-recalc` cuts it sits at the top of `ops_pgss_delta` (~340 K blocks/hour, **27.6 s per call**, pg_cron **jobid 456** every 10 min). It was created 2026-09-04 by the Atlas audit and it is **converging, not spinning**: rows corrected per hour ran **146,804 → 122,719 → 68,704 → 95,413 → 59,285** through the initial drain (16:00–20:00Z) and has settled at **58–632/hour** since 21:00Z, with **4–6 of 6 ticks productive** and `cycles = 3`. ⭐ Contrast `sales-counterparty-backfill` in item 8, which is the opposite shape — 470 of 470 ticks correcting nothing. ⚠ **Its telemetry is one-sided by construction: it calls `log_pipeline_run` only `IF v_n > 0`**, so an unproductive tick writes NO row — which is why productivity is measurable from `pipeline_runs` at all, and also why a silence alarm on this pipeline would fire on health. 👉 **The open question is a POLICY one, not a bug:** ~8.2 M blocks/day to correct ~150 rows/hour is an insurance premium, and dropping the cadence from 10 min now that the drain is done is the obvious lever — but it is someone's same-day work and the drift it catches is real.
11. 📏 **The upstream cause of that defect class is now COUNTED, and the count is a trap — inbox `2026-09-05T0555Z`.** `const { data } = await <supabase read>` (discarding `error` at the point it is produced) is **197 sites / 86 files**; restricted to GET-only non-job routes it is **17 routes / 28 sites**; and of three spot-read sites **only one was a real false claim**. ⛔ **Do NOT sweep it.** A mechanical "add `error` everywhere" pass would touch 197 sites to fix perhaps a third of 28 and would have to invent an error policy per call site. ✅ **The one named as next — `app/api/recent-sales/route.ts:55` — is FIXED (ledger 2026-09-04)**, and it was NOT the open behaviour question I called it: the same file already forbade this shape for its sibling parameter ten lines up. A failed lookup now errors honestly; a resolved-but-missing one returns an empty list.

## STEER — added 2026-09-03 (Trevor's box, interactive; six "findings" re-derived, four ships — read before the 09-04 pass drains anything)

1. ⛔ **Do NOT re-file `topshot_impossible_parallel_serials`.** The 09-03 daytime filing's "NEW HIGH regression at 8" was the self-healer's routine inflow, read at :48 and healed at :52 on the same cadence. **Jobid 219 now heals at `:43 0,6,12,18`, BEFORE leg 324 reads at `:48`** (migration `20260903221540`). From 2026-09-04 00:48Z the arm reports the post-heal RESIDUE: a persisting nonzero is real (a timed-out heal, a `circulation_count = 0`, or a writer out-running four heals a day); the inflow rate lives in `impossible_parallel_circ_raises`. ⚠ The arm's in-view description still says "pages a future regression" — the correction is in `trust-board-and-safety.md`, not in the view.
2. ⛔ **Do NOT raise `candy_scarcity_board.max_ms`.** The board is MATERIALISED (`mv_candy_scarcity_board`, pg_cron **436** hourly at `:26`, 300 s; migration `20260903221326`); the wrapper reads 4 buffers. `max_ms` stays 3000 — a reading near 3 s again means the wrapper stopped reading the MV. Watch: `board_mv_refresh_stale_hours` now includes it at 6 h.
3. ⭐ **wmc REINDEX is weekly maintenance now:** jobids **438–441** (`3 2` / `23 2` / `43 2` / `3 3 * * 0`, cron_heavy, one bare `REINDEX INDEX CONCURRENTLY` each) + **442** verify (`23 3 * * 0`). **First reading: Sunday 2026-09-06 03:23Z `wmc-reindex-verify` row, exit = `ok=true`.** ⚠ A pg_cron `failed` on 438–441 is NOT the falsifier (REINDEX CONCURRENTLY commits outside the block); `invalid_left` on the verify row means a `<index>_ccnew` to drop by hand as ONE bare `DROP INDEX CONCURRENTLY IF EXISTS`. If `idx_wmc_cohort_cover` is ever dropped, unschedule 441 in the same pass. Register #56.
4. ⛔ **`allday-pack-opens-backfill` is not wedged, and there is nothing to ship.** It walks DOWN (a falling `scanned_floor` is progress); `status 0` is its own documented abort budget on the spork proxy (41 of 98 runs / 48 h, 4,081 rows written). The function is on the do-not-deploy list until its gate secret is set, so a skip-poison-range guard would sit undeployed anyway.
5. ⛔ **`wallet-username-resolver` "stopped 08-30" is the deliberate dead-host pause** (cron-job.org entry disabled, cadence arm paused by `20260831030424`) — `public-api.nbatopshot.com` is decommissioned. The open work is a PORT, not a schedule; do not re-enable.
6. 📏 **`refresh_wmc_fmv_changed` (#36): a fresh T2 baseline was laid 2026-09-03 22:24Z** (`rwfc_cron_T2_20260903` / `rwfc_rest_T2_20260903` in `_rpc_waste_baseline_20260825`). **Read it as a FLOW ≥ 24 h out (from 2026-09-04 22:30Z), delta ÷ calls, on a quiet instance** — that is the first clean read of the 08-30 skip-unchanged change. The register's "STAYS until that decision is made" was stale: the fast path was reverted 08-27.
7. ✅ **Already done — do not carry forward:** the concierge distribution/quota item (mounts on insights/home/about/blog/early-access, `feature_quotas` free 5→40, `cache_control` in the route — all 09-02); the spork-wall counterparty backfill (floor + self-healing cursor `20260902042214`, recovering ~20k rows/day); the pack OG card + grails `buyableOnly` now share the 72 h EV freshness bar (code, 09-03).
8. ⏳ **Still owed (from the 09-03 CI-audit session's ledger entry):** R61's first clean `scheduler-liveness` read is the **09-04 ~12:5xZ** report (one day is one sample — read several); R86's positive half needs a real `PGRST002` window. Three `apply_migration`s ran 22:13–22:32Z on 09-03 with no sentinel run inside them (last sentinel 21:50Z) — no observation yet.
9. 🧭 **Accuracy-gate diagnosis, so it is not redone:** of Top Shot canonical editions at LOW with ≥5 sales/30d (**315**), median snapshot age is **2.6 h** — the recalc reaches them; 271 have ≥7 sales and are demoted by the dispersion gate (`MEDIUM_MAX_DISPERSION`), i.e. by design. There is no reach defect in the confidence pipeline; moving the headline is a policy or data-source question (Trevor).
10. ✅ **`sales-serial-backfill` Top Shot lane is ON-CHAIN (edge fn v38/v39, 09-03 late PT) — do NOT re-file the 3,071 `unknown` rows.** They were the decommissioned `public-api.nbatopshot.com` (CF 530/1033 + 429/1015). First tick 02:40Z: 131 → 128 resolved. v39 flips `ok=false` when a lane fails EVERY (≥20) target on transport; the pipeline now has a watchlist row (silent 360 / no-success 720). ⚠ Its `unknown` bucket newest `last_failed_at` must stay ≤ 2026-09-04 00:40:49Z — a later one is a NEW transport failure. ✅ **Drift parity control PASSED (2026-09-04 11:46Z run): 6 content-drifted, the same known set — neither `sales-serial-backfill` nor `match-topshot-players` appears.**
11. ⏳ **Sentinel "Pipeline Success" arm is LIVE and quiet** — first production run 02:20:54Z (`checks_run` 16, only the two pre-existing WARN arms). Its positive control is still owed: the first real `ok=false`-only stall it names. ✅ **The three dead-host Top Shot pipelines are DECIDED (2026-09-04 PT), not still pending:** `topshot-badge-set-backfill` and `topshot-catalog-backfill` are both **unscheduled — routes kept, one-line restore in each route header** — because the Atlas edition walk already does their work (badges/tier, and, via migration `20260905024630`, prose + media); `topshot-misattrib-drain` was measured at **215 residual rows** and is a one-off script when the residue matters, not a pipeline. ⚠ **There is a FOURTH dead-host Top Shot pipeline and it is deliberately KEPT: `ingest-topshot-challenges`** (same 530/1033, last success 2026-08-28, 7 of 7 failing since). Its own `pipeline_runs` history is what `lib/challenges/hub-fetchers.ts` reads to decide whether `/challenges` may promise new challenges — **the dead cron is the instrument that keeps that page honest**, and it is the only thing that could notice the host returning. One run/day. ⛔ Do NOT re-file any of the four as an open dead-host item;  ~80 unwatched pipelines listed there too — ⛔ do NOT auto-seed 80 arms from the night pass.
12. 📏 **`pipeline_gap_hourly` is accumulating (pg_cron 443, `:07`, migration `20260904024810`) — a CALIBRATION SERIES, not an alarm.** Read it, do not act on it, until it holds weeks and at least one real correlated dip; the `_all_` row per hour carries scheduled/ticks/expected/skipped. ⚠ If `pipeline-gap-hourly-rollup` stops (watchlist: silent 180 / no-success 360, info), the series has a hole — re-run `rollup_pipeline_gaps(N)` with N covering the gap while `pipeline_runs` still retains it (≤ 73 h).
13. ✅ **Top Shot BASE circulation now comes from the chain daily** — `app/api/cron/topshot-circulation-onchain` (Vercel cron `5 4 * * *`, 04:05Z), `apply_topshot_onchain_circulation()` with the series-8 normaliser lifted into `topshot_normalize_circulation()` (migration `20260904031944`). ⚠ Base rows ONLY; parallels (`::`) stay with `backfill-topshot-subedition-circulation`. ✅ **VERIFIED 2026-09-04 13:20Z (manual trigger after the chunk-size fix `7df740cae`): `ok=true`, 9,523 pairs read, 239 script calls, 0 script errors, 25 rows changed, 8,434 retired, 89.7 s, `complete: true`; 8 of 8 sampled changed rows were BASE rows, 0 parallels touched.** The 04:05Z tick before it failed honestly at 250 pairs/script (Flow error 1110, computation limit). Nothing further owed — the next scheduled tick is routine. ⛔ Do NOT re-file `topshot-catalog-backfill`'s daily 530 at all — **the cron is gone as of 2026-09-04 PT.** Circulation is on-chain, tier/badges and (migration `20260905024630`) prose + media come from the Atlas walk. **The only piece nobody inherited is new-edition creation** (Atlas knows parallels we do not carry — 16 of 100 on a sampled page); that is deliberately open, because creating editions rows ripples into circulation, the sitemap and every entity surface.
14. ✅ **#54 decided: `match-topshot-players` gates its walk behind its inputs** (migration `20260904043939`, edge fn v30). ✅ **VERIFIED: the 2026-09-04 08:00Z tick logged `ok=true`, `gated: true`, 990 ms** (against 16,687 ms on 09-03) with the reason naming the next full run. Next full run on/after 2026-09-11 — that one is still owed. ⛔ Do NOT suppress `check_pipelines_running_but_not_succeeding()` for it — a gated tick has rows_written 0 by design; read `extra.gated` before calling it a stall.
15. 📏 **40 `info` watchlist arms seeded (migration `20260904044417`) for every pipeline that had none, sized from each one's own 73 h gap profile.** The sentinel pages only on `high`; these show in its WARN list. **Read, do not bulk-promote:** an arm that trips repeatedly in the first week is either a real gap (promote it) or a cadence the 73 h window under-sampled (widen it — the row's notes carry what it was sized from). Four dead-host/never-ok pipelines have the silent arm only. ⚠ `topshot-circulation-onchain` sits on the no-success list until its 2026-09-05 04:05Z tick succeeds — that is the arm working, not a defect.


## STEER — added 2026-08-28 ⚠ READ BEFORE QUOTING THE ACCURACY GATE

🚨 **The gate has TWO instruments and they disagree by 9.4 points. Priority 1's 30.1% and the trust
board's `*_fmv_high_med_share_pct` are NOT the same measurement**, and comparing one to the other
overstates progress by ~4×.

- **Priority 1's 30.1% baseline (08-23)** came from `fmv_current` with **no Top Shot filter**.
- **The trust board / `rpc_fmv_confidence_share()`** filters Top Shot to **canonical `external_id`**
  (`^[0-9]+:[0-9]+(::[0-9]+)?$`), excluding UUID dupe residue — deliberate, shipped 2026-08-04, but the
  baseline was never re-based onto it.

**Measured both ways on 2026-08-28 so the comparison is like-for-like:**

| | 08-23 baseline | today, SAME method | today, canonical-filtered |
|---|---:|---:|---:|
| priced editions | 27,075 | **27,143** | 20,717 |
| HIGH/MEDIUM | 8,140 | **8,950** | 8,787 |
| share | **30.1%** | **33.0%** | **42.4%** |

⭐ **The real five-day move is +2.9 points (30.1% → 33.0%), and it is GENUINE** — the denominator is
stable (27,075 → 27,143, +68), so it is 810 more editions reaching HIGH/MEDIUM rather than weak ones
leaving. Top Shot 34.3% → 37.5%.
⛔ **Do NOT quote 54.4% against 34.3%, or 42.4% against 30.1%.** Those are cross-instrument comparisons.

👉 **DECISION NEEDED (not a measurement): pick ONE basis and re-base the baseline explicitly.** The
canonical-filtered figure is arguably more honest — pricing dupe residue is not an accuracy achievement
— but switching silently books a **+9.4 point one-off no edition earned**, against a 50+ WAU gate whose
history is all on the unfiltered basis.

⚠ **Pinnacle is a THIRD basis and is in neither total** (`rpc_fmv_confidence_share()` returns five
collections, Pinnacle not among them; its 44% comes from a separate leg). **Both totals cover four of
five published collections**, exactly as the 08-23 capture warned. Full working:
ledger 2026-08-28 "the accuracy gate has TWO instruments".

## PRIORITIES — what tonight's pass should weigh

1. ✅ **CAPTURED 2026-08-23 22:05Z — the four-week gap is closed, so do not re-ask for it; re-MEASURE it.** **21 accounts** (was 20 on 07-26, so **+1 in four weeks**), **0 signups in 7 d** (newest 2026-08-08), **signed-in WAU = 0**, MAU = 2. Roadmap gate is 50+ WAU. ⛔ **Do NOT read `funnel_events` sessions as users — that instrument is wrong by ~3 orders of magnitude**: 16,463 weekly "non-bot" sessions of which **99.67% fire exactly one event and never return**, 4 `wallet_paste` sessions, 0 `signin_click`, 0 `account_created`. ⚠ **Vercel Web Analytics is NOT enabled**, so `funnel_events` is the only traffic instrument and has no independent corroborator. ⚠ **`bot_ua` is only meaningful from 2026-08-23 02:00Z forward** — before that the column exists with no UA to classify, so `false` means "never saw one", not "human". Full capture, with the accuracy gate alongside it: [inbox 2026-08-23T2205Z](inbox/2026-08-23T2205Z-priority-1-captured-wau-is-zero-and-the-accuracy-gate-is-30-percent.md). **Demand is still the gate that matters and the roadmap's answer to a 0 is ACCURACY FIRST, not growth tactics.** ⭐ **And the accuracy gate is measured too, for the first time: 30.1% HIGH/MEDIUM** (Top Shot 34.3%, All Day 21.4%, Golazos 0.0%, UFC 0.0%, Candy 61.6%) — ⚠ **excluding Pinnacle, which has ZERO rows in `fmv_current`** and prices through its own triple-keyed path, so the headline covers four of five published collections. ⛔ **Do NOT chase Golazos' 0%** — measured, every edition that sold there has **1–3 sales a month (avg 1.4)**, so a threshold change would manufacture confidence, not accuracy. ⭐ **The gate is mostly a LIQUIDITY CEILING, not an engineering defect:** on All Day, confidence tracks volume monotonically — HIGH averages 11.8 sales/30d and **100% have ≥5**, MEDIUM 5.5, LOW 2.6. 🚨 **THAT QUESTION IS ANSWERED AND IT IS BIGGER THAN IT LOOKED: ~1,000 editions estate-wide are labelled LOW while their own `sales_count_30d` is ≥5, and in EVERY collection the LOW cohort trades ~2× the MEDIUM cohort** (Top Shot 499 editions at avg 28.9 vs MEDIUM 15.2 vs HIGH 14.0; All Day 454 at 15.6 vs 9.3 vs 9.4; Candy 44 at 48.1). Controls rule out staleness (145/150 computed in 24 h), legacy algo (137/150 are `1.7.0`, same as MEDIUM) and my own query (the figure is the pipeline's own column). `computeConfidence` makes LOW the floor case, so the rule says this cannot happen. **Two readings, both defects:** the LABEL is wrong (~+2 points on the gate, and we publish our lowest confidence on the editions users look up most), or the COLUMN is wrong (`sales_count_30d` may hold the 90-day widened count — it reads 12.7 where a raw 30 d count reads 7.6). ⭐ **RESOLVED the same evening — and it is a CALIBRATION question, not a bug.** Re-measured from `sales` directly (never the column): **368 Top Shot editions publish LOW on a TRUE 39.6 sales/30 d average vs MEDIUM's 22.2**, all 352 current-algo ones computed within 24 h, `days_since_sale` 4.7, true counts 5 → **643**. The cohort is **sub-$2.50 WNBA Series 8 moments at 5/5 liquidity** — a **$0.49** FMV built from **643 trades** publishes as LOW. The demotion is **BY DESIGN**: `MEDIUM_MAX_DISPERSION = 0.35` demotes MEDIUM→LOW once count ≥7, so only high-volume editions are even eligible. ✅ **The dispersion measurement RAN in a quiet window (2 active / 1 IO waiter) and settles it: LOW avg CV **0.731** with 76% over the 0.35 ceiling, HIGH avg CV **0.222** with 10% over.** The mechanism is confirmed. 🚨 **But my sub-dollar-tick calibration story is FALSIFIED by its own prediction** — it predicts HIGH skews expensive; measured, **HIGH has the LOWEST median price ($0.31) and the MOST sub-dollar editions (72%)**, while LOW's median is $1.08. Price level does not drive the demotion. ⛔ **The "build a tick-aware dispersion measure" recommendation is WITHDRAWN — do not act on it.** ✅ **The system is behaving correctly**: 643 trades with a CV of 0.73 genuinely is an uncertain price. ⭐ **What survives is one user-facing observation: `LOW` is doing two opposite jobs** — *"we have almost no data"* and *"we have 643 sales and they disagree"*. To a collector those are opposite messages, and merging them into one badge is the honesty canon's three-states problem applied to a LABEL. Splitting it is a product decision (a new enum touches every surface, the OG cards and the concierge) — **the measurement is filed so the call can be made on it.** Filing: [inbox 2026-08-24T0225Z](inbox/2026-08-24T0225Z-a-thousand-editions-are-labelled-LOW-while-they-are-the-most-traded-on-the-platform.md).
2. **Prefer DB/artifact work that does not need a push.** Cloud-sandbox passes have repeatedly been NO-PUSH. Work that lands as a migration or an artifact ships; work that needs a git push may not. (⚠ Push from Trevor's **local** box is fine — verified 2026-08-17 — so a NO-PUSH night is a *sandbox* limitation, not a repo one.)
3. **Do not open new investigations into disk-IO saturation symptoms.** The fmv-recalc kill rate, `public_board_slow_count`, the board-warm failures, the pg_cron statement-timeouts and the `get_collection_stats` timeout are **one root cause** (disk-IO budget on the SMALL 2 GB instance). The lever is cutting work — page size, precompute, fan-out — **never** raising a timeout and never upgrading the tier.

## STEER — added 2026-08-27 (memory-refresh pass: CLAUDE.md, the register, the roadmap, the trust board and the schema stamp re-derived against live sources)

1. 🚨 **THE ONE THING THAT WILL WASTE A PASS IF UNREAD: the daytime monitor's 08-28T0310Z filing asks you to
   "check whether the next scheduled run (08-28 22:10Z) lands" — THAT TICK WILL NEVER FIRE.** The night pass
   moved `candy-editions-ingest` `10 22 * * *` → `10 1 * * *` in the same window (`544f3e6c0`, deploy READY
   2026-08-28 03:11Z), and because the deploy landed AFTER 01:10Z that day, **the first run at the new slot is
   2026-08-29 01:10Z**. Against a last success of 08-26 22:10Z that is **~51 h of silence on a 30 h arm**, so
   ⛔ **the `cron_silent` BREACH you will see is the transition plus the 08-27 kill, not a new fault** — do not
   re-diagnose it, do not raise `max_silent_minutes`, do not suppress the arm. ✅ **The live watchlist note has
   been corrected in place** (it still said "10 22" and carried the unexecutable verify step) — record:
   `supabase/migrations/20260828040000_audit_20260827_correct_candy_editions_watchlist_note.sql`. **FALSIFIER,
   and the only thing worth acting on: if 08-29 01:10Z and the ticks after it keep missing at ~45%, the HOUR is
   not the cause — it is D8's wmc row-lock contention, and the durable fix is the `paginateGroup` chunking
   (handoff 2026-08-04, Item 2, unshipped).** Register: **#47** (new).
   ⭐ **Two sessions characterised this pipeline within five minutes of each other and reached compatible
   conclusions from different instruments — but the FILING went stale before it was read.** When you file an
   action item that names a clock time, name the schedule it depends on too.
2. ⏳ **Pack-pool wedge fix (`20260828025307`): first post-fix reading is 6 ok / 4 failed over 10 ticks
   (40.0%) against 131 consecutive failures before it, backlog 368 → 360, dists with pool rows 1,715 → 1,736.**
   The ledger's prediction was *"settles near 33%, NOT 0%"* — 40% on n = 10 is inside that. ⛔ **Do NOT close
   it on this**: the exit condition is a conversion rate over ~100+ ticks, and ⚠ **the 2-day rollup still reads
   74.1% failed because it is dominated by the pre-fix window — that number is not the current rate.**
3. ✅ **Re-verified live, so do not re-derive these before ~08-29 unless you are acting on them:** headline
   metric (every collection INSIDE its own 5-sample range; **the "Candy is moving monotonically" claim broke at
   the fifth sample — 64.0 → 63.2 — and neither the run nor its break is a trend**) · demand (**23 / 2 / WAU 2
   / MAU 4, identical to 08-26 — a confirmation, not a new number**) · the all-keys accuracy denominator
   (**30.1% → 33.4%**) · #22 the defeated purge (unchanged, 25 days, third instrument) · #8 the sports proxy
   (**0 ok / 16 in 48 h**) · #34 Sentry (**newest stored event STILL 2026-08-18T13:21:59Z — the "a quota resets,
   it will heal itself" hypothesis predicts a resumption that has not happened in 9 days**) ·
   `compute_pack_ev_per_edition_weighted` (fix still unshipped in live `prosrc`).
4. ⚠ **Trust board is 3 breached, up from 2 on 08-26 — diff the SET: the new arm is `board_mv_refresh_stale_hours`
   (9.57 vs 8), the one the 08-26 entry recorded as "all clear".** ⛔ It is the documented cadence-vs-threshold
   mismatch (a 6-hourly refresher against an 8 h threshold has 2 h of headroom), not a new incident.
   `unmapped_resolution_backlog_max` is the one arm still climbing (258 → 291 → **338**).
5. ⭐ **A durable lesson was promoted to CLAUDE.md this pass and is worth applying beyond the job it came from:
   a killed `after()` run is ABSENT from `pipeline_runs_daily` rather than counted as a failure, so that rollup
   reads `runs 1 · ok 1 · failed 0` for every recorded day while a job dies nightly.** The **47 still
   un-heartbeated `after()` routes cannot be audited at all** by either rollup — that is the argument for
   continuing the E5 conversions, and it is a measurement, not a preference. ✅ **And the correlation is no longer a hand-derivation: `npm run pipelines:kills` (`lib/pipeline/kill-rate.ts`, shipped the same night) does it with the recency test built in — use it, and split any candy-editions rate at the 08-28 03:11Z deploy from 08-29 onward.**

## STEER — added 2026-08-26 (memory-refresh pass: CLAUDE.md, the register, the roadmap and the schema stamp re-derived against live sources)

1. ✅ **FIXED THE SAME DAY (2026-08-26) — `/insights/underpriced-serials`'s React #418, plus the guard gap that let it through. ⏳ The one thing left is VERIFICATION: watch `E2E DOM Smoke` across SEVERAL scheduled runs, because the failure is intermittent and one green run proves nothing.** Detail + the mutation checks: known-issues #37. ⛔ Do not re-open the diagnosis from the ingest cadence — that hypothesis was checked and found insufficient; the cause is that the CACHED HTML can be hours old (measured 2.5 h), not that the spine is stale. **Original filing, kept because it is what a next session should recognise:** 🚨 **it was throwing in PRODUCTION and nothing but a scheduled badge could see it.** Two consecutive `E2E DOM Smoke` runs (08-26 13:37Z and 21:09Z), all retries, 1 failed / 95 passed. **This is the highest-value shippable item on the board right now**: it is user-facing, the mechanism is identified in code (`UnderpricedSerialsBoardClient.tsx:341` computes `listingsAgeHours` from `Date.now()` during render, rendered at :391, on a `revalidate = 900` ISR page — so server HTML and hydration disagree whenever the rounded hour ticks over), and the fix is local. ⛔ **Do NOT reach for `suppressHydrationWarning`** — that silences the only instrument. Compute the age in an effect after mount, or pass a server-stamped age as a prop.
2. ⚠ **Priority 1's demand numbers below are SUPERSEDED — re-read 2026-08-26: 23 accounts (+2 in 7 d, newest 08-25), WAU 2, MAU 4.** The "0 signups in 7 d / WAU = 0" line is no longer true. **n = 2 is not a trend and does not move the 50+ WAU gate**, but do not re-publish the zero.
3. ⭐ **The register was swept 08-26 and SEVEN items moved** (#8, #27, #29, #30, #34, #35, #36) with two new ones (#37, #38). **Read the sweep stamp at the top of `### Open` before re-deriving anything** — an item the stamp does not name still carries its own older date.
4. 🟡 **#38 is the new "filed but never registered" case: `topshot-pack-pool-backfill` is 1 ok / 273 failed per day** and has been since at least 08-25, sitting in the inbox. Most of it is a correct report of an empty queue — **but 15 statement timeouts are hiding inside the 258-strong signature**, and a permanently-red pipeline is a permanently-unread one. Do not suppress the arm.
5. ⏳ **Do NOT re-litigate the `refresh_wmc_fmv_changed` fast path before its scheduled read.** Correctness is proven; the cost claim is not, and the A/B is confounded. The clean baseline rows (`rwfc_*_T1_CLEAN_20260827` in `public._rpc_waste_baseline_20260825`) must be re-read **in a quiet window ≥24 h after 2026-08-27 02:05Z**. Reverting or re-measuring earlier repeats the mistake in the other direction.

## STEER — added 2026-08-24 (memory-refresh pass: CLAUDE.md + the reference set re-derived against live sources)

- 🚨 **A NEW MEMBER OF THE HONESTY CANON, AND IT IS A CACHING ONE: ISR bakes a failed read into the whole
  `revalidate` window.** A COLD regeneration that exceeds the 8 s `BOARD_LIVE_TIMEOUT_MS` fails the page's
  read, and `revalidate = 900` then serves that failure for **up to 15 minutes** — observed live on
  `/insights/pack-drops` at `x-vercel-cache: HIT`, `age: 158`, while the API itself answered in **1.2 s**.
  ⚠ **It self-heals on the first warm pass, so it is easy to declare fixed by accident** — the honest test
  is *"does a COLD regeneration still exceed the budget"*, never *"is the page OK now"*. ⛔ **Do NOT raise
  `BOARD_LIVE_TIMEOUT_MS`** (shared by every board), **do NOT retry** (the abandoned query keeps running
  server-side), **do NOT lower `revalidate`** (that hits the cold path MORE). The two real options — a
  per-caller budget, or a genuine stale snapshot — are **Trevor's call**. Full write-up:
  [key-files-and-honesty.md](../reference/key-files-and-honesty.md); filing:
  [inbox 2026-08-24T1441Z](inbox/2026-08-24T1441Z-a-cold-isr-regeneration-can-bake-a-failed-read-into-15-minutes-of-cached-html.md).
- ⛔ **`metrics-latest.json`'s `inbox_archiving_note` was REFUTED and has been corrected in place.** It told
  a push-capable pass to archive resolved inbox filings and framed ~200 un-archived files as *"the accrued
  cost of the NO-PUSH streak"*. Both halves are wrong — see the DO-NOT-ARCHIVE section below, which was
  already in this file and was not read. **This is the shape CLAUDE.md flags as least re-checked: a filed
  DECISION, and specifically a queued CLEANUP, because housekeeping reads as obviously safe.**
- ✅ **#22 (the defeated credential purge) re-verified 2026-08-24 and it is STILL LIVE.**
  `refs/heads/claude/todo-implementation-e4tib3` is still on origin at `ee94c8a2a`; the values-free control
  re-ran **e4tib3 2 · main 0 · qi4350 0**, so the exposure is still exactly one branch. ⚠ **`git cat-file -e
  <blob>` is NOT a per-branch test** — after fetching either branch it answers `yes` for both. Operator-only,
  unchanged: triage → delete via the GitHub UI → ask Support to GC → **rotate regardless**.
- ⚠ **CLAUDE.md no longer carries the collection UUIDs in prose** — they were displaced 2026-08-24 to pay for
  the ISR rule above, and [schema-truth.md](../reference/schema-truth.md) is now the **only in-repo copy**
  (all 7 re-verified against live `public.collections` that day, zero drift). **A stale stamp on that section
  is now a silent failure, not a redundant one** — re-verify it whenever you touch it.
- **Re-derived the same day, so do not re-quote the older figures:** repo map `lib/` **301 → 308** and
  `scripts/` **97 → 103** (everything else held); and **both "measured-but-unshipped DB fixes" are still
  unshipped**, confirmed from live `prosrc` rather than re-read from the filings — `drain_fmv_cold_tail`
  still opens on the unscoped `GROUP BY edition_id` over `fmv_snapshots`, and
  `compute_pack_ev_per_edition_weighted` still carries its `fmv_current` leg.

## STEER — added 2026-08-22 (a long interactive day; these change what the next pass should and should not do)

- 🚨 **`job startup timeout` now has a NAMED CAUSE — stop treating it as ambient.** `max_worker_processes = 6`
  against `cron.max_running_jobs = 32`; pg_cron cannot get a background worker, so the body never runs
  and **nothing reaches `pipeline_runs`**. 169 timeouts / 28 jobs in 24 h. Detail + the hour table:
  [cron-and-schedulers.md](../reference/cron-and-schedulers.md).
- ⛔ **BUT DO NOT extend that to the 01:00–19:00Z band.** Measured 08-22: pg_cron **run count is FLAT all
  day** (480–552/hr) while busy-seconds swing **10×**. The band is not caused by scheduling density —
  it is the same work taking longer (burst-credit depletion). **Staggering fixes an individual job and
  the startup-timeout class, NOT the band.** Anyone proposing "stagger the crons" as a band fix has
  mis-read this.
- ⛔ **Before planning ANY `cron.alter_job`, read `cron.job.username`.** 42 of 93 jobs are owned by
  `cron_heavy`, and **no session-reachable role can reschedule those** (`postgres` may EXECUTE but does
  not own; `cron_heavy` owns but may not EXECUTE; `postgres` cannot UPDATE `cron.job`). The ledger's
  08-22 success on jobids 83/84 was on `postgres`-owned jobs and does not generalise.
- ⚠ **`pinnacle-sync`'s cadence arm was DEACTIVATED and replaced by `pinnacle-fmv-recalc` @ 1560 min.**
  The old arm watched a best-effort log line whose write is swallowed by design; the work is fine. **Do
  not re-activate it** — and note the new arm is blind to "the 10:07Z HTTP caller stopped", which the
  22:37Z pg_cron backstop makes invisible to users anyway.
- ⚠ **`wallet-username-resolver`'s cadence arm is 450 min now (was 75).** Trevor cut the cron to every
  3 h on 08-18 and the arm was never re-pointed, so it fired on every tick. Its **failure rate is a
  separate, still-live problem** (84% via the pooler) — the cadence arm is structurally blind to it, so
  do not read a quiet cadence arm as a healthy resolver.
- 🚨 **OPEN, OPERATOR-ONLY, and none of it is fixable from a sandbox:** pg_cron **jobid 70** is the SOLE
  refresher of `mv_topshot_misattrib_candidates` and has failed 15 of 16 runs since 08-07 (the MV is
  stale since 08-16) — the one-line fix is blocked by the ownership split above. **atlas-proxy** still
  needs a `wrangler deploy`. **`topshot-moments-hydrator`'s cron is declared in NO repo file** and a
  `wrangler deploy` would delete it. And the **P0 stale branch `e4tib3`** still carries the pre-purge
  credential blob on a public repo.
- ✅ **Two new CI guards exist; do not duplicate them.** `check-memory-doc-links.mjs` (CLAUDE.md +
  `docs/reference/**` pointers) and `check-driver-message-leaks.mjs` (ungated handlers returning
  `err.message`). Both are ban-at-population-zero and both carry their own inspected-count assertions.

## STEER — added 2026-08-23 (interactive; verifies one standing instruction and re-opens one it closed)

- ✅ **The `db-pin-staleness` verification this file ASKED FOR was run, and the answer was NOT the predicted
  "187 clean, 0".** The 08-23 07:51Z run reported **189 pins — 187 clean, 2 needing attention**, naming
  `refresh_cross_collection_cohort_step1` / `_step2` — a **DIFFERENT pair** from the six closed the evening
  before. The 08-22 ~20:35Z lock-window rewrite shipped its own new pin file and left those two pointing at
  the superseded 08-16 snapshot, so the instrument re-opened hours after #24 was closed. Both are now
  re-pinned, and a third (`log_pipeline_run`, from the same-day `clock_timestamp()` fix) with them.
  **VERIFIED GREEN by workflow_dispatch at 20:31Z: `checked 189 pins — 189 clean, 0 needing attention`** —
  the sweep's first green since 2026-08-09. ⚠ **The durable lesson is that #24-style closure is not a state,
  it is a moment: any migration that redefines a pinned function re-opens it the same day.** Re-pointing a
  pin is part of shipping the change, not a follow-up chore.
- 🚨 **`migration-parity` went RED on 08-23 07:58Z — its FIRST failure after 14 consecutive greens** — and
  the gap **re-opened behind this morning's recovery**. Measured 20:30Z: 61 migrations applied since 08-21,
  **17 with no committed file**; I committed the one I needed, so **16 remain**, fifteen of them applied
  17:29–19:28Z as one still-moving `series_detail_rollup` / `edition_fmv_current` piece. ✅ **CORRECTED 20:50Z — RUN THE
  SCRIPT.** My first version of this steer said do not reconstruct them, reasoning from
  `pg_get_functiondef`. Wrong: `supabase_migrations.schema_migrations` **stores the applied statements**, and
  **`scripts/recover-fileless-migrations.mjs`** (written this morning, `307ce25e`) writes each file
  byte-exactly and verifies it against the md5 prod computes. It needs `SUPABASE_SERVICE_ROLE_KEY` — which is
  why I did not run it from this sandbox — and it never commits. Only the **authored header and revert
  block** still belong to the session that applied each migration. ⚠ Scan the stored statements for
  secret-shaped content first; I did, and all sixteen are clean. ✅ I dispatched `migration-parity` on demand
  at 20:43Z so the sixteen are named in a log now rather than at 07:40Z. Filing:
  `docs/overnight/inbox/2026-08-23T2030Z-sixteen-more-migrations-applied-today-still-have-no-committed-file.md`.
- ⚠ **NEW DB TRAP, and it is general: on PostgreSQL 17 a partial index whose predicate says
  `col IS NOT NULL` on a column declared `NOT NULL` is UNREACHABLE.** PG 17 removes the redundant qual before
  partial-index predicate proving, so the planner drops the index from the candidate set entirely — not
  out-costed, invisible. A strict clause on that column (`col = $1`, `col <> $1`) restores it. Three of the
  six such indexes here are dead, including the **98 MB `idx_sales_2026_fmv_recalc_window`** that
  `fmv_recalc_edition_page` needs: made reachable it runs the same 30-day window **2.9× faster on 2.0× fewer
  buffers**, and at the real 90-day window it completes in ~16 s where the as-written form does not complete
  in 60 s. ⚠ **`idx_scan > 0` is NOT evidence of reachability** — `idx_sales_2026_top_sales_board` has 502
  recorded scans and is unreachable today, so the unused-index advisor is structurally blind to this class on
  exactly the indexes that used to work. **Repair is an index rebuild (DDL on the FMV path) → Trevor's call,
  not a night-pass item.** Evidence:
  `docs/overnight/inbox/2026-08-23T2130Z-postgres-17-makes-partial-indexes-with-is-not-null-predicates-unreachable.md`.
- ✅ **A 24 h Vercel runtime-error sweep (21:56Z) found NO new honesty-canon instance — recorded so nobody
  re-sweeps it tonight.** 50 error groups; every one resolves to **saturation collateral** (statement
  timeouts, pool-acquire timeouts, `maxDuration` kills) or to **honest degradation working as designed**. The
  `[pack-detail] …` and `[entity-section] … — degrading to empty` families in particular are the R19 pattern
  firing, not a defect: `lib/pack-dist/fetchers.ts` returns `{ ok: false }` and the page consumes it through
  `summarizeDegraded(boardStatus(…))` at every one of those sections. ⛔ Per PRIORITY 3, none of this is a new
  investigation.
- ⚠ **BUT COUNT-BASED TRIAGE OVER `[pack-detail]` READS DOUBLES, and the pairs are exact.** Every bound breach
  logs **twice**: `bounded()` in `lib/pack-dist/fetchers.ts` catches the timeout and logs
  `[pack-detail] <label> bound …`, then returns `{ error }` so the caller's own `if (error)` branch logs
  `[pack-detail] <label> error …`. Measured, not inferred — six labels, identical counts in both groups:
  `pack_lifecycle` **138/138**, `pack_realized_ev` **129/129**, `ev_contributors` **126/126**,
  `pack_sales_history` **75/75**, `pack_table_rows` **59/59**, `pack_distributions` **50/50**. **Read one of
  each pair, never their sum.** Both lines carry different information (the bound fired / the caller handled
  it), so this is noise rather than a lie — **not touched**, because that file shipped hours ago and belongs
  to the session still working it.
- ⚠ **A retraction, so nobody re-derives it:** the 20:00Z `fmv-recalc` filing claimed `(saturation-class)` was
  a misattribution "because the database was measurably quiet". **Withdrawn** — that reading was taken at
  19:50Z and applied backwards to failures at 17:48–18:49Z, while the daytime monitor's positive control at
  18:10Z reads `io_wait=12 / active=11 / total=46`. **A control must be contemporaneous with what it
  controls.** The structural half stands and is now stated in **buffers**, which load cannot move.

## STEER — do NOT re-flag these (current)

- **The three standing trust breaches are all known-class.** `panini_sale_price_capture_dry_days` (an arm that is **crying wolf** — it counts dry days on a field deliberately abandoned and replaced on 08-08, while the replacement works at ~22%; the fix is to RE-POINT the arm, not to chase the capture), `unmapped_resolution_backlog_max` (AllDay permanent floor — its own text says do NOT raise `breach_at`), `public_board_slow_count` (saturation collateral; **do not characterize its direction from fewer than several days** — it has been called both "climbing" and "oscillating down" on ~1-day windows and both were fair).
- **Sentry issues titled `smoke check could not run: …` are the honest-degradation path WORKING**, not security failures. Verify against the live invariant (`check_public_security_invariants()`, `check_anon_write_surface()`) before treating one as a breach.
- **`rpc-topshot-pack-opens-history` returning `done: true` ~96×/day is a DELIBERATE STANDBY.** It looks like a dead cron on every instrument. Do not unschedule it.
- **SERIAL-FMV-MULT-CRON — BY DESIGN.** `serial_fmv_multipliers` and `serial_fmv_power_model` refresh **weekly** via pg_cron. Staleness ≤7d is expected; do not re-queue as an escalating cron-silent item.

## ⚠ DO NOT ARCHIVE `docs/overnight/inbox/` FILES (measured 2026-08-17 — a queued action that would have broken things)

The `inbox/` convention says files are "archived to `inbox/archive/` after draining", and the 08-17 handoff had ~40 Aug 9–14 files queued to archive "once push is restored". **Do not run that.** Those files have become **permanent citation targets**: they are referenced by exact path from `CLAUDE.md` (4), `docs/overnight/ledger.md` (many), a dozen handoffs, the roadmap, `docs/sessions/2026-08.md`, **four committed `supabase/migrations/*.sql` files**, and **`lib/analytics/rpc-with-retry.ts:268`** (live product source). Moving them breaks every one of those, and migrations are immutable history that must not be edited to chase a path.

Evidence this has already bitten: `inbox/archive/2026-08-10T0515Z-…md` cites `inbox/2026-08-09T1941Z.md` — an already-archived file pointing at a still-live inbox path.

**The convention and the citation practice are in conflict, and the citations win.** Treat `inbox/` as append-only. If the directory's size becomes a real problem, the fix is a redirect/stub or an index — not a `git mv`. ⚠ **THIS IS ENFORCED: `__tests__/inbox-is-append-only-since-the-rule.test.ts` bans any filing dated on or after the rule from sitting in `archive/`.** ⛔ **And the 2026-08-24 night-pass handoff queued the OPPOSITE** — *"a push-capable pass should archive resolved items"*, calling ~200 live filings *"the accrued cost of the long NO-PUSH streak"*. **They are not debt; they are the intended steady state.** A push-capable pass tried it on 2026-08-24 and the guard stopped it. **Retire a filing by annotating it in place with a ✅ RESOLVED section — never by moving it.**

## STANDING (added 2026-06-22 — do NOT drop on the next focus rewrite) — pg_cron failure check

Every monitor + night-pass health sweep, also run `SELECT * FROM check_pgcron_recent_failures();` — this surfaces the pg_cron-internal failure class that `detect_stalled_pipelines()` CANNOT see (it watches `pipeline_runs`, not `cron.job_run_details`). Empty array = all pg_cron healthy. A listed job is a real finding **only if its `last_run` is AFTER the relevant same-day fix landed**; a failure timestamp that predates a fix is a STALE pre-fix run that clears on the job's next tick — do NOT alarm on it. A genuinely-recent pg_cron failure = HIGH-PRIORITY inbox candidate. (Also permanent in both task SKILL.md health-sweep sections; this note is belt-and-suspenders.)

## SENTINEL DECISION-QUEUE (2026-08-17 PT) — dispositions, so nobody re-derives these

The queue's own warning was that **re-derivation is this project's recurring cost**, and three of its five
items had already been measured elsewhere. Current state:

- ✅ **Item 5 (`pinnacle-nft-resolver`, ~900 null-edition rows) is CLOSED — it is the 08-15 catalog gap.**
  `pinnacle_sales.edition_id` FKs to `pinnacle_editions` (551 rows); the editions live only in
  `pinnacle_catalog` (2,561). Re-measured 08-18T0112Z: `distinct_editions=161 · in_editions=0 ·
  in_catalog=161`, up from 114 on 08-15 (**+41 % in three days**). Filed:
  `inbox/2026-08-18T0112Z-pinnacle-null-edition-pool-is-the-catalog-gap-…md`.
  ⛔ **Do NOT "park the unresolvable rows"** — they are not permanently unresolvable, and parking them
  hides a widening gap. ⚠ The resolver's `failed: 0` means it **never reaches** these rows (946 of 954
  have `resolution_attempts = 0`), not that it declines them gracefully.
- ⏸ **Item 1 (pack-EV `fmv_current` JOIN) — mechanism CONFIRMED, still correctly unshipped.** Fully
  measured already in `inbox/2026-08-16T1829Z-fmv-current-does-not-push-down-through-distinct-on.md`
  (~3,100× — 335 buffers vs 1,046,192). ⚠ **The queue framed this as one coordinated migration because
  "the function is pinned AND two of three are unmeasured". Those two facts are DECOUPLED — verified
  08-18 — and splitting them makes the expensive half shippable on its own:**

  | function | pinned? | measured? |
  |---|---|---|
  | `compute_pack_ev_per_edition_weighted` | **YES** — `supabase/tests/compute_pack_ev_per_edition_weighted.sql`, PINS entry at `__tests__/db-invariants-drift-guard.test.ts:170` | **YES** — jobid 71's callee, confirmed by the timeout CONTEXT; ~100 min/week of `cron_heavy` for zero rows |
  | `compute_pack_ev_from_pool` | no pin file | no |
  | `compute_pack_ev_from_pool_tier_weighted` | no pin file | no |

  So the **pinned one is the measured one**, and it is the one actually burning the budget. It can ship
  alone (migration + pin `.sql` + repoint the PINS migration name); the two unpinned ones need
  measurement but **no pin work**, and must not gate it. ⛔ Never `CREATE OR REPLACE VIEW fmv_current`
  (resets `security_invoker`); fix the CALLERS via a lateral accessor. Measure in a quiet window —
  during a saturation spell no timing is interpretable.
- 🔑 **Item 2 (`wallet-username-resolver`) is OPERATOR-ONLY — it is not pg_cron.** Caller enumerated
  08-18: absent from `vercel.json` (36 crons), pg_cron (94 jobs), GHA and in-repo fetches. It is
  **cron-job.org**, firing `POST /api/cron/resolve-wallet-usernames` 2×/hour. Trevor chose lever (a),
  cut the cadence → **every 3 h**. Sizing: 72.3 % of runs fail, and the failures still pay the full
  21-day `sales` scan before `wallet_usernames_unresolved`'s `statement_timeout=60s` kills them, for
  ~31 usernames/day. ⚠ Cadence only — **do not narrow the 21-day window** (breaks the 14-day retry).
- 🔍 **Item 3's lever is NOT the index alone.** The cost is in `aggregate_saved_wallet_stats`, whose
  `top_tier` **correlated subquery re-scans `wallet_moments_cache` once per `collection_id`**, and no
  index carries `tier` (confirmed: 14 indexes, none include it; `idx_wmc_cohort_cover` is now **464 MB**,
  not 458). Fold the subquery into the existing `GROUP BY` before considering a wider index — a fold
  costs no write amplification on a 98 %-non-HOT table.

⚠ **Measurement hygiene, learned the hard way 08-18:** the Supabase MCP 60 s cap abandons the RESULT, not
the query — a "timed out" EXPLAIN keeps running (seen at 86 s) and retrying it stacks copies onto the
saturation being measured. Take a positive control first (`count(*) FILTER (WHERE wait_event_type='IO')`
over `pg_stat_activity`); if most active sessions are in IO wait, **every duration that hour is
uninterpretable** — compare Buffers, never wall time.

## STANDING — read the three daily instrument LOGS, not their badges (added 2026-08-22)

Three credentialed detectors run daily and are the only things that can see their rot classes:
`edge-fn-drift` (06:40Z) · `db-pin-staleness` (07:20Z) · `migration-parity` (07:40Z). **Measured 2026-08-22:
edge-fn-drift red 14 consecutive runs, db-pin-staleness red 13, migration-parity 14/14 green — and BOTH red
ones are LOUDLY CORRECT, not broken.** 25 edge functions are not running `main`; 6 of 187 DB pins no longer
match live. Details: known-issues **#23**, **#24**.

⚠ **Nothing surfaces them (#25).** The sentinel — the thing actually read — has no GitHub Actions arm, so a
correct detector can stay red indefinitely and nobody notices. Until that is fixed, **reading these three
logs is a MANUAL step in any health sweep**, and CLAUDE.md's rule applies literally: *check the LOG, not the
badge* — a permanently-red instrument and a broken one look identical.

✅ **#24 is RESOLVED as of 2026-08-22 — all six pins re-pinned, assertions reviewed, not merely repointed.**
The warning that stood here (repointing without reviewing the assertions converts a working alarm into a
silent green) was honoured: every pin got its own diff, and five gained mutation-tested assertions for the
behaviour that had drifted. ⚠ **VERIFY, do not assume:** the 07:20Z `db-pin-staleness` run should now report
**187 clean, 0 needing attention** — its first green since 2026-08-03. If it names a pin instead, read WHICH
before reopening. 🚨 **IT DID NAME PINS — this prediction was FALSIFIED on 08-23 and the reason is durable;
see the 2026-08-23 STEER above.** Closure is a moment, not a state: a same-day rewrite of a pinned function
re-opens the instrument within hours. Recipe + what the six taught: [database.md](../reference/database.md).

⚠ **#23 and #25 remain OPEN and are operator-blocked.** 25 edge functions are still not running `main`
(redeploy needs both `deno.json` in `files` AND `import_map_path`, or a stale-but-working function goes
hard-down — and the list includes off-limits `compute-*-pack-ev` / `ingest-*`). Nothing still reads the
daily detectors; that fix is a sentinel arm keyed on a failure STREAK, blocked on putting a GitHub token
with `actions: read` into Vercel env.

## DECIDED 2026-08-22 — two open decisions closed, so nobody re-opens them

- ✅ **pg_cron jobid 70 / the `cron_heavy` privilege question: do NOT grant, no grant is needed.**
  `postgres` IS a member of `cron_heavy`, and `cron_heavy` already holds EXECUTE on `cron.schedule` and
  `cron.unschedule` — only `cron.alter_job` is missing, and `cron.schedule` upserts on
  `(jobname, username)`, so rescheduling under the job's own role updates it in place. Granting
  `alter_job` would widen a privilege to buy a capability the role already has by another door.
  ⚠ **The blocker is the HARNESS, not the database** — the Claude Code auto-mode classifier denies
  `SET ROLE`. The two-line self-checking recipe (and what to do if it returns a jobid other than 70) is
  in **known-issues item 19**, which has been corrected: its old "NO session-reachable role can
  reschedule" headline was **REFUTED**. **Transferable:** *"the sandbox could not do it" is not evidence
  that the DATABASE forbids it* — that conflation turned a two-line fix into a privilege-grant proposal.
- ✅ **The FMV-confidence accuracy meter: the destination is a NIGHTLY MATERIALISED TALLY**, not a
  cheaper in-band query. Rationale in §7 of
  `inbox/2026-08-22T1745Z-the-headline-accuracy-metric-is-unreadable-20h-a-day-…md`. The short form: a
  3.3× cheaper query still runs in-band and can still be killed in a spell — it lowers the odds without
  removing the dependency; and **a GATE metric needs a SERIES, which the in-band rewrite does not
  provide at all**. ⚠ **The lateral rewrite is NOT the alternative — it is the tally's WRITER**, so the
  23:10Z measurement (`trig_01H3p6o5iB7yyjLVzrbbviaA`) keeps its full value and has been repointed at
  that question. ⚠ **The tie check is now BLOCKING**: a tally is computed once and read all day, so an
  arbitrary tie-break is frozen into the published number. ⚠ **New third state for the redesign:
  STALE** — a stored tally that silently ages is worse than a query that loudly times out, because a
  timeout is falsifiable. Nothing has shipped: no table, no writer, no migration.

## RETIRED STEERS — these were in this file and are now WRONG; do not re-add

- ⛔ **"TS on-chain unmapped spike — do NOT skip/retire this class."** `topshot-flowty-unmapped-drain` was **deliberately RETIRED 2026-08-16** (schedule removed from `vercel.json`, verified absent) because its queue reached **0 open** and proving emptiness cost a full backlog scan on ~73 ticks/day. The old steer now argues against a decision that was correctly made.
- ⛔ **"evm-transfers-ingest Base-429 — benign, don't chase."** That cron was **disabled 2026-08-02** as pure waste (`evm_nft_transfers` holds ZERO rows; absent from `vercel.json`, GHA and pg_cron). There is nothing left to not-chase.
- ⛔ **"`unmapped_resolution_backlog_max` self-clears <100 in ~1–2 days."** It did not. It is **291** and is now understood as an AllDay **permanent-class floor**, continuously replenished. Do not wait for it to clear and do not raise its threshold.
- **The whole 2026-06-24 studio-platform post-ship watch** (3 backfill routes, the watchlist follow-ups, the TS dead-media tail, the spork-proxy correction, the UFC studio resolver) — all shipped and long since folded into `CLAUDE.md` + the ledger. Kept out of this file to stop it decaying into an archive again.
