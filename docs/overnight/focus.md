# Focus — 2026-06-24 (studio-platform deep-history program SHIPPED — post-ship watch + 1 watchlist follow-up)

Written by the 2026-06-24 Cowork session. CC shipped the studio-platform GQL deep-history backfill end-to-end across 3 collections (commits 87ab529→79cd8e7, all deploys READY, tsc clean, security invariant 0 after every migration). Cowork verified the data + security live this session.

## SHIPPED 2026-06-24 — POST-SHIP WATCH ONLY (do NOT re-flag / re-queue / auto-revert)
- **3 new backfill routes + `*/3h` Vercel crons + RLS-on progress tables**: pipelines `allday-studio-sales-history-backfill` (52 */3), `golazos-studio-sales-history-backfill` (56 */3), `pinnacle-studio-sales-history-backfill` (58 */3). Sources `allday_studio_history_v1` / `golazos_studio_history_v1` (→ `sales`) and `pinnacle_studio_history_v1` (→ `pinnacle_sales`). They drain the Dapper **studio-platform GQL** (`api.production.studio-platform.dapperlabs.com/graphql`, reachable from Vercel egress with an `Origin` header — same path the green pinnacle-catalog/metadata crons use) for per-edition sale history (the ~2023→present window; AllDay `edition_id==external_id`, Pinnacle render-keyed). ~200/tick, ~1–4 days to fully drain. **Cowork-verified clean this session:** 956 build-time verification sales, **perfect dedup** (rows==distinct tx/id), sane prices ($0.50–$250), depth AllDay→2023-06 / Golazos→2023-01 / Pinnacle→2024-12; **security invariants 0, `v_fmv_sanity_flags` 0, all 3 progress tables RLS-on + 0 anon-write grants**. Do NOT flag the new pipeline activity or the backfilled-sales volume as an anomaly. Revert per source: `DELETE FROM sales WHERE source='..._studio_history_v1'` (`pinnacle_sales` for pinnacle).
- **WHY studio beats the on-chain backfills here (don't try to "fix"/merge):** `edition_id` filter == `editions.external_id` ⇒ zero edition creation, no mis-key, **zero `unmapped_sales` writes** — the opposite of the HISTORY-BACKFILL-UNMAPPED-SPIKE class the on-chain TS-Flowty route generates. Complementary, not a replacement; dedup by tx_hash/id ⇒ no double-count.

## WATCHLIST FOLLOW-UP — 3 of 4 DONE (Cowork, shipped 2026-06-25 `audit_20260625_watchlist_studio_sales_history_backfills`)
The 3 `*/3h` studio crons (allday/golazos/pinnacle) are watchlisted at 600 min / medium (gate met: 4 clean 3h ticks; verified `detect_stalled_pipelines()` `[]`, security 0). Revert: `DELETE FROM public.pipeline_cadence_watchlist WHERE pipeline LIKE '%-studio-sales-history-backfill';`.

**DONE — watchlisted 2026-06-25 (`audit_20260625_watchlist_ufc_studio_sales_history_backfill`, 90m/medium/active; confirmed live in `pipeline_cadence_watchlist` by the 2026-06-26 night pass, which reconciled this stale PENDING note). Original PENDING text kept for context — `ufc-studio-sales-history-backfill` (CC shipped the route 2026-06-25; verified healthy: first tick wrote 16,745 launch-era sales, perfect dedup, 0 null editions, 0 unmapped spill, security/fmv 0).** It had only **1 clean tick at this writing** so it's NOT yet watchlisted. **After it banks 2 consecutive clean ticks, add the row — but note its cadence is `1,21,41 * * * *` = every 20 min (NOT the 3h of the other three), so use `max_silent_minutes ≈ 90` / medium, do NOT copy the 600 value.** It self-terminates to a cheap no-op once the ~half-day drain finishes (the no-op keeps firing every 20 min, so 90 min still holds). `INSERT INTO public.pipeline_cadence_watchlist (pipeline, max_silent_minutes, severity, notes, is_active) VALUES ('ufc-studio-sales-history-backfill', 90, 'medium', 'UFC studio deep-history drain — 20-min cadence (1,21,41 * * * *); self-terminates to no-op post-drain', true) ON CONFLICT (pipeline) DO NOTHING;` then verify `detect_stalled_pipelines()` `[]`. (CC also flagged this in its report.)

## TS dead-media tail (WNBA/Base-Set) — FULLY RESOLVED 0/0 (Cowork + CC 2026-06-25, 3 migrations)
The irreducible 06-23 media tail (was 1,541 thumbs/1,559 videos; the daily art cron drained the IPFS-catalog-covered ones to 803/823) is now **15/15**. The remaining 803 thumbs / 823 videos were **catalog-uncovered** (WNBA + residual Base-Set plays absent from `topshot_ipfs_assets`, so neither the IPFS recovery nor the art cron could touch them). Rather than wait on the deferred IPFS-catalog-extension worker, repointed the **788** that have a representative wmc `moment_id` to the project-established per-moment CDN form (`media/<nft_id>/image?width=400` — the grid's own primary; `media/<nft_id>/video` — the trophy-modal form). **Strictly better-or-equal** (old `editions/` URLs already 404'd → worst case is the same graceful broken state; grid builds its own URL regardless). Backup `audit_20260625_ts_wnba_media_recovery` (RLS-on, anon-revoked); security 0. **Spot-verify DONE (2026-06-25):** confirmed live — the recovered WNBA Base Set edition page (258:8666, Jordin Canada) renders a `media/<nft_id>/image?width=1080` at full 1080×1080 (`assets.nbatopshot.com` is Chrome-blocklisted so verified via the live RPC edition page, not direct CDN). Image form proven; `/video` is the same endpoint via the trophy-modal path. Reversible if ever needed via `UPDATE editions e SET thumbnail_url=b.old_thumbnail_url, video_url=b.old_video_url FROM audit_20260625_ts_wnba_media_recovery b WHERE e.id=b.edition_id;`. **Down to 11 (2026-06-25 round 2 `audit_20260625_recover_ts_dead_media_via_moments_nft_id`):** recovered 4 more (Heroes of the Game / Diamond editions) that had no wmc holder but DID have a `moments.nft_id` — the wmc-only first pass missed them (same "wmc ≠ only resolution path" lesson as the Flowty drain). **FULLY RESOLVED — 0/0 (2026-06-25 round 3 `audit_20260625_recover_ts_wnba_ultimates_media_via_minted_flowid`).** The residual **11** (2024/2025 WNBA Rookie Ultimates, sets 151/198) had NO moment in wmc/moments/sales, so the per-moment passes couldn't reach them. CC did the on-chain mint discovery: resolved a representative minted `flowId` per edition via `searchMintedMoments(byEditions:[{setID,playID}])` through the topshot-proxy (set UUID from `sets`, play UUID parsed from the dead URL; serial #1, any minted; TopShotIPFSResolver.getCIDs returned NIL for all 11 so IPFS was a dead end), **verified the per-moment CDN form serves real image/jpeg + video/mp4 (HTTP 200) before writing**, then repointed via the same `media/<nft_id>` form + backup table. **TS dead-media tail is now 0 thumbs / 0 videos.** All 3 `audit_20260625_recover_ts_*media*` migrations repo-synced.

**Studio-platform pre-2023 floor — RE-VERIFIED (2026-06-25), conclusion HOLDS: AllDay/Golazos pre-2023 deep tail genuinely needs the spork worker (do NOT re-investigate as "maybe under-sampled").** After the Flowty-drain premise reversal I re-tested the AllDay floor against heavily-traded **star 2021 Genesis QBs** (Josh Allen 40/159, Burrow 71, Herbert 194, Henry 334) — all return `totalCount 0` from `searchAllDayMarketplaceHistory`; Brady (329) returns 1 (a 2024 sale). The query shape works (recent editions return 100s), so studio genuinely has ~no pre-2023 AllDay — its AllDay marketplace indexing started ~2023 (UFC's 2022 floor is just that collection's earlier onboarding, NOT evidence AllDay goes to 2021). So the pre-2023 AllDay/Golazos + Flowty-venue tail is on-chain-only.

**CORRECTION (2026-06-25, CC measured): the spork-proxy worker is NOT a "non-functional stub."** The prompt/handoff claim was based on probing only the *intentionally-unauthenticated* health check (a bare GET returns `{ok:true,worker:"spork-proxy"}` by design, so the secret isn't shipped to probers). The repo [workers/spork-proxy/index.ts](../../workers/spork-proxy/index.ts) is a **complete, real** implementation (events proxy + newest→oldest tx-result walk across mainnet19→26, Bearer-auth-gated), and it is **deployed + functional** — direct probe of the deployed worker returns `{"error":"unauthorized"}` for both `?tx=` and `?start_height=` paths (a true stub would return `{ok:true}` everywhere), and CLAUDE.md's 06-19 entry confirms it already powers the 2023-24 historical-buyer backfill in prod. **RESOLVED as far as public infra allows (2026-06-25, CC measured + extended the worker).** Probed every historical spork access node on :8070: **mainnet17 (root 27,341,470 = 2022-04-06) and mainnet18 are ALIVE** — both serve blocks AND events (HTTP 200); **mainnet16 and older are dead** (mainnet16/15/14/12/10/8/5 → 503 "upstream connect error"; mainnet1 → no DNS). So the recoverable floor via public sporks is **2022-04-06**; everything before (mainnet1–16, 2020-10 → 2022-02) is **permanently unrecoverable** this way. CC then (a) **rewrote the worker `SPORKS` list to canonically-correct boundaries** (the old `maxHeight`s were set near each spork's *root*, not its end — a real events-path mis-routing bug — now `maxHeight = next spork root − 1`, verified against `onflow/flow/sporks.json`), (b) **added mainnet17 + mainnet18** (extends the floor to 2022-04-06) **+ mainnet27** (was missing entirely), and (c) **lowered `backfill-topshot-buyers` `HIST_WINDOW_START` 2023-01-01 → 2022-04-06** to cover the newly-reachable window. **Remaining operator steps (creds-gated, lowest value):** (1) `cd workers/spork-proxy && wrangler deploy`; (2) `wrangler secret put SPORK_PROXY_SECRET`; (3) set `SPORK_PROXY_URL` + `SPORK_PROXY_SECRET` in Vercel. **Remaining code (deliberately NOT shipped blind):** `topshot-flowty-sales-history-backfill` hard-stops at `SPORK_FLOOR_HINT = 137_390_146` (current-spork floor) and reads only the current node — extending it into the sporks is a structural rework of a **sales-ingest route** (the repo's off-limits/invisible-failure class) that is **untestable until SPORK_PROXY is wired in Vercel**, so it stays a documented operator/CC task, not an unverifiable blind ship. Do NOT re-state "the worker is a stub / needs writing."

## TS on-chain unmapped spike (HISTORY-BACKFILL-UNMAPPED-SPIKE) — RESOLVED by a DRAIN (CC 2026-06-25); do NOT re-flag, do NOT skip/retire, do NOT raise the threshold
**The Cowork "99.9% unresolvable" premise was WRONG** — it only tested the wmc (tracked-holder) path. CC tested the route's holder-independent `getMintedMoment`: **46/46 random backlog nft_ids resolve** to real editions that exist in `editions`. So the backlog is **real, resolvable Top Shot Flowty sales** (98.7% sub-$5 = normal common-moment floors, NOT dust). **Trevor's call: DRAIN, not skip/retire.** SHIPPED `topshot-flowty-unmapped-drain` (cron `9,29,49 * * * *`) — resolves the queue via wmc→nft_edition_map→getMintedMoment and promotes to `sales` (dedup tx_hash); verified live (+627 sales, backlog 2,860→2,689 net-draining, 0 retired). The proxy rate-limits getMintedMoment to ~55/tick, so the drain runs ~165/hr > producer inflow ~67/hr → the `unmapped_resolution_backlog_max` BREACH **self-clears <100 in ~1-2 days**. **Night-pass:** do NOT re-flag the standing BREACH as a new incident while it's trending down; do NOT skip-at-capture or retire this class; do NOT raise the trust threshold. Cadence (20 min) is the only lever if faster clearing is wanted. Ledger has full detail + revert.

## UFC studio-history — UNBLOCKED (Cowork 2026-06-24, resolver shipped); CC builds the route
The earlier "gated" conclusion was overturned. `searchUFCMarketplaceHistory` (no edition filter) returns all UFC sales with `nft.set.metadata.{athlete_name, edition_size}` + `nft.edition_num` inline; our 518 UFC editions match studio 1:1 on the unique `(player_name, circulation_count)` key (**verified 40/40**). **Cowork shipped** `audit_20260624_ufc_studio_history_resolver_and_targets` (resolver `resolve_ufc_edition_by_studio_meta(athlete,edition_size)→editions.id`, service_role-only, verified; + queue view `ufc_studio_sales_history_backfill_targets`; security 0/0). CC's UFC route = walk `searchUFCMarketplaceHistory` unfiltered + resolver per row + write `source='ufc_studio_history_v1'` (full build spec in `docs/handoff-2026-06-24-studio-platform-gql-deep-history.md` §3). Lowest priority (thinnest market) but no longer blocked. Revert: `DROP FUNCTION public.resolve_ufc_edition_by_studio_meta(text,bigint); DROP VIEW public.ufc_studio_sales_history_backfill_targets;`.

---

## STEER — do NOT re-flag these

- **SERIAL-FMV-MULT-CRON — BY DESIGN.** `serial_fmv_multipliers` (37 cells) AND `serial_fmv_power_model` (5 segments) refresh **weekly** via pg_cron (jobs 5+6, both active, Sun 11:00). Staleness ≤7d is expected. Do NOT re-queue as an escalating cron-silent item — reclassify/close.
- **evm-transfers-ingest Base-429** — benign off-to-the-side EVM/Beezie indexer (Base RPC rate-limit). Don't chase.

## STANDING (added 2026-06-22 — do NOT drop on the next focus rewrite) — pg_cron failure check

Every monitor + night-pass health sweep, also run `SELECT * FROM check_pgcron_recent_failures();` — this surfaces the pg_cron-internal failure class that `detect_stalled_pipelines()` CANNOT see (it watches `pipeline_runs`, not `cron.job_run_details`). Empty array = all pg_cron healthy. A listed job is a real finding **only if its `last_run` is AFTER the relevant same-day fix landed**; a failure timestamp that predates a fix is a STALE pre-fix run that clears on the job's next tick — do NOT alarm on it (e.g. the 2026-06-22 remap fix landed ~13:46Z so its 12:23Z failure was stale and clears at the 18:23Z tick; the special-serial-owners-MV refresh failure clears at its 16:13Z tick). A genuinely-recent pg_cron failure = HIGH-PRIORITY inbox candidate. (Now ALSO permanent in both task SKILL.md health-sweep sections as of 2026-06-22 — `rpc-daytime-monitor` + `rpc-weekly-health-check` step 3b; this focus note is belt-and-suspenders.)
