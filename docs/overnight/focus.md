# Focus — night of 2026-06-09 → 06-10

Written by the 2026-06-09 evening Cowork session (DBSAT root-cause + remediation day). Three CC ships landed late tonight: f41caf4 (change-detecting upsert_wmc_batch RPC across all 4 wallet-backfill call sites — the wmc rewrite-storm fix), c55d394 (202+after() wrap on 11 sync cron routes), 838d89b (badge-taxonomy TTL cache). Earlier: 56ad4ff (reconcile 202-wrap), 50acf94 (footer), 87d07ab (verify ownership gate + holo fix).

1. TOP PRIORITY — wave-watch the 06:00Z wallet-backfill wave on the NEW upsert path (first full wave on f41caf4; the 00Z wave mostly predated the alias). Success looks like: wallet-backfill pipeline_runs clean; upsert_wmc_batch in pg_stat_statements with low mean (target <500ms; the legacy PostgREST wmc INSERT...ON CONFLICT query stops accruing calls); cross-pipeline statement-timeout fails during 05-08:30Z sharply down vs the last two nights; wmc-fmv-populate write-rate DROPS (proof the per-wave fmv_usd wipe stopped); no 5xx storm on /api/wallet-backfill-* (~06:50Z was the storm signature). If the wave instead shows wmc writes missing/wrong (edition_key/serial regressions), that's revert territory: git revert f41caf4 + the call sites fall back to .upsert() — but verify against pipeline_runs + row spot-checks before any revert.
2. Verify the 202-wrap sweep (c55d394): wrapped routes' next ticks log to pipeline_runs as before (the wrap moved bodies into after(); a route that stops logging entirely = its after() never runs — escalate, don't ignore). Cron dashboard should go green across the board.
3. PINNACLE-RECONCILE-TIMEOUT is RESOLVED (entry re-enabled + 56ad4ff wrap; pinnacle_ask_stale_hours 19h -> fresh). Watch it stays <3h. The auto-disable mechanism is documented — if any OTHER entry shows "Inactive" on cron-job.org, queue it for the operator with the recovery recipe (ledger 2026-06-09 entries).
4. DO NOT touch (fresh <24h): lib/chains/flow/wallet-backfill-helpers.ts, the upsert_wmc_batch RPC, the 11 wrapped cron routes, badge-taxonomy, verify-challenge, collection/page.tsx, SiteFooter/rpc-tokens.css.
5. Carried unchanged: PINFMV-DRIFT-14 — RESOLVED 2026-06-10 (root cause was the smoke drift guard's comparison-set truncation, NOT a route keying leak; searchPinnacleDeals was already render-keyed since a9f86af). Guard fix shipped; NEXTJS-14 to resolv
6. WAVE-WATCH AMENDMENT (Cowork ~07:00Z 06-10, measured): f41caf4 swapped the 4 HELPER call sites, but a FIFTH inline wmc upsert exists in app/api/wallet-backfill/route.ts (~L274 flush path, includes last_seen_at: now()) — the TS-collection route, 303 runs/24h. So tonight's wave will STILL rewrite TS rows via the legacy PostgREST query (the pg_stat entry that kept accruing, +1,786 calls during the 00Z pre-alias wave). Interpret accordingly: partial improvement (AllDay/Golazos/UFC/Pinnacle waves cheap, mean 169ms on upsert_wmc_batch; TS still heavy) is SUCCESS for f41caf4, not failure — do NOT revert it on TS-wave heaviness alone. The fifth-site swap is queued for CC in docs/handoff-2026-06-10-wmc-fifth-call-site.md (ingest-adjacent = not night-pass-shippable). cache-refresh route's wmc upsert is DEAD (0 runs/14d, stale 2-col onConflict) — cleanup only, no urgency.

7. ITEM 6 SUPERSEDED (Cowork ~07:30Z): the fifth site SHIPPED in a3c1a0c (both wallet-backfill flush paths -> upsert_wmc_batch + wallet-scoped post-pass JOIN), and acf85c0 + edge fn v20 fixed THREE more wmc writers that had been silently 42P10-no-oping on the pre-2026-05-06 2-col onConflict since the constraint change (wallet-search L915/921 now 3-col direct upsert with collection_id; wallet-cache POST -> upsert_wmc_batch; scan-pinnacle-wallet edge fn v20 3-col). cache-refresh is NOT dead (client-side caller, never logs) — its onConflict was fixed in a3c1a0c. So: expect the TS wave leg CHEAP too (the legacy TS pg_stat entry should now freeze like the helper entries), and expect NEW legitimate wmc write activity from wallet-search/wallet-cache that did not exist before (previously silent no-ops) — that is recovery, not a regression. Wave-watch success criteria from item 1 apply to ALL legs now. Do-not-touch additions: app/api/wallet-backfill/route.ts, app/api/wallet-search/route.ts, app/api/wallet-cache/route.ts, app/api/cache-refresh/route.ts, scan-pinnacle-wallet edge fn.
