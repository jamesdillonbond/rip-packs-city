# Focus — night of 2026-06-09 → 06-10

Written by the 2026-06-09 evening Cowork session (DBSAT root-cause + remediation day). Three CC ships landed late tonight: f41caf4 (change-detecting upsert_wmc_batch RPC across all 4 wallet-backfill call sites — the wmc rewrite-storm fix), c55d394 (202+after() wrap on 11 sync cron routes), 838d89b (badge-taxonomy TTL cache). Earlier: 56ad4ff (reconcile 202-wrap), 50acf94 (footer), 87d07ab (verify ownership gate + holo fix).

1. TOP PRIORITY — wave-watch the 06:00Z wallet-backfill wave on the NEW upsert path (first full wave on f41caf4; the 00Z wave mostly predated the alias). Success looks like: wallet-backfill pipeline_runs clean; upsert_wmc_batch in pg_stat_statements with low mean (target <500ms; the legacy PostgREST wmc INSERT...ON CONFLICT query stops accruing calls); cross-pipeline statement-timeout fails during 05-08:30Z sharply down vs the last two nights; wmc-fmv-populate write-rate DROPS (proof the per-wave fmv_usd wipe stopped); no 5xx storm on /api/wallet-backfill-* (~06:50Z was the storm signature). If the wave instead shows wmc writes missing/wrong (edition_key/serial regressions), that's revert territory: git revert f41caf4 + the call sites fall back to .upsert() — but verify against pipeline_runs + row spot-checks before any revert.
2. Verify the 202-wrap sweep (c55d394): wrapped routes' next ticks log to pipeline_runs as before (the wrap moved bodies into after(); a route that stops logging entirely = its after() never runs — escalate, don't ignore). Cron dashboard should go green across the board.
3. PINNACLE-RECONCILE-TIMEOUT is RESOLVED (entry re-enabled + 56ad4ff wrap; pinnacle_ask_stale_hours 19h -> fresh). Watch it stays <3h. The auto-disable mechanism is documented — if any OTHER entry shows "Inactive" on cron-job.org, queue it for the operator with the recovery recipe (ledger 2026-06-09 entries).
4. DO NOT touch (fresh <24h): lib/chains/flow/wallet-backfill-helpers.ts, the upsert_wmc_batch RPC, the 11 wrapped cron routes, badge-taxonomy, verify-challenge, collection/page.tsx, SiteFooter/rpc-tokens.css.
5. Carried unchanged: PINFMV-DRIFT-14 — RESOLVED 2026-06-10 (root cause was the smoke drift guard's comparison-set truncation, NOT a route keying leak; searchPinnacleDeals was already render-keyed since a9f86af). Guard fix shipped; NEXTJS-14 to resolv
6. WAVE-WATCH AMENDMENT (Cowork ~07:00Z 06-10, measured): f41caf4 swapped the 4 HELPER call sites, but a FIFTH inline wmc upsert exists in app/api/wallet-backfill/route.ts (~L274 flush path, includes last_seen_at: now()) — the TS-collection route, 303 runs/24h. So tonight's wave will STILL rewrite TS rows via the legacy PostgREST query (the pg_stat entry that kept accruing, +1,786 calls during the 00Z pre-alias wave). Interpret accordingly: partial improvement (AllDay/Golazos/UFC/Pinnacle waves cheap, mean 169ms on upsert_wmc_batch; TS still heavy) is SUCCESS for f41caf4, not failure — do NOT revert it on TS-wave heaviness alone. The fifth-site swap is queued for CC in docs/handoff-2026-06-10-wmc-fifth-call-site.md (ingest-adjacent = not night-pass-shippable). cache-refresh route's wmc upsert is DEAD (0 runs/14d, stale 2-col onConflict) — cleanup only, no urgency.

8. FMV-AUDIT FOLLOW-THROUGH (Cowork ~14:00Z 06-10): the audit fix wave (e3aee28 + the audit-repair migrations) is live. (a) COMMIT-AND-PUSH the one uncommitted file .github/workflows/badge-sync.yml — it adds the badge catalog-walk GHA schedule (45 2,8,14,20 UTC, separate job, curl --max-time 600; deliberately GHA not cron-job.org because the route runs ~300s sync). Genuinely low-risk YAML-only; verify the first scheduled tick's run logs + pipeline_runs pipeline='topshot-badge-catalog'. (b) Watch metric: TS editions with latest FMV >3x their own 90d sales median was 63/4,971 pre-fix — re-measure after 1-2 fmv-recalc sweep cycles; should trend toward ~0. (c) Watch the 67 audit-repair snapshots (algo_version LIKE 'audit-repair%') aren't re-clobbered to spike values by topshot-gql-v1 or Step 1 sweeps — the writer guards should prevent it; if a repaired edition re-spikes >3x its repair value, escalate (writer-guard gap), don't silently accept. (d) badge_editions TS coverage baseline 3,138/9,136 — track it rising once (a) lands. Do-not-touch additions: app/api/fmv-recalc/rout
9. NIGHT OF 06-10 -> 06-11 (Cowork interactive, ~16:20Z 06-10): DAYTIME-DBSAT (10:00-15:30Z) was root-caused + fixed live — populate_wmc_image was seq-scanning wmc 2x per 5-min tick (partial-index defeat); migration audit_20260610_populate_wmc_image_partial_index_fast_path shipped, post-fix 2-554ms vs 43-111s. Full record: ledger top entry + inbox/2026-06-10T15-15Z.md addendum. Tonight: (a) confirm fails-rate is back to ~1%/hr and the 3 laggards (pinnacle-wmc-render-id, refresh-pack-grail-metrics-mv, populate-pinnacle-wmc-fmv) resumed logging — they were saturation casualties, their crons fire fine; (b) fmv-recalc step3_delete_chunk_failed: d4b058f now logs the real PG error + retries once — if it STILL fails on a calm DB, that is a REAL step3 regression, escalate; if green, the e3aee28 wave-watch (focus item 8b/c) can finally start; (c) cron-job.org job 7491767 "RPC Pipeline Runs Cleanup" was deliberately disabled (broken anon-key entry; weekly maintenance now self-fires from the daily prune-logs tick via d4b058f, first firing expected Sat 06-13) — do NOT flag it as an auto-disable casualty; (d) GIT GHOST: if .git/index.lock blocks commits and rm says "No such file"/exits 0 while ls still shows it, the file does NOT exist on Windows (verified) — it is a sandbox-mount phantom; work around with GIT_INDEX_FILE plumbing: export GIT_INDEX_FILE=/tmp/idx; git read-tree origin/main; git add <paths>; TREE=$(git write-tree); COMMIT=$(git commit-tree $TREE -p origin/main -m "..."); git update-ref refs/heads/main $COMMIT; git push origin main (used 4x successfully today); (e) one-time probe: any wmc rows with image_url='' (post-fix these are force-path-only) — if found, UPDATE to NULL off-peak; expect zero. DO NOT touch additions: app/api/cron/prune-logs/route.ts + the fmv-recalc step3 block (d4b058f, fresh).
11. NIGHT OF 06-12 -> 06-13 (Cowork interactive, ~19:30Z 06-12): (a) TOP PRIORITY — UFC-WMC-NULLKEY is a CONFIRMED REGRESSION, not pending-verify: 3,837/4,584 null edition_key at 19:16Z, UP from the 3,150 baseline, measured AFTER the 18Z wave completed on a recovered DB (DBSAT-0612 ended ~15Z; 17Z hour was 240 runs/0 fails). The b28a22f self-heal premise is wrong or the seed-refresh UFC leg is skipping enrichment (skip_cached short-circuit?). Diagnose the UFC details-walk path; a forced re-walk (`?force=true` on /api/wallet-backfill-ufc) is the likely fix — diagnose first, then fix or queue for CC with evidence. **UPDATE 06-13 (CC, SHIPPED `fb2fbac`):** root-caused + fixed. Null-key had dropped to 1,252 concentrated in 8 wallets; the real defect is that `triggerUfcEnrichmentChain` always restarts the full-wallet walk at offset 0, so the 1,571-moment `0x60daa8…` wallet can't finish 16 pages in one lambda and never reaches its later null rows (b28a22f couldn't fix this). New `/api/cron/ufc-enrichment-drain` enriches NULL-key moment_ids directly (GET_META verified live). It is INERT until the operator wires the cron-job.org entry (see cron-schedule.md Pending additions + ledger 06-13 OPERATOR note). **Night pass: do NOT re-diagnose or `?force` UFC; just verify null_key trends <100 once the cron is live. The fossil wallet `0x6d1f8c18` (2 rows, NFTs transferred away) is an expected permanent residual.** DO-NOT-TOUCH (fresh): app/api/cron/ufc-enrichment-drain/route.ts. (b) DBSAT-0612 RESOLVED ~15Z — re-baseline metrics-latest.json on tonight's clean values (the 06-12 run's file was incident-partial); verify fmv-recalc/analytics-smoke streaks held and TFP's 01:15Z tick fired (its watchlist is already restored at 480, gate met: 01:15Z + 19:15Z both ok outside saturation — do NOT re-apply). (c) ARTIFACT ESTATE CHANGED 06-12: all active artifacts now load via ONE consolidated jsonb_build_object payload query (single execute_sql call; some carry a SET statement_timeout prefix); rpc-live-health absorbed rpc-fmv-watch + rpc-insights-health; the "(RETIRED)" tombstones (audit-followups, pipeline-reliability, security-drift, fmv-watch, insights-health) are INTENTIONAL static pages with mcpTools:[] — never "repair" them back to live. When repairing an active artifact, preserve the single-payload pattern — never reintroduce parallel per-panel calls. (d) Wave-pacing CC handoff written: docs/handoff-2026-06-12-wave-pacing.md — Item 1 SHIPPED by CC (`eba6491`, cohort=K&of=N route support, deploy READY); Item 2 (cron split) EXECUTED by Cowork 22:00Z: 4 cohort entries live (jobs 7801778/7801780/7801781/7801782 @ 00:45/00:59/01:13/01:27 + 06:45/06:59/07:13/07:27 etc. UTC), legacy 7491038 disabled-not-deleted. (e) CORRECTIONS from CC's 06-12 evening session (`62735e5`): the wmc-fifth-call-site handoff was ALREADY SHIPPED 06-09 (`a3c1a0c`+`acf85c0`) — it is annotated SHIPPED/DO-NOT-RE-EXECUTE; never re-queue it. AND item 6's "cache-refresh route's wmc upsert is DEAD" claim above is WRONG — /api/cache-refresh is client-triggered on-demand by collection/page.tsx (:687, :1178), it just never logs pipeline_runs; its onConflict was fixed in a3c1a0c; do NOT propose deleting it. (f) DECISION CHECKPOINT — tonight's 06:45–07:27Z cohort wave (which the 01:02 PDT night pass runs right after) is the compute-add-on evidence test: the wmc WRITE storm is closed, the residual wave pressure is per-wallet pipeline FAN-OUT (top shapes from CC's 6.4h pg_stat window: refresh-stats family p_wallet_address RPC 763 calls/8.2s mean, upsert_wmc_batch 3.1s mean under load vs 169ms calm, loadCachedMomentIds 2.3s, backfill_wmc_metadata 5.3s). Judge: does the 07:00Z+ window stay clean with the cohort split live? Report fails/hr 06:45–08:30Z + whether those shape means stay near calm baselines. Clean = pacing solved it; exhausted again = genuine throughput limit, the clean trigger for Trevor's add-on decision.

10. RESTORED (was lost in today's doc churn; condensed): (a) SHIP-eligible at low load — pinnacle-wmc-render-id candidate-read partial index: CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_wmc_pinnacle_render_id_null ON public.wallet_moments_cache (moment_id) WHERE collection_id = '7dd9dd11-e8b6-45c4-ac99-71331f959714' AND render_id IS NULL; — STANDALONE execute_sql; if an INVALID leftover exists, DROP INDEX first; daytime attempts hit the MCP statement cap. Until it lands its hourly :37 candidate_read 8s-timeout failure is KNOWN. (b) Today's Cowork ships (verify, don't re-derive): drops api_harvest+marketplace_offers 24/25 (DB 6.9->4.2GB), reconcile 60s fn timeout (3 green ticks), trust-health pinnacle leg, audit_lt_matches RLS hardening, pick_verification_target skip-locked, fmv-recalc early-exit logging (89c4891, monitor-verified green). (c) Remaining unlogged fmv-recalc exit = maxDuration 300s hard-kill (handoff addendum 4). (d) This is your first clone-flow run — pushurl carries the PAT, never echo it.
