{
  "generated_at_pt": "2026-09-18 ~07:5x PT",
  "generated_at_utc": "2026-09-18T14:5xZ",
  "pass": "rpc-nightly-autonomous-pass (sandbox) — OFF-HOURS monitor (~07:5x PT) + NO-PUSH (sandbox git has no credential; desktop pushurl harvest dead). Handoff/ledger/this file UNCOMMITTED on mount.",
  "shipped": 0,
  "reverted": 0,
  "verdict": "MONITOR-ONLY. Live DB read-availability event in progress (external clients starved, engine ACTIVE_HEALTHY). Cause NOT asserted (§1c — could not take the pg_stat_activity positive control). Nothing shippable/safe; loud handoff + queue. No ship in prior 24-48h => nothing to auto-revert.",
  "EVENT_2026_09_18": {
    "summary": "External DB reads failing while engine healthy and internal pg_cron work completing normally.",
    "onset_utc": "~12:48Z (NEW <!DOCTYPE html> runtime-error cluster: fmv-recalc, refresh-insights-cache candy/panini/player/scarcity boards, popular-on-collection golazos/candy/pinnacle)",
    "acute_conn_refusal_utc": "~14:20-14:55Z (MCP SELECT 1 refused ~13x, 'Connection terminated due to connection timeout')",
    "instruments": [
      "MCP execute_sql: connection establishment refused",
      "prod /insights/squeeze + /insights/rookies: honest PARTIAL-DATA degraded state (no fabricated zeros)",
      "Vercel runtime errors: new <!DOCTYPE html> fetch cluster + chronic slow-read RPC timeouts, last=14:45Z",
      "get_project=ACTIVE_HEALTHY, prod deploy READY (00662ce)"
    ],
    "discriminator": "postgres_logs 12:00-14:30Z show Atlas drains(462/463/448/449/466)+hydrate(469)+sync_sales(471)+wmc-backfill(302) starting AND completing normally; only 2 internal statement cancels. Internal work on reserved conns proceeds; external MCP+PostgREST starved (prod reads abort on 8s client budget before DB logs a cancel). 14:30-14:55Z log gap = ingestion lag, not a second signal.",
    "cause": "NOT ASSERTED. Best-supported: connection-pool/IO saturation from concurrent pipeline load on SMALL tier (wallet-backfill fan-out back-pressure gap, inbox 2026-09-13T1445Z, first suspect). Connection-layer/pooler incident not excluded. Separable only by the positive control.",
    "attribution": "NONE — no ship in prior 24-48h; operational, not a code regression.",
    "owed_next_db_access": "positive control: SELECT count(*) FILTER (WHERE wait_event_type='IO') io_wait, count(*) FILTER (WHERE state='active') active, count(*) total FROM pg_stat_activity WHERE pid<>pg_backend_pid(); then confirm self-clear + record window; then find pinning reader from pipeline_runs/pg_cron around 12:48-15:00Z."
  },
  "health_at_close": "UNMEASURED — DB unreachable all run (~13 connection refusals over 35 min). Security block, trust health, stalled_pipelines, rpc_ops_snapshot, FMV split, db_size, sentinel all UNMEASURED (not reported clean; no delta fabricated).",
  "carried_forward_from_2026_09_14": {
    "note": "Values below are the 2026-09-14 close, NOT re-measured this run.",
    "db_size_mb": 30574,
    "editions_by_collection": {"nba_top_shot": 14015, "nfl_all_day": 6190, "ufc_strike": 518, "laliga_golazos": 575, "candy_mlb": 125},
    "sentinel_ts_uuid_editions_48h": 0,
    "trust_note": "#82 topshot_impossible_parallel_serials RESOLVED 09-14 (metric read 0)."
  },
  "sentry_24h": "skipped — browser SDK off / events dropped per #34 (no-spend). Server picture taken from Vercel runtime errors instead.",
  "artifacts": "11 enumerated, none flagged broken, none repaired. Payload validation DEFERRED — every payload query hits the same unreachable DB; do not fire heavy payloads into a possible spell. Re-validate in a quiet window.",
  "vercel": "latest prod deploy READY (00662ce, migration-autorecover bot). No new deploy this run. Runtime errors dominated by the event's read timeouts + chronic slow-read RPCs.",
  "queued": [
    "[THIS EVENT] diagnose via positive control on next DB access; relieve source (wallet-backfill back-pressure first suspect) — needs diagnosis + push/decision",
    "Q-SCB sales-claimable partial indexes (inbox 2026-09-13T1800Z)",
    "Q-MISKEY #82 re-key + writer fix (destructive + route-logic; Trevor/CC)",
    "Q-CONCIERGE goofy soft-probe contract decision",
    "#100 master-alarm GHA trigger rate; #101 topshot-misattrib-drain backlog; #102 suppression is_active scoping",
    "Q-RETENTION cron.job_run_details + Atlas events (destructive; ~+1.36GB/24h)",
    "#55 Routines enabled:false; #22 credential-purge GC + rotate; cron-job.org re-enables",
    "inbox archival (needs push-capable pass)"
  ],
  "ops_note": "NO-PUSH: handoff + ledger entry + this file UNCOMMITTED on mount. A push-capable pass must git fetch + reconcile, re-splice the ledger entry at the first ^### , run the three ledger guards, then commit. CLAUDE.md Recent-sessions line owed to a push-capable pass. Inbox NOT archived (needs push)."
}
