# RPC overnight pass — ledger

Rolling record of items the nightly autonomous pass has shipped / queued / declined. The night pass reads this fully before acting and never re-suggests anything under "Declined — do not re-suggest" (that heading is Trevor's to edit).

Format per item: date · status · what · revert path (if shipped) · target metric · night-count (if queued).

---

## Shipped (autonomous, with revert path)

_None yet._

---

## Queued — awaiting a clean window or operator decision

- **Q1 · RESOLVED 2026-05-31** (operator/Cowork, migration `audit_20260531_pack_reality_views_security_invoker_and_revoke_anon_write`; also revoked inert anon write grants). Originally: Flip remaining 3 `topshot_pack_reality_*` views to `security_invoker`.
  Clears the last 3 Supabase `security_definer_view` ERRORs (down from 14; the operator flipped 11 live this afternoon via `audit_20260530_secdef_views_to_security_invoker`). NOT auto-shipped because the operator was actively executing this exact remediation during the run — collision. Verified safe (all readers are service_role; no anon client reads).
  Ready migration: `ALTER VIEW public.topshot_pack_reality_{top_ev,stats,dist} SET (security_invoker = on);`
  Revert: `... SET (security_invoker = off);`
  Target: `get_advisors(security)` SECDEF-view ERROR 3 → 0; `/api/public/insights/pack-reality` still returns rows.
  **Verified by night pass 2026-05-31 08:10Z:** all 3 `topshot_pack_reality_*` views `security_invoker=on`; 0 RLS-off public base tables; 0 base-table anon/authenticated write grants (`relkind in (r,p)`). Closed.

- **Q2 · 2026-05-30 (night 2) · Confirm `compute-laliga-pack-ev` cron cadence (inbox P4).**
  Idle ~17h, 0 fails — likely by-design (Golazos has no confirmed primary pack path). Operator to verify the cron-job.org entry. Not autonomous (pack-EV logic off-limits + external cron). **Update 2026-05-31 08:10Z:** ran 05:30Z (2.6h ago) → appears active now; downgrade to watch.

- **Q3 · 2026-05-31 (night 1) · [HIGH] `topshot-sales-indexer` external-trigger stall.**
  `topshot-sales-indexer` + `topshot-listing-cache`/`-v2` chain has NO `pipeline_runs` entries since 2026-05-31 **01:32/01:35 UTC** (~6.5h at detection; prior cadence 1–2.25h). The 01:32 run succeeded cleanly (`ok=true`, 26 rows, no error) → stopped EXTERNAL trigger (cron-job.org), not a route crash. `sales` max `ingested_at` for `nba_top_shot` = 01:32:31Z (stale); AllDay sales fresh (08:00Z) → TS-specific. NOT caused by any deploy (stall predates the 02:27–06:29Z deploys). No acute user impact (prod READY, FMV fresh via fmv-recalc) but TS sales/analytics freshness degrades while it persists.
  **Operator action:** check + re-fire the cron-job.org entry for `topshot-sales-indexer` (and chained `topshot-listing-cache`); route is healthy (last invocation OK). Not autonomous (ingest route logic off-limits + external cron + NO-PUSH this run). Corroborated by Sentry `NEXTJS-B`.

- **Q4 · RESOLVED 2026-05-31 (Claude Code, commit `48f5a98`, deploy `dpl_Gaz5NMzE2JTpViRPHZoQjKwJ9pKx` READY).** Root cause was NOT a counting bug — it was a wrong-table resolver. Both `pinnacle-listings-indexer` and `-retry` derived an `edition_key` then looked it up ONLY in `editions` (whose Pinnacle coverage is ~0; live: 0 of 104 distinct failing keys were in `editions`), queuing a failure on every miss → genuinely-new failures every tick → the warn fired honestly each tick. But `cached_listings_v2.edition_id` is a UUID FK to `editions.id`, so Pinnacle (editions live in `pinnacle_editions`) could NEVER populate it — the whole failure/retry machinery chased an impossible target.
  Fix: both routes now resolve the edition_key against `pinnacle_editions` AND `editions`; a hit in either = resolved (no failure / mark resolved), `edition_id` stays null for the pinnacle_editions-only case (expected, by design). Miss reason renamed `edition_key_not_in_editions_table` → `edition_key_unmapped` (key in neither table = genuinely unknown edition). Sentry capture rate/reason-gated: pages only on a real spike (>25 new/tick) OR an unexpected reason, not per-tick. Scoped to the two Pinnacle-only routes; AllDay indexer (editions-backed, legit reason) untouched. One-time DML stamped 224 now-resolvable capped-backlog rows resolved (945 → 719 unresolved, the genuinely-unmappable tail). Live: 73 of 104 distinct mapped keys are in `pinnacle_editions`.
  Revert: `git revert 48f5a98` (code only; the resolved_at stamp is harmless to leave).
  Target: `NEXTJS-15` stops firing every tick (mark Sentry issue resolved after 24h quiet); new `listing_resolution_failures` inserts trend toward the genuinely-unmappable residual only.

- **Q5 · 2026-05-31 (night 1) · [MED] smoke `analytics_pipeline_health.sales` lag threshold (30m < ~2h indexer cadence).**
  Intermittent false `degraded` (`NEXTJS-B`). **Do NOT blindly raise the threshold — tonight it is a TRUE positive flagging Q3.** Better fix: compute lag from the last *successful sales-indexer run* (not newest `sales.sold_at`), so "no sales happened" ≠ "pipeline unhealthy," while still catching a real multi-hour stall. Touches `analytics_pipeline_health` RPC + smoke config → operator/CC.

- **Q6 · 2026-05-31 (night 1) · [LOW] `evm-transfers-ingest` Base-429.**
  6/21 fails/24h "over rate limit"; Beezie/Base parallel plane, no product consumer, self-recovers next tick. Add backoff/jitter or lower per-tick block range. Pure ingest-cadence tweak; low priority.

- **Q7 · 2026-05-31 (night 1; root-caused 2026-05-31 PM) · [INFRA] recurring `.git/index.lock` + `.git/HEAD.lock` orphaned by the scheduled sandbox.**
  ROOT CAUSE (confirmed via `.git/logs/HEAD` reflog): the scheduled-task sandbox shares Trevor's REAL working `.git`. The reflog shows commits by both `Trevor <tdillonbond@gmail.com>` (−0700, Windows/Claude Code) AND `rpc-daytime-monitor <monitor@rippackscity.com>` (+0000, sandbox) interleaved against the same repo. No custom hooks (`.git/hooks` is all `.sample`). The monitor/night-pass run `git pull --rebase` + commit; when that collides with a concurrent Windows commit, or the sandbox is killed mid-rebase, it orphans `index.lock`/`HEAD.lock` — and the sandbox then can't `rm` them (`Operation not permitted`; the locks are owned by the Windows uid / the `.git` mount denies the unlink), so they persist until Trevor clears them on Windows (`Remove-Item .git\index.lock, .git\HEAD.lock`). The stray `.git/.rpc_write_test` + `.git/__wtest` files are leftover writability-probes — a symptom, not the cause.
  DURABLE FIX (infra, Trevor's call): give the scheduled-task runner its OWN clone of the repo — a separate checkout that pulls from / pushes to `origin`, NOT the mounted Windows working tree. Then the sandbox's pulls/commits/crashes can never lock Trevor's repo; the only shared point becomes `origin`, which handles concurrency correctly. This ALSO unblocks the night pass shipping code: private clone + GitHub creds ⇒ it can push to `main` like Claude Code does. Until then the night pass stays structurally DB-migration + artifact + on-disk-docs only. (An in-sandbox stale-lock auto-clear guard was considered and REJECTED: the sandbox can't reliably unlink the locks anyway, and age-based clearing risks clobbering a lock a concurrent Windows commit legitimately holds.) Full CC/infra handoff: `docs/handoff-2026-05-31-git-locks-and-followups.md`.

---

## Resolved during the day by the operator (logged so the pass doesn't re-raise)

- **C1 (handoff `docs/handoff-2026-05-31-git-locks-and-followups.md`) · allday-listings-indexer Sentry noise (`NEXTJS-15`)** — RESOLVED by Claude Code commit `221ab64` (deploy `dpl_GQdnj3EJspQhgJSDjxsmtaCWzQMf` READY). The Q4 pinnacle fix (`48f5a98`) gated this captureMessage for the two Pinnacle routes only and explicitly left AllDay untouched, so the AllDay indexer kept firing `listing_resolution_failures_inserted` on every tick that queued any rows (~50 events/day for a 3-row standing backlog) — pure noise masking real spikes. Mirrored `48f5a98` exactly: added `SENTRY_SPIKE_THRESHOLD=25` + an `EXPECTED_FAILURE_REASONS` set (`edition_external_id_not_in_editions_table`, `cadence_fallback_cap_hit`, `wmc_miss_no_seller_cadence_attempt`); the capture now pages only on `queuedFailures > 25` OR an unexpected reason. Failure-row inserts unchanged; no resolution-logic change (AllDay is editions-backed). Revert: `git revert 221ab64`. Target: `NEXTJS-15` stops accruing AllDay events every tick — mark the Sentry issue resolved + confirm 24h quiet (re-opens only on a real spike); `pipeline_runs` for `allday-listings-indexer` unchanged.
- **P2 (inbox 2026-05-30T21:32) · pinnacle-listings-indexer Sentry noise** — RESOLVED by commit `bd4d8c4` (deploy `dpl_4UBShmk267fUqit46PFZNuTLRFJd` READY). Counter now only increments on genuinely-new failure inserts.
- **P3 (inbox) · pinnacle/moment null destructure (`JAVASCRIPT-NEXTJS-1B`)** — RESOLVED by `01b3878`→`26d5968`→`fe96d4b`. Stopped firing ~5h before the run. Mark the Sentry issue resolved once 24h clean.
- **P1 (inbox) · 11 of 14 SECDEF views** flipped to `security_invoker` via `audit_20260530_secdef_views_to_security_invoker` (operator, 21:48 UTC). Tail-3 tracked as Q1.
- **Entity-pages pass (operator/Cowork, 2026-05-30) · 3 migrations shipped LIVE + code packaged for Claude Code.** All read-only SECDEF, `service_role`-only, verified: `audit_20260530_fix_get_team_players_dedup_roster` (team roster no longer fans out duplicate `players` rows — Lakers 30×LeBron → 73 distinct), `audit_20260530_fix_get_team_detail_player_count_dedup` (`player_count` 116→73), `audit_20260530_add_get_team_top_editions` (NEW rpc — revert `DROP FUNCTION public.get_team_top_editions(uuid,text,integer,integer);`; the two fixes' prior bodies are in the 2026-05-30 session transcript but reverting a bug fix is not expected). **Do NOT re-suggest team-roster / player-count work — fixed.** Remaining entity-page work (P0 edition-404 colon-decode fix, JSON-LD ×6, pack contents grid, hero montages, branded OG cards, hover-video) is packaged for Claude Code in `docs/handoff-2026-05-30-entity-pages.md` — **night pass should NOT auto-ship these (operator/CC owns them).** Full audit: `docs/audits/entity-pages-improvement-2026-05-30.md`.
  **UPDATE 2026-05-31 — CODE HALF FULLY SHIPPED by Claude Code (commits `bf3f4f6`, `b106a27`, `5797235`, `29d2e46`; all 4 CI+Smoke green, all deploys READY).** All 8 handoff items live + verified. Also opened the entity detail pages (`/<collection>/{edition,set,player,team,series,pack}/<slug>`) + `GET /api/entity/*` + `/api/og/*` to anon in `proxy.ts` (with Trevor's go-ahead) — they were auth-gated despite being advertised in the sitemap, so the SEO work was invisible to crawlers. One extra migration shipped live: `audit_20260530_entity_edition_rpcs_thumbnail_filter_and_video_url` (grid RPCs filter `thumbnail_url IS NOT NULL` + expose `video_url`). See CLAUDE.md Recent sessions 2026-05-31. **Night pass: entity-pages work is DONE — do not re-raise.**
- **Entity-pages verification pass (operator/Cowork, 2026-05-30) · 1 migration shipped LIVE + backfill scoped.** Independent anon-side check of the now-public surfaces: confirmed `/api/entity/*` leaks no private data (sales expose only public on-chain fields — wallet addresses, tx hashes; team/player expose only public catalog fields; no email/user_id/token), and edition `Product`+`BreadcrumbList` JSON-LD validates live. **Found + fixed a pre-existing bug:** `get_edition_recent_sales` had 3 args but both callers pass a 4th `p_offset`, so Recent Sales 500'd / silently emptied on every edition page (now user-visible since editions render). Shipped `audit_20260530_get_edition_recent_sales_add_offset` (added `p_offset DEFAULT 0` + OFFSET; dropped the old 3-arg overload; service_role-only) — fixes it with ZERO code change (callers already pass 4 args); verified live anon API now 200 (was 500). Revert: `DROP FUNCTION public.get_edition_recent_sales(uuid,text,integer,integer);` then re-CREATE the prior 3-arg body (in session transcript). **Thumbnail backfill scoped:** only ~24 real tradeable canonical TS editions lack art (the other 216 artless-canonical are circulation-null stubs); 0 are recoverable in-DB (no sibling art, no video) — they need a small Top Shot GQL fetch via topshot-proxy (low priority, optional CC task). Not shipped.
- **Entity-pages QA sweep (operator/Cowork, 2026-05-30) · 1 migration shipped LIVE + cross-collection render verified.** Caller-vs-signature audit of all entity RPCs: clean (the recent-sales `p_offset` was the only mismatch). Cross-collection render verified — edition pages resolve + render for all 5 collections incl. Pinnacle's colon+space slugs (`20CS-…:Silver Sparkle:1` → "Prolemuris", 4 JSON-LD blocks) and AllDay integer slugs; Pinnacle set/series/character/franchise all resolve; UFC has no teams by design. **Found + fixed:** `get_player_detail` picked an arbitrary `players` row (`LIMIT 1`, no ORDER BY) among ~3× name-dupes, so 349 TS players showed an arbitrary team + team-link (LeBron could show Cavaliers). Shipped `audit_20260530_get_player_detail_dominant_team_pick` (pick the row whose team holds the most of the player's editions; aggregates unchanged via the player_name OR-join) — verified LeBron → Los Angeles Lakers, 148 editions complete. Revert: re-CREATE the prior body (session transcript); reverting a bug fix not expected. **Minor code-side follow-ups (NOT shippable from Cowork, low priority, for a future CC pass):** the entity edition grids ARE correctly thumbnail-filtered (verified `get_player_editions` returns 89/148 for LeBron, 0 null, 0 dupe slugs), but the player-hero portrait uses `editions[0].thumbnail_url`, which can be a non-null-but-DEAD CDN url — LeBron's #1 edition "2020 NBA Finals" ($4049) renders a broken hero + lead tile. Two angles: (a) cheap — add a client-side `onError` fallback on the portrait/tile `<img>` to the next edition's thumbnail or a placeholder; (b) root — a tail of canonical edition `thumbnail_url`s 404 on the nbatopshot CDN (distinct from the null-coverage gap); a TS-GQL url-refresh would fix both the dead URLs and the 24 null-canonical tradeable editions. **5 migrations total across this entity-pages effort (3 team + recent-sales offset + player-detail team); night pass: all DONE — do not re-raise.**
- **"What's next" push (operator/Cowork, 2026-05-31) · 1 migration shipped LIVE + 3 handoffs + a P0 funnel leak found.** (1) **SEO indexing readiness:** edition pages have correct canonical + `robots index,follow` + robots.txt allows them, BUT the LIVE sitemap.xml has **0 entity URLs** (258 total) — `getEditionRows`/`getCollectionSeries` use a PostgREST embedded join (`collections!inner(slug)`) that yields `[]` in prod while RPC-based entries populate; pack pages were never in the assembly; the bare `/profile` robots disallow prefix-blocks public profiles. Robust fix (collection_id-filter rewrite + packPages + robots) in `docs/handoff-2026-05-31-sitemap-seo.md`. **This is the real SEO blocker — resubmitting the sitemap is pointless until entity URLs are in it.** (2) **Insights surfaces:** pack-reality + rookies already built + live (verified anon 200). Shipped `audit_20260531_pack_reality_views_security_invoker_and_revoke_anon_write` — closes ledger **Q1** (last 3 of 14 SECDEF views → `security_invoker` + revoked the inert anon WRITE grants, kept SELECT; reader is service_role so verified-safe; views still return rows + page renders). Revert: `ALTER VIEW public.topshot_pack_reality_{stats,dist,top_ev} SET (security_invoker=off);`. (3) **Entity polish handoff** (onError image fallback for dead-CDN thumbnails): `docs/handoff-2026-05-31-entity-polish.md`. (4) **P0 FUNNEL LEAK found (handoff, not shipped):** the marketing homepage (shown ONLY to logged-out users) routes its primary wallet-paste CTA to the auth-gated `/<collection>/collection` → every anon user hits `/login` at the activation moment; `/share/[wallet]` (the built results card) is also not anon-public. Fix (open `/share` to anon + route the marketing box there + signup CTA + an RPC Index hub) in `docs/handoff-2026-05-31-wallet-paste-onboarding.md` — directly attacks the pre-traction activation bottleneck. **Night pass: do NOT auto-ship the 3 handoffs (operator/CC owns funnel + sitemap + robots changes); the pack-reality security migration is DONE.**
- **"All 4 / what's next" traction push (operator/Cowork, 2026-05-31) · 3 migrations + 1 table shipped LIVE + traction dashboard + funnel/ops handoff.** Traction reality check: **13 total users** (0 signups in 7d, last May 9), **0 outbound clicks in 30d+** (last Apr 25), **~1 real concierge/wk** — deeply pre-traction, and all of it pre-funnel-fix. Shipped LIVE (all service_role-only unless noted): (1) `get_rpc_traction_snapshot()` + enhanced the existing `rpc-traction` artifact with a Users & signups row (the WAU-toward-50 metric it lacked). (2) `funnel_events` table — RLS on, anon INSERT-only (no anon SELECT), event_type allowlisted + length-capped — to instrument anon top-of-funnel (home/share/insights views + wallet-paste); route emits in `docs/handoff-2026-05-31-funnel-instrumentation-and-ops.md`. (3) `detect_stalled_pipelines()` + added the 4 `*-sales-indexer` pipelines to `pipeline_cadence_watchlist` (TS 180m, others 90m) — closes the absence-of-runs gap that let the TS-sales-indexer stall go undetected (Q3 class); monitor/sentinel wiring + the **Q5 smoke-lag rebase** are in that same handoff. **LIVE finding: `pinnacle-resolve-buyers` is stalled (284m silent vs 180m, high) — operator: re-fire its cron** (same class as the TS stall). Reverts: `DROP FUNCTION get_rpc_traction_snapshot();` · `DROP FUNCTION detect_stalled_pipelines();` · `DROP TABLE funnel_events;` · `DELETE FROM pipeline_cadence_watchlist WHERE pipeline LIKE '%-sales-indexer'`. Task 22 (extend cohort product) scoped, not built — awaiting product direction. **Night pass: don't auto-ship the funnel/ops handoff (operator/CC owns route + scheduled-task wiring).**

---

## Declined — do not re-suggest
_(Trevor owns this section. Add an item here to stop the night pass proposing it.)_

_None._
