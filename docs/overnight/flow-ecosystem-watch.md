# Flow Ecosystem Watch — running log

Weekly READ-ONLY external-intelligence sweep for RPC. Beat = the OUTSIDE world (new marketplaces, contracts, standards, execution venues, IPFS expansion) for the Flow collections RPC tracks — complements `rpc-daytime-monitor` / weekly-health, which watch RPC's own pipelines. Design + rationale: `docs/strategy/flow-onchain-intelligence-2026-06-09.md`; dapper.market recon: `docs/research/dapper-market-recon-2026-06-08.md`.

Each entry records what was checked + found so the next run has a baseline and doesn't re-report known items. Newest first.

---

## 2026-09-02 — no new material ECOSYSTEM developments (no new marketplace / contract / execution venue / IPFS resolver; venue detection CLEAN, positive control fired). ⚠ One INTERNAL, non-ecosystem finding surfaced and flagged for the pipeline monitors (NOT my beat): the `topshot_gql` TS-sales source tag has been **DARK since ~08-29** (5+ days) and TS `onchain` volume stepped down ~2.5x over the same window — so this run's TS volume numbers UNDERCOUNT and must NOT be read as a market decline. WNBA `resolver_misses` FLAT at 80 → no refresh action.

Verdict: nothing clears the new-venue / competitive-threat escalation bar → **no Trevor ECOSYSTEM notification**. No IPFS-refresh action (canonical `resolver_misses` = 80, unchanged vs 08-26 — no DROP). One-week gap since 08-26. **The one thing worth a heads-up is internal, not ecosystem** (§3, TS feed) — handed to `rpc-daytime-monitor` / weekly-health, which own pipeline health; I only sense the outside world and am read-only.

**1. Ecosystem / web sweep**
- **No new Flow marketplace / venue / contract / standard** for RPC's collections. Web surfaced nothing dated late-Aug/Sept-2026 — only the known May-2026 NFL All Day minting-pause / next-gen story (still unshipped) and pre-existing venues (Flowty, Flowverse/Gaia/Rarible aggregation, Flow↔OpenSea Crescendo interop). No **dapper.market** change found via web (not live-crawled): still presumed 3 collections (TS/AllDay/Golazos), no Pinnacle/UFC, no unified search, no analytics/FMV tab. RPC moat intact.
- **NFL next-gen reveal — STILL unshipped; the imminent watch item.** 2026 NFL regular season kicks off ~Sept 10 (the promised "closer to the start of the season" reveal window is NOW open). Nothing on-chain yet: AllDay acct `0xe4cf4bdc1751c65d` carries only the `AllDay` contract (§2a), and no new Dapper NFL contract account surfaced. **Carry (elevated):** if a next-gen NFL product ships a new contract/collection over the next few weeks that's a new index target — re-check AllDay acct + hunt any new Dapper NFL contract next run.

**2. IPFS expansion tripwires**
- (a) **No new AllDay/Pinnacle IPFS resolver.** AllDay acct `0xe4cf4bdc1751c65d` → `AllDay` only (standard Series/Sets/Plays/Editions/Badges/Parallels; REST base64 truncates inside AllDay's own source mid-`addBadgeToEntity`, same as every prior run — tail resolver not 100%-excludable via REST but no announcement). Pinnacle acct `0xedf9df96c92f4595` → `PackNFT` + `Pinnacle`, both fully readable — PackNFT `getViews()`→`[]` / `resolveView`→nil, its NFTCollectionDisplay media is the **generic Flow wordmark SVG** (`assets.website-files.com/…/Flow_Wordmark.svg`); Pinnacle NFT is render-keyed off-chain (renderID-driven Series/Sets/Shapes/Editions + Inscriptions/XP, no media/IPFS resolver). No new contract on either account. HIGH-value find still absent.
- (b) **WNBA `resolver_misses` = FLAT at 80.** Canonical daily 09:49 UTC art cron = **80** on 09-01 and 09-02 (was 80 on 08-26 and 08-19) — dead flat. The 3-hourly `:22` runs under the same `pipeline` name read **291–300** (consistent with the 250–300 baseline; wider scan scope / second job). No DROP → **do NOT run `refresh-ipfs-catalog.mjs`** this run. Daily art cron green (`ok=true`) every run.
- (c) **Reference-app** `https://dapperlabs.github.io/dapperlabs-ipfs-reference-app/` — fetched OK (~61 KB client-rendered shell), still **TS-only** (title "NBA Top Shot IPFS Reference", topshot-logo.svg, Series 1–8 + TS parallels Astra/Bit/Blockchain/Bubbled/Club Collection…; **zero** AllDay / Pinnacle / WNBA mentions). No change.

**3. On-chain data-anomaly sweep (read-only)**
- **Venue detection — CLEAN, no new execution venue.** TS onchain last 1d: the ONLY proposer-bearing pair is the known Dapper custodial rail proposer `0xead892083b3e2c6c` / payer `0x18eb4ee6b3c026d2` (`distinct_payers=1`) = **589 sales / 78 buyers** (positive control fired → instrument has signal). New-proposer query (TS onchain proposer ≠ `0xead…` over last 7d) = **`[]`** (empty). No new/other proposer anywhere → no new venue. Capture live: `proposer_address`/`payer_address`/`buyer_address` columns present and populated on `onchain` rows.
- **Source mix — no new EXTERNAL tag, but one INTERNAL tag went DARK.** TS 2d: `onchain` 1,209 / `offer_fill` 1,152 — and **`topshot_gql` = 0** (was 2,495/2d on 08-26). AllDay 7d: `onchain_dapper_v2` 751 / `onchain_dapper_v1` 329 (known rail, organic post-pause). Golazos 7d: `onchain_dapper_v2` 7 (near-dead). No UFC secondary (≈0, Aptos). No new external venue tag anywhere. (`candy_mlb solana_das` = Candy's own audit, out of scope.)
- ⚠ **INTERNAL finding (NOT ecosystem, flagged for `rpc-daytime-monitor`/weekly-health):** `topshot_gql` daily ramped down then **stalled** — ~1,300–1,500/day through 08-25, then 759 (08-26) → 100 (08-27) → 100 (08-28) → **0 (08-29 → 09-02, dark 5+ days)**. Concurrently TS `onchain` stepped down ~2.5x — ~2,000–2,700/day through 08-27 → ~500–980/day from 08-29. The `topshot-sales-indexer` reports `ok=true` with a live heartbeat (155 runs/2d, 1,209 written), so this is a source-tag/coverage change and/or NBA-offseason softness, **not an indexer crash and not a new venue.** Both feeds moving together (~08-27→29) argues against pure seasonality — worth the pipeline monitors disambiguating feed-vs-market. **Consequence for this log: TS volume figures here UNDERCOUNT true market volume — do NOT read the drop as an ecosystem/market signal.**
- **Volume (as recorded, undercounted per above).** TS daily 881–2,126 (09-02 553 partial). AllDay ~1,080/7d (organic). Golazos dead. No UFC secondary. **Pinnacle** (separate `pinnacle_sales`): normal 59–288/day (08-24 83 → 09-01 288 → 09-02 141 partial) — the post-D23 wave has fully receded to baseline; no venue signal.

**4. Flowscan glance:** skipped (slow/client-rendered; web + DB sufficient).

**Baselines carried to next run:** WNBA canonical 09:49 `resolver_misses` = **80** (FLAT since 08-19; DROP = pinning → refresh, RISE = new-mint churn → no action) · `:22` runs read **291–300** (unchanged divergence, wider scope) · TS custodial pair `0xead…`/`0x18eb…` = only proposer-bearing rail, **0 new proposers/7d**, positive control ~589/1d · **venue-detection MUST filter `source='onchain'`, not `marketplace='onchain'`** · **query `sales_2026` partition-direct + `collection_id` + ≤1–2d windows** · ⚠ **`topshot_gql` source tag DARK since ~08-29 + `onchain` stepped down ~2.5x — INTERNAL, owned by pipeline monitors; if still dark next run and TS volume still depressed, confirm it's tracked (feed retirement vs outage vs offseason) — NOT an ecosystem escalation** · no AllDay/Pinnacle IPFS resolver · reference-app TS-only · Pinnacle secondary back to 59–288/day baseline · dapper.market presumed still 3 collections / no analytics tab (not live-crawled) · `candy_mlb solana_das` in mix (Candy's own audit) · **NFL next-gen reveal window OPEN (~Sept 10 kickoff) — watch AllDay acct + any new Dapper NFL contract.**

Sources: NFL All Day pause / next-gen — decrypt.co/367926/nfl-all-day-stops-issuing-nfts-dapper-labs-future-plans-league ; Flow marketplaces (Flowty/Flowverse/OpenSea, no new aggregator) — flowverse.co/categories/nft-marketplaces , flow.com/use-case/nft ; Disney Pinnacle (no Sept-2026 drop news) — commonclubapparel.com/blogs/blog/disney-nfts-state-of-2026 . On-chain contract reads via rest-mainnet.onflow.org accounts `e4cf4bdc1751c65d` (AllDay) and `edf9df96c92f4595` (PackNFT+Pinnacle); reference-app dapperlabs.github.io/dapperlabs-ipfs-reference-app/ ; DB reads on `sales_2026` / `pinnacle_sales` / `pipeline_runs` (Supabase bxcqstmqfzmuolpuynti).

---

## 2026-08-26 — no new material ecosystem developments. No new marketplace / contract / execution venue / IPFS resolver. Venue detection CLEAN (positive control fired). WNBA `resolver_misses` FLAT at 80 → no refresh action. Post-D23 Pinnacle secondary still decaying, captured.

Verdict: nothing clears the new-venue / competitive-threat escalation bar → **no Trevor notification**. No IPFS-refresh action (canonical `resolver_misses` = 80, unchanged vs the 08-19 baseline — no DROP, so `refresh-ipfs-catalog.mjs` has nothing to absorb). One-week gap since 08-19; baselines re-verified below.

**1. Ecosystem / web sweep**
- **No new Flow marketplace / venue / contract / standard** for RPC's collections. Web search surfaced nothing dated Aug-2026 — only the Feb-2026 Dapper ecosystem PR (known), the May-2026 NFL All Day minting pause / next-gen-reveal story (known, still unshipped), and historical launch pages. The Flow↔OpenSea integration that surfaced is the pre-existing Crescendo/Flow-EVM interop (not new); Flowty/Flowverse are pre-existing. No **dapper.market** feature change found via web (not live-crawled this run) — still presumed 3 collections (TS/AllDay/Golazos), no Pinnacle/UFC, no unified search, no analytics/FMV tab. RPC moat intact. (Aside: a search surfaced a Disney Pinnacle marketplace fee/hours change — listing fee $0.25→$0.50, 24/7 availability — but that is dated **Dec 2025**, already known/past, and is a fee change, not a venue/contract signal.)
- **Carry:** NFL All Day next-gen reveal still unshipped (~Sept kickoff window; a new NFL contract/collection would be a new index target — check AllDay acct + any new Dapper NFL contract). D23 Anaheim (Aug 14–16) has passed; its Pinnacle drop wave is visible in RPC data (§3) and decaying.

**2. IPFS expansion tripwires**
- (a) **No new AllDay/Pinnacle IPFS resolver.** AllDay acct `0xe4cf4bdc1751c65d` → `AllDay` only (standard Series/Sets/Plays/Editions/Badges/Parallels; REST response truncates inside AllDay's own base64 source mid-`addBadgeToEntity`, same as every prior run — a tail resolver isn't 100% excludable via REST, but no announcement). Pinnacle acct `0xedf9df96c92f4595` → `PackNFT` + `Pinnacle`, both fully readable this run — PackNFT `getViews()`→`[]` / `resolveView`→nil and its NFTCollectionDisplay media is the **generic Flow wordmark SVG** (`assets.website-files.com/…/Flow_Wordmark.svg`); Pinnacle NFT is render-keyed off-chain (renderID-driven Series/Sets/Shapes/Editions + Inscriptions/XP, no media/IPFS resolver). No new contract on either account. HIGH-value find still absent.
- (b) **WNBA `resolver_misses` = FLAT at 80, NOT a pinning event.** Canonical daily 09:49 UTC art cron = **80** on both 08-25 and 08-26 (was 80 on 08-19) — dead flat vs baseline. The 3-hourly `:22` runs under the same `pipeline` name read **250–300** (consistent with the 08-19 baseline note of 244–300; a wider scan scope / second job, unchanged divergence). No DROP → **do NOT run `refresh-ipfs-catalog.mjs`** this run. Daily art cron green (`ok=true`) every run.
- (c) **Reference-app** `https://dapperlabs.github.io/dapperlabs-ipfs-reference-app/` — fetched OK (~61 KB client-rendered shell), still **TS-only** (grep: only "Top Shot"/"topshot"; **zero** AllDay / Pinnacle / WNBA mentions). No change.

**3. On-chain data-anomaly sweep (read-only)**
- **Venue detection — CLEAN, no new execution venue.** Sampled the last 7d in 1-day slices (days 0-1, 1-2, 4-5, 6-7): the ONLY proposer-bearing pair in every slice is the known Dapper custodial rail proposer `0xead892083b3e2c6c` / payer `0x18eb4ee6b3c026d2` (`distinct_payers=1`). No new/other proposer appears anywhere. **Positive control fired** — that pair = **2,471 sales / 286 buyers in the last 1d** — so the instrument has signal and the empty new-venue result is trustworthy.
- **Source mix — no new tag.** TS 2d: `onchain` 5,115 / `topshot_gql` 2,495 / `offer_fill` 815. AllDay 7d: `onchain_dapper_v2` 1,694 / `onchain_dapper_v1` 387. Golazos 7d: `onchain_dapper_v2` 5 (near-dead). All internal/known; no new external venue tag vs baseline. (`candy_mlb solana_das` still expected in the wider mix — Candy's own audit owns it, out of scope.)
- **Volume normal.** TS daily 3,868–5,956 (08-19 5,258 → 08-26 2,456 partial; 08-21 peak 5,956, all within historical range). AllDay ~2,081/7d (organic post-pause). Golazos dead. No UFC secondary. **Pinnacle** (separate `pinnacle_sales`): the D23-timed wave keeps receding — 08-17 **967** → 08-18 560 → 08-21 497 → 08-24 416 → 08-25 176 → 08-26 107 (partial). Product cadence, not a venue.
- ⚠ **METHODOLOGY (query cost) — this run the parent `sales` table timed out on any window ≥ ~2d, and even a collection-less `count(*)` over 6h died.** `sales` is year-partitioned (`sales_2020…2026`) with a `(collection_id, sold_at)` composite index and NO bare `sold_at` index. The working pattern: query the **current partition directly (`sales_2026`) + `collection_id=` + a tight `sold_at` window (≤1–2d), and slice a 7d window into 1-day probes.** Grouping by proposer/payer over >2d also timed out under load — do it per-day. (Instrument, not a finding — likely IO pressure; a collection_id-scoped 1d query returned instantly.)

**4. Flowscan glance:** skipped (slow/client-rendered; web + DB sufficient).

**Baselines carried to next run:** WNBA canonical 09:49 `resolver_misses` = **80** (FLAT since 08-19; DROP = pinning → refresh, further RISE = new-mint churn → no action) · `:22` runs read **250–300** (unchanged divergence, wider scope) · TS custodial pair `0xead…`/`0x18eb…` = only proposer-bearing rail, **0 new proposers/7d**, positive control ~2,471/1d · **venue-detection MUST filter `source='onchain'`, not `marketplace='onchain'`** · **query `sales_2026` partition-direct + `collection_id` + ≤1–2d windows (parent `sales` + wide windows time out)** · no AllDay/Pinnacle IPFS resolver · reference-app TS-only · Pinnacle secondary decaying post-D23 (08-17 967 → 08-26 ~107) · dapper.market presumed still 3 collections / no analytics tab (not live-crawled) · `candy_mlb solana_das` in mix (Candy's own audit) · NFL next-gen reveal still unshipped (~Sept — watch AllDay acct + any new Dapper NFL contract).

Sources: NFL All Day pause / next-gen — decrypt.co/367926/nfl-all-day-stops-issuing-nfts-dapper-labs-future-plans-league ; Dapper Feb-2026 ecosystem PR — globenewswire.com/news-release/2026/02/07/3234119/0/en/Dapper-Labs-Ecosystem-Disney-Pinnacle-NFL-ALL-DAY-and-NBA-Top-Shot-Drive-Consumer-Engagement-on-Flow.html ; Flow↔OpenSea (pre-existing Crescendo interop) — flow.com/post/flow-and-opensea-a-new-era-for-onchain-consumer-apps-and-digital-assets ; Disney Pinnacle marketplace (Dec-2025 fee/hours, old) — disneypinnacle.com/news/marketplace-101 . On-chain contract reads via rest-mainnet.onflow.org accounts `e4cf4bdc1751c65d` (AllDay) and `edf9df96c92f4595` (PackNFT+Pinnacle); reference-app dapperlabs.github.io/dapperlabs-ipfs-reference-app/ .

---

## 2026-08-19 — no new material ecosystem developments. No new marketplace / contract / execution venue / IPFS resolver. Venue detection CLEAN (re-verified after a query-column footgun). Two internal notes carried, both below the ecosystem bar: WNBA `resolver_misses` drifting UP (new-mint churn, not a pinning event) and a `source` vs `marketplace` gotcha that fakes a "feed stalled".

Verdict: nothing clears the new-venue / competitive-threat escalation bar → **no Trevor notification**. No IPFS-refresh action (the drift is an INCREASE, not a DROP — `refresh-ipfs-catalog.mjs` only helps on a drop). ~7-week gap since the last logged run (08-13); baselines updated below.

**1. Ecosystem / web sweep**
- **No new Flow marketplace / venue / contract / standard** for RPC's collections. No Aug-2026 Dapper announcement surfaced (most recent material news remains the **05-14 NFL All Day minting pause**; next-gen NFL product still unshipped, reveal framed ~season start). Ecosystem healthy (Messari Q1-2026 context: TS +12.2% QoQ, AllDay −19.3%). No **dapper.market** feature change found via web (not live-crawled this run) — still presumed 3 collections (TS/AllDay/Golazos), no Pinnacle/UFC, no unified search, no analytics/FMV tab. RPC moat intact.
- **Carry:** NFL next-gen reveal (~Sept kickoff, still unshipped — if a new NFL contract/collection ships it's a new index target). Disney Pinnacle **D23 Anaheim Aug 14–16 2026 event window has now PASSED** — its drop wave is visible in RPC data (§3) and decaying; captured, not blind.

**2. IPFS expansion tripwires**
- (a) **No new AllDay/Pinnacle IPFS resolver.** AllDay acct `0xe4cf4bdc1751c65d` → `AllDay` only (standard Series/Sets/Plays/Editions/Badges/Parallels; truncates inside base64 source, tail resolver not 100% excludable via REST but no public announcement). Pinnacle acct `0xedf9df96c92f4595` → `PackNFT` + `Pinnacle` (Pinnacle's `NFTCollectionDisplay` points at a **generic Flow wordmark SVG**, no per-edition media/IPFS resolver). HIGH-value find (an AllDayIPFSResolver / Pinnacle equivalent) still absent.
- (b) **WNBA `resolver_misses` = drifting UP, NOT a pinning event.** Canonical daily 09:49 UTC art cron = **80** (was 57 on 08-13, ~7–8 on 07-01). The 3-hourly `:22` runs under the same `pipeline` name read **244–300** — a higher/wider scan scope or a second job; the divergence is new vs 08-13 and worth a one-time characterization by `rpc-daytime-monitor`. An INCREASE = more unresolved on-chain media (consistent with 2025-26 / Series-8 new-mint churn outpacing Dapper's pinning), **not** a pinning DROP → **do NOT run `refresh-ipfs-catalog.mjs`** this run. Internal coverage note, below the ecosystem escalation bar.
- (c) **Reference-app** `https://dapperlabs.github.io/dapperlabs-ipfs-reference-app/` — fetched OK (live, ~61 KB, JS-rendered), still **TS-only** (grep: only "Top Shot"/"topshot"; no All Day / Pinnacle / WNBA). No change.

**3. On-chain data-anomaly sweep (read-only)**
- **Venue detection — CLEAN, no new execution venue.** New-proposer query (`source='onchain'` TS proposers whose first appearance within 14d is in the last 7d) = **[]**. Positive control confirms the instrument has signal: dominant pair proposer `0xead892083b3e2c6c` / payer `0x18eb4ee6b3c026d2` = **5,825** in 3d (the known Dapper custodial rail, still the ONLY proposer-bearing pair). Capture healthy: last 2d **4,166 / 4,605 (~90.5%)** onchain rows carry proposer+payer.
- ⚠ **METHODOLOGY CORRECTION for the next run — key venue-detection on `source='onchain'`, NOT `marketplace='onchain'`.** This run, `marketplace='onchain'` returned **0 rows over the last 7d** (last non-null a week back) and read like a stalled feed / false-clean; switching to `source='onchain'` showed the feed is fully current (last sale 08-19 16:03Z, d2=4,605). The `[]` new-venue result is only trustworthy because the positive control fired. (Classic footgun — the two columns are not the same.)
- **Source mix — no new tag.** 14d: TS `onchain` 35,535 / `topshot_gql` 14,862 / `offer_fill` 9,099; AllDay `onchain_dapper_v2` 3,943 / `onchain_dapper_v1` 708; Golazos `onchain_dapper_v2` 17 (near-dead); `candy_mlb` `solana_das` 1,595 (Candy's own audit — noted, not blind). All internal/known; no new external venue tag vs baseline.
- **Volume normal.** TS daily ~1,445–8,826 (08-15 spike **8,826** then 5,062 08-16 — a drop/promo weekend, within historical range, internal). AllDay ~124–813/day. Golazos 1–9 (dead). No UFC rows (secondary ≈0). **Pinnacle** (separate `pinnacle_sales`): ramped 08-05 (86) → **08-16 (1,151)** then decaying (08-17 967, 08-18 560, 08-19 246 partial) = the **D23-timed drop wave** receding. Captured, product cadence, not a venue.

**4. Flowscan glance:** skipped (slow/client-rendered; web + DB sufficient).

**Baselines carried to next run:** WNBA canonical 09:49 `resolver_misses` = **80** (was 57; DROP = pinning → refresh; further RISE = new-mint churn, no action) · `:22` runs read **244–300** (characterize the divergence once) · TS custodial pair `0xead…`/`0x18eb…` = only proposer-bearing rail, **0 new proposers/7d**, capture ~90.5% · **venue-detection MUST filter `source='onchain'`, not `marketplace='onchain'`** · no AllDay/Pinnacle IPFS resolver · reference-app TS-only · Pinnacle secondary decaying post-D23 · dapper.market presumed still 3 collections / no analytics tab (not live-crawled) · `candy_mlb solana_das` live in mix (Candy's own audit owns it).

Sources: NFL All Day pause — decrypt.co/367926/nfl-all-day-stops-issuing-nfts-dapper-labs-future-plans-league ; Messari State of Flow Q1 2026 — messari.io/report/state-of-flow-q1-2026 ; Dapper ecosystem (Pinnacle/AllDay/TS) — globenewswire.com/news-release/2026/02/07/3234119/0/en/Dapper-Labs-Ecosystem-Disney-Pinnacle-NFL-ALL-DAY-and-NBA-Top-Shot-Drive-Consumer-Engagement-on-Flow.html ; Flow marketplaces (Flowty/Flowverse/OpenSea, no new aggregator) — flowverse.co/articles/guide-to-flow-blockchain-nft-marketplaces . On-chain contract reads via rest-mainnet.onflow.org accounts `e4cf4bdc1751c65d` (AllDay) and `edf9df96c92f4595` (PackNFT+Pinnacle).

---

## 2026-08-13 — no new material ecosystem developments. No recurring action (WNBA `resolver_misses` FLAT at 57). Venue detection clean; no new marketplace / contract / execution venue. **Elevated watch — both event windows are NOW open: Disney Pinnacle D23 Anaheim Aug 14–16 (starts tomorrow) + NFL next-gen reveal (~Sept kickoff, still unshipped).**

Verdict: nothing clears the new-venue / competitive-threat escalation bar → **no Trevor notification**. No IPFS-refresh action: `resolver_misses` held **57** on the latest runs (band 56–66; 58 → 57 vs last run) — no pinning event (no DROP) and no material new-mint churn, so **do NOT run `scripts/refresh-ipfs-catalog.mjs`** (it only helps on a DROP). Everything else at baseline; venue detection clean.

**1. Ecosystem / web sweep**
- **No new Flow marketplace / venue / contract for RPC's collections.** Web search surfaced nothing dated Aug-2026 — only the Feb-2026 Dapper ecosystem PR (already known, re-ranked #1 across 8 mirrors), the May-2026 NFL-pause/next-gen coverage, and the ~$5M VPPA privacy settlement (legal-only, known). No dapper.market feature change surfaced (not live-crawled): still presumed 3 collections (TS/AllDay/Golazos), no Pinnacle/UFC added, no unified cross-league search, no analytics/FMV tab. RPC moat intact.
- **NFL All Day next-gen — STILL not shipped; the imminent watch item.** Story unchanged: May-2026 pause (issuance halted 05-14) + new NFL licensing deal + next-gen reveal promised **"closer to the start of the next [NFL] season"** (2026 regular season kicks off ~Sept 10 — reveal window is now open, nothing shipped yet); existing Moments still trade + "Founding Collector" label + 5% Dapper-balance rebate. Teaser blog *"A New Generation: The Future Of Team NFTs On NFL ALL DAY"* unchanged. Product cadence, NOT a new contract/venue. **Carry (elevated watch):** if the next-gen NFL product ships a new on-chain contract/collection, check the AllDay account (`0xe4cf4bdc1751c65d`) + any NEW Dapper NFL contract account.
- **Disney Pinnacle D23 Anaheim Aug 14–16 2026 is NOW (~1 day out).** No D23 marketplace/product announcement surfaced this run; searches returned only the 2023 launch + Feb-2026 Legendary-drop coverage. **Elevated watch:** a D23-timed Pinnacle drop/feature or a secondary-volume spike Aug 14–16 is the single most likely near-term event — watch `pinnacle_sales` daily next run. No AllDay/Pinnacle IPFS extension announced — public IPFS scope remains TS-only (the 06-12 "Every Moment verifiable" move, already integrated).

**2. IPFS expansion tripwires**
- (a) **No new AllDay/Pinnacle IPFS resolver.** AllDay acct `0xe4cf4bdc1751c65d` → only the `AllDay` contract (standard Series/Sets/Plays/Editions/Badges/Parallels; tier COMMON/UNCOMMON/RARE/LEGENDARY/ULTIMATE, parallels Ruby/Emerald/Sapphire/Opal/Diamond/Obsidian/Topaz; response truncates inside AllDay's own base64 source mid-`addBadgeToEntity`, so a tail resolver can't be 100% ruled out via REST, but no announcement). Pinnacle acct `0xedf9df96c92f4595` → `PackNFT` + `Pinnacle`, **both fully readable this run** — PackNFT `getViews()`→`[]` / `resolveView`→nil and its NFTCollectionDisplay media is the generic Flow wordmark SVG (assets.website-files.com), Pinnacle NFT is render-keyed off-chain (renderID-driven Series/Sets/Shapes/Editions + MetadataViews/Inscriptions/XP, no media/IPFS resolver). **HIGH-value find still absent.**
- (b) **WNBA pin progress FLAT — no refresh action.** `topshot-onchain-art-backfill` `resolver_misses` held **57–66** across 08-12→08-13 (57 on the latest 08-13 12:22Z + the 09:49Z daily art cron; a couple 08-13 overnight runs at 66; 58 on 08-12), essentially flat vs last week's 58. No step-down (no new pinning) and no sustained spike (no material new-mint churn). Daily art cron green (`ok=true`) every run. **Action this run: NONE** — the refresh only helps on a DROP. Residual ~57 = newest still-unpinned mints, self-heals.
- (c) **Reference-app** `https://dapperlabs.github.io/dapperlabs-ipfs-reference-app/` — fetched OK, ~61KB client-rendered Next.js shell: title still **"NBA Top Shot IPFS Reference"**, meta "Browse NBA Top Shot plays with IPFS asset information", topshot-logo.svg, **zero `allday`/`pinnacle` string mentions** (confirmed via grep). **TS-only, no AllDay/Pinnacle reference app.** No change.

**3. On-chain data-anomaly sweep (last 14d, read-only)**
- **Venue-detection — CLEAN, no new venue.** TS onchain sales with non-null proposer collapse to the **single** known Dapper custodial pair proposer `0xead892083b3e2c6c` / payer `0x18eb4ee6b3c026d2` = **26,558/14d** (1,256 distinct buyers) — the known dapper.market / nbatopshot.com rail; no other proposer/payer pair appears in the 14d non-null set. New-proposer query (TS onchain proposer with first-ever sale within 7d) = **`[]`** (empty). **No new/rapidly-growing custodial signer → no new execution venue.**
- **Source mix normal — no new tag.** 14d: TS onchain **27,364** / TS `topshot_gql` 10,032 / TS `offer_fill` 10,021 / **candy_mlb `solana_das` 3,768** (Candy chain-two — own audit tasks, out of scope here, NOT a Flow venue) / AllDay `onchain_dapper_v2` **3,075** / AllDay `onchain_dapper_v1` 707 / Golazos `onchain_dapper_v2` 43. **No new source tag** vs baseline. AllDay `onchain_dapper_v2` continues its organic post-pause secondary decline (8,328 → 4,337 → **3,075**/14d; same tag, same rail — NOT a venue signal). No UFC secondary in `sales` (≈0, Aptos migration).
- **Volume normal.** TS daily 1,497–4,083 (2,175 on 08-13 = window-edge partial; 1,497 on 08-03 = window-edge partial). AllDay 103–359/day (steady organic, post-pause). Golazos near-dead (1–9/day). Pinnacle (separate `pinnacle_sales`) recent 43–357/day (today 287) with the tail of the 07-31 700 bump receding — **no D23 spike yet (D23 starts 08-14); watch next run.** No venue signal.

**4. Flowscan glance:** skipped (slow/client-rendered; web + DB sufficient).

**Baselines carried to next run:** WNBA `resolver_misses` = **57** (flat, band 56–66; a DROP = new pins → refresh, a RISE = new-mint churn → don't) · TS custodial pair `0xead…`/`0x18eb…` = ONLY non-null-payer rail (26,558/14d), **0 new proposers/7d** · AllDay `onchain_dapper_v2` **3,075/14d** (declining organic, post-pause) · candy_mlb `solana_das` **3,768/14d** (Candy, chain-two, out of scope) · Golazos near-dead (43/14d) · Pinnacle secondary 43–357/day recent + 07-31 700 tail · **⚠ D23 Anaheim Aug 14–16 is NOW — watch `pinnacle_sales` for a D23-timed spike next run** · **NFL next-gen reveal window OPEN (~Sept kickoff) — watch AllDay acct + any new Dapper NFL contract** · no AllDay/Pinnacle IPFS resolver (Pinnacle PackNFT resolveView→nil + Pinnacle NFT render-keyed confirmed on-chain) · reference-app TS-only (zero AllDay/Pinnacle mentions) · dapper.market presumed still 3 collections / no analytics tab (not live-crawled). Open methodology note (unchanged): REST `?expand=contracts` truncates on the AllDay source — a tail AllDay resolver needs a names-only endpoint or live Flowscan to fully rule out.

Sources: Dapper Feb-2026 ecosystem PR — globenewswire.com/news-release/2026/02/07/3234119/0/en/Dapper-Labs-Ecosystem-Disney-Pinnacle-NFL-ALL-DAY-and-NBA-Top-Shot-Drive-Consumer-Engagement-on-Flow.html ; NFL All Day next-gen / pause / new licensing — decrypt.co/367926/nfl-all-day-stops-issuing-nfts-dapper-labs-future-plans-league , blog.nflallday.com/posts/a-new-generation-the-future-of-team-nfts-on-nfl-all-day ; Dapper $5M VPPA settlement — openclassactions.com/settlements/dapper-labs-vppa-privacy-settlement.php ; NBA Top Shot IPFS reference — dapperlabs.github.io/dapperlabs-ipfs-reference-app/ ; dapper.market — dapper.market/

---

## 2026-08-05 — no new material ecosystem developments. No recurring action (WNBA `resolver_misses` DEAD FLAT at 58). Venue detection clean; no new marketplace / contract / execution venue. NFL next-gen reveal STILL unshipped (season kickoff ~Sept, still ahead) — carry.

Verdict: nothing clears the new-venue / competitive-threat escalation bar → **no Trevor notification**. No IPFS-refresh action: `resolver_misses` held pinned at **58** every run 08-04→08-05 (inside the 56–63 band) — no pinning event (no DROP) and no new-mint churn (no RISE), so **do NOT run `scripts/refresh-ipfs-catalog.mjs`** (it only helps on a DROP). Everything else at baseline; venue detection clean.

**1. Ecosystem / web sweep**
- **No new Flow marketplace / venue / contract for RPC's collections.** Web search surfaced nothing dated Aug-2026 — only the Feb-2026 Dapper ecosystem PR (already known), the WNBA-interest rookie-season story (Oct-2025, known), the ~$4M TS securities settlement (legal-only, known) and the May-2026 NFL-pause coverage. No dapper.market feature change surfaced (not live-crawled): still presumed 3 collections (TS/AllDay/Golazos), no Pinnacle/UFC added, no unified cross-league search, no analytics/FMV tab. RPC moat intact.
- **NFL All Day next-gen — STILL not shipped; the imminent watch item.** The whole story remains the May-2026 pause (issuance halted 05-14) + new NFL licensing deal + next-gen reveal promised **"closer to the start of the next [NFL] season"** (2026 regular season kicks off ~Sept 10, so the reveal window is now, but nothing has shipped). The teaser blog *"A New Generation: The Future Of Team NFTs On NFL ALL DAY"* (blog.nflallday.com) is unchanged from last run — product cadence, NOT a new contract/venue. **Carry (elevated watch):** if the next-gen NFL product ships a new on-chain contract/collection over the next few weeks, that's a new index target — check the AllDay account (`0xe4cf4bdc1751c65d`) and for any NEW Dapper NFL contract account next run.
- **Disney Pinnacle D23 Anaheim Aug 14–16 2026 still ahead (~9 days out).** No D23 marketplace/product announcement surfaced; searches returned only the 2023 launch coverage. Carry: watch for a D23-timed Pinnacle drop/feature. No AllDay/Pinnacle IPFS extension announced — public IPFS scope remains TS-only (the 06-12 "Every Moment verifiable" move, already integrated).

**2. IPFS expansion tripwires**
- (a) **No new AllDay/Pinnacle IPFS resolver.** AllDay acct `0xe4cf4bdc1751c65d` → only the `AllDay` contract (standard Series/Sets/Plays/Editions/Badges/Parallels; tier COMMON/UNCOMMON/RARE/LEGENDARY/ULTIMATE, parallels Ruby/Emerald/Sapphire/Opal/Diamond/Obsidian/Topaz; response truncates inside AllDay's own base64 source mid-`addBadgeToEntity`, so a tail resolver can't be 100% ruled out via REST, but no announcement). Pinnacle acct `0xedf9df96c92f4595` → `PackNFT` + `Pinnacle`, **both fully readable this run** — PackNFT `getViews()`→`[]` / `resolveView`→nil and its NFTCollectionDisplay media is the generic Flow wordmark SVG (assets.website-files.com), Pinnacle NFT is render-keyed off-chain (renderID-driven Series/Sets/Shapes/Editions + MetadataViews/Inscriptions/XP, no media/IPFS resolver). **HIGH-value find still absent.**
- (b) **WNBA pin progress DEAD FLAT — no refresh action.** `topshot-onchain-art-backfill` `resolver_misses` held **58** on every run 08-04 09:49Z → 08-05 15:22Z (incl. the 08-05 09:49Z daily art cron at 58), inside the 56–63 baseline band. No step-down (no new pinning) and no spike (no new-mint churn). Daily art cron green (`ok=true`) every run. **Action this run: NONE** — the refresh only helps on a DROP. Residual ~58 = newest still-unpinned mints, self-heals.
- (c) **Reference-app** `https://dapperlabs.github.io/dapperlabs-ipfs-reference-app/` — fetched OK, ~61KB client-rendered Next.js shell: title still **"Top Shot IPFS Reference"**, meta "Browse NBA Top Shot plays with IPFS asset information", **zero `allday`/`pinnacle` string mentions in the shell** (confirmed via grep). **TS-only, no AllDay/Pinnacle reference app.** No change.

**3. On-chain data-anomaly sweep (last 14d, read-only)**
- **Venue-detection — CLEAN, no new venue.** TS onchain sales with non-null proposer collapse to the **single** known Dapper custodial pair proposer `0xead892083b3e2c6c` / payer `0x18eb4ee6b3c026d2` = **23,097/14d** (1,182 distinct buyers) — the known dapper.market / nbatopshot.com rail; no other proposer/payer pair appears at all in the 14d non-null set. New-proposer query (TS onchain proposer with first-ever sale within 7d) = **`[]`** (empty). **No new/rapidly-growing custodial signer → no new execution venue.**
- **Source mix normal — no new tag.** 14d: TS onchain **23,667** / TS `offer_fill` 7,996 / TS `topshot_gql` 4,399 / AllDay `onchain_dapper_v2` **4,337** / **candy_mlb `solana_das` 4,136** (Candy chain-two — own audit tasks, out of scope here, NOT a Flow venue) / AllDay `onchain_dapper_v1` 653 / Golazos `onchain_dapper_v2` 82 / AllDay `allday_studio_history_v1` 77. **No new source tag** vs baseline. **Two movements, both benign/expected:** (i) AllDay `onchain_dapper_v2` **down 8,328→4,337/14d** = organic post-pause secondary decline (no minting since 05-14; NOT a venue signal — same tag, same rail). (ii) candy_mlb `solana_das` **up 602→4,136/14d** = the expected Candy go-live ramp (public 07-31); chain-two, out of scope. No UFC secondary in `sales` (≈0, Aptos migration).
- **Volume normal.** TS daily 2,183–3,648 (1,469 on 08-05 = window-edge partial). AllDay 131–587/day (settling from the 07-31 443 / 07-29 587 tail). Golazos near-dead (1–24/day). Pinnacle (separate `pinnacle_sales`) 122–700/day — a mild 07-30/31 bump (639/700) settling to 254–357, captured by the pipeline (likely a mini-event/promo tail; watch for a D23-timed 08-14→16 spike), no venue signal.

**4. Flowscan glance:** skipped (slow/client-rendered; web + DB sufficient).

**Baselines carried to next run:** WNBA `resolver_misses` = **58** (flat, band 56–63; a DROP = new pins → refresh, a RISE = new-mint churn → don't) · TS custodial pair `0xead…`/`0x18eb…` = ONLY non-null-payer rail (23,097/14d), **0 new proposers/7d** · AllDay `onchain_dapper_v2` **4,337/14d** (declining organic, post-pause) · candy_mlb `solana_das` **4,136/14d** (Candy go-live ramp, chain-two, out of scope) · Golazos near-dead (82/14d) · Pinnacle secondary 122–700/day (07-30/31 bump; **watch for a D23-timed spike Aug 14–16**) · no AllDay/Pinnacle IPFS resolver (Pinnacle PackNFT resolveView→nil + Pinnacle NFT render-keyed confirmed on-chain) · reference-app TS-only (zero AllDay/Pinnacle mentions) · dapper.market presumed still 3 collections / no analytics tab (not live-crawled) · **NFL next-gen reveal window OPEN (~season kickoff Sept) — watch AllDay acct + any new Dapper NFL contract for a new on-chain collection.** Open methodology note (unchanged): REST `?expand=contracts` truncates on the AllDay source — a tail AllDay resolver needs a names-only endpoint or live Flowscan to fully rule out.

Sources: Dapper Feb-2026 ecosystem PR — globenewswire.com/news-release/2026/02/07/3234119/0/en/Dapper-Labs-Ecosystem-Disney-Pinnacle-NFL-ALL-DAY-and-NBA-Top-Shot-Drive-Consumer-Engagement-on-Flow.html ; NFL All Day next-gen / pause / new licensing — decrypt.co/367926/nfl-all-day-stops-issuing-nfts-dapper-labs-future-plans-league , blog.nflallday.com/posts/a-new-generation-the-future-of-team-nfts-on-nfl-all-day , cryptonews.com.au/news/dapper-labs-halts-new-nfl-all-day-nft-drops-amid-licensing-reset-133856/ ; TS $4M settlement — finance.yahoo.com/news/dapper-labs-settles-nba-top-090101761.html ; NBA Top Shot IPFS reference — dapperlabs.github.io/dapperlabs-ipfs-reference-app/ ; dapper.market — dapper.market/

---

## 2026-07-29 — no new material ecosystem developments. No recurring action this week (WNBA `resolver_misses` FLAT at 56–63, no new pins to absorb). Venue detection clean; no new marketplace / contract / execution venue.

Verdict: nothing clears the new-venue / competitive-threat escalation bar → **no Trevor notification**. Unlike last week there is no IPFS-refresh action: `resolver_misses` held flat at ~56–63 (vs 55–63 last week) — last week's refresh already absorbed the ~165–175 pins, and no further pinning event or new-mint churn moved the number, so **do NOT run `scripts/refresh-ipfs-catalog.mjs` this run** (it only helps on a DROP). Everything else at baseline; venue detection clean.

**1. Ecosystem / web sweep**
- **No new Flow marketplace / venue / contract for RPC's collections.** Web search surfaced nothing dated July 2026 — only the Feb-2026 Dapper ecosystem PR (already known, re-ranked #1 across 8 mirrors) and the May-2026 NFL-pause coverage. No dapper.market feature change surfaced (not live-crawled): still presumed 3 collections (TS/AllDay/Golazos), no Pinnacle/UFC added, no unified cross-league search, no analytics/FMV tab. RPC moat intact.
- **NFL All Day next-gen — still the May-2026 story, not shipped.** Pause (issuance halted 05-14) + new NFL licensing deal; reveal promised **"closer to the start of the next season"** (≈Aug/Sept, now imminent) with existing Moments trading uninterrupted. A teaser blog **"A New Generation: The Future Of Team NFTs On NFL ALL DAY"** (blog.nflallday.com) surfaced this run — product cadence, NOT a new contract/venue; **carry as watch:** if the next-gen NFL product ships a new on-chain contract/collection, that's a new index target. Nothing on-chain yet.
- **Carry (unchanged):** Disney Pinnacle D23 Anaheim Aug 14–16 2026 still ahead (Disney+ promo ended 07-23). No AllDay/Pinnacle IPFS extension announced — public IPFS scope remains TS-only (the 06-12 "Every Moment verifiable" move, already integrated).

**2. IPFS expansion tripwires**
- (a) **No new AllDay/Pinnacle IPFS resolver.** AllDay acct `0xe4cf4bdc1751c65d` → only the `AllDay` contract (standard Series/Sets/Plays/Editions/Badges/Parallels; tier COMMON/UNCOMMON/RARE/LEGENDARY/ULTIMATE, parallels Ruby/Emerald/Sapphire/Opal/Diamond/Obsidian/Topaz; response truncates inside AllDay's own base64 source mid-`addBadgeToEntity`, so a tail resolver can't be 100% ruled out via REST, but no announcement). Pinnacle acct `0xedf9df96c92f4595` → `PackNFT` + `Pinnacle`, **both fully readable this run** — PackNFT `getViews()`→`[]` / `resolveView`→nil and its NFTCollectionDisplay media is the generic Flow wordmark SVG (assets.website-files.com), Pinnacle NFT is render-keyed off-chain (renderID-driven Series/Sets/Shapes/Editions + MetadataViews/Inscriptions/XP, no media/IPFS resolver). **HIGH-value find still absent.**
- (b) **WNBA pin progress FLAT — no refresh action.** `topshot-onchain-art-backfill` `resolver_misses` held **56–63** across 07-28→07-29 (56 on the 07-29 09:49Z daily art cron; 63 on the overnight runs), essentially unchanged from last week's 55–63. No step-down (no new pinning) and no spike (no new-mint churn). Daily art cron green (`ok=true`) every run. **Action this run: NONE** — the refresh only helps on a DROP; last week's run already absorbed the ~165–175 pins. Residual ~56–63 = newest still-unpinned mints, self-heals.
- (c) **Reference-app** `https://dapperlabs.github.io/dapperlabs-ipfs-reference-app/` — fetched OK and **rendered cleanly this run** (61KB shell): title still **"NBA Top Shot IPFS Reference"**, meta-description "Browse NBA Top Shot plays with IPFS asset information", topshot-logo.svg, Series 1–8 + parallels (Astra/Bit/Blockchain/Bubbled/Club Collection/…). **TS-only, no AllDay/Pinnacle reference app.** No change.

**3. On-chain data-anomaly sweep (last 14d, read-only)**
- **Venue-detection — CLEAN, no new venue.** TS onchain sales with non-null proposer collapse to the **single** known Dapper custodial pair proposer `0xead892083b3e2c6c` / payer `0x18eb4ee6b3c026d2` = **21,452/14d** (1,152 distinct buyers) — the known dapper.market / nbatopshot.com rail; no other proposer/payer pair appears at all in the 14d non-null set. New-proposer query (TS onchain proposer with first-ever sale within 7d) = **`[]`** (empty) — even cleaner than last week's 2. **No new/rapidly-growing custodial signer → no new execution venue.**
- **Source mix normal — no new tag.** 14d: TS onchain **23,005** / AllDay `onchain_dapper_v2` **8,328** / TS `offer_fill` 8,125 / TS `topshot_gql` 2,537 / AllDay `onchain_dapper_v1` 716 / **magic_eden `solana_das` 602** (Candy chain-two ingest — own audit tasks, out of scope here, NOT a Flow venue) / AllDay `allday_studio_history_v1` 279 / Golazos `onchain_dapper_v2` 91 / TS `ts_history_backfill_v1` 7 (tailing off). **No new source tag** vs baseline. AllDay `onchain_dapper_v2` steady (8,165 last week → 8,328/14d, organic Dapper-V2-storefront). No UFC secondary in `sales` (≈0, Aptos migration).
- **Volume normal.** TS daily 1,732–2,776 (128 on 07-19 = window-edge partial). AllDay 211–583/day. Golazos near-dead (2–30/day). Pinnacle (separate `pinnacle_sales`) 116–441/day — steady with a mild 07-24→27 bump (405/441/318/343, likely a mini-event/promo tail), captured by the pipeline, no venue signal.

**4. Flowscan glance:** skipped (slow/client-rendered; web + DB sufficient).

**Baselines carried to next run:** WNBA `resolver_misses` = **56–63** (flat; a DROP = new pins → refresh, a RISE = new-mint churn → don't) · TS custodial pair `0xead…`/`0x18eb…` = ONLY non-null-payer rail (21,452/14d), **0 new proposers/7d** · AllDay `onchain_dapper_v2` **8,328/14d** (steady organic) · Golazos near-dead (91/14d) · Pinnacle secondary steady 116–441/day · candy_mlb = `magic_eden`/`solana_das` in `sales` (chain-two, out of scope) · no AllDay/Pinnacle IPFS resolver (Pinnacle PackNFT resolveView→nil + Pinnacle NFT render-keyed confirmed on-chain) · reference-app TS-only (rendered cleanly this run) · dapper.market presumed still 3 collections / no analytics tab (not live-crawled) · **NFL next-gen reveal imminent (~season kickoff) — watch for a new on-chain contract.** Open methodology note (unchanged): REST `?expand=contracts` truncates on the AllDay source — a tail AllDay resolver needs a names-only endpoint or live Flowscan to fully rule out.

Sources: Dapper Feb-2026 ecosystem PR — globenewswire.com/news-release/2026/02/07/3234119/0/en/Dapper-Labs-Ecosystem-Disney-Pinnacle-NFL-ALL-DAY-and-NBA-Top-Shot-Drive-Consumer-Engagement-on-Flow.html ; NFL All Day next-gen / pause / new licensing — decrypt.co/367926/nfl-all-day-stops-issuing-nfts-dapper-labs-future-plans-league , blog.nflallday.com/posts/a-new-generation-the-future-of-team-nfts-on-nfl-all-day ; NBA Top Shot IPFS reference — dapperlabs.github.io/dapperlabs-ipfs-reference-app/ ; dapper.market — dapper.market/

---

## 2026-07-24 — no new material ecosystem developments. Recurring action returns: IPFS catalog refresh (WNBA `resolver_misses` fell 219–237 → 55–63 = ~165–175 newly-pinned editions). No new venue / marketplace / contract.

Verdict: nothing clears the new-venue / competitive-threat escalation bar → **no Trevor notification**. The one recurring positive with a concrete action is back: the ~1,900-mint unpinned backlog that pushed the WNBA tripwire UP to 219–237 last week has now been largely pinned by Dapper — `resolver_misses` fell back to ~55–63 → **run `scripts/refresh-ipfs-catalog.mjs`** so catalog/badges/pin-exports absorb the newly-pinned media. Everything else at baseline; venue detection clean.

**1. Ecosystem / web sweep**
- **No new Flow marketplace / venue / contract for RPC's collections.** Web search surfaced nothing dated July 2026 — only the Feb-2026 Dapper ecosystem PR (already known) and historical pages (Genies marketplace, BloctoBay, OpenSea OS2 — all pre-existing). No dapper.market feature change surfaced (not live-crawled): still presumed 3 collections (TS/AllDay/Golazos), no Pinnacle/UFC added, no unified cross-league search, no analytics/FMV tab. RPC moat intact.
- **Carry (unchanged, product cadence):** NFL All Day next-gen still paused (issuance halted 05-14); Dapper reconfirmed the new NFL licensing deal + next-gen reveal "closer to the start of next season" (≈Aug/Sept) + "Founding Collector" badge & 5% Dapper-balance rebate (released after a 1-yr hold) — no reveal shipped yet. Disney Pinnacle: Disney+ promo ended 2026-07-23; D23 Anaheim Aug 14–16 2026 still ahead. No new on-chain/product surface in any of these.
- **No AllDay/Pinnacle IPFS extension announced.** Public IPFS scope remains TS-only (the 06-12 "Every Moment verifiable" move, already integrated).

**2. IPFS expansion tripwires**
- (a) **No new AllDay/Pinnacle IPFS resolver.** AllDay acct `0xe4cf4bdc1751c65d` → only the `AllDay` contract (standard Series/Sets/Plays/Editions/Badges/Parallels; tier COMMON/UNCOMMON/RARE/LEGENDARY/ULTIMATE, parallels Ruby/Emerald/Sapphire/Opal/Diamond/Obsidian/Topaz; response truncates inside AllDay's own base64 source, so a tail resolver can't be 100% ruled out via REST, but no announcement). Pinnacle acct `0xedf9df96c92f4595` → `PackNFT` + `Pinnacle`, both readable this run — PackNFT `getViews()`→`[]` / `resolveView`→nil and its NFTCollectionDisplay media is a generic Flow wordmark SVG (assets.website-files.com), Pinnacle NFT is render-keyed off-chain (renderID-driven Sets/Shapes/Editions + MetadataViews/Inscriptions/XP, no media/IPFS resolver). **HIGH-value find still absent.**
- (b) **WNBA pin progress = the actionable item (refresh trigger fired).** `topshot-onchain-art-backfill` `resolver_misses` fell from last week's **219–237** to **55–63** (07-23 18:22Z=55 → 60–63 across every 07-24 run incl. the 09:49Z daily art cron at 60). ~165–175 of the ~1,900 newly-minted-but-unpinned editions from the 07-04→07-16 mint wave have now been pinned on-chain by Dapper; daily art cron green (`ok=true`) every run. **Action this run: run `scripts/refresh-ipfs-catalog.mjs`** (IPFS_LOADER_TOKEN from the `ipfs-catalog-loader` edge fn) so `topshot_ipfs_assets` + verified-media badges + pin-exports absorb the newly-pinned media. Residual ~60 = newest still-unpinned mints, self-heals as Dapper keeps catching up.
- (c) **Reference-app** `https://dapperlabs.github.io/dapperlabs-ipfs-reference-app/` — fetched OK, still titled **"NBA Top Shot IPFS Reference"**, TS-only; web_fetch renders the ~61KB client-side Next.js shell as text so the build-chunk hash isn't cleanly diffable (same limitation as prior runs). No AllDay/Pinnacle reference app. Not over-invested per task guidance.

**3. On-chain data-anomaly sweep (last 14d, read-only)**
- **Venue-detection — CLEAN, no new venue.** Dominant custodial pair proposer `0xead892083b3e2c6c` / payer `0x18eb4ee6b3c026d2` = **30,452/14d** (1,253 distinct buyers) — the known dapper.market / nbatopshot.com rail. Every one of the ~19 tail TS proposers (5–16 sales each) routes through the **same** Dapper payer `0x18eb4ee6b3c026d2` → individual custodial sub-wallets, not a new rail. New-proposer query (first-ever sale within 7d) = **2** proposers only (`0xd4588326a815f0b2` 5 sales / `0x3f38fe8443290991` 1 sale), each `distinct_payers=1` = individual self-custody buyers. **No new/rapidly-growing custodial payer account → no new execution venue.**
- **Source mix normal — no new tag.** 14d: TS onchain 29,789 / offer_fill 8,965 / topshot_gql 3,015 / ts_history_backfill_v1 285 (tailing off, last row 07-17; all internal/known); AllDay `onchain_dapper_v2` **8,165** / `onchain_dapper_v1` 818 / allday_studio_history_v1 195; Golazos `onchain_dapper_v2` 84. **No new source tag** vs baseline. AllDay `onchain_dapper_v2` steady (7,415 last week → 8,165/14d, organic Dapper-V2-storefront). candy_mlb now appears in `sales` (`solana_das` 147/14d) = expected chain-two ingest (own audit tasks, out of scope here), NOT a Flow venue. No UFC secondary in `sales` (≈0, as expected — Aptos migration).
- **Volume normal.** TS daily 1,732–3,130 (194 partial 07-25). AllDay 207–1,821/day (settling from the 07-16/17 highs). Golazos near-dead (3–30/day). Pinnacle (separate `pinnacle_sales`) 77–205/day steady with one 405 day (07-24) — captured by the pipeline (likely a Disney+-promo-tail / mini-drop bump, not blind); no venue signal.

**4. Flowscan glance:** skipped (slow/client-rendered; web + DB sufficient).

**Baselines carried to next run:** WNBA `resolver_misses` = **55–63** (fell from 219–237 = ~165–175 newly pinned; a further DROP = more pins → refresh again; a RISE = new-mint churn, don't) · TS custodial pair `0xead…`/`0x18eb…` = only high-volume signer (30,452/14d); all tail proposers share the `0x18eb…` payer; new proposers = individual self-custody buyers (≤5 sales, 1 payer each), **no new venue** · AllDay `onchain_dapper_v2` **8,165/14d** (steady organic) · Golazos near-dead (84/14d) · Pinnacle secondary steady 77–205/day (one 405 day 07-24) · candy_mlb now in `sales` (`solana_das`, chain-two, out of scope) · no AllDay/Pinnacle IPFS resolver (Pinnacle PackNFT resolveView→nil + Pinnacle NFT render-keyed confirmed on-chain) · reference-app TS-only (chunk-hash not diffable) · dapper.market presumed still 3 collections / no analytics tab (not live-crawled). Open methodology note (unchanged): REST `?expand=contracts` truncates on the AllDay source — a tail AllDay resolver needs a names-only endpoint or live Flowscan to fully rule out.

Sources: Dapper Feb-2026 ecosystem PR — globenewswire.com/news-release/2026/02/07/3234119/0/en/Dapper-Labs-Ecosystem-Disney-Pinnacle-NFL-ALL-DAY-and-NBA-Top-Shot-Drive-Consumer-Engagement-on-Flow.html ; NFL All Day halt / new licensing / next-gen — finance.yahoo.com/markets/crypto/articles/nfl-day-stops-issuing-nfts-210400296.html , kucoin.com/news/flash/dapper-labs-halts-new-nfl-all-day-nft-issuance-signs-new-licensing-agreement-with-nfl ; dapper.market — dapper.market/

---

## 2026-07-16 — no new material ecosystem developments. No recurring action this week: WNBA `resolver_misses` jumped UP (~0–1 → 219–237) = new-mint churn, NOT a pinning event → do NOT run the catalog refresh yet.

Verdict: nothing clears the new-venue / competitive-threat escalation bar → **no Trevor notification**. Unlike prior weeks there is no "Dapper pinned more media → refresh" action: `resolver_misses` rose (opposite direction) because ~1,900 new TS editions were created in the last ~12 days (subedition-cataloging + new drops), whose media isn't pinned to IPFS yet — the refresh script only helps when misses DROP (new pins to absorb), so skip it this run. Everything else at baseline; venue detection clean.

**1. Ecosystem / web sweep**
- **No new Flow marketplace / venue / contract for RPC's collections.** Web search surfaced only the Feb-2026 Dapper ecosystem PR (Pinnacle Legendary drop, Super Bowl 60 AllDay, TS Steph Curry $17.5k) and historical UFC-Strike / Pinnacle launch pages — nothing dated July 2026. No dapper.market feature change surfaced (not live-crawled this run): still presumed 3 collections (TS/AllDay/Golazos), no Pinnacle/UFC added, no unified cross-league search, no analytics/FMV tab. RPC moat intact.
- **Carry (unchanged, product cadence):** NFL All Day next-gen still paused (since 05-14) pending the new NFL licensing deal / next-gen reveal (~Aug/Sept + "Founding Collector" rebate); Disney Pinnacle Disney+ offer promo runs through 2026-07-23 + confirmed for D23 Anaheim Aug 14–16 2026; Dapper ~$5M VPPA privacy settlement (legal-only). No new on-chain/product surface in any of these yet.
- **No AllDay/Pinnacle IPFS extension announced.** Public IPFS scope remains TS-only (the 06-12 "Every Moment verifiable" move, already integrated).

**2. IPFS expansion tripwires**
- (a) **No new AllDay/Pinnacle IPFS resolver.** AllDay acct `0xe4cf4bdc1751c65d` → only the `AllDay` contract (standard Series/Sets/Plays/Editions/Badges/Parallels; tier enum COMMON/UNCOMMON/RARE/LEGENDARY/ULTIMATE, parallels Ruby/Emerald/Sapphire/Opal/Diamond/Obsidian/Topaz; response truncates inside AllDay's own base64 source, so a tail resolver can't be 100% ruled out via REST, but no announcement). Pinnacle acct `0xedf9df96c92f4595` → `PackNFT` + `Pinnacle`, **both fully readable this run** — PackNFT `getViews()`→`[]` / `resolveView`→nil and its NFTCollectionDisplay media is a generic Flow wordmark SVG (assets.website-files.com), Pinnacle NFT is render-keyed off-chain (renderID-driven Sets/Shapes/Editions, no media/IPFS resolver). **No media/IPFS resolver in either → HIGH-value find still absent.**
- (b) **WNBA pin progress REVERSED to new-mint churn.** `topshot-onchain-art-backfill` `resolver_misses`: last week ~0–1 → this week **219 (07-15) → 237 (07-16)** on the 3-hourly runs (the 09:49Z daily art cron run shows 80, a narrower scope). Cause confirmed on-chain: new TS editions created/day = 506 (07-05), 758 (07-07), 131 (07-09), 30–85 daily since (~1,900 in 12d) — subedition-cataloging + new drops, media not yet IPFS-pinned. Daily art cron green (`ok=true`) every run. **Action this run: NONE** (misses rose, not dropped → nothing newly pinned to absorb; the refresh script is for the drop case). Self-heals as Dapper pins the new mints; watch for a drop back toward baseline next week (that would be the refresh trigger again).
- (c) **Reference-app** `https://dapperlabs.github.io/dapperlabs-ipfs-reference-app/` — fetched OK but returns a large (~61KB) client-rendered Next.js shell; the build-chunk hash wasn't cleanly extractable this run, so no diff signature. TS-only historically, no signal of an AllDay/Pinnacle reference app. Not over-invested per task guidance.

**3. On-chain data-anomaly sweep (last 14d, read-only)**
- **Venue-detection — CLEAN, no new venue.** Dominant custodial pair proposer `0xead892083b3e2c6c` / payer `0x18eb4ee6b3c026d2` = **35,479/14d** (1,325 distinct buyers) — the known dapper.market / nbatopshot.com rail. Every other TS proposer (14 tail signers, 9–58 sales each) routes through the **same** Dapper payer `0x18eb4ee6b3c026d2` → individual custodial sub-wallets, not a new rail. New-proposer query (first-ever sale within 7d) = **4** proposers, each 1–5 sales with `distinct_payers=1` (self-custody buyers). **No new/rapidly-growing custodial payer account → no new execution venue.**
- **Source mix normal — no new tag.** 14d: TS onchain 33,691 / offer_fill 9,926 / topshot_gql 3,120 / ts_history_backfill_v1 1,620 (all internal/known); AllDay `onchain_dapper_v2` **7,415** / `onchain_dapper_v1` 847 / allday_studio_history_v1 108; Golazos `onchain_dapper_v2` 10. **No new source tag** vs baseline. AllDay `onchain_dapper_v2` ~stable (8,087 last week → 7,415/14d) — the earlier "confirm organic, not double-count" watch resolves: it's steady organic Dapper-V2-storefront volume, not runaway. No UFC secondary in `sales` (≈0, as expected — Aptos migration).
- **Volume normal.** TS daily 2,839–6,073 (823 partial 07-16). AllDay hundreds/day; Golazos near-dead. Pinnacle (separate `pinnacle_sales`) 43–234/day over the window — steady baseline, no drop-wave spike. Captured, not blind.

**4. Flowscan glance:** skipped (slow/client-rendered; web + DB sufficient).

**Baselines carried to next run:** WNBA `resolver_misses` = **219–237** (rose from ~0–1 = ~1,900 new unpinned TS mints in 12d; a DROP next week = new pins → run `scripts/refresh-ipfs-catalog.mjs`) · TS custodial pair `0xead…`/`0x18eb…` = only high-volume signer; all tail proposers share the `0x18eb…` payer; new proposers = individual self-custody buyers (≤5 sales, 1 payer each), **no new venue** · AllDay `onchain_dapper_v2` **7,415/14d** (steady organic, internal Dapper-V2-storefront tag) · Pinnacle secondary steady 43–234/day · no AllDay/Pinnacle IPFS resolver (Pinnacle PackNFT resolveView→nil + Pinnacle NFT render-keyed confirmed on-chain) · reference-app TS-only (chunk-hash not diffable this run) · dapper.market presumed still 3 collections / no analytics tab (not live-crawled). Open methodology note (unchanged): REST `?expand=contracts` truncates on the AllDay source — a tail AllDay resolver needs a names-only endpoint or live Flowscan to fully rule out.

Sources: Dapper Feb-2026 ecosystem PR — globenewswire.com/news-release/2026/02/07/3234119/0/en/Dapper-Labs-Ecosystem-Disney-Pinnacle-NFL-ALL-DAY-and-NBA-Top-Shot-Drive-Consumer-Engagement-on-Flow.html ; dapper.market — dapper.market/ ; UFC Strike (historical) — dapperlabs.com/newsroom/dapper-labs-and-ufc-launch-ufc-strike-an-all-new-nft-experience-for-mma-fans-everywhere

---

## 2026-07-09 — no new material ecosystem developments. Recurring action: IPFS catalog refresh (WNBA `resolver_misses` 7–8 → 0–1, near-fully pinned). Watch: AllDay `onchain_dapper_v2` volume up ~2.3x (internal tag, not a new venue).

Verdict: nothing clears the new-venue / competitive-threat escalation bar → **no Trevor notification**. Same one recurring positive as prior weeks — Dapper pinned essentially all remaining WNBA media on-chain (WNBA tripwire fell to ~0–1) → run `scripts/refresh-ipfs-catalog.mjs`. One volume note logged for the baseline (NOT a new venue): AllDay secondary through the already-scanned Dapper V2 storefront (`onchain_dapper_v2`) roughly doubled since 07-01.

**1. Ecosystem / web sweep**
- **No new Flow marketplace / venue / contract for RPC's collections.** No dapper.market feature change surfaced via web (not live-crawled this run): still presumed 3 collections (TS/AllDay/Golazos), no Pinnacle/UFC added, no unified cross-league search, no analytics/FMV tab. RPC moat intact.
- **Carry (unchanged, product cadence):** NFL All Day next-gen still paused (since 05-14) pending the new NFL licensing deal / next-gen reveal (~Aug/Sept + "Founding Collector" rebate); Disney Pinnacle **Disney+ offer promo runs 2026-06-26 → 2026-07-23** (marketing bundle, not a contract/venue change) + confirmed for D23 Anaheim Aug 14–16 2026; Dapper ~$5M VPPA privacy settlement (legal-only, up to $5 for TS/AllDay/Pinnacle/UFC/Golazos users). No new on-chain/product surface in any of these yet.
- **No AllDay/Pinnacle IPFS extension announced.** Public IPFS scope remains TS-only (the 06-12 "Every Moment verifiable" move, already integrated).

**2. IPFS expansion tripwires**
- (a) **No new AllDay/Pinnacle IPFS resolver.** AllDay acct `0xe4cf4bdc1751c65d` → only the `AllDay` contract (standard Series/Sets/Plays/Editions/Badges/Parallels; response truncates inside AllDay's own base64 source, so a tail resolver can't be 100% ruled out via REST, but there's no announcement). Pinnacle acct `0xedf9df96c92f4595` → `PackNFT` + `Pinnacle`, **both fully readable this run** — PackNFT exposes only NFTCollectionData/NFTCollectionDisplay views (the Display media is a generic Flow wordmark SVG, not per-edition art) and Pinnacle's own NFT `getViews()`→`[]` / `resolveView`→nil, i.e. **no media/IPFS resolver in either** (Pinnacle art stays render-keyed off-chain, the known per-edition image dead-end). HIGH-value find still absent.
- (b) **WNBA pin progress = the actionable item.** `topshot-onchain-art-backfill` `resolver_misses`: 8 (07-02) → 8 (07-03) → 9 (07-04) → 7 (07-05) → **0 (07-06)** → 1 (07-08) → **1 (07-09)**. Dropped from the 7–8 baseline to ~0–1 = Dapper pinned essentially all remaining WNBA (and stray) media on-chain; the daily art cron is green (`ok=true`) every run. **Action: run `scripts/refresh-ipfs-catalog.mjs`** (IPFS_LOADER_TOKEN from the `ipfs-catalog-loader` edge fn) so catalog/badges/pin-exports absorb the newly-pinned media. (The 1 residual is likely a brand-new unpinned mint, self-heals.)
- (c) **Reference-app** `https://dapperlabs.github.io/dapperlabs-ipfs-reference-app/` — fetched OK but returns a large (~61KB) client-rendered Next.js shell; the build-chunk hash wasn't extractable this run (saved copy already GC'd), so no diff signature. TS-only historically ("NBA Top Shot IPFS Reference"), no signal of an AllDay/Pinnacle reference app. Not over-invested per task guidance.

**3. On-chain data-anomaly sweep (last 14d, read-only)**
- **Venue-detection — CLEAN, no new venue.** The new-proposer query returned **8** TS proposers with a first-ever sale in the last 7d (vs `[]` on 07-01), but each is a **tiny individual self-custody buyer** — 1–5 sales apiece, `distinct_payers = 1` (self-paying), max 5 sales. A new marketplace shows up as a *sustained, growing custodial signer with many distinct payers routed through it*; these are the opposite. The dominant custodial pair proposer `0xead892083b3e2c6c` / payer `0x18eb4ee6b3c026d2` = **30,963/14d** (the known dapper.market / nbatopshot.com rail) is the only high-volume signer. No new/rapidly-growing custodial account → no new execution venue.
- **Source mix normal — no new tag.** 14d: TS onchain 31,367 / offer_fill 9,554 / topshot_gql 3,000 / ts_history_backfill_v1 1,184 (all internal/known); AllDay `onchain_dapper_v2` **8,087** / `onchain_dapper_v1` 690 / allday_studio_history_v1 73; Golazos `onchain_dapper_v2` 83. **No new source tag** vs the 06-24 baseline. **Watch (not escalated):** AllDay `onchain_dapper_v2` ~doubled (3,587 on 07-01 → 8,087/14d) — this is RPC's own indexer taxonomy for the already-scanned **Dapper V2 storefront** leg (not an external venue); the uptick reflects AllDay secondary activity + the V2 indexer catching up, worth confirming next run it's organic volume and not a double-count.
- **Volume normal.** TS daily thousands; AllDay hundreds/day; Golazos near-dead. Pinnacle (separate `pinnacle_sales`) ~23–355/day over the window — the post-Legendary-drop wave has fully receded to baseline. Captured, not blind. No UFC secondary in `sales` (≈0, as expected).

**4. Flowscan glance:** skipped (slow/client-rendered; web + DB sufficient).

**Baselines carried to next run:** WNBA `resolver_misses` = **0–1** (near-fully pinned; was 7–8) · TS custodial pair `0xead…`/`0x18eb…` = only high-volume signer, new proposers = individual self-custody buyers (≤5 sales, 1 payer each), **no new venue** · AllDay `onchain_dapper_v2` **8,087/14d** (up ~2.3x from 07-01; internal Dapper-V2-storefront tag — confirm organic next run) · Pinnacle secondary settled ~20–355/day (Legendary-drop wave gone) · no AllDay/Pinnacle IPFS resolver (Pinnacle NFT resolveView→nil confirmed on-chain) · reference-app TS-only (chunk-hash not diffable this run) · dapper.market presumed still 3 collections / no analytics tab (not live-crawled). Open methodology note (unchanged): REST `?expand=contracts` truncates on the AllDay source — a tail AllDay resolver needs a names-only endpoint or live Flowscan to fully rule out.

Sources: Dapper ecosystem / Pinnacle Legendary drop — globenewswire.com/news-release/2026/02/07/3234119/0/en/Dapper-Labs-Ecosystem-Disney-Pinnacle-NFL-ALL-DAY-and-NBA-Top-Shot-Drive-Consumer-Engagement-on-Flow.html ; NFL All Day pause / next-gen — finance.yahoo.com/markets/crypto/articles/nfl-day-stops-issuing-nfts-210400296.html ; Disney+ Pinnacle promo (06-26→07-23) — perks.disneyplus.com/en_us/terms-and-conditions-dapper-labs-pinnacle ; Dapper $5M VPPA settlement — openclassactions.com/settlements/dapper-labs-vppa-privacy-settlement.php , classaction.org/news/5m-dapper-labs-settlement-ends-class-action-lawsuit-over-alleged-data-sharing

---

## 2026-07-01 — no new material ecosystem developments. Recurring action: IPFS catalog refresh (WNBA `resolver_misses` 14→7-8). Newly logged for the baseline: OpenSea OS2 is a pre-existing (~Mar-2025) Top-Shot venue RPC's Cadence indexer is blind to.

Verdict: nothing clears the new-venue / competitive-threat escalation bar → **no Trevor notification**. One recurring positive item: Dapper pinned more TS media on-chain (WNBA tripwire fired again) → run `scripts/refresh-ipfs-catalog.mjs`. One context item recorded for the first time (NOT new, NOT escalated): OpenSea OS2 lists NBA Top Shot, but it does not appear as a distinct execution venue in RPC's Cadence sales — a standing intelligence gap, not a new threat.

**1. Ecosystem / web sweep**
- **No new Flow marketplace / venue / contract for RPC's collections.** dapper.market still presumed 3 collections (TS/AllDay/Golazos), no Pinnacle/UFC, no unified cross-league search, no analytics/FMV tab — web found no feature change (not live-crawled this run). RPC moat intact.
- **OpenSea OS2 (context, NOT new — ~March 2025).** This run's search surfaced that NBA Top Shot Moments are tradable on OpenSea's OS2 (Flow-on-OpenSea; globenewswire 2025-03-18; the TS "bridge" tweet is a Mar-2025 snowflake id). ~15 months old and publicly well-known → does NOT clear the "new this week" bar, so not escalated. Recorded here for the first time as a baseline: it's an alternative TS venue RPC's Cadence indexer cannot see — OS2 Top Shot trading settles on the Flow-EVM / bridged plane, and §3 confirms zero non-Dapper signer in RPC's Cadence sales. Standing intelligence gap (EVM-side blindness), not a new development. (OpenSea's SEA token launched Q1 2026 — OpenSea-wide, not a Flow-collection venue change.)
- **TS IPFS pinning continues (the recurring action).** Public PR is still the 06-12 TS-only "Every Moment verifiable" IPFS move (already integrated). No AllDay/Pinnacle IPFS extension announced.
- **Legal: Dapper ~$5M VPPA privacy / data-sharing settlement** (up to $5 for TS/AllDay/Pinnacle/UFC/Golazos users) — refines the 06-24 "~$4M securities suit" note; legal-only, no contract/venue/product impact.
- **Carry (unchanged):** NFL All Day next-gen reveal ~Aug/Sept (new NFL licensing deal — if a new contract/collection ships it's a new index target); Pinnacle at D23 Anaheim Aug 14–16 2026.

**2. IPFS expansion tripwires**
- (a) **No new AllDay/Pinnacle IPFS resolver.** Pinnacle acct `0xedf9df96c92f4595` returns exactly `PackNFT` + `Pinnacle` (cleaner read than prior runs — both contract keys visible before truncation; standard renderID + MetadataViews + Inscriptions/XP, no media/IPFS resolver). AllDay acct `0xe4cf4bdc1751c65d` returns `AllDay` (standard Series/Sets/Plays/Editions/Badges/Parallels, no resolver) then truncates inside its base64 source — a tail resolver still can't be fully ruled out via REST, but no public announcement exists. HIGH-value find still absent.
- (b) **WNBA pin progress = the actionable item.** `topshot-onchain-art-backfill` `resolver_misses` fell from the 14 baseline (06-24) → transient 29 (06-25, new-mint churn) → settled **7–8** (06-26 → 07-01); ~6–7 more editions pinned. Daily art cron green (`ok=true`) every day. **Action: run `scripts/refresh-ipfs-catalog.mjs`** (IPFS_LOADER_TOKEN from the `ipfs-catalog-loader` edge fn) so catalog/badges/pin-exports absorb the newly-pinned media.
- (c) **Reference-app** `https://dapperlabs.github.io/dapperlabs-ipfs-reference-app/` — fetched OK, still titled **"NBA Top Shot IPFS Reference"**, TS-only (topshot logo, "Browse NBA Top Shot plays with IPFS asset information"). No AllDay/Pinnacle reference app. No change.

**3. On-chain data-anomaly sweep (last 14d, read-only)**
- **Venue-detection — CLEAN, no new venue.** New-proposer query (TS onchain proposers with first-ever sale within 7d) = **[]**. All TS onchain sales with non-null proposer collapse to the single known Dapper custodial pair proposer `0xead892083b3e2c6c` / payer `0x18eb4ee6b3c026d2` (17,592/7d). No new or rapidly-growing custodial signer → no new execution venue (incl. no OpenSea-attributable Cadence signer).
- **Source mix normal — no new tag.** 14d: TS onchain 31,350 / offer_fill 8,786 / topshot_gql 3,258 / ts_history_backfill_v1 897 (all internal/known); AllDay onchain_dapper_v2 3,587 / onchain_dapper_v1 1,914 / allday_studio_history_v1 332; Golazos onchain_dapper_v2 137 / golazos_studio_history_v1 35. No new source tag vs the 06-24 baseline (which introduced `onchain_dapper_v2`).
- **Volume normal.** Daily TS ~1,500–4,300; AllDay ~120–962; Golazos ~1–41 (dead); no UFC rows (UFC secondary ≈0 in `sales`). Pinnacle (separate `pinnacle_sales` table): the post-Legendary-drop wave is decaying — daily ~1,558 (06-24 peak) → ~103–178 (06-29 → 07-01), settling back toward baseline. Captured, not blind.

**4. Flowscan glance:** skipped (slow/client-rendered; web + DB sufficient).

**Baselines carried to next run:** WNBA `resolver_misses` = **7–8** (was 14; further drops = more pinning) · TS custodial pair `0xead…`/`0x18eb…` = only non-null-payer rail, **0 new proposers/7d** · `onchain_dapper_v2` = internal Dapper-V2-storefront tag (AllDay/Golazos) · Pinnacle secondary decaying to ~100–180/day (post-Legendary-drop wave receding) · **OpenSea OS2 = pre-existing TS venue, Cadence-invisible (EVM/bridged), 0 signal in RPC data** · no AllDay/Pinnacle IPFS resolver · reference-app TS-only · dapper.market presumed still 3 collections / no analytics tab (not live-crawled). Open methodology note (unchanged): REST `?expand=contracts` truncates on source bloat — a tail AllDay/Pinnacle resolver needs a names-only endpoint or live Flowscan to fully rule out.

Sources: Flow×OpenSea OS2 — globenewswire.com/news-release/2025/03/18/3044923/0/en/Flow-Brings-Major-Brands-Creators-to-OpenSea-2-0-Ushering-in-a-New-Generation-of-NFTs-Fan-Engagement.html , flow.com/post/flow-and-opensea-a-new-era-for-onchain-consumer-apps-and-digital-assets , gam3s.gg/news/nba-top-shot-now-on-opensea/ ; TS IPFS PR — globenewswire.com/news-release/2026/06/12/3311071/0/en/Every-Moment-Video-Is-Now-Independently-Verifiable-on-NBA-Top-Shot-Built-by-Dapper-Labs-on-Flow-Network.html ; Dapper $5M VPPA settlement — classaction.org/news/5m-dapper-labs-settlement-ends-class-action-lawsuit-over-alleged-data-sharing , openclassactions.com/settlements/dapper-labs-vppa-privacy-settlement.php ; Feb-2026 ecosystem / Pinnacle Legendary drop — globenewswire.com/news-release/2026/02/07/3234119/0/en/Dapper-Labs-Ecosystem-Disney-Pinnacle-NFL-ALL-DAY-and-NBA-Top-Shot-Drive-Consumer-Engagement-on-Flow.html

---

## 2026-06-24 — one actionable item (Dapper pinned more Top Shot media on-chain → run the IPFS catalog refresh). No new venue / marketplace / contract.

Verdict: nothing clears the competitive-threat / new-venue escalation bar. **One positive ecosystem development with a concrete action:** Dapper pinned additional Top Shot media on-chain ~06-21/22 (WNBA tripwire fired) → recommend running `scripts/refresh-ipfs-catalog.mjs` so RPC's catalog/badges/pin-exports pick up the newly-pinned media. Two product-cadence items to carry (NFL next-gen, Pinnacle Legendary drop). Baselines updated below.

**1. Ecosystem / web sweep**
- **WNBA / Top Shot IPFS pin progress (the actionable one)** — the daily art cron's `resolver_misses` dropped from the flat **28** baseline (06-18→20) to 22 (06-21), **1 (06-22)**, then settled at **14** (06-23, 06-24). A clear step-down = Dapper pinned ~14 more editions' media on-chain (`getCIDs` now resolves them; likely the WNBA sets). Daily `topshot-onchain-art-backfill` green every day. **Action: run `scripts/refresh-ipfs-catalog.mjs`** (IPFS_LOADER_TOKEN from the `ipfs-catalog-loader` edge fn) so `topshot_ipfs_assets` + verified-media badges + pin-exports absorb the new media.
- **Disney Pinnacle "Legendary Edition" drop** sold out (40 Legendary Mystery Capsules $499.99; 1-of-1 "Apex" Donald Duck $7,500; ~16K capsules unboxed). Corroborated in RPC data (§3): Pinnacle secondary volume spiked **~3–5x from 06-19** (367–774 sales/day vs ~50–160 before). RPC's `pinnacle_sales` pipeline is capturing it — not blind. Product cadence, not a venue/contract. Pinnacle is also confirmed for **D23 (Anaheim, Aug 14–16 2026)**.
- **NFL All Day next-gen** — pause (since 05-14) reframed: Dapper signed a **new NFL licensing deal**, will reveal the next-gen product **"closer to the start of next season"** (≈Aug/Sept), and added a **"Founding Collector" label + 5% Dapper-balance rebate** for existing holders. Carry: if a new NFL contract/collection ships, it's a new thing to index.
- **NBA Top Shot class-action — tentative ~$4M settlement** (the securities suit). Legal only; no contract/venue/product impact. Context.
- **No new Flow marketplace or aggregator.** Flowty still dead for RPC's collections; Flowverse/TopExpo pre-existing. (Tangential: Japan's 24Karat vending-machine collectibles network — not a marketplace for RPC's collections.) **No dapper.market feature change found** via web (not live-crawled this run): still presumed 3 collections (TS/AllDay/Golazos), no Pinnacle/UFC, no unified search, no analytics/FMV tab. RPC moat intact.
- Forward-looking (carry): the 06-12 TS IPFS PR reiterated Dapper "plans to embed IPFS CIDs directly into on-chain Edition Metadata on Flow" — not yet shipped; would be a new on-chain media source RPC could read directly. Still **TS-only** scope, no AllDay/Pinnacle IPFS extension announced.

**2. IPFS expansion tripwires**
- (a) **No new AllDay/Pinnacle IPFS resolver.** AllDay acct `0xe4cf4bdc1751c65d` → `AllDay` contract (standard Series/Sets/Plays/Editions/Badges/Parallels, no media/IPFS resolver); Pinnacle acct `0xedf9df96c92f4595` → `PackNFT` + `Pinnacle` (standard renderID + MetadataViews, no resolver). Both `?expand=contracts` responses truncate inside the first contract's base64 source (persistent limitation — a resolver named e.g. `AllDay…`/`Pinnacle…` would sort into the truncated tail), but there's **no public announcement** of an AllDay/Pinnacle resolver, consistent with the TS-only IPFS scope. HIGH-value find still absent.
- (b) **WNBA pin progress = the §1 actionable item.** `resolver_misses` 28→14 (see above).
- (c) **Reference-app** `https://dapperlabs.github.io/dapperlabs-ipfs-reference-app/` — **fetched OK** (live, not empty), titled **"NBA Top Shot IPFS Reference"** ("Browse NBA Top Shot plays with IPFS asset information"), **TS-only**. web_fetch renders it as content (HTML/build-chunk hash not extractable that way), so the diff signature is just "present + TS-only" — no AllDay/Pinnacle reference app. Not a new signal.

**3. On-chain data-anomaly sweep (last 14–21d, read-only)**
- **Venue-detection — CLEAN, no new venue.** TS `onchain` sales (26,404/14d) with non-null payer collapse to a **single** proposer/payer pair: proposer `0xead892083b3e2c6c` / payer `0x18eb4ee6b3c026d2` = **23,442** (the known dapper.market / nbatopshot.com custodial rail). **Zero TS proposer accounts had their first-ever sale in the last 7 days** (new-proposer query returned `[]`) → no new or rapidly-growing custodial signer → **no new execution venue**.
- **New source tag `onchain_dapper_v2`** appears for AllDay (735, from ~06-14) and Golazos (42, from ~06-12). This is **RPC's own indexer taxonomy** now distinguishing the **Dapper V2 storefront** leg (`0x4eb8a10cb9f87357.NFTStorefrontV2`) from V1 (`onchain_dapper_v1`, 3,886) — Dapper's own already-scanned infra, **not an external venue**. Note for next run so it isn't mistaken for a new surface.
- Mix otherwise normal: TS `offer_fill` 7,167 (OffersV2, internal), `topshot_gql` 2,811 (internal RPC ingest, buyer-blind by design), `source=null` 683 (flow-backfill, stopped 06-13, healthy). Pinnacle secondary spike ~06-19 (§1) captured by `pinnacle_sales`.

**4. Flowscan glance:** skipped (slow/client-rendered; web + DB sufficient).

**Baselines carried to next run:** WNBA `resolver_misses` = **14** (was 28; watch for further drops = more pinning) · TS custodial pair `0xead…`/`0x18eb…` ≈ only non-null-payer rail, **0 new proposers/7d** · `onchain_dapper_v2` = Dapper V2 storefront leg (internal tag, AllDay/Golazos) · Pinnacle secondary ~400–800/day post-Legendary-drop (was ~50–160) · no AllDay/Pinnacle IPFS resolver · reference-app = TS-only · dapper.market presumed still 3 collections / no analytics tab (not live-crawled). **Open methodology note:** REST `?expand=contracts` truncates on source bloat — full contract-name enumeration of the AllDay/Pinnacle accounts (to rule out a tail resolver) needs a names-only endpoint or live Flowscan, not the current fetch.

Sources: TS IPFS PR — globenewswire.com/news-release/2026/06/12/3311071/0/en/Every-Moment-Video-Is-Now-Independently-Verifiable-on-NBA-Top-Shot-Built-by-Dapper-Labs-on-Flow-Network.html ; TS class-action settlement — betakit.com/dapper-labs-reaches-tentative-settlement-in-nba-top-shot-class-action-suit/ ; NFL next-gen — bitcoinworld.co.in/dapper-labs-pauses-nfl-all-day-nft-minting/ , decrypt.co/367926/nfl-all-day-stops-issuing-nfts-dapper-labs-future-plans-league ; Pinnacle Legendary drop — cointrust.com/market-news/dapper-labs-unveils-disney-pinnacle-a-digital-evolution-of-pin-collecting , globenewswire.com/news-release/2026/02/07/3234119/0/en/Dapper-Labs-Ecosystem-Disney-Pinnacle-NFL-ALL-DAY-and-NBA-Top-Shot-Drive-Consumer-Engagement-on-Flow.html

---

## 2026-06-17 — first run (baseline). No new material ecosystem developments.

Verdict: nothing clears the escalation bar. No Trevor notification. Baselines established below for diffing next week.

**1. Ecosystem / web sweep**
- NBA Top Shot **"Every Moment Video Is Now Independently Verifiable"** (announced ~2026-06-12) = the formal public PR of the IPFS move RPC already integrated (TopShotIPFSResolver + daily art cron + verified-media badges). **Explicitly NBA Top Shot only** — no AllDay/Pinnacle IPFS extension announced. Already known; not new.
- **NFL All Day primary minting paused 2026-05-14**; Dapper (Roham) framed it as building a "next evolution / next-generation" NFL collectibles product, details TBD. Already reflected in RPC (AllDay primary sales are historical-only). **Carry as watch item:** if that next-gen NFL product ships, it may be a new contract/collection to index.
- **Messari State of Flow Q1 2026:** NBA Top Shot volume +12.2% QoQ, new-contract deployments +25.2%, Flow on pace to cross 1B lifetime txns in Q2. Healthy ecosystem; no threat/new venue.
- **No new Flow marketplace or aggregator surfaced.** Flowty remains dead for RPC's collections; Flowverse NFT (multi-buy) is pre-existing. **No dapper.market feature change found** — no unified cross-league search live yet, no Pinnacle/UFC added (still 3 collections: TS/AllDay/Golazos), no analytics/FMV tab. RPC's differentiators (FMV confidence, squeeze, pack EV, cross-collection, badges/rookies/trophies, concierge) remain intact.

**2. IPFS expansion tripwires**
- (a) **New IPFS-resolver contracts:** AllDay account `0xe4cf4bdc1751c65d` REST enumeration is *partial* — `?expand=contracts` returns full base64 source and the response truncates; `AllDay` contract confirmed present, no IPFS resolver visible in the returned portion. Pinnacle account `0xedf9df96c92f4595` was **not fetched** — `web_fetch` provenance rejected the URL because the task prompt abbreviates it (".../accounts/edf9df96c92f4595"). **Methodology fix for next run: write the full Pinnacle REST URL verbatim in the task prompt** (`https://rest-mainnet.onflow.org/v1/accounts/edf9df96c92f4595?expand=contracts`) so web_fetch accepts it. No public signal of an AllDay/Pinnacle IPFS resolver (the 06-12 announcement is TS-only), so the HIGH-value find is absent this week.
- (b) **WNBA pin progress:** `topshot-onchain-art-backfill` `resolver_misses` = **28**, flat across 06-12 → 06-17 (baseline was 27 on 06-10). No WNBA pinning event. Daily art cron green (`ok=true`) every day through 2026-06-17 09:49Z. If `resolver_misses` later drops, Dapper pinned WNBA — run `scripts/refresh-ipfs-catalog.mjs` (IPFS_LOADER_TOKEN from the ipfs-catalog-loader edge fn) so the catalog/badges/pin-exports pick it up.
- (c) **Reference-app dataset HEAD:** not checked — URL not in the prompt verbatim → web_fetch provenance. Deferred; add the full URL to the task prompt to enable.

**3. On-chain data-anomaly sweep (last 14–21d, read-only)**
- **Execution-account capture is LIVE and healthy** (handoff Items 1–2 shipped). Of 29,787 TS `onchain` sales in 14d, 28,960 (~97%) carry buyer+payer+proposer; capture running since ~2026-05-27. AllDay V1 (`onchain_dapper_v1`) carries buyer only, no payer/proposer (by design).
- **Venue-detection profile — CLEAN, no new venue.** TS sale flow is dominated by the two known Dapper custodial accounts: proposer `0xead892083b3e2c6c` (DUC treasury) = 47,481 sales, payer `0x18eb4ee6b3c026d2` (Dapper ops) = 49,202 sales (21d) — i.e. the dapper.market / nbatopshot.com custodial rail. The proposer tail is small individual self-custody buyers (≤92 each, present since capture start). One single-day blip: `0x31e6b70630aeb7d6` proposed 38 TS buys on 06-12 only — an individual spree, not a venue (a new marketplace shows up as a *sustained, growing* custodial signer like 0xead…/0x18eb…). **No new or rapidly-growing custodial signer account → no new execution venue this week.**
- Marketplace/source mix normal. Note: a `topshot_gql` source tag appears for 1,285 TS sales since 06-13 (buyer/exec-account-blind by design) — an internal RPC ingest path, not an external venue.

**4. Flowscan glance:** skipped (slow/client-rendered; web + DB sufficient this run).

**Baselines carried to next run:** WNBA `resolver_misses` = 28 · TS custodial proposer `0xead892083b3e2c6c` / payer `0x18eb4ee6b3c026d2` (≈98%+ of TS flow) · no AllDay/Pinnacle IPFS resolver observed · dapper.market = 3 collections, no analytics tab, no unified search. **Open methodology fixes:** full Pinnacle REST URL + reference-app URL must be written verbatim in the task prompt for those two tripwires to run.

Sources: NBA Top Shot IPFS PR — finance.yahoo.com/markets/crypto/articles/every-moment-video-now-independently-130000916.html ; pr.nba.com/tag/dapper-labs/ ; Messari State of Flow Q1 2026 — messari.io/report/state-of-flow-q1-2026 ; NFL pause / Flow updates — coinmarketcap.com/cmc-ai/flow/latest-updates/
