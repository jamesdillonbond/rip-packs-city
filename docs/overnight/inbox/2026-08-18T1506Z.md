# Daytime monitor — 2026-08-18 ~08:06 PT (15:06Z) · FIRST-TICK-OF-DAY

**⚠ Observed during an ACTIVE saturation spell — everything below is a SYMPTOM. Do NOT act on a causal conclusion or a cost figure from these numbers; re-measure in a quiet window (positive control clear) before acting.**

Positive control at 15:06Z: `pg_stat_activity` = 30/35 active sessions in IO wait (86%). `rpc_ops_snapshot()`, `check_pgcron_recent_failures()`, and `v_rpc_trust_health` all returned `57014 canceling statement due to statement timeout`. We are in the spell, so those three are not broken instruments — they are the spell.

## Candidate 1 (NEW today) — cross-collection mats ~35h stale; BOTH refresh steps timed out
- **Source:** `cron.job_run_details` for `rpc-ccm-step1` (04:10Z) and `rpc-ccm-step2` (04:25Z).
- **Observed:** both jobs ran on schedule 2026-08-18 and **both `status=failed` with `canceling statement due to statement timeout`** (step1 on the `INSERT INTO cross_collection_cohort_mat`, step2 on `INSERT INTO cross_collection_ts_set_overlap_mat`). Both **succeeded** on 2026-08-17 (`1 row` each). Result: `cross_collection_cohort_mat.computed_at` = 2026-08-17 04:10Z and `cross_collection_ts_set_overlap_mat.computed_at` = 2026-08-17 04:25Z — ~35h old against the 26h freshness bar. `cohort_count` = 179 (healthy, ~150+).
- **Risk read:** LOW-to-MEDIUM data-freshness. Both steps failed identically on statement-timeout, which is the saturation signature, NOT a logic/schema error (both jobs are `active=true` and the SQL is unchanged from yesterday's success). This is expected to self-heal on the next clean 04:10Z/04:25Z tick.
- **Suggested action (SYMPTOM → RE-MEASURE):** do NOT treat as a broken refresh. Re-check `computed_at` on both mats after the next 04:10Z/04:25Z tick in a quiet window. **ESCALATE only if it fails a second consecutive day** (mats would then be ~2 days stale), which would make it more than saturation collateral. The recovery recipe if a manual catch-up is ever wanted (night pass / Trevor only) is a self-cleaning pg_cron one-shot for the failed step — but the correct lever here per focus.md is cutting the refresh's work, never raising its timeout.

## Not re-raised (already known / filed — listed so the night pass sees they were seen)
- **`compute-golazos-pack-ev`** silent since 2026-08-17 06:37:31Z (~32.5h, thresh 800m). Already settled/filed (golazos-specific, not saturation, not retention — see prior inbox + 08-18 ledger). No new info.
- **`candy-editions-ingest`** last_run null / medium — chronic 300s route timeout since 08-03, handoff exists.
- **Cluster of 5 more silent cadence jobs** (`reconcile-saved-wallet-stats` ~14h, `topshot-moments-hydrator` ~12h, `refresh-pack-grail-metrics-mv` ~4.8h, `pinnacle-metadata-backfill` just over, `wallet-username-resolver` ~3h) — this is the saturation-collateral cluster, filed as ONE symptom (this spell), not N bugs, per CLAUDE.md. Re-measure in a quiet window; if any persists AFTER the spell clears, that one is a real stop.

## Health line
Vercel: latest production READY = `c50ef186` (data-integrity RPC bound fix); 0 ERROR deploys in last 20 (CANCELED entries are normal superseded-by-newer-push). Security/trust/artifact validation NOT completed this run — deferred: running them mid-spell only stacks IO and any timeout would be a spell symptom, not a real breach (per SKILL 1b/1c). Demand gate unchanged (nightly: 21 users / 1 WAU).
