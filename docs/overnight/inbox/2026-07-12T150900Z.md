# daytime-monitor inbox — 2026-07-12T15:09Z (~08:09 PDT, first-tick-of-day)

Run mode: READ-ONLY sweep. Bash/git sandbox DOWN (`useradd: cannot create directory /sessions/… exit 12`, same infra failure the 07-12 night pass hit) → **inbox written to mount, push unavailable**; night pass picks up locally. Lock RELEASED 08:10Z (not held). Health otherwise GREEN (security 0/0/0/0, trust 16/16 ok breaches [], stalled_pipelines [], sentinel 0, Sentry 0 new/24h, Vercel prod `e52669f` READY no ERROR on HEAD).

---

## 1. CROSS-COLLECTION-CCM-STEP1-STALE — HIGH (1a daily finding, NEW today)
- **Source:** `check_pgcron_recent_failures()` + 1a cross-collection verify. `rpc-ccm-step1` (cohort refresh, 04:10 UTC) **FAILED today 04:10:00Z** — `ERROR: canceling statement due to statement timeout` on the `INSERT INTO cross_collection_cohort_mat` step. `rpc-ccm-step2` (overlap, 04:25Z) **succeeded**.
- **State:** `cross_collection_cohort_mat` max(computed_at)=**2026-07-11 04:10Z (~35h stale**, past the 26h bar; count 154 = last-good). `cross_collection_ts_set_overlap_mat` fresh (07-12 04:25Z). Fresh-overlap + stale-cohort = step1 specifically failed (inverse of the documented step2-fail example, same class).
- **Prior ticks:** 07-10 + 07-11 step1 both succeeded → this is a NEW first-miss, not a standing condition.
- **Blast radius:** LOW user-facing — the /insights cross-collection cohort surface + `rpc-cross-collection` artifact render yesterday's cohort data (overlap panel is fresh). No trust breach. Job is daily, so it will NOT self-retry until 07-13 04:10Z, and under today's contention (candidate #2) tomorrow's tick may also fail → staleness likely persists >24h without intervention.
- **Suggested action (night pass):** self-cleaning pg_cron one-shot for JUST step1 — re-run the cohort refresh fn with a raised `statement_timeout` (e.g. `SET LOCAL statement_timeout='600s'`), body ending in `cron.unschedule` of itself. A FAILED one-shot does NOT self-unschedule → clean up next day. (Do not touch step2; it's healthy.) Read-only monitor did not schedule it.

## 2. PGCRON-STMT-TIMEOUT-WAVE-20260712 — MED (broad contention cluster; escalation of carried DAYTIME-CONTENTION family + one regression-of-a-closed-fix)
- **Source:** `check_pgcron_recent_failures()` — **10 jobs** all failing today with `canceling statement due to statement timeout`, spanning 04:10Z→13:55Z: `rpc-remap-misattributed-sales` (4/4 runs failed), `rpc-allday-ev-corrected-refresh` (2/4), `rpc-fmv-clamp-disconnected-ask` (13:55Z), `rpc-refresh-fmv-display-guard` (13:45Z), `rpc-refresh-thin-fmv-guard` (13:30Z), `rpc-refresh-misattrib-candidates` (10:45Z), `rpc-attribute-pack-rips-empirical` (09:40Z), `rpc-serial-fmv-power-model-weekly` (11:00Z), `rpc-serial-fmv-jersey-weekly` (11:05Z), `rpc-ccm-step1` (=#1). Also Vercel `fmv-recalc` fails 13/24h (up from 7 baseline).
- **Regression-of-a-closed-fix (the sharp part):** `rpc-serial-fmv-power-model-weekly` was CLOSED 2026-07-04 via `audit_20260630_serial_fmv_fits_statement_timeout_600s` (600s in proconfig). It **timed out anyway** at today's 11:00Z Sunday tick, and the sibling `rpc-serial-fmv-jersey-weekly` too → the 600s budget is now insufficient at current scale, OR they're being canceled under sustained contention. Worth the night pass re-opening SERIAL-FMV-POWER-MODEL-WEEKLY-TIMEOUT rather than leaving it closed.
- **Likely root:** DB at **11,255 MB (+2.1GB since 07-11**, night-pass DB-SIZE-CREEP-11GB) shrinking headroom + Sunday weekly heavy-job window (11:00Z serial-FMV fits) + autovacuum on the freshly-grown wmc/pack_rips + ongoing studio backfills = a broader-than-usual daytime contention window. Members are individually the carried DAYTIME-CONTENTION family (remap/allday-ev/display-guard/thin-fmv-guard/misattrib), but the breadth (incl. normally-healthy jobs) + the serial-weekly re-fail is new signal.
- **Blast radius:** LOW-MED, internal/backstop. FMV stays FRESH (topshot 0.3h / allday 0.3h — the P1b inline clamp on every fmv-recalc covers the failed backstop crons); no trust breach. Impact = serial-FMV multipliers stay at last week's values, cohort/misattrib/pack-rip-attribution refreshes lag, AllDay corrected-EV board may lag. Self-heals as IO settles (most members recover on the next quiet tick).
- **Suggested action (night pass):** (a) re-open + re-measure the serial-FMV-weekly fits — decide if the proconfig ceiling needs a raise beyond 600s or the fits need chunking; (b) treat the rest as the standing contention family (recover as IO settles); (c) keep watching DB headroom (11.25GB and climbing) as the probable amplifier.

---

## Watch (not new candidates — noted for continuity)
- **wap_usd write errors STOPPED** — Vercel `DB write failed: Could not find the 'wap_usd' column` (from the 07-11 WAP→ASP rename) last seen **06:44:32Z**; none since across many fmv-recalc ticks + FMV is fresh → the ASP-rename code deployed and resolved it. Confirm it stays at zero next tick.
- **Artifact validation LIMITED this run** — artifact HTML lives outside the connected folder + bash down, so embedded payload queries could not be read/run. Validated via the shared data layer instead: all backing signals healthy EXCEPT `cross_collection_cohort_mat` behind `rpc-cross-collection` (stale per #1 — renders, shows day-old cohort). The 07-11 WAP→ASP rename was output-contract-preserving (readers alias `asp_usd AS wap_usd`, `fmv_current` still exposes `wap_usd`) so FMV artifacts reading through fns/views are not at schema-break risk.
- **Carried, unchanged:** BASH/GIT-SANDBOX-PROVISION-FAILURE (recurring, operator/infra — hit again this run), DB-SIZE-CREEP-11GB, SMOKE-PACK-DIST-SALES-HISTORY-FAIL (still false-failing, last 15:19Z), TOPSHOT-MOMENTS-HYDRATOR-GETMINTEDMOMENT-ERRORS (10/24h, upstream GQL, hydrator worker deploy still pending operator), ALLDAY-PACK-OPENS-BACKFILL-404 (30/24h), cron-job.org dropout family, BUYERBF, CROSS-SOURCE-DEDUP, + standing owned/operator/gated/CC queue.
