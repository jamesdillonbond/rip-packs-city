# Daytime monitor candidates — 2026-08-12T03:09Z (≈2026-08-11 20:09 PDT)

Written by `rpc-daytime-monitor`. Shell down (`/sessions` no-space → no git), so this file is written to the MOUNT, push unavailable — night pass picks it up locally.

## 1. `/api/public/ipfs-media/[cid]` 502s — CHARACTERIZED (closes the 08-11 night-pass queue item) · LOW · upstream

**The 08-11 nightly queued this "5xx elevated 103/6h → daytime monitor to characterize." Done. Finding: broad, transient upstream IPFS-gateway flakiness, fails soft — NOT a dead pin and NOT a code regression.**

- **Source:** Vercel runtime logs, prod. Grouped by statusCode over 6h: **502 × 65, 200 × 41, 302 × 12** (~11 502s/hr; steady vs the 103/6h the night pass saw — not spiking).
- **Distribution (the diagnostic detail):** grouped by `requestPath` over 3h with `statusCode=502` → **33 distinct CIDs, each count 1** (mix of `Qm…` v0 and `bafy…` v1 CIDs). A single dead pin would concentrate on one CID; instead every 502 is a different CID, so this is the upstream IPFS gateway intermittently failing per-request, not a specific missing asset.
- **Blast radius:** fails soft — a 502 on the media endpoint means that one `<img>`/media element shows a placeholder; the page itself renders 200. No user-facing page break, no Sentry issue (0 new unresolved in 12h). Not on the trust board.
- **No code shipped this route today** (today's deploys are search/follows/sale-print-charts/editions.description) → not a regression; it's ambient gateway behavior.
- **Risk read:** LOW. This side has no lever over a third-party IPFS gateway's per-request reliability.
- **Suggested action (night pass / CC, code change — NOT autonomously-shippable blind):** add a **multi-gateway fallback + one short retry** in the pass-through (try gateway A, on non-200/timeout fall to gateway B before returning 502), and/or a short negative-cache so a flaky CID isn't re-fetched every hit. Pure read-path resilience, fails-soft either way. If not worth the code, this can be **closed as accepted upstream noise** — it is not an incident.

---

**Everything else on this tick was known-class, nothing new to log:** security 4/4 clean; 2 trust breaches both known (`panini_sale_price_capture_dry_days=14` operator runner outage; `unmapped_resolution_backlog_max=215` net-draining backfill inflow — focus.md says do-not-re-flag while trending down); pg_cron scheduler HEALTHY (jobs 55/56 pack-opens fire every tick — 18 & 12 runs/3h — the `cron_silent` is the documented post-floor no-op-walk that stopped writing `pipeline_runs` rows, verified directly against `cron.job_run_details`, not a scheduler stop); 3 pg_cron MV-refresh failures (misattrib-candidates, thin-sale-ask-disclosure, new-collectors) all >12h old, the known self-recovering saturation class; `sync-nba-projections` 100% fail = known NBA-offseason/Akamai infra-gate; `topshot-active-listings-ingest` egress_blocked = known; `allday_pack_opens_forward`/`allday_sales_v1_backfill` cursor_stalled = AllDay backfill/primary-market wind-down (AllDay ended primary pack sales, so no new forward activity is expected). Deploys: latest READY; the CANCELEDs are superseded rapid-fire CC pushes. DB 12,560 MB.
