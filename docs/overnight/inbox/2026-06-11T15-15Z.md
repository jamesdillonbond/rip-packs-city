# Daytime monitor inbox — 2026-06-11T15:15Z (~8:15am PDT)

Run context: lock RELEASED (night pass 09:47Z, 11e5ae3 = current prod, READY). Security 0/0 both checks; trust health 7/7 ok; sentinel ts_uuid_dupes_24h 0 / UUID-leak-48h 0; 20/20 deploys READY; DB 4,258 MB (−2.6GB vs 06-10 baseline = the partition drops, expected); TS HIGH+MED 2,865 (baseline 2,852 — flat, but HIGH 567→834 / MEDIUM 2285→2031, an up-quality shift consistent with the audit wave + sales-history backfill); TS NO_DATA 4,715→4,687.

## 1. HIGH — DAYTIME-DBSAT-RECUR: NEW daytime saturation window 07:00→14:30Z, peaked 13:30Z at 59% fails, self-recovered by ~14:45Z
- Source: pipeline_runs histogram. Fails/hr: 07Z=52/197, 08–12Z=15–26/hr, 13:30 bucket=33/56 (59%), 14:00=15/108, 14:30=5/115, **15:00=0/32 (recovered)**. Run-count also COLLAPSED 13:00–13:30Z (47 + 56 total runs vs ~110 normal) — runs going dark, not just failing.
- Error class is broad-spectrum `upstream request timeout` (PostgREST gateway) + statement timeouts across ≥20 pipelines (buyer-backfill 24, pack-ev 22, pinnacle-nft-resolver 18, wmc-fmv-populate 9, hydrator 8 …). Smoke tests did NOT trip during the window (the Sentry smoke cluster last-seen 12–13h ago = the overnight residual; nothing new today).
- DISTINCT from 06-10's populate_wmc_image storm (that fix verified live, 2–554ms) and from the 00Z anchor window. Yesterday's daytime window was 10:00–15:30Z; today 07:00–14:30Z — second consecutive day of a daytime-hours pattern.
- Suggested action (night pass): pg_stat_statements top-total-time scoped to today's 07–14:30Z window to name the new driver; re-baseline DBSAT; consider whether heavy interactive/CC sessions (TS sales-history validation, ASK_ONLY work, morning ship wave 02–10Z) correlate. Do NOT assume the 06-10 index fix regressed — it was verified.
- Risk read: no user-facing breach observed (trust 7/7, smoke quiet, FMV writers all ok again by 15Z), but ~7.5h at 8–59% fails/hr is far above the ~1%/hr target and recurring.

## 2. HIGH — TFP-ROUND-BUG: topshot-fmv-populate has a NEW failure mode — `function round(double precision, integer) does not exist`
- Source: pipeline_runs 2026-06-11 14:06:34Z, ok=false, upserted=0. Prior fails were all `sets read failed: upstream request timeout` (the known TFP-NO-OK carry); this tick got PAST the sets read and died on a SQL type bug — round(dp, int) needs a ::numeric cast.
- Pipeline has had 0 ok runs since 06-09 (06-10 0/3, 06-11 0/1); at the new 4/day slot cadence every fail costs ~6h of TS marketplace FMV refresh.
- Suggested action: grep the topshot-fmv-populate route + upsert_topshot_marketplace_fmv chain (and any recently-migrated fn it calls) for `round(` on a float expression; add `::numeric`. Single-cast fix, low risk, but FMV-writer path → CC/Trevor, not night-pass auto-ship.
- Operator note: the TFP alert threshold was temporarily 480→800m (f81b8698); once a tick goes green post-fix, restore 480.

## 3. MED-HIGH — AF1 CONFIRMED STILL BROKEN (carry + fresh evidence): v_tracked_wallet_fmv_confidence times out at 15:11Z on a recovered DB
- Source: direct `SELECT count(*) FROM v_tracked_wallet_fmv_confidence` → statement timeout at 15:11Z, AFTER the saturation window ended (squeeze board, trust health, sentinel RPC all fast at the same moment). The rpc-tracked-fmv-confidence artifact therefore still errors on open.
- 3rd+ breakage of this view (05-31 lateral, 06-06 EXISTS semi-join, now). Per CLAUDE.md: stop optimizing the view — fix the writer (DUPE1-MIT/B2 class). Re-confirming so the carry isn't dropped post-drain.

## 4. MED — MULTICOLLECTION-COMPLETE telemetry gap: detect_stalled flags it (497m vs 420m) while the actual backfill wave ran
- Source: detect_stalled_pipelines() 15:06Z. wallet-backfill-multicollection-complete last logged 06:50Z, but dispatch logged 329 runs/8h (last 12:54Z) and every per-collection variant ran through 13:07Z — the ~12:45Z wave's "complete" row never landed (likely killed in the 13Z saturation; same unlogged-after()-exit class 553f850 fixed elsewhere).
- Suggested action: confirm tonight's ~06:45Z wave logs a complete row; if not, apply the 553f850 try/catch pattern to the complete-logger. Until then treat this detect_stalled HIGH as the known telemetry-artifact class (06-09 precedent).

## 5. WATCH — Sentry NEXTJS-1P: new auth-token lock-stolen event on /:collection/fast-break (1 event, ~14:13Z)
- Same class as NEXTJS-1N (edition page, 22h ago). Browser Supabase auth-lock contention, 1 user, no recurrence. Record-only; resolve-with-regression-arming once 24h quiet.

## Green this run (verifications, no new action)
- pack-events-ingest NOT re-wedged: 14:24/14:39Z statement-timeout fails (48-row batch) were saturation casualties; 14:54Z + 15:09Z ok, cursor advancing (p25/p26 holding).
- topshot-sales-indexer 15/15 ok (6h); fmv-recalc 13/13 ok; compute-topshot-pack-ev recovered (last ok 15:07Z); topshot-buyer-backfill made progress through the storm (last ok 15:04Z; fails are the saturation class — watchlist @90m already live).
- analytics-smoke 0/11 (6h) — KNOWN: ANALYTICS-SMOKE-RESIDUAL already queued for CC by the night pass; not re-logged.
- topshot-sales-history-backfill: 5 runs/14h all ok, zero edition-creation leaks (UUID-leak-48h 0) — sparse cadence is the already-escalated TSHB-GHA-NOSCHED, not new.
- Artifact spot-checks: squeeze board (insights-health leg) 501 rows fast; trust-health view fast; pipeline/FMV freshness queries fast. Full 17-artifact query sweep not run this pass (saturation triage prioritized); AF1 is the only known-broken artifact.
