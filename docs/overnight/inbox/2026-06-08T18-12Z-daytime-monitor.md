# Daytime monitor — 2026-06-08T18:12Z

Read-only sweep. **Platform GREEN.** 0 pipeline fails in the last ~5h (13:02Z → now), `detect_stalled_pipelines() = []`, security 0/0 (all four checks), sentinel DUPE1 cleared to **0**, 20/20 deploys READY, 16/16 artifacts' backing queries execute. **0 new candidates this run** — everything observed is already ledger/inbox-owned, owned by an active task, or freshly fixed by Trevor in-flight. This file carries status reconciliations only (so the night pass can update the ledger) + a corroboration block.

## Status reconciliations for the night pass (NOT new candidates — verified this sweep)

- **LISTCACHE-CRON-DROP — `-v2` half RESOLVED (operator).** `topshot-listing-cache-v2` is now `is_active=false` in `pipeline_cadence_watchlist` (note: "RETIRED 2026-06-08: redundant duplicate of the primary… is_active=false to stop the false-positive stall alert. Drop the cron-job.org -v2 entry to fully retire."). That is why `detect_stalled_pipelines()` is now `[]` (it was the single entry at the 08:15Z baseline + 15:10Z monitor). The PRIMARY `topshot-listing-cache` recovered and is healthy (last run 35m ago, 8/24h, 0 fails) and keeps `cached_listings` fresh. **Only residual = the cosmetic cron-job.org `-v2` entry removal** (operator dashboard cleanup, non-urgent). Ledger LISTCACHE-CRON-DROP can move to mostly-resolved.

- **Pinnacle render-FMV `statement_timeout` — FIXED IN-FLIGHT by Trevor (do not re-raise).** The 15:10Z monitor (#13) + the `pinnacle-sync-tick-verify-jun8` task flagged `pinnacle_fmv_recalc_render_all()` canceling on statement timeout (06-08 10:07Z tick failed; per-render FMV stale ~29h then). Trevor shipped the fix this hour: commit `5880eeb` (migration `audit_20260608_pinnacle_render_all_statement_timeout` — `SET statement_timeout TO '120s'` on the fn, body byte-identical so displayed FMV can't move; route maxDuration 30→60) + docs `953563f`, deploy `dpl_M2yE2J156Wc2burvmp9t1fdpTzqQ` READY (~17:39Z). **Verified working:** `pinnacle_catalog.fmv_computed_at` max is now 17:38Z (fresh, was ~29h stale), priced 1,797. Residual = confirm the NEXT scheduled daily cron tick (~10Z 06-09) logs `ok=true` — already covered by `pinnacle-sync-tick-verify-jun8` + the queued PIN-SYNC-CRON. No night-pass action.

- **DUPE1 sentinel fully cleared to 0 (CC-owned merge gate well met).** TS-UUID-keyed-48h = **0** (was 542 at the 08:15Z baseline, 43 at 15:10Z) with **0/hr** new (1h counter = 0); ts_uuid_total flat at 6,404 (historical fossils only). The inert re-mint has fully stopped and the 48h window rolled off. The focus.md DUPE1-merge gate ("sentinel < 250 and falling") is comfortably met — CC-owned per Trevor's 06-07 decision; **noted only, not pre-empted.** DUPE1-MIT (`drain_fmv_cold_tail` skip-inert) is holding (TS NO_DATA 5,196→5,129 improving).

- **SMOKE-EDITION-TIMEOUT (Sentry NEXTJS-1H) — recurred; recurrence condition now MET.** 2 events now (was 1 at baseline), last seen ~2h ago (~16:0xZ), still the single edition-page "Recent Sales" smoke assertion timeout (assertion-class → bypasses SMOKE-RETRY), 2 users. The queued item's escalation trigger was "if it recurs, bump the smoke per-fetch timeout for the heavier edition endpoint or route the abort through SMOKE-RETRY" — that condition is **now satisfied** (it recurred). Suggest promoting from "single transient / watch" to that CC/operator action next clean window. Still LOW (no user-facing impact; 2 events/13h, not spiking). Touches `app/api/smoke-test/route.ts` (hot file) → not auto-shippable.

## Corroborated / already-owned — NOT new candidates (logged so the night pass sees the monitor checked them)

- **DBSAT-0911 (daytime DB-saturation window) — corroborated; the cross-collection confounder hypothesis looks RIGHT.** Today's fails/hour: 10Z 32, 11Z 39, 12Z 10 (the :00 rush, 1,285 runs), 13Z 2, **14Z–18Z all 0**. The platform went and stayed clean from 13:02Z — i.e. right after Cowork's 14:55Z cross-collection refresh fix (`cohort_step1/step2` set-oriented rewrite + `idx_wmc_cohort_cover`). This supports the 15:10Z read that the sustained 09–11Z saturation was confounded by the broken per-wallet FOR-LOOP refresh, **not** insufficient cron stagger. The real test remains the **06-09 clean-day re-baseline** the 15:10Z file called for. All fails were the I1 statement-timeout/pool class on the DB-read-heavy set (compute-topshot-pack-ev, pinnacle-nft-resolver, topshot-moments-hydrator's `v_moments_needing_hydration` candidate_read = N2, wmc-fmv-populate, pinnacle-listings-reconcile); every one has `last_ok > last_fail` and 0 fails in the last 5h.

- **N2 (`v_moments_needing_hydration` candidate-read timeout)** — 2 fails in the 10–13Z window (last 13:02Z), same `canceling statement due to statement timeout` at the rush, self-recovered. Unchanged from the queued N2; part of the DBSAT window above.

- **N1 (`snapshot-institutional-wallets`)** — 692m since last run vs its 1800m watchlist threshold = within envelope, NOT stalled (absent from `detect_stalled`). Running on its irregular cadence; operator item unchanged.

- **DB size 6,616 MB** (+134 vs the 08:15Z baseline, but only +19 since the 15:10Z sweep) — the bulk was the one-time 14:55Z `idx_wmc_cohort_cover` covering-index build; creep has settled. Watch-only, already noted.

- **pack-EV drained further** — `topshot_pack_ev_targets` stale-24h **23** (was 91 at baseline), stale-48h **0**, newest last_ev_at 1m ago. PACKEV-THROUGHPUT stays closed; off-limits batch raise stays declined.

## Clean (no action)
Security 0/0 (RLS-off [], anon/auth-write-on-RLS-off [], `check_secdef_anon_execute_violations()` [], `check_public_security_invariants()` []). FMV flat-to-improving (TS HIGH+MED 2,938 / NO_DATA 5,129↓; AllDay 498 / 527). All core writers fresh (TS fmv write 0m, TS last sale 7m, fmv-recalc 20m/74-24h-0-fail, allday-fmv-populate 6m, topshot-sales-indexer 5m). 20/20 recent prod deploys READY, latest `953563f` (17:40Z); the heavy 06-08 CC/Trevor wave (Pinnacle legacy-FMV retirement steps, render-timeout fix, HybridCustody read-only verify, Candy/Panini chain-two groundwork, pack/moment TS-parity) is all READY with **0 attributable Sentry regressions** (only the pre-existing NEXTJS-1H). Artifact backing queries spot-validated direct: AF1 `v_tracked_wallet_fmv_confidence` 20 rows (no 57014), cross_collection_deals_board 329, pinnacle_scarcity_board 500+, topshot_squeeze_board 500+, set_squeeze 129. unmapped_sales 183 (flat).
