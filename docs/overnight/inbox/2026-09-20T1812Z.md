# Daytime monitor candidates — 2026-09-20T1812Z (rpc-daytime-monitor, ~11:0x AM PT tick)

Context: mid-day tick (nightly released lock 08:16Z, verdict HEALTHY). Trevor ACTIVELY shipping during this window (8 prod deploys in ~18 min, tip BUILDING, no ERROR states). Positive control io_wait 3 / active 4 / total 22 + a 36-job "job startup timeout" cluster ⇒ treat DB-side timeouts as SATURATION SYMPTOMS, not N distinct bugs (Section 1c).

## HIGH — genuine non-saturation logic bug (distinct from the timeout storm)
- **Title:** `rpc-weekly-wmc-reindex-6` fails on a non-existent index
- **Source:** `check_pgcron_recent_failures()` — jobname `rpc-weekly-wmc-reindex-6`, last_run 2026-09-20 03:43Z, `ERROR: relation "public.idx_wmc_wallet_coll_ek_fmv" does not exist`
- **Why it's not saturation:** it is a LOGIC error, not `statement timeout`/`job startup timeout` — the index the reindex job targets was renamed/dropped and the job body still names the old one.
- **Risk read:** low to investigate. Job is a weekly REINDEX (maintenance); its failure means that index isn't being rebuilt — and if the index genuinely no longer exists, some wmc read path may have lost its supporting index (possible saturation contributor — NOT asserted). No user-facing write risk.
- **Suggested action (night pass):** read the job's `cron.job.command`, confirm the live index name on the wmc table (`\d` / pg_indexes), and either repoint the job at the current index name or drop the job if the index was deliberately retired. Additive/maintenance — no route/.tsx.

## SYMPTOM (defer cause) — #126 daytime escalation, now user-facing
- **Title:** #126 cron/DB saturation has escalated during the day to user-facing read paths
- **Source:** rpc_ops_snapshot() trust breaches [board_mv_refresh_stale_hours=15.98h (breach 8), public_board_slow_count=11 (breach 1, up from 3 at 08:16Z)]; check_pgcron_recent_failures() 36 jobs failing (statement + job-startup timeouts, incl. MV-refresh jobs → board_mv staleness); Vercel runtime errors last ~25 min: pack-detail pack_lifecycle read>5000ms (140/32 users), edition get_edition_recent_sales statement timeout→empty (57/41 users), edition market_bundle timeout (37), team detail timeout (32).
- **Symptom, not cause:** observed under saturation (Section 1c) — every duration/cost read here is uninterpretable; do NOT assert a cause, cost, or "cheap/expensive" judgment. #126 cause is NOT established (leading candidate R117 unproven) and it is already queued for Trevor with no autonomous lever.
- **What IS new vs the nightly HEALTHY verdict:** the saturation is now (a) exhausting the pg_cron worker pool ("job startup timeout"), (b) breaching board_mv_refresh freshness (16h), and (c) surfacing as statement timeouts on live pack-detail/edition/team pages for real users (32–41 users/group). Honesty layer is behaving correctly — degrading to empty, no fabricated zeros.
- **Suggested action (night pass / Trevor):** quiet-window RE-MEASURE (warm-vs-warm, per-call pgss blocks/call) before any lever; this is a re-measure/observe item, not a fix. board_mv_refresh_stale + public_board_slow_count clear on their own once the MV-refresh jobs stop being killed — verify they recover in a quiet window rather than treating them as independent bugs.

## Notes
- Artifact deep-validation (Section 1b payload queries) DEFERRED this tick: running the heavy payloads under active saturation would time out (a symptom, not a broken artifact) and stack IO. Enumerated 11 active artifacts via list_artifacts; re-validate in a quiet window.
- `get_edition_detail` "Could not query the database for the schema cache" errors = expected `apply_migration` PGRST002 bursts from Trevor's concurrent shipping — NOT a finding.
- topshot-active-listings-ingest stalled 1014min (>900 threshold): the KNOWN residential-box-dark detection, expected, not a new finding.

---

## 🔁 BOTH ITEMS CLOSED — verified 2026-09-20 ~2:5x PM PT (Claude Code, Windows box). DO NOT RE-CHASE.

**This filing was written at 11:12 AM PT, which is the LAST hour of the spell it describes.** Both items were re-derived
against live state ~3.5 h later and neither is open. Recorded here rather than by editing the body above, because the
body is an honest record of what was visible at 11:12 AM PT.

### HIGH `rpc-weekly-wmc-reindex-6` — REFUTED. It was already fixed 11 minutes after the failure it cites.

⛔ **The filing's diagnosis ("the job body still names the old one") was true when the run failed and false when the
filing was written.** The dated chain:

| when (PT) | what |
|---|---|
| 09-14 | `idx_wmc_wallet_coll_ek_fmv` **dropped**, superseded by the `_tier` covering index (migration `20260914001340`) |
| 09-19 20:43 | jobid 478 runs, fails: `relation "public.idx_wmc_wallet_coll_ek_fmv" does not exist` ← **the row this filing read** |
| 09-19 **20:54** | jobid 478 **repointed** at the successor (migration `20260920035444`) — **11 min after the failure** |
| 09-19 21:17 | `run_wmc_reindex_verify()` target array updated in lockstep (migration `20260920041743`) |

✅ Live now: `cron.job` 478 reads `REINDEX INDEX CONCURRENTLY public.idx_wmc_wallet_coll_ek_fmv_tier`; that index
**exists, is valid, 408 MB**; no INVALID `*_ccnew` leftovers on `wallet_moments_cache`; all three migrations are present
as repo files (parity clean). Nothing to do — the next real run is **Sat 09-26 20:43 PT**.

⚠ **THE INSTRUMENT TRAP THAT PRODUCED THIS, and it will produce it again.** `check_pgcron_recent_failures()` reports
`latest_status='failed'` for jobid 478 **right now**, with the identical pre-fix message — because the job is **WEEKLY**
and its window has not yet contained a post-fix run. ⛔ **For a weekly job, "latest_status = failed" CANNOT distinguish
"still broken" from "fixed, awaiting next run."** Read `cron.job.command` against `pg_indexes` — one query, decisive —
before filing a pg_cron failure whose schedule is longer than the instrument's window.

⚠ Second trap, and it cost this filing its headline: the failure message names `idx_wmc_wallet_coll_ek_fmv`, the live
command names `idx_wmc_wallet_coll_ek_fmv_**tier**`. **Two objects one suffix apart, opposite conclusions** — exactly
CLAUDE.md's *never infer the callee from the name*. The 15:13Z filing had already declined to re-file this item as known
(ledger 09-19); this one re-filed it as HIGH.

### SYMPTOM #126 escalation — RESOLVED ON ITS OWN, exactly as this filing predicted.

✅ The filing's own call was right: *"board_mv_refresh_stale + public_board_slow_count clear on their own once the
MV-refresh jobs stop being killed."* They did. Same instrument (`rpc_ops_snapshot()`), 21:52Z:

| metric | this filing, 11:12 AM PT | 2:52 PM PT | breach at |
|---|---|---|---|
| `board_mv_refresh_stale_hours` | **15.98** | **1.88** | 8 |
| `public_board_slow_count` | **11** | **0** | 1 |
| `trust_health_breaches` | (2 above) | **`[]`** | — |

⭐ **The clean change point, and it is a STEP not a drift** — pg_cron failures per hour PT at **constant run volume**
(~390–410 runs/hr, so this is the same work succeeding, not less work attempted):

`01:00` 71 · `02:00` 27 · `03:00` 29 · `04:00` 80 · `05:00` **110** · `06:00` 101 · `07:00` 56 · `08:00` 24 ·
`09:00` 40 · `10:00` 40 · **`11:00` 0 · `12:00` 0 · `13:00` 0 · `14:00` 0**

Positive control, the same one this filing took: `pg_stat_activity` **io_wait 0 / active 0 / total 26** (was 3/4/22 here,
and 16/16 in the 17:30Z filing). Security arms 4/4 clean. The spell ended ~11:00 AM PT — **within the hour this filing
was written**, which is why it read as "escalating."

### One consequence worth carrying: a HIGH alert that is pooled across that change point

`rpc_ops_snapshot().pipeline_alerts` currently reports **`backfill-pack-rip-metadata` 36/59 failed (61.0%), severity
high**. ⛔ **That rate pools the spell with the recovery and measures the spell.** Split on 11:00 AM PT:

- during the spell: **15 ok / 44 runs**
- after: **3 ok / 3 runs**

✅ And the #128 drain is healthy: the `zero_repriced`/`zero_cleared` counters shipped at **14:22 PT** (`b34c199e2`), and
the **14:53 PT tick is the first to carry them — `zero_repriced 68 + zero_cleared 7 = 75`**, matching focus.md's ~75/tick
exactly. ⚠ Earlier ticks read **NULL, not 0**, purely because they predate that deploy — per focus.md a **0** there would
have meant the leg stopped reaching the rows, which is a different and real failure. `pack_rips.pull_value_usd = 0` is
**81,859** rows and falling.
