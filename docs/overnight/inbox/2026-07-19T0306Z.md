# Daytime monitor — 2026-07-19T03:06Z (~20:06 PDT Jul 18)

No clock skew (shell 03:05:59Z ≈ DB `now()` 03:06:16Z ≈ newest sale 03:02Z ≈ newest FMV 02:54Z). Lock RELEASED (15:40Z, night-20260718-22284). Not first-tick-of-day (prior ticks 21:06Z, 00:10Z) → section 1a daily extras skipped; trust health came through `rpc_ops_snapshot()` regardless and is clean.

**Health: GREEN. No breakage.** security 0/0/0/0 (re-verified at 03:06:29Z, i.e. AFTER the 02:45–03:02Z DDL wave) · trust **15/15 ok, 0 breaches** · pg_cron failures [] · invalid indexes 0 · sentinel TS-UUID-48h 0 · DB **10,010 MB** · Sentry 3 unresolved, **none newer than ~8h and all already logged** · Vercel no ERROR, prod `67b46fb4` READY (2 deploys BUILDING — Trevor pushing live).

Both trust metrics the 07-18 overnight pass queued as breaching remain cleared: `topshot_impossible_parallel_serials` **0** (breach 3), `pinnacle_fmv_stale_hours` **4.5** (breach 30).

**Concurrent-session note:** a Claude Code session is actively shipping right now — **10 migrations since the 00:10Z tick**, 7 of them in the last ~45 min (`scb_claim_topshot_scope`, `scb_claim_widen_allday_ufc`, 4× `candy_*`, `external_pack_drops_*`, `panini_coverage_audit_view`), plus 2 in-flight builds. The night pass should re-read the ledger before acting; much of tonight's surface is <24h old.

---

## Candidates

### 1. CI-STATUS-IS-NOT-IN-ANY-AUTOMATED-SWEEP — MEDIUM (real monitoring gap)

**Source:** commit `67b46fb4` (02:20Z, Trevor): *"CI: the unit-test job has been failing on main since the 07-18 wave. The seed-wallet 12h cadence gate and the pinnacle-sync `after()` conversion both changed route contracts without updating tests."*

**The finding is the blind spot, not the red build.** The build is already fixed (57/57 across the 6 affected suites, `tsc --noEmit` clean). What matters is that `main`'s `unit-tests` job was red for roughly a day and **no automated pass noticed** — not this monitor, not the 07-18 overnight pass, not the 21:06Z or 00:10Z ticks. Reviewing the monitor's own health sweep: it covers `pipeline_runs`, `detect_stalled_pipelines()`, `check_pgcron_recent_failures()`, security catalog, Sentry, Vercel deploys and artifacts — **there is no CI check anywhere in it**, even though the harvest section names "failing CI" as a candidate source.

**Why it's more than cosmetic:** the nightly autonomous pass is CI/typecheck-gated. A silently-red `main` either blocks its ships or, worse, gets reasoned around — and the two commits that reddened it (`4c631412` seed-wallet gate, `8ceca936` pinnacle-sync `after()`) are exactly the kind of route-contract change the pass is meant to verify. Vercel stayed green throughout because Vercel builds don't run the vitest job, so the existing deploy check cannot substitute.

**Caveat — not independently verified this run.** The GitHub Actions API is outside this session's fetch provenance set (blocked URL: `api.github.com/repos/jamesdillonbond/rip-packs-city/actions/workflows/ci.yml/runs`). The red-since-07-18 claim rests on the fix commit's own message. Whoever picks this up should confirm against the Actions history.

**Risk read:** LOW risk, MEDIUM–HIGH value. Adding a read-only CI-status check is additive and touches no production surface.

**Suggested action:** add a GitHub Actions status read for `ci.yml` on `main` to the monitor + night-pass health sweeps (latest conclusion per job, and how long any failure has persisted). If the GH connector isn't reachable from the sweep, the cheap fallback is a repo-side workflow that writes its conclusion into `pipeline_runs` so it lands in the existing `pipeline_fails_24h` signal for free.

---

### 2. POST-SHIP WATCH — `sales-counterparty-backfill`: **PASS**, and better than the ledger's own numbers

First independent watch since the worker went live (~23:00Z). Not a problem — recording it because the ledger's throughput/bloat concerns are now measurable and both read favourably.

**Throughput (pipeline_runs, `*/5` cadence, 120 rows/tick ⇒ 1,440/hr ceiling):**

| Hour (UTC) | Ticks | ok | Recovered |
|---|---|---|---|
| 23:00 | 1 | 1 | 120 |
| 00:00 | 12 | 12 | 791 |
| 01:00 | 12 | 11 | 544 |
| 02:00 | 12 | 12 | **1,439** |
| 03:00 | 2 | 2 | 239 |

02:00Z landed **1,439 of a possible 1,440 ≈ 100% recovery** — against the ledger's documented pre-fix "erratic 1–98%". The `CONCURRENCY=3` + retry-pass throttle fix (`f4f476fa`) and the AllDay/UFC widening (`68e161e0` / `audit_20260719_scb_claim_widen_allday_ufc`) are both holding in prod. Audit table 3,392 rows, cursor descended to 2026-07-17T14:03Z.

**The IOPS/bloat concern the ledger raised is not materialising.** A ~2.3M-row UPDATE campaign on the hottest table is the right thing to watch, so I measured it: dead-tuple ratios across every `sales` partition are **sales_2026 1.91% · sales_2025 1.33% · sales_2022 0.96% · sales_2024 0.86% · sales_2023 0.06% · sales_2021 0.02%**. Autovacuum is keeping up. Worth re-measuring in a few days as the cursor descends into the big historical partitions, but there is no bloat problem today.

**One fail, benign:** 01:00Z `claim failed: canceling statement due to statement timeout` — contention-class, self-healing by construction (the row stays NULL and is re-claimed). Folds into candidate 3.

**Verdict: PASS, no revert.** Watch item for the next pass: whether ≈100%/hr holds once the cursor reaches the pre-2024 partitions.

---

### 3. PACK-EV CONTENTION FAILS STILL CREEPING — LOW (watch, tracks the non-wave-driver item)

`compute-topshot-pack-ev` **9 fails/24h** — 3 (two days ago) → 8 (00:10Z tick) → 9 now. Same signature throughout: `targets: canceling statement due to statement timeout`. `fmv-recalc` steady at 5. Latest runs on both are **ok**, and there is no data harm: `pack_ev_board_max_stale_days` 0.28 (breach 2), `topshot_fmv_stale_hours` 0.3, `allday_fmv_stale_hours` 0.2.

Not worth its own fix — it is the *metric* for the already-queued **NON-WAVE-WALLET-BACKFILL-DRIVER** item (`lib/allow-list/prewarm.ts` remains the lead). Re-stating only that the trend is still upward, so it is a fair before/after gauge when that item is worked.

---

### 4. DB CROSSED 10 GB — LOW (watch, no action)

**10,010 MB**, first time over 10 GB (9,878 at the 07-18 overnight pass, 9,994 at the 00:10Z tick ⇒ ~+130 MB/day). No single pathological table; growth is spread across the counterparty audit table, the new candy/panini/external-pack-drops scaffolds, and normal `::` edition cataloging (TS editions 19,451). Recording the threshold crossing so the trend has a datum, not proposing action. Relevant mainly because the instance is IOPS-constrained and disk pressure is the standing theme.

---

### 5. PINNACLE-SYNC — CARRIED, unchanged, but the decisive tick is ~7h out

Silent **2,459 min (41h)**, last run 2026-07-17T10:07Z; missed the 07-18 10:07Z tick (cron-job.org dropout class). FMV half remains healthy on the 07-18 pg_cron backstop (`pinnacle_fmv_stale_hours` 4.5, `pinnacle_render_floor_stale_hours` 1.3); **catalog half still unbackstopped**. Already in the 21:06Z + 00:10Z inboxes and the ledger — no new information this tick. **The 07-19 10:07Z tick is the one that matters:** a second consecutive miss means it is not self-healing and the catalog half needs the same backstop treatment the FMV half got.

---

## Post-ship watch — tonight's DDL wave

| Ship | Verdict |
|---|---|
| `external_pack_drops_scaffold` + `_upsert_and_score` (02:58/02:59Z) | **PASS** — table created **with** RLS on + 1 policy + **0** anon/authenticated write grants, in the same migration. Notably this is the correct version of the pattern the 00:10Z **SECURITY-SMOKE-RLS-TRANSIENT-1809Z** candidate flagged during the 18:0xZ Panini wave (create-then-normalize-5-min-later). Evidence the gap is not systemic — but that candidate should still be closed on its own merits, not on this. |
| `candy_wmc_purge_ghost_owner_rows` + `_selfheal` (02:48Z) | **PASS** — `candy_mlb` wmc = **25,375**, exactly the expected serial count from Item-0 discovery. Purge hit its intended scope; no collateral. Other collections intact (TS 1,608,798 · AllDay 333,115 · Pinnacle 44,368 · UFC 4,571 · Golazos 44). |
| `scb_claim_topshot_scope` → `scb_claim_widen_allday_ufc` (01:50Z → 02:14Z) | **PASS** — see candidate 2. Both fns present, security invariants clean after. |
| `audit_20260718_get_topshot_sniper_deals_add_subedition_name` (00:22Z, DROP+CREATE) | **PASS** — fn present, `secdef_anon_violations` []. The re-grant after the forced DROP+CREATE held. |

Security invariants (`invariants` / `secdef_anon_violations` / `rls_off_base_tables` / `anon_write_holes`) all **[]** at 03:06:29Z, after every migration above.

---

## Validation notes

**Artifacts — inferred, not directly replayed (same constraint as the 00:10Z tick).** `list_artifacts` returns 17, but the artifact directory (`C:\Users\TDill\Claude\Artifacts`) is outside this session's connected folders, so the embedded payload queries cannot be read and replayed. Validated the failure mode that section exists to catch — a same-day schema change silently breaking a dashboard — by exercising every object tonight's DDL touched plus the shared dashboard surfaces: `pack_ev_latest` 1,824 · `funnel_events` 893 (24 in 24h) · `cross_collection_cohort_mat` 154 rows @ 07-18 04:10Z (23h, inside the 26h bar; next tick 04:10Z) · 8 panini views · all 6 key fns present · `v_rpc_trust_health` 0 non-ok. **No artifact flagged broken.** Standing ask, third tick running: making the artifact dir a connected folder would convert this from inferred to actually executed.

**Sentry — nothing new.** All 3 unresolved issues are pre-existing and already captured: `JAVASCRIPT-NEXTJS-1C` (security smoke, last seen ~8h, i.e. has NOT re-fired since the 18:0xZ Panini window — see candidate 1 of the 00:10Z inbox), `JAVASCRIPT-NEXTJS-1X` (edition-page Activity smoke, ~11h, the documented streamed-Suspense-body class), `JAVASCRIPT-NEXTJS-1Z` (pack-detail pool exhaustion, ~11h, already logged). Zero new issue classes since the 00:10Z tick.
