# Daytime monitor inbox — 2026-06-13T16:05Z (first-tick-of-day)

PLATFORM GREEN. First FULL daytime window on the new Small compute tier is clean — decisive confirmation DBSAT-IO-EXHAUSTION-0612 stays resolved. Hourly 06Z-15Z: 08/09/10/11/15Z = 0 fails; waves 07Z 1507/3 (0.2%), 13Z 1512/4 (0.3%); 12Z 731/1; 14Z 244/1 — vs 06-12's run-count collapse + blackout. Security 4/4 clean, trust 8/8 ok, detect_stalled [], Sentry 0 new, Vercel 5/5 READY 0 ERROR, sentinel UUID-leak 48h = 0, cross-collection pg_cron both steps succeeded today (04:10/04:25Z). 12 active artifacts validated (rpc-live-health + rpc-traction payloads run clean; 5 RETIRED tombstones skipped). CC 06-13 ships all PASS: buyer-backfill 36/0 (10,800 rows/6h draining), ufc-drain null_key=2 floor, cohort split waves clean.

## Candidate work

### LOW — ALLDAY-V1-UNMAPPED-DRIFT: AllDay V1-Dapper price-uncertain unmapped_sales slowly accumulating
- Source: `unmapped_sales` — nfl_all_day 237 open (+23 in last 24h, oldest 2026-05-21); recent rows `sale_source=v1_dapper` with a `price_extraction` failure in resolution_hint. `allday-unmapped-resolver` 71 runs / 0 fails / resolved only 1 row in 24h; `promote_unmapped_sales` 215/0 / resolved 0 (correctly skips price_usd=0 via the COALESCE>0 guard). Backlog trend ~183 (06-12) -> 215 (08:14Z) -> 238 (16:00Z).
- Risk read: LOW. No failures, no outage. These rows DO carry an edition_id but an uncertain V1 price, so they're correctly held out of `sales` (not corrupting FMV/analytics). The issue is a steady ~+23/day accumulation = an AllDay sales-completeness gap (those native sales never get a recorded price) that also inflates the unmapped-backlog metric.
- Suggested action (investigate-only, no hot-path change): confirm whether `/api/admin/recover-v1-budget-exhausted` (the V1 tx re-decode path) has any automated drain; if not, decide between (a) wiring it on a low cadence to clear the price-uncertain tail, or (b) formally classifying the V1 price-uncertain rows as a known permanent residual so they stop inflating the backlog signal.

## Notes (already tracked / not new — no action requested)
- `offer_edition_gap_max_usd` $45 vs $50 breach (trust-health still ok) — near threshold, single-offer driven; OFFER-SANITY-RAISE is resolved + monitored (focus 12a). Watch only.
- DB 4503 MB (+48 vs 08:14Z), mild creep; pipeline_runs unpruned since 06-07; ~06-14 04:23Z weekly-db-maintenance self-fire should prune (verify next pass per metrics note).
- TFP :15-slot statement-timeout fails persist (wave-coincident) — TFP-SLOT-WAVE-COLLISION already queued by the 06-13 night pass; not re-raised.
