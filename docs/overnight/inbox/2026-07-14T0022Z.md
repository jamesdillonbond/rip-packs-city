# RPC daytime monitor — candidate inbox 2026-07-14T00:22Z

Run context: late-day tick (DB now() 00:13Z ≈ 17:13 PDT Jul 13 — NOT the ~8am first-tick, 1a extras skipped). **Bash/git sandbox RECOVERED this run** (clone + push both work — the 07-12/07-13 `useradd exit 12` provisioning failure did NOT recur; the 07-13 daytime monitor's push-unavailable condition is cleared this tick), so this inbox file is committed + pushed normally. Concurrency lock RELEASED (08:12Z, stale) → inbox write permitted.

Health **GREEN**: security 0/0/0/0 (invariants / anon-write / rls-off-base / secdef-anon all `[]`); trust 16/16 ok, breaches `[]`; `detect_stalled_pipelines()` `[]`; sentinel TS-UUID-48h 0; Sentry 0 unresolved production/24h; Vercel prod newest READY `dpl_9wEwCVsK` (`fd785c25`, log_pipeline_run 3-arg overload) — the newer `51c70fc` is a CANCELED docs-only commit, NO ERROR-state deploy. Editions TS 19,284 (+43 `::` since overnight, sentinel 0 = no UUID leak) / AllDay 6,190 / Golazos 575 / UFC 518. FMV TS H+M 5,204 / AllDay 808 / UFC 15 / Golazos 4 (stable–improving). **DB 8,771 MB — down ~2.3 GB from the overnight 11,044** (tonight's off-peak reindex campaign reclaim, see below; favorable). Artifacts validated live via backing RPCs: rpc-set-challenge-roi → `get_active_challenges` returns **31** challenges (healthy); pack-EV views read fine post-`security_invoker` change (`pack_ev_latest` 1816 / `pack_table_rows` 5439 / `topshot_pack_ev_targets` 823); `v_rpc_trust_health` 16; `get_pipeline_alerts` 2 INFO (golazos/ufc resolving_editions, benign).

NOT re-logged (already queued/logged today): misattrib-candidates 03:25Z MV-refresh timeout (already in the 07-13 T0625Z + T18-07Z files; ALSO already fixed today — rescheduled 03:25Z→15:35Z off-peak per commit `51c70fc`, so its 03:25Z failure is a STALE pre-fix timestamp, do NOT alarm); special-serial-owners-mv / remap contention (T0625Z); BASH/GIT-SANDBOX-PROVISION-FAILURE (recovered this run — note for reconciliation, below).

## Candidates

### 1. ORPHANED first-gen whole-table REINDEX cron jobs (99 + 100) fail nightly — not cleaned by the campaign's finalize (LOW, DB-only, recurring)
- **Source:** `check_pgcron_recent_failures()` + `cron.job` / `cron.job_run_details`, measured 00:13Z.
- **What happened:** today's Cowork stabilization session ran an index-bloat cleanup campaign. It FIRST scheduled two **whole-table** `REINDEX TABLE CONCURRENTLY` jobs — `rpc_ox_reindex_wmc2` (jobid **99**, `13 23 * * *`, on `wallet_moments_cache`) and `rpc_ox_reindex_pack_rips2` (jobid **100**, `20 23 * * *`, on `pack_rips`) — which both **FAILED tonight**: 99 → `statement timeout` at 23:23Z (10 min), 100 → `canceling statement due to user request` at 23:28Z. It then SUPERSEDED them with a granular per-index generation `rpc_ox2_*` (jobids 110–127, staggered 23:36Z→00:53Z) that is **succeeding** — all 5 pack_rips indexes + 4/5 wmc indexes already `REINDEX`ed (walletcoll running at 00:12Z; sales partitions / fmv_snapshots_2026 / moment_acquisitions upcoming). This is what reclaimed the ~2.3 GB.
- **The gap:** the campaign's `rpc_ox2_finalize` (jobid 127, `53 0 * * *`) RESETs `statement_timeout`, drops leftover invalid `%ccnew%` indexes, and unschedules the whole batch — but its pattern is `rpc\_ox2\_%`, which does **NOT** match jobs 99/100 (named `rpc_ox_reindex_*`). So after tonight's finalize, jobs 99 + 100 stay `active=true` on their daily schedules and will **re-fire and re-fail every night at 23:13Z / 23:20Z** — redundant with the granular batch that already covers both tables. (No bloat leak right now: current invalid-index scan = `[]`, so the failed CONCURRENTLY runs left nothing behind; finalize's DROP-loop would catch any that appear.)
- **Risk read:** LOW. Purely two redundant nightly pg_cron failures (they'll trip `check_pgcron_recent_failures()` every night); no data risk (CONCURRENTLY, off-peak), no user surface. Only downside is recurring noise + wasted 10-min reindex work each night.
- **Suggested action:** night pass / operator — unschedule the two orphaned jobs: `SELECT cron.unschedule(99); SELECT cron.unschedule(100);` (or by jobname `rpc_ox_reindex_wmc2` / `rpc_ox_reindex_pack_rips2`). Also confirm INTENT of the `rpc_ox2_*` batch: those 18 jobs (110–127) carry daily `* * *` schedules but `rpc_ox2_finalize` self-unschedules them at tonight's 00:53Z, so the campaign is a **one-shot tonight** — verify finalize ran clean (00:53Z) and the batch is gone by tomorrow; if the intent was recurring maintenance, that's a separate decision. Reconciles with WMC-INDEX-BLOAT-SECONDARY (below).

## Reconciliation notes (not new candidates — for the night pass to CLOSE)

- **TOPSHOT-IMPOSSIBLE-PARALLEL-SERIALS-BREACH (the 07-13 T18-07Z candidate #1, 18/3) SELF-HEALED → 0.** `trust_health` `topshot_impossible_parallel_serials` = **0** this tick (was 18 at 11:07 PDT). The per-parallel circ backfill reconciled the freshly-cataloged `::` stragglers, exactly as that candidate predicted. No action — close it.
- **WMC-INDEX-BLOAT-SECONDARY (queued, night-count 2) appears RESOLVED via DROP.** The specific bloated index `idx_wmc_lower_wallet_coll_edkey` (~339 MB / ~29 scans) is **no longer present** in `pg_stat_user_indexes` — the queued "REINDEX or DROP" looks actioned as a DROP during today's stabilization session (part of the ~2.3 GB reclaim). Night pass: verify + close (confirm no planner regression on wmc edition-key lookups).
- **BASH/GIT-SANDBOX-PROVISION-FAILURE (queued, escalating 2-night) — RECOVERED this tick.** The sandbox VM provisioned cleanly this run (clone + push both succeeded); the `useradd exit 12` failure that blocked the 07-12 + 07-13 overnight passes and the 07-13 daytime monitor did NOT recur. May still be intermittent (infra) — leave queued for one more clean cycle before closing, but note the recovery.
