# Daytime monitor — 2026-06-12T18:20Z

Run context: ~3h after the MONITOR-MODE night pass (13:49Z) that caught DBSAT-IO-EXHAUSTION-0612 live. Lock RELEASED 14:42Z. Sweep window 11:00–18:15Z.

## Headline: DBSAT-IO-EXHAUSTION-0612 RECOVERED ~15:30Z
pipeline_runs fails/hr: 13Z=54/181, 14Z=41/48 (blackout peak) -> 15Z=4/165, 16Z=1/237, 17Z=0/240, 18Z=0/22. Telemetry volume fully restored. fmv-recalc ok 17:48Z; offers-sweep / allday-fmv-populate / pinnacle-listings-reconcile / topshot-sales-indexer / wmc-fmv-populate all green on last ticks. Window ran ~07:00-15:30Z — longest of the 3 consecutive days; datapoint for the compute/IO-budget upgrade decision (operator morning item #1 in the night-pass handoff).

## Candidates (new/updated)

### 1. VERCEL-DEPLOY-ERROR-X4 — canary = THIS commit's deploy (MED-HIGH, night pass / operator)
Source: Vercel prod deploys for 3ffdd54 (~14:17Z), cccd744 (~14:28Z), 4b75dd3 (~14:46Z), f3128d5 (~15:13Z) — 4 consecutive ERRORs, ALL docs-only commits. Build log (f3128d5, dpl_DMNYPwXSAooheJWQfpHGd24GUYQs): "Compiled successfully 9.5s" -> Sentry sourcemap upload -> silent ERROR — the known invisible-failure class (CLAUDE.md Vercel section). Window coincides exactly with peak DB saturation; 06-05 precedent of transient build-infra ERRORs. Prod safe on 46500e4 (READY, rollback candidate). No deploy attempted since 15:13Z.
Action: this monitor run's push fires a fresh deploy on a calm DB — if READY, close as incident-coupled; if ERROR again, main is genuinely undeployable -> escalate operator/CC immediately (read the build log PAST the sourcemap step; suspect the maxDuration>800 invisible class or turbopack cache).
Risk read: prod serving unaffected; blocks all shipping until resolved.

### 2. TFP-DOWN-AGAIN — topshot-fmv-populate silent 17h (HIGH, operator)
Source: detect_stalled_pipelines() = [{topshot-fmv-populate, silent 1012m vs 480m, last ok 2026-06-12T01:15:25Z}]. The 01:15Z tick was the round-cast GREEN verdict; zero runs since — same cron-job.org entry-failure class as 06-09/06-11 (TFP-SLOT-MOVE-FAILED, ledger). Saturation cannot explain the 15:30Z->18:15Z silence (DB calm, sibling crons firing).
Action: operator — cron-job.org console per the 06-09 recovery recipe; verify the entry is active with a sane next-execution. Watchlist already back at 480m (correct; do not raise).
Risk read: TS marketplace-FMV freshness degrades; bounded — fmv-recalc covers the sales path.

### 3. UFC-WMC-NULLKEY verify REGRESSING — b28a22f post-ship metric moving the wrong way (MED, night pass re-verify)
Source: wmc UFC null edition_key = 3,565/4,584 vs 3,150/4,584 at ship (~03:38Z). Expected to FALL via the 6h seed-refresh self-heal; instead +415 in ~15h. Most likely the IO incident starved the after() enrichment chains all day while ID-walks kept inserting ID-only rows (the original starvation mechanism). Cannot yet call the fix failed.
Action: night pass — re-measure on a calm DB after >=2 post-recovery seed-refresh cycles (~06:00Z 06-13). Falling -> keep watching to 0. Still rising -> b28a22f's maxDuration lever is not reaching enrichment -> escalate CC (docs/handoff-ufc-enrichment-decoupled-cron is the ready alternative).
Risk read: read-only verify; no route action.

### 4. LISTCACHE-SILENT-0612 — partial recovery, cadence still thin (LOW-MED, watch)
Source: topshot-listing-cache last ok 16:02Z (recovered from the 00:19Z silence the night pass flagged) but only 1 run in the last 2h vs ~20m historical cadence; under its 360m watchlist threshold so detect_stalled stays quiet.
Action: if the next sweep still shows >=2h gaps, operator re-fires/inspects the cron entry (or accept the new cadence and record it in docs/operations/cron-schedule.md).

### 5. Sentry smoke cluster — in-window, resolve-after-quiet (LOW, night pass routine)
11 unresolved, all known: 9 = smoke-test cluster last seen ~15:1xZ inside the saturation window (NEXTJS-W/E/A/13/14/1C/1D/1E/1H), NEXTJS-15 (pinnacle listing_resolution, 2 ev), NEXTJS-1P (auth-token lock steal on /:collection/sniper, 2 ev / 1 user, new 19h ago — watch-only). Zero NEW post-recovery events. Action: standard resolve-with-regression-arming after 24h quiet.

## Health summary (rest green)
Security 0/0 (RLS-off=0; anon/authenticated write-on-RLS-off=0). Sentinel TS-UUID-leak-48h = 0; ts_uuid_dupes_created_24h = 0. Trust health 8/8 ok (pinnacle ask 0.0h, pin FMV 8.0h, fmv_sanity 0, offer gap 0, pack-EV stale 1.64d). DB 4,321 MB (baseline 4,311 — flat). FMV latest-per-edition: TS HIGH+MED 3,242 (carry 3,226, rising), NO_DATA 4,381 (falling), AllDay HIGH+MED 643 (601, rising) — both improved THROUGH the incident. Prod = 46500e4 READY.

## Artifact validation
17 artifacts enumerated. Spot-validated the historically fragile ones: v_tracked_wallet_fmv_confidence (AF1 / rpc-tracked-fmv-confidence) returns 21 rows OK on calm DB; trust-health + freshness legs all ok; pipeline/deploy queries succeed. Did not run all 17 query sets this run (incident follow-up prioritized); no artifact errors observed.

## Monitor-run notes
- Sandbox quirk hit twice this run: files/dirs created by a PREVIOUS bash invocation are permission-denied to later invocations (clone at /tmp/rpc unusable for commit; also blocks rm). Workaround: do clone+write+commit+push in a single invocation. Worth adding to the task prompts if it recurs.
