# Daytime monitor candidates — 2026-08-10T15:09Z (first PT tick, ~08:09 PDT)

Source: rpc-daytime-monitor sweep. **Written to the MOUNT — bash/git sandbox is down (`useradd … /sessions no space left on device`, the documented Cowork-shell failure, now 3rd+ consecutive night per the .lock release note), so no clone/push. The night pass picks this up locally.**

Sweep otherwise clean-with-known-noise: security 4/4 invariant arrays `[]`; latest prod deploy READY (`601e82d95` D20 set-merge fix), no ERROR (the several CANCELED states above it are rapid-push supersessions, not build failures); DB 12,401 MB. Trust health 35/38 ok; the 3 breaches all known/tracked: `panini_sale_price_capture_dry_days`=13 (upstream home-box outage), `public_board_slow_count`=5 (crawler/saturation class), `unmapped_resolution_backlog_max`=194 (AllDay floor; info alert confirms live inflow 62/24h vs outflow 1345/24h + 4094 historical-backfill arrivals, ~35d to clear).

**An active disk-IO statement-timeout cluster is running right now** (6 pg_cron jobs timing out: `rpc-ccm-step2` 04:25Z, `rpc-refresh-allday-pack-realized` 3/4, `rpc-refresh-misattrib-candidates`, `rpc-trust-health-precompute-refresh` 12:58Z [recovered — `trust_precompute_max_age_hours`=8.1 ok], `rpc-thin-sale-ask-disclosure-refresh` 09:25Z, `rpc-reconcile-saved-wallet-stats` 13:33Z). **All already queued and NOT re-logged** — covered by `inbox/2026-08-10T0555Z-cron-budget-headroom-audit.md`, `0620Z-headroom-audit-corrections.md`, and `1430Z-reconcile-saved-wallet-stats-profile.md` (jobid 259 refuted twice — do not re-raise — + jobid 261). Cross-collection **step2 stale a full day** (`cross_collection_ts_set_overlap_mat` max 2026-08-09 04:25Z; step1 fresh, cohort 179 rows) is the recurring saturation casualty already noted in `0612Z.md` and self-recovers on tomorrow's 04:25Z tick unless a manual one-shot is run — night-pass/Trevor call, not mine.

Two items below are NEW since the 0612Z + 1430Z files.

---

## 1. (MEDIUM) New user-facing Sentry: edition pages timing out on connection-pool acquire

- **Source:** Sentry `JAVASCRIPT-NEXTJS-26` — "edition detail unavailable: Timed out acquiring connection from connection pool." Culprit `GET /[collection]/edition/[slug]`. **8 events / 1 user, first seen ~5h ago (≈10:00Z), last seen ~4h ago.** Not in `ledger.md` or any existing inbox file.
- **Context:** the documented slug-keyed-entity-lookup + pool-acquire-timeout class (CLAUDE.md "A pool-acquire timeout is a SATURATION symptom" + Sentry NEXTJS-1Y/-20 team/player siblings). This is the **user-facing** consequence of today's disk-IO saturation window — distinct from the internal cron timeouts already queued, because it renders a broken edition page to a visitor.
- **Risk read:** MEDIUM. Live public surface, but low volume (8 events, 1 user) and it stopped ~4h ago — tracks the saturation window rather than a code regression. Same root cause (disk-IO budget depletion, 512 MB shared_buffers on the Small tier) as the queued cron cluster; no independent fix.
- **Suggested action (night pass):** no new code lever beyond what's already queued (the precompute/caching direction on the saturation cluster). Confirm it does not persist outside saturation windows; if it recurs on a quiet instance, the slug-lookup expression-index coverage (per the CLAUDE.md entity-page note) is the lever, not a tier bump.

## 2. (LOW) `pinnacle-sync` missed its daily tick — cron_silent ~29h, downstream FMV healthy

- **Source:** `get_pipeline_alerts()` / `detect_stalled_pipelines()` — `pinnacle-sync` last ran **2026-08-09 10:07Z**, silent 1739 min vs a 1560-min threshold (daily 10:07Z Pinnacle catalog+FMV sync, watchlisted 2026-06-09). Today's ~10:07Z tick did not land. Not a pg_cron job (`cron.job_run_details` empty for it → externally triggered, cron-job.org / route). Not in the 0612Z or 1430Z files.
- **Risk read:** LOW. Pinnacle FMV is **NOT stale** — `pinnacle_fmv_stale_hours`=16.5 (ok, breach 30), `pinnacle_render_floor_stale_hours`=1.3, `pinnacle_fmv_pct_stale_30d`=0 — because Pinnacle FMV is computed by the separate `pinnacle-fmv-recalc` / `catalog-floor-refresh` pg_cron jobs (per the D13 finding), not by this sync. So one missed catalog-sync tick has no visible user impact yet.
- **Suggested action (night pass / Trevor):** verify against the ledger first (pinnacle-sync has prior finicky history — the retired `/api/cron/pinnacle-sync` 401 entry `e719e5e5`, and the 08-08 `/api/admin/backfill-pinnacle-catalog` Vercel backstop at `37 21 * * *`). If the cron-job.org 10:07Z entry silently dropped a tick (the documented GHA/cron-job.org drop class), confirm the Vercel backstop is covering catalog freshness; if the sync is genuinely dead, re-point it. Do not escalate on one missed tick with healthy FMV.
