# Daytime monitor candidates — 2026-07-13 ~06:25Z (last/~23:25 PT tick)

Run context: last tick of the day (~23:25 PT). Health GREEN — security 0/0/0/0 (invariants/secdef-anon/rls-off/anon-write-holes all []), trust 16/16 ok breaches [], `detect_stalled_pipelines()` [], sentinel TS-UUID-48h 0, Sentry 0 unresolved/24h, prod `dpl_9ePT` (2d57889f) READY with NO ERROR-state deploy, editions TS 19,241 (+31 `::` subedition, benign)/AllDay 6,190/Golazos 575/UFC 518, DB 11,026 MB (stable). Artifacts spot-validated OK (rpc-tracked-fmv-confidence payload, rpc-live-health 15/15 schema deps, rpc-set-challenge-roi `get_active_challenges` → 31). **Peak overnight-contention window active (~00:23–06:13Z):** `rpc_ops_snapshot()` and the full `v_rpc_trust_health` both timed out for the monitor (pieces gathered individually; metric+status trust scan succeeded, all ok). **BASH/GIT sandbox still down (useradd exit 12, same failure the 07-12 night pass hit) → inbox written to mount, push unavailable.**

## Candidate 1 (LOW–MED, DB-only, sharpens carried items) — pg_cron misattrib/special-serial MV-refresh timeouts: post-cron_heavy-fix delta
- **Source:** `check_pgcron_recent_failures()` + `cron.job_run_details` durations/roles, measured 2026-07-13 06:08Z.
- **Finding** (three jobs, all internal data-quality MV/remap helpers — none FMV/ingest/auth/user-facing):
  - `rpc-refresh-special-serial-owners-mv` — ran **120.0s** as role **postgres**, timed out 04:13Z on `REFRESH MV CONCURRENTLY allday_special_serial_owners_mv`. It was **NOT** included in the 07-12 `cron_heavy` migration (15 jobs moved to the 600s role); it's still on the 120s cluster default while its siblings got 600s. Schedule `13 4,16` (2×/day); succeeded 07-12 04:13Z + 16:13Z, so it's intermittent-under-contention, not persistently broken.
  - `rpc-refresh-misattrib-candidates` — ran the **full 600.0s** as **cron_heavy** and still timed out 03:25Z on `REFRESH MV mv_topshot_misattrib_candidates`. Single daily run (`25 3`), 1/1 failed today → that MV is now ~1 day stale. Genuinely exceeds even the raised 600s ceiling.
  - `rpc-remap-misattributed-sales` — **600.0s** cron_heavy, timed out the 00:23Z overnight tick (succeeded 18:23Z off-peak). The carried DAYTIME-CONTENTION item; exceeds 600s only in the overnight-backfill peak.
- **Risk read:** low. All three are internal misattribution / special-serial-owner helpers; only impact is ≤1-day MV staleness, and off-peak ticks pass. This **refines** the carried `REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT` + `DAYTIME-CONTENTION` items with fresh post-fix data — reconcile, don't double-count / don't re-alarm.
- **Suggested action (night pass / CC):** (a) cheapest lever — move `rpc-refresh-special-serial-owners-mv` onto the `cron_heavy` role (600s) like its siblings the 07-12 fix left behind; it's a CONCURRENTLY refresh, so verify no long-lock interaction. (b) For `misattrib-candidates` + `remap` now hitting the 600s ceiling, a further timeout raise risks prolonging contention (per the carried DAYTIME note) — prefer optimizing the MV/remap scan or shifting the 03:25/00:23 schedules out of the overnight-backfill peak. Monitor made no change (read-only).

## Context (not a separate candidate) — 06:00–06:08Z pg_cron worker "job startup timeout" cluster
`rpc-backfill-pack-pool`, `rpc-pinnacle-mints-backfill`, `rpc-allday-badge-low-ask-refresh`, `rpc-roll-pack-ask-hourly-low` each hit ONE **"job startup timeout"** (pg_cron couldn't spawn a bg worker) at the top of the 06:00 hour; all have high run counts (29–288/window), 1–2 fails, and succeeded every other tick (allday-badge verified succeeding every 30-min tick 16:00Z→05:30Z, single miss at 06:00Z). = pg_cron worker-pool saturation at the peak-contention window, self-healing next tick. Distinct from statement timeouts (the 600s role fix can't touch a *startup* timeout). Symptom of the DAYTIME/overnight-CONTENTION family, not a new incident.

## Minor note (doc/ledger reconciliation, no breakage) — `v_set_challenge_roi` absent vs commit claim
Commit `d2acff6d` (07-13 challenges-unify) states it "repointed `v_set_challenge_roi` … so the Cowork artifact is unaffected (verified 31 rows)", but `to_regclass('public.v_set_challenge_roi')` is NULL and no relation matching `set_challenge`/`challenge_roi` exists. **Harmless** — the live `rpc-set-challenge-roi` artifact reads `get_active_challenges(NULL,<TS>)` (validated healthy, 31 challenges), NOT the view; nothing consumes `v_set_challenge_roi`. Likely dropped by the later VARIABLE-slot rebuild (`56152f47`). Night pass: optionally reconcile the commit/ledger claim; no fix needed.
