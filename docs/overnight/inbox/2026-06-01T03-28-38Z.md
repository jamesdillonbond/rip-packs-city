# Daytime monitor — candidate inbox · 2026-06-01T03:28Z

Run: `rpc-daytime-monitor` (Cowork). Repo: shared `rip-packs-city` (no bot clone reachable — `rip-packs-city-bot` absent from the sandbox mount, per ledger Q7). Baseline: night-pass `metrics-latest.json` @ 2026-05-31T08:10Z. Lock: `.lock` present but ~19h stale (08:18Z marker) — treated as STALE per the <45-min rule; inbox commit permitted (lock not created/removed by this run).

**Health this run: ✓ healthy.** `detect_stalled_pipelines()` = `[]` (no stall). Security 0/0/0 — RLS-off public base tables 0; anon/auth write-grants on RLS-off base tables 0; anon EXECUTE on destructive SECDEF fns 0 (the 2 heuristic hits — `mcp_find_set_completion` read-only concierge lookup + `pack_purchases_set_is_primary_drop` trigger fn — are benign "set_" name matches, not callables). Sentinel TS-UUID-48h **190** (↓ from 311 @ 00:15, ↓ from 1099 baseline; under the 250 ok-line). FMV writers fresh on all 4 collections (<16m). TS FMV HIGH+MED **852** (↑ from 776 baseline / 831 @ 00:15), NO_DATA **5249** (↓ from 6055 / 5325), ASK_ONLY 687 (stable — intended `65421e26` behavior). AllDay HIGH+MED 262 (↑ from 243). Editions/DB normal (TS 16,293; AllDay 6,191; Golazos 581; UFC 446; DB 5,889 MB). unmapped_sales 147 open (144 baseline; 3 new/24h). Vercel: 20/20 recent prod deploys READY, **zero ERROR** (heavy ship day continues — team-hub Phases 1–5, `/my-teams`, `/share` wallet-intel, `/insights/market`, badge-sync on-chain-key fixes `0a8c5db`→`5bd3cd6`, FMV ask-over-WAP `65421e26`, funnel-track `await` fix `2339f30`). Artifacts: 10 enumerated, all backing objects validated (8 insights views resolve, `get_rpc_traction_snapshot()` executes, security/FMV/pipeline/Vercel/DB queries all ran clean this sweep) — no broken artifact.

All ok=false `pipeline_runs` in the window are transient with verified recovery: `evm-transfers-ingest` Base-429 @ 03:00Z (known **Q6**; succeeded 02:00Z, hourly, self-heals next tick); the 00:00–00:09Z midnight-cron-rush cluster — `pack-events-ingest` (00:09Z), `wmc-fmv-populate` (00:04Z), `pack-events-ingest-backfill` (00:01Z) — all pool/upstream timeouts, all back to ok=true within 1 tick (verified ok through 03:16–03:24Z). TS + AllDay sales-indexers healthy on ~20min cadence (Q3 stall stays resolved).

---

## P3 (LOW–MED · operator/CC — NOT auto-ship; touches smoke-test route + schedule) · Smoke suite false-REDs during the :00 / :06 cron-rush from DB connection-pool exhaustion (cry-wolf on security/FMV gates)

- **Source:** Sentry `JAVASCRIPT-NEXTJS-1D` (anon-SECDEF), `-14` (Pinnacle-FMV drift guard), `-12` (pricing page renders), `-A` (fmv pipeline healthy), `-4` (market API returns TS listings) — all culprit `POST /api/smoke-test`, each **first seen ~21h ago (the documented 06:00Z bad-deploy window) and last seen ~3h ago (~00:05Z)**, 1–3 events each over 21h. Their last-seen exactly coincides with the 00:00–00:09Z midnight cron-rush pool exhaustion (3 pipelines ok=false on connection-pool/upstream timeouts in that same window).
- **Risk read:** LOW real impact — the underlying state is independently verified CLEAN this run (security 0/0/0, FMV fresh+improving, `detect_stalled_pipelines()` `[]`), and the sporadic 1–3-events-in-21h cadence is the signature of transient smoke-query errors under pool pressure, NOT a persistent regression (a real anon-SECDEF hole or FMV stall would fire every ~20min tick = dozens of events). BUT it has now **recurred at a second cron-rush window** (06:00Z documented by the night pass, now 00:00Z), so it's a recurring pattern, not a one-off. The harm is cry-wolf: the security/FMV/sales smoke assertions go RED on transient pool timeouts, desensitizing the operator so a *real* security/FMV smoke failure later gets dismissed as "just the midnight flake."
- **Suggested action:** make the DB-dependent smoke assertions retry-once on a transient connection-pool / statement-timeout error before asserting failure, OR shift the smoke schedule a few minutes off the `:00`/`:06` cron rush (or add a touch of pool headroom for that window). **Fold with ledger Q5 (smoke sales-lag threshold — same cron-rush family) + the 2026-06-01T00:15Z inbox P2 (`evm-transfers-ingest` trips the new `detect_stalled_pipelines` smoke gate)** into one "harden smoke/sentinel against cron-rush transients" change so the suite stays trustworthy.

---

## Watch (folded into sweep — already tracked, NOT new candidates)

- **P2 (00:15Z inbox) — `evm-transfers-ingest` trips the `detect_stalled_pipelines()` smoke/sentinel gate (`NEXTJS-1E`).** Still open: 1 event, last seen ~3h ago (~00:05Z), no re-fire since. `detect_stalled_pipelines()` returned `[]` this run (evm was inside its window at sweep time), so it flaps with evm's known Base-429 tick-skips. Already logged ≤3.5h ago — not re-logged here; subsumed by P3's "cron-rush transients" fold.
- **P3 (00:15Z inbox) — rookies view name (`topshot_rookies_board` in ledger vs live `topshot_2025_rookie_index`).** Confirmed again this run: the live view is `topshot_2025_rookie_index` (resolves); `rpc-insights-health` references the correct name and is healthy. Pure ledger-vs-schema naming nit for operator/CC to reconcile; already logged, not re-logged.
- **`NEXTJS-15` (`allday-listings-indexer`, ledger C1).** Fired once ~23:21Z — ~2h AFTER the `221ab64` rate/reason-gate deploy (~21:21Z), 10 events total. Could be the gate working as designed (a genuine >25-new-failure spike still pages) or needs more soak; **not yet 24h-quiet**, so don't mark the Sentry issue resolved yet — keep watching per C1.
- `NEXTJS-B` (sales pipeline healthy smoke) — last seen 21h ago, no re-fire; corroborated the Q3 TS-sales stall which is resolved. Effectively stale.
- Sentinel TS-UUID-48h 190 sits under the 250 ok-line and ↓ hard from 1099 — trending right, not a finding.
- No regression attributable to any of today's ~18 deploys (all READY); FMV ASK_ONLY level + NO_DATA fall are the intended `65421e26` ask-over-WAP behavior, not drift. funnel-track `await` fix + team-hub Phases 1–5 + `/insights/market` all clean.
- Artifact validation: 10 enumerated; `rpc-insights-health` (8 backing views resolve), `rpc-traction` (`get_rpc_traction_snapshot()` executes), `rpc-security-drift` (catalog 0/0), `rpc-fmv-watch` (`sentinel_fmv_confidence_rows` both collections), `rpc-live-health` (pipeline+FMV+freshness), `rpc-pipeline-reliability` / `rpc-deploys-and-cost` (pipeline_runs + Vercel + DB size) all exercised clean this sweep. `rpc-my-wallet` not exhaustively re-run (its wmc/fmv backing tables are healthy by dependency). No broken artifact observed.
