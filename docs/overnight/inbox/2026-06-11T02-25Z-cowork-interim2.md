# Cowork interim findings #2 — 2026-06-11T02:25Z (calm-DB measurements; supplements 00:50Z file)

1. AF1 CLOSED: v_tracked_wallet_fmv_confidence reads clean on the calm DB (50-row probe instant, 21 collections rows) — the wmc-churn fixes were the cure, per the "fix the writer" memory. No view rewrite needed. Validate the rpc-tracked-fmv-confidence artifact on next monitor pass, then drop AF1 from carries.
2. FOCUS 8b MEASURED (e3aee28 wave metric, interim): TS editions with latest FMV >3x own 90d median (>=3 sales): HIGH 2/890 (0.2%), MEDIUM 42/2,185 (1.9%) — vs 63 pre-fix; trending toward ~0 as the sweep recycles. (Unfiltered-by-confidence the number reads 451/8,049 but that includes ASK_ONLY/STALE where >3x-median is legitimate — do NOT cite the unfiltered number as poison; sizing-trap class.)
3. TFP ROOT CAUSE (operator decision, NEW): topshot-fmv-populate's 44h of "sets read failed: upstream request timeout" is NOT a code/data bug — sets is 902 rows/960kB healthy. Its cron slots (00:50/06:50/12:50/18:50Z) sit exactly 5 minutes behind the 6h seed-wallet wave (xx:45Z), so every tick fires into peak pool contention and the gateway 504s the connection. Fix = move the entry's slots ~30+ min off the wave shadow (e.g. xx:15Z), per the stagger discipline. Cowork offered to drive the console; awaiting Trevor's go (or operator/night-pass note).
4. backfill-pack-rip-metadata: same "unlogged after()-error exit" disease as fmv-recalc had — cron acks green (sub-second 202s on the dashboard) but pipeline_runs silent since 15:53Z because the after() body's error path under saturation evidently exits before log_pipeline_run. Small CC item: give this route (and ideally all c55d394-wrapped routes) the every-exit-logs treatment that 89c4891 gave fmv-recalc. If its 02:53Z+ ticks log ok on the calm DB, severity drops to hygiene.
5. Champion's Path player_name gap: CLOSED as working-as-intended — 322 canonical TS editions have NULL player_name, only 5 have parseable player-bearing names; they are overwhelmingly genuinely playerless (team reels/redemptions) and CC's baf9802-era set-name display fallback covers the squeeze-board UX. Do not queue a name-parse backfill.
