# Daytime monitor — 2026-06-15 ~15:10Z (08:10 PDT, first tick of day)

PLATFORM GREEN. Read-only sweep + first-tick extras (trust health + cross-collection) + artifact validation all clean. Nightly pass (21feca4) released its lock 08:23Z; 3rd consecutive clean overnight baseline holds.

## Health snapshot (measured this tick)
- pipeline fails 6h: **0** · detect_stalled_pipelines() **[]** · get_pipeline_alerts() **[]** — N1 `snapshot-institutional-wallets` **RECOVERED** (ran ok=true 10:07Z, 3 rows; no longer alerting, no longer in metrics-latest's get_pipeline_alerts=1).
- security **0/0** (RLS-off base tables none; anon/auth write on RLS-off base tables none, relkind r/p).
- trust health **8/8 ok** (ts_uuid_dupes_created_24h 0/200; pinnacle_fmv 5.0h/30; pinnacle_ask 0.2h/3; pack_ev_board 0.79d/2; offer_edition_gap 0/50; pack_ev_depleted 0/30; edition_integrity 4/50; fmv_sanity 0/1).
- sentinel TS-UUID-keyed-48h **0**.
- Sentry **0 unresolved / 0 new in 24h**.
- Vercel **0 ERROR** (prod `b8837bc` READY; the morning badge commits both deployed READY; CANCELED entries = docs-only build-skips via ignoreCommand, working as designed).
- cross-collection pg_cron: cohort_mat 158 fresh 04:10Z, overlap_mat 246 fresh 04:25Z, both jobs active + today's runs succeeded. (Note: `cross_collection_cohort_stats` is a 1-row summary view by design — `count(*)... FROM cohort_mat`, no GROUP BY — so insights_counts cross_collection=1 is CORRECT, not the "592" some night-pass notes list, which was a different object.)
- FMV: TS hi+med **3373** (NO_DATA 3964, down from 3998) / AllDay **721** / Pinnacle **728/1845** (himed 764->728 = normal daily recompute churn, pinnacle_fmv fresh 5.0h). golazos/ufc hi+med 1 each (known thin-market floor, flat).
- Artifacts: rpc-live-health consolidated payload ran clean — all 11 insights boards return rows (squeeze 9058, set_squeeze 238, pack_reality_top_ev 9 [thin by design, C2], rookies 61, market 646, first_mint 559, cross_collection 1 [summary view], pinnacle_scarcity 2121, offer_spread 5583, deals 565, trophies 683). Spot-checked backings: v_tracked_wallet_fmv_confidence 21 (AF1 view, no timeout), v_offer_sanity_flags 211 (edition gap 0), v_rewards_economy 1. None broken; none repaired (read-only).

## Candidate (LOW — post-ship watch note; no code action)
**BADGE-BACKFILL-WATCH** · source: commits `0df701c` + `b8837bc` (Trevor/CC, 07:19 / 07:32 PDT this morning — post-night-pass, so unticketed in the ledger; deploys J2eRo8/Ek1Tc2 READY). · risk: **LOW**.
- What: set-driven badge backfill for catalog-invisible Ultimate "trophy" editions (new INGEST-gated route `/api/admin/backfill-badges-from-sets` querying `searchEditions(bySetIDs)`) + paginated `.range()` reader fix (PostgREST clamps `.limit()` at 1000, so ~15.5k editions / ~9k badge rows were truncating and falsely reading "missing") + dropped the invalid `parallelName` field from the searchEditions GQL query (422). One-off bulk runner `scripts/backfill-badges-from-sets.mjs` ran live (120/199 missing editions filled incl. flagship KD Supernova).
- Post-ship health NOW: `topshot-badge-catalog` ok=true every tick through 14:47Z @ 8924 rows (the 14:47Z run is AFTER the 14:32Z b8837bc deploy — no regression); badge_editions int-pair grain holding (Q8 guard: trust ts_uuid_dupes_created_24h 0); no new Sentry; deploys READY.
- Known follow-up (Trevor's, NOT night-pass-shippable — additive/INGEST-route work): b8837bc notes ~79 still-missing editions + 7 GQL-UUID-less sets remain, to be closed by a `getMintedMoment(moment_id)` fallback pass. Tracking-only.
- Suggested action: night-pass **post-ship-watch only** — confirm `topshot-badge-catalog` stays ok=true and `badge_editions` stays 100% int-pair-keyed (the `badge_editions_block_topshot_uuid_key_trg` guard) over tonight's ticks. No revert/code action warranted.

No new actionable/shippable candidates — platform clean. Carried items unchanged (N1 operator [recovered this tick], ALLDAY-V1-UNMAPPED-DRIFT operator/draining, VERCEL cost family, A1-WORKER-PASSTHROUGH-CLEANUP, etc.).
