# Daytime monitor inbox — 2026-06-08T00:16Z

Run: rpc-daytime-monitor (read-only sweep 00:06–00:16Z, Sun evening). Health GREEN. Security 0/0; `detect_stalled_pipelines()` = `[]`; `get_pipeline_alerts()` = `[]`; **Sentry 0 unresolved** (CC resolved the 5 smoke transients + NEXTJS-4 today with regression auto-reopen armed — zero new events since, across the entire 06-07 ship wave incl. PIN-FMV-REKEY Waves 2/3, AllDay video backfill, verify/rewards/profile, pack-viz). 16/16 artifacts validated healthy.

---

## 1. CRON30S-VERIFY · [LOW · verification, night pass] `76b6c2e`'s own deploy ERRORed (superseded — code live via descendants); assert analytics-smoke's first-ever `pipeline_runs` row

**Source:** Vercel `dpl_5JjJJAzxtk2BcYatetEJ6uHZaABa` (commit `76b6c2e`, 23:48Z) state=ERROR; descendants `9d77c95` (23:51Z) / `87f5f83` (00:01Z) / `5ad46fc` (00:05Z, current prod) all READY → transient build-infra class (same as the 05-31 `8f3fff9` blip), tree builds clean, **nothing to revert**.

**What:** `pipeline='analytics-smoke'` has 0 `pipeline_runs` rows EVER as of 00:08Z — expected, not a failure: its cadence is `13,43 * * * *` and the first READY descendant went live ~23:54Z, so the first wrapped tick is 00:13Z. The sibling `lock-check-batch` is already verified on new code: 00:08Z ok=true, cadence preserved (8,38), every tick ok across 12h.

**Action:** assert ≥1 `analytics-smoke` row with ok=true after 00:13Z (and that lock-check-batch cadence holds). If STILL zero rows at the night run, the cron-job.org entry was likely auto-disabled by the pre-wrap timeout streak → operator re-enable (exactly what happened to `topshot-fmv-populate`). Also note the ERROR deploy as superseded in the ledger if recording the wave.

## 2. STAGGER-INTERIM · [DATA · input to the histogram verification due ~06-08 evening] minute-0 is still the dominant start minute post-stagger; fail impact has collapsed anyway

**Source:** `pipeline_runs` 8h minute histogram (16:06–00:06Z): **:00=532**, :02=269, :07=242, :20=179, :12=160, :40=156, then ≤54. Fails last 6h: **six total, all inside one 18:15–18:22Z burst** (compute-topshot-pack-ev `targets` stmt-timeout ×2, wmc-fmv-populate lock-timeout ×2, pinnacle-nft-resolver pool ×1, hydrator N2-class ×1); **zero fails in the ~5.7h since** — every evening :00 boundary (19:00→00:00Z) clean. 14d fail rate 0.44%.

**Read:** the stagger is working where it matters (rush fail bursts now rare + milder), but ~66 runs/hr still anchor :00 after both the 21-entry cron-job.org stagger and the GHA stagger (`306a7ed`) — candidates: chained sub-pipeline log rows, parents that start :58–:59 and log at :00, or entries deliberately left anchored. **Action:** fold this data into the already-due histogram verification; identify the residual :00 trigger(s) before considering more moves. No further stagger action needed if the fail profile stays at this level.

## 3. INFO datapoints for already-queued/gated items (no new items)

- **DUPE1 gate progress:** sentinel TS-UUID-48h **1,363** (6,382 → 2,644 → 1,624 → 1,363), 0/hr new, TS editions flat 15,541. On pace to clear WARN <250 ~Jun 8 PM — the CC-owned merge gate (focus.md #3) is approaching; night pass must NOT pre-empt.
- **PACKEV drain ahead of forecast:** stale-24h **354** (665 → 473 → 354; focus expected <~350 by morning — effectively there), stale-48h **97** (330→177→97), oldest `last_ev_at` 06-05 17:08Z advancing. v21 + cron-frequency lever confirmed sufficient.
- **TFP healthy on the :50 slot:** 18:50Z ok=true; watchlist row live at 480m (TFP-WATCH closed). Next tick ~00:50Z.
- **pinnacle-sync daily cadence:** last logged 10:07Z 06-07 (daily); second tick expected ~10Z 06-08 → PIN-SYNC-CRON watchlist becomes wireable after it.
- **FMV:** TS HIGH+MED 2,998 (churn-flat vs 3,008 baseline), TS NO_DATA 5,291 (5,444 → improving), AD HIGH+MED 495 (flat). fmv writes 19,263/24h.
- **C3:** `v_offer_sanity_flags` 129 — inside the 118–138 baseline band.
- **DB 6,433 MB**; unmapped 183 (flat); Trevor wmc 18,452.
- **GIT-IDENT (raised 21:11Z) still present:** all evening commits incl. 00:05Z remain authored `rpc-daytime-monitor` — one-liner fix still outstanding in the shared clone.
- **Hygiene:** `docs/overnight/.lock` is stale litter from the 14:1xZ run (mtime 14:13Z); too old to block anything, night pass should clean it on next start/exit. `.git/index.lock` (06-07 00:52Z, Q7 class) still present in the sandbox view and un-removable from here.

---

## Artifact validation (16/16 healthy)

AF1 `v_tracked_wallet_fmv_confidence` completes without 57014 (full distribution returned); bounded insights counts OK (squeeze 500+, scarcity 500+, rookies 61, pack-reality 3 — up from 1, still honestly thin); rewards economy view 1 row; edition_offers 9,702; my-wallet/fmv-watch/pipeline-reliability/live-health/security-drift/deploys-and-cost backing reads all returned sensible fresh data via the sweep. No repairs needed, none attempted.
