# Inbox index — 287 live filings

**Generated 2026-08-22 (PT) by Claude Code, deep-audit R27. Reconciled twice on 2026-08-22 evening: first from rot (193 listed / 196 on disk), then from a CONCURRENT CLOBBER — `a2bc6e9a` wrote back a copy read before the first reconciliation and took the file 198 → 192, burying nine filings including a HIGH-PRIORITY one. Both were caught by `__tests__/inbox-index-lists-every-filing.test.ts`, not by a reader. Counts here are asserted against the directory on every CI run, so do not hand-edit one without adding the entry it counts. ⚠ **ARCHIVING a filing means DELETING its entry here in the same commit** — this file maps the LIVE queue, and an entry for an archived filing tells the next session an item is open when it is closed (that happened 2026-08-23 and the guard caught it).**

## Why this file exists

`docs/overnight/inbox/` had not been archived since 2026-08-13 and had grown to 188 live
files nine days deep, so **drained and open became indistinguishable** and the run-3 audit's own
brief estimated "~29 files" — off by more than 5x. That miscount is itself the evidence the
signal had degraded silently, and it breaks cheap-check (1) of the audit protocol, which tells
every session to grep ALL of `inbox/*` before measuring anything.

## Why the files were NOT archived

Archiving by date was considered and **rejected**. The `rpc-nightly-autonomous-pass` task
DRAINS this directory: moving a filing that was never acted on would silently remove it from
that queue, and nothing would ever surface it again. A date is not a drained-determination, and
no per-item drained state exists to read. **Archiving is Trevor's call, not a chore** — the
safe half (making the contents scannable in one read) is done here; the destructive half is not.

## How to use it

Scan this file instead of opening 188 documents. ⚠ It is a **listing, not a status**:
presence here says nothing about whether an item was acted on. The canonical open list is
[deep-audit-register.md](../../audits/deep-audit-register.md) — an item that matters and is
still open should have a register row, and if it does not, that gap is the finding.

⚠ **Regenerate this file when you add filings**, or it becomes another rotted map — the exact
failure it documents.

---

## 2026-08-27 — 30 filings

- [⛔ RETRACTION — my own kill-rate table **pooled across a fix**, and two of six verdicts were wrong; the instrument now lives in code](2026-08-28T0350Z-RETRACTION-my-own-kill-rate-table-pooled-across-a-fix-and-two-of-six-verdicts-were-wrong.md) — corrects the 0330Z and 0305Z filings. ⛔ **`candy-listings-indexer` is NOT "still killed on 58% of ticks".** The kills are a contiguous block that ENDED; split at the deploy that landed 08-27 03:48Z (`6455fb9f9`, batching ~1,600 sequential round trips into ~32): **PRE-fix 16 ticks / 14 killed / 87.5% / avg 322 s (of a 300 s wall) → POST-fix 9 ticks / 0 killed / 0% / avg 28.5 s.** **11× faster, zero kills, holding at the peak hour** (18:35Z in 34.2 s). ✅ **The user-facing question is settled: `/insights/candy-mlb` is NOT stale** — `candy_listings max(last_seen_at)` is **2.4 minutes** old, 2,025 active rows (⚠ the freshness column is `last_seen_at`; there is no `snapshotted_at`). ⭐ **THE MECHANISM OF THE ERROR: a kill rate with no recency discriminator cannot tell "broken now" from "was broken, fixed, and the pooled rate still carries the corpse."** Both records produce 56%; only the ORDER separates them. ⚠ **The fix date was in my own sentence** — I wrote *"was fixed on 08-26"* and still averaged across it. Adding one column pair (`last_kill` vs `last_ok`) **flips two of the six flagged pipelines** (`candy-listings-indexer`, `pinnacle-sync`). ⭐ **The two actions shipped off that table are unaffected, and not by luck: both were corroborated by a second instrument before shipping** (`compute-laliga-pack-ev` against lifetime `pipeline_runs_daily`; `candy-editions-ingest` against the 22-day heartbeat-vs-terminal rollup) — **the two rows that flipped are the two that were not.** ✅ **SHIPPED: `lib/pipeline/kill-rate.ts` + `scripts/analysis/killed-after-routes.mjs` (`npm run pipelines:kills`) + 17 tests** — the heartbeat correlation was **the only instrument that can see a killed `after()` route and it lived nowhere**, which is how it was re-derived wrong. ⭐ **It does not accept a rate** — it requires the tick sequence and derives recency from it, so it *cannot* be called with the two columns that misled. ⭐ **The recovery test is a TEST, not a threshold:** p = (1−killRate)^cleanTicks, and **pooling is the CONSERVATIVE direction** — it deflates the null rate (56% not 87.5%), raising p and making `recovered` HARDER to reach, so it can under-call a recovery but cannot manufacture one. ✅ **Verified where it was wrong:** the real 25-tick record is a fixture; **NEGATIVE CONTROL — the same 14 kills moved to the END returns `failing`** (identical count and rate, only order differs); **POSITIVE CONTROL — deleting the terminal rows makes all 25 read as killed**, so a correlation matching nothing cannot pass for the wrong reason; and the failure path is exercised live (unreachable host → `could not measure`, **exit 2**). ⭐ **Exit codes are three-state on purpose — 0/1/2 — because collapsing "could not measure" into "failing" would let a read failure render as a finding, in the very instrument built to detect that.** ⛔ Not claimed: `pinnacle-sync`'s recovery (n=3, p=0.33 — the classifier correctly refuses it), `lock-check-batch` (heartbeat added the same night, n=4 IS its history), `fmv-recalc` (62.3%, stands, and reproduces CLAUDE.md's 64–73% by another method — the sweep's positive control). ⚠ The ~73 h retention bounds everything: **a short report is not a clean bill of health**
- [⚠ `candy-editions-ingest` missed its 08-27 22:10Z tick — the **first miss since the 08-22 move to the quiet slot**](2026-08-28T0310Z-candy-editions-missed-its-first-tick-at-the-new-quiet-slot.md) — filed by the daytime monitor (20:06 PT) from a NO-PUSH session. `pipeline_runs` shows ok/28,483 rows at 08-25 and 08-26 22:10Z and **no row at all for 08-27**, ~29 h silent, so `rpc_ops_snapshot()`'s `cron_silent` arm fired (medium). ⭐ **n=1 cannot separate the two explanations and the filing refuses to pick one**: a one-off Vercel cron miss / single kill (the route logs to `pipeline_runs` only on completion, so a killed run leaves no row and reads as silence) versus the 22:10Z slot not being reliably safe either. ✅ **Controls say it is not user-facing yet** — `candy_fmv_stale_hours` 0.2, `candy_sales_stale_hours` 3.4, `candy_fmv_pct_stale_30d` 0, and editions are byte-identical at 28,483 rows every run, which is why it is medium not high. 👉 Action is **re-measure the next tick (08-28 22:10Z)**, not conclude; if that also misses, the durable fix is the already-identified `paginateGroup` chunking (ingest-route logic — needs a push, off-limits for unattended auto-ship). ⛔ Do NOT raise the 1800 m threshold or re-point the arm — **the arm is correct, it caught a real missed tick**
- [🚨 `compute-laliga-pack-ev` has written **ZERO rows in its entire life** — three defects — and its failure was **PROTECTING the honesty canon**](2026-08-28T0330Z-compute-laliga-pack-ev-has-written-ZERO-rows-in-its-entire-life-and-its-failure-was-protecting-us.md) — found by generalising the candy diagnosis into a **repo-wide heartbeat correlation** across every heartbeated pipeline. ✅ **Positive control: the sweep reads `fmv-recalc` at 64.6%, and CLAUDE.md independently documents it as "64–73% wall-kills"** — it reproduces someone else's figure by another method. Top of the list: **`compute-laliga-pack-ev` 3/3 killed (100%)**. ⛔ **My first alarm, "Golazos pack EV is dead", was WRONG**: `pack_ev_history` is fresh (2.6 h, 5,664 rows) because the pg_cron edge function writes it — *name the caller before you touch the function*. 🚨 **The lifetime record: 2026-08-02 → 08-23, 20 runs, 19 reported `ok`, `sum(rows_written) = 0`.** Never written a row. **Three proven defects:** an unbounded `pack_drop_pool` `.select()` (`rows_found: 1000` against a **1,958**-row pool — the cap confirmed empirically, and ⚠ NOT covered by `invariants-postgrest-cap`, whose allowlist entry is for a different read in the same file); an insert of `algo_version`, **a column `pack_ev_history` does not have** (the 08-23 `last_error` verbatim); and `confidence: "PACK_EV"`, **not a member of the `fmv_confidence` enum** (proven with a control: PACK_EV → false, NO_DATA → true). ⚠ The sentinel failure is invisible by construction — error to `console.log`, then `logPipelineRun` with **hard-coded `ok: true, errorMsg: null`**. ⭐⭐ **THE FINDING: do NOT repair it.** The sentinel writes **`fmv_usd: 0`**, and **507 pool editions have zero 30-day sales** — a working version would publish **$0 as a price** into `fmv_snapshots` → `fmv_current` → portfolio totals, **replacing the honest `NO_DATA`/`STALE` those editions already carry**. **Three bugs have prevented a fabricated-price write for the feature's entire life.** Generalises: *when a broken feature has never produced output, establish what it WOULD have produced before repairing it.* ✅ **Cron entry removed** (300 s lambda daily, dying, adding statement timeouts, emitting a misleading `ok`; EV half already covered). ⛔ Route and tests kept — it needs a redesign, not a repair. ⛔ `fmv-recalc` (64.6%) and `candy-listings-indexer` (58.3%, "fixed" 08-26) left alone and filed — ⛔ **that last claim is RETRACTED by the 0350Z filing: `candy-listings-indexer` had already RECOVERED (87.5% → 0% across the 08-27 03:48Z deploy); the 58.3% pooled across the fix**
- [🚨 `candy-editions-ingest` is KILLED on ~45% of nights, and every kill is invisible in **both** rollups — moved out of the peak band](2026-08-28T0305Z-candy-editions-ingest-is-killed-45pct-of-nights-and-every-kill-is-invisible-in-both-rollups.md) — found by sweeping for dark pipelines, settled by the **heartbeat correlation**: 2026-08-27 has a heartbeat and **NO terminal row**, and Vercel's log names the cause — `wmc upsert err: canceling statement due to lock timeout` ×2, `wmc metadata denorm err: statement timeout` ×2, then `Task timed out after 800 seconds`. **That is register item D8 killing a whole daily ingest, not merely slowing it.** 🚨 **Both summary instruments call it healthy.** `pipeline_runs_daily` shows **22 days of `runs 1 · ok 1 · failed 0`** and 08-27 is not a failure row but **ABSENT** — ⭐ **a killed run does not appear as a failure, it appears as a MISSING DAY, so the lifetime record reads 100% ok.** Cross-referencing the heartbeat rollup against the terminal rollup gives **4 heartbeat-without-terminal days (08-17/18/20/21) plus 08-27 = 5 kills in the ~11 days the heartbeat has existed, ~45%.** ⚠ The cadence alarm cannot catch it either: 30 h `max_silent_minutes` on a DAILY job means **it can only alarm on two consecutive failures**. ⚠ My first read, "2 runs in 30 days", was a **retention artifact** of the ~73 h window — corrected from `pipeline_runs_daily`. **Action: moved `10 22 * * *` → `10 1 * * *`.** ⭐ Hour **1**, not the "quietest" hour 0, because hour 0 is where the `*/2`, `*/3` and `*/6` pg_cron cohorts all coincide (#42); hours not divisible by 2 or 3 avoid them. ⛔ 02:10Z rejected — `topshot-catalog-backfill` runs 02:12 and also writes `wmc`. ⚠ **The hour attribution is INFERRED, not measured for this pipeline** (every run has been at 22:10, so its own data has no hour contrast) — **the move is as much an experiment as a fix, and it creates that contrast. Falsifier: if kills continue at ~45% from 01:10Z, the cause is D8 itself, not the schedule.** ⚠ Expect a one-off cadence alarm from the transition; it is not a new fault. ⭐ **Durable lesson: a killed `after()` route is invisible in `pipeline_runs_daily` too, and there it makes the record look PERFECT** — an argument for continuing the E5 conversions, since the remaining 47 un-heartbeated routes cannot be audited this way at all. §6 carries the pack-pool post-ship watch and a falsifiable prediction: **its failure rate should settle near 33%, not 0%**, since one tick in three is the rips tier's dedicated slot
- [✅ SHIPPED (DB) — the pack-pool wedge is FIXED: a tier of **8** can no longer consume a **3**-wide window](2026-08-28T0255Z-SHIPPED-the-pack-pool-wedge-is-fixed-a-tier-of-8-can-no-longer-consume-a-3-wide-window.md) — acts on [2026-08-28T0145Z](2026-08-28T0145Z-the-pack-pool-stall-is-a-WEDGE-and-the-mechanism-is-8-dists-in-front-of-a-3-wide-window.md); direct successor to `20260827030000`. **User cost measured first: 1,715 of 2,083 TS dists (82.3%) have pool rows and 368 do not — a 17.7% hole in pack-EV coverage**, with **349 of the 360** unreachable rows updated in the last 30 days. The backlog drained 710 → 368 in a day, then **stopped dead for 11 h and could never restart.** ⭐ **ORDER BY ONLY — the WHERE is byte-identical, eligible set verified 368 → 368, so no computed pack-EV number can change.** That is why this is scheduling, not the pricing logic `autonomous-tasks.md` puts off-limits (a list scoping what the UNATTENDED pass may auto-ship). The rotation added by `20260827030000` only shuffles WITHIN a tier; the tier is a hard head of 8 against a 3-wide draw. Now `(has_rips AND bucket % 3 = 0)`, so a wedged tier is **mathematically incapable of consuming every tick** while ripped packs keep a slot every ~15 min. **Simulated before applying: buckets reaching the 360 went 0/12 → 8/12; progress slots/hour 0 → 24**, draining the backlog in ~15 h. ✅ Verified after: eligible set unchanged, anon/auth EXECUTE false, SECDEF clean, live sampler draws 3 targets all `has_rips=false`. ⚠ **The SECDEF check returns jsonb NOT SETOF, so `count(*)` is always 1 and reads as a violation** — correct read is `jsonb_array_length` = 0; the documented mixed-return trap fired here. ⛔ **NOT failure memory** — the 8 keep being retried in their one-in-three slot; this stops them BLOCKING, not being retried. ⚠ Exit condition is a CONVERSION over ~20+ ticks, not one green tick
- [⚠ ADDENDUM — the liveness probe **DIES at the busy hours**, so ⛔ do NOT add a slot to measure them; and my own ranking-bias claim is **REFUTED**](2026-08-28T0245Z-ADDENDUM-the-liveness-probe-DIES-at-the-busy-hours-so-do-not-add-a-slot-and-my-ranking-claim-is-refuted.md) — follows the 0235Z filing; **separate rather than an edit, because the inbox is append-only and one half CORRECTS ME.** 🚨 **The obvious next move — add a probe slot inside R6's band — was measured before proposing and is WRONG.** jobid 288 costs **161 s at 00Z but 679 s at 18Z (4.2×)** and hits its own 900 s ceiling: **14/14 succeed at 00Z, 6/11 at 12Z, 8/12 at 18Z**. The truncation shows in the data it leaves: **views recorded per sweep 45.0 at 00Z → 26.9 at 18Z**, so at the busy hours the sweep records 60% of the boards and dies — **the history is missing NOT AT RANDOM.** ⛔ A fifth slot would add **~11 minutes of query time at the worst hour** to an IO-bound instance (the opposite of focus.md PRIORITY 3) and would probably time out anyway. **The cheap way to measure the bad hour does not exist — that is the finding, not an oversight.** ⛔ **REFUTED: my hypothesis that the truncation biases R50's RANKING.** Across all 45 boards the 00Z share spans only **36–46%** (mean 42.6) — near-uniform. **R50's ORDERING is sound; only its LEVELS are understated**, exactly as the 0235Z filing claimed and no further. ⭐ Small residual in the unflattering direction: the slowest boards have the LOWEST 00Z share, so the **fast-looking** boards are the more understated — a 10-point spread, stated as a direction, not a correction. ⭐ And the probe's own success rate is a **free, already-collected saturation signal nobody reads**
- [🚨 R50's numbers are measured at the FASTEST hour — the probe samples 4 fixed slots, **42% of them at 00Z**, and latency is **5× worse at 18Z**](2026-08-28T0235Z-R50-is-measured-at-the-fastest-hour-the-probe-samples-4-slots-and-42pct-are-00Z.md) — `public_board_liveness_history` looks like a 12-day distribution; it is **four fixed hours** (00/06/12/18Z), unevenly sampled, **42% at 00Z**. Latency climbs monotonically across them — **p50 ×4.1, p90 ×7.5, mean ×6.8** from 00Z to 18Z. ✅ **PAIRED CONTROL, because different hours were sampled on different day-counts and a naive split would just read a different day mix: 296 pairs (same board, SAME DAY, 00Z vs 18Z) across 45 boards / 11 days — 273 (92.2%) slower at 18Z, p50 101 ms → 520 ms.** The gradient is real, not a day-mix artifact. 🚨 **So R50 understates itself**: its pooled figures sit between the 00Z and 06Z values because that is where the samples are. `allday_scarcity_board` p50 is **14,922 ms at 00Z but 169,448 ms at 18Z** — **20× budget, 11× its own best hour** — against R50's pooled 23,429. **Its "highest-value single target" call is confirmed and the gap is far bigger than the number it was made on.** ⚠ `candy_pack_market` is **UNDER budget at 00Z** (3,661 ms) and **90% over at 18Z** — a single-hour reading of any board is not evidence of health. ✅ Corroborates **#42's hour thesis from a completely independent instrument** (board latency vs pg_cron waste). ✅ Strengthens R50's *do NOT raise the budgets* — no one threshold can fit both hours. ⛔ Does NOT establish user-facing latency (ISR; R50's refusal stands) and ⛔ does NOT answer **R6** — `get_collection_stats` is not in this table and the probe never samples 16:20–18:05Z, so that degraded-band re-measure still needs a live reading in the band
- [🚨 `refresh_wmc_fmv_changed` — the CLEAN A/B is in, and the fast path **FAILS its own exit condition on BOTH callers**](2026-08-28T0215Z-rwfc-CLEAN-AB-the-fast-path-fails-its-own-exit-condition-on-both-callers.md) — closes handoff **open reading #5** and the ⏳ scheduled re-read #36 was waiting on. Taken under BOTH conditions the steer demanded: **24.13 h** after `T1_CLEAN` and with the instance **quiet (1 active / 0 IO waiters)** — it was 34/31 ninety minutes earlier, and reading then would have rebuilt the very confound this window exists to remove. Measured as a **FLOW**, not a per-call figure off a cumulative stock: **pg_cron 87,352 reads/call vs a 74,159 threshold (+17.8%)**, **PostgREST 10,029 vs 7,195 (+39.4%)**. **The exit condition is MET.** ⭐ **The control that makes it robust: the confounded and clean windows AGREE** (86,533 → 87,352 and 10,472 → 10,029) on a sample 2× larger — if the index churn had driven the earlier result, removing it would have moved the number. ⚠ That does not make the earlier refusal wrong; it was only knowable afterwards. ⭐ It is a real two-resource trade — **26.5% faster wall time for 18.5% more disk reads** — but the same ms/call figures were in the 08-26 entry's own table when its author chose reads, and this instance is IO-bound, so the metric was right. ✅ **Reverting costs no correctness** — the freshness guard is scaffolding for the optimisation, and rows failing it already *"fall through to the incumbent and are computed exactly as before"*. ⛔ **NOT reverted, for one specific reason:** the pin file carries the body verbatim and **`SUPABASE_SERVICE_ROLE_KEY` is absent here**, so `db:pins:check` cannot run — mutating the instance's largest writer (36.7% of all dirtied blocks) with the pin half unverified is the wrong order of risk. Full operation specified for whoever holds the key
- [✅ D30 — three production-dead components removed, and a **same-named LIVE sibling** is why they survived](2026-08-28T0230Z-D30-three-dead-components-removed-and-a-same-named-live-sibling-is-why-they-survived.md) — re-derived by resolving every import specifier to an absolute module path, with **positive controls** (two known-live components correctly report 1 production importer each) so the three zeros are not a blind matcher. 🚨 **`components/InsiderSignals.tsx` is dead while `components/analytics/InsiderSignals.tsx` is LIVE** — same basename, and a name grep reports **seven** "importers", every one the live sibling, an API path, or a type. **My own first pass got this wrong**; anyone spot-checking D30 with a name grep concludes it is live and moves on. ⭐ **For a dead-code claim, grep the RESOLVED PATH, never the identifier.** ⭐ **The tests existed BECAUSE the components were at 0%** (their headers say so) — so the component coverage gate was satisfied by covering code nothing imports: **a coverage gate cannot tell "covered" from "covered and dead"**. 🚨 **NEW, not in D30: `/api/profile/tier-breakdown` is now orphaned too** (TierBreakdownCard was its only caller) — ⛔ not deleted, because external callers cannot be enumerated from the repo. ⚠ Two traps caught by the repo's own guards: the brand-token guard names its protected files and correctly failed CI on the missing one; and `portfolio-history/route.ts` justified being PUBLIC by naming the deleted component — **which is how a live route gets removed as orphaned**
- [✅ E5 — two `after()` routes converted on **measured kill risk**, budget 49 → 47; and `duration_ms` is not execution time](2026-08-28T0205Z-E5-two-after-routes-converted-on-measured-kill-risk-and-duration-ms-is-not-execution-time.md) — ⛔ **The three obvious ways to pick which routes to convert are all wrong**: by failure rate (**a failing run WROTE A ROW** — a high fail rate is evidence the route is logging, the opposite of the problem), by cadence shortfall (every candidate runs at **133–16,363%** of its watchlist minimum), and in principle **no query over `pipeline_runs` can count kills, because a kill writes nothing** — which is the whole reason the marker exists. ✅ **The criterion that works is proximity to the wall**: p90 `duration_ms` vs each route's own `maxDuration` — `lock-check-batch` **80%** (max 98.5%) and `run-insider-detectors` **75%**, against 42% and 40% for the next two. Those top two are converted. 🚨 **Separate finding that changes how a common number is read: `duration_ms` on an `after()` route is NOT execution time.** Three pipelines record durations ABOVE their `maxDuration` (352,922 / 339,605 / 322,813 ms vs 300,000) — not a lambda outliving its wall, but `log_pipeline_run` having **no `p_finished_at`**, so `finished_at` defaults to the INSERT and absorbs retry/queueing on the terminal write. ⭐ **It over-reports by an amount that grows exactly when the instance is saturated — i.e. when someone is reading it to diagnose saturation.** The helper documents this for MARKER rows and pins their timestamps; nothing documented it for TERMINAL rows. ⚠ NOT CLAIMED: no kill observed on either route, and a heartbeat cannot detect its own kill
- [✅ #41 CONFIRMED on a **three-point dose–response**, its confound ruled out — and the cadence cut bought **2.7×, not 6×**](2026-08-28T0155Z-41-CONFIRMED-on-a-three-point-dose-response-and-the-cadence-cut-bought-2.7x-not-6x.md) — closes handoff **open reading #3**. ⛔ **The stated falsifier could not have answered it**: a 24 h read on jobid 235 is **four runs**. ⚠ And the obvious wider read is a trap — a naive pre/post split buckets 260 "pre-cut" runs at **18.7/day where `*/2` predicts 12**, because there were **THREE** schedules, not two. ⭐ **Split properly, the p50 tracks the interval almost exactly: `*/1` 52.5 s → `*/2` 113.4 s (2.16×) → `*/6` 318.8 s (6.07×)** — a near-perfectly linear dose–response across three points, which **confirms #41's `REFRESH … CONCURRENTLY` mechanism** far more strongly than its own two points, and **rules out the index-build saturation confound it named** (the effect persists 12 days / 49 runs and reproduces at an intermediate dose #41 never measured). 🚨 **The cut bought 2.7×, not 6×** (8.03 → 5.17 → 2.99 busy h/week) — a cadence cut on a CONCURRENTLY refresh *cannot* save proportionally to run count — **and cost a ~10× failure rate, 2.9% → 28.6%**, with p90 pinned just under the 600 s ceiling. ⚠ **NOT CLAIMED: the fail-% column is NOT monotonic** — `*/1` failed at 7.1%, worse than `*/2`, and that is unexplained; only the p50 dose–response is solid. ⭐ Also: **#42's hour ordering does not reproduce here** — its control jobid 211 had midnight best at 97%, while jobid 235 reads 00h 81% / 06h 88% / 12h 73% / **18h 92%**. 👉 On this evidence `*/2` is the optimum (57% of the achievable saving for 10% of the failure rate) — ⛔ stated, not shipped
- [🚨 RESOLVED — the pack-pool stall is a **WEDGE**, and the mechanism is **8 permanently-empty dists in front of a 3-wide window**](2026-08-28T0145Z-the-pack-pool-stall-is-a-WEDGE-and-the-mechanism-is-8-dists-in-front-of-a-3-wide-window.md) — answers the ambiguity `86ecc4687` identified but could not resolve, and closes handoff open-reading #4. ⭐ **`get_topshot_pool_backfill_targets` sorts `has_rips DESC` FIRST and the 5-minute rotation hash only shuffles WITHIN that tier** — so the tier is a hard head. Backlog **368** = **8 with rips** (tier 1) + **360 without** (tier 2); the route draws **3** per tick, **8 > 3**, so every tick draws from the same 8, all 8 return no editions, the tier never empties and **the 360 behind it are unreachable forever**. The rotation works exactly as designed and is powerless because it rotates *inside* the blockage. ✅ **Positive control, not just reasoning: simulated across 12 rotation buckets — 12/12 drew all three targets from the 8, ZERO reached the 360.** If it were a drained queue the sampler would be drawing the 360 and finding them empty; it never draws them at all. ✅ Extends the series to **n=271**: statement-timeout residual **1/271 (0.37%)** vs 5.5% pre-fix, so **open reading #4 is answered**. 🚨 **But the exit condition is NOT met** — 53.9% failure at n=271 against the handoff's "9 ok, 0 failures" at n=9: the [2026-08-26T0525Z](2026-08-26T0525Z-the-cron-reschedule-exit-condition-passes-74pct-of-the-time-if-the-fix-did-nothing.md) trap again, since a 9-tick streak inside an 11-hour working window cannot tell a fix from its honeymoon. ⛔ Nothing shipped (#38 is off-limits); the fix is stated — failure memory in the sampler predicate, and record WHICH dists were tried (`extra.dists` is null on all 121 stalled ticks)
- [⚠ jobid 256 — "the cheapest decision on the list" rests on **two wrong premises** and hides a CI-reddening ordering trap](2026-08-28T0130Z-jobid-256-the-cheapest-decision-rests-on-two-wrong-premises-and-hides-an-ordering-trap.md) — re-derives §7 of [2026-08-27T0450Z](2026-08-27T0450Z-PROVEN-48-cron-jobs-declare-a-statement-timeout-that-does-nothing.md), which had already self-corrected once inside 20 minutes. ✅ **The load-bearing half CONFIRMS**: `fmv_thin_sale_ask_disclosure_cache` has **zero** readers across `pg_proc`, `pg_views`, `pg_matviews`, `cron.job`, `pg_trigger` and a full repo grep, with anon/auth SELECT both false. 🚨 **But both supporting numbers are wrong, in opposite directions**: not "never succeeded" but **5 succeeded / 17 failed in 30 d with the LATEST run succeeding**, and not "14-day-old rows" but **0.66 days old**. ⭐ **The split is the real finding** — successes avg **120 s**, failures avg **498 s** and die at the **600 s** `cron_heavy` ceiling: the same statement finishes in two minutes or not at all, which is focus.md PRIORITY 3's shared disk-IO budget, **not** an intrinsically oversized job. 🚨 **The proposed `DROP FUNCTION` would RED MAIN** — it is a **registered DB-invariant pin** (`supabase/tests/…` + `db-invariants-drift-guard.test.ts:1130`, gated by CI and `DB pin staleness`), the known-issues #0 ordering trap again. ⭐ **Strictly cheaper alternative the filing did not consider: `cron.unschedule(256)`** — removes 100% of the ~2.4 h/30 d of ceiling-bound `cron_heavy` time, keeps CI green, one statement to revert. ⛔ **Not shipped**: the cache was deliberately built 3 weeks ago for a handoff feature, and **I had just falsified both premises the original disposition argued from** — so the decision should be re-made on this evidence, not inherited
- [✅ CLOSED — the unbounded-`fetch()` class is down to **1**, and that one is a CHECKED exclusion](2026-08-28T0115Z-the-unbounded-fetch-class-is-CLOSED-at-1-and-the-last-one-is-a-CHECKED-exclusion.md) — completes [2026-08-27T0320Z](2026-08-27T0320Z-unbounded-fetch-is-a-class-29-sites-carry-the-shape-whose-failure-is-invisible.md). **21 → 1 across two pushes**, the ratchet's anti-slack arm forcing each step. ⭐ **Not one bound is a new number** — every value reuses one already measured and shipped for the SAME upstream (10 s for Resend/Telegram/Discord from `alerts-send`: 276 runs, p95 1,644 ms, one 58,670 ms outlier within **1.3 s** of a kill; 15 s for the Top Shot GQL proxy from `lib/verify-wallet-gql.ts`), which is the filing's own lesson — **it was the FIX that failed to spread, not the defect.** 🚨 **The one that mattered most is `support-chat`'s HIGH escalation page:** its `pageDelivered` guard exists so a real emergency can never vanish behind a false "you've been paged", and **an unbounded fetch defeated it from the side it does not watch** — a hang burns the 60 s budget, so the page fails AND the catch never runs AND nothing is logged. ⭐ **The last site is left unbounded DELIBERATELY and is now CHECKED rather than excused**: `smokeFetch` is caller-bounded, so a new test asserts all **24** call sites carry a signal — with the inspected count asserted and a negative control — because *an exclusion justified by another instrument is a claim about it*. ⛔ `RATCHET = 1` is **not** "zero defects": the 115-site population outside the `after()`+`maxDuration` shape is untouched
- [✅ ROOT CAUSE FOUND AND FIXED — the shared comment stripper desyncs on a nested template literal, and fails BOTH ways at once](2026-08-28T0056Z-ROOT-CAUSE-FOUND-the-shared-stripper-desyncs-on-nested-template-literals-and-fails-BOTH-ways.md) — closes the diagnosis half of [2026-08-27T0500Z](2026-08-27T0500Z-the-shared-comment-stripper-leaves-a-comment-line-intact-in-one-file.md) by taking exactly the instrumentation step it asked for. ⭐ **The two opposite symptoms are ONE desync:** copying `${...}` verbatim lets a NESTED template literal close the OUTER one, after which the machine sits in `code` inside HTML text where `/` opens a regex and `//` opens a comment — so it leaves a comment INTACT on line 80 of `check-alerts` and BLANKS the real Telegram URL on line 100. ⛔ **The header's claim that the boundary was "the safe direction — KEEPING too much, never blanking too much" was FALSE**; that is why it is now fixed with a `tplStack` rather than pinned. ⭐ The negative control was found by SEARCH — four hand-written reproducers all re-synced and would have been VACUOUS. ✅ **Full suite 1,386 files / 15,204 tests green**, so revealing hidden source to 49 importers reddened nothing — read honestly, that means no guard *depended* on the bug, not that none was *misreading*. ⚠ Records **DEFECT 4, known and unfixed**: JSX text is not JS, so an apostrophe in `<p>Couldn't</p>` opens an `sq` state — **8 files**, but in the genuinely safe direction
- [✅ SHIPPED — all three `*-dune` routes bounded, unbounded-fetch ratchet **21 → 11**, and the bound is DERIVED not guessed](2026-08-28T0100Z-the-three-dune-routes-are-bounded-by-a-DERIVED-deadline-and-the-ratchet-falls-21-to-11.md) — takes the largest-budget block (10 sites at `maxDuration = 800`) of the class triaged in [2026-08-27T0320Z](2026-08-27T0320Z-unbounded-fetch-is-a-class-29-sites-carry-the-shape-whose-failure-is-invisible.md). ⭐ **There was NOTHING to size a cap from** — all three routes are drained (`windows_done: 0`) and `pipeline_runs` holds zero runs with real Dune work — **so no cap was chosen.** Each signal is derived from `HARD_BUDGET_MS`, the sweep deadline the route already declares, less a 30 s log reserve: a request that would outlive it is already doomed, so aborting it **cannot** turn a working call into a failing one, only a SILENT kill into a LOGGED failure. That property is what the tests assert. ⭐ New shared `lib/http/sweep-deadline.ts` **because the triage filing's own lesson was that the FIX failed to spread** — 20 inline `AbortSignal.timeout` sites in `lib/` and no helper. ✅ **Three independent detectors agree at 11**, and the ratchet's anti-slack arm fired and forced the lowering. ⚠ My ad-hoc sweep first read **22** — the extra was a FALSE POSITIVE from the stripper defect above; **fixing it is what made the detectors agree**. ⛔ No retries (Dune datapoint budget), no `maxMs` chosen, and the new paths are **not exercised in production yet**
- [A saturation spell, and three pipeline symptoms inside it — 2026-08-27 17:06 PT / 00:10Z](2026-08-28T0010Z-saturation-spell-symptoms-allday-mv-refresh-and-two-stalls.md) — ⭐ **A model filing: `rpc_ops_snapshot()` timed out, the positive control read 37 of 37 active sessions in IO wait, and it therefore files every finding as a SYMPTOM with a quiet-window re-measure as the action — never a cost figure or a cause.** All three were then answered when the window came: **jobid 211 `refresh_allday_pack_realized` succeeded in 59 s quiet, which REFUTES its own suggested remedy ("cut the refresh's work") — the job is contended, not oversized, and the schedule was moved instead**; `allday-pack-opens-backfill` is neither done nor scheduler-stopped (cursor 19.1 M blocks above the floor, still descending, ~6% of dispatches logging — the 08-24 EarlyDrop mechanism); `compute-golazos-pack-ev` fired on schedule at 00:37:30Z, its "stall" being a 6-hourly job under a 13.3 h threshold.
- [Daytime monitor candidates — 2026-08-27 ~11:08 PT / 18:08Z](2026-08-27T1808Z-daytime-monitor.md) — mid-day tick, **NOT in a spell** (positive control io_wait 3 / active 3 / total 43). Security clean, 0 RLS-off public tables, sentinel edition-writer leak 0/48h, 11 artifacts validated by backing-relation existence. Three candidates, and ⭐ **the night pass resolved all three in the quiet window (see the 2026-08-27 ledger entry): candy standing-offer book back to 20 active and refilled — the 999 sentinel was a genuinely empty book, so ⛔ do NOT add the "legit-empty branch" it floats, that is what would have hidden the 08-05..08 outage; `fmv-recalc` resumed at 23 runs/6h; and the MV-refresh timeout cluster was saturation collateral.** ⚠ Its own standing warning is the durable half — **do not file one saturation window as N distinct bugs.**
- [🚨 A function’s `SET statement_timeout` is **PATH-DEPENDENT** — inert on pg_cron, **load-bearing via PostgREST**](2026-08-27T1404Z-a-function-statement-timeout-is-INERT-on-pg-cron-and-BINDS-via-postgrest.md) — ⭐ **runs the decisive experiment `database.md` designed on 08-17 and could not execute** (no egress / no service-role key in the sandbox; Trevor’s box has both). Two `SECURITY INVOKER` probes, identical but for the declaration, both sleeping 40 s, both POSTed to `/rest/v1/rpc/…` with the service-role key: **bare → HTTP 500 at 31 s** (`57014`), **declaring `60s` → HTTP 200 at 41 s**. ✅ Reproduced on the real object (`wallet_usernames_unresolved`, declares 60 s, dies at **61 s**). ⭐⭐ **The value is the RECONCILIATION, not the result:** the 08-17 refutation demanded exceedances and found them — `refresh_mv_pack_ev_latest` declares 120 s and hits **596,559 ms** — but **every one is a bare `SELECT public.fn()` from `cron_heavy`**. Split on the `pgrst_source` wrapper: pg_cron statements overrun **44 of 248**, PostgREST **2 of 106**, and both exceptions declare **8 s, BELOW `service_role`’s 30 s** — so on that path a HIGHER declaration RAISES the ceiling, a LOWER one stays inert. **Both sides of this file’s long-running argument were right about their own evidence, and the discriminator is the CALLER.** ✅ Closes two questions `database.md` left open: the *“UNIDENTIFIED ~60 s client-side bound”* is no client bound at all, and the 58.2-vs-60 s match it called *“a COINCIDENCE”* is the mechanism. ⛔ **Scopes known-issues #43 rather than refuting it** — its four probes reproduce exactly on the session path. ⛔ **The live hazard is now the MIRROR of #43’s warning: 122 of 195 declarations exceed 30 s** (up to 900 s), **so stripping them as proven no-ops would cap 122 functions at 30 s.** ⛔ Mechanism deliberately NOT claimed (four data points, no unifying story). ⚠ `anon`/`authenticated` not re-probed — it would need an anon-executable sleep function, a DoS primitive that trips the SECDEF sentinel. ⓘ Also records the pipeline this came from: `wallet-username-resolver` spends ~150k buffers and 60 s every 3 h to find **15 rows**, and a *provably equivalent* rewrite of its `sales` leg is **REFUTED by its own BUFFERS** (it makes the plan scan `sales` twice)


- [⚠ The SHARED comment stripper leaves a `//` line INTACT in one file — symptom confirmed, root cause NOT found, four hypotheses falsified](2026-08-27T0500Z-the-shared-comment-stripper-leaves-a-comment-line-intact-in-one-file.md) — found while building the unbounded-fetch ratchet, which the defect was silently corrupting. `scripts/lib/strip-comments.mjs` — the MANDATORY shared stripper (`MAX_LOCAL_STRIPPERS`, down only) — does **not** blank line 80 of `app/api/check-alerts/route.ts` (`` // `fetch()` has no default timeout … ``); `stripComments(raw)` returns it **byte-identical**, while blanking the identical line in isolation and in every other file swept. ⭐ **Why it matters beyond one file: 49 files import this helper, and its whole purpose is to stop a guard reading its own explanation as evidence** — here it failed at exactly that job, and my new ratchet counted the comment as an unbounded `fetch(` call site. **The trap arrived THROUGH the mandated protection rather than around it: "we used the shared stripper" is not by itself a guarantee that comments were stripped**, and any guard using it on a file with the same (unknown) property is silently reading comment text as code while still reporting a number. ⭐ **The useful artifact is the reproduction boundary**: bisecting by starting offset, a chunk starting at **line 69 reproduces and line 70 does not** — line 69 being the closing line of a multi-line HTML template (`` </body></html>`; ``), which read naively suggests a closing backtick being taken for an opening one. ⚠ **But that explains the CHUNK, not the FULL FILE, and the full file reproduces too** — a lead, not the mechanism. ⛔ **FALSIFIED, do not re-test:** backtick imbalance (counted — 86 total, 20 in `//` comments, 66 outside, **all even**); a template containing a URL; a multi-line template with or without a URL; a regex literal carrying quote chars (`/[&<>"']/g`, line 73, incl. single- and double-quote-only variants); a `//` line with paired or odd backticks. All blank correctly in isolation. 👉 Remaining suspect is **cumulative state across the file** — the recorded *block-first* weakness is the obvious relative, but that one HIDES code and this one EXPOSES comments. **Cheapest next step: instrument the stripper's state machine at line 80 rather than bisect inputs further.** ⛔ Do NOT "fix" it by rewording the comment — the comment is correct and the stripper is wrong. ⚠ Changing the helper is its own job (49 importers; the ratchet exists because a hand-rolled copy once blanked 100k+ chars and hid a live P0). ✅ **Not blocking:** the new ratchet was made IMMUNE instead — a zero-arg `fetch()` is never a real call site, so it is skipped and the count no longer depends on stripping having worked. ⭐ **General tactic worth reusing: where a shared helper is unreliable, prefer a check that does not NEED it to be right over one that assumes it is**


- [🚨 PROVEN BY EXPERIMENT — **48 pg_cron jobs declare a `statement_timeout` that has no effect**](2026-08-27T0450Z-PROVEN-48-cron-jobs-declare-a-statement-timeout-that-does-nothing.md) — ⭐ **`statement_timeout` arms a timer ONCE, at top-level statement start; changing the GUC inside a function — `proconfig` or `set_config(…, true)` — does not re-arm it **on the pg_cron path**.** ⛔ **SCOPED 2026-08-27 — the original "in EITHER direction" is RETRACTED:** the A/B on Trevor's box shows a HIGHER declaration **RAISES** the ceiling via PostgREST (30 s → 60 s), so **122 declarations are load-bearing and must not be stripped**; see [2026-08-27T1404Z](2026-08-27T1404Z-a-function-statement-timeout-is-INERT-on-pg-cron-and-BINDS-via-postgrest.md). ⭐ **My four probes went through the admin SQL path, not the production caller — the mistake `database.md`'s own "A CONTROL THAT DOES NOT USE THE PRODUCTION CALLER IS NOT A CONTROL" heading names.** Four probes, including one where **a function declaring `1s` ran for 4 seconds and finished while `current_setting()` inside it read `1s`** — which is why this survived so long: **every check anyone could run reports the setting as applied.** **36 `cron_heavy` jobs run at that role's 600 s whatever their function says, and 26 of them declare 60–480 s.** Confirmed against production: jobid 217 declares 120 s and has succeeded at **595 s**. ⛔ **Do NOT "make the declarations real" as a batch** — a declaration that never bound is not a promise anything currently depends on, and arming 26 of them at once would convert silent overruns into a wave of new failures. Adds the durable rule to [database.md](../../reference/database.md) and **corrects #42 twice**


- [⭐ 22.6% of this instance's cron time is thrown away — and it is **schedule ALIGNMENT**, not a load band, not a slow job](2026-08-27T0430Z-cron-waste-is-SCHEDULE-ALIGNMENT-not-a-load-band-and-one-third-of-the-hours-carry-72pct-of-it.md) — the session's largest finding, registered as known-issues **#42**. Over 7 days, **206,362 s of 914,843 s of pg_cron time produced nothing**, and **85.2% of that waste is statement timeouts** from just **384 of 1,502 failures**. ⚠ **That 22.6% is the SAME ratio the earlier Class-A audit measured — after its named "lever" was pulled and reduced to 0.9% of waste**, i.e. the lever was real and the ratio did not move, so the driver was never what that audit named. ⭐ **Introduces a triage instrument — `max(success duration) ÷ that job's OWN ceiling`** — splitting the fleet into headroom-fixable / inconsistent-ceiling / blocked-or-starved, and names a job that has **NEVER succeeded** (jobid 256) and appears in no register. 👉 **The driver is alignment: hours divisible by 3 are 8 of 24 but carry 47% of busy time and 72% of ALL timeout waste.** The controlled evidence is jobid 211 — **one function, four slots, 97% / 51% / 54% / 42%** success. ⛔ Nothing shipped; ⚠ and its Class B is weakened by the `statement_timeout` proof filed 20 minutes later


- [Claude Code handoff — Cowork cloud session, 2026-08-26 PT](2026-08-27T0420Z-CLAUDE-CODE-HANDOFF.md) — the session's own handoff document: what shipped to prod (the `upsert_pack_ask_state` change-detection guard), what was measured and deliberately **not** shipped, the live audit objects created via `execute_sql` (table + function + pg_cron **jobid 370**, self-unscheduling 2026-08-29) and their cleanup SQL, and the two retractions. ⚠ **Written before #42 and #43 landed**, so §7 is an addendum covering them — and §8 records that **this session's later filenames run up to ~30 min AHEAD of the real clock** because they were stamped by estimating elapsed time rather than reading one. **The PT dates are correct**, which is what the dating rule and `find-future-dated-ledger-headings.mjs` actually check (**0**); filenames left as-is because the cross-links depend on them


- [⚠ The cadence cut on jobid 235 saved **~1.4 h/wk, not the 3.7 h/wk claimed** — because a `REFRESH … CONCURRENTLY` costs what CHANGED](2026-08-27T0415Z-the-cadence-cut-on-jobid-235-did-not-save-what-it-claimed-because-a-CONCURRENTLY-refresh-scales-with-the-delta.md) — registered as known-issues **#41**. ⭐ **The error is a shape worth keeping: cutting a refresh's cadence `7 */2` → `7 */6` does NOT divide its cost by three, because each run then has 3× the delta to reconcile** — a `REFRESH MATERIALIZED VIEW … CONCURRENTLY` is priced by what changed, not by how often you ask. The saving is real but **~2.6× smaller than the figure recorded when it shipped**. ⚠ Also measured: the job **fails 21.1% of runs** with a **565 s max SUCCESS against a 600 s budget** — i.e. it is not comfortably inside its ceiling, and the cadence cut made each run bigger. **Confound named, falsifier stated, nothing shipped**


- [🚨 SECOND RETRACTION, same night, same route — I refused to guess a number and then reasoned as if it were zero](2026-08-27T0405Z-SECOND-RETRACTION-refusing-to-guess-a-number-is-not-a-reason-to-assume-it-is-small.md) — ⛔ corrects **#40** and the "do not merge the batching diff" disposition. ⭐ **The lesson is sharper than the first retraction's:** having correctly declined to invent a per-round-trip latency, I then argued from the conclusion that the unmeasured term was negligible — **which is the same fabrication, with the number left implicit.** Declining to guess is only honest if the reasoning downstream also treats the quantity as UNKNOWN; an unstated assumption of zero is still an assumption, and it is the one nobody audits because there is no figure on the page to challenge


- [⭐ The Sentry read scope is NOT blocked in a Cowork session — and with it the shipped quota guard checks out at **93.8% coverage**](2026-08-27T0400Z-the-sentry-read-scope-is-NOT-blocked-here-and-the-quota-guard-checks-out-at-93.8pct.md) — ⛔ **overturns a standing conclusion.** "Blocked on a read scope, not on money" came from a CORRECT probe that the build-plugin token is upload-scoped — but **that proves the CREDENTIAL is dead, not that the CAPABILITY is unreachable**: this session's Sentry MCP connector has org read access. ⭐ **The transferable rule: a dead credential is evidence about that credential, never about the whole capability — probe the CAPABILITY through every channel you have before recording it as blocked.** With it: the blackout start is **confirmed by a second, independent instrument** — Sentry's newest stored event is **`2026-08-18T13:21:59Z`**, agreeing to the second with the ingest rate-limit header, and all top-20 issues `lastSeen` 8–11 d, i.e. **total, not partial**. `sentry-quota-guard.ts` audited against Sentry's own store: **93.8% coverage / 89.1% reduction**. Registered as **#40**; `f11fe69` independently verified


- [🚨 I shipped a guard a concurrent session had explicitly argued against — and the argument was circular, but I did not know it existed](2026-08-27T0340Z-i-shipped-a-guard-a-concurrent-session-had-argued-against-and-the-argument-was-circular.md) — the account of shipping past **`beafd66`'s recorded objection**. ⭐ **Both halves matter and they pull in opposite directions:** the objection was **circular** — so the guard was right to ship — **and I shipped without having read it**, so being right was luck rather than judgement. ⚠ **A recorded decision NOT to act is the one nobody re-checks** (CLAUDE.md's own measurement-discipline rule), and this is the mirror case: a concurrent session's objection is invisible to anyone who does not go looking, so **the coordination failure is real even when the outcome is correct**. 👉 Read the ledger and recent commit messages for a recorded objection **before** shipping into shared state, not after


- [🚨 RETRACTION — I fit the constant to the answer on the candy indexer, and a concurrent session had already falsified the premise](2026-08-27T0330Z-RETRACTION-i-fit-the-constant-to-the-answer-on-the-candy-indexer.md) — ⛔ withdraws the claim that N+1 `wallet_moments_cache` lookups consumed the candy indexer's 300 s budget. ⭐ **How the wrong claim passed review: I had exactly two numbers — a HYPOTHESISED operation count (3,626 round trips) and a KNOWN deadline (300,000 ms) — divided them, got 83 ms, observed that 83 ms is a plausible per-round-trip cost, and wrote "the arithmetic closes exactly."** It closes for **any** operation count near the true one, because the quotient is determined by the deadline already known. ⭐ **The rule: when a hypothesised operation count and a known deadline are your only two numbers, their quotient is ARITHMETIC, NOT EVIDENCE — measure one operation, or say the magnitude is unknown.** Upstream `f11fe69` measured the blamed leg at **1.39 ms / 3 buffers** (Index Only Scan) and found the real cause: two **unbounded `fetch()` calls** to Magic Eden. ⚠ Companion rule from that work: **a plan measured against a guessed UUID is not the real plan.** ⛔ The batching diff was held OUT of tree so `f11fe69` could prove itself unconfounded — **a change that is correct is still the wrong move if it destroys the measurement that says whether it was needed**


- [`fetch()` with no timeout is a CLASS — **115 sites**, and **29 carry the shape whose failure is INVISIBLE**](2026-08-27T0320Z-unbounded-fetch-is-a-class-29-sites-carry-the-shape-whose-failure-is-invisible.md) — generalises the measured instance fixed the same evening (the PUBLIC candy board dark **44.3 h**). ⭐ **Why the shape matters:** `fetch()` has no default timeout, and in an `after()` route a hang consumes the whole `maxDuration` — a KILLED lambda runs neither the success path nor the `catch`, so **no terminal `pipeline_runs` row is written at all** and the outage reads as *"the cron never fired"*. Measured on `/api/candy-listings-indexer`: 15 heartbeats / ONE terminal row in 48 h, Vercel `Task timed out after 300 seconds`. ⭐ **The generalisable half: the fix already existed ONE FILE AWAY and had never spread** — `solUsd()` in `lib/chains/solana/das.ts`, called by that route one line above the walk that hung, carries `AbortSignal.timeout(8000)` and a comment naming this exact failure mode. **CLAUDE.md's "grep for the EXPRESSION, not the file" in the direction nobody checks: it was not the DEFECT that spread by copy-paste, it was the FIX that failed to.** Comment-stripped sweep of `app/api` + `lib`: **115 unbounded sites / 86 files**; filtered to `after()` + `maxDuration`: **29 sites / 18 files**, table in the filing. 🚨 Two named: **`candy-sales-indexer` (300 s) is the top candidate by a distance** — sibling of the fixed route, same Magic Eden host, same egress, and already observed taking **Cloudflare 1015 / HTTP 429** from Vercel; and **`sales-indexer` (120 s) is a HIGH-severity watchlist pipeline** that now has two independent ways to go dark quietly, since the GHA backstop was separately measured delivering only 16 of 48 runs. ⛔ **Do NOT blanket-fix** — the correct timeout is not a constant (internal peers, already-bounded callers, legitimately slow upstreams), and the three `*-dune` routes at `maxDuration = 800` must not gain retries (one ownership walk is already 87.7% of the monthly datapoint budget). ⚠ **And a per-request cap is NOT sufficient alone**: a paginating walk with a 15 s cap and a 100-page bound still permits **1,500 s**, 5× a 300 s `maxDuration` — **a loop needs a whole-sweep deadline as a SEPARATE guarantee**, or copying only the `AbortSignal` half looks like a fix and leaves the invisibility intact. 👉 One route at a time, each with a test asserting the property on the **REQUEST INIT, not the source text** (a source grep is satisfied by the comment documenting the fix), proven to fail against the unbounded version first


- [🚨 `ufc-sales-indexer` has ONLY the flaky GHA backstop left — and the backstop delivers **16 of 48** scheduled runs a day](2026-08-27T0250Z-ufc-sales-indexer-has-only-the-flaky-gha-backstop-left-and-the-backstop-delivers-a-third-of-its-schedule.md) — found while checking why `Smoke Tests` went red on `main` (green 02:09:45Z → hard-fail 02:33Z and 02:40Z, `ufc-sales-indexer silent 251m (>240m)`; last run 22:30:11Z + 240m = 02:30Z, so a fresh crossing). ⚠ **Not caused by the commits it fails on** — identical failure on a docs-only commit and on a pack-ask DB function. ⭐ **The measurement, with three same-instrument controls:** `topshot` **85 runs/24h, avg gap 16.9m, max 40.0m**; `allday` **85 / 16.9m / 40.0m**; `golazos` **87 / 16.5m / 39.0m**; **`ufc` 16 / 76.9m / 191.9m**. Three of four fire on cron-job.org's ~17-minute primary; UFC fires 16×/day and **its timestamps match the `Sales Indexers Backstop` GHA runs exactly** (22:30:06→22:30:11, 19:18:13→19:18:18, 16:58:15→16:58:21). 👉 **Reading: cron-job.org's UFC trigger is dead and only the GHA backstop still fires it** — precisely the failure that workflow's header was written to survive (*"cron-job.org auto-disables a job that persistently fails its 30s client cap"*); UFC chains off `/api/ufc-pipeline` (18,38,58). ⚠ **INFERENCE, not a direct reading** — the cron-job.org console was deliberately NOT opened (its bearer sits in that page's DOM and this repo has leaked it twice); whoever confirms must read `input[type=text]`[2] only. 🚨 **The half that matters more than UFC: the backstop's stated guarantee is measured FALSE.** Its header claimed *"Twice hourly (30-min max gap) … even if GitHub drops a scheduled run"* — but GitHub delivered **16 of 48**, budgeting for one drop where the real rate is two in three. **UFC is the canary that already fired**: it is the only one of the four whose primary is gone, so it is the only pipeline whose cadence measures THIS WORKFLOW ALONE — and it is the safety net for the three LIVE indexers. **A 191.9m max gap BREACHES All Day's 90m `max_silent`**, so the net does not hold the line its header promised. ✅ **SHIPPED: the false comment is corrected in place** with the measured table and a ⛔ against "just tighten the cron expression" (GitHub drops the extra fires too). ⛔ **The alarm is CORRECT — do not silence it**, even though UFC's data impact is nil: newest `ufc_strike` sale is **2026-05-13**, **0 sales in 30d and 90d** of 813,934 total, every run reports `ok:true / 0 rows`, and `extra.v2_dapper_typeids_seen` has **never once carried a UFC type ID** (control: newest Top Shot sale is minutes old, so the instrument works). Widening the threshold would convert a true positive into a blind spot on the mechanism protecting three live pipelines, and *a permanently-red instrument is indistinguishable from a broken one*. 👉 **Needs Trevor:** (1) is the cron-job.org `/api/ufc-pipeline` entry disabled? (2) the real question underneath — **UFC Strike appears to have left Flow, so should this indexer be RETIRED rather than repaired?** `ufc_strike` is still `chain=flow, is_active=true` with 813,934 historical sales, so retirement is a product call, not a cleanup. **Either answer is fine; the current state — scanning ~13,000 blocks a run to find nothing, alarmed on liveness — is the one that is not**

## 2026-08-26 — 9 filings

- [🚨 HIGH — CANDY LISTINGS FEED DARK ~38h while the heartbeat fires (+2 lower-priority)](2026-08-26T2120Z-candy-listings-feed-dark-38h-while-heartbeat-fires.md) — filed by `rpc-daytime-monitor` at 21:1xZ and left uncommitted in the shared tree; landed here. **NOT a spell** (positive control io_wait=4 / active=6). `candy_listings` `max(last_seen_at)` = **2026-08-25 06:42Z**, **0 rows in 24h**, **1 terminal row in 48h** against **15 heartbeat rows** — so the route is invoked every ~3h and writes nothing. Blast radius: **/insights/candy-mlb is PUBLIC** and its Deals/Spread/Serials tabs read `candy_listings`, i.e. asks up to 38h stale on a public board. ✅ **ANSWERED the same night by Claude Code — it is (b), a post-heartbeat `maxDuration` KILL**, not a legit-empty upstream: Vercel runtime logs show `GET /api/candy-listings-indexer 202` followed by **`Vercel Runtime Timeout Error: Task timed out after 300 seconds`**, and the route is in that cluster's route list (24,027 events/7d, the largest on the platform). ⭐ **The filing's sharpest line — "the heartbeat is the instrument that lies here" — is exactly the DOCUMENTED kill signature**: an `after()` route writes its heartbeat BEFORE the work, `try/catch` cannot catch a maxDuration kill, and CLAUDE.md says to **read kills by CORRELATION (heartbeat present, terminal row absent)**. The instrument is not lying — it reports the only thing it can see, and the missing terminal row is the other half of the designed signal. 👉 **Likely cause filed as a HYPOTHESIS, not a finding:** the sibling `candy-offers-indexer` is being **Cloudflare-1015 rate-limited by Magic Eden (HTTP 429)** across 6 wallets in one tick; if the listings walk retries through the same throttled upstream it would slow past 300s. ⚠ Not measured for the listings route specifically. **Discriminator: count ME response codes inside the walk — 429s = upstream throttle (back off / page smaller); 200s-but-slow = our own pagination.** The 300s kill looks identical either way. ⛔ Still not auto-shippable (route logic on a PUBLIC surface). ⚠ **And the board is degraded on a SECOND independent axis in the same window** — every `refresh-insights-cache` tick logs 4+ candy boards failing `canceling statement due to statement timeout`, one tick 504 after 60s. **Stale asks and unrenderable boards are two different faults on the same page**

- [✅ CLOSED — the `roll_pack_ask_hourly_low` deadlock WATCH did not graduate: **2 deadlocks ever, none in 8 days**](2026-08-26T1745Z-CLOSED-the-roll-pack-ask-deadlock-watch-did-not-graduate.md) — closes the 2026-08-18 WATCH on **that filing's own stated exit condition** (*"graduates to a fix only if it climbs across days"*), not on a fresh judgement. Measured over the full retained history of pg_cron jobid **77** (2026-07-12 → 2026-08-26, **4,326 runs**): 66 failures of any kind, of which **2 carry a `deadlock detected` message** — dated **2026-08-17 12:22Z** and **2026-08-18 18:07Z**, with **~770 runs and 8 days since, zero deadlocks**. Recent failures are `job startup timeout`, the known `max_worker_processes` class. ⭐ **It did not climb; it did the opposite** — the original filing's own hypothesis (*"pure spell aggravation that clears on its own"*) is what the data supports, so the recommended `FOR UPDATE SKIP LOCKED` rewrite (correctly flagged there as pricing-write-path, not auto-shippable) **should not be spent**. ⭐ **It also explains that filing's own headline:** it reported *"9 fails in the check window"* with a deadlock as the `last_fail_message`, but only 2 runs in 45 days carry a deadlock message — those nine were failures of MIXED kinds with a deadlock as the most recent one. Same instrument shape established independently in the 0525Z filing: **`check_pgcron_recent_failures()` gates on `l.status = 'failed'` and reports LATEST-RUN status**, so here it made one signature look like nine. ⚠ **Neither filing was wrong** — 08-18 read its instrument correctly and labelled the whole thing a symptom. **The durable point: a `last_fail_message` attaches ONE message to a COUNT of heterogeneous failures, so the count silently inherits the most recent signature — read the message DISTRIBUTION, never the message beside the count.** 👉 Consequence for the open 726 MB/day WAL fix on the same function: **unaffected either way**, and stated rather than implied — `ON CONFLICT DO UPDATE … WHERE` **still takes the row lock** (Postgres must lock the conflicting row to evaluate the predicate), so it skips the WRITE, not the LOCK, and must not be argued for on deadlock grounds

- [Sentry without paying: Vercel already covers the **server** half for free — and the residual gap is client-side](2026-08-26T1730Z-sentry-without-paying-vercel-already-covers-the-server-half.md) — answers the question that follows Trevor's decision **not to raise the Sentry subscription** (consistent with the roadmap's no-infra-spend-pre-revenue line). ⭐ **We are not blind:** `get_runtime_errors` returns grouped clusters — signature, count, first/last seen, sample stacks — **50 groups over 7 days, free, and working the whole time Sentry has been dark.** Top clusters: Vercel task timeouts **24,027**, `Timed out acquiring connection from connection pool` **~18,600**, the 45s RPC-deadline family ~10,000, `canceling statement due to statement timeout` ~10,000. ⚠ **Counts only** — that tool's `users=`/`routes=` are documented as untrustworthy (smeared attribution, 2026-08-21), so no user-impact figure is claimed. ⛔ **The residual gap is CLIENT-side and it is real:** Vercel sees only server execution, and a repo check found **no non-Sentry client capture at all** (no `window.onerror`, no `unhandledrejection`, no client logging endpoint) — so browser-only classes like the React #418 hydration bug this repo has already been bitten by are currently captured by **nothing**. ✅ **SHIPPED 1:** the quota guard sampled ONE signature; rebuilt its sizing from Vercel (Sentry cannot report on itself), restricted to errors actually **THROWN** (a `console.error` line never becomes a Sentry event), and added `pg-statement-timeout` — **≈3,818/7d ≈ 16,400/month**, several times a 5,000 quota, the largest thrown class it was missing. ⚠ Pool-acquire timeouts deliberately NOT sampled (the existing negative control is right: a different fault, and it is the loudest signal about the real bottleneck). ✅ **SHIPPED 2 — a user-facing false claim:** `global-error.tsx` said *"Our team has been notified"*, which **has been false since 2026-08-18** — the capture fires, the event is accepted with a 200, and it is dropped. ⭐ **The test guarding that sentence could not see it:** it pinned *claim ⇒ capture-called*, which is right about the CODE and wrong about the WORLD, because **no test can observe whether a report is STORED** — the capture fired for every one of those users and the suite stayed green for eight days. Copy now matches the sibling boundary's honest voice (*"We logged it — trying again often works"*), and the test is **INVERTED, not deleted**: it still pins the capture and now pins the ABSENCE of a delivery promise, matched on the property rather than one spelling and **proven in both directions** (old copy + two synonym rewordings red it; new copy and the sibling's copy do not). 👉 Left, all free: the quota resets on the billing cycle and the guard's sufficiency needs one post-reset week to measure; **nothing watches "did Sentry STORE anything recently"** (a dropping collector looks exactly like a quiet week — that is how this went unnoticed); and **the real lever stays cutting query cost**, since every top cluster is a timeout — **an error tracker that can see the fire is not a fire extinguisher**

- [The four change-detection-less writers, measured — and the list is really ONE, not four](2026-08-26T1650Z-the-four-unguarded-upserts-measured-and-only-one-is-pinned.md) — completes the 2026-08-25 handoff's §7, then **corrects it twice, the second time against my own filing 20 minutes later.** Each function matched to its `pg_stat_statements` row **by SIGNATURE** (the PostgREST rows show only `json_to_record(…)`): `roll_pack_ask_hourly_low()` **726.3 MB WAL/day / 304,851 dirtied**, `apply_sales_counterparty(p_rows)` **530.5 / 180,192**, `upsert_pack_ask_state(...)` **385.6 / 55,879**, `refresh_wmc_fmv_drift_active(...)` **282.2 / 140,701** — **≈1.92 GB WAL/day, ~6.6% of all WAL.** ⛔ **CORRECTION 1 — ONE is pinned, not two** (checked three ways; the other three are not mentioned in the pin tree at all). ⛔⛔ **CORRECTION 2, appended to my own filing — "four unguarded writers" is ONE.** I re-derived the COST and the PIN STATUS and **did not re-derive the PREMISE**, which is the exact *a filed FINDING is a hypothesis* failure. Reading the four bodies reverses it: `apply_sales_counterparty` already carries `AND (s.seller_address IS NULL OR s.buyer_address IS NULL)` **and** an `ON CONFLICT … DO NOTHING` — guarded twice; `refresh_wmc_fmv_drift_active` carries `abs(wmc.fmv_usd - p.fmv_usd) > p.fmv_usd * v_frac`, a **deviation threshold STRICTER than `IS DISTINCT FROM`**; and `upsert_pack_ask_state` has no WHERE but sets `last_checked_at = v_now`, a **per-call HEARTBEAT**, so the row genuinely changes every call and **no predicate can skip it**. ⭐ **Two of the three "fixes" would have done DAMAGE** — one duplicates an existing guard, and gating the heartbeat would **delete the liveness signal** that shows the ask feed was polled at all. ⭐ **And their cost is largely not waste**: `apply_sales_counterparty` is a backfill IN PROGRESS, filling `sales` rows that genuinely lack counterparties. 👉 **Corrected picture: ONE mechanical target** — `roll_pack_ask_hourly_low`, where `DO UPDATE SET low_ask = LEAST(existing, EXCLUDED)` has no WHERE and `WHERE existing > EXCLUDED.low_ask` is provably equivalent (LEAST cannot change a row that predicate excludes) — **and it is the pinned one**; **ONE design question** (is a per-call heartbeat worth 385 MB of WAL a day to maintain a 4 MB table? not a predicate — whether to write it on every poll or on change plus a keepalive); **TWO closed, do not re-open.** ⚠ **Addressable WAL is therefore ~726 MB/day, not the ~1.92 GB the filing led with** — the 1.92 GB is correct as what those statements WRITE and wrong as what could be SAVED. ⛔ Not shipped: the one real target is pinned, and this session already spent its budget on a pin change it could not execute locally

- [The OG honesty convention is structurally UNAVAILABLE to a supabase-backed card — and one card claims "Live" on a failed read because of it](2026-08-26T1640Z-the-og-honesty-convention-is-structurally-unavailable-to-supabase-backed-cards.md) — ⭐ **mostly a NEGATIVE result, which is the useful part.** ✅ First: **known-issues #30's OG residual is STALE and should be struck** — the Underpriced-#1s card was fixed in `d2101312`, and it now computes `boardMaxAgeHours` → `boardLivenessLabel` **and renders it** (verified rendered, not just computed, since a dropped honesty signal is its own defect). ⛔ **Then my sweep falsified itself:** "reads data in a try/catch but uses neither `boardEmptyCopy` nor a `fetched` flag" flags **15 insights cards**, and almost all are FINE — they guard at the RENDER site (`total > 0 ? `${total} drops scored` : "Public · No signup"`, `rips > 0 ? fmtInt(rips) : "—"`), so a failed read leaves the counter at its initialiser and the card shows an em-dash or a description, **not a measured zero**. ⭐ **"Does not use the helper" is not the property; "publishes a number or a claim it did not measure" is.** Recorded so the next sweep does not re-open 15 non-defects. ⓘ Most `?? 0` hits there are per-ROW display defaults on successfully-fetched rows, not counts synthesised from a failure. 🚨 **The one real instance is `/api/og/insights/candy-mlb`**, with two defects: on a failed read it renders **"Live secondary FMV"** (no `fetched` flag, so empty and unreadable are the same branch), and ⚠ **its `catch` is nearly dead code** — it destructures `{ data }` and **not `error`**, and supabase-js RESOLVES with `{ data: null, error }` rather than throwing, so the failure never reaches the catch at all. ⛔ **The fix was WRITTEN AND REVERTED**, and why is the durable half: adopting `boardEmptyCopy` enrols the card in `api-og-insights-empty-vs-unavailable`, whose population is "every card containing the helper" and whose three assertions all assume a card that **self-fetches its own API** (`if (r.ok) { fetched = true`) and **renders a JSX rows list** (`) : (`). candy-mlb is neither — it produces a headline STRING and reads `supabaseAdmin` **directly and deliberately**, because (its own header) a self-fetch goes back through `proxy.ts` and is 302'd to /login while the surface is launch-gated. ⭐ **So the honesty helper is reachable only by fetch-shaped cards, and the one card that deviates for a documented, correct reason is the one that cannot adopt it — a convention enforced by a SHAPE excludes the cases that legitimately have a different shape, and the excluded case is exactly where the defect survived.** 👉 Two options (widen the guard's `fetched`/arm assertions, re-proven against all current cards; or fix candy-mlb without the helper), **both touching a guard protecting 15 cards, so neither is a side-edit**. ⛔ Not shipped: writing bespoke honesty copy specifically to stay OUTSIDE the guard that enforces honesty copy is the wrong instinct. ⚠ **`{ data }` → `{ data, error }` in that card is unconditionally correct and could ship alone** — it is currently a try/catch that cannot catch the failure it exists for

- [🚨 The public Underpriced-#1s board **503s**, and the query behind it is **32 ms warm / 19,895 ms cold**](2026-08-26T1630Z-the-underpriced-serials-board-503s-in-public-and-it-is-32ms-warm.md) — found while verifying the deal-board ingest fix: **the ingest now works and the board it feeds still fails.** `GET /api/public/insights/underpriced-serials` → **503 after 62 s**, reproduced twice ~10 min apart, while `?limit=5` → 200 in 18 s. ⚠ **The limit is a red herring** — `fetchUnderpricedSerials` always issues `.limit(500)` and applies `opts.limit` **in JS afterwards**, so both URLs run the identical query; the difference is cache state. ✅ **The honesty layer is working**: the route returns `boardUnavailable()`, so the failure surfaces as a 503 rather than as an empty board. ⭐ **The measurement that reframes it, same statement twice in one DO block: cold 19,895 ms, warm 32 ms — 622x** on a working set of just **3,069 buffers / 230 disk reads**. At this instance's ~74 ms per cold random read, 230 × 74 ms ≈ 17 s, which is the number. **There is no plan defect and nothing to optimise** — the board is tiny and simply always cold, because a 15-min `s-maxage` on a low-traffic public route means nearly every execution is the first in a long while. ⛔ **NOT caused by today's index work, checked rather than assumed**: the FMV leg uses the NEW index as an Index Only Scan and is only 1,499 of the 3,069 buffers, the dropped index was a strict subset of one that still exists, and the board's input held 280 active rows before today's ingest and 272 after. ⛔ **The obvious fix — add it to `WARM_BOARDS` — is argued against by [2026-08-15T1200Z](2026-08-15T1200Z-the-insights-cache-warms-half-its-boards-and-reports-perfect-health.md), which I read BEFORE shipping it**: three of that cron's five boards already fail the majority of ticks, its six board means sum to ~60.0 s = the route's entire `maxDuration`, and each failed warm burns 30 s of DB time producing nothing (~90 s per 5-min tick), so it is *"a meaningful contributor to the saturation it exists to survive"*. **Adding a sixth board to a Promise.all already losing three of five is a plausible way to make four of six lose.** ⭐ **What is new: this board is not in that cost class** — those five are 10–12 s means with 29.x s maxes clipped by `statement_timeout`; this one is 32 ms warm, so it adds almost no *work*, only a one-off cold tick. 👉 Two options (the lighter `/api/cron/warm` buffer warmer, lowest blast radius; or the snapshot cache, strictly better for users but the contention trade that filing reserves for Trevor). ⛔ **Not shipped — a freshness-vs-reliability trade on a public board is a product call.** ⚠ Worth fixing either way: the board's OG card is already flagged in known-issues #30 for asserting `Live deals` with **no age signal at all**

- [Daytime monitor — 2026-08-26 08:19 PT (first tick of day)](2026-08-26T1519Z-daytime-monitor.md) — read-only sweep, filed by `rpc-daytime-monitor` while a concurrent session was working. **An IO-saturation spell is ACTIVE** and is stated with a positive control (io_wait 6 / active 4; `rpc_ops_snapshot()` and the value-bearing `v_rpc_trust_health` scan both time out while the status-only scan and catalog reads succeed), so **everything is filed as a SYMPTOM and every cost figure is deferred to a quiet-window re-measure**. ⭐ New candidate: **`candy_offers_oldest_active_hours` and `candy_offers_unverified_pct` both went BREACH during the day** — neither was breaching in the 08:07Z nightly baseline, so both are new since ~01:07 PT. `detect_stalled_pipelines()` does **not** flag `candy-offers-indexer`, so the indexer is firing on cadence and the breach is offer freshness/verification aging rather than a stall — **hypothesis, not a measurement**, and the filing says so. Risk read low-to-medium (`/insights/candy-mlb` is public; no security or write impact). 👉 **Re-measure OUTSIDE a spell before concluding anything** — only a breach that survives the quiet window is a real candy-offers regression. Explicitly NOT re-filed as new: the 8-job pg_cron statement-timeout cluster (**one root, not 8 bugs** — #27 / R46 saturation collateral), the `board_mv_refresh_stale_hours` / `public_board_empty_count` / `public_board_slow_count` trust breaches (same family), and `compute-golazos-pack-ev`'s 874-minute silence (already filed)

- [🚨 Sentry is STILL dropping everything — but the 429 is gone, so the probe that "SETTLED" it now returns a clean 200](2026-08-26T1545Z-sentry-still-drops-everything-but-the-429-is-gone-so-the-probe-that-settled-it-now-reads-clean.md) — ⛔ **corrects a claim of mine from the same session ("the blackout is over") and qualifies the 0525Z filing's `SETTLED` headline.** The 0525Z `429 … error_usage_exceeded` is **no longer reproducible**: two probes today (14:33Z minimal, 15:22Z a properly-framed error envelope with item headers and byte `length`) both returned **200 with NO rate-limit header and an echoed event id — and neither has ever appeared.** Issues in 24 h: **0**. Error events in 24 h: **0**. **Last stored event is still `2026-08-18T13:21:59Z`**, eight days ago. ⓘ Controls rule out the boring answers: the DSN's project id resolves to the org's only project, which holds **5,386 events over 90 days**, so DSN and project are right and it has ingested normally before. ⭐ **The lesson: a 200 from the envelope endpoint means RECEIVED, not STORED.** The prior session concluded from a 429 and I nearly concluded the opposite from a 200 — **neither is a storage signal; the only sound test is whether a probe event shows up in the product.** ⚠ **The new behaviour is strictly worse to operate against**: a 429 is self-describing, a silent 200-and-drop is **unfalsifiable from the emitter's side**, so every SDK in the estate now believes it is reporting successfully — the honesty canon's own shape one layer down. 👉 **Operator action is unchanged** (Subscription → raise plan / on-demand budget), but **do NOT verify by re-POSTing and reading the status code** — that is what produced the false positive; verify by finding the probe in Issues. ⚠ **Re-derive the 15,388-events-in-a-week figure before quoting it again** — the project holds only 5,386 STORED events across 90 days, so it cannot be a count of stored events. ✅ Unchanged: the quota guard is deployed, defaults to SEND, and is still the right thing to ship before raising the quota — we just cannot tell it is working until storage resumes. ⚠ **Nothing watches this**: an arm on "Sentry STORED an event recently", not "answered 200", is the follow-up

- [⭐ The `targets` RPC: #30's hypothesis is now MEASURED and CONFIRMED, three candidate fixes falsified, and the fix is one INCLUDE column](2026-08-26T1500Z-the-targets-rpc-is-50s-of-DISTINCT-ON-and-edition-fmv-current-is-NOT-a-drop-in.md) — reproduces the `RPC Deal Board Ingest` failure live from Trevor's box (`-DryRun`, no writes): it dies at `GET targets failed: 500 canceling statement due to statement timeout` **before the first Atlas call**. ⭐ **known-issues #30 recorded on 08-23 that *"the DISTINCT ON is the cost" is a well-supported hypothesis, not a measurement*, and named the instrument — `EXPLAIN (ANALYZE, BUFFERS)`. It has now been run and the hypothesis HOLDS.** Warm-vs-warm decomposition of a 927,466-buffer call: the `latest_fmv` `DISTINCT ON` + join with **no estimate calls at all** is **841,312 buffers = 90.7%**, so all 14,748 `serial_fmv_estimate` calls are **~9.3%** — and #30's *mechanism* is confirmed verbatim by the plan: an `Index Scan` NOT Index Only, 871,797 rows read to return 19,678, because `confidence` is uncovered so the 120 MB `INCLUDE (fmv_usd)` sibling is declined. Production distribution (69 calls): **mean 13,513 ms, max 29,949 ms, 11,224 disk reads/call**. 🚨 **RETRACTS A CLAIM OF MY OWN made earlier the same session** — that ~96% of the cost was `serial_fmv_estimate`. I measured the CTE "in isolation" selecting **only `edition_id`**, a covered column, so Postgres served an **Index Only Scan**: the same CTE reads **32,641 buffers in that isolate and 818,698 in context, a 25x gap produced entirely by the projection**. ⭐ **Changing what a query SELECTs can change its PLAN, so an isolated sub-measurement is only valid with the real query's output columns** — I had a number, a control and a subtraction, all measuring a plan the code never runs. ⛔ **THREE fixes falsified by measurement:** (a) deferring the second `serial_fmv_estimate` is **provably equivalent** (`EXCEPT ALL` both directions at floor 100 **and** floor 0: 984=984 0/0, 7,374=7,374 0/0 — only **984 of 7,374 (13.3%)** survive the floor) but saves **1.7%**; (b) swapping to `edition_fmv_current` is 111x faster on an identical row count yet carries **233 fmv_usd + 79 confidence disagreements**, moving HIGH/MEDIUM 7,538 → 7,568 and **admitting 30 editions the board does not show** — second time in one day this lagging materialisation matched on COUNT and diverged on VALUE, and unlike the `rwfc` fix there is **no timestamp to guard against**; (c) a LATERAL is exactly equivalent but **2.5x worse on buffers**. 👉 **The fix is adding `confidence` to the INCLUDE list of an index that already exists**, restoring the Index Only Scan (~25x on 91% of the query) — **no semantics change, and index DDL pays no PGRST002 burst**. ⏳ **NOT BUILT: instance read 9 active / 8 IO-wait / 1 autovacuum, a saturation spell** (recipe wants ~3/1/0), and some of that pressure was **mine**

## 2026-08-25 — 17 filings

- [🚨 SETTLED — Sentry is not broken, misconfigured or blocked: the ORG ERROR QUOTA is exhausted, and ONE already-tracked signature burned it](2026-08-26T0525Z-SETTLED-sentry-is-not-broken-the-org-error-quota-is-exhausted-and-one-signature-burned-it.md) — closes the diagnosis half of known-issues **#38** after seven days. ⭐ **Settled by a single request**, not by more code reading: one POST of a minimal envelope at the production DSN answers `429` with `x-sentry-rate-limits: 60:default;error;security;attachment:organization:**error_usage_exceeded**`. ⛔ **The premise that kept this open is FALSE** — three prior filings recorded that the sandbox *"gets the agent proxy's 403, not Sentry's"*; ingest **is** reachable from the Cowork cloud container, and the empty-body control (`400 empty request body`) proves the key is live. **When an observability pipeline goes dark, probe the COLLECTOR before auditing the emitters.** ⭐ **The burn is one signature:** `edition detail unavailable: rpc get_edition_detail timed out after 45000ms` threw **15,388 times / 2,963 users in ONE week** — so a single known-broken RPC eats a monthly error budget in ~2 days, and **raising the quota alone changes nothing**. Patch written (not pushed): `lib/observability/sentry-quota-guard.ts` samples that signature 1-in-20, **DEFAULT IS SEND** for everything else, stamps `sentry_sample_rate` on kept events so counts stay readable, and leaves the drops visible in Sentry's own client-report stats. Four mutations run against it, all caught. ⛔ Operator half — the quota itself — is Trevor's

- [⭐ SHIPPED — the PG17-unreachable index is repaired, "only EXPLAIN settles it" is REPLACED by a decidable rule, and one judgement is RETRACTED](2026-08-26T0620Z-SHIPPED-the-pg17-index-is-repaired-and-the-shape-IS-a-decidable-rule-after-all.md) — `idx_sales_2026_fmv_recalc_window` rebuilt without the redundant `edition_id IS NOT NULL` conjunct via the one-off pg_cron CIC recipe — **202 s, `indisvalid`, old index dropped, renamed back, role budget and job count verified restored**. On the **unmodified** production query the plan node goes **51,040.92 → 15,264.74** and `EXPLAIN (ANALYZE, BUFFERS)` reads **18,124 ms** against the 50,471 ms on record — **~2.8x, reproducing the predicted 2.9x**. ⚠ **The buffer half did NOT reproduce and the reason is named:** `Heap Fetches: 82,082`, because `relallvisible/relpages` was 83.2%; a `VACUUM (INDEX_CLEANUP OFF, ANALYZE)` took it to 88.6%. ⭐ **The rule:** such an index is reachable **iff the QUERY independently supplies a strict clause on that column (`=`, `<>`, `>`) or an inner join on it** — all six sweep indexes now classified, **5 reachable, 1 not**, and `idx_sales_2026_top_sales_board` is reachable **today** (its "563 scans and unreachable" worked example is refuted; a *paired* idx_scan delta does settle current reachability where a cumulative total cannot). 🚨 **RETRACTION: `sales_2022` is NOT an empty partition — `count(*)` = 750,702**, and its indexes carry **43.8M scans**; `n_live_tup` read 0 only because the partition has never been analyzed. **`n_live_tup` is an ESTIMATE and on a never-analyzed relation it is indistinguishable from empty**

- [⭐ SHIPPED (cadence) — two pack-sales backfills were **71.9 GB/day of disk reads and 4.55 GB/day of WAL** to add ~165 rows a day](2026-08-26T0635Z-SHIPPED-two-pack-sales-backfills-were-72GB-a-day-of-disk-reads-for-165-new-rows.md) — previously unfiled, found while checking whether an unrelated filing's "empty partition" claim was safe to act on. `topshot_pack_sales_history` reads **3,558 inserts against 14,903,768 updates**; allday **106 against 9,956,771** — two tables of immutable historical sales, rewritten wholesale forever, because PostgREST's default `ON CONFLICT DO UPDATE SET <every column>` carries **no change-detection predicate**. Four statements = **~9.2% of the instance's ~780 GB/day of disk reads**; ⭐ **the bigger half is the two `LIMIT $1 OFFSET $2` SELECTs at 19,183 and 16,587 disk blocks PER CALL** — the same "OFFSET does not paginate" defect, re-reading whole tables ~200x/day each. **Shipped:** jobids 25/29 cut `*/3` → 15-minute, offset by a minute (480 → 96 dispatches/day each); not a freshness regression — the newest rows were already 3.4 h and 15.3 h old. ⛔ **`suppress_redundant_updates_trigger()` deliberately NOT shipped**: it makes suppressed rows emit no `RETURNING`, PostgREST's `page_total` falls, and **the writers are edge functions with no committed source (R21) so the caller cannot be read**. ⚠ **Falsifier named: if INSERTS fall too, the walk was covering ground — revert**

- [🚨 `refresh_wmc_fmv_changed` is **40% of every block this database dirties** — and its lever is a 13 MB table that already exists](2026-08-26T0645Z-refresh-wmc-fmv-changed-is-40pct-of-all-writes-and-the-lever-is-a-13MB-table-that-already-exists.md) — **10.0% of disk reads, 40.2% of blocks dirtied, 37.2% of WAL, 11.0 h/day of exec time** across its two callers — larger by itself than the whole pack-sales cluster filed beside it. ⛔ **The obvious lever is FALSIFIED, recorded so nobody spends the afternoon:** `v_chunk constant integer := 5` is conspicuous and commented and is **not** the cost — the loop's working set is only **515 rows**. ⭐ The cost is a **correlated latest-FMV subquery carrying no partition key**, so it `Append`s across every `fmv_snapshots` partition (707 MB) reading ~64 rows to return 1, **~147,000 times a day**. **`public.edition_fmv_current` already exists** — a real 13 MB table keyed on `edition_id`, refreshed within minutes — so the fix is one CTE. ⭐ **Equivalence PROVEN over a hash sample rather than asserted:** 274 editions, **0 value disagreements**, differing only in coverage; population gap **244 / 27,257 = 0.9%** — which is why the recommendation is **`COALESCE(efc.fmv_usd, <subquery>)`**, strictly better than a straight swap. ⚠ Expect the READ figure to fall sharply and dirtied/WAL much less — the UPDATE half is real work. ⛔ **NOT SHIPPED: the function carries a DB-invariant pin, and re-pointing a pin needs a commit.** ⭐ **Generalised: a pinned SQL function is PUSH-GATED — a no-push session's real DB levers are pg_cron schedules, indexes, and brand-new objects**

- [⛔ Git push from a Cowork cloud session — the credential is PRESENT AND VALID; the repo is not in the session's authorized set](2026-08-26T0650Z-git-push-is-a-repo-AUTHORIZATION-problem-the-credential-is-present-and-valid.md) — answers Trevor's standing question and sharpens CLAUDE.md's framing. ⭐ **The decisive control, never run before:** same session, same token — `GET /user` returns **200 `{"login":"jamesdillonbond"}`** while `GET /repos/jamesdillonbond/rip-packs-city` **and** an unrelated public repo both return **403**. **So it is a per-repository allowlist applied ahead of the credential, not a missing or expired token** — and `/user` 200 is the one-line test a blocked session should run first. Tested and does NOT work: the device VM (reaches github.com, has no credential helper, `gh` absent — it is **not** the Windows box); an `add_repo` tool (named by the proxy's own error, **not available in this session**). ⚠ **The Project's GitHub sync is a different system** — a read-only knowledge connector with no relationship to the git proxy's authorized set, and its presence invites exactly this confusion. **Fix is at session creation: attach the repo as a session source** (or `/web-setup` in a real terminal, which fixes the NEXT session). ⛔ Routing around it was not attempted — the proxy README and CLAUDE.md both forbid it. ⭐ **Second gate, unrelated to git: `apply_migration` itself creates a push obligation**, since `migration-parity` is enforcing and needs a committed file per applied name

- [✅ RESOLVED by the owning session (was: SECURITY, low-harm, filed-not-fixed) — `_rpc_waste_baseline_20260825` was ANON-READABLE and is what is reddening the smoke test right now](2026-08-26T0650Z-a-scratch-baseline-table-is-anon-readable-and-is-reddening-the-smoke-test.md) — `/api/smoke-test` has been logging a HARD FAILURE (newest **2026-08-26T06:32:45Z**): `check_public_security_invariants` → `1 violation(s): rls_off_base_table:_rpc_waste_baseline_20260825`. Measured against `pg_class`: **RLS false, 0 policies, `has_table_privilege('anon', …, 'SELECT') = true`** — so it is live at `/rest/v1/_rpc_waste_baseline_20260825`. ✅ **Sensitivity checked BEFORE deciding and it is LOW:** 11 rows of `pg_stat_statements`-style counters (`calls, total_exec_ms, blks_read, wal_bytes, …`) — **no user data, no PII, no secrets**. ⛔ **DELIBERATELY NOT FIXED, for measured reasons:** (1) it is **another session's in-flight artifact** — stamped `20260825`, holding an "rpc waste baseline" a concurrent session was building tonight, so dropping it destroys their data and altering it out-of-band diverges it from a migration they may be about to commit; (2) **an ACTIVE saturation spell, measured: `io_wait 17 / active 24 / 48 total` at 06:35Z**, and any DDL triggers a ~10–20 s user-facing `PGRST002` burst — the worst moment to add one; (3) the exposure is low-harm enough not to outweigh those overnight. 👉 **Morning decision:** `ENABLE ROW LEVEL SECURITY` (no policies) in a quiet window if the baseline is still wanted — `postgres`/`service_role` BYPASSRLS so the owner is unaffected — or `DROP TABLE` if finished with, **which is the owner's call, not a sweeper's**; ⚠ re-stat the object immediately before acting. ⭐ **Found by a POSITIVE CONTROL, not a security sweep** — proving "did my own deploys break anything" required a project-wide error query to show the instrument was not merely quiet, and the control's answer contained a finding nobody was looking for. ✅ **RESOLVED ~08:1xZ by the owning Cowork session** — re-measured 10:0xZ: `relrowsecurity=true`, `anon` SELECT revoked, **0 RLS-off anon-readable base tables remain**. The fix applied is the one recommended here, and **the decision to file rather than fix held up**: waiting cost ~3.5 h of low-harm exposure and avoided destroying another session's data or firing a `PGRST002` burst mid-spell

- [🚨 `atlas-proxy` will NOT un-red `topshot-active-listings-ingest` — re-verified live, and the real blocker is ONE RPC](2026-08-26T0630Z-atlas-proxy-will-not-un-red-the-listings-ingest-the-targets-rpc-is-the-blocker.md) — the last **12 of 12** GHA runs are `failure`, and the newest (**2026-08-26T04:07Z**) dies **31 s in** at `GET targets failed: 500 {"error":"canceling statement due to statement timeout"}`, **before the first Atlas call**. ✅ **known-issues #20's own warning — *"do not credit #20 with un-redding this workflow"* — is therefore RE-VERIFIED, not corrected.** 🚨 **The instrument trap, and I nearly published the opposite:** read from `pipeline_runs` alone the answer is *"9 of 9 failures in 72 h are `egress_blocked`, zero DB timeouts"*, which reads as *"ship atlas-proxy and it's fixed"*. That is wrong **by construction** — the route writes its row only on the FINAL POST (`if (body.final || body.deactivate)`) while `?phase=targets` runs FIRST, **so a targets timeout can never produce a row**. Corroborated: only **13 runs logged in 72 h** (112 over 22 days) for a pipeline firing ~8–11×/day, with `last_error` = `egress_blocked` on every one of those days — exactly what a blind instrument looks like. ⭐ **This is also why the original ~60% `egress_blocked` figure arose**: it is the rate *among rows that exist*. 👉 **Cause located:** `topshot_serial_board_candidates` calls `serial_fmv_estimate` **TWICE PER EDITION** across **13,199** eligible TopShot editions, and the `$100` floor sits in the OUTER select — **the floor bounds OUTPUT, not COST** — on top of a `DISTINCT ON` over **871,886** snapshot rows. Same family as the `drain_fmv_cold_tail` fix. ⛔ **NOT shipped — FMV/ingest route logic, Trevor's call**; two candidate directions filed (defer the second call behind the floor via LATERAL; or a monotonicity pre-filter that must be MEASURED, not assumed), each requiring whole-population equivalence proof and a **buffers** comparison

- [⛔ CORRECTED — the pg_cron re-stagger's exit condition passes **~33%** of the time if the fix did nothing (published 74%, WITHDRAWN) — `check_pgcron_recent_failures()` reports LATEST-RUN status, not "has this job been failing"](2026-08-26T0525Z-the-cron-reschedule-exit-condition-passes-74pct-of-the-time-if-the-fix-did-nothing.md) — the function gates on `where l.status = 'failed'`, so a job appears only if its **most recent** run failed. 🚨 **Job 331 is already absent from the report and was before any tick under the new schedule** — its 2026-08-26 03:09Z run succeeded, so all 8 of its startup timeouts sit in the window unreported. Measured baselines over full retention: **198 → 4/38 (10.5%)**, **249 → 4/23 (17.4%)**, **331 → 8/39 (20.5%)**, giving `P(all three quiet tomorrow | reschedule did nothing) ≈ 1.00 × 0.895 × 0.826 = 74%`. ⚠ **The last-3-runs snapshot reads `fail, fail, succeed` = 2-in-3 and is wrong by ~6x** in the direction that flatters the fix — a distribution, not a snapshot. ⚠ **n = 0, not n = 1:** every retained run still fired on the OLD minute (`:40`, `:09`); 331 did not also fire at 03:55Z, bracketing the change to after that, so first observations are 09:54/09:55/09:56Z. `cron.job` confirms all three carry the new schedule, owner preserved, `active=true` — config right, evidence absent. ⭐ **The check is ASYMMETRIC and still worth running: silence proves nothing, but one `job startup timeout` falsifies the fix outright** — read it as a falsifier, never a clearance. 👉 **Suggested gate: 331 at 14 clean ticks ≈ 3.5 days** (4 ticks/day, highest rate); 249 needs ~16 days and 198 ~27 days at their own baselines, so neither daily job can be the thing anyone waits on. ⚠ Arithmetic assumes independent ticks and they are NOT — failures cluster in a ~15 min/day worker-pool spike, so the counts are **optimistic**; order-of-magnitude only. ⛔ **CORRECTED 05:45Z — the 74% is WITHDRAWN; it was 33%, and the recommended gate was BACKWARDS.** The baseline was **POOLED ACROSS A REGIME CHANGE**: per-day, 198 and 249 each ran **0 failures / 15** through 08-19 and **4 / 6** from **08-20**, while 331 sits at 4/16 (25%) since 08-22. 🚨 **198 and 249 fail on EXACTLY the same days** — direct evidence of the collision (both on `40 9 * * *`), and it means **they are ONE arm, not two**, so multiplying them overstates the evidence. ⭐ **The gate INVERTS: 198+249 need just 3 consecutive clean daily ticks (~3 days)** at their 66.7% failure rate — the fastest, strongest signal — while 331 needs 11–14 ticks (~3 days). The earlier "~16 and ~27 days" was a pooling artifact. ⭐ **The durable lesson: "a distribution, not a snapshot" is necessary but NOT sufficient — the distribution must be REGIME-AWARE**, and a pooled rate across a changepoint is the most convincing of the three wrong answers because it is computed off a large n. ✅ Unchanged: the `l.status='failed'` latest-run finding, `n = 0`, and the asymmetric reading

- [⭐ PROMOTED 2026-08-25 — a stale `.git/index.lock` makes `git checkout --` and `git merge` SILENTLY no-op — and `merge` PRINTS `Updating X..Y` while HEAD never moves](2026-08-26T0411Z-git-gotcha-a-stale-index-lock-makes-merge-print-Updating-while-HEAD-never-moves.md) — docs-only candidate from a live incident pushing the cron-reschedule ledger entry (`2f2736c5`). 🚨 **The novel half is a LYING INSTRUMENT:** a zero-byte `index.lock`, 5 min old with **no git process alive**, ate a `git checkout --` and a `git merge`, and the merge still printed a success line. **Verify a merge/checkout by `git rev-parse HEAD` before/after, never by the printed `Updating` line.** A sharper variant of the documented phantom-`index.lock` class on the Windows↔sandbox mount — the new part is that the failure reports success. ⓘ Second lesson is a REINFORCEMENT, not new: the same session hit two identical-looking push rejections with unrelated causes (sandbox `could not read Username` = dead pushurl harvest; local `tip of your current branch is behind` = non-fast-forward, 3 behind), reaffirming **diagnose a push failure from the ERROR STRING, not from the fact that it failed**. ✅ **PROMOTED — both halves now live in `docs/reference/tooling-gotchas.md`.** Lesson 1 is its own section, placed beside the `git push | tail` banner as the same family (a git command reporting success over a no-op). ⛔ **One instruction in this filing was NOT followed, deliberately:** its *"then archive this inbox file"* conflicts with the **append-only rule** (nothing filed on/after 2026-08-17 may move to `inbox/archive/`), so it stays here and is marked promoted instead. ⛔ **And its suggested removal step was CORRECTED, not copied:** *"confirm no git process is running, then remove the lock"* is the fix that was measured and **REJECTED on 2026-05-31** — the `.git` is SHARED with the Cowork sandbox, so a process check on one side cannot see a live git process on the other, and an age-based auto-clear can unlink a lock a concurrent writer legitimately holds. Manual clearing from Windows stays a human step; only the **detection** half (`git rev-parse HEAD` before/after) generalises. ⭐ Lesson 2 (the string-spread splice corruption) landed as the **fifth instance** in the existing ledger-splice section, where it defines the heading-count check’s blast radius: it catches SHAPE, not IDENTITY


- [🚨 `candy-listings-indexer` is NOT silent — the cron fires, the route runs, and the work is KILLED at 300 s on 73% of ticks; zero completions in 21 h](2026-08-26T0400Z-candy-listings-indexer-is-not-silent-it-is-killed-at-300s-on-73pct-of-ticks.md) — the one entry left **unclassified** in the live-alert triage, and it became the night's largest finding. `detect_stalled_pipelines()` says *"silent 1202 min"*, which reads as a stopped scheduler and sends you to the trigger — **the trigger is fine**. It is a **Vercel cron** (`35 */3 * * *`), so the pg_cron discriminator does not apply, but the route's **invocation heartbeat** (deep-audit R11) gives the same three-way split over 72 h: **6 completed · 16 invoked-then-KILLED · 2 never fired** (24 expected). ⭐ **Mechanism confirmed by a second independent instrument:** route-filtered Vercel runtime errors show `Task timed out after 300 seconds`, **count=17**, `last=2026-08-26T00:35:42.000Z` — matching the last heartbeat **to the second**. ⚠ **NOT the Sentry filing's 2,666-event figure** — route-filtering gives **17**, and this repo's own rule says `get_runtime_errors` attribution is smeared and must be re-grouped on `requestPath`. ⚠ `first=2026-06-16`: not new, but it now fails *every* tick. ⭐ **Why nothing caught it: a killed `after()` writes NO terminal row, so `pipeline_runs` holds only the successes and reads 6/6 = 100% ok** — "green pipeline blind to its own work" in its purest form; `try/catch` cannot catch a `maxDuration` kill, which is why the heartbeat exists, and **no instrument consumed it**. ⛔ **NOT shipped — ingest logic**, and the route's own *"complete sweep or abort"* invariant means naive chunking would start **deactivating live asks**. 👉 **The reusable half:** nothing anywhere watches *heartbeat-without-terminal*; one such arm would have caught this on 06-16 and generalises to every `after()` route. ⓘ Sibling control: `candy-offers-indexer` 11/12 (~92%) — Candy ingest is healthy, this is one sweep outgrowing its budget


- [⭐ SHIPPED — `weekly-db-maintenance` is DAILY not weekly, and the 24h floor was marking a perfectly healthy WEEKLY job red ~6 days in 7](2026-08-26T0320Z-weekly-db-maintenance-is-daily-and-the-24h-floor-makes-a-healthy-weekly-job-permanently-red.md) — found by applying the dispatch/landing discriminator to the **live** `detect_stalled_pipelines()` set rather than to golazos alone. 🚨 **The nightly pass's own falsifier FIRED:** its 08-25 handoff said *"watch for a second miss"*, and jobid 198 (`40 9 * * *`) has now **failed 3 of 5 days with `job startup timeout`** — worker starvation. ⚠ **Sharpens the discriminator:** starvation shows up as EITHER no `job_run_details` row OR a row with `status='failed'`/`job startup timeout`. ⛔ **The obvious consequence is FALSE, measured not assumed** — `pipeline_runs` retention is fine at 74.0 h because `prune_pipeline_runs` has its own `41 */6` cron; the real residue is ~30 MB of log tables, **minor**. ⭐ **Two instrument bugs shipped:** (a) `pipeline-health` declared it **7 days** for a **daily** job — while its twin `weekly-wmc-prune` (jobid 199, `20 10 * * 0`) was correctly 8 days, **two identical-looking entries where only `cron.job.schedule` tells them apart**; (b) 🚨 the `>24h → red` branch fired **before** the 2×/5× multiples **unconditionally**, so any `expectedMin >= 720` had an **unreachable yellow branch** and a genuinely weekly job read **RED ~6 days in 7 by construction** — `weekly-wmc-prune` ran exactly on schedule Sunday and reads red 65 h later. For `expectedMin <= 288` the floor was already redundant, so **it only ever changed the answer where it was wrong**. ⚠ **I had this wrong first** (claimed pipeline-health read it green; the unconditional floor already reds it) — reading `classify()` instead of publishing the tidy story caught it. Tests are a **PAIR**: unconditional floor reds the weekly case, deleting the floor reds the daily control, correct scoping 9/9. ⛔ **Starvation itself NOT fixed** — a platform-capacity decision


- [⛔ §2 RETRACTED — the golazos "scheduler-skip" is REFUTED (the cron fired 8/8), but the pg_net DNS mechanism I published was WRONG](2026-08-26T0210Z-the-golazos-scheduler-skip-is-refuted-22pct-of-all-pg-net-dispatches-time-out.md) — **supersedes the 1809Z filing's root cause.** `cron.job_run_details` for **jobid 44** (pg_cron `37 */6 * * *` → `net.http_get` to an edge fn, **not** a Vercel cron) shows **8 of 8 ticks fired and succeeded in 48 h**, including all three called missing. **The dispatch always happens; the `pipeline_runs` row does not**, so the failure sits downstream of pg_cron and upstream of the function's own logging. Measured over 701 `net._http_response` rows: **155 timed out (22.1%)**, and **70 consumed their ENTIRE timeout inside DNS** (`DNS time == total time`) so those requests never left the box — **a 10.0% hard floor**; the other 85 are unclassified because several crons legitimately out-wait a short `timeout_milliseconds`. ⭐ **NOT saturation collateral:** hourly rate is **20.0–24.3% flat** across `io_wait`=19/21 (spell) → 0 (quiet), so PRIORITY 3's bar on re-opening saturation does not apply. ⛔ **CORRECTED 02:50Z by a no-change control that cuts against my own attribution:** applying the discriminator to every `net.http_get` job (with a `name_matches` control, without which six jobs show thousand-dispatch "gaps" that are pure naming mismatches) gives **jobid 42 `compute-pinnacle-pack-ev` — same family, same `*/6` cadence 20 min apart, same 120 s timeout — landing 12 of 12 over 72 h, against golazos's 5 of 12.** A uniform ~10% DNS failure cannot produce that asymmetry (losing 4+ of 8 at p≈0.10 is ~0.5%), so **the platform rate does NOT explain golazos and something golazos-specific dominates.** Not a retention artifact — pinnacle's own oldest retained row is 72 h old. ✅ The platform measurement and the discriminator both STAND; only the causal link to golazos is withdrawn. ⛔ Same caution for jobid 55 (282/13), which has a standing "finite walk is DONE" filing. 👉 Settle it by sampling `net._http_response` within ~6 h of the 06:37/12:37/18:37Z tick: timeout = egress, non-200 = function/gate, 200-with-no-run-row = a third defect. ⛔⛔ **§2 RETRACTED 04:50Z by a prior measurement already in my own memory store.** I claimed 10.0% of dispatches *"never left the box and the function definitively never ran"*. **pg_net attributes TOTAL ELAPSED to the resolution field on timeout**, so `DNS time == total time` is an artifact of the timeout, not a DNS measurement — and the tell was in my own quoted data (`Total time: 90002.632000` / `DNS time: 90002.632000`, identical **to the microsecond**). ⭐ **Verified live:** in the same 6 h window `net._http_response` held 708 responses / 140 timed out while **jobid 84 dispatched ~180× and logged 176 completed runs (~98%)** — timeouts and completions coexist at scale. It also better explains the "flat across the spell" result: a rate dominated by jobs whose configured timeout is shorter than their runtime is stable **by construction**. ✅ **Still standing:** the cron fired 8/8, the dispatch/landing discriminator, and the pinnacle 12/12 sibling control. ⛔ **The golazos gap is now simply UNEXPLAINED.** 🚨 **The expensive lesson:** `pgnet-http-response-is-the-edge-fn-instrument` recorded this exact caution on 2026-08-08 and I did not read it before publishing

- [🚨 Sentry is dark on **DAY SEVEN** — and the monitor has been reading that silence as a clean bill of health](2026-08-26T0100Z-sentry-is-dark-on-day-seven-and-the-monitor-reads-the-silence-as-health.md) — third re-measurement: newest Sentry event **still 2026-08-18** (`lastSeen:-48h` returns nothing) against a Vercel control of **50 error groups, newest 00:50:37Z — taken in the same minute**. Duration **~7d 11h**, up from 4d 17h. 🚨 **The new finding is second-order and it is FIXED here:** both 08-25 monitor filings logged *“no new Sentry issues in 24h”* in the **healthy** column, because `rpc-nightly-autonomous-pass/SKILL.md` §2 says “run Sentry, then distrust each” and lists distrust bullets for **six** instruments with Sentry not among them — a seventh bullet now states the discriminator (**a Sentry zero is health ONLY if the Vercel 24h groups are also near zero**), promoted to `testing-and-ci.md` so it outlives the skill. ✅ **Two negatives VERIFIED, not inherited:** the MCP catalogue genuinely has **no** stats/usage/quota tool (searched, not assumed), and ⛔ **the decisive ingest probe cannot run from a cloud sandbox** — it returns the AGENT PROXY’s `403 Host not in allowlist`, which is **not** a Sentry signal and must not be read as one. ⛔ **Attribution UNCHANGED and still operator-gated** (quota remains a hypothesis; Stats/Usage, or a probe from a box with egress, settles it in one command). ⚠ Also re-tests and **fails** the exit condition on the two-client-init filing — do not consolidate the Sentry inits while ingest is dark

- [Daytime monitor — saturation spell in progress at 21:10Z (SYMPTOM only)](2026-08-25T2110Z-daytime-monitor-saturation-spell-symptom.md) — `rpc_ops_snapshot()` **timed out** inside `get_pipeline_alerts_core()` (a snapshot timeout is itself a spell signal); positive control **io_wait=21 / active=33 / total=44** (~64% of active sessions in IO wait) = **confirmed spell** on the SMALL 2 GB IO-bound instance — the known root-cause class, not a new one. ⭐ **Independently corroborated:** a separate interactive read 18 min earlier (20:52Z) returned **io_wait=19 / active=33 / total=44** — two instruments, same picture. ⚠ **The ONE novel, un-attributed signature:** **25 identical stacked `pgrst_source` PostgREST scalar reads** all in IO wait, oldest ~3m01s, plus `refresh_mv_pack_ev_latest()` in IO wait ~6m09s — query text was truncated at capture, so the calling route is UNKNOWN. If one caller is fanning out 25 concurrent identical scalar RPCs that is a work-cutting lever (PRIORITY 3: cut work, never raise a timeout). ✅ Healthy and unaffected: security 0/0, Vercel last prod deploy READY, no new Sentry issues in 24h. ⛔ **Heavy checks deliberately SKIPPED** (spell discipline — each stacks IO onto the spell and a timeout during one is a symptom, not a broken artifact); every causal/cost claim deferred to a quiet-window re-measure

- [⭐ The `nfl_all_day` promote leg is NOT dormant — it resolved a row **16 minutes** before measurement, and the SUNSET label describes the COLLECTION, not this pipeline](2026-08-25T2113Z-the-allday-promote-leg-is-not-sunset-it-resolved-a-row-16-minutes-ago.md) — **closes open item 3 of the 08-25 handoff** (*"reconcile the label with the backlog before acting"*). Measured: **105,027 unresolved**, `last_resolved_at` **20:36Z**, **~1,524 resolutions in a rolling 7 d** against **~135/week of new durable residual** — decisively **net-draining**, newest unmapped sale **from today**. ⛔ **CORRECTED 02:40Z: an earlier "net-draining ~966/week" here was WITHDRAWN** — it subtracted a survivorship-confounded `ingested_at` count, because `promote_unmapped_sales` DELETEs resolved rows 7 d after resolution, so `unmapped_sales` is a **staging queue, not a log**. ⭐ **The data-quality sweep's metric was the sound one and mine was the broken one:** unresolved rows are never purged, so unresolved-by-`sold_at` is complete and permanent, while any `ingested_at` window is depleted by the archive. ⚠ `resolved_ever == resolved_7d` is that DELETE's **retention boundary, not a start date**. ⭐ **The contrast case sits in the same table:** UFC and Golazos read `last_resolved_at` = **NULL — never resolved a row, ever** (which is why parking those two on 08-24 was right), and Top Shot reads **0 unresolved of 24,583** (genuinely finished). **AllDay matches neither shape.** ⛔ **Do not retire the leg on the label**; if the 7,606 s/day is the concern that is a separate COST question, and the 0.6% hit rate points at an over-wide candidate predicate. ⚠ **One half is deliberately UNSETTLED:** the sweep reads inflow *decreasing* (58 vs 137 by `sold_at`) and this read *increasing* (611 vs 177 by `ingested_at`) — **different clocks on the same rows, so neither is wrong**; the `sold_at` split timed out twice against a **live spell** (`io_wait=19 / active=33`), so it is open for a quiet window, and the reconciliation does not depend on it

- [⛔ SUPERSEDED (root cause) — `compute-golazos-pack-ev` has skipped ~2–3 scheduled ticks (cadence-silent ~17.5 h) while the pipeline is healthy every time it runs](2026-08-25T1809Z-compute-golazos-pack-ev-cadence-silent-17h-pipeline-healthy-when-it-runs.md) — **LOW, and NOT user-facing**: the golazos pack-EV board is fresh (`pack_ev_board_max_stale_days` 0.62 of 2, `pct_depleted` 0, `publish_shortfall_pct` 0.8) and the market is thin (MEDIUM 2 / LOW 87). Last five runs are all `ok:true`, `rows_written` 29–36 — so this is a **scheduler-skip, not a pipeline failure**: the 08-24 18:37 tick and BOTH of 08-25’s 06:37 and 12:37 ticks wrote no row at all. ⚠ **Filed as a symptom, not a diagnosis** — the skips coincide with a 24h cluster of `statement timeout` / `job startup timeout` across ~13 MV/rollup jobs (the `max_worker_processes`=6 vs `cron.max_running_jobs`=32 starvation class, one IO root cause not N bugs), but the file-time positive control read `io_wait=2 / active=2 / total=41`, i.e. **not in a spell at that instant**, so the skip is historical and the cause must be re-measured, not asserted. ⛔ First step is to establish **what actually triggers it** (pg_cron entry vs Vercel cron) — check `compute-allday-pack-ev` and `compute-topshot-pack-ev` in the same pass, since worker starvation would be a shared symptom rather than a golazos one

- [🚨 `rpc-ccm-step2` is NOT saturation collateral — it nested-loops 220 whale wallets holding **72% of the Top Shot partition**](2026-08-25T1535Z-ccm-step2-is-not-saturation-collateral-it-nested-loops-72pct-of-wmc.md) — **SUPERSEDES the 00:11Z filing's root-cause attribution.** That candidate called the timeout "a disk-IO saturation symptom" and cited a PRIORITY 3 bar on re-investigating it — but the job is **8-for-8 failed since 08-18 across two different clock windows**, the durations are **bimodal (9.2/9.7/13.1 s vs 300.0 s exactly)**, and the filing's own control read `io_wait=0`. Measured cause: the 220 cohort wallets hold **1,363,128 of 1,888,824 TS rows = 72.2 % of the partition**, and the planner estimates **664,888 (2.05× under)**, so it picks a nested loop where one sequential pass is right. ✅ **RESOLVED AND SHIPPED THE SAME DAY.** The A/B ran once the spell cleared (`io_waiters 2`): **hash join 122,524 buffers / 28.4 s COLD**, against a nested loop that **did not complete in 140 s WARM**. The predicted join cardinality (1,363,128) matched the actual (**1,364,074**) to **0.07 %**. `SET LOCAL enable_nestloop = off` shipped; the mat is fresh for the first time since 08-17 (**66.5 h → 3 min**), and it completed **under active contention**. `db:pins:check` **189/189 clean**. ⛔ Open: the SQL invariant test runs only in CI (no `psql` on this box), and **the falsifier is named — `rpc-ccm-step2` at 23:25Z tonight**

## 2026-08-24 — 7 filings

- [🚨 All three Flowty listing caches hold EXACTLY 100 rows, because `offset` does not paginate — at least 5,000 are obtainable in ONE request](2026-08-25T0620Z-every-flowty-listing-cache-holds-exactly-100-rows-because-offset-does-not-paginate.md) — Top Shot / All Day / Golazos each sit on **exactly 100** cached rows; three markets do not independently produce 100, a `PAGE_LIMIT` does. Offsets 0–400 at limit 100 = **500 fetched, 103 unique**. ⚠ `reportedTotal=10000` is a PLACEHOLDER (**identical with and without the filter**), so the loop's own completeness test can never fire and no instrument could see the gap. 🚨 **The tell is written in the routes' own comments TWICE** — "`PAGE_LIMIT=50` runs at ~56 listings/run", "`PAGE_LIMIT=24` … capping runs at ~48" — the book size tracked the page arithmetic and both times the fix was to raise the limit. ⛔ **NOT shipped: `cached_listings` feeds the deals board, alerts and the ASK side of FMV, and 100→5,000 rows × 72 ticks/day is ~360k upserts/day against R46's IO ceiling.** A cadence+volume decision, not a constant

- [`topshot-pack-pool-backfill`'s last error shifted from the ledger's documented 403 to a 200-level "no editions"](2026-08-25T0312Z-pack-pool-backfill-error-shifted-from-403-to-a-200-level-no-editions.md) — jobid 16, **224/225 runs failed (99.6%) over 2 days / 259 fails in 24h**, the single largest failing pipeline on the board. ⚠ **The ledger's "403-dead historical lane" belief for this job is now STALE** — the sync path reaches the endpoint at 200 and finds nothing to convert. Two readings the monitor cannot discriminate read-only: **exhausted finite backfill** (a mis-classified done-state dominating the fail board) or a **real conversion regression**. Spell positive control `io_wait=0, active=0` — not saturation collateral. A decision, not a diagnosis

- [`cross_collection_ts_set_overlap_mat` is ~51h stale and NO standing instrument watches it](2026-08-25T0011Z-cross-collection-overlap-mat-is-51h-stale-and-no-standing-metric-watches-it.md) — step1 fresh (~1h) + step2 stale (~51h) is the signature of `rpc-ccm-step2` specifically failing (`canceling statement due to statement timeout` on its `CREATE TEMP TABLE`). ⛔ **The timeout root cause is ALREADY FILED and PRIORITY 3 bars re-opening it.** The NEW part is a MONITORING gap: this MV is absent from `v_rpc_trust_health` / `rpc_ops_snapshot()`, so between ~8am first-tick verifies the overlap surface can serve multi-day-stale data invisibly. Proposed: an additive `cross_collection_overlap_stale_hours` arm — a decision, not a diagnosis

- [⭐ SHIPPED — nothing ever checked whether a materialized view is anon-readable, and every new one is born that way](2026-08-24T2345Z-nothing-ever-checked-whether-a-materialized-view-is-anon-readable.md) — `check_public_security_invariants()` had **5 arms, all scoped to `relkind IN ('r','p')`/`'v'`/pg_proc**: a MISSING invariant, not a broken check. ⛔ `information_schema.role_table_grants` returns **0 rows for all 34 MVs** while they demonstrably hold grants — any guard reading MV grants through it passes **vacuously**. ⛔ `ALTER DEFAULT PRIVILEGES` gives `postgres`-created MVs `anon=rxm` (**r = SELECT**), so every new MV is **born anon-readable** and PostgREST serves it. Live exposure **0 of 34** (latent, not a P0). Sixth arm shipped `20260824233704`, **ban at zero**, proven by a rolled-back positive control (`rows=1 names=mv_panini_squeeze`)
- [There are TWO client `Sentry.init()` calls, and which one wins depends on the BUNDLER — under production's turbopack the richer config is INERT](2026-08-24T1510Z-there-are-two-client-sentry-inits-and-which-one-wins-depends-on-the-bundler.md)
- [`allday-pack-opens-backfill` `cron_silent` watchlist arm is now a FALSE POSITIVE — the finite walk finished and the arm cannot tell "done" from "scheduler stopped"](2026-08-24T1507Z-allday-pack-opens-cron-silent-arm-is-a-false-positive-now-the-finite-walk-is-done.md)
- [A COLD ISR regeneration can exceed the 8 s board budget and bake the failure into 15 minutes of cached HTML — and `pack-drops` has no stale snapshot](2026-08-24T1441Z-a-cold-isr-regeneration-can-bake-a-failed-read-into-15-minutes-of-cached-html.md)

## 2026-08-23 — 23 filings

- [⛔ The FMV haircut's Top Shot leg is **800,545 buffers / 101 s to find 14 rows** — and the obvious `edition_fmv_current` fix **silently loses 71% of them**](2026-08-24T0455Z-the-fmv-haircut-topshot-leg-costs-800k-buffers-and-the-obvious-fix-loses-71pct-of-it.md) — the scan runs **twice** per leg (~200 s vs a ~120 s **gateway** cap, NOT `statement_timeout` — the route comment names the wrong bound). ⚠ Column projection is **not** the lever (narrowed still 101.4 s). ⛔ **Refuted before shipping** by a one-statement same-snapshot set difference: `old 14 / new 4 / in_old_not_new 10 / in_new_not_old 0`. A lagging materialisation is unsafe as a **filter for a predicate over the columns it lags on**. Severity **MEDIUM** — `/api/fmv-recalc` applies the haircut inline; `topshot_fmv_stale_hours` 0.1 of 6
- [🔴 `topshot-active-listings-ingest` is 100% red for 5+ days, and the documented cause is 22% of it](2026-08-24T0430Z-topshot-active-listings-ingest-is-100pct-red-and-the-documented-cause-is-22pct-of-it.md) — **40/40 runs failed** 08-19→08-24, classified by reading every log: **29 die on a DB statement timeout at `?phase=targets` before Atlas is reached**, 9 on the Atlas WAF, 2 on a 504. ⚠ **Refutes the register's "~60% `egress_blocked`" (it is 22.5%) and means #20's `wrangler deploy` cannot un-red this workflow.** `topshot_serial_board_targets` mean **13,163 ms** / max **29,949 ms** against `service_role`'s 30 s ceiling — marginal at rest, an R46 symptom not a cause (#30)
- [⛔ The largest job on the instance writes NO telemetry — and a second, cheaper caller wears its name in `pipeline_runs`](2026-08-24T0400Z-the-biggest-job-on-the-instance-has-no-telemetry-and-another-caller-wears-its-name.md)
- [⭐ SHIPPED — the UFC and Golazos promote legs are parked, using the function's own recheck mechanism rather than a new gate](2026-08-24T0345Z-ufc-and-golazos-promote-legs-parked-using-the-functions-own-mechanism.md)
- [⛔ The public-board watchdog discards ALL completed probes when any single board times out — and a "succeeded" tick covered 6 of 45](2026-08-24T0225Z-the-board-watchdog-loses-every-probe-it-completed-when-any-one-times-out.md)
- [The read path ATTRIBUTED — 15 RPCs are 93 h of DB time in 11 d, and 0.9% of the calls are 14.6% of it; plus `apply-fmv-haircut` is NOT an FMV-staleness incident and `sync-nba-projections` is off-season](2026-08-23T2235Z-the-read-path-attributed-and-two-severity-corrections.md)
- [⭐ SHIPPED — one leading-wildcard `LIKE` was seq-scanning 88,086 rows 4,684 times per refresh, and it cost 13,000 worker-seconds a day](2026-08-23T2210Z-one-leading-wildcard-LIKE-was-costing-13000-worker-seconds-a-day.md)
- [🚨 ~1,000 editions are labelled LOW confidence while their own row records them as the MOST-traded on the platform — in every collection the LOW cohort trades ~2× the MEDIUM cohort](2026-08-24T0225Z-a-thousand-editions-are-labelled-LOW-while-they-are-the-most-traded-on-the-platform.md) — ⭐ **RESOLVED in-file, then SELF-FALSIFIED**: the demotion is BY DESIGN (dispersion — LOW avg CV **0.731** vs HIGH **0.222**, measured), but my sub-dollar-tick story is **REFUTED by its own prediction** (HIGH has the LOWEST median price, $0.31, and 72% under $1). ⛔ Recommendation withdrawn. ⭐ What stands: **`LOW` conflates "no data" with "643 sales that disagree"**
- [⭐ PRIORITY 1 CAPTURED, first time since 2026-07-26: signed-in WAU is 0, and the accuracy gate stands at 30.1% HIGH/MEDIUM — with three denominator caveats](2026-08-23T2205Z-priority-1-captured-wau-is-zero-and-the-accuracy-gate-is-30-percent.md) — ⚠ **§5 RETRACTS its own "Golazos is the target"** (follow-up in-file): Golazos is market thinness (max 3 sales/edition/month), the confidence model is VALIDATED against volume, and the one bounded question is **167 LOW editions with ≥5 sales/30d**
- [⭐ The series-rollup fix WAS load-tested — its best run happened in the day’s worst hour, and I spent all evening saying it was unverified](2026-08-23T2055Z-the-series-rollup-fix-was-already-load-tested-i-just-never-read-the-neighbours.md)
- [Daytime monitor — the saturation spell DEEPENED since 18:10Z and `apply-fmv-haircut` missed a full daily cycle](2026-08-23T2110Z-daytime-monitor-spell-deepened-and-apply-fmv-haircut-missed-a-full-daily-cycle.md)
- [SIXTEEN more migrations applied today still have no committed file — a SECOND batch, after this morning's recovery](2026-08-23T2030Z-sixteen-more-migrations-applied-today-still-have-no-committed-file.md)
- [Postgres 17 makes a partial index UNREACHABLE when its predicate says `col IS NOT NULL` and the column is declared NOT NULL — including the 98 MB index `fmv-recalc` needs](2026-08-23T2130Z-postgres-17-makes-partial-indexes-with-is-not-null-predicates-unreachable.md)
- [`fmv-recalc` is NOT wedged mid-catalogue — page 0 can no longer COMPLETE, and the trust board has been breaching for 20 h unread](2026-08-23T2000Z-fmv-recalc-is-not-wedged-mid-catalogue-page-0-can-no-longer-complete.md) — 🚨 **the TITLE is FALSIFIED** (re-test in-file): page 0 completed at 20:28:06Z and the arm self-cleared for the fifth time; severity DOWN, mechanism unchanged
- [Sentry went dark at a precise minute, and the burst before it is the defect fixed today](2026-08-23T1930Z-sentry-went-dark-at-a-precise-minute-and-the-burst-before-it-is-the-defect-fixed-today.md)
- [`promote_unmapped_sales` burns 1,598 s/week on two collections that promote ZERO rows — visible only now that duration_ms works](2026-08-23T1930Z-promote-unmapped-sales-burns-1598s-a-week-on-two-collections-that-promote-nothing.md)
- [The index built for fmv-recalc has never been used in 72 days, and is bigger than the one that beats it](2026-08-23T1910Z-the-index-built-for-fmv-recalc-has-never-been-used-and-is-bigger-than-the-one-that-beats-it.md)
- [⭐ `pipeline_runs.duration_ms` was structurally 0 for TEN pipelines — fixing one word exposed a 37.8s job and a 78.4s job that had always reported 0](2026-08-23T1910Z-pipeline-runs-duration-ms-was-structurally-zero-for-ten-pipelines.md)
- [`get_team_detail` is the same defect family, but the expensive half is 30d SALES not FMV — and it needs a rollup, not a rewrite (measured, NOT shipped)](2026-08-23T1850Z-team-detail-is-the-same-defect-family-and-the-sales-half-needs-a-rollup-not-a-rewrite.md)
- [Daytime monitor — saturation spell ACTIVE at ~18:10Z; everything measured in that window is a SYMPTOM, re-measure quiet](2026-08-23T1812Z-daytime-monitor-saturation-spell-symptoms.md)
- [R6 re-measured in-band: 1 of 5 not 4 of 5, and the expensive lateral runs TWICE per request](2026-08-23T1805Z-r6-re-measured-in-band-one-of-five-not-four-and-the-lateral-runs-twice.md)
- [The allday-pack-opens-backfill mechanism is REFUTED and the proposed fix would lose data](2026-08-23T1609Z-the-pack-opens-backfill-mechanism-is-refuted-and-the-proposed-fix-would-lose-data.md)
- [R46 is a working-set problem not an IOPS one and the first lever may be free](2026-08-23T1610Z-R46-is-a-working-set-problem-not-an-IOPS-one-and-the-first-lever-may-be-free.md) — ⛔ **DECIDED 2026-08-23: Trevor stays on Small, no spend** ($96/mo declined). The free lever was REFUTED (22 MB/s is `ci_small`'s own budget, not a gp2 disk). Filing now carries the **numbered cost of declining** and the four conditions that re-open it — read those before re-suggesting an upgrade, and before proposing any new recurring IO
## 2026-08-22 — 35 filings

- [The pack simulator's flip animation keeps calling `setState` after unmount — an intermittent unhandled rejection that fails `npm test` while every test passes](2026-08-23T0555Z-the-pack-simulator-flip-loop-keeps-setting-state-after-unmount.md)
- [R21 re-derived: 67 / 38 / 29 reproduces exactly — but its named example LEFT the set, and nobody had enumerated it](2026-08-23T0555Z-r21-re-derived-the-count-held-and-the-set-did-not.md)
- [`check_public_security_invariants()` is RED for the first time since it was written — on a table whose grants are correct](2026-08-23T0510Z-the-rls-invariant-check-is-red-on-a-table-that-is-correctly-locked.md)
- [HIGH-PRIORITY — `reconcile_all_saved_wallet_stats` broken by today's R14 `ALTER PROCEDURE … SET search_path` (a SET-clause procedure cannot COMMIT)](2026-08-23T0311Z-reconcile-saved-wallet-stats-broken-by-set-clause-on-committing-procedure.md)
- [`public_board_slow_count` / `public_board_empty_count` write their "could not measure" sentinel at 21 of their last 36 slots — the standing breach is partly an unmeasured arm, not 45 unhealthy boards](2026-08-23T0255Z-the-board-liveness-arms-read-their-999-cannot-measure-sentinel-at-21-of-36-slots.md)
- [🚨 Sentry has ingested NOTHING since 2026-08-18 13:21:59Z — and Vercel logged the *identical* error string 447 times in the last 24 hours](2026-08-23T0250Z-sentry-has-received-nothing-since-08-18-while-production-throws-the-same-error-hundreds-of-times-a-day.md)
- [The `.range()` guard states the uniqueness rule in its own header and asserts something weaker — a repo-wide measurement of what it cannot see](2026-08-23T0236Z-the-paginated-range-guard-states-the-uniqueness-rule-in-a-comment-and-cannot-check-it.md)
- [`/sitemap/3.xml` truncates on a statement timeout and returns the partial set as if it were complete — and it pages on a key where 72% of rows are ties](2026-08-23T0228Z-the-sitemap-truncates-on-a-statement-timeout-and-pages-on-a-key-that-is-72pct-ties.md)
- [Pinnacle bulk ONE-WAY transfers are a real, untracked ownership change](2026-08-22T2030Z-pinnacle-bulk-one-way-transfers-are-untracked.md)

- [RESOLVED, SHIPPED, and carrying three of my own corrections: Golazos Series 2/3 never existed on chain (nextSeriesID=2) and the rows are deleted; the series route was 500ing on every collection because a rollup that ALREADY EXISTED had no reader — now 26/26 URLs return 200](2026-08-23T0210Z-golazos-series-2-and-3-never-existed-and-the-whole-series-route-is-dead.md)

- [/api/ready has been 500ing for eight days, and it settles a disagreement](2026-08-23T0025Z-api-ready-has-been-500ing-for-eight-days-and-it-settles-a-disagreement.md)

- [pg_cron reports 141/144 successes while allday-pack-opens-backfill has written nothing for 12.6h](2026-08-23T0200Z-allday-pack-opens-backfill-silent-while-pg-cron-reports-141-successes.md)

- [`collection_series` claims LaLiga Golazos has Series 2 and Series 3 — no instrument we own can see a single moment from either, and the series filter offers both](2026-08-23T0020Z-golazos-collection-series-claims-two-seasons-that-no-instrument-can-see.md)
- [The production bundle rendered a user-facing sentence that the committed source does not contain — a `+`-joined template literal lost the tail of its first chunk](2026-08-22T2135Z-production-bundle-dropped-a-string-segment-the-source-contained.md)
- [20 guards share a copy-pasted `stripComments` that hides 109,123 characters of real source — a `//` comment containing `/*` swallows every line until the next `*/`](2026-08-22T2105Z-stripcomments-hides-109k-chars-of-real-source-from-20-guards.md)
- [The cross-collection board hardcodes "143 wallets" in its SEO description and in the text users share — the true count is 221](2026-08-22T2050Z-cross-collection-board-hardcodes-143-wallets-in-seo-and-share-text.md)
- [The saturation is structural, not a spell — a measured waste ledger](2026-08-22T1956Z-the-saturation-is-structural-a-waste-ledger-from-pg-stat-statements.md)
- [The unbounded-server-read class bit a FOURTH time — and 23 more instances sit outside the guard written to stop it](2026-08-22T1929Z-23-unbounded-server-reads-live-outside-the-guard-that-exists-for-them.md)
- [Five public pages exceeded 30s to `domcontentloaded` in the 13:15Z e2e run — and the same pages were clean at 07:16Z and 18:58Z](2026-08-22T1905Z-five-public-pages-exceeded-30s-to-domcontentloaded-inside-the-degraded-band.md)
- [The zero-denominator percentage family: two gate meters fixed, three write-sites filed rather than changed](2026-08-22T1850Z-zero-denominator-percentages-two-gate-meters-fixed-three-write-sites-filed.md)
- [86 distinct mobile controls are under the 44px tap-target floor — a DESIGN decision, not a bug list](2026-08-22T1836Z-86-mobile-tap-targets-under-44px-and-the-instrument-that-found-them.md)
- [The roadmap's #1 metric is unreadable ~20 hours a day, and the cause is structural — but my candidate fix did NOT validate](2026-08-22T1745Z-the-headline-accuracy-metric-is-unreadable-20h-a-day-and-the-cause-is-structural.md)
- [🚨 P0 — the 2026-08-03 credential purge was DEFEATED: the purged blob is still reachable on a stale branch of the PUBLIC repo](2026-08-22T1620Z-P0-the-08-03-credential-purge-was-defeated-by-a-stale-public-branch.md)
- [The pg_cron `job startup timeout` class finally has a named cause — and a near-miss that would have frozen a stale MV permanently](2026-08-22T1600Z-the-startup-timeout-class-has-a-named-cause-and-a-near-miss-that-would-have-frozen-an-mv.md)
- [`candy-editions-ingest` — the 08-04 timeout fix SHIPPED and it is silent again for the same reason at a higher ceiling](2026-08-22T1600Z-candy-editions-is-band-killed-not-timeout-fixed.md)
- [`pinnacle-sync` was a FALSE stall — the arm watched a self-report while the outcome had no arm at all. Plus: the hydrator's schedule is declared nowhere in the repo](2026-08-22T1545Z-pinnacle-sync-was-a-false-stall-and-the-hydrator-schedule-is-in-no-repo-file.md)
- [Daytime monitor — 2026-08-22T1509Z (~08:06 PT, first-tick-of-day)](2026-08-22T1509Z-daytime-monitor.md)
- [One of the four never-run walkers is a DUPLICATE of a live pipeline, not a gap — and that halves the decision](2026-08-22T1500Z-one-of-the-four-never-run-walkers-duplicates-a-live-pipeline.md)
- [Two dropped checks, executed: the concierge probe PASSES, and NBA projections has been dark for 18 days on zero arms](2026-08-22T1450Z-nba-projections-18-dark-days-and-the-concierge-probe-check-nobody-ran.md)
- [The disk-IO budget, ranked: 7,809 GB / 242h, and the two priciest boards are LIVE (I nearly filed the opposite)](2026-08-22T0340Z-the-disk-io-budget-ranked-and-three-switches-that-mean-live.md)
- [Ask-corroboration is Top-Shot-only BY DESIGN — and extending it to All Day is worth 320 editions (+5.2pp on that collection, +1.1pp on the gate)](2026-08-22T0320Z-ask-corroboration-is-top-shot-only-BY-DESIGN-and-it-is-worth-320-allday-editions.md)
- [The accuracy gate, measured: 31.3% at HIGH/MEDIUM — and it tracks LIQUIDITY, not the pricing algorithm](2026-08-22T0256Z-the-accuracy-gate-measured-31pct-and-it-tracks-liquidity-not-the-algorithm.md)
- [`reconcile-saved-wallet-stats`'s "is the sweep keeping up?" metric measures a population the sweep excludes by construction — and it misled me today](2026-08-22T0130Z-oldest-cache-h-measures-a-population-the-sweep-excludes-by-construction.md)
- [`refresh_wmc_fmv_changed`'s temp-table build costs 120× what it needs to — and my own "permanently backlogged" reading of that function is WITHDRAWN](2026-08-22T0010Z-refresh-wmc-fmv-changed-temp-build-is-120x-its-necessary-cost-and-my-backlog-reading-is-withdrawn.md)
- [Daytime monitor — 2026-08-22T00:06Z (2026-08-21 17:06 PT)](2026-08-22T0006Z-daytime-monitor.md)

## 2026-08-21 — 16 filings

- [ESCALATION — the cross-collection mats have failed EVERY daily run since 08-18, the board is 4d19h stale, and the 08-18 filing's own escalation trigger fired three days ago](2026-08-21T2340Z-ESCALATION-the-cross-collection-mats-have-failed-every-day-since-08-18.md)
- [The `deals` board is a VICTIM of a global 20h/day slowdown, not its cause — and the same window costs FMV recalc ~19 hours a day](2026-08-21T2315Z-the-deals-board-is-a-victim-and-fmv-recalc-loses-19-hours-a-day.md)
- [The flagship `deals` board fails ~80% of refreshes, is up to 15.1h stale, and the failure is ~20h/day — not the 05–08:30Z saturation window](2026-08-21T2245Z-the-deals-board-fails-80pct-of-refreshes-and-is-20h-a-day-not-a-saturation-window.md)
- [Daytime monitor — 2026-08-21T21:05Z (14:05 PT)](2026-08-21T2105Z-daytime-monitor.md)
- [259 reads in `app/api/**` never destructure `error` — and two existing honesty guards exclude that tree on a premise that is false](2026-08-21T1945Z-259-route-reads-never-look-at-their-error-and-two-guards-exclude-app-api-on-a-false-premise.md)
- [`allday-pack-opens-backfill` throughput collapsed ~1,000× on 2026-08-11 — ETA to floor is now ~12 years](2026-08-21T1735Z-allday-pack-opens-backfill-throughput-collapsed-1000x.md)
- [Vercel's runtime-error ROUTE attribution is smeared — the homepage is not failing on a Panini board](2026-08-21T1730Z-vercel-runtime-error-route-attribution-is-smeared.md)
- [Four of the nineteen hardened event-range walkers have never run](2026-08-21T1701Z-four-of-the-nineteen-hardened-walkers-have-never-run.md)
- [Daytime monitor — 2026-08-21T15:05Z (≈08:05 PT, first tick of day)](2026-08-21T1505Z-daytime-monitor.md)
- [An upstream HTTP error defeats the cursor hold in 7 of 8 block-scan indexers](2026-08-21T1420Z-an-http-error-defeats-the-cursor-hold-in-7-of-8-indexers.md)
- [Two wrangler configs deployed to ONE Cloudflare worker, and the fossil would have silently downgraded the live one](2026-08-21T0510Z-two-wrangler-configs-deployed-to-one-worker-and-the-fossil-would-have-downgraded-it.md)
- [Two findings from the coverage pass's tail — 2026-08-20 (PT)](2026-08-21T0400Z-two-findings-from-the-coverage-pass-tail.md)
- [Daytime monitor — 2026-08-21T03:09Z (≈20:09 PT, 08-20 evening)](2026-08-21T0309Z-daytime-monitor.md)
- [Area (7)'s render-layer gap is already closed — 30 of 31 server pages discriminate, and a source guard would red on correct code](2026-08-21T0245Z-area-7s-render-layer-gap-is-already-closed-and-a-source-guard-would-red-on-correct-code.md)
- [The five-source caller rule is blind to `pg_trigger` — 33 live functions read as dead, and area (5)'s headline item is a false positive](2026-08-21T0140Z-the-caller-rule-is-blind-to-triggers-and-item-5s-headline-is-a-false-positive.md)
- [Three anon-public tabs declare a canonical pointing at a URL that 302s Googlebot to /login](2026-08-21T0030Z-three-anon-public-tabs-canonicalise-to-a-login-wall.md)

## 2026-08-20 — 4 filings

- [Test-coverage analysis — 2026-08-20 (PT)](2026-08-20T2325Z-test-coverage-analysis.md)
- [Daytime monitor — 2026-08-20T21:08Z (≈14:08 PT)](2026-08-20T2108Z-daytime-monitor.md)
- [Daytime monitor — 2026-08-20T03:06Z (≈20:06 PT)](2026-08-20T0306Z-daytime-monitor.md)
- [Daytime monitor — 2026-08-20T00:12Z (≈17:12 PT)](2026-08-20T0012Z-daytime-monitor.md)

## 2026-08-19 — 3 filings

- [Daytime-peak saturation spell — SYMPTOM filing (do not act causally)](2026-08-19T2107Z-daytime-peak-saturation-spell-symptom.md)
- [Daytime monitor — 2026-08-19T15:11Z (≈08:11 PT, first tick)](2026-08-19T1511Z-daytime-monitor.md)
- [Daytime monitor — 2026-08-19T00:06Z (2026-08-18 ~17:06 PT)](2026-08-19T0006Z-daytime-monitor.md)

## 2026-08-18 — 29 filings

- [Daytime monitor — 2026-08-18T21:10Z (in an active saturation spell)](2026-08-18T2110Z-daytime-monitor.md)
- [UFC dormancy: every DB-side instrument agrees, one apparent counter-signal is REFUTED, and the decisive test is still operator-only](2026-08-18T2100Z-ufc-dormancy-is-consistent-but-the-decisive-test-is-still-operator-only.md)
- [⛔ RETRACTION — Top Shot is NOT losing ground. I read a burst as a trend, from two points.](2026-08-18T2100Z-RETRACTION-top-shot-is-not-losing-ground-i-read-a-burst-as-a-trend.md)
- [The rotation works — and Top Shot is still losing ground. The one unverified inference is refuted.](2026-08-18T1930Z-the-rotation-works-and-top-shot-is-still-losing-ground.md)
- [The wmc backfill converts again (0 → 1,000 a tick), and the cross-collection mat timeout was saturation, not growth](2026-08-18T1835Z-the-wmc-backfill-starvation-is-fixed-by-scoping-and-the-ccm-timeout-was-saturation.md)
- [52% of `net._http_response` is one HEALTHY pipeline abandoning its own replies at the 5000 ms default](2026-08-18T1835Z-half-of-net-http-response-is-one-healthy-pipeline-at-the-5000ms-default.md)
- [Six of the eight "de-hardcoded" gate functions never had that code deployed — their secrets are inert](2026-08-18T1830Z-six-de-hardcoded-gate-fns-never-had-that-code-deployed-BLOCKER.md)
- [Candidate — `rpc-roll-pack-ask-hourly-low` shows a `deadlock detected` signature (SYMPTOM, observed in a spell)](2026-08-18T1808Z-roll-pack-ask-hourly-low-deadlock-signature-during-a-saturation-spell-SYMPTOM.md)
- [`idx_wmc_fmv_conf_null` is BUILT — and the route that built it overturns a recorded "impossible". The backfill is still converting ~0 rows, for a different reason.](2026-08-18T1725Z-wmc-fmv-conf-index-SHIPPED-via-pg_cron-and-the-backfill-is-starved-by-a-pinnacle-head-block.md)
- [The four-source caller rule is blind to Cowork artifacts — 8 live objects read as dead](2026-08-18T1620Z-the-four-source-caller-rule-is-blind-to-cowork-artifacts.md)
- [`idx_wmc_fmv_conf_null` — finding VERIFIED, fix READY, build BLOCKED on an idle window (and it left an INVALID index you must drop first)](2026-08-18T1545Z-wmc-fmv-confidence-index-verified-and-ready-but-BLOCKED-on-an-idle-window.md)
- [`get_fmv_coverage()` plans the SAME correlated `EXISTS` twice — and it took the whole data-integrity monitor down](2026-08-18T1510Z-get-fmv-coverage-plans-the-same-exists-twice.md)
- [Daytime monitor — 2026-08-18 ~08:06 PT (15:06Z) · FIRST-TICK-OF-DAY](2026-08-18T1506Z.md)
- [pack-EV / `fmv_current`: the LATERAL fix is verified equivalent — and the fixture-preserving alternative is refuted](2026-08-18T1410Z-pack-ev-lateral-verified-and-the-array-pushdown-does-not-transfer.md)
- [`compute-golazos-pack-ev` is SETTLED: not cron, not retention, NOT saturation — and the freshness arm reads green through it](2026-08-18T1406Z-golazos-pack-ev-is-not-saturation-and-the-freshness-arm-cannot-see-it.md)
- [`drain-fmv-cold-tail`: the caller is healthy — the `after()` work is killed at `maxDuration`, and has been since June](2026-08-18T1400Z-drain-fmv-cold-tail-is-killed-by-maxduration-not-its-caller.md)
- [`drain-fmv-cold-tail` has STOPPED — and the other "genuine miss" was a false positive](2026-08-18T0515Z-drain-fmv-cold-tail-has-stopped-and-resolve-topshot-stubs-was-a-false-positive.md)
- [Blast radius of the watchlist derivation: 67 added → 4 breaching, 0 chronic — but only under a rule the rows do not use](2026-08-18T0455Z-watchlist-derivation-blast-radius-measured.md)
- [Watchlist coverage was measured against rows the monitor ignores — the blind spot is 67, not 62](2026-08-18T0450Z-watchlist-coverage-was-measured-against-rows-the-monitor-ignores.md)
- [Candidate — the durable fix for jobid 218: make the `LIMIT` bind. Plans + cardinalities MEASURED; the timing A/B is NOT](2026-08-18T0425Z-the-durable-fix-for-jobid-218-make-the-limit-bind-plans-measured-timing-not.md)
- [Candidate — systemic hourly heavy-cron pileup in the :13–:34 band is manufacturing IO-saturation spells](2026-08-18T0330Z-heavy-cron-collision-pinnacle-backfill-io.md)
- [Test-coverage analysis — where the remaining risk actually is (2026-08-18 02:30Z / 2026-08-17 19:30 PT)](2026-08-18T0230Z-test-coverage-analysis.md)
- [Answering the open question on #8: restoring ESPN alone will NOT refill `nba_players` — the roster fetch is SLATE-GATED, and the season is over](2026-08-18T0120Z-restoring-espn-alone-will-not-refill-nba_players-the-roster-fetch-is-slate-gated.md)
- [The Pinnacle null-edition pool IS the catalog-coverage gap — and parking those rows would hide a hole that is still widening](2026-08-18T0112Z-pinnacle-null-edition-pool-is-the-catalog-gap-and-parking-it-would-hide-a-widening-hole.md)
- [The board-liveness SWEEP completes only ~half the time — and `cron.job_run_details.status` under-reports that by 40%](2026-08-18T0105Z-the-board-liveness-sweep-completes-only-half-the-time-and-cron-status-under-reports-it.md)
- [The sports-proxy 403 is TWO different causes needing TWO different fixes — the "three providers tightening bot-blocking" framing is refuted](2026-08-18T0100Z-the-sports-proxy-403-is-TWO-causes-not-one.md)
- [`topshot-misattrib-drain` resolves fine but has NOT applied a re-key since 2026-08-07 — and my first severity read was 6.5x too high](2026-08-18T0050Z-misattrib-drain-resolves-but-has-not-applied-a-rekey-since-08-07.md)
- [Top Shot series 6/7/8 carry TWO different display labels, and both are user-visible](2026-08-18T0045Z-top-shot-series-6-7-8-have-two-different-display-labels-and-both-are-user-visible.md)
- [Daytime monitor: `compute-golazos-pack-ev` silent 17.5h + `get_team_players` 45s timeout on team pages](2026-08-18T0013Z-golazos-pack-ev-silent-17h-and-team-roster-rpc-timeout.md)

## 2026-08-17 — 21 filings

- [The 2026-08-15 conflated-subedition reorder FIXED the drain — and its `ok` flag makes the recovery invisible](2026-08-17T2345Z-the-conflated-subedition-drain-fix-WORKED-and-its-ok-flag-hides-it.md)
- [Watchlist coverage audit: 62 of 149 active pipelines are unwatched, 5 of them have zero successes — and a curated list is why](2026-08-17T2320Z-watchlist-coverage-audit-62-of-149-pipelines-are-unwatched.md)
- [`panini_sale_field_mapping_shortfall` is mathematically incapable of firing while `panini_sale_price_capture_dry_days` is breached — and its own comment says it "Reads 0" when it reads −19](2026-08-17T2245Z-the-panini-mapping-shortfall-arm-cannot-fire-while-its-sibling-is-breached.md)
- [UFC Strike has recorded ZERO sales for 96 days, and `ufc-sales-indexer` reports 112/113 ok while writing 0 rows](2026-08-17T2210Z-ufc-strike-has-had-zero-sales-for-96-days-and-the-indexer-reports-ok.md)
- [Daytime monitor — 2026-08-17T21:06Z (14:06 PT, afternoon tick)](2026-08-17T2106Z-daytime-monitor.md)
- [The sports-proxy 403 is NOT a secret problem — it is three upstreams 403'ing across TWO independent egress networks](2026-08-17T1845Z-the-sports-proxy-403-is-not-a-secret-it-is-three-upstreams-across-two-egress-networks.md)
- [Daytime monitor — 2026-08-17T18:06Z (11:06 PT, later tick)](2026-08-17T1806Z-daytime-monitor.md)
- [`raise_impossible_parallel_circ` is NOT waste — it is real data-integrity healing running at 28× the cadence its work arrives at](2026-08-17T1712Z-the-impossible-parallel-selfheal-is-not-waste-it-is-28x-over-cadenced.md)
- [`topshot-wmc-fossil-drain` times out proving emptiness — and it is on no watchlist, so neither arm sees it](2026-08-17T1656Z-the-fossil-drain-times-out-proving-emptiness-and-nothing-watches-it.md)
- [Daytime monitor — 2026-08-17T15:18Z (08:18 PT, first tick of day)](2026-08-17T1518Z-daytime-monitor.md)
- [The saturation self-throttle fails OPEN on a returned error — 9 routes, and it fails open exactly when it is needed](2026-08-17T1211Z-the-saturation-self-throttle-fails-OPEN-on-a-returned-error-in-9-routes.md)
- [`fmv-recalc` re-breached ~4 h after being marked CLOSED, and current saturation does not explain it](2026-08-17T0450Z-fmv-sweep-re-breached-4h-after-being-marked-CLOSED-and-saturation-does-not-explain-it.md)
- [`wallet-username-resolver` dies in its CANDIDATE SELECTION — and chasing why exposed an untested half of this repo's `statement_timeout` rule](2026-08-17T0440Z-wallet-username-resolver-dies-in-its-candidate-selection-and-the-proconfig-rule-has-an-untested-half.md)
- [The pg_cron startup timeout is NOT a worker-slot cap — it is the saturation, and the obvious config fix does nothing](2026-08-17T0410Z-the-pgcron-startup-timeout-is-not-a-worker-slot-cap-it-is-the-saturation.md)
- [Pipeline restoration sweep — 2 restored, 3 blocked on access I don't have, and the monitoring gap that let all of it run for days](2026-08-17T0320Z-pipeline-restoration-sweep-what-is-fixed-what-is-blocked-and-the-monitoring-gap.md)
- [Daytime monitor — 2026-08-17 03:10Z (20:10 PT Aug 16)](2026-08-17T0310Z-daytime-monitor.md)
- [⛔ REFUTED TWICE, ROOT CAUSE FOUND: pack-EV is slow because `fmv_current` never pushes down — not because the 12 h target is unaffordable](2026-08-17T0225Z-backfill-historical-pack-ev-chases-a-12h-target-it-can-never-hit.md)
- [`match-topshot-players` — failing every daily run since 08-14, and its declared 300 s budget is INERT](2026-08-17T0200Z-match-topshot-players-has-failed-every-run-since-08-14-and-its-300s-budget-is-inert.md)
- [`allday-unmapped-resolver-tail` — NOT broken; it is grinding an exhausted backlog at ~52 min / 3 days for 5 rows](2026-08-17T0130Z-the-allday-tail-resolver-is-not-broken-it-is-grinding-an-exhausted-backlog.md)
- [`candy-editions-ingest` — the runtime is unbounded on fixed work, and the maxDuration lever is exhausted](2026-08-17T0030Z-candy-editions-runtime-is-unbounded-and-the-clock-lever-is-exhausted.md)
- [⛔ The concierge is TWO faults stacked. Topping up fixes the newer one and leaves a ~94% failure rate in place.](2026-08-17T0015Z-concierge-is-TWO-faults-topping-up-leaves-a-94pct-failure-rate.md)

## 2026-08-16 — 33 filings

- [`panini_sale_price_capture_dry_days` is CRYING WOLF — it watches a field abandoned on 2026-08-08 while the live replacement works](2026-08-16T2355Z-panini-sale-price-is-wallet-scoped-not-a-lighter-payload.md)
- [`unmapped-sales-nfl_all_day` — the resolver is stuck in December, and the approved lever is NOT lossless](2026-08-16T2145Z-unmapped-resolver-is-stuck-in-december.md)
- [Daytime monitor — 2026-08-16T21:06Z (14:06 PT)](2026-08-16T2106Z-daytime-monitor.md)
- [✅ RESOLVED — the denominator existed all along: `fmv-recalc-heartbeat`. It was in my own 16:40Z query output and I read it as noise.](2026-08-16T2030Z-RESOLVED-the-denominator-existed-fmv-recalc-heartbeat-and-I-had-it-at-1640Z.md)
- [⛔⛔ CORRECTION #2 — `pipeline_runs` recorded **1 of 6** `fmv-recalc` invocations. My "deterministic page-0 poison" was a SELECTION ARTIFACT.](2026-08-16T2020Z-CORRECTION-2-pipeline_runs-sees-1-of-6-fmv-recalc-invocations.md)
- [⛔ CORRECTION — the FMV sweep failure is **NOT saturation**. Page 0 fails deterministically, and it will not self-heal.](2026-08-16T2015Z-CORRECTION-the-fmv-sweep-is-NOT-saturation-page-zero-is-deterministically-poison.md)
- [The institutional wallet walk is unstable, and everything derived from its diff is fiction](2026-08-16T1930Z-the-institutional-wallet-walk-is-unstable-and-its-diff-is-fiction.md)
- [pg_cron never STARTS ~2-4% of all scheduled ticks, and a lost tick writes no `pipeline_runs` row](2026-08-16T1921Z-pg-cron-loses-2-to-4-pct-of-all-ticks-and-writes-no-row.md)
- [86 anon-executable SECURITY INVOKER functions are invisible to `check_secdef_anon_exec_drift()`](2026-08-16T1910Z-86-anon-executable-invoker-fns-are-invisible-to-the-secdef-drift-check.md)
- [✅ FALSIFIER PASS 18:55Z — leg 324 fired, the arm CLEARED to 11.84. ⏰ The predicted RE-BREACH at ~20:07Z is still ahead.](2026-08-16T1855Z-FALSIFIER-PASS-leg-324-fired-arm-cleared-re-breach-still-expected-2007Z.md)
- [All Day realized pull value is capped at ~14% by pull ingest, not by the rollup — and the TS dist_id "collapse" is a maturation curve](2026-08-16T1840Z-allday-realized-pull-value-is-capped-at-14pct-by-pull-ingest.md)
- [`fmv_current` does not push a JOIN predicate through its `DISTINCT ON` — 1.05M buffers to fetch 40 rows](2026-08-16T1829Z-fmv-current-does-not-push-down-through-distinct-on.md)
- [Two operator-only blockers, both currently degrading a live user-facing path](2026-08-16T1750Z-two-operator-only-blockers-anthropic-403-and-atlas-proxy.md)
- [✅ SHIPPED 17:38Z — the trust-health freshness view is APPLIED. Its first query proves the pre-split monolith needed **928.6 s of a 600 s budget**.](2026-08-16T1740Z-SHIPPED-freshness-view-applied-and-the-monolith-needed-928s-of-a-600s-budget.md)
- [`ownership-onchain-walk` has failed two consecutive daily ticks, and nothing watches it](2026-08-16T1734Z-ownership-onchain-walk-has-failed-two-daily-ticks-unwatched.md)
- [⛔ The 08-12 "cheapest first" fix for wmc FMV drift is FALSIFIED — the catch-all sweep already times out on **26 wallets**](2026-08-16T1710Z-the-08-12-cheapest-fix-for-wmc-drift-is-FALSIFIED-the-sweep-times-out-on-26-wallets.md)
- [The React #418 hydration class is wider than `/insights` — 17 more date/time sites, FILED not fixed](2026-08-16T1706Z-the-418-hydration-class-is-wider-than-insights.md)
- [✅ VERIFIED 16:47Z — the `:13` collision fix passed its positive control. And the "contradiction" was never one.](2026-08-16T1650Z-VERIFIED-the-13-collision-fix-passed-its-positive-control.md)
- [2026-08-16T16:40Z — `fmv_sweep_wedge_hours` is diagnosed: the sweep FINISHED its catalogue pass, then died on the next pass's first page. Its dedicated stall arm reads **49.0 against breach 50.**](2026-08-16T1640Z-the-fmv-sweep-climber-is-diagnosed-and-its-own-stall-arm-is-1-point-from-firing.md)
- [The `:13` pg_cron collision — ✅ FIXED 2026-08-16, and the "operator-only" premise in this title was WRONG](2026-08-16T1630Z-the-13-collision-is-operator-only-and-thats-why-it-persists.md)
- [2026-08-16T16:15Z — the leg split will clear, RE-BREACH for ~40 min, then go green; and `pipeline_runs_daily` nearly made me file a fake 4-hour stall](2026-08-16T1615Z-leg-split-will-re-breach-at-2007Z-and-runs-daily-is-a-6h-stale-rollup.md)
- [DECISION — `rpc-panini-squeeze-v2`'s "not yet public" footer is WRONG and has been for 15 days: refresh it](2026-08-16T1600Z-DECISION-panini-squeeze-artifact-footer-is-15-days-stale.md)
- [`candy-editions-ingest`: the `maxDuration` lever is EXHAUSTED, not unshipped — and the route is not the defect](2026-08-16T1545Z-candy-editions-the-maxDuration-lever-is-EXHAUSTED-not-unshipped.md)
- [Daytime monitor candidates — 2026-08-16T15:25Z (~08:25 PT, first-tick)](2026-08-16T1525Z-daytime-monitor.md)
- [⛔ DO NOT RUN THE `:13` STAGGER — it is harmful as written, and it is currently QUEUED as "ready-to-run"](2026-08-16T1520Z-the-13-stagger-is-REFUTED-do-not-run-it.md)
- [Session close — 2026-08-15/16, Cowork cloud: the gate-key rotation item is closed, and three of my own filings were refuted](2026-08-16T1500Z-session-close-gate-key-closed-and-three-of-my-filings-refuted.md)
- [The trust precompute is starving leg-by-leg — and the fix I filed 15 h ago is already obsolete](2026-08-16T1455Z-trust-precompute-starving-leg-by-leg-and-my-own-fix-is-obsolete.md)
- [The gate-key rotation item is CLOSED — all 14 gate-keyed crons verified passing](2026-08-16T1455Z-gate-key-rotation-item-is-CLOSED-all-14-verified.md)
- [Daytime monitor — 2026-08-16T14:46Z (2026-08-16 07:46 PT · first tick of day)](2026-08-16T1446Z-daytime-monitor.md)
- [MEMORY / CORRECTION — `cron_heavy`-owned pg_cron jobs ARE reschedulable from the MCP/SQL editor; the "needs superuser/dashboard" claim is WRONG](2026-08-16T0030Z-cron-heavy-jobs-ARE-reschedulable-from-mcp-via-set-local-role.md)
- [The wallet-backfill error classifiers miss object-shaped rejections, and the cost is a permanently-red mega-wallet](2026-08-16T0025Z-wallet-backfill-error-classifiers-miss-object-shaped-rejections.md)
- [Daytime monitor — 2026-08-16T00:15Z (2026-08-15 ~17:15 PT)](2026-08-16T0015Z-daytime-monitor.md)
- [R8's heal would write TEAM NAMES into `editions.player_name` — do not run it](2026-08-16T0010Z-R8-would-write-team-names-into-player-name.md)

## 2026-08-15 — 20 filings

- [`remap_topshot_realign_miskeyed_subeditions` is SCAN-bound, so the filed p_limit fix is a no-op](2026-08-15T2350Z-realign-is-scan-bound-not-row-bound.md)
- [The driver-message leak has a SIXTH spelling, and both shared guards are blind to it by construction](2026-08-15T2341Z-the-driver-message-leak-has-a-sixth-spelling-in-a-sibling-key.md)
- [The trust-precompute 999 sentinel is unreachable on a timeout, and the fix is structural](2026-08-15T2240Z-the-999-sentinel-is-unreachable-on-a-timeout.md)
- [The Panini throughput gate will self-silence on the collapse it is currently catching](2026-08-15T2140Z-panini-throughput-gate-self-silences.md)
- [`drain-conflated-subeditions`: the route really is dead, but R7's stated impact is wrong — and the real one is worse](2026-08-15T1930Z-the-drain-is-dead-but-not-for-the-reason-filed.md)
- [The static-extension bypass is REAL — but the proof it ships with is not, and the control test settles it](2026-08-15T1838Z-static-ext-bypass-is-real-but-its-headline-proof-is-not.md)
- [Panini ingest: enumeration stops early, throughput down ~6×, and the freshness check is blind to it](2026-08-15T1830Z-panini-enumeration-stops-early-and-the-freshness-gate-is-blind-to-it.md)
- [Daytime monitor — 2026-08-15T~1810Z (≈11:10 PT)](2026-08-15T1810Z-daytime-monitor.md)
- [The nightly reconcile is failing and the cards are stale — but the proposed query-shape fix is REFUTED by measurement](2026-08-15T1700Z-reconcile-saved-wallet-stats-the-query-shape-fix-is-refuted.md)
- [Three heavy pg_cron jobs collide at minute :13 — the saturation has a schedule, and the fix is two `cron.alter_job` calls](2026-08-15T1630Z-three-heavy-pg-cron-jobs-collide-at-minute-13.md)
- [`fmv-recalc` is not running less — it is being KILLED at maxDuration, and nothing watches that](2026-08-15T1600Z-fmv-recalc-is-not-running-less-it-is-being-killed.md)
- [R4 is a CATALOG COVERAGE gap, not an indexer regression — and the FK is what makes it unfixable in place](2026-08-15T1530Z-R4-is-a-catalog-coverage-gap-not-an-indexer-regression.md)
- [Daytime monitor — 2026-08-15T15:10Z (~08:10 PT, first tick)](2026-08-15T1510Z-daytime-monitor.md)
- [The platform has TWO serial-multiplier models, and deep-audit run 2's P2 compared the homepage against the wrong one](2026-08-15T1507Z-two-serial-multiplier-models-and-the-audit-p2-is-wrong.md)
- [`refresh-insights-cache` fails to warm half its boards and has logged `ok: true` every single time](2026-08-15T1200Z-the-insights-cache-warms-half-its-boards-and-reports-perfect-health.md)
- [`pinnacle_fmv_history` silently drops the ASK_ONLY revision — 776 renders currently show a history value the catalog never published](2026-08-15T0620Z-pinnacle-fmv-history-silently-drops-the-ask-only-revision.md)
- [Daytime monitor — 2026-08-15T06:12Z (off-hours, 23:12 PT Aug 14)](2026-08-15T0612Z-daytime-monitor.md)
- [The `pg_net_http_403` CRITICAL is ONE job, and the Pinnacle "deployed or ineffective?" question is closed](2026-08-15T0540Z-the-403-critical-is-one-job-and-the-pinnacle-deploy-question-is-closed.md)
- [The entity-page timeouts are TWO families, and the biggest one is not a slow query](2026-08-15T0450Z-the-entity-page-timeouts-split-into-two-families-and-only-one-is-a-slow-query.md)
- [`refresh_wmc_fmv_changed` is the #2 disk reader and it is NOT a defect — it is the price of the `wmc.fmv_usd` denormalization](2026-08-15T0350Z-refresh-wmc-fmv-changed-is-the-price-of-the-wmc-denormalization.md)

## 2026-08-14 — 11 filings

- [The Top Shot catalog walk faulted 11 of 258 sets and nothing watches that counter](2026-08-14T2300Z-topshot-catalog-walk-faulted-11-sets-and-nothing-watches.md)
- [Two schedulers run the same wmc FMV propagation, and they lock each other out](2026-08-14T2230Z-two-schedulers-run-the-same-fmv-propagation-and-lock-each-other-out.md)
- [The Pinnacle FMV drift guard compares `pinnacle_catalog` to itself](2026-08-14T1930Z-pinnacle-fmv-drift-guard-is-a-tautology.md)
- [`get_pack_detail_bundle` cold-scans 3k× per pack page — the top user-facing error on the board](2026-08-14T1830Z-pack-detail-bundle-cold-scan-is-the-top-user-facing-error.md)
- [All Day's WAF 403 is almost certainly a ONE-LINE env change — `ALLDAY_PROXY_URL` route](2026-08-14T0500Z-allday-waf-403-is-a-one-line-env-fix.md)
- [Search players by college — a real collector hook RPC cannot answer today](2026-08-14T0330Z-search-players-by-college.md)
- [Daytime monitor — 2026-08-14T03:12Z (~20:12 PT, 08-13)](2026-08-14T0312Z-daytime-monitor.md)
- [Narrative search does not work: set names outrank the prose that answers the question](2026-08-14T0310Z-narrative-search-is-outranked-by-set-names.md)
- [Three older swallowed ledger headings (2026-08-11), unrepaired](2026-08-14T0210Z-three-older-swallowed-ledger-headings.md)
- [`/api/search` has no smoke probe, and it now has two consumers](2026-08-14T0204Z-api-search-has-no-smoke-probe-and-now-has-two-consumers.md)
- [Daytime monitor — 2026-08-14T00:14Z (~17:14 PT, 08-13)](2026-08-14T0014Z-daytime-monitor.md)

## 2026-08-13 — 14 filings

- [Profile flair + social-preview backlog (Claude Code, 2026-08-13)](2026-08-13T2345Z-profile-flair-and-social-preview-backlog.md)
- [`[collection]/analytics` renders a failed fetch as an empty market — 8 sections, 1 already fixed](2026-08-13T2340Z-collection-analytics-tabs-render-a-failed-fetch-as-an-empty-market.md)
- [The schema-cache 500s are SELF-INFLICTED: one ~20-second burst of user-facing errors per migration we apply](2026-08-13T2320Z-the-schema-cache-500s-are-self-inflicted-by-our-own-migrations.md)
- [The Sentry board triaged against the live DB — 2 reds were stale, 3 were fixed today, 6 are real](2026-08-13T2230Z-sentry-board-triaged-stale-vs-live.md)
- [Two `.from()` table reads on the edition page are still unbounded — the RPC half is fixed, this half is not](2026-08-13T2155Z-entity-page-from-table-reads-are-still-unbounded.md)
- [`get_series_detail` is a live, tens-of-seconds PUBLIC page query (Sentry NEXTJS-27), and the two obvious fixes are both WORSE](2026-08-13T2115Z-series-detail-is-a-live-20s-public-page-and-two-obvious-fixes-are-falsified.md)
- [Golazos is genuinely market-limited (measured ceiling ~2.8%), and the trust board's "stale" metric does not mean what its name implies](2026-08-13T2030Z-golazos-is-genuinely-market-limited-and-two-different-stales.md)
- [Inbox triage — 2026-08-13](2026-08-13T2000Z-inbox-triage.md)
- [The AllDay drain attribution is settled (keep jobid 22), and the insider-detector telemetry can drop 12 scans to 3](2026-08-13T1950Z-allday-drain-attribution-settled-and-the-insider-telemetry-merge.md)
- [Both open attribution questions are CLOSED — and neither needed the instrumentation we thought](2026-08-13T1930Z-403s-fully-attributed-and-jobid22-cleared.md)
- [Correction to the `get_allday_unresolved_pulls` prescription — the `ORDER BY` is load-bearing, and dropping it would break the only useful work this pipeline does](2026-08-13T1845Z-allday-pull-drain-is-a-forward-resolver-not-a-backlog-drain.md)
- [Rank the DB by disk reads and cache-hit ratio, not by time — and the one "obvious" index that turned out to be a regression](2026-08-13T1730Z-disk-read-ranking-and-the-pack-rips-plan-defect.md)
- [The recurring `pg_net_http_403` CRITICAL is ONE job — jobid 16 — and it is one of the three D2b calls "rotated + verified"](2026-08-13T1530Z-pg-net-403-attributed-to-jobid-16.md)
- [Daytime monitor — 2026-08-13T14:53Z (~07:53 PDT, first-tick-of-day)](2026-08-13T1453Z-daytime-monitor.md)

## 2026-08-12 — 9 filings

- [The board-warm failures now have names, and the numbers say capacity — not a missing index](2026-08-12T2330Z-board-view-timeouts-now-named.md)
- [pg_cron jobid 16 is 403ing 100% of its dispatches — real misconfiguration, LOW data impact, but it makes a brand-new CRITICAL arm permanently red](2026-08-12T1354Z-jobid16-403s-and-a-newly-critical-arm.md)
- [Three follow-ups on the D2 outage — one hazard, one confirmation, one fix that isn't a new monitor](2026-08-12T0430Z-suppression-predicate-and-secret-echo.md)
- [AllDay GraphQL returns 403 (HTML WAF block) through `ALLDAY_PROXY_URL` — edition hydrate may be silently dead](2026-08-12T0428Z-allday-graphql-403-waf-block.md)
- [Finding (CORRECTED) — the STALE label does not survive denormalization into wmc](2026-08-12T0358Z-stale-label-lost-in-wmc-denorm.md)
- [RESOLVED — the pack-opens "spork stall" was a 403 auth outage across 7 pg_cron jobs](2026-08-12T0330Z-edge-fn-403-outage-RESOLVED.md)
- [Daytime monitor candidates — 2026-08-12T03:09Z (≈2026-08-11 20:09 PDT)](2026-08-12T0309Z.md)
- [Decision-ready — `topshot_first_mint_trophy_stats` slow board: root-caused, urgency re-scoped, index-vs-precompute settled](2026-08-12T0056Z-first-mint-trophy-stats-index-vs-precompute.md)
- [Daytime monitor — 2026-08-12T00:12Z (≈17:12 PDT Aug 11)](2026-08-12T0012Z.md)

## 2026-08-11 — 2 filings

- [Inbox — 2026-08-11T17:46Z (10:46 PDT Aug 11) — Claude Code (interactive, read-only DB)](2026-08-11T1746Z-precompute-split-silent-stale-under-role-timeout.md)
- [The gate-key rotation — sized, and an ordering that does NOT reproduce the outage](2026-08-11T0300Z-gate-key-rotation-runbook.md)

## 2026-08-10 — 3 filings

- [Daytime monitor — 2026-08-10T21:07Z](2026-08-10T2107Z-daytime-monitor.md)
- [Queued — the liveness probe can be pruned by the planner, and 2 boards are still genuinely slow](2026-08-10T1900Z-board-liveness-probe-prunes-and-remaining-slow-boards.md)
- [Queued — cron budget headroom audit, the post-ship closes, and the next MV lever](2026-08-10T0555Z-cron-budget-headroom-audit.md)

## 2026-08-09 — 1 filing

- [PARTIALLY DRAINED — deals/rookies public-board backing views are I/O-bound](2026-08-09T1941Z.md)

