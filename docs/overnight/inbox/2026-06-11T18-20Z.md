# Daytime monitor inbox — 2026-06-11T18:20Z (~11:20am PDT)

Run context: lock RELEASED (night pass 09:47Z). Security 0/0 (RLS-off [], anon-write base tables 0); trust health 7/7 ok; sentinel UUID-leak-48h 0 / ts_uuid_dupes_24h 0; detect_stalled_pipelines []; 20/20 deploys READY (prod `1f2c844a`); DB 4,265 MB (+14 vs 08:25Z baseline — calm); unmapped 183 (flat); TS HIGH+MED 3,127 (913 H / 2,214 M, up from 3,103 baseline); sales flowing 1,528/6h. Midday DBSAT wave (the already-logged DAYTIME-DBSAT-RECUR, 07–14:30Z window) fully recovered: fails/hr 16Z=2, 17Z=7, 18Z=0.

## 1. MED/WATCH — SMOKE-CLUSTER-REFIRE: smoke Sentry issues re-fired 17:43–17:53Z on a CALM DB; possible interaction with the new service_role 30s statement-timeout cap
- Source: Sentry NEXTJS-1E (sales indexers / detect_stalled, 14 ev, last 17:53Z), NEXTJS-A (analytics_pipeline_health, 16 ev, last ~17:53Z), NEXTJS-E (/nfl-all-day/overview 200, last ~17:53Z), NEXTJS-W (/disney-pinnacle/overview 200, last ~16:5xZ). The 15:15Z monitor pass saw these quiet 12–13h; new events landed late afternoon while pipeline fails were near zero (17Z=7, 18Z=0) and detect_stalled returned [] at 18:08Z.
- Hypothesis to check (don't assume): `audit_20260611_service_role_statement_timeout_30s` (shipped today) lowers the ceiling for the smoke route's heavy legs — analytics_pipeline_health read ~24s under ambient load per the 18:10Z Cowork note, and the overview-page checks do heavy reads; legs that used to squeak through may now hit 30s. This overlaps the already-queued "smoke legs" residual in the alert-quiet handoff (e7ceebf) — treat this as corroborating evidence + a timing datapoint, not a separate workstream. If the night pass sees smoke ticks failing on a calm DB, per-leg timing on the smoke route's checks (esp. analytics_pipeline_health + the two overview pages) will confirm or kill the 30s-cap theory.
- Risk read: monitoring-signal quality only; no user-facing breach observed (trust 7/7, pages confirmed 200 by prior anon fetches, FMV writers green).

## 2. STATUS — TFP-ROUND-BUG fix CONFIRMED SHIPPED; awaiting the 19:15Z decisive tick
- `audit_20260611_fix_upsert_topshot_marketplace_fmv_round_cast` is live (fn body now has `ROUND(w.fmv_usd::numeric, 2)` — verified in pg_proc). topshot-fmv-populate still 0 ok runs since 06-09 00:50Z (last attempt 14:06Z died on the round bug AFTER passing sets-read + GQL sweep — feed_exhausted, 70 nodes, 243 sets mapped — so the cron entry itself IS firing again).
- Already queued by the alert-quiet handoff ("TFP 19:15Z verdict"); restore the watchlist 800→480 only after the first ok tick. No new action — recording the pg_proc verification so the verdict check doesn't re-derive it.

## 3. STATUS — TSHB-GHA-NOSCHED may be resolving: 2 ok runs in the last 6h (last 17:48Z ≈ the :52 slot)
- topshot-sales-history-backfill logged ok ticks this afternoon after the overnight pass saw 0 schedule events ever. Cadence still sparse vs the 7,22,37,52 spec — the "tshb cadence" residual already queued in e7ceebf covers the follow-through; this is corroborating evidence the GHA cron registered. Verify via the workflow's run history (event=schedule vs dispatch) before closing.

## 4. STATUS — AF1 (v_tracked_wallet_fmv_confidence) is LOAD-SENSITIVE, not hard-broken: 22 rows fast at 18:12Z
- The 15:15Z pass measured a statement-timeout at 15:11Z (tail of the saturation window); on the calm 18Z DB the view returns 22 rows instantly. The rpc-tracked-fmv-confidence artifact works when the DB is calm and errors under saturation. Keeps the standing CLAUDE.md guidance intact (3rd breakage ⇒ fix the writer, not the view) but the night pass should NOT treat it as currently down.

## Green this run (verifications, no action)
- Recent ships healthy: analytics optimization wave (4 migrations, 18:10Z note) — analytics fn legs + dq overview fast; buyer-backfill watchlist @90m no false positive (12 saturation fails but last ok 18:04Z, 20 ok/6h); pack-events p25/p26 cursor advancing (last ok 17:54Z); fmv-recalc 10 ok/6h; topshot-sales-indexer 15/15 ok.
- Artifact spot-sweep: 17/17 enumerated; fragile legs validated live — AF1 22 rows fast, sentinel_fmv_confidence_rows ok, squeeze board 500+ rows, trust health 7/7, deploys API ok. None broken this pass.
- NEXTJS-1P (auth-lock steal, fast-break) still 1 event / 4h quiet — record-only per the 15:15Z entry.
