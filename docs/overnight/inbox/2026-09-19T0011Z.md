# Daytime monitor candidates — 2026-09-19 ~00:12Z (17:12 PT 09-18)

Context: read-only daytime tick, ~5 h after the 09-18 platform read-availability outage recovered (~19:01Z) and ~45 min after the nightly pass declared HEALTHY (shipped 9). Positive control at run time: pg_stat_activity io_wait 2 / active 2 / total 24 — NOT a saturation spell, so durations/state are interpretable. Security 4/4 clean. Vercel: newest tip is docs-only + CANCELED (expected, ignoreCommand); real code tips READY, no ERROR. These are the only NEW low-risk items not already in the ledger/owed/declined.

## HIGH-ish — FMV confidence precompute (jobid 506) missed its post-recovery tick; split is aging with no arm guarding it
- **Source:** `cron.job_run_details` jobid 506 (`rpc-refresh-fmv-confidence-precompute`, sched `35 1,5,9,13,17,21` UTC); `rpc_ops_snapshot().fmv_by_collection_computed_at`.
- **Observed:** last SUCCEEDED **2026-09-18 13:35Z**. 01:35Z and 05:35Z **failed** (`canceling statement due to statement timeout`, outage-adjacent). **17:35Z and 21:35Z produced NO run row at all** — the job was not dispatched (pg_cron worker starvation class: `job startup timeout` also seen on rpc-refresh-set-completers 12:20Z). 17:35Z fell inside the outage window; **21:35Z is ~2.5 h POST-recovery**, which is the one that matters. `fmv_by_collection_computed_at` therefore reads 13:35Z everywhere — ~10.5 h stale at run time, ~12 h by the next tick.
- **Why it can hide:** the underlying FMV data is fresh (topshot_fmv_stale_hours 0.2, fmv_stale_touch_hours 1 — all ok). Only the **confidence-SPLIT snapshot** consumed by `rpc_ops_snapshot` and the FMV/confidence artifacts is aging, and there is **no trust-health arm on `fmv_by_collection_computed_at` age** (the existing `trust_precompute_max_age_hours`=5.97 guards a DIFFERENT precompute). The go-live headline (TS/AD HIGH+MED share) is read from this split, so it can quietly drift stale.
- **Risk:** LOW to ship-nothing now — likely self-heals on the 01:35Z tick.
- **Suggested action (nightly):** verify the **01:35Z** run reads `succeeded`; if 506 keeps missing/failing under recovery-tail load, consider (a) a trust-health arm on `fmv_by_collection_computed_at` age (breach ~13 h, mirroring the trust precompute), and/or (b) moving 506 off the tightest recovery-load minutes — its own seed already ran 106.9 s of the 120 s ceiling, so it has ~0 headroom under load. NOT a code fix tonight; measure first.

## LOW — cross-collection cohort step1 (rpc-ccm-step1) statement-timeout at 23:10Z
- **Source:** `check_pgcron_recent_failures()`.
- **Observed:** `rpc-ccm-step1` **failed 2026-09-18 23:10:00Z** — `canceling statement due to statement timeout` on `CREATE TEMP TABLE _ccm_step1_next ... SELECT ... COUNT(DISTINCT collection_id)`. Post-recovery, single occurrence. (Note: observed slot 23:10Z; the monitor doc records this job at 04:10 UTC — the schedule may have moved, worth a glance.)
- **Impact now:** none visible — `board_mv_refresh_stale_hours`=1.99 (ok, breach 8), so the cross-collection board MVs are fresh. This is the documented heavy-temp-table saturation-collateral class, amplified by the recovery tail.
- **Risk:** LOW.
- **Suggested action (nightly):** confirm the next rpc-ccm-step1 + step2 pair completes and `cross_collection_cohort_mat` / `..._ts_set_overlap_mat` refresh within 26 h. If step1 keeps timing out on the temp-table build, it's the cursored/temp-table cost class — page a bounded slice, don't widen the timeout.

## Not re-filed (already owed / outage-shadow / chronic — recorded for continuity, no action)
- `candy_offers_unverified_pct`=100 (BREACH) — EXACTLY the nightly's owed item; candy-offers-indexer last ran 06:50Z (pre-outage), missed 12:50/18:50Z ticks, **next tick 00:50Z** not yet due. candy_offers_oldest_active_hours=17.3 (ok). Pending self-heal at 00:50Z per the owed note; do not re-diagnose before then.
- `detect_stalled_pipelines()` = 3: **candy-editions-ingest** (chronic band-killed, documented at length) + **compute-golazos-pack-ev** (last 06:37Z, next ~00:37Z) + **candy-offers-indexer** (above). The latter two last ran pre-outage and are pending imminent post-outage ticks — outage shadow, matches the nightly's discharged falsifier.
- 2-day `pipeline_alerts` failure-rates (price-snapshots 55.6%, fmv-backfill 44.4%, backfill-pack-rip-metadata 38.9%, lock-check-batch 31.5%, run-insider-detectors 31.4%) — POOLED across the 09-18 outage window; the outage inflates them. Chronic statement-timeout / upstream-timeout classes (M11/#42/#73/#84). Split on the 19:01Z change point before quoting any rate.
- Sentry is dark by design (SDK removed this session, #34) — no Sentry sweep possible; Vercel deploy state used instead.

---

## ⛔ CORRECTION APPENDED 2026-09-18 ~17:2x PT (Cowork cloud) — THE "HIGH-ish" ITEM ABOVE IS REFUTED ON THREE POINTS. Read this before acting on it.

The item's own words are left intact above; this is a separate correction rather than an edit, per this repo's convention.

**1. The schedule in the item is WRONG, and it is what the whole finding rests on.** The item records jobid 506 as `35 1,5,9,13,17,21` UTC and concludes that *"17:35Z and 21:35Z produced NO run row at all — the job was not dispatched (pg_cron worker starvation class)"*. The LIVE schedule is:

```
jobid 506  rpc-refresh-fmv-confidence-precompute   35 1,5,9,13 * * *   active=true
```

**There is no 17:35 or 21:35 tick. Nothing was missed, because nothing was due.** `cron.job_run_details` agrees across three days — exactly four runs per day, at 01:35 / 05:35 / 09:35 / 13:35, on 09-16, 09-17 and 09-18. ⭐ **13:35Z was its LAST SCHEDULED TICK and it SUCCEEDED, in 6.7 s.** The next is 01:35Z.

**2. "Outage-adjacent" is wrong for the two real failures.** 09-18 **01:35Z** and **05:35Z** both failed on `canceling statement due to statement timeout` at ~120 s — but the outage began ~**12:19Z**, so both PRECEDE it by seven hours. They are not recovery-tail collateral. Both slots recovered on their own: 09:35Z succeeded in 9.5 s, 13:35Z in 6.7 s, and all four of 09-17's ticks succeeded in 11–14 s.

**3. The headroom claim inverts the measurement.** The item says *"its own seed already ran 106.9 s of the 120 s ceiling, so it has ~0 headroom under load"*. Measured runtimes: **6.7 s · 9.5 s · 9.8 s · 10.6 s · 11.5 s · 11.7 s · 13.6 s · 13.9 s · 14.1 s · 21.3 s** on every SUCCESS. The ~120 s figures are the two FAILURES — that is the timeout ceiling being hit, not the job's cost. Typical headroom is roughly **12×**, not zero.

⇒ **CONSEQUENCE FOR THE SUGGESTED ACTION.** "Move 506 off the tightest recovery-load minutes" is aimed at a dispatch failure that did not happen, and sized from a runtime that is the failure mode rather than the work. ⛔ **Do not move the schedule on this evidence.**

✅ **WHAT SURVIVES, and it is worth keeping.** There is genuinely **no trust-health arm on `fmv_by_collection_computed_at` age** — the existing `trust_precompute_max_age_hours` guards a different precompute — and the go-live headline (TS/AD HIGH+MED share) is read from this split. ⚠ **But the threshold must be derived from the TRUE schedule:** at four ticks a day the maximum healthy gap is ~12 h (13:35Z → 01:35Z), so ~11 h stale at 00:19Z is the **designed steady state, not drift**. An arm seeded from the item's assumed 4-hourly cadence would fire every single night on healthy behaviour — the "a pin re-derived from a believed state" trap, one step before it was set. **Size it at ~14–16 h, and only after watching the 01:35Z tick.**

⭐ **The reusable point: the item read the cadence from a doc and the doc was stale. `cron.job.command` / `cron.job.schedule` is the only thing that says what a job does and when — this file's own canon, caught mid-flight.**
