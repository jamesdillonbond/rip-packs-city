# Daytime monitor — 2026-08-19T00:06Z (2026-08-18 ~17:06 PT)

READ-ONLY sweep. One new candidate worth the night pass's attention; everything else red folds into a single daytime IO-pressure episode. **Positive control taken first** (Section 1c): `io_wait=3 / active=3 / total_backends=43`, and `rpc_ops_snapshot()` + every probe returned fast — so this is the **tail of a daytime disk-IO pressure episode, not an acute spell right now**. The MV-refresh timeout cluster is at 18:xx Z (~11:xx PT midday) and has eased. Per focus.md priority 3, the timeout mass is ONE root cause (disk-IO budget on SMALL) — **not opening a new saturation investigation**; the durable items below are the parts that are NOT just "saturation happened."

## Health headline
- **Security: clean** — invariants / anon_write_holes / rls_off_base / secdef_anon all `[]`.
- **Vercel: production healthy.** Newest READY prod = 08-18 21:08Z (Claude Code `get_fmv_coverage` single-probe + Top Shot series-filter fix). Later commits are docs-only CANCELED (ignoreCommand, expected). One **ERROR at 08-18 15:09Z** (Trevor's `drain-fmv-cold-tail` heartbeat fix) — **superseded by 5 later READY builds, and that commit is an ancestor of the current live build, so the change IS deployed.** Near-certain saturation-collateral build failure (CLAUDE.md documents that a disk-IO spell can fail a prerendered `/insights` build). No action.
- **DB 13312 MB. Editions:** TS 19820 · AllDay 6190 · Golazos 575 · UFC 518 · Candy 125.
- **Trust 6/38 breached** — dispositions below.

## Candidate 1 (NEW, low urgency) — `topshot_impossible_parallel_serials` breached 21 (breach_at 3); it read **0/green** when its self-heal cadence was cut hourly→6h on 2026-08-17
- **Source:** trust arm `topshot_impossible_parallel_serials`; self-heal is pg_cron jobid 219 `rpc-selfheal-impossible-parallel-circ`, cut `52 * * * *` → `52 */6 * * *` on 2026-08-17 (ledger ~L1201; `supabase/migrations/20260817185500_..._selfheal_cadence_cut.sql`).
- **Why it's a candidate and not just saturation:** this is recent-ship health. The 08-17 cadence cut recorded an ACCEPTED COST of "~3 editions/day, offenders wait ≤6h, direction conservative, arm reads 0 at time of change." The arm is now 21. That is either the cadence genuinely running underwater (the 6-hourly self-heal itself getting saturation-killed, so the backlog grows past the ~3/day estimate) OR partly a stale reading — the value is served from `rpc_trust_health_precompute`, which is ~15h stale right now (see Disposition D), so the 21 was computed ~09:00Z.
- **Risk:** LOW and self-bounding. Direction is conservative by construction — while `serial > circulation_count` the `serialMultiplier` tail term clamps to 1.0, so an unhealed edition is *under*-premiumed, never inflated. No user-facing over-pricing.
- **Suggested action (quiet window):** measure the **LIVE** impossible-parallel count (not the stale precompute), and jobid 219's last-N run outcomes; if the 6-hourly self-heal is being killed during spells / backlog exceeds the ~3/day estimate, decide whether the accepted-cost note still holds or the cadence needs a cheaper/retry-safe shape. **SYMPTOM caveat:** if measured during IO pressure, treat the count as a symptom and re-measure quiet before concluding the cadence cut is at fault.

## Dispositions — known-class, NOT re-raising (recorded so the night pass doesn't re-derive)
- **A. `public_board_empty_count=999` + `public_board_slow_count=999` — precompute staleness, NOT 999 dark boards.** Both are PRECOMPUTED by pg_cron `rpc-public-board-liveness-sweep`, which is timeout-killed (last fail 12:28Z on `SELECT count(*) FROM allday_scarcity_board`). A missing/stale precompute row reports the 999 sentinel and BREACHES (absence must not read as health). Only 45 boards are watched, so 999 cannot be a literal empty count. Known saturation collateral; the board-liveness/precompute-transaction coupling was already filed 2026-08-10. ⚠ Real cost while red: the arm cannot distinguish a genuine dark public board from its own staleness — a monitoring blind spot exactly during pressure. Lever per focus priority 3 is cutting the sweep's work so it survives a spell, never raising its timeout. Re-measure quiet.
- **B. pg_cron failures (17 jobs) — all `statement timeout` / `job startup timeout` / `lock timeout`, zero logic errors** → saturation collateral, one root cause, not 17 bugs. Most-recent MV-refresh kills cluster 18:xx Z; `rpc-thp-leg-pinnacle-fmv-share` 21:09Z is a scheduler `job startup timeout`. Includes `rpc-ccm-step1/step2` (04:10/04:25Z) — a pre-dawn cross-collection-refresh kill; cross-collection freshness may lag until its next clean tick.
- **C. `fmv_sweep_stall_pct_24h=66.3` (breach 50)** — the known 08-03 cursor-reset (PostgREST 1000-row clamp resets `cursor_after` NULL → sweep restarts at page 0). Pre-existing, not today's.
- **D. `trust_precompute_max_age_hours=14.96` (breach 13)** — the trust-health precompute refresher (jobid 222, single-transaction, no Leg-7 handler) was killed by timeout ~2 cycles ago; this is the arm doing its job (naming the dead refresher before the 24h auto-999 cliff reddens ~13 arms at once). Its staleness is why the `21` in Candidate 1 may be stale.
- **E. `unmapped_resolution_backlog_max=316` (breach 100)** — AllDay permanent floor; focus.md says do NOT raise `breach_at`.
- **F. Pipeline failure-rate alerts (~26)** — overwhelmingly `statement/lock/pool timeout` + `upstream request timeout` = saturation collateral. Non-saturation exceptions, all already known: `topshot-active-listings-ingest` `egress_blocked` (atlas-proxy, needs operator `wrangler deploy`); `candy-editions-ingest` stalled (handoff `2026-08-04-candy-editions-timeout.md`); `compute-golazos-pack-ev` silent 2489min (filed today `inbox/2026-08-18T1406Z-...`).
- **G. Recent-ship health OK.** Today's `reconcile-saved-wallet-stats` autovacuum fix (ledger 08-18) shows 7 fails/24h — those predate the ~17:25Z fix; the arm should be draining. No regression correlated to any 24h ship.

## Artifact validation
Deferred deep payload validation this tick to avoid stacking IO onto the tail of the pressure episode (Section 1c: a payload timeout during pressure is a symptom, not a broken artifact, and re-running heavy payloads adds IO). Estate enumerated via `list_artifacts` (11 present, manifest intact). No code has shipped that would break a payload (git push standing-blocked; only two function migrations landed via Claude Code, low artifact-schema risk). Re-validate payloads on the next quiet tick.
