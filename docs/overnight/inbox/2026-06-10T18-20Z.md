# Daytime monitor inbox — 2026-06-10T18:20Z

Run context: lock stale (13:53Z, >45min) — proceeded. Artifact query-replay abbreviated AGAIN (17 enumerated; DB still timing out simple view reads — replay would add load + false verdicts). `v_rpc_trust_health` UNMEASURABLE this run (2x 57014). Trevor is actively shipping DBSAT remediation (d198e68 load-shed deployed ~17:52Z) — items below are framed as verification work, not fresh diagnosis.

## 1. HIGH — DBSAT-RESIDUAL-17Z: saturation persisted past the 16:20Z populate_wmc_image fix; verify the 17:52Z d198e68 load-shed lands
- Source: pipeline_runs hourly fails: 15Z=54, 16Z=47, 17Z=37 (vs 09Z=1) — the partial-index fix (post-fix probe 2–554ms) did NOT return fails to baseline. At 18:07Z pg_stat_activity showed: autovacuum VACUUM wallet_moments_cache 3.5min IO:DataFileWrite + 2 live PostgREST RPC calls with the `(p_collection_id, p_force/p_limit…)` signature 60–100s in IO:DataFileRead (populate_wmc_image or backfill_wmc_metadata_from_editions — disambiguate via pg_stat_statements deltas, cumulative means are pre-fix-polluted). Monitor's own v_rpc_trust_health reads hit 57014 at 18:08Z + 18:14Z.
- Confound: wmc autovacuum (day's churn) competing for IO on the same table — the fn may be index-fast on a quiet disk and slow under vacuum contention.
- Trevor shipped `d198e68` (~17:52Z): seed-wave pacing over ~9min, 35s fcl.query bound, 202-wraps on pinnacle-wmc-render-id / populate-pinnacle-wmc-fmv / run-insider-detectors. NIGHT PASS: (a) confirm hourly fails drop to ~1–5/hr post-18:30Z; (b) confirm the wrapped routes log pipeline_runs (a wrapped route that goes silent = dead after(), escalate per focus item 2); (c) if long `(p_collection_id,…)` IO reads persist on a calm DB, the populate_wmc_image fix has a gap (force/Pinnacle leg or sibling fn) — identify via fresh pg_stat_statements deltas, do not guess.
- Risk of action: none (verification). Do NOT revert d198e68 on first-tick ambiguity.

## 2. HIGH — SEC: `audit_lt_matches` RLS-off base table with FULL anon+authenticated write grants
- Source: pg_tables rowsecurity=false → `{audit_lt_matches}` (expect 0); role_table_grants confirms anon+authenticated INSERT/UPDATE/DELETE/TRUNCATE (relkind 'r'). PostgREST exposes public schema → anon-writable surface, live now.
- Likely a scratch table from today's interactive audit work (same class as the 06-05 `audit_dq*` incident, resolved then by `audit_20260605_harden_dq_scratch_tables_rls`).
- Suggested action (night pass, SHIP-eligible): mirror the dq recipe — `ALTER TABLE public.audit_lt_matches ENABLE ROW LEVEL SECURITY; REVOKE ALL ON public.audit_lt_matches FROM anon, authenticated;` — or DROP if confirmed disposable scratch (verify rowcount + creator intent in today's transcripts first; if in doubt, harden not drop). Revert: re-grant/disable RLS.

## 3. MED — detect_stalled trio; pinnacle-wmc-render-id has NOT resumed (contradicts focus 9a expectation)
- `detect_stalled_pipelines()` at 18:06Z: pinnacle-wmc-render-id silent 510m (last 09:37Z, med) · backfill-pack-rip-metadata 134m (info) · refresh-pack-grail-metrics-mv 104m (info; had logged 16:23Z then went quiet again).
- pinnacle-wmc-render-id was a "saturation casualty whose cron fires fine" — but it has logged NOTHING since 09:37Z, 9h. d198e68 just 202-wrapped it (it was a sync candidate-select 500ing BEFORE log_pipeline_run → blind). First post-deploy tick should restore logging; if not by tonight, the wrap's after() body is dying — escalate.
- Action: watch next ticks post-d198e68; operator re-fire only if still silent on a calm DB.

## 4. WATCH — Sentry: NEXTJS-1N new (auth-token lock steal, edition page, 1 event 1h ago) — GoTrue lock contention class, watch-only. Smoke cluster (1J/1H/1E/A/4/E/W) consistent with saturation, last seen ~2h; resolve-after-recovery. 1M/1K/15 known carries.

## 5. WATCH — drift: DB 6903 MB (+20 vs 08:20Z baseline 6883 — creep continues, watch-only). unmapped_sales 183 (flat). Sentinel TS-UUID-48h = 0. Security otherwise: only the Item-2 table (RLS-off check is otherwise clean). 20/20 deploys READY (prod d198e68). Editions/FMV histograms deliberately not measured under saturation.
