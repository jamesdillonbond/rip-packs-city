# Daytime monitor candidates — 2026-09-03 (~14:10 PT / 21:10Z tick)

Two NEW findings this tick, both genuine (spell control clean: pg_stat_activity IO-wait 0 / active 0 — causal reads are interpretable). Neither is in the ledger's 09-03 night entry as an open item.

## MEDIUM/HIGH — `topshot_impossible_parallel_serials` newly BREACHING at 8 (breach_at 3); guard was meant to page this

- **Source:** trust-health arm `topshot_impossible_parallel_serials` = **8** (BREACH, threshold 3). Value is PRECOMPUTED — `rpc_trust_health_precompute` row `computed_at 2026-09-03 18:48:00Z` (duration 19,223 ms), value 8. Not the 999 stale-sentinel, so this is a real, fresh count, not a missing precompute.
- **What it catches (arm's own text):** "F1 parallel mis-attribution: sales keyed onto :: parallel editions as impossible serials (serial > parallel circ); invisible to editions-flat / UUID / FMV sentinels — the 0->6 offer_fill drift on 2026-07-02 was caught only by a manual sweep. **Guarded at all 4 writers; pages a future regression.**"
- **Why it's new, not carried:** the **18:07Z** daytime tick reported only 2 trust breaches (`unmapped` + `public_board_slow`) and the 09-03 **night pass** reported only `unmapped`. The precompute that carries this arm refreshed at **18:48Z** — AFTER the 18:07Z snapshot — so the crossing to 8 happened in roughly the last ~2.3h. This is the regression the arm was explicitly built to page on.
- **Reconstruction caveat (measurement discipline):** a naive reconstruction — TS `::`-pattern editions (`external_id ~ '^[0-9]+:[0-9]+::[0-9]+$'`) joined to `sales` where `serial_number > editions.circulation_count` — returned **0 rows**. So the real F1 sweep uses a DIFFERENT circulation source (parallel-edition circ vs the base `circulation_count`, or a wmc/subedition figure). Do NOT trust the naive query; the authoritative count is the precompute's 8.
- **Risk read:** data-accuracy, not availability. Blast radius: up to 8 Top Shot sales attributed to serials that cannot exist on their parallel edition -> can distort per-edition sale history / floor / offer_fill for those editions (the 2026-07-02 precedent moved offer_fill 0->6). No user-facing page is *down*; the numbers on the affected editions are wrong.
- **Suggested action (night pass):** run the ACTUAL F1 impossible-parallel sweep query (the one behind the precompute — find it in the `rpc-trust-health-precompute-refresh` function / the F1 guard code, not reconstructed) to enumerate the 8 offending rows; determine which of the 4 guarded writers admitted them (the guard "pages a future regression" — so either a writer path is unguarded or a guard regressed); correct/remove the 8 mis-keyed rows and close the writer gap. The 8-row data correction is a DB mutation the night pass can do directly once the sweep identifies them; the writer-side fix is likely route/worker code = handoff/queue.

## MEDIUM — `allday-pack-opens-backfill` has crossed from transient to WEDGED on a poison block range (forward cursor not advancing ~70 min)

- **Source:** `rpc_ops_snapshot()` pipeline_alert `running_but_not_succeeding` (medium) + `pipeline_fails_24h` = 37 for this pipeline (upstream 0).
- **Observed:** the last **8 consecutive runs** (19:46Z, 19:56Z, 20:06Z, 20:16Z, 20:26Z, 20:36Z, 20:46Z, 20:56Z — ~70 min) all `ok=false`, `rows_written=0`, on the **identical** range `events 83302329-83302578 status 0`. `extra.scanned_floor` is pinned at **83302579** across all 8, `progress_blocks: 0`, `spork_available: true`, `resolve_exhausted: false`, `partial: false`. `extra.transient: true` on every one.
- **Why this is NOT the benign class the night pass cleared:** the 09-03 night entry classified this pipeline's fails (07:26Z/07:46Z) as benign because "interleaved ok runs around them write rows" and "the forward resolver re-scans next tick." That is no longer true — there are **zero ok runs across 8 straight ticks**, and the cursor (`scanned_floor`) has not moved. The forward resolver keeps re-scanning the SAME poison range and failing identically, so it cannot advance past blocks 83302329–83302578. `transient:true` is the code's optimistic self-label; a range that fails byte-identically for 70+ minutes is functionally a wedge, not a transient blip. Pack-opens (rips/pulls) ingestion for AllDay has made no forward progress for over an hour.
- **Risk read:** MEDIUM. Data-completeness for AllDay pack-opens (feeds pack EV / rip history), not a user-facing outage and not FMV. But it self-heals only if the upstream Flow event API stops returning status 0 for that specific range; if the range is permanently poison, the lane stays wedged indefinitely with `ok=false, rows=0` every tick — silent except this arm.
- **Suggested action (night pass / handoff):** confirm whether blocks 83302329–83302578 are permanently unscannable (status 0 persisting) vs a passing upstream hiccup. If persistent, the resolver needs a skip-past-poison-range guard: after N identical failures on one range, advance `scanned_floor` past it (recording the skipped range) so the forward cursor is not held hostage by one bad window — same shape as the AllDay pull-drain window fix. That is worker/route code = **handoff/queue** (off-limits to ship here). Watch: if it clears on its own next tick, downgrade to noise.

---
### Sweep summary (this run, all read-only)
- `rpc_ops_snapshot()`: security all-clean ([] x4 — invariants / anon_write_holes / rls_off_base_tables / secdef_anon_violations); `stalled_pipelines` []; `sentinel_ts_uuid_editions_48h` 0; `ts_uuid_dupes_created_24h` 0; db **15,845 MB**.
- Trust health: 38 arms, **2 BREACH** — `topshot_impossible_parallel_serials` 8 (NEW, above) + `unmapped_resolution_backlog_max` 201 (KNOWN structural nfl_all_day drain, flat vs 209 last night — not re-raised). `public_board_slow_count` back to **0** (the 18:07Z flicker on candy_scarcity_board resolved, as that tick predicted — its recalibration candidate still stands).
- `check_pgcron_recent_failures()`: **[] clean**.
- `detect_stalled_pipelines()`: [] (note: it and the cadence arm read max(started_at) with no ok filter, so the allday-pack-opens wedge above is invisible to them — the `running_but_not_succeeding` arm is the only one that sees it).
- `pipeline_fails_24h`: dominated by known external/upstream classes (wallet-backfill* Flow Access API, offers-sweep upstream 36/36, ingest 7/7, projections, topshot-* backfills 1 each) — plus the allday-pack-opens 37 (above). No new logic-error class.
- Spell control: pg_stat_activity IO-wait 0 / active 0 — NOT a spell.
- Vercel: latest production **READY = `7bd573d5`** (saved-wallets fail-open cap fix). No ERROR deploys in the last 20; the CANCELEDs are docs-only tips (ignoreCommand suppresses rebuild) — expected, not drift.
- Artifacts: `list_artifacts` = 11 active. Their HTML lives at `C:\Users\TDill\Claude\Artifacts\...`, OUTSIDE the sandbox mount, so embedded payload queries could not be read/validated this run (same limitation as the 18:07Z tick). DB backing surfaces are healthy (no renamed-view/column signal, no empty/slow public board), so no artifact is flagged broken.
- Section 1a (first-tick-of-day) extras skipped — this is a mid-afternoon tick, not the ~8am run.

---
## RESOLVED 2026-09-03 (15:20 PT, Claude Code on Trevor's box) — both findings re-derived; one was the instrument's own echo, the other is a known abort class

**Finding 1 (`topshot_impossible_parallel_serials` = 8) — NOT a new regression; it was the self-healer's routine inflow, read four minutes before it was healed.** The arm is read by pg_cron leg 324 at `:48 0,6,12,18`; jobid 219 `rpc-selfheal-impossible-parallel-circ` ran `raise_impossible_parallel_circ()` at `:52 */6` — the same four hours, over the IDENTICAL predicate (`external_id ~ '::'`, `circulation_count > 0`, `serial_number > circulation_count`; the filing's reconstruction used the stricter `^[0-9]+:[0-9]+::[0-9]+$` regex, which is why it read 0). At 18:52Z the healer raised **five editions** (`258:8989::16` 75→87, `258:8892::16` 1504→3002, `270:8984::18` 39→42, `269:8965::19` 11→20, `266:8924::19` 10→13 — all in `impossible_parallel_circ_raises`), which is where the 8 sales went. A live re-run of the leg's own SQL at 22:00Z read **1**: a 21:29Z sale on `252:8919::21` (Courtney Williams, Galactic), serial 5 against a declared circulation of 4 — and `topshot_moment_subeditions` says nft `52537696` IS subedition 21, so the sales-indexer keyed it correctly off the on-chain map and the parallel's declared circulation is the stale figure. That is the class the healer exists for; it clears at the next heal. **No writer regressed, no data correction is owed.** ✅ **Shipped (DB):** jobid 219 moved to `43 0,6,12,18 * * *` (migration `20260903221540`, `SET LOCAL ROLE cron_heavy`, jobid/owner preserved) so the heal runs BEFORE the read. The arm now reports the post-heal residue; the inflow rate lives in `impossible_parallel_circ_raises` (9 on 09-03, 8 on 08-31). Doc: `docs/reference/trust-board-and-safety.md` (2026-09-03 section).

**Finding 2 (`allday-pack-opens-backfill` wedged) — the cursor DID move; this is the backward-walking backfill lane, and `status 0` is its own documented abort.** `scanned_floor` went 83302579 → 83302079 (21:06Z ok) → 83301829 (21:36Z ok, +4 rows) — it walks DOWN, so a falling floor is progress, and the eight identical failures were followed by three advancing ticks. `status 0` = "fetch threw", i.e. the function's own `AbortSignal.timeout` on the spork-proxy events call — `supabase/functions/ingest-allday-pack-opens/index.ts` line ~157 already records "`status 0` on 40 of 42 backfill runs / 72h is that abort". 48 h window: **98 runs · 56 ok · 41 status-0 · 4,081 rows written.** Forward ingestion is a different lane (`pack-events-ingest`, 32/32 ok, 2,657 rows in 8 h). ⛔ **No code change is shippable here regardless:** `ingest-allday-pack-opens` is on the do-not-deploy list (gate secret unset; memory `edge-fn-deploy-blocked-by-unset-gate-key`), so a skip-past-poison-range guard would sit undeployed until Trevor sets `*_GATE_KEY`. Nothing to do; the alert arm `running_but_not_succeeding` reads correctly and will clear on the next ok tick.
