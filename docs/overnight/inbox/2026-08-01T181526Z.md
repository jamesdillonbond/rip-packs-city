# Daytime monitor candidates — 2026-08-01 ~11:06 PDT (18:06Z, first tick)

Health GREEN overall: security 0/0, trust-health 25/25 arms ok (0 breaches — the overnight fmv_sanity FP has cleared), cross-collection refresh fresh (step1 04:10Z + step2 04:25Z both succeeded, cohort 174), Vercel prod READY (heavy CC test-coverage deploy wave today, all green; 2 CANCELED = docs-only superseded), Sentry 0 new/24h, merged rpc-live-health payload runs clean (FMV/PackEV/offers all fresh within minutes, leak_48h 0). One new candidate below.

## MISATTRIB-MV-REFRESH-TIMEOUT (new, low/medium — likely transient contention)
- **Source:** pg_cron `rpc-refresh-misattrib-candidates`, `check_pgcron_recent_failures()` — failed 2026-08-01 15:35:00Z with `canceling statement due to statement timeout` on `REFRESH MATERIALIZED VIEW public.mv_topshot_misattrib_candidates` (via `refresh_topshot_misattrib_candidates()`).
- **First failure in its recent history** — succeeded daily 07-25 → 07-31 at the same 15:35Z tick (rescheduled 03:25Z→15:35Z on 2026-07-13 per ledger `audit_20260713_reschedule_misattrib_candidates_offpeak` precisely to dodge overnight-peak contention). This is a `cron_heavy`/600s job whose refresh seq-scans the full Top Shot sales history (documented ~613k-cost, contention-bound not index-bound).
- **Context:** the 15:35–18:00Z window today carried a broad *transient* statement-timeout wave (wallet-username-resolver, pinnacle-nft-resolver, allday-lock-refresh, compute-topshot-pack-ev, lock-check-batch, alerts-dispatch, classify-acquisitions) coincident with today's heavy CC deploy activity; most have healthy neighbors and recovered, and by 18:13Z `db_active` is back to 5. So the MV timeout reads as this-afternoon contention, not a new defect.
- **Blast radius:** none user-facing. `mv_topshot_misattrib_candidates` is an internal QA/drain helper (feeds the misattribution-drain detection); a one-day-stale refresh does not affect FMV, insights, or any public surface. The live corrupt-moment detector paths are separate.
- **Risk read:** low. Likely self-clears on the next tick (08-02 15:35Z).
- **Suggested action (night pass):** verify the 08-02 15:35Z tick succeeds via `check_pgcron_recent_failures()`. If it recurs / persists, the ledger 2026-07-13 entry already scopes the levers — a further off-peak reschedule and/or the deferred off-peak `REINDEX CONCURRENTLY` sweep of the 7 `sales_YYYY` partitions (61–71% leaf-fragmented). Do NOT alarm on the single occurrence; do NOT auto-revert anything.

## Already-known / NOT re-raised
- OPENS-HISTORY-BACKFILL-UPSTREAM-500 (`topshot-pack-opens-history-backfill`, upstream `events … status 500`) — queued by the 2026-08-01 overnight pass; unchanged, still firing (12 fails/6h). Not re-raised.
- `candy-offers-indexer` 1 fail "offer sweep 13/48 active (<50%) — deactivation suppressed" — BY DESIGN (partial sweep never deactivates standing offers). Not a finding.
- The broad afternoon statement-timeout wave — transient saturation-class, healthy neighbors, cleared by 18:13Z. Not a finding.
