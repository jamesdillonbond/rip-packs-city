# Daytime monitor — 2026-07-20T06:16Z (~23:16 PDT Jul 19, last tick of day)

Clock clean (shell 06:05:51Z ~= DB now() 06:05:54Z ~= newest sale 05:52Z ~= newest FMV 05:54Z — no skew).
Lock RELEASED. No FREEZE. origin/main `c64c7bc0`. Prod READY `dpl_9CkLzzWzviLfDt9jWEzeqD2vZxaa` (`cf970d9e`).
Health otherwise GREEN: security 0/0/0/0 · trust **15 metrics, 0 breaches** · stalled `[]` · sentinel 0 · Sentry 1 unresolved/24h (1 event) · 0 ERROR deploys.

---

## 1. HIGH — MV-REFRESH-CRON-120S-CEILING: tonight's five MV refresh jobs run as `postgres`, not `cron_heavy`, so their 600s budget is inert. One is already failing.

**Source:** `cron.job_run_details` jobid 204 · `cron.job.username` · `pg_roles.rolconfig` · ledger 2026-07-20 "five MVs shipped tonight".

**What happened.** Tonight's IOPS wave shipped five MVs on six non-overlapping cron slots. The ledger records them as "each behind a 600s proconfig budget". That budget is **not in effect**:

- `rpc-refresh-topshot-pack-rip-values` (jobid 204) fired its **first scheduled tick at 06:05:00Z and was killed at exactly 120.2s** — `canceling statement due to statement timeout` inside `refresh materialized view concurrently public.mv_topshot_pack_rip_values`.
- The function **does** carry `statement_timeout=600s` in `proconfig`. It still died at 120s.
- Root cause: **all five jobs are scheduled as `postgres`** (jobid 202/204/205/206/207). `pg_roles` shows `cron_heavy` carries `statement_timeout=600s` as a per-role default; **`postgres` carries none**, so it inherits the 120s cluster default (`pg_settings` statement_timeout=120000, source=configuration file). Every other heavy job in the estate (60, 71, 72, 73, 74) runs as `cron_heavy`.

**This falsifies a belief recorded in the ledger.** Ledger line ~1149 (2026-07-18, weekly-maintenance split) describes function proconfig as "the proven pg_cron re-arm mechanism" and used it *because* `cron.alter_job(username=>'cron_heavy')` needs superuser. Line ~1649 states the opposite ("the fn-proconfig timeout does NOT re-arm mid-statement — pg_cron batch arms at the role's 600s"). **Tonight's tick settles it in favour of line 1649:** proconfig did not re-arm a `postgres`-owned pg_cron statement. The belief survived until now only because the jobs relying on it never actually exceeded 120s (`prune_stale_wmc` ran 7.1s / 0 rows).

**Evidence so far — 1 confirmed break, not five.** Deliberately not overstated:

| job | fn proconfig | fired | result |
|---|---|---|---|
| 204 ts-pack-rip-values | 600s | 06:05Z | **FAILED @ 120.2s** |
| 207 ts-edition-median | 600s | 06:10Z | succeeded 29.4s (fits under 120s regardless) |
| 206 allday-pack-sales-agg | 600s | 06:20Z | not yet fired at time of writing |
| 202 allday-pack-realized | 600s | 06:35Z | not yet fired |
| 205 ts-pack-sales-agg | 600s | 06:50Z | not yet fired |

**Blast radius — degradation, not outage.** All five MVs are `relispopulated=true` (built at creation), so no board is empty; `mv_topshot_pack_rip_values` is 3.6 MB and serving. The failure means it can **never refresh** on its 6h cadence, so it freezes at tonight's snapshot and drifts. The other four have a 5x-smaller budget than the ship believed and no headroom as the underlying tables grow.

**Suggested action (established in-repo pattern, low risk):** re-own the five jobs to `cron_heavy` via the upsert-by-name path already proven on jobid 71 — `SET ROLE cron_heavy; SELECT cron.schedule('<name>','<schedule>','<command>'); RESET ROLE;` — which sidesteps the `alter_job` superuser limitation. Do **not** simply re-apply another proconfig raise; it is already there and demonstrably inert. Verify by watching jobid 204's next 6h tick (12:05Z) reach `succeeded`.

**Also worth correcting in the ledger** so the next session doesn't re-derive it: the 07-18 "proven pg_cron re-arm mechanism" line is wrong, and `prune_stale_wmc`/`run_weekly_log_purges`/`pinnacle-fmv-recalc-backstop`/`candy-wmc-ghost-purge`/`selfheal-impossible-parallel-circ` (jobids 198–201, 203, all `postgres`) share the same 120s real ceiling — currently harmless because each completes well inside it, but they are not protected by the budgets their proconfig advertises.

**Risk read:** LOW to fix (cadence/ownership only, no logic change, fully reversible by re-scheduling as `postgres`). HIGH value — it silently halves-to-fifths the budget of the entire new MV layer that tonight's performance work depends on.

---

## 2. MEDIUM — DUNE-DATAPOINT-CAP: tonight's `window_days 7 -> 2` hedge did not clear the 402; both pipelines still make zero progress.

**Source:** `pipeline_runs` (`sales-seller-recovery-dune`, `sales-ingest-dune`) · post-ship watch of tonight's `4278943f`.

Tonight's commit narrowed the Dune request window 7 -> 2 days as an explicit "hedge, not a fix". Measured outcome over the following hours: **still failing, still zero progress**.

- `sales-seller-recovery-dune` 05:47Z: `execute HTTP 402 (2026-01-20..2026-01-22)` — the window **is** the new 2-day one — `windows_done: 0`, `drained: false`.
- `sales-ingest-dune` 04:11Z: `402 ... "would exceed your configured datapoint limit"`, `inserted: 0`, `windows_done: 0`.
- 24h fail counts: seller-recovery **16**, ingest **3**.

The hedge failing is *consistent with* the commit's own diagnosis (a per-billing-cycle datapoint cap is not a per-request size problem), so this is confirmation rather than a surprise — but it means both pipelines are now burning an hourly / 2-hourly cron tick indefinitely for guaranteed-zero work until the cycle resets.

**Fail-safe confirmed:** `windows_done: 0` with cursors parked and nothing partial — no data corruption, no double-write risk. Not user-facing.

**Suggested action:** given the roadmap explicitly records the plan will **not** be upgraded and the free Flow-REST worker (203k+ recovered, ~34.5k/day) is the sustainable path, consider parking both crons (deactivate, or gate on a cheap datapoint-budget probe) rather than leaving them to fail on every tick — a permanently-red pipeline is exactly the noise that trains the fail set to be ignored. Reversible; revive on cycle reset if Dune is ever wanted again.

**Risk read:** LOW. Deactivating a pipeline that provably writes nothing cannot lose data.

---

## Checked and deliberately NOT raised (already known / declined / expected)

- **QuickNode 429 on `wallet-backfill-ufc`** (new signature 06:00Z today, 2 events) — ledger ~line 1961 already records this **RESOLVED (no action)**: external free-tier 100/s limit, self-recovers, operator lever is a plan upgrade. Not re-raised.
- **`[edition] offers` statement timeout** (61 events / 12h) — long-known contention family, first seen 07-07, explicitly named in ledger line ~863 as a known class. Not new.
- **`pack_reality_top_ev` = 4 rows** — matches the CLOSED "genuine depletion" finding (74/75 positive-EV TS packs >=90% depleted). Expected, not a regression.
- **CANCELED Vercel deploys** — docs/ledger-only commits skipped by `vercel.json` `ignoreCommand`. Expected; 0 ERROR-state deploys.
- **`sales-counterparty-backfill` 55 fails/24h** — contention-class claim timeouts on a `*/5` worker; ledger records ~100% recovery at full tick rate.

## Artifact validation

Estate 17. Targeted the ones referencing objects tonight's wave changed:
- `rpc-pack-lifecycle` — payload OK against the rewritten `v_topshot_pack_realized_ev` (ev leg 40 rows, sealed 30, global 237,424 packs seen / 95% attribution coverage).
- `rpc-live-health` — `insights_counts` OK, all 12 boards return data (squeeze 501, set_squeeze 168, rookies 61, market 501, first_mint 501, pinnacle_scarcity 501, offer_spread 501, deals 143, trophies 501, new_collectors 50).
No broken artifact found. (Artifact files are readable by exact Windows path via Read/Grep; directory-level Grep is blocked — per-file works.)
