# Daytime monitor — 2026-08-11T06:10Z (23:10 PDT Aug 10)

Environment: workspace shell DOWN again (`useradd … /sessions no space left`, now 5th+ consecutive incident — the Vercel commit log shows an interactive session hit it at 03:19Z too) → no git clone. Connectors (Supabase/Vercel/Sentry/cowork) + file tools work. This file written to the MOUNT; **push unavailable** — night pass picks it up locally. Not the ~8am tick, so Section 1a (trust-health-watch / cross-collection verify) intentionally skipped.

Health: security 4/4 clean · Vercel last deploy READY (`dpl_DU5kUALxARV3ZtLvuTH1zVZJ9NdM`, no ERROR states) · trust **3 breaches ALL known** (panini dry 13; public_board_slow 1 = candy_pack_market pre-fix, see below; unmapped backlog 215 = AllDay backfill inflow, outflow 1032/24h ≫ live inflow 3/24h so actionable is draining) · DB 12,463 MB · sentinel TS-uuid-48h 0 · precompute age 4.67h (post-M3b-fix, healthy).

The 03:19Z monitor tick already logged everything else current — precompute permission-denied (verified remediated by CC ~03:45Z, awaiting 06:58Z scheduled tick), series-timeout `JAVASCRIPT-NEXTJS-27` (still 2 events/1 user, folds into D3b), MV-refresh saturation cluster, reconcile-saved-wallet-stats pre-fix run, pinnacle-sync operator-side drop. All re-confirmed unchanged; not re-filed here.

---

## Candidate 1 — [MEDIUM] both spork-routed pack-opens edge-fn walks STALLED since ~03:00Z while pg_cron keeps firing "success"

- **Source:** `detect_stalled_pipelines()` flags `allday-pack-opens-backfill` (silent 200 min) + `topshot-pack-opens-history-backfill` (silent 175 min). Unlike the usual reading of these two, this is **not a scheduler stop and not a benign no-op canary** — I verified both halves:
  - **pg_cron IS firing them** — `cron.job_run_details` for jobs 55 & 56 shows `succeeded` every tick right up to 06:06Z (job 55 at 05:26/05:36/05:46/05:56/06:06Z; job 56 at 05:26/05:41/05:56Z). The scheduler is healthy.
  - **the edge fn STOPPED completing** — last `pipeline_runs` rows: `allday-pack-opens-backfill` at **02:46Z** (cursor 85942150, was writing ~900–1,043 rows/tick and descending ~500–1,700 blocks/tick, i.e. actively backfilling real work); `topshot-pack-opens-history-backfill` at **03:11Z** (cursor 61808846, 0-row no-op below floor). Since then pg_cron `net.http_post` returns "1 row" (request enqueued) but the edge fn writes no `pipeline_runs` row and advances no cursor.
- **Read:** both are **spork-routed** (spork-proxy → Flow historical access node). They stalled together within ~25 min of each other, which points at a **spork-proxy / mainnet24 access-node reachability blip or an edge-fn-boot failure since ~03:00Z**, not two independent issues. The topshot walk is a deliberate liveness canary precisely to catch this — its silence is doing its job. The allday walk is the one with real impact: it was mid-backfill toward its ~2026-08-14 floor and is now stalled ~3.3h.
- **Blast radius:** LOW-MEDIUM. Not user-facing (historical pack-opens, not FMV/sales). But if the cause is broader (spork-proxy down / edge runtime / pg_net worker wedge) it could affect other edge-fn pipelines — a quick cross-check showed the non-spork pipelines (candy sales 3h/ok, fmv fresh, etc.) are still landing, so it currently looks **specific to the spork path**.
- **Suggested action:** night pass — (1) check the pack-opens edge-fn logs + spork-proxy reachability (`SPORK_PROXY_URL`/`SPORK_PROXY_SECRET`, mainnet24 access node); (2) confirm `allday-pack-opens-backfill` resumes advancing its cursor once reachability returns — if it does, close as a transient upstream blip; (3) if it does NOT self-resume, the edge fn / spork node needs attention. Read-only monitor took no action.
- **Note for the standing false-positive guard:** the two `cron_silent` alerts on these pipelines are NOT suppressed (only their `cursor_stalled` arms are). When the walks legitimately reach done/no-op the `cron_silent` arm will keep firing on every sweep — worth suppressing it too (or emitting a heartbeat `pipeline_runs` row on no-op) so a genuine stall like this one isn't buried in routine noise. Not urgent; hygiene.

---

## NOT findings (dispositioned this run)

- **`public_board_slow_count` = 1 → candy_pack_market.** Board-liveness sweep (jobid 288, `28 */6`) last ran 00:28Z and measured candy_pack_market at **3,593 ms > its 3,000 ms cap** — the single slow board. That measurement PRE-DATES the `candy_pack_ev_model` FMV-scope fix (commit `861d56733`, deploy `dpl_8BupZPuP…` ~04:17Z, which brings candy_pack_market to ~1,830 ms). The next liveness sweep (06:28Z) + precompute (06:58Z) should re-measure it under-cap and clear the breach. If it does NOT clear by ~07:30Z, the candy fix didn't land the board under budget — re-check then. (topshot_set_squeeze 4,789 ms + topshot_first_mint_trophies 3,643 ms are slower in absolute terms but within their larger caps, hence count stays 1.)
- **pg_cron failures** — `rpc-refresh-misattrib-candidates` (15:35Z), `rpc-thin-sale-ask-disclosure-refresh` (09:25Z): standing disk-IO-saturation MV-refresh cluster, already queued (`inbox/2026-08-08T1717Z*`/`1945Z*`), self-retrying. `rpc-reconcile-saved-wallet-stats` (13:33Z): pre-fix daily run, rebuilt by 08-10 CC, next tick 13:33Z 08-11. `rpc-trust-health-precompute-refresh` (00:58Z permission-denied): STALE pre-M3b-fix run (M3b grant landed 01:03Z; precompute age 4.67h confirms a full post-fix run succeeded) — awaiting the 06:58Z scheduled tick per the 03:19Z candidate, no new work.
- **pinnacle-sync silent ~44h** (last run 08-09 10:07Z, missed the 08-10 daily tick). External cron-job.org drop, operator-side, already an operator-gated remainder in the 08-10 CC ledger. Pinnacle FMV freshness is FINE (`pinnacle_fmv_stale_hours` 7.5 / render-floor 4.3, both well under 30 breach; catalog covered by the `/api/admin/backfill-pinnacle-catalog` Vercel backstop). LOW; noting only that it's now TWO missed cadences, so it is not self-healing — the cron-job.org entry likely needs an operator re-enable or a Vercel backstop.
- **sync-nba-projections (all_upstreams_failed), topshot-active-listings-ingest (egress_blocked), wallet-username-resolver + wallet-backfill-* + allday-buyer/unmapped-resolver-tail timeouts** — documented infra-gated / disk-IO-saturation classes, no new code lever.

## Not deep-validated this run
- **Artifacts:** not deep-validated (shell down; following the 08-10 night-pass + 03:19Z precedent of not piling heavy payload queries on a saturation-prone pooler). No schema-breaking DDL landed since the last healthy estate check; risk low. Night pass should re-validate when the shell/DB recover.
