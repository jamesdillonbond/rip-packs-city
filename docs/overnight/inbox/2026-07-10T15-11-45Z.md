# Daytime monitor candidates — 2026-07-10T15:10Z (first-tick-of-day)

Platform GREEN at depth: security 0/0/0/0, trust 16/16 ok (breaches []), sentinel TS-UUID-48h 0, pg_cron failures [], cross-collection refresh fresh (04:10Z/04:25Z both succeeded, cohort 154), Sentry 0 new/24h, prod deploy c70137c READY. rpc-live-health payload validated clean (insights surfaces populated, freshness 15:07-15:08Z).

## NEW candidate (1, LOW)

### PACK-REALITY-TOP-EV-EMPTY — `topshot_pack_reality_top_ev` returns 0 rows
- **Source:** rpc-live-health artifact `insights_counts` panel (pack_reality_top_ev=0); confirmed via direct count.
- **Signal:** `topshot_pack_reality_top_ev` = 0 rows, while sibling `topshot_pack_reality_stats` = 1 and `topshot_pack_reality_dist` = 6, and backing `pack_ev_latest` has 1,187 healthy TS rows (total 1,813). So the pack-EV pipeline is healthy — the ranked "top available positive-EV packs" cut is what's empty. Trust `pack_ev_board_*` metrics were ok/null this tick.
- **Risk read:** LOW. Not a data-layer break. Two plausible causes: (a) EXPECTED — all currently-buyable TS packs are stress-test/depleted/negative-EV, so nothing qualifies for the top-EV ranked cut (consistent with the ongoing pack-board depletion theme); (b) a filter regression from yesterday's 07-10 pack-board wave (`4969aef` stress-test-dist hiding on all boards + `audit_20260710_*` pack_dist migrations) may have inadvertently zeroed the surface. The public `/insights/pack-reality` TS page would render empty in either case, so worth disambiguating.
- **Suggested action (night pass):** Read the `topshot_pack_reality_top_ev` view definition; confirm whether 0 is expected given current TS pack availability (compare to what the surface returned pre-07-10 wave, e.g. via a dated snapshot or the view's underlying filter against available/positive-EV TS dists). If it's a filter regression tied to the 4969aef stress-test exclusion or the pack_dist migrations, repair; if it's genuine depletion, note it as expected so future ticks don't re-flag. Not autonomously actionable without the expected-vs-regression determination (pack-EV surface logic is off-limits to blind edits).

## Carried / already-queued (no new file needed — for context)
- TOPSHOT-MOMENTS-HYDRATOR-GETMINTEDMOMENT-ERRORS — 89 fails/24h (was 31 when queued 07-10 overnight); still alternating ok=true (resolves 50/tick) / ok=false (0/tick), stubs_created 0 / edition_resolution_failures 0 / graphql_failures 0 = NO corruption, self-limiting upstream GQL errors. Already queued; noting the fail-count uptick only.
- ULTIMATE-FMV-RECALC-V1-MISSED-TICK — silent 1951 min (last run 07-09 06:35Z), INFO, operator cron (RPC_ADMIN_TOKEN), self-heals; already carried. Pipeline_alerts notes it was "Repaired this session after 49h silence."
