# Daytime monitor — 2026-06-25T01:06Z (evening tick, ~18:06 PDT Jun 24)

Platform GREEN. **No new actionable candidate** — this file is a STATE UPDATE: the 18:06Z security finding is RESOLVED, and the heavy historical-sales backfill wave is escalating on a 2nd metric (both already-owned). DB now() 01:06Z AGREES with the sandbox clock (no ~5.5h skew this run). Prior tick 18:06Z (logged UFC-VIDEO-BACKFILL-AUDIT-TABLE-RLS + Sentry NEXTJS-X). Lock RELEASED. §1a first-tick extras SKIPPED (already ran 15:14Z).

## RESOLVED since 18:06Z — UFC-VIDEO-BACKFILL-AUDIT-TABLE-RLS (close it; do NOT re-action)
The 18:06Z security finding (`public.audit_20260624_ufc_video_backfill` RLS-off + authenticated INSERT/UPDATE/DELETE/TRUNCATE grants) is FIXED. Trevor's ship **`0ae1b26e`** ("feat(media): recover UFC video_url on-chain (518/518) + widen hover gate; repo-sync anon-grant default", deployed ~00:30Z, prod READY) repo-synced the live `20260624173428` default-privileges anon-DML revoke AND enabled RLS on the table. Now verified clean: `check_public_security_invariants()` [] · catalog `rls_off_base` [] · `anon_write_rls_off` [] · `pg_class.relrowsecurity=true` on the table. The smoke test independently caught the breach at ~19:06Z (Sentry **NEXTJS-1C** "public base tables: RLS on + no anon write", 1 event, quiet since) — a true-positive now stale/resolvable. **Night pass: drain the 18:06Z candidate as ALREADY-RESOLVED.**

## ESCALATION (already-queued root, now a 2nd symptom) — HISTORY-BACKFILL wave
The historical-sales-capture backfill is running HARD: **28,691 TS history-sourced sales ingested in the last 12h**. Two metrics are climbing from this single root (both already owned by the night pass):
- **HISTORY-BACKFILL-UNMAPPED-SPIKE** (ledger l.675, queued): `unmapped_resolution_backlog_max` **1338**/100 BREACH (725 nightly -> 996 @18:06Z -> 1338 now). Edition-unresolvable backfill rows quarantined OUT of `sales` — NOT corruption, no board/FMV impact.
- **Conflation guard now 97** (was 63 @18:06Z / 68 nightly) — a LIVE count (`topshot_conflated_editions`) rising since the last `refresh-conflated-editions` remap (15:17Z, ok=true, ~10h ago) as backfill parallel-sales land on base editions before their `::` rows exist. NOT user-facing (conflated editions are suppressed from deal boards); the daily remap converges it (next ~15:17Z). **Fold into the night pass's existing backfill fix decision** (resolver-coverage / retire-mechanism / pace-cron / remap-cadence) — same root, not a separate incident.
- NEW lever Trevor surfaced today (commit `bb802b42`, docs-only): the **studio-platform GQL** (`api.production.studio-platform.dapperlabs.com/graphql`) serves deep per-edition sales history for all 5 collections (search{AllDay,Golazos,Pinnacle,TopShot,UFC}MarketplaceHistory, real tx_hash/block_time/price/serial, filterable by edition_id) — a possible resolver source for the unmapped backlog; handed to CC as open item #2.

## Health summary
- `detect_stalled_pipelines()` [] · `check_pgcron_recent_failures()` [] · `get_pipeline_alerts()` 1 INFO (ufc_sales resolving_editions, benign/long-standing)
- pipeline_runs ok=false last 6h: **4**, all a self-recovered **19:15-19:33Z contention micro-cluster** (wmc-fmv-populate lock/upstream timeout x2, alerts-dispatch deal-leg statement timeout, check-alerts statement timeout) — known transient classes, **no recurrence in ~5.5h**. 24h fails = 6 total (+ compute-topshot-pack-ev 19:01Z, offers-sweep 04:42Z), ~0.1%.
- security **0/0/0/0** (RLS-off base [], anon/auth-write-on-RLS-off [], `check_public_security_invariants` [], `check_secdef_anon_execute_violations` []) — UFC table hardened, above
- sentinel TS-UUID-48h **0** (no writer leak; `ts_uuid_dupes_created_24h` 0/200) · trust-health **8/9** (1 BREACH = the queued unmapped spike; other 8 ok incl fmv_sanity 0/1, pinnacle_ask 0.2h, pinnacle_fmv 15.0h)
- editions: AllDay/Golazos/UFC **FLAT** (6191/581/518). TS total **17435** (+117 vs the 17318 baseline) = ALL canonical/int-pair growth (UUID-form frozen at 6428, leak_48h 0, dupes_24h 0) — the backfill cataloging new plays, **NOT a UUID leak** (the 06-21 writer-leak fix holding under heavy load). Canonical TS 11005.
- FMV **TS HIGH+MED 4475** (improving: 4399 nightly -> 4413 -> 4420 -> 4475) / **AllDay 902** (flat) / fmv_sanity 0 · TS NO_DATA 3322 (improving)
- DB **5303 MB** (+43 over ~7h, benign creep) · Vercel **0 ERROR** across 20 (prod **0ae1b26e** READY; docs-only `bb802b42` CANCELED as expected via ignoreCommand) · Sentry **3** unresolved, all single-event smoke transients (NEXTJS-W Pinnacle-overview 5h / NEXTJS-X Golazos-analytics 9h / NEXTJS-1C the now-stale security-smoke true-positive 6h), **0 brand-new issue groups in 12h**
- Artifacts: all **12** flagship insights backing relations resolve + return rows (squeeze/deals/scarcity/serial-prem/perfect-mint/offer-sanity/underpriced/rewards/tracked-fmv/top-sales/trophies/rookies). No schema change today touched an artifact backing object. **None broken, none repaired.**

## Post-ship watch handoff (for night pass — new since nightly baseline)
Prod advanced 1e5300fe -> **0ae1b26e** (Trevor, ~00:30Z, READY): UFC video_url on-chain recovery (518/518 -> `editions.video_url`; backup table `audit_20260624_ufc_video_backfill`, now RLS-on) + EditionsGridPaginated `videoEnabled` adds "ufc" + the anon-DML-revoke repo-sync (which RESOLVED the UFC security finding). Clean so far: READY, 0 ERROR, editions flat, 0 attributable Sentry, security clean. Docs-only `bb802b42` on top (CANCELED, expected) records the studio-platform GQL discovery (item #2) + this session's UFC recovery. Fold both into the post-ship watch.
