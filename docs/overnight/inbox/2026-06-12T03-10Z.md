# Daytime monitor inbox — 2026-06-12T03:10Z (~8:10pm PDT 06-11)

Run context: lock RELEASED; security 0/0 (RLS-off [], anon-write-on-RLS-off 0); detect_stalled `[]`; trust health 7/7 ok (closest: pinnacle_fmv 17.0/30h); sentinel ts_uuid_48h 0 / ts_uuid_dupes_24h 0; 20/20 deploys READY (prod `5dfb543`); DB 4,292 MB (+5 vs 01:20Z — creep slowed); unmapped 183 (flat); TS HIGH+MED **3,226** (942 H / 2,284 M, flat vs 3,227); artifacts 17/17 enumerated, fragile legs green (AF1 view 21 rows fast, squeeze 500+, sentinel rows ok, trust view ok, deploys API ok).

## 1. DATAPOINT — PACK-META-SILENCE is SOFTENING: backfill-pack-rip-metadata logged consecutive hourly ticks 01:53Z + 02:53Z (after the alternating-miss pattern 20:53–00:53); refresh-pack-grail-metrics-mv's only miss in 6h was 00:23Z (inside the 00Z wallet-backfill wave)
- Source: pipeline_runs, both pipelines, 20:00–03:00Z window. detect_stalled is now `[]` (the 01:10Z info flag cleared itself).
- Read: supports the dropped-dispatch-under-load theory over a broken entry — misses cluster with the saturation windows, recovery is unprompted. Suggested action: keep the existing PACK-META-SILENCE watch open one more night; only send the operator to cron-job.org history if the alternating pattern returns OUTSIDE a saturation window. No threshold tuning.

## 2. NEW WATCH — TS-GQL-429: offers-sweep took 3 fails in 6h on Top Shot GraphQL "429 Too Many Requests" / "Error with SearchMarketplaceEditions" (latest 02:42Z), each self-recovered on the next 20m tick (03:02Z ok)
- Source: pipeline_runs pipeline='offers-sweep'. First time the monitor has seen explicit upstream 429s (distinct from our own DB-saturation class).
- Risk read: low now (self-recovering, edition_offers authority stays fresh within ~40m worst case). The watch matters because `topshot-sales-history-backfill` (ASK_ONLY Phase 2, cd77861) is about to ramp to 4 ticks/hr of searchMarketplaceTransactions paging through the same topshot-proxy — added GQL volume could turn intermittent 429s into a sustained throttle hitting offers-sweep + sniper-feed + ingest.
- Suggested action (night pass / CC): when tshb reaches full cadence, re-measure the 429 rate across all TS-GQL pipelines; if rising, pace tshb (lower editions/tick or add jittered sleeps between pages) rather than touching offers-sweep. Record-only until then.

## Green this run (verifications, no action)
- **Saturation fully recovered:** hourly fails 21Z 7/194 → 22Z 11/224 → 23Z 9/222 → 00Z 13/860 (the wave itself, 1.5%) → 01Z 7/367 → 02Z **1/234** → 03Z 0/27. The 00:50–01:08Z timeout cluster (wmc-fmv-populate, lock-check-batch, fmv-recalc sales_refetch, pool timeouts) cleared without intervention — same vacuum-debt class, folded into the standing DBSAT re-baseline watch, no culprit chase.
- **analytics-smoke: 14 consecutive ok ticks 21:13Z → 02:43Z** — liquidity LATERAL fix + 110s stopgap holding; ANALYTICS-SMOKE-RESIDUAL stays queued at CC's leisure, no new urgency.
- TFP: the 01:15:25Z ok=true run remains the latest (next 6h slot ~07:15Z); the watchlist 800→480 restore stays SHIP-READY for the night pass (logged 01:20Z item 1, unchanged).
- tshb: still 1 clean run (22:20Z) — sparse-cadence residual already queued, no new item.
- Sentry: 2 unresolved touched in 8h, both known: NEXTJS-1P (auth-lock steal, 3 events/2 users, last 4h, WebKit-class watch) and NEXTJS-15 (single pinnacle-listings event 6h ago). Zero new issues.
- fmv_sanity_flags 0; pack-EV board fresh (1.02d max stale); pinnacle ask 0.2h.
