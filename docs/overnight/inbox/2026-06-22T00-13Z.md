# Daytime monitor candidates — 2026-06-22T00:13Z (4th/evening tick, 17:05 PDT)

Run: rpc-daytime-monitor, evening tick (last of the day before tonight's 01:03 PDT night pass). Lock RELEASED (08:18Z night pass, stale). 1a (trust-health + cross-collection refresh) **SKIPPED** — later-tick-only.

Platform **GREEN**. The substantive change since the 21:17Z tick: prod moved `6b9e89a → 962fc0b8` — a **whole pack-sniper feature wave shipped this evening** (Trevor, post-21:17Z), healthy. It yields **2 net-new low-risk candidates** (both pack-sniper, neither in the ledger). Everything else carried/known.

---

## NEW CANDIDATE 1 (LOW; future-watchlist, night-pass-shippable once cadence banks) — SNAPSHOT-PACK-ASKS-WATCHLIST

**Title:** Add the new `snapshot-pack-asks` cron to `pipeline_cadence_watchlist` so `detect_stalled_pipelines()` can catch it if it silently stops.

**Source:** Vercel deploy `0f19da4` (new route `/api/cron/snapshot-pack-asks`) + `pipeline_runs` (8 runs ever, last **00:08:13Z**, ~7-min cadence, **0 fails**).

**Context:** The new pack-sniper live feed (`0f19da4`) added `/api/cron/snapshot-pack-asks`, which pulls the fresh live pack book per collection and feeds `pack_ask_state` (the board's recency / NEW / price-drop signal) via `upsert_pack_ask_state`. The operator **already wired** the cron-job.org entry — it's firing ~every 7 min (8 runs since ~23:10Z, all ok, last 00:08Z), and `pack_ask_state` is populated (2,882 rows). But it is **NOT on `pipeline_cadence_watchlist`**, so a silent stop is invisible to `detect_stalled_pipelines()`. Same MON1 / Q10 / UFC-DRAIN-WATCHLIST class.

**Risk read:** LOW. If the cron dies the board degrades gracefully to value-order (per the `0f19da4` commit) and the recency signal just goes stale — no corruption, no outage. It should be watchlisted only so a dead cron surfaces.

**Gate: NOT met yet** — the cron is ~1h old. Ship the watchlist row once it banks ~24-48h of clean cadence (mirrors the ufc-enrichment-drain / buyer-backfill watchlist precedent). Suggested once banked: `max_silent_minutes` ~30-40m (≈4-5 missed ~7-min ticks + grace), severity medium/low. Until then: post-ship-watch only (confirm it keeps firing + 0 fails).

---

## NEW CANDIDATE 2 (LOW; night-pass read-only QA) — PACK-SNIPER-INSIGHTS-QA

**Title:** Run the rpc-insights-qa 8-point checklist against the new public `/insights/pack-sniper` surface (shipped `0f19da4` + `962fc0b8` this evening).

**Source:** Vercel deploys `0f19da4` / `962fc0b8` — new public insights surface + Sniper controls, backed by `pack_ask_state` (Cowork `audit_20260621_pack_ask_state_table_and_diff_rpc`) + `get_pack_ask_state_map` RPC + `lib/packs/pack-deals`.

**Context:** A brand-new public `/insights/pack-sniper` surface shipped this evening (live pack-deal feed + Sniper sort/filter controls). It already had one **[HIGH] data-correctness bug caught & fixed same evening** (`962fc0b8` Item 1 — `lib/packs/pack-deals` read `pack_ask_state` via `.from().limit(5000)`, but PostgREST clamps selects to 1000 rows and TS has ~1,900 listed dists, so ~900 were dropped and most TS deals showed no NEW/drop flags; fixed by swapping to the one-row jsonb RPC `get_pack_ask_state_map`. AllDay 979<1000 was unaffected, which isolated the cause). Also fixed: React #418 hydration on the "Updated" label (`962fc0b8` Item 2). This mirrors the SERIAL-PREMIUMS / TOP-SALES / TROPHIES insights-QA closes the night pass has done before.

**Risk read:** LOW (read-only QA verification; any fix would be CC route/.tsx). Security is **already clean** — the new `pack_ask_state` table did NOT trip `check_public_security_invariants()` (still []), so RLS is correct (0/0/0/0). The checklist is mostly: backing-object `security_invoker` + anon-SELECT, route under the public allowlist, sitemap entry, param-stripped canonical, OG 1200×630, crawlable drill-downs, freshness caption, brand tokens.

**Suggested action:** night pass — run rpc-insights-qa on `/insights/pack-sniper`, log any gap (missing sitemap/OG/canonical/etc.) for CC. The data half is verified healthy this run (`pack_ask_state` 2,882; `cross_collection_deals_board` 680; snapshot cron firing 0-fail).

---

## POST-SHIP WATCH — pack-sniper wave (`0f19da4` live-feed + `962fc0b8` clamp/hydration fix) — HEALTHY, no regression

- Current prod = **`962fc0b8` READY** (~23:57Z). 20 recent deploys **0 ERROR** (CANCELED = superseded docs/monitor/in-flight commits).
- New `pack_ask_state` table (Cowork `audit_20260621_pack_ask_state_table_and_diff_rpc`): exists, **2,882 rows**, RLS-correct (no security-invariant trip).
- New `snapshot-pack-asks` cron: **8 ok runs**, last 00:08:13Z, ~7-min cadence, **0 fails** (Candidate 1 = watchlist it once banked).
- No new Sentry from the React #418 fix (Item 2) — the only unresolved Sentry is the carried NEXTJS-1C, quiet 8h.
- `cross_collection_deals_board` 680 rows (returns rows under the load — unaffected by the new pack surface).
- **Night-pass action:** with many more ticks elapsed by overnight, confirm `snapshot-pack-asks` keeps firing 0-fail; run Candidate 2 (insights-QA) on `/insights/pack-sniper`; watchlist the cron once cadence banks. No fix to ship now — wave is clean.

---

## POST-SHIP WATCH — other recent ships (all PASS)

- **Thin-data deal-board flag (`498790cd` + `audit_20260621_topshot_thin_fmv_deal_flag` + `audit_20260621_dispatch_alerts_suppress_thin_fmv`):** verified working exactly as designed — `topshot_thin_fmv_editions` = **96** (matches CC's "96 flagged"), `topshot_deals_vs_fmv` = **522 deals / 10 `low_confidence_fmv`** flagged (matches CC's precise-definition 10). New `low_confidence_fmv` column live on both boards; `cross_collection_deals_board` still returns 680. No security drift (the new RLS+anon-SELECT table did NOT trip invariants). Alerts suppress flagged editions (Pass-1 guard).
- **Today's full TS mis-attribution closeout (`f796447` / `f908c83` / `6b9e89a` / 7 `audit_20260621_*` migrations / `d240fb85` daily drain cron):** conflation guard **17** (converged, stable — not rising), `ts_uuid_dupes_24h` 22/200, FMV reconciles, security 0/0/0/0, trust carried 9/9. The 21:17Z post-ship watch on `6b9e89a`'s onchain leg held; topshot_gql leg + ingest-canonical-guard re-confirm is the night pass's job with full ticks. `d240fb85` daily drain first AUTOMATED fire is ~11:00 UTC.
- **Launch-polish (`c05435d` / `c81f0cb` / `5076f85` — hide Fast Break/RTR/Rewards, hard-gate routes, drop sitemap entry, remove Cart shell):** prod READY, 0 ERROR, no attributable Sentry. Nav/visibility + dead-shell only.

---

## Carried unchanged (NOT re-logging — already queued)
- **REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT** (operator) — MV current at **6778 rows** (fresh, board not broken). No new signal this tick. Operator must re-enable the cron-job.org entry; night-pass watchlists after the first ok tick.
- **refresh-conflated-editions cron** (operator wire daily cron; safe-DELETE fix live, guard converged 17). The thin-fmv refresh is wired into this same route as a non-fatal sibling step.
- **NEXTJS-1C** (Sentry) — smoke RLS-leg failure, cause fixed by CC 16:13Z; **quiet 8h** (5 events, last 8h ago). Cosmetic / regression-arm cleanup only (operator/night-pass mark resolved). NEXTJS-A aged out of unresolved.
- Standing backlog: BUYERBF-PERINVOCATION-WORK, ALLDAY-V1-UNMAPPED-DRIFT (59 open: 56 AllDay / 3 Golazos, flat vs baseline), UFC-EDITIONS-SEED-GAP, TS-WMC-UUID-FOSSILS (TS hyphen-UUID fossils 6428, inert/suppressed), N1 snapshot-institutional-wallets, BADGE-CATALOG-CRONJOB-DUP, VERCEL cost family, A1-WORKER-PASSTHROUGH-CLEANUP, get_user_top_owned_moments 3-arg orphan, PIN-FMV-REKEY-WAVES 2/3, PIN-SYNC-CRON, P3-BUYERS, DUPE1, Q2/Q5/Q6, ANALYTICS-SMOKE-RESIDUAL, IPFS ×2. **STEER honored:** SERIAL-FMV-MULT-CRON BY DESIGN (weekly pg_cron); evm-429 benign.

## Benign note (not a finding)
FMV latest-per-edition rows sum to TS 17,308 vs 17,318 editions (gap 10) / AllDay 6,189 vs 6,191 (gap 2). These are the freshly-cataloged Stage B `::` parallels (TS parallels grew 1,390→1,775 through the day) awaiting their first FMV snapshot — benign cataloging lag, repriced on the next canonical sweep. Night pass: don't misread the tiny non-exact reconcile as a break.

## Artifact validation
16 artifacts enumerated, **0 RETIRED**, none broken/repaired. HTML on OneDrive (outside this run's mount — MONITOR-ARTIFACT-ACCESS), so validated via backing-object row-probe: serial_premiums 178, perfect_mint 13, trophies 685, top_sales 979, underpriced 11, allday_floor 3927, cross_deals 680, special_serial_mv 6778, squeeze 500+, tracked_fmv 22 — all return sane rows; no schema break from today's `audit_20260621_*` thin-fmv or `pack_ask_state` migrations.

## Health one-liner
security 0/0/0/0 · trust (1a skipped — later tick; carried 9/9) · detect_stalled [] · pipeline 6h: evm-429 ×2 (benign) only · sentinel hyphen-48h 24/250 · FMV TS H+M **4313** (1197+3116) / AllDay **884** (251+633), both improving vs overnight 4247/874 · editions TS 17318 (15543 + 1775 `::`) / AllDay 6191 / Golazos 581 / UFC 446 · conflation guard 17 (converged) · thin-fmv flag live (96 / 10 deals) · unmapped 59 open (56 AllDay / 3 Golazos, flat) · DB 5067 MB · Vercel **0 ERROR prod `962fc0b8` READY** · Sentry 1 (NEXTJS-1C cause-fixed/quiet 8h) · pack_ask_state 2882 + snapshot-pack-asks cron firing 0-fail · 16 artifacts (0 RETIRED), all boards return rows.
