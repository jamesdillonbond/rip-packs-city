# Daytime monitor inbox — 2026-06-25T18:11Z (~11:08 PDT)

Platform GREEN. No clock skew (DB now() 18:07Z == shell 18:06Z; FMV computed 18:09Z / sales ingested 17:58Z can't be future-dated). 3rd monitor tick of the day. **One NEW operator candidate** (listing-cache cron drop) + a major **POST-SHIP-WATCH handoff**: Trevor shipped the HISTORY-BACKFILL-UNMAPPED-SPIKE fix this morning and it is LIVE + WORKING.

Health one-liner: detect_stalled = [topshot-listing-cache only, see below] · pgcron [] · get_pipeline_alerts 2 (listing-cache cron_silent + benign ufc_sales INFO) · security 0/0/0/0 · pipeline fails 0 in last 6h · sentinel TS-UUID-48h 0 · trust 8/9 (1 owned BREACH = unmapped 2695, NOW REVERSING) · editions FLAT (TS 17435 / AllDay 6191 / Golazos 581 / UFC 518) · FMV TS H+M 4557 improving / AllDay 908 flat (reconcile exact, fmv_sanity 0) · conflation 104 / thin_fmv 121 · DB 5564 MB (+119/3h, active backfill drains, benign) · Vercel prod 43a86ea READY, 0 ERROR/20 · Sentry 0 unresolved in last 12h · artifacts: data-layer validated, all insights boards + serial/top-sales/conflation/thin-fmv tables resolve & return rows (squeeze 9165 / deals 563 / xcoll_deals 763 / serial_prem 174 / top_sales 1077), no schema change since the 15:15Z full-payload pass, none broken.

## NEW CANDIDATE (operator) — LISTCACHE-CRON-DROP: topshot-listing-cache silent ~8.2h
- Source: detect_stalled_pipelines() (493 min silent / 360 threshold) + get_pipeline_alerts() cron_silent. Last run 2026-06-25 09:53:33Z, ok=true; SILENT since (~8.2h at 18:07Z). The 12 most-recent runs are ALL ok=true / no error -> this is a clean EXTERNAL-cron DROP (the cron-job.org trigger stopped firing), NOT a route failure.
- Crossed threshold AFTER the 15:15Z tick (silent_minutes was ~322 then, < 360) -> genuinely new this tick. This is the recurring LISTCACHE class (LISTCACHE-CRON-DROP / LISTCACHE-SILENT, historically operator-re-fire); the 8.2h gap also exceeds the historical ~285m max gap noted in CLAUDE.md.
- Risk: LOW-MED, bounded. It feeds cached_listings -> badge_editions.low_ask -> fmv-recalc ASK_ONLY. Core/sales-path FMV is FRESH (fmv-recalc ran 1 min ago; latest TS fmv computed 0 min ago) so HIGH/MEDIUM is unaffected; only the ASK_ONLY-minority editions risk ~8h+ ASK staleness while it's down.
- Suggested action (operator): re-fire / verify the topshot-listing-cache cron-job.org entry; confirm the next tick logs ok=true and detect_stalled clears. (If the redundant -v2 entry still exists, consider retiring it — carried LISTCACHE note.)

## POST-SHIP WATCH handoff (for night pass) — NEW flowty-unmapped-drain ship; the UNMAPPED-SPIKE fix is LIVE + WORKING
Prod advanced 8707ab5 -> **98c35dc** (~16:48Z) -> **43a86ea** (~17:52Z, current, READY). Both Trevor, both READY. First monitor sighting (15:15Z predates them). SALES-PATH ship (promotes into `sales`) -> priority watch.
- **98c35dc** (feat): new dedicated consumer cron **`topshot-flowty-unmapped-drain` (9,29,49 * * * *)**. Resolves the open `topshot_flowty_history` `unmapped_sales` backlog via wmc -> nft_edition_map -> getMintedMoment (holder-independent), promotes each into `sales` (dedup on tx_hash, price-agnostic), marks the unmapped row resolved, and fills `nft_edition_map` so the producer route's cheap path catches them next time (shrinks future inflow at the source).
- **REFRAMES HISTORY-BACKFILL-UNMAPPED-SPIKE** (was queued night-count 2): the commit overturns the focus.md "99.9% unresolvable" read — that was a measurement error (it only tested the wmc/tracked-holder path); direct holder-independent getMintedMoment resolved 46/46 to a real setID:playID whose edition exists. The backlog is REAL, RESOLVABLE Top Shot Flowty secondary sales backlogged purely on resolution throughput, NOT junk. **=> the queued "build a topshot-studio-sales-history-backfill leg + retire the Flowty #3 backfill" plan is SUPERSEDED by this drain** (don't pursue it unless this drain proves insufficient). 43a86ea then swapped the candidate filter off a no-precedent PostgREST JSON-path filter onto plain indexed columns (marketplace=flowty, source=onchain) to de-risk the first tick.
- **Verified WORKING this tick:** 4 drain ticks (17:09/17:29/17:49/18:09Z) all ok=true, 0 error. The 18:09Z tick (first on the 43a86ea fixed filter) resolved **198 unmapped in 20 min / 363 in 70 min** (last_resolved 18:10:48Z). Backlog now REVERSING: 2695 (18:07Z) -> 2497 (18:11Z). Promoted sales land as **`source='onchain'`** (NOT a flowty-tagged source — watch via onchain/tx; +333 onchain TS sales/70m includes them). `v_fmv_sanity_flags` 0, editions FLAT, security 0/0/0/0.
- **Night pass — confirm:** `topshot-flowty-unmapped-drain` keeps logging ok=true; `v_rpc_trust_health.unmapped_resolution_backlog_max` crosses BELOW its 100 breach (drain ~198/tick vs producer #3 ~33/20min => net ~165/tick => ~2497 should clear in ~5h, roughly by 23-24Z if sustained); promoted sales dedup clean (distinct tx == rows, no dup-key); fmv_sanity stays 0; editions stay FLAT. Revert (per commit): the new cron is additive/idempotent — remove the `topshot-flowty-unmapped-drain` vercel.json cron to stop it (promoted sales are real; leave them).

## STATE update (NOT a new candidate) — ufc-studio-sales-history-backfill watchlist gate MET
The PENDING watchlist row from focus.md is now gate-met: **8 consecutive clean 20-min ticks** (15:41 -> 18:01Z, all ok=true). focus.md has the ready INSERT (90m/medium — NOT the 600m the other studio crons use, because this one fires every 20 min). Ship-eligible for the next genuine overnight run:
`INSERT INTO public.pipeline_cadence_watchlist (pipeline, max_silent_minutes, severity, notes, is_active) VALUES ('ufc-studio-sales-history-backfill', 90, 'medium', 'UFC studio deep-history drain — 20-min cadence (1,21,41); self-terminates to no-op post-drain', true) ON CONFLICT (pipeline) DO NOTHING;` then verify detect_stalled_pipelines() stays [].

## Owned / not re-logging (trajectory only)
- HISTORY-BACKFILL-UNMAPPED-SPIKE — see POST-SHIP WATCH above; the fix is live and actively draining (no longer needs the studio-leg plan).
- conflation guard 104 — HOLDING (105 -> 104); parallel sales landing on base editions before `::` rows exist; converges on the daily refresh-conflated-editions remap; suppressed from deal boards. Owned.
- offer_edition_gap_max_usd 0/50 ok — clean (the oscillating OFFER-SANITY class; was the self-clearing $192 transient last night).
- PINNACLE-EDITION-KEY-UUID-CAST — already queued (LOW, CC route-side).

## Benign / not-an-incident (do NOT chase)
- get_pipeline_alerts() ufc_sales resolving_editions INFO — benign / long-standing.
- DB +119 MB/3h — the active studio + UFC-studio + flowty-drain backfill wave writing sales; expected, benign.
- The 2 tombstone artifacts (pack-drops-ev-check, rpc-ts-data-mission) — intentional retirements, skipped.
