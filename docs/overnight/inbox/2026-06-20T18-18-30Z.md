# Daytime monitor inbox — 2026-06-20 ~18:08Z (11:08 PDT tick)

Platform GREEN overall: security 0/0/0/0 (detect_stalled [], both invariant checks []), Sentry 0 unresolved, Vercel 0 ERROR (prod c66f088 Stage B foundation READY; today's heavy CC wave all READY/CANCELED). Editions reconcile cleanly — TS 16,917 = 15,543 baseline + exactly 1,374 Stage B `::` parallels; AllDay/Golazos/UFC flat. FMV improving as Stage B intends (TS HIGH+MED 3,332→4,159, ASK_ONLY 2,421→1,831; 15 brand-new `::` rows pending the fmv-recalc sweep = expected convergence). All 11 backing-object boards return rows (no schema break from today's add-column migrations).

Two candidates + notes for the night pass.

---

## CANDIDATE 1 (MEDIUM) — `refresh-conflated-editions` broken at birth: DELETE without WHERE → conflation guard frozen at the 741 seed, over-suppressing de-blended editions

**Source:** pipeline `refresh-conflated-editions` — single run ever, `2026-06-20 15:17:13Z`, ok=false, error `"DELETE requires a WHERE clause"`, `flagged_editions: 0` (wrote nothing). Function `public.refresh_topshot_conflated_editions()` contains the unqualified statement `DELETE FROM public.topshot_conflated_editions;` (verified via pg_get_functiondef), which errors under `sql_safe_updates` before the re-INSERT. Shipped today as part of `a6f731c` (interim parallel-conflation guard); the ledger documents the route but NOT this bug. Not in ledger Queued, not on Declined.

**Risk read / blast radius:** LOW-risk fix, real-but-bounded product consequence. The guard table `topshot_conflated_editions` is frozen at its original 741-row seed (confirmed count = 741, unchanged). Stage B's re-key cut REAL conflation to ~30 (the 676→30 fingerprint in CLAUDE.md), but because the refresh can't run, the interim guards still treat all 741 as conflated: `topshot_deals_vs_fmv` excludes them from the deal board + omni-channel alerts; `topshot_serial_premiums_board` flags 245 rows as "Parallel'd"; `topshot_underpriced_serials_board` suppresses some. Net: ~711 now-de-blended editions are wrongly suppressed/badged — legitimate deals/signals hidden. No corruption, no security/FMV-writer/auth impact. If the operator wired the requested daily cron, it will log ok=false every day until fixed.

**Suggested action:** one-line fix — `CREATE OR REPLACE FUNCTION public.refresh_topshot_conflated_editions()` identical except `DELETE FROM public.topshot_conflated_editions WHERE true;` (satisfies sql_safe_updates), or `TRUNCATE public.topshot_conflated_editions;`. Then invoke once and verify the guard shrinks 741→~30, the deal board grows, the serial-premium "flagged" count drops, and `check_public_security_invariants()`=0 / trust-health stays clean. ALTERNATIVE: if Phase 4 guard teardown is imminent (Stage B was the convergence step), accelerate removing the interim guards instead of refreshing them. SECDEF data-quality guard fn — not off-limits territory; night-pass-shippable or a quick CC migration. Revert: restore the prior function body (the no-WHERE version from a6f731c).

---

## CANDIDATE 2 (LOW-MED) — trust-health `offer_edition_gap_max_usd` BREACH: single-edition chain>gql $250 (likely transient, NOT a stall)

**Source:** `v_rpc_trust_health` → `offer_edition_gap_max_usd = $250` (status BREACH, breach_at $50; was $0/ok at the 08:25Z night-pass baseline). Driver (via `v_offer_sanity_flags WHERE has_sub_serial=false ORDER BY gap_usd DESC`): external_id `57:1909` LeBron James "Deck the Hoops" — 2 open on-chain offers, chain_max_offer $1,000 vs gql_highest_offer $750, flag `chain_exceeds_gql`. Every OTHER flagged edition shows only a benign ~$2 chain-vs-GQL offset.

**Risk read:** bounded + likely transient — NOT urgent. `offers-sweep` is HEALTHY (every ~20min, all ok=true, last 18:02Z) so this is not a pipeline stall. `57:1909` is a normal int-pair edition (NOT a Stage B `::` parallel) so it's not a re-key side effect. One edition's surfaced highest-offer is understated by $250 because a $1,000 on-chain offer (likely placed directly via DapperOffersV2, bypassing the marketplace UI the GQL reads) isn't reflected in `edition_offers.highest_offer` ($750). Self-clears if the GQL catches up or the offer is filled/withdrawn. The $50 tripwire is simply sensitive to a single large on-chain-only offer.

**Suggested action:** night pass re-checks `v_rpc_trust_health` next run. If it persists, consider having `raise_edition_offers_from_chain` surface the on-chain max into `edition_offers.highest_offer` for `chain_exceeds_gql` cases so deal/offer surfaces reflect the true best on-chain offer (CC/operator — touches offer-surfacing logic, judge before shipping). If cleared, no action (transient). Do NOT raise the $50 threshold reflexively — the tripwire is working as designed.

---

## NOTES (not formal candidates)

- **Artifact estate grew beyond the 2026-06-12 documented list.** `list_artifacts` returns 16; the SKILL estate names 12. New (non-RETIRED): `rpc-serial-premiums` (06-16), `rpc-fmv-dialin` (06-16), `rpc-ts-data-mission` (06-20), `pack-drops-ev-check` (06-20, ~1.5h old — Trevor's in-flight build). Night pass: fold these into the estate list and run their payload queries. Backing objects for all are healthy this tick (serial_premiums 181, perfect_mint 11, squeeze 9102, trophies 683, top_sales 840, tracked_fmv 23, offer_sanity 244, special_serial_mv 5929, deals_board 628). Validation this tick was a backing-object sweep (all return rows, no schema break), not per-artifact payload execution.
- **analytics-smoke 17:13Z transient (known ANALYTICS-SMOKE-RESIDUAL):** the failing leg was `rpc_fmv_tier_pulse_collections` (severity fail) + 2 warns (freshness_fmv_per_collection, pipeline_health_24h); recovered fully ok at 17:43Z. Consistent with momentary DB load during the fmv-recalc sweep over the new `::` editions. Not a new finding; watch only if it recurs across multiple consecutive ticks.
- **ts_uuid_dupes_created_24h = 22/200 (ok):** the known inert DQ4 dupe-writer leak (TS-WMC-UUID-FOSSILS/DUPE1), dedup trigger keeps rows inert; far below the 200 threshold and the ~5k/day escalation bar. Editions net-grew by exactly the +1,374 Stage B `::` rows, not from UUID dupes. No action.
- **Stage B convergence is healthy** (FMV reconciliation, de-blending, editions math all consistent with the intended outcome) — flagging only as post-ship-watch context, not an issue.
