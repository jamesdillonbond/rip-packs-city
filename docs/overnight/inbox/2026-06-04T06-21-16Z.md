# Daytime monitor — 2026-06-04T06:21Z

Run: `rpc-daytime-monitor` (daytime, ~23:21 PDT June 3). READ-ONLY sweep. Repo: shared `rip-packs-city` (bot clone not mounted; HEAD=origin/main=`a50f3dd`, 0 ahead — refs clean this run). Lock: 21.7h-old RELEASED marker (overnight pass finished 06-03 08:23Z) — stale, safe to write.

## Health: ✓ GREEN

- `detect_stalled_pipelines()` = `[]`. All active watchlist pipelines under threshold, **including the two new offers indexers** (see SHIP1) at 90m/active, both last-ran <30m ago.
- Security **0/0**: 0 RLS-off public base tables; 0 anon/authenticated write grants on RLS-off base tables (`relkind in (r,p)`). Declined list still `None`.
- Pipelines (6h): only the known transient cron-rush class, all at the **00:00–02:08Z** rush, **nothing in the last ~4h** — `compute-topshot-pack-ev` ×2 (01:08/02:08Z, PEV1 `time_budget_exceeded_after_fetch`), `topshot-moments-hydrator` (00:22Z, N2 candidate-read stmt timeout — do NOT revert the CTE fix), `check-alerts` (00:20Z), `pack-pull-source-rip-id-backfill` (00:11Z, pool timeout). All self-recovered; none logic-faults; none deploy-attributable.
- FMV: `fmv-recalc` fresh (~22m at sweep, mid-cadence; **6,472 TS + 2,043 AllDay** snapshot rows trailing 2h — primary writer healthy). TS HIGH+MED 932→1012→**1025** (HIGH 294 / MED 731), NO_DATA 4424→4155→**4151** (F-series cleanup still compounding). AllDay flat (HIGH+MED **269**, NO_DATA 522).
- Sentinel TS-UUID-48h **19** (baseline 43, last run 22 — still falling; <250 ok). unmapped_sales **148** open (nflallday 147 / golazos 1) — flat vs last run. DB **5913 MB** (baseline 5999; shrank, fine). editions TS 16,352 / AllDay 6,191 / Golazos 581 / UFC 446 (flat vs last run).
- Vercel: **20/20** recent prod deploys READY, **0 ERROR**. Tip `a50f3dd` (offers scope/handoff doc). **Two NEW feature ships since the 03:17Z run** (SHIP1): `cc8a3e78` (AllDay on-chain offers → `edition_offers`, deploy 03:35Z) + `91ac5e10` (TS on-chain offers → `offers` table, deploy 04:02Z). `ce36102f` (item-7 brand tokens, was BUILDING last run) now READY.
- Sentry: **2 unresolved**, both NEW ~2h ago, single-event/single-user, **self-recovered** (last seen = first seen): `NEXTJS-4` "smoke: market API returns Top Shot listings" + `NEXTJS-J` "smoke: pack-listings responds", both `POST /api/smoke-test`. Fired ~04:06Z — squarely inside the `91ac5e10`/`a50f3dd9` deploy-swap window (04:02/04:07Z). Transient mid-swap, not a sustained regression (see SHIP1). PIN1/`NEXTJS-15` has aged out of the unresolved set.
- Artifacts: **14/14** data layers validated healthy (12 prior + **2 new today**: `rpc-offers-intelligence`, `rpc-qa-scorecard`). `offers`/`topshot_offer_depth`/`v_offer_sanity_flags`/`edition_offers` all query clean; squeeze 3101 / market-index 561 / rookie-index 61 non-empty; `get_rpc_traction_snapshot()` → object; `get_wallet_collection_stats('0xbd94…')` F4 split intact (TS 14,274 moments, $66,517.66 excl-STALE / $4,421.07 STALE×177). None broken; none repaired.

## New candidate (1)

### SHIP1 · LOW · post-ship watch (no action unless recurrence) — Today's on-chain offers indexers (`cc8a3e78` + `91ac5e10`) verified healthy; 2 transient smoke blips in the deploy-swap window
- **Source:** Vercel deploys `cc8a3e78` (03:35Z, AllDay offers → `edition_offers`) + `91ac5e10` (04:02Z, TS offers → `offers` table); Sentry `NEXTJS-4` + `NEXTJS-J` (both `POST /api/smoke-test`, 1 event each, ~04:06Z); `pipeline_runs` + `pipeline_cadence_watchlist`.
- **What I verified (read-only, this run):**
  - Both indexers running clean: `topshot-offers-indexer` 11 runs/24h all_ok, last 05:52Z (20m); `allday-offers-indexer` 8 runs/24h all_ok, last 05:47Z (25m). Both watchlisted at 90m/medium/active → `detect_stalled` covered, comfortably under threshold (the AllDay commit's "operator: add cron-job.org entry" TODO is already done — the cron is firing).
  - Data layer populated + queryable: `offers` 361 open / 418 total / 114 distinct bidders / 21 filled / 36 cancelled / 228 editions; `topshot_offer_depth` returns rows; `v_offer_sanity_flags` 37 gaps (10 GQL-blank); `edition_offers` AllDay 196 editions with `highest_offer>0` (top $983). The new `rpc-offers-intelligence` artifact works.
- **Risk read:** very low / likely closed. The 2 smoke failures fired **once each** in the 04:02–04:07Z deploy-swap window and have **not recurred** in the ~2h since (last seen = first seen); the failing endpoints (market-listings via `badge_editions`, pack-listings) are **unrelated to the offers code paths** (offers ships are ADDITIVE — new `offers` table + 2 views + 2 indexer routes; no drops/renames/alters). Classic mid-swap transient, same class as prior single-event smoke clusters in deploy windows.
- **Suggested action:** **none unless recurrence.** Night pass: confirm `NEXTJS-4`/`NEXTJS-J` stayed single-event (mark resolved after 24h quiet); no revert warranted. The offers feature is healthy and needs no intervention. (`v_offer_sanity_flags` = 37 reconciliation gaps is **by-design** — the TS indexer deliberately does not write `edition_offers`; the GREATEST-based raise is an explicit deferred product call, not a monitor finding.)

## No action / already tracked (not re-raised)
- **Recent-ship watch GREEN — nothing to revert.** `f3011d9` (FMV mis-key sweep) still compounding correctly (TS HIGH+MED 1025, NO_DATA 4151, no Sentry correlation). `d8cc6c2` + the `ce36102f`/`cad3253`/`a50f3dd9` doc/brand commits clean. Offers ships covered by SHIP1.
- **MON1** (add `fmv-recalc` to `pipeline_cadence_watchlist` @120m) — logged 03:17Z, still open/unshipped, night-pass SHIP-eligible. Not re-raised.
- **M1** (reschedule `topshot-fmv-populate` off :00 cron peaks) — logged 18:18Z. Not re-raised.
- **PEV1** (`compute-topshot-pack-ev` `time_budget_exceeded_after_fetch`; recurred 01:08/02:08Z this window, ~upstream TS-GQL latency; `topshot_pack_ev_targets` 0 packs >7d stale → no product impact) — tracked. Not re-raised.
- **N2** (hydrator candidate-read stmt timeout, recurred 00:22Z — do NOT revert the materialized-CTE fix), **N3** (payer-wallet funding/cron-revival, operator), **L1** (league-drift cron-wiring), **PIN1** (`NEXTJS-15`, now aged out of unresolved), **Q2/Q5/Q6/Q7/Q8**, **F1/F2-TierB** — all already tracked in the ledger.
- **Q7 / env (INFRA):** no push creds + bot clone not mounted (only the shared tree). Refs were clean this run (HEAD=origin/main=`a50f3dd`, no index corruption) — inbox file written to disk regardless; note if push is unavailable.
