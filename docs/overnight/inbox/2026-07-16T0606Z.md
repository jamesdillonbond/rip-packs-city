# Daytime monitor candidates — 2026-07-16T06:06Z

Run context: late tick (~23:06 PDT Jul 15). Lock RELEASED by night-20260715 at 05:14Z. No clock skew (db_now 06:05:52Z ≈ shell 06:05:36Z). Health GREEN overall — security 0/0/0/0, stalled_pipelines [], sentinel 27 (ok). Only NEW un-queued item below; the two other non-green signals (impossible-parallel breach 16, pinnacle_sales_backfill cursor-stall) are already queued by last night's pass and are known self-healing / spork-floor-terminal classes — NOT re-logged.

## Candidate 1 — OFFERS-RAISE-BACKSTOP-STATEMENT-TIMEOUT (LOW; night-count 1; night-pass-actionable, clean DB-only fix)
- **Source:** `check_pgcron_recent_failures()` → `rpc-raise-edition-offers-backstop` (jobid 48, `34 * * * *`), latest_status=failed at 2026-07-16 05:34:06Z, 2 failed / 24 runs in 24h. Error: `canceling statement due to statement timeout` on the raise fn's `WITH chain AS (SELECT ... max(o.offer_amount_usd) ... FROM offer...)` GREATEST-chain query.
- **Diagnosis:** `raise_edition_offers_from_chain()` proconfig = `["search_path=public"]` only — NO `statement_timeout`, so it inherits the 120s cluster default. It SUCCEEDS in ~11.5s avg on 22/24 runs; the 2 fails are contention-window ticks where the offer-set scan (offers ~73.8k total / ~19k open per the 07-15 weekly sweep, and still accruing) crosses 120s. Flapping, not stuck — 92% success — and the fn is a pure in-DB pg_cron `SELECT public.raise_edition_offers_from_chain()` (no HTTP route / after()-lambda kill-trap), so a per-function statement_timeout raise is the correct + complete fix (same class as the serial-FMV weekly fits that were fixed with a 600s proconfig, unlike the HTTP-route cases).
- **Risk read:** LOW. Additive proconfig on a service_role-only SECDEF fn; behavior-identical when it already completes, only lets the heavy contention-window tick finish. No user-facing surface (this is the offers-sanity durable-raise backstop that clears `offer_edition_gap_max_usd`; currently gap=0, so a missed tick has no live impact today). Note it also gates the weekly sweep's proposed `v_offer_sanity_flags` GREATEST raise (1,804) — if the backstop keeps timing out the gap won't clear.
- **Suggested action (night pass):** `ALTER FUNCTION public.raise_edition_offers_from_chain() SET statement_timeout = '600s';` then verify the next 34-past tick succeeds and `check_pgcron_recent_failures()` clears it. Revert: `ALTER FUNCTION public.raise_edition_offers_from_chain() RESET statement_timeout;`.
