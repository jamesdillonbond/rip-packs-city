# Daytime monitor — 2026-06-03T18:18Z

Run: `rpc-daytime-monitor` (daytime, ~11:18 PDT). READ-ONLY sweep. Repo: shared `rip-packs-city` (bot clone not mounted). Lock: released marker (overnight pass finished 08:23Z) — safe to write.

## Health: ✓ GREEN
- `detect_stalled_pipelines()` = `[]`. Overnight C-PAYER/C-PIN watchlist ship verified live: `cadence-payer-balance-check` is_active=false; `pinnacle-metadata-backfill` max_silent_minutes=200.
- Security 0/0: 0 RLS-off public base tables; 0 anon/authenticated write grants on RLS-off base tables.
- Pipelines (6h): only the known transient cron-rush class — `allday-unmapped-resolver` (pool timeout @18:00Z, 1/19), `pinnacle-nft-resolver` (stmt timeout @12:00Z, 1/72), `topshot-moments-hydrator` (the queued N2, @12:00Z, 1/35). All self-recovered; no logic faults; none deploy-attributable.
- FMV: `fmv-recalc` fresh (~21m, 19 runs/6h, primary writer healthy). TS HIGH+MED 932→971, NO_DATA 4424→4347 (improving — consistent with today's F5 NO_DATA recovery + F2/F3 mis-key cleanup). AllDay flat (HIGH+MED 274, NO_DATA 522).
- Sentinel TS-UUID-48h 35 (baseline 43; <250 ok). unmapped_sales 292 (baseline 278). DB 6008 MB (baseline 5999).
- Vercel: 15/15 READY, 0 ERROR. New prod HEAD `f3011d9` (Claude Code FMV mis-key sweep F2/F3/F4/F5, live ~15:18Z). Post-ship watch GREEN (FMV improving, no Sentry, READY).
- Sentry: 0 new issues in 24h; 0 unresolved firing in last 6h (PIN1/NEXTJS-15 last seen ~05:00Z, not spiking).
- Artifacts: 12/12 healthy. Spot-validated rpc-my-wallet (LATERAL join → TS 14,274 moments/$74.5k), 6 insights backing views (squeeze 3099, deals 131, offer-spread 5560, rookie-index 61, market-index 558, pack-reality-stats 1), get_rpc_traction_snapshot() non-null. No schema break from today's F-items.

## New candidate (1)

### M1 · LOW · operator — Reschedule `topshot-fmv-populate` external cron off the :00 cron-rush peaks
- **Source:** `pipeline_runs`. Last 4 runs: 06-03 06:00Z FAIL (statement timeout), 06-02 18:00Z FAIL (pool timeout), 06-02 13:55Z OK (144 rows), 06-02 12:00Z FAIL (statement timeout). It fires only at :00 peaks and fails ~75% there on connection-pool/statement timeout; its one off-peak run (13:55Z) succeeded.
- **Risk:** very low. cron-job.org schedule change only (move :00 → e.g. :07/:37). No code / DB / route logic. Non-load-bearing: `fmv-recalc` is the primary FMV writer (fresh, healthy) and covers FMV regardless; `topshot-fmv-populate` is a supplementary GQL-marketplace writer. NOT watchlisted (correct — its sparse cadence would false-positive `detect_stalled`), so this is not a stall-coverage gap.
- **Action:** operator moves the cron entry off the :00/:06/:12/:18 boundaries to dodge pool contention; expect the ~75% failure rate to drop. Alternatively accept as-is (CLAUDE.md 2026-05-29 already closed its `ok=false` as known not-a-bug) and drop it from any ok-flag alerting to cut the recurring `pipeline_runs` noise. NOT night-pass auto-shippable (external cron); operator-only.

## No action / already tracked (not re-raised)
- Recent-ship watch on `f3011d9` (F-items) is GREEN — nothing to revert. 8:62 post-F2 verification + the F1 broader mis-key batch + F2-Tier-B Cosmic residual stay in the ledger (F1/F2-TierB).
- The transient cron-rush timeouts (N2 hydrator + allday-unmapped-resolver + pinnacle-nft-resolver) are the known self-recovering class.
