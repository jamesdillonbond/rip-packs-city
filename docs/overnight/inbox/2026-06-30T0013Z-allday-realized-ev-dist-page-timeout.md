# rpc-daytime-monitor — 2026-06-30T00:13Z (evening tick, ~17:13 PDT, no clock skew)

Run context: shell/DB now() 00:07–00:13Z == app-stamped editions batch 23:30Z + pipeline runs through 00:12Z (no skew). Later tick (not the ~8am first-tick), so 1a daily extras (cross-collection pg_cron verify) skipped; trust health read via rpc_ops_snapshot. Lock RELEASED (overnight pass, ~16h stale) — inbox commit permitted. Push available (clone HEAD bced12eb).

Platform GREEN: security 0/0/0/0 · trust 15/15 ok (breaches []; +2 new metrics today from the 06-29 Pinnacle hardening audit: pinnacle_render_floor_stale_hours 14.5/30, pinnacle_fmv_impossible_flags 0/3) · detect_stalled [] · pipeline_alerts 1 INFO (ufc_sales, benign) · check_pgcron_recent_failures [] (the SERIAL-FMV-POWER-MODEL-WEEKLY-TIMEOUT jobid-6 fail aged out of the window; resurfaces ~07-05, do not re-log) · editions real-flat (TS 17,489 / AllDay 6,191 / Golazos 581 / UFC 518) · FMV TS H+M 4,671 (up from 4,645 overnight) / AllDay 920 (up from 905) · fmv_sanity 0 · DB 6,593 MB (+68/24h benign) · Vercel prod bced12eb READY, 0 ERROR (all CANCELED deploys = docs-only, expected).

pipeline_fails_24h: 12 pipelines, but EVERY one's latest run ok=true (all within the last 47 min) — known transient connection-pool-saturation during the backfill wave, 0 genuine stalls (verified per-pipeline).

Sentinel TS-UUID-48h = 17 (was 0 overnight): the known inert DQ4 UUID-dupe-writer leak — a single 23:30Z batch, all 17 UUID-pair form, 0 sales / 0 wmc attached (fully inert, not poisoning), 17 << WARN 250 / trust breach 200. ts_uuid_dupes_created_24h 17/200 ok. Not escalating (« 5,000/day), real editions flat. Carried/known class, NOT a new candidate.

Recent-ship post-glance — the heavy 06-29 Cowork/CC AllDay wave (prod cd0c71a → bced12eb): the now-live AllDay pack-mechanics data layer (pack-OPEN ingestion + realized pull-value + per-dist Mint-attribution crons jobid 20-24, all intentional per ledger) was surfaced to the frontend by bced12eb (dist-page lifecycle strip + realized-EV reality check, edition pull-provenance, new /insights/allday-pack-reality board). All 6 new AllDay views verified security_invoker=on. v_allday_edition_pull_provenance 314 rows; v_allday_pack_realized_ev resolves (0 rows = honest "still gathering" empty board). One new post-ship signal → candidate below.

Artifacts: 12 active in manifest (no RETIRED-tombstone false positives). bced12eb is frontend-only — it consumes views that were already live and validated by the 21:13Z tick (rpc-pack-lifecycle backing views + rpc-live-health legs, ~2h45m ago, no schema change since). Today's only NEW backing surface (the 6 AllDay views) validated directly this run. No artifact broken.

Sentry: connector returned "connection invalidated — needs reconnect" again this run (2nd consecutive tick), so the Sentry leg could not run. Substituted Vercel get_runtime_errors for crash visibility — no new crash classes beyond the candidate below; the rest are the known heavy-query statement-timeout + wmc-dup-key + url.parse-deprecation families. Operator: reconnect the Sentry connector when convenient.

---

## NEW candidate (1)

### ALLDAY-PACK-REALIZED-EV-DIST-PAGE-TIMEOUT (MED — new, attributable to today's bced12eb ship; CC/night-pass; performance, same class as the 21:13Z ALLDAY-CORRECTED-EV candidate)
- Source: Vercel runtime errors — `[pack-detail] allday_pack_realized_ev error canceling statement due to statement timeout`, count 2, users 2, first-seen 2026-06-29T23:28:51Z, last 23:53:46Z, lastDeployment dpl_9dzE3L2A2HoPTnCYssaks3BnfbU4 (commit bced12eb).
- What it is: bced12eb ("surface AllDay pack lifecycle … and model-vs-reality board", deployed ~23:13Z) added a realized-EV reality-check leg reading v_allday_pack_realized_ev to the AllDay /[collection]/pack/dist/[distId] page. That leg statement-times-out on request — a SECOND heavy AllDay aggregation on the same page that already carries the (still-firing, last 23:53Z) allday_corrected_ev timeout logged this afternoon (ALLDAY-CORRECTED-EV-DIST-PAGE-TIMEOUT, 2026-06-29T21:13Z inbox). The view itself is sound: it resolves server-side (0 rows currently = the honest "still gathering" empty board), security_invoker=on. So this is purely a request hot-path query-budget/contention issue, not a broken view or a security/data problem.
- Risk read: LOW-MED. Not a crash, no data corruption, no security exposure (view secured, 0 rows). Bounded to AllDay pack-dist pages. Low count so far (2 in ~25 min) but brand-new + attributable to a deploy that postdates the last inbox file, and will grow as AllDay pack-dist traffic hits the leg — exactly the "recent ship regressing a user-facing surface" class. Page still renders (the leg is non-fatal / empties on timeout); cost is slow loads + an empty reality-check where data will exist once dist attribution fills.
- Suggested action (night-pass/CC — NOT a monitor fix; read-only here): fold into ONE AllDay pack-dist query-budget pass covering BOTH legs — allday_corrected_ev AND allday_pack_realized_ev — rather than fixing them separately. Move the per-request aggregations off the hot path: precompute/materialize the corrected-EV + realized-EV outputs on the existing AllDay cadence (mirror the TS calibrated-EV / v_topshot_pack_realized_ev precompute pattern). Canonical gross_ev is untouched by both (display-only consumers), so low blast radius. Verify: AllDay pack-dist pages load both EV legs without timeout and the two allday_* timeout classes go quiet.
- Cross-ref: extends ALLDAY-CORRECTED-EV-DIST-PAGE-TIMEOUT (docs/overnight/inbox/2026-06-29T2113Z-allday-corrected-ev-dist-timeout.md) — the fix scope GREW with bced12eb (that file predates this leg). The adjacent standing heavy-query class on /pack/dist/[distId] (TS pack_table_rows / pack_lifecycle / pack_realized_ev, on the prior deploy) is the pre-existing connection-pool-saturation tail — fold in opportunistically, don't track separately.

---

No other new candidates. Carried/known left untouched (SERIAL-FMV-POWER-MODEL-WEEKLY-TIMEOUT queued; ALLDAY-CORRECTED-EV-DIST-PAGE-TIMEOUT queued today; WEEKLY-SURFACE-QA-PROSE; ALLDAY-V1-UNMAPPED-DRIFT; THIN-FMV-GUARD-CONTENTION; HISTORY-BACKFILL-UNMAPPED drain; etc. — see ledger). STEER honored: did not re-flag the DQ4 inert UUID leak (17/200, owned), studio-backfill activity, alerts-dispatch/send activity, evm-429, the AllDay pack-mechanics crons 20-24 (intentional), or the SERIAL-FMV weekly cadence. Declined items not re-raised.
