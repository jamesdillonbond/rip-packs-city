# Daytime monitor inbox — 2026-08-02 (~20:06 PDT / 03:06Z tick)

Health sweep GREEN. security 0/0/0/0 · trust-health 31 metrics 0 breaches (the 08-01 benign fmv_sanity FP has cleared to 0) · detect_stalled_pipelines() [] · sentinel TS-UUID-48h 0 · latest prod deploy READY · DB 11,715 MB · rpc-live-health artifact payload validated clean (all boards populated, FMV written 03:08Z). The 08-01 open follow-up is RESOLVED: both Vercel-migrated crons authenticate — topshot-listing-cache 18/18 ok, allow-list-reconcile 12/12 ok, 0 fails on 08-01 (non-zero ⇒ CRON_SECRET good, no revert needed).

## Candidate (1) — LOW/MEDIUM: misattrib-candidates MV refresh timed out AGAIN despite the 07-31 VACUUM; re-justifies the queued sales-partition REINDEX

- **Source:** `check_pgcron_recent_failures()` → `rpc-refresh-misattrib-candidates` failed 2026-08-01 15:35:00Z with `canceling statement due to statement timeout` on `REFRESH MATERIALIZED VIEW public.mv_topshot_misattrib_candidates` (via `refresh_topshot_misattrib_candidates()`). This is a genuinely-recent failure (not a stale pre-fix run) — the job's next tick is ~08-02 15:35Z.
- **Context:** the prior 4 daily ticks (07-28..07-31) all succeeded. The job is already `cron_heavy`/600s and was already rescheduled 03:25Z→15:35Z into the quiet window for exactly this timeout class (ledger `audit_20260713_reschedule_misattrib_candidates_offpeak`). The 07-31 CC session VACUUM'd the 7 sales partitions and cut this refresh 43.5s→30.4s (commit e9102fe) — but that headroom did NOT hold under 08-01 contention, so it breached again. Root cause is a full seq-scan of Top Shot sales history on set_a/set_b (collection_id not selective; the purpose-built collide indexes are used + pristine), so it's contention, not a missing index.
- **Risk read:** LOW blast radius — internal helper MV feeding the misattribution-drain candidate pool, not user-visible; a single miss recovers on the next daily tick. But the VACUUM win is proven temporary, so recurrence is likely. Not auto-actionable by a monitor (read-only).
- **Suggested action (night pass / operator):** the fix candidate is ALREADY scoped in the ledger (7739) but was deliberately not run — an off-peak `REINDEX CONCURRENTLY` sweep across the 7 `sales_YYYY` partitions (their `*_collection_id_sold_at_idx` are 61–71% leaf-fragmented from out-of-order backfill inserts). Alternatively/additionally, re-VACUUM before the 15:35Z tick, or bump this job's statement_timeout above 600s. Watch whether 08-02 15:35Z fails a second consecutive day — that would upgrade this from noise to a real trend.

## Noted, NOT re-logged (already ledgered / queued — carried for context only)
- `topshot-pack-opens-history-backfill` 47 fails/24h — the QUEUED OPENS-HISTORY item; 08-01 CC shipped the descending-walk monotonic-progress fix (a3817e14) + flagged the follow-up "needs a rate/ETA signal since the cursor now advances with ok=true." Backfill-only, no live loss. Known.
- `topshot-active-listings-ingest` 12/24 egress_blocked (HIGH pipeline_alert) — the known TOPSHOT-ACTIVE-LISTINGS-ATLAS-BLOCK / GHA egress class; GHA lane is `disabled_manually` by design (superseded by the home-machine task). Operator item. Known.
- Sentry JAVASCRIPT-NEXTJS-23 "player editions unavailable: statement timeout" (GET /[collection]/player/[slug], 1 event/1 user, 6h ago) — the documented entity-page saturation class (pack/player/team), deliberately left alone per CLAUDE.md. Not a new query regression.
- unmapped-sales nba_top_shot / nfl_all_day — info-level, draining as designed.
