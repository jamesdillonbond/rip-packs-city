# Daytime monitor candidates — 2026-07-04T21:23Z (~14:23 PDT tick)

Health GREEN (transient-contention noise only). security **0/0/0** (invariants/secdef_anon/rls_off_base all `[]`); trust **1 breach = offer_edition_gap_max_usd $73** — KNOWN benign OFFER-SANITY self-clearing transient (chain_exceeds_gql edition-grain; `raise_edition_offers_from_chain` ratchets it up within one ~20-min offers-sweep — ledger explicitly says NOT an incident); `detect_stalled_pipelines()` `[]`; `get_pipeline_alerts()` 2 INFO (golazos+ufc resolving_editions, benign bridge-pending); sentinel TS-UUID-48h **0**; leak_48h validated **0**; editions FLAT (TS 17,491 / AllDay 6,191 / Golazos 581 / UFC 518); FMV TS H+M **4,814** (improving from 18:16Z 4,805) / AllDay 854; DB **7,939 MB** (+122 vs nightly, benign ts_history_backfill drain); Sentry **0** unresolved/24h; Vercel prod **`ab670f5e` READY** (F9 conflated-editions drain, deployed 21:00Z) — no ERROR deploys; **rpc-live-health payload validated clean** (all 12 insights surfaces resolve — squeeze 501 / set_squeeze 239 / pack_reality 6 / rookies 61 / market 501 / first_mint 501 / cross_collection 1 / pinnacle_scarcity 501 / offer_spread 501 / deals 501 / trophies 501 / new_collectors 50; FMV/PackEV/offers fresh 21:07–21:19Z).

**POST-SHIP WATCH — PASS:** the 18:16Z-verified `allday-pack-opens` fix (`26982cc3`, DEFAULT_FLOOR → current-spork floor) HOLDS — `allday-pack-opens-backfill` 21:12Z ok=true (ran a real drain window), `allday-pack-opens-forward` 21:09Z ok=true. Night pass can **CLOSE** the queued ALLDAY-PACK-OPENS-BACKFILL-404 item.

**FORWARD NOTE (F9 ship `ab670f5e`, 21:00Z READY — do NOT flag as leak):** Trevor shipped the conflated-editions drain pipeline (seed→resolve→catalog→split): new SECDEF service_role-only fns (`seed_topshot_conflated_subedition_targets` / `catalog_topshot_subedition_editions_from_resolved` / `remap_topshot_split_resolved_subeditions`) + `/api/admin/drain-conflated-subeditions` + daily cron `30 20 * * *`. **Security invariants 0 confirmed post-migration this run.** When the drain runs it will CREATE `base::subID` TS editions (the intended subedition catalog) and split sales/wmc/moments off the base — do NOT flag that TS edition growth as a writer-leak; it's by-design + reversible (`audit_20260704_subedition_split_remap`). Not yet run this tick (editions flat; cron's 20:30Z slot already passed pre-deploy, first real fire tomorrow).

---

## Candidate 1 — DAYTIME-CONTENTION-CLUSTERS-BROADENING (LOW / WATCH; escalation of the carried OVERNIGHT-0623Z-CONTENTION-CLUSTER + statement-timeout family; DB-only fns / operator)

- **Source:** TWO distinct daytime DB-contention windows today, each tripping multiple duration-capped jobs at once:
  - **~17:40–18:23Z:** `rpc-remap-misattributed-sales` (18:23Z, 120.0s) + `rpc-allday-ev-corrected-refresh` (18:23Z, 120.1s) both FAILED via `check_pgcron_recent_failures()` (1 fail / 4 runs each); plus allday-cross-source-dedup 17:40Z (18:16Z Cand 1), lock-check-batch 17:38/18:08Z (18:16Z Cand 2), classify-acq 18:06Z.
  - **~21:04–21:13Z:** fmv-recalc (21:08Z), lock-check-batch (21:08Z again), wallet-username-resolver (21:08Z), analytics-smoke (21:13Z), compute-topshot-pack-ev (21:07Z) — AND timed out `rpc_ops_snapshot()` + the trust-health/combined-security reads from THIS monitor run. `pg_stat_activity` at 21:06Z showed the driver: `autovacuum: VACUUM public.wallet_moments_cache` running ~381s + a 293s PostgREST RPC.
- **What's NEW vs the ledger:** the remap + allday-ev-corrected-refresh cluster (queued 07-03 as OVERNIGHT-0623Z-CONTENTION-CLUSTER, previously **06:23Z-only**) has now graduated to a **DAYTIME** tick (18:23Z) — the exact "if it fails multiple ticks it graduates" escalation the 07-03 note predicted. A second daytime window (21:0xZ) tripped even more jobs + the ops snapshot itself. Pattern is now **multiple-times-daily**, correlating with DB size (7,939 MB, climbing from the still-draining ts_history_backfill_v1 on Pro Micro) + autovacuum IO on hot tables (wmc).
- **Risk read:** LOW / transient. Every job recovers on its next tick (compute-topshot-pack-ev already recovered 21:19Z ok=true; allday-pack-opens 21:12Z; remap/allday-ev next tick 00:23Z; `detect_stalled_pipelines()` `[]`). No user-facing / FMV-output / security impact — FMV latest write 21:18Z fresh, TS H+M improving, editions flat, security 0/0/0. remap = the misattribution self-healer (converges over days); allday-ev-corrected-refresh backs a precomputed matview (a skipped tick just serves the prior precompute). Same DB-growth statement-timeout family as the carried CLASSIFY-ACQ-ALLDAY + THIN-FMV-GUARD-CONTENTION + FMV-RECALC-EDITION-FETCH-TIMEOUT-CREEP items.
- **Suggested action (night pass):** (1) re-measure remap / allday-ev-corrected-refresh / lock-check-batch durations OFF the contention windows — if remap keeps failing daytime ticks it's graduated (bump its night-count). (2) Both failing pg_cron fns are pure DB-only — a bounded `statement_timeout` raise OR a recent-window scan bound is in-scope for a verifiable DB-only change (verify each is a plain pg_cron `SELECT`, NOT an `after()`-lambda route, before picking the lever — the special-serial-MV anti-pattern). (3) Strategic: root is Pro Micro IO headroom vs a DB at 7,939 MB and growing from the still-draining ts_history_backfill drains — weigh pacing the backfill drains or the disk-headroom lever; passive watch for now. Do NOT re-log the dedup / lock-check-batch pieces individually — they're in the 18:16Z inbox file; this consolidates the pattern.

_Note: `rpc_ops_snapshot()` (the monitor's fast baseline) timed out both attempts this run under the 21:0xZ contention; ran the component checks (security / trust / editions / FMV / pipelines / artifact) directly instead — all completed. Not a broken function; it's the same heavy-read-under-contention symptom._
