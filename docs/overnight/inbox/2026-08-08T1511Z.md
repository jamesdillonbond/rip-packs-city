# Daytime monitor inbox — 2026-08-08T15:11Z (~08:11 PT, first tick)

Written to the mount; **push unavailable** (workspace shell down: `/sessions` "no space left on device" — the escalated class from the 08-08 overnight NO-PUSH). The night pass will pick this up locally.

Sweep verdict: **BREAKAGE-adjacent — active DB-pooler saturation with real (recoverable) data loss.** Security 0/0/0 clean. Trust precompute fresh (12:58Z), only known breaches (UFC fmv-stale 96.1 by-design, panini dry-days 11 upstream home-box, board-slow 6 crawler). Cross-collection refresh both steps succeeded today. 0 new Sentry/12h. No Vercel ERROR deploys (the CANCELED ones are superseded rapid-push builds). `detect_stalled_pipelines()` = topshot-moments-hydrator only (info, 36m). Artifact HTML unreadable this pass (files outside the mounted folder + shell down) — validation skipped, not a finding.

---

## HIGH — Pooler saturation is actively dropping wallet_moments_cache rows (runtime face of the DB-headroom issue Trevor is profiling today)

- **Source:** `pipeline_runs` (last 6h, live at 15:11Z). `SELECT count(*) ... error ILIKE '%connection pool%'` = **64 pool-acquire-timeout fails in 6h, 24 in the last 1h (accelerating)**. `sum(rows_lost)` from wmc_upsert_chunk_failures = **5,343 wallet_moments_cache rows lost in 6h**. Blast radius (all "Timed out acquiring connection from connection pool"): wallet-backfill-{allday 32 / pinnacle 21 / ufc / golazos / multicollection}, pinnacle-nft-resolver (16), compute-topshot-pack-ev (11), lock-check-batch (7), topshot-flowty-unmapped-drain (7), fmv-recalc (5, step3 delete lock-timeout), populate-pinnacle-wmc-fmv, snapshot-pack-asks, check-alerts, run-insider-detectors. Concurrent pg_cron MV-refresh statement timeouts: `rpc-refresh-perfect-mint-premiums` (5/24), `-allday-pack-realized` (2/4), `-topshot-edition-median` (2/4), `-backfill-pinnacle-acquisitions` (2/4), `-allday-pack-sales-agg` (1/4). Raw PG reads calm (37 active / 47 total) — classic **pooler-pool** exhaustion, not raw-connection cap.
- **Risk read:** LOW-permanence / MEDIUM-impact. The wmc losses are **recoverable** — wallet-backfill RPCs are idempotent upserts and wmc self-heals on the next successful walk (not a cursor-advance-past-rows permanent loss). But 5,343/6h and climbing means the wallet-viewer/`/share`/snapshot surfaces carry transiently-incomplete holdings for affected wallets until re-walked. This is the **runtime harm** dimension that the cron-waste ledger analysis (2026-08-08 entries) does not quantify — Trevor already independently hit this exact `PGRST003` timeout on read-only catalogue queries and recorded "the instance has no headroom right now."
- **Already covered / do not duplicate:** the CAUSE-side is deep in Trevor's ledger (perfect-mint-premiums 2,417s/day #1 waster; the `ed_med` 180d-median → own-MV split recommendation; "cadence-cut is NOT the safe fix"). The two chronically-stale MV caches (`mv_topshot_misattrib_candidates`, `fmv_thin_sale_ask_disclosure_cache`) are queued in `inbox/2026-08-08T1443Z.md` + the 2026-08-08 QUEUED ledger entry. The 5 concurrent MV statement-timeouts above are the **same saturation class** and will self-recover once headroom returns — symptoms, not separate items.
- **Suggested action (night pass):** do NOT bump statement timeouts or cut MV cadence blindly (both explicitly guarded against in Trevor's ledger). The additive value here is: (1) treat the `ed_med`-split / heavy-MV-optimization work as **higher priority than "wasted seconds" framing implies** — it is actively losing wmc data, not just burning CPU; (2) if a genuinely-quiet window opens, the profiled lever (move the 180d perfect-mint / edition-median medians off the hot ingest path into their own daily/6h MVs, per the ledger) is the headroom win; (3) consider whether the wallet-backfill wmc-upsert path should retry the lost chunk rather than log `rows_lost` and move on, so a pool-timeout mid-backfill self-repairs same-run instead of waiting for the next scheduled walk. Verify-after: `SELECT count(*) FROM pipeline_runs WHERE ok=false AND error ILIKE '%connection pool%' AND started_at > now() - interval '1 hour'` should fall back toward single digits once load clears.
