# Daytime monitor — 2026-06-17T18:09Z (11:09 PDT, mid-day tick)

**PLATFORM GREEN except one watch.** `detect_stalled_pipelines()` + `get_pipeline_alerts()` both flag `topshot-buyer-backfill` (silent 104m vs 90m threshold) — NOT an outage: 0 fails, cursor still advancing, finite historical backlog, zero user-facing/data impact. But it's a leading-indicator escalation on the just-shipped `95c07c5` maxDuration fix and the one thing worth the night pass's eyes tonight. Everything else clean. **1 NEW candidate** (BUYERBF-DURATION-CREEP). Daily extras skipped (the 8am tick already ran them: trust 9/9, cross-collection PASS).

## Health (every-run sweep)
- `detect_stalled_pipelines()` = **topshot-buyer-backfill** (silent 104m / thr 90m, medium); `get_pipeline_alerts()` = same one (cron_silent). Every other watched pipeline current.
- `pipeline_runs`: 6h **1 fail** — offers-sweep 17:02Z "Error with SearchMarketplaceEditions" (transient AllDay GQL, recovered next tick; offers-sweep 18:02Z ok). ~3,200 runs/6h otherwise clean.
- Security **0/0/0/0**: RLS-off base tables [], anon/auth-write-on-RLS-off (none — 0 RLS-off base tables), `check_public_security_invariants()` [], `check_secdef_anon_execute_violations()` [].
- Sentinel TS-UUID-keyed-editions-48h = **0**.
- Sentry: **0 new** unresolved firstSeen in 12h.
- Vercel: prod **`c17f47a` READY** (NEW Trevor ship since 8am — alerts buy-link 404 correction); **0 ERROR** across 20 recent (CANCELED entries = superseded docs builds, normal). DB **4764 MB** (+5 vs 8am, benign).
- Drift vs baseline: editions flat (TS 15543 / AllDay 6191); fmv_sanity_flags 0; FMV TS HIGH+MED **3224** (up from 2848 night low, ~= 8am 3216 — recovering toward 3435 peak, benign).

## Recent-ship health (since the 8am tick)
- **`c17f47a` "fix(alerts): stop emitting 404 Top Shot buy links" (~17:18Z) — PASS.** Corrects the 2dd19af buy-link feature that shipped pointing at `nbatopshot.com/marketplace/editions/<setID>/<playID>` (404s): `lib/alerts/format.ts` topshotEditionMarketUrl now returns null; `app/api/sniper-feed/route.ts` tsBuyUrl uses the verified `/moment/<id>` page. alerts-dispatch 24/0 + alerts-send 36/0 post-ship; READY; corrective/additive, no schema change. Healthy.

## Artifacts (14 active validated; 5 RETIRED tombstones skipped)
Backing data resolves for all: serial-premiums **271** . perfect-mint **9** . underpriced deal board **43** . tracked-fmv-confidence **21** . v_offer_sanity_flags **212** . v_fmv_sanity_flags **0** . cross_collection_cohort_mat **158** (fresh 04:10Z) . v_rewards_economy 1 / balances 10 . get_rpc_traction_snapshot() non-null . trophy/editions healthy . live-health core fresh (TS FMV 18:08Z). None broken; none repaired (read-only).

## NEW candidate
1. **BUYERBF-DURATION-CREEP — post-ship-watch escalation on `95c07c5` (MED · night pass owns the 95c07c5 watch; the batch lever is CC/Trevor route code).** Source: `detect_stalled_pipelines()` + pipeline_runs hourly breakdown. `topshot-buyer-backfill` ran 5-6x/hr cleanly through 08:14Z (the night pass's "max 503s<600s, 0 fails/37 runs/12h" reading) then collapsed to **~1 run per 2.5h since 09:14Z** (09:14 -> 11:24 -> 13:44 -> 16:24Z). 0 fails logged and the cursor is still advancing (now draining 2023-06 sales), BUT per-run duration crept to **566s — only ~34s under the 600s maxDuration ceiling** (vs 503s / ~97s headroom this morning), with `decode_failed` climbing 0 -> 64 as the cursor reaches denser/older 2023 data. **Risk read:** the headroom `95c07c5` bought (maxDuration 300->600, batch 300->200) is being eaten as the drain hits a harder region; if a run crosses 600s it resumes dying invisibly mid-`after()` before the finally-block log — the exact silent-cron-gap class `95c07c5` just fixed. The missing afternoon ticks may already be that, OR cron-job.org tick-drops (can't distinguish from pipeline_runs alone). **Blast radius LOW:** historical backlog drain, finite (clears regardless), no live-data/user impact, no corruption, TS sales-indexer fresh/healthy. **Not re-raising** the CLOSED BUYERBF-CRON-DROP or the DECLINED watchlist-widen — this is the maxDuration-headroom angle, not the cron cadence or threshold. **Suggested action:** night pass re-measure `max(duration_ms)` over the next window; if it's still >=550s / climbing, trim `batch_size` 200->100 in the buyer-backfill route (CC/Trevor — route constant, keeps runs well under 600s in the dense region) to restore headroom, or accept the slow grind and note it explicitly so the detect_stalled flag is understood as benign.

## Status updates (already-queued; no new candidate)
- **SERIAL-FMV-MULT-CRON — drifting further (escalating).** `serial_fmv_multipliers` last computed 2026-06-15 23:06Z = **~43h stale** (37 cells; 40.1h at 8am, 33h at night). Backs 2 live surfaces (/insights/serial-premiums 271 + underpriced deal board 43 via serial_fmv_estimate). Slow-moving population-level tier x circ-band premiums so low-risk per-day, but the drift compounds the deal-board "discount" staleness. Operator/Trevor: wire the `refresh-serial-fmv-multipliers` cron (cron-wiring, not auto-shippable).
- **FMV-HIGHMED-DIP-WATCH — recovery confirmed, night pass can close.** TS HIGH+MED **3224** (up from the 2848 night low; 8am 3216), climbing back toward yesterday's 3435 daytime peak. Confirms the benign daily-cycle ASK_ONLY reclassification by canonical writers (1.7.0 fmv-recalc + cold-tail-1.0); fmv_sanity_flags 0, writers fresh (18:08Z), not ship-attributable. Recommend closing once it re-confirms HIGH+MED >= ~3400.
- **OFFER-SANITY-VIEW-REFINEMENT — slow drift, in scope.** v_offer_sanity_flags **212** (210 at 8am, 177 earlier). ~99% sub/serial offers correctly out of edition-grain scope; edition-grain is covered by the trust leg offer_edition_gap (0/50). Already queued.

## Noted, not findings
- offers-sweep single transient fail 17:02Z (AllDay SearchMarketplaceEditions GQL) — recovered next tick; normal external-API hiccup, not attributable to any ship.
