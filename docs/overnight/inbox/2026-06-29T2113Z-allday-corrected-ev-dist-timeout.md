# rpc-daytime-monitor — 2026-06-29T21:13Z (afternoon tick, ~14:13 PDT, no clock skew)

Run context: shell 21:06Z == DB now() 21:06Z == app-stamped fmv write 21:09Z (no skew). Later tick (not the ~8am first-tick), so the 1a daily extras (cross-collection pg_cron verify) were skipped; trust health still read via rpc_ops_snapshot. Lock RELEASED (not held) — inbox commit permitted. Push available (clone HEAD e3753024).

Platform GREEN: security 0/0/0/0 · trust 13/13 (breaches []) · detect_stalled [] · pipeline_alerts [] · check_pgcron_recent_failures [] · sentinel TS-UUID-48h 0 · editions flat (TS 17,472 / AllDay 6,191 / Golazos 581 / UFC 518) · FMV TS H+M 4,673 (up from 4,645 overnight) / AllDay 910 · DB 6,582 MB (+57/24h, benign) · Vercel prod READY (cd0c71a / dpl_DmW576QnzxxNM4tuKPdDBiZJates), 0 ERROR deploys (the CANCELED ones are docs-only, expected). pipeline_fails_24h: 13 pipelines but EVERY one's latest run ok=true (transient/recovered — wmc-fmv-populate 7, pinnacle-nft-resolver 6, compute-topshot-pack-ev 4 all latest-ok within last ~30m).

Heavy 06-29 Trevor+Cowork daytime wave observed (current prod cd0c71a): AllDay pack-OPEN ingestion (edge fn + crons), AllDay corrected-EV adoption in UI/OG (f5595fa/d279d83/4a4c1ad), per-parallel :: ASK floor for STALE TS subeditions (656b506 + topshot_parallel_asks table + fmv-recalc Step 5e), TS @usernames in pack sales history, trophy-picker team+series. -> night pass should post-ship-watch this wave.

Artifacts: 13 active (12 prior + new rpc-pack-lifecycle), no RETIRED-tombstone false positives, none broken. Validated the two most blast-radius-exposed dashboards live: rpc-pack-lifecycle backing views all resolve (global 1, sealed 1738, life 377, ev 199 dists, 60d daily 146,208 rows, $496,851 realized) and rpc-live-health non-snapshot legs all resolve (12 insights boards return rows; FMV/pack-EV/edition-offers all written within ~2 min; Pinnacle 2,179 priced renders). The other 11 artifacts are outside today's pack-EV/AllDay/parallel-ask blast radius and share core views confirmed healthy via the snapshot — morning-tick validation relied on for those.

Sentry connector returned "connection invalidated — needs reconnect" this run, so the Sentry leg could not run. Substituted Vercel get_runtime_errors for crash visibility: no new crash classes — all entries are the known connection-pool-saturation + heavy-query-statement-timeout families (last overnight already classified these benign). Operator: reconnect the Sentry connector when convenient.

---

## NEW candidate (1)

### ALLDAY-CORRECTED-EV-DIST-PAGE-TIMEOUT (MED — new, attributable to today's ship; CC/night-pass; performance + display-correctness)
- Source: Vercel runtime errors — `[pack-detail] allday_corrected_ev error canceling statement due to statement timeout`, count 42, 18 users, route /[collection]/pack/dist/[distId], first-seen 2026-06-29T17:08:45Z, last 2026-06-29T20:39:03Z (ongoing). lastDeployment dpl_DmW576QnzxxNM4tuKPdDBiZJates.
- What it is: the AllDay corrected-EV leg adopted on the pack-dist page today (commits f5595fa "surface odds/median-corrected EV in UI" + d279d83/4a4c1ad, reading v_allday_pack_ev_corrected / v_allday_pack_info) statement-timeouts on request. The per-tier-median x published-odds/circulation-weight aggregation is computed per-request and is too heavy for the page's query budget. The commit messages say the leg is non-fatal/falls back, so the page still renders — but on timeout it falls back to the un-corrected canonical EV (the inflated "$4 pack at $430" the change was meant to replace), i.e. intermittent display-correctness regression + slow loads on AllDay pack-dist pages.
- Risk read: LOW-MED. Not a crash, no data corruption, core platform green; bounded to AllDay pack-dist pages. But it is a same-day post-ship signal on a user-facing surface and is recurring (42 hits in ~3.5h), exactly the "recent ship regressing" class. NOT yet in the ledger (today's wave postdates the last night pass).
- Suggested action (night pass / CC — NOT a monitor fix; read-only here): materialize or index the corrected-EV computation (e.g. a precomputed v_allday_pack_ev_corrected-backing table refreshed on the existing AllDay cadence, or push the aggregation off the request hot-path), mirroring the TS calibrated-EV / v_topshot_pack_realized_ev precompute pattern. Verify by AllDay pack-dist pages loading the corrected EV without the timeout and the allday_corrected_ev error class going quiet. Canonical gross_ev is untouched by this (display-only consumer), so the fix is low-blast-radius.
- Adjacent (watch-only, NOT a separate candidate): `[edition] pack provenance Timed out` (count 6, new today, from c4935320 edition-page pack provenance) and the pack-dist heavy-leg cluster (pack_table_rows 74 / pack_lifecycle 13 / pack_realized_ev 6) are the standing connection-pool-saturation + heavy-query class on /pack/dist/[distId] — known, not new today; fold into any pack-dist query-budget pass rather than tracking separately.

---

No other new candidates. Already-known/carried items left untouched (SERIAL-FMV-POWER-MODEL-WEEKLY-TIMEOUT queued; WEEKLY-SURFACE-QA-PROSE; ALLDAY-V1-UNMAPPED-DRIFT; THIN-FMV-GUARD-CONTENTION; etc. — see ledger). STEER honored: did not re-flag SERIAL-FMV-MULT/POWER weekly cadence, evm-429, studio-backfill activity, alerts-dispatch/alerts-send activity, or allday-listing-serial-backfill ok=false (external 1009 WAF). Declined items not re-raised.
