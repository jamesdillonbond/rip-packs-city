# Daytime monitor candidates — 2026-07-27 ~00:09Z (17:09 PDT tick)

Health sweep GREEN: security 0/0/0/0, trust 20 metrics 0 breaches, stalled_pipelines [], pg_cron check_pgcron_recent_failures() [], sentinel_ts_uuid_editions_48h 0, Sentry 0 unresolved/24h, Vercel prod f9e56aed READY (0 ERROR; the 3 CANCELED tips are docs-only, correctly skipped by ignoreCommand). Primary merged dashboard (rpc-live-health consolidated payload) runs clean with fresh data (FMV/PackEV/offers all < 2 min old, leak_48h 0, sales 24h TS 2403 / AllDay 374 / candy 99). No migration or view/table rename shipped today, so no dashboard-breaking schema drift.

---

## 1. ALLDAY-DECODE-LEG-EFFICACY — the always-on stage-2 decode leg is buying nothing at real Flow-REST cost (LOW/MEDIUM; the 07-26 CC ship's own flagged WATCH, now data-backed)

- **Source:** `pipeline_runs` `allday-unmapped-resolver` (extra->>'decode_attempted' / 'resolved_via_decode' / 'scan_chunks'); the 2026-07-26 CC ledger entry's WATCH note "ALLDAY-DECODE-LEG-EFFICACY" (`lib/chains/flow/allday-edition-onchain.ts` + `allday-resolve-unmapped`).
- **Observation (~9h since the 15:01Z resolver-fix deploy):** every tick logs `decode_attempted=60`, `resolved_via_decode=0` — the unblocked decode leg (which now runs on every unresolved row, ~60 extra Flow-REST tx fetches/tick) has resolved 0 rows on 100% of ticks. `scan_chunks` sits at ~198 every tick (the scan spends its full Flow-REST budget for 0 scan resolutions — `scan_ineffective`). One tick (21:36Z) blew out to a 156,830 ms `resolve:upstream request timeout` (vs the normal ~31–40s), i.e. the added Flow-REST load occasionally pushes the whole route past its upstream timeout.
- **NOT a regression / already-explained:** the resolver flipping to ok=false + 0 promoted from 15:00Z onward is the INTENDED behavior of the same-day CC tripwire fix (it now honestly reports `degraded: onchain resolution failing, 0 promoted` where it previously lied ok:true). This is the carried HIGH `ALLDAY-UNMAPPED-SALES-BACKLOG-GROWTH` item (27,135 actionable open rows; resolution genuinely failing because only ~30 of 26,251 open price-certain rows are pre-mapped and old moments are non-borrowable). Do NOT re-flag the tripwire fails as new breakage.
- **Risk:** this is resolver ROUTE logic (ingest-adjacent), so it likely falls in the night pass off-limits set — flagging for Trevor/Claude-Code rather than autonomous auto-ship. The change itself is low-risk and additive-narrowing.
- **Suggested action (per the CC WATCH note, decidable now):** confirm the 0/60 trend holds at the 24h mark, then narrow the decode leg to fire only when the stored buyer was excluded/absent (not on every nil borrow) and reclaim `SCAN_CHUNK_BUDGET` given `scan_ineffective`. Net effect: cut ~60 wasted Flow-REST fetches/tick + the scan budget, removing the upstream-timeout blowout risk, with no loss of resolutions (the buyer leg / `decodeV1SaleTx` produced 196/196 of recent resolutions).

---

Nothing else new. Carried/known (already in ledger, not re-raised): DUNE-DATAPOINT-CAP-402 (sales-ingest-dune / sales-seller-recovery-dune HTTP 402, operator/billing), ALLDAY-UNMAPPED-SALES-BACKLOG-GROWTH (HIGH, resolver throughput / off-limits), SET-DETAIL-PAGE-POOL-RETRY-GAP, and the standing queue.
