# Daytime monitor — 2026-08-13T14:53Z (~07:53 PDT, first-tick-of-day)

Written to mount, push unavailable (workspace shell down — `useradd … /sessions no space left on device`, same class as the 4-consecutive-night overnight NO-PUSH; operator provisioning fix still owed). The night pass / Claude Code picks this up locally.

## Candidate 1 — HIGH: `compute-pinnacle-pack-ev` fails EVERY 6h tick, deterministic ON CONFLICT, not saturation, not in the ledger

- **Source:** `pipeline_runs` — `compute-pinnacle-pack-ev`, `ok=false`, error `upsert pack_distributions: ON CONFLICT DO UPDATE command cannot affect row a second time`.
- **Scope/recurrence:** EVERY scheduled run fails with the identical error — verified back through 2026-08-11 18:17Z (08-13 12:17 / 06:17 / 00:17, 08-12 18:17/12:17/06:17/00:17, 08-11 18:17 all `ok=false`, same message). This is the pg_cron job `rpc-compute-pinnacle-pack-ev` (jobid 42, `17 */6 * * *`). It is NOT a `check_pgcron_recent_failures()` hit because the job's `net.http` dispatch "succeeds" — the failure is inside the route and only shows in `pipeline_runs`.
- **Why it's a real finding, not saturation noise:** the message is a deterministic Postgres 21000 — the `pack_distributions` upsert batch contains ≥2 rows that collapse to the same `ON CONFLICT` conflict-target within a single statement, so PG aborts the whole upsert. It reproduces at every tick regardless of DB load (the 00:17Z quiet-window ticks fail identically to the 12:17Z saturated ones), which rules out the disk-IO-saturation class that dominates the rest of today's fail list.
- **Blast radius:** Pinnacle pack-EV compute aborts at the `pack_distributions` upsert step → Pinnacle pack-EV / `/disney-pinnacle/packs` surface data goes stale (no fresh EV rows land). Bounded to Pinnacle packs; not user-auth / not money / not FMV-math. Medium user impact (low-traffic gated-ish surface), high confidence + cleanly fixable → filing HIGH so the night pass looks at it.
- **Suggested action (night pass, low-risk + CI/typecheck-gated):** in the `compute-pinnacle-pack-ev` edge fn's `pack_distributions` upsert, DEDUPE the batch by the ON CONFLICT key before the upsert (keep last-writer), OR split into per-row upserts with the 23505/21000 row-by-row fallback pattern already used by the sales indexers. This is a pricing-adjacent edge fn — per CLAUDE.md the pack-EV *math* is hand-off-for-review, but a batch-dedup that changes NOTHING about the EV computation (only stops the self-collision) is mechanical and safe; verify byte-identical EV output on a dry run before deploy. ⚠ It's a Supabase edge fn → needs deploy access + `--no-verify-jwt` (verify_jwt=false), so this may be an operator/edge-session ship, not a git-only one. Root-cause first: `SELECT external_id, count(*) FROM (…the batch the fn assembles…) GROUP BY 1 HAVING count(*)>1` to confirm which dist keys collide.
- **Revert:** n/a until a fix ships; the fix itself reverts via `git revert <sha>` + edge redeploy.

## Context — NOT new candidates (known/carried, logged so the night pass doesn't re-triage)

- **Active disk-IO saturation storm ~13:00–14:49Z.** The 6h `ok=false` list is ~18 pipelines nearly all `canceling statement due to statement timeout`: compute-topshot-pack-ev, wallet-username-resolver, refresh-insights-cache (deals + rookies boards — the known I/O-bound public boards), topshot-flowty-unmapped-drain, pinnacle-nft-resolver, lock-check-batch, alerts-dispatch, check-alerts, refresh-error-triage, fmv-recalc (`sales_refetch_failed … saturation-class`), etc. Plus the known non-saturation carried ones: `sync-nba-projections` all_upstreams_failed (NBA offseason), `topshot-active-listings-ingest` egress_blocked (GHA runner IP), `wallet-backfill-{allday,golazos,pinnacle,multicollection}` Flow HTTP-400 / pagination-chunk / dispatch-gap (Flow computation-limit class). All saturation-class; no individual pipeline is down, this is the documented Small-tier disk-IO over-subscription. Not shippable (fix is precompute/caching, CC/operator-owned).
- **pg_cron `rpc-trust-health-precompute-refresh` 12:58Z FAILED** (statement timeout on an editions⨝sales count leg) — the documented midday-saturation pattern: 08-13 06:58/00:58 + 08-12 18:58 all `succeeded`, only the 12:58Z tick fails (same on 08-11, 08-12). Post-split (jobid 287) this loses ONE leg for one tick and recovers at 18:58Z; not a new regression.
- **pg_cron `rpc-refresh-misattrib-candidates` FAILED** (last run 08-12 15:35Z, statement timeout on `REFRESH MATERIALIZED VIEW mv_topshot_misattrib_candidates`) — once-daily 15:35Z slot lands in the midday saturation window; fails 08-11 + 08-12 identically. Known disk-IO-saturation MV-refresh class. Today's 15:35Z tick hasn't fired yet.
- **`topshot-moments-hydrator` silent 49 min** (vs 30 threshold) — severity `info`, the documented "remaining silence is signal" no-op-walk class (nothing to hydrate). Not alarming on a single over-threshold read.
- **Trust health 4 breaches, all known/carried** (unchanged from the 08-11 night pass): `panini_fmv_stale_hours` 37.4 / `panini_sale_price_capture_dry_days` 15 (residential Panini home-box runner outage, operator-side), `unmapped_resolution_backlog_max` 225 (AllDay backfill inflow, net-fluctuating), `public_board_slow_count` 3 (saturation-collateral; down from 16 last night). No NEW trust breach.

## Health summary
Security 4/4 clean (RLS-off 0 · anon-write 0 · invariants [] · secdef-drift []). Cross-collection refresh FRESH (cohort 04:10Z n=179, overlap 04:25Z — both steps succeeded today). Sentry 0 new/12h. Vercel 0 ERROR-state deploys (recent tip CANCELED = docs-only, expected). DB 12 GB. FMV HIGH+MED rows/48h: TS 19,977 · AllDay 4,634 · candy 223 · golazos 12. Artifact estate enumerated (11 active) — heavy payload re-validation SKIPPED this tick: an active saturation storm makes piling 11 read-heavy payloads onto a throttling DB the wrong call, and nothing shipped today (night pass NO-PUSH, 0 deploys, no migrations) so schema-break risk to the dashboards is nil.
