# Daytime monitor inbox — 2026-06-05T03:12Z

Run: `rpc-daytime-monitor` (READ-ONLY sweep). Shared repo (no `rip-packs-city-bot` clone mounted). `docs/overnight/.lock` present but STALE/RELEASED (explicit "RELEASED 2026-06-04T13:19:02Z by runid=458930129" marker, age ~13.8h >> 45min) -> not a live run; no concurrency skip.

**Active interactive session in progress** — this evening's work landed live during the sweep: migrations `audit_20260605_dq1_build_dupe_canonical_map` (02:40Z), `audit_20260605_dq1_merge_cohortA_repoint_retire` (02:42Z), `audit_20260605_dq2_backfill_canonical_circulation_onchain` (03:06Z) + 4 `audit_2026060*_rewards_*` migrations; newest `editions.updated_at` = 03:10Z (mid-sweep); new prod HEAD `b3dae3db` (share-OG route-handler fix) deployed ~minutes ago. Treat the large TS deltas below as Trevor's intentional dedup, NOT a regression.

Platform **GREEN** apart from one new low-blast-radius security-posture item (SEC1). Health: `detect_stalled_pipelines()` = **[]** (N1 self-recovered since the 00:20Z run); 4 single transient cron-rush fails in 6h (pinnacle-nft-resolver / wmc-fmv-populate / evm-transfers-ingest / wallet-backfill-multicollection — all 00:00-00:10Z pool-timeout class, self-recovered); **no R1 hydrator-FK fails** (R1 RESOLVED — see notes); 20/20 recent prod deploys READY, 0 ERROR; Sentry 1 new transient from tonight's share-OG fix iteration (NEXTJS-1G, self-resolving) + the 2 known smoke blips (NEXTJS-4/-J, ~23h quiet). FMV improving hard (TS HIGH+MED 1062 -> **1638**); DB 5965 MB.

---

## NEW candidate — SEC1 (MED · night-pass DB or operator): RLS-off + anon/auth-writable scratch table from the in-flight DQ1 dedup

- **Title:** Harden or drop `public.audit_dq1_dupe_map` — RLS off with full anon/authenticated INSERT/UPDATE/DELETE/TRUNCATE grants.
- **Source:** security sweep (Section 1). Both catalog checks that normally return 0 now return exactly this one table: `pg_tables rowsecurity=false` -> `audit_dq1_dupe_map`; the anon/auth write-grant-on-RLS-off check -> 8 grant rows (INSERT/UPDATE/DELETE/TRUNCATE x anon+authenticated) on the same table. Baseline (night 06-04 13:05Z) was 0/0.
- **What it is:** the dupe->canonical mapping table (`dupe_id uuid, canonical_id uuid`, 5,137 rows) created tonight by `audit_20260605_dq1_build_dupe_canonical_map` (02:40Z) as a working table for the TS edition dedup. Created as a plain `CREATE TABLE` so it inherited the project's default broad grants and never got RLS — unlike the `rewards_*` tables shipped the same evening, which are RLS-clean (verified: absent from both security checks).
- **Blast radius:** LOW. `public` is the PostgREST-exposed schema, so the public anon key could in principle TRUNCATE/write it — but it backs **no** product surface (internal uuid->uuid dedup map, reproducible from the migration; no PII/secrets; DB-only scratch artifact). Worst case = corrupting Trevor's in-flight dedup map, which is rebuildable.
- **Risk read:** LOW. Fix is one statement, fully reversible, mount-independent (night-pass DB lane). **BUT it is the live working table of an operation that was still writing during this sweep** — the night pass MUST re-verify it still exists and is no longer in use before acting (Trevor may drop it himself when the DQ dedup / DQ3+ follow-ups finish). Do NOT touch while the dedup is mid-flight.
- **Suggested action (after the dedup settles + re-verify):** drop if finished — `DROP TABLE public.audit_dq1_dupe_map;` — or harden in place: `ALTER TABLE public.audit_dq1_dupe_map ENABLE ROW LEVEL SECURITY; REVOKE INSERT, UPDATE, DELETE, TRUNCATE ON public.audit_dq1_dupe_map FROM anon, authenticated;`. Revert (harden path): `ALTER TABLE public.audit_dq1_dupe_map DISABLE ROW LEVEL SECURITY;` (broad grants are default-privilege; re-grant only if a reason emerges). Verify post-action: both security checks back to 0/0.

---

## Status notes (NOT new — fresh measurements for the morning reconcile)

- **R1 (hydrator FK) — RESOLVED during the day; ledger lags.** Migration `audit_20260604_offers_moment_fk_on_delete_set_null` (06-04 14:47Z) applied the exact Option-A fix from the R1 ledger entry (recreate `offers_moment_id_fkey` ON DELETE SET NULL). Zero `offers_moment_id_fkey` violations in the trailing 6h. Night pass: mark R1 resolved in `ledger.md`.
- **N1 (snapshot-institutional-wallets) — self-recovered; `detect_stalled_pipelines()` back to [].** It was the lone entry at the 00:20Z run (1871m); the daily cron caught up off-peak. No re-fire needed this run. Leave threshold as-is.
- **TS edition dedup + FMV jump = intentional, beneficial.** TS editions 16,355 -> 11,530 (uuid-keyed 2,431 / int-pair 9,097); FMV NO_DATA 4,049 -> 1,782, LOW 9,909 -> 7,000, **HIGH+MED 1,062 -> 1,638**, STALE 667 -> 306. Driven by tonight's DQ1/DQ2 dedup (retiring UUID-keyed dupes onto canonical int-pair editions) compounding with the daytime A1 FMV keystone (`1c5ccf50` — paginate Step-1 sales re-fetch past the PostgREST 1000-row cap, the held-wallet LOW-fossil generator) + Step-6 fossil fix (`bf4cbd5`). Recent-ship health GREEN; the night-pass metrics baseline needs a full re-capture — these are not drift anomalies.
- **NEXTJS-1G ("failed to pipe response", `GET /share/[wallet]/opengraph-image`) — transient from tonight's share-OG fix iteration; self-resolving.** First/last seen ~1h ago, 2 events. The final fix (current HEAD `b3dae3db`) deletes the `opengraph-image.tsx` route entirely and serves the card from `/api/og/share`, so this culprit cannot recur. Confirm 24h quiet, then mark resolved.
- **AF1 (`rpc-tracked-fmv-confidence` artifact / `v_tracked_wallet_fmv_confidence` view times out) — STILL OPEN, already queued at the 00:20Z run.** No fix migration since; the view (created 06-04 14:54Z via `audit_20260604_v_tracked_wallet_fmv_confidence`) is unchanged. Not re-logged — see the 2026-06-05T00:20Z inbox for the full root-cause + ready CREATE-OR-REPLACE optimization + revert. Night-pass DB lane.
- **Sentinel TS-UUID-48h = 234** (night 19 -> 00:20Z 68 -> 234) — climbing but still under the 250 WARN floor; known inert `compute-topshot-pack-ev`/`seed_topshot_editions` seed source (dedup trigger keeps rows inert), plausibly amplified by the new set-257/258 ingest + the dedup churn. WATCH only — if it crosses 250 the sentinel WARNs; not a finding yet.
- **Rewards subsystem (new tonight) — RLS-clean.** 4 `rewards_*` migrations (core tables / mutation functions / owner-audit views / seed rules + shop) + the new `rpc-rewards-console` artifact (03:01Z). New tables do NOT appear in either security check (RLS on, no anon write) — no finding. Artifact is mid-build; not validated this run (defer to next sweep once the rewards build settles).
- **Transient pipeline noise:** the 4 fails above are all single, self-recovering, at the 00:00Z cron rush. No pipeline down; nothing recurring.
- **Artifacts:** the stable artifacts' backing-query classes (FMV breakdown, security catalog, pipelines, detect_stalled, editions) all returned healthy in this sweep; AF1 remains broken (queued); `rpc-rewards-console` new/in-progress (deferred). No artifact repaired (read-only monitor).
