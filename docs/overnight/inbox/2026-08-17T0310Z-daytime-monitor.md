# Daytime monitor — 2026-08-17 03:10Z (20:10 PT Aug 16)

Read-only sweep. Security 4/4 clean; DB 13,102 MB; `db_active` 8 (not saturated at read). Trust board 3 breached, all known-class. No ERROR deploys, no new/spiking Sentry, main dashboard artifact query schema-valid. One genuinely-new candidate below; everything else was already filed today.

## NEW candidate (medium) — `rpc-candy-wmc-ghost-purge` crossed clean → failing, on a now-PUBLIC collection's data-integrity self-heal

- **Signal:** `check_pgcron_recent_failures()` shows it `failed`. History (`cron.job_run_details`, schedule `10 9 * * *`): **succeeded every day 2026-07-24 → 08-14 (21 consecutive), then FAILED 08-15 AND 08-16** — both `canceling statement due to statement timeout` in the 09:10 UTC morning-saturation window.
- **Why it matters:** this is the daily self-heal that dedups ghost-owner rows in candy `wallet_moments_cache` (the DAS group-walk never deletes the prior owner's row on transfer — CLAUDE.md, jobid 201). `candy_mlb` has been **PUBLIC since 2026-07-31** (`CANDY_MLB_PUBLIC=true`), so accrued ghost rows are now user-facing (holder counts on `/insights/candy-mlb`). Two missed days on a small collection (125 editions / 25,375 serials, low transfer volume) is bounded and self-heals the next time the purge succeeds — so **low blast radius, but a clean→failing crossover worth watching**, not a loud alarm.
- **Root cause = the standing saturation story** (same class as the 08-16 filings): its dedup CTE (`row_number() OVER (PARTITION BY moment_id …)` over candy wmc) now exceeds the 120s global `statement_timeout` at the 09:xx cron rush. It is NOT on `pipeline_cadence_watchlist` (pg_cron-internal, invisible to `detect_stalled_pipelines()`), so nothing pages on it beyond `check_pgcron_recent_failures()`.
- **Suggested action (night pass):** (a) verify candy ghost-owner accrual has not skewed live holder counts (`candy_holder_board` / treasury-excluded collector count) — if clean, this stays a watch; (b) if a fix is wanted, the lever is the WORK, not the clock — the same treatment as the trust-precompute leg split (isolate/bound the dedup) or move the tick off the 09:xx rush, NOT raising the function's declared `statement_timeout` (the 195-inert-timeout class: a function-level SET is inert; binding budget is the caller role / global 120s).

## Related observation (low) — the two WEEKLY serial-FMV pg_cron jobs also failed their single Sunday tick

- `rpc-serial-fmv-multipliers-weekly` (Sun 11:00Z) and `rpc-serial-fmv-jersey-weekly` (Sun 11:35Z) both `failed` with `statement timeout` on 2026-08-16. Unlike a daily/hourly job, **a failed WEEKLY tick does not self-heal until next Sunday**, so `serial_fmv_multipliers` / jersey multipliers will be ~14d stale if 08-23 also fails. focus.md's STEER ("staleness ≤7d is by design, don't re-queue as escalating cron-silent") covers a normally-refreshing week — it does NOT cover a *failed* refresh. Same saturation root cause; noting so a two-week-stale multiplier surface isn't a surprise. `rpc-thin-sale-ask-disclosure-refresh` also failed (09:25Z) — that is the canonical 900s-declared / 120s-actual inert-timeout function, already documented.

## Verified healthy / already-filed (no action)
- **Trust breaches (3):** `panini_sale_price_capture_dry_days`=19 (crying-wolf on dead field, filed 08-16T2355Z), `public_board_slow_count`=5 (Candy board slowness, down from 9), `unmapped_resolution_backlog_max`=291 (AllDay permanent-class floor). No NEW breach.
- **Recovered unaided:** `fmv_sweep_wedge_hours` 9.39→**1.97** (the prior lock's "undiagnosed monotone climb, top candidate next" has cleared); `fmv_sweep_stall_pct_24h`=34.3 (ok); `trust_precompute_max_age_hours`=5.95 (8-way leg split holding at predicted steady state).
- **Pipeline failure_rate alerts** (allday-buyer-backfill, allday-unmapped-resolver-tail, populate-pinnacle-wmc-fmv, reconcile-saved-wallet-stats [soft-deadline by design], topshot-active-listings-ingest [egress_blocked / atlas-proxy inert], topshot-fmv-populate, wallet-username-resolver, fmv-recalc, refresh_wmc_fmv_*): all saturation-class, all covered by the 08-16/08-17 inbox filings.
- **Stalled:** candy-editions-ingest (filed 08-17T0030Z, unbounded runtime), candy-listings-indexer (heartbeat writes but terminal doesn't — monitoring defect, documented).
- **Sentry:** 2 issues (NEXTJS-2F/2G), both `smoke check could not run` couldNotRun honest-degradation, 1 event each ~6h ago, not spiking.
- **Deploys:** very active CC session; latest tip READY, no ERROR states (CANCELED = docs commits superseded by rapid pushes).
