# Daytime monitor candidates — 2026-08-06T01:45Z (~18:45 PDT Aug 5, evening tick)

Not the first tick of the day (skipped 1a). Overall: green-with-known-noise PLUS **one new, unhandled pg_cron failure** (rollup, below) and the DB-side collateral of a scraper→pooler-exhaustion incident that CC/Trevor are ALREADY actively hardening (~00:30Z 08-06). Security fully clean (all 4 `rpc_ops_snapshot` invariant arrays `[]`). `detect_stalled_pipelines()` = only the known info-level `candy-offers-indexer`. Vercel latest is READY (`c9aa9d6e`), no ERROR-state deploy. Artifact estate intact (11 active; critical backing views — `rpc_ops_snapshot`, `v_rpc_trust_health`, board-liveness — all read cleanly, so no schema-break signal). DB 12,195 MB.

The 5 `trust_health_breaches` this tick: 3 are documented do-not-re-flag (`ufc_fmv_stale_hours` red-by-design, `panini_sale_price_capture_dry_days`=8 upstream outage, `unmapped_resolution_backlog_max`=127 draining), and 2 are the `public_board_*`=999 pair (LOW candidate below — budget-exhaustion, not dark boards).

---

## HIGH — `rpc-pipeline-runs-daily-rollup` pg_cron job failing: `cannot call jsonb_object_keys on an array` → indefinite-history gap forming

- **Source:** `check_pgcron_recent_failures()` — jobname `rpc-pipeline-runs-daily-rollup` (jobid 233, `11 */6 * * *`), `latest_status=failed`, last_run 2026-08-06 00:11Z. `cron.job_run_details`: **2 consecutive fails** (18:11Z + 00:11Z); the 12:11Z/06:11Z/00:11Z-08-05 runs succeeded, so the break started between 12:11Z and 18:11Z on 08-05. Error: `ERROR: cannot call jsonb_object_keys on an array / CONTEXT: SQL statement "INSERT INTO public.pipeline_runs_daily ..."`.
- **Immediate cause (identified):** exactly ONE `pipeline_runs` row has an array-shaped `extra` — `pipeline='diag-step6-cohort'`, written 2026-08-05 16:40:19Z (the CC Step-6-cohort diagnostic one-shot; ledger 08-05 "cron_heavy one-shot ... 379s"). `rollup_pipeline_runs()` computes `extra_key_counts` via `jsonb_object_keys(extra)`, which hard-errors on a JSON array, aborting the whole INSERT for EVERY pipeline that day.
- **Blast radius:** `pipeline_runs_daily` is the ONLY indefinite pipeline history (CLAUDE.md: `pipeline_runs` retains ~73h; the rollup "cannot be backfilled earlier"). Measured live: `pipeline_runs_daily` has **0 rows for 08-06** and latest_day=08-05 — a permanent hole is opening in the exact archive multiple sessions rely on for >73h defect archaeology.
- **Risk read:** LOW-risk fix, HIGH-value target. Not user-facing; it's an observability-durability defect. Will *partly* self-heal when the `diag-step6-cohort` row ages out of the 73h window (~08-08 17:40Z) — but the defect RECURS on any future non-object `extra` (a single diagnostic row took the whole rollup down), and the 08-06+ rows lost until then are unrecoverable.
- **Suggested action (night pass / Trevor):** harden `rollup_pipeline_runs()` to be shape-defensive — only feed `jsonb_object_keys` where `jsonb_typeof(extra)='object'` (treat array/scalar `extra` as no-keys), so one malformed row can never abort the rollup. Optionally also delete the stray `diag-step6-cohort` row to restore the next tick immediately. Additive robustness on a monitoring/rollup DB function — not on any off-limits surface (no FMV/ingest/pricing/auth/wallet). Verify post-fix: 06:11Z tick `succeeded` and `pipeline_runs_daily` gains 08-06 rows.

---

## LOW — `public_board_empty_count` / `public_board_slow_count` = 999/999: the board-liveness probe is exhausting its own 180s budget under contention, blinding BOTH arms

- **Source:** `rpc_ops_snapshot()` trust_health — both arms 999 (breach_at 1). Traced: the arms read `rpc_trust_health_precompute`; the row is FRESH (written 00:58Z with every other metric) but its stored VALUE is literally 999. `rpc_trust_health_precompute_refresh()` Leg 8 sets both to 999 when `public_board_liveness_probe()` returns `budget_exhausted=true`. The probe's `p_budget_ms` default is 180000; the precompute stored `duration_ms=182289` for these arms, and the whole refresh took **265s** (`cron.job_run_details`, 00:58Z, `succeeded`) — so leg 8 alone crossed 180s and bailed. The underlying `public_board_liveness_state` is healthy (47 boards, **0 errored**), i.e. no board is actually dark/slow — the MONITOR timed out, not the boards.
- **Why it matters (durable):** this is the candy_holder_board detector class (a public board rendering blank/slow while data is fine). When it 999s, BOTH arms go fully blind, so a genuinely dark board this tick would be indistinguishable from this budget timeout. The known class (ledger 08-02/08-03, "do NOT re-budget autonomously") was `public_board_slow_count`=N (small real count); THIS is the worse 999/999 mode where the probe can't finish. Actionable sub-point not previously logged: **leg-8's 180s probe budget is tighter than the refresh's own 600s statement_timeout**, so a slow-but-successful refresh (contention, not failure) still blinds the arms.
- **Context — collateral of the ~00:30Z scraper incident:** the 00:58Z refresh ran slow because of the same DB I/O contention (see watch note). The prior refresh (00:58Z 08-05) took 71s and was fine; the 12:58Z-08-05 one died at 600s during yesterday's saturation. So it's intermittent/contention-driven and likely to ease as the scraper incident resolves.
- **Risk read:** none to act on urgently (no board is actually down). Known class + current collateral.
- **Suggested action (night pass / Trevor):** consider raising leg-8's `public_board_liveness_probe(p_budget_ms=>...)` call toward the refresh's own headroom (e.g. 300–420s, still inside the 600s function timeout) so a slow-but-successful refresh reports the real per-board result instead of a blind 999; OR profile which of the 47 views dominate the sweep (perfect-mint ~14.8s, pack-reality ~8.4s warm are the known long poles) and materialize/trim them. Additive trust-view/probe change only. Do NOT hand-write the metric value.

---

## Watch-only (no new candidate — noted for continuity)

- **Scraper → Supabase pooler-exhaustion incident, ~00:30Z 08-06 — ALREADY being handled by CC/Trevor.** Vercel `c9aa9d6e` (00:33Z) caps 3 edition-page read RPCs at `statement_timeout=8s` "incident hardening (scraper-burst pooler-exhaustion, Aug 06 00:30 UTC)"; docs `c265e938` records the playbook (fix = Vercel Firewall per-IP rate limit, operator). The entity-page Sentry cluster below and the LOW board-999 above are both collateral of this. Real fix is edge rate-limiting the scraper (operator) — not a night-pass item. No action from me.
- **Sentry — 6 unresolved for-review, all the entity-page saturation class:** NEXTJS-1Y team / 1Z pack / 24 set (`canceling statement due to statement timeout`), NEXTJS-20/23 player / 22 set (`Timed out acquiring connection from connection pool`). First seen 14–20h ago, last 2–3h ago; correlate with the scraper/pooler incident. CLAUDE.md-documented "pool-acquire timeout = SATURATION symptom, profile warm first" class — not new inflow, being hardened by `c9aa9d6e`.
- **Known do-not-re-flag breaches holding:** `ufc_fmv_stale_hours`=43.1 (red PERMANENTLY by design — Step-6 self-termination, root-caused 08-05, decision owed to Trevor: retire-or-rebase, do NOT raise breach_at); `panini_sale_price_capture_dry_days`=8 (upstream capture outage, mechanism needs live A/B on the runner box — interactive); `unmapped_resolution_backlog_max`=127 (continuously-replenished permanent-class floor, draining; fix is a resolver failure-REASON, not a threshold).
- **Already queued (not re-logging):** `wallet-username-resolver` (40% statement-timeout, profiled + queued 08-05; Option A cron-cadence cut is operator-only), `topshot_flowty_backfill` cursor false-positive, `fmv-recalc-maxDuration`.
- **`rpc-pinnacle-fmv-recalc-backstop`** — 1 statement-timeout in `pinnacle_fmv_recalc_render_all()` at 22:37Z; `pinnacle_fmv_stale_hours`=15.5 (ok, breach_at 30), so Pinnacle FMV stayed fresh via the daily pinnacle-sync. Single failure, not a new finding; retests on its next tick.
- **`candy-offers-indexer`** info-level cron_silent (1,488 min) — known; best-offer signal only, not FMV, and its watchlist rationale-note staleness was already flagged 08-05 (commit `5326403`).
