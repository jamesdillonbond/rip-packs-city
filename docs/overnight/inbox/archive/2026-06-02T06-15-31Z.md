# RPC daytime monitor — inbox candidates

- **Run:** 2026-06-02T06:15Z · read-only daytime sweep (local ~23:15 PDT, edge of the 8am–11pm window).
- **Platform state:** GREEN. `detect_stalled_pipelines()` returns **1** entry — `evm-transfers-ingest` (66m vs 60m), which is a periodic tick-boundary false-positive, NOT a real stall (see P1 below). Security base-table 0/0 · anon-readable SECDEF views exactly 1 (the known S1, unchanged) · 20/20 deploys READY (0 ERROR) · FMV writers fresh (~22m) · Sentry 2 unresolved (1 known-gated + 1 new-low-signal) · 8/8 live-insights backing views resolve with sensible data.
- **Lock:** `docs/overnight/.lock` present but `RELEASED` (embedded start 2026-06-01T13:54:59Z, ~16h ago) → STALE per its own >45-min rule; took over, inbox commit permitted.
- **Baseline:** `metrics-latest.json` @ 2026-06-01T13:54Z (night pass; shipped nothing / NO-PUSH).
- **HEAD / prod:** newest production deploy `dadcc57` (`dpl_2ktaTGCm55TVGYrbXnEHaXjSaN3g`, READY) — unchanged since the 03:15Z sweep, so nothing new to regression-watch.
- **Commit status:** inbox written to disk, **commit/push UNAVAILABLE** — orphaned `.git/index.lock` (0 bytes, ~33h old, from 2026-05-31T21:15Z) blocks `git add`; `rm` → "Operation not permitted" from the sandbox (Q7). This file persists on the mount, so the night pass picks it up from the working tree regardless. Operator: clear it on Windows (`Remove-Item .git\index.lock`) if you want monitor commits to land.

---

## NEW candidate this run

### P1 — [LOW–MED] `evm-transfers-ingest` watchlist threshold (60m) too tight for its actual hourly logged cadence → `detect_stalled_pipelines()` periodic false-positive
- **Source:** `detect_stalled_pipelines()` this run (`evm-transfers-ingest` silent 66m vs `max_silent_minutes=60`); `pipeline_cadence_watchlist` row; `pipeline_runs` cadence.
- **What's actually happening:** the pipeline's *logged* runs are strictly **hourly at :00** (last 14: 16:00 → 05:00 every hour at :00:4x), but its watchlist threshold is **60m** and its note claims `*/30 at 8,38` — a stale/incorrect cadence note. With a 60m threshold on a ~60m cadence, `silent_minutes` crosses 60 near **every hour boundary** before the next top-of-hour run logs (this run caught it at 66m, the 06:00 tick delayed in the 06:00Z cron rush). The single 18:00Z `base_mainnet 429` fail recovered on the very next hourly tick (19:00) → a real 429 skip is only ~2h.
- **Why it's worth fixing (not just evm):** `detect_stalled_pipelines()` is THE deterministic stall check this monitor relies on (built after the 05-31 Q3 silent-stall incident). An hourly cry-wolf on a consumer-less plane trains us to ignore its output — the exact failure mode it exists to prevent. This is the Q9 "don't false-positive during normal cadence" lesson applied to evm.
- **Risk:** low — monitoring-config only (`UPDATE pipeline_cadence_watchlist`), not route logic, not destructive, not off-limits. Additive/reversible. **Same class as Q10 → SHIP-eligible on a true overnight run.**
- **Relationship to Q6/D2:** distinct. Q6's ingest-side 429 backoff already shipped (`8605c43`, getLogs 10k→5k + 4th retry). This is purely the *watchlist threshold*, untouched by that.
- **Suggested action (night pass / operator):** raise to 150m (covers hourly cadence + one 429-skipped tick) and reconcile the cadence note. Ready migration:
  ```sql
  UPDATE pipeline_cadence_watchlist
  SET max_silent_minutes = 150,
      notes = 'Beezie Collectibles ERC-721 transfer indexer on Base. Logged cadence is hourly at :00 (the prior */30 8,38 note was stale); 150m grace covers one 429-skipped tick. No product consumer — monitoring only.'
  WHERE pipeline = 'evm-transfers-ingest';
  ```
  Revert:
  ```sql
  UPDATE pipeline_cadence_watchlist
  SET max_silent_minutes = 60,
      notes = 'Beezie Collectibles ERC-721 transfer indexer on Base. Cron 8,38 every 30min. Catching up from block 41M.'
  WHERE pipeline = 'evm-transfers-ingest';
  ```
  Verify post-apply: `detect_stalled_pipelines()` no longer lists `evm-transfers-ingest` (it runs hourly, always < 150m).

---

## Status updates to already-logged items (NOT re-raised as duplicates)

### S1 — STILL LIVE (anon-readable SECURITY DEFINER view `v_moments_needing_hydration`; logged 2026-06-01T21:14Z inbox)
- Re-verified this sweep: the catalog "anon-readable SECDEF view" check returns **exactly** `v_moments_needing_hydration` (`relkind=v`, `reloptions=NULL` → executes as definer, bypasses base-table RLS; both `anon` AND `authenticated` hold SELECT). Posture unchanged since 21:14Z — still the lone exception, no new drift.
- Already standalone-shippable per the 03:15Z note (the C2 view-optimization shipped without bundling the fix). Migration + revert are in the **2026-06-01T21:14Z inbox (S1)** — not duplicated here. Not re-raised; just confirming still-live for the night pass.

### Sentry NEXTJS-1F — NEW, low-signal, WATCH (not a code finding at 1 event)
- `JAVASCRIPT-NEXTJS-1F` "TypeError: Load failed", culprit `/dashboard`. **1 event / 1 user**, first+last seen ~2h ago (~04:00Z); has not recurred. "Load failed" is WebKit/Safari's phrasing for a failed/aborted `fetch()` — almost always a transient client network drop or mid-navigation abort, not a server regression. No deploy touched `/dashboard` since `dadcc57` (23:18Z, ~5h before first-seen). **Watch for recurrence; promote only if it spikes.** Not logged as a shippable candidate.

### Sentry NEXTJS-15 — known, gated, stale (no change)
- The gated AllDay `listing_resolution_failures_inserted` capture (ledger C1). 2 events, last seen ~14h ago, not spiking. Known/watched.

### Recent-ship watch — clean
- No new ship since `dadcc57` (H1–H6 audit follow-ups). It remains newest prod + READY; no attributable new Sentry issue or pipeline fault. Nothing to regression-watch this run.

---

## Sweep evidence (baseline; not findings)

- `detect_stalled_pipelines()` = `[evm-transfers-ingest 66m/60m]` — tick-boundary false-positive (P1), not a stall.
- `ok=false`/6h — 5 pipelines, **1–2 fails each**, all transient connection-pool / statement-timeout / upstream-timeout clustered at the **00:00Z + 06:00Z cron rushes**, all self-recovered: `pinnacle-nft-resolver` (2; runs every 5min, the 06:05:03 run hit a 189s statement-timeout under rush load, prior + surrounding ticks all `ok=true`), `topshot-fmv-populate` (1, 06:00:44 pool timeout), `wmc-fmv-populate` (1, 06:05:24 pool timeout), `allday-listings-indexer` (1, 00:15Z), `ufc-stub-thumbnail-resolver` (1, 00:12Z upstream timeout). None attributable to a deploy.
- Security: 0 RLS-off public base tables, 0 anon/authenticated write-grants on RLS-off base tables (`relkind r,p`). Anon-readable SECDEF views = 1 (S1; unchanged).
- FMV: TS HIGH+MED **925** (HIGH 259 / MED 666; NO_DATA **4,827** ↓ from 4,851 prior / 5,109 baseline — still improving). AllDay HIGH+MED **263** (HIGH 51 / MED 212; STALE 505; NO_DATA 522). TS fmv-recalc fresh (latest computed_at 05:48:26Z, ~22m — slightly older than usual from the 06:00Z rush `topshot-fmv-populate` hiccup, well within healthy).
- Sentinel TS-UUID-48h **47** (<250 ok; baseline 40 / prior 45 — normal inert accumulation).
- Drift: editions TS **16,330** (+22 vs baseline 16,308 / +3 vs prior) · AllDay 6,191 · Golazos 581 · UFC 446 (flat). `unmapped_sales` 150 unresolved / 271 total / 144 open ≤7d / 11 new-24h (flat vs 147/267 prior). DB **5,951 MB** (+39 vs baseline 5,912 — normal growth).
- Vercel: 20/20 READY, 0 ERROR. Newest `dadcc57` (H1–H6) → `bec07b3d` (docs) → all prior READY.
- Sentry: 2 unresolved — NEXTJS-1F (new, /dashboard, 1 event, WATCH) + NEXTJS-15 (known gated AllDay, 14h-stale). No spike.
- Insights backing views (artifact `rpc-insights-health` signal harvest — validated by direct query; artifact HTML not sandbox-mounted, lives under OneDrive): squeeze **3,099** · offer_ask_spread **5,564** · market_index_daily 548 · deals_vs_fmv 128 · pack_reality_top_ev 91 · pack_reality_dist 6 · pack_reality_stats 1 · rookie_index 61 — all resolve, all sensible, all close to the 03:15Z values (no schema breakage). `rpc-security-drift` / `rpc-pipeline-reliability` queries schema-valid against the current DB.
