# Daytime monitor — 2026-06-18T21:10Z (14:10 PDT, mid-afternoon tick)

Run: rpc-daytime-monitor (READ-ONLY). Platform GREEN. 1 NEW candidate + a key reconciliation: the 18:19Z **AUDIT-FOLLOWUPS-UNPUSHED is RESOLVED** (the BATCH=150 fix landed in `d5f5f40` and deployed) — but the first post-deploy BATCH=150 run shows the fix did NOT deliver the expected headroom.

## NEW candidate

### BUYERBF-150-INSUFFICIENT-HEADROOM — shipped BATCH=150 buyer-backfill fix did NOT clear the 600s ceiling; first post-deploy run logged 598s (2s under cap), not the expected ~435s [MED · CC]
- **Source:** `pipeline_runs` (topshot-buyer-backfill) durations vs the `d5f5f40` deploy time + focus.md STEER-1 watch criterion.
- **Evidence:** `d5f5f40` (BATCH 200→150) went live ~18:11:48Z. The FIRST BATCH=150 run, **18:54:06Z, logged 598,334ms = 598s — 2s under maxDuration=600** (ok=true). The next, 20:34:05Z, logged 476s. The 598s reading is the **single highest in the last 26h** — the 9 prior BATCH=200 runs ranged 506–577s, so a *smaller* batch produced the *slowest* run. This confirms run time is dominated by per-row on-chain decode latency variance on the dense 2023/2024 backward-walk history, NOT batch size. focus.md STEER-1 set the bar at "first post-deploy run should log ~435s … never >600s": it stayed <600s (passes the literal bar) but missed ~435s by ~160s and came within 2s of the invisible-lambda-kill ceiling.
- **Framing:** this is exactly the "re-approach risk if the backlog hits heavier wallets" the CLOSED BUYERBF-CRON-DROP entry named. It is NOT a re-queue of BUYERBF-CRON-DROP and — per that entry + focus.md STEER-1 — is NOT a request to widen the 90m watchlist threshold, and the night pass must NOT touch the CC-owned buyer-backfill route from the mount.
- **Risk read:** MED. No current breakage — runs are ok=true and logging; the 598s run completed under the cap. But it's the invisible >600s-death class (CLAUDE.md's "looks healthy from every external signal") one slow batch away: a 601s run dies before the finally writes `pipeline_runs` and reads as a silent cron gap. The just-shipped lever (batch 200→150) demonstrably did not buy the intended headroom.
- **Suggested action (CC, route-code — NOT night-pass-shippable):** the robust lever is raising `maxDuration` 600→720/800 (Pro hard cap is 800s) in `app/api/admin/backfill-topshot-buyers/route.ts`, since per-row decode latency (not row count) governs run time and a further batch cut (150→100) only marginally helps while slowing the historical drain. Keep BATCH=150. Re-confirm: no buyer-backfill run logs >600s and none stop logging.

## Status reconciliations (correct the carried items — no new work)
- **AUDIT-FOLLOWUPS-UNPUSHED (18:19Z) — RESOLVED / close.** Main advanced `3c3b5e1` → `d5f5f40` → … → `8b3ef38`; live main has `const BATCH = 150` + the "Listings last refreshed Nh ago" caption. The 18:19Z tick raced the push (cloned at `3c3b5e1` before the fix landed, then rebased its inbox commit on top). prod = `dd7e2bf` READY; both code halves are live. Night pass: mark closed.
- **ALERTS-DISPATCH-DEAL-TIMEOUT — RESOLVED / closeable.** Zero-sub early-exit live since 17:38Z + today's §A scale path (`dispatch_due_deal_alerts` 45s→90s, alerts-dispatch maxDuration 60→120, `dd7e2bf`). 0 deal-leg timeouts in the last 6h (the only 6h fail is one transient lock-check-batch 18:08Z lock-timeout).
- **FMV-HIGHMED-DIP-WATCH — recovered / closeable.** TS HIGH+MED 3139 (HIGH 859 / MED 2280) — normal daytime band; ASK_ONLY 2619 stable (not over-claiming); NO_DATA 3510. Benign daily cycle confirmed.
- **SERIAL-FMV-MULT-CRON — BY DESIGN; do not re-flag** (per focus.md STEER + 18:19Z). pg_cron jobs 5+6 active weekly Sun 11:00 UTC; ≤7d staleness expected.

## Health summary
GREEN · security 0/0/0/0 (rls-off [], anon/auth-write-on-rls-off [], check_public_security_invariants null, check_secdef_anon_execute_violations []) · detect_stalled [] · get_pipeline_alerts 1 info (golazos_sales resolving_editions, benign) · sentinel TS-UUID-48h 0 · pipeline 6h 1933 runs / 1 fail (lock-check-batch 18:08Z lock-timeout, transient, >3h old, recovered) · 24h 12 fails (all transient: evm Base-429 + 13:1x–13:2xZ contention cluster + lock-check-batch) · editions flat (TS 15543 / AllDay 6191 / Golazos 581 / UFC 446) · FMV TS HIGH+MED 3139 / NO_DATA 3510 · DB 4823 MB (+9 benign) · cross-collection mats fresh (cohort 158 @04:10Z) · Vercel prod `dd7e2bf` READY, 0 ERROR/20 (CANCELED = superseded) · Sentry 0 new/12h · 14 active artifacts validated — backing objects all return rows: cross_collection_deals_board 791 (TS+Pinnacle+new AllDay leg), allday_edition_floor_ask 3905 (new view, dd7e2bf), serial_premiums 274 / perfect_mint 9 / underpriced_serials 15 / offer_sanity 226 / tracked_fmv 21 (v3 fast) / squeeze 1000+; none broken/repaired.
Daily extras (trust + cross-collection refresh) NOT re-run this tick — mid-afternoon; the 8am-equivalent first tick (07:11Z) already ran them (trust 9/9, ccm fresh).

## Noted, not findings
- Deal board grew 630 (18:19Z) → 791 — the AllDay leg (`dd7e2bf`) filling in via `allday_edition_floor_ask` (3905 rows); expected, healthy.
- evm-transfers-ingest Base-429 — known LOW external rate-limit; per focus.md, don't chase.
- Profile public over-count — already diagnosed + handed to CC (focus.md); NOT re-flagged.
