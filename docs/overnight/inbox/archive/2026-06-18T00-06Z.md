# Daytime monitor — 2026-06-18T00:06Z (17:06 PDT, evening tick)

**PLATFORM GREEN; 1 NEW candidate.** `detect_stalled` cleared (buyer-backfill ran 23:34Z), security 0/0/0/0, trust 9/9, sentinel 0, Sentry 0-new, Vercel 0 ERROR (3 new Trevor ships all READY), 14/14 active artifacts validated. The one flag: `alerts-dispatch` "deal:" leg hit the 30s statement timeout **twice in 6h** (18:44Z + 22:44Z) — and with **0 alert_subscriptions** the cost is the deal-candidate-set scan over the two deal sources, not per-sub fan-out, so it only worsens as subscribers are added. This is the recurrence the 21:19Z tick set as the trigger to investigate `dispatch_due_deal_alerts` cost. Daily extras skipped (8am tick ran them: trust 9/9, ccm PASS @04:10Z).

## Health (every-run sweep)
- `detect_stalled_pipelines()` = **[]** (clear — the BUYERBF flag from the 18:16/21:19Z ticks resolved; buyer-backfill last ran 23:34Z, 32m ago). `get_pipeline_alerts()` = 1 **info** only: `golazos_sales` resolving_editions (1 sale/24h, edition-resolution bridge pending — longstanding benign Golazos gap).
- `pipeline_runs`: 24h **8995 / 4 fails (0.04%)**; 6h **1938 / 2** — both fails = `alerts-dispatch` "deal:" leg 30s timeout (18:44Z + 22:44Z, see NEW candidate). Everything else clean.
- Security **0/0/0/0**: RLS-off base tables `{}`; anon/auth-write-on-RLS-off **0** (relkind r/p); `check_public_security_invariants()` clean (0 rows); `check_secdef_anon_execute_violations()` [].
- Sentinel TS-UUID-keyed-editions-48h = **0**.
- Sentry: **2 unresolved**, both single-event smoke transients (NEXTJS-1E sales-indexer-detect_stalled + NEXTJS-A fmv-pipeline-healthy), first+last seen ~16h ago (06:45–07:35Z wave window); **0 new in 12h**.
- Vercel: prod **`967a1b2` READY** (jersey per-moment match); **0 ERROR** across 20 (CANCELED = superseded docs/monitor builds). DB **4786 MB** (+10 vs 21:19Z, benign).
- Drift vs baseline: editions flat (TS 15543 / AllDay 6191 / Golazos 581 / UFC 446); fmv_sanity_flags **0**; trust health **9/9 ok** (edition_integrity 4/50, pinnacle_fmv 14.0h/30, unmapped_resolution_backlog 9/100, all green).

## Recent-ship health (3 new Trevor ships since the 21:19Z tick — all READY, no regression)
- **`3a804cc` discord-bot global command registration (DM support) — PASS.** New register route + shared command manifest; READY, no Sentry, no pipeline impact.
- **`247651d` "enforce serial deal filters + ship per-edition FMV alerts" (~23:19Z) — PASS, watch.** Changed `topshot_serial_deal_alerts_for_subscription` (enforce team/jersey/never-sold/badges) + reconciled `fmv_alerts.alert_type` CHECK to the dispatcher vocab (FMV alerts were silently un-triggerable before). READY; security clean; the 3 alerts-dispatch runs after the deploy (23:29/23:44/23:59Z) are all sub-second. Does NOT cause the deal-leg timeouts (those predate it at 18:44/22:44Z). Too early to say it changed the deal-query base cost.
- **`967a1b2` "per-moment jersey-match" (current prod, ~23:30Z) — PASS.** Additive `editions.jersey_number` + backfill (8,814 TS editions) + changed `get_edition_special_serials`/serial consumers. Editions count flat (additive), fmv_sanity 0, serial-premiums board healthy (274), no Sentry. No regression.

## Artifacts (14 active validated; 5 RETIRED tombstones skipped)
Backing data resolves for all: serial_premiums **274** / perfect_mint **9** / underpriced deal board **18** (listings churn from 43 — still healthy non-zero) / tracked_fmv **21** (v3 view fast) / cross_collection_cohort_mat **158** fresh @04:10Z / offer_sanity **220** / rewards econ 1 + balances 10 / traction snapshot non-null / squeeze 501+ / rookies 61 / FMV-by-collection resolves. None broken; none repaired (read-only). The jersey ship did not break the serial-premiums board.

## NEW candidate
1. **ALERTS-DISPATCH-DEAL-TIMEOUT — deal-leg statement timeout recurred (LOW-MED · CC — alerts dispatch RPC/query, off-limits to night-pass auto-ship).** Source: `pipeline_runs` `alerts-dispatch` (65/67 ok / 24h; the 2 fails both `deal: canceling statement due to statement timeout`, 30.4s = the 30s cap, at 18:44Z + 22:44Z) + a visible 17–30s cost cluster (22:59Z 21.1s, 23:14Z 16.9s — one timed out at 22:44Z). **Decisive datapoint: `alert_subscriptions` = 0 (0 active), `fmv_alerts` = 0, deliveries 0/24h** — so the deal-leg cost is NOT per-subscriber fan-out; it is the deal-candidate-set computation over the two `alert_kind='deal'` sources (`cross_collection_deals_board` edition-level + `topshot_underpriced_serials_board` per-serial, wired in by `ac47129d`). That means the cost will only WORSEN once subscribers are added (per-sub work layers on top). **Risk read:** LOW user impact today (0 subs → 0 missed alerts; 97% of runs sub-second) but a structural query-cost fragility on a feature Trevor is actively rolling out; the deal leg is already brushing the 30s cap under DB contention. The 21:19Z tick set "if the deal-leg timeouts recur → look at `dispatch_due_deal_alerts` query cost now that it processes two sources" as the trigger — **met** (the 22:44Z fail is NOT buyer-backfill-coincident, unlike the 18:44Z one). **Suggested action:** night pass profile `dispatch_due_deal_alerts` / `build_deal_alerts_for_subscription` over the two deal sources; either (a) short-circuit the deal-set computation when there are 0 due subscriptions, (b) bump the deal-leg statement_timeout 30s→60s (route maxDuration permitting), or (c) optimize the two-source candidate query — before subscriber growth makes it page in earnest. Not auto-shippable (alerts route/RPC logic).

## Status updates (already-queued; re-measured — no new candidate)
- **BUYERBF-DURATION-CREEP — STABLE, detect_stalled now CLEAR.** `topshot-buyer-backfill` last 4 runs 16:24/18:44/21:14/23:34Z, durations 463/566/568/**505s**, all **ok=true**, decode_failed 49–65. The 568s peak (21:14Z) did NOT climb further — latest run 505s. NO run crossed the 600s maxDuration ceiling (the `95c07c5` invisible-death class stays fixed). The detect_stalled 90m flag from the 18:16/21:19Z ticks has cleared (cadence tightened; last run 32m ago). Worst-case headroom still ~32s (568s vs 600s) so the night pass's batch_size 200→100 lever remains in reserve if a future run approaches 600s — but no escalation this tick.
- **SERIAL-FMV-MULT-CRON — drifting further (escalating).** `serial_fmv_multipliers` last computed 2026-06-15 23:06Z = **49.1h stale** (37 cells; 46.2h @21:19Z, 43h @18:16Z). Backs serial-premiums (274) + underpriced deal board (18) via `serial_fmv_estimate`. Slow-moving population-level premiums (low per-day risk) but the drift compounds the deal-board "discount" staleness. Operator/Trevor: wire the `refresh-serial-fmv-multipliers` cron (cron-wiring, not auto-shippable).
- **FMV-HIGHMED-DIP-WATCH — recovered from the low but today's peak undershot; NOT closeable yet.** TS HIGH+MED **3140** now (HIGH 855 ↑ from 769 / MEDIUM 2285 ↑ from 2079). Strong recovery from the 2848 overnight low, but today's intraday peak (~3228 @2pm) came in BELOW yesterday's 3435 daytime peak, and it has softened to 3140 into the evening. ASK_ONLY stable (2624 vs 2600 baseline — not over-claiming); writers fresh; fmv_sanity_flags 0; benign daily-cycle reclassification, not ship-attributable. The 21:19Z close criterion (re-confirm HIGH+MED ≥ ~3400 at the daytime peak) was NOT met today — keep the watch open one more day to confirm the slightly-lower day-over-day peak (3435 → ~3228) is normal sales-recency churn and not a slow ASK_ONLY over-claim.
- **OFFER-SANITY-VIEW-REFINEMENT — slow benign accrual.** v_offer_sanity_flags **220** (213 @21:19Z). ~99% sub/serial offers correctly out of edition-grain scope (the edition-grain signal is covered by trust leg offer_edition_gap 0/50). Already queued.

## Noted, not findings
- **alerts-dispatch deal-leg fast again post-`247651d`** (23:29/23:44/23:59Z all sub-second) — but only 40min/3 runs of evidence, and `247651d` adds filter enforcement (more cost per matched row), so this is NOT yet evidence the deal-query cost is resolved. Folded into the NEW candidate.
- **underpriced deal board 43 → 18 rows** — natural `topshot_active_listings` churn (every-3h home-machine ingest + 6h-staleness deactivation), not a regression; board still healthy/non-zero. Would only be a finding at 0 (ingest dead).
- **golazos_sales info alert** (`get_pipeline_alerts`) — longstanding Golazos edition-resolution-bridge gap, info severity, 1 sale/24h. Benign.
