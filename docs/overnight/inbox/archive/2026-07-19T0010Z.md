# Daytime monitor — 2026-07-19T00:10Z (~17:10 PDT Jul 18)

No clock skew (shell 00:05:59Z ~= DB now() 00:06:15Z ~= newest sale 00:06:05Z). Lock RELEASED. Not first-tick-of-day (previous tick 2026-07-18T21:06Z) -> section 1a daily extras skipped, but trust health came through `rpc_ops_snapshot()` anyway and is clean.

**Health: GREEN.** security 0/0/0/0 · trust **15/15 ok, 0 breaches** · sentinel TS-UUID-48h 0 · pg_cron failures [] · invalid indexes 0 · RLS-off base tables 0 · DB 9,994 MB · Sentry 3 unresolved (all pre-existing classes, none from a post-18:00Z ship) · Vercel prod `965d213b` READY.

Two trust metrics the 07-18 overnight pass queued as breaching have **cleared on their own fixes**: `topshot_impossible_parallel_serials` **27 -> 0** (wave-5 circ floor raise, migration `audit_20260718_circ_floor_raise_impossible_parallel_wave5`) and `pinnacle_fmv_stale_hours` **29.3 -> 1.5** (`audit_20260718_pinnacle_fmv_recalc_pgcron_backstop` firing as designed).

---

## Candidates

### 1. SECURITY-SMOKE-RLS-TRANSIENT-1809Z — MEDIUM (investigate; not a live problem)

**Source:** Sentry `JAVASCRIPT-NEXTJS-1C` — "smoke test failed: public base tables: RLS on + no anon write", 3 events, first==last seen ~2026-07-18T18:09Z, culprit `POST /api/smoke-test`.

**Why it matters:** this is the security invariant smoke, and it fired **three times** — not once. It is currently clean (`rls_off_base_tables` [], `anon_write_holes` [], `secdef_anon_violations` [], `check_public_security_invariants()` [], `pg_tables rowsecurity=false` = 0), so the window closed, but the smoke was correct at the time.

**Correlation (strong, timestamps near-exact):** the Panini DDL wave landed in that exact window —
`audit_20260718_panini_pack_ev_fotl_differentiated_v2` 17:30:05 · `_panini_pack_ev_confirmed_odds_v3` 17:47:13 · `_panini_pack_ev_remaining_basis_v4` 17:54:23 · `_panini_nation_board` **18:04:13** · `_panini_views_security_invoker_normalize_on` **18:08:48**.
So a Panini object was almost certainly created without RLS at ~18:04Z and normalized ~5 min later at 18:08Z, with the hourly smoke landing inside the gap.

**Risk read:** LOW risk to investigate (read-only), MEDIUM value. The exposure window has closed and nothing is wrong now — but this is the invariant that is supposed to be un-trippable, and a repeatable "create table, enable RLS in a *later* migration" pattern means the next wave re-opens the same window. This is exactly the class the repo post-flight checklist exists to prevent.

**Suggested action:** identify which object tripped it (diff the two migrations' created objects), and if confirmed, make the Panini DDL pattern enable RLS + revoke anon in the *same* migration that creates the table. Do **not** resolve the Sentry issue as a false alarm without that check.

---

### 2. NON-WAVE-WALLET-BACKFILL-DRIVER — **already queued 07-18; NEW EVIDENCE ONLY, do not double-queue**

The ledger queued "identify the non-wave wallet-backfill driver (hours 5/10/15/16/21, ~2,253 runs/day)" with `lib/allow-list/prewarm.ts` as the lead. Two measurements from this tick materially strengthen it:

**(a) It is now the single largest hour, and it survived the gate.** Hourly `wallet-backfill*` runs over the last 30h: 21:00Z **585** · 10:00Z 590 · 05:00Z 572 · 01:00Z 493 · 15:00Z 430 · 07:00Z 395 · 19:00Z 374 · 13:00Z 325 · 00:00Z 325 · 18:00Z 207 · 06:00Z 193 · 12:00Z 158. The 21:00Z burst fired **after** the 12h gate deployed (~20:50Z) and is the biggest hour in the window — the gate cannot touch it, confirming it is not seed-wallet-refresh.

**(b) It correlates 1:1 with the contention/timeout family.** Failures land only in hours that have wallet-backfill fan-out, and scale with it: 05:00Z 572 runs/5 fails · 15:00Z 430/4 · 10:00Z 590/3 · 21:00Z 585/2 · 19:00Z 374/2 · 13:00Z 325/2 — and **every zero-run hour has zero failures**. The 15:37-15:39Z pool-exhaustion cluster (`fmv-recalc`, `allday-unmapped-resolver`, `compute-topshot-pack-ev` all "Timed out acquiring connection from connection pool") sits inside a non-wave burst hour, as does the user-facing pack-detail 500 (Sentry `JAVASCRIPT-NEXTJS-1Z`, `GET /[collection]/pack/dist/[distId]`, last seen ~16:09Z).

**Read:** the non-wave driver is not just the larger remaining *cost* — it appears to be the proximate cause of the recurring statement-timeout/pool-exhaustion class, including the one user-visible 500. That reframes it from a cost item to a reliability item. Prewarm remains the lead (`force=true` => full-cost walks, ~150-wallet bursts match).

---

### 3. PACK-EV / FMV-RECALC CONTENTION FAILS UP — LOW (watch, no action)

`compute-topshot-pack-ev` **8 fails/24h** (prior 24h: 3), `fmv-recalc` **5** (prior 24h: 3). All contention-class — `targets: canceling statement due to statement timeout`, `edition_page_fetch: upstream request timeout`, pool exhaustion. Both pipelines' latest runs are **ok** (00:07Z / 00:06Z) and there is **no data harm**: `pack_ev_board_max_stale_days` 0.73 (breach 2), `topshot_fmv_stale_hours` 0, `allday_fmv_stale_hours` 0.

Folds into DAYTIME-CONTENTION and tracks candidate 2 — worth watching as the *metric* for whether fixing the non-wave driver actually buys reliability, not worth its own fix.

---

### 4. PINNACLE-SYNC CATALOG HALF — CARRIED (operator), no change

`pinnacle-sync` silent **2,279 min (38h)**, last run 2026-07-17T10:07Z; it missed the 07-18 10:07Z tick (cron-job.org dropout class). The **FMV half is now backstopped** and healthy (`pinnacle_fmv_stale_hours` 1.5, `pinnacle_render_floor_stale_hours` 4.3) — the pg_cron backstop shipped 07-18 is doing its job. The **catalog half remains unbackstopped**. Already flagged in the 21:06Z inbox and the ledger; **watch the 07-19 10:07Z tick** — if it misses again that is two consecutive days and the dropout is not self-healing.

---

## Post-ship watch — 07-18 evening wave: ALL PASS, 0 reverts

| Ship | Verdict |
|---|---|
| `09821b48` + `audit_20260718_funnel_events_collection_view` | **PASS, confirmed recording** — `collection_view` 18 events/12h, newest 23:49Z. The Phase-1 blind spot is genuinely closed. |
| `audit_20260718_moments_needing_hydration_unfence_cte` + `idx_ma_hydration_queue_desc` | **PASS** — view returns 172,136 rows, index present, **0 invalid indexes DB-wide**. |
| `audit_20260718_pack_history_sold_any_status` | **PASS** — `get_wallet_pack_history` present, grants intact, security invariants []. |
| `audit_20260718_sales_counterparty_*` + worker `fe56ccf4`/`965d213b` | **PASS and working** — `sales_counterparty_recovered` **566 rows** and climbing. The telemetry fix (`965d213b`, 11-arg `log_pipeline_run`) is the current prod deploy. |
| `88c1ca15` edition-Activity resilience | **PASS so far** — Sentry `JAVASCRIPT-NEXTJS-1X` ("edition page has Activity section") last seen ~16:09Z, deploy ~19:41Z => **no recurrence in ~4.4h**. Early; keep on watch. |
| `66414958` `?force=1` backstop bypass | **NOT YET TESTABLE** — next GHA backstop hour is 02:00Z 07-19. |
| `4c631412` 12h gate | **NOT YET MEASURABLE, correctly** — the deploy (~20:50Z) postdates every gate-skip hour in the window; first true skip hours are 06:00/07:00Z 07-19. Hours 06/07 and 18/19 showing full fan-out today is **pre-deploy**, not gate failure. Measure after a full post-gate day per the ledger. |
| `audit_20260718_circ_floor_raise_..._wave5` | **PASS, closed** — `topshot_impossible_parallel_serials` 27 -> **0**. |
| `audit_20260718_pinnacle_fmv_recalc_pgcron_backstop` | **PASS, closed** — `pinnacle_fmv_stale_hours` 29.3 -> **1.5** despite pinnacle-sync still being down. |

**Non-finding, noted for completeness:** the tip commit `07bc2c8e` (ledger, docs-only) has a **CANCELED** Vercel deploy. Prod serves `965d213b`, which is the newest *code* commit — so no code is missing from production and this is not the docs-tip trap biting. Flagging only because CLAUDE.md calls that trap out twice.

---

## Artifact validation — coverage note (read this, it is partial)

`list_artifacts` enumerated **17** artifacts. The artifact directory (`C:\Users\TDill\Claude\Artifacts`) is **outside this session's connected folders for Grep**, and reading all 17 HTML payloads file-by-file was not proportionate for a 3h read-only tick — so I did **not** extract and replay every embedded payload query this run.

What I validated instead, which covers the failure mode that section exists to catch (a same-day schema change silently breaking a dashboard): every DB object touched by today's DDL wave, plus the shared surfaces the dashboards read. All green —
`v_moments_needing_hydration` 172,136 · `funnel_events` 892 · all 5 new/changed fns present (`get_wallet_pack_history`, `get_allday_market_editions`, `get_insights_hub_stats`, `claim_sales_counterparty_batch`, `apply_sales_counterparty`) · 7 panini views · `pack_ev_latest` 1,824 · `v_rpc_trust_health` 0 non-ok · `cross_collection_cohort_mat` 154 rows fresh 04:10Z (<26h) · traction surfaces readable (`support_conversations` 480/7d — still smoke-dominated, `outbound_clicks` 5/7d, 30 distinct sessions/7d, `email_subscribers` 0).

**No artifact is flagged broken.** But treat artifact validation as *inferred* this tick, not directly executed. If a night pass wants a true per-artifact replay, the artifact dir needs to be a connected folder.
