# Daytime monitor — 2026-06-07T13:58Z (06:58 PDT)

Read-only sweep. Platform healthy and notably CLEANER than the last 3 runs: `detect_stalled_pipelines()` = `[]` (first empty result in days — ALLDAY-FMV-STALL, TFP-RUSH, and N1 all recovered), security 0/0 (RLS-off + anon-write both empty), 20/20 recent prod deploys READY (head `87b9d57`, docs-only), sales fresh (TS 3,878 / AllDay 296 in 24h, latest 13:40Z), P1-CAD fix still holding (pinnacle-metadata-backfill ok through 13:22Z, 0 fails/12h). Pipeline fails last 6h all known transient classes at the :00/:06/:22 rush (pinnacle-nft-resolver 2, hydrator/N2 1, pack-ev targets-read 1, listings-reconcile 1, ufc-stub 1, wmc 1, allday-pack-ev 1 — all single-shot with recovery). Artifacts: key fragile views validated OK — `pinnacle_scarcity_board` 501-cap (wave-1b re-key alive), `topshot_squeeze_board` 501-cap, `v_tracked_wallet_fmv_confidence` completes (AF1-v2 holding), `topshot_first_mint_trophies`, `v_rewards_economy`, `edition_offers` (6,373), Trevor wmc (18,452), outbound_clicks query OK (0 rows/7d — plausibly real at current traffic, not an error).

## Candidates

### 1. NIGHTPASS-MISS (NEW · MED · operator) — nightly autonomous pass did NOT fire in the 2026-06-07 window
- Source: `docs/overnight/` state — `.lock` last touched 06-06 08:41Z (released marker), `metrics-latest.json` still captured_at 06-06 08:30Z, no `docs/handoff-2026-06-07-overnight-pass.md`, and 6 inbox files (06-06 15:15Z → 06-07 04:46Z) sit undrained/unarchived. The pass's window (00:00–06:00 PDT = 07:00–13:00Z) has fully elapsed.
- Risk read: no platform impact (today's sweep is green), but the autonomous loop is degraded — queued items (DUPE1-MIT, CROSS1, SMOKE follow-ups, STALE-LOCK hygiene) aren't being drained, and metrics-latest.json is ~30h stale, weakening drift detection.
- Suggested action: operator check the `rpc-nightly-autonomous-pass` scheduled task in Cowork (Scheduled) — did it error, get paused, or miss its trigger? Also note the stale `.lock` cleanup is still pending from the 04:46Z run's STALE-LOCK item.

### 2. PACKEV-THROUGHPUT (NEW · MED · CC/night pass) — v21 confirmed UNWEDGED, but steady-state throughput can't meet the 24h freshness bar
- Source: `pipeline_runs` compute-topshot-pack-ev post-v21-deploy (f39761a, live ~06:13Z): every completed tick since writes EV (`ev_rows_written` 3–4, batch_size 4, elapsed 27–41s — well inside budget; zero `time_budget_exceeded_after_fetch` in the sampled 8 ticks; `function_version: 21`). The lone 12:08Z fail was a pre-fetch `targets:` pool timeout (transient rush class), not the wedge.
- BUT: `topshot_pack_ev_targets` stale-24h kept climbing 611 (04:42Z) → **665/800** (13:55Z); stale-48h 266 → **330**. Math: 4 packs/30min ≈ 192/day < 800 targets needing daily EV → staleness grows structurally even with the wedge gone; the oldest packs DO rotate now (sentinel advance works), it's pure throughput.
- Suggested action: once ~24h of v21 stability is confirmed, raise batch_size (4 → 8–12) and/or tighten cadence — the per-pack 20s timeout now bounds worst-case per-pack cost, so a bigger batch no longer risks the old head-of-line wedge. Edge-function config change; low-risk but touches the pack-EV pipeline → CC or gated night-pass item.

### 3. SMOKE-MARKET-EMPTY (NEW · LOW-MED · night pass/CC) — market-API smoke assertion failing intermittently; not covered by SMOKE-RETRY by design
- Source: Sentry NEXTJS-4 "smoke test failed: market API returns Top Shot listings" — 3 events spanning ~18:55Z 06-06 → ~06:55Z 06-07 (recurring-intermittent, post-SMOKE-RETRY which only retries transient throws/5xx, not assertion failures like an empty listings array). Sibling one-shot cluster same window: NEXTJS-1E (sales indexers, 2ev), NEXTJS-A (fmv health, 2ev), NEXTJS-W/-E (overview 200s, 1ev each). All quiet ~7h now; platform checks they assert are verified green live this sweep.
- Risk read: most likely the TS GQL proxy intermittently returning empty at rush windows (the documented `tsCount: 0` reliable-failure signal) surfacing as a green-but-empty `/api/market` response — i.e., a real intermittent data gap, not smoke flakiness.
- Suggested action: investigate whether `/api/market` can distinguish "proxy returned empty" from "no listings" (e.g., treat tsCount 0 as 5xx/transient so SMOKE-RETRY covers it, or add a one-shot in-route retry on empty GQL). Until then: if all 6 issues stay quiet 24h (~06:55Z 06-08), mark resolved in Sentry.

## Reconciles (for the next pass's ledger sweep)

- **ALLDAY-FMV-STALL RESOLVED** — `allday-fmv-populate` resumed ~07:4xZ; 26 ok / 0 fails in 12h, last ok 13:42Z. Cron re-fired (9d35a48 Bearer fix live).
- **TFP-RUSH RESOLVED** — `topshot-fmv-populate` logged its first successes since 06-05 18:00Z (2 ok/12h, last 12:50Z, off-rush timing). CRON-30S fire-and-forget + slot move worked; the MON-WATCH watchlist gate (first ok=true) is now met if the night pass wants to watchlist it.
- **N1 RECOVERED + slot moved** — `snapshot-institutional-wallets` ran ok 07:25Z (off the 06:00Z rush peak, per the standing recommendation). Consider closing N1 if the next 2 daily slots hold.
- **PACKEV-BUDGET-2 SHIPPED as v21** (f39761a) — see candidate 2 for verification; the queued CC fix can be marked shipped.

## Drift vs metrics-latest.json (baseline 06-06 08:30Z — now ~30h stale, see candidate 1)

- TS FMV HIGH+MED 3041 → **3008** (HIGH 564 / MED 2444; flat-churn). AllDay 490 → **493**. TS NO_DATA **5444** (exactly flat — DUPE1 tail stable).
- Sentinel TS-UUID-48h: 5840 → **2644** — still CRITICAL (≥2000) but halved; at the current re-mint rate (~+18 edition rows/hr, TS editions 14,999 → 15,541) it should drop below the 2000 floor within ~a day. DUPE1/DUPE1-MIT unchanged, no new action.
- unmapped_sales open: 162 → **183** (flat vs 04:46Z's 183).
- DB size: 6257 → **6397 MB** (+140 MB/29h, modest).

## Recent-ship health (the ~06:1xZ overnight train + evening waves — all green)

- All 20 recent prod deploys READY, zero ERROR. Ships verified live this sweep: pack-ev v21 (writes advancing — candidate 2), cron-auth Bearer (9d35a48 — allday-fmv-populate green post-resume), CRON-30S fire-and-forget (36eee2f — topshot-fmv-populate's first ok since 06-05), PIN-SYNC-FLOWTY (ae6d78f — pinnacle-sync ok 10:07Z, 0 fails), wave-1b/2 re-keys (scarcity board + moment-page views all validate). No Sentry attributable to any of them (the 06:55Z smoke cluster pre-dates none and matches the rush-transient class — candidate 3).
