# Daytime monitor inbox — 2026-06-08T06:16Z

Off-hours pre-night-pass run (23:16 PDT). Platform healthy; **2 new low-risk candidates**. Declined list empty. The stale RELEASED `.lock` (14:13Z 06-07, ~16h old) is treated as released — commit not skipped.

## 1. LISTCACHE-V2-STALL · [LOW-MED · operator re-fire, then a keep-or-retire decision] `topshot-listing-cache-v2` cron dropped ~7.4h while its primary sibling kept running
**Source:** `detect_stalled_pipelines()` + `get_pipeline_alerts()` both flag it (severity medium). `topshot-listing-cache-v2` last run **2026-06-07 22:48:52Z** — silent ~441 min vs its 360m watchlist threshold (crossed ~04:48Z, just after the 05:05Z audit's detect-stalled snapshot, which is why that audit reported `[]`). **0 fails** → a clean external-cron-trigger dropout (cron-job.org), not a route crash. The PRIMARY `topshot-listing-cache` is HEALTHY (last 05:11Z, 6 runs/12h, 0 fails) and `topshot-sales-indexer` healthy (06:03Z, 41/12h). Same class as Q3 / N1 / P3-BUYERS / the 05-31 listing-cache stall.
**Blast radius:** LOW. The two are ~2s-apart siblings feeding the same `cached_listings → badge_editions.low_ask → fmv-recalc ASK_ONLY` path; the primary kept that feed fresh through the whole window, so FMV ASK honesty is unaffected. The cost is the medium alert + a half-redundant feed being down; if the PRIMARY also drops, listing/ASK freshness degrades.
**Action:** operator re-fire the `topshot-listing-cache-v2` cron-job.org entry (self-recovers when the external trigger fires; the Q10 watchlist row is doing its job by surfacing this). SECONDARY decision (same framing as CRON-30S items 3/4): since the primary alone keeps `cached_listings` fresh and v2 only trails it ~2s with the same rationale, evaluate whether `-v2` is still needed at all — if it's a redundant duplicate, retire its cron entry + watchlist row instead of re-firing. Don't auto-ship either way (external cron + a needed-or-not product call).

## 2. SMOKE-EDITION-TIMEOUT (Sentry NEXTJS-1H) · [LOW · CC/operator watch — single transient so far] the new edition-page "Recent Sales" smoke assertion timed out once on a cold lambda
**Source:** Sentry `JAVASCRIPT-NEXTJS-1H` — "smoke test failed: edition page has Recent Sales", **1 event** 2026-06-08 05:03:20Z, release `de01542`, endpoint `/nba-top-shot/edition/124:4493`. Extra `detail: "The operation was aborted due to timeout"`, `expected: html-contains-Recent-Sales`. Lambda boot 05:00:17Z / app-start 05:00:34Z / request 05:03:20Z = a cold-start window.
**What:** the page fetch timed out, so the asserted "Recent Sales" string wasn't found — a fetch timeout, NOT a genuinely-missing section (the edition page works; it's the one `81f686fc` just added the Recent Sales + Special Serials boards to today). It's an ASSERTION-class failure, so SMOKE-RETRY (`ff853a2`, retries only the 5xx/408/timeout-throw class) didn't suppress it — same bypass class as SMOKE-MARKET-EMPTY.
**Action (watch):** single occurrence, no recurrence in the ~1h since — most likely a one-off cold-start timeout. IF it recurs: the heavier post-`81f686fc` edition page (Recent Sales via `get_edition_recent_sales` + Special Serials) may be exceeding the smoke fetch budget on cold starts — either bump the smoke check's per-fetch timeout for the edition endpoint, or route the timeout-abort through SMOKE-RETRY. Mark NEXTJS-1H resolved if quiet 24h.

## 3. INFO datapoints (verified, no new item)
- **TS-SET-254 (03-15Z item 2 / Cowork seed) → VERIFIED RESOLVED.** Hydrator `catalog_gap set_id_onchain=254` fired 19× in the 00:08–04:08Z window but **0× in the last 2h** (cleared ~04:08Z, right after `audit_20260608_seed_sets_wnba_skyline_254`). Both editions `254:8622` + `254:8623` now bridge to the seeded sets row `3913af54…`. Verification PASSES — close it.
- **SMOKE-PIN-DRIFT (Sentry NEXTJS-14, 03-15Z item 1) → RESOLVED.** The drift-guard re-key (CC handoff Item 1) worked — NEXTJS-14 is GONE from unresolved (was every-tick red at the 05:05Z audit). Sentry now has only **1** unresolved issue total (= item 2 above).
- **DUPE1 gate:** sentinel TS-UUID-48h **612** (942 → 612 since the 05:05Z audit), **0/hr** new (0 in 6h) — pure roll-off, on pace to clear <250 today. CC-owned merge gate; night pass must NOT pre-empt (focus.md #3). TS editions flat (15,542) = re-mint still stopped.
- **PACKEV ahead of forecast:** pack-EV stale-24h **143** (665 baseline → 248 at 03-15Z → 143), stale-48h **0**, oldest 06-06 09:09Z. Cron-frequency lever sufficient (PACKEV-THROUGHPUT closed).
- **I1 rush:** the only ok=false cluster in 6h was the **00:48–01:05Z** burst (wmc-fmv-populate, pack-ev targets, fmv-populate sets-read, hydrator candidate_read, pinnacle-nft-resolver, allday-listings-retry — all lock/stmt/pool-timeout class); **0 fails in the ~5h since.** Known I1, milder; histogram-verify still due this evening.
- **FMV:** TS HIGH+MED **2,972** (churn-flat vs 3,008 baseline), TS NO_DATA **5,206** (improving from 5,444), AllDay HIGH+MED **497** (flat). Writers fresh (fmv-recalc 2m, allday-fmv-populate 8m, offers-sweep 8m, sales-indexers 7–14m; 0 fails/6h).
- **Vercel:** day's full CC wave deployed READY, tip `29715ed` (Item 7 polish, authored **Trevor** — GIT-IDENT fix has taken on recent commits). 1 ERROR deploy `76b6c2e` (CRON-30S wrap) = superseded transient build blip; code live + verified via descendant `9d77c95` (analytics-smoke logging since 00:13Z). Not a finding.
- **Drift:** DB **6,472 MB** (slow creep, +8 since 03-15Z; watch-only). unmapped_sales open **183** (flat). Security **0/0** base tables (RLS-off + anon/auth write-grant).

## Artifact validation
16/16 healthy (confirmed 16/16 at the 05:05Z Cowork audit, after all of today's Pinnacle-rekey churn deployed; nothing schema-relevant shipped since). Re-ran the highest-risk backing queries this run: 7 insights views all resolve (squeeze/offer_spread/scarcity/market 500+, deals 307, rookies 61, pack_reality 4 — `scarcity` is `pinnacle_catalog`-backed post-Wave-1b rekey and works); `v_tracked_wallet_fmv_confidence` (AF1) returns without the 57014 timeout; `get_rpc_traction_snapshot()` non-null; Trevor's wallet 18,452 moments / 5 collections. None broken, none touched.
