# Daytime monitor — 2026-07-25T06:05Z (late tick, ~23:05 PDT Jul 24)

Health GREEN. security invariants: 1 rls_off_base_table (see below), anon_write_holes [], secdef []. trust 15/15 ok, 0 breaches. stalled_pipelines []. sentinel_ts_uuid_48h 0. DB 10,957 MB. Vercel prod `1c33c291` READY (several CANCELED = superseded rapid interactive pushes, normal; 0 ERROR). Sentry 0 new/24h. 17 artifacts present (estate touched/validated 07-24; no view drop/rename shipped today, so not individually re-run this late tick). editions TS 19,513 / AllDay 6,190 / Golazos 575 / UFC 518 / candy_mlb 125.

## Candidates

### LOW — audit backup table breaks the RLS-on invariant and is anon-readable
- **Source:** `rpc_ops_snapshot().security.invariants` → `rls_off_base_table: audit_20260725_allday_nem_from_sales`; confirmed live `has_table_privilege('anon',…,'SELECT')=true` AND `authenticated=true`, RLS off, 8,025 rows / 608 kB.
- **What it is:** the migration backup table for today's AllDay unmapped-residue recovery (ledger 2026-07-25, "AllDay unmapped residue Phase 2+3" / the free-lane `nft_edition_map`-from-sales recovery). Schema is just `nft_id text, inserted_at timestamptz` — **non-PII** (nft_ids are public on-chain; no usernames/wallets/prices). Its REVERT path in the ledger already references dropping it.
- **Risk read:** LOW. Not a data leak (on-chain nft_ids only), but it does violate the standing "RLS on every public table / 0 rows with rowsecurity=false" security invariant and carries the default anon+authenticated SELECT grant — exactly the route-gating≠data-gating footgun class. Trips the platform's own `rls_off_base_table` invariant every sweep until resolved.
- **Suggested action (night pass / owner):** either (a) once the CC session's revert window has closed, `DROP TABLE public.audit_20260725_allday_nem_from_sales;` (cleanest — it's a one-time backup), or (b) if kept, `ALTER TABLE … ENABLE ROW LEVEL SECURITY;` + `REVOKE SELECT … FROM anon, authenticated;` and verify via `has_table_privilege`. Fresh from today's active interactive session — night pass should confirm it's no longer needed before dropping.

## Noted, NOT re-logged (already documented)
- **pg_cron 4 statement-timeouts** (`rpc-raise-edition-offers-backstop` 05:34Z 1/24; `rpc-remap-misattributed-sales` 00:23Z 1/4; `rpc-backfill-pinnacle-acquisitions` 00:17Z 1/4; `rpc-refresh-topshot-edition-median` 00:10Z 1/4) — all single-in-window, self-recovering, overnight daytime-contention/IOPS class. Already in the 0308Z inbox + ledger (OVERNIGHT-0623Z-CONTENTION-CLUSTER, CORRELATED-PIPELINE-DROPOUT-DETECTOR). Standing guidance: no timeout bump on transient failures.
- **pipeline_fails_24h:** `sales-seller-recovery-dune` 23 + `sales-ingest-dune` 11 = known DUNE-DATAPOINT-CAP-402 (operator/billing); `sales-counterparty-backfill` 14 + `compute-topshot-pack-ev` 7 + the rest = self-recovering contention-class flaps, none stalled.
