# rpc-daytime-monitor inbox — 2026-08-05T06:15Z (~23:15 PDT Aug 4, late tick)

Read-only sweep. Health GREEN overall. Security clean (0 RLS-off, 0 anon-write holes, 0 new Sentry). `detect_stalled_pipelines()` = []. Latest prod deploy READY (bc99369, disconnected-ask disclosure view); no ERROR-state deploys. FMV HIGH+MED = 8,464 (sweep payoff still climbing, was ~6,855 at the 08-04 overnight). Trust-health: only the two KNOWN breaches (`panini_sale_price_capture_dry_days`=7, `unmapped_resolution_backlog_max`=105) — both documented in the 08-04 ledger, not re-logged.

**NOTE:** clone/push unavailable this run (sandbox /sessions disk 90% full, 1 GB free -> git checkout failed midway — same "no space" class as the 08-04 overnight). Inbox written to the MOUNT only; night pass picks it up locally. No git run against the mounted .git.

Two candidates, both LOW and likely-already-resolved — logged as data points, not alarms.

---

## LOW — `rpc-fmv-clamp-disconnected-ask` failed one daily tick (08-04 08:55Z, permission denied); ALREADY REMEDIATED, confirm next tick
- **Source:** `check_pgcron_recent_failures()` -> `rpc-fmv-clamp-disconnected-ask`, last_run 2026-08-04 08:55:00Z, `ERROR: permission denied for function fmv_clamp_disconnected_ask`.
- **What:** Daily job (`55 8 * * *`, runs as role `cron_heavy`, cmd `SELECT public.fmv_clamp_disconnected_ask(NULL,false)`). Succeeded 11 straight days (07-24->08-03), failed ONCE on 08-04, has not ticked since (next: 08-05 08:55Z).
- **Root cause (inferred):** the 08-04 disconnected-ask disclosure-view work (`bc99369`, predicate swap) `CREATE OR REPLACE`'d / touched the clamp function's ACL. A recreate resets ACL to owner+default and drops the explicit `cron_heavy` EXECUTE grant -> the 08:55Z tick (in the gap before the re-grant) failed. **Already fixed:** current ACL is `{postgres=X, service_role=X, cron_heavy=X}` — cron_heavy's grant is back, so the 08-05 08:55Z tick should succeed. Per the focus-note rule this is a STALE pre-fix failure; NOT an active break.
- **Risk read:** none active. The clamp caps disconnected-ask FMVs daily (an accuracy guard under the roadmap's accuracy gate); one missed day is negligible.
- **Suggested action:** post-ship watch only — confirm the 08-05 08:55Z tick logs `succeeded`. If it fails again: `GRANT EXECUTE ON FUNCTION public.fmv_clamp_disconnected_ask(uuid,boolean) TO cron_heavy;`. Durable note for the night pass: **when `CREATE OR REPLACE`-ing a function a pg_cron job calls as `cron_heavy`, re-apply the `cron_heavy` grant in the same migration** — the ACL reset silently drops it (same class as the standard anon/service_role re-grant trap).

## LOW — IOPS/connection-pool saturation window ~05:2x–06:1x UTC (known recurring class); notable 9,400-row wmc loss
- **Source:** `pipeline_runs` ok=false last 6h = 41, clustered; plus `check_pgcron_recent_failures()` "job startup timeout" cluster (5 jobs, 05:39–05:42Z).
- **What:** ~15 HTTP pipelines + 5 pg_cron jobs failed inside a ~40-min window with pool/statement-timeout signatures ("Timed out acquiring connection from connection pool", "canceling statement due to statement timeout", "Could not query the database for the schema cache"). Sharpest symptom: `wallet-backfill-allday` `wmc_upsert_chunk_failures=47 rows_lost=9400` (also wallet-backfill 800, wallet-backfill-pinnacle 230 lost).
- **Self-healing confirmed:** `allday-resolve-pull-editions` succeeded on its very next tick (06:09Z); wmc backfills are idempotent and re-attempted every wave, so the lost rows are re-written, not permanently gone. Benign externals in the same window (not findings): `sync-nba-projections` all_upstreams_failed (off-season upstream), `candy-offers-indexer` <50%-sweep deactivation-skip (safety guard working), `topshot-active-listings-ingest` egress_blocked (single transient).
- **Risk read:** low/transient, but this is the SAME recurring IOPS-contention class already logged in the 08-04T2114Z inbox note (18:10Z peak). This instance is larger (15 pipelines, 9,400 wmc rows) and at a different hour — a data point that these saturation windows recur daily and occasionally cost real (idempotently-recovered) wmc write volume.
- **Suggested action:** none required now; sense-and-track. If the night pass sees the pattern worsening (rows_lost climbing, or a wave failing to re-attempt), the levers are wmc-upsert chunk sizing / backfill concurrency during peak windows. Do NOT chase individual transient rows.
