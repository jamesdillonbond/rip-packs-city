# Daytime monitor — candidate inbox · 2026-06-01T06:18Z

Run: `rpc-daytime-monitor` (Cowork). Repo: shared `rip-packs-city` (no bot clone reachable — `rip-packs-city-bot` absent from the sandbox mount, per ledger Q7). Baseline: night-pass `metrics-latest.json` @ 2026-05-31T08:10Z. Lock: `.lock` present but is a RELEASED marker (embedded start ts 08:02Z, ~22h stale) — treated as STALE per the <45-min rule; inbox commit permitted (lock not created/removed by this run; sandbox can't unlink it — Q7).

**Health this run: ✓ healthy.** `detect_stalled_pipelines()` = `[]`. TS sales-indexer fully recovered/healthy (Q3 stays resolved): 18 runs/6h, 0 fails, last 05:40Z, ingesting (986 TS sales in 2h, `MAX(ingested_at)` 05:40Z / 32m lag). AllDay sales-indexer, fmv-recalc, allday-fmv-populate, topshot-fmv-populate all fresh (<27m) and 0 fails/6h. Security **0/0** (RLS-off public base tables 0; anon/auth write-grants on RLS-off base tables 0). Sentinel TS-UUID-48h **119** (↓ from 190 @ 03:28, ↓ from 1099 baseline; well under the 250 ok-line). FMV improving on both collections vs baseline — TS HIGH+MED **864** (227 HIGH + 637 MED; vs 776 baseline / 852 @ 03:28), NO_DATA **5190** (↓ from 6055 baseline / 5249), ASK_ONLY 732 (intended `65421e26` ask-over-WAP behavior); AllDay HIGH+MED **261** (52+209; vs 243 baseline). Editions/DB normal (TS 16,295; AllDay 6,191; Golazos 581; UFC 446; DB 5,899 MB). Vercel: 20/20 recent prod deploys READY, **zero ERROR** (heavy ship continues — team-hub Phases 1–5, `/insights/market`, badge-sync on-chain-key fixes, FMV `65421e26`, and the new audit-followups batch `a79b778`+`b90b6e7` ~1h ago). Sentry: 7 unresolved, **none in the last 6h** — all map to already-logged items (see Watch). Artifacts: 11 enumerated (incl. the new `rpc-audit-followups`), all backing objects validated this sweep — 6 insights views resolve under live names, `get_rpc_traction_snapshot()` executes, `sentinel_fmv_confidence_rows` both collections, security/pipeline/Vercel/DB queries all ran clean, `edition_offers` + `offers-sweep` (new ship) live and healthy. No broken artifact.

All `ok=false` `pipeline_runs` in the window are the 06:00Z cron-rush transients with self-recovery: `evm-transfers-ingest` (2, statement-timeout on upsert — known **Q6**), `snapshot-institutional-wallets` (1, pool-acquire timeout — known **Q9** non-finding), `hybrid_custody_events` (1, single "Signal timed out"). No new pipeline logic faults.

---

## P1 (LOW–MED · auto-shippable by night pass — non-destructive monitoring-config DML, not route logic / not destructive SQL / not off-limits) · `topshot-listing-cache` + `-v2` are absent from `pipeline_cadence_watchlist`, so `detect_stalled_pipelines()` is structurally blind to a listing-cache stall — the exact other half of the 05-31 Q3 chain that an `ok=false`-only scan misses

- **Source:** `pipeline_cadence_watchlist` (full active list pulled this run = 39 pipelines; **neither** `topshot-listing-cache` nor `topshot-listing-cache-v2` is present) + `pipeline_runs` 48h cadence for `topshot-listing-cache`.
- **Detail:** `topshot-sales-indexer` IS watchlisted (`max_silent_minutes=180`), but its sibling `topshot-listing-cache`/`-v2` — which stalled *together with it* during Q3 on 05-31 (01:35→09:28Z) — are not. So the only reason the Q3 listing-cache half was caught was the night pass manually eyeballing absence-of-runs; the deterministic `detect_stalled_pipelines()` gate can never see it. These pipelines feed `cached_listings` → `badge_editions.low_ask` → fmv-recalc's ASK_ONLY path; since `65421e26` (prefer fresh TS ask over stale WAP, shipped today) ASK freshness is more load-bearing for FMV honesty than before (TS ASK_ONLY now 732), so a silent listing-cache stall now degrades FMV quality, not just listing freshness.
- **Current state (NOT itself an alert):** listing-cache last ran 02:00Z (~4.1h silent at sweep time) vs its irregular daytime cadence ~60–206 min. This is high-end but **within its own historical envelope** — it logged a 3.0h gap (05-30 19:46) and a 3.4h gap (05-31 14:47) that both recovered cleanly, plus the genuine 7.9h Q3 stall. So it's plausibly normal overnight slack, just invisible to the stall check. Worth a glance next run: if it crosses ~5–6h without a run, it may be the Q3-class external-trigger (cron-job.org) flake recurring.
- **Risk read:** LOW. The fix is a single non-destructive `INSERT` into `pipeline_cadence_watchlist` (monitoring config). The only real failure mode is a too-tight threshold false-positiving during normal overnight gaps — the Q9 / `snapshot-institutional-wallets` lesson — so the threshold must be generous.
- **Suggested action:** INSERT `topshot-listing-cache` (and optionally `-v2`, which trails it by ~2s) into `pipeline_cadence_watchlist` with `max_silent_minutes` ≈ **300–360** (5–6h) + `is_active=true`, chosen ABOVE its observed non-stall envelope (max routine gap ≈206 min daytime; the 7.9h was the real Q3 stall) so it pages only on a genuine multi-hour stall, not routine overnight slack. Verify post-insert: `SELECT detect_stalled_pipelines();` should still return `[]` immediately (listing-cache currently 4.1h < 5h). Night pass owns the exact threshold; flag operator if a tighter bound is wanted. (Pairs naturally with ledger **Q5**'s "compute lag from last successful run" smoke fix — same "catch a real multi-hour stall without crying wolf" goal.)

---

## Watch (folded into sweep — already tracked, NOT new candidates)

- **Smoke cron-rush false-REDs (`NEXTJS-1E`,`-1D`,`-14`,`-12`,`-A`,`-4`)** — 6 `POST /api/smoke-test` issues, 1 event each, all last-seen ~6h ago (~00:08Z midnight cron rush); **zero re-fire in the last 6h** (the 06:00Z rush today caused 3 pipeline `ok=false` but tripped NO new smoke events). Underlying state independently verified clean this run (security 0/0, FMV fresh+improving, `detect_stalled_pipelines()` `[]`, market/pricing fine). Already logged as **03:28 inbox P3** + **ledger Q5** ("harden smoke/sentinel against cron-rush transients"). Not re-logged.
- **`NEXTJS-15` (`allday-listings-indexer`, ledger C1)** — 8 events, last seen ~7h ago (~23:08Z), no re-fire in ~7h → going quiet after the `221ab64` rate/reason gate. Not yet 24h-clean, so don't mark the Sentry issue resolved yet; keep watching per C1.
- **Rookies view name** — `to_regclass('topshot_rookies_board')` = NULL; live view is `topshot_2025_rookie_index` (resolves). Confirms the known ledger-vs-schema naming nit (ledger Resolved-section migration name vs live view); `rpc-insights-health` references the correct live name and is healthy. Already logged (00:15 + 03:28 inboxes). Pure operator/CC ledger reconcile. Not re-logged.
- **New audit-followups ship (`a79b778` + ledger-doc `b90b6e7`, ~1h ago)** — VERIFIED HEALTHY, no regression: deploy READY, 0 new Sentry in 6h, `edition_offers` table live, `offers-sweep` pipeline already firing (4 runs, last 06:00:40Z, `ok=true`), security still 0/0 post-ship. The `b90b6e7` "operator TODO offers-sweep cron" is effectively already done (the cron is running). No action.
- **06:00Z cron-rush pipeline transients** — evm (Q6), snapshot-institutional (Q9), hybrid_custody (single) — all 1–2 events, self-heal next off-peak tick. Known.
- **Sentinel TS-UUID-48h 119** — under the 250 ok-line, ↓ hard from 1099 baseline. Trending right, not a finding.
- **`.lock` released-marker** still un-unlinkable from the sandbox (Q7 infra). Known; the high-value DB/artifact/health work is mount-independent. Not re-logged.
