# Daytime monitor candidates — 2026-07-07T06:06Z (~23:06 PDT, late tick)

Health GREEN. Security 0/0/0/0 (invariants/secdef-anon/rls-off/anon-write all []); trust 16/16 ok, breaches []; detect_stalled_pipelines() []; check_pgcron_recent_failures() []; sentinel TS-UUID-48h 0; DB 8,360 MB. Vercel: prod dpl_CoZR (64d31f2) READY; latest dpl_owuv (Golazos pack-EV f5857d8, Trevor) BUILDING in-flight — not an error; no ERROR deploys in the last 20. All high-fail-count pipelines' LATEST runs recovered ok=true.

## RESOLVED since the 03:08Z tick (do NOT re-log / do NOT action)
- PACK-EV-HISTORICAL-BACKFILL-CRON-120s-TIMEOUT (the 03:08Z HIGH) — SELF-RESOLVED. pg_cron rpc-backfill-historical-pack-ev (jobid 43) is now green: last 6 ticks 05:13Z->06:03Z all "succeeded" ("1 row"). Whatever adjustment landed (batch size / cron-level timeout) took effect. check_pgcron_recent_failures() [] confirms. Night pass: this candidate is cleared — verify once more and close, no fix needed.

## LOW / watch (new since 03:08Z)
### DAILY-PORTFOLIO-SNAPSHOT-GATEWAY-TIMEOUT (LOW; single missed daily tick; verify self-heal)
- Source: pipeline_runs — daily-portfolio-snapshot latest run 2026-07-06 07:05:21Z ok=false, "upstream request timeout", duration 125,184 ms (the Supabase ~120s API-gateway cap). The 6 prior daily ticks (06-30->07-05) were all clean (5-12 s). Daily cadence (07:05Z), so this is one missed tick, next fire ~07:05Z 07-07 (~1h after this file).
- Risk read: benign gateway-timeout transient (same class as the ultimate-fmv / apply-fmv-haircut one-off micro-clusters) unless it repeats. Not user-facing beyond a 1-day-stale portfolio snapshot.
- Suggested action: night pass — confirm the 07-07 07:05Z tick logged ok=true (self-heal) and close. If it fails a 2nd consecutive day it graduates to a real gateway-cap fix (route runs the aggregate synchronously past 120s -> needs an after()/chunked rework — operator/CC, sales-adjacent care).

### SENTRY-JAVASCRIPT-NEXTJS-1T-OG-PIPE-ABORT (LOW; single-event; likely benign)
- Source: Sentry JAVASCRIPT-NEXTJS-1T — "Error: failed to pipe response", culprit GET /api/og/insights/parallel-premiums, 1 event / 1 user, first+last seen ~04:06Z (2h ago).
- Risk read: distinct from the 500 just fixed at 71356b8 (parallel-premiums/market-pulse OG Satori repair) — "failed to pipe response" is a client-abort during the OG image stream, not a render failure. Single event, not spiking. The new insights OG cards are freshly shipped so a stray crawler/client disconnect is expected.
- Suggested action: night pass — no fix; only escalate if the count climbs across the post-ship watch of today's insights wave.

## Notes
- pipeline_fails_24h top families (compute-topshot-pack-ev 10, fmv-recalc 10, topshot-buyer-backfill 5, analytics-smoke 4, lock-check-batch 3, ...) are the known overnight-contention / DBSAT-timeout classes; every one's latest run is ok=true and detect_stalled_pipelines() [] — not new, not flagged.
- golazos_sales / ufc_sales resolving_editions = benign INFO (1-2/24h), standing.
- Heavy Trevor+Cowork daytime wave today (Golazos pack-EV route, pack-EV secondary-ask reframe, survivor-bias suppression, parallel-premium/market-pulse insights, Dune ownership top-owners, dashboard tokenization). Additive; no view drops/renames observed that would break existing artifact payloads. Artifact estate not deep-run this late tick (ops-snapshot covers the core DB health rpc-live-health surfaces; overnight pass validated the estate).
