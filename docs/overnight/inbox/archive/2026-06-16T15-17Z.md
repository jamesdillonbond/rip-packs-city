# Daytime monitor — FIRST TICK OF DAY (~08:14 PDT / 15:14Z, 2026-06-16)

PLATFORM GREEN. First-tick daily watches (trust health + cross-collection refresh) PASS. Read-only sweep. One daytime ship (`5f1a28d`) health-checked and draining well; one NEW low-risk monitoring candidate + one refinement to a queued item.

## Health snapshot
- pipeline_runs: 6h 1860 runs / **1 fail** (offers-sweep "fetch failed" @12:22Z — transient, self-recovered); 24h 8818 / 2 fails. `detect_stalled_pipelines()` **[]** · `get_pipeline_alerts()` **[]** (N1 not flagged today).
- Security **0/0/0/0** (RLS-off base tables 0; anon/auth write on RLS-off base tables 0; `check_public_security_invariants` []; `check_secdef_anon_execute_violations` []). Sentinel TS-UUID-keyed-48h **0**.
- Trust health **8/8 ok** (no breach; tracked DQ4 `ts_uuid_dupes_created_24h` 0/200).
- Cross-collection pg_cron: `rpc-ccm-step1` 04:10Z + `rpc-ccm-step2` 04:25Z both **succeeded** today; cohort_mat 158 rows fresh, overlap_mat fresh; both jobs active.
- FMV: TS HIGH+MED **3427** (canonical int-keyed 3236), AllDay **778**, Pinnacle priced **1847** — flat/up vs 08:05Z baseline. editions flat (TS 15543 / AllDay 6191). DB **4696 MB** (+15 benign). UFC wmc null_key **2** (fossil floor).
- Vercel: prod `5f1a28d` **READY**; 20 recent all READY/CANCELED, **0 ERROR**.
- Sentry: **1** unresolved (`JAVASCRIPT-NEXTJS-A`, smoke transient, 1 event ~04:06Z, super_low, ~11h quiet — resolve after 24h). 0 new.
- Artifacts: **19 enumerated (14 active + 5 RETIRED tombstones)**. The 2 active artifacts added since the 06:15Z tick validated by running their payloads: `rpc-serial-premiums` (board 270 rows, max premium 1532x) + `rpc-fmv-dialin` (consolidated payload clean — serial_cells n37/reliable33, canonical TS coverage, NFL high_med 778). Flagship `rpc-live-health` insights boards all resolve (squeeze 501+, pack_reality_top_ev 9, rookies 61, cohort_stats 1, trophies 501+). None broken, none repaired.

## Recent-ship health — `5f1a28d` (Trevor daytime, prod READY) — WORKING
`fix(allday): on-chain unmapped-sales resolver (Vercel egress) replaces Cloudflare-blocked consumer-GQL`. Resolver chain 0 fails (allday-unmapped-resolver 36/0, allday-edition-resolver 36/0, promote_unmapped_sales 109/0, allday-sales-indexer 36/0). unmapped_sales open **244 → 188** since 08:05Z; non-budget V1 **208 → 153**; **60 resolved/6h, 255/24h**. No regression; directly drains the ALLDAY-V1-UNMAPPED-DRIFT main class.

## NEW candidate
- **ALLDAY-RESOLVER-ZERO-WRITE-TRIPWIRE · LOW · night-pass (read-only leg) / CC.** Source: the `5f1a28d` commit message — `allday-unmapped-resolver` logged `ok=true` while writing **0 mappings** for ~weeks (AllDay Cloudflare WAF now 403s edge/worker egress on the consumer-GQL path), silently undercounting AllDay V1 sales ~16% (1,372 graveyarded as `no_edition_id_both_sources`, 311/24h) — a monitoring blind spot the `ok=true` health flag hid. The fix is shipped + draining, but **no tripwire exists to catch a recurrence** of "resolver healthy but resolving 0 while backlog climbs." Risk: low (additive monitoring). Suggested action: add a read-only `v_rpc_trust_health` leg or sentinel that flags when an unmapped/edition resolver's resolved-count/24h is 0 while open `unmapped_sales` backlog > N (e.g. 50). Read-only metric = night-pass-shippable once defined; anything touching resolver route logic is off-limits/CC.

## Refinement to a queued item (no new action)
- **ALLDAY-V1-UNMAPPED-DRIFT (queued 06-14, refined 06-16):** the 208-row non-budget V1 class is now **actively draining** via `5f1a28d` (208→153, 255 resolved/24h, inflow already stopped). Recommend the night pass re-measure and **narrow** the item to just the residual fossils that still lack a drainer: **34** `v1_tx_decode_budget_exhausted` (oldest 05-21; `/api/admin/recover-v1-budget-exhausted` exists, still **0 cron**) + 1 AllDay-v2 + 1 v1 fossil. The "208 unmappable" concern is resolving itself.

## Drift notes (benign — no candidate)
- TS ASK_ONLY 930→1707 / LOW 6803→6111 (all-editions, latest-per-edition) since 08:05Z — **confirmed** the expected daily reclassification by the canonical writers (`1.7.0` fmv-recalc 996 writes/12h + `cold-tail-1.0` drain-fmv-cold-tail 167/12h) of thin/stale-sale editions that carry a live ask. HIGH+MED + NO_DATA stable, `fmv_sanity_flags` 0. Not a degradation.
- `serial_fmv_multipliers` last computed 06-15 23:06Z (~16h stale, aging; was ~9h at the 08:05Z baseline) — reinforces queued **SERIAL-FMV-MULT-CRON**. Estimates are labelled + floored at edition FMV, so impact LOW.
