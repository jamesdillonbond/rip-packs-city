# Daytime monitor — 2026-06-12T21:15Z

Run context: 3rd tick of 2026-06-12 (15:15Z, 18:20Z prior). Sweep window 15:00–21:10Z — fully post-recovery from DBSAT-IO-EXHAUSTION-0612. Lock RELEASED. Skipped 1a extras (not first tick).

## Headline: recovery HOLDING; deploys unblocked; UFC-WMC-NULLKEY now regressing on a CALM DB

## Candidates (new/updated)

### 1. UFC-WMC-NULLKEY — regression CONFIRMED on calm DB; escalation case strengthened (MED-HIGH, night pass -> CC)
Source: wmc UFC null edition_key = 3,837/4,584 at 21:08Z vs 3,565 (18:20Z) vs 3,150 at b28a22f ship (03:38Z). +272 in 2.75h with the DB calm (17Z fails 0/240, 20Z 0/242) and AFTER the 18Z seed-refresh wave (1,401 runs) — the "incident starved the enrichment" explanation no longer covers it. ID-walks keep inserting ID-only rows faster than any self-heal fills them.
Action: night pass re-measures at its formal gate (>=2 post-recovery cycles, ~06:00Z 06-13) but the trend through a clean wave already points at: b28a22f's maxDuration lever is NOT reaching the after() enrichment chain. Escalate CC to docs/handoff-ufc-enrichment-decoupled-cron (ready spec, referenced in 4b75dd3).
Risk read: read-only verify; UFC dashboards degrade (null keys = no FMV/set/player join) but bounded to 1 collection.

### 2. VERCEL-DEPLOY-ERROR-X4 — CLOSE as incident-coupled (resolution, no action)
Source: 183e8fc (monitor push, ~18:13Z) READY; eba6491 (Trevor seed-refresh cohort split, ~20:17Z) READY. The 4 ERRORs (3ffdd54/cccd744/4b75dd3/f3128d5, 14:11–15:13Z) sat exactly inside peak saturation; canary test passed per the 18:20Z plan. Prod = eba6491. Main is deployable again. Ledger: mark VERCEL-DEPLOY-ERROR-X4 CLOSED (incident-coupled build-infra, same 06-05 class).

### 3. DBSAT-IO-EXHAUSTION-0612 — recovery verified holding 6h (status, operator decision still open)
fails/runs: 15Z 4/165, 16Z 1/237, 17Z 0/240, 18Z 7/1401 (wave; all 7 = connection-pool blips inside the 18Z backfill wave, 0.5%), 19Z 2/317, 20Z 0/242, 21Z 0/28. fmv-recalc 12 ok runs/3h (last 20:48Z). TFP ok 19:15:36Z — first ok tick on the new slot OUTSIDE saturation; TFP-480-RESTORE gate needs ONE more (next due ~01:15Z 06-13, night pass can restore the watchlist 800->480 if it lands ok). Window 07:00–15:30Z stands as the worst of 3 consecutive days — the compute/IO-budget upgrade decision (operator morning item) remains open and is now well-bounded by data.

### 4. eba6491 post-ship watch — cohort split INERT until cron-side split lands (note for night pass)
Source: deploy READY 20:17Z. ?cohort=K&of=N is opt-in; absent params = byte-identical single wave (the 18Z wave predated the deploy anyway). The cron-side N-entry split is a separate Cowork/operator task — until it lands, expect NO behavior change in seed-refresh waves. Night pass: verify the 00:00Z 06-13 wave is normal (no 400s from the new param validation on existing entries), and don't read "no improvement in wave pacing" as a failed ship.

### 5. LISTCACHE-SILENT-0612 — still thin (LOW-MED, carry)
topshot-listing-cache last ok 20:34Z, 2 runs in 3h vs ~20m historical cadence. Recovered from full silence but cadence is ~6x thinner than historical; under the 360m watchlist threshold so detect_stalled stays quiet. Carry: operator inspects the cron entry's execution history (auto-disable/throttle?) or records the new cadence in docs/operations/cron-schedule.md.

### 6. DB size creep — 4,350 MB (+39 vs 14:25Z baseline 4,311; watch-only)
Post-drop baseline ~4.2GB; mild steady creep continues. No action; night pass tracks.

## Health summary (rest green)
Security 0/0 (RLS-off []; anon/auth write-on-RLS-off []). Sentinel TS-UUID-leak-48h = 0. detect_stalled_pipelines() = []. get_pipeline_alerts() = []. FMV latest-per-edition: TS HIGH+MED 3,251 (946 HIGH / 2,305 MED; 18:20Z 3,242 — rising), NO_DATA 4,350 (falling); AllDay HIGH+MED 643 (flat); Pinnacle per-render 805/1,830 HM. FMV writers fresh (latest write 21:08Z; pack EV 21:07Z; offers sweep 21:02Z). Sales 24h: TS 2,018 / AllDay 82 / Golazos 1. Sentry: 11 unresolved, ZERO new post-recovery (all last-seen ~15:1xZ in-window or older; NEXTJS-1P 22h quiet) — standard resolve-after-quiet. unmapped_sales open: AllDay 214 + Golazos 1 (carry ~183 at 03:10Z — AllDay drifted up modestly, resolver class, watch). v_offer_sanity_flags 166 (baseline 132, OFFER-SANITY-RAISE already with Trevor).

## Artifact validation
17 enumerated (12 active + 5 retired tombstones skipped). Ran the rpc-live-health consolidated jsonb payload end-to-end INCLUDING the heavy FMV legs (busy-gate active=0, trend 295 rows, all keys sensible) — the merged dashboard is fully healthy on a calm DB. AF1 view (rpc-tracked-fmv-confidence) 21 rows OK. rpc-rewards-console (v_rewards_economy) OK. rpc-offers-intelligence legs (v_offer_sanity_flags + edition_offers freshness) OK. No artifact errors observed.
