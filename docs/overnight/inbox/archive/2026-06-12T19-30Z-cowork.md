# Inbox — 2026-06-12T19:30Z (Cowork interactive session)

## 1. UFC-WMC-NULLKEY-REGRESSION — HIGH
- **Source:** live measurement 19:16Z: `wallet_moments_cache` UFC (`9b4824a8-736d-4a96-b450-8dcc0c46b023`) null `edition_key` = **3,837/4,584**, UP from the 3,150/4,584 baseline in the 06-12 overnight handoff (§2, b28a22f row). Measured AFTER the 18Z seed-refresh wave completed cleanly on a recovered DB — so this is NOT incident noise.
- **Read:** the b28a22f "self-heal via 6–24h of seed-refresh waves" premise is failing; null count is rising, meaning new UFC rows land keyless faster than (or instead of) enrichment. Likely the UFC details-walk short-circuits on `skip_cached` (rows exist → walk skipped → JOIN-update has nothing new) or the enrichment leg errors silently.
- **Risk:** null edition_key rows can't join `editions` → no FMV/tier/set on ~84% of UFC holdings.
- **Suggested action:** diagnose the UFC backfill path; candidate fix = forced re-walk (`POST /api/wallet-backfill-ufc?force=true` per wallet, or body `skip_cached:false`) for seeded wallets, then verify null count falls. Route logic = CC territory if a code fix is needed; the forced re-walk itself is operator/cron-fireable.

## 2. Artifact estate consolidation — FYI (no action)
- All active Cowork artifacts now single-payload-query (1 execute_sql per open). rpc-live-health absorbed rpc-fmv-watch + rpc-insights-health. Five "(RETIRED)" tombstones are intentional (mcpTools:[]). Repair rule: preserve the single-payload pattern; never re-split into parallel per-panel calls; never revive tombstones.
- Scheduled-task estate: trust-health-watch + cross-collection-refresh verify folded into the daytime monitor's first tick (§1a of its prompt); rewards pulse folded into the Monday weekly-health-check (§14).
