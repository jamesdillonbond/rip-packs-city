# Daytime monitor candidates — 2026-07-07T21:06Z (~14:06 PDT, later tick)

**Two deltas since the 15:06Z tick — one is closure, one is a small escalation. Platform is otherwise fully GREEN.**

## CLOSURE — CRONJOB-ORG-TRIGGER-SURFACE-DEGRADED has RECOVERED (de-escalate the 15:06Z HIGH)
The cron-job.org trigger-surface dropout the overnight pass + the 15:06Z tick flagged (~15 cron-job.org-only pipelines silent 06:24-06:48Z→15:06Z, ~8.5h) is **resolved as of 21:06Z**. Evidence:
- `rpc_ops_snapshot()` `stalled_pipelines` = **[]**; `trust_health` all ok, breaches **[]**.
- The previously-dead cron-job.org-only pipelines are all firing again with latest run ok=true: `wmc-fmv-populate` 21:03Z, `snapshot-pack-asks` 21:03Z, `pinnacle-listings-reconcile` 20:54Z, `golazos-listings-indexer` 21:07Z, `topshot-buyer-backfill` 21:04Z.
- The downstream `pinnacle_ask_stale_hours` breach (8.7h at 15:06Z) is back to **0.2h**. `topshot_fmv_stale_hours` 0.2h, `pinnacle_render_floor_stale_hours` 1.4h — all fresh.
- Consistent with the recurring class (self-heals on cron-job.org recovery, cursor-based ⇒ no data loss). The 15:06Z durable-backstop suggestion (widen GHA backstop to idempotent still-dead pipelines like `wmc-fmv-populate`, listings indexers, studio backfills) remains valid as a hardening item for the recurring wobble, but there is no live outage now.

## LOW — DAYTIME-CONTENTION: rpc-remap-misattributed-sales hit the 120s cap at 18:23Z (known class, self-healing)
- **Source:** `check_pgcron_recent_failures()` + `cron.job_run_details`. `rpc-remap-misattributed-sales` (`23 */6`) FAILED its **18:23Z** tick at exactly **120.0s** (statement timeout). The other three same-day ticks succeeded (00:23Z 42.0s, 06:23Z 47.7s, 12:23Z 59.5s); 07-06 18:23Z ran 106.4s. So the 18:00-00:00Z window is the contention-heavy one and the remap crept into the cap there.
- **Class:** this is the already-tracked **DAYTIME-CONTENTION-CLUSTERS / OVERNIGHT-0623Z-CONTENTION** family (ledger). Prior instances timed out the 06:23Z overnight tick and recovered next tick; this is the same pattern shifted into the 18:23Z evening window. Cursor-based ⇒ no data loss; expected to recover at the 00:23Z tick.
- **Escalation note (why logged despite being a known class):** the remap durations are creeping (37→106→120s across recent 18:23Z ticks), and this is now the second time-of-day window (evening as well as overnight) hitting the cap — consistent with the standing "DAYTIME-CONTENTION-CLUSTERS-BROADENING" watch. If it fails multiple consecutive ticks it graduates to the classify-acq statement-timeout class and wants a real lever (bounded candidate CTE / raised fn statement_timeout — CC, not a naive bump per the 07-01 measurement).
- **Risk read:** LOW. No data loss, no user-facing impact, single-tick self-healing. Track-only; do NOT auto-fix (the remap is on the `sales` DELETE path).

## Notes
- Security 0/0/0/0 (invariants/secdef-anon/rls-off/anon-write all []); sentinel TS-UUID-48h **0**; DB **8,465 MB**.
- FMV/HM: TS 5,043 (1338+3705), AllDay 817, UFC 15, Golazos 4. Editions TS 18,897 (+746 vs overnight 18,151 = the 07-07 daytime subedition/`::`-parallel cataloging wave — benign, sentinel 0 confirms no hyphen-UUID leak) / AllDay 6,190 / Golazos 575 / UFC 518.
- `pipeline_fails_24h` top families (compute-topshot-pack-ev 10, fmv-recalc 9, lock-check-batch 5, …) all have latest run ok=true — transient overnight-contention/DBSAT class, not new.
- `golazos_sales`/`ufc_sales` resolving_editions = benign INFO, standing.
- Vercel prod READY on `5d4422d8` (CollectionMomentTable Step 3c, the collection-page monolith extraction series). The ERROR `f6cd1ef` (reducer Step 2) was superseded by the Step 3a/3b/3c READY builds; the CANCELED tail = superseded docs commits; BLOCKED `dpl_HbEj` = the overnight pass's own docs commit. Not live issues.
- Artifact estate not deep-run this tick (validated by the overnight pass + `rpc_ops_snapshot()` covers the rpc-live-health DB surfaces); no view drops/renames in today's daytime wave (pack-EV survivor-bias guards, deal-floor parallel-aware serials, collection-page component extractions, flowty orphan cleanup) that would break an artifact payload.
