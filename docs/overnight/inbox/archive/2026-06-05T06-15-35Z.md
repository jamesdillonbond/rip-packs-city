# Daytime monitor inbox — 2026-06-05T06:15Z

Run: `rpc-daytime-monitor` (READ-ONLY sweep). Shared repo (`rip-packs-city-bot` clone still not mounted). `docs/overnight/.lock` present but **STALE/RELEASED** (explicit "RELEASED 2026-06-04T13:19:02Z by runid=458930129" marker, age ~17h >> 45min) → not a live run; no concurrency skip. Inbox commit attempted (Section 2).

**Trevor's interactive session is STILL active** — new prod HEAD `8f3fff9` (pack-reality honesty filter) deploying mid-sweep; the big TS edition/FMV deltas below are tonight's intentional DQ1/DQ2 dedup + A1/A2 FMV keystone compounding, NOT regressions.

Platform **GREEN**. Two new low-risk candidates this sweep (SEC1-EXP, DUPE1), both DB-lane / known-class. Health: `detect_stalled_pipelines()` = **[]** (N1 stayed recovered); 2 single transient cron-rush fails in 6h (pinnacle-nft-resolver, wmc-fmv-populate — both 06:0xZ pool/upstream-timeout class, self-recovered); **no R1 hydrator-FK fails** (R1 stays resolved); Sentry only the known NEXTJS-1G share-OG transient (last seen ~4h ago, can't recur post-`b3dae3db`); 1 Vercel ERROR but it's transient + the same SHA is rebuilding (see DEPLOY note). FMV improving hard (TS HIGH+MED **2936**, NO_DATA **1243**). DB 5975 MB.

---

## NEW candidate — SEC1-EXP (MED · night-pass DB or operator): SEC1 expands from 1 → 3 RLS-off, anon/auth-writable dedup scratch tables

- **Title:** Harden-or-drop **all three** `audit_dq*` staging tables — RLS off + full anon/authenticated INSERT/UPDATE/DELETE/TRUNCATE grants. SEC1 (logged 03:16Z) covered only `audit_dq1_dupe_map`; the DQ2 stage added two more of the identical class.
- **Source:** security sweep (Section 1). `pg_tables rowsecurity=false` → **`audit_dq1_dupe_map`, `audit_dq2_merge_map`, `audit_dq2_uuid_resolve`** (baseline night 06-04 13:05Z was 0). The anon/auth write-grant-on-RLS-off check → 24 grant rows = INSERT/UPDATE/DELETE/TRUNCATE × {anon,authenticated} × the same 3 tables.
- **What they are:** internal dedup map/staging tables from tonight's TS edition dedup — `audit_dq1_dupe_map` (dupe→canonical, created 02:40Z), plus `audit_dq2_merge_map` + `audit_dq2_uuid_resolve` (the DQ2 UUID-resolver's set-batched output, per commit `8e35190`). All created as plain `CREATE TABLE` in `public`, so they inherited the project's broad default grants and never got RLS — unlike the same evening's `rewards_*` tables (RLS-clean, absent from both checks).
- **Blast radius:** LOW. `public` is PostgREST-exposed, so the anon key could in principle TRUNCATE/write them — but they back **no** product surface (uuid→uuid dedup maps, rebuildable from the migrations/resolver; no PII/secrets). Worst case = corrupting a rebuildable scratch map.
- **Risk read:** LOW, fully reversible, mount-independent (night-pass DB lane). **BUT they are the working tables of an operation that may still be in use — Trevor was still deploying mid-sweep.** Re-verify they exist and are no longer being written before acting (Trevor may drop them himself once DQ wraps).
- **Suggested action (after the dedup settles + re-verify):** drop if finished — `DROP TABLE IF EXISTS public.audit_dq1_dupe_map, public.audit_dq2_merge_map, public.audit_dq2_uuid_resolve;` — or harden in place: `ALTER TABLE … ENABLE ROW LEVEL SECURITY; REVOKE INSERT, UPDATE, DELETE, TRUNCATE ON … FROM anon, authenticated;` per table. Verify post-action: both security checks back to 0/0. (Going forward, dedup scratch should land in a non-`public` schema or be dropped at end-of-run to avoid this recurring.)

---

## NEW candidate — DUPE1 (LOW-MED · operator/CC root, night-pass WATCH): inert TS UUID-dupe re-mint at ~550/hr is re-filling the backlog tonight's dedup just drained; sentinel CRITICAL imminent

- **Title:** Ongoing TS ingest is re-minting inert UUID-keyed editions at ~550/hr immediately after the DQ1/DQ2 drain — the dedup is a treadmill without the writer-side fix; the TS-UUID-48h sentinel will cross CRITICAL (≥2000) within ~30-40 min.
- **Source:** sentinel sweep + `editions` catalog. TS-UUID-keyed-48h counter: **19 (night baseline) → 234 (03:16Z) → 1680 (now)**, ALL 1680 inert (`set_id_onchain IS NULL`, kept inert by `editions_block_topshot_uuid_dupe_trg`). 30-min mint buckets: 03:30Z 310 · 04:00Z 520 · 04:30Z 298 · 05:00Z 274 · 05:30Z 278 → sustained ~550/hr, onset ~03:30Z (right after the dedup migrations at 02:40-03:10Z), still firing at the last bucket.
- **What it means:** tonight's dedup drained the historical UUID-dupe backlog (DQ2 = 2,383 merged + 26 promoted; DQ1 = 5,137-row map) and the ongoing ingest immediately began re-creating inert UUID dupes at the known-leak rate. Sentinel thresholds: `<250 ok / <2000 warn / ≥2000 critical` → currently WARN, crosses CRITICAL imminently and will keep climbing through the night (could reach ~10k+ in the 48h window if the rate holds).
- **Blast radius:** LOW *for data* (rows are inert — no canonical corruption, no outage, FMV/insights/analytics unaffected; the dedup trigger holds). The real costs are (a) an imminent sentinel CRITICAL = alert noise, and (b) the dedup work is being undone in real time.
- **Risk read:** the immediate sentinel-CRITICAL is cosmetic/expected given the inert rows; the durable fix is **route/worker code (off-limits to night-pass auto-ship)** — the documented `seed_topshot_editions` / `buildEditionKey` UUID-fallback leak (CLAUDE.md "Open/deferred" Item B2: residual UUID writes now come from `compute-topshot-pack-ev` → `seed_topshot_editions`, not `/api/ingest`).
- **Suggested action:** (1) night-pass: re-measure the rate — if it's a one-off artifact of Trevor's manual DQ2/pack-EV re-runs it'll age out; if steady-state ingest it confirms the leak. (2) Don't blindly raise the sentinel threshold — the WARN is a true positive. (3) operator/CC: the dedup drain won't *stick* until the `seed_topshot_editions` writer prefers the on-chain int-pair over the UUID fallback (Item B2 full code-side fix in `docs/handoff-2026-05-30-overnight-pass.md`). No DB action fixes the source.

---

## Status notes (NOT new — fresh measurements for the morning reconcile)

- **TS dedup + FMV jump = intentional, continuing.** TS editions 16,355 (baseline) → 11,530 (03:16Z) → **10,827** now (DQ2 still merging). FMV latest-per-edition: TS HIGH+MED 1062 → 1638 → **2936**, NO_DATA 4049 → **1243**; AllDay HIGH+MED 267 → **495**, NO_DATA 523 (flat). Driven by DQ1/DQ2 dedup + the daytime A1 keystone (`1c5ccf5` paginate Step-1 past the PostgREST 1000-row cap), Step-6 fossil fix (`bf4cbd5`), A2 ask-corroboration (`b8a0a49`), F2 edition_offers ASK fallback (`d881a75`). Recent-ship health GREEN — these are wins, not drift. **The night-pass metrics baseline needs a full re-capture.**
- **DEPLOY: 1 Vercel ERROR (`8f3fff9`, pack-reality honesty filter) — transient build-infra, NOT a code error.** Build logs show `✓ Compiled successfully in 9.6s` + Sentry sourcemap upload + "Bundled 233 files", then ERROR with no error text — the exact transient signature CLAUDE.md documents. The **same SHA is rebuilding right now** (`dpl_8BnEQx9ciCLCjbxEbcMp2STHmesp`, BUILDING). Production is serving the prior READY (`0441d8b`), and the DB half (`audit_20260605_pack_reality_top_ev_honesty_filter`) is already live — verified `topshot_pack_reality_top_ev` returns the intended **3 honest packs**. Night pass / next sweep: confirm the retry reached READY.
- **R1 (hydrator FK) — stays RESOLVED.** Zero `offers_moment_id_fkey` violations in the 6h scan (migration `audit_20260604_offers_moment_fk_on_delete_set_null`, 06-04 14:47Z). Ledger still lists R1 under Queued — night pass: move to Resolved.
- **N1 (snapshot-institutional-wallets) — stays recovered.** `detect_stalled_pipelines()` = []. No re-fire needed.
- **AF1 (`rpc-tracked-fmv-confidence` / `v_tracked_wallet_fmv_confidence` view times out) — STILL OPEN, already queued (00:20Z + 03:16Z inboxes).** Not re-tested (timeout is wasteful) and not re-logged — see the 00:20Z inbox for the root cause + ready CREATE-OR-REPLACE optimization + revert. Night-pass DB lane.
- **NEXTJS-1G — stays self-resolving.** Only Sentry issue in 12h; last seen ~4h ago (2 events); HEAD `b3dae3db` deletes the `opengraph-image.tsx` culprit so it can't recur. Mark resolved after 24h quiet.
- **Pipelines:** the 2 fails (pinnacle-nft-resolver ×2 statement-timeout @06:05Z; wmc-fmv-populate ×1 upstream-timeout @06:03Z) are the known transient 06:00Z cron-rush class — single, self-recovering, nothing down.
- **Artifacts (16 total):** stable backing-query classes re-run healthy this sweep — FMV breakdown (rpc-live-health/rpc-fmv-watch), security catalog (rpc-security-drift — correctly surfacing the 3 audit_dq* tables), pipeline fails (rpc-pipeline-reliability), deploys+db_size (rpc-deploys-and-cost), and the 3 public insights views post-dedup/post-view-swap (rpc-insights-health: pack_reality 3 / squeeze 3135 / rookie_index 61). `rpc-tracked-fmv-confidence` = AF1 (broken, queued). `rpc-rewards-console` (new 03:01Z) still mid-build — deferred, consistent with 03:16Z. No artifact repaired (read-only monitor).
- **Other health:** security base-table checks otherwise clean (only the 3 SEC1-EXP tables); unmapped_sales 147 open (flat); UFC 446 / Golazos 581 / AllDay 6191 editions (flat). 20/20 prior prod deploys READY before the one transient ERROR.
