# Daytime monitor — 2026-06-17T15:15Z (08:15 PDT, first-tick-of-day)

**PLATFORM GREEN.** Health sweep + first-tick daily extras + 14/14 active-artifact validation all clean. 0 NEW candidates. 2 status updates on already-queued items (FMV-HIGHMED-DIP-WATCH, SERIAL-FMV-MULT-CRON). No breakage, no regression from the morning Trevor ship.

## Health (every-run sweep)
- `detect_stalled_pipelines()` = **[]**; `get_pipeline_alerts()` = **[]**.
- `pipeline_runs`: 6h **0 fails**; 24h **8898 runs / 2 fails (0.022%)**.
- Security **0/0/0/0**: RLS-off base tables [], anon/auth write-on-RLS-off (relkind r/p) [], `check_secdef_anon_execute_violations()` [], `check_public_security_invariants()` [].
- Sentinel TS-UUID-keyed-editions-48h = **0**.
- Sentry: **0 new** unresolved issues firstSeen in 12h (the 2 night-pass smoke-transients are >12h old).
- Vercel: prod **`2dd19af` READY** (alerts buy-link feature); **0 ERROR** across 20 recent (newest `a345b27` is a docs-only commit whose build CANCELED on supersede — normal). DB 4759 MB (+21 benign).
- Drift vs metrics-latest baseline: editions flat (TS 15543 / AllDay 6191); unmapped_sales draining (43); deal board 37->**43** (home-machine ingest working).

## First-tick-of-day extras
- **Trust health 9/9 ok.** Every `v_rpc_trust_health` leg ok incl. the known `ts_uuid_dupes_created_24h` = **0/200** (not even firing). No breach.
- **Cross-collection pg_cron refresh PASS.** `rpc-ccm-step1` (04:10Z) + `rpc-ccm-step2` (04:25Z) both active + **succeeded** today; `cross_collection_cohort_mat` fresh 04:10Z / **158 rows**, `cross_collection_ts_set_overlap_mat` fresh 04:25Z. Both within 26h.

## Recent-ship health (morning Trevor wave)
- `2dd19af` feat(alerts): Top Shot marketplace buy link in deal alerts (~13:50Z, ~1.3h pre-sweep) — **PASS**. `alerts-dispatch` 31/31, `alerts-send` 49/49, 0 fails post-ship; additive (lib/alerts/format.ts, no DB/schema change). Alerts loop still in activation (3 notification_channels, 0 subs, 0 deliveries — Trevor wiring, healthy).
- Last night's deal-board/alerts wave (`b1a52d0`/`489a04b`/`998b8a3`/`6222c0b`/etc.) — `topshot-active-listings-ingest` 4/4 (last 13:13Z, home-machine task), `topshot-buyer-backfill` 50/50 (95c07c5 maxDuration fix holding), `allday-unmapped-resolver` 72/72, `offers-sweep` 72/72. All 0 fails.

## Artifacts (14 active validated; 5 RETIRED tombstones skipped)
Backing data resolves for all: serial-premiums board 271 . perfect-mint 9 . underpriced deal board 43 . v_tracked_wallet_fmv_confidence 21 . v_offer_sanity_flags 210 . cross_collection_cohort_mat 158 . v_rewards_economy 1 / v_rewards_user_balances 10 . get_rpc_traction_snapshot() non-null . v_fmv_sanity_flags 0 . supernova/editions (trophy-ladder) healthy . live-health core tables fresh. None broken; none repaired (read-only).

## Status updates (no new candidates)
1. **FMV-HIGHMED-DIP-WATCH — CONFIRMING RECOVERY (night pass can close after peak).** TS HIGH+MED recovered overnight 2848 -> **3216** at 08:xx PDT, climbing back toward yesterday's 3435 daytime peak. ASK_ONLY 2640 (vs 2600 night). This confirms the night pass read: a benign daily-cycle reclassification by the canonical writers (1.7.0 fmv-recalc + cold-tail-1.0) of thin/aging editions carrying a live ask — NOT ship-attributable. fmv_sanity_flags 0, writers fresh. Recommend the night pass close FMV-HIGHMED-DIP-WATCH once it re-confirms HIGH+MED at/above the ~3400 daytime band.
2. **SERIAL-FMV-MULT-CRON — freshened staleness + now materially live.** `serial_fmv_multipliers` last computed 2026-06-15 23:06Z = **40.1h stale** (37 cells, no `refresh-serial-fmv-multipliers` cron). Was ~33h at night; drifting. Now backs TWO live surfaces: the `/insights/serial-premiums` board (271) AND the new `topshot_underpriced_serials_board` deal board (43, ingesting every 3h) via `serial_fmv_estimate`. The multipliers are population-level tier x circ-band premiums that move slowly, so 40h is low-risk for now — but the longer it drifts, the staler the deal-board "discount" estimates get. Operator/Trevor: wire the refresh cron (carried, escalating priority now that the deal board is live). Not auto-shippable (cron-wiring).

## Noted, not findings
- 4 pipelines quiet >180m (topshot-sales-history-backfill 198m, pinnacle-owner-discovery 187m, editions-hydrate-at-insert 187m, topshot-listing-cache 183m) all ran **7x/24h** = normal ~3.4h cadence, due shortly — NOT stalls (`detect_stalled` []). No false alarm.
- v_offer_sanity_flags 177->210 = the queued OFFER-SANITY-VIEW-REFINEMENT class (~99% sub/serial offers correctly out of edition-grain scope; edition-grain covered by trust leg offer_edition_gap 0/50). Already queued.
