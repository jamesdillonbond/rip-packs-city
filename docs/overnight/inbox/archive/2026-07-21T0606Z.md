# Daytime monitor — 2026-07-21T06:06Z (~23:06 PDT Jul 20)

Sixth and last tick of the day (1520Z, 1805Z, 2105Z, 0012Z, 0315Z prior). Health is **GREEN**
— trust 15/15, security clean on both the old and the new broader checks, `stalled_pipelines []`,
pg_cron `[]`. Evening tick ⇒ Section 1a first-tick extras skipped.

No skew (shell 06:06:28Z ≈ DB `now()` 06:06:47Z ≈ newest sale 06:02Z ≈ newest FMV 06:06Z).
Lock RELEASED by the 07-20 night pass.

Health: security **0/0/0/0** and `check_secdef_anon_exec_drift()` = `[]` (read as a value, not
a row count — the 0315Z trap) · trust **15 metrics, 0 breaches** · `stalled_pipelines []` ·
pg_cron `[]` · sentinel 24 · DB 10,383 MB · editions TS 19,488 / AD 6,190 / GZ 575 / UFC 518 /
candy 125 · FMV TS H+M 3,287 · **prod `c983335a` READY, 0 ERROR-state deploys** · Sentry
3 unresolved, 1 event each — the same three as the last two ticks, no new issue, no spike.

---

## Post-ship watch — prod moved to `c983335a` at 03:30Z, *after* the last tick looked

The 0315Z tick recorded prod as `8c14fdd2`. It has since advanced, and the reason is worth
recording because it was a real deploy-pipeline defect that a session caught and fixed itself:
**the 07-20 security code (`f0bae4c` — durable per-IP concierge rate limiter + crypto session
ids) was committed but never deployed.** Every production build after `8c14fdd` (`cb8bc82`,
`f0bae4c`, `74010ea`, `7e7f780`) was CANCELED by concurrent-session push churn, and because
`vercel.json`'s `ignoreCommand` inspects only the single tip commit, every docs-only tip after
that also skipped the build — so prod sat on `8c14fdd` while the security wave looked shipped.
`c983335a` deliberately touched a non-docs source file to force the build. It is READY and
carries the full tree.

This is the CLAUDE.md "an empty or docs-only commit can never force a rebuild" trap, in a new
shape: not one skipped commit, but a **run** of them masking an earlier CANCELED build. Worth
knowing the DB half was never affected — those three migrations were applied live via MCP.

Structural post-ship checks on the newly-live limiter **PASS**: `concierge_ip_rate` exists,
RLS on, `anon` cannot SELECT (checked functionally), `bump_concierge_ip_rate()` present.
Behavioural verification is Item 2 below.

---

## Item 1 — TOPSHOT-BADGE-CATALOG has **zero successful runs in the entire retention window**, is unwatchlisted, and prunes rows on an incomplete sweep (LOW/MED, new)

**Source:** `pipeline_runs` (`topshot-badge-catalog`) · `pipeline_cadence_watchlist` ·
`badge_editions` counts.

Both runs of this pipeline inside the retention window failed, and neither has ever
logged an `ok=true`:

| started_at | ok | pages | nodes | upserted | deleted_stale | terminated_reason |
|---|---|---|---|---|---|---|
| 2026-07-21 05:36:58Z | false | 13 | 1,300 | 512 | 512 | `gql_error` (TS GraphQL **429**) |
| 2026-07-19 05:37:56Z | false | 0 | 0 | 0 | 0 | `gql_error` (TS GraphQL **429**), died in 363 ms |

Three things make it worth a line:

1. **`last_ok` is NULL** — unlike every other entry in `pipeline_fails_24h`, which all have a
   `last_ok` newer than their `last_fail`. This one is not a contention blip in an otherwise
   working loop; it has not completed a sweep in the window at all.
2. **It is absent from `pipeline_cadence_watchlist`**, so `detect_stalled_pipelines()` is
   structurally blind to it — the same gap filed for `topshot-wmc-fossil-drain` at 0012Z.
3. `sweep_complete: false` **with `deleted_stale_rows: 512`** — a sweep that prunes and then
   aborts is the shape that silently destroys coverage.

**On that third point I checked before alarming, and it is the benign reading.** TS
`badge_editions` currently holds **9,283 rows** (up from the 6,930 CLAUDE.md records after the
07-16 backfill), with `max(updated_at)` = 05:37:09Z — i.e. the partial sweep's own write.
`deleted_stale = upserted = distinct_editions = 512` exactly, which is the signature of a
per-batch delete-then-insert, **not** a global prune of unreached rows. No net regression.
Stating this explicitly so a later tick doesn't re-open it as a data-loss incident.

**Also mitigating:** the additive backstop is healthy — `topshot-badge-set-backfill` ran
**4/4 ok** today (newest 03:15Z), which is the cron CLAUDE.md added precisely because this
marketplace sweep is structurally lossy. Coverage is being maintained *around* the failure.

**What I cannot determine from `pipeline_runs` alone, and am not going to guess:** its cadence.
Two runs in a 3.3-day retention window (floor 07-18 00:41Z) is consistent with a ~2-day
schedule, or with a more frequent schedule that is mostly missing. CLAUDE.md describes a 4×/day
GHA badge-sync, which would imply ~13 runs — so either this pipeline name is not that job, or
runs are being lost before they log. Resolving that needs the GHA / cron-job.org side, which is
operator-visible, not DB-visible.

**Suggested action (two parts):**
- Cheap and safe: add a `pipeline_cadence_watchlist` row so a silent stop becomes visible.
  Pick `max_silent_minutes` **after** the cadence is confirmed, not before.
- If the 429s persist, the lever is **pacing/backoff** in the sweep — it pulled 13 pages in
  16.5 s, which is plausibly faster than the TS proxy's rate limit tolerates. **Do not propose a
  proconfig `statement_timeout` change**: this is an upstream HTTP 429, not a DB timeout, and
  that pattern has now been disproven three times.

**Risk read:** the watchlist row is additive DB-only and trivially revertible
(`DELETE FROM public.pipeline_cadence_watchlist WHERE pipeline='topshot-badge-catalog';`).
The pacing change is route/worker code ⇒ Claude Code, not an autonomous ship.

---

## Item 2 — the durable concierge IP limiter is live in prod but its backing table is empty; currently unverifiable at n=1 (LOW / watch, new)

**Source:** `concierge_ip_rate` · `support_conversations` · deploy `c983335a` (03:30:09Z).

`concierge_ip_rate` (`ip, window_start, count`) holds **0 rows**. Only **1 chat** has occurred
since the deploy — at 03:30:58Z, **49 seconds** after the build went READY, so it may well have
been served by the outgoing build during alias propagation.

So 0 rows is close to vacuous, and I am flagging it as watch rather than defect for that
reason. It is *not* evidence the limiter is broken, and it is *not* evidence it works. The
windowed shape also means rows may legitimately expire or be pruned, which would produce the
same empty table.

**Why it earns a line anyway:** this is a **security control** that has been in the tree since
07-20 and only reached production 2.5 h ago, having been invisibly skipped by four consecutive
builds. If it is not writing, the per-IP rate limit on the public concierge endpoint is
silently unenforced, and nothing else in the sweep would surface that.

**Precise next-tick test:** re-read both. If `support_conversations` has gained chats
comfortably after 03:30Z **and** `concierge_ip_rate` is still empty with no pruning job to
explain it, that is a real finding worth escalating. Until then, do not act and do not alarm.

**Risk read:** read-only verification, no change proposed.

---

## Item 3 — CLAUDE.md's "Golazos badge `low_ask` frozen since 2026-07-08" looks stale (LOW, doc accuracy)

**Source:** `badge_editions` joined to `collections`.

The Deferred-hardening section records Golazos at **104/218** with `highest_offer` 0/218,
"frozen since 2026-07-08", and asks for a `golazos-badge-low-ask-refresh` cron to be built.
Measured now: **111/218** with `low_ask`, `max(updated_at)` = **2026-07-21 04:26:27Z** — today.

**Honest caveat, because this is one delta and not a trend:** there is still **no**
`golazos-badge-low-ask-refresh` pipeline — the 24 h badge set is `allday-badge-low-ask-refresh`
(48/48 ok), `topshot-badge-catalog`, `topshot-badge-set-backfill`, `allday-badge-ingest`. So
something else is touching those rows and I have not identified what. A single count change
does not prove a live refresh loop.

**Suggested action:** one cheap measurement next tick (`with_low_ask` + `max(updated_at)`). If
both move again, close the deferred item as resolved-by-something-else rather than building the
cron it asks for. Nothing to build either way right now.

**Risk read:** read-only; the only outcome is a documentation correction.

---

## Follow-up on 0315Z Item 1 (signup funnel) — answered, and the answer is *still* vacuous

The prior tick set a specific criterion: a post-deploy `home_view` with still-zero
`signin_click` would be a real finding. **That criterion is still not satisfiable.** All three
new event types (`signin_click`, `account_created`, `email_capture_submitted`) remain at zero
rows, and the newest `home_view` is **still 2026-07-20 23:50:47Z** — before the 02:13Z deploy.
No eligible traffic has hit the instrumented path.

One adjacent observation that is genuinely informative rather than a defect: **post-deploy
traffic does exist, just not on the home page.** `insights_view` has 25 events (newest 05:16Z,
comfortably after the deploy) and `collection_view` 43. So the site is being used and the
public `/insights` boards — not the marketing home page — are where visitors actually land.
For a funnel whose signup CTA instrumentation lives on the home header and pricing page, that
is worth knowing before anyone concludes the counter is broken. Carry the watch.

---

## NOT re-raised (measured, already known — recorded so tomorrow's first tick doesn't re-spend)

- **`sales-counterparty-backfill`** 35 fails / 287 runs = **12.2%**, continuing the documented
  monotonic improvement (18.8% 07-19 → 13.0% → 12.8% → 12.5% → 12.2%). `last_ok` 06:05Z is
  newer than `last_fail` 05:45Z. Connection-pool contention, self-limiting, cursor-safe.
- **DUNE-DATAPOINT-CAP-402** — unchanged. Both walkers still 100 % fail on HTTP 402
  (`sales-seller-recovery-dune` 24/24, `sales-ingest-dune` 12/12); `sales-ingest-dune`'s newest
  attempt is the `2025-09-01..2025-09-03` window, i.e. the cursor is still parked exactly where
  the 2105Z tick left it. That writeup already framed the real risk — unsupervised resumption
  on billing-cycle reset, on the `sales` write lane. Nothing new.
- **TOPSHOT-WMC-FOSSIL-DRAIN** — filed in full at 0012Z. No new information.
- **ALLDAY-LOCK-REFRESH** `wallet_fetch` timeouts 3/22 — declined with proof by `438360f`; real
  fix queued as ALLDAY-LOCK-REFRESH-SELECTION-COST. Do not propose the proconfig fix again.
- **The rest of `pipeline_fails_24h`** — `wallet-username-resolver` 10.4 %,
  `pinnacle-nft-resolver` 2.9 %, `compute-topshot-pack-ev` 1.9 %, `fmv-recalc` 2.3 %
  (self-classified saturation), `ufc-studio-sales-history-backfill` 1.4 %, `snapshot-pack-asks`
  0.3 %, `run-insider-detectors` 4.2 %. Every one has a `last_ok` newer than its `last_fail`.
  One contention family, not several faults. **`topshot-badge-catalog` is the sole exception**
  — hence Item 1.
- **Sentry's three entity-page errors** (pack / team / player detail) — identical set to the
  last two ticks, 1 event each, the intended post-07-14 "throw retryable instead of silently
  404" shape. Underlying constraint is the connection pool.
- **Golazos `resolving_editions`** info alert — 2 unmapped in 24 h, severity `info`, standing.
- **CANCELED production deploys** — several in the window; all are the expected `ignoreCommand`
  supersede or concurrent-push churn. 0 ERROR-state. See the post-ship section for why this
  particular run of CANCELEDs mattered.

---

## Drift vs the 07-20 08:02Z night-pass baseline (`metrics-latest.json`, ~22 h)

- editions TS 19,464 → **19,488** (+24) · AD/GZ/UFC/candy unchanged.
- DB 10,274 → **10,383 MB** (+109 in ~22 h — *slower* than the +220/24 h the baseline flagged).
- FMV TS H+M 3,384 → **3,287** (−97). Oscillating in a 3,28x–3,38x band across today's ticks
  (3,332 → 3,294 → 3,281 → 3,287); the documented sales-cooldown redistribution, confirm-only.
- sentinel 0 → **24**. A real move, but the breach threshold is 200 and this is the known inert
  `::`-cataloging class. Note, don't chase.
- unmapped backlog 27 → **26** · `topshot_impossible_parallel_serials` 0 → **0** ·
  `edition_integrity_flags` 5 → 5.
- **Closed by observation:** the baseline recorded `offer_edition_gap_max_usd` opening a BREACH
  mid-run (1 → 201 vs threshold 50) and predicted it as the self-clearing OFFER-SANITY class.
  It now reads **0**. Prediction held; no action needed.

---

## Artifact validation

Estate unchanged at **15 active** (17 listed − `rpc-growth-funnel` and `rtr-pack-finder`,
retired tombstones). Validated from the DB side, extending the 0315Z pass to the four artifacts
its 12-object list did not cover (`rpc-pack-lifecycle`, `rpc-panini-squeeze`,
`rpc-set-challenge-roi`, `rpc-moment-fmv-ev-dialin`): **21/21 backing objects resolve**, and
the six probed directly all return data — `v_topshot_pack_lifecycle_global` 1,
`v_topshot_pack_lifecycle` 2,028, `v_topshot_pack_realized_ev` 184, `panini_coverage_summary` 1,
`candy_best_offers` 25, `pack_table_rows` 5,444.

`pack_table_rows` returning 5,444 to `service_role` is a useful incidental confirmation that
`26727e97`'s anon revoke did not over-reach — the view still reads fine for its actual (all
`supabaseAdmin`) consumers.

No artifact edited or repaired; validation only.
