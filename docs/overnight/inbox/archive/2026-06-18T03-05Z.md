# Daytime monitor — 2026-06-18T03:05Z (20:05 PDT, evening tick)

**PLATFORM GREEN; 0 NEW candidates (status record).** detect_stalled [], security 0/0/0/0, trust 9/9, sentinel 0, Sentry 2 stale (0-new/~22h), Vercel 0 ERROR (prod `a6c3afd` — the four 06-18 "parallel type" alert ships + the 06-17 alerts/jersey ships all READY), 14/14 active artifacts validated. The deal-leg timeout flagged at 00:06Z has **NOT recurred since 22:44Z** (~4.3h / 17 runs ok): the 06-17 materialize-pools-once migration appears to have mitigated it, and the 06-18 parallel ships (which add a WHERE + subqueries to `dispatch_due_deal_alerts`) did not reintroduce a timeout. Daily extras skipped (8am tick ran them; ccm re-confirmed fresh @04:10Z this tick).

## Health (every-run sweep)
- `detect_stalled_pipelines()` = **[]**. `get_pipeline_alerts()` = 1 **info** only (`golazos_sales` resolving_editions — longstanding benign Golazos edition-resolution-bridge gap, 1 sale/24h).
- `pipeline_runs`: 24h **9022 / 5 fails (0.06%)**; 6h **1928 / 2** — the 2 = `alerts-dispatch` deal-leg 30s timeout (22:44Z, pre-mitigation; see status) + `evm-transfers-ingest` Base 429 rate-limit (02:19Z, known LOW). Else clean.
- Security **0/0/0/0**: RLS-off base `{}`; anon/auth-write-on-RLS-off 0 (relkind r/p); `check_public_security_invariants()` 0 rows; `check_secdef_anon_execute_violations()` `[]` (read the VALUE — the four 06-18 CREATE OR REPLACEs on `dispatch_due_deal_alerts` preserved grants, no anon hole).
- Sentinel TS-UUID-keyed-editions-48h = **0**.
- Sentry: **2 unresolved**, both single-event smoke transients (NEXTJS-1E sales-indexer-detect_stalled + NEXTJS-A fmv-pipeline-healthy), first+last ~22h ago (06:45-07:35Z wave window); **0 new**.
- Vercel: prod **`a6c3afd` READY** (parallel/variant deal filter); **0 ERROR** across 20 (CANCELED = superseded docs/monitor builds incl. HEAD docs commit 3c60300). DB **4790 MB** (+4 vs 00:06Z, benign).
- Drift vs baseline: editions flat (TS 15543 / AllDay 6191 / Golazos 581 / UFC 446); fmv_sanity 0; trust 9/9 ok (pinnacle_fmv 17.1h/30, unmapped_resolution_backlog 9/100, all green).
- FMV (`fmv_current` latest-per-edition): TS HIGH+MED **3111** (HIGH 851 / MED 2260; ASK_ONLY 2622 stable; NO_DATA 3581 down from 3697 baseline) — recovered from the 2848 overnight low, normal evening softening from 3140 @00:06Z. AllDay HIGH+MED **821** (up from 768 baseline).

## Recent-ship health (06-18 "parallel type" wave + 06-17 alerts/jersey — all READY, no regression)
- **`0933067`/`11c6c1a`/`2ac4bcf`/`a6c3afd` parallel-type alert wave + 2 migrations (`dispatch_due_deal_alerts_surface_parallel_variant`, `alert_subscriptions_parallel_names_filter`) — PASS.** All READY; security 0/0/0/0 (CREATE OR REPLACE preserved grants — no SECDEF anon hole); `alert_subscriptions.parallel_names` present; deal board + dispatch healthy; deal-leg added a WHERE + parallel subqueries but no post-deploy timeout.
- **`247651d` serial filters + per-edition FMV alerts — PASS.** fmv_alerts 0 (none created yet), `topshot_serial_deal_alerts_for_subscription` clean, no errors.
- **`967a1b2` per-moment jersey — PASS** (editions flat/additive, serial-premiums board 274 healthy).
- **`3a804cc` discord global commands / `b00b914` concierge DM personalization — PASS** (register route + inert helper).
- **Alerts loop delivering:** alert_deliveries 24h = **24 sent / 0 failed** (Trevor testing; 0 alert_subscriptions, 0 fmv_alerts, 3 notification_channels) — send path healthy.

## Artifacts (14 active validated; RETIRED tombstones not in manifest)
Backing data resolves: serial_premiums **274** / perfect_mint **9** / underpriced board **19** (listings churn from 18, healthy non-zero) / tracked_fmv **21** (v3 view fast) / cross_collection_cohort_mat **158** fresh @04:10Z / offer_sanity **212** / squeeze **501+** / rewards econ 1 + balances 10 / FMV-by-collection (`fmv_current`) resolves. None broken; none repaired (read-only).

## NEW candidates
None.

## Status updates (already-queued; re-measured)
- **ALERTS-DISPATCH-DEAL-TIMEOUT (queued 00:06Z) — acute timeout MITIGATED, structural cost remains.** No deal-leg fail since 22:44Z (~4.3h / 17 runs ok). The 06-17 `audit_20260617_dispatch_due_deal_alerts_materialize_pools_once` migration (materialize both boards once + statement_timeout 45s) appears to have stopped the 30s timeouts; the 06-18 parallel ships did NOT reintroduce one. BUT durations stay bimodal (sub-second … 12-16s spikes: 14.8s @01:59Z, 12.5s @02:59Z) — the 2-source deal-set computation is real cost that grows with subscribers (still 0 subs). Night pass: likely DOWNGRADE to "watch as subs grow" / close if the materialize fix holds another day; the deal-query optimization stays the right pre-emptive lever before subscriber growth.
- **SERIAL-FMV-MULT-CRON (queued) — drifting further (escalating).** `serial_fmv_multipliers` last computed 2026-06-15 23:06Z = **52.1h stale** (37 cells; 49.1h @00:06Z, 46.2h @21:19Z). Backs serial-premiums (274) + underpriced board (19) via `serial_fmv_estimate`. Slow-moving population premiums (low per-day risk) but compounding the deal-board "discount" staleness. Operator/Trevor: wire the `refresh-serial-fmv-multipliers` cron (cron-wiring, not auto-shippable).
- **FMV-HIGHMED-DIP-WATCH (queued) — recovered, keep open one more day.** TS HIGH+MED 3111 (well above the 2848 low); ASK_ONLY 2622 stable (not over-claiming), writers fresh, fmv_sanity 0 — benign daily-cycle reclassification. Re-confirm the day-over-day peak settles, then close.

## Noted, not findings
- alerts-dispatch deal-leg durations bimodal but all < 30s cap since 22:44Z (folded into the status update above).
- underpriced board 18 -> 19 / offer_sanity 220 -> 212 — natural churn, healthy non-zero.
- evm-transfers-ingest Base-429 (02:19Z) — known LOW external rate-limit, recovers next tick.
