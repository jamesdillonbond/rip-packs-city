# Inbox — 2026-08-09T00:11Z (daytime monitor, 5:11 PM PT Aug 8)

⚠ **inbox written to MOUNT, push unavailable** — the workspace shell is down again (`useradd ... no space left on device`, the `/sessions` no-space class that killed the 08-08 overnight pass), so no sandbox clone/push. Written directly to the mounted tree for the night pass to pick up locally. No git run against the mounted `.git`.

## Health sweep summary (all read-only)
- `detect_stalled_pipelines()` = `[]`. Security clean: RLS-off base tables 0, anon/authenticated write-on-RLS-off 0. Sentry: **0 new unresolved issues in 12h**. No Vercel deploys in ERROR (latest READY prod = `e0805f0c`, the CI-unblock fix; the several CANCELED rows are normal superseded-by-next-push, not failures).
- Trust health: 3 BREACH, **all already known/tracked** — `panini_sale_price_capture_dry_days`=11 (upstream Panini outage, climbing 8→11), `public_board_slow_count`=8 (crawler-class contention), `unmapped_resolution_backlog_max`=175 (AllDay backlog, held flat). UFC red-by-design is GONE (repointed to `ufc_flow_revival_sales_30d`=0/ok on 08-08, as designed).
- `rpc_ops_snapshot()` itself **timed out** inside `sentinel_fmv_confidence_rows` (the known expensive latest-per-edition DISTINCT-ON), a saturation symptom in its own right.

## The one thing worth the night pass's attention: an ACTIVE daytime disk-IO saturation episode is hitting PUBLIC surfaces

This is the **same root cause already queued** (the pg_cron MV-refresh cluster + Small-tier disk-IO budget) — NOT a new defect. Logging it only as **fresh blast-radius evidence + one distinct mitigation angle**, so it isn't double-counted against the existing queue.

**Already queued, do NOT re-log (confirmed present):**
- 5 pg_cron MV-refresh timeouts — `rpc-refresh-allday-pack-realized`, `rpc-backfill-pinnacle-acquisitions`, `rpc-refresh-topshot-edition-median` (per 1945Z: resolved-by-diagnosis, 59s vs 600s, just confirm its 00:10Z tick), `rpc-refresh-misattrib-candidates`, `rpc-thin-sale-ask-disclosure-refresh` → all in inbox 2026-08-08T1945Z / 1717Z / 1443Z.
- `sync-nba-projections` (sports-proxy secret drift + v9 staged) → inbox 2026-08-08T2130Z.
- `topshot-active-listings-ingest`, `allday-lock-refresh` → inbox 2026-08-08T2130Z.
- `wallet-username-resolver` statement-timeout → queued in the 2026-08-05 overnight pass.

**NEW evidence (this tick):** the 21:00→00:11Z saturation wave is degrading **user-facing public pages**, not just crons/entity SSR. From Vercel runtime errors (6h): public insights boards timing out per-request — `/insights/candy-mlb` (pack-market 42, scarcity 42, player 37, parallel-premium 36, pack-ev 36, special-serials 32 timeouts), `/insights/panini-squeeze` backing view 16, `/insights/deals` 21, `/insights/rookies` ~39, `/insights/first-mint` ~50; pack-detail `bundle`/`pack_lifecycle`/`pack_realized_ev` timeouts (34/46/54) → "pack detail bundle unavailable" 500s; and `/[collection]/set/[slug]` `get_set_editions` logging **STRUCTURAL — throwing** (a real 500, not a fail-soft degrade). `pipeline_runs` corroborates: ~24 distinct pipelines failing in 6h, sample errors overwhelmingly `statement timeout` / `Timed out acquiring connection from connection pool` / `lock timeout`, plus wallet-backfill `rows_lost` (self-heals next scan).

**Distinct mitigation angle for the night pass to weigh** (separate from the pg_cron MV refresh internals already queued): the loudest casualties are **public `/insights` boards that compute heavy queries on every request** with no cached/materialized payload, so they collapse first when the disk-IO budget throttles. Candidate = back the hottest public boards (candy-mlb, panini-squeeze, deals, rookies, first-mint) with a cached/materialized payload (or short-TTL server cache) so they survive IO throttling independent of when the pg_cron MV cluster is repaired. Risk: read-path/caching only — LOW — but it is a design change, not a config tweak, so it belongs in the night pass's judgment, not an auto-ship. Do NOT bump statement-timeouts as the fix (per the standing guardrail) and do NOT upgrade the compute tier (disk-IO-bound, not compute-bound — Medium is the same 2 cores).

Suggested action: prioritize draining the queued MV-refresh cluster (it frees the disk-IO budget the boards are starved of), and separately consider the public-board caching mitigation above.
