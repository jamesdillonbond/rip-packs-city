# Daytime monitor inbox — 2026-06-19 ~20:06 PDT tick (8pm, last tick of day)

Platform GREEN. **One NEW candidate** — a recent-ship regression from today's CC "Special Serial Owners" MV. The ongoing ALLDAY-SERIAL item is unchanged (do NOT re-log). A fresh OffersV2 fill→sales wave shipped in the last ~2h is flagged for the 1am night pass to post-ship-watch.

## Candidate

### REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT — NEW — today's MV refresh cron fails every run on the 30s service_role statement_timeout
- **Source:** `pipeline_runs` `refresh-special-serial-owners-mv` — **0 ok / 2 fails** since the operator wired its cron (~00:32Z 06-20), both `canceling statement due to statement timeout`. Backs the Special Serial Owners surface shipped today (CC, deploys `f995ebef` + `93ff06c4`; ledger 2026-06-19 "Special Serial Owners surface").
- **Root cause (pinned):** `refresh_topshot_special_serial_owners_mv()` does `REFRESH MATERIALIZED VIEW CONCURRENTLY public.topshot_special_serial_owners_mv`. The MV's base query full-scans TS wmc (~70s per the ship note; CONCURRENTLY is slower still). The SECDEF fn pins **no** statement_timeout of its own, so it inherits the caller — the cron route calls it via `supabaseAdmin` (service_role), and **`service_role` has a role-level `statement_timeout=30s`**. 70s+ refresh > 30s → cancelled every run. Route `maxDuration=120` is irrelevant: Postgres cancels at 30s before the lambda budget matters.
- **Blast radius — LOW.** The MV still holds its **5,927-row initial snapshot** (populated at ship time, verified present), so the board serves data; the surface is **auth-gated + robots:noindex + not in sitemap** (brand-new, low traffic). No FMV / pricing / sales / security / outage impact. Only consequences: the board never updates holders, and persistent ok=false noise (NOT watchlisted, so it never trips detect_stalled — but it will fail forever until fixed).
- **Risk read:** MED (recent-ship; the refresh path is structurally broken and self-perpetuating; very low blast radius). Fix-forward, NOT an auto-revert.
- **Suggested action (CC migration, ± 1-line route):** pin a statement_timeout on the SECDEF fn to cover the refresh — `ALTER FUNCTION public.refresh_topshot_special_serial_owners_mv() SET statement_timeout = '180s';` — and confirm the route's `maxDuration` (currently 120) covers the after() refresh wall-clock (raise to ~200, or drop CONCURRENTLY to shorten it; the MV is only ~1.5 MB / 5,927 rows so a plain REFRESH's brief AccessExclusive lock is fine on a daily cron — but plain REFRESH still exceeds 30s, so the fn-level timeout is the real fix either way). The cited sibling `refresh_pack_grail_metrics_mv` / `/api/cron/refresh-pack-grail-metrics-mv` is the proven hourly-MV pattern — diff against it. Verify: next cron tick logs ok=true and the MV refresh succeeds.

## Notes (not candidates — for the record)
- **ALLDAY-SERIAL-BACKFILL — UNCHANGED, still 100% 1009-blocked (do NOT re-log; already queued + escalated at the 8am and 2pm ticks).** `allday-listing-serial-backfill` 2 more fails (last 00:34Z 06-20), `allday_moment_serials` still **2 rows**, 0 ok ever. No new evidence beyond "still failing"; the 2pm next-diagnostic stands (diff the backfill fn's request shape vs the proven `allday-unmapped-resolver` on the same /allday-consumer proxy path). Keep OPEN.
- **WATCH for the 1am night pass — fresh OffersV2 fill→sales wave** (`8ffb2913` "capture accepted TS offers as sales (OffersV2 fill gap)" + `06ad6e82` "fire-and-forget offer-fill backfill + stamp fill_tx_hash", deployed ~01–03Z 06-20). New `source=offer_fill` TS sales path (~340/day previously uncaptured) — touches the sales/FMV-feeding path. Healthy so far: `topshot-offers-indexer` 36/0, `offers-sweep` 36/0, `backfill-offer-fill-sales` 1/0. Too fresh (<1h) for a real post-ship watch this tick — the night pass should verify sales counts climb without duplicate `transaction_hash` rows and that FMV still reconciles. (`v_insights_top_sales` already grew 576→746, plausibly these new sales surfacing — benign.)
- **BUYERBF-PERINVOCATION-WORK — holding, not worsening.** `topshot-buyer-backfill` 68/0/24h, max **740.2s** (= the 2pm peak; no further creep), < the 770s watch line, < the 800s Pro cap, detect_stalled []. Item holds as queued.
- **FMV healthy, reconciles EXACTLY** — latest-per-edition TS 15,543 = 15,543 editions / AllDay 6,191 = 6,191. TS HIGH+MED **3,292** (up from 3,144 baseline / 3,178 @2pm — improving), NO_DATA 3,385, ASK_ONLY 2,477. AllDay HIGH+MED 850, NO_DATA 2,331 (benign zero-90d-sale class). Sentinel TS-UUID-48h **0**.
- **Health:** security 0/0/0/0, detect_stalled [] / get_pipeline_alerts [], trust health **9/9 ok** (pinnacle_fmv 17.1h/30, pack_ev_board 0.63d/2, unmapped_backlog 10/100, ts_uuid_dupes 0/200 — all well under breach). pipeline fails last 6h all transient/known (evm-429, allday-serial 1009, + the new MV-refresh timeout above). DB 4,887 MB (+44 benign vs baseline). Cohort `cohort_mat` fresh 06-19 04:10Z (next pg_cron refresh 06-20 04:10Z). 14 artifacts GREEN (deals_board 822, allday_floor_ask 3913, serial_premiums 285, trophies 683, squeeze 9102, top_sales 746, underpriced 12, tracked_fmv 21, offer_sanity 231). Vercel prod `06ad6e82` READY, 0 ERROR. Sentry 0 new/24h.
