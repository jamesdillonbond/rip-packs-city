# Daytime monitor candidates — 2026-08-09T18:15Z (~11:15 AM PT)

Written by `rpc-daytime-monitor`. Bash/git sandbox is DOWN again (`useradd … /sessions no space left on device`, 3rd consecutive autonomous run) so this file is **written to the mount, push unavailable** — the night pass picks it up locally. Lock was RELEASED (08-09 night pass), not held.

Sweep was **green with known saturation noise**. Security 4/4 clean (invariants/anon-write/rls-off/secdef-anon all `[]`). `detect_stalled_pipelines()` `[]`. Latest deploy READY (tip `105deb95`, the nc1 insights snapshot-cache). Trust health: **2 BREACH, both known/carried** — `panini_sale_price_capture_dry_days`=12 (upstream home-box outage) and `unmapped_resolution_backlog_max`=162 (permanent AllDay floor, draining). DB 12,273 MB.

The one theme worth the night pass's eyes is a **wide disk-IO saturation window right now** (~15:00–18:00 UTC) — everything below is one root cause, and the nc1 board-cache mitigation just shipped READY, so this is really post-ship-watch fuel.

---

## 1. LOW / visibility · Saturation window is unusually WIDE — 9 pg_cron jobs timing out at once (typical ~4), incl. the new saved-wallet reconcile job

- **Source:** `check_pgcron_recent_failures()` — 9 jobs, every one `canceling statement due to statement timeout`, all the documented disk-IO-budget MV/refresh cluster: `rpc-refresh-mv-ts-set-play-catalog` (15:17Z), `rpc-allday-serial-fmv-jersey` (11:25Z), `rpc-ccm-step1` (04:10Z — cross-collection cohort mat now ~14h stale), `rpc-allday-ev-corrected-refresh` (12:47Z), `rpc-serial-fmv-jersey-weekly` (11:35Z), `rpc-refresh-misattrib-candidates` (15:35Z), `rpc-trust-health-precompute-refresh` (12:58Z — `trust_precompute_max_age_hours`=11.14, still under the 13 breach so an earlier tick banked), `rpc-thin-sale-ask-disclosure-refresh` (09:25Z), **`rpc-reconcile-saved-wallet-stats` (13:33Z — the 08-08 job, newly a casualty)**.
- **Risk:** none new — all self-retrying, all the standing saturation class the ledger documents repeatedly. Flagged only because (a) 9-at-once is wider than the usual 4, and (b) `rpc-reconcile-saved-wallet-stats` (display-only, self-corrects on next run) has joined the casualty list.
- **Suggested action:** none beyond the existing nc1 post-ship watch — `105deb95` (public-board snapshot-cache) is the active read-path mitigation and just went READY; watch whether the window narrows on the next daytime ticks. `pipeline_alerts` corroborates: `allday-lock-refresh` 50% / `wallet-username-resolver` 41% / `wallet-backfill-*` fails are all statement-timeout, same cause.

## 2. LOW / instrument-lies · smoke-test emitted a Sentry error whose title asserts the OPPOSITE of what happened (a pool-timeout, not a real violation)

- **Source:** Sentry `JAVASCRIPT-NEXTJS-25` — *"smoke test failed: cursor-stall threshold shared by classifier and alert arm"*, POST `/api/smoke-test`, **1 occurrence, 15:11Z**, release `991291be`, endpoint tag `rpc:check_cursor_stall_threshold_drift`.
- **What actually happened:** `detail: "rpc error: Timed out acquiring connection from connection pool"` — the drift check did **not** find a threshold-sharing violation; the RPC timed out acquiring a pool connection during the same saturation window as item 1. So the error title (the check's descriptive name) reads like a genuine cursor-stall-guard regression when it is a transient saturation false-positive. Single event, not recurring.
- **Risk:** low, but it is the "instrument that lies" class — a saturation pool-timeout surfaced under a title that would send a triager hunting a non-existent classifier/alert-arm coupling bug. Could train noise if it recurs each saturation window.
- **Suggested action (night pass judgment):** consider having `/api/smoke-test` distinguish a check *violation* from a check *infra failure* (pool-timeout / statement-timeout) so the emitted title/Sentry message says "smoke check errored (pool timeout)" rather than restating the assertion it never got to evaluate. Read-path/observability only. Do NOT touch the underlying cursor-stall guard — it is behaving; the check simply couldn't run. Safe to resolve the Sentry issue as transient-saturation if it doesn't recur.

---

**Not re-logged (already known/queued — verified against ledger):** `sync-nba-projections` 19/19 `all_upstreams_failed` (queued operator: v9 + secret drift; also NBA offseason), `topshot-active-listings-ingest` 81% `egress_blocked` (ledger 2026-08-07 "INTERMITTENT — do NOT suppress" + queued Vercel-cron-backstop call), `candy-offers-indexer` 40% 700s-deadline (by-design partial-sweep, deactivation skipped), `topshot-pack-opens-history-backfill` 28.8% (backfill status-0 noise), the two carried trust breaches, and the ERROR deploy `0fffb2ae` (superseded, documented transient build-time saturation; tip is READY).

**Artifact validation:** estate enumerated (11 active artifacts, no broken manifest entries). Per-artifact payload queries **deliberately skipped this run** to avoid piling heavy read load on the DB during the active disk-IO saturation window (consistent with recent passes' judgment); `rpc_ops_snapshot` already validated the core health vector that `rpc-live-health` surfaces.
