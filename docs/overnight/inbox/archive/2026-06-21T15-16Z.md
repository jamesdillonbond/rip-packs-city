# Daytime monitor candidates — 2026-06-21T15:16Z (first tick of day, 08:16 PDT)

Run: rpc-daytime-monitor (first tick after the 01:03 PDT overnight pass, which shipped 0 / pushed 03cf862). Lock RELEASED. Platform GREEN except one self-detected security-posture drift (below). Trust 9/9, detect_stalled [], FMV reconciles EXACTLY (TS H+M 4250/16933, AllDay 878/6189, both improving vs overnight 4247/874), DB 5048 MB, Vercel 0 ERROR (prod c05435d READY — Trevor launch-polish, see post-ship watch).

---

## NEW CANDIDATE 1 — AUDIT-TABLE-RLS-20260621 (HIGH; security posture off 0/0/0/0 + platform smoke test red)

**Title:** 3 new `audit_topshot_*_20260621` remap/revert tables are RLS-off with anon write grants — harden them (ENABLE RLS + REVOKE anon/authenticated), mirroring `audit_20260605_harden_dq_scratch_tables_rls`.

**Tables (created by the 06-21 daytime CC mis-attribution remediation — the 9,336-sale / 2,842-moment re-key):**
- `audit_topshot_sale_remap_reverted_ambiguous_20260621`
- `audit_topshot_moment_misattrib_remap_20260621`
- `audit_topshot_sale_misattrib_remap_20260621`

**Source:**
- `check_public_security_invariants()` returns these 3 as `rls_off_base_table` + `anon_write_base_table` (was [] / 0/0/0/0 at the 01:03 PDT overnight, which ran BEFORE the daytime CC remediation created them).
- `information_schema.role_table_grants` anon/auth write-on-RLS-off count = **24** (3 tables × 8 grant rows).
- **Sentry `JAVASCRIPT-NEXTJS-1C`** "smoke test failed: public base tables: RLS on + no anon write" — LIVE + recurring (2 events, first 41 min ago, last 14 min ago, culprit `POST /api/smoke-test`). The platform's own hard smoke assertion is firing on exactly this drift. `check_secdef_anon_execute_violations()` = [] (clean).

**Risk read:** LOW blast radius (internal per-row audit/revert bookkeeping for the TS remap — no product reader, no PII), but a real posture violation by the platform's own invariant, and it keeps the security smoke test RED, which masks future genuine regressions. Same class as the already-closed DQ scratch tables (`audit_20260605_harden_dq_scratch_tables_rls`) and SEC1-EXP — both hardened autonomously by a night pass.

**Suggested action (night-pass-shippable, low-risk, matches prior autonomous ship):** `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` + `REVOKE INSERT,UPDATE,DELETE ON ... FROM anon, authenticated` for the 3 tables. service_role bypasses RLS, so the daytime CC revert path (service_role-driven) is unaffected. Clears NEXTJS-1C + restores 0/0/0/0. Verify: `check_public_security_invariants()` back to []; resolve NEXTJS-1C with regression-arming after one clean smoke tick.

---

## EXISTING ITEM — REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT (sharpening, NOT a new candidate)

Fresh data point that **revises the overnight's "cron disabled, operator must re-enable" read.** A NEW fire landed at **09:13Z 06-21** (AFTER the 08:03Z overnight) — ran **125s then failed "upstream request timeout"** (NOT the old 30s "canceling statement due to statement timeout" of the two 06-20 fails). So:
- The cron is firing again (not fully disabled), and the deployed fix (route maxDuration 240 + fn statement_timeout 200s, `3613a94`) addressed the 30s DB-statement cap but the route now dies on a ~120-125s **gateway** "upstream request timeout."
- The MV is **current anyway** (6778 rows, up from 6774 at the 06:06Z tick) — so the CONCURRENTLY refresh almost certainly COMPLETES server-side (~125s) while the route logs a gateway failure after the work finishes. Board is not stale/broken; LOW blast radius (niche latest-seen-captioned board).
- **Lever for CC/operator:** the route runs the CONCURRENTLY refresh synchronously and a gateway caps it ~120s before the route can log ok. Either move the refresh behind `after()` + 202 with its own end-of-run `log_pipeline_run` (so the gateway timeout stops converting a successful refresh into a logged failure), or confirm which gateway (cron-job.org 30s vs Vercel) is capping. The 3/3-ever-fail logging is cosmetic while the MV stays current — do NOT auto-revert the deployed fix.

---

## Post-ship watch (recent ships, all PASS — no regression, nothing to revert)
- **c05435d (Trevor, this morning) — "hide Fast Break, RTR, Rewards; remove Cart shell for launch polish":** prod READY, 0 ERROR across 20 recent deploys, no attributable new Sentry. Nav/UI-visibility + dead-shell removal only — no data/FMV/auth/pricing logic. Clean.
- **06-20/21 Stage B / Phase 3/4 conflation wave + F1-F3 remediation:** conflation guard = **44** (expected — the 06-21 daytime CC F1-F3 re-scope explicitly "honestly shows 44 (365d)", not the overnight's 27). All edition-keyed boards resolve: serial-premiums 181, perfect 10, underpriced 13, deals 680, squeeze 9102, top-sales 942, trophies 685, pack_ev_latest 1695, edition_offers 10275. FMV reconciles exactly. No leak.

## Health one-liner
security 3-RLS-off-audit-tables (CANDIDATE 1) · trust 9/9 · detect_stalled [] / alerts [1 INFO golazos] · sentinel UUID-leak 22/250 (raw 1390 = benign Stage-B `::` parallels) · pipeline 24h 9197/12 (0.13%, all transient/known) · FMV TS H+M 4250 AllDay 878 · editions TS 16933/AllDay 6191/Golazos 581/UFC 446 · DB 5048MB · Vercel 0 ERROR prod c05435d · Sentry 2 (NEXTJS-1C live=CANDIDATE 1; NEXTJS-A 18h FMV-smoke transient benign) · cross-collection step1+2 succeeded today, mats fresh, jobs active · 16 artifacts enumerated (12 estate + 4 known additions, 0 RETIRED); HTML on OneDrive outside this run's mount (MONITOR-ARTIFACT-ACCESS) so payload queries not run, but ALL backing objects validated GREEN.
