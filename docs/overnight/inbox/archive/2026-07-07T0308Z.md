# Daytime monitor candidates — 2026-07-07T03:08Z (~20:08 PDT, evening tick)

Health GREEN except ONE genuinely-recent pg_cron failure (shipped today, 100% failing). Security 0/0/0/0; trust 16/16 ok, breaches []; detect_stalled_pipelines() []; sentinel TS-UUID-48h 0; Vercel prod `f6ee2a9b` READY (no ERROR deploys); Sentry 0 new/12h.

## HIGH-PRIORITY (genuinely-recent pg_cron failure, new job shipped today)

### PACK-EV-HISTORICAL-BACKFILL-CRON-120s-TIMEOUT (pg_cron `rpc-backfill-historical-pack-ev`, jobid 43)
- **Source:** `check_pgcron_recent_failures()` — jobid 43 latest_status=failed, **4/4 runs in window failed**, each cancels at **exactly 120.0s** with `canceling statement due to statement timeout`. Runs 02:33 / 02:43 / 02:53 / 03:03Z (every 10 min, `3,13,23,33,43,53 * * * *`). First run 02:33Z = job creation → it has NEVER succeeded.
- **Context:** shipped today (CC commit `4eb8b0ab` "unstall the historical pack Contents+EV backfill", migration `20260707003000_*`; ledger line 19 records `audit_20260706_backfill_topshot_historical_pack_ev` + this cron). Job command is a pure pg_cron `SELECT public.backfill_topshot_historical_pack_ev(30);` (batch 30).
- **Diagnosed root cause (measured, read-only):** the function carries proconfig `statement_timeout=280s`, **but the run cancels at 120.0s, not 280s** → the proconfig raise is NOT in effect. This is the special-serial-owners-MV timeout class (CLAUDE.md 06-20/06-22): a top-level pg_cron statement arms `statement_timeout` (~120s, role/session default) at statement start, and a function-entry proconfig `SET statement_timeout` does NOT reschedule an already-armed top-level command. Batch 30 needs >120s, so every tick is cancelled mid-transaction → single-txn → **zero dists committed, ever.** The dev-time "Verified: dist 4184 → +$123.63" was a small manual call, not the batch-30 cron path.
- **Risk read:** LOW-MED blast radius. It's a NEW backfill that overrides `$0`-sentinel / no-EV historical TS pack dists with real Pack EV; while it fails those dists keep showing `$0`/no EV on the packs page — their pre-existing state, so no regression from a working surface, just a non-delivering new feature. BUT it reddens `check_pgcron_recent_failures()` every 10 min continuously and accomplishes nothing until fixed. Not user-facing-critical; not FMV/deal-board/security.
- **Suggested action (night pass / CC — proconfig alone is proven insufficient here):**
  (a) reduce batch so a run completes under the 120s cap (e.g. `SELECT public.backfill_topshot_historical_pack_ev(30)` → 8–10), OR
  (b) arm the timeout at the cron-command level (the reliable pattern): `cron.alter_job`/reschedule the command to `SET statement_timeout='280s'; SELECT public.backfill_topshot_historical_pack_ev(30);` so 280s is armed before the SELECT, OR
  (c) speed the per-dist compute.
  NOT for autonomous ship without a chosen approach + verification (it's pack-EV-adjacent + a cron-command change). Revert of the whole feature is in the ledger (`DROP FUNCTION backfill_topshot_historical_pack_ev(int); SELECT cron.unschedule('rpc-backfill-historical-pack-ev');`).

## Notes
- `pipeline_fails_24h` top entries (compute-topshot-pack-ev 10, fmv-recalc 10, topshot-buyer-backfill 5, analytics-smoke 4, etc.) are the known overnight-contention / DBSAT-timeout families; `detect_stalled_pipelines()` [] confirms their latest runs recover — not new, not flagged.
- `golazos_sales` / `ufc_sales` resolving_editions = benign INFO (1–2/24h), standing.
- Artifact estate: 14 active enumerated. Today's wave was additive (pack-EV pipelines, insider-signal detector summaries, onboarding/collection-page refactor) with no schema drops/renames that would break existing artifact payloads; not deep-run this evening tick (overnight pass validated the estate ~19h ago).
