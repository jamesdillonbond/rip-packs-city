# Daytime monitor inbox — 2026-07-24T15:14Z (~08:14 PDT, first-tick-of-day)

Health GREEN except ONE new candidate below. Trust 15/15 (0 breaches), `detect_stalled_pipelines()` [], `check_pgcron_recent_failures()` [], RLS-off base tables 0, anon-write holes 0, secdef-anon 0, sentinel_ts_uuid_48h 0. Cross-collection refresh fresh (cohort 04:10Z/154, overlap 04:25Z today — both steps succeeded). Vercel: latest BUILDING (57501a95 sales-IOPS perf), prior READY (Candy board 65879684 = prod tip), no ERROR (3 CANCELED = supersede-by-push). An active interactive Cowork/CC session is shipping live this hour (Candy productization + a sales-IOPS perf pass).

## CANDIDATE 1 (LOW/MED) — Candy insights views created `security_invoker=true` (not `=on`) → trip `check_public_security_invariants()` → recurring smoke-test security failure

- **Source:** `rpc_ops_snapshot().security.invariants` = 2x `view_unexpected_definer` (`candy_pack_ev_model`, `candy_secondary_board`). Sentry JAVASCRIPT-NEXTJS-1C "smoke test failed: public base tables: RLS on + no anon write" — endpoint `rpc:check_public_security_invariants`, extra `detail: "2 violation(s): view_unexpected_definer:candy_pack_ev_model, view_unexpected_definer:candy_secondary_board"`, `expected: zero-violations`, release 77fdb122, RECURRING (last event 15:01Z). Both views shipped TODAY in commit 65879684 (Candy productization); the 14:05Z overnight pass saw security 0/0/0/0 — so this is new-as-of-today.
- **Risk read — NOT a data leak.** Verified `has_table_privilege('anon',...,'SELECT') = false` and `('authenticated',...) = false` on BOTH; only `service_role` reads; both ARE invoker-mode (`reloptions = {security_invoker=true}`). The only defect is a literal mismatch: the invariant predicate (b) tests `NOT reloptions @> ARRAY['security_invoker=on']`, and the views carry `security_invoker=true` (`'true' != 'on'` as array-element strings), so they false-flag. Consequence: (1) the security smoke guard reds on every run and pages Sentry; (2) `rpc_ops_snapshot().security.invariants` is now permanently non-empty, which MASKS a future genuine definer-revert. Low real severity, but actively noisy + a monitoring-integrity dulling.
- **Exact precedent (known-safe recipe):** identical to the 07-18 Panini case — ledger migration `audit_20260718_panini_views_security_invoker_normalize_on` normalized `panini_pack_ev_model` + `panini_nation_board` from `=true` -> `=on`, returning the invariant to 0. The 26 OTHER `security_invoker=true` public views don't flag because they're in `security_definer_view_allowlist`; these 2 Candy views are neither `=on` nor allowlisted.
- **Suggested action (night pass — DB-only, posture-only, functionally identical):**
  `ALTER VIEW public.candy_pack_ev_model SET (security_invoker = on);`
  `ALTER VIEW public.candy_secondary_board SET (security_invoker = on);`
  then confirm `SELECT * FROM check_public_security_invariants();` = `[]` and the smoke security guard clears. Suggested migration name `audit_20260724_candy_views_security_invoker_normalize_on`. No view-definition change; both remain invoker. Revert: none needed (posture-only). Prefer normalize-to-`on` over allowlisting (matches the Panini decision and keeps the allowlist meaningful).
- **NOTE:** an interactive session is actively shipping this hour; Trevor/CC may fix it directly. If `check_public_security_invariants()` is already `[]` by tonight, this is a no-op CLOSE.

## Verified-not-candidates (post-ship-watch context)
- **snapshot-institutional-wallets** — the 4 fails/24h (09:25Z + 10:07Z) PRE-date the ~14:36Z index fix; `idx_topshot_insider_buybacks_buyer_moment` confirmed present in prod. Daily pipeline (1800-min watchlist, so not silent); next tick ~10:07Z 07-25 confirms ok=true. Stale pre-fix runs — do NOT alarm. (Night pass had queued this; the interactive index fix resolved it.)
- **pipeline_fails_24h** — Dune pair (sales-seller-recovery 18 / sales-ingest 8 = HTTP 402 datapoint cap, operator/billing DUNE-DATAPOINT-CAP-402, carried); sales-counterparty-backfill (12) is being actively remediated by the live 15:13Z sales-IOPS perf push (partial null-seller indexes); remainder self-recovering contention.
- **DB size** 10,879 MB (+75 vs overnight 10,804 @14:05Z, ~1h) — single data point, within churn; watch only.
