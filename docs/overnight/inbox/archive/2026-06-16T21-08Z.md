# Daytime monitor — late-afternoon tick (~14:08 PDT / 21:08Z, 2026-06-16)

PLATFORM GREEN. Read-only sweep (4th run today, after 15:14Z first-tick + 16:33Z weekly DQ + 18:17Z midday). Not first-tick → Section 1a skipped (trust health spot-checked anyway since the 9th leg is fresh — 9/9 ok). **2 NEW LOW candidates**, both monitoring-hygiene: a retired-indexer orphaned cursor now emits a permanent HIGH `get_pipeline_alerts()` false-positive, and `topshot-buyer-backfill` cron dropped ticks (174m silent, pipeline healthy/bursty). One coordination note: the 16:33Z DQ-sweep inbox file is mount-only (not on origin).

## Health snapshot
- pipeline_runs: 6h **1 fail** (`analytics-smoke` @16:43Z "1 fail(s)" — known DB-IO barometer, 17:13Z+ ticks ok; carried from the 18:17Z tick). `detect_stalled_pipelines()` → **`topshot-buyer-backfill`** (174m silent — see candidate). `get_pipeline_alerts()` → 2 entries: `topshot_listings` cursor_stalled HIGH (retired-indexer false-positive — see candidate) + `topshot-buyer-backfill` cron_silent medium (same pipeline).
- Security **0/0/0/0** (RLS-off base tables 0; anon/auth write on RLS-off base tables 0 with relkind r/p; `check_public_security_invariants` []; `check_secdef_anon_execute_violations` []). Sentinel TS-UUID-keyed-48h **0**.
- Trust health **9/9 ok** — incl. the new `unmapped_resolution_backlog_max` 8/100 (confirms the ALLDAY zero-write tripwire is in place — see verify+close). No breach; DQ4 `ts_uuid_dupes_created_24h` 0/200.
- FMV: not re-measured this tick (heavy latest-per-edition histogram; today's ships additive-only, `fmv_sanity_flags` 0, writers fresh). Carried from 18:17Z: TS HIGH+MED 3435 / AllDay 788 / Pinnacle 1263. editions flat (TS 15543 / AllDay 6191 / UFC 446). DB **4712 MB** (+10 vs 18:17Z, benign).
- Vercel: prod **`a41552a`** READY; 20 recent all READY/CANCELED, **0 ERROR**.
- Sentry: **1** unresolved (`JAVASCRIPT-NEXTJS-A`, smoke transient, 1 event ~17h ago, super_low) — **0 NEW in 12h**. Resolve after 24h quiet (~04:06Z 06-17).
- Artifacts: **19 enumerated (14 active + 5 RETIRED tombstones)**. Flagship `rpc-live-health` consolidated payload re-run (read from its real index.html): all 11 insights boards resolve (squeeze 501 / set_squeeze 239 / pack_reality 11 / rookies 61 / market 501 / first_mint 501 / cross_collection 1 / pinnacle_scarcity 501 / offer_spread 501 / deals 501 / trophies 501). `rpc-serial-premiums` board 271 + `topshot_perfect_mint_premiums_board` 10 (today's #1/Perfect toggle). The other 12 active were full-estate-validated at the 18:17Z tick; all of today's ships are additive (new columns/boards, no ALTER/DROP of any artifact-backing view), so nothing broke since. None broken, none repaired.

## Recent-ship health — 4 NEW Trevor ships since the 18:17Z tick, all prod READY / additive
- `116072ba` #1/Perfect-mint toggle on /insights/serial-premiums — reads the existing serial-premiums board + `topshot_perfect_mint_premiums_board` (10 rows, validated). Additive.
- `35e2c2d1` tshb throughput dial (ELAPSED_BUDGET 120→180s, EDITIONS_PER_TICK 40→80) on `topshot-sales-history-backfill` — throughput-only; commit notes worst-case ~265s clear of the 300s cap. GHA-driven; night pass: confirm no 300s hard-kill on its ticks.
- `1e06cdaf` 30d price-band badge Phase 2 on grid/wallet tiles — migration adds `price_band_30d` to `get_wallet_moments_with_fmv` (additive CASE-gated column, mirrors the validated 30d2acaf moment-detail version; ~4 banded rows / 100-row page). May surface in `rpc-my-wallet`; additive, no regression signal.
- `a41552a` retire dead `allday-unmapped-resolver` edge-fn source + `fireSupabaseEdgeFunction` helper — dead-code removal (0 callers; the live Supabase edge fn left as idle code, its pipeline_runs name reused by the new Vercel resolver). No runtime impact.

## NEW candidates (2, both LOW)
- **TOPSHOT-LISTINGS-CURSOR-FALSEPOS · LOW · night-pass / operator (monitoring hygiene).** Source: `get_pipeline_alerts()` now returns a permanent **HIGH** `cursor_stalled` for pipeline `topshot_listings` ("Cursor updated 21 days ago at block 152767514"). Confirmed it is the RETIRED TS listings-indexer: `event_cursor.id='topshot_listings'` last_processed_block 152767514, updated_at **2026-05-26 19:32Z** (= the documented 2026-05-26 listings-indexer retirement); no `topshot-listings-indexer` in pipeline_runs/24h (TS asks come from the live `topshot-listing-cache`, 6 runs/24h, last 19:26Z). The cursor just crossed `get_pipeline_alerts()`'s ~21-day staleness gate **today** (the 08:05 / 15:14 / 18:17Z ticks correctly saw `alerts []`), so it will now alert HIGH permanently and **mask a real TS cursor stall**. Same class as the 06-03 C-PAYER cleanup. Fix: park/delete the orphaned `topshot_listings` event_cursor row, or exclude retired pipelines from the cursor_stalled check (the 3 older parked pack-backfill cursors — `topshot_pack_purchases_primary_backfill` etc. — are already excluded). Risk LOW — removing a retired-pipeline cursor; there is no live reader (indexer retired). Not in the ledger.
- **BUYERBF-CRON-DROP · LOW · operator / watch.** `topshot-buyer-backfill` (the null-buyer drain engine, cron-job.org job **7776255** — "do NOT disable", focus 12d) is silent 174m (last run **18:14Z**; all 10 recent runs `ok=true`, dur ~90–322s). detect_stalled tripped its 90m watchlist threshold. Pipeline is healthy + bursty — it already gapped **3h20m (14:04→17:24Z) earlier today and self-recovered** — so this likely self-recovers, but today's two multi-hour gaps both exceed the 90m threshold (tuned 06-11 against a 50m max-gap), pointing to cron 7776255 dropping ticks (external-cron-drop class, like N1/TFP). Impact LOW: it walks the **2024** historical null-buyer backlog (cursor at 2024-04-07; the ~23.5k null-buyer in the 2026 partition is the known drain that auto-catches ~270/day on resume) — a pause slows the historical drain, no corruption / user / FMV impact. Action: night pass verify self-recovery; if still silent, operator check/re-fire cron 7776255; if multi-hour bursty gaps are now chronic, widen the watchlist threshold (e.g. 90→200m) to stop recurring detect_stalled noise.

## Verify + close (no new candidate)
- **ALLDAY-RESOLVER-ZERO-WRITE-TRIPWIRE (8am candidate) — CONFIRMED addressed.** The new 9th trust-health leg `unmapped_resolution_backlog_max` reads **8 / breach 100 / ok**; its `catches` text = "a collection's priced sales failing edition-resolution (marketplace-GQL WAF block / resolver stall) → sales undercount" — exactly the recurrence class. Close as-is (per the 18:17Z note, the only residual is that it's a backlog-SIZE tripwire, not a literal resolved-count/24h=0 throughput check; it catches the same failure once the backlog climbs >100).

## Already-logged today — do NOT re-log (carried in today's inbox files for the night pass)
SERIAL-PREMIUMS-INSIGHTS-QA (18:17Z); ALLDAY-V1-UNMAPPED-DRIFT narrowed to the 34 budget-exhausted fossils (15:14Z + 16:33Z); UFC-EDITIONS-SEED-GAP + TS-WMC-UUID-FOSSILS + OFFER-SANITY-VIEW-REFINEMENT (16:33Z DQ-sweep).

## Coordination note
- The **16:33Z weekly DQ-sweep inbox file** (`inbox/2026-06-16T16-33Z-data-quality-sweep.md`, written by the `rpc-data-quality-sweep` task) is on the **MOUNT only — not pushed to origin** (this fresh clone's inbox has only 15-17Z + 18-17Z + this file). Its candidates (UFC-EDITIONS-SEED-GAP etc.) won't reach the night pass if it drains only from its clone. **Night pass: also scan the mount inbox** (`/sessions/<s>/mnt/rip-packs-city/docs/overnight/inbox/`) so the DQ-sweep candidates aren't dropped.
