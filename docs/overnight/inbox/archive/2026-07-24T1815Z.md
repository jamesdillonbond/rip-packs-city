# Daytime monitor inbox — 2026-07-24T18:15Z (~11:15 PDT, mid-day tick)

Health GREEN. No skew (shell 18:06Z ≈ DB now() 18:06Z). Trust **15/15, 0 breaches**; `detect_stalled_pipelines()` = only `candy-listings-indexer` (info/new-today/non-user-facing); `check_pgcron_recent_failures()` `[]`; RLS-off base 0; anon-write holes 0; secdef-anon 0; sentinel_ts_uuid_48h 0. Cross-collection fresh (cohort 04:10Z/154, overlap 04:25Z — both ccm steps succeeded today). Vercel: latest READY `f4b18cc4` (merge tip); tip `200cdf96` CANCELED = expected docs-only ignoreCommand skip ([ledger-roll] context hygiene); **zero ERROR**. Sentry: 1 active recurring (JAVASCRIPT-NEXTJS-1C = Candidate 1) + 1 stale carried (player-detail pool timeout, 2 events, last 12h ago). DB 10,894 MB (+90 vs overnight 14:05Z 10,804, ~4h — normal). rpc-live-health re-validated clean (12/12 insights boards live, alerts + freshness current).

## CANDIDATE 1 (LOW/MED) — SUPERSEDES the 1514Z Candy-view candidate: scope GREW 2 → 11 views (fix must cover all 11, not the 2 originally listed)

- **Source:** `rpc_ops_snapshot().security.invariants` = **11** `view_unexpected_definer` (was 2 at 1514Z); Sentry JAVASCRIPT-NEXTJS-1C "smoke test failed: public base tables" — first seen ~15:06Z, RECURRING (last event ~17:59Z), 6 events / 5 users, culprit POST /api/smoke-test. The interactive session kept shipping Candy productization boards through the day, all created `security_invoker=true`.
- **The 11 flagging views (all `security_invoker=true`, NOT `=on`):** candy_deals_board, candy_holder_board, candy_listing_floor, candy_offer_spread_board, candy_pack_ev_model, candy_parallel_premium, candy_player_board, candy_scarcity_board, candy_secondary_board, candy_special_serials_board, candy_treasury_wallet. (candy_best_offers already carries `security_invoker=on` — leave it.)
- **Risk read — NOT a data leak (re-verified this tick):** `has_table_privilege('anon',v,'SELECT')` AND `('authenticated',v,'SELECT')` = **false** on ALL 11; only service_role reads. Same literal mismatch as the 07-18 Panini case — the invariant predicate tests `reloptions @> ARRAY['security_invoker=on']`; these carry `=true` so they false-flag. Consequence: the security smoke guard reds + pages Sentry on every run, and `rpc_ops_snapshot().security.invariants` is permanently non-empty → MASKS a future genuine definer-revert (monitoring-integrity dulling). Low real severity, actively noisy.
- **Suggested action (night pass — DB-only, posture-only, functionally identical):** normalize ALL 11 to `=on`. Use a dynamic sweep so no candy view is missed:
  `DO $$ DECLARE r record; BEGIN FOR r IN SELECT c.relname FROM pg_class c JOIN pg_namespace n ON n.oid=c.relnamespace WHERE n.nspname='public' AND c.relkind='v' AND c.relname LIKE 'candy_%' AND NOT (c.reloptions @> ARRAY['security_invoker=on']) LOOP EXECUTE format('ALTER VIEW public.%I SET (security_invoker = on)', r.relname); END LOOP; END $$;`
  then confirm `SELECT * FROM check_public_security_invariants();` = `[]` and the smoke security guard + Sentry JAVASCRIPT-NEXTJS-1C clear. Migration name `audit_20260724_candy_views_security_invoker_normalize_on`. No view-def change; all remain invoker. Revert: none needed (posture-only). Precedent: ledger `audit_20260718_panini_views_security_invoker_normalize_on`.
- **NOTE:** the 1514Z inbox's 2-ALTER recipe would leave 9 views still flagging — use the sweep above. If an interactive/CC session already normalized these by tonight, this is a no-op CLOSE.

## Post-ship watch (both 2026-07-24 overnight interactive fixes) — 1 PASS, 1 on-track
- **sales-counterparty-backfill — 6 null-seller partial indexes (~15:00Z, deploy 57501a95 READY): HOLDING / PASS.** Last failure 16:10Z ("claim failed: statement timeout"); every 5-min tick since (16:15Z → 18:10Z, 24 consecutive) ok=true, rows_written 8–113/tick. The 11 fails/24h in the snapshot all pre-date 16:15Z (pre-fix + index-build tail). Draining steadily; no revert warranted.
- **snapshot-institutional-wallets — `idx_topshot_insider_buybacks_buyer_moment` (~14:36Z): ON TRACK.** Daily pipeline (1800-min watchlist); the 4 fails/24h pre-date the fix; index confirmed present. No new fail; next tick ~10:07Z 07-25 confirms ok=true. Do NOT alarm on the stale pre-fix runs.

## Verified-not-candidates
- **candy-listings-indexer** (sole stalled_pipelines entry / sole new pipeline_alert): info-severity, added 2026-07-24 with the Candy board, last_run null (first Vercel `35 */3` tick pending). candy_mlb unpublished/gated → non-user-facing; clean no-op by design (ME listedCount 0 under quest-hold). Not an alarm.
- **Dune pair** (sales-seller-recovery-dune 18 / sales-ingest-dune 8): HTTP 402 datapoint cap = operator/billing DUNE-DATAPOINT-CAP-402, carried.
- **Vercel tip 200cdf96 CANCELED:** docs-only [ledger-roll] context-hygiene → ignoreCommand skip. Expected; prod correctly stays on f4b18cc4.
