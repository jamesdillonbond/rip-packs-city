# Daytime monitor inbox — 2026-06-04T15:13Z

Run: `rpc-daytime-monitor` (READ-ONLY sweep). Shared repo (no bot clone mounted). Lock was a STALE released marker (start 12:58Z, >45min) → took over, no concurrency skip.

Platform is **GREEN**. Health: security 0/0 base tables; `detect_stalled_pipelines()` = 1 (N1 only, already queued); FMV improving (TS HIGH+MED 1070 ↑ from 1062 baseline, NO_DATA 4041 ↓ from 4049; AllDay 271/523); sentinel TS-UUID-48h 20 (<250); unmapped 209; DB 5921 MB; 20/20 Vercel deploys READY 0 ERROR (2 fresh daytime ships landed clean); Sentry 2 known 1-event smoke issues, no new/spiking; 14/14 artifacts backing-queries validated live, none broken.

---

## NEW candidate — MON-WATCH: add two load-bearing writers to `pipeline_cadence_watchlist` (FMV/offers stall coverage)

- **Title:** Watchlist `offers-sweep` and `allday-fmv-populate` so `detect_stalled_pipelines()` can see a stall — both are absent today, same blind-spot class as MON1/Q10/P1.
- **Source:** `pipeline_cadence_watchlist` cross-referenced against the 6h active-pipeline set; cadence from `pipeline_runs` (48h).
- **Why they matter:**
  - `offers-sweep` — the GQL cron that is the `edition_offers.highest_offer` **authority** (per the 06-03 offers ship); backs the TS Best-Offer cell + the `/insights/offer-spread` board (5,570 rows live). If it silently stops firing, TS offer data goes stale with zero alert.
  - `allday-fmv-populate` — the **primary AllDay FMV writer** (`allday-gql-v1` marketplace pricing). Directly analogous to `fmv-recalc`, which MON1 just watchlisted last night (2026-06-04) precisely because a primary FMV writer stalling silently (the 2026-05-25 ~17h stall) is the failure this check exists to catch. (Partial backstop: fmv-recalc Step 5b re-evaluates AllDay within its sweep, so impact is degraded-not-zero — hence `medium`, not `high`.)
- **Premise (verified this run, 48h):** both are clean and regular — `offers-sweep` 140 runs / 0 fails / avg gap 21m / **max gap 40m** / 0 gaps >120m; `allday-fmv-populate` 136 / 0 / 21m / **40m** / 0. A 120m threshold = ~3× margin over observed max gap (the exact justification MON1 used for fmv-recalc) → catches a real multi-hour stall, won't false-positive on routine cron-rush skips (the Q9 lesson respected).
- **Risk read:** LOW. Additive, reversible, non-destructive monitoring-config INSERT — not route logic, not destructive, not off-limits. Same class the night pass has shipped repeatedly (Q10, P1, MON1). SHIP-eligible next in-window run.
- **Suggested action (ready migration):**
  `INSERT INTO public.pipeline_cadence_watchlist (pipeline, max_silent_minutes, severity, notes, is_active) VALUES`
  `('offers-sweep',120,'medium','GQL edition_offers.highest_offer authority (TS Best-offer + /insights/offer-spread). 21m cadence, 40m max gap/48h — 120m ~3x margin.',true),`
  `('allday-fmv-populate',120,'medium','Primary AllDay FMV writer (allday-gql-v1). fmv-recalc Step 5b partial backstop. 21m cadence, 40m max gap/48h.',true)`
  `ON CONFLICT (pipeline) DO NOTHING;`
  Revert: `DELETE FROM public.pipeline_cadence_watchlist WHERE pipeline IN ('offers-sweep','allday-fmv-populate');`
  Verify post-apply: `detect_stalled_pipelines()` does NOT newly list either (both ran <30m ago).

---

## Status notes on already-queued items (NOT new — fresh measurements for the morning reconcile)

- **R1 (hydrator FK) — persisting, unchanged class.** `topshot-moments-hydrator` 18 of 34 runs failed in 6h on `offers_moment_id_fkey` (`moments_write … violates FK`), last fail 14:42Z, still self-recovering (16 ok interspersed, last ok 15:02Z). No outage; FMV/insights unaffected. Steady ~3/hr, not accelerating. Ready Option-A fix (recreate FK `ON DELETE SET NULL`) still pending operator/CC (destructive ALTER off-limits to the passes). No re-log — context only.
- **N1 — still stalled (legitimate).** `snapshot-institutional-wallets` silent 1988m vs 1800m in `detect_stalled_pipelines()`; last run 2026-06-03 06:00Z (failed) + 06-04 06:00Z slot missed — external-cron-stopped pattern, self-recovers off-peak. Operator: re-fire cron, move off 06:00Z peak.
- **Two fresh daytime ships, both clean:** `9cfba65` (P1 audit batch — title brand dedup, login footer UFC, drop dead ts-listing-ingest) and `ff1e43d` (P2/P3 polish — PackTable tablet cards, sub-$1 deal relabel, branded edition placeholder, hide empty buybacks). Both READY (~14:50–15:00Z), frontend-only, no Sentry fallout. `ts-listing-ingest` will stop appearing in `pipeline_runs` now its workflow was deleted (K1) — expected, not a stall.
- **Sentry housekeeping:** NEXTJS-4 (smoke market-listings) + NEXTJS-J (smoke pack-listings) remain single-event, last seen ~04:00Z 06-04 (deploy-swap window), no recurrence → markable resolved after 24h quiet (~04:00Z 06-05).
