# Daytime monitor candidates — 2026-07-29 ~16:28 PT / 23:28Z

Source: rpc-daytime-monitor. Health otherwise GREEN (security 0/0/0/0, trust 23 metrics 0 breaches, pg_cron [], Sentry 0 new/24h, Vercel prod READY, DB 11,514 MB). One new candidate.

## LOW/medium — `topshot-active-listings-ingest` scheduler stopped firing ~22h
- **Source:** `detect_stalled_pipelines()` + `pipeline_runs`. Ran clean every 3h (all ok=true, no error rows) up to **2026-07-29 01:13:02Z**, then STOPPED — 7 consecutive expected ticks missed (04:13/07:13/10:13/13:13/16:13/19:13/22:13Z). Silent ~1,336 min vs its 900-min watchlist threshold; exceeds even the documented 7d max gap (758 min), so this is beyond the normal GHA dropout it's known for, not a transient skip.
- **Nature:** a GitHub Actions scheduled workflow (`topshot-active-listings-ingest.yml`) that stopped triggering — a scheduler dropout, NOT an ingest failure (zero error rows, last run was ok=true). Runs are firing at :13 despite the 2026-07-26 ledger stagger to `29 */3` — worth confirming the live cron string matches the workflow file while checking.
- **Risk read:** LOW. Severity medium = visibility-only, does not page; the underpriced-serials board has a read-fallback (`underpriced-serials-board.ts`) so the surface degrades gracefully. Primary sales/FMV pipelines all healthy. Precedent: TOPSHOT-ACTIVE-LISTINGS-ATLAS-BLOCK (ledger 2026-07-16) — the residential/Atlas ingest lane has dropped before and recovered on its own.
- **Suggested action (night pass / Trevor):** check the GHA workflow — is it disabled (Actions auto-disable, manual disable) or just delayed? Re-run `workflow_dispatch` to confirm the lane still works, and reconcile the schedule string (:13 vs the staggered :29). If it self-recovers on the next tick, close as a normal GHA dropout — do NOT auto-ship a fix.
