# Daytime monitor — 2026-07-01 ~06:08Z (2026-06-30 ~23:07 PDT, late-evening tick)

Run: rpc-daytime-monitor. **No clock skew** (shell 06:06Z == DB now() 06:07Z == app-stamped sales 06:03Z / fmv 06:06Z). Late tick, not the ~8am first-tick -> Section 1a daily extras skipped.

**Health GREEN.** security 0/0/0/0 (invariants/secdef-anon/rls-off/anon-write all []); trust **15/15 ok** (breaches []); `detect_stalled_pipelines()` []; `check_pgcron_recent_failures()` []; sentinel TS-UUID-48h **17** (inert DQ4, < warn 250); editions FLAT (TS 17,489 / AllDay 6,191 / Golazos 581 / UFC 518); FMV TS H+M **4,675** / AllDay 900; fmv_sanity 0; `unmapped_resolution_backlog_max` **29** (Flowty drain holding, well below 100); DB **6,982 MB** (-191 vs the 00:14Z tick = benign prune/vacuum). Prod **705fb202 READY** (active daytime perf wave: bundle insight-links into one RPC / drop dead special_serials query / stream player top-sales via Suspense — all directly attacking the connection-pool-saturation class). No deploy in ERROR; the CANCELED deploys are docs/monitor/ledger commits correctly caught by the ignoreCommand.

**Artifact estate — 11 shared backing objects all resolve + sensible** (validates the merged data layer the dashboards read): ad_ev_corrected 2331, ad_pack_market 1164, ad_realized_ev 302 (filling from 147), ts_pack_market 1770, squeeze 6351, deals_vs_fmv 593, xcoll_deals 824, first_mint 628, underpriced_serials 7, xcoll_cohort_mat 153 (fresh), cached_listings_v2 95,550. No schema-break from today's perf wave.

## NOT a new candidate — SHARPENING the already-queued CLASSIFY-ACQ-ALLDAY-STATEMENT-TIMEOUT (inbox 2026-07-01T001716Z, night-count 2). Do NOT double-count.

- **12-tick history resolves the 00:14Z open disambiguation (contention-collateral vs volume-creep) -> volume-creep / structurally-marginal AllDay leg, NOT pure contention.** The `nfl_all_day` leg of `classify-acquisitions-multicollection` now runs **68-95s on PASSING ticks** (05:06Z 74.6s, 04:06Z 75.4s, 03:06Z 68.7s) -> it is hugging its ~95s statement_timeout and tips over on any added load: FAIL at 22:06Z (**185.1s**), 00:06Z, 01:06Z, 02:06Z, 06:06Z (95.5s); ok on the rest. Still recurring 6h after first log (latest fail 2026-07-01T06:06:14Z). ~5 of the last 12 ticks fail; not all inside dense micro-clusters (06:06Z was relatively isolated). Pipeline still firing on cadence (`detect_stalled_pipelines()` []). Likely driver: AllDay `sales` volume grew materially via the studio-history backfill (~124k+ rows), pushing the leg's per-run scan past the cap.
- **Refined fix for the night pass:** this now leans to a **low-risk DB-only lever** — `ALTER FUNCTION` to bump the classify AllDay-leg `statement_timeout` (same class/shape as the 2026-06-30 serial-fmv `statement_timeout -> 600s` fix), and/or add/verify an index on the AllDay classify predicate — rather than "close as DBSAT contention-collateral." Blast radius LOW (acquisition classification; NOT FMV / deal boards / pack-EV). No user-facing impact. Route logic itself is off-limits to autonomous shipping, but a pure `statement_timeout` ALTER on the classify fn is in the low-risk DB-only class.
