# Daytime monitor inbox — 2026-06-21 ~00:09Z (2026-06-20 17:09 PDT tick)

Platform **GREEN**. security 0/0/0/0 (RLS-off base [], anon/auth-write-on-RLS-off [], `check_public_security_invariants()` [], `check_secdef_anon_execute_violations()` []); `detect_stalled_pipelines()` []; `get_pipeline_alerts()` = 1 INFO only (`golazos_sales` resolving_editions, 1/24h — long-standing, not a finding); **trust-health 9/9 ok**; sentinel TS-UUID-48h 22 (ok); Sentry **1 unresolved** (transient, Note D); Vercel **0 ERROR**, prod `592e725` READY. Editions reconcile EXACTLY (TS 16,933 = 15,543 baseline + 1,390 `::` parallels; AllDay 6,191 / Golazos 581 / UFC 446 flat). FMV improving as Stage B intends — **TS HIGH+MED 4,231** (HIGH 1,201 + MED 3,030; night baseline 3,332 -> 4,159 -> 4,227 -> 4,231), ASK_ONLY 1,815, NO_DATA 3,380; AllDay HIGH+MED 874. FMV reconciles exactly to the 16,933 / 6,191 edition counts. `fmv-recalc` 104 ok / 0 fail / 24h, last 00:08Z. All 10 artifact backing objects return rows (deals_board 697, serial_premiums 182, perfect_mint 11, squeeze 9,102, trophies 683, top_sales 888, tracked_fmv 21, offer_sanity 242, underpriced_serials 12, special_serial_mv 6,774). DB 5,002 MB (+103/~1.5d vs 4,899 night baseline — benign).

This was a quiet post-ship-watch tick over today's very heavy Stage B / Phase 3 / Phase 4 CC wave. **No new incident.** The one item below is a RECONCILIATION that supersedes/closes the prior two ticks' candidates (the fix they asked for landed AFTER the 21:18Z tick), plus one carried status update and positives.

---

## RECONCILE / CLOSE — `refresh-conflated-editions` DELETE-without-WHERE bug is FIXED + VERIFIED live (supersedes 18:18Z CANDIDATE 1 + 21:18Z CANDIDATE A)

**What changed since the last tick:** migration **`20260620213921`** (`audit_20260620_refresh_conflated_editions_safe_delete`) landed at **21:39Z — ~21 min AFTER the 21:18Z monitor tick**, which is why that tick still read "1-line WHERE/TRUNCATE fix still needed." This is the first monitor pass to see it in place.

**Verified live this tick** (via `pg_get_functiondef`): `public.refresh_topshot_conflated_editions()` now contains a qualified `DELETE ... WHERE` (`has_where = true`) so it no longer errors under the route's `sql_safe_updates=on` context. The durable remap fn `remap_topshot_base_keyed_parallel_sales()` (`audit_20260620_remap_base_keyed_parallel_sales_fn`) is present and, per CLAUDE.md + commit `592e725`, is wired at the **route** level to run before the guard refresh (so `calls_remap=false` *inside* the fn is expected, not a gap). Guard table `topshot_conflated_editions` is at **27 rows** = the converged Phase-4 state (CLAUDE.md: conflation 46->27, `remaining_remappable_strands=0`). The acute 741-row over-suppression symptom is fully gone (deal board 697).

**Disposition:** the two prior inbox candidates (18:18Z #1, 21:18Z A) are RESOLVED-by-fix — do NOT re-ship a WHERE/TRUNCATE fix, it's already live. **Residual = operator-only:** the daily `refresh-conflated-editions` cron-job.org entry still isn't wired (the only route fire ever was the 15:17Z pre-fix failure; no run since). CLAUDE.md already lists "wire the daily refresh-conflated-editions cron" as an operator item. Until it's wired, conflation will slowly re-drift from 27 as new parallel sales occasionally land on a Standard row before its `::` row exists (the forward-keying + remap converge it once the cron runs). **Suggested follow-on (after the operator wires the cron + one ok run is observed):** add `refresh-conflated-editions` to `pipeline_cadence_watchlist` (generous threshold) so a future failure/silence is visible to `detect_stalled_pipelines()` — it is NOT watchlisted today. Premature to watchlist before the cron exists. No code/migration action needed from the night pass right now beyond reconciling the ledger.

---

## STATUS (carried, no new action) — `refresh-special-serial-owners-mv` timeout: fix DEPLOYED, still NO post-fix automated run

Already queued as **REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT** (night-count 2) + 21:18Z NOTE B. Update only: CC's fix is live (`3613a94` route `maxDuration` 120->240 + migration `audit_20260620_special_serial_owners_mv_refresh_timeout_200s` fn 180->200s, prod READY). The MV **data is current** (6,774 rows incl. `::` parallels — note it churns 5,929->7,307->6,774 across the day as CC re-refreshes it via MCP after each Phase-3a circulation backfill; that's expected dial-in churn, not a regression). BUT the **cron path is still unverified**: last `refresh-special-serial-owners-mv` pipeline_runs entry is **02:16Z (pre-fix, statement-timeout fail), ~22h ago, none since**, and the pipeline is **not on `pipeline_cadence_watchlist`** (so its silence is invisible to `detect_stalled`). The night-pass watchlisting gate (one post-fix ok tick) is NOT yet met — keep watching for the first post-fix automated fire to log ok=true, then add the watchlist row.

---

## NOTE — positives confirmed this tick (post-ship watch on today's wave; 0 reverts warranted)

- **Today's heavy CC wave is healthy end-to-end.** New/affected pipelines all firing clean in the last 24-26h: `topshot-sales-history-backfill` (the `592e725` subedition-aware + durable-remap conflation-leak fix) **12 ok / 0 fail**, last 22:53Z; `topshot-subedition-circulation-backfill` (Phase 3a daily Vercel cron) **3 ok / 0 fail**, last 22:05Z; `topshot-buyer-backfill` **51 ok / 0 fail** (BUYERBF-PERINVOCATION-WORK holding, no creep); `fmv-recalc` **104 ok / 0 fail**.
- **Stage B convergence is healthy** — editions math (+16 `::` since 21:18Z = CC's 16 residual `::` catalog), FMV de-blending, improving HIGH+MED (4,231) all consistent with the intended outcome; `::` parallels reprice off canonical `fmv-recalc`.
- **21:18Z transients cleared:** `offer_edition_gap_max_usd` back to $0/ok (the LeBron 57:1909 chain>gql $250 spike was transient as predicted); Phase 3a circulation backfill firing on schedule.

## NOTE — knowns / no action

- **Sentry `JAVASCRIPT-NEXTJS-A`** ("smoke test failed: fmv pipeline healthy / analytics_pipeline_health", `POST /api/smoke-test`) — 1 event ~21:08Z, Seer super_low, 0 new in 12h. Same recurring smoke false-alarm class; FMV verified fresh (fmv-recalc 00:08Z, 0 fails/24h, `fmv_sanity_flags` 0, editions reconcile). Resolve after 24h quiet; watch only if it recurs across consecutive ticks.
- **pipeline_runs fails (6h):** `evm-transfers-ingest` x1 (Base 429 — benign STEER item, don't chase). Nothing else.
- **`ts_uuid_dupes_created_24h` = 22/200 (ok):** known inert DQ4 dupe-writer leak (TS-WMC-UUID-FOSSILS/DUPE1); editions net-grew only by `::` parallels, not UUID dupes. No action.
- **Artifact estate = 16** (4 beyond the 06-12 list, already noted prior ticks: `rpc-serial-premiums`, `rpc-fmv-dialin`, `rpc-ts-data-mission`, `pack-drops-ev-check`). Validation this tick was a consolidated backing-object sweep (all 10 return rows, no schema break from today's add-column / Stage B / Phase 3 migrations), not per-artifact payload execution.
