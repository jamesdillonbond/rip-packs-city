# Daytime monitor inbox — 2026-06-25T14:05Z (first tick of day, ~07:05 PDT)

Platform GREEN except the one OWNED + escalating BREACH below. No clock skew this run (DB now 14:00Z == app stamps; shell == DB). Health one-liner: detect_stalled [] · pgcron [] · security 0/0/0 (invariants/secdef/RLS-off-tables all clean) · pipeline fails 0 in last 6h (7/24h all transient: 19:01-19:33Z micro-contention cluster wmc-fmv-populate/alerts-dispatch/check-alerts/pack-ev + refresh-pack-grail-mv Cloudflare blip + topshot-moments-hydrator GetMintedMoment GQL) · sentinel TS-UUID-48h 0 · editions FLAT (TS 17435 / AllDay 6191 / Golazos 581 / UFC 518) · FMV TS H+M 4535 improving / AllDay 908 flat / Pinnacle 2169 (328 HIGH / 593 MED / 0 NO_DATA), all reconcile to edition counts, fmv_sanity 0 · DB 5434 MB (+120 benign, deep-history sales backfill) · cross-collection mats fresh (cohort 04:10Z/158 rows, overlap 04:25Z, rpc-ccm-step1+step2 active+succeeded) · Vercel 0 ERROR, prod 11c8a23e READY (Trevor's deep-history studio-platform program shipped today: AllDay/Golazos/Pinnacle studio sales-history backfills — additive, edition_id-keyed, NO unmapped writes) · Sentry 3 stable single-event smoke transients (NEXTJS-1C/W/X), 0 new/0 spiking · artifacts: rpc-live-health full consolidated payload validates (11 insights boards return rows: squeeze 501 / set_squeeze 239 / pack_reality_top_ev 7 [known-thin] / rookies 61 / market 501 / first_mint 501 / cross_collection 1 / pinnacle_scarcity 501 / offer_spread 501 / deals 501 / trophies 501; freshness FMV 13:58Z / PackEV 14:01Z / EdOffers 14:02Z), v_tracked_wallet_fmv_confidence 25 (the 3x-historically-fragile view resolves), v_offer_sanity_flags 345.

## Candidates

### 1. [ESCALATION of queued HISTORY-BACKFILL-UNMAPPED-SPIKE — NOT a new item; status correction] TS unmapped backlog climbing monotonically; the 06-25T03-16Z "plateaued" call was wrong
- **Source:** `v_rpc_trust_health.unmapped_resolution_backlog_max` = **2370** (BREACH, breach_at 100). `unmapped_sales` TS open = 2370, all `marketplace='flowty'`, newest hint `{"backfill":"topshot_flowty_history","sale_source":"v2_flowty"}` — exact same class as the 06-24 finding.
- **Trajectory (disproves plateau):** 06-24 night pass **725** -> 06-25T01:06Z monitor **1338** -> 03:16Z monitor logged "1338 plateaued" -> **14:00Z now 2370** (+1032 since 03:16Z ~= +94/hr; +603 in the last 6h; +1648/24h). **NOT plateaued — monotonic climb.** TS rows RESOLVED in last 6h = **0** (the resolver has no working path for this class; confirms 06-24 "only 3 resolved"). Inflow is bursty (0 new in the last 1h = currently between the `topshot-flowty-sales-history-backfill` #3 `*/3h` ticks). Conflation co-creeping same root: 63 (06-24) -> 97 (01:06Z) -> **105** now.
- **Risk read:** LOW user-facing / MED hygiene. NOT corruption — rows quarantined OUT of `sales` (06-24 framing holds; FMV/insights/sanity unaffected, fmv_sanity 0). But it is unbounded-growing and keeps `unmapped_resolution_backlog_max` a standing red, which masks any genuinely-new unmapped issue on another collection. Off-limits for the monitor + night pass auto-ship (sales-path route logic + hot file + deliberately-shipped founder feature) -> log, do not action.
- **Suggested action (CC/Trevor — same three options as 06-24, now with stronger evidence the "transient" prediction failed twice):** (a) a drain/resolver for backfill-era `topshot_flowty_history` rows; OR (b) a retire mechanism (mirror the AllDay `flowty_no_edition_id` retire class); OR (c) pace/throttle the `topshot-flowty-sales-history-backfill` (#3) `*/3h` cron so inflow <= drain. NOTE: the studio-platform backfills Trevor shipped today (AllDay/Golazos/Pinnacle, edition_id == editions.external_id) correctly write ZERO unmapped_sales — only the on-chain Flowty #3 backfill carries this leak, so the fix is scoped to that one route/cron.

## Benign / confirmed-not-an-incident (do NOT chase / re-queue)
- **`offer_edition_gap_max_usd` = 192 (trust BREACH, breach_at 50):** offers-sweep is HEALTHY + fresh (72/0 ok in 24h, last run 13:42Z) -> NOT a stalled writer. Known oscillating OFFER-SANITY-class metric (0 on 06-14, 132 baseline 06-07); a single/few edition-grain on-chain offer not surfaced in `edition_offers`. Already carried as OFFER-SANITY-VIEW-REFINEMENT. No action.
- **7 pipeline fails/24h** all transient (the recurring 19:01-19:33Z lock/statement-timeout micro-contention cluster + 2 external GQL/Cloudflare blips); 0 in last 6h. Not a finding.
