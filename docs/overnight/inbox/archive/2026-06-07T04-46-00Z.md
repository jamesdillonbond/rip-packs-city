# Daytime monitor — 2026-06-07T04:46Z (21:46 PDT 06-06)

Read-only sweep. Platform healthy overall: security 0/0 (+ secdef-anon-execute violations = empty), 20/20 prod deploys READY (head `a96c2a2`), sales ingestion fresh (TS 4,174 / AllDay 247 in 24h, both minutes old), P1-CAD fix HOLDING (pinnacle-metadata-backfill hourly :22 ok=true through 04:22Z), Sentry quiet (2 unresolved, both known: NEXTJS-4 own 24h clock, no events since ~20:40Z; NEXTJS-15 = PIN1). All 16 artifacts validated OK (incl. re-keyed `pinnacle_scarcity_board` 501+ rows, `v_tracked_wallet_fmv_confidence` completes post-AF1-v2, insights-health bounded legs, rewards/offers/wallet/cross-collection sources alive).

## Candidates

### 1. ALLDAY-FMV-STALL (NEW · MED · operator) — `allday-fmv-populate` external cron stopped firing after 02:22Z
- Source: `detect_stalled_pipelines()` → silent 138m vs 120m threshold (watchlisted @120m by MON-WATCH 06-05); `pipeline_runs` shows clockwork :02/:22/:42 ticks all ok=true through 02:22:10Z 06-07, then nothing (7+ missed ticks by 04:46Z).
- Risk read: route/edge fn is NOT failing (0 fails over its full history this window) — the external trigger stopped. Same stopped-cron class as Q3 (TS sales-indexer 05-31) and N1. Sales-path AllDay FMV unaffected (fmv-recalc wrote AllDay snapshots 04:28Z); what stales is the marketplace-GQL leg (averageSale LOW + lowestPrice ASK_ONLY refresh).
- Suggested action: operator re-fire/re-enable the cron-job.org entry — plausibly collateral from the CRON-30S re-enable sweep of cron entries earlier tonight (check whether the entry got disabled/edited ~02:2xZ). Night pass: do NOT code around it; if still silent at next run, escalate to HIGH.

### 2. PACKEV-BUDGET-2 (UPDATE, already queued for CC — new datapoints only)
- `compute-topshot-pack-ev` 11 more `time_budget_exceeded_after_fetch` fails in the last 6h (latest 04:38Z) — wedge ongoing per the 02:15Z characterization (head-of-line batch of 8).
- Staleness trend: stale-24h on `topshot_pack_ev_targets` 547 (00:50Z) → 563 (01:55Z) → **611 (04:42Z)** of 800; stale-48h 221 → **266**. ~17/hr compounding, matches the wedge model. No new action — reinforces the queued CC fix (per-pack fetch timeout + skip-and-sentinel-advance preferred).

### 3. N1 RECONCILE (ledger update) — `snapshot-institutional-wallets` RECOVERED
- Ran ok=true 2026-06-07T02:14:32Z (first success since 06-05 01:21Z; 3rd stall cleared — presumably re-fired during the evening session). `detect_stalled_pipelines()` no longer lists it.
- Keep the standing recommendation: move its cron slot off the 06:00Z rush so it stops re-stalling.

### 4. TFP-RUSH (datapoint, already queued · operator) — `topshot-fmv-populate` now 0-for-2 post-after()-fix
- 00:00:49Z 06-07 tick logged (CRON-30S logging works) but failed again on the :00 rush: `sets read failed: Timed out acquiring connection from connection pool`. Last SUCCESS still 06-05 18:00Z → TS marketplace-FMV sweep ~35h stale and counting.
- Reinforces the queued operator action: move the slot off :00 (e.g. :40). Watchlist gate (first ok=true) still not met. Next natural tick 06:00Z — lands on the rush again.

### 5. STALE-LOCK (LOW · hygiene, night pass) — `docs/overnight/.lock` left behind
- mtime 2026-06-06 08:41Z (~20h old; matches the last night-pass end). Monitors treat <45m as fresh so it blocks nothing, but the night pass should clear it on its next run so a future fresh-looking collision check isn't confused.

## Drift vs metrics-latest.json (baseline 06-06 08:30Z)

- TS FMV: HIGH+MED 3041 → **3002** (−39, churn-range; LOW 5325 → 5788, NO_DATA 5444 → 5524 — DUPE1 inert tail + cold-tail churn, not a pricing regression signal yet; watch next run).
- AllDay FMV: HIGH+MED 490 → **497** (improving); ASK_ONLY 82 → 115.
- Sentinel TS-UUID-48h: 5840 → **5254** — still CRITICAL (≥2000) but decelerating; DUPE1/DUPE1-MIT known, no new action.
- unmapped_sales open: 162 → **183** (+21; resolver had one 00:04Z timeout fail — transient class).
- DB size: 6257 → **6378 MB**. Editions/pipelines otherwise consistent with baseline notes.

## Recent-ship health (last 24h, all green)

- 11 prod deploys since the daytime pass (PIN-FMV-REKEY waves 1a/1b/2 + Phase A, PACKVIZ, PIN-SYNC-OBS/FLOWTY, CRON-30S, SMOKE-RETRY, deals/digest fixes) — every one READY, no attributable Sentry, no pipeline regression. P1-CAD confirmed fixed (no `cadence: Invalid character` since 17:22Z 06-06).
- Wave-1b `pinnacle_scarcity_board` DROP+CREATE re-key: view alive (501-cap hit), backing the public /insights page — no breakage seen.
- Wave-3 health-view re-points + 5 orphan fn drops: no errors observed in any validated artifact or sweep query.
