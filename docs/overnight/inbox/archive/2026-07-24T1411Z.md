# RPC daytime monitor — candidate work

Run: 2026-07-24T14:11Z (first tick of day, ~07:05 PDT). Health GREEN overall (security 0/0/0/0, trust 15/15 ok, stalled_pipelines [], pg_cron [], sentinel 0, Vercel prod 99f3bd9f READY, cross-collection refresh fresh). One real degradation + one LOW watch below. Nothing else new — the Dune cluster remains the carried DUNE-DATAPOINT-CAP-402 (owner decision), the sales-counterparty-backfill / wallet-username-resolver timeouts are the documented contention class, and candy_mlb now showing ~53 sales/24h is the expected Candy price-signal arriving (not an anomaly).

## MED — SNAPSHOT-INSTITUTIONAL-WALLETS-COMMUNITY-DIFF-TIMEOUT (new)
- Source: pipeline_runs `snapshot-institutional-wallets` ok=false; extra.op_label=`diff_95f28a17`, wallet_hint `0x4d2c9216f1dca098` (NBATopShotCommunity), op_attempts 3, partial:true. 8 fails over 07-23 + 07-24, 0 successes on either day (last clean 07-22, 2 ok runs). Each run ~149s.
- What: the run snapshots its other wallets fine (63,596 moments, 1,268 buybacks inserted this run) but the single NBATopShotCommunity mega-wallet's TopShot holdings diff (`diff_95f28a17`) cancels on the statement timeout after 3 retries, marking every run partial/ok=false. That one institutional wallet's TS snapshot has been stale since 07-22.
- Risk read: LOW blast radius — scoped to ONE analytics/leaderboard wallet snapshot; other wallets + buyback inserts unaffected; not a user-facing surface. But it is a persistent 2-day complete-failure of the run ok flag with growing staleness on that wallet, so it will not self-heal.
- Suggested action (night pass / CC / owner — NOT auto-ship): make the `diff_95f28a17` step cheaper for very large wallets (covering index on the diff's join key, or page the diff), NOT a proconfig statement_timeout bump — the 3 retries already hit the ceiling and the documented lesson is proconfig does not extend the service_role/PostgREST ceiling. Also worth confirming whether any other mega-wallets sit near the same edge.

## LOW — SIGNUP-FUNNEL-EVENTS-ZERO-POST-DEPLOY (watch refresh; first raised 07-21 0315Z monitor)
- Source: funnel_events by_type 7d = {home_view 24, collection_view 102, insights_view 29, share_view 3, wallet_paste 2}; the 3 signup-funnel events shipped 07-20 (signin_click, account_created, email_capture_submitted) = 0 rows across 7d. user_profiles flat at 20, email_subscribers 0.
- Risk read: benign-most-likely — 24 home_views / 3 share_views over 7d is thin traffic, so genuinely 0 conversion clicks is fully consistent with the "20 users, 0 signups" reality. BUT it is now ~4 days of post-deploy home traffic with 0 of ALL three new events, so a one-time wiring confirmation is warranted (the funnel_events CHECK already whitelists all three, so any absence is client/route wiring, not the DB).
- Suggested action: night pass or CC does a one-time check that `signin_click` actually fires from the HomePageMarketing.tsx header/pricing CTAs (cheap client read). If wired correctly, close as "genuinely 0 clicks — a traffic problem, not an instrumentation one."
