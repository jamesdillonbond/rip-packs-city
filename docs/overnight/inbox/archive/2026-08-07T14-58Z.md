# Daytime monitor candidates — 2026-08-07T14:58Z (~07:58 PDT, first tick of day)

First tick — ran 1a extras. Overall **green-with-known-noise, plus two freshness-stall candidates below**. Security fully clean (all 4 `rpc_ops_snapshot` invariant arrays `[]`). `check_pgcron_recent_failures()` = `[]` (the 08-06 `rpc-pipeline-runs-daily-rollup` `jsonb_object_keys-on-array` failure has CLEARED — either the `diag-step6-cohort` array-`extra` row aged out of the 73h window or `rollup_pipeline_runs()` was hardened; the 08-06 HIGH candidate no longer reproduces). `detect_stalled_pipelines()` = only the two info-level stalls (candy-offers-indexer, panini-ingest — both below). Sentry: **0 new issues in 12h** (the 08-06 entity-page saturation cluster has NOT re-spiked). Board-liveness RECOVERED: `public_board_slow_count`/`public_board_empty_count` both **0** this tick (vs the 999/999 budget-exhaustion pair on 08-06 — that was scraper-incident collateral, now eased). Vercel: an active CC session is mid-deploy (`401624ca` BUILDING, NaN-guard route fixes); all prior deploys READY, **no ERROR-state deploy**. DB 12,368 MB (+173 vs 08-06's 12,195).

**1a extras — both clean.**
- Trust health: 4 BREACH rows, all covered below (3 documented do-not-re-flag + 1 new panini).
- Cross-collection pg_cron refresh: `cross_collection_cohort_mat` fresh 04:10Z (n=174), `cross_collection_ts_set_overlap_mat` fresh 04:25Z; `rpc-ccm-step1`/`step2` both active + `succeeded` today. ✓

---

## MEDIUM — Panini board (PUBLIC since 08-01) is serving stale FMV: `panini_fmv_stale_hours`=37.6 BREACH, home-box `panini-ingest` runner dark ~37.6h

- **Source:** `v_rpc_trust_health` — `panini_fmv_stale_hours`=37.6 (breach_at 36), NEW this tick (was OK on 08-06). `detect_stalled_pipelines()` shows `panini-ingest` silent 2257 min (~37.6h), last run 2026-08-06 01:21Z. The two move together — the residential home-box runner (5th scheduler, fires every 4h on 01/05/09/13/17/21 UTC) has not fired since 08-06 01:21Z, so `panini_editions`/`_fmv_snapshots` have gone stale and the FMV-freshness metric crossed its threshold. This is the known "home-box-sleep" failure mode (roadmap 07-26: box went dark 12.6h on 07-23).
- **Why it's now a candidate, not just noise:** `PANINI_PUBLIC` flipped `true` on 2026-08-01, so `/insights/panini-squeeze` is a PUBLIC, indexable surface. `panini_fmv_stale_hours` is now a **user-facing freshness metric**, not a pre-launch nicety. The board's own rotation-age disclosure banner (`oldest_family_refresh_h`) will honestly widen, but the underlying cause is the runner being dark. **Also:** the `panini-ingest` watchlist row is STILL `severity=info`, and its own notes say verbatim *"RAISE TO medium/high AT PANINI GO-LIVE alongside the PANINI_PUBLIC flip"* — go-live was 6 days ago and the severity was never raised, so a genuine board-freshness stall currently pages only at `info`.
- **Risk read:** LOW-risk, two-part. (a) **Operator (Trevor):** the home box needs to be awake/online — this is his machine, not a code defect, so it's a nudge not a night-pass ship. (b) **Night pass (config-only, safe):** raise `pipeline_cadence_watchlist.severity` for `panini-ingest` from `info` → `medium` (or `high`) now that the board is public, per its own note. `UPDATE public.pipeline_cadence_watchlist SET severity='medium' WHERE pipeline='panini-ingest';` Revert: set back to `info`. No FMV/ingest/auth/wallet surface touched.
- **Note:** distinct from `panini_sale_price_capture_dry_days`=9 (documented upstream sale-price-capture outage, separate mechanism, needs a live A/B on the runner box — interactive, do-not-re-flag).

---

## LOW — `candy-offers-indexer` Vercel cron silent ~62h (last run 08-05 00:50Z), and it may now be user-facing via the public `/insights/candy-mlb` Spread tab

- **Source:** `detect_stalled_pipelines()` — `candy-offers-indexer` silent 3728 min (~62.1h), `max_silent_minutes`=800, severity `info`. Last `pipeline_runs` row 2026-08-05 00:50Z. It is a **Vercel cron** (`50 */6 * * *`), not pg_cron (confirmed: no `cron.job` row matches), so it should have fired ~10 times since — the cron is simply not executing. Flagged as "known info" on both 08-05 and 08-06, but never resolved and the stall keeps growing.
- **New nuance not previously logged:** the watchlist note asserts *"candy_mlb is unpublished (no route dirs) so a stall is not user-facing"* — but per CLAUDE.md the public board `/insights/candy-mlb` went LIVE 2026-07-31 with a **Spread** tab backed by `candy_offer_spread_board` / `candy_best_offers`, which the offers indexer feeds. So a 62h offers stall likely means the Spread tab's best-offer/bid data is ~62h stale on a **public** surface. Worth re-checking whether the note's "not user-facing" premise still holds post-go-live. (Ask/FMV/sales legs are fine: `candy_ask_book_drop_pct`=3.7, `candy_sales_stale_hours`=3.3, `candy_fmv_stale_hours`=0 — only the bid/offers leg is dark.)
- **Risk read:** LOW. Bid best-offer is explicitly a signal, never FMV (`fmv_snapshots` untouched). But a Vercel cron that has silently stopped firing for 62h is worth an operator look — either the cron entry was dropped from `vercel.json`, or the route is erroring pre-log. Suggested night-pass/operator action: confirm the `50 */6 * * *` entry still exists in `vercel.json` and hit the route once with `INGEST_SECRET_TOKEN` to see whether it 200s or errors; if the Spread tab is genuinely user-facing now, raise the watchlist severity off `info`.

---

## Known do-not-re-flag / continuity (no new candidate)

- **`ufc_fmv_stale_hours`=80.5 (BREACH 30)** — RED BY DESIGN. UFC market closed 2026-05-13 (`collections.market_closed_at`); FMV deliberately not refreshed. Decision owed to Trevor (retire-or-rebase the metric); do NOT raise breach_at, do NOT treat as incident.
- **`unmapped_resolution_backlog_max`=175 (BREACH 100)** — documented continuously-replenished permanent-class floor; the fix is a resolver failure-REASON, not a threshold. ⚠ Noting for continuity that it has **climbed 105 (08-05) → 127 (08-06) → 175 (08-07)** — a steady 3-day rise, not draining. Still within the known class (do-not-raise-threshold), but if it keeps climbing it may warrant the resolver-reason investigation being pulled forward. AllDay actionable unmapped is separately draining (outflow 1571/24h ≫ inflow 14/24h per `pipeline_alerts`).
- **Saturation-class `pipeline_alerts` (statement timeout)** — `allday-lock-refresh` (69.5% fail, QUEUED as ALLDAY-LOCK-REFRESH-SELECTION-COST), `lock-check-batch` (37%), `snapshot-institutional-wallets` (50%), `wallet-username-resolver` (46.6%, profiled+QUEUED 08-05). Known daytime-contention class; not new inflow.
- **`sync-nba-projections` 21/21 fail (100%) `all_upstreams_failed`** — almost certainly NBA-offseason (August, no games/projections upstream), a seasonal-expected red rather than a break; non-user-critical (Fast Break/RTR nicety). Watch-only — worth the night pass one-line-confirming it's seasonal vs a dead proxy, but not a ship.
- **`topshot-pack-opens-history-backfill` cursor-stalled + 500s / `topshot-active-listings-ingest` egress_blocked** — CC is ALREADY handling the pack-opens wedge (deploy `8d01cc61`: "handoff for wedged topshot-pack-opens-history-backfill (spork status-0 range)"). Historical backfill, not user-facing. No action.
- **Artifact estate:** 11 active + 2 newer (candy-chain-two-onboarding-v2, rpc-panini-squeeze-v2). Not per-artifact-payload-run this tick, but all shared backing views validated clean (`rpc_ops_snapshot`, `v_rpc_trust_health`, cross-collection mats, `public_board_liveness` 0 errored) — no schema-break signal.
