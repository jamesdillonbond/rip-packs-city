# Daytime monitor — 2026-06-11T06:15Z

Health: GREEN overall. Security 0/0 (all four checks). detect_stalled_pipelines() = []. Trust health 7/7 ok (pinnacle_fmv 20.0h/30 — expect reset at the ~10:07Z pinnacle-sync tick). Sentinel TS-UUID-48h = 0. 20/20 prod deploys READY (head f81b869). DB 4,239 MB (the 06-10 partition drops landed: 6,883 → 4,239, −38%). unmapped_sales open 183 (flat). TS FMV latest-per-edition: HIGH 900 / MED 2,197 (HIGH+MED 3,097 vs 2,852 baseline — improving; e3aee28 wave + tshb). 23–01Z midnight-anchor saturation window recurred (39+37 fails 23Z/00Z, quiet by 01:12Z) then calm (1–2 fails/hr 02–06Z) — matches the known 00Z window, no action beyond the queued DBSAT re-baseline. NEW ship health: `topshot-sales-history-backfill` (cd77861/18fdf7e) running clean — last tick 05:55Z, 0 fails ever. fmv-recalc ok 05:48Z; topshot-sales-indexer ok 06:03Z. TFP (topshot-fmv-populate): last run 18:50Z 06-10, watchlist temp-raised to 800m per f81b869 — decisive first post-slot-move tick due 07:15Z (already tracked; not re-raised). Artifacts: 17/17 enumerated; fragile backing queries validated OK (v_tracked_wallet_fmv_confidence 21 rows fast, sentinel_fmv_confidence_rows, v_rpc_trust_health, pipeline aggregates).

## 1. MED-HIGH — ANALYTICS-SMOKE-60S: analytics-smoke failing ~every tick for ~20h, including calm-DB hours
- Source: `pipeline_runs` pipeline='analytics-smoke' — fail streak since 2026-06-10 10:13:08Z; in the last 12 ticks (00:13–05:43Z) only ONE ok=true (02:13Z); error every time: `analytics_smoke_run: canceling statement due to statement timeout`.
- Key discriminator: the failures persist through 02–05Z when global fails were 1–2/hr — this is NOT the saturation class anymore. `analytics_smoke_run` is SECDEF with `statement_timeout=60s` (verified pg_proc) and still exceeds it, so one internal check is structurally >60s.
- Blast radius: monitoring blind spot — the analytics smoke signal has been dark ~20h (its Sentry echoes NEXTJS-A/1E fired 13–19 events over 16h, quiet ~3–4h since the storm ended). No user-facing impact found.
- Possible cause to check first: the 06-10 cost-flat drops (`marketplace_offers` 2024/2025 partitions, `flowty_archive.api_harvest`) changed stats/plans the same day the streak began (10:13Z, mid-DBSAT — but it never recovered when DBSAT did).
- Suggested action (night pass): run each internal leg of `analytics_smoke_run` standalone / check pg_stat_statements for the >60s statement; optimize or bound the slow leg (or split the fn). Don't just raise the timeout — find the leg. FMV/ingest paths untouched.

## 2. LOW-MED — NEXTJS-14 (Pinnacle FMV drift guard) re-fired once post-fix
- Source: Sentry JAVASCRIPT-NEXTJS-14, 1 event ~19:0xZ 06-10, unresolved (was verified zero-event on the 4138db6 fixed release at the 06-10 08:20Z baseline).
- Single event, no recurrence in ~11h. Verify whether it's a genuine cross-character borrow or another comparison-set edge in the guard; if clean on the next ticks, resolve with regression arming.

## 3. LOW — NEXTJS-1N new: Supabase auth-token lock stolen on /:collection/edition/:slug
- Source: Sentry JAVASCRIPT-NEXTJS-1N, 1 event ~17:0xZ 06-10. Same client-side WebKit/dual-request class as 1K/1M ("Load failed"). Watch only; fold into the existing NEXTJS-1M/1K watch — flag if accumulating.
