# Cowork interactive pass — 2026-06-07T02:15Z (19:15 PDT 06-06)

Not a monitor run — Trevor-directed Cowork session (the Dumbo/Pinnacle thread). Logged here so the night pass doesn't duplicate or collide. NOTE: 8 DB migrations shipped this evening (wave-2 + wave-3-partial + deals) — night pass should treat the touched functions/views as recently-shipped and leave them alone.

## Shipped this session (live, all verified — full details in docs/handoff-2026-06-06-pinnacle-wave2-consistency.md + docs/handoff-2026-06-07-pinnacle-wave3-partial.md)

- Wave-2 DB (5): get_cross_collection_portfolio (Pinnacle $0 bug), get_cross_collection_deals (PGRST203 overload dedupe + render-spine pinnacle leg + >=8-sales gate), holdings_summary (wmc-sourced Pinnacle), get_wallet_moments_with_fmv (render-true base_pinnacle, additive render_id field).
- Wave-2 code (CC, commits b3e9f06 + a96c2a2): digest deals consumption, get_moment_detail additive renders[] + /moment disambiguation, colon-key decode fix.
- Deals TS leg (audit_20260607_cross_collection_deals_topshot_low_ask_leg): badge_editions.low_ask vs latest FMV, gates HIGH/MED + sales_count_30d>=8 + ask<=24h. Feed now 152 deals (137 TS / 15 Pinnacle).
- Wave-3 partial (audit_20260607_health_views_pinnacle_render_spine, audit_20260607_health_checks_pinnacle_render_spine, audit_20260607_drop_orphan_pinnacle_legacy_readers): data_coverage_dashboard / data_quality / pipeline_health + health_check() / pinnacle_health_check() re-pointed at pinnacle_catalog (fmv:pinnacle freshness now watches the LIVE engine; listings:pinnacle now floor_ask_updated_at — was 142 frozen Flowty rows); 5 orphan fns DROPPED (bodies captured in the wave-3 doc).
- Sentry: NEXTJS-1E + NEXTJS-A resolved (00:17Z 06-06 smoke cluster, 24h+ quiet, SMOKE-RETRY validated under the 00:00Z rush). NEXTJS-4 left (own 24h clock from ~21:00Z 06-06). NEXTJS-15 untouched (PIN1).

## PACKEV-BUDGET-2 — characterization UPDATE (sharpens the 00:52Z item; CC queue, not auto-ship)

Measured 01:55–02:10Z:

1. **The :08/:38 logged ticks are head-of-line blocked.** Every tick 19:38Z→01:38Z processes the IDENTICAL batch of 8 (counters byte-identical across ticks: nodes_processed=8, int_pair_keys=455, secondary_asks_count~1902, ev_rows_written=0, pool_rows_written=0); fetch_phase_ms 113–128s consumes the whole ~125s budget; sampled error GetPackListing_DynamicData "Signal timed out" on dist 1742. The 8 oldest targets (dists 5460/1737/3584/1742 @ 06-04 10:09; 1683/3140/1560/7598 @ 10:39) never advance — they ARE the recurring batch.
2. **A parallel write path still lands ~75–80 pack_ev_history rows/hr, but ~90% are pack_price=0 reward packs — which topshot_pack_ev_targets EXCLUDES.** Net refresh on the 800 priced targets ≈ 5–10/hr vs ~33/hr needed for 24h freshness → staleness compounding: 547/800 (00:50Z) → 563/800 (01:55Z). stale_48h already 221.
3. **Root-cause hypothesis (fits onset + counters): the 06-06 pool remap (40→80.7% FMV coverage) roughly doubled priced editions per pack, making per-pack fetch/hydration heavier — the fixed time budget can no longer clear batch_size=8, so the oldest batch wedges permanently.** A sentinel-advance of the head 8 (the v17 pattern) would only rotate the wedge onto the next 8 — NOT shipped for that reason.
4. **Recommended CC fix (edge fn `compute-topshot-pack-ev`, worker code):** any of (a) batch_size 8→3-4, (b) per-pack fetch timeout with skip-and-sentinel-advance (so one slow pack can't eat the budget), (c) budget raise. (b) is the durable one — it makes head-of-line blockage structurally impossible. Verify after: counters vary tick-to-tick, ev_rows_written>0, stale-24h trending down from 563.

## TFP-RUSH (carrying the 00:52Z item, one gate note)

topshot-fmv-populate 00:00Z tick LOGGED (after()-fix working — first pipeline_runs row since 06-05 18:00Z) but failed in the :00 rush (pool timeout at stage sets_read). Operator: move the cron slot off :00 (e.g. :40). Watchlist (~480m/medium) only AFTER the first ok=true — still 0-for-1, gate not met. Next natural tick 06:00Z.

## Telegram/alert state at 02:10Z

get_pipeline_alerts() = exactly 1: N1 snapshot-institutional-wallets cron_silent HIGH (silent 2,919m vs 1,800m; last run 06-05 01:21Z). Known, operator-only (re-fire the cron-job.org entry + move its slot off the 06:00Z rush). detect_stalled_pipelines() = same single entry. Security 0/0; no new Sentry since ~21:00Z 06-06.
