# Daytime monitor inbox — 2026-06-10T21:15Z

Run context: lock RELEASED (13:53Z marker) — commit allowed. Artifact replay abbreviated a 3rd consecutive run (17 enumerated; DB still timing out view reads — see item 1). `v_rpc_trust_health` UNMEASURABLE again (3x 57014 this run). Addendum-2 regressions (510a359, 20:45Z) re-verified live below — one still down, one recovered.

## 1. HIGH — PACK-EVENTS-FROZEN (verification of addendum-2 Item 9): still dead at 21:13Z, ~3.2h
- Source: `event_cursor 'topshot_pack_purchases'` last updated 18:09:36Z; `pipeline_runs pipeline='pack-events-ingest'` last entry 18:09Z (a fail: `topshot_pack_opens: pack_rips batch upsert (46 rows): statement timeout`), nothing since — while the cron acks green. Exactly the "green ack, dead body" class the addendum diagnosed against the d198e68 ack-early deploy.
- Blast radius: TS pack purchase/open ingestion frozen → pack history / pack-EV inputs staling since 18:09Z.
- Action: already queued for CC in `docs/handoff-2026-06-10-dbsat-residuals-and-cron30s.md` addendum 2. Night pass: verify the fix lands + cursor advances; if no CC fix by tonight, escalate to operator (re-firing alone won't help while the ack-early body keeps dying).
- Counterpart RECOVERED: **fmv-recalc pipeline_runs logging resumed 20:48Z** (runs 20:48Z + 21:08Z ok; FMV writes fresh 21:02Z). Addendum-2's second regression appears healed — confirm continuity overnight, then strike it.

## 2. HIGH — DBSAT-RESIDUAL-19-20Z: fails did NOT drop to baseline post-d198e68; first clean signal only at 21Z
- Source: pipeline_runs hourly fails 18Z=54, 19Z=31, 20Z=40 (target was ~1–5/hr post-18:30Z per the 18:20Z inbox); 21Z partial = 1 fail/27 runs at 21:13Z — possibly the turn, too early to call. Failure class still statement/upstream timeouts: wmc-fmv-populate (19), compute-topshot-pack-ev (19), topshot-buyer-backfill (15), pinnacle-nft-resolver (14), topshot-moments-hydrator (13).
- Note: 18Z total=774 runs (vs ~120–215 other hours) — the paced seed-wallet wave ran through 18Z; elevated 18Z fails partly wave-coincident.
- Action (night pass): re-measure hourly fails 21–00Z; if 21Z's 1/27 holds, this resolved via d198e68 + the DB drops (DB now 4,171 MB after the -40% drops — expected, not a finding); if fails resume on a calm DB, continue the pg_stat_statements delta hunt from the 18:20Z inbox item 1c. Do NOT revert anything on ambiguity.

## 3. HIGH — TFP-NO-OK-44H: `topshot-fmv-populate` has had ZERO ok=true runs since 06-09 00:50Z
- Source: max(started_at) WHERE ok=true = 2026-06-09 00:50Z (~44h); last attempt 18:50Z failed `sets read failed: upstream request timeout`; no run at all in the ~2.4h since. Persists/escalates the 15:15Z inbox item 4. Watchlist @480m only catches absence, not perma-fail — it keeps slipping under detect_stalled.
- Impact: TS marketplace ASK/LOW price-source writer down ~2 days; TS ASK_ONLY freshness degrading (core FMV unaffected — fmv-recalc + sales-indexer healthy).
- Action: operator/CC — check the cron entry is firing at cadence (sparse runs suggest dropped ticks too), then root-cause the `sets read` timeout. Consider a perma-fail watchlist leg (N consecutive ok=false = alert) as a night-pass-shippable monitoring migration.

## 4. MED-HIGH — ARTIFACT: `rpc-tracked-fmv-confidence` broken right now (AF1 view timing out — 3rd-break territory)
- Source: `SELECT count(*) FROM v_tracked_wallet_fmv_confidence` → 57014 at ~21:10Z (the artifact's backing view; EXISTS-semijoin v3 from `audit_20260606_...`). Standing night-pass rule: "if this view breaks a 3rd time: stop optimizing, fix the writer (DUPE1-MIT/B2)".
- Caveat: plausibly ambient DBSAT, not the view regressing — it returned 20 rows fast at the 08:20Z baseline. Verify on a calm DB before declaring break #3.
- Action (night pass): re-run in a quiet window; if still >8s, apply the standing rule (writer fix, not a v4 optimization).

## 5. MED — NEXTJS-14 drift guard firing AGAIN despite the 4138db6 fix: 23 events/21h, last ~19Z
- Source: Sentry JAVASCRIPT-NEXTJS-14 unresolved, 23 events, first seen ~00Z 06-10, last seen ~19Z — yet the 08:20Z night pass recorded "zero events on the fixed release, resolved". Regression auto-reopen has fired.
- Likely cause: saturation — the guard's pinnacle_catalog comparison read failing/short-reading under statement timeouts → false drift (timeout-driven cousin of the original truncation bug). Needs event-payload disambiguation (release + error detail), not a guess.
- Action: CC/night pass — pull 2–3 event payloads; if comparison reads error under saturation, make the guard SKIP (not fail) on read error; if a genuine triple-key leak, escalate.

## 6. LOW/WATCH — pinnacle-wmc-render-id: logging restored by the d198e68 wrap, but recent runs fail candidate_read timeout
- Source: runs visible again (last 20:37Z) after the 9h blind window — wrap works; ok_recent=false, `candidate_read: statement timeout` x3 in 3h. The queued render-id index (focus item 10 / d118252) is the durable fix.
- Action: night pass verify post-saturation ticks; if still failing on a calm DB, the index handoff is the path.

## 7. WATCH — badge catalog GHA first scheduled tick (20:45Z) not yet logged
- Source: `topshot-badge-catalog` last pipeline_runs 17:44Z (manual dispatch via 3d0da01); the new `45 2,8,14,20` schedule's 20:45Z tick had not logged by 21:13Z. GHA schedule lag is common (10–30m) and the walk runs ~300s — verify at the 02:45Z tick before calling it broken (focus item 8a verification).

## 8. WATCH/INFO — detect_stalled: backfill-pack-rip-metadata 314m (info), refresh-pack-grail-metrics-mv 104m (info)
- Both pack-metadata family, both info-severity, plausible saturation casualties; rip-metadata at 2.6x threshold — operator re-fire if still listed tonight. Clusters with item 1 (pack family degradation).

## Green this run
- Security 0/0 both checks — `audit_lt_matches` (18:20Z inbox item 2) is RESOLVED (no RLS-off tables, no anon/auth write grants on base tables).
- Sentinel TS-UUID-48h = **0**. Sales fresh (21:03Z). fmv-recalc / offers-sweep / allday-fmv-populate / wmc-fmv-populate / sales-indexer all ok in last 2h. pinnacle-listings-reconcile ok 20:54Z.
- Deploys: prod `510a359` READY; lone ERROR `d118252` (docs-only, superseded same hour — transient build-infra class, no action).
- DB 4,171 MB (down from 6,883 baseline — Trevor's -40% drops, expected). unmapped_sales unresolved 183 (flat). Sentry otherwise quiet: only NEXTJS-1N (auth-lock steal, 1 event, known watch).
