# Daytime monitor inbox — 2026-06-03T01:49Z

Run: `rpc-daytime-monitor` (READ-ONLY sweep). Working tree: shared repo (no bot clone). Lock: ~12h-stale RELEASED marker → took over. Baseline: `metrics-latest.json` @13:42Z (night pass); cross-checked vs the 2026-06-02T15:14Z daytime inbox. Repo HEAD `23d51a4` = current prod deploy.

## Verdict: ⚠ ALERT — data plane is green, but the Cadence service PAYER WALLET is fully DRY (0.000 FLOW). 1 NEW high-priority operator candidate (A1). Everything else flat or improving.

---

## NEW candidate

### A1 · [HIGH · operator — NOT auto-shippable] Cadence service payer wallet `0x73f55c4450b8d466` drained to 0.000 FLOW — `cadence-payer-balance-check` failing every 30 min since 20:00Z
- **Source:** `pipeline_runs` ok=false scan — `cadence-payer-balance-check` 12/12 fails in 6h, each `"payer balance 0.0… FLOW below alert threshold 0.05 FLOW — top up 0x73f55c4450b8d466"`. 30h trend: last OK **2026-06-02 19:30:29Z** → first fail **20:00:41Z** → drained **0.024280 → 0.000000 FLOW** by 01:30Z (latest run). The alert pipeline itself is healthy and firing correctly — this is the funded-resource alarm doing its job, not a code fault.
- **Context (CLAUDE.md "Hot wallet & secrets"):** `0x73f55c4450b8d466` is the gas *payer* for backend-submitted Cadence transactions, monitored every 30 min by `/api/cron/cadence-payer-balance-check`, alerting below 0.05 FLOW. If it runs dry, **every backend Cadence transaction fails pre-execution with INSUFFICIENT_GAS_FUNDS (Flow error 1118).** It is now at zero.
- **Blast radius:** Cart (Open #1) and Trade Hub (Open #3) are both shelved, so the set of *active* gas-consuming production txns is small/unclear — no user-facing break was observed this sweep (reads route through proxies; wallet backfills are gas-free Cadence *scripts*). BUT the balance demonstrably fell ~0.024 FLOW over ~5.5h, so something IS spending gas (or FLOW left the account), and any backend txn from here on fails with 1118. Latent landmine + continuous 30-min alert noise.
- **Risk read:** HIGH-priority but **off-limits to the autonomous passes** — funding the hot/payer wallet touches wallet + secrets (explicitly off-limits). Operator/Trevor only.
- **Suggested action (Trevor):** (1) top up `0x73f55c4450b8d466` to a comfortable buffer above 0.05 FLOW; (2) investigate what consumed ~0.024 FLOW over ~5.5h given Cart/Trade-Hub are shelved — confirm no unexpected backend txn submitter or external transfer. No code / migration / deploy by the passes.

---

## Observed during sweep — no new action
- **N1 confirmed resolved (live):** `detect_stalled_pipelines()` = `[]`. `snapshot-institutional-wallets` no longer stalled (caught up 06-02 13:55Z per the 15:14Z inbox). Night pass: reconcile N1 → resolved in ledger.
- **S1 confirmed still closed (live):** `v_moments_needing_hydration` is NOT in the anon-readable non-`security_invoker` view set (`s1_view_present=false`). Night pass: reconcile S1 → resolved in ledger.
- **N2 still open, did NOT trip this window:** `topshot-moments-hydrator` had 0 fails in the trailing 6h (the 00:00Z rush didn't hit the candidate-read timeout this time). Deeper fix (statement_timeout bump / supporting index / further view optimization) stays operator/CC. Do NOT revert the materialized-CTE fix.
- **P1 still queued, not tripping:** `evm-transfers-ingest` ran within window; `detect_stalled_pipelines()` `[]`. SHIP-eligible next in-window night run as logged.
- **L1 (15:14Z inbox) unchanged:** `league-drift-detection` cron-wiring question — operator/CC, no new info this sweep.
- **Transient cron-rush cluster (no action):** 4 single fails at the 00:00Z rush — `compute-allday-pack-ev`, `pinnacle-listings-reconcile`, `pinnacle-nft-resolver`, `wmc-fmv-populate` (connection-pool / statement / lock timeouts), all self-recovering, none logic faults, none deploy-attributable. Known recurring class.
- **Sentry (no new / not spiking):** 1 unresolved issue total — `NEXTJS-1F` (/dashboard "TypeError: Load failed", 22h, 1 event, transient client abort) → watch. NEXTJS-15 no longer in the unresolved set.
- **Vercel:** 20/20 recent prod deploys READY, 0 ERROR. HEAD `23d51a4` ("docs(fmv)…") = current prod, READY. Today's code ships (packs cheapest-sort `64e3f4a`, smoke soft-fail `0e1da2d`, entity ask/offer `dadcc57`) all READY — none correlate with A1 (gas/wallet) or the cron-rush DB timeouts. No recent-ship regression.
- **Artifacts — 11/11 validated:** every backing query succeeds + returns sensible data. Insights views non-empty (deals 129, offer-spread 5561, squeeze 3099, rookies 61, market 554, pack-reality 90, trophies 487, pinnacle-scarcity 103, cross-cohort 1 stats row); `get_rpc_traction_snapshot()` OK; my-wallet wmc 18,451 rows / 5 collections; FMV / pipeline / security / deploy paths all exercised clean. None edited (validate-only).

## Drift vs 13:42Z baseline (all flat or improved)
- FMV TS HIGH+MED **937** (baseline 933) ↑; NO_DATA **4474** (4634) ↓ improving. AllDay HIGH+MED **272** (274) flat; NO_DATA 522 flat.
- FMV fresh: TS 01:28Z, AllDay 01:42Z — fmv-recalc healthy.
- editions TS 16,342 (+8) / AllDay 6,191 / Golazos 581 / UFC 446. DB **5,989 MB** (+23, normal). sentinel TS-UUID-48h **45** (flat, <250). unmapped 271 total (flat).
- Security **0/0** base tables (RLS-off + anon-write grants both empty); S1 view closed. The 25 anon-readable SECDEF views are the by-design insights wedge — not a finding.

## Infra note (Q7, already tracked)
- `docs/overnight/.lock` is a ~12h-stale RELEASED marker (sandbox can't unlink — the known Q7 fragility). Did not block this run. Left untouched.
