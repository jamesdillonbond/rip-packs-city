# Daytime monitor inbox — 2026-07-13T00:18Z (2026-07-12 17:11 PDT)

Run: `rpc-daytime-monitor`, later-day tick (not the ~8am first-tick, so Section 1a deep watches skipped; trust-health still read live via the sweep = 0 breaches). Lock RELEASED (no concurrent run). **Bash/git sandbox DOWN** (`useradd: cannot create directory` exit 12 — same failure the 2026-07-12 overnight pass hit; still down ~16h later) → no clone, no push; **this file written to the MOUNT, push unavailable.** One new candidate below; everything else green or already-queued.

## HIGH — FMV-RECALC-HIGH-STALL-CONTENTION-20260712 (escalation of the carried FMV-recalc-contention class into a full HIGH stall tonight)

- **Title:** `fmv-recalc` watchlist-HIGH stalled ~3h — last SUCCESS 21:02Z; the 22:08Z tick failed and no successful completion since; cron is still firing but the route keeps failing under evening DB contention.
- **Source:** `detect_stalled_pipelines()` → `fmv-recalc` silent 122–125m (threshold 120, **high**). `get_pipeline_alerts()` independently → `fmv-recalc` **high** `cron_silent` (corroborates). `pipeline_runs` newest row 22:08:05Z **ok=false** `step3_delete_chunk_failed: canceling statement due to lock timeout` (stage `step3_today_purge`, del_status 500); an earlier 19:28Z fail was `sales_refetch_failed …(saturation-class)` and self-recovered next tick. Vercel runtime errors: `[FMV-RECALC] Sales fetch error … canceling statement due to statement timeout` last **00:08Z** ⇒ the route IS still being invoked (this is contention, NOT a pure cron-trigger dropout).
- **Blast radius: LOW right now.** `fmv_snapshots` stays fresh — newest write 00:10Z, **1,855 rows/30min across 1,676 editions** (other FMV writers keep it current); `v_rpc_trust_health` `topshot_fmv_stale` NOT breached, **0 trust breaches**; newest sale 00:10Z (ingest fine). Every OTHER cron-job.org pipeline is current (sales/offers/wmc-fmv-populate 165·/3h/snapshot-pack-asks all ≤17m ago) ⇒ **fmv-recalc-specific, NOT the broad cron-job.org dropout**. Caveat: if it stays down many more hours the *full* recompute (WAP haircuts + wash-trade filter + inline disconnected-ask clamp) stops applying even while snapshot timestamps look fresh.
- **Likely cause:** evening DB lock/statement-timeout contention window, amplified tonight by the 20+ deploy 07-12 CC session wave (test-coverage push + challenges + special-serial + cron_heavy landing this afternoon/evening) + heavy wallet-backfill connection-pool exhaustion (Vercel shows hundreds of `Timed out acquiring connection from connection pool` on `wallet-backfill-*`). Same family as carried **FMV-RECALC-EDITION-FETCH-TIMEOUT-CREEP** (07-11: improving 14→7 fails) + **DAYTIME-CONTENTION**, escalated into a HIGH stall.
- **Risk read:** low-risk to leave; self-limiting. NOT monitor-actionable (read-only). NOT a clean auto-ship — `fmv-recalc` is FMV/pricing route logic (night-pass off-limits).
- **Suggested action (night pass, runs ~01:03 PDT once the contention window has passed):** VERIFY `fmv-recalc` has resumed successful ~20-min ticks (contention usually clears overnight — tonight's 19:28Z saturation fail already recovered next tick). If STILL failing to complete, the lever is contention mitigation on the route, not a cron re-enable (it is firing): e.g. `lock_timeout`/retry handling on the `step3_today_purge` delete, or shifting its heavy legs off the peak window. Treat as verify + (if persistent) CC/operator, not a blind ship.

## Context — already-queued / known, logged for continuity (NOT new candidates)

- **BASH/GIT-SANDBOX-PROVISION-FAILURE persists** (the 07-12 overnight-pass top finding). Still failing at 17:11 PDT — this monitor run also could not clone/push. Operator/infra: check Cowork sandbox provisioning; it removes the night pass's ship capability if it recurs tonight.
- **DB-SIZE-CREEP:** 11,366 MB (+205 vs the 08:06Z 11,161 baseline) — benign organic growth (wmc + pack_rips), already the DB-SIZE-CREEP-11GB watch. Monitor disk headroom.
- **classify-acquisitions-multicollection** stalled 244m (medium) = carried CLASSIFY-ACQ family (flaps under contention; last run 20:06Z). **`topshot_pack_reality_top_ev` empty (0 rows)** = the night-pass-CLOSED genuine-depletion item (74/75 +EV TS packs ≥90% depleted; 1 survivor is a reward pack) — NOT a regression, do not re-flag.
- **Security 0/0/0/0** after the full 07-12 CC migration wave (invariants []/secdef_anon []/rls_off null/anon_write_holes null). **Artifacts:** rpc-live-health schema-validated (all 12 insights views + freshness objects + alerts fn resolve); its full consolidated query timed out on live contention = documented busy-window behavior, not a break.
