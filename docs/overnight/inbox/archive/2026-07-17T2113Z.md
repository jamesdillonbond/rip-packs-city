# Daytime monitor candidates — 2026-07-17T2113Z (afternoon tick, ~14:06 PDT)

Health GREEN otherwise: security 0/0/0/0 (live), trust 15/15 ok (0 breaches; impossible_parallel 2/3 self-healing), stalled_pipelines [], pg_cron failures [], sentinel 39 (<200), DB 9,713 MB (+68 benign vs the 06:41 PDT overnight pass), Vercel prod `ae535e06` READY — no ERROR-state deploys (the newer `457de250` is docs-only, CANCELED by the ignored-build-step; normal). Sentry: 2 known/recovering issues only (details in Watch below). Candy Drop-1 launched today ~10 AM PT — new `candy_mlb` rows / candy pipeline_runs are EXPECTED, not anomalies.

## NEW candidate

### CANDY-EDITIONS-INGEST-DAS-401 (HIGH · operator/secret · launch-day go-live blocker) — NEW
- **Source:** pipeline `candy-editions-ingest`, 1 fail `2026-07-17T20:24:24Z`, `pipeline_runs.error = "DAS getAssetsByGroup HTTP 401: Unauthorized"`. This is the ONLY run of that pipeline in 48h.
- **Read:** Candy Drop 1 launched today (~10 AM PT). The ledger's 2026-07-17 Cowork Candy entry (`ddfc290`) queued the operator go-live step: "run POST /api/ingest/candy-editions then POST /api/wallet-backfill-candy once, verify pipeline_runs ok=true + editions/wmc counts, THEN wire crons." That step was evidently attempted at 20:24Z (1:24 PM PT) and FAILED — the editions-ingest route enumerates the candy_mlb Metaplex Core collection (`JkJA4yUBweFQdKAWNDhoFj8zHMZrQ1uZEYfjbkc3p8n`) via the Helius DAS `getAssetsByGroup` method through helius-proxy, and got a 401.
- **Blast radius (measured):** candy_mlb is still `is_active=false`, `edition_rows=0`, `wmc_rows=0` — NO editions/wmc landed, so the chain-two go-live is stalled at the first ingest. NO live-site impact (candy_mlb is inactive / not user-facing) — this is a launch-READINESS blocker, not a prod outage.
- **Likely cause (hypotheses only — did NOT probe the proxy or read any secret):** CLAUDE.md records the Helius gate as "CLOSED 07-16, DAS verified end-to-end," but that verification was `getAsset` (single-asset). `getAssetsByGroup` (collection enumeration) is a DIFFERENT DAS method and is the one 401'ing. Two most-likely operator causes: (a) the Helius plan/key tier doesn't entitle `getAssetsByGroup` (Helius gates some DAS methods by plan), or (b) the helius-proxy worker only whitelists/authorizes `getAsset` and rejects `getAssetsByGroup` with 401.
- **Off-limits for autonomous:** secrets/env/auth + Cloudflare worker + ingest-route are all off-limits classes — NOT night-pass-shippable. Operator/Trevor action.
- **Suggested action (operator/Trevor):** (1) confirm the Helius plan entitles `getAssetsByGroup`; (2) check the helius-proxy worker routes/authorizes `getAssetsByGroup` the same as the verified `getAsset` path; (3) re-run POST /api/ingest/candy-editions and confirm `pipeline_runs` ok=true + editions/wmc land for candy_mlb before wiring crons / flipping is_active.

## Watch / no-action (recovering or known)
- **ATLAS-POOL-INGEST-UNMAPPED-SHAPE (raised in the 1507Z inbox) — RESOLVED same-day by CC.** The 177 fails/24h are all pre-fix history: edge fn `ingest-topshot-atlas-pool` v5 now reclassifies valid-but-empty Atlas envelopes as ok=true `atlas_empty` and marks the both-form-empty bundle Box/Case dists; targets 30->5; latest run 15:13Z ok=true. Do NOT re-flag.
- **LOCK-CHECK-BATCH-DEADLOCK-UPTICK** — 9 fails/24h but 0 fails / 12 runs in the last 6h; CC's skip-locked row-claim write-back (07-17) is holding. Self-recovered.
- **Normal overnight-contention family** (fmv-recalc 6, compute-topshot-pack-ev 3, wallet-username-resolver 2, + assorted singles) — all latest-ok in the last 6h; `stalled_pipelines []` confirms none silently stopped.
- **Sentry NEXTJS-1C "public base tables: RLS on + no anon write"** (7 events/21h, last 5h ago) — tail of CC's atlas-targets-view `security_invoker` restore; security invariants are live-clean 0/0/0/0 now, no event in 5h. Recovering, no action.
- **Sentry NEXTJS-1J "pack dist page has Sales History"** (1 event, 23h ago) — the known single-event Suspense-stream/contention false-fail class. No action.
- **candy-editions-ingest note for the night/monitor passes:** per CLAUDE.md, do NOT treat candy activity itself as an anomaly — the finding above is the AUTH FAILURE specifically, not the presence of candy pipeline_runs.

## Run limitation
- **Artifact payload-query validation SKIPPED this run** — the artifact HTML lives on the Windows `C:\Users\TDill\Claude\Artifacts` path, which is not mounted in this sandbox (only `outputs`, `rip-packs-city`, `uploads`), so the embedded payload queries could not be executed. Validated the artifacts' BACKING data via the health sweep instead: the trust view returns 15 rows cleanly after today's `pinnacle_ask_stale_hours` retirement; `pack_table_rows` / `mv_pack_ev_latest` healthy after the additive `typical_ev` columns; `rpc_ops_snapshot` green; no artifact-breaking table/view DROP shipped today — so the estate is very likely intact. Recommend a full artifact pass on a session that has artifact-file access.
