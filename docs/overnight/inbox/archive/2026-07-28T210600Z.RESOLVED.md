# Daytime monitor candidates — 2026-07-28T21:06Z

Health sweep otherwise GREEN (security 0/0/0/0, trust-health 23/23 ok, 0 stalled pipelines, 0 pg_cron failures, 0 Sentry <6h, latest prod deploy 9de6b47 READY). One broken artifact found.

## HIGH — rpc-panini-squeeze artifact is fully broken (dropped column `real_sales`)
- **Source:** artifact validation — the single-payload SQL in `rpc-panini-squeeze` (index.html line 188) selects `real_sales FROM panini_squeeze_board`; ran the exact payload query, it errors `42703: column "real_sales" does not exist`. Because the whole dashboard loads from one payload query, EVERY panel is down (KPIs, squeeze/deals/players/nations/packs/serials all fail).
- **Cause:** today's interactive wave dropped the column — migration `audit_20260728_panini_squeeze_drop_real_sales_contract_half` + deploy `8e53b93` ("panini-squeeze: read serials_with_recorded_price instead of real_sales"). The Cowork artifact was never repointed to the replacement.
- **Verified replacement column exists:** `panini_squeeze_board.serials_with_recorded_price` (confirmed live; `real_sales` is gone). It counts serials with a recorded PRICE (price coverage), NOT sales activity — so the label/tooltip meaning changed.
- **Risk:** low-risk repair (artifact-only, no prod/DB/main touch), but the fix is not a pure rename because the semantics changed.
- **Suggested action (night-pass artifact repair):** in `rpc-panini-squeeze/index.html`, swap `real_sales` → `serials_with_recorded_price` in the payload SQL (line ~188) AND in the two JS reads (`r.real_sales` at line ~249: the `rs` badge + its title "N real serial sales back this FMV"). Reword that tooltip to reflect price-coverage, not sales (e.g. "N serials with a recorded price"), matching the app-side rename. Re-run the payload query to confirm it loads. Do NOT re-add the column to the view (the app deliberately dropped it).

---

## RESOLVED 2026-07-28 ~21:2xZ (Cowork interactive) — do NOT re-drain

Repaired directly in Cowork (`update_artifact`), which is where artifact work belongs — artifacts live outside the git repo, so Claude Code correctly could not reach this.

Done exactly as suggested, plus two things the item did not ask for:

1. **The breakage.** Payload SQL now selects `serials_with_recorded_price,coverage_flag`. Probed the real payload query against live before shipping: no 42703, 200 squeeze rows, totals object present. `real_sales` occurrences in the file: **0**.
2. **The tooltip overclaim.** Old title asserted these sales "back this FMV". They do not — `fmv_confidence` derives from the upstream `ms.txns` marketplace transaction count, while this counts serials carrying a recorded price (17.3% of serials). Reworded to say coverage explicitly and to state that confidence comes from elsewhere.
3. **(Extra) Adopted the honest-headline ruling** shipped the same day in `audit_20260728_panini_squeeze_coverage_weighted_totals`, so the artifact does not contradict the public board. KPI now reads `panini_squeeze_totals` and leads with the lower-bias subset, with the all-sets blend demoted to a labelled sub-line quoting `pct_sealed_usd_from_biased_sets`. **Fail-soft:** if the `_hc` columns are ever absent it shows the blend WITHOUT the lower-bias labelling — mislabelling a blended number is worse than showing a blend.
4. **(Extra) Per-row `SAMPLE` badge** on rows outside the broad/partial bands. Worth knowing why: the board's top row by FMV is Lionel Messi / Base Prizms Black at **$450,009**, `coverage_flag='listing_gated'`, with **0** serials carrying a recorded price. That row previously sat at the top of the table with nothing qualifying it.

⚠ **Operator action required:** a remote `update_artifact` clears the artifact's connector grants. Trevor must re-grant Supabase in the desktop Cowork UI before the board will load. Nothing else outstanding.

Never use the word "coverage" for `coverage_flag` on this surface — it bands purely on `sum(for_sale_count)/sum(pulled_count)` per (set, parallel_family). It is sample breadth / listing bias, not catalogue coverage.
