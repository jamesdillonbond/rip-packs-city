# Daytime monitor inbox — FIRST TICK OF DAY (~08:0x PDT)

Platform GREEN. Sweep summary: security 0/0/0/0; trust-health 9/9 ok (ts_uuid_dupes 2/200 known-DQ4); detect_stalled []; pg_cron 2 STALE pre-fix fails only (see Watch); FMV TS H+M 4333 (1210+3123, improving vs 4328) / AllDay 890 (253+637, flat); editions FLAT (TS 17,318 / AllDay 6,191 / Golazos 581 / UFC 446); unmapped 71; DB 5106 MB (+16 benign); Vercel prod 7fe106d3 READY, 0 ERROR; Sentry 0 new/12h; cross-collection mats fresh (rpc-ccm-step1/step2 both succeeded today 04:10Z/04:25Z, cohort 158); special-serial MV self-logged ok=true 3516ms @08:27Z (night-pass force_hashjoin fix proven). 11/11 insights boards return rows; rpc-live-health payload + v_tracked_wallet_fmv_confidence both validate clean.

## Candidates (1)

### CAND-1 (LOW · artifact measurement-accuracy) — rpc-live-health `leak_24h`/`leak_48h` panel over-counts `::` parallel editions
- Source: artifact `rpc-live-health` payload keys `leak_48h` = 1775 (reads in the WARN band), while the authoritative DQ4 leak metrics are healthy: trust-health `ts_uuid_dupes_created_24h` = 2/200; sentinel "TS Edition Writer Leak (48h)" (hyphen `%-%` form) = 24/250.
- Root cause: the panel predicate `editions.external_id !~ '^[0-9]+:[0-9]+$'` ALSO matches the legitimate `::subID` parallel editions cataloged by the 2026-06-20 -> 06-22 subedition + mis-attribution work. Direct disambiguation this run: of 1775 noncanonical-48h TS editions, 1775 contain `::` (benign parallels), only 24 also contain a hyphen, and editions total is FLAT at 17,318 -> there is NO active UUID-dupe writer leak. The 1775 is a measurement artifact, identical in spirit to the canonical-edition predicate bug the 2026-06-22 Cowork asset audit already fixed in the `rpc-data` skill (`^[0-9]+:[0-9]+(::[0-9]+)?$`) but which this artifact still carries the old form of.
- Risk: trivial / read-only. Dashboard panel predicate only; no production code, data, or backing object touched.
- Suggested action (night pass, artifact repair via update_artifact): update both `leak_24h` and `leak_48h` predicates in the rpc-live-health CONSOLIDATED_SQL from `^[0-9]+:[0-9]+$` to `^[0-9]+:[0-9]+(::[0-9]+)?$` so the panel reports only the real hyphen-UUID leak (~24) and stops showing a misleading near-WARN number. (The sentinel route's own `%-%` form is already correct — only the artifact needs the fix.)

## Watch (no action — already tracked / documented-stale; NOT findings)
- Two pg_cron jobs surfaced by `check_pgcron_recent_failures()` are STALE pre-fix failures that clear at their next ticks, exactly per the focus.md STANDING note: `rpc-refresh-special-serial-owners-mv` (last fail 04:13Z < the night-pass `enable_nestloop=off` fix; force-run already verified ok=true 3516ms; 16:13Z tick is the live cron verify) and `rpc-remap-misattributed-sales` (last fail 12:23Z < the ~13:46Z remap fix; 18:23Z tick verifies). Both predate their same-day fixes -> not alarmed. Next monitor tick / night pass should confirm both post-fix ticks log ok/succeeded.
