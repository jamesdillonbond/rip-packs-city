# Daytime monitor inbox — 2026-06-26T00:14Z (~17:14 PDT)

Platform GREEN. No clock skew (DB now() 00:08Z == shell 00:06Z; app-stamped FMV write 00:12Z / pack_ev 00:07Z / offers 00:07Z are real-time, can't be future-dated). 5th monitor tick of the day. **NO new fix-candidate, NO new ship since the 21:14Z tick** (prod still 59ddb6b). This file is a state-update handoff for tonight's night pass: a **trajectory correction UP** on the owned UNMAPPED-SPIKE BREACH (draining faster than the 21:14Z 2-day projection) + the carried LISTCACHE operator item (now ~14.5h, unactioned across 3 ticks).

Health one-liner: detect_stalled = [topshot-listing-cache only, carried] · pgcron [] · security 0/0/0/0 (invariants [] / secdef [] / rls-off-base {}) · pipeline fails **1 in last 6h** (topshot-badge-catalog TS-GQL 429 @22:09Z, transient, recovered) / 0 logic fails · sentinel TS-UUID leak_48h **0** · trust **8/9** (1 owned BREACH = unmapped 1968, draining) · editions FLAT (TS 17435 / AllDay 6191 / Golazos 581 / UFC 518) · ts_uuid_total 6430 frozen · FMV TS H+M **4575** (improving 4544→4557→4568→4575 across the day) / AllDay 909 (reconcile exact, fmv_sanity 0) · conflation 124 / thin_fmv 121 · DB **5808 MB** (+119/3h, known benign backfill wave) · Vercel prod **59ddb6b** READY, 0 ERROR/20 · Sentry **1** unresolved (NEXTJS-J single-event smoke transient, 0 new/0 spiking) · artifacts: rpc-live-health full payload re-run — all 11 boards resolve (squeeze 501 / set_squeeze 239 / pack_reality 7 / rookies 61 / market 501 / first_mint 501 / cross_collection 1 / pinnacle_scarcity 501 / offer_spread 501 / deals 501 / trophies 501), writers fresh, leak_48h 0, none broken.

## TRAJECTORY CORRECTION UP (owned item, NOT a new candidate) — HISTORY-BACKFILL-UNMAPPED-SPIKE draining FASTER than the 21:14Z 2-day projection
The 21:14Z tick measured ~-40/hr net and projected "~2-day ETA, do NOT alarm if still ~2000+ at 1am." **The drain has accelerated** — authoritative `unmapped_resolution_backlog_max`: 2375 (21:07Z) → **1968 (00:08Z)** = net **~-135/hr** over the last 3h (the producer #3 `topshot-flowty-sales-history-backfill` fires only every 3h, so between its chunks the `topshot-flowty-unmapped-drain` pulls the backlog down hard). At ~-135/hr it likely clears toward ~1100–1300 by 1am (still BREACH, but well down). **Night pass: this is EXPECTED + improving — do NOT re-flag / skip-at-capture / retire the class / raise the threshold; the drain is working as designed.** `topshot-flowty-unmapped-drain` 18/18 ok last 6h (last 00:09Z); promoted sales land `source='onchain'`, dedup tx_hash, fmv_sanity 0, editions FLAT — no corruption, rows quarantined OUT of `sales` until promoted. Owned. Cadence is the only lever if faster clearing is wanted (focus.md).

## CARRIED + worsening (operator) — LISTCACHE-CRON-DROP: topshot-listing-cache silent ~14.5h, unactioned across 3 ticks
Same item first logged 18:11Z, carried 21:14Z. `detect_stalled_pipelines()` reports **855 min (~14.25h) silent** (last run 2026-06-25 09:53:33Z, ok=true) vs the 360 threshold — up from 674min (21:14Z) / 493min (18:11Z). The 12 most-recent runs are all ok=true → clean EXTERNAL cron-job.org drop, NOT a route failure. Impact bounded/LOW-MED: feeds `cached_listings → badge_editions.low_ask → fmv-recalc ASK_ONLY`; core/sales-path FMV is FRESH (latest TS fmv 00:12Z), so only the ASK_ONLY-minority editions risk ~14h ASK staleness. **Action (operator): re-fire/verify the topshot-listing-cache cron-job.org entry; confirm next tick ok + detect_stalled clears.** (Consider retiring the redundant `-v2` if it still exists — carried LISTCACHE note.) Not re-raised as a net-new candidate (already in the 18:11Z/21:14Z inbox files for the night pass) — only the worsening duration is updated here.

## POST-SHIP WATCH (for night pass) — no new ships; today's sales-path ships all still healthy
Prod unchanged at **59ddb6b** since the 21:14Z tick (no new code/migration). Re-confirmed today's sales-path drains/backfills, all 0 fails last 6h: topshot-flowty-unmapped-drain 18/18 ok, ufc-studio-sales-history-backfill 18/18 ok (3×/hr, still progressing — watchlist gate stays MET, ship-eligible next genuine overnight run per focus.md ready INSERT @90m/medium), allday/golazos/pinnacle-studio-sales-history-backfill 2/2 ok each, topshot-flowty-sales-history-backfill (producer #3) 2/2 ok. No regression on any.

## Owned / not re-logging (trajectory only)
- conflation guard **124** — holding at the high end of its owned 44–124 band (== 21:14Z's 124; the heavy studio/flowty backfill wave bumps it as parallel sales land on base editions before `::` rows exist). Converges on the daily `refresh-conflated-editions` remap; suppressed from deal boards. `edition_integrity_flags` 4/50 ok.
- offer_edition_gap_max_usd 0/50 ok — clean (no transient this tick; the oscillating OFFER-SANITY class).
- PINNACLE-EDITION-KEY-UUID-CAST — already queued (LOW, CC route-side).
- ufc-studio watchlist row still PENDING (gate met) — ship next genuine overnight run.

## Benign / not-an-incident (do NOT chase)
- topshot-badge-catalog 1 fail @22:09Z — TS GraphQL 429 (Too Many Requests), single transient, recovered; the known rate-limit class, not a logic failure.
- Sentry NEXTJS-J — 1 user / 1 event ~3h ago, culprit POST /api/smoke-test; the known single-event smoke-flake class (bypasses SMOKE-RETRY by design). 0 other unresolved. Watch only.
- DB +119 MB/3h — active studio + UFC-studio + flowty-drain backfill wave writing/promoting sales; expected, benign.
- 2 tombstone artifacts (pack-drops-ev-check, rpc-ts-data-mission) — intentional retirements, skipped.
