# Daytime monitor — 2026-06-15 ~03:05Z (late-evening tick, 20:05 PDT)

PLATFORM GREEN. Two newer code ships landed AFTER the 00:16Z inbox (Trevor is actively shipping — newest commit 2275d1b landed ~7 min before this run); both READY + clean, logged below as post-ship-watch + a queued→shipped status update. No breakage, no broken artifacts.

## Health snapshot (read-only; deltas vs the 00:16Z tick)
- pipeline_runs: 1 transient fail / 6h (offers-sweep @01:42Z "Error with SearchMarketplaceEditions" — known AllDay/marketplace GQL-upstream transient; offers-sweep otherwise clean). detect_stalled_pipelines() = []. get_pipeline_alerts() = [].
- Security: 0/0 (no RLS-off base tables; no anon/auth write on RLS-off base tables, relkind r/p). 60c1438 (below) additionally CLOSED a SECDEF anon hole — checks confirm clean.
- Sentinel TS UUID-keyed editions 48h: 0.
- Trust health: 8/8 ok (read-only re-confirm, not the daily extra). pinnacle_fmv_stale 17.1h/30 (normal drift between daily pinnacle-sync recomputes; 14.0h at 00:16Z). offer_edition_gap_max_usd 0/50, ts_uuid_dupes_24h 0/200, fmv_sanity 0/1, pack_ev_stale 0.70d/2.
- FMV (still improving): TS hi+med 3365 (00:16Z 3372 / baseline 3350 — normal recompute drift), AllDay 712 (711 / 679), TS NO_DATA 4017 (down from 4092 baseline). Editions flat (TS 15543 / AllDay 6191 / Golazos 581 / UFC 446). UFC wmc null_key 2 (fossil floor). DB 4627 MB (benign +8 vs 00:16Z).
- Vercel: 0 ERROR across the 20 most-recent deploys. Prod HEAD 2275d1b (docs-only, CANCELED build-skip via 0e7e627 ignoreCommand); last READY app build fa60e80. CANCELED = docs-only build-skips / superseded by Trevor's rapid pushes.
- Sentry: 0 unresolved issues in the last 24h (NEXTJS-15 fully quiet, past its resolve mark).
- Artifacts: 12 active validated. rpc-live-health: trust 8/8, FMV panel resolves (sentinel_fmv_confidence_rows), all 11 insights boards return rows (squeeze 501 / set_squeeze 235 / pack_reality 8 / rookies 61 / market 501 / first_mint 501 / cross_collection 1 / pinnacle_scarcity 501 / offer_spread 501 / deals 501 / trophies 501). rpc-offers-intelligence v_offer_sanity_flags 193 (sub/serial out-of-scope; edition-grain gap 0 in trust health). rpc-tracked-fmv-confidence AF1 v_tracked_wallet_fmv_confidence 21 rows no-timeout. Other 9 active artifacts validated green at 15:10/21:05/00:16Z with no artifact-backing schema change since (today's migrations — team-moment RPCs additive, badge_editions int-pair guard, OFFER-SANITY-RAISE offers — rename/drop nothing an artifact reads). 5 RETIRED tombstones skipped. None broken/repaired.

## NEW — post-ship watch (two ships landed after the 00:16Z tick; both READY, clean)
- **SEED-REFRESH-WIDEN-WATCH · [LOW · night-pass post-ship watch]** — `0f3b8ca` (READY, deploy 52mLd) shipped the previously-GATED **VERCEL-FLUID-RIGHTSIZE Item 2**: low-priority seeded wallets (priority >= SEED_REFRESH_LOWPRI_MIN, default 4 — the ~76% discovered-cohort herd) are now SKIPPED on a 0/6/12/18h wave when their last actual walk (seeded_wallets.last_refreshed_at) is fresher than SEED_REFRESH_LOWPRI_INTERVAL_HOURS (default 24); high-priority (<=3/NULL), never-seeded, and truncation-signature wallets always refresh (unchanged). Thins the wave that has been the DBSAT-contention + Fluid-GB-hr driver. **Night-pass watch:** (a) high-priority/logged-in wallets still refresh every wave (last_refreshed_at advancing for priority <=3); (b) the discovered cohort does NOT go staleness-broken on the analytics/discovery boards (cross_collection / squeeze stay fresh); (c) the **06:45–07:27Z cohort wave the night pass runs right after gets LIGHTER, not heavier** — first wave-pacing change since the cohort split, so judge fails/hr + seed-refresh child fan-out against the clean-night baseline. No regression at this tick. Advances/closes VERCEL-FLUID-RIGHTSIZE. Revert: `git revert 0f3b8ca` (falls back to 6h for all). Source: commit 0f3b8ca.
- **OFFER-SANITY-RAISE · queued → SHIPPED `60c1438` (READY) — recommend CLOSE + post-ship watch.** The long-queued edition-grain offer raise shipped: edition-grain filter on the raise (offer_type='edition' only, never clobber down) + closes a SECDEF anon hole. Night pass: close the queued OFFER-SANITY-RAISE ledger item; post-ship-watch that offer_edition_gap_max_usd stays 0/50 (currently ok) and security stays 0/0 (anon-write + SECDEF checks confirm the hole is closed). v_offer_sanity_flags 193 (was 197/202 — residual is correctly out-of-scope sub/serial offers). Source: commit 60c1438.

## Already-queued / tracked, unchanged (NOT re-raised)
- TOP-SALES-INSIGHTS-QA (LOW, night-pass — /insights/top-sales rpc-insights-qa static-source review; backing view secure + 501 rows; logged 21:05Z).
- ALLDAY-V1-UNMAPPED-DRIFT (operator recover-v1 cron — 236/246 open AllDay unmapped = v1_tx_decode_budget_exhausted).
- VERCEL-* cost family (Item 2 now SHIPPED as 0f3b8ca above; Observability-sampling / Spend-pause / Fluid-concurrency still Trevor-dashboard).
- Carried: TEAM-MOMENT-DISPLAY follow-ups (all shipped 0ec62ce/f43c316/a3da7be per ledger), PIN-FMV-REKEY-WAVES 2/3, N1, P3-BUYERS, the OFFER-SANITY companion monitor (pairs with the shipped raise).
