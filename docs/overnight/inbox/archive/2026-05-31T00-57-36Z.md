# Daytime monitor — candidate inbox · 2026-05-31T00:57Z

Run: `rpc-daytime-monitor` (read-only sweep). HEAD `9172cba` on `main`; prod deploy `dpl_D4zvv2J7WkhAvJLbQLoPvg1D79qn` (9172cba) READY.
Lock present but STALE (`2026-05-30T22:14:02Z` runid=8a4b884c off-hours-monitor-mode, ~2h45m old, > 45m) → treated as a leftover from a finished run. **Commit DEFERRED:** an orphaned `.git/index.lock` (~19h stale, see C4) blocked git; this file persists on disk so the night pass picks it up locally regardless. Did not touch either lock (read-only posture).

Platform is healthy overall — security clean (0 base-table RLS-off, 0 base-table anon/authenticated write grants), all pipeline failures transient-with-recovery, FMV flat vs 22:25 baseline (TS HIGH+MED 778, AllDay 242), unmapped_sales unchanged (144 open), all 7 /insights backing views non-empty, FMV/Pack-EV fresh. 3 candidates below.

---

## C1 · [MEDIUM · P2 follow-up — recent-ship health] `bd4d8c4` did NOT quiet Sentry `JAVASCRIPT-NEXTJS-15`

- **Title:** pinnacle-listings-indexer `listing_resolution_failures_inserted` warning still fires ~every tick post-fix.
- **Source:** Sentry `JAVASCRIPT-NEXTJS-15` (last seen 9 min before sweep; `search_events count()=5` in the 2h window) + commit `bd4d8c4` (ledger lists P2 as RESOLVED by this commit, deploy `dpl_4UBShmk267fUqit46PFZNuTLRFJd` ~21:52Z).
- **Evidence:** Fix (count only genuinely-new inserts via `.select()` on the ignoreDuplicates upsert) deployed ~21:52Z, yet the warning fired **5× in the ~2h since** ≈ every 20-min tick — the same cadence as before the fix. Pipeline itself is healthy (`pinnacle-listings-indexer` runs_24h=72, fails_24h=0, ok=true), so this is warning-level noise, not breakage.
- **Risk read:** LOW to investigate (read-only). Harm is that the per-tick warning masks real errors; no user impact.
- **Suggested action:** Inspect the listing-resolution-failures queue for genuinely-new vs re-observed inserts in the last hour. (a) If Pinnacle truly inserts ≥3 NEW unresolved listings/tick, the warn threshold (≥3) sits just under Pinnacle's normal new-failure churn → raise the threshold for Pinnacle, or exclude the permanently-capped (~1.5k) retry backlog from the "new" count. (b) If the counter still includes non-new rows, the `.select()`-returns-only-inserted-rows assumption is wrong for this upsert. Re-verify before treating P2 as closed. (NOT a re-raise of a declined item — this is evidence the P2 fix did not move its target metric.)

## C2 · [MEDIUM] Smoke test `sales pipeline healthy (analytics_pipeline_health.sales)` flaps `degraded` — threshold tighter than indexer cadence

- **Title:** 30m max sales-lag threshold is tighter than the `topshot-sales-indexer` ~2h cadence → intermittent false `degraded`.
- **Source:** Sentry `JAVASCRIPT-NEXTJS-B` (POST /api/smoke-test; 75 lifetime occ, 5 in last ~11h, last 21:53Z, release f9388c7; extra: `detail=status=degraded lag=56m (max 30m)`, `endpoint=rpc:analytics_pipeline_health.sales`). Confirmed via sales-freshness read: TS newest sale 98m old mid-cycle; `topshot-sales-indexer` 12 runs/24h (≈ every 2h), last_ok 23:17Z; volume healthy (200 TS + 40 AllDay sales in 6h).
- **Risk read:** LOW (config/threshold; read-only to diagnose). Real harm = alert fatigue: operators learn to ignore smoke failures, which can later mask a genuine sales stall.
- **Suggested action:** Either raise the sales-lag `max` in `analytics_pipeline_health` to ~2.5–3h (match indexer cadence + overnight sparsity), OR compute lag from the last successful sales-indexer run rather than the newest `sales.sold_at` row, so "no sales happened" ≠ "pipeline unhealthy." Keep it tight enough to still catch a real multi-hour stall.

## C3 · [LOW · P4] `evm-transfers-ingest` recurring Base RPC 429 ("over rate limit")

- **Title:** Base mainnet ingest hits 429 rate-limit ~4×/24h.
- **Source:** `pipeline_runs` — `evm-transfers-ingest` ok=false `"base_mainnet proxy returned 429: over rate limit"`, last 00:00:45Z; fails_24h=4 / runs_24h=22, last_ok 23:00Z. Distinct from the benign connection-pool transients (external rate-limit, not pool contention).
- **Risk read:** LOW. Beezie/Base is the parallel data plane with no product consumer yet (CLAUDE.md "keep parallel for now") — failures have zero user-facing impact and self-recover next tick. Not in the off-limits set.
- **Suggested action:** Add backoff/jitter or lower the per-tick block range / batch size on the Base ingest call to stay under the Base mainnet RPC rate limit. Pure ingest-cadence tweak; low priority.

## C4 · [INFRA · flag for Trevor / night pass] Orphaned `.git/index.lock` (~19h stale) is blocking all local git commits

- **Title:** A stale `.git/index.lock` dated **2026-05-30 05:41:38Z** (~19h old) blocks `git add`/`commit` in the repo mount; this daytime run could not commit its inbox file (written to disk regardless).
- **Source:** `git commit` → `fatal: Unable to create '.git/index.lock': File exists`; `stat .git/index.lock` = 2026-05-30 05:41:38Z. Also a stale `docs/overnight/.lock` (2026-05-30T22:14:02Z, runid 8a4b884c) is present.
- **Risk read:** LOW to clear, but it will also block the **night pass's** own commits at 1am if not removed first — so it can silently stall autonomous shipping. No data risk (orphan from a long-dead process; HEAD is intact at `9172cba`).
- **Suggested action:** `rm -f .git/index.lock` (confirmed orphan — 19h with no live git) and delete the stale `docs/overnight/.lock`. The monitor intentionally did NOT remove either (read-only posture + "don't touch the lock" rule). Night pass: clear `.git/index.lock` before attempting any commit this cycle.

---

### Validated this run (no action)
- Security: 0 public base tables with RLS off; 0 base-table anon/authenticated write grants (`relkind IN (r,p)`; the by-design SECDEF/insights views are excluded). Matches `check_public_security_invariants()` baseline (0/0).
- Vercel: 1 ERROR deploy in the recent list (`dpl_9BVVoDCcUfHtyDGUHSAQLrxuSQba`, commit `01b3878`) — historical Phase-D shim gap, already superseded by 11+ READY deploys; current prod READY.
- Sentry `JAVASCRIPT-NEXTJS-1B` (pinnacle/moment null destructure) — ledger P3, last seen ~7h ago, not firing since the fix chain landed; leave to the "mark resolved after 24h clean" plan.
- Sentinel TS edition-writer leak 48h = 1596 (WARN band, < 2000) and DOWN from baseline 1707 — known/monitored inert-row condition, trending right; not re-raised.
- Artifacts: rpc-insights-health (7/7 backing views non-empty, FMV/Pack-EV fresh), rpc-fmv-watch, rpc-pipeline-reliability, rpc-security-drift, rpc-live-health, rpc-deploys-and-cost all validated via their backing queries. rpc-cross-collection `cross_collection_cohort_stats`=1 is a by-design stats rollup (artifact reads it as OK). rpc-my-wallet / rpc-traction backing tables unchanged today; not re-run.
- Already-tracked (NOT re-logged): Q1 (3 `topshot_pack_reality_*` SECDEF views → security_invoker), Q2 (compute-laliga-pack-ev cadence).
