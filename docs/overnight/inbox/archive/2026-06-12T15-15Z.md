# Daytime monitor — 2026-06-12T15:15Z

Run context: first monitor sweep after the off-hours night pass (ended 14:42Z) caught DBSAT-IO-EXHAUSTION-0612 live. Lock RELEASED. This sweep measured the incident STILL ACTIVE and WORSE on the telemetry axis.

## 1. HIGH — DBSAT-IO-EXHAUSTION-0612 ongoing: pipeline run-rate collapsed to near-zero at 14–15Z
- Source: `pipeline_runs` hourly — 12Z: 760 runs/13 fails (seed wave), 13Z: 179/53 (30% fail), **14Z: 6 runs**, **15Z (first ~10 min): 2 runs** vs ~200/hr norm. Last-run cascade: most pipelines went silent in a stagger 12:1x→14:0xZ.
- `detect_stalled_pipelines()` at 15:09Z returned **21 stalled** — incl. `fmv-recalc` HIGH (141m silent; last 12:48Z), `allday-sales-indexer` HIGH (94m), `topshot-moments-hydrator` (178m), `pinnacle-listings-reconcile` (151m), `hybrid_custody_events` (157m), `evm-transfers-ingest` (171m), plus the two carried items (`topshot-listing-cache` 891m = LISTCACHE-SILENT-0612; `topshot-fmv-populate` 834m = TFP-SLOT-MOVE/480-gate — NOT re-raised, already queued).
- My own MCP reads hit "Connection terminated due to connection timeout" **3×** during this sweep (15:06–15:12Z) on simple queries — connection-level starvation persists, intermittently.
- Smoke (Sentry) fired mass-fail clusters at ~14:30Z and ~14:48Z: **public page /disney-pinnacle/overview + /nfl-all-day/overview non-200** (NEXTJS-W, NEXTJS-E — user-facing), "edition page has Recent Sales" (1H), "sales indexers running" (1E, 12 events/23h), "fmv pipeline healthy" (A). User-facing impact continues.
- Risk read: incident, not a code regression (predates today's ships per night-pass root-causing; 4th consecutive daytime window, worst yet).
- Action: reinforces the night pass's morning-#1 operator escalation — Supabase disk-IO budget graphs → compute add-on decision; wmc fifth-call-site swap (CC, handoff exists); pace the 6h wave dispatch. Nothing new to ship from monitor side; this entry is the 15Z datapoint for the incident record. NOTE for night pass: a large share of "stalled" pipelines may be firing-but-dying-before-logging rather than cron-disabled — but given the 06-09 precedent, ALSO check cron-job.org for mass auto-disable once IO recovers (smoke + promote_unmapped_sales still log, so dispatch is at least partially alive).

## 2. HIGH — VERCEL-DEPLOY-ERROR-X3: three consecutive production deploys ERROR (docs-only commits)
- Source: Vercel `list_deployments` — `dpl_3h5kAPKGsu2zC6sAbHo5YvXQ2SoZ` (3ffdd54, 14:17Z), `dpl_3C4v9XMbeLNMA6JcqjP4WRjkznwm` (cccd744 night-pass docs, 14:29Z), `dpl_68FbL7AiBWCF9N1zQCGDsSeZQs9Y` (4b75dd3, 14:47Z) — all `state: ERROR`, all docs-only commits on the same tree as READY `46500e4`.
- Build log of the latest: zero error/failed/fatal lines; ends mid-Sentry-sourcemap-upload — the documented invisible-failure deploy class (CLAUDE.md Vercel section). No maxDuration change shipped (docs-only), so the 800s-cap cause doesn't apply; likely transient build-infra or a Sentry-upload-step failure.
- Blast radius: NONE user-facing — production still serves `46500e4` (READY, 05:44Z). But main is currently undeployable; any real code fix pushed now would not go live.
- Action (night pass / Trevor): re-fire a deploy (empty commit or Vercel redeploy-with-fresh-cache); if a 4th consecutive ERROR, pull full build logs JSON for the missing tail + check Vercel status page. Do not ship code expecting it to deploy until one goes READY.

## 3. MED — smoke security legs false-fail under saturation (NEXTJS-1C, NEXTJS-1D)
- Source: Sentry NEXTJS-1C ("public base tables: RLS on + no anon write"), NEXTJS-1D ("anon no EXECUTE on destructive SECDEF"), 2 events each, 14:30–14:48Z.
- Direct catalog SQL this sweep verified BOTH clean: RLS-off tables = 0; anon/authenticated write grants on RLS-off base tables = 0. So the smoke legs failed on query timeout under IO starvation, not a real posture change — same class the 06-10/11 sweeps saw.
- Action (CC, low risk, after incident): extend the `isSaturationError()` warn-downgrade (631b1de) or rpcRetry treatment to the smoke security legs so saturation doesn't mint security-titled Sentry issues that desensitize the signal. Genuine posture breaches must still fail loud — keep the assertion, soften only the timeout/abort error class.

## 4. INFO — sweep coverage notes
- Sentinel TS-UUID-leak-48h = **0** (clean, measured direct).
- Security 0/0 (both checks, direct).
- Sentry otherwise: 11 unresolved, no new non-smoke issues; NEXTJS-1P (auth-token lock steal on /:collection/sniper, 2 events, 16h ago) and NEXTJS-15 single-event are known/low.
- Artifact validation SKIPPED this run: DB connections intermittently refused — artifact query validation would mint false "broken artifact" findings under saturation. Re-validate next run.
- FMV counts / trust-health / metrics-latest drift: NOT measured (heavy reads unsafe/unreliable mid-incident; carry TS HIGH+MED 3,226 @ 03:10Z baseline).
