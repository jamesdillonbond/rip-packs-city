# Daytime monitor inbox — 2026-06-02T15:14Z

Run: `rpc-daytime-monitor` (READ-ONLY sweep). Working tree: shared repo (no bot clone). Lock: released + 80m stale → took over. Baseline: `metrics-latest.json` @13:42Z (last night pass).

## Verdict: ✓ HEALTHY — platform green; 1 NEW low candidate; 2 baseline items self-resolved during the day.

---

## NEW candidate

### L1 · [LOW] `league-drift-detection` has run exactly once — confirm it's wired to a recurring cron (or is a deliberate one-shot)
- **Source:** `pipeline_runs` last-run-age scan — only ONE row ever: `2026-05-31 14:00:40Z`, `ok=true`, 0 found / 0 written (no drift). 49h silent at sweep; NOT in `pipeline_cadence_watchlist`, so `detect_stalled_pipelines()` is structurally blind to it (same class as the pre-Q10 listing-cache gap).
- **Context:** introduced ~2026-05-31 alongside the WNBA / league-aware team-hub work. Reads like a periodic data-integrity check (NBA/WNBA league-mislabel drift). No mention in CLAUDE.md / ledger / `cron-schedule.md`.
- **Risk read:** LOW. If it's an intentional one-shot, nothing to do. If it's meant to recur and the cron was never wired, a league-drift integrity check silently never re-runs — low product impact (its single run found 0 drift; it is not a writer for any user surface), but it's a monitoring blind spot.
- **Suggested action (operator/CC — NOT auto-ship):** confirm intent. If recurring: wire a cron-job.org entry (or GH Action) + add it to `pipeline_cadence_watchlist` at a generous `max_silent_minutes` per its design cadence so `detect_stalled_pipelines()` covers it. If one-shot: record that in `cron-schedule.md` so future sweeps don't re-flag. Either path is a doc/monitoring-config item — no route logic, no destructive SQL.

---

## Observed during sweep — no new action (already queued, or self-resolved during the day)

- **S1 RESOLVED (do not re-raise):** `v_moments_needing_hydration` is no longer anon/authenticated-readable (live: `anon_select=false`, `auth_select=false`, `reloptions` still null → the queued `REVOKE ALL ... FROM anon, authenticated` was applied during the day). Anon-readable non-`security_invoker` view count back to **0** (was 1). Night pass: reconcile S1 → resolved in ledger.
- **N1 RESOLVED (do not re-raise):** `snapshot-institutional-wallets` ran **2026-06-02 13:55:39Z, ok=true, 3 rows** (after the 13:42Z baseline flagged it stalled at 1892m). `detect_stalled_pipelines()` now `[]`; the missed 06:00Z daily slot caught up off-peak. Night pass: reconcile N1 → resolved.
- **N2 still open (already queued):** `topshot-moments-hydrator` hit the `v_moments_needing_hydration` candidate-read statement-timeout once more this window (`12:22:04Z`, 12:00Z cron rush). 1/window, self-recovering, no observed backlog. Do NOT revert the materialized-CTE fix. Deeper fix (statement_timeout bump / supporting index / further view optimization) remains operator/CC.
- **P1 still queued, NOT tripping now:** `evm-transfers-ingest` watchlist 60→150. `detect_stalled_pipelines()` `[]` this sweep (it ran within window). SHIP-eligible next in-window night run as already logged — no change.
- **Transient cron-rush cluster (no action):** 8 pipelines with 1–2 transient fails each, all clustered at the 12:00Z rush (connection-pool / statement / upstream timeouts), none since ~12:22Z (~2.75h clean), none logic faults, none deploy-attributable — `wmc-fmv-populate`, `pinnacle-nft-resolver`, `golazos-listings-indexer`, `pack-pull-source-rip-id-backfill`, `compute-topshot-pack-ev`, `topshot-fmv-populate`, `evm-transfers-ingest`, + the N2 hydrator. Known recurring class.
- **Sentry (no new / not spiking):** 2 unresolved, both 1-event & stale — `NEXTJS-1F` (/dashboard "Load failed", 11h, transient client abort) watch; `NEXTJS-15` (gated AllDay `listing_resolution_failures`, 23h) → operator can mark resolved at 24h-clean (ledger C1).
- **Recent ship:** newest prod deploy `27fdfbde` (Trevor, ~13m ago) is docs-only (commit of the overnight outputs) — READY, no code, no regression. 20/20 recent prod deploys READY, 0 ERROR.

## Drift vs 13:42Z baseline (all flat or improved)
- FMV TS HIGH+MED **930** (was 933); NO_DATA **4621** (was 4634, ↓). AllDay HIGH+MED **274** (flat).
- editions TS 16,334 / AllDay 6,191 / Golazos 581 / UFC 446 (identical). DB **5,969 MB** (+3). sentinel TS-UUID-48h **44** (was 45). unmapped **269** (was 271).
- Security **0/0/0** base tables; anon-readable-SECDEF-view **0** (↓ from 1 = S1). FMV fresh to **15:08Z** (fmv-recalc healthy).
- **11/11 Cowork artifacts validated:** all 8 insights views resolve + non-empty (deals 127, offer-spread 5,562, squeeze 3,099, rookies 61, market 551, pack-reality 91); `get_rpc_traction_snapshot` + `funnel_events` present; pipeline/FMV/security/editions/deploy data paths all exercised clean.
