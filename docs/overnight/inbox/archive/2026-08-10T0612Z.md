# Daytime monitor candidates — 2026-08-10T06:12Z (late-night PT tick, ~23:12 PDT Aug 9)

Source: rpc-daytime-monitor sweep. **Written to the MOUNT — bash/git sandbox is down (`useradd … /sessions no space left on device`, the documented Cowork-shell failure), so no clone/push. The night pass picks this up locally.**

Sweep was otherwise clean-with-known-noise: security 4/4 invariant arrays `[]`; `detect_stalled_pipelines()` `[]`; latest prod deploy READY (`975e25c5`), no ERROR; artifact `rpc-live-health` validated (backing views resolve, FMV/Pack-EV/offers writes all <10 min fresh). Every saturation finding this tick is **already queued** and not re-logged: the 6 pg_cron statement-timeout failures (ccm-step2 04:25Z recent; 5 others >12h stale + self-retrying) and the MV-refresh cluster are covered by `inbox/2026-08-10T0555Z-cron-budget-headroom-audit.md` (jobid 235 market-index-daily + the cron_heavy squat population + alert-path jobid 261). Trust breaches all known/tracked (panini price-capture 12 = upstream home-box outage; public_board_slow_count 1 = crawler/saturation class; unmapped_resolution_backlog_max 194 = AllDay floor, inflow 62/24h vs outflow 1709/24h per the info alert). `wallet-username-resolver` **confirmed healthy post-fix** — 7 of last 8 runs ok, the lone 06:08Z fail is a single saturation-tick blip, not a regression (the 53.7%/2d rate is pre-fix runs aging out).

---

## 1. (LOW) New Sentry smoke-test failure: cursor-stall threshold shared by classifier and alert arm

- **Source:** Sentry `JAVASCRIPT-NEXTJS-25` — "smoke test failed: cursor-stall threshold shared by classifier and alert arm". Culprit `POST /api/smoke-test`. **1 event, first+last seen ~14h ago (≈2026-08-09 16:00Z), has NOT recurred since.** Not in `ledger.md` or any existing inbox file.
- **Context:** the 2026-07-28 ledger entry deliberately unified the cursor-stall threshold so it is "expressed ONCE and guarded" (`cursor_stall_threshold()`), closing the 4-hour window where a stalled indexer was invisible to every alert branch. This smoke assertion appears to be the guard that enforces the classifier and alert arm read that single threshold.
- **Risk read:** LOW. A single, non-recurring smoke-test assertion — either a transient (a smoke run that fired during a saturation window / partial state) or a one-time drift the run happened to catch. Not a user-facing error (no runtime issue), and the invariant it protects is data-integrity alerting, not a live surface.
- **Suggested action (night pass):** glance only — confirm the classifier path and the alert arm still both source `cursor_stall_threshold()` (i.e. the 07-28 single-source invariant is intact) and that the smoke assertion is asserting on the same constant. If it's just an assertion that flaps under load, tighten/relax it; if the two paths have genuinely drifted, that's the same shared-threshold coupling class the deep-audit register tracks. Do not treat as urgent — it hasn't fired in 14h.
