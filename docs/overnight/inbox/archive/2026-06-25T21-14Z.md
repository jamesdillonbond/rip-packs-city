# Daytime monitor inbox — 2026-06-25T21:14Z (~14:08 PDT)

Platform GREEN. No clock skew (DB now() 21:07Z == shell 21:05Z; app-stamped sales 21:05Z / fmv 21:06Z are real-time, can't be future-dated). 4th monitor tick of the day. **NO new fix-candidate.** This file is a POST-SHIP-WATCH handoff + a **trajectory correction on the owned UNMAPPED-SPIKE BREACH** (the 18:11Z "clear by 23-24Z tonight" projection was over-optimistic — see below) + one carried/worsening operator state (LISTCACHE).

Health one-liner: detect_stalled = [topshot-listing-cache only, carried] · pgcron [] · security 0/0/0/0 · pipeline fails **2 in last 24h** (104 pipelines) / 0 in last 6h · sentinel TS-UUID/leak_48h **0** · trust **8/9** (1 owned BREACH = unmapped 2375, draining) · editions FLAT (TS 17435 / AllDay 6191 / Golazos 581 / UFC 518) · FMV TS H+M **4568** improving (4544→4557→4568 across the day) / AllDay 912 (reconcile, fmv_sanity 0) · conflation 124 / thin_fmv 121 · DB **5689 MB** (+125/3h, known benign backfill wave) · Vercel prod **59ddb6b** READY, 0 ERROR/20 · Sentry 1 single-event smoke transient (NEXTJS-J, below) · artifacts: rpc-live-health full insights_counts re-run — all 11 boards resolve (squeeze 501 / set_squeeze 239 / pack_reality 7 / rookies 61 / market 501 / first_mint 501 / cross_collection 1 / pinnacle_scarcity 501 / offer_spread 501 / deals 501 / trophies 501), leak_48h 0, writers fresh (FMV 21:09 / pack_ev 21:08 / offers 21:02Z), none broken.

## TRAJECTORY CORRECTION (owned item, NOT a new candidate) — HISTORY-BACKFILL-UNMAPPED-SPIKE drain is proxy-capped; realistic ETA ~2 days, NOT tonight
The 18:11Z handoff projected the BREACH would "clear below 100 by ~23-24Z tonight" (it measured a 198/tick burst on the first fixed-filter tick). **That projection is wrong** — measured the drain's steady-state directly this tick:
- `topshot-flowty-unmapped-drain` is **healthy** (6/6 ok last 2h, last 21:09Z), but **every tick reports `rate_limited:true`**: `promoted` 52→63/tick, `get_minted_used` 70→81, `transient_nulls` ~20 (the proxy throttles `getMintedMoment` to ~55 successes/tick — the same throttle the 22461b5 "never retire on transient nulls" fix was written for). So gross drain ≈ **~52/tick × 3/hr ≈ ~165/hr**, NOT 198/tick sustained.
- Authoritative `unmapped_resolution_backlog_max`: 2370 (15:15Z) → ~2695 peak (~18:07Z) → 2497 (18:11Z) → **2375 (21:07Z)**. Net **only ~-40/hr** after the producer #3 (`topshot-flowty-sales-history-backfill`, every 3h) adds its recurring chunk. At ~-40/hr net, clearing 2375 → <100 takes **~2 days**, not hours.
- **Night pass: do NOT alarm if this BREACH is still ~2000+ at 1am — that is expected, the drain is working as designed and proxy-rate-limited.** Per focus.md the only lever is drain **cadence** (e.g. 20-min → 10-min) and/or pacing the producer #3 down; both are owned/queued. Promoted sales land `source='onchain'`, dedup tx_hash, `fmv_sanity` 0, editions FLAT — no corruption, no board/FMV impact (rows quarantined OUT of `sales` until promoted). Owned, do NOT re-flag / skip-at-capture / retire / raise the threshold.

## POST-SHIP WATCH handoff (for night pass) — 3 daytime ships landed since the 18:11Z tick, all clean
Prod advanced **43a86ea → 22461b5 → c1f3cd8 → 59ddb6b** (current, READY; all Trevor/CC). Intervening docs/monitor commits show CANCELED in Vercel = superseded by faster builds, normal. No regression signal on any.
- **22461b5** `fix(flowty-drain): never retire on transient (rate-limit) getMinted nulls` — refines the drain I'm watching: only a clean HTTP-200 with no set/play payload (definitive burned) can retire; any non-200/error/timeout is transient and never bumps `drain_attempts` (prevents falsely retiring ~290 resolvable rows/tick = data-loss guard). **Verified working:** `retired:0` on every tick this 2h, drain still net-positive. This is what makes the drain proxy-capped (see trajectory above) — intended.
- **c1f3cd8** `fix(media): recover final 11 TS dead-media editions to 0/0` — data UPDATE (repoint via per-moment `media/<nft_id>` CDN form + backup table). editions FLAT (TS 17435), no churn; focus.md already documents dead-media as FULLY RESOLVED 0/0. No prod-route impact.
- **59ddb6b** `feat(spork-proxy): extend historical floor to mainnet17` — **worker-only** (`workers/spork-proxy/`) + a `HIST_WINDOW_START` constant in `backfill-topshot-buyers`. The commit itself says **deploy is creds-gated** (`wrangler deploy` + `SPORK_PROXY_SECRET` + Vercel vars not done), so it has **zero prod runtime effect** right now — nothing to watch beyond confirming `backfill-topshot-buyers` keeps running ok (it does).

## CARRIED + worsening (operator) — LISTCACHE-CRON-DROP (same item as 18:11Z, NOT re-raising as net-new)
`topshot-listing-cache` still silent — last run **2026-06-25 09:53:33Z**, now **674 min (~11.3h) silent** vs the 360 threshold (was 493min at 18:11Z; the operator has not re-fired across two ticks). The 12 most-recent runs are all ok=true → clean EXTERNAL cron-job.org drop, not a route failure. Impact bounded/LOW-MED: feeds `cached_listings → badge_editions.low_ask → fmv-recalc ASK_ONLY`; core/sales-path FMV is FRESH (latest TS fmv 21:09Z), so only the ASK_ONLY-minority editions risk ~11h ASK staleness. Action (operator): re-fire/verify the `topshot-listing-cache` cron-job.org entry; confirm next tick ok + detect_stalled clears (and consider retiring the redundant `-v2` if it still exists).

## STATE update (NOT a candidate) — ufc-studio-sales-history-backfill watchlist gate still held
The PENDING watchlist row (focus.md / 18:11Z) remains gate-met: 6 clean 20-min ticks in the last 2h (3/hr cadence, all ok=true). Ready INSERT (90m/medium — NOT 600m) in focus.md; ship-eligible next genuine overnight run, then verify `detect_stalled_pipelines()` stays [].

## Owned / not re-logging (trajectory only)
- conflation guard **124** — holding at the high end of its owned 44–124 oscillation band (105→104→124 across the day; the heavy studio/flowty backfill wave bumps it as parallel sales land on base editions before `::` rows exist). Converges on the daily `refresh-conflated-editions` remap; suppressed from deal boards. Owned. (`v_rpc_trust_health.edition_integrity_flags` 4/50 ok — data-integrity canary clean.)
- offer_edition_gap_max_usd 0/50 ok — clean (the oscillating OFFER-SANITY class; no transient this tick).
- PINNACLE-EDITION-KEY-UUID-CAST — already queued (LOW, CC route-side).

## Benign / not-an-incident (do NOT chase)
- Sentry **NEXTJS-J** "smoke test failed: pack-listings responds" — **1 user / 1 event**, first==last seen ~20:49Z (~16 min before this sweep), culprit `POST /api/smoke-test`, low actionability. Single-event smoke transient (the known smoke-flake class; certain checks bypass SMOKE-RETRY by design). 0 other unresolved in last 12h. Watch only — flag if it recurs/spikes.
- DB +125 MB/3h — the active studio + UFC-studio + flowty-drain backfill wave writing sales/promoting rows; expected, benign (same +119/3h as 18:11Z).
- 2 pipeline fails / 24h — far below any alarm; transient, not chased.
- Tombstone artifacts (pack-drops-ev-check, rpc-ts-data-mission) — intentional retirements, skipped.
