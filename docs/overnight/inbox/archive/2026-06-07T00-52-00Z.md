# Daytime monitor inbox — 2026-06-07T00:52Z (17:52 PDT 06-06)

## NEW candidates

### PACKEV-BUDGET-2 · `compute-topshot-pack-ev` time-budget pairing now EVERY tick (10 consecutive, 19:38–00:38Z) + queue 68% stale-24h — MED→HIGH (characterize, then CC tuning)
- **Source:** `pipeline_runs` — every tick 19:38Z→00:38Z logs paired ok=true AND ok=false `time_budget_exceeded_after_fetch` (10 consecutive; 19:08Z was the last clean pair). Queue state at 00:50Z: `topshot_pack_ev_targets` **800 targets, 547 (68%) stale >24h, 0 stale >7d, oldest last_ev_at 2026-06-04 10:09Z**.
- **Read:** escalation of the 21:12Z PACKEV-BUDGET item (was 3 of 4 ticks; now persistent). Not queue poison (no 7d tail, oldest 2.6d) — the v20 budget cap gracefully truncates each tick, but coverage has degraded to ~32% fresh-24h. Onset still matches the 06-06 `audit_20260606_pack_pool_canonical_remap_and_rpcs` (FMV coverage 40→80.7% = more priced editions = more EV math per pack).
- **Suggested action:** night pass characterize per the 21:12Z item (where the budget goes; staleness trend over 24h — compare against tonight's 547/800). If stale-24h grows again, queue an edge-fn budget/batch tune for Trevor/CC (worker code = off-limits to auto-ship). A stopgap the night pass COULD ship if evidence supports it: nothing DB-side obvious — the lever is in the edge fn.
- **Risk:** analysis read-only; any fix is CC-queued.

### TFP-RUSH · `topshot-fmv-populate` cron re-enabled but first run failed at the 00:00Z rush peak — MED (operator: move slot off :00; watchlist after first ok=true)
- **Source:** `pipeline_runs` 2026-06-07 00:00Z, ok=false, `sets read failed: Timed out acquiring connection from connection pool.` Only run since 06-05 18:00Z → the cron-job.org entry WAS re-enabled (21:12Z TFP-CRON item, operator action done ✓), but it fires exactly at :00 — the worst contention window (tonight's rush also threw 1 transient fail each across ~8 other pipelines, known I1 class, all recovered).
- **Suggested action:** (1) Operator: shift the cron slot off the :00 rush (e.g. :40, mirroring the fmv-recalc dial-back convention). (2) Night pass: watchlist `topshot-fmv-populate` (~480m/medium) only AFTER the first post-shift ok=true run logs — do not watchlist on a 0-for-1 record (C-PAYER lesson). Next natural tick ~06:00Z; if it fails again outside a rush, escalate to route-level investigation (36eee2f after() path).
- **Risk:** operator console action + one idempotent watchlist INSERT later. Low.

### SMOKE-CLUSTER-RESOLVE · 00:17Z 06-06 smoke cluster now 24h+ quiet AND SMOKE-RETRY verified under live rush — LOW (Sentry housekeeping)
- **Source:** Sentry unresolved is down to 4 (from 8 at 21:12Z); tonight's 00:00Z 06-07 cron rush — the exact condition that fired 7 one-event smoke issues yesterday — produced **zero new smoke Sentry events** with `ff853a2` (SMOKE-RETRY) live. First real-world validation of the retry.
- **Suggested action:** mark the remaining 00:17Z-cluster issues (NEXTJS-1E, NEXTJS-A) resolved; NEXTJS-4 (last 20:25Z, deploy-churn window) resolvable after its own 24h quiet per SMOKE-MKT; NEXTJS-15 stays (known PIN1 class).
- **Risk:** none — Sentry status flips only.

## Not re-logged (status re-confirmed this run, 00:50Z)
- **P1-CAD holding:** `pinnacle-metadata-backfill` 19:22→23:22Z = 5 consecutive ok=true.
- **PIN-SYNC-FLOWTY (`ae6d78f`) holding:** 20:42Z ok=true; the 20:26Z 'traits' fail predates the deploy. PIN-SYNC-CRON (operator wires daily cron) still open.
- **Evening ship wave (b7d19e8→a96c2a2, incl. Wave 1b scarcity re-key + wave-2 digest/moment fixes): all deploys READY, no attributable Sentry.** `pinnacle_scarcity_board` returns 2,079 rows (expected); `pinnacle_sales` render-keyed **13,163/13,163 (100%)** — new sales rows arriving pre-keyed, the cron drain works.
- **DUPE1:** TS-UUID-48h 6,382 (sentinel CRITICAL ≥2000) but **0 new in the last hour**; TS editions flat 15,541. Pure roll-off now; counter should clear ~06-08 if quiet.
- **N1** `snapshot-institutional-wallets`: still the only `detect_stalled_pipelines()` entry (2,846m vs 1,800m). Operator re-fire + move off 06:00Z rush.
- **C2** `topshot_pack_reality_top_ev` still 1 row.

## Artifact validation (this run)
16 artifacts enumerated. Backing queries run read-only, all pass: rpc-tracked-fmv-confidence (7 buckets, fast — AF1-v2 holding), rpc-insights-health (squeeze bounded 500+, rookies 61, pack_reality 1 = C2), rpc-rewards-console (v_rewards_economy 1), rpc-offers-intelligence (edition_offers 9,593), rpc-qa-scorecard (v_offer_sanity_flags 123; v_fmv_sanity_flags 0), rpc-security-drift (0/0), rpc-live-health/rpc-fmv-watch (FMV write 00:48Z, minutes old), rpc-deploys-and-cost (20/20 READY, DB 6,371 MB). No artifact references the renamed scarcity columns directly (view kept old names as aliases — Wave 1b). None broken; none repaired.

## Headline metrics (00:50Z vs 21:12Z / 08:30Z baseline)
- FMV HIGH+MED: TS **3,025** (3,035 / 3,041 — flat), AllDay **504** (497 / 490 — up). TS NO_DATA 5,587 (5,710 — DUPE1 tail draining).
- TS editions 15,541 (flat); sentinel TS-UUID-48h 6,382 / **0 per hr**; unmapped_sales 183 (flat); DB 6,371 MB (+41).
- Security 0/0 (RLS-off + anon-write, relkind r/p). Deploys 20/20 READY (latest `a96c2a2` 22:23Z). Sentry 4 unresolved, no spike, nothing new in ~4h.
- Pinnacle: renders priced 1,790; sales 100% render-keyed; scarcity board 2,079 rows.
