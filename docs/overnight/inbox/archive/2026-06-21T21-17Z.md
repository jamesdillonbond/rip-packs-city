# Daytime monitor candidates — 2026-06-21T21:17Z (3rd tick of day, 14:17 PDT)

Run: rpc-daytime-monitor, third tick. Lock RELEASED (08:18Z night pass, stale). Platform **GREEN** — security 0/0/0/0, trust 9/9, detect_stalled [] / alerts [1 INFO golazos], FMV writers fresh + 0-fail, Vercel **0 ERROR prod `6b9e89a` READY**, DB 5065 MB. **0 net-new candidates.** The substantive content is the post-ship watch on a fresh writer-path ship (`6b9e89a`, ~20:57Z) that supersedes the 11am SALES-ON-UUID-EDITIONS-RESIDUAL finding.

---

## POST-SHIP WATCH — `6b9e89a` "never key TS sales/moments onto UUID-dupe editions" (deployed ~20:57Z READY, current prod) — EARLY-POSITIVE; night pass: re-confirm with full ticks

This is the NEW writer fix since the 18:18Z tick (newer than f796447). It closes the two forward-writer leaks the f796447 mis-attribution fix did NOT: `/api/ingest` (`source=topshot_gql`, the dominant ~30/2h leak) + sales-indexer 4b/4c (`source=onchain`). It directly addresses the 18:18Z tick's SALES-ON-UUID-EDITIONS-RESIDUAL (which was the wmc/moments-inherited path the GQL-only f796447 fix couldn't cover). `7cb2c77` on top is a CANCELED docs commit; prod = `6b9e89a` (READY, rollback-candidate).

**Measured this tick (cutoff 20:58Z = deploy):**
- **`onchain` (sales-indexer) leg — CONFIRMED working.** The sales-indexer ran 21:03Z (post-deploy) and ingested **13 TS sales, 0 on UUID/non-canonical editions.** 72 ok / 0 fail / 24h.
- **`topshot_gql` (ingest) leg — NOT YET EXERCISABLE (not a concern).** `gql_total_since_deploy = 0` — no `/api/ingest` tick has produced a TS sale since ~20:57Z (next GHA ingest tick ~21:16Z+). So the new `ingest-canonical-guard` telemetry is legitimately `guard_runs_ever = 0` (the guard can only log once ingest runs post-deploy and hits a UUID-pair-after-redirect case). `/api/ingest` does NOT log to pipeline_runs under `ingest`/`topshot-ingest` (last_ingest_run null) — the topshot_gql sales themselves are the activity signal; do not chase the pipeline name.
- **All 10 non-`(null)` UUID-edition sales in the 2h window predate the deploy:** 7 `topshot_gql` @19:56:27–31Z + 3 `onchain` @18:43/19:23×2/19:43Z. Zero since 20:58Z.
- **The 22 `(null)`-source UUID sales/2h are the documented OUT-OF-SCOPE path** (`scripts/flow-backfill.ts` / 2020-dated, empty-txhash; contained by the daily on-chain drain) — NOT a deployed forward writer, NOT a `6b9e89a` regression. Ledger Shipped entry calls this out explicitly.

**Convergence machinery (unchanged, no new code warranted):** conflation guard **17** (stable/converged — 44 → 27 → 17, NOT rising), `ts_uuid_dupes_created_24h` 2/200 (editions NOT re-polluted), `fmv_sanity_flags` 0, FMV reconciles. The new daily Vercel drain `/api/admin/drain-topshot-misattribution?rekey=1` (`d240fb85`, `0 11 * * *`) first AUTOMATED fire is tomorrow 11:00 UTC (no `topshot-misattrib-drain` pipeline_runs row yet — expected).

**Night-pass action (post-ship-watch, no fix to ship):** with many more ingest+sales-indexer ticks elapsed by overnight, re-confirm `gql_uuid_since_deploy` + `onchain_uuid_since_deploy` stay at 0, that `ingest-canonical-guard` begins logging (`uuid_resolved_onchain`/`uuid_skipped`), and the conflation guard keeps trending toward 0. Do NOT treat the residual `(null)`-source UUID sales or a transiently non-zero `sales_on_uuid_editions` as a writer regression. No auto-revert warranted — early signal is clean.

---

## SUPERSEDED — SALES-ON-UUID-EDITIONS-RESIDUAL (11am 18:18Z tick)

The 18:18Z LOW finding (live + history-backfill sales landing on UUID fossils via the wmc/moments-inherited path the f796447 GQL fix didn't cover) is now directly addressed by `6b9e89a` above — the `onchain` leg is confirmed closed and the `topshot_gql` leg is fixed pending its first post-deploy tick. Folded into the post-ship watch; not a standing residual anymore.

---

## Carried unchanged (NOT re-logging — already queued)
- **REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT** (night-count 3, operator) — MV current at **6778 rows** (fresh, board not broken); cron still not firing (deployed fix sufficient; operator must re-enable the cron-job.org entry, then night-pass watchlists). No new signal this tick.
- **refresh-conflated-editions cron** (operator wire daily cron; safe-DELETE fix live, guard converged 17).
- **NEXTJS-1C** (Sentry) — the RLS-leg smoke failure CC fixed at 16:13Z; **quiet 5h** (last seen ~16:0xZ, 5 events). Cause resolved; cosmetic/regression-arm cleanup only (operator/night-pass mark resolved). NEXTJS-A has rolled out of the 24h window (quiet >24h).
- Standing backlog: BUYERBF-PERINVOCATION-WORK, ALLDAY-V1-UNMAPPED-DRIFT (59 open, 34 budget-exhausted fossils), UFC-EDITIONS-SEED-GAP, TS-WMC-UUID-FOSSILS, N1 snapshot-institutional-wallets, BADGE-CATALOG-CRONJOB-DUP, VERCEL cost family, A1-WORKER-PASSTHROUGH-CLEANUP, get_user_top_owned_moments 3-arg orphan, PIN-FMV-REKEY-WAVES 2/3, PIN-SYNC-CRON, P3-BUYERS, DUPE1, Q2/Q5/Q6, ANALYTICS-SMOKE-RESIDUAL, IPFS ×2. **STEER honored:** SERIAL-FMV-MULT-CRON BY DESIGN (weekly pg_cron); evm-429 benign.

## Post-ship watch (other recent ships — all PASS)
- **Today's full mis-attribution closeout** (f796447 / f908c83 / 7 `audit_20260621_*` migrations / d240fb85 drain cron): security 0/0/0/0, trust 9/9, conflation guard 17, no schema break (all backing boards return rows). `AUDIT-TABLE-RLS-20260621` resolved 16:13Z (security smoke leg green since).
- **c05435d/c81f0cb launch-polish** (hide Fast Break/RTR/Rewards, hard-gate routes, remove Cart shell): prod READY, 0 ERROR, no attributable Sentry. Nav/visibility + dead-shell only.

## Artifact validation
16 artifacts enumerated, **0 RETIRED**, none broken/repaired. All backing objects return sane rows (deals_board 675, serial_premiums 181, perfect_mint 13, trophies 685, top_sales 991, underpriced 11, allday_floor 3920, special_serial_mv 6778, squeeze 500+, tracked_fmv 21) — consistent with the 18:18Z validation; no schema break from today's additive `audit_20260621_*` migrations. (HTML on OneDrive; backing-object row-probe used in lieu of per-payload reads — same MONITOR-ARTIFACT-ACCESS pattern.)

## Health one-liner
security 0/0/0/0 · trust 9/9 · detect_stalled [] / alerts [1 INFO golazos] · pipeline 6h: analytics-smoke ×3 (resolved RLS-leg tail 15:13/15:43Z + 1 transient FMV-tier-pulse 17:43Z; GREEN since) + evm-429 ×3 (benign) · sentinel hyphen-48h 24/250 · FMV writers fresh 0-fail (fmv-recalc 21:08Z 93/0, allday 72/0, sales-indexer 72/0) · editions TS 17318 (15543 + 1775 `::` Stage B) / AllDay 6191 / Golazos 581 / UFC 446 · conflation guard 17 (converged) · unmapped 59 · DB 5065 MB · Vercel 0 ERROR prod `6b9e89a` READY · Sentry 1 (NEXTJS-1C cause-fixed/quiet 5h) · 16 artifacts (0 RETIRED), all boards return rows.
