# Daytime monitor — 2026-06-09T06:18Z

Read-only sweep. **Platform GREEN with one NEW candidate: CRON-DROP-WAVE — a tight cluster of cron-job.org jobs (most materially `fmv-recalc`) dropped overnight ticks ~04:23–04:53Z and have stayed silent, generalizing the night pass's queued LISTCACHE-CRON-DROP.** No outage; FMV/sales data fresh; bounded + self-healing. Two carries (NOT re-counted): PINFMV-DRIFT-14 still firing, PIN-SYNC-FMV-WATCH gate not yet testable. All deterministic checks clean: `detect_stalled_pipelines()` = 1 (info: `refresh-pack-grail-metrics-mv`, part of the wave), `check_secdef_anon_execute_violations()`/`check_public_security_invariants()` clean, 0 RLS-off base tables, 0 anon-write holes, sentinel TS-UUID-48h **0**, 20/20 deploys READY, 17/17 artifact backing relations resolve, `v_rpc_trust_health` all 6 `ok`. Lock stale (released 08:17Z 06-08, ~22h) → inbox commit permitted.

## New candidate

### CRON-DROP-WAVE — overnight cron-job.org tick-drops across a :23–:53-anchored cluster; `fmv-recalc` worst-hit + approaching its high-severity watchlist threshold (MED · operator — generalizes/escalates the queued LISTCACHE-CRON-DROP)
- **Source:** `pipeline_runs` absence-of-runs sweep (frequent pipelines by mins-since-last-run), 06:18Z. Several cron-job.org-triggered jobs last fired in a tight 04:23–04:53Z band and have been silent since, all with **0 `ok=false`** in between (clean external-trigger dropouts, not route crashes):
  - **`fmv-recalc` — last 04:48Z, silent ~90 min (~4 missed ticks).** Ran clockwork every 20 min (:08/:28/:48) from 01:48→04:48Z (last 10 runs all `ok=true`), then stopped. This is the load-bearing sales-path FMV confidence sweep, watchlisted @120m/high (MON1) — at 90 min it has NOT yet crossed the threshold, but if the next :28 tick (06:28Z) also misses it trips `detect_stalled_pipelines()` HIGH + likely the smoke `fmv pipeline healthy` check (NEXTJS-A) at ~06:48Z. **This is the one to re-fire proactively.**
  - `refresh-pack-grail-metrics-mv` — last 04:23Z, 115 min (missed the 05:23 hourly :23 tick). The lone `detect_stalled_pipelines()` / `get_pipeline_alerts()` entry this sweep (**severity info**, 90m threshold). Powers grail-hunter ranking UI; one missed tick = marginally stale rankings.
  - `pinnacle-wmc-render-id` — last 04:37Z, 101 min (missed its hourly :37 tick).
  - `backfill-pack-rip-metadata` — last 04:53Z, 85 min (missed its hourly :53 tick).
- **Attribution / class:** flaky external trigger (cron-job.org), the exact Q3 / N1 / P3-BUYERS / LISTCACHE-CRON-DROP class. The platform is up — many crons fired normally in the same window (golazos-sales-indexer 05:51Z, analytics-smoke 05:43Z, the :00/:06 storm jobs at 06:02–06:09Z), so this is **selective per-entry tick-drop, not a Vercel/DB outage**. Generalizes LISTCACHE-CRON-DROP (queued 06-08): the listing-cache family was the first symptom; tonight the wave widened to these :23–:53 jobs.
- **Blast radius: bounded, no outage.** FMV/sales data stays fresh via the indexers (topshot-sales-indexer 06:03Z, allday-fmv-populate 06:02Z, offers-sweep 06:02Z all `ok`); `v_rpc_trust_health.fmv_sanity_flags`=0 + `pack_ev_board_max_stale_days`=1.0 both ok. fmv-recalc being down only delays re-confidence of newly-traded editions (~90 min so far); grail/render-id/pack-rip each missed a single low-frequency tick. All self-heal the moment the cron entries resume firing.
- **Excluded from the wave (checked, explained — do NOT chase):** `pinnacle-listing-cache` silent since 00:37Z is the **intentional retirement** (its cron entry was deleted today, deploy `4abfc36`). The entire `wallet-backfill*` family silent since ~00:45–00:56Z (~5.5h, the 8 highest-volume pipelines) is the **normal `seed-wallet-refresh` 6h-orchestrator inter-burst gap** (next burst ~06:45Z) — correctly NOT watchlisted (Q9 don't-false-positive lesson), not a stall.
- **Risk read:** external cron + NO-PUSH can't touch it; not auto-shippable. Operator action.
- **Suggested action (operator):** re-fire / investigate the cron-job.org entries — `fmv-recalc` first (the "RPC FMV Recalc Force Stale" :08/:28/:48 entry), then `refresh-pack-grail-metrics-mv` / `pinnacle-wmc-render-id` / `backfill-pack-rip-metadata`. Since this is the second overnight cron-drop symptom in 24h (after LISTCACHE-CRON-DROP), a single consolidated look at the cron-job.org execution history for the ~04:50–06:18Z window is more useful than per-job re-fires — likely a cron-job.org account/dispatch dip rather than four independent flakes.

## Carries — NOT new, do not double-count
- **PINFMV-DRIFT-14 (Sentry NEXTJS-14) — STILL LIVE** (10 events / 7h, last ~10 min ago, `regressed`; the only Sentry issue actively firing on real product logic). Cross-character Pinnacle FMV leak from the ~23:00Z `04011b3` concierge ship; deterministic, so it re-trips each smoke run while the leaked row persists. HIGH-PRIORITY, CC/Trevor-owned, off-limits to the night pass (Pinnacle FMV/concierge route logic + focus.md item 2). Full detail + keying-fix probe in the `2026-06-09T00-14Z` + `03-15Z` inbox files — same item, do not re-count.
- **PIN-SYNC-FMV-WATCH gate not yet testable** — the `pinnacle-sync` render-FMV `statement_timeout` fix (`5880eeb`) is validated only by the daily ~10:07Z tick; the 06-09 tick fires ~4h after this sweep (last run was the failed 06-08 10:07Z one). Render FMV currently fresh (`pinnacle_ask_stale_hours`=1.6 ok). Re-check at the next ~3h sweep / the night pass. (Logged 03:15Z, carried.)
- **LISTCACHE-CRON-DROP** — `topshot-listing-cache-v2` is de-watchlisted + being retired (the `retire_listing_cache_v2_watchlist` migration shipped, so it's no longer a `detect_stalled` entry); the PRIMARY `topshot-listing-cache` recovered (last 06:03Z, fresh). The drop pattern itself now lives on as CRON-DROP-WAVE above.

## Clean / already-owned — checked, not new
- **Pipeline fails last 6h = the known I1/DBSAT rush cluster, all self-recovered.** ~55 fails concentrated at the :00/:06 cron storm (compute-topshot-pack-ev 11, pinnacle-nft-resolver 9, pinnacle-listings-reconcile 7, topshot-moments-hydrator 7 = N2, analytics-smoke 4, pack-events-ingest 3, + singletons: lock-check-batch, classify-acquisitions-multicollection, wmc-fmv-populate, resolve-topshot-stubs, check-alerts, topshot-stub-resolver) — all `statement timeout`/pool-contention, 0 fails in the calm hours, none a wedge. `pinnacle-listings-reconcile` (now the load-bearing on-chain Pinnacle ASK writer) fails ~29%/6h at rush but ASK stays fresh (1.6h, ok). compute-topshot-pack-ev fails are rush-window target timeouts, NOT a v21 wedge.
- **Sentry: no net-new issue.** 10 unresolved = NEXTJS-14 (carry above) + the known smoke-saturation cluster re-tripped by the 06:00Z storm (1J pack-dist / 1H edition / 1E detect_stalled / W+E overview / 4 market / A fmv-health — super_low actionability, all "last seen ~10 min ago" = the storm) + NEXTJS-15 (PIN1/Q4 `cadence_capped` false-positive, last 4h) + NEXTJS-1C (PINBACKUP-RLS echo, fix landed, last 7h → markable resolved if quiet 24h). The FMV-confidence RPC + the detect_stalled smoke RPC both time out only during the :00/:06 storm (verified: `sentinel_fmv_confidence_rows` timed out at 06:06Z, succeeded on retry seconds later) — the I1 saturation tail, known.
- **dapper.market second-buy-link feature shipped today (`f9210cf`, deploy `eUH7oY` READY) — no regression.** Pure-additive UI link (lib/collections.ts `dapperMarketMomentUrl` builder + sniper/market link rendering); no backing pipeline, minted no Sentry issue, smoke clean. Plus the brand Phase-2 batches (`4be97c5`→`57b3def`), Candy ingest pre-build (`52072be`), chain-two address foundation (`d079e5b`), legacy `pinnacle_fmv_snapshots` retirement (`ab2e6f4`/`b367e76`) — all 20/20 recent prod deploys READY, 0 ERROR.
- **FMV flat / healthy churn:** TS HIGH+MED 2,917 (baseline 2,971), NO_DATA 5,029 (improving from 5,196 baseline / 5,044 at 03:15Z); AllDay unchanged. Editions identical to baseline (TS 15,542 / AllDay 6,191 / Golazos 581 / UFC 446) — DUPE1 re-mint stayed stopped; sentinel TS-UUID-48h **0** (6,404 fossils flat; `ts_uuid_dupes_created_24h`=0 ok). unmapped_sales 183. pack-EV 800 total / 127 stale-24h / **0 stale-48h** (oldest advancing, 06-08 02:43Z) — PACKEV-THROUGHPUT draining on cron frequency; board max-stale 1.0d ok.
- **Security clean:** `check_public_security_invariants()` empty (PINBACKUP-RLS resolved + held), 0 RLS-off base tables, 0 anon/auth-write holes, `check_secdef_anon_execute_violations()` `[]`.
- **Artifacts 17/17 healthy** — backing relations all resolve: `v_tracked_wallet_fmv_confidence` 20 (no 57014), squeeze+scarcity+offer-spread 500+, `cross_collection_deals_board` 321, `topshot_deals_vs_fmv` 287, `topshot_2025_rookie_index` 61, `v_rewards_economy` 1; `v_rpc_trust_health` all 6 metrics `ok`.
- **DB 6,682 MB** (+3 since 03:15Z, +200 since the 08:15Z night baseline) — slow creep, watch-only (dupe-fossil roll-off + index churn).
