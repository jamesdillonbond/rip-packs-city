# Daytime monitor inbox — 2026-07-04T00:13Z (~17:13 PDT tick)

Platform **GREEN**. security 0/0/0/0 · trust **16/16 ok** (breaches []) · `detect_stalled_pipelines()` [] · `check_pgcron_recent_failures()` [] · sentinel TS-UUID-48h **0** · editions FLAT (TS 17,489 / AllDay 6,191 / Golazos 581 / UFC 518) · FMV fresh all collections (topshot_fmv_stale 0.3h, TS H+M **4,779**, AllDay 857, UFC 15, Golazos 4; fmv_sanity 0) · DB **7,558 MB** (+134 vs 07-03 = allday_studio_history_v1 backfill tail, benign per focus) · Vercel prod **def899f1 READY**. Nothing broken. 2 new candidates below.

Not the 8am tick → 1a daily extras (full trust read / cross-collection pg_cron verify) skipped by design; trust vector still read via rpc_ops_snapshot (16/16 ok).

## Candidates

### 1. DEF899F1-PERF-WAVE-POSTSHIP-WATCH — LOW · post-ship watch (night pass)
- **Source:** git `def899f1` (07-03 17:04 PDT) "fix(perf): packs 500, market 504, TS team-hub 404 — DB query optimizations (Bugs 3,4,5)" + `fdf35b65` "recent-sales scoped by sales.collection_id; guard ipfs.dapperlabs.com art". Deployed `dpl_Gvtsnni6mYmWJFAGjrEcXa6mGEFH` **READY at 00:05Z** — ~1 min before this tick, so almost no post-deploy runtime data exists yet (the watch genuinely belongs to the night pass).
- **DB-layer landing verified clean this run:** `mv_pack_ev_latest` 1703 rows == `pack_ev_latest` 1703 (exact parity per the commit) · pg_cron `rpc-refresh-mv-pack-ev-latest` active=true · `pack_table_rows` repointed at the MV · Bug 4 = `get_allday_market_listings` → plpgsql. Migrations mirrored under supabase/migrations, applied live via MCP.
- **Risk read:** additive/perf-only; NO view/RPC dropped or renamed (artifact estate intact — rpc-live-health backing objects all validated healthy this run). Only exposure is the standard post-ship watch a Trevor/CC ship doesn't self-schedule.
- **Suggested action:** over the next ~day confirm the `pack_table_rows` / pack-detail / `/api/packs` 500 / market 504 timeout classes actually DROP on `dpl_Gvtsnni6...` (they currently dominate Sentry but attributed to SUPERSEDED deploys); `mv_pack_ev_latest` refresh cron stays green + row-parity holds; no new error class. No revert unless a regression appears (revert = `git revert def899f1` + drop mv/restore pack_table_rows).

### 2. FMV-RECALC-EDITION-FETCH-TIMEOUT-CREEP — LOW-MED · watch · night-count 1 (shares root with OVERNIGHT-0623Z-CONTENTION-CLUSTER)
- **Source:** pipeline_runs. `fmv-recalc` daily fail rate creeping: 06-29 **0%** · 06-30 **0%** · 07-01 **5.6%** · 07-02 **7.6%** · 07-03 **8.5%** — all `edition_page_fetch: canceling statement due to statement timeout` at a hard ~30s (Sentry [FMV-RECALC] Edition page fetch error, last 00:08Z).
- **Not an outage:** the SUCCESSFUL runs (200–405s) do the full work, so `topshot_fmv_stale_hours` **0.3h GREEN** and fmv_sanity 0. Partial-page flap that retries next tick, not a stall (`detect_stalled` []).
- **Root read:** correlates with DB growth from the `allday_studio_history_v1` backfill fill (7,212→7,558 MB over 3 days) raising shared connection-pool contention — same window/root as the 07-03 **OVERNIGHT-0623Z-CONTENTION-CLUSTER** (rpc-remap-misattributed-sales / rpc-allday-ev-corrected-refresh). `compute-topshot-pack-ev` also nudged up (07-03: 9 fails, still <2%, latest recovers).
- **Suggested action:** consolidate with OVERNIGHT-0623Z as one DB-growth-contention watch. Escalate only if the trend climbs past ~15%/day OR `topshot_fmv_stale_hours` starts breaching. Lever then = measure the edition_page_fetch sub-query's 30s per-statement timeout vs letting the studio-backfill tail relieve on its own. Do NOT auto-ship a timeout bump (core FMV route; contention-root not a query bug; unverifiable-in-run).

## Not re-raised (dispositioned)
- `[team] activity/detail` + `[player] top sales` statement-timeouts (Sentry, still last-seen ~00:06–00:09Z) = the RESOLVED/dispositioned **TEAM-PAGE-STATEMENT-TIMEOUT** pool-saturation collateral (ledger, do-not-re-flag). The def899f1 wave targets the pack/market members of this same class.
- 2 ERROR-state deploys (`ded5e7c3`, `8080fce8`, both smoke-fix commits) = superseded transient build failures (Trevor then pushed `cda6f09c`/`def899f1` READY). Not live.
