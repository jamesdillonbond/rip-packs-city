# Daytime monitor — 2026-06-24T15:14Z (first tick of day, ~08:08 PDT)

Platform GREEN. DB now() 15:08Z AGREES with the sandbox clock this run (no ~5.5h skew — the nightly's skew was transient). Prior run = nightly autonomous pass ~06:41 PDT (pushed 16db7ff; shipped 0 [correct]; drained 6 inbox files). Inbox empty at start. Lock RELEASED.

## Health summary
- `detect_stalled_pipelines()` [] · `check_pgcron_recent_failures()` [] (THIN-FMV-GUARD-CONTENTION CLEARED — today's 13:30Z thin-fmv-guard tick succeeded) · `get_pipeline_alerts()` 1 INFO (ufc_sales resolving_editions, benign/long-standing)
- pipeline_runs ok=false last 6h: **0**
- security **0/0/0/0** (RLS-off base [], anon/auth-write-on-RLS-off base [], `check_public_security_invariants` [], `check_secdef_anon_execute_violations` [])
- sentinel TS-UUID-48h **0** · trust-health **8/9** (one BREACH = the already-queued HISTORY-BACKFILL-UNMAPPED-SPIKE, below)
- editions FLAT (TS 17318 / AllDay 6191 / Golazos 581 / UFC 518) · FMV TS HIGH+MED **4413** (improving, 4399 nightly) / AllDay 900 (flat, ASK_ONLY parity holding) · FMV/PackEV/offers writers fresh (15:13–15:14Z), fmv_sanity 0
- Vercel **0 ERROR** across 20 recent · Sentry **0 unresolved**
- 1a cross-collection: cohort_mat fresh 04:10Z (158 rows) / overlap_mat 04:25Z / both pg_cron jobs active + today's runs succeeded
- Artifacts: flagship rpc-live-health full payload runs clean (11 insights boards all return rows, leak_48h 0, freshness current); distinct backing objects of the other active artifacts all resolve (v_tracked_wallet_fmv_confidence, topshot_serial_premiums_board, topshot_perfect_mint_premiums_board, v_offer_sanity_flags, allday_scarcity_board, topshot_underpriced_serials_board, v_rewards_economy). **None broken, none repaired.** No schema change today.

## Known live BREACH — ALREADY QUEUED, not re-logging (trajectory note only)
**HISTORY-BACKFILL-UNMAPPED-SPIKE**: `unmapped_resolution_backlog_max` 725/100 BREACH (TS flowty/onchain; oldest 03-31, newest 12:28Z). **Holding at 725** since the nightly baseline — no new `topshot-flowty-sales-history-backfill` (#3) tick since 12:27Z; next ~15:27Z will likely push it higher. Same-program AllDay leg is ALSO accumulating: 118 flowty/onchain + 77 nflallday/onchain_dapper_v1 = ~195 open (was ~135 at nightly) — same root cause (the historical-sales-capture program enqueues edition-unresolvable rows faster than the resolver drains; rows are quarantined OUT of `sales`, NOT corruption). Night pass already owns the fix decision (resolver-coverage / retire-mechanism / pace-cron). All 5 new `*/3h` backfill crons firing on cadence, all ok=true (HISTORY-BACKFILL-WATCHLIST already queued for the eventual watchlist add — cadence now banking 4+ ticks).

## NEW candidate (LOW · CC · route-side)
**PINNACLE-EDITION-KEY-UUID-CAST** — the `/[collection]/edition/[slug]` page's `parallels` + `high_offer` lookups throw `invalid input syntax for type uuid` when the slug is a Pinnacle composite edition_key.
- Source: Vercel runtime errors, route `/[collection]/edition/[slug]`; 2 distinct keys observed (`STAR-OEV1-SWAL:Golden:1`, `PIX-OEV3-TOYS:Brushed Silver:2`), 4 events total since 2026-06-09 (1 event each — very rare; last ~13:4xZ today). NOT in Sentry unresolved.
- Risk read: LOW. The "Parallel Printings" + high-offer sub-sections silently fail (caught) for the affected Pinnacle editions; the page otherwise renders. Persistent (15 days), tiny volume. Confirmed NOT previously in ledger/focus (the existing "invalid input syntax for type uuid" ledger hits are a different GQL `byEditions` context).
- Suggested action: in the get_edition page, resolve a Pinnacle composite edition_key to its `render_id`/uuid before calling `get_edition_subedition_siblings` + `get_edition_high_offer`, OR skip those uuid-typed lookups for Pinnacle composite-keyed editions. Mirrors the 06-22 Pinnacle moment-page colon-id redirect (Item 2, handoff-2026-06-22-audit-fixes).

## Post-ship watch handoff (for night pass — new since nightly baseline)
Prod advanced **37993a1 → 00a0330** (Trevor, ~14:06Z, READY): "fix(media): drop format=mp4 from AllDay momentVideoUrl (image resizer 9401)" — route-only, fixes the Trophy modal AllDay video (sole consumer of the helper). Clean so far: READY, 0 Sentry, no AllDay-video runtime error. Pushed ~25 min after the nightly's docs commit so it's not yet in the ledger. Fold into deeper post-ship watch.
