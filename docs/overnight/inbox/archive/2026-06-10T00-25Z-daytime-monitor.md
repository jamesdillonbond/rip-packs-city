# Daytime monitor — 2026-06-10T00:25Z

Run: `rpc-daytime-monitor` (read-only). ~3.3h after the 21:06Z run. Took over the STALE released `.lock` (08:42Z marker). Big evening CC wave landed after the 21:06Z run (`3fdbf25` → `a553f61`, 13 commits incl. username resolver + fix, pack-EV survivor-bias, top-buyers board, verify burned-moment gate, reconcile 202-wrap, IPFS block) — 20/20 deploys READY, 0 ERROR, prod `a553f61` @23:34Z. **PINNACLE-RECONCILE-TIMEOUT is RESOLVED** (Cowork cron re-enable + `56ad4ff`; `pinnacle_ask_stale_hours` 16.5h → **0.2h**, reconcile ok-ticks 23:51/23:54Z) — already ledger-recorded via `9d371eb`, not re-raised here.

## 🔴 LEAD — DBSAT-00Z: NEW midnight-UTC saturation window, ACTIVE at time of writing (NEW · HIGH · night-pass/operator)
- **Source:** `pipeline_runs` 00:04Z→00:24Z+ · Sentry smoke cluster re-fire ~00:06Z incl. **net-new NEXTJS-A** ("fmv pipeline healthy (analytics_pipeline_health)", first seen 00:06Z).
- **What:** statement-timeout cascade — 00Z hour already has **15+ fails vs 0 in each of 20/21/22/23Z**: `topshot-buyer-backfill` ×2 (00:04, 00:14), `compute-topshot-pack-ev` ×2 (00:13, 00:19 "targets"), `wmc-fmv-populate` 00:13, `analytics-smoke` 00:13, `check-alerts` 00:15 (the alerting fn itself timed out), `pinnacle-nft-resolver` 00:16, `evm-transfers-ingest` 00:19, `topshot-moments-hydrator` 00:22 (N2 signature). Still live: 7 fails / 26 runs in the 10 min before 00:24Z. Monitor's own reads of `v_tracked_wallet_fmv_confidence` timed out 3/3 (00:05–00:12Z) — view fine at 21:06Z (20 rows), so environmental, not a 3rd AF1 structural break; but the `rpc-tracked-fmv-confidence` artifact WILL error if opened during this window.
- **Why it matters:** this is a **4th, previously-unknown DBSAT window** (known: 05–08:30Z, 09–11Z, 18–20Z). The DBSAT re-baseline queued item should now cover 00Z too.
- **Most-likely cause (correlated, not proven):** the **new `topshot-buyer-backfill` cron** went live 22:23Z (every 10 min, ~56s heavy ticks: 300 per-sale `/v1/transactions` decodes + `sales` UPDATEs on the partitioned table, cursor-walking history backward from 06-08). It ran clean 22:23–23:54Z, then began timing out exactly at the 00:00Z daily-anchor pile-on (its 00:14 tick couldn't even read its own cursor). Heavy writer × midnight anchor cluster = cascade.
- **Suggested action (night pass / operator / CC):** (1) confirm the window self-cleared and record its bounds in the DBSAT re-baseline; (2) consider slowing `topshot-buyer-backfill` (10min → 20–30min) or moving it off the daily-anchor band — it's a finite history drain, cadence is a free lever; route is sales-path so code changes are CC/Trevor; (3) re-check `check-alerts` + smoke ticks post-window and resolve the smoke cluster + NEXTJS-A after 24h quiet. NOT touched by this run.

## NEW candidates
1. **BUYERBF-WATCHLIST (MED · night-pass-shippable):** `topshot-buyer-backfill` (new pipeline, first run 22:23Z 06-09) is NOT in `pipeline_cadence_watchlist`. After ~24–48h of observed cadence, INSERT @ ~40m/medium (10-min cadence × ~3 misses + margin). Standard MON1-class monitoring add. Revert: `DELETE FROM pipeline_cadence_watchlist WHERE pipeline='topshot-buyer-backfill';`
2. **B7211FB-VOLUME-WATCH → CLOSEABLE (info):** TS sale volume at the 14–23Z peak was 66–204/hr (normal diurnal range; the post-deploy 4–21/hr was the documented dip). Buyer coverage healthy (most hours 90–100%; partial hours get drained by buyer-backfill). Recommend ledger close.
3. **IPFS-SHIP-VALIDATED (info, positive):** `topshot_ipfs_assets` = 12,546 rows vs 9,136 canonical int-keyed TS editions; `a553f61` (edition-page IPFS block + blog post) READY. No pricing surface touched. Night pass: standard post-ship look at the edition page render + sitemap entry.

## Carried (not re-logged as new)
- **USERNAME-CRON-UNWIRED** — still **0** `pipeline_runs` rows for `resolve-wallet-usernames` even after the `cfe6271` resolver fix (getUserProfile swap). The cron-job.org entry is still missing. Operator.
- `topshot-fmv-populate` — only 2 runs/24h (~6h cadence with drops); 18:50Z tick failed on pool timeout (fire-and-forget so server-side work may have completed); next ~00:50Z tick is the tell (CRON-DROP-WAVE/TFP class).
- `ultimate-fmv-recalc-v1` **recovered** — ran clean 23:49Z after ~38.5h silent (cron drift re-fired). Off `detect_stalled`.
- `compute-topshot-pack-ev` 67 fails/403 runs 24h — known rush class; note its cadence is now ~20/hr (frequency raise working the queue; `pack_ev_board_max_stale_days` 1.4, ok).

## Health snapshot
✓ security **0/0** (all four checks: rls-off [], anon-write-on-RLS-off base tables [], `check_secdef_anon_execute_violations()` [], `check_public_security_invariants()` []) · sentinel TS-UUID-48h **0** (dupe re-mint still stopped) · `detect_stalled_pipelines()` = **[]** (first clean read in days — reconcile + ultimate + TFP all recovered) · `v_rpc_trust_health` **6/6 ok** (pinnacle_ask 0.2h post-fix; pack_ev_max_stale 1.4; fmv_sanity 0) · TS FMV HIGH 549 + MED 2315 = **2864** (flat), NO_DATA 4814 (improving), AllDay HIGH+MED **482** (flat) · unmapped_sales **183** (flat) · DB **6783 MB** (+15 since 21:06Z; day creep continues, watch-only) · 20/20 deploys READY, prod `a553f61` · Sentry 9 unresolved: smoke cluster re-fired 00:06Z (saturation window — see LEAD; incl. net-new NEXTJS-A), NEXTJS-15 (Q4, 6 events, carried), NEXTJS-14 (PINFMV-DRIFT-14, off-limits carry, 17 events incl. 00:06Z tick) · artifacts 17 enumerated; spot-checked backing queries OK (`topshot_pack_reality_top_ev` 6, `topshot_squeeze_board` 500+, `topshot_2025_rookie_index` 61, `v_sale_execution_accounts` populating, sentinel FMV RPC, trust-health) EXCEPT `v_tracked_wallet_fmv_confidence` unreadable during the active saturation window (see LEAD — do not repair; re-validate after the window).
