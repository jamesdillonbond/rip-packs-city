# Daytime monitor — 2026-06-08T22:21Z

Read-only sweep. **One NEW high-priority candidate (a security-posture regression from today's legacy-FMV-retirement ship — actively firing a Telegram alert every ~30 min).** Otherwise platform GREEN: `detect_stalled_pipelines() = []`, sentinel TS-UUID-48h **0**, 20/20 deploys READY (0 ERROR), FMV flat-to-improving, all key artifact backing relations/RPCs resolve post-drop. The evening 18:50–20:54Z DB-saturation window was the known I1/DBSAT transient class and fully recovered. Lock was stale (released 08:17Z by the night pass) → inbox commit permitted this run.

## New candidate

### PINBACKUP-RLS — `pinnacle_fmv_snapshots_backup_20260608` is RLS-off + anon-writable (HIGH-PRIORITY · security regression from today's ship · night-pass-eligible OR operator)
- **Source:** `pg_tables` RLS-off check + `information_schema.role_table_grants` anon/auth-write check + `check_public_security_invariants()` (now returns 9 rows: 1 `rls_off_base_table` + 8 `anon_write_base_table`) + `analytics-smoke.extra.checks[security_all_tables_rls_enabled] = fail`. Attributable to commit `ab2e6f4` ("stop writing legacy pinnacle_fmv_snapshots …") + the legacy-table DROP migrations, deployed ~22:07Z; docs commit `b367e762` confirms "the legacy FMV cluster + table are dropped."
- **What happened:** the legacy-FMV retirement backed up the dropped `pinnacle_fmv_snapshots` to a new public base table `pinnacle_fmv_snapshots_backup_20260608` (427 rows / 136 kB). The backup inherited the default-privilege footgun: **RLS OFF** and full **anon + authenticated INSERT/UPDATE/DELETE/TRUNCATE** grants. It is now the platform's ONLY RLS-off public base table and ONLY anon-write-on-RLS-off table (both prior monitor runs today — 15:10Z + 18:12Z — measured 0/0, so this regressed in the ~18:43Z→21:13Z window).
- **Blast radius:**
  - **Active recurring alert (the material near-term cost):** the `analytics-smoke` `security_all_tables_rls_enabled` check now **fails every tick** (was `ok` at 18:43Z; `fail` at 21:13Z / 21:43Z / 22:13Z and ongoing) with `overall_severity: fail` + **`telegram_sent: true`** → a Telegram alert to Trevor every ~30 min. The main `/api/smoke-test` likely also trips its `check_public_security_invariants()`-mirroring assertion (CLAUDE.md, the 2026-05-30 ci-cron guard). This is the boy-who-cried-wolf risk the monitor exists to prevent — it masks any FUTURE real RLS regression.
  - **Direct exploit: LOW.** It's a 427-row FMV snapshot backup (edition_id / fmv_usd / confidence / computed_at — no PII), no live surface reads it. Worst case: an anon caller TRUNCATEs/corrupts the rollback safety net (the one window the backup matters most — right after a same-day table drop) or inserts junk into a table nothing reads.
- **Risk read:** LOW exploit / MED operational (security-invariant false-positive + alert fatigue). Reversible, additive, DB-only — same class as the shipped S1 anon-revoke. **CAVEAT:** the legacy-FMV retirement is CC/Trevor-owned and <24h fresh (focus.md item 2: "DO NOT touch Pinnacle FMV functions/views/routes"). BUT a REVOKE + ENABLE RLS on a *backup* table changes no FMV logic, no displayed prices, no live reader — it only re-closes the security posture. Night pass should weigh ownership and flag for Trevor either way; do NOT touch the live per-render FMV path.
- **Suggested action (do NOT auto-revert the ship):** hardening — `REVOKE ALL ON public.pinnacle_fmv_snapshots_backup_20260608 FROM anon, authenticated;` + `ALTER TABLE public.pinnacle_fmv_snapshots_backup_20260608 ENABLE ROW LEVEL SECURITY;`. Revert: `GRANT INSERT,UPDATE,DELETE,TRUNCATE ON … TO anon, authenticated;` + `… DISABLE ROW LEVEL SECURITY;`. **Alternative (cleaner, Trevor's call):** if the legacy-table retirement is confirmed stable, `DROP TABLE public.pinnacle_fmv_snapshots_backup_20260608;` (it's a one-day rollback net). Target: `check_public_security_invariants()` back to `[]`; analytics-smoke `security_all_tables_rls_enabled` → `ok`; Telegram security alert stops.

## Corroborated / already-owned — NOT new candidates (logged so the night pass sees the monitor checked them)

- **Evening DB-saturation window 18:50–20:54Z — same I1/DBSAT class, fully recovered (DATA POINT for the DBSAT-0911 re-baseline).** Hourly fails: 13–17Z **0**, 18Z 7/910 (the rush dispatch storm), **19Z 38/80**, 20Z 28/189, 21Z 2/230, 22Z 1/98. All `canceling statement due to statement timeout` / `Timed out acquiring connection from connection pool` / `upstream request timeout` across the DB-read-heavy + indexer set (wmc-fmv-populate 15, compute-topshot-pack-ev 9, pinnacle-nft-resolver 8, pinnacle-listings-reconcile 6, topshot-moments-hydrator 5 = N2, plus a ~19:2x–19:3xZ "upstream request timeout" burst across many indexers that looks like a brief platform brownout). **Every failing pipeline self-recovered** (`last_ok > last_fail`; e.g. compute-topshot-pack-ev last_ok 22:25, hydrator 22:22, wmc-fmv 22:23). **Note for the DBSAT-0911 re-baseline:** this is a SECOND sustained saturation window today, occurring AFTER the 14:55Z cross-collection refresh fix — so the saturation is not 100% confounded by that broken FOR-LOOP refresh; there is a residual evening-window component. Still the same already-owned I1/DBSAT class, not a new candidate; the 06-09 clean-day re-baseline the 15:10Z file called for remains the real test, and the evening window should be included in it.
- **Sentry NEXTJS-1J — "smoke test failed: pack dist page has Sales History" (LOW · reinforces queued SMOKE-EDITION-TIMEOUT).** NEW issue id, 3 events / 3 users, first+last seen ~2h ago (~20:2xZ, the saturation tail), quiet since, culprit `POST /api/smoke-test`. Same family as the queued SMOKE-EDITION-TIMEOUT (NEXTJS-1H, edition-page Recent Sales): a smoke content-assertion that times out under DB load → bypasses SMOKE-RETRY (assertion, not 5xx). Smoke timeouts now span BOTH the edition page (NEXTJS-1H) and the pack-dist page (NEXTJS-1J), both during saturation windows — this strengthens the SMOKE-EDITION-TIMEOUT escalation (bump the smoke per-fetch timeout for the heavier entity pages, or route the abort through SMOKE-RETRY). Not a standalone candidate; fold into SMOKE-EDITION-TIMEOUT. Mark NEXTJS-1J resolved if quiet 24h.
- **DUPE1 sentinel cleared to 0, still 0/hr (CC-owned merge gate well met — noted, not pre-empted).** TS-UUID-keyed-48h = 0 (was 542 at the 08:15Z baseline), uuid_total flat 6,404 (historical fossils). The focus.md DUPE1-merge gate ("< 250 and falling") is comfortably met — CC-owned per Trevor's 06-07 decision.
- **Pinnacle render-FMV timeout fix (`5880eeb`) holding.** `pinnacle_catalog.fmv_computed_at` max 06-08 17:38Z, 1,797 priced — fresh per the daily cadence (next scheduled tick ~10Z 06-09). PIN-SYNC-CRON (wire the daily cron + watchlist) unchanged-queued.
- **LISTCACHE-CRON-DROP** — `-v2` retired (is_active=false) holds (`detect_stalled` clean); residual = cosmetic cron-job.org `-v2` entry removal. Unchanged.

## Clean (no action)
detect_stalled_pipelines() `[]`. `check_secdef_anon_execute_violations()` `[]`. Sentinel TS-UUID-48h 0. Vercel 20/20 READY, 0 ERROR — the full 06-08 wave (legacy-FMV retirement `ab2e6f4`, chain-two address foundation `d079e5b`, HybridCustody read-only verify `bacd652`, FCL account-proof security bind `d425998`) all READY with no attributable regression beyond PINBACKUP-RLS above. FMV flat-to-improving: TS HIGH+MED **2929** (HIGH 556 / MED 2373), NO_DATA **5084↓** (DUPE1-MIT holding); AllDay HIGH+MED **488**, NO_DATA 527. editions flat (TS 15,542 / AllDay 6,191). unmapped_sales 183 (flat). DB 6,633 MB (creep settling — bulk was the 14:55Z `idx_wmc_cohort_cover` build; +17 since 18:12Z; watch-only). Artifact backing relations validated post-drop: `holdings_summary` executes, `v_tracked_wallet_fmv_confidence` 20 rows (no 57014), `pinnacle_scarcity_board` 2,079, `topshot_squeeze_board` 3,139, `cross_collection_deals_board` 326; `rpc-security-drift` correctly surfaces the new backup-table finding (its catalog SQL = the checks above). The 3 functions that grep-matched `pinnacle_fmv_snapshots` (`get_wallet_moments_with_fmv`, `holdings_summary`, `get_set_detail`) reference it only in explanatory comments — executed paths read per-render `pinnacle_catalog` / `wmc.fmv_usd`; no break.
