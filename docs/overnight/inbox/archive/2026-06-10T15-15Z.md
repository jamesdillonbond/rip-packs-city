# Daytime monitor inbox — 2026-06-10T15:15Z

Run context: ACTIVE INCIDENT during sweep. `.lock` present but stale (73 min) — proceeded per >45min rule. Artifact validation abbreviated: 17 artifacts enumerated; per-artifact query replay deliberately skipped this run (DB is actively timing out on simple reads — replaying 17 dashboards' queries would add load and produce false "broken" verdicts). Re-validate next run.

## 1. HIGH — DAYTIME-DBSAT: active DB IO-saturation window, ~10:00Z → ongoing at 15:10Z
- Source: `pipeline_runs` (hourly fails: 10Z=31, 11Z=48, 12Z=57, 13Z=41, 14Z=45 — vs 09Z=1); ~190 statement-timeout/pool fails across ~20 pipelines (compute-topshot-pack-ev 41, pinnacle-nft-resolver 39, topshot-buyer-backfill 26, topshot-moments-hydrator 23, pinnacle-listings-reconcile 16, analytics-smoke 10…). Monitor's own reads of `v_rpc_trust_health` and `fmv_snapshots` freshness ALSO hit 57014 at ~15:09Z — saturation live, not historical.
- `pg_stat_activity` at 15:08Z: PostgREST RPC calls with param signature `(p_collection_id, p_force, p_limit)` active 60s+ in `IO DataFileRead` (matches `populate_wmc_image`, possibly `backfill_wmc_metadata_from_editions` — check `pg_stat_statements` to disambiguate). `wmc-fmv-populate` (which loops `populate_wmc_image` 50k/collection/tick) ran 120x in 12–13Z window.
- This is a NOVEL daytime window — the fixed DBSAT storm was 05–08:30Z overnight and the 06Z wave on the f41caf4 fix verified PASS. Different signature (cold IO reads, not wmc rewrites).
- Possible (unconfirmed) correlation: `a3c1a0c` (fifth wmc call-site swap, deployed ~06:51Z) added a per-wallet `backfill_wmc_metadata_from_editions` JOIN-UPDATE post-pass to the TS backfill route; 12Z had a 543-run spike. Do NOT revert on this alone — premise needs pg_stat_statements confirmation.
- Suggested action (night pass / Trevor): identify the IO hog via pg_stat_statements deltas; if `populate_wmc_image`, consider per-tick limit reduction or cadence dial-down; re-baseline metrics after recovery.

## 2. HIGH — CRON-AUTODISABLE-CASCADE: 8 pipelines stopped firing 09:37–12:50Z (operator)
- Source: `detect_stalled_pipelines()` = 6 entries (first non-empty in days) + last-run scan.
- Silent since: pinnacle-wmc-render-id 09:37Z (329m, med) · drain-fmv-cold-tail 10:17Z · refresh-pack-grail-metrics-mv 10:23Z · backfill-pack-rip-metadata 10:53Z · populate-pinnacle-wmc-fmv 11:03Z (med) · run-insider-detectors 11:26Z (med) · **fmv-recalc 12:08Z (HIGH)** · topshot-fmv-populate 12:50Z.
- Other entries on the same cron-job.org account still fire normally (sales indexers ok 14:43/14:56Z) → per-entry auto-disable after repeated 30s-timeout fails during the saturation window is the likely mechanism (documented in ledger 2026-06-09, with recovery recipe).
- Suggested action (OPERATOR, urgent): open cron-job.org console, re-enable every "Inactive" entry per the 06-09 recipe. Risk: none (re-enable only).

## 3. HIGH — fmv-recalc down ~5h; e3aee28 grail-guard wave-watch CANNOT START
- Source: pipeline_runs. Last OK 10:08Z; 11:08 + 12:08Z ticks failed `step3_delete_chunk_failed` (saturation collateral), then silence (auto-disable suspected, see #2).
- Blast radius: primary sales-path FMV writer — TS/AllDay FMV freshness degrading since ~10Z. Also: `e3aee28` (dampenGrailSpike + Step-6 stale-touch change) deployed ~13:45Z and fmv-recalc has had ZERO runs on the new code — the focus.md item 8(b)/(c) watch metrics can't be measured until the cron is re-fired. First post-re-enable ticks need extra scrutiny (new code + first run = ambiguity; judge by error class).

## 4. HIGH — topshot-fmv-populate: 0 OK runs in 24h, silent since 12:50Z
- Source: pipeline_runs (`last_ok` NULL over 24h window; last attempt 12:50Z). Escalates this morning's carried watch (missed 06:50Z tick). Primary TS marketplace-FMV writer. Likely same auto-disable + its known pool-timeout failure mode under saturation. Operator: re-enable + watch one tick.

## 5. MED — Sentry smoke cluster recurring 15h (saturation corroboration, resolve-after-recovery)
- NEXTJS-1J/1H/1E/A/4/E (~12 events each, last seen ~14:49Z): pack-dist Sales History, edition Recent Sales, detect_stalled, fmv-pipeline-healthy, market API, AllDay overview 200. All POST /api/smoke-test, consistent with DB saturation + the known SMOKE-EDITION-TIMEOUT class. No new non-smoke issue except NEXTJS-1M (/dashboard Load failed, 1 event, known watch) + NEXTJS-1K (/share Load failed, 2 events, 12h ago — same WebKit class, watch).

## 6. MED — stale `.git/index.lock` blocking ALL autonomous git ops (operator, 30 seconds)
- Zero-byte `.git/index.lock` dated 2026-06-09 23:33 (≈16h old), `rm` → "Operation not permitted" from the sandbox (the Q7 un-removable-lock class, last seen 05-31). This run could NOT commit/push this inbox file (it persists on disk via the mount — the night pass reads it locally). Trevor: delete `C:\Users\TDill\rip-packs-city\.git\index.lock` from Windows; otherwise tonight's pass starts NO-COMMIT too.

---

## ADDENDUM 16:00Z (interactive session, Trevor present + authorized) — root cause FOUND + FIXED; items 1/2 re-scoped

- **Item 2 was WRONG (cron auto-disable): retracted.** Console list-page check (no job pages opened, no Advanced tabs): NOTHING is auto-disabled — all 8 "silent" pipelines' entries fire on schedule (only the deliberately-paused Cadence Payer check is Inactive). The silence is server-side: routes return fast 202s ("Successful" sub-second on the dashboard) but their `after()` bodies died 