# Daytime monitor — 2026-06-09T18:06Z

Run: `rpc-daytime-monitor` (read-only). ~3h after the 15:06Z daytime run, ~9.6h after the 08:30Z night-pass baseline. Took over the STALE released `.lock` (08:42Z marker, >45min old — not active). Platform **GREEN except the carried Pinnacle ASK breach, which is STILL WORSENING** + the carried CRON-DROP-WAVE stalls. Security 0/0 (all four checks). Sentinel TS-UUID-48h 0. 20/20 deploys READY, 0 ERROR. Artifact backing queries all resolve.

## 🔴 LEAD — PINNACLE-RECONCILE-TIMEOUT still climbing: ASK now 13.5h stale (trend update on the queued item; CC/operator, off-limits to auto-ship)
- `v_rpc_trust_health.pinnacle_ask_stale_hours` = **13.5h** (breach_at 3) — the breach keeps deepening: **3.8h @08:30Z → 10.5h @15:06Z → 13.5h @18:06Z**. Freshest `ask_source='pinnacle_direct'` ask still frozen at the last successful reconcile **04:39Z** (~13.5h). This is the only one of the 6 `v_rpc_trust_health` metrics in breach; the other 5 are healthy (edition_integrity_flags 4/50, fmv_sanity_flags 0, pack_ev_board_max_stale_days 1.15/2, pct_depleted 0, ts_uuid_dupes_24h 0).
- `pinnacle-listings-reconcile`: `last_ok` **04:39Z**, `last_any` run **11:24Z**, 66 runs / **33 fails** in 24h. So it timed out ~half its ticks (the ~8s service_role statement cap, `canceling statement due to statement timeout`) and **has not even attempted a run since 11:24Z** (~6.7h silent vs 60m threshold — cron-job.org dropped it too, CRON-DROP-WAVE compounding). It is now in `detect_stalled_pipelines()` at 403m silent.
- **The 15:06Z falsification holds and strengthens:** DB saturation fully cleared hours ago (last hour: 217 runs / 1 fail; 0 fails 14–15Z and 16–18Z) yet the ASK is STILL frozen at 04:39Z and the staleness is still rising linearly. This is sustained, worsening degradation of the load-bearing Pinnacle floor/ask surface (load-bearing since the ~23:00Z `04011b3` Flowty-cache retirement), NOT a transient saturation artifact.
- **Action (CC/Trevor — Pinnacle pricing path off-limits to auto-ship; tonight's pass is NO-PUSH-blocked on the route side anyway):** re-firing the cron will NOT fix it — when it ran it timed out every other tick. Per the queued PINNACLE-RECONCILE-TIMEOUT item, the fix is either (1) `SET statement_timeout TO '120s'` on the reconcile RPC (caveat: fires ~every 15min, a 120s hold risks pool contention — validate across a rush), or preferably (2) make reconcile **incremental** (only changed listings) so it never approaches the 8s cap. Not auto-shipped/reverted. Already queued — this is a trend update, not a new item.

## CRON-DROP-WAVE — still stalled, morning auto-recovery prediction not holding (operator; carried)
- `topshot-fmv-populate`: **1037m / ~17.3h silent** (last run 00:50Z; was 857m at 15:06Z) — still tripping its 480m TFP-WATCH threshold. Fire-and-forget route returns 202; cron-job.org dropped its ~6h :50 tick and the operator has not re-fired since. Operator: re-fire "RPC TS FMV Populate".
- `ultimate-fmv-recalc-v1`: **2132m / ~35.5h silent** (last run 06-08 06:35Z; info severity, ~daily). Now ~1.5 daily cycles missed — drifting past "expected daily" into the same external-cron-drop pattern. Operator: confirm/re-fire if a fresh ultimate-edition FMV pass is wanted.
- These two are the full `detect_stalled_pipelines()` set besides the Pinnacle-reconcile LEAD. Both external-trigger drops, no route crashes, bounded impact (TS core FMV stays fresh via fmv-recalc, which logged ok at 18:08Z).

## Low / hygiene (NEW this run)
- **USERNAME-CRON-UNWIRED (LOW · operator/CC, part of today's ship wave the night pass watches):** today's `3fdbf25` shipped `/api/cron/resolve-wallet-usernames` (the @handle populator broadening `analytics_resolve_usernames`), but it has **zero `pipeline_runs` rows** — its cron-job.org entry is not wired yet, so the wallet→@handle resolution populator isn't actually running. The migration + route are deployed (15ca7915 prod, READY) and the new `UserLabel` UI falls back to truncated addresses harmlessly, so no user-facing breakage — but the intel surface stays under-populated until the cron fires. Operator: add the cron entry (or confirm it's intentionally manual for now). Expected for a same-day ship; flagging so it isn't forgotten.

## ✅ Positives / clean-on-read
- **b7211fb post-ship watch still PASS:** `topshot-sales-indexer` 68 runs / **0 fails** in 24h, last ok 18:03Z; the new per-sale `/v1/transactions?expand=result` buyer decode is not throttling. `v_sale_execution_accounts` resolves (populating). b7211fb-VOLUME-WATCH effectively closeable (confirmed recovered to peak at 15:06Z).
- **Today's daytime ship wave (dapper.market / on-chain-intel handoff Items 1–6, `b7211fb`→`15ca7915`, all 20/20 READY) still clean on read:** `topshot_pack_reality_top_ev` (re-keyed by today's `b40ce05` Item-4 migrations) still resolves (6 rows); no new Sentry signature attributable to the wave (only the carried NEXTJS-14 + NEXTJS-15); no new pipeline-fail signature. Night pass owns the deep post-ship watch (highest-attention: Item-4 pack-EV migrations + Item-3 username cron's first runs once wired).
- **PIN-SYNC gate (a) holding:** `pinnacle-sync` last ok at its daily 10:07Z tick, 0 fails — the `5880eeb` render-FMV `statement_timeout 120s` fix is durable. Unblocks the queued PIN-SYNC-CRON watchlist INSERT + the (b) `pinnacle_fmv_stale_hours` trust-health tripwire (night-pass-eligible).
- **GIT-IDENTITY-DRIFT (carried, LOW):** today's CC feature commits still authored as `rpc-daytime-monitor <monitor@rippackscity.com>` rather than Trevor — cosmetic, reset `git config user.name/email` at the next CC session. Already logged 15:06Z; not re-logging as a new item.

## Health snapshot
✓ security **0/0** (rls-off none · anon-write-on-rls-off none · `check_secdef_anon_execute_violations()` clean · `check_public_security_invariants()` clean) · sentinel TS-UUID-48h **0** (total 6406, flat — DUPE1 re-mint stopped) · editions TS 15542 / AllDay 6191 / Golazos 581 / UFC 446 (identical to 08:30Z baseline) · TS FMV (latest-per-edition, all editions) HIGH 551 + MEDIUM 2338 = **HIGH+MED 2889** (flat vs 2917 baseline), NO_DATA 4921 (improving from 5029), STALE 239, ASK_ONLY 1000, SALES_ONLY 19 · AllDay HIGH+MED **484** (flat) · `fmv_sanity_flags` 0 · writers fresh (fmv-recalc 18:08Z, topshot-sales-indexer 18:03Z/0 fails, allday-fmv-populate 18:02Z/0 fails, offers-sweep 18:02Z/0 fails, compute-topshot-pack-ev 18:07Z) · unmapped_sales 183 (flat) · DB **6743 MB** (+12 since 15:06Z, +56 since 08:30Z baseline — slow creep continues, watch-only) · 20/20 deploys READY, 0 ERROR (prod `15ca7915` ~13:05Z; no new deploys since) · Sentry: 2 unresolved, both known/queued (NEXTJS-14 = PINFMV-DRIFT-14 off-limits, last seen 3h ago; NEXTJS-15 = PIN1/Q4 listing_resolution_failures, last seen 1h ago) — no new/spiking · artifact backing queries validated (v_tracked_wallet_fmv_confidence 20 = AF1-v2 holding, topshot_squeeze_board 3140, topshot_pack_reality_top_ev 6, topshot_2025_rookie_index 61, v_sale_execution_accounts 1) — 17 artifacts, none broken.

Pipeline fails last 6h were all the **12:47–13:43Z DBSAT / connection-pool window** (wmc-fmv-populate lock timeout, compute-topshot-pack-ev + topshot-moments-hydrator + analytics-smoke statement timeouts, the allday-indexer/hybrid-custody pool blips) — all recovered; last hour clean (217 runs / 1 fail). One isolated post-window fail: `pack-events-ingest-backfill` 17:16Z (`allday_pack_purchases_backfill: aborted due to timeout`) — single self-recovering backfill-walk timeout, not a wedge.

`detect_stalled_pipelines()` = 3: `pinnacle-listings-reconcile` (medium, 403m — PINNACLE-RECONCILE-TIMEOUT LEAD), `topshot-fmv-populate` (medium, 1037m — CRON-DROP-WAVE/TFP-WATCH), `ultimate-fmv-recalc-v1` (info, 2132m — daily, CRON-DROP-WAVE).
