# Monitor inbox — 2026-07-19 (first tick of day, ~08:19 PDT / 15:19Z)

Health GREEN. `rpc_ops_snapshot()`: security 0/0/0/0, trust **15 metrics 0 breaches**,
`stalled_pipelines []`, sentinel 0, pg_cron failures `[]`, Vercel 0 ERROR, DB 10,121 MB.
First-tick extras both PASS: trust health all `ok`; cross-collection refresh fresh
(cohort 04:10Z / 154 rows, overlap 04:25Z, both jobs active, today's runs `succeeded`).
Artifacts 17, `rpc-panini-squeeze` payload validated live (9/9 keys, data_fresh 13:47Z).
Concurrent CC/Cowork session pushing all morning (mount HEAD `018df7c9` behind
origin `505e70a9`) — weight everything below accordingly.

---

## CLOSEABLE — DUNE-SELLER-RECOVERY-EXECUTE-400 is RESOLVED (no action needed)

The night pass's queued MED item is fixed. `sales-seller-recovery-dune` ticks at
**13:47Z ok=true (windows_done 5)** and **14:47Z ok=true (windows_done 4)**, draining
backward (now at window `2026-04-16..2026-04-23`). Closed by the CC Dune `date`→`text`
param fix (ledger entry at line 9). The last 400 was 12:47Z, i.e. pre-fix.

**Checked and NOT a finding:** the 720,201 / 720,340 ms tick durations are *by design* —
`HARD_BUDGET_MS = 720_000` under `export const maxDuration = 800` in the route, an
explicit stop-before-the-ceiling budget, not a lambda-cap near-miss.

---

## 1. SNAPSHOT-INSTITUTIONAL-WALLETS-TS-DIFF-TIMEOUT — MEDIUM (new, same-day regression)

**Source:** `pipeline_runs`, pipeline `snapshot-institutional-wallets`.

Clean on 07-17 (09:15Z, 10:07Z) and 07-18 (08:52Z). Then **both** 07-19 runs failed
identically:

- `09:08Z` — `diff_95f28a17 exhausted retries: canceling statement due to statement timeout`
- `10:07Z` — same, `op_attempts: 3`, `wallet_hint: 0x4d2c9216f1dca098` (NBATopShotCommunity),
  `pages_walked: 233`, `pairs_diffed: 3`, `elapsed_ms ~142,000`, `function_version: 2`

`95f28a17` is the Top Shot collection UUID, so this is the TS-collection diff for one
whale wallet crossing its statement-timeout budget.

**Risk read:** not a contention blip — it exhausts 3 retries *deterministically* on two
consecutive runs, where the prior two days were clean. The run still completes partially
(`partial: true`, other pairs diffed), so blast radius is one wallet's institutional
snapshot going stale, not a pipeline stop. Rising-cost class: whichever query backs
`diff()` has grown past the budget for the largest TS holder and will not self-heal.

**Suggested action (night pass / CC):** measure the diff query for that wallet+collection
first — if it is a scan that grew, the honest fixes are chunking the diff or raising the
per-op timeout for it specifically; do NOT blanket-raise the pipeline timeout. If the
wallet is simply too large to diff in one statement, degrading it to a skip-with-notice
beats a silent partial.

---

## 2. DB-POOL-EXHAUSTION-HAS-CROSSED-INTO-USER-FACING-PAGES — MEDIUM (re-prioritization, not a new bug)

**Source:** Sentry + `pipeline_runs`.

The long-known daytime-contention family now has a **user-visible** symptom, which it did
not have when it was last triaged:

- `JAVASCRIPT-NEXTJS-1Z` — *"pack detail bundle unavailable: Timed out acquiring connection
  from connection pool."*, culprit `GET /[collection]/pack/dist/[distId]`. **First seen 23h
  ago** = a new issue class, not a long tail.
- `JAVASCRIPT-NEXTJS-1Y` — *"team detail unavailable: canceling statement due to statement
  timeout"*, culprit `GET /[collection]/team/[slug]`, 5 events over 5 days, **last seen
  ~13:00Z today**.

Pipeline-side corroboration — pool *exhaustion* (distinct from statement timeouts) appears
only in the two heaviest wave hours in 24h:

| hour (UTC) | runs | fails | pool-exhaustion fails |
|---|---|---|---|
| 07-19 10:00Z | 1,042 | 11 | **3** |
| 07-18 15:00Z | 725 | 4 | **3** |
| all other hours | 313–803 | 0–3 | 0 |

The 10:00Z cluster took out `pinnacle-nft-resolver`, `topshot-base-parallel-probe`,
`allday-unmapped-resolver`, `compute-topshot-pack-ev` and `sales-counterparty-backfill`
within ~9 minutes.

**Risk read:** these pages throw retryable errors rather than 404ing (the 07-14 contention-404
fix), so a visitor sees a failure, not wrong data — but with the surface public since 07-17
this is now a real first-impression cost, and traction is the only gate that matters.

**Suggested action:** the lever is **reducing concurrent load in the 10:00Z hour, not adding
reads or indexes**. The already-queued *non-wave wallet-backfill driver* (~48% of the
`wallet-backfill*` fan-out; strongest lead `lib/allow-list/prewarm.ts` calling with
`force=true`) is the highest-value thread and should be pulled before any new perf work.
Explicitly NOT auto-shippable: touches ingest fan-out.

---

## 3. LIVE-HEALTH-ARTIFACT-CREDITS-A-DROPPED-TABLE — LOW (artifact repair)

**Source:** `C:\Users\TDill\Claude\Artifacts\rpc-live-health\index.html:240`.

The footer credits its sources as *"…`fmv_snapshots`, `pinnacle_fmv_snapshots`,
`pinnacle_fmv_history`…"*. `pinnacle_fmv_snapshots` was **dropped 2026-06-08** —
`to_regclass('public.pinnacle_fmv_snapshots')` returns NULL.

**Risk read:** display-only. The SQL body is correct (the Pinnacle leg reads
`pinnacle_fmv_history`, and the in-page caption at line 225 says so), so nothing errors.
But it is the text someone reads while debugging the dashboard, and it points at a table
that has not existed for six weeks — same honesty class as the 07-19 wallet-backfill
watchlist `notes` correction.

**Suggested action:** one-line footer edit during the night pass's artifact-repair step.

---

## 4. CORRECTION for the artifact-replay unblock note — INFO (no work, record only)

The night pass recorded that `C:\Users\TDill\Claude\Artifacts` is readable via
"`Read`/`Grep`". Refinement measured this run: **`Read` works with an exact Windows path;
`Grep` does NOT** — it rejects the path as outside the session's connected folders, same as
sandboxed `bash`. So artifact replay is strictly file-by-file via `Read`; there is no way to
pattern-sweep the estate for, say, every SQL identifier at once. Worth carrying so the next
run does not spend a call rediscovering it.

---

## Confirm-only (no action)

- `sales-counterparty-backfill` **healthy** — 3 claim-timeouts in 5h against ~57 good ticks
  (~95%), recovering 88–120 rows/tick, cursor marching back through 2026-04-07. The timeouts
  land in the contention hours above; folds into item 2.
- `candy-offers-indexer` 12:50Z ok (47 offers, 2 bidders, SOL $75.96) · `candy-sales-indexer`
  12:20Z ok / 0 sales (correct by construction under the quest hold).
- `pinnacle-sync` self-healed — absent from `stalled_pipelines`, `pinnacle_fmv_stale_hours`
  5.0 / breach 30. The night pass's "the 10:07Z tick decides it" is settled green.
- Sentry `JAVASCRIPT-NEXTJS-1C` (smoke: RLS-on + no anon write) last seen 20h ago, i.e. the
  pre-fix create-then-normalize window; live security invariants are `[]`×4 now.
- All 22 migrations stamped `20260719` are additive (new tables/views/functions); none drop
  or rename an artifact dependency.
