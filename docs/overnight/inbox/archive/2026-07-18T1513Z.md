# Daytime monitor inbox — 2026-07-18 ~15:13Z (first-tick-of-day, ~08:06 PDT)

Run: rpc-daytime-monitor, first tick (~08:06 PDT) — Section 1a daily extras RAN. No skew (shell 15:06Z ~= DB now() 15:07-15:13Z ~= newest TS sale minutes ago ~= newest FMV write 15:08Z). Lock present but 7h STALE (nightly pass, HELD 08:03Z, not marked RELEASED but >45min → not blocking) → inbox commit proceeds. origin/main `b14f4376` (Trevor actively pushing UX fixes ~15:05Z). Security 0/0/0/0. **Health GREEN except one down pipeline (pinnacle-sync) whose downstream trust metric is ~1h from breaching — Candidate 1, lead item.**

Section 1a extras (first-tick): trust-health 15/15 ok (0 breaches; via rpc_ops_snapshot). Cross-collection refresh VERIFIED FRESH — cohort_mat 2026-07-18 04:10Z (154 rows), ts_set_overlap_mat 04:25Z; rpc-ccm-step1 (04:10Z) + step2 (04:25Z) both `succeeded` today. pg_cron failures [].

## Candidate 1 (HIGH · escalating · operator + night-pass) — PINNACLE-SYNC missed its 07-18 10:07Z daily tick; Pinnacle FMV recompute 29h stale, breaching the 30h trust threshold within ~1h
- **Source:** `detect_stalled_pipelines()` / rpc_ops_snapshot — `pinnacle-sync` last run **2026-07-17T10:07:11Z** (silent 1740 min > 1560 threshold, medium). `pipeline_runs` has NO 07-18 row for it at all (07-17 + 07-16 10:07Z both ok=true, renders_priced ~2095). Downstream: `pinnacle_fmv_stale_hours` = **29 / breach 30** and rising 1:1 with the gap; `pinnacle_fmv_recalc_render_all()` (the daily recompute, run ONLY by this pipeline) hasn't run in 29h.
- **Root cause (classified, NOT today's ship):** the pinnacle-sync after()-pattern fix (`8ceca936`) deployed **14:44Z** — ~4.5h AFTER the missed 10:07Z tick — so OLD code was live at 10:07Z. Old code logged ok=true whenever invoked (proven by 07-16/07-17), so the ABSENT row = the cron-job.org trigger did not fire at 10:07Z. This is the recurring external **cron-job.org trigger-dropout** class (05-31 / 06-08 / 06-09 / 07-07 / 07-11), not a code regression. The 14:44Z fix is NOT implicated; it only means the NEXT tick, if it fires, won't hit cron-job.org's 30s timeout.
- **Blast radius:** Pinnacle FMV recompute stale during launch-QA; `pinnacle_fmv_stale_hours` flips trust_health to BREACH within ~1h and stays breached until the **07-19 10:07Z** daily tick (~19h) unless the recompute is fired sooner. Pinnacle catalog + render-floor pipelines are separately healthy (`pinnacle_render_floor_stale_hours` 1.4; `populate-pinnacle-wmc-fmv` hourly ok — but that only PROPAGATES existing FMV to wmc, it does not recompute).
- **Suggested action:**
  - **Night pass (DB-only, fastest, clears the breach now):** `SELECT pinnacle_fmv_recalc_render_all();` — pure in-DB SECDEF recompute (~15s, the exact leg the daily route runs; renders_priced ~2095). Refreshes Pinnacle FMV and clears `pinnacle_fmv_stale_hours` without waiting ~19h. Not a logic change — just triggers the established pipeline manually. (Confirm the fn takes no args before running.)
  - **Automatic self-heal:** the 07-19 10:07Z tick now runs the after()-fixed route (returns 202, recalc in after()) so it self-heals THEN if the trigger fires — but leaves the breach standing ~19h.
  - **Operator:** inspect the cron-job.org "RPC Pinnacle Sync" execution history from ~10:07Z 07-18 to confirm the trigger dropout / re-fire it. (Secret-bearing console — operator only; do NOT drive it from the monitor.)

## Candidate 2 (LOW · watch, DRIVER CONFIRMED — no action) — TS FMV HIGH+MED continued declining 5237 → 3939 → 3357 (explained by the 07-12→14 sales-spike cooldown + Saturday dip)
- **Source:** rpc_ops_snapshot fmv_by_collection nba_top_shot HIGH 966 + MEDIUM 2391 = **3357** (15:07Z) vs 3939 (06:07Z 07-18, the 0620Z tick's Candidate 2) vs 5237 (metrics-latest 07-17 13:44Z). Now ~-36% over ~25h, and STILL falling since the 0620Z watch → the multi-tick continuation that tick said would "graduate to investigation."
- **Driver (confirmed this run):** TS daily sales spiked **07-12 3,182 → 07-13 6,090 (peak) → 07-14 4,990**, then cooled to **~2,800-3,100/day** (07-16 3,107, 07-17 2,828, 07-18 ~3,360 extrapolated / Saturday). HIGH/MEDIUM confidence keys on recent 7/30d sales counts + velocity; as the 07-13/14 volume peak ages and momentum drops back to baseline, editions redistribute HIGH/MED → LOW/SALES_ONLY/STALE. Tracked-FMV set size is STABLE (buckets sum ~19,325 vs 19,429 TS editions), so this is internal redistribution, not editions dropping out.
- **Risk read: BENIGN, no action.** FMV freshness green (`topshot_fmv_stale_hours` 0.3), `fmv_sanity_flags` 0, fmv-recalc healthy (2 stale fails/24h), no FMV-methodology change in any recent deploy. This is genuine market-driven confidence redistribution off a spike, not a broken recompute. Downgrades the 0620Z watch from "graduate to investigation" to "driver confirmed = sales cooldown; keep a light eye, close if it stabilizes near the new ~3,300 baseline." Only investigate if H+M keeps falling WHILE TS sales velocity recovers (decoupling = a real input/threshold regression).

## Noted / NOT re-logged (known, resolved, expected, or already in an earlier inbox)
- **rpc-backfill-pinnacle-mint-acquisitions statement-timeout** (0620Z Candidate 1) — `check_pgcron_recent_failures()` is now **[]**; the 05:19Z fail aged out / next tick ok. Self-recovered; the 0620Z ready 600s-fix candidate still stands for the night pass if it recurs, not re-raised here.
- **topshot-moments-hydrator** 9 fails/24h + **compute-topshot-pack-ev** 7 — known carried classes (upstream GetMintedMoment GQL errors / pool-timeout under contention); latest ticks ok, `stalled_pipelines` shows neither silently stopped. No action.
- **populate-pinnacle-wmc-fmv** 2 fails/24h + **candy-editions-ingest** 1 — both single-tick transients (upstream timeout / a 07-17 20:24Z DAS 401) surrounded by ok=true; candy daily cron fired 08:40Z ok=true. Not findings.
- **golazos_sales / ufc_sales** resolving_editions INFO — benign known (edition-resolution bridge pending; UFC Flow market frozen by design).
- **candy_mlb 125 editions** — EXPECTED (Candy Drop-1 discovery landed 07-17 per CLAUDE.md); not an anomaly.
- **Sentry NEXTJS-1C** "smoke test failed: public base tables RLS on + no anon write" — 3 events, first+last seen 23h ago (single burst, no recurrence); live security invariants 0/0/0/0. Stale transient contention-window smoke read, self-cleared. No action.
- **Vercel** — many CANCELED deploys today are the normal ignored-build-step cancels from rapid Cowork/Trevor pushes (0 ERROR-state); current prod `b14f4376` READY.

## Artifact validation
Artifact HTML IS readable this run (via the Read tool on `C:\Users\TDill\Claude\Artifacts`, though not under the sandbox mount). **rpc-live-health payload validated** — reconstructed + ran its backing-object probe: all 12 /insights board views return rows (squeeze/set_squeeze/pack_reality/rookies/market_idx/first_mint/cross_collection/pinnacle_scarcity/offer_spread/deals/trophies/new_collectors), and FMV/Pack-EV/offers freshness live (fmv_snapshots 15:08Z, pack_ev_latest 15:13Z, edition_offers 15:07Z; cached_listings_v2 / unmapped_sales / pinnacle_fmv_history queryable). No artifact-breaking DROP/rename shipped today (07-18 ships = market route, pinnacle-sync after(), weekly-maintenance split, cache s-maxage, UX — no schema renames). Remaining active artifacts read subsets of the same now-validated core objects; none flagged. rpc-panini-squeeze (pre-launch panini_*) not separately re-validated.

## Health snapshot (15:07-15:13Z)
security 0/0/0/0 · trust 15/15 ok, 0 breaches (pinnacle_fmv_stale 29/30 imminent — Candidate 1) · stalled_pipelines [pinnacle-sync] · pg_cron failures [] · cross-collection refresh fresh (step1/step2 succeeded 04:10Z/04:25Z) · sentinel_ts_uuid_48h 0 · editions TS 19,429 (+4 :: benign) / AllDay 6,190 / Golazos 575 / UFC 518 / candy_mlb 125 · FMV TS H+M 3,357 (Candidate 2) / AllDay H+M 826 · DB 9,877 MB (+232 vs 07-17 9,645, benign churn) · Sentry 1 unresolved (NEXTJS-1C, stale/self-cleared) · Vercel prod b14f4376 READY, 0 ERROR.
