# RPC daytime monitor — candidate harvest 2026-07-06T00:11Z

Run context: scheduled fire stamped ~2026-07-05T03:06Z but executed live at 2026-07-06T00:11Z (~21h session suspend/resume gap — shell `date`, DB `now()`, and the newest `pipeline_runs.started_at` all agree within seconds, so NO live clock skew, just a delayed start; the first `rpc_ops_snapshot` of this run was ~21h stale and re-baselined). Health GREEN at execution: security 0/0/0/0, trust 16/16 ok (breaches []), sentinel TS-UUID-48h 0, leak_48h 0, `check_pgcron_recent_failures()` [], Sentry 0 unresolved/24h, prod `c09f9693` READY, rpc-live-health consolidated payload resolves every panel. TS editions 17,678->18,144 (+466 over the gap) = ongoing 07-05 subedition `::` cataloging (by-design; sentinel + leak both 0), NOT a writer leak.

## Candidate — sharpens the 2026-07-05T18:06Z item (NOT a fresh duplicate)

**ULTIMATE-FMV-RECALC-V1-CRON-NOT-FIRING (INFO/LOW; ops/operator).**
- Source: `detect_stalled_pipelines()` + `get_pipeline_alerts()` — `ultimate-fmv-recalc-v1`, silent 2496 min (~41.6h), last run 2026-07-04T06:35Z, watchlist threshold 1440 min (daily). Auth: RPC_ADMIN_TOKEN.
- New evidence vs the 18:06Z "missed the 07-05 tick" note: it is STILL silent ~6h later AND the alert detail now reads "Repaired this session after 49h silence" — yet the last logged run is still 07-04 06:35Z. So a manual repair ran but did not stick; the SCHEDULED daily trigger is not firing. This is a dead/unscheduled cron, not a one-off miss.
- Blast radius LOW: Ultimate is the smallest TS tier; general `fmv-recalc` is fresh (topshot_fmv_stale 0h) and covers the broad FMV surface; no trust-health breach. Only Ultimate-tier FMV goes stale.
- Suggested action (night pass / operator): verify the scheduling entry for `ultimate-fmv-recalc-v1` exists + is enabled + RPC_ADMIN_TOKEN auth is valid; re-enable/re-add if missing. A single manual re-run will not fix the recurring miss.

## Reconciliation — no action (prevents chasing a stale item)

**BLOCKED 07-05 code deploys (flagged by the 06:12Z monitor tick) are RESOLVED.** They were captured ~21h before this run's live execution. Current deploy window (07-05 15:32Z -> 07-06 00:04Z) shows a healthy pipeline: newest code deploy `c09f9693` (drain-orchestrator NEXT_PUBLIC_SUPABASE_URL fix) READY and serving prod, with 7+ other READY code deploys through 07-05 evening; later commits are docs-only (correctly CANCELED via ignoreCommand). No BLOCKED deploys remain. The serial=0 NULL-ingest fix is live via both its DB coercion trigger and downstream code. Night pass: do not chase the BLOCKED-deploys item.
