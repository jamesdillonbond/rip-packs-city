# Inbox — 2026-06-13T00:06Z (daytime monitor, 17:06 PDT tick)

Sweep verdict: PLATFORM GREEN. DBSAT-IO-EXHAUSTION-0612 recovery HOLDING 9h+ (17Z–23Z fails 0–2/hr; 18Z seed wave absorbed 1,401 runs with 7 pool-timeout fails ≈0.5%, cleared by 19:03Z). detect_stalled `[]`; security 0/0 (RLS + anon-write); sentinel TS-UUID-48h **0**; trust health **8/8 ok** (incl. ts_uuid_dupes_created_24h=0); Sentry **0 unresolved seen in 8h**; latest prod deploy READY (`62735e5`); the 4 ERROR deploys 13:53–14:33Z are the closed DEPLOY-ERROR-X4. Streaks: analytics-smoke 16/16-ok/8h, offers-sweep 24/24, fmv-recalc 30/31 (1 pool-timeout 18:48Z), TFP ok 19:15Z (next gate tick 01:15Z). FMV: TS HIGH+MED **3,259** (up from 3,251 @21:15Z), AllDay 651. DB 4,356 MB (+45 vs 4,311 baseline, normal).

## 1. UFC-WMC-NULLKEY — datapoint amendment (no new action; night-pass TOP PRIORITY per focus §11a)
- **Source:** live 00:06Z measurement: UFC wmc null `edition_key` = **3,837/4,584 — FLAT since the 19:16Z measurement** (~5h, zero further regression).
- **Read:** the regression is wave-coupled, not continuous — `wallet-backfill-ufc` has been idle since 18:59Z (174/174 ok runs in 8h) and the count didn't move while idle. The next 6h seed wave (~00:50–01Z) is the decisive observation: if null count RISES after the wave, the b28a22f after()-chain enrichment is still being starved/skipped on the seed-refresh path (skip_cached short-circuit suspect); if FLAT, new rows aren't the leak and only the backlog needs the forced re-walk.
- **Suggested action:** night pass should measure null_key immediately after the ~01Z wave completes before choosing between diagnose-the-path vs straight `?force=true` re-walk.

## 2. LISTCACHE-SILENT-0612 — recovery datapoint (closure candidate)
- **Source:** `pipeline_runs`: `topshot-listing-cache` 4/4 ok runs in last 8h, last ok 00:00:11Z.
- **Read:** the cron entry is firing again (was 0 runs since 00:19Z 06-12). Cadence looks ~2h, within its 360m watchlist threshold; detect_stalled clean.
- **Suggested action:** night pass can mark LISTCACHE-SILENT-0612 CLOSED after confirming 2+ more ticks; note whether ~2h is the intended post-stagger cadence (vs historical 20m) or the entry needs a frequency check on cron-job.org.

## 3. MONITOR-ARTIFACT-ACCESS — process gap, LOW (new)
- **Source:** this run: artifact HTML lives under the OneDrive Artifacts folder, outside the monitor session's connected folders — `list_artifacts` works but the embedded single-payload queries can't be read, so per-artifact payload validation (§1b of the monitor prompt) is impossible from a scheduled run. Payload queries are not documented in-repo either (grep found none).
- **Risk:** a schema change can silently break a dashboard and the monitor won't see it until Trevor opens the artifact.
- **Suggested action (night pass or Trevor):** export the 12 active artifacts' payload queries to a repo doc (e.g. `docs/operations/artifact-payloads.md`) the monitor can read and execute, or connect the Artifacts folder to monitor sessions. This run validated backing surfaces directly instead (v_rpc_trust_health 8/8, FMV histogram, pipeline stats, deploys — all green).

## Notes
- Sandbox hygiene: `/tmp/rpc` is a leftover clone owned by nobody:nogroup (from the 06-12 night-pass run) that this user can't remove; this run cloned to `/tmp/rpc-mon` instead. Harmless, self-resolves on VM recycle.
- eba6491 (seed-wave cohort split) post-ship: additive/no-param behavior confirmed healthy — 18Z wave ran full-size, clean. Cron-side 4-entry split (Item 2, Cowork/operator) still pending per ledger.
- Not run this tick (not first-of-day): §1a daily extras already covered earlier today; trust health was run anyway as artifact-proxy validation — 8/8 ok.
