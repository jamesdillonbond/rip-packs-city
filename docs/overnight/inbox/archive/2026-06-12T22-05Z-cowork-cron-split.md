# Inbox — 2026-06-12T22:05Z (Cowork interactive session)

## 1. SEED-REFRESH COHORT SPLIT EXECUTED — verify tonight's first cohort wave
- **Source:** wave-pacing Item 2 (docs/handoff-2026-06-12-wave-pacing.md); route support shipped by CC as `eba6491` (deploy READY). Cron side executed 22:00Z via the console API: 4 new entries — cohort 0/4 job 7801778 @ :45 (hours 0,6,12,18 UTC), cohort 1/4 job 7801780 @ :59 (same hours), cohort 2/4 job 7801781 @ :13 (hours 1,7,13,19), cohort 3/4 job 7801782 @ :27 (same hours). Legacy job 7491038 DISABLED (not deleted — rollback = re-enable it + disable the 4). Server-truth verified via GetJobs (nextExecution 00:45/00:59/01:13/01:27Z tonight). docs/operations/cron-schedule.md updated.
- **Verify tonight (00:45–01:40Z window):** each cohort tick logs a seed-wallet-refresh done-line with `cohort=K/4` and processed ≈ 1/4 of active wallets (~63); wallet-backfill-multicollection dispatches cluster into 4 sub-windows instead of one herd; no 401s (the Bearer header was donor-cloned) and no 200-that's-actually-/login (check X-Matched-Path on the first tick); wave-window fails/hr at or below the calm baseline. The 06:45Z+ wave is the decisive one (daytime IO window).
- **If a cohort tick 401s or hits /login:** re-enable legacy 7491038, disable the 4 cohort entries, and queue for the operator — do not leave the herd unserved.
- **After one clean day:** delete legacy 7491038 (operator or queue it).

## 2. UFC-WMC-NULLKEY — forensic added (22:15Z, supersedes the "regression" framing in the 19-30Z file)
- **Measured:** the 3,837 null-edition_key UFC rows span **79 wallets** with last_seen back to **2026-05-08** — these are OLD rows never enriched, NOT a fresh writer leak. Non-null rows: only 747/4,584 (41 wallets). Even Trevor's wallet (0xbd94cade097e50ac) has all 247 UFC rows keyless. Top null holders: 0x60daa8bea8cf25bf (1,201), 0xed8707e2ae5bba5a (304), 0x9b879eee67d839cc (276).
- **Second signal:** `pipeline_runs` has ZERO rows for any UFC wallet-backfill pipeline (only ufc-listings-indexer / ufc-sales-indexer / ufc-listing-cache / ufc-stub-thumbnail-resolver) — either the UFC details-walk child logs under another name or it never logs/never runs.
- **Re-read:** b28a22f (maxDuration fix) treated the symptom; the real question is whether the UFC details-walk EVER writes edition_key (UFC Cadence gotcha: generic borrowNFT, Traits call FAILS — the walk may structurally never capture editionID). The 3,150→3,837 "rise" is probably backfill adding NEW keyless rows for new/refreshed wallets, same root cause.
- **Action:** diagnose lib/wallet-backfill-helpers.ts + the UFC details path (lib/cadence/ UFC scripts) for whether edition_key is ever derivable/written; if the Cadence path can't yield it, this is a CC item (likely needs the UFC edition mapping from a different source). A forced re-walk alone will NOT fix it if the walk never writes keys — verify on one wallet before recommending the fleet-wide re-walk.
