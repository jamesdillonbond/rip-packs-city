# Daytime monitor candidates — 2026-07-05T06:12Z (~23:06 PDT late tick)

Clock verified NO skew (shell 06:06Z ≈ DB now() 06:06Z ≈ app-stamped sales 06:03Z / fmv 06:06Z). Late-window app-launch tick; Section 1a (8am extras) skipped. Lock RELEASED (nightly 08:16Z), commit permitted.

Health **GREEN**. security **0/0/0/0** (invariants/secdef_anon/rls_off_base/anon_write all []); trust **16/16 ok** (breaches []); `detect_stalled_pipelines()` []; `check_pgcron_recent_failures()` [] (the DAYTIME-CONTENTION `rpc-remap-misattributed-sales` + `rpc-allday-ev-corrected-refresh` pg_cron legs have RECOVERED — latest tick ok, no longer surfaced); sentinel TS-UUID-48h **0**; leak_48h **0**; editions TS **17,678** / AllDay 6,191 / Golazos 581 / UFC 518 (non-TS flat; last-7h TS +created = 187/187 canonical `::N` subedition form, 0 hyphen-UUID — F9 write-time-split continuation, by-design); FMV TS H+M **~4,877** (HIGH 1306 / MED 3571, improving) / AllDay 837; DB **8,000 MB** (+37 vs 00:06Z, benign ts_history_backfill drain, decelerating; crossed 8.0 GB — passive Pro-Micro disk-headroom watch, carried); Sentry **0** new unresolved/24h; conflation guard **501** (down from 504 @00:06Z — F9 write-time split holding, not re-climbing); phantom serial-0 rows **0** (07-05 serial→NULL trigger live).

Vercel prod serving **`9c9c3dd1`** (de-conflate set 263) READY; runtime errors all 28 KNOWN families (url.parse DEP0169 / 300-60-120s route timeouts / DBSAT statement-timeouts on player·team·pack·edition / wallet-backfill schema-cache retries / wmc dup-key / FMV-recalc creep), latest clustered 00:46–01:43Z overnight-contention window, **no new class** from the daytime wave.

**rpc-live-health artifact — VALIDATED** (consolidated payload run directly). db_active 1; all 12 insights surfaces resolve (squeeze 501 / set_squeeze 239 / pack_reality 7 / rookies 61 / market 501 / first_mint 501 / cross_collection 1 / pinnacle_scarcity 501 / offer_spread 501 / deals 460 / trophies 501 / new_collectors 50); FMV/PackEV/offers fresh 06:02–06:13Z; leak_48h 0; fmv_by_collection resolves. Other artifacts (OneDrive path, unmounted) validated indirectly — the daytime wave was additive (de-conflate remap / sets_summary refresh / golazos title repair / serial recovery); no view/RPC dropped or renamed; rpc_ops_snapshot GREEN.

## Candidate 1 — BLOCKED-CODE-DEPLOYS-NOT-ADVANCED (LOW; operator/CC; rolling-release promotion)
- **Source:** Vercel `list_deployments` — the two NEWEST code commits are `state=BLOCKED` and never advanced to prod: `5c1b0db9` "fix(sales): write NULL not phantom serial 0" (~03:52Z) and `82704b02` "fix(topshot): seed current-era subedition targets" (03:24Z). Newest READY+serving prod = the earlier `9c9c3dd1` (de-conflate set 263); everything after it is BLOCKED (2 code) or CANCELED (3 docs, ignore-command). Prod SHA confirmed via runtime-error lastDeployment=dpl_72V9 (9c9c3dd1).
- **Risk read:** LOW — impact contained by DB companions already applied via MCP: the serial `0->NULL` BEFORE-trigger is live (verified phantom_serial0_2026 = 0) and `seed_topshot_recent_base_subedition_targets` fn is present in DB. So only the *route halves* are stranded: (a) `/api/sales-indexer` + `/api/ingest` writing NULL at ingest — redundant with the live trigger; (b) the daily `drain-conflated-subeditions` **step 1c** wiring — the proactive current-era subedition self-heal won't fire on the daily cron until deployed (the existing 2 seeds still cover current collisions; guard stable at 501). Matches the KNOWN rolling-release staging pattern (ledger "VERCEL Rolling Releases operator note", ~07-03): BLOCKED canaries were superseded before advancing and will NOT auto-advance; HEAD is now docs-only so a plain redeploy is ignore-command-skipped.
- **Suggested action (operator/CC — the night pass cannot promote; /promote 422s under rolling releases):** force a real build of current HEAD (push any code-touching change, or Vercel "Redeploy" the `5c1b0db9` SHA without cache) then advance/promote the release so the two route halves serve — OR confirm they're intentionally deferred (given the DB backstops, nothing is currently broken). Verify after promote: serving prod SHA == `git log` HEAD; step-1c telemetry appears in the next daily `drain-conflated-subeditions` run.

## Carried / observed — do NOT re-log
- **allday-pack-opens-backfill cursor_stalled HIGH** = EXPECTED terminal state, already dispositioned by the 2026-07-05 CC ledger entry ("NOT broken again / no action"): cursor parked at spork floor 137390146, every tick ok=true/done=true/0 rows; the 68 fails/24h are all pre-`26982cc`-fix. `get_pipeline_alerts()` flags HIGH only because the cursor is permanently at the recoverable floor (sub-floor opens pruned/unreachable — intended tradeoff). No action.
- **OVERNIGHT-CONTENTION cluster** (01:03–01:43Z tick): fmv-recalc / analytics-smoke / compute-topshot-pack-ev / topshot-buyer-backfill / lock-check-batch / wallet-username-resolver / check-alerts / populate-pinnacle-wmc-fmv each failed once then recovered (all latest_ok=true; 6h scan + snapshot agree). Carried DAYTIME-CONTENTION-CLUSTERS-BROADENING / DB-growth-contention watch; remap + allday-ev pg_cron legs recovered (pgcron check now []). Not re-log.
- **classify-acquisitions-multicollection** flapping (8 fails/24h, latest ok) — carried night-count 4, do NOT close.
- **F9 realign + 323 base-circ reconcile wave** (07-04 → early 07-05) — night-pass verify-only; DB-side clean this tick (security 0/0/0/0, edition_integrity 4/50, impossible_parallel 1/3, fmv_sanity 0, conflation guard 501 holding, leak 0). No standalone fix.
