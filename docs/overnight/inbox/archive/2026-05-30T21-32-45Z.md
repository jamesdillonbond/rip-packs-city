# RPC daytime monitor — inbox 2026-05-30T21:32:45Z

Run: `rpc-daytime-monitor` (READ-ONLY daytime pass). Operator absent; autonomous.
HEAD at sweep: `f9388c7`. Branch: `main` (origin/main even, 0/0).

NOTE — overnight scaffold absent: `docs/overnight/{ledger.md, focus.md, metrics-latest.json}` do not exist yet, so (a) no dedupe against a ledger / Declined list was possible — candidates below were de-duped only against `CLAUDE.md`'s documented known/intentional/monitored items; (b) no drift baseline existed — headline metrics are captured below as a first reference. `metrics-latest.json` was NOT written (the night pass owns it).

## Health snapshot (~21:32 UTC)
- **Deploys:** latest production READY (HEAD `f9388c7`; prior READY `5af7580`). One historical ERROR deploy `dpl_9BVVoDCcUfHtyDGUHSAQLrxuSQba` (commit `01b3878`) — already fixed by the next commit `1b7cfde` (Phase D shims) + 9 READY deploys since. Not actionable.
- **Pipelines:** 66 active in 48h. 6 transient failures in last 6h, all "Timed out acquiring connection from connection pool" / `time_budget_exceeded`, clustered 18:00–18:24 UTC; every affected pipeline has logged clean runs since (fails_6h ≤ 2 each). See P5.
- **Cadence:** only notable gap is `compute-laliga-pack-ev` silent ~16h (P4). All `*/20` pipelines current (≤30 min since last run).
- **Sentinel — TS Edition Writer Leak (48h):** 1,842 → WARN band (<2000), DOWN from 2,695 (2026-05-30 pulse). Known Item B2 (`compute-topshot-pack-ev` → `seed_topshot_editions`; rows kept inert by `editions_block_topshot_uuid_dupe_trg`). Monitored + improving — not re-raised.
- **FMV (Top Shot, latest-per-edition):** HIGH 196 / MEDIUM 588 → **HIGH+MED 784** (≈ baseline 778). LOW 8,446 · NO_DATA 6,100 · STALE 826 · ASK_ONLY 114 · SALES_ONLY 1.
- **Editions:** nba_top_shot 16,271 · nfl_all_day 6,191 · laliga_golazos 581 · ufc_strike 446 (Pinnacle lives in `pinnacle_editions`, not counted here).
- **unmapped_sales:** 144 open · 138 within 7d · **0 new in 24h** (resolver keeping up; flat).
- **Supabase advisors:** security 14 ERROR / 63 WARN / 24 INFO; performance 4 WARN / 354 INFO (329 `unused_index`).
- **Sentry:** 3 unresolved issues total (P2, P3, and the P5-related smoke capture). No spike.

## Candidates for the night pass

### P1 — [security] 14 SECURITY DEFINER views flagged ERROR (regression vs documented "0 security ERRORs")
- **Source:** `get_advisors(security)` — `security_definer_view` ERROR ×14: `topshot_squeeze_board`, `topshot_set_squeeze_board`, `topshot_pack_reality_top_ev`, `topshot_pack_reality_stats`, `topshot_pack_reality_dist`, `topshot_2025_rookie_players`, `topshot_2025_rookie_index`, `topshot_2025_rookie_cohort_stats`, `topshot_first_mint_trophies`, `topshot_first_mint_trophy_stats`, `cross_collection_cohort_stats`, `pinnacle_scarcity_board`, `collection_chains`, `pack_distributions_v`.
- **Risk:** LOW practical exposure — every one backs a PUBLIC-by-design `/insights` surface (or `collection_chains`, anon-granted by design); the data is meant to be anon-readable. But it is a posture regression vs `CLAUDE.md` (May-3 "17 SECDEF views dropped"; stated "0 security ERRORs") and Supabase rates it ERROR (a SECDEF view runs with the definer's rights, bypassing the caller's RLS). Today's HEAD `f9388c7` ("SECDEF anon-EXECUTE regression guard") covered SECDEF *functions*, not these *views* — the gap is open.
- **Suggested action:** triage the 14 — preferred: `ALTER VIEW <v> SET (security_invoker = on)` on each, then verify each still returns rows for anon on its public route (the underlying tables must permit anon SELECT for invoker-mode to keep working). If invoker-mode breaks a public read, consciously document the view as an intentional SECDEF public view and accept the lint. Deliberate per-view decision; not a blind flip.

### P2 — [sentry/noise] `listing_resolution_failures_inserted` firing steadily on `/api/pinnacle-listings-indexer`
- **Source:** Sentry `JAVASCRIPT-NEXTJS-15` (26 events / 2 users; first seen ~23h, last seen ~9 min — active). Culprit `GET /api/pinnacle-listings-indexer`.
- **Risk:** LOW — almost certainly an instrumented capture when Pinnacle listing-resolution rows enter the retry queue (`CLAUDE.md` documents the ~1,582 capped Pinnacle retries as known-benign), not a crash. Concern is Sentry-error-level noise masking real errors.
- **Suggested action:** inspect the capture call in the pinnacle-listings-indexer route; if it's an expected condition, downgrade `captureException` → breadcrumb / `console.log` or add to a known-benign filter. No behavior change.

### P3 — [sentry/likely-fixed] `TypeError: Cannot destructure property 'data' of null` on `/pinnacle/moment/[id]`
- **Source:** Sentry `JAVASCRIPT-NEXTJS-1B` (3 events / 1 user; first + last seen ~4h ago, no recurrence). Culprit `GET /pinnacle/moment/[id]`.
- **Risk:** VERY LOW — this is the exact "destructuring null on bad RPC fallback" bug commit `01b3878` + the `/pinnacle/moment` fix chain (`26d5968` SECDEF RPC, `fe96d4b` decodeURIComponent) addressed; it stopped firing ~4h ago as those READY deploys shipped.
- **Suggested action:** confirm zero recurrence over a full 24h and mark the Sentry issue resolved. No code change expected.

### P4 — [pipeline/cadence] `compute-laliga-pack-ev` silent ~16h
- **Source:** `pipeline_runs` — last_run 2026-05-30 05:30 UTC (957 min; 0 runs/6h) while `compute-allday-pack-ev` / `compute-topshot-pack-ev` run ~every 20 min.
- **Risk:** LOW — Golazos has no confirmed primary pack-drop path (`CLAUDE.md`: Golazos pack events unverified, no primary drops observed), so the EV computer may legitimately idle / run low-cadence; not user-facing.
- **Suggested action:** confirm the intended cron cadence / that its cron-job.org entry is enabled. Benign if low-cadence by design.

### P5 — [known-theme, data point only] connection-pool pressure
- **Source:** `pipeline_runs` (6× connection-pool / time-budget timeouts 18:00–18:24 UTC: pack-events-ingest, topshot-stub-resolver, allday-listings-retry, compute-topshot-pack-ev ×2, pack-events-ingest-backfill) + Sentry `JAVASCRIPT-NEXTJS-B` (smoke "sales pipeline healthy" failed, last seen ~3h, Seer super_low) + `get_advisors(performance)` `auth_db_connections_absolute` WARN.
- **Risk:** LOW / KNOWN — the recurring connection-pool / statement-timeout transient mode `CLAUDE.md` documents repeatedly ("real lever = DB connection-pool pressure"); all affected pipelines recovered, fails are low single digits. NOT breakage. Logged only as corroboration — the performance advisor now explicitly flags connection saturation.
- **Suggested action:** none required beyond awareness. Do NOT treat as breakage.

---
_Generated by `rpc-daytime-monitor`. READ-ONLY pass; this inbox file is the run's only write._
