# Daytime monitor — 2026-05-31T18:06Z

Run mode: read-only daytime sweep. `docs/overnight/.lock` is present but STALE (mtime 08:18Z, ~9.8h old ≫ 45min) → treated as released, so the inbox write proceeds. OneDrive artifact dir IS readable this run (it was NOT at 15:14Z). Commit/push is likely unavailable (no GitHub creds + the Q7 `.git/*.lock` files); if so this file is disk-only and the night pass picks it up locally from the persisted mount.

## Health: ✓ HEALTHY (no breakage)

- **Security:** 0 RLS-off public base tables; 0 anon/authenticated write grants on RLS-off base tables (relkind r,p). Clean.
- **Deploys:** current prod = `c0e2179` (docs commit) READY. Three new READY deploys since the 15:14Z run — `c3ad47c` (funnel+ops + pipeline-silence detection), `2339f30` (fix: await funnel/outbound inserts so rows land), `c0e2179` (docs). No new ERROR; the only ERROR deploy is the long-superseded morning `26fa6be` (06:08Z).
- **Pipelines:** all ingest chains within cadence. `topshot-sales-indexer` 32m ago (Q3 fully recovered, 6 runs/6h); `fmv-recalc` 24m ago (23/6h, primary FMV writer healthy); `pinnacle-resolve-buyers` 71m ago (recovered from its traction-push 284m stall). Only 4 transient fails/6h (pack-events-ingest, compute-topshot-pack-ev targets-timeout, pack-events-ingest-backfill pool-timeout, topshot-moments-hydrator) — all connection-pool/statement-timeout with same-tick recovery. `detect_stalled_pipelines()` (now live via c3ad47c) flags only `evm-transfers-ingest` (medium, 70m/60m — Q6 Base-429, no product consumer).
- **FMV:** fresh ~24m via fmv-recalc. TS HIGH+MED 796 (↑ from 786 @15:14, 776 baseline), NO_DATA 5898 (↓ from 5962/6055). AllDay HIGH+MED 249 (↑ from 248/243). Flat-to-improving.
- **Sentinel** TS-UUID-keyed-48h = 633 (↓ from 821 @15:14, 1099 baseline; WARN band but improving, rows inert by trigger).
- **unmapped_sales** 144 unresolved / 138 within-7d / 0 new-24h (flat vs baseline). DB 5861 MB. Editions flat (TS 16287 / AllDay 6191 / Golazos 581 / UFC 446).
- **Artifacts:** 10 enumerated; OneDrive readable. Every backing data path validated healthy this sweep — squeeze 2634 / pack_reality 1·98·6 (rpc-insights-health), pipeline_runs (pipeline-reliability/live-health), security catalog 0/0 (security-drift), fmv confidence (fmv-watch), Vercel + DB-size (deploys-and-cost), editions (cross-collection/trophy/my-wallet). Both new-today RPCs return: `get_rpc_traction_snapshot()` (rpc-traction) ✓, `get_insights_hub_stats()` ✓. Today's DDL was additive/property-only (new RPCs, funnel_events table, view security_invoker flips) — no column/view drop, so no artifact could have silently broken. None flagged broken.

## Status flags for the night pass

- **Q3 (topshot-sales-indexer stall) — stays RESOLVED.** 6 runs/6h, last 17:40Z; TS sales fresh; not in the stall list.
- **Q4 (NEXTJS-15) — Pinnacle half WORKS, but the issue will NOT go 24h-quiet → see candidate C1.** Pinnacle `edition_key_not_in_editions_table` inserts = 0 in the last 3h (fix held); the new `edition_key_unmapped` reason (7/3h, unresolved by design) is the genuinely-unmappable tail and is rate/reason-gated. The events still hitting NEXTJS-15 (~16:06Z) come from `/api/allday-listings-indexer`, which Q4 deliberately left ungated. Do NOT mark NEXTJS-15 resolved on the "24h quiet" criterion until C1 is addressed — it can't go quiet.

## New candidates

- **C1 · [LOW] NEXTJS-15 (`listing_resolution_failures_inserted`) keeps firing from the *ungated* AllDay indexer — Q4 only gated the two Pinnacle routes.** Source: Sentry `JAVASCRIPT-NEXTJS-15` (culprit `POST /api/allday-listings-indexer`, last seen ~16:06Z, 1006 events / 20d) + `listing_resolution_failures` (AllDay `cadence_fallback_cap_hit` 11/24h, 2 new/3h, **unresolved=0 → self-healing**). Risk: low — pure Sentry/observability noise; the underlying AllDay failures resolve themselves; no functional/user impact. Effect: ledger-Q4's "mark NEXTJS-15 resolved after 24h quiet" can never trigger while AllDay keeps feeding the shared fingerprint ungated. Action: apply the same spike(>25/tick)/unexpected-reason `captureMessage` gating the Q4 fix gave `pinnacle-listings-indexer`+`-retry` to the AllDay indexer's capture call, OR give AllDay its own Sentry fingerprint, so the shared issue only pages on a real AllDay spike. Ownership: ingest-route code (operator/CC; the change is to the capture call / observability, not data logic).

- **C2 · [LOW] `funnel_events` has 0 rows all-time despite instrumentation (`c3ad47c`, ~15:16Z) + the await-fix (`2339f30`, ~16:36Z) being live ~3h/1.5h.** Source: `funnel_events` total=0, last_24h=0, latest=null. Risk read: most likely simply NO anon traffic in the window — RPC is ~13 users pre-launch and the lockdown means only the logged-out home + `/share` + `/insights` emit events, so honest-zero is plausible, not necessarily a bug. BUT the table has never been proven to persist a row in prod, and the await-fix's stated purpose was exactly to stop the fire-and-forget insert from being dropped on Vercel. Action: fire one manual beacon to `POST /api/track-funnel` (or load the logged-out home once) and confirm a single row lands; if it still doesn't, the await fix didn't take. Can't distinguish no-traffic from broken while read-only. Ownership: ~1-min operator/CC verification.

## Minor watches (not candidates)

- 18:00–18:08Z brief connection-pool / statement-timeout cluster (compute-topshot-pack-ev targets, pack-events-ingest-backfill, a one-off `get_insights_hub_stats` query timeout) — transient, all recovered (the two RPCs return fine standalone); the documented connection-pool behavior, not a new break.
- `snapshot-institutional-wallets`: still 1 run/24h (06:00Z) — known cron-reliability watch (also flagged 15:14Z + 2026-05-29); it did run today. Not new.
- `compute-laliga-pack-ev` (Q2): last 05:30Z — low / by-design Golazos cadence.
- `topshot-fmv-populate`: last 14:46Z (~3.4h) — secondary FMV writer, not in the cadence watchlist; fmv-recalc covers FMV freshness, so low-signal. Watch only.
- DEP0169 `url.parse()` deprecation (logged by the 15:14Z daytime run) — still open upstream, not re-logged here.
