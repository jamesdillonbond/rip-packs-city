# Daytime monitor inbox — 2026-06-03T05:30Z

Run: `rpc-daytime-monitor` (READ-ONLY sweep). Working tree: shared repo (no bot clone). Lock: ~16h-stale RELEASED marker → took over. Baseline: `metrics-latest.json` @2026-06-02 13:42Z (night pass); cross-checked vs the 06-03T01:49Z + 06-02T15:14Z daytime inboxes. Current prod HEAD `6e4638e` (docs), latest code ship `6e90f3f`. **Clock note:** the sandbox `date -u` ran ~2h behind the DB this run (sandbox 03:08Z vs DB now 05:30Z); all ages/times below are anchored to the authoritative DB `now()`.

## Verdict: ✓ HEALTHY — platform green; the 06-03T01:49Z gas-wallet ALERT (A1) has SELF-RECOVERED; 1 NEW low candidate (PIN1). detect_stalled_pipelines()=[]; 0 pipeline fails in the last 90m; security 0/0; 12/12 artifacts OK.

---

## NEW candidate

### PIN1 · [LOW · operator/CC — not auto-shippable as route logic] `NEXTJS-15` (Pinnacle listing-indexer) Sentry gate trips on `cadence_capped` throughput deferrals, not true failures
- **Source:** Sentry `JAVASCRIPT-NEXTJS-15` (`listing_resolution_failures_inserted`, culprit `GET /api/pinnacle-listings-indexer`, `collection:disney_pinnacle`) — re-entered the unresolved set (the 01:49Z inbox had it absent). 3 gated events in ~2h; last 05:00Z. Corroborated by `pipeline_runs.extra`.
- **Diagnosis (sharpened from the run-by-run `extra`):** pinnacle-listings-indexer (every 20m) sits at **0 queued_failures most ticks** — then took a one-off inflow burst: `05:00Z queued_failures=38` (`cadence_capped:27` + `edition_key_unmapped:11`, `unexpected_reason:false`) which crossed the Q4 `SENTRY_SPIKE_THRESHOLD=25` and fired; the very next tick `05:20Z queued_failures=23` (all `cadence_capped`) correctly stayed under. So the gate is mostly working — but `cadence_capped` items are **deferred** by the per-tick cadence budget (`cadence_attempted:12`), not terminal failures; they drain on subsequent ticks. Counting them toward the spike threshold lets a transient, self-draining inflow burst trip a warning.
- **Risk read:** LOW. No user-facing break (Pinnacle FMV is served from `pinnacle_fmv_snapshots`; listing-resolution backlog is internal and self-draining). `pinnacle-listings-indexer` runs are all `ok=true`. This is the same Sentry-gating-tuning class as Q4/C1, not pricing/destructive — but it touches the route's capture logic, so it's operator/CC, not night-pass auto-shippable.
- **Suggested action (operator/CC):** in `app/api/pinnacle-listings-indexer/route.ts`, exclude `cadence_capped` (a deferral) from the value compared against `SENTRY_SPIKE_THRESHOLD` — count only genuinely-terminal reasons (`edition_key_unmapped`) toward the spike, and treat `cadence_capped` as throughput backpressure (log-only, or raise the per-tick cadence budget so bursts drain in one tick). Optionally mark `NEXTJS-15` resolved afterward and watch 24h. If left as-is it's occasional low-noise, not a fire — file is for the record so a chronic version is catchable.

---

## Observed during sweep — no new action

- **A1 (gas payer wallet) SELF-RECOVERED — reconcile in ledger.** The 06-03T01:49Z inbox alerted that `0x73f55c4450b8d466` had drained to 0.000 FLOW and `cadence-payer-balance-check` was failing every 30m since 20:00Z (06-02). It was **topped up between 01:30Z and 02:00Z**: the check went `01:30Z fail → 02:00Z/02:30Z/03:00Z/.../05:00Z all ok=true` (latest 05:00Z, `ok=true`). A new prod deploy `6e4638e` "docs: stop FLOW drain + monitor noise handoff" also landed ~05:19Z. The acute dry-wallet emergency is over. **Ledger N3 stays open** (operator decision: stop the retired `cleanup-storefront-wallets.mjs` drain + decide whether to keep funding / silence the 30-min check) — re-draining is expected if the cleanup driver is still running.
- **N2 still open, did NOT trip this window:** `topshot-moments-hydrator` had **0 fails in the trailing 6h** (the 00:00Z rush didn't hit the candidate-read statement timeout this time). Do NOT revert the materialized-CTE fix; deeper fix (statement_timeout bump / supporting index / further view optimization) remains operator/CC.
- **P1 still queued, not tripping:** `evm-transfers-ingest` ran within window; `detect_stalled_pipelines()` `[]`. SHIP-eligible next in-window night run as logged (60→150m watchlist).
- **L1 (06-02T15:14Z inbox) unchanged:** `league-drift-detection` cron-wiring question — operator/CC, no new info this sweep (still a single 05-31 14:00Z run; not in `pipeline_cadence_watchlist`).
- **Transient cron-rush cluster (no action):** 4 single fails at the 06-03 00:00Z rush — `wmc-fmv-populate` (lock timeout), `pinnacle-listings-reconcile` (pool), `compute-allday-pack-ev` (pool), `pinnacle-nft-resolver` (statement timeout) — all self-recovering, none logic faults, none deploy-attributable. Known recurring class. 0 fails in the last 90m.
- **Sentry:** 1 unresolved issue total = `NEXTJS-15` (PIN1 above). `NEXTJS-1F` (/dashboard "Load failed") no longer in the unresolved set.
- **Vercel:** 20/20 recent prod deploys READY, 0 ERROR. Today's code ships (`6e90f3f` STALE-confidence JSON-LD gate, `64e3f4a` packs cheapest-sort, `0e1da2d` smoke soft-fail) all READY; the newest commits are the 06-03 FMV-sweep docs + `6e4638e` FLOW-drain handoff. No recent-ship regression (none correlate with the gas-wallet or the 00:00Z DB timeouts).
- **Artifacts — 12/12 validated (1 new since last run):** every backing query re-run succeeds + returns sensible data. NEW `rtr-pack-finder` (RTR daily-quest pack finder over `pack_table_rows`): 60 rows / 9 +EV / cheapest $2 — OK. Insights views non-empty (deals 131, offer-spread 5563, squeeze 3099, rookies 61, market 554, pack-reality-top-ev 90, pinnacle-scarcity 103, cross-cohort 1 stat, first-mint-trophy 1 stat); `v_fmv_sanity_flags` = **1 row** (the expected 8:62 mis-key kept, the 74:2650 false-positive dropped — v2 working, F1/F2 still queued for sign-off); `get_rpc_traction_snapshot()` OK; my-wallet wmc 18,451 / 5 collections; `funnel_events` 43. None edited (validate-only).

## Drift vs 06-02 13:42Z baseline (all flat or improved)
- FMV TS HIGH+MED **933** (baseline 933, =); NO_DATA **4442** (4634) ↓ improving. AllDay HIGH+MED **275** (274, ~flat); NO_DATA 522 flat.
- FMV fresh: TS + AllDay latest `computed_at` 05:22Z (<1m at sweep) — fmv-recalc healthy.
- editions TS **16,344** (+10) / AllDay 6,191 / Golazos 581 / UFC 446. DB **5,993 MB** (+27 vs baseline, normal). sentinel TS-UUID-48h **45** (flat, <250). unmapped_sales **278** total (271 baseline, +7 normal).
- Security **0/0** base tables (RLS-off + anon/auth write-grants both empty). The ~25 anon-readable SECURITY DEFINER insights views are the by-design distribution wedge — not a finding.

## Infra note (Q7, already tracked)
- `docs/overnight/.lock` is a ~16h-stale RELEASED marker (embedded text confirms RELEASED; sandbox can't unlink it — the known Q7 Windows↔sandbox fragility). Did not block this run. Left untouched.
- Sandbox clock lagged the DB by ~2h this run (see header) — flagged in case it recurs; all findings were anchored to DB `now()`, so no impact on the sweep.
