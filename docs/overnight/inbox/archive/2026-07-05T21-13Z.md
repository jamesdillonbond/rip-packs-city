# Daytime monitor inbox — 2026-07-05T21:13Z (~14:13 PDT, tick 3 of day)

Platform GREEN, 1 trust BREACH (wave-correlated, in-flight — see candidate 1). Security 0/0/0/0 (invariants/secdef-anon/rls-off-base/anon-write-holes all []), `check_pgcron_recent_failures()` [], Sentry 0 unresolved/24h, Vercel prod `1bc4732` READY (no ERROR deploy in the 20-deploy window; CANCELED = docs ignoreCommand, BLOCKED = superseded concurrent), Vercel runtime errors all known-benign families (DEP0169 url.parse warning; heavy-backfill 300s timeouts; wmc idempotent-upsert dup-key on `wmc_wallet_moment_unique_idx`; pack-detail/edition connection-pool + statement timeouts) — no new error class on current prod. sentinel TS-UUID-leak 48h **0**. rpc-live-health payload validated LIVE this tick (all 22 backing relations + `get_pipeline_alerts()` resolve, 0 missing after today's schema churn); rest of estate validated 12/12 at 18:06Z, no schema-breaking renames in today's wave.

The heavy 07-05 daytime CC subedition wave CONTINUES (Population A remap 1,733 wmc/45 moments/28 sales; Population B base-parallel probe edge fn + 15m Vercel cron; collision-knot orchestrator wiring `1bc4732`; AllDay serial on-chain rewrite v17; special-serial owner resolver Path-B; serial=0->NULL sentinel) — all deploys READY, DB-clean. Post-ship watch of the full wave is the night pass's job.

Headline metrics: security 0/0/0/0; trust 15/16 (1 BREACH, candidate 1); FMV TS HIGH+MED **4,915** (1322+3593, improving from 4,894 @18:06Z / 4,785 @07-04) / AllDay 832 / UFC 15 / Golazos 4; editions TS **18,144** (+464 vs 18:06Z; +654/2d, of which 654 of 655 new = `::` subedition parallels from the live cataloging wave; sentinel-leak 48h 0 / ts_uuid_dupes_24h 0 = NOT a writer leak) / AllDay 6,191 / Golazos 581 / UFC 518; DB **8,134 MB** (+31 vs 18:06Z, benign backfill + Population-B probe queue). pipeline fails 24h all transient — every noisy pipeline's LATEST run ok=true (fmv-recalc / compute-topshot-pack-ev / analytics-smoke / topshot-buyer-backfill / wallet-username-resolver all 21:0xZ ok).

## 1 NEW candidate (LOW)

### 1. TOPSHOT-IMPOSSIBLE-PARALLEL-SERIALS-BREACH (LOW; wave-correlated in-flight; night-pass post-ship-watch)
- Source: `v_rpc_trust_health` / `rpc_ops_snapshot()` metric `topshot_impossible_parallel_serials`; the 3 offending rows queried directly.
- Signal: metric flipped **ok -> BREACH (3/3)** since the 18:06Z tick (was <=2 then; threshold 3). It counts TS sales keyed onto a `::` parallel edition whose `sales.serial_number > editions.circulation_count` (the F1 parallel-mis-attribution guard — "pages a future regression"). The 3 rows:
  - `259:8818::18` circ 29, sale serial 36, source offer_fill, sale ingested 2026-06-26, **edition created 2026-07-04 23:23Z**
  - `257:8654::18` circ 1, sale serial 7, source onchain, sale ingested 2026-06-23, **edition created 2026-07-04 23:23Z**
  - `118:4134::8` circ 1, sale serial 9, source onchain, sale ingested 2026-06-14, edition created 2026-06-21
- Risk read: LOW / almost certainly transient. These are OLD sales (ingested Jun 14/23/26) remapped onto FRESHLY-cataloged `::` parallels by today's live de-conflation — 2 of 3 parallel editions were created yesterday 23:23Z. Their `circulation_count` is the documented max-observed-serial FLOOR seed, which sits below the real serials (36>29, 7>1, 9>1): the authoritative per-parallel circulation (GQL `circulationCount`, GREATEST-raise via `backfill-topshot-subedition-circulation`) has not yet raised circ above these serials, OR the sale is on the wrong subedition and the ongoing remap will re-key it. Blast radius tiny — 3 sales / 3 `::` editions -> impossible `#36/29`-style serial display + slightly-off per-parallel serial premium on those specific parallel pages ONLY; NOT core FMV / deal boards / pack-EV / sentinel-leak / UUID (all clean). NOT a novel outage.
- Suggested action (night pass): this is post-ship watch of the 07-05 subedition wave. Verify whether the per-parallel circulation backfill / de-conflation clears these 3 within a tick or two (expected -> close). If any persist after the circ backfill runs, decide per-row: raise that parallel's `circulation_count` to authoritative GQL circ (if the serial is genuinely in-circ), OR re-key the sale to the correct subedition (if mis-keyed). Do NOT ship blind — subedition/FMV-adjacent + off-limits sales-path; the guard is correctly doing its job. Metric sits exactly at threshold, so expect it to oscillate around 3 during the active wave.

## Carried / already-logged (NOT re-logged)
- ULTIMATE-FMV-RECALC-V1-MISSED-TICK (LOW) + BADGE-CATALOG-STALE-429 (LOW) — both logged in the 2026-07-05T18-06Z inbox, both STILL open this tick (ultimate-fmv-recalc-v1 silent ~38.5h since 07-04 06:35Z, threshold 1440m; topshot-badge-catalog last run 07-04 21:38Z ok=false 429, no run since). No new info; night pass drains per the 18:06Z file.
- apply-fmv-haircut 06:31Z daily pool-timeout (overnight-contention transient, re-fires 07-06); classify-acquisitions-multicollection AllDay statement-timeout flap (night-count 4; latest 21:06Z tick ok=true); ALLDAY-V1-UNMAPPED-DRIFT (AllDay unmapped_open held out of `sales`, owned); ufc_sales resolving_editions (info, 122/24h); the standing owned/operator/gated queue in ledger.md. STEER honored: SERIAL-FMV weekly by design; evm-429 benign.
