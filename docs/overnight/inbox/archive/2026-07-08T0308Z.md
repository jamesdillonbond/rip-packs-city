# Daytime monitor candidates — 2026-07-08T03:08Z (~20:08 PDT Jul 7, late app-launch tick)

**Platform is GREEN except one fresh cron-job.org trigger dropout on the Pinnacle ASK writer. Security 0/0/0/0, trust 16/16 ok (breaches []), core ingest fresh (sales lag 5m, FMV lag 2m).**

## LOW-MED — PINNACLE-RECONCILE-CRON-DROP-20260708: pinnacle-listings-reconcile silent since 01:24Z, approaching pinnacle_ask_stale_hours breach ~04:24Z
- **Source:** `rpc_ops_snapshot()` stalled_pipelines + `detect_stalled_pipelines()` + `pipeline_runs`. `pinnacle-listings-reconcile` (cron-job.org */15 at 9,24,39,54; 96 runs/24h baseline) last ran **2026-07-08 01:24:13Z ok=true (3.4s)**, then STOPPED firing — silent ~104 min at 03:08Z vs 60-min watchlist threshold.
- **Not a failure, a trigger dropout:** the last 8 runs were all ok=true (23:09Z→01:24Z, 3–34s), so this is NOT cron-job.org auto-disabling after a failure streak (the 06-09 PINNACLE-RECONCILE-TIMEOUT mechanism) — the entry simply stopped being triggered. Same recurring **cron-job.org trigger-dropout class** as yesterday's overnight TOP FINDING (CRONJOB-ORG-TRIGGER-DROPOUT-20260707) and the CRON-DROP family in the ledger. This is a FRESH instance — the 21:06Z tick had logged this pipeline recovered (firing 20:54Z).
- **Blast radius (why it's not just LOW):** this is the load-bearing on-chain Pinnacle ASK writer (Flowty cache retirement consolidated Pinnacle ASK onto `ask_source='pinnacle_direct'` via this path). `v_rpc_trust_health.pinnacle_ask_stale_hours` = **1.7h now (breach_at 3h)** → will breach ~04:24Z if it doesn't recover. `pinnacle_render_floor_stale_hours` 1.4h + `pinnacle_fmv_stale_hours` 8.9h still ok (separate daily writers).
- **Scope check:** NOT a broad dropout — the core */20 cron-job.org ingest chain (topshot-sales-indexer 03:03Z, wmc-fmv-populate 03:03Z, offers-sweep 03:02Z, snapshot-pack-asks 03:03Z) is all fresh within ~5 min. Isolated to the pinnacle-listings-reconcile entry (and its sibling */15 pinnacle-owner-discovery last ran 01:13Z — worth a glance too).
- **Risk read:** LOW-MED, self-healing on cron-job.org recovery, cursor-based ⇒ no data loss. Operator-actionable only (external cron-job.org console is off-limits/operator; Pinnacle pricing route+fn logic is off-limits to auto-ship per focus.md). **Operator:** check cron-job.org execution history from ~01:24Z and re-fire / re-enable the "Pinnacle Listings Reconcile" entry (job serves pinnacle_editions.ask). If it self-heals before then, close.

## Notes (all GREEN, no action)
- Security **0/0/0/0** (invariants/secdef-anon/rls-off/anon-write all []); `check_pgcron_recent_failures()` **[]**; trust **16/16 ok, breaches []**.
- Sentinel TS-UUID-48h **12** (< WARN 250); ts_uuid_dupes_24h 12 (ok). Editions TS **18,909** / AllDay 6,190 / Golazos 575 / UFC 518 — the +758 vs overnight 18,151 is the 07-07 daytime `::`-subedition cataloging wave (sentinel 12 confirms no hyphen-UUID leak, benign). DB **8,521 MB** (+~156, benign).
- FMV/HM: TS **5,032** (1307+3725, improving from 4,948) / AllDay 814 / UFC 15 / Golazos 4.
- `pipeline_fails_24h` top families (compute-topshot-pack-ev 11, fmv-recalc 7, topshot-buyer-backfill 5, check-alerts 3, wallet-username-resolver 3, golazos-listings-indexer 2, …) ALL have latest run ok=true — transient overnight/DBSAT-contention class, not new.
- `ufc_sales` resolving_editions = benign INFO (standing).
- Vercel prod **READY** on `3a684e07` (revive real TS badge art site-wide + trophy PDF). Trailing CANCELED deploys = superseded docs commits (docs-ignoreCommand); no ERROR-state deploy.
- Artifact estate intact (15 active, none RETIRED); rpc-live-health DB surfaces validated via `rpc_ops_snapshot()`; no view drops/renames in the 07-07 daytime wave (badge art, trophy-case PDF, OG hardening, subedition offer re-key) that would break an artifact payload. Not deep-run this late tick.
