# Daytime monitor inbox — 2026-07-18 ~06:19Z

Run: rpc-daytime-monitor, late-evening tick (~23:19 PDT Jul 17 / 06:19Z Jul 18) — NOT the ~08:00 first-tick, so Section 1a daily extras (trust-health row-by-row + cross-collection refresh verify) were skipped by design. No skew (shell 06:06Z ~= DB now() 06:07Z ~= newest TS sale 06:10Z ~= newest FMV 06:09Z). Lock RELEASED (13:48Z 07-17). Security 0/0/0/0. Health GREEN apart from the known self-healing impossible-parallel breach. Post-ship watch of the 07-17 CC/Cowork wave: clean. 1 real candidate + 1 watch note.

## Candidate 1 (LOW, ready DB-only fix) — rpc-backfill-pinnacle-mint-acquisitions statement-timeout
- Source: check_pgcron_recent_failures() — pg_cron job 85 `rpc-backfill-pinnacle-mint-acquisitions` (`19 * * * *`) failed 1/24 at 2026-07-18 05:19:00Z: "canceling statement due to statement timeout" in the `WITH candidates AS (SELECT m.nft_id, wmc.wallet_address AS wallet, m.minted_at ...)` body. (compute-topshot-pack-ev `targets` also timed out at the same 05:19Z tick — one shared brief contention spike, not a pinnacle-specific regression.)
- Risk read: LOW / self-recovering. 1 fail in 24 hourly runs (other 23 ok). Pure in-DB pg_cron SELECT (`SELECT public.backfill_pinnacle_mint_acquisitions(...)`) — no HTTP route / after()-lambda kill-trap; historical mint-attribution backfill (gap-only, never overrides marketplace legs, not user-facing). Same class as the offers-raise 600s + serial-fmv weekly fits: SECDEF fn on the 120s cluster default, crossed only under a contention window.
- Suggested action (night pass, DB-only, reversible): `ALTER FUNCTION public.backfill_pinnacle_mint_acquisitions(int) SET statement_timeout='600s';` (confirm the exact arg signature first). Correct because it is a pure pg_cron SELECT with no lambda kill-trap. Target metric: job 85 absent from check_pgcron_recent_failures(). Revert: `ALTER FUNCTION public.backfill_pinnacle_mint_acquisitions(int) RESET statement_timeout;`

## Candidate 2 (WATCH / likely-benign, confirm-only — NOT a ship) — TS FMV HIGH+MED 5237 -> 3939 (~-25%) vs the 07-17 baseline
- Source: rpc_ops_snapshot fmv_by_collection nba_top_shot HIGH 1073 + MEDIUM 2866 = 3939 (06:07Z 07-18) vs metrics-latest.json 07-17 13:44Z HIGH 1418 + MEDIUM 3819 = 5237.
- Risk read: LIKELY BENIGN. FMV freshness green (topshot_fmv_stale 0.2h), fmv_sanity 0, fmv-recalc healthy (100 ok/24h, latest 06:08Z; its 2 fails are stale from 07-17 10:28Z), sales flowing real-time (newest TS sale 06:10Z). No FMV-methodology change in recent deploys (IA reorg / Pinnacle market / launch-QA / Panini / ledger only). Reads as genuine confidence-tier redistribution (weekend + 07-17 launch/Candy-day activity aging out of the 7/30d sales windows), not a broken recompute.
- Suggested action: confirm-only against the night pass's fresh baseline — a single-point drop with green freshness needs no action; if H+M keeps falling multi-tick it graduates to a real investigation (sales-recency inputs / tier thresholds).

## Noted / NOT re-logged (known, resolved, or expected)
- topshot_impossible_parallel_serials 3/3 BREACH — known self-healing WNBA `::`-cataloging class; sanctioned interactive/Trevor circ floor-raise (NON-autonomous), already carried. Re-accumulated from 1 (07-17 CC wave-4 clear) as new `::` editions cataloged. Do not auto-ship.
- topshot-atlas-pool-ingest 177 ok=false — STALE pre-fix tail; all from Trevor's 13:58-14:18Z 07-17 harvest (err "unmapped_shape"), last atlas run ok=true 15:13Z; the 07-17 CC v5 `atlas_empty` reclassify + marker fix is holding. Ages out of the 24h window by ~14:18Z 07-18. Not a finding.
- lock-check-batch 4 + fmv-recalc 2 ok=false — all stale (07-17 08:38-10:38Z / 10:28Z), both green since (44/100 ok, latest 06:08Z); the 07-17 skip-locked write-back fix held.
- candy_mlb 125 editions + candy-editions-ingest activity — EXPECTED (Candy Drop 1 discovery landed 07-17); not an anomaly per CLAUDE.md.

## Artifact validation
rpc-live-health fully validated (24h pipeline health, FMV-by-collection, sales, all 12 /insights backing views return rows, freshness FMV 06:14Z / Pack EV 06:17Z / offers 06:07Z). rpc-panini-squeeze 3 backing views live (squeeze 501 / pack_ev 1 / special_serials 477). Remaining artifacts read subsets of the same now-validated core objects + Vercel (healthy). None flagged.
