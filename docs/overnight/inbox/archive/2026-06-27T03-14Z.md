# Monitor inbox — 2026-06-27T03:08Z (20:08 PDT, evening tick)

Platform **GREEN** (one confirmed-benign monitor false-positive, explained below — NOT an outage). Health sweep + core artifact data-layer validation clean. Post-ship watch on the evening CC wave that landed since the 00:19Z tick (`f7c3f63`→`e137570`) ALL PASS. **1 new LOW candidate.**

## Recent-ship watch (commits since the 00:19Z monitor tick)
- **`94863cc` pack-dist "value still sealed" strip** (Item 3) — page-layer, additive; deploy `dpl_Dx3L…` READY, smoke green (ledger). 0 pipeline/Sentry impact. PASS.
- **`fe8fd84` + `e137570` data-integrity route** (drop the 14.7s `health_check()` 504 call + repoint to current `health_check()` shape + re-enable the daily schedule) — GHA/cron-route change, NOT pipeline_runs-tracked, so GHA run status can't be verified from the monitor (same limitation the 00:19Z tick noted for the ops-monitor 504 fix). Indirect PASS: 0 new Sentry, 0 pipeline fails/6h, security 0/0/0/0.
- **`b004686` migration repo-sync** (AllDay heuristic-badge-label scrub) — record-only, no runtime effect.
- **`3f4c2c1` New Collectors** — `mv_insights_new_collectors_cohorts` = 48 rows, surface healthy. **The 00:19Z "adjunct" (add new-collectors to the live-health insights_counts panel) is now DONE** — rpc-live-health was reinstalled 03:00Z and its `insights_counts` already includes `new_collectors` (validated, returns 48).
- **`e56e4e3` allday-badge-ingest** — new residential-Atlas pipeline; too new / wrong class to watchlist (gate not met), as the 00:19Z tick noted.

## NEW candidate

### ALLDAY-FMV-POPULATE-NOOP-STALL — superseded no-op writer stopped firing; now a standing detect_stalled / alerts false-positive (LOW · night-pass DB-only + operator)
- **Source:** `detect_stalled_pipelines()` (allday-fmv-populate, 266 min silent vs 120m threshold) + `get_pipeline_alerts()` (cron_silent, medium).
- **Confirmed BENIGN — NOT an FMV outage.** `allday-fmv-populate` last fired 2026-06-26 22:42Z and has missed every 21-min tick since. But it is the **superseded no-op writer** the 06-26 CC finding already documented (CLAUDE.md / ledger, audit-followups Item 5): its last 8 runs all logged `ok=true / upserted=0` (writes zero rows every tick), and **AllDay FMV is fresh** — `fmv-recalc 1.7.0` owns the latest-per-edition (3,924 AllDay editions, latest computed ~2 min ago; AllDay H+M 903, flat). The `get_pipeline_alerts` detail text ("Primary AllDay FMV writer … fmv-recalc Step 5b is a partial backstop only") is **stale** — fmv-recalc 1.7.0 is now the full owner, not a partial backstop.
- **Why it matters (LOW but real):** a permanent benign entry in `detect_stalled_pipelines()` + `get_pipeline_alerts()` is monitor noise that degrades the silent-stall signal (a genuine future stall could hide behind this known one). The 06-26 CC entry already named the fix as "Optional operator: retire the no-op allday-fmv-populate cron."
- **Suggested action:** (a) night-pass DB-only, low-risk — remove the `allday-fmv-populate` row from `pipeline_cadence_watchlist` so `detect_stalled` stops flagging it (the writer is permanently superseded and never needs cadence monitoring again): `DELETE FROM public.pipeline_cadence_watchlist WHERE pipeline='allday-fmv-populate';` (revert: re-INSERT at 120m/medium). (b) operator — retire the cron-job.org "allday-fmv-populate" entry entirely. Verify after (a): `detect_stalled_pipelines()` no longer lists it AND AllDay FMV stays fresh via fmv-recalc.
- **Risk:** removing a watchlist row for a verified-superseded no-op writer is near-zero; AllDay FMV does not depend on it.

## Notes (owned/carried — NOT candidates, do NOT re-flag)
- **unmapped_resolution_backlog_max BREACH 441** (trust 8/9): owned/Declined. `topshot-flowty-unmapped-drain` is the fix; backlog 428→441 over ~7h (still ~plateaued, producer inflow ≈ drain rate). Do NOT re-flag/skip/retire/raise-threshold; cadence is the only lever. Night pass owns DRAIN-WATCHLIST.
- **NEW-COLLECTORS-INSIGHTS-QA** (logged 00:19Z, still open) — live-HTTP/visual close still pending for night-pass/interactive; not re-logged.
- **ufc_sales resolving_editions** INFO (13 unmapped/24h) — benign/carried.
- **allday-fmv-populate** alert mirrored in the rpc-live-health get_pipeline_alerts panel — same benign item; self-clears when the watchlist row is removed.
- AllDay `unmapped_open` (ALLDAY-V1-UNMAPPED-DRIFT budget-exhausted fossils), SERIAL-FMV-MULT-CRON (weekly by design), evm-transfers Base-429 — all carried/benign.

## Health line
security 0/0/0/0 · detect_stalled **1 (allday-fmv-populate — confirmed benign no-op; AllDay FMV fresh via fmv-recalc)** · pgcron [] · alerts 1 medium (the benign stall) + 1 INFO (ufc_sales) · pipeline fails 6h **0** · trust 8/9 (1 owned BREACH unmapped 441 draining) · FMV TS H+M **4577** / AllDay **903** (both flat, fresh @03:08Z) · editions FLAT 17471/6191/581/518 · ts-uuid-leak-48h **34** inert · writers fresh (FMV 03:08Z / PackEV 03:08Z / offers 03:07Z / cross-collection mat 06-26 04:10Z daily) · 12 insights boards all return rows (incl new_collectors 48) · Vercel prod `e137570` READY, 0 ERROR (CANCELED = superseded rapid pushes) · Sentry 0 unresolved/24h · 14 active artifacts — rpc-live-health payload + FMV + insights + cross-collection mat + offers freshness validated, none broken.
