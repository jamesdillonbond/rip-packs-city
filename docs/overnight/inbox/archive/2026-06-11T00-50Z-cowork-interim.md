# Cowork interim findings — 2026-06-11T00:50Z (for tonight's pass; supplements the 00:15Z monitor file)

## 1. DBSAT-RESIDUAL re-framed: the old drivers are DEAD; the residual is a marginal-query basket, not a storm
- Clean pg_stat_statements DELTA (~00:25Z -> 00:37Z, 12 min): ZERO new calls/time on every historical heavy-hitter — legacy wmc INSERT 37,615 calls static (fifth-site fix holding), both (p_collection...) RPC entries static, wmc SELECT moment_id static at 41,306. The pre-fix cumulative means are pollution; do NOT cite them as live evidence.
- pg_stat_activity sampled mid-window: effectively IDLE (1 active query at 70ms). The DB is NOT continuously saturated.
- Yet 00Z fails = 21/75 (28%). The failing set is the KNOWN chronically-marginal 8s-cap basket, same cast as every rush-window incident this week: pinnacle-nft-resolver load-batch-v2 (7), compute-topshot-pack-ev targets (6), topshot-moments-hydrator v_moments_needing_hydration candidate_read (5), topshot-buyer-backfill (4), wmc-fmv-populate schema-cache retry (4), refresh-error-triage / get_topshot_stub_targets / analytics_smoke_run / evm upsert (1-2 each). Interpretation: spiky coincidence — when several marginal reads land on the same tick the burstable Micro IO briefly queues and everything at 7-9s tips over. Likely aggravated today by wmc autovacuum debt from the day's churn + cold cache after the 2.7GB drops.
- STEER: don't chase a single culprit tonight. Options in rising order of invasiveness: (a) accept + re-baseline once autovacuum settles (check pg_stat_user_tables wmc last_autovacuum + dead tup trend overnight — if fails decay toward ~1%/hr by 06Z, this was vacuum debt, close it); (b) the proven fn-level statement_timeout bump (mirror reconcile-60s) on the 2-3 worst RECURRING fns IF they are SECDEF RPCs (verify which of load-batch-v2 / pack-ev targets / stub targets are fns vs direct view selects first — bumps don't help PostgREST view selects); (c) slot-stagger any of these whose crons collide on the same minute (check cron-schedule doc). Do NOT raise pipeline batch sizes (pack-EV lesson) and do NOT touch pricing-route logic.

## 2. LiveToken crosscheck COMPLETED from audit_lt_matches (989 matched rows, 23 wallets; table now RLS-hardened)
- Headline: post-fix-wave FMV accuracy is GOOD. HIGH: median rpc/lt 0.86, p10-p90 0.80-1.34, 1/85 severe outlier. MEDIUM: median 1.05 but a 7% severe-low tail. LOW: median 0.91 (4% combined tails). ASK_ONLY: median 1.11 with 6 severe-HIGH (the known ask-multiplier tail; memory says realization ~0.75 not 0.90 — a multiplier tighten is a PRICING change, review-gated, not night-shippable). STALE honest (worst spread, correctly labeled).
- The entire high-value severe-low class is ONE structural pattern: serial #1/#7/#10 of 2025-26 draft rookies (Knueppel 238:8008 lt $4,770 vs rpc $146; Flagg, Edgecombe, Harper similar) — LT prices the SERIAL, RPC prices the EDITION. This is the parked per-serial-premium gap, now quantified; it is NOT a recalc bug and the grail-spike guard is working as designed. One true overprice: Booker 8:110 ASK_ONLY $6,750 vs lt $1,460.
- Badges: of 487 LT-badged moments, RPC has tags for 421 (86.5%); the 66 gaps are the catalog-walk backlog (34% edition-coverage baseline, rising 4x daily). No action beyond the existing GHA.

## 3. Already shipped/verified this evening (do not re-do): pick_verification_target skip-locked migration (verify-challenge handoff has route residuals); fmv-recalc early-exit logging verified green by the 00:15Z monitor; audit_lt_matches RLS hardened; focus item 10 restored (condensed).
