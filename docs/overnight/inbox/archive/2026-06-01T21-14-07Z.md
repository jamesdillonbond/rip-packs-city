# RPC daytime monitor — inbox candidates

- **Run:** 2026-06-01T21:14Z · read-only daytime sweep
- **Platform state:** GREEN — security base-table checks 0/0 · `detect_stalled_pipelines()` `[]` · 20/20 deploys READY (0 ERROR) · FMV writers fresh (~10s) · Sentry no new/spiking · 11/11 artifacts validated functional (insights-health all 10 backing views non-empty).
- **Lock:** `docs/overnight/.lock` present but contents marked `RELEASED` (embedded start 13:54:59Z, ~7.3h ago) → treated STALE per its own >45-min rule; took over, inbox commit permitted.
- **Baseline:** `metrics-latest.json` @ 13:54Z (night pass, shipped nothing / NO-PUSH).

ONE new candidate this run (a security-posture finding surfaced while validating the `rpc-security-drift` artifact). Nothing in the ledger queue or Declined list re-raised; today's 18:24Z C1/C2 are NOT duplicated — status notes only at the bottom.

---

## S1 — [LOW–MED · security] `v_moments_needing_hydration` is an anon/authenticated-readable SECURITY DEFINER view → Supabase `security_definer_view` ERROR + RLS-bypass read surface

- **Source:** `rpc-security-drift` artifact check #4 (anon-readable SECDEF views), re-run live this sweep. The catalog query returned exactly one view: `v_moments_needing_hydration`.
- **Verified live:**
  - `pg_class`: owner `postgres`, `reloptions = (none)` → **no `security_invoker`**, so the view executes as its definer (postgres) and bypasses RLS on its base tables.
  - `role_table_grants`: **`anon` AND `authenticated` both hold SELECT** (plus the inert default INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER set — harmless on a non-updatable view; the SELECT is the live issue).
  - This is the same view `topshot-moments-hydrator` candidate-reads (cf. today's C2 timeout) — internal pipeline plumbing, NOT a by-design public `/insights` view, so anon has no legitimate reason to read it.
- **Why it matters:** an anon-readable definer view is exactly the Supabase `security_definer_view` ERROR class that ledger **Q1 drove to 0 on 2026-05-31**; posture has regressed to 1. Reads bypass RLS on the underlying tables (low-sensitivity here — moment-hydration candidate metadata, no user PII / no secrets — but it is a real advisor ERROR and an unintended anon surface). Whether newly introduced or long-standing-but-newly-surfaced, it is a live anon RLS-bypass read path today.
- **Risk:** low — read-only finding; the fix is additive/reversible and matches the Q1 precedent (not route logic, not destructive, not off-limits → SHIP-eligible on a true overnight run).
- **Suggested action (night-pass / operator):** confirm no anon/public HTTP route selects this view (it should not — the hydrator reads via service_role, which is unaffected by either fix below), then either:
  - **Preferred (internal view, no anon reader): REVOKE.** `REVOKE ALL ON public.v_moments_needing_hydration FROM anon, authenticated;` — revert: `GRANT SELECT ON public.v_moments_needing_hydration TO anon, authenticated;`
  - **Or (Q1 precedent, keeps grant but enforces caller RLS):** `ALTER VIEW public.v_moments_needing_hydration SET (security_invoker = on);` — revert: `... SET (security_invoker = off);`
  - Verify after: the `rpc-security-drift` "anon-readable SECDEF views" check returns 0; `topshot-moments-hydrator` still runs (service_role read path unaffected).

---

### C1 / C2 status (already in the 18:24Z inbox — NOT re-raised, notes only)

- **C1 (`topshot-listing-cache` ~6h daytime cadence):** it fired **19:03:39Z** — 5.98h after the 13:04Z run, landing right at Q10's 360m threshold, so `detect_stalled_pipelines()` correctly stayed `[]` (never tripped). No run since 19:03Z (~2.2h at this sweep, within threshold). The sustained ~6h cadence persists, so the threshold-vs-cadence tension C1 flagged is now confirmed live: a genuinely ~6h cadence will occasionally cross 360m and surface as a "stall." Still — confirm the cron cadence wasn't inadvertently slowed ~06-01 00:00Z; do NOT lower the 360m threshold (Q9 lesson).
- **C2 (`v_moments_needing_hydration` statement timeout):** recurred once more (18:12Z, in the failure list below) — same self-healing 1/tick transient. NOTE: S1 above is the SAME view (security angle, distinct from C2's perf angle). If the night-pass `CREATE OR REPLACE`s / optimizes this view for C2, it should also set `security_invoker = on` in the same change so S1 + C2 close together.

### Sweep evidence (night-pass baseline; not findings)

- `detect_stalled_pipelines()` = `[]`. `check_secdef_anon_execute_violations()` = `[]`.
- `ok=false`/6h — all single, transient, self-recovered at the 17:00–18:12Z cron rush (connection-pool / statement-timeout / Base-429): `check-alerts` 1, `evm-transfers-ingest` 1 (Q6), `pack-events-ingest` 1, `pinnacle-listings-reconcile` 1, `pinnacle-nft-resolver` 1, `topshot-moments-hydrator` 1 (C2). Lighter than the 18:24Z rush.
- FMV: TS HIGH+MED **881** (HIGH 233 / MED 648; NO_DATA 4,924 ↓ from 5,021 prior / 5,109 baseline) · AllDay HIGH+MED **262** (HIGH 50 / MED 212; STALE 461). Writers fresh 21:08Z. TS ASK_ONLY 683.
- Sentinel TS-UUID-48h **36** (baseline 40; <250 ok). Base-table security: 0 RLS-off base tables, 0 anon/authenticated write-grants on RLS-off base tables (the S1 SECDEF-view item is a separate, view-level surface).
- Drift: editions TS 16,310 / AllDay 6,191 (flat). `unmapped_sales` open 147 (flat vs baseline; total 1,191 — resolved rows pruned since the 2,786 baseline snapshot). DB 5,927 MB (+15 vs baseline).
- Vercel: 20/20 READY, 0 ERROR. Newest since 18:24Z = `bec07b3d` (docs: insights offer-spread/deals + ledger; monitor identity; ~19:53Z) — docs-only, no regression. Recent-ship watch (a79b778, 65421e26, 2f26044, e246f22, eda078c, 8605c43, 7c1b81b) — no attributable regression.
- Sentry: only `NEXTJS-15` (AllDay listings-indexer gated capture) fired in the last 6h — 3 events/21h, last seen ~16:00Z, not new, not spiking (known/watched).
- Artifacts (11/11 functional): `rpc-insights-health` all 10 backing views non-empty (squeeze 3,099 · set_squeeze 127 · pack_reality_top_ev 92 · rookies 61 · market 546 · first_mint 470 · cross_collection 1 · pinnacle_scarcity 103 · offer_spread 5,564 · deals 125) + freshness current (FMV/PackEV/offers all ~21:0xZ); `rpc-security-drift`, `rpc-pipeline-reliability`, `rpc-audit-followups` queries all schema-valid against the current DB.
