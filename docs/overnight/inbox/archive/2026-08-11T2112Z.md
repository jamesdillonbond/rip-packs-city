# Daytime monitor candidates — 2026-08-11T21:12Z

Written to mount, push unavailable (workspace shell down — `/sessions` no-space, 5th consecutive incident; no git clone possible this run). Night pass / operator: pick up locally.

Context: mid-day tick (~14:12 PDT). Security 4/4 clean. Vercel latest READY (708145e7). pg_cron scheduler healthy (jobs 55/56 fire every tick — documented no-op-walk false positive, confirmed 12/8 ticks/2h). 3 trust breaches all known-class (see bottom). Inbox was empty on entry.

---

## Candidate 1 (HIGH) — `allday-pack-opens-forward` edge fn silently stopped ingesting ~18h ago while pg_cron reports success

- **Source:** `rpc_ops_snapshot()` pipeline_alert `cursor_stalled` (severity high) + `pipeline_runs` (last row 2026-08-11 03:09:01Z) + `cron.job_run_details` (jobid 20).
- **What's happening:** pg_cron job 20 `rpc-allday-pack-opens-forward` (`9,39 * * * *`, `net.http_get` forward mode) is firing normally — 6 ticks in the last 3h, `last_status=succeeded`, `last_msg="1 row"` (that's just pg_net queuing the async HTTP request). BUT the downstream edge function has written **no `pipeline_runs` row since 03:09 UTC (~18h)**. Before 03:09 it logged a run every 30m regardless of `rows_found` (00:39/01:09/01:39 all logged `ok=true, rows_found=0`), so the absence of ANY run since 03:09 is the edge fn not completing, not "no on-chain data." Forward cursor frozen at block **160980787** since 03:09 while the chain advances (~2,100 blocks/tick), so AllDay pack-opens are being silently missed and the gap is growing.
- **Likely cause:** `net._http_response` at the `:09/:39` minutes over the last 6h shows a mix of **200s, 23× HTTP 403, and 2× 55s timeouts** — the 403/timeout pattern points at a downstream auth/WAF rejection or an unreachable Flow access node (consistent with the recent spork-floor / decommissioned-access-node work), OR the edge fn erroring before it logs. Could not cleanly attribute the 403s to job 20 alone from pg_net (the `:09/:39` bucket contains other jobs); the night pass has edge-fn logs + git to root-cause.
- **Risk read:** LOW-blast-radius data completeness (AllDay is secondary-market/historical now, pack opens are sparse — cursor moved only ~2k blocks/tick with rows_found 0–3), but it is a real, growing ingest gap that the standard `cron_silent`/`stalled_pipelines` checks mask because the pg_cron layer reports success. NOT a scheduler stop (job 20 active + firing).
- **Suggested action (night pass):** pull the `allday-pack-opens-forward` edge fn logs since ~03:00Z (`get_logs` / Supabase edge fn invocations) to see the actual failure; check the spork-proxy / Flow access-node reachability the 403s imply; if it's a transient upstream block, confirm it self-heals on the next reachable window, else the edge fn needs the same floor/reachability treatment as the 55/56 backfills. Do NOT bump the schedule.

---

## Known-class breaches (NOT candidates — logged so they're not re-raised)

- `panini_sale_price_capture_dry_days` = 14 (breach 3) — operator residential Panini-runner outage; upstream/operator-side.
- `public_board_slow_count` = 12 (breach 1) — disk-IO saturation collateral; `public_board_empty_count`=0 so every board still returns rows. The 08-11 Trevor/CC candy_pack_ev_model FMV-scope fix (861d5673) cleared candy_pack_market; residual 12 is the standing saturation class.
- `unmapped_resolution_backlog_max` = 215 (breach 100) — AllDay backfill net-draining inflow (outflow 700/24h vs inflow 12/24h; ~68.9d to clear the actionable pile). Documented, draining.
- pg_cron MV-refresh timeouts (`rpc-refresh-allday-pack-realized`, `rpc-refresh-misattrib-candidates`, `rpc-thin-sale-ask-disclosure-refresh`, `rpc-refresh-new-collectors`) — the documented disk-IO-saturation MV cluster; self-recovering, already queued historically.
- `sync-nba-projections` 100% fail (all_upstreams_failed) + `topshot-active-listings-ingest` egress_blocked — infra-gated (NBA offseason/Akamai; GHA runner IP block, atlas-proxy inert pending operator deploy).
