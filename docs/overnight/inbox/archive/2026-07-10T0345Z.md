# Daytime monitor candidates — 2026-07-10T03:45Z (late tick, ~20:45 PDT 07-09)

Health baseline GREEN: security 0/0/0/0, stalled_pipelines [], sentinel TS-UUID-48h 0, editions growth legit (`::` subedition cataloging, ts_sub 3,256), FMV TS H+M 5,173 (improving), AllDay H+M 809, DB 8,869 MB (+502/2.5d, studio+parallel backfill, benign), Vercel prod 187669ed READY, Sentry 0 unresolved/24h. Trust 15/16 ok.

## Candidate 1 — FMV-CLAMP-BACKSTOP-CRON-120s-TIMEOUT (LOW; night-pass / CC)
- Source: `check_pgcron_recent_failures()` → `rpc-fmv-clamp-disconnected-ask` failed its 2026-07-09 13:55Z daily tick, `canceling statement due to statement timeout` on the `WITH latest AS (SELECT DISTINCT ON (fs.edition_id) …)` scan over `fmv_snapshots`.
- Genuinely-recent failure (no same-day fix precedes it), so not a stale pre-fix run. fails_in_window 1/1 (daily cron; next tick 07-10 13:55Z).
- Blast radius LOW: this is the *backstop* clamp. Per CLAUDE.md 07-03, the disconnected-ASK clamp runs INLINE every `fmv-recalc` (Step-10, `clamp_rows` ok each run), so the daily backstop timing out does not leave FMV unclamped — the inline path is primary. Same growing-`fmv_snapshots`-contention family as the carried FMV-RECALC-EDITION-FETCH-TIMEOUT-CREEP.
- Suggested action (night-pass, if it fails again 07-10 13:55Z): pin the backstop fn `statement_timeout` higher (e.g. 300s) or constrain the `latest` CTE to a recent `computed_at` window; verify it clears on the next tick. Do NOT alarm on the single 07-09 miss alone.

## Candidate 2 — TOPSHOT-IMPOSSIBLE-PARALLEL-SERIALS at 4 (LOW/known-self-clearing; awareness only)
- Source: `v_rpc_trust_health` — `topshot_impossible_parallel_serials` = 4, breach_at 3 (the sole trust breach; +1 over threshold).
- KNOWN recurring class (CLAUDE.md 07-04 documented it self-resolving 3→1): old sales remapped onto freshly-cataloged `::` parallels whose floor-seed circulation briefly sits below the observed serial; the per-parallel circulation backfill raises the floor and it self-clears. Consistent with the active `::` subedition cataloging (ts_sub now 3,256).
- Suggested action: track only. Re-flag as escalating ONLY if it climbs materially or persists past the next circulation-backfill sweep. Do NOT queue a fix.

## Noted, not logged
- `topshot-moments-hydrator` 30 fails/24h = upstream `GetMintedMoment` GQL flakiness; 110 ok / 141 total (78%), cursor-based, not a stall — known intermittent class.
- Artifact deep-validation limited this tick: artifact HTML lives on OneDrive (outside the sandbox mount), so payload queries weren't re-run. All 14 artifacts enumerated via list_artifacts (estate intact). No schema DDL shipped recently → breakage risk low.
