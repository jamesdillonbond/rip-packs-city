# RPC daytime monitor — candidate harvest 2026-07-06T00:06Z (17:06 PDT Jul 5)

Evening tick (~3h cadence; prior tick 21:13Z). NOT first-of-day -> Section 1a (trust-health / cross-collection daily verifies) skipped. NO clock skew: DB now() 00:08Z ~= shell 00:06Z ~= newest sale 00:07Z ~= newest FMV 00:06Z. Mount `.lock` present but STALE (nightly-pass 08:03Z, ~16h old, unreleased) -> not a live concurrency guard.

CONCURRENCY NOTE: a sibling delayed monitor tick (the 03:06Z-stamped fire, suspended/resumed) executed at 00:11Z and committed `2026-07-06T0011Z.md` (3888e601) minutes into this run. Different filename -> zero merge contention (per design). That file covers ULTIMATE-FMV-RECALC-V1-CRON-NOT-FIRING + the BLOCKED-deploys reconciliation; this file adds the ONE candidate it did not catch (below). Not duplicated.

Health GREEN: security 0/0/0/0 - trust 16/16 ok (breaches []) - sentinel TS-UUID-48h 0 - leak_24h/48h 0 - Sentry 0 unresolved/24h - Vercel prod c09f9693 READY (no ERROR-state deploy; CANCELED entries = docs-only/rolling-release cancels) - no new runtime-error class - rpc-live-health deep-validated (all 12 insights surfaces resolve, FMV/PackEV/Offers fresh <15m, db_active 2). FMV TS H+M 4,913 (up from 4,785 Jul4) / AllDay 829 / UFC 15 / Golazos 4. Editions TS 18,144 (+654/2d = ALL `::` subedition-parallel cataloging, 0 canonical-base / 0 UUID / sentinel 0 -- by-design, not a leak). DB 8,159 MB (+342, studio+subedition backfill drain -- expected per focus STEER).

## NEW candidate (1)

### CROSS-SOURCE-DEDUP-STATEMENT-TIMEOUT -- LOW-MED -- DB-only fn (night-pass-actionable)
- Source: `check_pgcron_recent_failures()` -> `rpc-allday-cross-source-sales-dedup` (pg_cron jobid 32, sched `40 * * * *`). Latest run 2026-07-05 23:40Z **FAILED** at 120.0s ("canceling statement due to statement timeout"); 3/24 fails in window. (The sibling 00:11Z tick reported pgcron []; its ~21h-stale re-baseline missed this. Confirmed live against cron.job_run_details.)
- Pattern: FLAPPING not stalled -- 23:40 FAIL(120s) - 22:40 ok-but-slow(91.3s) - 21:40 FAIL(120s) - 20:40 ok(11.7s) - earlier ticks 9-30s. Failures cluster in the 21:00-00:00Z evening-contention window; per-run duration creeping as `sales` grows (DB 7,817->8,159 MB/2d via studio+subedition backfills).
- Root: `dedup_allday_cross_source_sales()` group-scans the FULL, growing `sales` table (`WITH g AS (SELECT nft_id, round(price_usd,2), date_trunc('day',sold_at) FROM sales WHERE ...)`) at the 120s cap; the all-time scan now tips over under evening I/O load.
- Blast radius LOW: dedup BACKSTOP only. The writer-side ingest trigger `trg_zzz_allday_cross_source_dedup` (shipped 07-02) prevents most cross-source AllDay dups at insert; a missed sweep tick just lets ~a handful of intra-batch-race dups linger ~1h until the next ok tick (20:40 + 22:40 both cleared). Not user-facing / not FMV / not deal-board.
- Suggested action (night pass, verifiable in-run): narrow the sweep's scan to a recent window (e.g. `WHERE sold_at > now() - interval '30 days'`) -- same-day cross-source dups are the target and historical rows are already settled, so it is result-identical for the rolling window while structurally capping scan cost; verify by force-running the fn (dur should drop to <30s) + confirming pgcron / `detect_stalled_pipelines()` clean. A bare `statement_timeout` raise is the weaker lever (delays the cliff, holds a connection longer under contention). Same DB-growth-contention family as carried OVERNIGHT-CONTENTION-CLUSTER / CLASSIFY-ACQ-ALLDAY-STATEMENT-TIMEOUT / FMV-RECALC-EDITION-FETCH-TIMEOUT-CREEP -- if the night pass reworks any of those, fold this in.

## Context (carried / no new log)
- Evening pack-detail page statement-timeouts (pack_lifecycle / pack_market / pack_realized_ev / allday_corrected_ev, last 23:34-23:46Z) = same evening-contention root as the dedup candidate; carried DBSAT pack/market/team class, no spike vs prior.
- unmapped_sales total open 3,774 (trust `unmapped_resolution_backlog_max` 30/100 ok) = carried ALLDAY-V1-UNMAPPED-DRIFT + UFC resolving-editions + TS-flowty-drain pool; drains continuously, not new.
- ultimate-fmv-recalc-v1 missed Jul-5 tick + BLOCKED-deploys reconciliation -> covered in 2026-07-06T0011Z.md; not re-logged here.
