# Daytime monitor — 2026-05-31T15:14Z

Run mode: read-only daytime sweep. The `docs/overnight/.lock` marker is stale/released (embedded start 08:02Z, released 08:12Z — >45min old, treated as STALE per its own note, so the inbox write proceeded). `.git/index.lock` + `.git/HEAD.lock` are still present (0-byte, 06:18–06:19Z, left by the overnight pass's failed pull — un-removable from this sandbox; Q7). This file is therefore written to disk only; the commit/push is expected to fail and the night pass picks it up locally from the persisted mount.

## Health: ✓ HEALTHY (no breakage)

- **Deploys:** current prod = `48f5a98` (Q4 pinnacle-listings fix) READY. Three deploys shipped since the 08:10Z baseline — `e93291e` (/share made public), `4a79770` (/insights live headline stats), `48f5a98` (Q4) — all READY, no regressions surfaced. The two ERROR deploys (`b20e483`/`26fa6be`) remain superseded/old.
- **Security:** 0 RLS-off public base tables; 0 anon/authenticated write grants on RLS-off base tables (relkind r,p). Clean.
- **FMV:** fresh <1m for both TS + AllDay (fmv-recalc 114 runs/24h, 0 fails). TS HIGH+MED 786 (↑ from 776), NO_DATA 5962 (↓ from 6055); AllDay HIGH+MED 248 (↑ from 243). Flat-to-improving.
- **Sentinel** TS-UUID-keyed-48h = 821 (↓ from 1099; WARN band but improving, trigger keeps rows inert — known).
- **unmapped_sales** 144 unresolved / 138 within-7d / 0 new-24h (flat vs baseline). DB 5853 MB. Editions flat (TS 16286 / AllDay 6191 / Golazos 581 / UFC 446).
- **Pipelines:** all */20 ingest chains within cadence; only low-single-digit transient fails (connection-pool / statement-timeout) with same-tick recovery — pack-events-ingest, compute-topshot-pack-ev, topshot-moments-hydrator, pinnacle-nft-resolver, evm-transfers-ingest (Q6 Base-429, 5/21). Nothing new or systemic.
- **/insights backing views** validated live: squeeze 2634, pack_reality stats/top_ev/dist = 1/98/6 — the 2026-05-31 security_invoker flip did not break them.
- **Artifacts:** 10 enumerated. Their HTML lives on the user's OneDrive (not mounted this run), so embedded queries could not be re-run directly — BUT every backing data path was independently validated via this sweep (pipeline_runs, fmv_snapshots, sales, editions, security catalog, Vercel deploys, /insights views), all fresh + sensible, and the only DDL shipped today was additive (`audit_20260531_get_wallet_collection_snapshot_rpc`), so no schema break could have silently broken a dashboard. No artifact flagged broken.

## Status flags for the night pass

- **Q3 (topshot-sales-indexer external-trigger stall) — RECOVERED, close it.** `topshot-sales-indexer` last ran 14:44Z (24m ago), 11 runs/24h, 0 fails; TS sales fresh (max ingest 14:44Z, 451 rows/6h); chained `topshot-listing-cache` + `-v2` both running (14:47Z). The 01:32Z→morning stall cleared (external cron re-firing). Sentry `NEXTJS-B` (sales-lag smoke) correspondingly quiet since 8h ago.
- **Q4 (NEXTJS-15 pinnacle listing warn) — fix confirmed working.** `NEXTJS-15` last fired ~14:14Z, BEFORE `48f5a98` went READY (~14:22Z); the post-deploy 14:40Z + 15:00Z pinnacle-listings-indexer/retry ticks did NOT re-fire it. On track for the "24h quiet → mark resolved" criterion in the ledger.

## New candidate

- **[LOW] DEP0169 (`url.parse()`) deprecation logged at `error` level on nearly every SSR render + `/api/wallet-search`.** Source: Vercel production runtime logs — 38 of 40 error-level lines in a 3h window were `(node:4) [DEP0169] Deprecat...`, all on HTTP 200 responses. Risk read: zero functional impact (cosmetic), but it floods the error-level log stream and makes genuine-error triage in Vercel logs hard (part of why Sentry is the authoritative error surface). Suggested action: grep `url.parse(` in SSR-common paths (proxy.ts / a shared server data-fetch helper) and migrate to the WHATWG `new URL()`; if the caller is a transitive dependency, prefer a scoped log filter over a blunt `NODE_OPTIONS=--no-deprecation`. Self-contained, low-risk, no behavior change.

## Minor watches (not candidates)

- `snapshot-institutional-wallets`: 1 run/24h (06:00Z) and that single run failed — consistent with the known 2026-05-29 "cron-reliability / needs operator re-enable" note rather than a new break. Verify the cron-job.org entry if institutional-wallet snapshots still matter.
- `compute-laliga-pack-ev` (Q2): last ran 05:30Z (~9.6h idle) — still the low/by-design Golazos cadence; remains a watch, not an alert.
