# Daytime monitor candidates — 2026-08-01 ~14:06 PDT (21:06Z, afternoon tick)

Health GREEN structurally: security 0/0/0/0 (RLS off = 0 tables), `detect_stalled_pipelines()` `[]`, trust-health 0 breaches, Vercel prod healthy (tip `cd153d2e` BUILDING = Trevor's "fix(qa): 8 rendered-DOM defects" QA commit — active build, not an error; the preceding ~18 deploys today are the CC DB-pin wave, all READY; 2 CANCELED = docs/superseded). editions 26,991 · TS FMV HIGH+MED-today 1,209 (lagging because `topshot-fmv-populate` is throttled by the contention below — the `fmv_current` latest-per-edition read is unaffected) · DB 11,688 MB. One continuity candidate below; artifact payload validation deliberately kept light this tick to avoid adding read load during the contention window (rpc-live-health payload last validated clean at the 18:06Z tick; the new MVs are additive to its inputs).

## SATURATION-WAVE-RECURRED + EDITION-MEDIAN-MV-JOINED (continuity of the 18:06Z misattrib item — low/medium, transient, IOPS-bound-instance class)
- **What's new vs the 18:06Z tick** (which logged the misattrib MV timeout and declined the first afternoon wave as "cleared by 18:13Z"): the statement-timeout wave did **not** stay cleared — a **second episode ran ~19:00–21:00Z** (hourly pipeline fail-rate 19:00Z = 2.9% / 10 fails, 20:00Z = 2.1% / 7, still firing at 20:38Z lock-check-batch, 20:34Z topshot-buyer-backfill lock timeout, 20:23Z allday-lock-refresh) vs a ~1% baseline. Peak is only **mildly** elevated (baseline band 0.3–2.9%; CLAUDE.md documents ~1–2.5% as normal), no stall, no data loss.
- **NEW second MV joined the timeout set:** `check_pgcron_recent_failures()` now shows **`rpc-refresh-topshot-edition-median`** failing (18:10Z, 1/4 in window — transient, 3 of 4 succeed) on `REFRESH MATERIALIZED VIEW CONCURRENTLY public.mv_topshot_edition_median_180d`, alongside the already-logged `rpc-refresh-misattrib-candidates` (15:35Z). Both MVs still hold data (edition-median 5,825 rows, misattrib 18,319 rows) — a stale-by-one-refresh internal QA/FMV-support input, no public surface.
- **Root-cause read (IOPS contention, not a defect):** `pg_stat_activity` at 21:06Z showed the long-runners all on `IO / DataFileRead` (disk-read waits, incl. `candy_special_serials_board` + several `get_*` PostgREST calls) — the IOPS-saturation signature. Drivers stack up today: (a) the tip deploy `cd153d2e` **materializes `topshot_market_index_daily` + backfills a covering INCLUDE index on `sales_2026`** = heavy IOPS DDL landing right now; (b) the all-day CC DB-pin migration wave; (c) new **Candy public read traffic** since the 07-31 go-live. All transient/recoverable — pipelines retry and mostly succeed; trust-health + sentinel classify these via `isSaturationError()` → warn, never false-green.
- **Blast radius:** none user-facing. FMV/insights/public surfaces serve from existing snapshots + `fmv_current`; the throttled `topshot-fmv-populate` catches up once contention eases.
- **Suggested action (night pass):** verify the next ticks self-clear — misattrib 08-02 15:35Z, edition-median on its cadence, and `topshot-fmv-populate` returns to ok. If EITHER MV refresh recurs across days (not just intra-day), the durable lever is the one the 18:06Z tick already scoped: a further off-peak reschedule and/or the **deferred off-peak `REINDEX CONCURRENTLY` of the fragmented `sales_YYYY` partitions** (61–71% leaf-frag) — the heavy full-sales-history seq-scans are the contention source. Do NOT auto-revert anything; do NOT alarm on the single intra-day occurrences.

## Already-known / NOT re-raised
- OPENS-HISTORY-BACKFILL-UPSTREAM-500 (`topshot-pack-opens-history-backfill`, upstream `events … status 500`, cursor stuck ~61.9M) — QUEUED by the 08-01 overnight pass + 18:06Z tick; still firing (2 fails/6h seen); unchanged. Not re-raised.
- The misattrib MV 15:35Z timeout itself — logged in the 18:06Z inbox file. This item only adds the recurrence + the edition-median MV newly joining.
- `candy-offers-indexer` partial-sweep "deactivation suppressed" — BY DESIGN. Not a finding.
