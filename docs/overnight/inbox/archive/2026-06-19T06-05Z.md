# Daytime monitor — 2026-06-19T06:05Z (23:05 PDT, last evening tick)

Run: rpc-daytime-monitor (READ-ONLY). Platform GREEN across the full sweep. **1 status-record finding:** the queued BUYERBF-150 lever was SHIPPED by Trevor ~04:40Z (`7a70a31`) with a counterintuitive post-deploy outcome. This ship landed AFTER the 03:05Z monitor, so this is its first observation — pre-digested here for the night pass's post-ship watch. Night pass still has not run since 06-17; inbox now ~12 files to drain.

## Post-ship watch — BUYERBF-150 lever SHIPPED, partial outcome [MED · CC route-code · SUPERSEDES the queued BUYERBF-150]
- **Ship:** Trevor `7a70a31` ("buyer-backfill BATCH 150->100 + maxDuration 600->800"), deploy `dpl_1R1iT6wq…` READY ~04:40Z (carried in current prod `b86caaf`). This IS the BUYERBF-150 candidate's recommended lever (maxDuration 600->800) plus a BATCH cut. **Stop re-recommending maxDuration->800 — it is live.**
- **Post-deploy reality (NOT what the commit predicted):** the BATCH 150->100 cut was meant to bound runtime to ~390s. It did the opposite — post-deploy `topshot-buyer-backfill` runs are HIGHER than pre-deploy: 04:54Z 531.8s / 05:14Z **670.5s** / 05:24Z **605.3s** / 05:44Z **710.5s** / 05:54Z 581.5s. Three consecutive runs (670/605/710s) exceeded the OLD 600s ceiling; max 710.5s now sits ~90s under the NEW 800s cap. ALL ok=true, all logging — no invisible death.
- **Read:** consistent with the candidate's own thesis (per-row on-chain decode latency, not batch size, governs) PLUS a time-budget drain that simply fills the larger 800s budget — i.e. raising maxDuration let each run do MORE work and re-approach the (now final) ceiling rather than adding true headroom. Pre-deploy ~590s under a 600s cap (~10s margin); post-deploy ~710s under an 800s cap (~90s margin) — but if the drain fills the budget, that margin is illusory and the near-ceiling pattern reasserts at 800.
- **Reframed residual (CC, NOT night-pass-shippable — CC-owned route + 800 is the Pro HARD cap):** maxDuration cannot go past 800 (CLAUDE.md: >800 silently ERRORs the deploy). So the next lever must REDUCE per-invocation work, not extend time — verify BATCH=100 actually took effect (runs ran LONGER, which is suspicious), and/or chunk/cap rows-per-run so one invocation can't fill an 800s budget. No action tonight (nothing broken, all ok, ~90s headroom). Watch: any run >770s, any run that stops logging, or a `detect_stalled` flag on topshot-buyer-backfill.

## Closeable / reconciled — for the night pass, no new work
- **FMV-HIGHMED-DIP-WATCH — closeable.** TS HIGH+MED 3144 (HIGH 874 + MED 2270), normal daytime band; ASK_ONLY 2624 stable (not over-claiming). FMV reconciles EXACTLY to editions on both collections (TS 15,543 = 15,543; AllDay 6,191 = 6,191) — 0 orphan/double-count. fmv-recalc fresh (last ok 06:08Z, 6h 20/0); TS snapshot 06:08Z. Benign daily cycle confirmed.
- **Sentry JAVASCRIPT-NEXTJS-A "fmv pipeline healthy" (smoke) — transient, leave unresolved.** 1 event ~05:00Z, super_low, culprit /api/smoke-test. FMV independently verified healthy (writers fresh 06:08Z, sanity_flags 0, reconciles exactly) -> smoke false alarm. Resolve after 24h quiet.
- **Alerts (live since 06-18, 1 active sub) — healthy, do NOT flag as anomaly.** alerts-dispatch 48 runs/0 fails/avg 8s/max 20s (well under the new 90s statement_timeout); alerts-send 72/0. The §A 45->90s scale path holds with a real subscription materializing the deal boards each tick.
- **Pinnacle mojibake — 0 board-wide** (the 06-18 normalize_pinnacle_edition de-double-encode hardening holding).

## Noted, not findings
- **allday-listing-serial-backfill (`b86caaf`, ~05:15Z) — 0 runs yet, cron not wired.** Fresh ship (Item 2 AllDay floor-serial recovery); edge fn deployed v1, `allday_moment_serials` has 2 seed rows; deal-board AllDay leg correctly shows `low_ask_serial` NULL (1/163) = the documented Item-2-pending state. Board unaffected (builds all 3 legs: TS 610 / AllDay 163 / Pinnacle 23). NOT a finding at <1h post-ship; if still 0 runs by the ~8am tick it joins the cron-not-wired class (SERIAL-FMV-MULT-CRON / ALLDAY-V1 family — operator wiring).
- **Night pass not run since 06-17** (NIGHTPASS-MISS, carried) — inbox ~12 files; focus.md (night of 06-18->06-19) expects tonight's ~01:00 PDT run to drain.
- SERIAL-FMV-MULT-CRON BY DESIGN (pg_cron weekly Sun 11:00); evm-transfers-ingest Base-429 known transient — do not flag (focus.md).

## Health summary
GREEN · security 0/0/0/0 (detect_stalled [], rls-off [], check_public_security_invariants [], check_secdef_anon_execute_violations []) · trust 9/9 ok (pinnacle_fmv 20.0h/30, pack_ev 1.37d/2, ts_uuid_dupes 0/200, unmapped 9/100, offer_edition_gap 0/50) · sentinel TS-UUID-48h 0 · pipeline 24h 9028/11 = 0.12% / 6h 1922/**0** · editions flat (TS 15543 / AllDay 6191 / Golazos 581 / UFC 446) · FMV reconciles exactly (TS HIGH+MED 3144 / AllDay 842; fmv-recalc 06:08Z, 6h 20/0) · DB 4840 MB (+3 benign) · cross-collection cohort_mat fresh 04:10Z (158) · Vercel prod `b86caaf` READY, 0 ERROR/20 (CANCELED = superseded rapid-push/monitor commits) · Sentry 1 unresolved (single-event smoke transient, FMV verified healthy) · 14 active artifacts validated — backing objects all return rows (deals_board 796 / serial_premiums 276 / perfect_mint 9 / top_sales 582 / underpriced 15 / offer_sanity 225 / trophies 683 / cohort 158 / squeeze 500+ / tracked_fmv 21); none broken/repaired.
Daily extras (1a trust-health + cross-collection verify) NOT separately run — evening tick, not the ~8am first tick; trust-health was read this sweep anyway (9/9) and cohort_mat confirmed fresh (04:10Z).
