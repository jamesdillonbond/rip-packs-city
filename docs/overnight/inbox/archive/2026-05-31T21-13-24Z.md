# Daytime monitor — 2026-05-31T21:13Z

Run mode: read-only daytime sweep (~3h after the 18:06Z run). `docs/overnight/.lock` is present but STALE/RELEASED (mtime 08:18Z, ~13h old ≫ 45min; its own note says treat as STALE and take over), so the inbox write proceeds. **`.git/index.lock` + `.git/HEAD.lock` are GONE this run** (cleared on Windows since 18:06Z) and HEAD is `main`, 0 ahead of origin — so unlike the last several runs the commit+push may actually land.

## Health: ✓ HEALTHY (no breakage)

- **Security:** 0 RLS-off public base tables; 0 anon/authenticated write grants on RLS-off base tables (relkind r,p). Clean. (The morning `NEXTJS-1C` security smoke false-fire is verified clean live.)
- **Pipeline silence:** `detect_stalled_pipelines()` → `[]` (nothing past its per-pipeline cadence threshold). The authoritative absence-of-runs check is clean — no silent stall like the 2026-05-31 TS-sales one.
- **Deploys:** current prod = `3838cdf` (collection-grid STALE/NO_DATA labels) READY. **5 new READY deploys since 18:06Z, all healthy, no regressions:** `59c860b` (TS sales-indexer fire-and-forget perf), `0f65db8` (team-hub Phase 1 + 2 live same-signature RPC migrations D1/D2), `221ab64` (C1 AllDay Sentry gate — see flags), `301a1ee` (docs/ledger), `3838cdf` (grid labels). The only ERROR deploys remain the long-superseded morning `b20e483`/`26fa6be` (06:08Z).
- **Pipelines:** all */20 ingest chains within cadence. `topshot-sales-indexer` last 21:00Z (16 runs/6h, 0 fails — Q3 stays resolved); `fmv-recalc` 21:08Z (25/6h, primary FMV writer healthy). Only single-run transient fails/6h — `compute-topshot-pack-ev` targets-timeout (1, known), `sync-nba-projections` games_upsert pool-timeout (1; hourly window just opened), `pinnacle-nft-resolver` / `pack-events-ingest-backfill` / `wmc-fmv-populate` / `pinnacle-listings-retry` / `ufc-stub-thumbnail-resolver` (1 each) — all connection-pool/statement/lock timeout with same-tick recovery. Nothing systemic or new. `evm-transfers-ingest` last 21:00Z, not in the stall list this run (Q6 Base-429 quiet this window).
- **FMV:** fresh <1m via fmv-recalc. TS HIGH+MED **813** (↑ 796 @18:06 / 776 baseline), NO_DATA **5800** (↓ 5898 / 6055). AllDay HIGH+MED **251** (↑ 249 / 243). Flat-to-improving all day.
- **Sentinel** TS-UUID-keyed-48h = **534** (↓ 633 @18:06 / 821 @15:14 / 1099 baseline; WARN band but steadily declining; rows inert by trigger — known).
- **unmapped_sales** 144 unresolved / 138 within-7d / 1 new-24h (flat vs every prior run). DB 5872 MB. Editions flat (TS 16289 / AllDay 6191 / Golazos 581 / UFC 446).
- **Sentry:** **0 new issues in 12h, no spikes.** 8 unresolved total = `NEXTJS-15` (see flags, quiet 5h) + the 6-issue morning smoke cluster (firstSeen/lastSeen ~06:06Z, quiet 15h — the `b20e483`/`26fa6be` ERROR-window transients already in baseline; the security + FMV + sales ones independently re-verified clean this sweep).
- **Artifacts:** 10 enumerated; OneDrive readable. Every backing data path validated healthy this sweep — squeeze 2628 / pack_reality 1·97·6 (insights-health), `get_insights_hub_stats()` + `get_rpc_traction_snapshot()` both return (rpc-traction/hub), pipeline_runs (live-health/pipeline-reliability), fmv_snapshots (fmv-watch), security catalog 0/0 (security-drift), editions + db-size (cross-collection/trophy/my-wallet/deploys-and-cost). Today's DDL was additive/same-signature only (team RPCs) — no column/view drop, so nothing could silently break a dashboard. None flagged broken.

## Status flags for the night pass (two prior candidates now close)

- **C1 (inbox 18:06 · `NEXTJS-15` AllDay ungated capture) — SHIPPED, closing.** `221ab64` ("fix(allday-listings): rate/reason-gate Sentry capture (C1, mirrors 48f5a98)") deployed READY (~20:40Z). `NEXTJS-15` lastSeen **5h ago (~16:06Z)** — no events since, and the AllDay feeder is now gated. This unblocks the ledger's "24h quiet → mark `NEXTJS-15` resolved" criterion that the 18:06 run flagged as unreachable while AllDay fed the shared fingerprint. No night-pass action needed.
- **C2 (inbox 18:06 · `funnel_events` 0 rows / await-fix unverified) — RESOLVED, closing.** `get_rpc_traction_snapshot()` now reports `funnel_last_event_at = 2026-05-31T19:50:25Z` with `funnel_7d`/`funnel_30d = {share_view:1, insights_view:1}` — rows are persisting, so the await-fix (`2339f30`) took. C2's "fire one beacon and confirm a row lands" is satisfied by live traffic. Sibling note: the same await-fix also covers `/api/track-click`; `outbound_clicks` is still 0 (last 2026-04-25), but since the identical await pattern is now proven to persist for `funnel_events`, that 0 is almost certainly honest no-traffic (~13 users, pre-launch), not a broken insert — not raising it as a candidate.
- **Q3 (topshot-sales-indexer stall) — stays RESOLVED.** 16 runs/6h, last 21:00Z, TS sales fresh; not in `detect_stalled_pipelines()`.

## New candidates

None this run. Every signal is healthy, already-logged (DEP0169 @15:14; Q1–Q7 in the ledger), now-resolved (C1, C2 above), or a known minor watch (below).

## Minor watches (not candidates)

- `snapshot-institutional-wallets`: still 1 run/24h (06:00Z) — the known cron-reliability watch (flagged 15:14, 18:06, 2026-05-29). It did run today; it's not in the cadence watchlist so `detect_stalled_pipelines()` doesn't cover it. Operator: verify its cron-job.org entry if institutional snapshots still matter.
- `compute-laliga-pack-ev` (Q2): last 05:30Z — low / by-design Golazos cadence.
- `topshot-fmv-populate`: last 19:45Z — secondary FMV writer, not in the cadence watchlist; fmv-recalc covers freshness. Watch only.
- DEP0169 `url.parse()` deprecation (logged 15:14Z) — still open upstream; not re-logged.
