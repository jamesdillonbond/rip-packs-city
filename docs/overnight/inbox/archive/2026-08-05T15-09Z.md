# Daytime monitor candidates — 2026-08-05T15:09Z (~08:09 PDT, first tick)

Health is green-with-known-noise. Security fully clean (all 4 invariant arrays `[]`), cross-collection refresh fresh (04:10/04:25Z both `succeeded`, cohort 174), flagship dashboard + all 12 insights boards + freshness sources all OK (FMV write 15:08Z, Pack EV 15:08Z, offers 15:02Z). Today's documented FMV-sweep-wedge / DB I/O saturation incident (~04:00–13:00Z, already in the ledger) recovered ~13:00Z — `fmv-recalc` last OK 14:55Z, writing live. The 12:xx pg_cron MV-refresh failures + the high-sev `snapshot-institutional-wallets` alert are the tail of that window and will retest on their next ticks.

Only ONE new, non-duplicative candidate this run (wallet-username-resolver, topshot-flowty-backfill cursor false-positive, and fmv-recalc-maxDuration are already queued in the ledger).

---

## LOW — `ufc_fmv_stale_hours` trust arm flaps on the closed UFC market during any sweep slowdown

- **Source:** `rpc_ops_snapshot()` trust_health — `ufc_fmv_stale_hours` = 32.6 vs `breach_at` 30 (new BREACH this tick; was not among the 3 breaches the 08-05 overnight pass recorded).
- **What happened:** UFC crossed the 30h staleness line because today's fmv-sweep wedge stalled the recalc cursor for ~9h. The arm measures *recompute* recency, not sale recency, so it should re-freshen and clear now that the sweep has recovered (verify next tick).
- **Structural read:** the UFC market has been CLOSED since 2026-05-13 (`collections.market_closed_at` set; `ufc_fmv_pct_stale_30d` = 96.1, near-total by design). Closed-market editions carry no new sales, so UFC FMV freshness depends entirely on the recalc sweep reaching them — which makes this arm prone to flapping any time the sweep slows, even though nothing is actually wrong with a closed market's pricing.
- **Risk:** none to act on now (self-clearing). The candidate is a small durable question, not an incident.
- **Suggested action (night pass / Trevor):** confirm it clears post-recovery; if `ufc_fmv_stale_hours` recurs absent a wedge, consider a relaxed threshold for closed-market collections or excluding `market_closed_at IS NOT NULL` collections from this arm — mirroring how the pricing layer already special-cases closed markets via `market_closed_at`. Additive trust-view change only; do NOT touch pricing math.

---

## Watch-only (no action; noted for continuity, all explained by the 04:00–13:00Z saturation window)

- 6 pg_cron jobs failed in the 12:xx window (`rpc-refresh-topshot-edition-median`, `rpc-refresh-allday-pack-realized`, `rpc-refresh-allday-pack-sales-agg`, `rpc-refresh-mv-ts-set-play-catalog`, `rpc-backfill-pinnacle-acquisitions`, `rpc-trust-health-precompute-refresh`) — statement/job-startup timeouts, all last_run BEFORE the ~13:00Z recovery; next 6h ticks (~18:xx) will confirm clearance. Stale pre-recovery runs, not a new finding.
- `snapshot-institutional-wallets` (high sev, 50% fail, statement timeout on the NBATopShotCommunity diff) — low-cadence, last activity 10:07Z, saturation collateral; not new inflow, not user-facing.
- `sync-nba-projections` (all_upstreams_failed) + `topshot-active-listings-ingest` (egress_blocked) — recurring external/upstream classes (NBA off-season; Cloudflare egress).
- Known do-not-re-flag breaches holding: `panini_sale_price_capture_dry_days`=8 (upstream outage), `public_board_slow_count`=7 (contention/do-not-re-budget), `unmapped_resolution_backlog_max`=105 (draining by design).
