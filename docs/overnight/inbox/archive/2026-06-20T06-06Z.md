# Daytime monitor inbox — 2026-06-19 ~23:06 PDT tick (late / last tick of day)

Platform GREEN. **No net-new candidate.** One decision-relevant RESOLUTION + the offer-fill post-ship watch the 8pm tick deferred is now done (PASS). The other two open items (REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT, BUYERBF-PERINVOCATION-WORK) are UNCHANGED — do NOT re-log.

## Resolution to action (night pass)

### ALLDAY-SERIAL-BACKFILL-CRON — RESOLVED by `18897fd7` (on-chain borrow); confirm next tick, then close + watchlist
- **Source:** `pipeline_runs` `allday-listing-serial-backfill` 03:34Z run **ok=true / serials_resolved=37 / 0 errors**; `allday_moment_serials` 2 -> **39**; deal-board `low_ask_serial` now populated. Vercel `18897fd7` (dpl 4mxJ.../6Wvs...) READY.
- **Root cause finally pinned:** the UA-fix hypothesis (`f53a9211`) was **wrong** — nflallday's Cloudflare bans the topshot-proxy worker egress on `/allday-consumer` outright (the sibling `allday-unmapped-resolver` only looked healthy because its backlog had drained to 0 targets, so it never actually calls the endpoint). Fix moved the serial source **off nflallday entirely** to on-chain `AllDay.borrowMomentNFT` against rest-mainnet (the script the healthy sales-indexer already uses) + extended `get_allday_listing_serial_targets` to return `seller_address`. Security invariants clean (0 / []).
- **Action (night pass):** per L1, do NOT close on a single run — confirm the **~06:34Z next tick** also logs ok=true / serials_resolved>0 (cadence is 3-hourly at :34). After >=2 consecutive ok ticks, CLOSE ALLDAY-SERIAL-BACKFILL-CRON and add a generous `pipeline_cadence_watchlist` row — size the threshold off the observed on-chain run cadence (borrow-per-target is slower/budget-bounded), not the old endpoint's.

## Post-ship watch done this tick (PASS — for the record, not candidates)
- **offer-fill -> sales wave** (`8ffb2913` / `06ad6e82` / `78ca042` / `c3a13a1`) — the watch the 8pm tick deferred as <1h fresh. **PASS:** `source=offer_fill` sales **4,234 rows / 4,234 distinct tx** (zero internal dup), FMV reconciles **EXACTLY** (TS 15,543 = 15,543 / AllDay 6,191 = 6,191), `fmv_sanity_flags` 0, `ts_uuid_dupes_24h` 0. GHA `?sync=1` backfill draining cleanly (`backfill-offer-fill-sales` 19/0; per-run sales_written + idempotent sales_duped skips + fill_tx_hash stamping; still `done:false` = catching up through history, expected). The 504 fix (`c3a13a1`: DEFAULT_RANGE 80k->20k + bounded GHA loop) holds — 0 ERROR deploys. New `SALES_ONLY` TS bucket (20) is the healthy footprint of sale-less editions getting a first data point. **Operator hygiene (from `78ca042`):** disable the old cron-job.org "RPC Backfill Offer-Fill Sales" entry now that GHA owns the drain — harmless 202+dropped-after() burn until then.
- **BUYERBF-PERINVOCATION-WORK** — holding, NOT worsening. `topshot-buyer-backfill` 68/0/24h, max **740.2s** (= 8pm peak, no further creep), avg 629.5s, < 770s watch line < 800s cap, detect_stalled []. Holds as queued.
- **REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT** — UNCHANGED (queued at the 8pm tick with the full fix recipe; do NOT re-log). `refresh-special-serial-owners-mv` still 0 ok / 2 fails (last 02:16Z; 30s service_role statement_timeout vs ~70s CONCURRENTLY refresh). MV serves its 5,929-row snapshot; surface is auth-gated/noindex. Fix = pin the SECDEF fn's `statement_timeout` (CC migration).

## Health (one line)
security 0/0/0/0 · detect_stalled [] / alerts [] · trust 9/9 ok · sentinel TS-UUID-48h 0 · FMV reconciles exactly (TS HIGH+MED 3,328 up / AllDay 853) · editions flat (TS 15543 / AllDay 6191 / Golazos 581 / UFC 446) · pipeline 24h 9,177/16 (0.17%, all transient/known) · DB 4,897 MB · Vercel 0 ERROR prod `c3a13a1` READY · Sentry 0 unresolved · 14 artifacts GREEN · cross-collection MVs fresh (04:10Z / 04:25Z).
