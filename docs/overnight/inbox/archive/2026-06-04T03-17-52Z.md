# Daytime monitor — 2026-06-04T03:17Z

Run: `rpc-daytime-monitor` (daytime, ~20:17 PDT June 3). READ-ONLY sweep. Repo: shared `rip-packs-city` (bot clone not mounted). Lock: 19h-old RELEASED marker (overnight pass finished 08:23Z) — not fresh, safe to write.

## Health: ✓ GREEN
- `detect_stalled_pipelines()` = `[]`. All 40 active watchlist pipelines verified under threshold (incl. the overnight C-PAYER `is_active=false` + C-PIN `pinnacle-metadata-backfill` 200m, both holding; evm-transfers-ingest at 150m per P1; topshot-listing-cache 306/360m — closest to threshold, still healthy).
- Security 0/0/0: 0 RLS-off public base tables; 0 anon/authenticated write grants on RLS-off base tables; S1 anon-revoke holds (`v_moments_needing_hydration` anon+authenticated SELECT both `false`).
- Pipelines (6h): only the known transient cron-rush class at the 00:00Z rush — `allday-unmapped-resolver` (00:01Z), `pinnacle-nft-resolver` (00:05Z), `allday-listings-retry` (00:08Z), `pack-pull-source-rip-id-backfill` (00:11Z), `check-alerts` (00:20Z), all pool/statement timeouts, self-recovered. Plus N2 (`topshot-moments-hydrator` candidate-read stmt timeout, 00:22Z) and PEV1 (`compute-topshot-pack-ev` ×3: 23:38/01:08/02:08Z, `time_budget_exceeded_after_fetch`). None logic-faults; none deploy-attributable.
- FMV: `fmv-recalc` fresh (~3m; 4,522 TS + 2,122 AllDay snapshot rows trailing 2h; primary writer healthy, 158 runs/48h). TS HIGH+MED 932→971→1007→**1012**, NO_DATA 4424→**4155** (F-series cleanup still compounding). AllDay flat (HIGH+MED 269, NO_DATA 522).
- Sentinel TS-UUID-48h **22** (baseline 43, 00:24Z 34 — still falling; <250 ok). unmapped_sales **148** open (nflallday 147 / golazos 1) — down hard from 292 @00:24Z / 278 baseline (flowty-archival 49 cleared, AllDay resolver draining). DB 6029 MB (baseline 5999). editions TS 16,352 (+8), AllDay/Golazos/UFC flat.
- Vercel: 20/20 recent prod deploys **0 ERROR**; newest `ce36102f` (brand-token style, item 7) **BUILDING** at sweep time, prior READY tip `f3011d9` (FMV mis-key sweep). Local repo in sync with origin/main at `ce36102` (0/0). Post-ship watch GREEN.
- Sentry: **0 new** issues in 24h; 1 unresolved = `NEXTJS-15` (PIN1, pinnacle-listings-indexer, 2 events / 2 users, last seen ~22h ago, NOT spiking).
- Artifacts: **12/12** data layers validated healthy. 5 insights views (squeeze/deals/offer-spread/rookie-index/pack-reality-stats) + `topshot_market_index_daily` execute non-empty; `get_rpc_traction_snapshot()` non-null; `get_wallet_collection_stats('0xbd94…')` returns the F4 STALE-split (TS 14,274 moments, $66,517.66 excl-STALE / $4,421.07 STALE×177); the new `rtr-pack-finder` backing `pack_table_rows` executes; `pack_ev_stale_gt7d` = 0 (PEV1 has no product impact). None broken; none repaired.

## New candidate (1)

### MON1 · LOW · operator/CC (night-pass SHIP-eligible) — Add `fmv-recalc` to `pipeline_cadence_watchlist` (stall-coverage gap)
- **Source:** `pipeline_cadence_watchlist` (exact-name count for `fmv-recalc` = 0; the `%fmv-recalc%` match is the unrelated daily `ultimate-fmv-recalc-v1`). The platform's **primary sales-path FMV writer** is not watchlisted, so `detect_stalled_pipelines()` is structurally blind to an fmv-recalc stall — precisely the **2026-05-25 silent-stall** failure mode (`fmv-recalc` stalled 22:03→14:53, ~17h, caught by smoke/manual, not by absence-of-runs; resolved `dd84526`). Same coverage-gap class as Q10 (`topshot-listing-cache`) and P1 (`evm-transfers-ingest`).
- **Risk read:** very low. Additive, reversible monitoring-config INSERT — not route logic, not destructive, not off-limits. Cadence is regular: 158 runs/48h, avg gap ~18m, max gap 40m/12h, 0 fails/12h, currently silent 9m. A 120m threshold tolerates cron-rush clustering / a few skipped ~18m ticks without false-positive, while catching a genuine multi-hour stall.
- **Action (night-pass auto-shippable next in-window run; or operator/CC):**
  - Ready migration: `INSERT INTO pipeline_cadence_watchlist (pipeline, max_silent_minutes, severity, notes, is_active) VALUES ('fmv-recalc',120,'high','Primary sales-path FMV writer (lib/fmv-confidence 1.7.0); ~18min cadence (158 runs/48h). detect_stalled coverage for the 2026-05-25 silent-stall class (~17h undetected by absence-of-runs). 120m tolerates cron-rush gaps, catches a multi-hour stall.',true) ON CONFLICT (pipeline) DO NOTHING;`
  - Revert: `DELETE FROM pipeline_cadence_watchlist WHERE pipeline='fmv-recalc';`
  - Target / verify post-apply: `detect_stalled_pipelines()` stays `[]` (fmv-recalc last ran ~9m < 120m); a future multi-hour fmv-recalc stall surfaces in `detect_stalled_pipelines()` instead of being invisible to it.
  - Note: leave the supplementary GQL-marketplace writers (`topshot-fmv-populate` = M1, `allday-fmv-populate`, `wmc-fmv-populate`) OFF the watchlist — their sparse/irregular cadence would false-positive (the M1 / Q9 lesson). This candidate is scoped to the regular-cadence primary writer only.

## No action / already tracked (not re-raised)
- **Recent-ship watch GREEN — nothing to revert.** `f3011d9` (FMV mis-key sweep F2/F3/F4/F5): FMV HIGH+MED climbing (1012), NO_DATA falling (4155), `get_wallet_collection_stats` F4 split correct, no Sentry correlation. `d8cc6c2` (storefront-cleanup removal) + the newer UI commits (`da11697` rtr overflow, `5081589` moment-modal, `396cef9` concierge, `9ace049` proxy) all folded into `ce36102f` (in sync, building) — no new errors.
- **M1** (reschedule `topshot-fmv-populate` off the :00 cron peaks) — logged 18:18Z, still open, not re-raised.
- **PEV1** (`compute-topshot-pack-ev` `time_budget_exceeded_after_fetch`) — logged 00:24Z. Recurred 3× in this 6h window (~25% of its 30-min runs), same upstream-TopShot-GQL-latency root cause; `topshot_pack_ev_targets` 0 packs >7d stale → still no product impact. Not re-raised.
- **N2** (hydrator candidate-read stmt timeout, recurred 00:22Z — do NOT revert the materialized-CTE fix), **PIN1** (`NEXTJS-15`), and the transient pool-timeout cron-rush class — all already tracked.
- **F1/F2-TierB** (broader serial>circ mis-key batch + 65 residual Cosmic 8:62 Tier-B sales) stays in the ledger; F3 guard protects WAP.
- **Q7 / env fragility (INFRA):** the sandbox git index is corrupt again this run (`fatal: index file corrupt`, "index uses ǴL? extension") — same shared-Windows-mount class as Q7 (recurring `.git` lock / NUL-corruption). Refs still resolve clean (HEAD=origin/main=`ce36102`); only the on-disk index is corrupt, which blocks `git add`. Inbox file written to disk regardless. Already tracked under Q7 (needs the sandbox-native bot clone); not re-raised as new.
