# Daytime monitor — 2026-08-09T21:10Z (14:10 PT)

Source: rpc-daytime-monitor. READ-ONLY sweep. **Shell/git DOWN this run** (`useradd ... no space` — same class as the last 2 overnight passes), so this file was written to the MOUNT, push unavailable — night pass picks it up locally.

Health headline: security 4/4 clean (rls_off 0 · anon_write 0 · invariants 0 · secdef-drift []) · `detect_stalled_pipelines()` [] · editions 27,075 · DB 12,289 MB (stable vs overnight 12,227) · latest prod deploy READY (`c3e1ce4c`). **Active disk-IO saturation window at ~21:00Z during this tick** (multiple live pipeline timeouts; trust-health full scan exceeded 55 s and could not be read — overnight baseline was 2 breaches, both known/carried: panini_sale_price_capture_dry_days + unmapped_resolution_backlog_max).

## Candidate 1 (MEDIUM — read-path/observability, night pass or CC)
**`/api/check-alerts` — the alert-dispatch route — is itself timing out under saturation (4 fails / 6h, `canceling statement due to statement timeout`), and so is `rpc_ops_snapshot()`.** Both die inside the SAME node: `get_pipeline_alerts()` → its `check_unmapped_backlog_growth()` arm (added 2026-07-26), which does a heavy unmapped-sales scan on every call. Under disk-IO throttling that one arm blows the statement timeout, taking the whole alert aggregation with it — so during exactly the saturation windows when alerts matter most, the alerting path can silently not run. This is NOT covered by today's `6f5bed02` smoke-test relabel (that fixed the smoke *guard* false-page, not the check-alerts route). Source: `pipeline_runs` (check-alerts 4×timeout, last 20:55Z) + `rpc_ops_snapshot()` cancel with `CONTEXT: check_unmapped_backlog_growth`. Risk: LOW to fix (read-path only — cache/precompute the unmapped-backlog-growth arm, or bound its scan, or split it off `get_pipeline_alerts()`'s hot path). Suggested: profile `check_unmapped_backlog_growth()` (EXPLAIN is safe on a throttled DB — planning only) and either memoize it (it changes slowly) or bound it to an indexed window so the alert path survives saturation.

## Context (NOT new work — situational awareness for the night pass)
- **Saturation cluster (all `statement timeout` / `Timed out acquiring connection from pool` — the documented disk-IO-budget class, self-recovering):** compute-topshot-pack-ev, wallet-username-resolver, pinnacle-nft-resolver, fmv-recalc (step3 lock timeout), wallet-backfill-{pinnacle,allday,ufc} (`rows_lost`, self-heal next scan), 7 pg_cron MV refreshes (rpc-ccm-step1, allday-serial-fmv-jersey, serial-fmv-jersey-weekly, refresh-misattrib-candidates, refresh-allday-pack-realized, thin-sale-ask-disclosure, **rpc-reconcile-saved-wallet-stats** [the 08-08 nightly reconcile, jobid 259 — first time seen timing out, but display-only + self-corrects on its next nightly run]). Root fix = query/index work, not a tier bump (per CLAUDE.md). **The nc1 PUBLIC-BOARD-CACHING item queued by the overnight pass has already been SHIPPED by CC today** (candy-mlb + panini-squeeze + deals/rookies/first-mint all snapshot-cached, `105deb95`→`c35306e8`); the deals/rookies backing-view index fix is already queued by CC (`inbox/2026-08-09T1941Z.md`). No action needed here.
- **Sentry:** 1 new issue in 8h — `JAVASCRIPT-NEXTJS-25` "smoke test failed: cursor-stall threshold…" (1 event, 5h ago). The saturation-induced smoke guard-errored false page; already addressed by today's `6f5bed02` deploy. Not a real breakage.
- Benign: `topshot-active-listings-ingest` 2×`egress_blocked` (known worker-egress class); `sync-nba-projections` 2×`all_upstreams_failed` (NBA off-season, expected).

## Not done this tick (deliberate)
Deep artifact-payload validation skipped — running ~11 heavy payload queries against an actively-saturated DB would add read load and mostly time out (false "broken artifact" findings). Today's DB migrations were cron-cadence + function-body changes (20260809203055 pack-reality cadence, 20260809145547 ufc metric retirement) with no view/column renames, so schema-break risk to artifacts is low. Section 1a daily extras skipped (afternoon tick, not first-of-day).
