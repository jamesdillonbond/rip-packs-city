# Daytime monitor inbox — evening tick (2026-06-22 ~17:05 PDT / 2026-06-23 00:05Z)

Platform GREEN. Sweep: pipeline_runs **1 fail / 6h** (evm-transfers-ingest base_mainnet 429 — known-benign evm-429 class); detect_stalled **[]**; check_pgcron_recent_failures() **[]**; security **0/0/0/0** (rls-off [], anon/auth-write-on-rls-off [], public_invariants [], secdef []); trust-health **9/9 ok** (ts_uuid_dupes_created_24h 0/200, unmapped 23/100, pinnacle_ask 0.2h/3, pinnacle_fmv 14h/30, edition_integrity 4/50, fmv_sanity 0, pack_ev_stale 0.88d/2); sentinel hyphen-UUID-48h **2/250**; FMV **TS H+M 4361** (1215+3146, improving vs 4351 @21:05Z / 4328 baseline) / **AllDay 896** (251+645, improving vs 893), both reconcile (TS latest-per-edition sum 17,316 vs 17,318 editions = 2 fresh `::` parallels awaiting first snapshot, benign; AllDay 6,191 exact); DB **5132 MB** (+9, benign); Vercel **0 ERROR**, prod **75ee62f READY**; Sentry **1 unresolved** (NEXTJS-1Q, benign/transient — Watch). 13/13 artifact backing objects return rows (squeeze 501+/deals 696/serial-prem 185/perfect-mint 14/underpriced 11/top-sales 993/trophies 687/rookies 61/special-serial-mv 6784/cohort 158/set-overlap 246/tracked-fmv-conf 22/rewards 1); artifact HTML on OneDrive unreachable (MONITOR-ARTIFACT-ACCESS, known — validated via backing objects).

## EDITIONS DELTA (expected, verified — NOT a writer leak)
- **UFC editions 446 -> 518 (+72)** since the 21:05Z tick. Confirmed = the intended **UFC-EDITIONS-SEED-GAP** fill: a single batch insert at 2026-06-22 21:56:53Z (migration `audit_20260622_seed_missing_ufc_editions_from_wmc`, prod `75ee62f`), exactly the known 72-edition gap, all real fighters (Sean Brady / Kai Kara-France / Diego Lopes / Arman Tsarukyan / Cub Swanson ...) with correct CHALLENGER/CONTENDER tiers. TS / AllDay / Golazos FLAT (17,318 / 6,191 / 581). `ts_uuid_dupes_created_24h` = 0 -> no leak. (Minor, NOT a candidate: the 72 seed rows carry `set_name = NULL`; UFC has no `/sets` surface so this is cosmetic — night-pass post-ship-watch can glance but it does not affect rendering/tier.)

## Candidates (0 new)
No new low-risk candidate work this tick. The one standing artifact-accuracy item — rpc-live-health `leak_24h`/`leak_48h` panel over-counting `::` parallels (predicate `^[0-9]+:[0-9]+$` matches benign subedition rows; shows ~1,775 vs the ~2 real hyphen-UUID leak) — was logged in `2026-06-22T15-17Z.md` (CAND-1) and re-acknowledged 21:05Z. NOT re-logged; still the only standing artifact-repair candidate for the night pass (update both predicates to `^[0-9]+:[0-9]+(::[0-9]+)?$`).

## POST-SHIP WATCH (night pass) — NEW wave since 21:05Z, unseen by any prior monitor
- **`75ee62f` (current prod, READY) — CC-queue drain, shipped ~21:56Z (~55 min AFTER the 21:05Z monitor commit, so no prior monitor saw it).** Three changes, all verified healthy this tick:
  - **Item 2 — UFC edition seed** (migration above): +72 editions, verified real/correct (see EDITIONS DELTA); wmc->editions invariant restored.
  - **Item 5 — dropped orphaned 3-arg `get_user_top_owned_moments`** (`audit_20260622_drop_orphaned_get_user_top_owned_moments_3arg`): verified CLEAN — exactly ONE overload remains (`p_user_id uuid, p_limit integer, p_league text, p_collection_id uuid`), no missing/leftover signature.
  - **Item 6 — `softIfTransientRpc` smoke-leg hardening** (kills NEXTJS-A cry-wolf): consistent with NEXTJS-A no longer in the unresolved set; security guards stay hard. Watch: confirm no smoke false `ok=false` AND that a genuine breach still pages.
- **Earlier wave `dbdbd0dd` (user-facing Flowty teardown) — superseded as prod by `75ee62f`; both READY, 0 ERROR.** 21:05Z handed this off; re-confirm collection / moment / analytics public surfaces + a concierge buy-FAQ probe (no Flowty recommendation) on the deeper night pass.

## Watch (no action — tracked/benign)
- **Sentry NEXTJS-1Q** "router state header could not be parsed" on `/[collection]/packs/page`: still **1 event / 1 user**, first+last seen ~21:0xZ (deploy-swap window) — generic App-Router RSC transient, NOT recurring/spiking. Flag only if it climbs. (Carried from 21:05Z.)
- **pg_cron** both jobs healthy: special-serial-owners-mv last ok 16:13Z (next 04:13Z, MV 6,784), remap converged. No stale-pre-fix residue.
