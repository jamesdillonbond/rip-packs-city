# Daytime monitor inbox — 2026-06-03T06:22Z

Run: `rpc-daytime-monitor` (READ-ONLY sweep). Working tree: shared repo (no bot clone). Lock: ~16h-stale RELEASED marker → took over. Baseline: `metrics-latest.json` @2026-06-02 13:42Z; cross-checked vs the 06-03T05:30Z + 01:49Z daytime inboxes. Current prod HEAD `d8cc6c2` ("remove storefront-cleanup machinery — kills FLOW payer-wallet drain at source"). Sandbox clock accurate this run (sandbox 06:06Z ≈ DB now 06:08–06:21Z).

## Verdict: ✓ HEALTHY — platform green; security 0/0; Vercel 20/20 READY, 0 ERROR; 12/12 artifacts OK; FMV fresh + flat. `detect_stalled_pipelines()` returned 2 entries, BOTH benign-and-explained (neither is a real stall): `cadence-payer-balance-check` (Trevor intentionally PAUSED its cron today) + `pinnacle-metadata-backfill` (one missed hourly tick). 2 NEW low/med monitoring-config candidates (C-PAYER, C-PIN) — both are watchlist tweaks of the P1/Q10 night-pass-SHIP-eligible class. PIN1 (NEXTJS-15) unchanged from 05:30Z.

---

## NEW candidates

### C-PAYER · [MED · monitoring-config — night-pass SHIP-eligible; shippable slice of N3] Deactivate the `cadence-payer-balance-check` watchlist row now that its cron is intentionally paused — it now permanently trips `detect_stalled_pipelines()`
- **Source:** `detect_stalled_pipelines()` → `{pipeline: cadence-payer-balance-check, severity: high, silent_minutes: 68, max_silent_minutes: 60}`. `pipeline_runs`: last run **2026-06-03 05:00:41Z was ok=true** (balance fine); 02:00–05:00Z all ok=true; then NO runs (the expected 05:30Z + 06:00Z ticks never fired). Watchlist row still `is_active=true` @60m.
- **Diagnosis:** Trevor intentionally PAUSED this cron today — prod HEAD `d8cc6c2` ("remove storefront-cleanup machinery … payer wallet is intentionally empty + `cadence-payer-balance-check` cron paused; route left dormant for revival") + CLAUDE.md known-issue #9 + ledger N3. So the silence is by-design, not a stall. But the still-active 60m watchlist row means a **HIGH-severity** `detect_stalled_pipelines()` entry will now fire on **every** sweep indefinitely — the exact "train us to ignore the deterministic stall check" anti-pattern P1 (evm) and Q9 call out. The 05:30Z sweep saw `detect_stalled=[]`; this is brand-new since the ~05:00Z pause.
- **Risk read:** LOW-risk fix, MED priority (a HIGH-severity false-positive on the one deterministic stall check degrades its signal fast). Pure DB monitoring-config `UPDATE` — NOT the wallet, NOT the external cron, NOT route logic, not destructive, not off-limits. Same class the night pass already ships (Q10, P1, the `*-sales-indexer` watchlist rows). Decouples cleanly from N3's operator-only parts (funding the wallet / un-pausing the cron-job.org entry / reviving Cart/Trade-Hub).
- **Suggested action (night pass):** `UPDATE pipeline_cadence_watchlist SET is_active=false, notes='Cron intentionally paused 2026-06-03 (d8cc6c2 — FLOW-drain removal; payer wallet dormant). Re-activate when funding the wallet + un-pausing the cron-job.org entry per ledger N3.' WHERE pipeline='cadence-payer-balance-check';`
  Revert: `UPDATE pipeline_cadence_watchlist SET is_active=true, notes='<prior>' WHERE pipeline='cadence-payer-balance-check';`
  Target: `detect_stalled_pipelines()` stops listing `cadence-payer-balance-check`. N3's wallet/cron-revival decision stays open + operator-owned; this only silences the now-meaningless stall flag.

### C-PIN · [LOW · monitoring-config / external-cron — watch + P1-class threshold raise] `pinnacle-metadata-backfill` missed its 05:22Z hourly tick → trips `detect_stalled_pipelines()` at 90m; threshold trips on a single missed hourly tick
- **Source:** `detect_stalled_pipelines()` → `{pipeline: pinnacle-metadata-backfill, severity: medium, silent_minutes: 106, max_silent_minutes: 90}`. `pipeline_runs`: reliable hourly at :22 (14:22 → 04:22 all ok=true) but **no 05:22Z row** (cron-job.org skipped the tick; also skipped 00:22Z). Last run 04:22Z; next expected 06:22Z.
- **Diagnosis:** Healthy pipeline (12+ consecutive clean hourly runs), not a route fault — an external cron-job.org tick-skip. But `max_silent_minutes=90` for an *hourly* pipeline trips on any single missed tick (60m normal → one skip = ~120m gap > 90m). Same mis-tuning class as `evm-transfers-ingest` (P1: hourly @60m → queued raise to 150m). Trivial impact (metadata backfill, queue caps 100/50, self-heals next tick).
- **Risk read:** LOW. Two framings: (a) threshold too tight → raise 90→150m (tolerate one skipped hourly tick — P1/Q10 monitoring-config class, night-pass SHIP-eligible); (b) if cron skips become frequent it's an external-cron reliability issue (operator). Lean (a) — a single trivial-impact skip shouldn't fire the deterministic stall check.
- **Suggested action:** FIRST **verify it self-recovered at 06:22Z** (if it ran, this was a one-off skip). If self-recovered: `UPDATE pipeline_cadence_watchlist SET max_silent_minutes=150, notes='Hourly :22; cadence-only. 150m grace covers one skipped tick.' WHERE pipeline='pinnacle-metadata-backfill';` (revert to 90). If 06:22Z ALSO skipped → real external-cron stall → operator re-fire the cron-job.org entry (Q3/N1 class). Target: a single missed hourly tick no longer trips `detect_stalled`; a true multi-hour stall still does.

---

## Observed during sweep — no new action
- **PIN1 (NEXTJS-15) unchanged from 05:30Z** — Sentry: 1 unresolved total = `JAVASCRIPT-NEXTJS-15` (`listing_resolution_failures_inserted`, `GET /api/pinnacle-listings-indexer`), 3 events, last ~1h ago, not spiking. Already logged 05:30Z (the `cadence_capped`-deferrals-counted-toward-spike gate tuning, operator/CC). No re-log.
- **N2 tripped this window at the 06:00Z rush (open; do NOT revert):** `topshot-moments-hydrator` failed 06:02:04Z — `candidate_read: v_moments_needing_hydration … canceling statement due to statement timeout` (47s). Consistent with N2 (recurs at the 00:00/06:00/12:00 cron rushes, self-recovering, 3/138-class). Do NOT revert the materialized-CTE fix (net-positive). Deeper fix (statement_timeout bump / supporting index / further view cost reduction; fold S1 `security_invoker=on` into the same `CREATE OR REPLACE`) stays operator/CC.
- **A1 (gas wallet) stays resolved/by-design:** the acute dry-wallet emergency ended (topped up ~01:30–02:00Z; checks ok 02:00–05:00Z) and the drain source was removed at the root today (`d8cc6c2` deleted `cleanup-storefront-wallets.mjs` + `cleanup.cdc`). Ledger N3 (operator decision on funding/alerting) stays open; **C-PAYER above is its monitoring-noise slice**, now actionable because the pause has made the stall flag fire.
- **Transient 06:00Z cron-rush cluster (no action, known recurring class):** 6 fails at the 06:00–06:05Z rush — `pinnacle-nft-resolver` + `wmc-fmv-populate` (upstream request timeout), `topshot-fmv-populate` (statement timeout — `fmv-recalc` is the primary FMV writer so freshness is unaffected), `snapshot-institutional-wallets` (upstream connect reset), `topshot-moments-hydrator` (N2). Plus the 00:00Z-rush fails (00:09–00:21Z: pinnacle-listings-reconcile pool, wmc-fmv-populate lock). All self-recovering, none logic faults, none deploy-attributable. The `sentinel_fmv_confidence_rows` RPC also statement-timed-out under this contention (transient; FMV freshness confirmed independently via `max(computed_at)`).
- **P1 still queued, not tripping:** `evm-transfers-ingest` ran within its 150m window; not in `detect_stalled` this sweep. SHIP-eligible next in-window night run as logged.
- **Vercel:** 20/20 recent prod deploys READY, 0 ERROR. Today's ships (`6e90f3f` STALE-confidence JSON-LD gate, `64e3f4a` packs cheapest-sort, `0e1da2d` smoke soft-fail, `d8cc6c2`/`6e4638e` storefront-cleanup removal + docs) all READY. **No recent-ship regression** — none correlate with the gas wallet, the `detect_stalled` entries, or the cron-rush timeouts; the cadence-payer silence is a deliberate consequence of `d8cc6c2`, not a regression.
- **Artifacts — 12/12 OK:** 11 backing queries re-run clean this sweep (deals 131, offer-spread 5563, squeeze 3099, rookies 61, market 554, pack-reality-top-ev 90, `v_fmv_sanity_flags` 1 [the expected 8:62 mis-key, F1/F2 queued], funnel_events 43, `pack_table_rows` 5221 [rtr-pack-finder], my-wallet wmc 18,451 / 5 colls, plus the pipeline/security/deploy/DB-size paths). `rpc-fmv-watch`'s confidence-breakdown query statement-timed-out under the 06:00Z rush (transient — validated clean 36m ago at 05:30Z, no schema change shipped since, freshness confirmed). None edited (validate-only).

## Drift vs 06-02 13:42Z baseline / 05:30Z sweep (all flat or improved)
- FMV TS HIGH+MED ~**933** (confidence RPC contended this sweep; last clean reads 05:30Z=933 / 01:49Z=937 — flat); FMV fresh (TS+AllDay latest `computed_at` 05:48Z, ~27m at sweep — `fmv-recalc` healthy).
- editions TS **16,344** / AllDay 6,191 / Golazos 581 / UFC 446 (flat). DB **5,994 MB** (+1 vs 05:30, normal). sentinel TS-UUID-48h **45** (flat, <250). unmapped_sales **278** (flat).
- Security **0/0** base tables (RLS-off + anon/auth write-grants both empty). The ~25 anon-readable SECURITY DEFINER insights views are the by-design distribution wedge — not a finding.

## Infra note (Q7, already tracked)
- `docs/overnight/.lock` is a ~16h-stale RELEASED marker (sandbox can't unlink — the known Q7 Windows↔sandbox fragility). Did not block this run. Left untouched.
