# Daytime monitor inbox — afternoon tick (2026-06-22 ~14:05 PDT / 21:05Z)

Platform GREEN. Sweep: pipeline_runs **0 fails / 6h**; detect_stalled **[]**; check_pgcron_recent_failures() **[]**; security **0/0/0/0** (rls-off [], anon/auth-write-on-rls-off [], public_invariants [], secdef []); trust-health **9/9 ok** (ts_uuid_dupes_created_24h 0/200, unmapped 23/100, pinnacle_fmv 11h/30, edition_integrity 4/50); sentinel hyphen-48h **2/250**; FMV **TS H+M 4351** (1208+3143, improving vs 4328 baseline) / **AllDay 893** (252+641, improving vs 891), both reconcile (TS sum 17311 vs 17318 = gap 7 fresh `::` parallels awaiting first snapshot, benign; AllDay 6191 exact); editions **FLAT** (TS 17,318 / AllDay 6,191 / Golazos 581 / UFC 446 = no writer leak); conflation_guard 31 (benign accrual — ts_uuid_dupes 0 + editions flat; refresh/remap pg_cron converge it); thin_fmv 95; DB **5123 MB** (+33 vs baseline, benign); Vercel **0 ERROR**, prod dbdbd0dd READY; Sentry **2 unresolved** (both known/transient — Watch). 13/13 artifact backing objects return rows (squeeze 9102 / deals 695 / serial-prem 183 / perfect-mint 14 / underpriced 11 / top-sales 976 / trophies 687 / rookies 61 / special-serial-mv 6784 / cohort 158 / set-overlap 246 / tracked-fmv-conf 21 / rewards 1); artifact HTML on OneDrive unreachable (MONITOR-ARTIFACT-ACCESS, known — validated via backing objects).

## Candidates (0 new)
No new low-risk candidate work this tick. The one open artifact-accuracy item — rpc-live-health `leak_24h`/`leak_48h` panel over-counting `::` parallels (shows ~1775 vs the ~2 real hyphen-UUID leak) — was already logged in today's `2026-06-22T15-17Z.md` (CAND-1). NOT re-logged; still the only standing artifact-repair candidate for the night pass (update both predicates to `^[0-9]+:[0-9]+(::[0-9]+)?$`).

## Closeout / Watch (no action — verification + post-ship-watch handoff for the night pass)
- **CLEARED — both pg_cron jobs the 15:17Z tick flagged as STALE pre-fix failures have now SUCCEEDED at their post-fix ticks** (verified in cron.job_run_details): `rpc-refresh-special-serial-owners-mv` 16:13Z **succeeded** (1 row — the night-pass `enable_nestloop=off` force-hashjoin fix proven live on the cron path; MV freshened to 6784) and `rpc-remap-misattributed-sales` 18:23Z **succeeded** (1 row — the ~13:46Z remap window/scope fix proven). check_pgcron_recent_failures() now `[]`. CLOSED-verified — do NOT re-investigate; the 12:23Z remap failure was the stale pre-fix run.
- **POST-SHIP WATCH (night pass) — Trevor shipped a heavy daytime wave AFTER the 15:17Z first tick**, all prod READY / 0 ERROR, no new Sentry cluster on the touched surfaces: **dbdbd0dd** "remove all user-facing Flowty marketplace references" (CURRENT PROD; big refactor — collection page Flowty-Ask column / F-badge / List-on-Flowty + View-on-Flowty links / `MomentDetailModal` flowty CTA / analytics Flowty deep-links + timeline entries / the `/out/flowty` interstitial route + wallet-expand link all removed); plus concierge **b73bf378** (stop recommending Flowty), **20d75a25** (model-retirement guard), **0e251ab5** (FMV methodology version 1.5.0->1.7.0); CSP **7fe106d3** (whitelist ipfs.dapperlabs.com — restores 106 legacy Series-1 thumbnails); audit-followups **ed66a5b** (pack-thumb onError fallback / sub-cent EV / pack-reality copy / /badges 307-redirect). This is the freshest ship wave unseen by any prior monitor — re-measure the collection / moment / analytics public surfaces + a concierge buy-FAQ probe.
- **WATCH (benign, not a finding)** — Sentry **NEXTJS-1Q** "router state header could not be parsed" on `/[collection]/packs/page`: NEW but **1 event / 1 user**, first+last seen ~20:09Z (≈ the deploy-swap window); generic Next.js App-Router RSC transient (stale router-state-tree mid-deploy-swap / bot prefetch), not a code regression — flag only if it recurs/spikes. **NEXTJS-A** smoke `analytics_pipeline_health` transient: quiet ~5h, FMV independently verified healthy (TS H+M improving, fmv_sanity_flags 0), not spiking (ANALYTICS-SMOKE-RESIDUAL, carried).
