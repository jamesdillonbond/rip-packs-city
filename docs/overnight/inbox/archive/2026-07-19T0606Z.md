# Daytime monitor — 2026-07-19T06:06Z (~23:06 PDT Jul 18)

No clock skew (shell 06:06:06Z ≈ DB `now()` 06:06:22Z ≈ newest sale 06:00Z ≈ newest FMV 05:54Z). Lock **RELEASED** (15:40Z, night-20260718-22284). Not first-tick-of-day (prior ticks 21:06Z, 00:10Z, 03:06Z) → section 1a daily extras skipped; trust health came through `rpc_ops_snapshot()` regardless and is clean.

**Health: GREEN, no breakage.** security 0/0/0/0 (re-verified 06:06:36Z, after tonight's full DDL wave) · trust **15/15 ok, 0 breaches** · sentinel TS-UUID-48h 0 · invalid indexes 0 · DB **10,041 MB** · Sentry **0 unresolved** (production, 24h) · Vercel **0 ERROR**, prod `ac04c4d7` READY.

**Two new candidates, both from pipelines armed in the last ~90 minutes.** Neither is user-facing and neither is data-destructive, but both fail on every tick until touched, and the session that armed them appears to still be active.

**Concurrent-session note:** still shipping hard — origin/main advanced to `4ea82128` (+~20 commits since the 03:06Z tick), including the Dune seller-recovery arm (`b69c6010`), the Candy offers indexer (`ead32361`), the Panini coverage disclosure (`ac04c4d7`), a ledger-clobber incident + restore + new ledger-guard CI job (`0f9e9acb`), and a CI-status watch added to ops-monitor (`eb5d7f6f` — which closes candidate 1 of the 03:06Z inbox). The night pass should re-read the ledger before acting; essentially all of tonight's surface is <24h old.

---

## Candidates

### 1. DUNE-SELLER-RECOVERY-EXECUTE-400 — MEDIUM (fresh; blocks a just-activated 3.6M-row drain)

**Source:** `pipeline_runs` — `sales-seller-recovery-dune`, 2026-07-19 05:47:35Z, `ok=false`, `threw: execute HTTP 400 (2026-06-11..2026-06-18)`. Route `app/api/cron/sync-sales-seller-recovery-dune/route.ts`, hourly Vercel cron `47 * * * *`.

**What happened.** The ledger's own one-step operator gate — *"add Vercel env `DUNE_SALES_SELLER_QUERY_ID=8027085`"* — was completed at ~05:33Z (`b69c601` *"chore: rebuild to bake DUNE_SALES_SELLER_QUERY_ID"*). The pipeline's **first non-inert run**, 14 minutes later, failed. It is no longer logging `skipped:dune_not_configured`, so the env var took; the failure is downstream, in the Dune `execute` call itself.

**The window in the error is the seed cursor, which confirms the route logic is fine.** `sales_seller_recovery_state` reads `cursor_end = 2026-06-18`, `window_days = 7`, `floor_date = 2020-01-01`, `updated_at 04:37:44Z` — so the attempted window `2026-06-11..2026-06-18` is exactly `cursor_end` minus one window. **The cursor did not advance** (correct fail-safe: it only advances on a successful window), so the same request will be retried at 06:47Z and hourly thereafter until fixed. No partial or duplicate writes are possible — `apply_sales_counterparty_external` is fill-only, idempotent and audited, and it was never reached.

**Blast radius: none today, but the intended work is not starting.** This lane exists to supersede the multi-week Flow-REST decode grind for the ~3.6M-row historical null-seller backlog. The `sales-counterparty-backfill` worker is unaffected and still running well (72 runs/6h, 4 contention fails, latest ok) — the RPC deliberately does not touch that worker's cursor. So the cost is opportunity, not damage.

**Risk read:** LOW risk to investigate (read-only), MEDIUM value. Diagnosis needs one `dune-proxy` execute against query 8027085 with the same two params — everything else is already proven (the same proxy, secret and pattern power `sync-topshot-ownership-dune`, which is healthy, and the per-wallet fast path ran successfully via Dune MCP earlier tonight).

**Suggested action:** reproduce the execute call and read Dune's 400 body — the route currently surfaces only the status code, which is why this is a guess rather than a diagnosis. Most likely candidates, in order: (a) parameter **name or type mismatch** between what the route sends (`start_date`/`end_date`) and how query 8027085 declares them (Dune 400s on an unexpected parameter key and on a text-vs-date type mismatch); (b) the saved query not being in a state the Query Execution API accepts. Cheap durable improvement regardless of cause: include the response body in the thrown message so the next failure is self-diagnosing. **Not auto-shippable blind** — it touches a sales-write lane and the fix depends on evidence this run could not read.

---

### 2. COMPUTE-LALIGA-PACK-EV-ALGO-VERSION-SCHEMA-MISMATCH — LOW/MEDIUM (silent daily failure; one-line fix or retire)

**Source:** `pipeline_runs` — `compute-laliga-pack-ev`, 2026-07-19 05:00:54Z, `ok=false`, `pack_ev_history insert: Could not find the 'algo_version' column of 'pack_ev_history' in the schema cache`, `rows_written 0`. Route `app/api/cron/compute-laliga-pack-ev/route.ts:186`, Vercel cron `30 5 * * *`.

**This is a real schema mismatch, not a stale PostgREST cache.** Verified against `information_schema`: `pack_ev_history` has 21 columns — `id, pack_listing_id, collection_id, dist_id, pack_name, pack_price, gross_ev, pack_ev, is_positive_ev, value_ratio, fmv_coverage_pct, edition_count, total_unopened, depletion_pct, snapshotted_at, primary_price, secondary_ask, price_source, primary_available, secondary_available, typical_ev` — and **no `algo_version`**. The route builds its EV row with `algo_version: ALGO_VERSION` at line 186, so the insert can never succeed.

**It fails every day, not once.** `pipeline_runs` shows a single row for this pipeline, but that is retention, not history — the daily prune holds ~9.5K rows against ~450 runs/45min, i.e. roughly a 15-hour window. The route dates to `234a20b0` (2026-05-09) and was last touched by `a47c88f9` (2026-07-11, the WAP→ASP rename, which the ledger records as having updated this file's physical-column writes). Whichever change dropped `algo_version` from `pack_ev_history`, the daily 05:xx tick has been dying at the insert since.

**Why it is LOW and not a breakage: Golazos pack-EV is fine.** `compute-golazos-pack-ev` writes the same collection successfully every 6h — 126 `pack_ev_history` rows in 24h, newest 00:40:22Z — and `pack_ev_board_max_stale_days` is **0.40** against a breach of 2. `pack_ev_latest` holds 1,824 rows. Nothing user-facing is stale. This route is the 2026-05-09 *fallback*, and it now looks superseded by the healthy one.

**One correction worth carrying:** only the `pack_ev_history` insert is wrong. The second `algo_version` at line 282 targets **`fmv_snapshots`**, which does have that column — that insert is correct and simply never runs, because the function returns on the EV insert error first. A fix that strips `algo_version` from both would be wrong.

**Risk read:** LOW risk, LOW-MEDIUM value. Two clean options: delete the single `algo_version` key from the `pack_ev_history` row (restores the route including its FMV-sentinel pass), or retire the cron entry if `compute-golazos-pack-ev` has genuinely superseded it. **The second is the real question** and it is a judgment call about which route owns Golazos pack-EV — related to the long-standing ledger item *Q2 · 2026-05-30 · Confirm `compute-laliga-pack-ev` cron cadence*, which can now be closed either way with this evidence.

---

### 3. PINNACLE-SYNC — CARRIED, and the decisive tick is ~4h out

Silent **2,639 min (44h)**, last run 2026-07-17T10:07Z; missed the 07-18 10:07Z tick. Unchanged since the 21:06Z / 00:10Z / 03:06Z inboxes — no new information, restated only because the **07-19 10:07Z tick decides it**. A second consecutive miss means it is not self-healing.

Since the 03:06Z tick this has been promoted to an explicit operator item in `docs/manual-steps-2026-07-19.md` (*"re-enable the dead `pinnacle-sync` cron-job.org trigger"*), which matches the cron-job.org dropout class. **The FMV half remains covered by the 07-18 pg_cron backstop and is healthy** — `pinnacle_fmv_stale_hours` **7.5** (breach 30), `pinnacle_render_floor_stale_hours` **4.4** — versus 29.3 and an imminent breach at the 07-18 overnight pass. The **catalog half is still unbackstopped**; that remains the residual exposure if the trigger stays dead.

---

### 4. UPSTREAM-429-CLUSTER — LOW (note only, no action)

Three unrelated rate-limit refusals in the 6h window, all self-limiting, all latest-run-recovered except the badge catalog:

- `wallet-backfill-ufc` 05:42Z — `Flow script HTTP 429: 100/second request limit reached` from **QuickNode**. 1 fail / 189 runs; latest ok. Worth a datum because it names a *provider-side* ceiling on the Flow script path rather than one of our own throttles, and the counterparty backfill is currently pushing Flow REST hard.
- `topshot-badge-catalog` 05:37Z — Top Shot GraphQL 429. Carried **BADGE-CATALOG-STALE-429** class, unchanged.
- `fmv-recalc` 05:48Z — `edition_page_fetch: upstream request timeout`, 1 fail / 20 runs in window (6/24h). Carried **FMV-RECALC-EDITION-FETCH-TIMEOUT-CREEP**; FMV freshness unaffected (`topshot_fmv_stale_hours` 0.3, `allday_fmv_stale_hours` 0.2).

---

### 5. pg_cron — one known-class failure, self-recovering

`check_pgcron_recent_failures()` returns exactly one row: **`rpc-allday-cross-source-sales-dedup`**, 2026-07-19 05:40:00Z, `canceling statement due to statement timeout`, **1 fail / 24 runs** in window. Recent enough to be a genuine finding rather than a stale pre-fix run, but it is the carried **CROSS-SOURCE-DEDUP-STATEMENT-TIMEOUT** class (contention-window flap, recovers on the next tick, deliberately not auto-shipped because the `sold_at`-window fix is not result-identical for the backfill-fed `sales` table and it is a DELETE path). No new information; not re-raised as a new item.

---

## Post-ship watch — tonight's wave (since the 03:06Z tick)

| Ship | Verdict |
|---|---|
| `b69c6010` Dune seller-recovery **armed** (env baked) | **FAIL** — first non-inert run 400s. See candidate 1. Cursor parked, no writes, no harm. |
| `ead32361` candy-offers-indexer (`candy_offers`, cron `50 */6`) | **PASS (pre-first-sweep)** — table present, RLS-on, 0 anon-write grants, **0 rows**. Zero rows is *expected*: the first sweep is 06:50Z, ~44 min out. Do NOT flag empty `candy_offers` before then. |
| `ac04c4d7` Panini coverage disclosure (page + API contract) | **PASS** — deploy READY; backing view `panini_coverage_summary` present and queryable, so the fail-soft path is not silently carrying the surface. |
| `eb5d7f6f` CI-status watch in ops-monitor | **PASS (structural)** — closes candidate 1 of the 03:06Z inbox, which flagged CI as absent from every automated sweep. Read-only GH API + explicit read-only permissions block. Cannot verify a live run from here (GitHub API is outside this session's fetch set); the first scheduled `13,43` tick validates. |
| `0f9e9acb` ledger clobber restore + ledger-guard CI job | **PASS** — `docs/overnight/ledger.md` intact at clone `4ea82128`, "Declined — do not re-suggest" heading present with both entries. The guard is the durable fix for the concurrent-session whole-file-rewrite hazard. |
| `1084a2de` / `dddeed2a` AllDay `lock_checked_at` stamping + its own retraction | **PASS (inert, self-corrected)** — the follow-up commit correctly retracts the "route is live" claim: `allday-lock-refresh` has zero `pipeline_runs` rows, so the stamping fix changes nothing until something schedules it. Recording that the retraction is accurate; the underlying WMC-LOCK-FRESHNESS question is unchanged and still owner-level. |

Security invariants (`invariants` / `secdef_anon_violations` / `rls_off_base_tables` / `anon_write_holes`) all **[]** at 06:06:36Z, i.e. after every migration above.

---

## Validation notes

**Artifacts — inferred, not directly replayed (fourth consecutive tick with this constraint).** `list_artifacts` returns 17, but the artifact directory (`C:\Users\TDill\Claude\Artifacts`) is outside this session's connected folders, so the embedded payload queries cannot be read and replayed. Validated the failure mode the section exists to catch — a same-day schema change silently breaking a dashboard — by exercising the shared dashboard surfaces plus every object tonight's DDL touched: `pack_ev_latest` **1,824** · `funnel_events` **23/24h** · `cross_collection_cohort_mat` **154 rows @ 04:10Z** (fresh today, well inside the 26h bar) · `panini_coverage_summary` present · `candy_offers` present · `sales_seller_recovery_state` readable · invalid indexes **0** · `v_rpc_trust_health` **0 non-ok**. **No artifact flagged broken.** Standing ask, fourth tick: making the artifact dir a connected folder would convert this from inferred to actually executed.

**Sentry — clean.** Zero unresolved production issues in 24h, down from 3 at the 03:06Z tick (`JAVASCRIPT-NEXTJS-1C` / `-1X` / `-1Z`, all previously logged) — those have aged out of the window without re-firing.

**Drift vs `metrics-latest.json` (07-18 15:23Z baseline).** DB 9,878 → **10,041 MB** (+163 over ~15h; crossed 10 GB at the 03:06Z tick, +31 since — steady, no single pathological table). TS editions 19,429 → **19,451** (normal `::` cataloging). `topshot_impossible_parallel_serials` **27 → 0** — the baseline's queued self-healing breach has cleared on its own, so **IMPOSSIBLE-PARALLEL-27 can be closed**. `pinnacle_fmv_stale_hours` 29.3 → **7.5** — the baseline's imminent breach resolved via the pg_cron backstop, without the queued manual `pinnacle_fmv_recalc_render_all()` ever being fired, so **PINNACLE-SYNC-FMV-STALE can be closed on the FMV half** (the catalog half stays open per candidate 3). `unmapped_resolution_backlog_max` 24 → 25. FMV TS HIGH+MED 3,349 → **3,372**, stable — the sales-cooldown redistribution has flattened as predicted, confirm-only.
