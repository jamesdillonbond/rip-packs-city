# Daytime monitor — 2026-06-16 ~06:15Z (23:15 PDT, final late-night tick before the 01:02 PDT night pass)

PLATFORM GREEN. Read-only sweep + artifact validation all clean. Last monitor tick before tonight's night pass (~08:02Z / 01:02 PDT, ~2h out). Two fresh Trevor ships landed AFTER the 03:23Z inbox — both READY, both verified DB-clean — logged below as one post-ship-watch candidate. No breakage, no broken artifacts.

> Delivery: sandbox clone + push (mount-fallback noted at bottom if push failed). PAT never printed.

## Health snapshot (measured this tick, 06:06-06:15Z)
- pipeline runs 6h: **3201 runs / 0 fails**. 24h fails: **1** — `analytics-smoke` @16:13Z (known DB-IO barometer transient, self-recovered). detect_stalled_pipelines() **[]** · get_pipeline_alerts() **[]** (N1 recovered earlier today).
- security **0/0/0/0**: RLS-off base tables 0; anon/auth write on RLS-off base tables 0 (relkind r/p); check_public_security_invariants []; check_secdef_anon_execute_violations [].
- sentinel TS-UUID-keyed-48h **0**; ts_uuid fossils total **6406** (flat). editions flat (TS 15543 / AllDay 6191 / Golazos 581 / UFC 446 - no DUPE growth).
- FMV healthy (via fmv_current, canonical int-pair): TS HIGH **926** + MED **2313** (NO_DATA only **202**); AllDay HIGH+MED **774**/6191; Pinnacle per-render priced ~1845. Writers fresh (fmv 06:14Z / pack-ev 06:13Z / edition_offers 06:07Z). sales 24h TS **2072** / AllDay **774** (healthy, no decode throttle).
- DB **4679 MB** (benign creep from 4667 @03:23Z).
- Sentry: **1 unresolved** - `JAVASCRIPT-NEXTJS-A` "smoke test failed: fmv pipeline healthy", culprit POST /api/smoke-test, **1 event**, first=last ~04:06Z, Seer super_low. Single-event transient of the known smoke-noise class (analytics_pipeline_health momentary FMV-freshness blip under DB-IO); not a spike, not a pipeline_runs fail. Resolvable after 24h quiet.
- Vercel: **0 ERROR** across 20 recent deploys. Prod HEAD `30d2acaf` (READY). CANCELED entries = docs-only build-skips via 0e7e627 ignoreCommand / superseded redeploys (by design).

## Artifacts (13 active validated this estate; none broken/repaired)
- **NEW artifact `rpc-fmv-dialin`** (Trevor, created 04:21Z today; not in the ledger's 12-active list) - VALIDATED GREEN by running its consolidated payload: `serial_fmv_multipliers` **37 cells / 33 reliable**, computed_at 23:06Z (first 16.58x / perfect 7.78x / low 3x / normal 1x); fmv_current TS coverage + stuck_low 249 (today's dense-LOW investigation). It reads `serial_fmv_multipliers` (DROPPED 06-14, **re-created tonight** as Trevor resumed the serial-FMV build) + `fmv_current` - both present and healthy. Tracking-only (Trevor's dial-in dashboard), not night-pass-shippable; add to the active-artifact roster.
- **`rpc-live-health`** (core merged dashboard) - VALIDATED GREEN by running its payload: all **11 insights boards return rows** (squeeze 501 / set_squeeze 239 / pack_reality_top_ev 9 [thin by design, C2] / rookies 61 / market 501 / first_mint 501 / cross_collection 1 [by-design summary view] / pinnacle_scarcity 501 / offer_spread 501 / deals 501 / trophies 501); alerts []; leak_48h 0; writers fresh. (top_sales still not in this payload - the known TOP-SALES-INSIGHTS-QA tracking item, not a break.)
- Other 11 active artifacts: validated GREEN at the 03:23Z + 00:15Z ticks (3-6h ago); the only schema changes since are **additive** function-column additions (below) touching no artifact backing view/table, so they remain green. 5 RETIRED tombstones skipped.

## NEW candidate - SERIAL-FMV-DEEPEN + PRICE-BAND (LOW · night-pass post-ship watch)
Two Trevor ships landed after the 03:23Z inbox; both READY, both verified DB-clean this tick:
- **`a3db4235`** "deepen serial-FMV estimate onto collection grid, trophy picker, sniper" - migration `serial_fmv_onto_collection_grid_and_top_owned_moments`. Verified live: `serial_fmv_estimate` is now **anon+authenticated-executable** (was service_role-only); it is SECDEF but **check_secdef_anon_execute_violations() = []** AND **check_public_security_invariants() = []** - the new anon grant did NOT open a hole (pure read estimate, deliberately public via get_moment_detail). `get_wallet_moments_with_fmv` (INVOKER) + both `get_user_top_owned_moments` overloads (3-arg + new 4-arg, SECDEF, authenticated-only) present with correct grants. Additive (serial_fmv column), never overwrites edition FMV. Minor note: the 3-arg get_user_top_owned_moments overload still exists alongside the new 4-arg - if the 3-arg is now orphaned it's a cleanup candidate (verify call sites; not urgent).
- **`30d2acaf`** "cleaned 30d price range beside high-volume LOW/MEDIUM FMV badge" (prod HEAD) - migration `moment_detail_price_band_30d`; `get_moment_detail` gains `price_band_30d {low,high,n}` (p10/p90 over cleaned 30d sales, gated to LOW/MEDIUM editions with >=10 sales). DISPLAY-ONLY; never touches any FMV value. `get_moment_detail` present + healthy (SECDEF, public).
- Risk: LOW - both additive, security clean, FMV-by-collection unchanged.
- Suggested action (night pass): standard post-ship watch - (a) no new Sentry on the moment-page render path; (b) `get_moment_detail` returns `price_band_30d` + `serial_fmv` without error on a LOW/MEDIUM #1/perfect moment; (c) confirm security invariants stay [] (the anon-grant on serial_fmv_estimate). Nothing to ship/revert.

## Refinements to already-queued items (NOT re-raising as new)
- **SERIAL-FMV-WATCH (00:15Z/03:23Z inbox):** `serial_fmv_multipliers` is now populated (37 cells, fresh 23:06Z) - but `refresh-serial-fmv-multipliers` STILL has **0 pipeline_runs** (no `%serial%`/`%multiplier%` pipeline in 24h). The cells were populated by a one-off/manual run tonight, not a recurring cron. Night pass: confirm with Trevor whether this refresh needs a cron-job.org entry to stay fresh, or is manual-on-demand by design (the dial-in is mid-build).
- **ALLDAY-V1-UNMAPPED-DRIFT / ALLDAY-UNMAPPED-CLIMB:** open unmapped now **244** (down from 256 @03:23Z / 254 @21:06Z), budget-exhausted **34** (down from 47) - DRAINING, consistent with the 03:23Z read that it's normal daytime accumulation the recover-v1 + resolver chain clears overnight. No resolver gap; re-measure at the night pass.

## Carried unchanged (in ledger - NOT re-raised)
N1 snapshot-institutional-wallets daily cron drop (operator, recovered today), VERCEL cost family (Trevor/operator dashboard), A1-WORKER-PASSTHROUGH-CLEANUP (Trevor wrangler), BADGE-BACKFILL-WATCH (post-ship watch, grain holds 0 non-int-pair), TFP-480-RESTORE + TFP-SLOT-WAVE-COLLISION (closed 06-14). A1 owner-coverage: do NOT re-explore (disproven dead end).
