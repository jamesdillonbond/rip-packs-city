# Daytime monitor — midday tick (~11:17 PDT / 18:17Z, 2026-06-16)

PLATFORM GREEN. Read-only sweep (3rd run today, after the 15:14Z first-tick + the 16:33Z weekly DQ sweep). One NEW low-risk candidate (insights-QA the new Serial Premiums surface). One queued-item update: the 8am inbox's `ALLDAY-RESOLVER-ZERO-WRITE-TRIPWIRE` appears already ADDRESSED by a same-morning ship — verify+close, don't re-build.

## Health snapshot
- pipeline_runs: 6h 1851 runs / **2 fails**; 24h 8807 / **2 fails**. Both transient + self-recovered: `analytics-smoke` @16:43Z ("1 fail(s)" — the known DB-IO barometer; 17:13Z + 17:43Z ticks both ok) and `offers-sweep` @12:22Z ("fetch failed", carried from the 8am tick's window). `detect_stalled_pipelines()` **[]** · `get_pipeline_alerts()` **[]**.
- Security **0/0/0/0** (RLS-off base tables 0; anon/auth write on RLS-off base tables 0 with relkind r/p; `check_public_security_invariants` []; `check_secdef_anon_execute_violations` []). Sentinel TS-UUID-keyed-48h **0**.
- Trust health **9/9 ok** (was 8/8 — a 9th leg `unmapped_resolution_backlog_max` shipped today, see below; reads 8/100 ok). No breach; tracked DQ4 `ts_uuid_dupes_created_24h` 0/200.
- FMV (latest-per-edition, all editions): TS HIGH+MED **3435** (HIGH 990 / MED 2445) · AllDay **788** (HIGH 205 / MED 583) · Pinnacle priced **1263** (pinnacle_fmv_history latest-per-render) — flat/up vs the 08:05Z baseline (TS 3426 / AllDay 774). editions flat (TS 15543 / AllDay 6191 / UFC 446). DB **4702 MB** (+6 benign). UFC wmc null_key fossil floor unchanged.
- Vercel: prod **`f94704c`** (omni-channel alerts) **READY**; 20 recent all READY/CANCELED, **0 ERROR** (CANCELED = docs-only/monitor build-skips, by design). Latest `43fac11` docs CANCELED (correct).
- Sentry: **1** unresolved (`JAVASCRIPT-NEXTJS-A`, smoke transient, 1 event ~14h ago, super_low) — **0 new**. Resolve after 24h quiet.
- Artifacts: **19 enumerated (14 active + 5 RETIRED tombstones)**. Flagship `rpc-live-health` consolidated payload run clean — all 11 insights boards resolve (squeeze 501 / set_squeeze 239 / pack_reality 11 / rookies 61 / market 501 / first_mint 501 / cross_collection 1 / pinnacle_scarcity 501 / offer_spread 501 / deals 501 / trophies 501); FMV/pack-EV/offers panels fresh. New `rpc-serial-premiums` validated (board 271 rows, max 1532×). The other 12 active were full-estate-validated GREEN at the 8am tick; today's ships are additive-only (new views/legs/boards — no ALTER/DROP of any existing artifact-backing view), so no breakage introduced since. None broken, none repaired.

## Recent-ship health (today's daytime ships) — all WORKING
- **`f94704c` omni-channel alerts (prod READY, ~17:44Z, the freshest ship) — INERT as documented.** Verified live: 0 notification_channels / 0 alert_subscriptions / 0 alert_deliveries / 0 `alerts-dispatch`+`alerts-send` pipeline_runs. Matches CLAUDE.md ("inert until activated"). New public bot endpoints (`/api/bots/telegram|discord`, `/api/alerts/channels/verify-email`) self-authenticate; 0 new Sentry; security invariants []. Operator activation (bot secrets + 2 cron entries) already tracked in the ledger. No action.
- **`abfb75a` Serial Premiums (#1 Watch) insights board (prod READY, ~15:29Z).** Backing view `topshot_serial_premiums_board` (security_invoker, anon, **271 rows**) healthy. Surface well-formed (see candidate). Also new today: `topshot_perfect_mint_premiums_board` (~17:58Z, **10 rows**, queryable) — no public surface/artifact reads it yet; healthy backing view.
- **`5f1a28d` AllDay on-chain unmapped resolver (8am ship) — draining well.** unmapped_sales open **66** now vs 244 (night baseline) / 188 (8am) — a normal intraday wiggle off the 16:33Z DQ-sweep's 43 (new V1 sales arriving between resolver ticks); trust-health `unmapped_resolution_backlog_max` = 8/100 ok. No regression.

## Queued-item update (no NEW candidate — verify+close)
- **ALLDAY-RESOLVER-ZERO-WRITE-TRIPWIRE (8am inbox candidate) — appears ADDRESSED same-morning.** Migration `audit_20260616_trust_health_unmapped_resolution_backlog_leg` (version 20260616155839, ~15:58Z — 44 min after the 8am monitor logged the candidate) added the 9th `v_rpc_trust_health` leg `unmapped_resolution_backlog_max` (catches "a collection's priced sales failing edition-resolution → sales undercount"; breach 100; currently 8/ok). Night pass: **verify it covers the "resolver healthy but resolving 0 while backlog climbs" recurrence class, then close the candidate — do not re-build.** Minor note: the leg is a backlog-*size* tripwire, not a literal "resolved-count/24h = 0" throughput check; it catches the same failure (backlog climbs past 100) but won't catch a resolver that holds the backlog flat below 100 while never draining the existing tail. If that edge matters, a small throughput companion is the only residual — otherwise close as-is.

## NEW candidate
- **SERIAL-PREMIUMS-INSIGHTS-QA · LOW · night-pass (read-only QA, mirrors prior closes).** Source: new public surface `/insights/serial-premiums` (`abfb75a`, deploy READY). The night pass closed TOP-SALES-INSIGHTS-QA (06-15) and TROPHIES-INSIGHTS-QA (06-14) with the rpc-insights-qa 8-point checklist; this fresh sibling surface (commit explicitly "mirroring the /insights/top-sales sibling") hasn't had one. Pre-assessed live this run (read-only): backing view `topshot_serial_premiums_board` security_invoker + anon + 271 rows ✓; `check_public_security_invariants()` [] (no base-table hole) ✓; `app/sitemap.ts:330` 'serial-premiums' entry ✓; proxy.ts `/insights/*` public block present ✓; route `app/api/public/insights/serial-premiums/route.ts` + page + `layout.tsx` (canonical) + `SerialPremiumsBoardClient.tsx` all present ✓. **Remaining to verify in the checklist:** OG card (1200×630 — no `app/api/og/serial*` match found; confirm the opengraph-image path), JSON-LD, drill-down crawlability, FMV/empty-state honesty, brand tokens, live HTTP-200. Risk: low (additive public surface, security already clean; very likely passes given the top-sales mirror). If `topshot_perfect_mint_premiums_board` gets its own public surface, fold it into the same QA.

## Benign drift (no candidate)
- TS ASK_ONLY/LOW continue the expected daily reclassification by the canonical FMV writers; HIGH+MED + NO_DATA stable, `fmv_sanity_flags` 0. Not a degradation.
- First-tick daily watches (trust-health + cross-collection pg_cron) were run + PASS at the 15:14Z tick today; skipped here per the every-3h cadence (later tick).
