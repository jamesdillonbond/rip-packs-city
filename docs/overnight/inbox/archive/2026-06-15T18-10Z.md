# Daytime monitor — 2026-06-15 ~18:10Z (11:10 PDT, midday tick)

PLATFORM GREEN. Read-only sweep + artifact validation all clean. No NEW night-pass-shippable candidates. One fresh Trevor ship landed since the 15:10Z first-tick (`52b986c`, the predicted badge fallback) — logged below as post-ship-watch. Lock RELEASED (night pass, 08:23Z). First-tick-of-day extras (trust-health daily + cross-collection) skipped — already ran at 15:10Z; trust health re-confirmed read-only below.

## Health snapshot (measured this tick, 18:06–18:10Z)
- pipeline fails 6h: **1 transient** — `analytics-smoke` @16:13Z, leg `rpc_fmv_tier_pulse_collections` severity=fail (138ms, telegram fired). Self-recovered next tick (16:43 / 17:13 / 17:43Z all ok=true). Known transient class (the :13/:43 heavy-RPC collision under ANALYTICS-SMOKE-RESIDUAL); the persistent `freshness_fmv_per_collection: warn` on every tick is the known baseline warn (never trips ok=false). Not a regression — no action.
- detect_stalled_pipelines() **[]** · get_pipeline_alerts() **[]** — N1 `snapshot-institutional-wallets` stays recovered (re-fired 10:07Z this morning; no longer alerting).
- security **0/0** (RLS-off base tables none; anon/auth write on RLS-off base tables none, relkind r/p).
- trust health **8/8 ok** (read-only re-confirm): edition_integrity 4/50, fmv_sanity 0/1, offer_edition_gap 0/50, pack_ev_board_stale 0.67d/2, pack_ev_depleted 0/30, pinnacle_ask 0.0h/3, pinnacle_fmv 8.0h/30, ts_uuid_dupes_24h 0/200.
- sentinel TS-UUID-keyed-48h **0**; `badge_editions` **0** non-int-pair (Q8 grain guard `badge_editions_block_topshot_uuid_key_trg` holds).
- Sentry **0 unresolved / 0 new in 24h**.
- Vercel **0 ERROR** across 20 recent deploys; prod HEAD `52b986c` READY (was `b8837bc` at the morning tick). CANCELED entries = docs-only build-skips / monitor inbox commits, by design.
- FMV writers fresh (fmv_snapshots 18:09Z / pack_ev 18:07Z / offers-sweep 18:07Z). Pinnacle per-render **728/1845** (this tick). TS hi+med 3373 (last measured 15:10Z; +recompute drift, writers current).
- Artifacts: all **12 active validated** by running their backing objects — rpc-live-health 12 insights boards all return rows (squeeze 501 / set_squeeze 239 / pack_reality_top_ev 9 [thin by design, C2] / rookies 61 / market 501 / first_mint 501 / cross_collection 1 [summary view] / pinnacle_scarcity 501 / offer_spread 501 / deals 501 / trophies 501 / top_sales 501); rpc-tracked-fmv-confidence (v_tracked_wallet_fmv_confidence 21, no timeout); rpc-offers-intelligence (v_offer_sanity_flags 203, edition-grain gap 0); rpc-rewards-console (v_rewards_economy 1); rpc-cross-collection (cohort_mat 158, fresh 04:10Z = pg_cron step1 ran today). Other 7 active read stable tables with no backing schema change since the 15:10Z green pass. 5 RETIRED tombstones skipped. None broken, none repaired.

## Post-ship watch — fresh Trevor ship since the 15:10Z tick (READY, clean)
- **BADGE-BACKFILL-WATCH (cont.) · `52b986c` (prod HEAD, READY, ~17:37Z) · [LOW · post-ship watch].** The predicted follow-up to this morning's `0df701c`/`b8837bc`: a `getMintedMoment(moment_id)` fallback (extends `/api/admin/backfill-badges-from-sets`) closing the set-sweep gap — ran live 45/79 seeded, 0 GQL failures; unbadged-canonical-TS dropped **199 -> 34**, Ultimate **115 -> 1** (KD Supernova / A'ja Wilson 122:4314 / Wembanyama 152:5370 verified via get_edition_badges_unified). The 34 residual have no moment_id anywhere (truly unindexed — Trevor's tracking-only, NOT night-pass-shippable). Post-ship health NOW: `badge_editions` **10,893 rows / 0 non-int-pair / 0 dupes** (grain guard holds; trust ts_uuid_dupes_24h 0); `topshot-badge-catalog` ok=true every tick through 16:13Z; 0 ERROR deploys; 0 Sentry. Night pass: **post-ship-watch only** — confirm `topshot-badge-catalog` stays ok=true + badge grain stays 100% int-pair. No code/revert action. Revert if ever needed: `git revert 52b986c`.

No new actionable/shippable candidates — platform clean. Carried unchanged: N1 (operator, recovered), ALLDAY-V1-UNMAPPED-DRIFT (operator recover-v1 cron, draining), VERCEL cost family (Trevor dashboard), A1-WORKER-PASSTHROUGH-CLEANUP (Trevor wrangler), TFP-SLOT-WAVE-COLLISION + TFP-480-RESTORE (both closed 06-14). A1 owner-coverage: do NOT re-explore (disproven, dead end).
