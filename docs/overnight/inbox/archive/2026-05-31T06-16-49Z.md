# Daytime monitor — candidate inbox · 2026-05-31T06:17Z

Run: `rpc-daytime-monitor` (read-only sweep). HEAD `26fa6be` on `main`. **Production is serving the last READY deploy `dpl_FXGa5A87aPECwDDFsA4V3X2huESP` (commit `959ec85`, docs-only on top of entity-pages `29d2e46`)** — the two newest commits both failed to deploy (see C1).
Lock present but STALE (`docs/overnight/.lock` = `2026-05-30T22:14:02Z` runid=8a4b884c, ~8h old, ≫45m) → treated as a leftover from a finished run; did not touch it (read-only posture). Prior-run's `.git/index.lock` blocker (C4) is **RESOLVED** — gone, and HEAD has advanced past it.

Trevor is **actively committing right now** (b20e483 @06:00Z → 26fa6be @06:08Z), so he may already be mid-fix — but the second deploy also erroring (below) is signal he may not have absorbed yet.

Platform is otherwise healthy: security clean (0 base-table RLS-off, 0 base-table anon/authenticated write grants; `check_public_security_invariants()` = [] and `check_secdef_anon_execute_violations()` = []; the 3 `topshot_pack_reality_*` views are now `security_invoker=on` — ledger Q1 landed clean). FMV flat vs 22:25 baseline (TS HIGH+MED **779** [202+577] vs 780, AllDay **243** [53+190] vs 241). unmapped_sales unchanged (144 open / 138 within-7d). Sentinel TS-UUID-keyed-48h = **1175**, DOWN from 1707 baseline / 1596 last run (WARN band <2000, trending right). All 11 Cowork artifacts validated OK (backing data present + fresh). All pipeline failures are low-single-digit transient (pool/timeout/deadlock/Base-429) with recovery.

2 candidates below.

---

## C1 · [HIGH · reproducible prod deploy failure] `b20e483` + its retrigger `26fa6be` BOTH errored post-compile — funnel+SEO fix is NOT live

- **Title:** Two consecutive production deploys (the funnel+SEO fix and its empty retrigger) errored post-compile; prod is stuck one code-commit behind on `959ec85`, so the wallet-paste activation-leak fix + the sitemap entity-URL fix are not in production.
- **Source:** Vercel deploys `dpl_98M1r2RGCLLn5yF15pF1WrVM8Snd` (b20e483, created 06:01Z, **ERROR**) and `dpl_GsR6VGPj5a34QcihhSD2eFbyn95n` (26fa6be "chore: retrigger deploy … transient", created 06:08Z, **ERROR**). Build log for 98M1: `✓ Compiled successfully in 9.2s`, then the log tails out on the Sentry sourcemap upload with **no error text** before the ERROR transition.
- **Evidence / why it's not transient:** the retrigger commit `26fa6be` is an **empty no-op over the same tree** and errored **identically** — so the "transient post-compile" hypothesis in its own commit message is disproven. The parent `959ec85` deployed READY, so the failure is introduced by the `b20e483` diff. The "compiled-successfully → silent post-compile ERROR with no logged error" pattern is the documented RPC signature for either (a) a route `maxDuration` > the 800s Pro hard cap, or (b) a build-time/static-generation failure — see CLAUDE.md "Vercel tool behavior" (the maxDuration=900 invisible-failure incident, commit 32de87a). `b20e483` touches `app/sitemap.ts` (now enumerates ~20K entity+pack URLs — prime suspect if it runs a DB query at generation time that fails/exceeds limits), `proxy.ts`, `components/HomePageMarketing*`, `app/share/[wallet]`.
- **Risk read:** No user-facing outage — prod (`959ec85`) is healthy and unaffected. Harm is that two pre-traction-critical fixes (the homepage wallet-paste funnel leak + the empty-sitemap SEO blocker, both from the 2026-05-31 "what's next" handoffs) are blocked from shipping, and a 3rd plain re-deploy will waste another cycle since the cause is reproducible in the tree.
- **Suggested action (operator / Claude Code — NOT the night pass):** Do **not** just re-deploy again (already failed twice). Pull the **full** build log for `dpl_98M1…` past line 635 (`get_deployment_build_logs … limit 200+`) to see the post-"Compiled successfully" phase, and inspect the `b20e483` diff: (1) `app/sitemap.ts` — does the new entity/pack enumeration run a Supabase query at module/generation time that can throw or blow the static-generation budget? (2) any `export const maxDuration` > 800 on a route in the diff. Likely fix is bounding/guarding the sitemap generation (try/catch + cap) or correcting a maxDuration. **Off-limits for the night pass:** `proxy.ts` is auth/lockdown and the funnel/sitemap changes are operator/CC-owned per the ledger — night pass should surface this, not ship a fix.

## C2 · [LOW · transient, cross-ref prior-inbox C2] Two NEW smoke-test security Sentry issues fired once at the failed-deploy window — invariants verified clean

- **Title:** `JAVASCRIPT-NEXTJS-1C` ("smoke test failed: public base tables: RLS on + no anon write") and `JAVASCRIPT-NEXTJS-1D` ("smoke test failed: anon has no EXECUTE on destructive SECDEF functions") each fired 1 event ~06:02Z (first-ever for both groups), coinciding with the b20e483 deploy window.
- **Source:** Sentry (firstSeen ~06:02Z, 1 event / 1 user each, culprit `POST /api/smoke-test`).
- **Evidence it's transient, not a real regression:** I ran both backing guards directly — `check_public_security_invariants()` → `[]` and `check_secdef_anon_execute_violations()` → `[]` (0/0, invariants hold right now). The DB showed elevated `Timed out acquiring connection from connection pool` transients across many pipelines in the same 06:00–06:08 window, so the smoke test's security RPCs most plausibly hit a pool timeout and reported failure (or ran against the in-flight failed deploy). Same class as prior-inbox **C2** (smoke assertions flapping on transients/thresholds).
- **Risk read:** LOW. No real security exposure (catalog + guard RPCs clean). Harm is alert-noise that can mask a genuine smoke failure.
- **Suggested action:** Confirm non-recurrence on the next few smoke runs. If these specific assertions keep flapping, give the smoke-test's security RPC calls a short retry/timeout guard so a transient pool timeout doesn't read as an invariant violation. Do **not** treat as a security regression unless the direct guard RPCs return rows.

## C3 · [INFRA · clear before any commit] Fresh `.git/index.lock` + `.git/HEAD.lock` left by THIS monitor's failed push

- **Title:** This run's `git pull --rebase --autostash` could not autostash the phantom-CRLF dirty tree and left two 0-byte locks (`.git/index.lock` @06:19Z, `.git/HEAD.lock` @06:18Z); the sandbox cannot remove them (`Operation not permitted` on the `.git` mount).
- **Source:** this monitor run — commit `a99ce2f` (the inbox file) was created locally but **push failed** (`could not read Username for 'https://github.com'` — sandbox has no GitHub creds), and the follow-on rebase aborted on `fatal: Cannot autostash`, leaving the locks.
- **Risk read:** LOW-but-blocking. `git status`/reads still work; `git add`/`commit` will fail with "Unable to create '.git/index.lock': File exists". If this mount shares Trevor's real `.git`, it will block the night pass's 1am commit (same failure mode as prior-inbox C4). NOTE: Trevor committed `26fa6be` at 06:08Z fine and my locks postdate that (06:18–06:19Z), so his next commit is the first that could hit it.
- **Suggested action:** `rm -f .git/index.lock .git/HEAD.lock` before any git write this cycle (night pass: do this first, exactly as it would for C4). This inbox file is committed locally (`a99ce2f`) but **unpushed** — it persists on disk so it's picked up locally regardless; no need to re-commit it.

---

### Validated this run (no action)
- **Security:** 0 public base tables with RLS off; 0 base-table anon/authenticated write grants (`relkind IN (r,p)`, by-design SECDEF/insights views excluded). `check_public_security_invariants()` = [], `check_secdef_anon_execute_violations()` = []. Ledger **Q1** confirmed landed: `topshot_pack_reality_{top_ev,stats,dist}` all `security_invoker=on`.
- **FMV (latest-per-edition):** TS HIGH 202 / MED 577 (HIGH+MED 779), AllDay HIGH 53 / MED 190 (HIGH+MED 243). Flat-to-slightly-up vs baseline. TS NO_DATA 6055 (↓ from 6091). FMV latest write 06:07Z (fresh); Pack-EV latest 05:38Z (fresh).
- **Pipelines:** every 24h failure is low-single-digit transient with recovery — `evm-transfers-ingest` Base-429 5/21 (prior-inbox **C3**, known), `compute-topshot-pack-ev` 6/92 (statement timeout on `targets`), plus 1-off pool timeouts/deadlocks on golazos-sales / pinnacle-listings-retry / pinnacle-nft-resolver / compute-allday-pack-ev / wmc-fmv-populate / snapshot-institutional-wallets. No pipeline silent past cadence (the days-old ones — migrate-wmc-edition-keys, topshot-listings-indexer, league-drift-detection, pack_dist_page_crash — are retired/expected). Note: `topshot-fmv-populate` last ran ~12.7h ago (2 runs/24h) but FMV HIGH+MED is flat/up, so no impact — not raised.
- **Artifacts (11/11 OK):** rpc-insights-health 7/7 backing views non-empty (squeeze 2633, set-squeeze 52, pack-reality-top-ev 101, rookies 61, first-mint 453, cohort-stats 1 [by-design rollup], pinnacle-scarcity 103) + FMV/Pack-EV fresh; rpc-cross-collection (cohort_mat 143, set_overlap_mat 240, stats 1); rpc-traction (support_conversations 2302, email_subscribers 0 [pre-traction, expected], outbound_clicks 25); rpc-pipeline-reliability / rpc-live-health / rpc-fmv-watch / rpc-security-drift / rpc-deploys-and-cost all covered by the direct sweep above; rpc-my-wallet + rpc-trophy-ladder read stable wallet/catalog data unchanged today (not re-run).
- **Already-tracked, NOT re-logged:** prior-inbox C1 (pinnacle `JAVASCRIPT-NEXTJS-15` post-fix noise), C2 (smoke sales-lag `degraded` flap), C3 (evm Base 429); ledger Q1 (resolved this run, noted above), Q2 (compute-laliga-pack-ev cadence — ran 2×/24h, 0 fails, benign). Prior-inbox C4 (the ~19h orphan `.git/index.lock`) was cleared before this run (HEAD advanced past it) — but this run regenerated a fresh pair of locks; see **C3** above.
