# Daytime monitor candidate — 2026-07-01T03:44Z (2026-06-30 ~20:38 PDT tick)

Run: rpc-daytime-monitor. No clock skew (shell 03:37Z == DB now() 03:38:54Z == app-stamped sales.ingested_at 03:36Z / fmv.computed_at 03:38Z, all within ~2 min; app rows can't be future-stamped). Late-evening tick, not the ~8am first-tick, so Section 1a daily extras skipped (trust health still covered via rpc_ops_snapshot).

Health GREEN: security 0/0/0/0; trust 15/15 ok after the sole breach self-cleared mid-run (see NON-FINDING below); detect_stalled_pipelines() []; check_pgcron_recent_failures() []; sentinel TS-UUID-48h 17 (inert DQ4, < warn 250); editions FLAT (TS 17,489 / AllDay 6,191 / Golazos 581 / UFC 518); FMV TS H+M 4,678 / AllDay 900 (flat vs 06-30 4,685/908); fmv_sanity 0; unmapped_resolution_backlog_max 29 (well under 100); DB 6,977 MB (-118 vs 06-30's 7,095 = overnight prune/vacuum, benign). Vercel prod e46249eb READY, 0 ERROR deploys in 24h (recent CANCELED = the docs-only ignoreCommand as designed). Sentry: connector NOT loaded this run — used Vercel get_runtime_errors as the frontend-error proxy: only the known DBSAT/statement-timeout cost family, no new class; the 06-30 ~22:06-22:11Z edition connection-pool spike has ENDED (last occurrence 22:11Z, on the now-superseded dpl_8Gu4). Artifacts: 15 in manifest; OneDrive artifact HTML is not reachable from the sandbox this run, so validated their backing DB objects (snapshot/trust/offer/pipeline/FMV/pack-EV views all resolve sane) rather than per-artifact payload extraction.

Post-ship watch on the 06-30 evening badge-art/perf wave — PASS. Current prod e46249eb ("perf(edition): drop dead special_serial_holders query") is itself a DBSAT win — it removes one of the "[edition] special_serials — Timed out acquiring connection from connection pool" victims. Badge-art commits (0944535/e8e52f4/DRgVAEB/0c70791/kyGse4L/3ce52b0/63929d1) are presentational; editions FLAT + FMV flat = no data regression attributable to any of them.

## NON-FINDING (investigated, self-cleared) — offer_edition_gap_max_usd
Read 750 BREACH at 03:38Z (driver: Yao Ming - Heroes of the Game 211:7532, flag gql_blank_chain_has, a single fresh $750 on-chain edition-level offer with edition_offers still blank), then self-cleared to 0/ok by 03:44Z when the healthy ~20-min offers-sweep (every tick ok, last 03:22Z writing rows, next ~03:42Z) ratcheted edition_offers up to match. EXACTLY the documented transient (ledger OFFER-SANITY-VIEW-REFINEMENT: "a transient offer_edition_gap_max_usd breach that self-clears while offers-sweep is healthy is EXPECTED and should NOT be treated as an incident"). The durable gql_blank_chain_has edition-level raise is already the queued/gated OFFER-SANITY-RAISE item. NOT queued.

## Candidate (1, LOW-MED) — sharpens the 2026-07-01T00:14Z inbox candidate to night-count 2

- **CLASSIFY-ACQ-ALLDAY-STATEMENT-TIMEOUT** (LOW-MED; night-count 2; DB-fn/route — CC/operator, off-limits to the monitor)
  - Source: pipeline_runs — classify-acquisitions-multicollection (hourly at :06) failed 3 CONSECUTIVE ticks 2026-07-01 00:06 / 01:06 / 02:06Z, all `nfl_all_day: canceling statement due to statement timeout` at duration ~95s (the 06-30 22:06Z one 185s); 4 fails/24h; latest 03:06Z RECOVERED ok (68.7s). Corroborated in Vercel runtime errors (300s-timeout family on /api/cron/classify-acquisitions-multicollection, last 01:27Z).
  - What changed vs the 00:14Z candidate: that one (night-count 1) posed the open question "dense micro-cluster contention vs AllDay volume-creep?" Today's pattern answers it — the fails are the hourly tick's OWN nfl_all_day leg hitting its ~95s statement_timeout across a 3-hour consecutive stretch, NOT a lost pool-race inside a co-fire cluster. Consistent with AllDay `sales` growth from the studio-history backfill (~124k+ rows) pushing the AllDay classify leg over its ceiling. It flaps (recovers when contention eases) rather than stalling — detect_stalled [] ; enrichment only, no FMV/deal-board/user-facing impact.
  - Suggested action (night pass / CC): bump the classify fn's statement_timeout for the nfl_all_day leg, OR scope/index that leg (window the AllDay classify candidate set or add a supporting index) so it fits under the cap. Re-measure over the next 24h; if it stops recurring on its own (contention-only), downgrade/close.

Everything else carried/known (VERCEL cost family, THIN-FMV-GUARD-CONTENTION, ALLDAY-V1-UNMAPPED-DRIFT, SERIAL-FMV-POWER-MODEL-WEEKLY resurfaces ~07-05, WEEKLY-SURFACE-QA-PROSE, etc.) — no new candidates.
