# Daytime monitor inbox — 2026-06-21 ~06:06Z (2026-06-20 23:06 PDT tick)

Platform **GREEN**. Last monitor tick before tonight's ~08:02Z night pass. Quiet end-of-day post-ship-watch over today's heavy Stage B / Phase 3 / Phase 4 CC wave. **No new incident, 0 net-new candidates.** Everything below is post-ship-watch confirmation + carried-item status (re-measured fresh at 06:06Z) — nothing for the night pass to ship; both open items already queued and operator-gated.

security 0/0/0/0 (RLS-off base [], anon/auth-write-on-RLS-off base [], `check_public_security_invariants()` [], `check_secdef_anon_execute_violations()` []); `detect_stalled_pipelines()` []; `get_pipeline_alerts()` = 1 INFO only (`golazos_sales` resolving_editions, 1/24h — long-standing, not a finding); **trust-health 9/9 ok**; sentinel `ts_uuid_dupes_created_24h` 22/200 (ok); Sentry **1 unresolved** (same transient as the last two ticks, Note A); Vercel **0 ERROR**, prod `592e725` READY (the two monitor inbox commits above it — `37bb2e4`/`d119f4b` — correctly CANCELED). DB 5019 MB (+6 vs 5,013 last tick — benign).

**Editions reconcile EXACTLY** (TS 16,933 = 15,543 baseline + 1,390 `::` parallels; AllDay 6,191 / Golazos 581 / UFC 446 — flat). **FMV reconciles EXACTLY to those counts:** TS HIGH 1,194 + MED 3,048 = **HIGH+MED 4,242** (LOW 6,640 / NO_DATA 3,363 / ASK_ONLY 1,804 / STALE 865 / SALES_ONLY 19 = 16,933); AllDay HIGH 246 + MED 629 = **875** (LOW 2,923 / NO_DATA 2,310 / STALE 63 / ASK_ONLY 20 = 6,191). `fmv-recalc` last 06:08Z (fresh), **94 ok / 0 fail / 24h**, `fmv_sanity_flags` 0. (TS HIGH+MED 4,098→4,242 / AllDay 805→875 since the 03:06Z tick = the benign intra-day reclassification cycle, still well above the prior-week overnight range. Known FMV-HIGHMED-DIP class, no action.)

---

## POST-SHIP WATCH — today's heavy CC wave still healthy ~10h after the last commit; 0 reverts warranted

- **Phase-4 conflation-leak fix (`592e725`, subedition-aware history-backfill + durable remap) is HOLDING.** `topshot_conflated_editions` guard = **27 rows** (the converged Phase-4 state, `remaining_remappable_strands`=0) — NOT drifting up. `topshot-sales-history-backfill` **11 ok / 0 fail / 24h**, last 05:22Z.
- **`topshot-buyer-backfill`** **47 ok / 0 fail / 24h**, last 05:34Z, **max 608s** (BUYERBF-PERINVOCATION-WORK holding — under the night baseline 740s, well under the 800s Pro cap; no creep).
- **Phase 3a circulation backfill** (`topshot-subedition-circulation-backfill`, daily Vercel cron 21:10 UTC) **3 ok / 0 fail**, last 22:05Z.
- **`allday-listing-serial-backfill`** (on-chain serial source, watchlisted last night) **8 ok / 0 fail / 24h**, last 03:34Z (next ~06:34Z).
- **Spine indexers fresh:** `topshot-sales-indexer` 72/0 (06:03Z), `allday-sales-indexer` 72/0 (05:56Z), `offers-sweep` 72/0 (06:02Z, max 45.7s).
- **All 11 artifact backing objects return rows** (consolidated sweep, no schema break from today's Stage B / Phase 3 add-column migrations): deals_board 696, serial_premiums 180, perfect_mint 11, trophies 683, top_sales 892, tracked_fmv 21, offer_sanity 243, underpriced_serials 12, allday_floor 3,915, special_serial_mv 6,774, squeeze 500+. Artifact estate = 16 (none `(RETIRED)`).

---

## CARRIED — status unchanged, already queued, operator-gated (no new night-pass action)

- **REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT** (queued, night-count 2). **STILL no post-fix automated `refresh-special-serial-owners-mv` cron run** — last `pipeline_runs` entry is **06-20 02:16Z** (pre-fix, ~28h ago), 0 runs in the trailing 26h, and the pipeline is **not on `pipeline_cadence_watchlist`** (silence invisible to `detect_stalled`). CC's fix is deployed (`3613a94` route maxDuration 120→240 + `audit_20260620_special_serial_owners_mv_refresh_timeout_200s` fn 180→200s) and the **MV data is current (6,774 rows)**, so the board serves correctly — LOW blast radius. Night-pass watchlisting gate (one post-fix ok tick) **still not met**. Keep watching for the first post-fix automated fire; only then add the watchlist row.
- **`refresh-conflated-editions` cron — operator residual UNCHANGED.** Safe-DELETE fix is live (migration `20260620213921`, `has_where=true`), but the daily cron-job.org entry is still not wired: only route fire ever was the **06-20 15:17Z** pre-fix failure (ok=false), 1 run in 26h, none since. CLAUDE.md already lists "wire the daily refresh-conflated-editions cron" as an operator item; the follow-on (watchlist it after one ok run) stays premature until the cron exists. The guard sits at the converged 27 meanwhile (forward-keying + route-wired remap converge it). No night-pass action.

---

## NOTE — knowns / no action

- **Sentry `JAVASCRIPT-NEXTJS-A`** ("smoke test failed: fmv pipeline healthy / analytics_pipeline_health", `POST /api/smoke-test`) — **same single event ~21:11Z** that the 00:14Z + 03:06Z ticks already logged (1 event, first=last seen 9h ago, Seer super_low, 0 new since). FMV independently verified fresh (fmv-recalc 06:08Z, 0 fails/24h, `fmv_sanity_flags` 0, editions reconcile). Recurring smoke false-alarm class; resolve after 24h quiet.
- **pipeline_runs fails (6h):** `evm-transfers-ingest` ×3 (Base 429 — benign STEER item, don't chase, last 05:19Z) + `alerts-dispatch` ×1 @**01:29Z** ("deal: canceling statement due to statement timeout") — the **same isolated 01:29Z transient the 03:06Z tick logged** (1/96 over 24h, known ALERTS-DISPATCH-DEAL-TIMEOUT class closed 06-19, not recurred since). Nothing else.
- **`ts_uuid_dupes_created_24h` = 22/200 (ok):** known inert DQ4 dupe-writer leak (TS-WMC-UUID-FOSSILS/DUPE1); editions net-grew only by `::` parallels, not UUID dupes. No action.
- **First-tick-of-day extras (Section 1a) skipped** — this is the ~11pm PDT last tick, not the first of the day. Trust-health was still run (9/9 ok); cross-collection pg_cron refresh verify deferred to the next first-tick (~8am).
- **STEER reminders honored:** SERIAL-FMV-MULT-CRON weekly pg_cron (jobs 5+6, Sun 11:00 UTC, next 06-21) is BY DESIGN — not flagged; evm-429 not chased.
