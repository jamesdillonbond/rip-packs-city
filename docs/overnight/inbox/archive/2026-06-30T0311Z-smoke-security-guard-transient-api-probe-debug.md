# rpc-daytime-monitor — 2026-06-30T03:11Z (evening tick, ~20:11 PDT, no clock skew)

Run context: shell 03:06Z == DB now() 03:07Z == app-stamped fmv-recalc write 03:10Z (no skew). Later tick (not the ~8am first-tick), so the 1a daily extras (cross-collection pg_cron verify + trust-health-as-daily-watch) were skipped; trust health still read via rpc_ops_snapshot (15/15 ok). Lock RELEASED (overnight pass ~01:15 PDT, ~19h stale) — inbox commit permitted. Push available (clone HEAD 60628758).

Platform GREEN: security 0/0/0/0 (invariants/anon-write/rls-off/secdef-anon all []; re-confirmed check_public_security_invariants() [] + 0 rls-off base tables) · trust 15/15 ok (breaches []) · detect_stalled [] · pipeline_alerts 1 INFO (ufc_sales resolving_editions, benign) · check_pgcron_recent_failures [] (the SERIAL-FMV-POWER-MODEL-WEEKLY-TIMEOUT jobid-6 fail stays aged-out of the window; resurfaces ~07-05, already queued — do not re-log) · sentinel TS-UUID-48h 17 (known inert DQ4 leak, 17 << WARN 250; ts_uuid_dupes_24h 17/200 ok; not escalating) · editions real-flat (TS 17,489 / AllDay 6,191 / Golazos 581 / UFC 518; TS == 00:13Z exactly) · DB 6,644 MB (+51 since 00:13Z, benign backfill wave) · Vercel prod 105c9e9c (dpl_4zeGFGnWcQfaAyNDnfLzvcwTkboY) READY, 0 ERROR (all CANCELED deploys = docs-only ignoreCommand, expected).

FMV note (NOT a regression): rpc_ops_snapshot fmv_by_collection reads TS HIGH+MEDIUM 4,252, but the DIRECT latest-per-edition truth is HIGH 1,318 + MEDIUM 3,366 = 4,684 — stable/slightly up vs the ~4,671 both earlier ticks reported today. The snapshot's coverage accounting just runs conservative; fmv-recalc is healthy (every ~20 min ok=true, 1,184–2,262 rows/run), topshot_fmv_stale_hours 0.3/6, fmv_sanity 0. AllDay H+M 790. (Flagging so a future tick doesn't read the snapshot's 4,252 as a drop — cross-check direct when in doubt.)

pipeline_fails_24h: 13 pipelines, but EVERY one's latest run is ok=true (verified per-pipeline, all within the last ~45 min) — known transient connection-pool-saturation during the backfill wave, 0 genuine stalls.

Recent-ship glance — since the 00:13Z tick (HEAD bced12eb → 60628758): the only non-docs production ship is 105c9e9c "feat(pinnacle): intraday render-floor refresh via floors_only Vercel cron" (READY; a cadence-only change — same Studio-GQL source + pinnacle_catalog_set_floor_asks RPC, NOT a pricing/logic change; new Vercel cron 45 1,7,13,19 UTC, first tick 07:45Z by design). pinnacle_render_floor_stale_hours 17.5/30 ok (maintained by the daily ~09:37 sweep; the intraday cron will tighten it — post-ship-watch the 07:45Z first proof on the next morning tick). Everything else (60628758, 1a0203a6, 794caf9f, faa5c508, 52fd579c, e720c2e7) is docs/handoffs → CANCELED deploys, expected. No fix landed for the two queued AllDay EV dist-page timeouts.

Artifacts: 13 active in manifest, no RETIRED-tombstone false positives, none broken. No schema change since the 00:13Z validation (only a cadence cron + docs), so prior validation holds; re-validated the day's hottest surface live this run — rpc-pack-lifecycle backing views all resolve (v_topshot_pack_lifecycle 1,991 / v_topshot_pack_realized_ev 201 / v_allday_pack_lifecycle 3,052 / v_allday_pack_realized_ev 45 (honest partial, filling) / v_allday_edition_pull_provenance 365 / pack_rips valued 188,370 / cross_collection_deals_board 834). Core live-health legs confirmed via snapshot (trust 15/15).

Sentry: connector returned "connection invalidated — needs reconnect" for a 3rd consecutive tick (also down 21:13Z + 00:13Z), so the Sentry leg could not run. Substituted Vercel get_runtime_errors for crash visibility — no NEW crash classes; all entries are known families (url.parse DEP0169 deprecation 284, wmc_wallet_moment_unique_idx dup-key backfill race 24+10+2, heavy-query statement-timeouts on /pack/dist + /edition + /insights/first-mint + /team, the two queued AllDay EV timeouts, and the api_probe_debug smoke transient below). OPERATOR: reconnect the Sentry connector when convenient (now 3 ticks blind on Sentry).

Queued-and-still-active (NOT re-logged): both AllDay EV dist-page timeouts remain live on current prod — allday_corrected_ev (ALLDAY-CORRECTED-EV-DIST-PAGE-TIMEOUT, 21:13Z inbox) last hit 02:59Z (count 34/6h), and allday_pack_realized_ev (ALLDAY-PACK-REALIZED-EV-DIST-PAGE-TIMEOUT, 00:13Z inbox) grown 2→11 hits as the 00:13Z note predicted. Both are the same one-page query-budget pass for the night pass/CC; no new action needed from this run.

---

## NEW candidate (1)

### SMOKE-SECURITY-GUARD-TRANSIENT-API-PROBE-DEBUG (LOW — folds into ANALYTICS-SMOKE-RESIDUAL; night-pass/CC; cry-wolf on the security smoke leg)
- Source: Vercel runtime errors — `SMOKE-TEST HARD FAILURES: [ { "endpoint": "rpc:check_public_security_invariants", "error": "1 violation(s): rls_off_base_table:api_probe_debug", "status": null } ]` (route app/api/smoke-test/route.ts), count 2 in 6h, first-seen 2026-04-19, last 2026-06-30T01:54:59Z, lastDeployment dpl_4zeGFGnWcQfaAyNDnfLzvcwTkboY.
- Verified NOT a live security hole (read-only, this run ~03:08Z): the table `public.api_probe_debug` does NOT currently exist; `check_public_security_invariants()` = [] and `SELECT count(*) FROM pg_tables WHERE rowsecurity=false` = 0. So the violation was transient — a scratch/probe table that briefly existed without RLS around 01:54Z and was gone by the next sweep. snapshot security 0/0/0/0 at 03:07Z confirms clean.
- What it is: the route-level smoke test deliberately keeps the two security guards (`check_public_security_invariants` / `check_secdef_anon_execute_violations`) HARD (per ledger Item 6, audit_20260622 — "a guard RPC error must page"), while analytics_smoke_run()'s RLS leg was softened for exactly this transient-scratch-table class (ledger Item D, audit_20260622_analytics_smoke_transient_falsepos_to_warn — momentary RLS-off audit_*/dedup tables created mid-migration). So when an ephemeral RLS-off table appears mid-sweep, the ROUTE smoke test hard-fails (→ Sentry/Telegram page) where analytics_smoke now warns. `api_probe_debug` is one such ephemeral table — and it has NO source anywhere in the repo (grep of .ts/.tsx/.sql/.mjs = 0 matches), so it is created out-of-repo (an ad-hoc probe / external session / migration scratch), not by committed code.
- Risk read: LOW. No data/security exposure (verified clean); the smoke endpoint still functions. Cost is recurring false hard-fails on the most important (security) smoke leg — the same cry-wolf class the analytics-smoke softening already neutralized on the other path. Not new today (2+ months) but never named by exact table in the ledger; the carried ANALYTICS-SMOKE-RESIDUAL covers the general class, not this specific route-smoke security-guard manifestation.
- Suggested action (night-pass/CC — NOT a monitor fix; read-only here): (a) identify what creates `api_probe_debug` without RLS (no repo source → likely an edge-fn/probe/ad-hoc migration; if a known probe, have it create the table WITH RLS, in a non-public schema, or drop it in the same txn); OR (b) mirror the analytics_smoke softening on the route smoke test — exclude known-ephemeral scratch-table names (api_probe_debug, audit_*, dedup temps) from the HARD `check_public_security_invariants` leg so a transient out-of-band table can't page the security guard, while a persistent product-table RLS hole still pages. Fold into ANALYTICS-SMOKE-RESIDUAL. Verify: the "SMOKE-TEST HARD FAILURES … rls_off_base_table" class goes quiet while a genuine RLS-off product table still hard-fails.

---

No other new candidates. Carried/known left untouched (SERIAL-FMV-POWER-MODEL-WEEKLY-TIMEOUT queued, resurfaces ~07-05; ALLDAY-CORRECTED-EV + ALLDAY-PACK-REALIZED-EV dist-page timeouts both queued today; WEEKLY-SURFACE-QA-PROSE; ALLDAY-V1-UNMAPPED-DRIFT; THIN-FMV-GUARD-CONTENTION; HISTORY-BACKFILL-UNMAPPED drain; TS-WMC-UUID-FOSSILS; PIN-FMV-REKEY-WAVES; etc. — see ledger). STEER honored: did not re-flag the DQ4 inert UUID leak (17/200, owned), the AllDay pack-mechanics crons / pack-OPEN ingestion (intentional per ledger), studio-backfill activity, alerts-dispatch/send activity, evm-429, or the SERIAL-FMV weekly cadence. New Pinnacle floors_only cron (105c9e9c) recorded as post-ship-watch (first tick 07:45Z), not a candidate. Declined items not re-raised.
