# Daytime monitor — 2026-06-09T21:06Z

Run: `rpc-daytime-monitor` (read-only). ~3h after the 18:06Z daytime run, ~12.6h after the 08:30Z night-pass baseline. Took over the STALE released `.lock` (08:42Z marker, >45min old — not active). Platform **GREEN except the carried Pinnacle ASK breach, which kept climbing all day** + the carried external-cron drift. Security **0/0** (all four checks). Sentinel TS-UUID-48h **0**. 20/20 deploys READY, 0 ERROR. All 6 spot-checked artifact backing views resolve. **No NET-NEW candidates this run — everything below is a carried/queued item; this is an end-of-day trend + continuity handoff for tonight's pass.**

## 🔴 LEAD — PINNACLE-RECONCILE-TIMEOUT: full-day worsening curve, ASK now 16.5h stale (carried/queued · CC/operator · Pinnacle pricing path off-limits to auto-ship)
- `v_rpc_trust_health.pinnacle_ask_stale_hours` = **16.5h** (breach_at 3). Full-day curve, monotonically climbing: **3.8h @08:30Z → 10.5h @15:06Z → 13.5h @18:06Z → 16.5h @21:06Z**. Still the ONLY breach of the 6 trust-health metrics (edition_integrity 4/50, fmv_sanity 0, pack_ev_max_stale 1.3/2, pct_depleted 0, ts_uuid_dupes_24h 0 all healthy).
- Freshest `ask_source='pinnacle_direct'` ask still frozen at the last successful reconcile **04:39Z** (~16.5h). `pinnacle-listings-reconcile`: `last_ok` 04:39Z, **last run of ANY kind 11:24Z** (now 583m / ~9.7h silent vs 60m threshold — in `detect_stalled_pipelines()`), 57 runs / **27 fails** in 24h. So it both (a) timed out ~half its ticks on the ~8s service_role cap when it ran, and (b) cron-job.org has dropped it entirely since 11:24Z (CRON-DROP-WAVE compounding).
- **Self-healing is now falsified across the WHOLE day:** DB saturation cleared long ago (last hour 221 runs / **0 fails**; 0 fails 14–15Z, 16–18Z, 20–21Z) yet the ASK staleness rose linearly for 16+ hours. This is sustained, worsening degradation of the load-bearing on-chain Pinnacle floor/ask surface (load-bearing since the ~23:00Z 06-08 `04011b3`/`ab2e6f4` Flowty-cache retirement), NOT a transient saturation artifact.
- **Action (CC/Trevor — already queued as PINNACLE-RECONCILE-TIMEOUT; tonight's pass is NO-PUSH-blocked on the route side anyway):** re-firing the cron alone will NOT fix it (it timed out every other tick when it ran). Options unchanged: (1) `SET statement_timeout TO '120s'` on the reconcile RPC — CAVEAT it fires ~every 15min so a 120s hold risks pool contention; validate across a rush; OR preferably (2) make reconcile **incremental** (only changed listings) so it never approaches the 8s cap. Not auto-shipped/reverted (Pinnacle pricing path off-limits + focus.md item 2). **This is the single most actionable live-degradation item on the platform tonight.**

## CRON-DROP-WAVE — one instance recovered, one still drifting (carried · operator)
- **`topshot-fmv-populate` RECOVERED.** Was 1037m silent at 18:06Z (CRON-DROP-WAVE/TFP-WATCH); it fired again at **18:50Z** so it's now OFF `detect_stalled_pipelines()` (136m, under its 480m threshold). That single 18:50Z tick logged a fail (`Timed out acquiring connection from connection pool`) inside the evening DBSAT window, but the route is fire-and-forget (202 + work in `after()`); the next ~6h tick (~00:50Z) is the real tell. No action needed unless it re-stalls.
- **`ultimate-fmv-recalc-v1` still drifting** — last run 06-08 06:35Z, now **2312m / ~38.5h** silent (info severity, ~daily). Past 1.5 daily cycles missed; same external-cron-drop pattern. Operator: confirm/re-fire if a fresh ultimate-edition FMV pass is wanted. These two are the full `detect_stalled` set besides the Pinnacle-reconcile LEAD.

## Carried lows — still open (no change, not re-logging as new)
- **USERNAME-CRON-UNWIRED** (logged 18:06Z · operator/CC): `/api/cron/resolve-wallet-usernames` (today's `3fdbf25`) still has **zero `pipeline_runs` rows** — its cron-job.org entry is still not wired, so the wallet→@handle populator isn't running. Migration + route deployed (`15ca7915` READY), `UserLabel` UI falls back to truncated addresses harmlessly → no user-facing breakage; the intel surface just stays under-populated. Operator: add the cron entry (or confirm intentionally manual).
- **GIT-IDENTITY-DRIFT** (logged 15:06Z · LOW · operator/CC): today's CC feature/brand commits (`b7211fb`→`15ca7915`, the Phase-2 brand batches) are all authored as `rpc-daytime-monitor <monitor@rippackscity.com>` instead of Trevor. Cosmetic (attribution only); reset `git config user.name/email` at the next CC session.

## ✅ Positives / clean-on-read
- **b7211fb post-ship watch still PASS:** `topshot-sales-indexer` 71 runs / **0 fails** in 24h, last 21:03Z; the per-sale `/v1/transactions?expand=result` buyer decode is not throttling. `v_sale_execution_accounts` resolves (1 row, populating). Effectively closeable.
- **Today's daytime ship wave (dapper.market / on-chain-intel Items 1–6, `b7211fb`→`15ca7915`, 20/20 READY) clean on read:** `topshot_pack_reality_top_ev` (re-keyed by today's Item-4 `b40ce05` migrations `pack_reality_top_ev_secondary_ask_gate` + `pack_ev_original_distribution_basis`) still resolves (6 rows); no Sentry signature attributable to the wave; no new pipeline-fail signature. Night pass owns the deep post-ship watch (highest-attention = Item-4 pack-EV migrations + Item-3 username cron's first runs once wired).
- **PIN-SYNC gate (a) holding:** `pinnacle-sync` last ok at its daily 10:07Z tick, 0 fails — the `5880eeb` render-FMV `statement_timeout 120s` fix is durable. Unblocks the queued PIN-SYNC-CRON watchlist INSERT + the (b) `pinnacle_fmv_stale_hours` trust-health tripwire (night-pass-eligible).
- **Evening DBSAT cluster ~18:46–19:06Z fully recovered:** routine I1/DBSAT pool/statement-timeout class (wmc-fmv-populate 4, compute-topshot-pack-ev 2, single fails on allday-unmapped-resolver / topshot-offers-indexer / ufc-listings-indexer / pinnacle-nft-resolver / hybrid_custody_events / topshot-fmv-populate). Last hour 221 runs / **0 fails**. None a wedge.

## Health snapshot
✓ security **0/0** (rls-off none · anon-write-on-rls-off base tables none · `check_secdef_anon_execute_violations()` clean · `check_public_security_invariants()` clean) · sentinel TS-UUID-48h **0** (total 6406, flat — DUPE1 re-mint stopped) · editions TS 15542 / AllDay 6191 / Golazos 581 / UFC 446 (identical to 08:30Z baseline) · TS FMV (latest-per-edition, all editions, sentinel RPC) HIGH 549 + MEDIUM 2316 = **HIGH+MED 2865** (flat vs 2889 @18:06Z), NO_DATA 4842 (improving from 4921), STALE 239, ASK_ONLY 997, SALES_ONLY 19 · AllDay HIGH 88 + MED 394 = **HIGH+MED 482** (flat vs 484) · `fmv_sanity_flags` 0 · writers fresh (fmv-recalc 20:48Z/5 fails-24h DBSAT-recovered, topshot-sales-indexer 21:03Z/0 fails, allday-fmv-populate 21:02Z/0 fails, offers-sweep 21:02Z/0 fails, wmc-fmv-populate 21:08Z, compute-topshot-pack-ev 21:07Z) · unmapped_sales 183 (flat) · DB **6768 MB** (+25 since 18:06Z, +81 over the day — slow creep, watch-only) · 20/20 deploys READY, 0 ERROR (prod `15ca7915` ~13:05Z; no new deploys since) · Sentry: 0 new issues in 12h; only NEXTJS-15 (PIN1/Q4 `listing_resolution_failures_inserted`, 7 events/21h, last 4h ago) active in 6h — not spiking; NEXTJS-14 (PINFMV-DRIFT-14, off-limits) quiet >4h · artifacts validated (v_tracked_wallet_fmv_confidence 20 = AF1-v2 holding, topshot_pack_reality_top_ev 6, topshot_squeeze_board 3140, topshot_2025_rookie_index 61, v_sale_execution_accounts 1, v_rpc_trust_health 6) — 17 artifacts, none broken.

`detect_stalled_pipelines()` = 2: `pinnacle-listings-reconcile` (medium, 583m — PINNACLE-RECONCILE-TIMEOUT LEAD), `ultimate-fmv-recalc-v1` (info, 2312m — daily, CRON-DROP-WAVE). `topshot-fmv-populate` cleared since 18:06Z (re-fired 18:50Z).
