# Cowork note — 2026-06-11 ~18:10Z: analytics fn optimizations SHIPPED (4 migrations, expected — don't flag)

Delegated-build, equivalence-gated (counts exact; moving-window/freshness tolerance only):
- audit_20260611_analytics_data_quality_overview_single_pass + audit_20260611_analytics_dq_overview_fresh_exists_guard — analytics_data_quality_overview 108,859ms -> ~102ms warm (~1,000x). One-pass GROUP BY consolidation of the per-collection editions counts; serial-zero counts via the new partial index; FMV freshness via the smoke-run LATERAL pattern PLUS an EXISTS pre-guard (zero-FMV collections like candy_mlb walked the entire fmv_snapshots_2026 partition backward — 220k buffers — without it; guard leg = 6.1ms). Revert: prior bodies in migration headers.
- audit_20260611_analytics_packs_summary_distinct_on — analytics_packs_summary 16,118ms -> 68-409ms (~39x): ROW_NUMBER-over-62k-rows -> DISTINCT ON riding idx_pack_ev_history_listing_time; join/normalize after dedup.
- audit_20260611_idx_sales_collection_serial_zero — partial index on sales (collection_id) WHERE serial_number=0; ~136KB total across 8 partitions.
- analytics_pipeline_health DELIBERATELY NOT shipped: 11ms calm — its ~24s readings were ambient saturation, not structure. fmv_pipeline_health (1,150ms calm) + pulse_24h (614ms calm) likewise structurally sound; analyze-only.
- DIAGNOSTIC NUANCE for the record: per-function statement_timeout proconfig did NOT govern in this session's tests (a GUC change cannot re-arm a running statement's timer; data_quality blew through its own 15s to 108s) — yet the 06-10 reconcile fn-level 60s bump empirically DID let 13-48s runs complete. Both observations are real; the safe rule going forward: do NOT rely on fn-level statement_timeout as a guarantee in either direction — prefer structural fixes (index/one-pass/LATERAL), and verify timeout behavior per-case. check_secdef violations [] after every ship.
