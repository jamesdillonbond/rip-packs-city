# Daytime monitor candidates — 2026-08-03T21:15Z (afternoon tick)

> inbox written to mount, push unavailable (sandbox bash/useradd VM down this run — no clone/push possible; night pass picks this up locally).

Health baseline: `rpc_ops_snapshot` — security invariants/anon-write/rls-off/secdef-violations all `[]`; sentinel TS-UUID 48h = 0; DB 11,908 MB. 3 trust breaches, all KNOWN/decaying/triaged (see notes) — NOT re-logged as incidents. One genuinely NEW finding below.

---

## HIGH — SECDEF-ANON-EXEC-DRIFT: new `backfill_acquisitions_for_collection` overload is anon/authenticated-EXECUTABLE

- **Source:** `rpc_ops_snapshot().pipeline_alerts` → `secdef-anon-exec-drift` (severity high); confirmed live via `has_function_privilege`.
- **What:** `public.backfill_acquisitions_for_collection(uuid, integer, timestamp with time zone)` is `prosecdef=true` and returns `anon_exec=true`, `auth_exec=true` (service_role also true). It is the SOLE overload — today's classify-acquisitions bounded-window fix (`c571e9f3` All Day + `8e62cf26` UFC follow-up) added `p_since timestamptz`, changing the signature; a new function object gets the default `PUBLIC` EXECUTE grant, so anon/authenticated can now execute this SECDEF **writer**. Classic mirror-case documented in CLAUDE.md ("a new function's default EXECUTE grant is to PUBLIC").
- **Risk read:** Real anon-reachable surface on a SECDEF write path (writes `moment_acquisitions`). Low blast radius today (function does a bounded backfill and is normally only called by service_role/internal), but it is an unauthorized-exec hole and it holds the drift sentinel red. Low-risk, well-scoped fix.
- **Suggested action (night pass):** `REVOKE EXECUTE ON FUNCTION public.backfill_acquisitions_for_collection(uuid, integer, timestamp with time zone) FROM PUBLIC;` — the owner (`postgres`) + `service_role` carry explicit grants and survive a PUBLIC revoke, so internal/service_role callers keep working. Verify with `has_function_privilege('anon', ...)` = false and re-run `check_secdef_anon_exec_drift()` → `[]`. Revoke (not allowlist): it is reached only by service_role, so shrinking the surface is correct. This will also clear the `secdef-anon-exec-drift` pipeline alert.

---

## KNOWN / decaying / triaged — NOT new incidents (recorded for continuity only)

- **`fmv_sweep_stall_pct_24h` = 99.2% (BREACH, 50):** the fmv-recalc cursor-stall fix (`484d08d7`, deploy `dpl_4K43mhkkCbseaJTqgJautqoJaLSh` READY) landed today ~20:48Z. New code verified live in `pipeline_runs.extra` (page_size=500, has_more=true, edition_limit=500). This is a rolling-24h metric still dominated by pre-fix stalled runs; it decays as the window rolls past the fix. No action — post-ship watch only.
- **`public_board_slow_count` = 3 (BREACH, 1):** `candy_special_serials_board` fixed today (`3655cf44`, deploy READY) — re-measured live at **3,883 ms < 4,100 ms budget** (the 18:58Z liveness sample of 11,193 ms was pre-fix); will clear on the next liveness tick. The other two (`topshot_first_mint_trophy_stats` 1.04×, `topshot_first_mint_trophies` 1.02×) are CC-triaged threshold-flap, explicitly do-not-fix (2–4% over a 3×-warm budget). No action.
- **`unmapped_resolution_backlog_max` = 105 (BREACH, 100):** nba_top_shot draining fast (inflow 0/24h vs outflow 3,687/24h, ~0.6d to clear; live pile from historical backfill). Per `focus.md` STEER, do NOT re-flag a standing backlog BREACH while trending down. No action.
- **Medium failure_rate alerts** (all known transient/by-design classes): `allday-lock-refresh` (statement timeout — proconfig can't extend the ceiling, documented), `allday-unmapped-resolver-tail` (connection-pool saturation), `topshot-active-listings-ingest` (egress_blocked, known), `wallet-username-resolver` (statement timeout saturation). `candy-offers-indexer` "failure" is the **by-design honesty guard** (partial sweep <50% → deactivation suppressed), not breakage.
- **pg_cron `rpc-refresh-allday-pack-realized`** — 1/4 failed at 18:35Z (`REFRESH MATERIALIZED VIEW CONCURRENTLY` statement timeout). Known transient IOPS/MV class (fn has `statement_timeout=600s`; 3/4 succeed). No action.
- **`candy-editions-ingest`** silent ~2,186 min / missed the 08:40Z daily tick — severity=info, `candy_mlb.is_active=false` pre-launch, does not page. Watchlisted already. Note only.
- **Sentry NEXTJS-23** — player-page `statement timeout`, 1 event/1 user, 11h ago, not spiking. Documented deliberate entity-page saturation class (`get_player_editions` ~161 ms warm; intentional throw-and-retry). No action.

## Notes
- No ERROR-state Vercel deploys (two READY prod deploys are today's candy-board + fmv-recalc fixes; CANCELED entries are superseded docs commits).
- Artifact estate enumerated (11 artifacts). Full per-artifact payload-query validation not run this tick (bash VM down); backing data sources (`v_rpc_trust_health`, `fmv_by_collection`, board liveness, editions) all confirmed healthy via `rpc_ops_snapshot`/direct SQL, no schema-break signals surfaced.

---

## DRAINED 2026-08-03 (Claude Code, same day — not the night pass)

**HIGH / SECDEF-ANON-EXEC-DRIFT — FIXED**, migration
`audit_20260803_revoke_anon_exec_backfill_acquisitions_for_collection`.

⚠ **The suggested action above was INCOMPLETE and would have left the hole open.** It
prescribed `REVOKE EXECUTE ... FROM PUBLIC` only, but the live ACL carried BOTH forms —
`{=X/postgres, postgres=X, anon=X, authenticated=X, service_role=X}` — i.e. the PUBLIC
default AND explicit `anon`/`authenticated` rows (Supabase's ALTER DEFAULT PRIVILEGES grants
EXECUTE to those two on function creation). Revoking only PUBLIC leaves
`has_function_privilege('anon', ...)` true via the explicit grant. Both revokes were applied.

Caller audit before revoking (both directions, per CLAUDE.md): the only direct caller is
`app/api/cron/classify-acquisitions-multicollection`, using `supabaseAdmin` — a genuine
service-role import, not an aliased name — behind Bearer `INGEST_SECRET_TOKEN`. Invoker-mode
callers: **zero** (no function body, view definition, or `cron.job` command references it), so
no anon-reachable SECURITY INVOKER path made the grant load-bearing. Revoke was correct over
allowlist.

Verified after: ACL `{postgres=X/postgres,service_role=X/postgres}`; anon/authenticated EXECUTE
**false**; service_role + owner **true**; `check_secdef_anon_exec_drift()` → `[]`;
`check_public_security_invariants()` 0; `check_anon_write_surface()` 0.

Everything under "KNOWN / decaying / triaged" was left alone as the monitor recommended. The
two entries describing this session's own work are confirmed accurate: the candy board
re-measured at 3,883 ms (< its 4,100 ms budget) and `fmv_sweep_stall_pct_24h` is decaying as
the rolling window rolls past the fix.
