# Daytime monitor candidates — 2026-06-05T18:25Z

Run: `rpc-daytime-monitor` (read-only). Lock stale (from the 08:35Z night pass, 571m old) → proceeded normally. No `FREEZE.md`, no `focus.md`. Declined list empty. Platform health **green except the already-queued DUPE1 sentinel CRITICAL**. One NEW candidate (a broken live artifact).

---

## P1 — [HIGH per §1b · broken artifact · LOW-risk fix · night-pass-shippable] `rpc-insights-health` backing query times out on `topshot_squeeze_board`

- **Title:** `rpc-insights-health` Q_COUNTS panel errors (Postgres 57014) — `count(*) FROM topshot_squeeze_board` exceeds the default ~8s statement timeout, so the artifact's main "backing data" table fails to render.
- **Source:** artifact `rpc-insights-health` (`index.html`, the `Q_COUNTS` UNION-ALL, `topshot_squeeze_board` leg). Reproduced live this run: bare `SELECT count(*) FROM topshot_squeeze_board` → **57014** under the default timeout; it completes (**3,136 rows**) only with `SET statement_timeout='30s'`. All 9 other insights views in `Q_COUNTS` count fast (deals 3,136 / offer_spread 5,610 / set_squeeze 128 / pack_reality 3 / cross_collection_stats 1 / rookies / market / first_mint / pinnacle_scarcity), so the whole `Q_COUNTS` dies on this single leg → the artifact's counts table shows the error string and the banner reads "Some checks could not run — open inside Cowork and Reload."
- **Likely cause:** `topshot_squeeze_board` is a plain VIEW (relkind `v`) whose full materialization runs a per-row `LEFT JOIN LATERAL (… fmv_snapshots … ORDER BY computed_at DESC LIMIT 1)` over the entire `badge_editions ⋈ editions` TopShot join (join key uses a cast: `e.external_id::text = be.external_id`). Its full-scan `count(*)` cost has crept past the timeout as `fmv_snapshots` + TS editions grew — **DUPE1**'s inert UUID re-mint added ~2,000 TS editions today (11,498 → 13,540). **Same failure class as AF1 (`v_tracked_wallet_fmv_confidence`), which the night pass optimized last night.**
- **Blast radius:** artifact-only. The PUBLIC `/insights/squeeze` page (server-rendered as of today's `39983de`) is **NOT** affected — its realistic default query (`WHERE squeeze_pct >= 50 ORDER BY squeeze_pct DESC LIMIT 100`) returns fast this run; only the unbounded `count(*)` blows the timeout.
- **Risk read:** LOW. Fix is additive/reversible; not destructive, not off-limits, no public-surface impact, no FMV/ingest/auth logic.
- **Suggested action (night pass):** repair the artifact — cheapen the squeeze liveness leg, e.g. replace `count(*) FROM topshot_squeeze_board` with an `EXISTS` / `SELECT count(*) FROM (SELECT 1 FROM topshot_squeeze_board LIMIT 1) z` liveness probe, or run just that leg with a raised `SET LOCAL statement_timeout` (artifact edit = night-pass job, monitor is read-only). Optionally also DB-optimize `topshot_squeeze_board` per the AF1 precedent. **Pattern watch:** AF1 + this = two FMV-snapshot-joining views crossing the statement timeout in two days as `fmv_snapshots` grows; worth a sweep for other heavy views (none else broke today — the other 9 insights views count fast).

---

## Health snapshot (for night-pass context — NOT new candidates)

- `detect_stalled_pipelines()` = `[]`. Security **0/0** base tables (RLS-off + anon/auth-write checks). Sentry **0** issues in the last 6h (the morning smoke cluster has gone quiet).
- Deploys: latest prod `bc8d1cd` (SEO series-label fix) READY; only 2 ERROR in 24h = the known transient `8f3fff9` pair (pack-reality honesty filter; code live via descendant READY deploys). Heavy daytime shipping today (SEO server-rendering of squeeze/rookies/first-mint/market boards, internal-linking, cross-collection refresh cron, rewards delivery follow-ups, FMV A2/F2/F4).
- **DUPE1 (already queued):** sentinel TS-UUID-48h **4,393** (08:15Z baseline 2,611), still CRITICAL, but mint rate slowing to ~186/hr (was ~450/hr overnight). All inert (`set_id_onchain` NULL, trigger-held). TS NO_DATA 2,010 → 4,333 is the documented DUPE1 inert re-mint, not a pricing regression — TS HIGH+MED 2,944 → 3,037 (still improving). Durable fix remains off-limits worker code (Item B2). Do NOT raise the threshold (true positive).
- Recent overnight ships verified WORKING: **AF1** `v_tracked_wallet_fmv_confidence` returns fast (no 57014); **MON-WATCH** rows `offers-sweep` (silent 10m) + `allday-fmv-populate` (29m) @120m live + healthy, not false-positiving; MON1 `fmv-recalc` (23m) + Q10 `topshot-listing-cache` (110m) within threshold.
- Pipeline fails last 6h all 1-each transient: 12:00Z cron-rush cluster (allday/pinnacle/ufc listings upstream/pool timeouts) + the 12:22Z `topshot-moments-hydrator` `candidate_read` statement timeout (queued **N2**) + the 18:00Z `topshot-fmv-populate` connection-pool timeout (the documented ~30%-of-runs class, not a bug).
- Drift vs 08:15Z baseline: AllDay FMV flat (HIGH+MED 486 vs 495); editions AllDay/Golazos/UFC flat; unmapped_sales 209 → 237 (minor); DB 5,978 → 5,984 MB. All within normal.
- Artifacts: **15/16 healthy**; `rpc-insights-health` is the lone break (P1 above). Spot-checked `v_rewards_economy` (1 summary row), `cross_collection_cohort_mat` (144 rows — new refresh cron's table populated), `edition_offers` fresh to 18:20Z.
- Note (not a new item): the shared repo tree's git identity is `rpc-daytime-monitor <monitor@rippackscity.com>`, so today's Claude Code feature pushes are author-attributed to the monitor — a symptom of the still-unmounted bot clone (tracked as **Q7**), not a new finding.
