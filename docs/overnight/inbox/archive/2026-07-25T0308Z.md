# Daytime monitor inbox — 2026-07-25T03:08Z (~20:08 PDT, evening tick)

Health GREEN. No skew (shell 03:05Z ≈ DB now() 03:08Z ≈ max sale 03:02Z ≈ max FMV 03:08Z ≈ max chat 03:03Z). **security 0/0/0/0** — the Candy-view `security_invoker` invariant both earlier ticks today logged (2→11 `view_unexpected_definer`) is now `[]`, normalized live during the day (`audit_20260724_candy_view_invoker_normalize` per CLAUDE.md); Sentry JAVASCRIPT-NEXTJS-1C smoke-test failure **last fired ~7h ago and has stopped** ⇒ that candidate is a no-op CLOSE, not re-logged. trust **15/15, 0 breaches**; `detect_stalled_pipelines()` `[]`; RLS-off base 0; anon-write holes 0; secdef-anon 0; sentinel_ts_uuid_48h 0. Vercel: prod tip `1e2acf1` READY, **zero ERROR** (a big day of interactive CC ships — pack-detail perf, best-offers, DB pins — all READY; CANCELED entries are docs-only/superseded, expected). rpc-live-health flagship re-validated clean (all payload deps resolve: cached_listings_v2 41,683 / pinnacle_fmv_history 62,223 / unmapped_open 45,339 / get_pipeline_alerts array / 7 slugs); estate unchanged **17 listed / 15 active**. DB 10,942 MB (+138 across the day, normal). This is an evening tick — Section 1a (trust-health / cross-collection first-tick extras) intentionally skipped.

## CANDIDATE 1 (LOW) — NEW sibling of the carried pool-timeout class: `/[collection]/team/[slug]` now throwing "connection pool" timeouts; a proven fix pattern landed today

- **Source:** Sentry **JAVASCRIPT-NEXTJS-1Y** "team detail unavailable: Timed out acquiring connection from connection pool" — culprit `GET /[collection]/team/[slug]`, **first seen ~5h ago (NEW), 1 event / 1 user**. Sibling of the carried **ACTIVATION-PATH-RPC-TIMEOUTS** (JAVASCRIPT-NEXTJS-20, "player detail unavailable: …connection pool", 2 events/24h, last seen 5h ago). Both are entity-detail server pages.
- **Risk read — LOW, contention-correlated.** Both fired during today's heavy interactive-build contention window (see verified-not-candidates); 1–2 events each over 24h, not a spike, non-blocking (page 500s on a cold DB pool, self-heals on retry). "Timed out acquiring connection from pool" is pool-exhaustion — could be (a) the team/player-detail RPC itself being slow (holding a connection long), or (b) collateral exhaustion while OTHER slow queries saturate the pool during a contention spike. Not yet diagnosed which.
- **Why worth a look now:** today's interactive session **root-cause-fixed the pack-detail equivalent** — `get_pack_detail_bundle` hero query rewritten to a MATERIALIZED-CTE + final-5-only join, **warm ~1.1s → 27ms** (ledger 2026-07-25 "(A)"). If the RPC(s) behind `/player/[slug]` + `/team/[slug]` carry the same "score/join the whole set before LIMIT" anti-pattern, the same rewrite applies and would cut their connection-hold time.
- **Suggested action (night pass — investigate first, likely low-risk read-optimization):** (1) identify the RPC(s) behind `GET /[collection]/player/[slug]` + `/team/[slug]`; (2) `EXPLAIN (ANALYZE,BUFFERS)` under a representative arg to distinguish a slow hero query vs. collateral pool exhaustion; (3) if it's the hero-query anti-pattern, apply the `get_pack_detail_bundle` MATERIALIZED-CTE pattern (byte-identical-output proof + grants preserved, per the pack-detail precedent). If it's purely collateral pool-exhaustion during contention, fold into ACTIVATION-PATH-RPC-TIMEOUTS and leave it. NOT auto-shippable blind — needs the EXPLAIN diagnosis first.

## Verified-not-candidates (context so the night pass doesn't re-triage)
- **pg_cron 3 statement-timeouts (~00:10–00:23Z / ~17:10 PDT) — documented daytime-contention/IOPS class, self-recovered, ALREADY QUEUED. Do NOT ship a timeout bump.** `rpc-refresh-topshot-edition-median` (cron_heavy/600s, MV refresh), `rpc-remap-misattributed-sales` (cron_heavy/600s), `rpc-backfill-pinnacle-acquisitions` (postgres/120s) each failed **1 of their last 4–5 6h-ticks** and succeeded on all others; next ticks ~06:xxZ. All three cluster at 10/17/23-past-every-6h and timed out in the SAME window as today's heavy interactive-build + sales-counterparty-backfill remediation. `rpc-remap-misattributed-sales` is already the queued **OVERNIGHT-0623Z-CONTENTION-CLUSTER** (WATCH — "do NOT ship a timeout bump; contention-root, self-recovering; bound the win_sales CTE window only if it fails multiple consecutive ticks"); the correlated-cluster angle is the queued **CORRELATED-PIPELINE-DROPOUT-DETECTOR**. CLAUDE.md standing guidance = no action on transient timeouts. The one nominal lever (move rpc-backfill-pinnacle-acquisitions postgres→cron_heavy for a 5× runway) is premature — it finishes <120s on 4/5 ticks; a role change for a once/day transient on a `moment_acquisitions`-writing backfill isn't warranted. NOT re-logged.
- **pipeline_fails_24h — same contention window, all HTTP pipelines recovered.** compute-topshot-pack-ev (7f/24h, but 54 ok / 2 fail last 3h, last_ok 03:07Z), pinnacle-nft-resolver (5f, 33 ok / 0 fail / 3h), sales-counterparty-backfill (14f, 31 ok / 3 fail / 3h, last_ok 03:05Z — the 6 null-seller partial indexes from ~15:00Z are holding; fails are the contention tail), wallet-username-resolver (9f, recovering, last_ok 03:08Z) — all statement-timeout/contention, self-recovering. **snapshot-institutional-wallets** (4f) are STALE pre-14:36Z-index-fix runs on a DAILY pipeline; next tick ~10:07Z 07-25 confirms ok=true (do NOT alarm — matches 1514Z/1815Z). **Dune pair** (sales-seller-recovery-dune 21 / sales-ingest-dune 10) = carried DUNE-DATAPOINT-CAP-402 (402 cap, cursors parked, fails safely).
- **unmapped_sales open 45,339** = the ledger-top **2026-07-25 rpc-data-quality-sweep** flag (AllDay `allday_v1_history` backfill parks V1-Dapper sales the resolver can't price). Bounded backfill winding down (inflow tapering 15,312→11,520→2,051→163/day), contained to the staging table, live AllDay data NOT degraded (FMV fresh, recent `sales` 0-null). Already flagged + handoff written today. NOT re-logged.
- **golazos_sales resolving_editions** (sole pipeline_alert, info) — standing, non-user-facing (same class as the standing ufc_sales one).
