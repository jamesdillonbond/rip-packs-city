# RPC daytime monitor — inbox candidates

- **Run:** 2026-06-02T03:15Z · read-only daytime sweep (local ~20:15 PDT, in-window).
- **Platform state:** GREEN — `detect_stalled_pipelines()` `[]` · security base-table 0/0 · anon-readable SECDEF views exactly 1 (the known S1, unchanged — no new regression) · 20/20 deploys READY (0 ERROR) · FMV writers fresh (~7m) · Sentry 1 unresolved (known, gated, 11h-stale) · 12/12 insights backing views resolve with sensible data.
- **Lock:** `docs/overnight/.lock` present but `RELEASED` (embedded start 2026-06-01T13:54:59Z, ~13h ago) → STALE per its own >45-min rule; took over, inbox commit permitted.
- **Baseline:** `metrics-latest.json` @ 2026-06-01T13:54Z (night pass; shipped nothing / NO-PUSH).
- **HEAD:** `dadcc57` (local working tree current with origin/main).

**No NEW candidate this run.** The only standing finding is S1 (already in the 2026-06-01T21:14Z inbox) — verified still-live this sweep, NOT re-raised as a duplicate. Two material STATUS UPDATES below for the night pass (one closes C2, one re-scopes S1). Nothing in the ledger queue or Declined list re-raised.

---

## Status updates to already-queued / already-logged items

### C2 — RESOLVED (was: `v_moments_needing_hydration` candidate-read statement timeout; 2026-06-01T18:24Z inbox)
- The 2026-06-01 PM full-platform audit shipped `audit_20260601_v_moments_needing_hydration_materialized_cte` (MATERIALIZED-CTE fence; batch 588ms→167ms) **after** the 21:14Z monitor sweep.
- Empirical confirmation this sweep: `topshot-moments-hydrator` last failed **2026-06-01 18:12:04Z**; **0 fails since** across ~90 runs through 03:12:04Z — including the 00:00Z midnight cron rush that previously tripped it. 3/132 fails in the trailing 24h are all pre-fix (≤18:12Z).
- **Night pass: C2 can be marked resolved.** No action needed.

### S1 — STILL LIVE + now STANDALONE-shippable (was: anon-readable SECURITY DEFINER view; 2026-06-01T21:14Z inbox)
- Re-verified live this sweep: `v_moments_needing_hydration` is `relkind=v`, `reloptions=NULL` (**no `security_invoker`** → executes as definer, bypasses base-table RLS), and **both `anon` AND `authenticated` hold SELECT**. The catalog "anon-readable SECDEF view" check returns exactly this one view — posture is still 1 (regressed from the Q1-era 0), unchanged since 21:14Z.
- **Material refinement (the reason this is worth a note):** the 21:14Z S1 entry suggested *bundling* the fix into the night-pass C2 view-optimization (`set security_invoker = on` in the same CREATE OR REPLACE). **That C2 optimization has now already shipped (the materialized-CTE migration above) WITHOUT setting `security_invoker` and WITHOUT revoking the anon grant** — so the bundling window has closed. S1 is now a **standalone** change.
- **Risk:** low — internal pipeline plumbing; the hydrator reads it via `service_role` (unaffected by either fix), and the data is moment-hydration candidate metadata (no PII / no secrets). Additive/reversible, not route logic, not destructive, not off-limits → SHIP-eligible on a true overnight run.
- **Suggested action (night pass / operator):** ship S1 **solo** — preferred `REVOKE ALL ON public.v_moments_needing_hydration FROM anon, authenticated;` (it has no anon HTTP reader), or the Q1-precedent `ALTER VIEW ... SET (security_invoker = on);`. Exact migration + revert are in the **2026-06-01T21:14Z inbox (S1)** — not duplicated here. Verify after: the anon-readable-SECDEF-view check returns 0; `topshot-moments-hydrator` still runs (service_role path unaffected).

### Recent-ship watch — `dadcc57` (H1–H6 audit follow-ups) shipped CLEAN since baseline
- New since the 13:54Z baseline (baseline HEAD was `7c1b81b`): CC executed the 2026-06-01 audit handoff (H1–H6: collection-aware ask/offer cells, hide best-offer when no source, AllDay `cross_market_ask`). Deploy `dpl_2ktaTGCm55TVGYrbXnEHaXjSaN3g` **READY**.
- DB half verified live: `get_edition_detail` now contains `cross_market_ask` (migration `audit_20260601_get_edition_detail_standard_cross_market_ask` landed). No new Sentry issue and no new pipeline failure attributable to it. (Live HTTP page render not verified this run — the sandbox `web_fetch` provenance rule blocks fetching production URLs; DB + deploy signals are green.)

### C1 — no change (`topshot-listing-cache` ~6h daytime cadence; 2026-06-01T18:24Z inbox)
- `detect_stalled_pipelines()` `[]` → within the Q10 360m threshold. The sustained ~5–6h cadence tension persists but is guarded; not re-raised. Do NOT lower the 360m threshold (Q9 lesson).

---

## Sweep evidence (night-pass baseline; not findings)

- `detect_stalled_pipelines()` = `[]`.
- `ok=false`/6h — 6 pipelines, **1 fail each**, all clustered 00:02–00:20Z (midnight cron rush), all self-recovered (transient connection-pool / statement-timeout / upstream / `time_budget_exceeded` class): `allday-listings-indexer`, `compute-topshot-pack-ev`, `golazos-listings-indexer`, `pinnacle-nft-resolver`, `ufc-stub-thumbnail-resolver`, `wmc-fmv-populate`. None recurred in the ~3h since; none attributable to a deploy.
- Security: 0 RLS-off public base tables, 0 anon/authenticated write-grants on RLS-off base tables (`relkind r,p`). Anon-readable SECDEF views = 1 (S1; unchanged).
- FMV: TS HIGH+MED **899** (HIGH 249 / MED 650; NO_DATA **4,851** ↓ from 4,924 prior / 5,109 baseline — still improving). AllDay HIGH+MED **261** (HIGH 52 / MED 209; STALE 490; NO_DATA 522). Both writers fresh @ 03:08:30Z.
- Sentinel TS-UUID-48h **45** (<250 ok; baseline 40 / prior 36 — normal inert accumulation).
- Drift: editions TS 16,327 (+19 vs baseline) / AllDay 6,191 / Golazos 581 / UFC 446 (flat). `unmapped_sales` 147 unresolved (flat) / 267 total. DB **5,947 MB** (+35 vs baseline 5,912).
- Vercel: 20/20 READY, 0 ERROR. Newest `dadcc57` (H1–H6, ~23:18Z) → `bec07b3d` (docs) → all prior READY.
- Sentry: 1 unresolved — `JAVASCRIPT-NEXTJS-15` (`POST /api/allday-listings-indexer`, gated capture), 2 events, last seen ~11h ago. Not new, not spiking (known/watched; the prior 6-issue smoke cluster is no longer surfacing as unresolved).
- Insights backing views (artifact signal harvest — `rpc-insights-health`, validated by direct query; artifact HTML not sandbox-mounted): squeeze **3,099** · set_squeeze 127 · pack_reality_top_ev 91 · pack_reality_stats 1 · pack_reality_dist 6 · rookie_index 61 · market_index_daily 544 · first_mint_trophies 474 · cross_collection_cohort 1 · pinnacle_scarcity 103 · offer_ask_spread **5,565** · deals_vs_fmv 124 — all resolve, all sensible (close to 21:14Z). `rpc-security-drift` / `rpc-pipeline-reliability` / `rpc-audit-followups` queries all schema-valid against the current DB.
