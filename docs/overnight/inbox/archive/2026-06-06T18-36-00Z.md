# Daytime monitor inbox — 2026-06-06T18:36Z (11:36 PDT)

Run context: lock = released marker from the 08:41Z night pass (stale >45min — commit allowed). Repo = shared tree (bot clone still unmounted). HEAD `b293ba1` == origin/main. Security 0/0; deploys 19/20 READY (1 known ERROR `949e10f`, superseded); Sentry quiet (8 unresolved = known 00:17Z smoke cluster ×7 + NEXTJS-15 single event ~13:10Z, known PIN1 class). 15/16 artifacts validated healthy; **1 broken** (rpc-tracked-fmv-confidence, see I2).

## NEW candidates

### I1 · 18:00–18:31Z DB saturation incident — worst-yet rush-window episode; wmc write amplification is the likely new aggravator — HIGH (analysis + cron-stagger fix)
- **Source:** observed live this run. pg_stat_activity at 18:15Z: `autovacuum: VACUUM public.wallet_moments_cache` running **16+ min**, plus wallet RPCs ~2min and `refresh_pack_grail_metrics_mv` 1:19, 6 active >30s. Vercel runtime logs 18:12–18:24Z: **~15 routes 504** (wallet-backfill-allday/pinnacle ×9, drain-fmv-cold-tail, offers-sweep, check-alerts, wmc-fmv-populate, pinnacle-listing-cache, ufc-sales-indexer, run-insider-detectors, allday-fmv-populate, allday-listing-cache, refresh-pack-grail-metrics-mv, pinnacle-listings-reconcile, pinnacle-events-ingest). **User-facing wobble 18:28:35Z:** `AuthRetryableFetchError` → 307 on `/api/packs`, `/api/sniper-feed`, `/api/profile/saved-wallets` (Supabase auth calls timing out). Monitor's own `SELECT 1` failed 18:18–18:29Z. Self-recovered ~18:31Z (active 8→3); `/api/health` stayed 200 throughout; fmv-recalc 88/24h 0 fails — hot path never failed.
- **Why it's getting worse:** wmc (1.575M rows) `autovacuum_count = 125` vs editions' 15 — the 06-05/06-06 cron additions (`b072ce7` FMV-drift refresh ~38k UPDATEs/20min, `a2cae0d` image denorm 50k/tick, `b6005cb` serial backfill, mint_count folds) are new sustained UPDATE volume on wmc; long autovacuums now collide with the :00/:20 cron rush. Same class as the known 12:0x–12:2xZ transient cluster but materially worse (first time user-facing API routes wobbled).
- **Suggested action (night pass):** (a) quantify wmc daily UPDATE volume per writer (`pipeline_runs.extra` + pg_stat); (b) stagger the heavy wmc writers off the :00/:20 rush (cron offsets — operator/cron-job.org list change, queue if needed); (c) consider per-table autovacuum tuning on wmc (e.g. `autovacuum_vacuum_cost_limit`/`scale_factor` migration — rpc-migration checklist applies); (d) do NOT blindly raise statement_timeout. Watch tonight's 00:00Z rush for recurrence.
- **Risk:** analysis read-only; tuning = small reversible ALTER TABLE storage-params migration; stagger = operator.

### I2 · AF1 3rd statement-timeout — ledger tripwire HIT: stop optimizing the view, fix the writer (DUPE1-MIT) — HIGH
- **Source:** `SELECT confidence, count(*) FROM v_tracked_wallet_fmv_confidence GROUP BY 1` → 57014 at 18:31Z AND again 18:34Z (post-vacuum, pool quiet: active=3, autovac done 18:33:24Z) — genuine, not contention-coincident. Passed at 15:06Z, so it regressed <3.5h after that verification; AF1-v2 (shipped last night) lasted <11h. TS editions 15,541 (+542 since 08:30Z baseline).
- **Read:** the ledger's own directive on AF1-v2: "If it crosses the timeout a 3rd time, stop re-optimizing — treat as DUPE1 fallout (DUPE1-MIT / Item B2)." That condition is now met. `rpc-tracked-fmv-confidence` artifact is broken again (internal-only; view is service_role, 0 production readers — no user impact).
- **Suggested action (night pass):** do NOT write an AF1-v4. Ship **DUPE1-MIT** (the ready-to-run `drain_fmv_cold_tail` skip-inert WHERE patch already in the ledger) to stop the NO_DATA stamper churn; Item B2 (worker `buildEditionKey`/`seed_topshot_editions` int-pair preference) remains the durable fix (off-limits worker code — queue for Trevor/CC). Re-test the view after DUPE1-MIT lands; only then decide if it needs anything.
- **Risk:** DUPE1-MIT is an FMV-writer change (previously queued as such); per-night-pass rules it may still be off-limits → if so, hand to Trevor/CC as morning #2 after P1-CAD.

### I3 · P1-CAD: fix COMMITTED+PUSHED mid-run (`bf4c38c`) — verification-only now — MED
- **Timeline this run:** at 18:07Z the `Buffer.from` fix existed only as an uncommitted working-tree edit (HEAD `b293ba1` still `btoa`, 5/5 fails, latest 17:22:10Z). At ~18:40Z Trevor committed + pushed `bf4c38c fix(pinnacle): P1-CAD em-dash x btoa crash in metadata-backfill Cadence encode` (+ `b7a950d` landing the night-pass NO-PUSH doc outputs). origin/main == HEAD verified.
- **Suggested action (night pass):** verify the Vercel deploy for `bf4c38c` reached READY, then confirm the next few `:22` `pinnacle-metadata-backfill` ticks log `ok=true` and the PIN-CAT Q4 + serial Q5 queues resume draining (`extra->>'catalog_upserted'` / `serials_filled` > 0). If ticks still fail post-deploy, P1-CAD re-opens with `git revert b6005cb` as the fallback.
- **Risk:** none — verification only.

## Not re-logged (already in ledger; status re-confirmed this run)

- **N1** `snapshot-institutional-wallets`: still stalled, 2,447m vs 1,800m (3rd occurrence; 0 runs/24h). Operator re-fire + move off 06:00Z rush.
- **CROSS1**: `refresh-cross-collection` still **0 runs** — route is deployed (`deb786e`); the daily cron-job.org entry is still missing. Operator.
- **P3-BUYERS**: `pinnacle-resolve-buyers` 29 runs/24h 0 fails, but last run 16:20Z (~2h gap on a 20-min chain) — cadence still spotty; watch holds.
- **DUPE1**: TS-UUID-48h **6,382** (CRITICAL ≥2000) but **0 new rows in the last hour** (peak 450–520/hr; 15:06Z 62/hr) — the re-mint has effectively stopped; the 48h counter drains by ~06-08 if it stays quiet. Sentinel threshold stays put.
- **C2** pack-reality thinned: `topshot_pack_reality_top_ev` = 1 row (re-confirmed; analysis queued 15:06Z).
- 12:0x–12:2xZ transient timeout cluster (9 pipelines, recovered) — already noted at 15:06Z; today's 18:0xZ episode is I1.

## Positive verification (trims the queued C1 item)

- **C1 (2e8cbd1 render_id re-key) largely VERIFIED:** `pinnacle_catalog` 2,079 rows; wmc Pinnacle `render_id` 36,406/36,414; **`wmc.image_url` 36,406/36,414 — full Pinnacle image coverage, up from ~82** (the `populate_wmc_image` → `pinnacle_catalog` repoint works end-to-end; the 06-05 "images dead-end" is operationally overturned). Remaining for night pass: smoke `/api/public/pinnacle-image/[renderId]` (200 + image content-type + bogus-id 404) and eyeball a `/share/[wallet]` Pinnacle tile.

## Headline metrics (18:36Z vs 08:30Z baseline)

| metric | value | delta |
|---|---|---|
| TS FMV HIGH+MED | 3,045 (569 H / 2,476 M) | flat (3,041) |
| AllDay FMV HIGH+MED | 493 | flat (490) |
| TS NO_DATA | 5,795 | −18 vs 15:06Z (stabilizing) |
| TS editions | 15,541 | +542 (DUPE1, now 0/hr) |
| sentinel TS-UUID-48h | 6,382 CRITICAL | +542; **0 last hour** |
| unmapped_sales open | 162 | flat |
| DB size | 6,306 MB | +49 |
| detect_stalled_pipelines() | 1 (N1 only) | unchanged |
| security | 0 RLS-off / 0 anon-write | clean |
| artifacts | 15/16 healthy; rpc-tracked-fmv-confidence BROKEN (I2) | −1 |
| prod deploy | `b293ba1` READY 15:43Z | 19/20 READY |
