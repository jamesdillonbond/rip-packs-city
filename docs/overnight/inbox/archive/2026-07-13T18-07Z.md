# RPC daytime monitor — candidate inbox 2026-07-13T18:07Z

Run context: midday tick (DB now() 18:06:56Z ≈ 11:07 PDT — NOT the ~8am first-tick, 1a extras skipped). **Bash/git sandbox DOWN** (`useradd` exit 12 — same class as the 07-12 + 07-13 overnight nights; now 3rd occurrence, and it blocks this monitor's clone+push too) → **this inbox file written to the mount, push unavailable** (night pass picks it up locally). Concurrency lock RELEASED (08:12Z, stale) → inbox write permitted.

Health GREEN apart from the two below: security 0/0/0/0 (invariants/anon-write/rls-off/secdef-anon all `[]`); `detect_stalled_pipelines()` `[]`; sentinel TS-UUID-48h 0; all 8 contention pipelines `latest_ok=true` (brief ~17:08Z midday blip, self-healed next tick); Vercel prod `dpl_78y2z…` (gifting Phase 1 `c96896d8`) READY, no ERROR deploy; Sentry 0 new/18h; DB 11,142 MB (flat vs overnight 11,044). Artifacts spot-validated live (rpc-live-health insights boards + get_pipeline_alerts + rpc-set-challenge-roi→get_active_challenges) all resolve; the dropped `v_set_challenge_roi` view is EXPECTED (today's challenges VARIABLE-rework; the artifact was repointed off it — not a break). Post-ship watch of the heavy 07-13 CC/Trevor wave: no regression (security clean, prod READY, Sentry 0).

NOT re-logged (already queued by the 07-13 overnight pass): BASH/GIT-SANDBOX-PROVISION-FAILURE (operator/infra, now 3rd occurrence), WMC-INDEX-BLOAT-SECONDARY.

## Candidates

### 1. TOPSHOT-IMPOSSIBLE-PARALLEL-SERIALS-BREACH re-opened — 18/3 (LOW, known self-healing class)
- **Source:** `rpc_ops_snapshot().trust_health` → `topshot_impossible_parallel_serials` = 18 (breach_at 3). Was clean (`trust_health_breaches []`) at the 08:03Z overnight pass, so it climbed during today's `::` cataloging wave (editions TS 19,241; sentinel 0 = no UUID leak).
- **Detail:** ~5 `::` parallels carry ~18 impossible (serial > circulation_count) sale rows. Sampled: `222:7447::20` circ 1 vs max sold serial 10 (4 sales, last 2025-12); `221:7463::20` circ 1 vs 9; older 06-20 stragglers `222:7433::20`/`221:7454::20`/`227:7571::20` circ 2–9 vs serial 10. All last_sale dates are OLD — old sales attributed to freshly-cataloged parallels whose floor-seed circ sits below a real serial, awaiting the per-parallel circ backfill.
- **Risk read:** LOW. Documented recurring self-healing class, closed repeatedly (07-06 3→1, 07-10 4→0, 07-13 00:25Z 11→1). Not user-facing beyond the trust gauge. NOT on the Declined list. Only notable point is the elevated 18 (6× threshold) vs the usual 1–4.
- **Suggested action:** night pass — either let the per-parallel circ backfill self-heal (historically drops <3 within a day), or apply the established targeted circ-floor-raise on the specific `::` stragglers (pattern `audit_20260711_circ_floor_raise_impossible_parallel_wave2`; revert = restore prior circ values). Do NOT hand-edit circ broadly on a sales-adjacent table.

### 2. MISATTRIB-CANDIDATES-MV-REFRESH-TIMEOUT — daily 03:25Z (LOW, contention; CCM-step1-shaped)
- **Source:** `check_pgcron_recent_failures()` → `rpc-refresh-misattrib-candidates` (schedule `25 3 * * *`) failed 2026-07-13 03:25:00Z: `canceling statement due to statement timeout` on `REFRESH MATERIALIZED VIEW public.mv_topshot_misattrib_candidates` (in `refresh_topshot_misattrib_candidates()`).
- **Risk read:** LOW. Single daily job → `mv_topshot_misattrib_candidates` (misattribution-candidate detection helper, not user-facing FMV) is ~1 day stale until tomorrow's 03:25Z tick. DISTINCT job from the already-tracked MISATTRIB-DRAIN-REKEY-UPSTREAM-TIMEOUT (that's the 11:00Z rekey leg). Same failure SHAPE (`REFRESH MATERIALIZED VIEW` statement-timeout) as the CCM-step1 case CC durably fixed today.
- **Suggested action:** night pass — apply the CCM-step1 playbook: check whether the MV's underlying scan index is bloated (that was the real CCM-step1 root cause: 856s→54s after a `REINDEX`, no timeout raise, no query change) and REINDEX if so (behavior-preserving); else confirm it succeeds off-peak on tomorrow's tick. If it's a genuinely heavy refresh, the `cron_heavy`-role default (600s) is the other lever. Nothing destructive required.
