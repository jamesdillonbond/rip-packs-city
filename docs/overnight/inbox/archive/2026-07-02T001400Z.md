# Daytime monitor — 2026-07-02 00:14Z (~17:14 PDT, 2026-07-01)

Late-afternoon tick (not first-of-day; Section 1a daily extras skipped). Lock RELEASED (stale 08:21Z overnight). **Health GREEN.** Clone HEAD c0fe9ff2; prod dpl_HLe4c (795d99b) READY. No clock skew (shell 00:07Z ≈ DB now() 00:07Z ≈ app-stamped FMV 00:11Z / sales fresh). Supabase + Vercel connectors loaded; Sentry not separately loaded (Vercel get_runtime_errors used as frontend-error proxy). Artifact tools available.

## NEW candidate (1, LOW)

**OG-BACKSLASH-500 — malformed `%5C`-suffixed OG routes throw MODULE_NOT_FOUND (500) instead of 404.**
- Source: Vercel get_runtime_errors — `Error: Cannot find module './.next/server/pages/api/og/insights%5C.js'` (+ `/api/og/insights/pack-reality%5C`, `/api/og/insights/pack-sniper%5C`, `/api/og/fast-break%5C`). ~8 events total across 4 OG routes, users=1 (single crawler/scanner), one burst first=last **2026-07-01T23:03:5x**, lastDeployment=dpl_HLe4c (current prod).
- Risk read: LOW / benign impact. `%5C` = URL-encoded trailing backslash appended by a crawler; Next resolves it to a pages-router module lookup that 500s instead of returning a clean 404. No user-facing surface (OG card generation for a non-existent path), no data risk, single non-human client. New error CLASS though (first seen 23:03, not in the 07-01 baseline), so logging it.
- Suggested action (night pass / CC): add a graceful 404/`notFound()` guard for malformed OG paths (or normalize/strip trailing `%5C` before the OG route resolves) so bot-appended backslash variants stop emitting 500 MODULE_NOT_FOUND. Not urgent; recurs only if the crawler returns. If it stays a one-off 23:03 burst, close as crawler noise.

## Post-ship watch signals (informational — for the night pass, not candidates)

- **CLASSIFY-ACQ-ALLDAY-STATEMENT-TIMEOUT (queued 07-01 overnight) — SHIPPED + HOLDING.** An attended session today shipped `795d99b` ("cap AllDay batch 300->80"; current prod READY) — the exact fix the night pass recommended (reduce AllDay p_limit). Monitor independently verified **3 consecutive green ticks post-deploy**: 22:06 ok 29.9s / 23:06 ok 41.8s / 00:06 ok 44.5s, following the last pre-fix flap at 21:06 (94.9s timeout). Every tick now well under the 90s fn timeout + 120s route maxDuration. Recommend the night pass CLOSE this item (post-ship watch PASS). The attended session also dispositioned the 07-01 first-tick monitor candidates (anon-DML drift = defense-in-depth only / accept RLS-on; team-page timeout = 1.75s cold, pool-collateral, already 8s-guarded) — those are handled, do not re-queue from `2026-07-01T151603Z.md`.
- **Today's daytime CC wave (e0afec3 + 7 migrations + 8 indexes) — post-ship watch PASS (DB-side).** AllDay low-ask cron `rpc-allday-badge-low-ask-refresh` (jobid 31, */30, active, last run 00:00 succeeded, watchlisted); `badge_editions.low_ask` AllDay = 947 populated; all 8 `sales_20XX_edition_id_price_usd_idx` indisvalid+indisready; editions FLAT (no writer leak); security 0/0/0/0 (new AllDay-branch fns no anon violations).

## Watch (benign, not a candidate)

- **DB size 7,168 MB (+176 vs 07-01 08:05Z baseline 6,992).** Explained: today's 8 CONCURRENTLY sales-partition indexes (~80MB per the CC entry) + the AllDay `allday_studio_history_v1` studio backfill still filling (expected per focus.md — do NOT flag the backfill volume as anomaly). Benign; watch the rate.

## Health baseline (this tick)

- rpc_ops_snapshot: security 0/0/0/0; trust **15/15 ok** (breaches []); stalled_pipelines []; pipeline_alerts 1 INFO (ufc_sales resolving_editions, benign); sentinel TS-UUID-48h **0** (down from 17); editions FLAT TS 17,489 / AllDay 6,191 / Golazos 581 / UFC 518; FMV TS H+M ~4,691 (1305+3387) / AllDay ~895 / UFC 15 / Golazos 5; fmv_sanity 0; unmapped 29/100; DB 7,168 MB.
- check_pgcron_recent_failures(): []. 24h pipeline fails (14 pipelines, all transient) — every one verified latest-run ok=true within ~26 min (connection-pool contention during backfill wave).
- Vercel: no ERROR deploys; prod dpl_HLe4c (795d99b) READY. Runtime errors all known families (url.parse DEP0169, 300s after()-lambda timeouts, PostgREST connection-pool saturation [operator #4: raise db-pool], allday_corrected_ev DBSAT collateral [do NOT reopen], wmc idempotent dup-key, OG font/image edge cases, Flow 429) EXCEPT the new OG-BACKSLASH-500 above.
- Artifacts: 15 active, none broken. rpc-live-health consolidated payload validated end-to-end (all 12 insights boards return rows; FMV/PackEV/offers freshness current within minutes; leak_48h 0). Cross-collection cohort_mat 153 rows/20.1h + overlap 19.8h fresh; pack_ev_latest 1701/0.1h.

STEER honored: SERIAL-FMV weekly by design, evm-429 benign, DQ4 inert, studio-backfill volume expected. Declined not re-raised.
