# Daytime monitor inbox — 2026-07-04T06:06Z (~23:06 PDT tick, last daytime tick)

No clock skew (shell 06:06Z == DB now() 06:06:43Z == app-stamped sales 05:56Z / fmv 05:58Z). Genuine late-evening tick, NOT the 8am first-tick -> 1a daily extras (full trust read / cross-collection pg_cron verify) skipped by design; trust vector read directly (`rpc_ops_snapshot()` timed out inside `detect_stalled_pipelines()` — the 06:0xZ overnight-contention window — so the health components were run individually).

Platform **GREEN**. security **0/0/0/0** (invariants []/secdef []/rls_off {}/anon_write 0) · trust **15/16 ok** (1 BREACH = offer_edition_gap $70, known self-clearing transient — below) · `detect_stalled_pipelines()` **1** (topshot-fmv-populate single missed tick — below) · `check_pgcron_recent_failures()` **[]** · sentinel TS-UUID-48h **0** · editions FLAT (TS 17,490 / AllDay 6,191 / Golazos 581 / UFC 518) · FMV fresh (topshot_fmv_stale 0.4h, allday 0.3h, ufc 0.2h, golazos 0.1h, fmv_sanity 0) · Sentry **0 unresolved/24h** · DB **7,803 MB** (+37 vs 03:06Z — backfill growth decelerating) · prod **7fb01a60 READY** (HEAD 7a8c439f docs-only -> correctly CANCELED; no ERROR deploys). 2 new LOW candidates.

## Candidates

### 1. TOPSHOT-FMV-POPULATE-MISSED-TICK — LOW · watch · night-count 1
- **Source:** `detect_stalled_pipelines()` — topshot-fmv-populate last_run 2026-07-03 19:38Z, silent 632 min vs 480 min threshold (medium).
- **What:** Perfect 6h cadence at :38 (01:38/07:38/13:38/19:38 Z) for many days, all ok. It **missed the single 01:38Z Jul-4 tick** — NO pipeline_runs row at all (silent non-fire, not an ok=false), landing inside the heavy 01:0x–01:16Z overnight contention cluster (compute-topshot-pack-ev 01:01Z, wmc-fmv-populate 01:13Z, analytics-smoke 01:13Z, check-alerts 01:15Z, pinnacle-nft-resolver 01:16Z — all statement/pool timeouts that window). Next scheduled tick 07:38Z.
- **Risk read:** LOW. This is a supplementary GQL-catalog-sweep FMV populate (extra: sets_mapped 243 / nodes_fetched / upserted+skipped), NOT the main FMV writer. TS FMV is fresh regardless — topshot_fmv_stale_hours 0.4h GREEN, latest fmv computed_at 05:58Z, fmv_sanity 0. Blast radius = one skipped 6h new-edition FMV-population window, caught up by the next tick. Two candidate roots: (a) a one-off cron-job.org trigger miss, or (b) a contention-driven silent crash before log_pipeline_run in the 01:0xZ window (invisible-failure class). Shares the overnight-I/O-contention root with OVERNIGHT-0623Z-CONTENTION-CLUSTER.
- **Suggested action (night pass):** confirm the 07:38Z tick fires + logs ok=true (self-heal). If it ALSO misses 07:38Z -> graduates to a real stall (check the cron-job.org entry + the route's log_pipeline_run coverage on its timeout path). Do NOT ship anything on a single missed tick.

### 2. ALLDAY-PACK-OPENS-BACKFILL-404 — LOW · watch · night-count 1
- **Source:** pipeline_runs — allday-pack-opens-backfill 14 fail / 130 ok / 24h, sample_err "events 137378483-137378732 status 404"; last_ok 03:52Z, last_fail 06:12Z.
- **What:** The HISTORICAL pack-opens gap-fill is flapping ~10%, failing on a specific Flow access-node 404 for an event height range, concentrated since ~03:52Z. The FORWARD path `allday-pack-opens-forward` is 100% healthy (48 ok / 0 fail / 24h, last_ok 06:09Z) — live pack-open capture is unaffected.
- **Risk read:** LOW, not user-facing / not FMV. Flow upstream 404 (not DB contention), likely the backfill cursor reaching a pruned / dead-spork-edge event range (137378483–137378732). Historical enrichment only.
- **Suggested action (night pass):** characterize whether the cursor is STUCK re-hitting 137378483–137378732 or advancing past it; if permanently-404 (pruned/spork edge), fast-forward past it (mirror the pack-events cursor pattern). Low priority.

## Not re-raised (known / carried / dispositioned)
- **offer_edition_gap_max_usd $70 BREACH** — known self-clearing OFFER-SANITY transient (a fresh edition-grain on-chain offer not yet ratcheted into edition_offers). Offers pipelines all HEALTHY (offers-sweep 06:02Z 9/0, topshot-offers-indexer 06:12Z 9/0, allday-offers-indexer 06:07Z 9/0 — every ~20 min), so NOT a stalled sweep. Per the 06-25 ledger precedent this class "should NOT be treated as an incident" — clears on the next sweep ratchet. Not queued.
- **Overnight DB connection-pool contention (01:0xZ + 06:0xZ clusters)** — fmv-recalc (edition_page_fetch 30s timeout, 5/6h, last 06:08Z), wallet-username-resolver, topshot-buyer-backfill (pool), populate-pinnacle-wmc-fmv (upstream), classify-acquisitions-multicollection, lock-check-batch, compute-{topshot,allday}-pack-ev — all statement/pool/upstream timeouts in the two overnight backfill windows; all enrichment (retry next tick), FMV fresh, editions flat. = the tracked FMV-RECALC-EDITION-FETCH-TIMEOUT-CREEP + CLASSIFY-ACQ-ALLDAY-STATEMENT-TIMEOUT + OVERNIGHT-0623Z-CONTENTION-CLUSTER classes; root = the still-filling ts_history_backfill_v1 / allday_studio_history_v1 deep-history backfills on Pro Micro. Not re-logged.
- **DB growth 7,803 MB** — the tracked ts_history_backfill_v1-dominated drain (03:06Z DB-GROWTH-DRIVER), decelerating (+37/3h vs +208 prior). Benign; night pass owns the metrics-latest.json note.
- **def899f1 / perf-wave post-ship watch** — already carried by the 00:13Z + 03:06Z inbox candidates (pack/market/set/team perf, calibrated-merge + ipfs-media residuals). Not re-logged.
- **Positive recent-ship signal (for night-pass post-ship watch):** eb44ac04's pinnacle-nft-resolver 30s-index + wmc-fmv-populate lock fixes appear to be HOLDING — neither class reappeared in the 06:0xZ contention cluster that hit their neighbors (both last failed 01:1xZ, quiet through the 06:0xZ window). Night pass to formally confirm fail counts stay down.

## Artifact validation note
`list_artifacts` = 15 present (none missing at manifest level). Artifact HTML lives on a Windows OneDrive path NOT mounted into the sandbox this run, so per-artifact embedded payload queries were not individually re-run. Backing validated indirectly: security 0/0/0/0 + trust view healthy + today's CC wave was ADDITIVE (new matviews mv_pack_ev_latest / mv_topshot_set_play_catalog, new indexes, LISTED collection_id scope, Golazos marketplace flag — no view/RPC dropped or renamed), so no artifact payload query can have been broken; `check_pgcron_recent_failures()` [] confirms the new matview-refresh crons are healthy.
