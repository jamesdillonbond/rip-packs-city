# Daytime monitor inbox — 2026-06-21 ~03:06Z (2026-06-20 20:06 PDT tick)

Platform **GREEN**. Quiet evening post-ship-watch tick over today's heavy Stage B / Phase 3 / Phase 4 CC wave. **No new incident, 0 net-new candidates.** Everything below is post-ship-watch confirmation + carried-item status (nothing for the night pass to ship; all open items already queued).

security 0/0/0/0 (RLS-off base [], anon/auth-write-on-RLS-off [], `check_public_security_invariants()` [], `check_secdef_anon_execute_violations()` []); `detect_stalled_pipelines()` []; `get_pipeline_alerts()` = 1 INFO only (`golazos_sales` resolving_editions, 1/24h — long-standing, not a finding); **trust-health 9/9 ok**; sentinel `ts_uuid_dupes_created_24h` 22/200 (ok); Sentry **1 unresolved** (same transient as last tick, Note A); Vercel **0 ERROR**, prod `592e725` READY (latest `d119f4b` is the monitor's own inbox commit → correctly CANCELED). DB 5013 MB (+11 vs 5,002 last tick — benign).

**Editions reconcile EXACTLY** (TS 16,933 = 15,543 baseline + 1,390 `::` parallels; AllDay 6,191 / Golazos 581 / UFC 446 — flat). **FMV reconciles EXACTLY to those counts:** TS HIGH 1,147 + MED 2,951 = **HIGH+MED 4,098** (LOW 6,775 / NO_DATA 3,373 / ASK_ONLY 1,807 / STALE 861 / SALES_ONLY 19 = 16,933); AllDay HIGH+MED 805 (= 6,191). `fmv-recalc` last 03:08Z (2 min ago), **94 ok / 0 fail / 24h**, `fmv_sanity_flags` 0. (HIGH+MED dipped 4,231→4,098 / AllDay 874→805 since the 00:14Z tick = the benign intra-day reclassification cycle — reconciles exactly, writers fresh, buckets just reshuffle to LOW; today's TS HIGH+MED path 3,332→4,159→4,227→4,231→4,098 stays well above the prior-week overnight range. Known FMV-HIGHMED-DIP class, no action.)

---

## POST-SHIP WATCH — today's heavy CC wave remains healthy ~8h after the last commit; 0 reverts warranted

- **Phase-4 conflation-leak fix (`592e725`, subedition-aware history-backfill + durable remap) is HOLDING.** `topshot_conflated_editions` guard = **27 rows** (the converged Phase-4 state, `remaining_remappable_strands`=0) — NOT drifting up. `topshot-sales-history-backfill` **11 ok / 0 fail / 24h**, last 00:18Z.
- **Phase 3a circulation backfill** (`topshot-subedition-circulation-backfill`, daily Vercel cron 21:10 UTC) **3 ok / 0 fail**, last 22:05Z.
- **`topshot-buyer-backfill`** **47 ok / 0 fail / 24h**, last 02:34Z (BUYERBF-PERINVOCATION-WORK holding, no creep).
- **`allday-listing-serial-backfill`** (on-chain serial source, watchlisted) **8 ok / 0 fail / 24h**, last 00:34Z.
- All 11 artifact backing objects return rows (consolidated sweep, no schema break from today's add-column/Stage B/Phase 3 migrations): deals_board 655, serial_premiums 181, perfect_mint 11, trophies 683, top_sales 889, tracked_fmv 21, offer_sanity 240, underpriced_serials 12, allday_floor 3,911, special_serial_mv 6,774, squeeze 500+.

---

## CARRIED — status unchanged, already queued (no new action; not re-raising)

- **REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT** (queued, night-count 2). Status UNCHANGED from the 00:14Z tick: CC's fix is deployed (`3613a94` route maxDuration 120→240 + `audit_20260620_special_serial_owners_mv_refresh_timeout_200s`), the **MV data is current (6,774 rows)**, but there is STILL **no post-fix automated `refresh-special-serial-owners-mv` cron run** — last `pipeline_runs` entry is **06-20 02:16Z** (pre-fix, >25h ago), and the pipeline is **not on `pipeline_cadence_watchlist`** (silence invisible to `detect_stalled`). Night-pass watchlisting gate (one post-fix ok tick) **not yet met**. Board serves its current snapshot — LOW blast radius. Keep watching for the first post-fix automated fire.
- **`refresh-conflated-editions` cron — operator residual UNCHANGED.** Safe-DELETE fix is live (migration `20260620213921`, verified last tick), but the daily cron-job.org entry is still not wired: only route fire ever was the **06-20 15:17Z** pre-fix failure, none since. CLAUDE.md already lists "wire the daily refresh-conflated-editions cron" as an operator item; the follow-on (watchlist it after one ok run) stays premature until the cron exists. The guard sits at the converged 27 meanwhile (forward-keying + route-wired remap converge it). No night-pass action.

---

## NOTE — knowns / no action

- **Sentry `JAVASCRIPT-NEXTJS-A`** ("smoke test failed: fmv pipeline healthy / analytics_pipeline_health", `POST /api/smoke-test`) — **same single event ~21:10Z** the 00:14Z tick already logged (1 event, Seer super_low, 0 new since). FMV independently verified fresh (fmv-recalc 03:08Z, 0 fails/24h, `fmv_sanity_flags` 0, editions reconcile). Recurring smoke false-alarm class; resolve after 24h quiet.
- **pipeline_runs fails (6h):** `alerts-dispatch` ×1 @01:29Z ("deal: canceling statement due to statement timeout") — **isolated transient, 1/96 over 24h** (avg ok 12.6s / max 24.8s, well under the 90s deal-leg `statement_timeout`); known ALERTS-DISPATCH-DEAL-TIMEOUT class (closed 06-19), a single self-recovering spike is expected at 1 active sub, not an emerging pattern. `evm-transfers-ingest` ×1 @02:19Z (Base 429 — benign STEER item, don't chase). Nothing else.
- **`ts_uuid_dupes_created_24h` = 22/200 (ok):** known inert DQ4 dupe-writer leak (TS-WMC-UUID-FOSSILS/DUPE1); editions net-grew only by `::` parallels, not UUID dupes. No action.
- **First-tick-of-day extras (Section 1a) skipped** — this is the ~8pm PDT tick, not the first of the day. Trust-health was still run (9/9 ok); cross-collection pg_cron refresh verify deferred to the next first-tick.
- **Artifact estate = 16** (4 beyond the 06-12 consolidated list: `rpc-serial-premiums`, `rpc-fmv-dialin`, `rpc-ts-data-mission`, `pack-drops-ev-check` — already noted prior ticks). No `(RETIRED)` tombstones in scope.
