# Daytime monitor — 2026-07-18T21:06Z (~14:06 PDT)

Clock clean (shell 21:05:59Z ≈ DB now() 21:06:17Z ≈ newest sale 21:01Z ≈ newest FMV 21:02Z). No skew.
Lock RELEASED (night pass 15:40Z). Inbox was empty at start. origin/main `4c631412`, prod `dpl_Coy7ZDw1DT2TonVoHAL6JabpQGzV` READY.
Health GREEN: security 0/0/0/0 · trust 15/15 ok, breaches [] · pg_cron [] · sentinel 0 · DB 9,923 MB.
**Both items the night pass queued this morning are CLOSED**: `topshot_impossible_parallel_serials` 27 → **0**; `pinnacle_fmv_stale_hours` 29.3 → **5.2**.

---

## 1. HIGH — SEED-WALLET-12H-GATE-SILENTLY-KILLS-THE-GHA-WALLET-BACKFILL-BACKSTOP

**Source:** `4c631412` `app/api/seed-wallet-refresh/route.ts` (gate: skip when `utcHour % 12 >= 2`) vs `.github/workflows/wallet-backfill-backstop.yml` (`cron: "38 2,8,14,20 * * *"`, which calls `/api/seed-wallet-refresh?cohort=$K&of=4`).

**Finding (arithmetic, not inference):** the backstop's four daily hours are 2, 8, 14, 20. `2%12=2`, `8%12=8`, `14%12=2`, `20%12=8` — **all ≥ 2, so every one of the backstop's invocations now no-ops.** The GHA wallet-backfill backstop is structurally dead as of today's deploy. The gate was reasoned about against the 4 cron-job.org cohort entries; the GHA backstop calls the same gated route and was not considered (the ledger entry `f952df3f` and the `cron-schedule.md` warning block both cover the cohort entries only — 0 mentions of the backstop, watchlist, or `detect_stalled_pipelines`).

**Blast radius:** the backstop exists specifically to cover **cron-job.org trigger dropout** — the platform's documented recurring failure class (07-11, 07-07, 06-09, 06-08 …), and one is **live right now** (`pinnacle-sync` 35h silent, same trigger source). Post-gate, a dropout on the seed-wallet cohorts leaves the platform's **largest ingest path with zero redundancy**. Nothing is broken today; the safety net is.

**Risk read:** LOW-risk fix, zero impact on the cost saving (the backstop only does work when the primary already failed to).

**Suggested action (preferred):** teach the gate an explicit bypass and have the backstop use it — `&force=1` (or reuse the existing `SEED_WALLET_REFRESH_EVERY_WAVE` semantics as a query param) checked alongside the env escape hatch, plus the one-line workflow URL change. Alternative if a code change is unwanted: move the backstop cron to `38 0,12 * * *` — cheaper, but it then runs inside the same wave as the primary and loses the "cover a dropout hours later" property, which is the whole point. Recommend the bypass.

---

## 2. MEDIUM / MEASURE-FIRST — WALLET-BACKFILL STALL THRESHOLDS vs THE NEW 12h CADENCE

**Source:** `pipeline_cadence_watchlist` (6 rows at `max_silent_minutes=420`, severity **high**, notes read "6-hour wave at 00/06/12/18 UTC") vs the same `4c631412` gate.

**Measured now (pre-gate):** max inter-run gap over 72h = **241 min** for all 7 `wallet-backfill*` pipelines (avg 2–3 min); headroom under the 420-min threshold = 179 min.

**Projection:** post-gate only the hour-0/1 and 12/13 waves execute. If seed-wallet-refresh is the dominant driver, the natural gap widens toward ~10–11h → **breaches the 420-min HIGH threshold twice daily across 6 pipelines**, paging through `get_pipeline_alerts()` → Telegram/email and making every health sweep look broken.

**Honest caveat — do not pre-raise blindly:** runs were observed in non-wave hours (5/10/15/21 UTC, and the largest batches at that), so a second driver exists and the post-gate gap may cap below 420 on its own. The ledger's own target metric ("`wallet-backfill*` runs/24h ≈ halves") is the same signal.

**Suggested action:** after a full post-gate day (07-19), re-run the 72h max-gap query; **if** max gap > 420, raise the 6 rows to ~800 to match `wallet-backfill-multicollection-complete` (DB-only, one UPDATE, trivially reversible). If it stays under, close this and correct the stale "6-hour wave" notes text only.

---

## 3. MEDIUM / POST-SHIP WATCH — CONNECTION-POOL EXHAUSTION IS NOW USER-FACING

**Source:** Sentry **JAVASCRIPT-NEXTJS-1Z** (new, unresolved, 2 events / 1 user, first+last seen ~16:06Z): `Error: pack detail bundle unavailable: Timed out acquiring connection from connection pool`, culprit `GET /[collection]/pack/dist/[distId]`.

**Corroborating:** `fmv-recalc` 5 fails/24h (15:39Z *"Timed out acquiring connection from connection pool"*, 19:28Z + 13:28Z statement timeout / saturation-class) and `compute-topshot-pack-ev` 8 fails/24h (all `targets: canceling statement due to statement timeout`). The failures **cluster on the wallet-backfill wave hours** — 19:28Z fail vs the 19:27Z wave, 13:28Z vs the 13:00 wave. This is independent empirical support for today's cost-lever thesis (the fan-out is the #1 pool/IOPS driver), and it is now reaching real users, not just pipelines.

**Suggested action:** treat as the **post-ship watch target for the 12h gate**. Expect this Sentry class and the `targets` / `edition_page_fetch` timeout counts to fall over 24–48h. If they do **not**, the pool-contention attribution needs revisiting before more cadence is cut. No action to ship now.

---

## 4. LOW / CARRIED — PINNACLE-SYNC 35h SILENT (catalog half unbackstopped)

`pinnacle-sync` last ran 2026-07-17 10:07Z — **2,099 min silent** vs its 1,560 threshold; it missed the 07-18 10:07Z tick. Known cron-job.org dropout class. The **FMV** consequence was closed today (pg_cron backstop; `pinnacle_fmv_stale_hours` 5.2/30 healthy), but the **catalog** half of that sync (new renders/editions) has no equivalent backstop.

**Suggested action:** watch the 07-19 10:07Z tick. Two consecutive misses = the cron-job.org entry is genuinely dropped or auto-disabled → operator (console is operator-only).

---

## Validation notes
- **Artifacts:** 17 in the manifest; dependency check on the merged `rpc-live-health` payload resolved **24/24 objects present** in `public` (insights views, boards, MVs, partitioned tables) — **zero schema drift** from today's heavy DDL wave (funnel `collection_view`, insights_hub single-scan, Panini `security_invoker`, `get_allday_market_editions`). No artifact repair needed.
- **Vercel:** no ERROR-state deployments; several CANCELED are supersedes from rapid concurrent pushes (normal for today's interactive wave).
- **Not re-raised:** `golazos_sales` / `ufc_sales` `resolving_editions` INFO alerts (benign, standing).
