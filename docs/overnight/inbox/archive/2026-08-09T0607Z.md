# Inbox — 2026-08-09T06:07Z (daytime monitor, 11:07 PM PT Aug 8, last tick of day)

⚠ **inbox written to MOUNT, push unavailable** — the workspace shell is down again (`useradd ... no space left on device`, the `/sessions` no-space class that killed the 08-08 overnight pass and was noted in the 0011Z inbox). No sandbox clone/push; written directly to the mounted tree for the night pass to pick up locally. No git run against the mounted `.git`.

## Health sweep summary (all read-only)
- `rpc_ops_snapshot()`: security clean (invariants / anon-write / rls-off / secdef-anon all `[]`); `detect_stalled_pipelines()` = `[]`; DB 12,158 MB.
- Vercel: no deploys in ERROR. Latest READY prod = `991291be` (saved_wallets cached-count fix). The CANCELED rows are normal superseded-by-next-push builds.
- Sentry: not separately re-queried this tick; Vercel runtime-error picture unchanged from the 0011Z saturation wave (same public-board timeout signature).
- Trust health: **2 BREACH, both already known/tracked** — `panini_sale_price_capture_dry_days`=11 (upstream Panini home-box outage, climbing 8→11) and `unmapped_resolution_backlog_max`=162 (AllDay backlog; down from 175 at 0011Z, draining). `public_board_slow_count` is back to 0. UFC red-by-design remains gone (repointed to `ufc_flow_revival_sales_30d`=0/ok).
- FMV HIGH+MED: TS 7,566 · AllDay 1,789 · Golazos 3 · Candy (denorm). Editions/DB stable.

## Nothing new to queue beyond ONE fresh saturation-cluster data point (below). Everything else confirmed already queued — NOT re-logging:
- **Disk-IO saturation cluster** (Small-tier disk-IO budget) — the pg_cron MV-refresh timeouts + public `/insights` board degradation + `lock-check-batch` / `wallet-username-resolver` statement-timeouts are all in inbox 2026-08-09T0011Z, 2026-08-08T1945Z/1717Z/1443Z, and the 2026-08-05 pass. Mitigation (drain the MV cluster; do NOT bump statement-timeouts; do NOT upgrade the tier) already recorded there.
- **`allday-lock-refresh`** (was 57.4% fail / statement-timeout in the 2-day snapshot) — **FIXED today**, `babdc1a1` (O(rows)→O(wallets) picker) + `e9d08618` (partial `idx_wmc_allday_lock_picker`), picker 1.5s→69 ms. The 2-day failure rate is pre-fix runs; expect it to clear on the next ticks. Do not re-queue.
- **`sync-nba-projections`** (15/15 fail, `all_upstreams_failed`) — operator secret drift (`SPORTS_PROXY_SECRET` ≠ worker `PROXY_SECRET`) + staged v9, fully specified in inbox 2026-08-08T2130Z §1–2. Operator-gated.
- **`topshot-active-listings-ingest`** (intermittent `egress_blocked`, ~half of ticks; `:13`-past GHA runs succeed ~every 6h so listings stay refreshed) — dispositioned in inbox 2026-08-08T2130Z §3 (deliberate circuit-breaker, GHA-runner IP throttling, self-healing; Vercel-cron-backstop is the lever, a scheduling call not a defect). Do not re-queue.
- **`candy-offers-indexer`** 700s-deadline skips — trust-health green (`candy_offers_oldest_active_hours`=5.3, `unverified_pct`=0), graceful degradation; only the 08-07 700s tick was the real one (2130Z "Not touched").

## The one fresh data point: `rpc-ccm-step1` timed out today → `cross_collection_cohort_mat` is now 26h stale
- Source: `check_pgcron_recent_failures()` — `rpc-ccm-step1` failed `2026-08-09 04:10Z` with `canceling statement due to statement timeout` on the `INSERT INTO cross_collection_cohort_mat` (the daily cross-collection cohort refresh). Also in that list, both **already-queued** and **stale-pre-fix**: `rpc-refresh-misattrib-candidates` (08-08 15:35Z) and `rpc-thin-sale-ask-disclosure-refresh` (08-08 09:25Z).
- Verified impact: `cross_collection_cohort_mat` newest `computed_at` = 2026-08-08 04:10Z = **26.0h old** (one daily tick missed); step2's `cross_collection_ts_set_overlap_mat` is **fresh at 1.7h** (04:25Z succeeded). So step1 alone missed.
- **This is the SAME disk-IO saturation root cause already queued — NOT a new defect.** Logging only because `rpc-ccm-step1` was not explicitly named in the existing cluster list and it has a distinct, low-impact consequence: the `rpc-cross-collection` artifact + the concierge's cross-collection reads render ~26h-old cohort data until the next clean 04:10Z tick. Do not double-count against the saturation queue.
- Risk read: LOW / self-recovering (idempotent daily catch-up on the next tick, provided the 04:10Z window isn't saturated again). No action needed beyond the already-queued cluster drain; if the night pass wants belt-and-suspenders it could stagger `rpc-ccm-step1` off the 04:xx UTC pileup, but that is config-only and secondary to draining the MV cluster that starves the IO budget.

## Artifacts (Section 1b) — enumerated only, deep payload execution deliberately DEFERRED this tick
- `list_artifacts` returned 11 active artifacts, all present (rpc-live-health, rpc-qa-scorecard, rpc-rewards-console, rpc-tracked-fmv-confidence, rpc-traction, rpc-deploys-and-cost, rpc-my-wallet, rpc-pack-lifecycle, rpc-set-challenge-roi, candy-chain-two-onboarding-v2, rpc-panini-squeeze-v2). No RETIRED tombstones surfaced.
- Payload queries were NOT run this tick: an active disk-IO saturation episode is in progress (public `/insights` boards + `rpc_ops_snapshot`'s confidence-rows scan were timing out at 0011Z), and running 11 heavy artifact payloads would add read load to an already-throttled DB and most likely time out for saturation reasons rather than reveal an artifact defect. No schema-breaking change shipped today (today's ships: saved_wallets fix, allday-lock picker + partial index, golazos wallet-search, profile/me test, odds-proxy /scores, allday locked-moment fix, brand-guard CI unblock — none dropped or renamed a view/column an artifact reads), so the schema-drift risk these validations guard against is absent this tick. Flagging the deferral honestly rather than piling load on a saturated DB.
