# Daytime monitor candidates — 2026-07-18T0007Z (evening tick, ~17:07 PDT Jul 17)

Health GREEN: security 0/0/0/0, trust 15/15 ok (0 breaches; impossible_parallel 2/3 self-healing, edition_integrity 5/50, unmapped 24/100), stalled_pipelines [], pg_cron failures [], sentinel 22 (<200), DB 9,784 MB (+71 benign vs the 2113Z tick), Vercel prod `3541d3d7` READY (Trevor's Pinnacle header-chrome fix; the many CANCELED deploys are the normal ignored-build-step cancels from rapid Cowork/Panini pushes — 0 ERROR-state). Sentry: 1 unresolved only (NEXTJS-1C, recovering — see Watch). Candy Drop-1 launched today; candy activity is EXPECTED, not an anomaly.

## NEW candidate

### DRAIN-CONFLATED-SUBEDITIONS-FINAL-STEP-TIMEOUT (LOW · CC/owner) — NEW
- **Source:** pipeline `drain-conflated-subeditions`, latest run 2026-07-17T20:30:48Z ok=false `canceling statement due to statement timeout`; the prior visible run (2026-07-14T20:30Z) failed identically. Daily 20:30Z TS subedition de-conflation orchestrator (`app/api/admin/drain-conflated-subeditions/route.ts`).
- **Read:** NOT a no-op failure — the run's `extra` shows the substantive de-conflation work COMMITTED before the timeout (07-17: knots_resolved 5, wmc_split 329 / sales_split 6 / moments_split 6, moments_realigned 99; 07-14: wmc_split 309 / sales_split 313, seeded 2023). A late orchestrator step (guard re-measure / trailing seed/realign) exceeds statement_timeout AFTER the real work lands, so the pipeline perpetually logs ok=false while de-conflation still progresses. NO trust breach: edition_integrity_flags 5/50 ok, topshot_impossible_parallel_serials 2/3 ok, sentinel 22 — working set not visibly growing; no user-facing impact (background subedition data-hygiene).
- **Risk:** LOW blast radius. Cost of leaving it: (a) permanent ok=false noise that can mask a real drain failure, and (b) whichever late step times out never completes, slowing convergence of the conflation working set. 20:30Z sits in the evening heavy-contention window → likely the same statement-timeout-under-IOPS class CC cleared repeatedly this week, not a logic regression.
- **Suggested action (night pass / CC):** inspect the route, identify the timing-out tail step (the `extra` truncates after the realign block → it's the knot/guard-remeasure tail) and either raise that step's statement_timeout, cut its per-tick batch, or move the 20:30Z cron off the contention window. Data-hygiene route re-keys wmc/moments/sales editions — night pass should weigh a statement_timeout raise as in-scope vs. queue for an owner. Not urgent; no trust/user impact.

## Watch / no-action (recovering or known)
- **CANDY-EDITIONS-INGEST-DAS-401 (raised HIGH in the 2113Z inbox) — RESOLVED.** Latest `candy-editions-ingest` run ok=true 46 min ago; the ledger's later 07-17 Candy entries reconciled the Vercel<->helius-proxy `HELIUS_PROXY_SECRET` mismatch and ran the ingest clean (27,876 assets -> 125 editions / 25,375 serials). candy_mlb stays is_active=false (pre-launch, Trevor's call). Do NOT re-flag.
- **topshot-atlas-pool-ingest** — 177 fails/24h are all pre-fix history; latest run ok=true (v5 `atlas_empty` reclassification, resolved same-day per the 2113Z inbox). Not user-facing.
- **lock-check-batch** — 7 fails/24h but latest run ok=true 30 min ago; CC's SKIP-LOCKED row-claim write-back (07-17) is holding. Self-recovered.
- **Normal contention singles** (fmv-recalc 3, compute-topshot-pack-ev 3, wallet-username-resolver 2, + hybrid_custody / populate-pinnacle-wmc-fmv / run-insider-detectors / topshot-deal-floor-serials 1 each) — all latest-ok; stalled_pipelines [] confirms none silently stopped.
- **topshot-moments-hydrator** — 9 fails/24h, known upstream `GetMintedMoment` GQL-error class (self-limiting, latest run ok=true). Carried, no action.
- **Sentry NEXTJS-1C** "smoke test failed: public base tables: RLS on + no anon write" — 3 events, last seen ~8h ago, none since; security invariants live-clean 0/0/0/0. Recovering tail of CC's atlas-targets-view security_invoker restore (noted 2113Z). No action.

## Run limitation
- **Artifact payload-query validation SKIPPED** (same as the 2113Z run) — artifact HTML lives on the Windows `C:\Users\TDill\Claude\Artifacts` path, not mounted here (only rip-packs-city / outputs / uploads). Validated BACKING data via the sweep instead: trust view returns 15 rows cleanly; `fmv_by_collection`, `rpc_ops_snapshot`, pack_table_rows/mv_pack_ev_latest (additive typical_ev) all healthy; no artifact-breaking table/view DROP shipped today. NEW artifact `rpc-panini-squeeze` created today (Trevor's Panini build) — not validated (unreadable + panini_* is a pre-launch surface). Full artifact pass wants a session with artifact-file access.
