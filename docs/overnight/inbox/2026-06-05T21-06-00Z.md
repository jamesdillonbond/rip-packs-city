# Daytime monitor candidates — 2026-06-05T21:06Z

Run: `rpc-daytime-monitor` (read-only). No bot clone → shared repo. Lock present but a 750m-old RELEASED marker from the 08:35Z night pass (not fresh) → proceeded normally, inbox commit permitted. No `FREEZE.md`, no `focus.md`. Prior daytime inbox `2026-06-05T18-25-00Z.md` already drained-pending (its P1 not re-logged here). Platform health **green except the two already-tracked items** (DUPE1 sentinel CRITICAL + the prior run's broken `rpc-insights-health` artifact). **One NEW candidate** (an unwired refresh cron shipped today).

---

## P1 — [LOW · operator/CC · NOT night-pass-auto-shippable (external cron, same class as L1/N1)] `refresh-cross-collection` cron route shipped today but its cron-job.org trigger is unwired

- **Title:** `/api/cron/refresh-cross-collection` (deployed today) has **zero `pipeline_runs`** — the durable server-side refresh of the `/insights/cross-collection` backing tables exists but is dark; the boards refresh only via the interim **desktop-only** Cowork scheduled task.
- **Source:** deploy `dpl_79vB2XuNwFFhW8HNfFxMzatUxgcd` (commit `deb786ea`, ~17:28Z today). `SELECT … FROM pipeline_runs WHERE pipeline='refresh-cross-collection'` → `[]` (never fired). The commit message's own closing line: *"Operator follow-up: add a daily cron-job.org entry hitting …/api/cron/refresh-cross-collection with the bearer token, then retire the interim Cowork task."* Not yet in `ledger.md`.
- **Likely cause / state:** route is live and logs to `pipeline_runs` like the other ~23 pipelines, but no cron-job.org entry hits it yet. The backing tables are **currently populated** (`cross_collection_cohort_mat` = 144 rows this run; `rpc-cross-collection` artifact validated healthy), so this is **not urgent** — it's a durability gap: whenever Trevor's desktop app is closed, the interim Cowork task can't fire and the boards silently drift stale while the just-built server-side refresh sits unused.
- **Blast radius:** `/insights/cross-collection` freshness only; no FMV/ingest/auth/pricing. Currently masked because the table is fresh.
- **Risk read:** LOW. The fix is an external-cron wiring action (operator-only, off-limits to the passes — exactly like L1 `league-drift-detection` and N1) plus an optional interim-task retirement.
- **Suggested action:** (operator/Trevor) add a daily cron-job.org entry `GET/POST https://www.rippackscity.com/api/cron/refresh-cross-collection` with `Authorization: Bearer $INGEST_SECRET_TOKEN` (or `?token=`), then retire the interim Cowork task. **Then** (night-pass-eligible, additive/reversible monitoring-config) add a generous `pipeline_cadence_watchlist` row so `detect_stalled_pipelines()` covers it — but NOT before it's firing (a watchlist row on a never-run pipeline would immediately false-positive, per the L1 lesson). Record the cadence in `docs/operations/cron-schedule.md`.

---

## Health snapshot (for night-pass context — NOT new candidates)

- `detect_stalled_pipelines()` = `[]`. Security **0/0** base tables (RLS-off + anon/auth-write checks, `relkind in (r,p)`). No new deploy ERRORs — the only 2 ERROR in 24h are the **known transient `8f3fff9` pair** (pack-reality honesty filter; code live via descendant READY deploys). Latest prod `69d5c6e` READY (~20:24Z).
- **DUPE1 (already queued):** sentinel TS-UUID-48h **4,758** (08:15Z baseline 2,611 → 18:15Z 4,393 → now 4,758), still CRITICAL but **decelerating** — ~128/hr over the last ~2.85h (was ~186/hr at 18:15Z, ~450/hr overnight). ALL inert (`set_id_onchain` NULL, trigger-held). TS editions 13,905 (the re-mint). TS NO_DATA 4,604 (08:15Z 2,010 → now 4,604) is the documented inert-re-mint NO_DATA tail, **not a pricing regression** — TS HIGH+MED **3,043** (2,944 → 3,037 → 3,043, still improving). Durable fix remains off-limits worker code (Item B2); do NOT raise the threshold (true positive).
- **`rpc-insights-health` artifact (already logged P1 in the 18:25Z inbox — NOT re-logged):** still broken — `count(*) FROM topshot_squeeze_board` (3,136 rows) exceeds the default ~8s timeout (completes only with a raised `statement_timeout`). DUPE1's edition growth keeps making it worse. **Confirmed NOT user-facing:** the public SSR `/insights/squeeze` board's bounded default query (`WHERE squeeze_pct >= 50 ORDER BY squeeze_pct DESC LIMIT 100`) ran in **39ms** (EXPLAIN ANALYZE) this run — today's A1 server-render ship is unregressed by the edition growth. Night-pass fix per AF1 precedent: optimize the view or cheapen the artifact's liveness leg.
- **Recent-ship watch — PIN-CAT (Pinnacle on-chain catalog create/repair, commits `373967d`+`69d5c6e`, ~20:22Z today, Claude Code):** `pinnacle-metadata-backfill` ok=true at 20:22 / 19:22 / 18:22Z. The 20:22:10Z run fired ~as the deploy went READY, so the **first fully-on-new-code run is ~21:22Z** — night pass / next monitor should confirm it clean and that the new Q4 `pinnacle_editions` upsert queue isn't erroring. No regression signal so far.
- **Sentry:** 8 unresolved, **all the documented overnight smoke-test cluster** (NEXTJS-1E/1C/12/A/4/14/13 = `POST /api/smoke-test`, first seen 15-17h ago; + 1G `/share/[wallet]/opengraph-image`). **Last seen 14-19h ago — zero recurrence in the last 6h, nothing new or spiking.** Markable resolved after 24h quiet (~07:00Z 06-06) per the baseline.
- **Pipeline fails last 6h (all 1-2 each, transient/recovering, known classes):** `pinnacle-nft-resolver` ×2 (statement timeout, last 18:17Z — pre-dates the 20:22Z PIN-CAT ship, not attributable), `pack-events-ingest` ×1 (`topshot_pack_opens` op timeout, 20:54Z), `wmc-fmv-populate` ×1 (lock timeout, 18:21Z), `compute-topshot-pack-ev` ×1 (targets statement timeout, 18:08Z), `topshot-fmv-populate` ×1 (connection-pool timeout, 18:00Z — the documented ~30%-of-runs class). N2 hydrator `candidate_read` timeout did NOT recur in the last 6h (last was 12:22Z, now >6h).
- **Drift vs 08:15Z baseline:** AllDay FMV flat (HIGH+MED 489 vs 495); editions AllDay 6,191 / Golazos 581 / UFC 446 all flat; unmapped_sales 209 → 237 (flat since 18:25Z); DB 5,978 → **6,008 MB** (DUPE1 + heavy daytime shipping). All normal.
- **Artifacts:** 16 total. Spot-validated the schema-adjacent ones live (rewards `v_rewards_economy` 1 row; `cross_collection_cohort_mat` 144; `edition_offers` 2,094 fresh in 30m; AF1 fmv views fast). `rpc-insights-health` remains the lone break (above). Others healthy at the 18:25Z full pass with no backing-schema change since.
- Note (not new): shared-tree git identity is still `rpc-daytime-monitor <monitor@rippackscity.com>`, so today's Claude Code feature pushes are author-attributed to the monitor — symptom of the unmounted bot clone (**Q7**), not a new finding.
