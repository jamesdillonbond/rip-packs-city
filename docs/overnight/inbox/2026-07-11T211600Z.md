# Daytime monitor — 2026-07-11T21:16Z (mid-afternoon tick, ~14:16 PDT)

**LEAD: origin/main is WIPED — a pushed commit deleted the entire repo tree. Live site UNAFFECTED (Vercel still serving last READY deploy). Active Trevor/Cowork session in flight.** Everything else GREEN, and the 15:10Z CRONJOB-ORG dropout has fully self-healed.

No clock skew (shell 21:06Z ≈ DB now() 21:14Z; sales/fmv pipelines fresh 1–13m). Security 0/0/0/0 (invariants [] · secdef_anon [] · rls_off 0 · sentinel TS-UUID-48h 0). Sentry 0 new/24h.

## CRITICAL — ORIGIN-MAIN-TREE-WIPE-20260711 (repo destroyed on origin; operator/CC, do NOT auto-ship a fix)
- **Source:** Vercel `list_deployments` (3 consecutive prod ERROR deploys) + fresh sandbox clone of origin/main.
- **What:** `origin/main` HEAD `1c51d088` tracks **1 file** (`.github/workflows/.gitkeep`). The last-good commit `803455f6` (the last READY prod deploy) tracks **2,118 files**. Commit `ac138b0c` ("fix(seo): hand-roll the sitemap children …") is the destroyer — its diff DELETES the entire tree (app/, lib/, components/, docs/, CLAUDE.md, .github/workflows/*, __tests__/, everything), despite a commit message describing only a 3-file sitemap change. `1c51d088` then added the lone `.gitkeep`.
- **Build failure chain (all prod ERROR):** `dff6a7f` (split sitemap via generateSitemaps) → ERROR "Conflicting route and metadata at /sitemap.xml"; `ac138b0c` (attempted fix) → ERROR; `1c51d08` (placeholder) → ERROR **"Couldn't find any `pages` or `app` directory"** (literally true — the pushed tree has no app/).
- **How it happened (near-certain):** the classic phantom-corruption / empty-checkout `git add -A` class the monitor + CLAUDE.md warn about — the wipe was pushed from a DIFFERENT clone than the mounted one. The MOUNTED working tree is fully intact (app/ lib/ components/ docs/ all present) and its local `.git` is still at `dff6a7f` (full tree), so **no working files were lost** — the damage is entirely in what got committed+pushed to origin.
- **Blast radius:** live site is FINE — Vercel serves the last READY deploy `803455f6` (`dpl_9h5KvNajUmPFTWmLDg9rypCwauPH`), and DB/pipelines are healthy. But main will not build and every push deploys broken until main is restored. Risk of a wrong-direction force-push during recovery.
- **Recovery (operator / Trevor — a real force-push, NOT autonomous):** from a clone with the good tree, `git reset --hard 803455f6` then `git push --force origin main` (803455f6 = last READY, full 2,118-file tree). Or from the mounted local at `dff6a7f` (also full tree; still has the unresolved sitemap-conflict build error, so 803455f6 is the cleaner base). After restore, redo the sitemap-segmentation work carefully — the `dff6a7f` generateSitemaps approach was itself failing on the `/sitemap.xml` route/metadata conflict, so that SEO change is still unshipped.
- **Note for the night pass:** if this is still unresolved at 01:00Z, main cannot build — restoring origin/main to a good tree is prerequisite to any other ship. This is a force-push recovery (off the normal auto-ship path); surface to Trevor rather than auto-executing unless the ledger shows it already handled.

## RESOLVED since 15:10Z — CRONJOB-ORG-TRIGGER-DROPOUT-20260711 self-healed
- The 15:10Z escalation (frozen subset since ~05:2x–05:43Z) has fully recovered. `detect_stalled_pipelines()` = []; previously-frozen pipelines all current: wmc-fmv-populate 1m ago, snapshot-pack-asks 2m, pinnacle-events-ingest 11m, pinnacle-listings-reconcile 21m, golazos/allday listings ~8m. The downstream `pinnacle_ask_stale` breach clears with pinnacle-events-ingest fresh. Cursor-based ⇒ no data loss, as predicted. Recurring class (07-07/06-09/05-31) — no new action.

## Carried / known (no new action)
- pg_cron `check_pgcron_recent_failures()`: 4 jobs, all DAYTIME-CONTENTION family in the 21:00Z window — `rpc-allday-cross-source-sales-dedup` (5 fails, statement timeout), `rpc-remap-misattributed-sales` (2, statement timeout), + 2× `job startup timeout` transients (allday-resolve-pull-editions, allday-pack-opens-forward). Known, self-recovers.
- `pack-opens-api-backfill-allday` 17 fails/6h + `-topshot` 4/6h — Trevor's ACTIVE pack-opens backfill work (af22f19a shipped ~1h ago, deploy READY). NOTE for whoever's on it: the fix edits `supabase/functions/ingest-allday-pack-opens/index.ts` (an EDGE function) — confirm it was `deploy_edge_function`'d, not just merged, or the old wedged code keeps running. Trevor's in-flight area, not a monitor candidate.
- topshot-moments-hydrator 9/6h (TOPSHOT-MOMENTS-HYDRATOR-GETMINTEDMOMENT-ERRORS, self-limiting), analytics-smoke 5 (false-fail/contention), lock-check-batch/wallet-username-resolver/buyer-backfill small counts — all carried.

## Drift (headline)
- editions TS **19,210** (+84 since 15:10Z = ongoing `::` subedition cataloging, sentinel 0 confirms no UUID leak) / AllDay 6,190 / Golazos 575 / UFC 518.
- FMV TS H+M **5,137** (−100 from 5,237 at 15:10Z — minor churn; WATCH if it keeps sliding).
- DB **~11 GB** (up from 9,563 MB at 15:10Z — correlates with Trevor's new TopShot + AllDay historical pack-opens backfills now filling; benign, WATCH the growth tail).
