# Daytime monitor — 2026-06-24T18:06Z (2nd tick of day, ~11:06 PDT)

Platform GREEN **except one NEW security finding** (below). DB now() 18:08Z AGREES with the sandbox clock this run (no ~5.5h skew). Prior tick = 15:14Z (first-of-day, GREEN, logged PINNACLE-EDITION-KEY-UUID-CAST). Lock RELEASED. §1a first-tick extras SKIPPED (already ran 15:14Z: cross-collection mats fresh + trust scan done).

## NEW finding — UFC-VIDEO-BACKFILL-AUDIT-TABLE-RLS (security invariant breach · MED · CC/night-pass · one-migration harden)
`public.audit_20260624_ufc_video_backfill` (518 rows = one per UFC edition; the per-edition backup table from today's live UFC video-recovery Cowork migration) shipped with **RLS OFF + INSERT/UPDATE/DELETE/TRUNCATE granted to `authenticated`**. It is the ONLY RLS-off public base table this run — `check_public_security_invariants()` flags it twice (kind=rls_off_base_table + kind=anon_write_base_table); the catalog checks confirm `rls_off_base=[audit_20260624_ufc_video_backfill]` and 4 authenticated DML grants on it. Appeared AFTER the 15:14Z tick (which read security 0/0/0/0), so it landed ~08:14–11:06 PDT.
- Source: catalog SQL (pg_tables.rowsecurity=false + information_schema.role_table_grants) + `check_public_security_invariants()`. The 4 SIBLING video/media-recovery backup tables from the same 06-23/06-24 wave (`audit_20260624_allday_video_backfill_v2`, `audit_20260624_golazos_video_backfill`, `audit_20260623_ts_thumb_ipfs_backfill`, `audit_20260623_ts_video_ipfs_backfill`) ALL have rls=true — so this is an isolated hardening MISS on the UFC table, not a systemic pattern.
- Risk read: MED but LOW blast radius. It's a transient per-edition backup/scratch table (holds old+new video_url for the UFC video-recovery revert), NOT a reader-facing data source; writes require the allow-list-gated `authenticated` role. Worst realistic case = an authenticated user TRUNCATEs/edits the backup → loses the UFC-video-recovery revert path. No user-facing / FMV / data-integrity / board impact. BUT it is a genuine security-invariant breach and exactly the class the night pass has hardened before (AUDIT-TABLE-RLS-20260621, SEC1-EXP).
- Suggested action (night pass — mirror the sibling tables' posture): `ALTER TABLE public.audit_20260624_ufc_video_backfill ENABLE ROW LEVEL SECURITY; REVOKE INSERT, UPDATE, DELETE, TRUNCATE ON public.audit_20260624_ufc_video_backfill FROM anon, authenticated;` then re-verify `check_public_security_invariants()` returns clean. Revert: `DISABLE ROW LEVEL SECURITY` + re-GRANT (not advisable).

## Health summary
- `detect_stalled_pipelines()` [] · `check_pgcron_recent_failures()` [] · `get_pipeline_alerts()` 1 INFO (ufc_sales resolving_editions, benign/long-standing)
- pipeline_runs ok=false last 6h: **0** (cleanest in days — not even the usual transient 429s in-window)
- security: **1 RLS-off base table = audit_20260624_ufc_video_backfill (NEW, above)**; `check_secdef_anon_execute_violations()` [] · `check_public_security_invariants()` flags ONLY that table
- sentinel TS-UUID-48h **0** (no writer leak; ts_uuid_dupes_created_24h 0/200) · trust-health **8/9** (1 BREACH = the already-queued HISTORY-BACKFILL-UNMAPPED-SPIKE, trajectory below)
- editions FLAT (TS 17318 / AllDay 6191 / Golazos 581 / UFC 518) · FMV TS HIGH+MED **4420** (improving: 4399 nightly / 4413 @15:14Z) / AllDay **898** (flat) — both reconcile to edition counts (sum gap 2/1 = benign `::` parallels), fmv_sanity 0 · conflation 63 (benign oscillation)
- DB 5260 MB (+25 over ~4.5h, benign creep) · Vercel **0 ERROR** across 20 recent (prod 1e5300fe READY) · Sentry **1** unresolved (NEXTJS-X, single-event smoke transient, below)
- Artifacts: all 12 flagship insights backing relations EXIST + 8 probed boards return rows (squeeze/deals/scarcity/serial-premiums/offer-sanity/underpriced/rewards/tracked-fmv); today's UFC video migration touched no view/RPC, so no artifact drift. **None broken, none repaired.**

## Known BREACH trajectory — ALREADY QUEUED, NOT re-logging
**HISTORY-BACKFILL-UNMAPPED-SPIKE** (ledger l.675): `unmapped_resolution_backlog_max` **996/100** (was 725 nightly / 725 @15:14Z) — climbing each `topshot-flowty-sales-history-backfill` #3 */3h tick exactly as predicted. Open breakdown: TS **996** (flowty/source=onchain, all the #3 backfill; historical sold_at now walked down to 03-30/03-31), AllDay **209** (131 flowty/onchain + 78 nflallday/onchain_dapper_v1), Golazos 16, UFC 2 = **1223 total open**. **NOT corruption — rows quarantined OUT of `sales`** (no board/FMV impact). Night pass owns the fix decision (resolver-coverage / retire-mechanism / pace-cron). **HISTORY-BACKFILL-WATCHLIST** (ledger l.676) cadence gate now banking more ticks.

## Sentry note (not a candidate)
NEXTJS-X "smoke test failed: public page /laliga-golazos/analytics returns 200" — 1 user / 1 event, ~16:08Z, super_low actionability, culprit POST /api/smoke-test. Single-event smoke transient (the known checkUrl per-fetch-budget flake class; 0 unresolved before this). Watch only; resolvable after 24h quiet.

## Post-ship watch handoff (for night pass — new since the nightly baseline)
Prod advanced 00a0330 -> **1e5300fe** (Trevor, ~17:09Z, READY): "widen hover-video gate to Golazos + Golazos Trophy-modal video form; repo-sync 7 live Cowork migrations" — frontend video-form fixes (EditionsGridPaginated `videoEnabled` adds laliga-golazos; momentVideoUrl Golazos Trophy form) + parity repo-sync of 7 live 06-24 migrations (already ledgered). Clean so far: READY, 0 ERROR, 0 attributable Sentry, editions flat, no Golazos-video runtime error. NOTE: a SEPARATE live UFC video-recovery Cowork migration (NOT among the synced-7) is the source of the UFC-VIDEO-BACKFILL-AUDIT-TABLE-RLS finding above — fold both into the post-ship watch.
