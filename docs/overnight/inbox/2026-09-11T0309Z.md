# Daytime monitor candidates — 2026-09-11T03:09Z

Source: `rpc-daytime-monitor` (READ-ONLY tick). DB `now()` ~03:09Z 09-11 (≈20:09 PT 09-10).
**Environment note: sandbox bash/clone unavailable all run (Windows Sept-8 Plan9-share fault) → NO-PUSH. This file written to the mount only, uncommitted — the night pass picks it up locally.** Not in a spell (io_wait 0, db_active 3, `rpc_ops_snapshot()` returned fast).

Health this tick: security 4/4 `[]`; trust 37/37 ok, breaches `[]`, precompute 5.31h; sentinel TS-UUID 48h = 0; latest prod deploy READY (`81b75a3` zero-yield sentinel arm), no ERROR states; rpc-live-health artifact payload replayed OK (FMV/PackEV/edition-offers all written <1 min ago, TS HIGH+MED 52.5%).

---

## 1. [RESOLVED — do not re-chase] 13-pipeline external-scheduler freeze (was ledger 09-10 HIGH + inbox `2026-09-11T0045Z`)
- **Status:** RESOLVED. All 13 frozen lanes ran ~03:03Z (`wmc-fmv-populate`, `alerts-send`/`alerts-dispatch`, `snapshot-pack-asks`, allday/golazos/pinnacle listings, `pinnacle-events-ingest`, `ownership-onchain-walk`, etc.). User-alert delivery restored; `pinnacle_render_floor_stale_hours` back to 1.4 (breach_at 30 — it was projected to breach ~09:00Z, so the fix landed in time).
- **How:** a concurrent Claude Code session diagnosed it (#76/#78/#79) and **shipped a GitHub Actions backstop giving the 10 short-cadence dead lanes a caller** — `sales-indexers-backstop.yml` extended, commit `41c036c9` (dpl `8kd5sQ6…`, READY ~02:47Z) — then manually `workflow_dispatch`ed it (the 02:27Z + 03:03Z run clusters).
- ⚠ **REMAINING WATCH (carry forward):** per that session's own commit `8f7a915c`, the GHA **scheduled** trigger has **not self-fired yet (0 of 2 slots at 02:42Z/02:57Z produced nothing)** — recovery so far is manual dispatch + backstop, not the scheduler resuming. **Action:** confirm the backstop workflow self-fires on its cron (check a scheduled slot lands rows with no manual dispatch); if it doesn't, the lanes re-freeze once hand-dispatching stops. Root cause of the original freeze (a GHA scheduled workflow or cron-job.org group that stopped ~14:03Z 09-10) is still operator-diagnosable, not established.
- **Risk:** none to file — this is a hygiene note so the freeze isn't re-raised, plus the one real open thread (self-fire verification).

## 2. `rpc-topshot-onchain-rekey` pg_cron job — single statement-timeout 09-10 11:33Z
- **Source:** `check_pgcron_recent_failures()` → job `rpc-topshot-onchain-rekey`, `last_run` 2026-09-10 11:33:00Z, `failed`, `canceling statement due to statement timeout` on `CREATE TEMP TABLE _mv ON COMMIT DROP AS SELECT m.id, m.nft_id, m.edition_id …`.
- **Context:** daily job at 11:33Z. Green the 5 prior days (09-05..09-09, each "1 row"); this is the only failure in window. Self-retries daily — **next tick 11:33Z 09-11 (had not run at monitor time).**
- **Risk:** LOW. A skipped rekey leaves some TS moments on stale edition keys for one day; a successful next tick self-heals. The 09-10 11:33Z timeout may have been IO collateral (that day carried a Vercel pause / P0 #76 and the scheduler freeze).
- **Suggested action (night pass):** watch the 11:33Z 09-11 tick. **If it times out again**, the `_mv` temp-table build likely does a full `moments`/`wmc` scan — `EXPLAIN (ANALYZE, BUFFERS)` the SELECT, add a collection/cursor scope, and compare BUFFERS (not wall-clock). One isolated timeout is not yet a bug; a second consecutive one is.

## 3. [INFO / known-chronic — not new] carried baseline
- `fmv-backfill` 54.5% / `price-snapshots` 36.4% statement-timeout failure_rate alerts — pre-existing, wasteful-not-broken (FMV freshness all ok in trust board). No action.
- Atlas `-upstream-403` (editions 3.5%, market 2.9%) and `flow-rest-moment-moved-400` (63.1%) — all `info`, attributed, benign-by-design (no rows lost; re-walk / re-ask). No action.
- `pack_distributions` data_stale 7d 22h (`medium`) — long-standing; carry as-is.
- DB size 26,334 MB (was 26,078 on 09-09 close) — Atlas-events growth; retention decision still queued (destructive, off-limits).

## Artifacts
Enumerated 11 active artifacts (all present). Deep payload-replay this tick: **rpc-live-health only** (validated OK). The other 10 share the same connectors/views, which `rpc_ops_snapshot()` confirms live (security invariants `[]`, no view errors, trust board fresh) — not individually replayed to avoid stacking IO during the concurrent active ship session; re-validate on the next quiet tick if a schema change lands.
