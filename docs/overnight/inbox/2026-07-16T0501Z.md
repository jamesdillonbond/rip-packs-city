# Daytime monitor candidates — 2026-07-16T05:01Z (late-evening tick, ~22:01 PDT Jul 15)

Health otherwise GREEN. One trust breach + one low watch item.

## 1. TOPSHOT-IMPOSSIBLE-PARALLEL-SERIALS breach — wave 4 (16/3), largest yet, NOT self-clearing
- **Source:** `v_rpc_trust_health` / `rpc_ops_snapshot` — `topshot_impossible_parallel_serials` = **16** (breach_at 3).
- **What:** Known self-healing `::`-cataloging straggler class. Stragglers confirmed to be freshly-cataloged `257:88xx::17` / `258:88xx::16` subeditions carrying placeholder `circulation_count=1` (a few 9/46) while a real observed sale exists at a serial above circ. Driven by the active `::` cataloging wave (TS editions 19,126 -> **19,408**, +282 since last snapshot; sentinel_ts_uuid_48h = 27, clean -> no hyphen-UUID leak, pure `::` growth).
- **Risk read:** LOW data-quality (impossible serial on a `::` parallel; the writer guards + sentinel already block bad *sales*). But this is the biggest wave so far (wave1=4 on 07-10, wave2=11 on 07-11, wave3=18 on 07-13, now 16) and the per-parallel circ backfill has NOT reconciled it under threshold on its own this time.
- **Suggested action (interactive / Trevor — NOT night-pass autonomous):** evidence-based circ floor-raise, identical class + fix to waves 1-3 (`audit_YYYYMMDD_circ_floor_raise_impossible_parallel_wave4`): for each straggler set `circulation_count = max observed sale serial` (sale at serial N proves circ >= N; audited + reversible). Per the ledger note this is a sanctioned interactive action, NOT the blind-autonomous hand-edit class — so the night pass should surface, not ship it. Revert: restore prior circ values.

## 2. Smoke "pack dist page has Sales History" recurred post-fix, now quiet (LOW / watch only)
- **Source:** Sentry `JAVASCRIPT-NEXTJS-1J` (2 events, first seen ~19h ago, last seen ~14h ago); `analytics-smoke` 18 fails/24h.
- **What:** The exact smoke class Trevor's `034e7cdd` (per-dist `get_pack_market_row` RPC) was written to kill "firing since 07-13" fired twice more on 07-15 after that deploy, then went silent. `analytics-smoke` last 6 runs are all ok=true; no Sentry events in 14h.
- **Risk read:** LOW — appears resolved/intermittent (contention-window flap during propagation). No action now.
- **Suggested action:** Watch only. If it recurs, the `get_pack_market_row` RPC may not cover one pack-market read path (or it is pure DB-contention). No ship.
