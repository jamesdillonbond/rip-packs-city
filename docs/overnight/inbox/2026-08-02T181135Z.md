# rpc-daytime-monitor — 2026-08-02 ~18:10Z (midday tick)

CONTINUATION of the 15:12Z ALERT (`2026-08-02T151239Z.md`), not a new incident. The IOPS-saturation wave is STILL ACTIVE and if anything broader at 18:06Z. Core remediation already QUEUED as IOPS-REINDEX (overnight pass 2026-08-02) + Trevor actively shipping fixes (`8650b93d`, `f407ee16`, `3275c58f`, `2777296f` in the last ~3h). Do NOT re-queue the base IOPS item. Two things worth the night pass's attention:

## 1. [MED, observability] alerts-dispatch is timing out during the saturation wave — health alerting is partially blind
- Source: `pipeline_runs` — `alerts-dispatch` ok=false at 13:29Z, error `deal: canceling statement due to statement timeout`. `rpc_ops_snapshot()` itself now times out on its FMV-sentinel leg (worse than the overnight pass, which could still read it).
- Risk read: LOW-to-MED. The alerting/health-read path degrading *during* a saturation wave is the "monitoring that times out looks healthy/blind" class — a genuinely new breakage could be under-alerted right now. The `/api/sentinel` consumer classifies saturation as `warn INCONCLUSIVE` rather than false-green (per ledger), so it's a visibility gap, not a silent-healthy failure. Not auto-actionable; folds under IOPS-REINDEX. Flagging so it's weighed when the reindex/board-materialize work is scheduled.

## 2. [INFO] pg_cron timeouts today are all the same saturation class — no genuinely-new cron failure
- `check_pgcron_recent_failures()`: rpc-refresh-allday-pack-realized, rpc-serial-fmv-multipliers-weekly (weekly, BY DESIGN per focus.md), rpc-refresh-new-collectors, rpc-weekly-wmc-prune, rpc-trust-health-precompute-refresh — all `statement timeout`, all downstream of the wave. `rpc-weekly-wmc-prune` = the standing WMC-PRUNE watchlist item.
- `detect_stalled_pipelines()`: `pinnacle-sync` (KNOWN observability gap — ran + wrote pinnacle_fmv_history 10:07Z today, logs no pipeline_runs row; ledger line 47) and `classify-acquisitions-multicollection` (KNOWN cron-job.org dropout; ledger line 50). Neither new.

Health otherwise: security clean (RLS-off 0, invariants clean, secdef_anon []). Vercel: latest BUILDING, no ERROR in last 20 (CANCELED = superseded rapid pushes). Sentry: 1 new (NEXTJS-24 set-editions statement-timeout, 1 event) = documented entity-page saturation class.

Note: artifact payload-query validation THROTTLED this tick — the DB is actively saturated (rpc_ops_snapshot timing out), so running the ~11 heavy dashboard payloads would add load and mostly time out. Deferred to a tick after the wave clears.
