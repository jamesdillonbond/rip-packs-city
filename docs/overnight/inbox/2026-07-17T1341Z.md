# Daytime monitor candidates — 2026-07-17T13:41Z (first tick)

Health GREEN overall. One new post-fix regression-class candidate; rest is a clean sweep.

## HIGH-PRIORITY — recent-ship watch: lock-check-batch has a NEW error class after the 07-16 index rewrite
- **Title:** `lock-check-batch` failing 18x/24h (4x/6h) on `lock timeout` / `deadlock detected` — a NEW signature that appeared *after* the 07-16 `get_lock_check_batch` index-driven rewrite landed.
- **Source:** `pipeline_runs` (pipeline=`lock-check-batch`); ledger 2026-07-16 rounds 7-9 (`get_lock_check_batch` rewrite + priority-arm fix + route split; target was "lock-check-batch stays ok=true").
- **Evidence:** hourly fail breakdown shows the OLD class (`get_lock_check_batch: canceling statement due to statement timeout` / `upstream request timeout`) stops at ~19:00Z 07-16 — the read rewrite worked, that class is gone. From 21:00Z 07-16 onward every fail is a DIFFERENT class: bare `canceling statement due to lock timeout` and `deadlock detected` (no `get_lock_check_batch:` prefix -> it's the route's write-back `UPDATE wmc.lock_checked_at`, not the SELECT fn). Last fail 10:38Z today. 24h: 48 runs / 30 ok / 18 fail.
- **Risk read:** LOW-MED. Not user-facing (squeeze/lock ingest path), cursor-based so no data loss, self-heals per tick (30/48 ok). But it is a genuine new class correlating with a recent ship, and the 07-16 "stays ok=true" target is not met — the fast index reads now race concurrent wmc writers (fmv-populate / rwfd drift) on row locks during contention windows.
- **Suggested action (night pass):** the write-back UPDATE is the contender, not the read. Options: (a) short `SET LOCAL lock_timeout` + retry/skip on the `lock_checked_at` write so a contended row is deferred not failed; (b) `SELECT ... FOR UPDATE SKIP LOCKED` when claiming the batch so it cannot deadlock against a writer holding the same wmc row; (c) touch rows in a stable key order to break the deadlock cycle. Do NOT revert the read rewrite (it fixed the real IOPS hog).

## Sweep summary (all clean — no candidates)
- Security invariants 0/0/0/0 (invariants / anon_write_holes / rls_off_base / secdef_anon all []).
- `detect_stalled_pipelines()` [] · `check_pgcron_recent_failures()` [] · trust_health_breaches [] (16/16 ok; impossible_parallel 1/3, ts_uuid_dupes 0/200).
- Sentinel TS-UUID-editions 48h = 39 (< 200 breach).
- First-tick cross-collection refresh: cohort_mat fresh 04:10Z (154 rows), overlap_mat fresh 04:25Z, rpc-ccm-step1/step2 both active. OK.
- Artifact estate: rpc-live-health merged-dashboard consolidated payload runs clean post-DDL-wave — every referenced view/column resolves (cached_listings_v2, unmapped_sales, pack_ev_latest.snapshotted_at, edition_offers.updated_at, insights boards, get_pipeline_alerts); freshness current (FMV 13:38Z / PackEV 13:43Z / offers 13:42Z).
- Vercel prod deploy dpl_7GyFMKd8... (c28a8c5, cadence-escrow CI gate) READY; no ERROR-state prod deploy.
- Other pipeline fails (fmv-recalc 8, analytics-smoke 9 last 21:43Z 07-16, compute-topshot-pack-ev 6, wallet-username-resolver 8) = the known overnight IOPS-contention family; analytics-smoke last fail predates today's analytics_sales_summary single-scan rewrite deploy, consistent with it improving. Not new.
- DB 9,645 MB (+518 vs 07-16 baseline, benign growth).
