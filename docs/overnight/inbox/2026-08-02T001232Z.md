# Daytime monitor candidates — 2026-08-01 ~17:12 PDT (2026-08-02 00:12Z, evening tick)

Health GREEN structurally: security 0/0/0/0, trust-health **25/25 arms ok, 0 breaches** (the overnight fmv_sanity FP stayed cleared), sentinel leak_48h 0, DB 11,703 MB, editions stable (26,989). The afternoon IOPS-contention wave (logged 18:06Z + 21:06Z) has **fully eased** — merged `rpc-live-health` payload validates clean with `db_active=1`, FMV/PackEV/offers all fresh within ~2 min (00:07–00:09Z), insights boards return data. Vercel prod tip `2eceb4da` (Trevor's read-only cart/gifting/trading deletion, 55 files) READY. One new low-priority candidate below.

## UFC-STUDIO-DRAIN-CRON-SILENT (new, low — completed self-terminating drain, cron-job.org scheduler dropout, ZERO data loss)
- **Source:** `detect_stalled_pipelines()` / `rpc_ops_snapshot`.stalled_pipelines — `ufc-studio-sales-history-backfill` last ran **2026-08-01 22:21:42Z**, silent **105 min** vs its 90-min watchlist threshold (cadence `1,21,41 * * * *` = every 20 min; ~5 ticks missed: 22:41 / 23:01 / 23:21 / 23:41 / 00:01Z).
- **Not an execution failure — every recent run is `ok=true`.** The last 12 runs are all `note:"walk_complete"` fast no-ops (~300ms), matching the focus-note design: this deep-history drain finished its ~half-day backfill long ago and now fires every 20 min doing nothing. So the scheduler stopped firing a **completed, no-op** job — **no rows lost, no backlog, no user-facing impact.** This is the cron-job.org scheduler-dropout class (same family as GHA-ACTIVE-LISTINGS-INGEST-DROPOUT), not a code defect.
- **Risk read:** low. Most likely self-recovers on the next cron-job.org tick (transient scheduler hiccup). It surfaces only because the watchlist row (added 2026-06-25, 90m/medium) trips `detect_stalled_pipelines()` -> a standing `cron_silent` alert in `get_pipeline_alerts()`.
- **Suggested action (night pass):** (1) confirm it resumed — if `ufc-studio-sales-history-backfill` shows fresh `ok=true walk_complete` runs, this fully self-cleared, close it. (2) If it recurs across ticks OR you want to stop the recurring `cron_silent` noise from a drain that is permanently a no-op: the durable lever is to **retire the completed drain** — relax/remove its watchlist row (`DELETE FROM public.pipeline_cadence_watchlist WHERE pipeline='ufc-studio-sales-history-backfill';`) and/or disable the cron-job.org entry, since it only ever re-writes `walk_complete` now. Do NOT auto-revert anything; do NOT alarm — no data is at risk either way.

## Already-known / NOT re-raised
- **OPENS-HISTORY-BACKFILL-UPSTREAM-500 — effectively RESOLVED by today's CC fix (a3817e14, DESCENDING monotonic cursor).** `topshot-pack-opens-history-backfill` is now overwhelmingly `ok=true` (only 2 transient upstream-500s in the last 8h vs the trailing 2-day 26.6% that predates the fix). The `failure_rate` alert will decay as the pre-fix window rolls off. Not a finding; noted here so the night pass doesn't re-queue it.
- **`topshot-active-listings-ingest` egress_blocked (failure_rate HIGH, 52.4%/2d)** — the known Atlas/WAF egress-block recurring pattern; each blocked attempt has a healthy `ok=true` neighbor within ~1–2h (22:13Z, 19:13Z, 16:37Z ok), so live TS listing coverage is intact and the sibling `topshot-listing-cache` is unaffected. The 44e97c34 fix (fail-fast ~1min vs ~24) landed today. Standing known class, not a new incident.
- **pg_cron `check_pgcron_recent_failures()`** — the same two already-logged transient MV timeouts (`rpc-refresh-misattrib-candidates` 15:35Z [18:06Z tick], `rpc-refresh-topshot-edition-median` 18:10Z 1/4 [21:06Z tick]); no new failures. Both self-clear on next cadence.
- **Sentry JAVASCRIPT-NEXTJS-23** (`GET /[collection]/player/[slug]` statement timeout, 1 event/1 user, first seen ~21:00Z) — a single saturation-window event, exactly the documented deliberate entity-page structural-throw class (CLAUDE.md: `get_player_editions` 161ms warm, left alone as an intentional honesty choice). Coincident with the afternoon contention wave; not a new defect.
- Vercel deploy `dpl_FXreyBruXr2B7iPAaD2NoPhWYkFV` (`38b7e345`, ERROR) — a superseded CI-flake-fix commit; production tip moved well past it (subsequent deploys READY). Not the live tip.
- `candy-offers-indexer` partial-sweep "deactivation suppressed" — BY DESIGN.
