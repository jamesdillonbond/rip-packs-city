# Daytime monitor candidates — 2026-09-11T00:45Z (filed by rpc-daytime-monitor)

Env note: sandbox bash/clone was UNAVAILABLE this run (Plan9 mount failure), so this file was **written to the mount, push unavailable** — night pass picks it up locally. All reads are direct Supabase MCP against `bxcqstmqfzmuolpuynti`.

---

## 🚨 HIGH — a 13-pipeline external-scheduler cluster STOPPED FIRING at ~13:45–14:03 UTC 2026-09-10 and has not run in ~10.7h

**Source:** `pipeline_runs` last-run buckets + `rpc_ops_snapshot()` + `cron.job_run_details`.

**What.** A coherent cluster of 13 pipelines (20 rows counting `-heartbeat` markers) froze inside a tight ~33-min window on 09-10 and has produced **zero runs since** (DB clock `now()` = 2026-09-11 00:43:50Z at read time):

| pipeline | last run (UTC) | user-facing role |
|---|---|---|
| `wmc-fmv-populate` | 09-10 14:03 | fmv→wmc propagation (/share, portfolio, wallet snapshots) |
| `refresh_wmc_fmv_changed` | 09-10 14:03 | wmc FMV refresh |
| `refresh_wmc_fmv_drift_active` | 09-10 14:03 | wmc FMV drift refresh |
| `snapshot-pack-asks` | 09-10 14:03 | **Pack Sniper** live-ask recency (NEW / price-drop) |
| `pinnacle-listings-retry` | 09-10 14:03 | Pinnacle listings |
| `allday-listings-indexer` | 09-10 14:02 | AllDay on-chain listings |
| `alerts-dispatch` | 09-10 13:59 | **user alert delivery** |
| `alerts-send` | 09-10 13:54 | **user alert delivery** |
| `allday-listings-retry` | 09-10 13:53 | AllDay listings retry drain |
| `golazos-listings-indexer` | 09-10 13:52 | Golazos on-chain listings |
| `pinnacle-events-ingest` | 09-10 13:49 | Pinnacle chain-event ingest |
| `pinnacle-catalog-floor-refresh` | 09-10 13:45 | Pinnacle render-floor → ASK_ONLY FMV |
| `ownership-onchain-walk` | 09-10 13:30 | ownership walk |

**This is NOT the DB, NOT pg_cron, NOT a saturation spell — the trigger stopped.** Positive controls, all read this run:
- Estate healthy: 218 `pipeline_runs` in the last 30 min, 2,155 in 2h; overall `max(started_at)` = 00:43:11Z (39 s before the read). 85 pipelines ran in the last 20 min.
- **pg_cron is fully healthy** — every `cron.job` last-run is 00:36–00:44Z `succeeded`; none of the frozen 13 are pg_cron jobs. (`check_pgcron_recent_failures()` returns only the chronic `rpc-topshot-onchain-rekey` statement-timeout at 11:33Z — a separate, known MV-rekey timeout, not this.)
- **Not a spell:** `pg_stat_activity` IO-wait = 0, active sessions = 0; `rpc_ops_snapshot()` returned promptly.
- **The `-heartbeat` markers froze at the SAME timestamps as the terminal rows.** Heartbeats are written *before* the work, so these pipelines are **not being invoked at all** — this is a scheduler-not-firing event, not a mid-run `maxDuration` kill or a DB timeout.

**Most-likely cause (hedged — NOT confirmable from this READ-ONLY seat):** a single shared external trigger that fans out to these HTTP endpoints stopped ~14:03Z. The clean, simultaneous, cross-collection stop across a mixed lane set fits **one GitHub Actions scheduled workflow** (e.g. `rpc-pipeline.yml` and siblings — the file the 09-07 `ingest`-lane retirement touched) being **disabled or out of Actions minutes**, or a **cron-job.org account/group** going silent. Cannot distinguish from here: no GitHub MCP this session (server failed to connect), cron-job.org needs the Chrome console, and bash/clone is down. `snapshot-pack-asks` is a known cron-job.org job (7878615); the alerts + wmc + listings lanes read more like a GHA matrix — so **check BOTH** the GitHub Actions tab (any scheduled workflow disabled / red / minutes-exhausted since ~14:00Z 09-10) and the cron-job.org console.

**Blast radius / why HIGH (pre-breach window).** Trust board shows **no breach yet**, which is exactly why this is worth catching now:
- **User alerts have not been delivered for ~10.7h** (`alerts-dispatch`/`alerts-send` frozen; `check-alerts` still detects at 00:35Z, so alerts are being *found* but not *sent*).
- Pack Sniper ask recency (NEW / price-drop) is stale ~10.7h.
- AllDay + Golazos on-chain listings and Pinnacle events are stale.
- `pinnacle_render_floor_stale_hours` = **11** against `breach_at` **30** — it will breach around ~09:00Z 09-11 if `pinnacle-catalog-floor-refresh` is not restored, at which point ASK_ONLY Pinnacle FMV goes stale on a public surface.
- `wmc-fmv-populate` frozen degrades /share & portfolio FMV propagation, though pg_cron backstops (`rpc-refresh-wmc-fmv-changed` 00:37Z, `rpc-populate-pinnacle-wmc-fmv` 00:09Z) still partially feed wmc, which is why `fmv_stale_touch_hours` still reads 0.2.

**Risk read.** Diagnosis is read-only and safe. The **fix is operator/night-pass** and NOT a code change: re-enable / re-run the stopped GitHub Actions workflow(s) and/or the cron-job.org group. Restoring a scheduler is idempotent and low-risk. Do **not** revert or edit route/worker code — nothing in the app is broken; the invocations simply stopped arriving.

**Suggested action (night pass / Trevor):**
1. Open the GitHub Actions tab → look for a scheduled workflow last succeeded ~14:00Z 09-10 that is now disabled, failing, or blocked on Actions-minutes/billing. Check org Actions usage/billing. Re-enable / re-dispatch.
2. Open the cron-job.org console → confirm jobs 7878615 (`snapshot-pack-asks`) and siblings show executions after 14:03Z 09-10; if the account/jobs went inactive, re-activate.
3. Confirm recovery by a fresh `pipeline_runs` last-run per pipeline in the table above (each should move to within its cadence) and watch `pinnacle_render_floor_stale_hours` back down before 30.

**Verify-before-acting note:** re-read `now()` and per-pipeline `max(started_at)` at fix time — do not trust `detect_stalled_pipelines()`/`rpc_ops_snapshot().stalled_pipelines` elapsed labels in isolation (they render the same freeze but are easy to misread as estate-wide; the estate is fine).
