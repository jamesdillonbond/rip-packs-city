# Daytime monitor candidates — 2026-07-06 ~21:15Z (14:11 PDT tick)

Run context: no clock skew (shell 21:06Z ≈ DB now() 21:07Z ≈ newest sale, within ~40s). No lock held (overnight RUN 2 released 08:21Z). Later daytime tick; the 1a first-tick-of-day extras were already covered by today's 18:10Z file (trust-health 16/16, cross-collection pg_cron refresh verified) — not re-run.

Health GREEN: security 0/0/0/0; trust 16/16 ok / breaches [] (topshot_impossible_parallel_serials 1/3, unmapped_resolution_backlog_max 30/100, edition_integrity 4/50, pack_ev_board_max_stale_days 1.87/2, pinnacle_fmv_stale 11/30 — all under); detect_stalled_pipelines() []; check_pgcron_recent_failures() [] (clears the queued CROSS-SOURCE-DEDUP jobid-32 concern again this tick); sentinel TS-UUID-48h 0; editions flat (TS 18,151 / AllDay 6,190 / Golazos 575 / UFC 518); FMV TS H+M 4,955 (1322+3633, ~flat vs 18:10Z 4,979 / improving vs overnight 4,934) / AllDay 825 / UFC 15 / Golazos 4; Pinnacle 2,203 renders priced; DB 8,312 MB (+21 since 18:10Z, +107 since overnight = benign ownership-MV + backfill growth; Pro-Micro at 8.3GB is the shared root of the contention family); Sentry 0 unresolved/24h; Vercel prod ba8fa61d READY, 0 ERROR deploy (CANCELED = docs/monitor), 47 runtime-error groups all known families, no new class. Flagship rpc-live-health consolidated payload ran clean — all 12 insights backing views resolve, pack_ev_latest fresh 21:13Z incl. new Pinnacle rows => artifact estate validated (additive-only schema changes today; not re-running all 15 individually given the same-day 18:10Z full validation).

## POST-SHIP WATCH TARGET (NEW today — post-dates the 18:10Z file) — Pinnacle pack-EV pipeline

Source: Vercel deploys bd7e3ff4 (feat: Pinnacle pack-EV pipeline + /disney-pinnacle/packs) + ba8fa61d (feat: v2 corrected-EV view), both prod READY ~20:20-20:47Z; pipeline_runs compute-pinnacle-pack-ev; cron.job rpc-compute-pinnacle-pack-ev.

A brand-new Pinnacle pack-EV pipeline shipped this afternoon, AFTER the 18:10Z monitor tick — so this is the first monitor pass to see it. New edge fn compute-pinnacle-pack-ev (render-keyed, supply-weighted inline EV mirroring the AllDay v8 model); owns pack_distributions + pack_ev_history for Pinnacle; pg_cron rpc-compute-pinnacle-pack-ev (17 */6, active) + cadence watchlist (800m, active); new view v_pinnacle_pack_ev_corrected (v2 median-robust, ASK-dampened, low_confidence_ev flag); /disney-pinnacle/packs surface + a disney-pinnacle merge block in /api/packs.

Data landed sane (verified live this tick): pack_distributions 140/140 disney_pinnacle; pack_ev_history 192 Pinnacle rows; v_pinnacle_pack_ev_corrected 84 rows / 14 flagged low_confidence_ev (the intended chase-pack guard — e.g. Quinova 531x raw -> corrected 386x + flagged). Security invariants 0/0/0/0 after the ship; fmv_sanity 0.

EARLY-LIFE BUG (already self-cleared, NOT a live incident): compute-pinnacle-pack-ev logged ONE ok=false at 20:07Z — "upsert pack_distributions: cannot insert a non-DEFAULT value into column total_sealed". total_sealed is GENERATED ALWAYS AS GREATEST(total_minted - total_opened, 0), so an upsert that lists it in the column set is rejected. This is a STALE PRE-FIX failure: the 3 subsequent runs (20:10 / 20:12 / 20:43Z) are all ok=true, so the edge fn was corrected (total_sealed dropped from the insert column list) within ~3 min. check_pgcron_recent_failures() = [] confirms no pg_cron latest-run failure.

Risk read: LOW. New pipeline, but wired correctly (pg_cron + watchlist), data sane, security clean, the one early error already fixed + verified green 3x. The only thing not yet observed is a GENUINE scheduled pg_cron tick — the 20:1x/20:4x runs were manual ship-time verifies; the next rpc-compute-pinnacle-pack-ev scheduled fire is 00:17Z.

Suggested action (night pass, post-ship watch — NOT an autonomous fix): (1) confirm the 00:17Z rpc-compute-pinnacle-pack-ev tick logs ok=true (the total_sealed class stays gone); (2) if /disney-pinnacle/packs is anon-public, confirm v_pinnacle_pack_ev_corrected's security_invoker + anon-read posture matches the other public pack-EV views (secdef-anon [] this tick); (3) spot-check a flagged low_confidence_ev pack renders the AllDay-style chip. IF the 00:17Z tick re-throws total_sealed, THEN it is a genuine recurring writer bug (edge fn still lists the generated column) -> CC: drop total_sealed from the compute-pinnacle-pack-ev pack_distributions upsert column list + redeploy the edge fn.

## Carried, no net-new (context only, do NOT re-log)

- 17:2x-17:3xZ contention cluster: compute-topshot-pack-ev (targets: statement timeout), allday-unmapped-resolver (load_open: pool timeout), fmv-recalc (edition_page_fetch: statement timeout x3) all timed out in the heavy daytime deploy+backfill window, ALL latest-run ok=true. Members of the carried DAYTIME-CONTENTION-CLUSTERS-BROADENING + FMV-RECALC-EDITION-FETCH-TIMEOUT-CREEP; the 18:10Z file already logged run-insider-detectors + daily-portfolio-snapshot from the same window. No net-new.
- ownership-sync-dune HTTP 429 at offset 20000 (15:09 / 15:16Z), latest ok 17:44Z — benign Dune free-tier rate-limit during the daytime CC ownership/Dune wave; the 0e8c07a throttle drains it in one invocation. Known.
