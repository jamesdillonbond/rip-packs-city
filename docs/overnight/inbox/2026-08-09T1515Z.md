# Daytime monitor inbox — 2026-08-09T15:15Z (08:15 PDT, first tick of day)

Written by `rpc-daytime-monitor`. READ-ONLY sweep. Inbox written to the MOUNT (workspace shell down — `/sessions` no-space, 3rd consecutive failure — so no clone/push available). Lock was RELEASED by the 08-09 night pass, so no concurrency skip.

## HEALTH SNAPSHOT
- Security 4/4 clean: `check_public_security_invariants` [], `check_secdef_anon_exec_drift` [], `check_anon_write_surface` [], rls_off=0.
- `detect_stalled_pipelines()` [] · `check_pgcron_recent_failures()` [].
- Trust-health precompute (fresh 06:58Z): all board legs 0/ok; FMV HIGH+MED share TS 56.5 / AllDay 28.9 / Candy 59.2 / Golazos 0.5 / UFC 0. Known-carried breach only: `panini_sale_price_capture_dry_days`=12 (home-box capture outage since 07-29). UFC stale metrics (`ufc_fmv_pct_stale_30d`=96.1) expected — market closed. Full `v_rpc_trust_health` view is saturation-blocked (statement timeout); relying on the precompute + the night pass's 04:28 baseline (2 breaches, both known: panini capture + `unmapped_resolution_backlog_max`).
- **DB is in an active disk-IO / connection-pool saturation window** right now (pooler timeouts observed 15:11–15:14Z). Both new signals below trace to it.

## CANDIDATE 1 — [HIGH] Production deploy in ERROR; `main` tip undeployed ~24h (build-time prerender vs saturated DB)
- **Source:** Vercel deploy `dpl_4Aj4p5gF8nQPKCGqDdnU6MLpKAqL`, commit `0fffb2ae` ("refactor(pages): extract pure view-logic from 4 server pages"), state **ERROR**, created 2026-08-08 ~15:08Z. The only later commit `ee18d86d` was docs-only (CANCELED = ignoreCommand-skipped), so nothing re-triggered a build.
- **Root cause (build logs, errorsOnly):** Next.js static export of `/(analytics)/analytics/sets/[set_id]/page` (set_id `61b86f8c-b234-43d2-b430-42a1f81401d1`) threw `Timed out acquiring connection from connection pool` / `canceling statement due to statement timeout` — the `analytics_sets_detail` RPC + the candy-mlb board queries could not acquire pooler connections during a build-time prerender while the DB was saturated. `⨯ Next.js build worker exited with code: 1` → `Command "npm run build" exited with 1`. **NOT a fault in the byte-identical page-extraction refactor.**
- **Blast radius:** prod is healthy but FROZEN on the prior READY deploy `991291be` (confirmed live via the Sentry event release tag + active traffic). The refactor + the CLAUDE.md session entry are on `main` but NOT in production. No auto-retry exists for a failed Vercel build, so `main`'s tip stays undeployed until someone re-triggers.
- **Suggested action (two parts):**
  1. **Redeploy `main` during a calm DB window** (v13 deployments POST, or `deploy_to_vercel`). Likely succeeds when the pooler isn't depleted — the failure was environmental, not a code fault.
  2. **Durable fix (CC, LOW/read-path):** the analytics set-detail page force-exports statically and hits the live DB at build time, so a saturated pooler can red the ENTIRE production build. Make that page dynamic/ISR (or exclude it from `generateStaticParams` / add a fail-soft prerender fallback) so DB saturation can never block a deploy. This is a distinct fragility from the already-queued PUBLIC-BOARD-CACHING item (that was request-time; this is build-time static export), but the same saturation root cause — worth handling together.
- **Risk:** redeploy is low-risk; the page-dynamic change is a small, reviewable code change. Do NOT "fix" the refactor — it is not broken.

## NOTE (not a separate candidate — saturation artifact, self-clearing)
- Sentry **JAVASCRIPT-NEXTJS-25** ("smoke test failed: cursor-stall threshold shared by classifier and alert arm", first seen 15:11Z, 1 event, release `991291be`) is the smoke assertion `rpc:check_cursor_stall_threshold_drift` failing with `detail: "rpc error: Timed out acquiring connection from connection pool"` — it could not acquire a connection, NOT a real cursor-stall-threshold data drift. It will clear on the next smoke run once the pooler recovers. Do NOT triage it as a data/config defect.

## KNOWN / CARRIED (not re-logged — already in ledger / night-pass queue)
- `rpc-ccm-step1` cross-collection cohort mat stale: `cross_collection_cohort_mat` last computed 08-08 04:10Z (~35h, >26h threshold) — step1 timed out at today's 04:10Z tick; `cross_collection_ts_set_overlap_mat` fresh (08-09 04:25Z). Self-recovering (retries 04:10Z daily); already in the night pass's disk-IO-saturation MV-cluster queue. cohort count 175, stable.
- Disk-IO saturation MV cluster, PUBLIC-BOARD-CACHING (nc1), and the standing operator/CC queue items — all carried in the 08-09 ledger entry.

## Artifacts
- 11 enumerated (candy-chain-two-onboarding-v2, rpc-panini-squeeze-v2, rpc-set-challenge-roi, rpc-pack-lifecycle, rpc-rewards-console, rpc-tracked-fmv-confidence, rpc-qa-scorecard, rpc-traction, rpc-deploys-and-cost, rpc-my-wallet, rpc-live-health), all present. Deep payload validation SKIPPED this tick to avoid piling read load on the actively-saturated DB (same deliberate deferral as the 08-08/08-09 night passes); no schema break shipped since last validation that any artifact reads.
