# Daytime monitor inbox — 2026-06-08T03:15Z

Run: rpc-daytime-monitor (read-only sweep 03:06–03:15Z, Sun late night; night pass due ~08:00Z). Health GREEN overall with ONE new red canary (item 1 — false-positive, verified not a real leak). Security 0/0; `detect_stalled_pipelines()` = `[]`; `get_pipeline_alerts()` = `[]`; latest prod deploy `26fc9f3` READY (the lone ERROR `76b6c2e` is the known superseded build-infra blip, code live via descendants). Artifacts spot-validated healthy (AF1 completes, squeeze bounded 501, rewards 1 row, offers 9,747, pin renders 1,794) on top of the 00:16Z full 16/16 pass — no breaking DDL since.

---

## 1. SMOKE-PIN-DRIFT · [MED-HIGH · CC route/code — red canary on every smoke tick, masks real leaks] Pinnacle drift guard fails post-concierge-swap; verified FALSE POSITIVE (stale guard), not an FMV leak

**Source:** Sentry JAVASCRIPT-NEXTJS-14 (old issue, 10 lifetime occurrences; re-fired 02:47Z + 03:01Z 06-08 on release `c9365e2`, which contains the `a9f86af` concierge per-render swap — deployed ~02:2xZ, first new event ~25 min later). Check: `app/api/smoke-test/route.ts` ~L1142–1199 "Pinnacle FMV not borrowed across characters (drift guard)".

**What:** the guard validates each `searchPinnacleDeals` row's `(player, set, tier)` against **`pinnacle_editions`** `(character_name, set_name, variant_type)`. Failing tuple: `Goofy / Walt Disney Animation Studios • Disney Mickey & Friends: Winter Adventures Vol.1 / Standard fmv=$1` (2 rows). **Verified by direct SQL — two independent mismatches, neither a mis-attribution:** (a) `pinnacle_editions` has **0** Goofy rows for that set at all, while `pinnacle_catalog` has 4 priced Goofy renders for it (the per-render source is simply more complete than the legacy table the guard checks); (b) `pinnacle_catalog.set_name` for this set carries a **leading space** (`" Walt Disney…"` vs editions' `"Walt Disney…"`), so even overlapping tuples can fail string-equality if the new code passes catalog set_names through raw. Character matches Goofy on both rows → not literally cross-character; FMV attribution itself looks correct.

**Action (CC):** re-key the guard to the table the code now reads — `pinnacle_catalog` `(character_name, trim(set_name), variant)` (note: catalog column is `variant`, NOT `variant_type`) — or trim/normalize in `searchPinnacleDeals` row output. Separate LOW data-hygiene candidate: `UPDATE pinnacle_catalog SET set_name = trim(set_name) WHERE set_name <> trim(set_name)` (count + backup first; writer may re-introduce — check pinnacle-catalog-backfill's source string). Also eyeball whether `fmv=$1` on those renders is the old Flowty $1-floor artifact resurfacing via floor/ask inputs — if so, that's an honesty issue worth its own look. Until fixed, EVERY smoke tick fails this check → resolve-with-regression-arming in Sentry is NOT appropriate (it would just re-open); fix the guard first.

## 2. TS-SET-254 · [MED · CC/night-pass DB+probe — catalog gap, 3 days old] hydrator `catalog_gap: parent set unknown` every tick since 06-05 for set_id_onchain=254 (new TS drop); self-heal can't bridge it

**Source:** `pipeline_runs` `topshot-moments-hydrator` — `catalog_gap: parent set unknown for nft_id=52041121 set_id_onchain=254 play_id_onchain=8622` (also nft 52041123) on effectively every tick: first seen **2026-06-05 03:02Z**, 128 ticks in the last 24h. Runs log `ok=true`, so no alarm ever fired — absence-of-success class for these specific moments.

**What:** 1 edition row exists with `set_id_onchain=254` but it joins to **no `sets` row** (`sets_via_editions=0`), so `ensure_topshot_edition_stub`'s sibling-bridge self-heal has nothing to bridge from — set 254 looks like the newest TS drop's set, never seeded into `sets`. The affected moments can't hydrate (newest-drop metadata missing from wmc/moments; invisible staleness on exactly the content collectors care about first).

**Action:** seed/repair the `sets` row for on-chain set 254 (Cadence `getSetSeries(254)` + `getPlayMetaData(8622)` via topshot-proxy, or GQL `searchEditions`), backfill the orphan edition's `set_id` FK, confirm the hydrator's catalog_gap line disappears next tick. Additive catalog write — LOW risk, but it touches the editions/sets spine so verify with the canonical-edition predicate before/after. Worth checking how many distinct nft_ids are stuck (likely just the 2 so far — early drop).

## 3. INFO datapoints (no new items)

- **CRON30S-VERIFY (00:16Z inbox item 1) → CONFIRMED CLOSABLE:** `analytics-smoke` logging since exactly 00:13Z as predicted — 6/6 ok=true, cadence :13/:43 held, durations 23–44s (the work the 30s cap was killing). `lock-check-batch` 14h clean on new code. The `76b6c2e` wrap is verified end-to-end; cron-job.org entries should now show Successful.
- **Stagger/histogram datapoint (for tonight's due verification):** fails last 6h = **12, all in one 00:48–01:05Z burst** (wmc-fmv-populate ×4, pack-ev `targets` ×3, topshot-fmv-populate sets-read, allday-listings-retry, pinnacle-nft-resolver, hydrator candidate_read — pool/stmt-timeout class), zero fails in the ~2h since. Note the rush has MOVED: top fail minutes are now :48–:05 (the staggered slots cluster ahead of the hour), and a new `:45` anchor carries 580 runs/6h (0 fails — likely a chained sub-pipeline logging there). Minute-:00 is no longer the top anchor. Fold into the histogram verification before any further moves.
- **DUPE1 gate progress:** sentinel TS-UUID-48h **1,072** (1,363 → 1,072 since 00:16Z), 0/hr new. On pace to clear <250 late 06-08 — CC-owned merge gate approaching; night pass must NOT pre-empt (focus.md #3).
- **PACKEV ahead of forecast:** stale-24h **248** (354 → 248; focus target was <~350 by morning). Cron-frequency lever confirmed sufficient.
- **FMV:** TS HIGH+MED 2,971 (churn-flat vs 3,008 baseline), TS NO_DATA 5,230 (improving from 5,444), AD HIGH+MED 496 (flat). Sales fresh.
- **C3:** `v_offer_sanity_flags` 134 — inside the 118–138 band.
- **DB 6,464 MB** — slow creep (+66 MB/36h; 6,398 → 6,433 → 6,464). Watch-only; dupe roll-off + new index account for most.
- **GIT-IDENT persists:** every commit today incl. CC's full Pinnacle wave remains authored `rpc-daytime-monitor` (raised 21:11Z + 00:16Z; one-liner `git config user.name/email` fix in the shared clone still outstanding).
- **Sentry otherwise:** 1 unresolved total (= item 1). The 5 smoke transients + NEXTJS-4 resolved by CC stayed quiet — zero re-opens across the whole 06-07 ship wave.

---

## Artifact validation

Spot-checked the schema-sensitive backing queries this run (AF1 `v_tracked_wallet_fmv_confidence` 20 rows no 57014; `topshot_squeeze_board` bounded 501; `v_rewards_economy` 1 row; `edition_offers` 9,747; `pinnacle_catalog` priced 1,794) — all healthy. Full 16/16 pass was 3h ago (00:16Z); no DDL since except `audit_20260608_*` `pinnacle_refresh_editions_ask` (additive fn; legacy table untouched, so no artifact reader breaks). No repairs needed, none attempted.
