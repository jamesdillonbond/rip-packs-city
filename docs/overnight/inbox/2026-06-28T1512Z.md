# Daytime monitor — 2026-06-28T15:12Z (first tick of day, ~08:12 PDT)

Health GREEN. No clock skew (DB now 15:09Z == app-stamped pipeline_run 15:09:24Z; snapshot 15:06:33Z == shell). security 0/0/0/0, trust 13/13 (the owned `unmapped_resolution_backlog_max` BREACH cleared 484->27), detect_stalled/alerts []. 1 new candidate.

## Candidate 1 — SERIAL-FMV-POWER-MODEL-WEEKLY-TIMEOUT (MEDIUM; CC/operator; weekly-job regression)
- **Source:** `check_pgcron_recent_failures()` -> pg_cron `rpc-serial-fmv-power-model-weekly` (schedule `0 11 * * 0`, weekly Sun 11:00 UTC). Run 2026-06-28 11:00:01Z **FAILED 11:02:01Z = "canceling statement due to statement timeout"** (ran exactly 120s = the statement_timeout cap). Immediately prior run 2026-06-21 11:00:00Z **SUCCEEDED in ~8s ("1 row")**.
- **This is NOT the closed SERIAL-FMV-MULT-CRON cadence item** (ledger line 788, "do NOT re-queue"). That closure was about the weekly crons existing/being-active + weekly staleness being by-design. This is a different signal: the power-model cron RAN and FAILED — an ~8s -> 120s-timeout regression in ONE week. Driver is almost certainly the week's data growth: the studio deep-history backfills (~124k+ studio sales, focus.md) + `topshot-flowty-unmapped-drain` inflated the sales the `compute_serial_fmv_power_model` fit scans (the cancel fired inside its `WITH latest_fmv AS (SELECT DISTINCT ON (fs.edition_id) ...)` leg per the cron return_message).
- **Risk read:** LOW blast radius — serial-FMV power-model/multipliers is a serial-premium refinement feeding `serial_fmv_estimate`, NOT core FMV / deal boards / pack-EV. NOT urgent (next scheduled run 2026-07-05 11:00Z). But left unaddressed the serial-FMV multipliers silently stop refreshing weekly.
- **Suggested action (night pass / CC):** raise the SECDEF fn statement_timeout for the weekly fit and/or optimize the `DISTINCT ON (fs.edition_id)` plan — the well-precedented special-serial-owners-MV fix pattern already in the ledger (fn `SET statement_timeout` + `enable_nestloop=off` / hash-join). DB-side function change, no route. Verify the next fit logs `succeeded` in `cron.job_run_details`.
- **Future-tick note:** once-weekly job, so `check_pgcron_recent_failures()` will keep surfacing THIS SAME failed run on every monitor tick until 2026-07-05 — do not re-log; it is this candidate.

## Not logged (known / recovered — recorded for context)
- Transient fail spread ~25/24h across ~12 pipelines (wmc-fmv-populate 10, pinnacle-nft-resolver 4, compute-topshot-pack-ev 3, several "Timed out acquiring connection from connection pool" + statement timeouts). ALL `latest_ok=true`; `detect_stalled_pipelines()` []; `get_pipeline_alerts()` []. Known self-healing DBSAT / connection-pool-saturation class (ledger lines 26, 885) during the ongoing backfill wave (DB +152MB/24h -> 6,434MB). Watch only; not a new candidate.
- Artifacts: 13 active (the 12-artifact estate + NEW `rpc-pack-lifecycle`, created 2026-06-27 by the `c688f673` pack-dist lifecycle/EV feature). Validated `rpc-pack-lifecycle` live this run — all 4 backing objects healthy (v_topshot_pack_lifecycle_global 1, v_topshot_pack_lifecycle packs_opened>0 362, v_topshot_pack_realized_ev 199, pack_rips 60d 147,117). No schema shipped today (night pass docs-only), so the 12 prior-validated artifacts have no new break vector.
