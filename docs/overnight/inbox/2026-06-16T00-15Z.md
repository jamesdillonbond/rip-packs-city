# Daytime monitor — candidate work (2026-06-16T00:06Z / 17:06 PDT evening tick)

Platform GREEN. One new candidate this tick — a post-ship watch on the serial-adjusted FMV ship that landed ~20 min before this run (after the night pass's ledger/metrics baseline), so the night pass won't otherwise have it on the watch list.

## NEW — SERIAL-FMV-WATCH (LOW · night-pass post-ship watch)
- **Title:** Post-ship watch the serial-adjusted FMV ship `0122f9c` (Trevor, 2026-06-15 23:47Z).
- **Source:** commit `0122f9c` "feat(fmv): serial-adjusted FMV — additive #1/perfect-mint premium line"; deploy `dpl_3xMmuVULmZHAofBqZcfmVAXJWqey` READY; new route `app/api/cron/refresh-serial-fmv-multipliers/route.ts` + `app/moment/[id]/page.tsx` + `components/TrophySlab.tsx` (+208 lines, additive only). DB migration (per deploy msg): `serial_fmv_estimate_helper_and_wire_into_moment_and_trophy` — new SECDEF helper `serial_fmv_estimate()` (service_role-only, anon cannot execute), wired into `get_moment_detail` (new `serial_fmv` field) + `get_trophy_slab_data`; anon by_username path left untouched.
- **Risk read:** LOW. Purely additive — never overwrites edition FMV, floored at it. Moment-page line gated behind `SERIAL_FMV_PUBLIC` env flag (OFF until the mandatory LiveToken cross-check). This tick confirmed: deploy READY, `check_public_security_invariants()`=0 / `check_secdef_anon_execute_violations()`=[] (no anon hole from the new SECDEF helper), Sentry 0 unresolved, FMV by-collection unchanged (the serial line is additive, not a clobber). No regression signal.
- **Suggested action (night pass):** (a) the new `refresh-serial-fmv-multipliers` cron route has **0 `pipeline_runs` so far** — confirm whether it's meant to be wired to a cron-job.org entry (likely operator, mirrors the per-serial multipliers refresh); if wired, verify its first ticks log `ok=true`. (b) Spot-confirm `get_moment_detail` returns the new `serial_fmv` field without error on a HIGH/MEDIUM #1 or perfect-mint moment. (c) Verify `SERIAL_FMV_PUBLIC` stays OFF (no public exposure) until Trevor's LiveToken gate. Not a regression — watch-only; nothing to ship/revert.

## Carried (already in ledger — NOT re-raising, listed for continuity)
- N1 `snapshot-institutional-wallets` daily cron drop (operator) — note: `get_pipeline_alerts()` = [] this tick (recovered since the 08:10Z baseline alert).
- ALLDAY-V1-UNMAPPED-DRIFT (operator) — recover-v1 cron (job 7818270) draining; open `resolved_at IS NULL` 270 incl. retired tail.
- VERCEL cost family (VERCEL-FLUID-RIGHTSIZE / CRON-CADENCE / OBSERVABILITY-SAMPLING / SPEND-PAUSE / FLUID-CONCURRENCY) — Trevor/operator.

## Health snapshot (this tick)
pipeline fails 6h: 0 (1882 runs / 69 pipelines). 24h fails: offers-sweep 1/72 + analytics-smoke 1/48 (both known transients, self-recovered). detect_stalled []; get_pipeline_alerts []. security 0/0/0/0 (RLS-off none, anon-write none, invariants [], secdef-anon []). sentinel TS-UUID-48h 0. FMV TS hi+med 3390 (up from 3364) / AllDay 759 (up from 715) / Pinnacle priced 1845. editions flat (TS 15543 / AllDay 6191 / Golazos 581 / UFC 446). DB 4661 MB (+21 benign). Sentry 0 unresolved. Vercel 0 ERROR (prod 0122f9c READY). Artifacts: 12 active validated (rpc-live-health consolidated query clean incl. 11 insights boards live; cross_collection_cohort_stats=1 is by-design single summary row, cohort_mat 158 + overlap_mat 246 refreshed today 04:10/04:25Z via pg_cron; v_tracked_wallet_fmv_confidence 21; v_offer_sanity_flags 200); 5 RETIRED tombstones skipped.
