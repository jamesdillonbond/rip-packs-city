# Daytime monitor — 2026-06-09T15:06Z

Run: `rpc-daytime-monitor` (read-only). ~6.6h after the 08:30Z night-pass baseline. Took over the STALE released `.lock` (08:42Z marker, >45min old — not active). Platform **GREEN except the carried Pinnacle ASK breach, which is now WORSENING** + routine external cron-drop dropouts. Security 0/0. Sentinel 0. 20/20 deploys READY. Artifacts spot-checked OK.

## 🔴 LEAD — PINNACLE-RECONCILE-TIMEOUT is WORSENING, not self-healing (trend update on the queued item; CC/operator, off-limits to auto-ship)
- `v_rpc_trust_health.pinnacle_ask_stale_hours` = **10.5h** (breach_at 3) — up from **3.8h** at the 08:30Z baseline. Freshest `ask_source='pinnacle_direct'` ask frozen since the last successful reconcile at **04:39Z** (~10.5h).
- `pinnacle-listings-reconcile` failed EVERY tick **05:09→11:24Z** on the ~8s service_role statement cap (`canceling statement due to statement timeout`, duration 8–23s) — this is **NO LONGER bounded to the 05–08:30Z saturation window**; it kept timing out through late morning. 24h: 33 fails.
- Since **11:24Z it has stopped logging entirely** (223m silent vs 60m threshold; ~15 missed 15-min ticks) — cron-job.org dropped it too (CRON-DROP-WAVE compounding). So **re-firing the cron alone won't fix it** — when it DID run it timed out every tick; the statement itself is too slow for the 8s cap.
- The morning prediction ("saturation-driven, self-heals when load lifts") is now **FALSIFIED**: saturation cleared by 14:00Z (0 pipeline fails 14–15Z) yet the ASK is still frozen at 04:39Z. Sustained, worsening degradation of the Pinnacle floor/ask intelligence surface (load-bearing since the ~23:00Z `04011b3` Flowty-cache retirement).
- **Action tonight (CC/Trevor — Pinnacle pricing path off-limits to auto-ship; NO-PUSH blocks route side):** the fix is NOT re-firing the cron. Per the queued PINNACLE-RECONCILE-TIMEOUT item: (1) `SET statement_timeout TO '120s'` on the reconcile RPC (caveat: fires ~every 15min → a 120s hold could worsen pool contention; validate across rush) — OR preferably (2) make reconcile **incremental** (only changed listings) so it never hits the 8s cap. Not auto-shipped/reverted.

## CRON-DROP-WAVE — new instance: topshot-fmv-populate stalled (operator)
- `topshot-fmv-populate` 857m silent (last run **00:50Z**) — now tripping its 480m watchlist (TFP-WATCH doing its job). Fire-and-forget route returns 202; cron-job.org dropped its ~6h :50 tick. Operator re-fire ("RPC TS FMV Populate"). Same external-trigger class as the rest of CRON-DROP-WAVE. `ultimate-fmv-recalc-v1` also in `detect_stalled` (info, ~daily, expected).

## I1 / DBSAT — window extended into mid-day + a connection-pool blip (data for the queued I1 analysis)
- The statement-timeout wave ran **~05:00→13:00Z** (LONGER than the historical 09–11Z / 18–20Z DBSAT windows; the novel overnight 05–08:30Z window extended). Cleared fully by 14:00Z (0 fails 14:00Z + 15:00Z).
- A distinct **"Timed out acquiring connection from connection pool"** mini-cluster hit ~12:47–12:56Z across `allday-sales-indexer` / `allday-listings-indexer` / `allday-edition-resolver` + `hybrid_custody_events` + `topshot-offers-indexer` (1 fail each, recovered next tick).
- 6h top statement-timeout fails: compute-topshot-pack-ev 30, pinnacle-nft-resolver 30, topshot-moments-hydrator 17 (N2), wmc-fmv-populate 12, pinnacle-listings-reconcile 10, analytics-smoke 7, lock-check-batch 6, resolve-topshot-stubs/topshot-stub-resolver 5+5, fmv-recalc 4 (`step3_delete_chunk_failed`, recovered, last ok 14:48Z), pack-events-ingest 4. All rush/saturation class, none a wedge.
- Oddity (0-harm, noting only): a **1,284-run spike in the 12:00Z hour** (~6× normal, only 8 fails) — likely a backfill/seed fan-out.

## ✅ Positives / closeable
- **PIN-SYNC-FMV-WATCH gate (a) CLEARED:** `pinnacle-sync` logged **ok=true at 10:07Z** — the `5880eeb` render-FMV `statement_timeout 120s` fix works. This unblocks PIN-SYNC-CRON's watchlist INSERT and the queued (b) `pinnacle_fmv_stale_hours` trust-health tripwire (night-pass-eligible: sequence the watchlist row first, then the (b) view UNION).
- **b7211fb-VOLUME-WATCH reassured:** TS sale volume recovered to **216/hr at 14:00Z** (vs the 4–21/hr early-morning dip the watch worried about) — confirms diurnal dip, NOT throttling from the new per-sale `/v1/transactions?expand=result` decode. Effectively closeable.
- **Today's daytime ship wave (the dapper.market / on-chain-intel handoff Items 1–6, `b7211fb`→`15ca7915`, all 20/20 READY) clean on first read:** dapper.market buy + pack-browse links, brand Phase-2 tokenization, @handle username resolution + new `/api/cron/resolve-wallet-usernames` (migration `broaden_username_resolution_and_unresolved_finder`), pack-EV original-distribution depletion fix (migrations `pack_reality_top_ev_secondary_ask_gate` + `pack_ev_original_distribution_basis`), mobile thumbnails. No attributable Sentry (only the carried NEXTJS-14) or new pipeline-fail signature; `topshot_pack_reality_top_ev` view still resolves (6 rows). **Night pass owns the deep post-ship watch** — freshest/highest-attention = Item 4 pack-EV migrations + Item 3 username cron's first runs.

## Low / hygiene (NEW, not in ledger)
- **GIT-IDENTITY-DRIFT (LOW · operator/CC):** today's Claude Code daytime feature commits (`b7211fb`, `3fdbf25`, `b40ce05`, `15ca7915`, brand batches, etc.) are authored as `rpc-daytime-monitor <monitor@rippackscity.com>` instead of Trevor — the git identity drifted back after the 2026-06-08 "set git identity back to Trevor" reset. Cosmetic (commit attribution only, no functional impact); reset `git config user.name/email` in the working tree at the next CC session.

## Health snapshot
✓ security **0/0** (rls-off none · anon-write-on-rls-off none · `check_secdef_anon_execute_violations()` [] · `check_public_security_invariants()` []) · sentinel TS-UUID-48h **0** (total 6404, flat — DUPE1 re-mint stopped) · editions TS 15542 / AllDay 6191 / Golazos 581 / UFC 446 (identical to 08:30Z baseline) · canonical TS FMV **HIGH+MED 2810 (30.8%)**, HIGH 523, NO_DATA 214, STALE 147, ASK_ONLY 1000 · AllDay HIGH+MED 491 · writers fresh (fmv-recalc 14:48Z, sales-indexer 15:03Z, allday-fmv-populate 15:02Z, offers-sweep 15:02Z; b7211fb buyer-resolution 0 fails) · `fmv_sanity_flags` 0 · unmapped_sales 183 (flat) · DB **6731 MB** (+44 since 08:30Z, slow creep — watch) · 20/20 deploys READY, 0 ERROR (prod `15ca7915` ~14:25Z) · Sentry: only NEXTJS-14 (queued PINFMV-DRIFT-14, off-limits) · artifacts spot-checked OK (rpc-insights-health 10/10 backing views resolve; rpc-tracked-fmv-confidence resolves, AF1-v2 holding).

`detect_stalled_pipelines()` = 3: `ultimate-fmv-recalc-v1` (info, daily), `topshot-fmv-populate` (medium, 857m — CRON-DROP-WAVE), `pinnacle-listings-reconcile` (medium, 223m — PINNACLE-RECONCILE-TIMEOUT).
