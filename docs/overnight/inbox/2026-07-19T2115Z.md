# Daytime monitor — 2026-07-19T21:15Z

Tick 3 of the day (priors: `2026-07-19T1512Z.md`, `2026-07-19T1820Z.md`). Lock RELEASED (night pass `night-20260719-12828`, 08:17Z) => inbox commit permitted.
No clock skew: DB `now()` 21:08:27Z ~= newest sale 21:07:22Z ~= newest FMV 21:12:34Z.
Concurrent interactive session VERY ACTIVE (Trevor + Claude Code: ~20 deploys 17:5x–21:04Z; prod now `34341197`, READY).

Health: security 0/0/0/0 · trust 15 metrics / 1 BREACH (known class) · `stalled_pipelines []` · pg_cron 2 · Sentry 1 issue / 1 event · Vercel **0 ERROR** · DB 10,197 MB · editions TS 19,464 · sentinel 0.

**Headline: the afternoon statement-timeout wave has kept escalating (1.7% of runs at 17:00Z -> 4.6% at 21:00Z, 100% statement timeouts, 0 pool exhaustion), and it has now taken down a pipeline shipped today and squeezed a lambda to 2% headroom. Sales + FMV ingest are unaffected and fresh.**

---

## HIGH — SALES-SELLER-RECOVERY-DUNE IS 16s FROM THE 800s LAMBDA CAP (escalation of the 18:20Z LOW/WATCH)

**Source:** `pipeline_runs` `sales-seller-recovery-dune`, ticks 13:47Z->20:47Z.

The 18:20Z tick flagged this at ~63s headroom (8%) and said "no action now." That margin has closed:

| tick (Z) | 13:47 | 14:47 | 15:47 | 16:47 | 17:47 | **18:47** | 19:47 | 20:47 |
|---|---|---|---|---|---|---|---|---|
| duration_ms | 720,588 | 720,441 | 724,227 | 737,178 | 735,309 | **784,048** | 770,072 | 739,631 |

**784,048 ms against a Vercel Pro `maxDuration` hard cap of 800,000 ms = 16.0s / 2.0% headroom.**

The route's own `HARD_BUDGET_MS = 720_000` is checked *between* windows, so a single in-flight Dune window can overrun it — observed overruns are now 0.1s -> **64s**. CLAUDE.md documents that crossing 800s fails **invisibly** (the `maxDuration: 900` incident: five deploys failed with no logged error). Here the failure mode is worse than a bad deploy: the tick would die mid-drain with no `pipeline_runs` row at all, so the pipeline would look *silent*, not failed.

**Risk read:** LOW to act, MED-HIGH to leave. The lane is doing the single highest-value bulk work on the platform right now (~84k seller rows in the afternoon per the 18:20Z tick), so a silent stall would be costly *and* hard to notice.

**Suggested action (night pass / CC):** lower `HARD_BUDGET_MS` from 720s to ~600s, and/or shrink `window_days` (currently 7) so a single window can't overrun by 64s. Both are one-constant changes in the route with no data-shape effect — the cursor is idempotent, so a shorter tick just does fewer windows. Do **not** raise `maxDuration`; 800 is the hard cap.

---

## HIGH — ALLDAY-LOCK-REFRESH-BATCH HAS BEEN DEAD 3 CONSECUTIVE HOURS (same-day ship, not watchlisted)

**Source:** `pipeline_runs` `allday-lock-refresh`. Route + cron shipped **today** (`app/api/cron/allday-lock-refresh-batch`, Vercel cron `23 * * * *`).

| tick (Z) | 13:23 | 14:23 | 15:23 | 16:23 | 17:23 | **18:23** | **19:23** | **20:23** |
|---|---|---|---|---|---|---|---|---|
| ok | yes | yes | yes | yes | yes | **NO** | **NO** | **NO** |
| wallets processed | 60 | 60 | 60 | 60 | 60 | **0** | **0** | **0** |
| lock_flips | 1,679 | 2,381 | 2,899 | 3,488 | 512 | — | — | — |

All three failures are identical: `wallet fetch: canceling statement due to statement timeout`, `extra.stage = "wallet_fetch"`, dying at **30,2xx ms** — i.e. the new SECDEF `get_allday_lock_refresh_wallets` is hitting a 30s statement timeout and the tick claims nothing. Not degraded — **hard zero for 3 hours**.

**Why this matters more than a generic contention casualty:** today's ship deliberately **suppressed the All Day "Locked FMV" tile and "Held/Locked" column** on `/nfl-all-day/collection`, on the explicit promise that this batch would refresh `lock_checked_at` coverage and the display would be un-suppressed as a fast-follow. If the refresher can't run during the afternoon band, that un-suppress date slips silently. The documented plan was ~206 wallets => full first pass in ~4h; at 60/tick it had done 5 good ticks and then stopped.

**It is also invisible to alerting** — `detect_stalled_pipelines()` returns `[]` because the pipeline has no `pipeline_cadence_watchlist` row yet (correctly, it's <24h old and hadn't banked clean ticks). So nothing pages on this.

**Suggested action (night pass / CC):** measure `get_allday_lock_refresh_wallets` directly — it's a stalest-first ordering query over wmc, the same access shape as `get_lock_check_batch`, whose 07-16 fix was an index-driven rewrite (1,522 MB -> 8.4 MB). If it's the same class, the same lever applies. Cheap interim: raise that fn's `statement_timeout` via proconfig (pure in-DB read, same class as the 07-16 offers-raise 600s ship). Add the watchlist row once it banks 2 clean ticks.

---

## MED — THE COUNTERPARTY WORKER IS NOW REDUNDANT, NOT MERELY SLOW (reframes the 18:20Z MED)

**Source:** `sales_counterparty_backfill_state`, `sales_seller_recovery_state`, `sales_counterparty_recovered`.

The 18:20Z tick logged the claim-timeout collapse and proposed "raise the claim timeout and/or shrink the batch — tune or accept." Cursor positions change the recommendation:

| lane | cursor | direction |
|---|---|---|
| `sales-counterparty-backfill` (CF worker, `*/5`, Flow REST tx decode) | **2026-03-29** | backward |
| `sales-seller-recovery-dune` (Vercel, `47 * * * *`, Dune) | **2026-01-29** | backward |

**The Dune lane has already passed the worker and swept 2026-06-18 -> 2026-01-29 in ~7 hours** — including the entire 2026-03-29 -> 2026-01-29 stretch the Flow-REST worker is still slowly grinding toward. Volume in the last 6h: **131,373** rows into `sales_counterparty_recovered` (Dune-dominated) vs the worker's ~100 rows/tick ~= 7k. The worker is contributing **~5%** of recovery while logging **39 failed runs / 72** (54%) in the same window.

CLAUDE.md already frames the Dune pipeline as "**the supersession** of the multi-week Flow REST grind." The supersession landed today; the superseded worker was never stood down, so both now write the same `sales` columns with no coordination (`apply_sales_counterparty_external` deliberately does not touch `sales_counterparty_backfill_state`).

**Risk read:** standing the worker down is *subtractive* and reversible, removes 12 contending ticks/hour from the hottest table, and loses ~5% of throughput on rows the other lane has already covered. But it is a `sales`-write-lane decision and the runner is a **Cloudflare Worker Cron Trigger** (operator/wrangler), so it is **not autonomously shippable**.

**Suggested action:** owner decision — either (a) disable the CF Cron Trigger and let the Dune lane own the drain, or (b) if the worker is kept for the TS *buyer* side (Dune covers seller only — buyer stays TopShot-only in the worker), narrow it to buyer-only claims so it stops re-walking seller ground already swept. Worth confirming which counterparty columns the Dune lane actually fills before choosing.

---

## MED — THE HEALTH-MONITORING SURFACE ITSELF IS NOW TIMING OUT

**Source:** this sweep.

- `SELECT rpc_ops_snapshot()` — **statement timeout**, twice. It is the documented "FAST BASELINE — one read" for both this monitor and the night pass.
- `SELECT * FROM v_rpc_trust_health` — **timed out at default**, and measured **63.4s** (21:12:34 -> 21:13:38) with the timeout raised to 240s.

At the 15:12Z tick `rpc_ops_snapshot()` returned normally, so this is same-day degradation. The 18:20Z tick hit the same wall and correctly deferred the artifact replay because of it.

**Why it's worth a line of its own:** when the baseline read times out, the failure mode is a monitor run that reports *less* health signal exactly when there is *more* to report. Tonight's night pass will hit this. Notably `pg_stat_activity` showed only **2 active queries** during the timeouts — so this is not connection contention, it is the documented disk-IOPS-burst class, and the levers are the load-reduction ones above, not more indexes.

**Suggested action:** none standalone — it resolves when the load items above resolve. Recorded so the night pass knows to fall back to the individual checks (they all work) rather than treating a `rpc_ops_snapshot()` timeout as an outage. *Practical note for the next run: `detect_stalled_pipelines()` / `check_public_security_invariants()` / `check_secdef_anon_execute_violations()` are set-returning — putting them bare in a multi-column `SELECT` list silently collapses the whole row to `[]`. Wrap each in a scalar subquery (`(SELECT jsonb_agg(x) FROM fn() x)`) or call them one per statement.*

---

## CLOSE — VERCEL-EMPTY-COMMIT-CANNOT-FORCE-REBUILD (18:20Z HIGH) is RESOLVED

The rebuild landed and **`sales-ingest-dune` is LIVE**. 18:11Z still logged `skipped:dune_not_configured`; the **20:11Z tick ran for real**:

    inserted 411 . filled 27,288 . skipped_existing 38,435 . skipped_unresolved 9,562
    skipped_multimoment 4 . map_skipped 0 . windows_done 8 . duration 747,326 ms
    cursor_end now 2025-11-05 (from 2025-12-31)

Shape looks correct against the design: heavy `skipped_existing` (we already hold most 2025 sales), a real `skipped_unresolved` count rather than silent drops, `skipped_multimoment` counted not dropped, `inserted` small and plausible. **This satisfies the "eyeball `pipeline_runs.extra` on the first real tick" gate CLAUDE.md requires before letting it drain** — but the second half of that gate (**watch FMV**) is still open: `topshot_fmv_stale_hours` is 0.0 and `fmv_sanity_flags` 0 right now, which is one clean reading, not a watch. Recommend the night pass re-check `fmv_sanity_flags` + TS FMV distribution after several ticks have drained.

**Note this route shares the same 800s exposure as its sibling** — 747,326 ms = 52.7s headroom (6.6%). The fix proposed in the first item should be applied to both.

---

## Confirm-only (no action)

- **Timeout wave profile** — platform fail rate by hour: 14Z 0.0% . 15Z 1.0% . 16Z 0.9% . 17Z 1.7% . 18Z 3.4% . 19Z 3.2% . 20Z 3.3% . 21Z 4.6%. **All statement timeouts; zero pool exhaustion** (unlike the 10:00Z and 07-18 15:00Z clusters, which had 3 each). Affected: `sales-counterparty-backfill` 39/72, `lock-check-batch` 5/12, `wallet-username-resolver` 4/12, `allday-lock-refresh` 3/6, `pinnacle-nft-resolver` 3/70, `compute-topshot-pack-ev` 3/117.
- **Ingest is NOT affected** — newest sale 21:07:22Z, newest FMV 21:12:34Z, all five FMV freshness metrics green (TS 0.0h / AllDay 0.0h / Golazos 0.0h / Pinnacle 11.1h / UFC 1.0h). The wave is hitting backfill/enrichment lanes, not the live path.
- **pg_cron 2** — `rpc-atlas-pack-ev` 3 fails / 24 runs (was 1/24 at 18:20Z; same contention class, still 21/24 green) and `rpc-remap-misattributed-sales` 1/4 at 18:23Z (the known DAYTIME-CONTENTION item). Neither is a new class.
- **`topshot_impossible_parallel_serials` = 9** vs breach 3 — unchanged from the 18:20Z reading, so the re-accumulation flagged there has **stopped climbing**, not continued. Known self-healing class; interactive/Trevor circ floor-raise, not autonomous. The "if it keeps climbing, fix the floor-seed at ingest" argument is not triggered.
- **`backfill-pack-rip-metadata` self-healed** — the 18:20Z INFO stall cleared; `detect_stalled_pipelines()` is now `[]`.
- **Sentry quiet** — 1 unresolved issue in 24h, `JAVASCRIPT-NEXTJS-1Y` (team detail statement timeout), **1 event / 1 user, last seen ~13:00Z**. Despite the wave escalating all afternoon it has **not** produced new user-facing errors — worth stating plainly, since the 15:12Z tick's concern was exactly that pool exhaustion had crossed into user-facing pages. It has not re-crossed since.
- **Vercel 0 ERROR** across ~20 deploys in the window; the CANCELED ones are `ignoreCommand` skips (docs-only) and superseded-concurrent builds, both by design.
- **Artifacts** — 17 enumerated; all 15 backing relations resolve with expected relkind (incl. the new `panini_squeeze_totals` view from today's Panini KPI fix). **Heavy payload replay deferred again, same measured reason as 18:20Z and now stronger**: `rpc_ops_snapshot()` and `v_rpc_trust_health` both timed out during this sweep, so firing `rpc-live-health`'s 14-day `MATERIALIZED` scan over `fmv_snapshots` would load the exact constrained resource and risk misattributing a contention timeout to a broken artifact. Replay wants a quiet window (<=06:00Z).
- **`rpc-live-health` footer credits a dropped table** (`pinnacle_fmv_snapshots`, dropped 06-08) — carried unchanged from the 15:12Z tick, still open, still display-only.
- **Drift vs `metrics-latest.json`** — DB 10,054 -> **10,197 MB** (+143, benign); editions TS 19,451 -> 19,464 (+13); candy_mlb 125 flat; sentinel TS-UUID-48h 0; `ts_uuid_dupes_created_24h` 0; `unmapped_resolution_backlog_max` 25.
