# Daytime monitor inbox — 2026-06-25T15:15Z (~08:15 PDT)

Platform GREEN except the one OWNED + queued BREACH (HISTORY-BACKFILL-UNMAPPED-SPIKE, holding). No clock skew (DB now() 15:07Z == shell 15:06–15:15Z). First monitor tick since the 06-25 night pass (drained the inbox + released the lock 14:10Z). **No new fix-candidate** — but two fresh sales-path prod ships landed this morning AFTER the night pass / last monitor sighting, so this file is a POST-SHIP-WATCH handoff + one state update for tonight's pass.

Health one-liner: detect_stalled [] · pgcron [] · alerts 1 benign INFO (ufc_sales resolving_editions) · security 0/0/0/0 · pipeline fails 0 in last 6h · sentinel TS-UUID-48h 0 · trust 8/9 (1 owned BREACH = unmapped 2370) · editions FLAT (TS 17435 / AllDay 6191 / Golazos 581 / UFC 518) · FMV TS H+M 4544 improving / AllDay 908 flat (reconcile, sanity 0) · conflation 105 / thin_fmv 122 · DB 5445 MB (+11 benign) · cross-coll mats fresh (cohort 04:10Z/158, overlap 04:25Z/249, both pg_cron steps succeeded) · Vercel prod 8707ab5 READY, 0 ERROR/20 · Sentry 3 stale single-event smoke transients (NEXTJS-W/1C/X 19–23h), 0 in last 12h · artifacts: rpc-live-health insights_counts all 11 boards resolve (squeeze 501 / set_squeeze 239 / pack_reality 7 thin / rookies 61 / market 501 / first_mint 501 / cross_collection 1 / pinnacle_scarcity 501 / offer_spread 501 / deals 501 / trophies 501) + tracked-fmv-conf 25 / offer-sanity 360 / top-sales 1069 / serial-premiums 174.

## POST-SHIP WATCH handoff (for night pass) — NEW UFC studio sales-path ship, first monitor sighting

Prod advanced **11c8a23 → 3573f56 → 8707ab5** (Trevor, both READY; 8707ab5 current ~15:06Z). New fresh sales-path writer — first seen by any monitor (the 14:05Z tick predates these by ~25min).

- **3573f56** (feat): new `/api/cron/ufc-studio-sales-history-backfill` — the one studio collection with no `edition_id` filter, so a single global block_time-ASC cursor walk over ~860k purchased rows, resolving each in-process to the 518 cataloged UFC editions via `(lower(athlete)|edition_size) → editions.id` (0 ambiguous keys). Matched rows → `sales` (`source='ufc_studio_history_v1'`), dedup by `transaction_hash`; uncataloged/pack rows skipped. Singleton cursor-state table `ufc_studio_sales_history_state`. Self-terminates to a cheap no-op at `done=true`.
- **8707ab5** (chore): bumped its cadence `41` → `1,21,41 */hr` (3×/hr) to drain the ~860k-row walk in ~half a day, + repo-synced the studio/UFC migrations.
- **Verified clean this tick:** the 14:41Z tick logged `ok=true`; **16,745 UFC studio sales / 16,745 distinct tx (perfect dedup)**, sold_at 2022-02-15→2022-02-18 (the deepest tail yet — global walk just started at the oldest block). `ufc_studio_sales_history_state` **RLS-on** (the 06-24 morning RLS-miss class NOT repeated). **editions FLAT** (UFC 518, edition-resolved, zero creation). UFC unmapped flat at 4 (this drain writes ZERO unmapped — it skips uncataloged/pack rows, like the other studio drains). `fmv_sanity_flags` 0. security 0/0/0/0.
- **Night pass — confirm:** `ufc-studio-sales-history-backfill` keeps logging `ok=true` across its 3×/hr ticks, `sales` source `ufc_studio_history_v1` climbs with distinct_tx == rows (no dup-key), UFC editions stay FLAT at 518, no UFC unmapped growth, `fmv_sanity` 0, and it flips to `done=true` no-op when the global walk completes. Revert (per commit): `DELETE FROM sales WHERE source='ufc_studio_history_v1'; DROP TABLE public.ufc_studio_sales_history_state;` + remove the vercel.json cron.

## State update (NOT a new candidate) — HISTORY-BACKFILL-WATCHLIST studio leg now SHIPPED

8707ab5 also repo-synced `watchlist_studio_sales_history_backfills` — confirmed live: the **3 studio crons** (`allday-/golazos-/pinnacle-studio-sales-history-backfill`) are now in `pipeline_cadence_watchlist` @600m/medium/active. That **resolves the studio half of the queued HISTORY-BACKFILL-WATCHLIST** (the night pass had it gate-met/ship-eligible; Trevor shipped it). Remaining open: the on-chain 5-cron leg (~21h/8-ticks, nearing its 24-48h gate) is still un-watchlisted; and `ufc-studio-sales-history-backfill` itself is NOT watchlisted (correctly — it's a finite self-terminating global walk that goes intentionally silent at done, so a cadence row would false-positive; leave it off).

## Owned symptoms — trajectory only, NOT re-logging (single root, owned ledger)

- **HISTORY-BACKFILL-UNMAPPED-SPIKE** — `unmapped_resolution_backlog_max` **2370**/100 BREACH, HOLDING (== the night pass's 13:55Z measure; newest TS-flowty ingest 12:28Z, between `*/3` ticks; +603/6h enqueued, 0 resolved/6h → still accumulating each tick). 100% TS `marketplace='flowty'` (the `topshot-flowty-sales-history-backfill` #3), historical sold_at 03-28→present, quarantined OUT of `sales` (NOT corruption, no FMV/board impact, fmv_sanity 0). AllDay leg 203 flowty + 95 nflallday, Golazos 19, UFC 4 — flat/small. Sharpened fix already queued (build `topshot-studio-sales-history-backfill` on `lib/studio-sales-history.ts` + retire the Flowty #3) — and note the UFC studio drain shipped today is the same proven external_id/key-resolved, zero-unmapped template, so the TS-studio leg is now a well-trodden build.
- **conflation guard 105** — HOLDING (105 @ night pass / 105 now); parallel sales landing on base editions before `::` rows exist; converges on the daily `refresh-conflated-editions` remap. Suppressed from deal boards. Owned.
- **offer_edition_gap_max_usd 0/50 ok** — self-cleared (was $192 mid-night-pass, ratcheted to $0 by `raise_edition_offers_from_chain`); known oscillating OFFER-SANITY class, not an incident.

## Benign / not-an-incident (do NOT chase)
- 1 `get_pipeline_alerts()` INFO (ufc_sales resolving_editions) — benign/long-standing.
- 0 pipeline fails in last 6h (the recurring 19:01–19:33Z contention micro-cluster is >18h ago, recovered).
- Sentry 3 transients are all 19–23h-old single events (incl NEXTJS-1C, the already-fixed RLS-smoke true-positive); 0 new/0 spiking.
