# Daytime monitor candidates — 2026-07-17T1507Z (first-tick-of-day)

Health GREEN otherwise: security 0/0/0/0, trust 15/15 ok (0 breaches; impossible_parallel 1/3), stalled_pipelines [], pg_cron failures [], Sentry 0 new/24h, cross-collection refresh fresh (ccm step1 04:10Z / step2 04:25Z, both succeeded, 154 rows), Vercel prod `df03165f` READY. Candy Drop-1 lands ~10 AM PT today — new `candy_mlb` rows / candy pipeline_runs are EXPECTED, not anomalies.

## NEW candidate

### ATLAS-POOL-INGEST-UNMAPPED-SHAPE (LOW-MED · CC/edge-fn) — NEW
- **Source:** pipeline `topshot-atlas-pool-ingest`, 177 fails/24h, still firing 2026-07-17T14:18Z (rapid retry loop, ~1 dist/2s descending 8524→8523→…). `pipeline_runs.extra = {"reason":"unmapped_shape","dist_id":"85xx","sample_keys":["editions","totalCount"]}`.
- **Read:** the v2 edge fn `ingest-topshot-atlas-pool` (built 2026-07-16, `upsert_topshot_atlas_pool` + `v_topshot_atlas_pool_targets`) is receiving a `{editions, totalCount}` payload (the raw studio/GQL editions-catalog shape) that its server-side normalizer doesn't recognize, so every dist logs `unmapped_shape` and writes nothing. Either the harvest (`scripts/atlas-pool-harvest.ps1`) is feeding the wrong endpoint's response shape, or the normalizer's expected shape drifted from what Atlas now returns. This is the Atlas *remaining-pool* enrichment lane (feeds the underpriced-#1s / atlas pool board) — NOT a user-facing site surface, and distinct from `topshot-active-listings-ingest` (the ATLAS-BLOCK item CLOSED 07-17, home-machine lane).
- **Risk:** LOW blast radius (enrichment only; no FMV/sales/site path; failures are pruned daily). But it is a *persistent tight retry loop* burning pipeline_runs volume and never advancing — worth a look at the v2 normalizer's shape-matching (`editions[]`/`totalCount` is a known Atlas/studio envelope; the normalizer likely expects the pool-node shape, not the catalog-page shape).
- **Suggested action (CC):** inspect `supabase/functions/ingest-topshot-atlas-pool/index.ts` normalizer vs the actual `{editions, totalCount}` payload; either teach it the `editions[]` shape or fix the harvest to POST the pool-node shape. Not autonomous (edge-fn deploy + ingest-adjacent). If it's operator harvest feeding the wrong shape, no code change needed — just correct the PS1 source endpoint.

## Watch / no-action (recovering or known)
- **LOCK-CHECK-BATCH-DEADLOCK-UPTICK** — 16 fails/24h but latest 4 runs ALL ok=true (14:38/14:08/13:38/13:08Z). CC's `apply_lock_check_batch` SKIP LOCKED fix (07-17) is holding; self-recovering. Post-ship watch PASS, no new action.
- **Normal overnight-contention family** (fmv-recalc 8, wallet-username-resolver 7, analytics-smoke 7, compute-topshot-pack-ev 6) — all latest-ok, stalled_pipelines [] confirms none silently stopped.
- **pinnacle_ask_stale_hours** retired from `v_rpc_trust_health` today (ASK-unify Phase 2, `524a01c9` READY) — trust metric count 16→15 is EXPECTED, not drift.
