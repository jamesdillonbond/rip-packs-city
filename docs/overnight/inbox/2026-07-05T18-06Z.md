# Daytime monitor inbox — 2026-07-05T18:06Z (~11:06 PDT, tick 2 of day)

Platform GREEN. Security 0/0/0/0 (invariants/secdef-anon/rls-off-base/anon-write-holes all []), trust 16/16 (0 breaches), `check_pgcron_recent_failures()` [], Sentry 0 unresolved/24h, Vercel prod `ef80868c` READY (no ERROR deploys in 2d window; CANCELED = docs ignoreCommand, BLOCKED = superseded concurrent deploys), sentinel TS-UUID-leak 48h **0**, rpc-live-health payload validated (12/12 surfaces resolve; FMV write 18:08Z / pack-EV 18:07Z / offers 18:07Z fresh). Heavy 07-05 daytime CC wave (special-serial owner resolver, AllDay serial on-chain rewrite, Population A/B subedition remap+probe, VGN-263 de-conflate, 134 collision knots, Jrue split, sets_summary refresh + jobid37 cron, golazos mojibake, serial=0->NULL sentinel) — all deploys READY, no new Vercel error class. Post-ship watch of that wave is the night pass's job.

Headline metrics: security 0/0/0/0; FMV TS HIGH+MED **4,894** (1312+3582, improving from 4,785 07-04) / AllDay 835 / UFC 15 / Golazos 4; editions TS **17,680** (+190 vs 07-04 = deliberate 07-05 subedition cataloging, sentinel-leak 0 / ts_uuid_dupes 0 = NOT a writer leak) / AllDay 6,191 / Golazos 581 / UFC 518; DB **8,103 MB** (+286/24h = ts_history_backfill_v1 drain + Population-B probe queue 134,071 rows, benign/attributable); sales 24h TS 3,014 / AllDay 1,026.

## 2 LOW candidates

### 1. ULTIMATE-FMV-RECALC-V1-MISSED-TICK (LOW; likely self-heals 07-06 06:35Z)
- Source: `detect_stalled_pipelines()` + `get_pipeline_alerts()` (both severity **info**); `pipeline_runs`.
- Signal: ran clean daily at 06:35Z for 7 straight days (06-28 -> 07-04, all ok=true), then MISSED the 07-05 06:35Z tick. Now ~35.5h silent vs 1440m threshold. The embedded alert note reads "Repaired this session after 49h silence" -> it has gone silent before and needed manual repair, so it is flap-prone.
- Risk read: LOW blast radius -- Ultimate is the rarest/smallest FMV surface; one missed daily tick = <=~1 extra day stale on Ultimate-tier FMV only (NOT FMV core / deal boards / pack-EV). Auth RPC_ADMIN_TOKEN (likely a cron-job.org entry).
- Suggested action (night pass): verify whether the 07-06 06:35Z tick fires. If it self-heals (like the 07-04 TOPSHOT-FMV-POPULATE-MISSED-TICK), close. If it misses a 2nd consecutive day the daily trigger is genuinely stuck -> operator/CC re-enable/verify the cron-job.org (or GHA) entry. Do NOT ship blind (secret-bearing cron config is operator-only).

### 2. BADGE-CATALOG-STALE-429 (LOW/watch; possibly just idle-with-no-work)
- Source: `pipeline_runs` (topshot-badge-catalog); GHA badge-sync.
- Signal: last run 07-04 21:38Z = ok=false ("Top Shot GraphQL failed with 429"); NO run since (~44h). Cadence is irregular/event-linked (07-02 16:26, 07-01 x2, 06-30, 06-29, 06-28 x3), so a gap alone is not proof of breakage -- but the last ATTEMPT failed and today's subedition wave added ~190 new TS editions that may want badge cataloging.
- Risk read: LOW -- badges render on edition/moment pages; a 1-2d catalog lag is cosmetic. NOT watchlisted, NOT trust-flagged (edition_integrity_flags 4, ok). 429 is the known-benign TS-GQL rate-limit class.
- Suggested action (night pass): confirm the badge-sync GHA is scheduled + firing (single un-retried 429 vs a stuck workflow), and spot-check that a couple of today's newly-cataloged TS editions carry badges. If genuinely stuck it is a GHA/operator item; if just idle, close.

## Carried / known (NOT re-logged, per ledger/focus)
- `apply-fmv-haircut` 06:31Z daily pool-timeout (overnight-contention transient, re-fires 07-06); `classify-acquisitions-multicollection` AllDay statement-timeout flap (night-count 4; latest 18:06Z tick ok=true); ALLDAY-V1-UNMAPPED-DRIFT (AllDay unmapped_open 3,439 held out of `sales`, owned); ufc_sales resolving_editions (info, 203 open); the standing owned/operator/gated queue in ledger.md. STEER honored: SERIAL-FMV weekly by design; evm-429 benign.
