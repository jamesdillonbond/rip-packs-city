# Daytime monitor — 2026-06-04T00:24Z

Run: `rpc-daytime-monitor` (daytime, ~17:24 PDT June 3). READ-ONLY sweep. Repo: shared `rip-packs-city` (bot clone not mounted). Lock: released marker (overnight pass finished 08:23Z, ~16h ago) — safe to write.

## Health: ✓ GREEN
- `detect_stalled_pipelines()` = `[]`. Overnight C-PAYER/C-PIN watchlist ship still holding — no stall.
- Security 0/0/0: 0 RLS-off public base tables; 0 anon/authenticated write grants on RLS-off base tables; 0 anon-readable non-`security_invoker` views (post today's F4 grant-revoke + `v_fmv_sanity_flags` v2 — no exposure crept in).
- Pipelines (6h): only the known transient cron-rush class. A live 00:00Z rush was in progress at sweep time: `pinnacle-nft-resolver` (pool timeout @00:05Z + @18:15Z), `allday-unmapped-resolver` (pool timeout @00:01Z), `allday-listings-indexer` (pool timeout @18:15Z). All self-recovering; no logic faults; none deploy-attributable. `compute-topshot-pack-ev` time_budget ×2 — see PEV1.
- FMV: `fmv-recalc` fresh (~18m; ~5.0k TS + 3.6k AllDay snapshot rows in trailing 2h; primary writer healthy). TS HIGH+MED 932→971→**1007**, NO_DATA 4424→4347→**4153** (still improving — F5 NO_DATA recovery + F2/F3 mis-key cleanup compounding). AllDay flat (HIGH+MED 271, NO_DATA 522).
- Sentinel TS-UUID-48h **34** (baseline 43; <250 ok). unmapped_sales 292 (flat vs 18:18Z; baseline 278; nflallday 242 / flowty-archival 49 / golazos 1). DB 6022 MB (baseline 5999). editions TS 16,350 (+6), AllDay/Golazos/UFC flat.
- Vercel: 20/20 recent prod deploys READY, **0 ERROR**. HEAD `f3011d9` (FMV mis-key sweep F2/F3/F4/F5) stable ~9h — no new deploy since the 18:18Z monitor. Post-ship watch GREEN.
- Sentry: 1 unresolved in 24h = `NEXTJS-15` (PIN1, pinnacle-listings-indexer, 3 events / 2 users, last seen ~19h ago, NOT spiking). No new issues.
- Artifacts: 12/12 data layers validated healthy. Insights views all execute + non-empty (squeeze / deals / offer-spread / rookie-index / market-index-daily / pack-reality-stats); `get_rpc_traction_snapshot()` non-null (live funnel); `get_wallet_collection_stats('0xbd94…')` works post-F4 (fmv_total $64,765.80 excl-STALE / $2,672.14 stale ×178 / 14,274 moments). Squeeze full-`count(*)` timed out under the live 00:00Z cron rush — bounded read returns rows fine; NOT breakage (was 3,099 @18:18Z, nothing changed it since).

## New candidate (1)

### PEV1 · LOW · operator/CC — `compute-topshot-pack-ev` zeroes a run with `time_budget_exceeded_after_fetch` when TopShot GQL is slow
- **Source:** `pipeline_runs` (`compute-topshot-pack-ev`, fn v19). Last ~12 runs (30-min cadence, batch_size 8): 2 failed — 23:38Z + 19:38Z — both `time_budget_exceeded_after_fetch` with `fetch_phase_ms` ≈ 113–116k (the whole ~116s budget spent in upstream fetch; `ev_rows_written:0`), `errors_sample` = `GetPackEditions` / `GetPackListing_DynamicData` → `fetch: Signal timed out`. Successful runs sit at `fetch_phase_ms` 52–96k and write 6–8 EV rows. So failures = upstream TopShot-GQL latency spiking past the budget, not a DB/logic fault.
- **Risk read:** very low, **no current product impact** — `topshot_pack_ev_targets`: 0 packs >7d stale, EV fresh to ~1h ago, null_ev 0. The queue (794 targets ÷ 8/run) self-drains; a zeroed run just retries next tick. Distinct from M1 (that's `topshot-fmv-populate`, DB pool/statement timeout; this is upstream fetch-latency, a different pipeline + root cause). Not a stall — `detect_stalled_pipelines()` clean; recent successes every cycle.
- **Action (operator/CC, NOT night-pass auto-shippable — pack-EV route logic is off-limits):** optionally reduce `batch_size` below 8 or lower the per-node fetch timeout so one slow upstream node can't consume the whole run budget and zero the batch; or accept as-is and drop `time_budget_exceeded_after_fetch` runs from ok-flag alerting to cut recurring `pipeline_runs` noise. Pure observability/tuning; no destructive SQL, no schema change, no route-logic risk to FMV/pricing.

## No action / already tracked (not re-raised)
- Recent-ship watch GREEN: overnight C-PAYER/C-PIN (`detect_stalled` stays `[]`) and Claude Code F-items / `f3011d9` (FMV HIGH+MED climbing, NO_DATA falling, `get_wallet_collection_stats` F4 split correct) — nothing to revert.
- M1 (reschedule `topshot-fmv-populate` off the :00 cron peaks) logged 18:18Z — still open, not re-raised.
- PIN1 (`NEXTJS-15`), N2 (hydrator candidate-read statement timeout), and the transient pool-timeout cron-rush class (pinnacle-nft-resolver / allday-unmapped-resolver / allday-listings-indexer) all already tracked.
- F1/F2-TierB (broader serial>circ mis-key batch + the 65 residual Cosmic 8:62 Tier-B sales) stays in the ledger; F3 guard protects WAP meanwhile.
