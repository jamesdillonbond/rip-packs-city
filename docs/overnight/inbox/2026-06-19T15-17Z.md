# Daytime monitor inbox — first tick (08:06 PDT)

Platform GREEN. One refinement candidate (updates an existing 06-19 night-pass queued item — do NOT double-count as new).

## Candidates

### ALLDAY-SERIAL-BACKFILL-CRON — UPDATE: cron is now wired, but its only run FAILED on Cloudflare 1009
- **Source:** pipeline_runs `allday-listing-serial-backfill` — exactly ONE run ever, 2026-06-19 14:47:29Z, ok=false, error `http_403:error code: 1009`. The night pass (08:02Z) queued this as "0 runs — operator wire a cron"; it has since been wired (a cron fired it at 14:47Z, between ~08Z and now).
- **What changed:** the `backfill-allday-listing-serials` edge fn (`b86caaf`, v1) now has a trigger, but its first/only invocation hit a Cloudflare 1009 (edge/IP block). `allday_moment_serials` holds only 2 rows; the deal-board AllDay leg `low_ask_serial` stays NULL until a run succeeds.
- **Risk read:** LOW. Serial DISPLAY only — the AllDay deal BUY LINK already works (Item 1, `low_ask_nft_id`). No FMV / pricing / sales impact. 1009 is the known transient Cloudflare-block class (cf. evm-transfers 429, allday-consumer WAF), so the next tick may simply succeed. BUT `b86caaf` says the edge fn routes through topshot-proxy `/allday-consumer` (which is meant to dodge Cloudflare) — a 1009 on that path could be a persistent proxy-egress block, in which case the serial backfill never populates.
- **Suggested action (operator / CC):** do NOT close ALLDAY-SERIAL-BACKFILL-CRON just because a cron now exists — confirm the NEXT scheduled run logs ok=true and `allday_moment_serials` grows past 2. If runs keep returning 1009, verify the edge fn's AllDay-consumer fetch actually goes through the proxy worker (not a direct nflallday.com/consumer call) and that the worker's egress isn't edge-blocked. Only after ≥1 ok run add the generous `pipeline_cadence_watchlist` row (L1 lesson: never watchlist before first successful fire).

## Notes (not candidates — for the record)
- **AllDay FMV NO_DATA elevated vs the 06-17 baseline (684 → 2341)** but BENIGN: 2340/2341 NO_DATA AllDay editions have zero sales in 90d (verified), writers fresh (allday-fmv-populate/fmv-recalc/allday-sales-indexer 0 fails/24h), HIGH+MED actually IMPROVED (768 → 851), and the latest-per-edition counts reconcile EXACTLY to 6,191 AllDay editions. Honest LOW/STALE → NO_DATA reclassification of thin/aging sale-less editions, same class as the (now-closed) FMV-HIGHMED-DIP-WATCH. No action.
- **BUYERBF-PERINVOCATION-WORK post-ship watch = PASS.** topshot-buyer-backfill last 14 runs all ok=true + logging, max 711.8s (< the 770s watch threshold, < 800s Pro cap), detect_stalled []. Overlap real but bounded; item holds as queued.
