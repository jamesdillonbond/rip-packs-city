# Daytime monitor — 2026-06-25T03:16Z (late-evening tick, ~20:16 PDT Jun 24)

Platform GREEN. **No new actionable candidate** — STATE UPDATE: Trevor is actively shipping the deep-history studio-platform program tonight (two new prod ships since the last tick on the sales-ingest path), and the two owned HISTORY-BACKFILL symptoms are holding (not new incidents). DB now() 03:06Z AGREES with the sandbox clock (no ~5.5h skew). Prior tick 01:06Z (closed UFC-VIDEO-BACKFILL-AUDIT-TABLE-RLS as resolved). Lock RELEASED (stale 13:43Z nightly). §1a first-tick extras SKIPPED (ran 15:14Z).

## NEW production ships since the last tick — POST-SHIP WATCH (for night pass)
Prod advanced **0ae1b26e → 7be31e3 → 77f0c65** (Trevor, both READY; 77f0c65 current ~03:13Z). This is the studio-platform GQL deep-history lever surfaced @01:06Z (open-cc item #2), now BUILT:
- **7be31e3** (~02:16Z): new `/api/cron/allday-studio-sales-history-backfill` — pages `searchAllDayMarketplaceHistory` per edition, fills 2023-11→present AllDay sales the forward indexer/on-chain backfill missed. Cron `52 */3`.
- **77f0c65** (~03:13Z, current prod): refactors the AllDay drain into shared `lib/studio-sales-history.ts` + adds `/api/cron/golazos-studio-sales-history-backfill`. Cron `56 */3`. Backed by `golazos_studio_sales_history_progress` (audit_20260624).
- **KEY SAFETY PROPERTY (both):** studio `searchXMarketplaceHistory` filters by `edition_id == editions.external_id` (numeric) → **ZERO edition creation, no mis-key, and NO `unmapped_sales` writes** (explicitly unlike the on-chain backfills). So this approach does NOT add to HISTORY-BACKFILL-UNMAPPED-SPIKE.
- **Verified clean this tick:** both deploys READY · both new progress tables (`allday_/golazos_studio_sales_history_progress`) **RLS-ON** (audit_2026* RLS-off count = 0 — the morning UFC sibling-RLS-miss class is NOT repeated) · security 0/0/0/0 · **683 sales** already inserted via Trevor's in-session verification drains (source `allday_studio_history_v1`/`golazos_studio_history_v1`, external_id-keyed; editions stayed FLAT, confirming zero edition creation).
- **Crons have 0 runs yet** (deployed after their slots; next ~03:52Z allday / ~03:56Z golazos). **Night pass: confirm first ticks log ok=true, sane AllDay/Golazos sales inserted, editions FLAT, NO unmapped growth from these, fmv_sanity 0.** Revert (per commit): `DELETE FROM sales WHERE source IN ('allday_studio_history_v1','golazos_studio_history_v1');`
- **Intersection (NOT a new incident, useful for the night pass's existing fix-decision):** the same studio-platform GQL exposes `searchTopShotMarketplaceHistory` (external_id-keyed) — the natural resolver/replacement for the TS-Flowty #3 backfill that IS feeding HISTORY-BACKFILL-UNMAPPED-SPIKE. The TS leg is not yet built (open-cc item #2); the now-shipped AllDay/Golazos studio precedent is the template.

## Owned HISTORY-BACKFILL symptoms — trajectory only, NOT re-logging (same single root, owned ledger l.675/676)
- **HISTORY-BACKFILL-UNMAPPED-SPIKE**: trust-health `unmapped_resolution_backlog_max` **1338**/100 BREACH — HOLDING at 1338 (725 nightly → 996 @18:06Z → 1338 @01:06Z → 1338 now); the steep daytime climb has plateaued. Raw TS flowty/onchain open ~1525 (the #3 backfill, historical 03-30→04-18), quarantined OUT of `sales` (NOT corruption, no board/FMV impact). AllDay leg 83 (nflallday/onchain_dapper_v1), Golazos 16 — flat/small.
- **Conflation guard 97** — HOLDING (97 @01:06Z / 63 @18:06Z); backfill parallel-sales landing on base editions before `::` rows exist; converges on the daily `refresh-conflated-editions` remap (next ~15:17Z). Suppressed from deal boards.

## Health summary
- `detect_stalled_pipelines()` [] · `check_pgcron_recent_failures()` [] (THIN-FMV-GUARD-CONTENTION stays cleared) · `get_pipeline_alerts()` 1 INFO (ufc_sales resolving_editions, benign/long-standing)
- pipeline_runs ok=false last 6h: **0** (clean; the 19:15–19:33Z contention micro-cluster @01:06Z has not recurred)
- security **0/0/0/0** (RLS-off base [], anon/auth-write-on-RLS-off [], `check_public_security_invariants` [], `check_secdef_anon_execute_violations` []) — UFC table + both new studio progress tables RLS-on
- sentinel TS-UUID-48h **0** · `ts_uuid_dupes_created_24h` **0/200** (no writer leak) · trust-health **8/9** (1 BREACH = the owned unmapped spike; other 8 ok incl fmv_sanity 0/1, pinnacle_ask 0.2h, pinnacle_fmv 17.0h, edition_integrity 4/50)
- editions: AllDay/Golazos/UFC **FLAT** (6191/581/518); TS **17435** (FLAT vs 01:06Z; UUID-form frozen 6428, leak_48h 0 = canonical-only growth, no leak)
- FMV **TS HIGH+MED 4513** (improving: 4399→4413→4420→4475→4513) / **AllDay 903** (flat) — TS sum 17433 vs 17435 editions (gap 2 benign `::`), AllDay 6191=6191 exact; writers fresh 03:09Z, fmv_sanity 0 · TS sales 3h 5240 (backfill wave hot) · conflation 97 (owned) · thin_fmv 109
- DB **5314 MB** (+11 over ~2h, benign creep) · Vercel **0 ERROR** across 20 (prod 77f0c65 READY; monitor/docs commits CANCELED as expected) · Sentry **3** unresolved, all single-event smoke transients (NEXTJS-W pinnacle-overview 7h / NEXTJS-1C the stale RLS-smoke true-positive [already resolved] 8h / NEXTJS-X golazos-analytics 11h), **0 new groups since the last tick**
- Vercel runtime errors: all known/benign classes (url.parse DEP, allday-fmv-populate 403 WAF, wmc dup-key race, contention statement-timeouts) + PINNACLE-EDITION-KEY-UUID-CAST (already queued @15:14Z; new keys WDAS-OEV1-WINN/ASTC, same class, last 17:23Z) — nothing new or attributable to 7be31e3/77f0c65
- Artifacts: all **12** flagship insights backing relations resolve + return rows (squeeze/deals/scarcity 500+, serial-prem 176, perfect-mint 14, underpriced 11, offer-sanity 320, rewards 1, top-sales/trophies 500+, rookies 61, tracked-fmv 23). Today's ships = editions UPDATEs + new cron routes + RLS-on progress tables; no artifact backing object changed. **None broken, none repaired.**
