# RPC daytime monitor — inbox candidates

- **Run:** 2026-06-01T18:24Z · read-only daytime sweep
- **Platform state:** GREEN (security 0/0 · `detect_stalled_pipelines()` `[]` · 20/20 deploys READY · FMV fresh ~28m · Sentry no new/spiking · 6/6 insights backing views OK)
- **Lock:** `docs/overnight/.lock` present but ~4h old and contents marked `RELEASED` (embedded start 13:54:59Z) → treated as STALE per its own >45-min rule; took over, inbox commit permitted.
- **Baseline:** `metrics-latest.json` @ 13:54Z (night pass, shipped nothing / NO-PUSH).

Two NEW low-risk candidates this run. Nothing in the ledger or Declined list re-raised.

---

## C1 — [LOW–MED] `topshot-listing-cache` (+ `-v2`) daytime cadence stretched ~1h → ~5–6h today; sits in the soft-slowdown band right under the Q10 360m stall threshold

- **Source:** `pipeline_runs` (pipeline `topshot-listing-cache`, history last 24h) + the Q10 watchlist row shipped this morning.
- **Observation:** every run is clean (`ok=true`, `rows_written=0`, ~2.5–3s) — this is a *trigger-cadence* change, not a route failure. Intervals:
  - 05-31 daytime: ~1.0–1.3h apart (14:47, 16:11, 17:35, 18:51, 20:10, 21:19, 22:20, 23:37Z).
  - 06-01: 02:00 → 07:08 (5.1h) → 13:04 (5.9h) → **no run since (5.3h at sweep, 18:24Z)**. Four consecutive ~5–6h intervals since ~00:00–02:00Z — a sustained shift, not a one-off gap. `-v2` moves in lockstep (also last 13:04Z).
- **Why it matters:** listing-cache feeds `cached_listings` → `badge_editions.low_ask` → `fmv-recalc` ASK_ONLY (load-bearing for FMV honesty since `65421e26`) and the just-shipped `/insights/offer-spread` + `/insights/deals` boards. A 5–6h refresh vs ~1h is a real freshness regression for the ASK surface — though **no user-visible impact yet** (TS ASK_ONLY 676 vs 680 baseline; offer-spread 5,560 / deals 121 both flat).
- **Threshold tension:** Q10 set `max_silent_minutes=360` (deliberately below the 7.9h Q3 hard-stall, above the ~206m daytime envelope — the Q9 don't-false-positive lesson). Today's sustained 5–6h cadence lands *inside* the 300–360m soft-slowdown band, so `detect_stalled_pipelines()` correctly stays `[]`. If no run fires by ~19:04Z it WILL trip the watchlist.
- **Risk:** low — read-only investigation.
- **Suggested action (operator / night-pass):** confirm the cron-job.org cadence for `topshot-listing-cache` (+ `-v2`) wasn't inadvertently slowed around 06-01 00:00Z; if the slower cadence is intentional/natural, no action — Q10 already guards the hard-stall case. **Do NOT blindly lower the 360m threshold** (Q9). Secondary: `rows_written=0` is steady across all 16 runs back to 05-30 (the TS GQL listings feed returns nothing new each sweep) — pre-existing, not new this run; only worth a glance if ASK_ONLY freshness starts to degrade.

## C2 — [LOW] `v_moments_needing_hydration` candidate-read periodically hits a statement timeout (`topshot-moments-hydrator` 3/132 fails/24h)

- **Source:** `pipeline_runs` (pipeline `topshot-moments-hydrator`), error `candidate_read: v_moments_needing_hydration select: canceling statement due to statement timeout`.
- **Observation:** 3 of 132 runs in 24h fail on the candidate-read view `SELECT` during the cron rush (12:12Z, 18:12Z seen); self-recovers next tick (`last_ok` 17:52Z). ~2.3% — transient, not a stall, no observed hydration backlog. Does **not** currently fire Sentry.
- **Risk:** low — a named, slow view = a concrete index/optimization target (cf. the 2026-05-24 `drain_fmv_cold_tail` grouped-agg rewrite that killed a similar correlated-subquery statement timeout).
- **Suggested action (night-pass / CC):** profile `v_moments_needing_hydration`; if a correlated subquery or missing index is the cost, optimize it so the candidate read stops timing out at peak. Low priority (self-healing today).

---

### Sweep evidence (for the night-pass baseline; not findings)

- `detect_stalled_pipelines()` = `[]`.
- `ok=false`/6h (all transient-with-recovery, cron-rush connection-pool / statement-timeout / Base-429): `check-alerts` 1/70, `pinnacle-listings-reconcile` 1/94, `pack-events-ingest` 5/95 (recovered), `topshot-moments-hydrator` 3/132 (→ C2), `evm-transfers-ingest` 4/23 (known Q6, `8605c43` shipped to soften it).
- FMV: TS HIGH+MED **877** (HIGH 234 / MED 643; NO_DATA 5021 ↓ from 5109) · AllDay HIGH+MED **265** (STALE 429 ↓ from 472). Writers fresh 17:48Z.
- Sentinel TS-UUID-48h **36** (baseline 40). Security 0/0. unmapped_sales open 147 (flat) / new_24h 21. editions TS 16,310 (+2). DB 5916 MB (+4).
- Sentry: 7 unresolved, all known cry-wolf/watch (NEXTJS-15 gated AllDay 3 events/19h; the 6-issue smoke cluster each 1 event ~18h ago = 00:00Z rush only — none recurred at 06/12/18:00Z, `a79b778` rpcRetry holding).
- Vercel: 20/20 READY, 0 ERROR. Trevor shipped past baseline today: `2f26044` (insights offer-spread + deals pages), `e246f22` (Trade Hub shelve guard, CLAUDE.md Open #3), `8605c43`, `eda078c`, `56a47ef` — no regression.
- Insights backing views all resolve: squeeze 3,099 · rookie_index 61 · market_index_daily 544 · pack_reality_stats 1 · offer_ask_spread 5,560 · deals_vs_fmv 121. (Cowork artifact HTML not sandbox-mounted this run — validated backing data directly instead.)
