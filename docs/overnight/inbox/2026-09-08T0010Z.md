# Daytime monitor candidates — 2026-09-08T00:10Z (17:06 PT tick)

Source: `rpc-daytime-monitor` health sweep. Written to mount, push unavailable (cloud NO-PUSH; `git push --dry-run` denied at the repo-authorization layer). Read-only sweep otherwise clean — see the run summary. One new candidate below; nothing else is a finding.

## Candidate 1 — `v_offer_sanity_flags` = 1313 while its named self-clear pipeline (`offers-sweep`) is dead; re-measure the trend (LOW)

- **Source:** artifact `rpc-qa-scorecard` payload (`offsan` card) + `pipeline_runs`. Measured 2026-09-08T00:10Z.
- **Observation:** `v_offer_sanity_flags` = **1313** (`gql_blank_chain_has` 814, `chain_exceeds_gql` 499). The scorecard's own `offsan` card detail says the count "self-clears as the offers-sweep GREATEST-raises `edition_offers`" — but **`offers-sweep` has been INACTIVE ~21h** (last run 2026-09-07T02:42Z, 0 runs in 6h; memory records it dead since ~08-28, deliberately set inactive on cron-job.org). So the mechanism that card names as the self-clear no longer runs.
- **Mitigating context (why LOW, not a breakage):** `edition_offers` is fresh (`updated_at` = 00:10Z), kept current by the two live indexers — `topshot-offers-indexer` (18 ok/6h) and `allday-offers-indexer` (18 ok/6h). The authoritative **trust board is clean on every offer metric** (`offer_edition_gap_max_usd` 5, `candy_offers_unverified_pct` 0, `candy_offers_oldest_active_hours` 5.3 — all ok). `offsan` is an info-ranked sanity view with **no baseline here showing a regression**; 1313 may be its steady resting level.
- **Risk read:** none to act on today. The real question is whether `offsan` still trends **down** now that only the indexers write `edition_offers`, or whether it is stuck/growing without `offers-sweep`.
- **Suggested action (night pass or Trevor):** (1) re-measure `v_offer_sanity_flags` over a few readings to confirm it is not monotonically climbing since `offers-sweep` went inactive; (2) if it is stable/declining, the fix is a **doc/artifact copy fix** — the `rpc-qa-scorecard` `offsan` card detail names a dead pipeline as its self-clear and should point at the live indexers instead (artifact edit is the night pass's job, not the monitor's). No DB/route change implied either way.

## Known / already-filed (NOT re-raised, listed so they aren't re-swept)

- `topshot_pack_reality_top_ev` insights board returns **0 rows** — this is the already-filed `refresh_atlas_pack_ev` fabricated `total_unopened=0` (register #65; commit d9af8546, "measured, costed, deliberately not shipped"). Every Atlas-written pack row gets `total_unopened=0` → `is_positive_ev=false`, so nothing qualifies for the top-EV board. Not new.
- Atlas 403 pipeline_alerts (`atlas-editions-upstream-403` 12.5%, `atlas-market-upstream-403` 10.8%) and `flow-rest-moment-moved-400` (16.2%) — all severity `info`, attributed and benign per today's ledger; no rows lost (re-walk from offset 0). Not findings.
- `pipeline_fails_24h` churn (atlas-editions-refresh 32, atlas-market-feed 22, sync-nba-projections 8) — expected upstream/enrichment churn, tracked. `sync-nba-projections` feed confirmation is an open Trevor item already in the ledger.
