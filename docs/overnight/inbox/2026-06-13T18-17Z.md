# Daytime monitor inbox — 2026-06-13T18:17Z (mid-day tick)

PLATFORM GREEN. Second daytime tick on the new Small compute tier; window stays clean. pipeline_runs last 6h = 6 fails, all transient/known + wave-coincident (12:53–14:22Z around the 13Z cohort wave): wmc-fmv-populate x2 lock-timeout, check-alerts + topshot-fmv-populate statement-timeout (the known TFP :15-slot wave collision), offers-sweep x1 TS-GQL 429, hybrid_custody_events x1 signal-timeout. detect_stalled []. get_pipeline_alerts []. Security 4/4 (RLS-off [], anon/auth-write-on-RLS-off [] with relkind r/p, check_public_security_invariants 0 rows, check_secdef_anon_execute_violations []). Sentinel UUID-leak 48h = 0. UFC wmc null edition_key = 2/4584 (fossil floor; UFC-WMC-NULLKEY stays closed). Vercel 12/12 recent deploys READY, 0 ERROR (new prod HEAD e26502e trophy-slab fix, READY ~17:51Z). Recent-ship pipelines all healthy: topshot-buyer-backfill 72/0, ufc-enrichment-drain 24/0 (30-min cadence), fmv-recalc 46/0, analytics-smoke 24/0 (60s restore holding), topshot-listing-cache 6/0. TS FMV HIGH+MED 3318 (up from 3282 @08:14Z baseline); NO_DATA 4182 (down). DB 4506 MB (mild creep; pipeline_runs unpruned since 06-07; weekly-db-maintenance self-fire ~06-14 04:23Z will prune).

Artifacts: 12 active validated this tick by RUNNING their payload/backing queries (the Read tool reaches the OneDrive HTML — see candidate). rpc-live-health all panels green (10 insights boards all return rows; alerts []; FMV/PackEV/offers fresh ~18:13Z). rpc-tracked-fmv-confidence: v_tracked_wallet_fmv_confidence (AF1) returns clean, no timeout. rpc-traction: get_rpc_traction_snapshot clean (25 users, 12 signups/7d, funnel_sessions_7d 90, clicks_7d 12). rpc-cross-collection: cohort_mat fresh (04:10Z today, 158 rows). rpc-deploys-and-cost (Vercel+DB) green. rpc-offers-intelligence backing green. 5 RETIRED tombstones skipped.

Sentry: 1 unresolved — NEXTJS-15 (pinnacle-listings-indexer "listing_resolution_failures_inserted", 1 event, first seen ~17:1xZ, NOT spiking). Known PIN1-class warning; the 2 prior incident-echo issues (NEXTJS-E/A) have cleared since the 08:14Z baseline.

## Candidate work

### LOW / INFO — MONITOR-ARTIFACT-ACCESS: per-payload artifact validation IS possible via the Read tool (the carried "impossible" framing is bash-only)
- Source: this run. The carried MONITOR-ARTIFACT-ACCESS item (metrics carried_queued; CLAUDE.md 06-13 ledger) states the artifact HTML lives in OneDrive (C:\Users\TDill\OneDrive\Documents\Claude\Artifacts\<id>\index.html), outside the scheduled-session sandbox mounts, so per-payload validation is "impossible." That is true ONLY for bash (which sees just the sandbox mounts). The Windows-side Read/Grep tools DO reach those paths — confirmed this run by reading the embedded payload queries out of rpc-tracked-fmv-confidence, rpc-live-health (L276), and rpc-traction (L139) and running them against the DB.
- Risk read: NONE (read-only discovery). Upside: the night pass can do TRUE per-payload artifact validation AND artifact repair (Read current HTML -> edit -> update_artifact) without the mount limitation.
- Suggested action: reframe/close MONITOR-ARTIFACT-ACCESS — record that artifact HTML is readable via the Read tool (not bash); future monitor/night-pass runs should Grep each artifact's index.html for its sql(...) / jsonb_build_object payload and run it. Note: heavily-templated payloads (e.g. rpc-live-health interpolates ${TS}/${AD}/CTEs at JS runtime) won't run verbatim — run the self-contained sub-queries or substitute the collection UUIDs.

## Notes (already tracked / not new — no action requested)
- ALLDAY-V1-UNMAPPED-DRIFT: nfl_all_day open unmapped 242 (+5 vs 16:00Z's 237; golazos 1) — same slow V1-Dapper price-uncertain accumulation logged in the 16:05Z inbox. No new action.
- v_offer_sanity_flags 204 (193 sub/serial = correctly out-of-scope; ~11 edition-grain-ish) — structural on-chain-offer growth, not a fault; OFFER-SANITY-RAISE already queued; trust-health offer_edition_gap was ok ($45/$50) at the 16:05Z tick.
- TFP :15-slot statement-timeout (01:15/07:15/13:15Z) keeps landing inside the cohort wave — TFP-SLOT-WAVE-COLLISION already queued (operator) by the 06-13 night pass; the TFP-480-RESTORE 2-clean-tick gate stays unmet.
- Sentry NEXTJS-15 single event — PIN1-class, not spiking.
