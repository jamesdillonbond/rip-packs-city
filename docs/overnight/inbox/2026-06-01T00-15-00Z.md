# Daytime monitor — candidate inbox · 2026-06-01T00:15Z

Run: `rpc-daytime-monitor` (Cowork). Repo: shared `rip-packs-city` (no bot clone reachable). Baseline: night-pass `metrics-latest.json` @ 2026-05-31T08:10Z. Lock: stale released marker (08:12Z, ~16h old) — treated as STALE, took over per its own embedded rule.

**Health this run: ✓ healthy + FMV improving.** Security 0/0 (RLS-off public base tables 0; anon/auth write-grants on RLS-off base tables 0). FMV writers fresh on all 4 collections (<25m: TS/AllDay 23:48Z, Golazos 00:02Z, UFC 00:00Z). TS FMV HIGH+MED **831** (↑ from 776 baseline), NO_DATA **5325** (↓ from 6055), ASK_ONLY **689** (↑ from 114) — attributable to today's shipped FMV deploy `65421e26` ("prefer fresh TS ask over stale WAP"). AllDay HIGH+MED 261 (↑ from 243). Sentinel TS-UUID-48h **311** (↓ from 1099). Editions/DB normal (TS 16,291; DB 5,878 MB). Vercel: 20/20 recent prod deploys READY, **zero ERROR** — heavy ship day (team-hub Phases 1–4, `/insights/market`, badge-sync on-chain-key fixes, FMV ask-over-WAP, funnel-track `await` fix).

---

## P2 (MED) · New smoke-test failure: `evm-transfers-ingest` trips the just-shipped `detect_stalled_pipelines()` smoke/sentinel gate (Sentry NEXTJS-1E)

- **Source:** Sentry `JAVASCRIPT-NEXTJS-1E` — "smoke test failed: sales indexers running (detect_stalled_pipelines)", first seen ~2026-06-01 00:05Z (≈3 min before this sweep), 1 event, culprit `POST /api/smoke-test`. `detect_stalled_pipelines()` (run twice this sweep) returns ONLY `evm-transfers-ingest` (severity medium, 67 min silent vs `max_silent_minutes=60`). Recent ship: commit `c3ad47c9` (today, "feat(funnel+ops)") wired `detect_stalled_pipelines()` into the sentinel + smoke-test as a Pipeline-Silence check.
- **Risk read:** LOW real impact, but it's a same-day-ship side effect that turns the smoke suite RED on a false-ish positive. `evm-transfers-ingest` = the Beezie/Base parallel data plane: no product consumer, regularly Base-429 rate-limited (ledger Q6), self-recovers next tick — so it crosses a 60-min silence threshold routinely and will re-fire this smoke failure intermittently, burning Sentry quota and (worse) desensitizing the sales-indexer smoke gate so a REAL `*-sales-indexer` stall later gets ignored (cry-wolf). The Sentry/check label "sales indexers running" is misleading — **no sales indexer is actually stalled** (all 4 are watch-listed; `detect_stalled_pipelines()` did not flag any); the check is failing on a non-sales pipeline.
- **Suggested action (operator/CC — NOT auto-ship; touches smoke-test route + watchlist):** pick/combine — (a) exclude `evm-transfers-ingest` (no-consumer Base plane) from the smoke/sentinel `detect_stalled_pipelines` gate, or scope the smoke assertion to `*-sales-indexer` only so the label matches its intent; (b) widen `evm-transfers-ingest` `max_silent_minutes` 60 → ~120–180 to tolerate its known Base-429 tick-skips, keeping its stall visible via sentinel (non-paging) rather than failing the smoke suite. Cross-ref ledger Q5 (smoke sales-lag threshold) + Q6 (evm Base-429 backoff) — same family; candidate to fold into one "make pipeline-silence thresholds cadence-aware" change.

---

## P3 (LOW · verify, doc/route) · Rookies view name mismatch: ledger says `topshot_rookies_board`, live view is `topshot_2025_rookie_index`

- **Source:** Live schema check this run — `to_regclass('public.topshot_rookies_board')` = **null** (does NOT exist), but `to_regclass('public.topshot_2025_rookie_index')` = **resolves** (exists). The ledger's "Insights batch" entry states the rookies surface shipped as `topshot_rookies_board` via mig `audit_20260531_topshot_rookies_board_view` (1,245 rows). The live canonical rookies view is named `topshot_2025_rookie_index`.
- **`rpc-insights-health` artifact is HEALTHY — not a finding.** It references `topshot_2025_rookie_index` (the name that exists) in its `Q_COUNTS` UNION; I verified all 8 of its backing views resolve (`topshot_squeeze_board`, `topshot_set_squeeze_board`, `topshot_pack_reality_top_ev`, `topshot_2025_rookie_index`, `topshot_market_index_daily`, `topshot_first_mint_trophies`, `cross_collection_cohort_stats`, `pinnacle_scarcity_board`), so its query succeeds and the dashboard renders real per-surface data. No artifact repair needed.
- **Risk read:** LOW. Pure naming inconsistency between the ledger note and live schema (the view was likely renamed/superseded after that ledger entry, or two names existed and only `topshot_2025_rookie_index` survived). The only real-bug possibility is if the live `/insights/rookies` *route* reads `topshot_rookies_board` — that would 500 — but I can't fetch the authed route from the sandbox to confirm (today's deploy list shows `/insights/market` commit `4b5289a7` but no rookies-route commit; the ledger had the rookies route as "PENDING CC").
- **Suggested action (operator/CC):** confirm the live `/insights/rookies` route (if shipped) reads `topshot_2025_rookie_index`, and correct the ledger's `topshot_rookies_board` references to the canonical name so future sweeps don't re-flag this.

---

## Watch (folded into sweep — not new candidates)

- `evm-transfers-ingest` silent 67m / Base-429 = known **Q6** (LOW), self-recovers. Logged above only because it now ALSO trips the new smoke gate.
- Sentinel TS-UUID-48h 311 sits above the `<250` "ok" line but is ↓ hard from 1099 and well under the 2000 warn — trending right, not a finding.
- No regression from any of today's ~18 deploys (all READY); FMV + security independently verified clean post-ship. FMV ASK_ONLY jump (114→689) is the intended `65421e26` behavior, not drift.
- Artifact validation: `rpc-insights-health` confirmed HEALTHY — all 8 backing views in its `Q_COUNTS` resolve (verified by `to_regclass`). Independently re-ran the catalog security checks (back `rpc-security-drift`, clean 0/0) and FMV confidence/freshness (back `rpc-fmv-watch` + `rpc-live-health`, fresh). 10 artifacts enumerated; no broken artifact observed. Did not exhaustively re-run every artifact's embedded queries (Supabase MCP intermittently timed out / buffered this cycle).
- **Inbox commit unavailable this run (known Q7):** `.git/config` line 18 holds 16 NUL bytes (`bad config line 18`) — the documented intermittent Windows↔sandbox `.git` view-corruption — so every git op fails. This file is written to disk (the mount persists to Trevor's machine); the night pass picks it up locally regardless.
