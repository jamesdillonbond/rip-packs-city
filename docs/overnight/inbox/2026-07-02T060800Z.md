# Daytime monitor — 2026-07-02 06:08Z (~23:08 PDT 2026-07-01, last tick of day)

Not first-of-day (Section 1a daily extras skipped; trust-health still covered via rpc_ops_snapshot, cross-collection verified opportunistically). Lock RELEASED (nightly 08:21Z). **Health GREEN.** No clock skew (shell 06:06Z ~= DB now() 06:06:40Z ~= app-stamped pipeline runs 06:08Z). Supabase + Vercel connectors loaded; Sentry not separately loaded (Vercel get_runtime_errors as frontend-error proxy). Artifact tools available.

## NEW candidate / correction (1)

**CLASSIFY-ACQ-ALLDAY-STATEMENT-TIMEOUT re-flapping post-fix — do NOT close (contradicts the 00:14Z tick's CLOSE recommendation).**
- Source: pipeline_runs, last 16 runs of classify-acquisitions-multicollection. The 795d99b "AllDay batch 300->80" fix (shipped 07-01, prod) cut the flap SHORT-TERM (22:06 30s, 23:06 42s, 00:06 45s = the 3 green ticks the 00:14Z tick saw and recommended CLOSE on), but the AllDay-leg duration is now CLIMBING BACK to the 90s fn statement_timeout cliff: 01:07 50s -> 02:07 78s -> 03:07 89s -> 04:07 68s -> 05:07 89s -> 06:07 FAIL 95s. 3 fails in the last ~15h (17:09, 21:07, 06:07) plus multiple 89s near-misses.
- Root read: the candidate CTE Merge-Anti-Join scans the FULL AllDay sales history, which is still GROWING as the allday_studio_history_v1 studio backfill fills (focus.md: ~1-4 day drain, started ~06-30). So batch=80 cost drifts UP with history size — the cap bought ~half a day of headroom that is already eroding. Impact still LOW (moment_acquisitions cost-basis enrichment; NOT FMV / deal-boards / pack-EV / user-facing); flaps-not-stalls.
- Suggested action (night pass / CC): keep QUEUED, do NOT close. Another batch reduction only re-erodes. Durable levers: (a) let it settle once the AllDay studio backfill finishes filling (history stops growing -> cost stabilizes; re-measure then), (b) bound the candidate CTE to a recent window, or (c) an index supporting the anti-join probe against wallet_moments_cache. Route runs in after() under maxDuration=120 with fn statement_timeout=90s — a naive fn-timeout bump risks the invisible after()-lambda-kill class (see 07-01 overnight write-up).

## Post-ship watch (informational — for the night pass, not candidates)

- Heavy attended 07-01->02 Cowork/CC "do everything" wave landing clean (DB-side). Current prod dpl_AVMg (27a0d07) READY; newer deploys CANCELED (docs-only, ignoreCommand). Verified: mis-attribution parallel writer-leak fix (a9c011c + audit_20260702 self-healer) — editions FLAT (TS 17,489 / AllDay 6,191 / Golazos 581 / UFC 518), sentinel TS-UUID-48h 0, fmv_sanity 0 (no writer leak); P1a FMV display-guard (dc8e103): topshot_fmv_display_guard 1,381 rows, rpc-refresh-fmv-display-guard cron active; Pinnacle render enrich (7fb73d5): pinnacle_fmv_history present, fresh 07-01 10:07Z (20h, under 30h breach = daily cadence); security 0/0/0/0 after the new migration.
- OG-BACKSLASH-500 (%5C OG routes) — logged 00:14Z; NO recurrence since the one-off 23:03:50 burst (~7h quiet). Trending to "crawler noise, close" per that candidate's own note.
- 07-01 first-tick candidates (anon-DML-revoke-regressed, team-page-timeout) already dispositioned by the attended session — do NOT re-queue.

## Health baseline (this tick)
- rpc_ops_snapshot: security 0/0/0/0; trust 15/15 ok (breaches []); stalled_pipelines []; pipeline_alerts 1 INFO (ufc_sales resolving_editions, benign); sentinel TS-UUID-48h 0; editions FLAT TS 17,489 / AllDay 6,191 / Golazos 581 / UFC 518; FMV TS H+M 4,704 (1318+3386) / AllDay 882 (239+643) / UFC 15 / Golazos 5; fmv_sanity 0; unmapped 29/100; DB 7,199 MB.
- check_pgcron_recent_failures(): []. cross-collection cohort 04:10Z/153 rows + overlap 04:25Z fresh, both jobs active. 24h pipeline fails: 14 pipelines; 13 latest-run ok=true (transient pool contention), only classify-acq false (above).
- Vercel: no ERROR deploys; prod 27a0d07 READY. Runtime errors all known families (url.parse DEP0169, 300s after() timeouts, PostgREST pool saturation, pack/team/insights DBSAT statement timeouts, wmc idempotent dup-key, OG %5C above).
- Artifacts: 15 active, none flagged broken. Light last-tick validation: estate backing objects resolve green (rpc_ops_snapshot / v_rpc_trust_health / cross_collection_cohort_mat / topshot_fmv_display_guard / pinnacle_fmv_history all present + sane); both earlier ticks today validated full payloads end-to-end; nothing shipped since 00:14Z would break a board. DB +207 vs overnight baseline (8 new sales-partition indexes + studio backfill) benign; watching rate.

STEER honored: SERIAL-FMV weekly by design (power-model-weekly resurfaces 07-05), evm-429 benign, DQ4 inert, studio-backfill volume expected. Declined not re-raised.
