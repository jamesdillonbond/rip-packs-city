# Daytime monitor candidates — 2026-07-04T18:16Z (~11:16 PDT tick)

Health GREEN. security 0/0/0/0; trust 16/16 ok (breaches []); `detect_stalled_pipelines()` []; sentinel TS-UUID-48h 0; leak_48h 0; editions FLAT (TS 17,490 / AllDay 6,191 / Golazos 581 / UFC 518); FMV TS H+M 4,805 (improving from nightly 4,785); Vercel prod `26982cc3` READY (0 ERROR deploys); Sentry 0 unresolved/24h; DB 7,913 MB (+96 vs nightly baseline, benign backfill drain); rpc-live-health payload validated clean (all 12 insights surfaces + backing objects resolve).

POST-SHIP WATCH — PASS: the nightly-queued **allday-pack-opens stuck sub-spork-floor backfill** is RESOLVED. CC shipped `26982cc3` (raise DEFAULT_FLOOR to the current-spork floor, edge fn v3, deployed + prod READY); latest `allday-pack-opens-backfill` run 18:02Z ok=true and `allday-pack-opens-forward` 18:09Z ok=true. The 81 fails/24h in the snapshot are pre-fix lag. Night pass can CLOSE this queued item.

The heavy 07-04 daytime CC wave (F1 circulation corrections 1,798 editions, F2/F3 badge parity, badge_editions circ sync, marketplace-status force-cache fix, moment-404 cached_listings_v2 fallback, mobile overview stacking) verified DB-side clean: security 0/0/0/0, fmv_sanity 0, edition_integrity_flags 4 (ok, breach 50), editions flat, no new Sentry/Vercel error class. No regression.

---

## Candidate 1 — ALLDAY-CROSS-SOURCE-DEDUP-STMT-TIMEOUT-FLAP (LOW; DB-only fn / operator)
- **Source:** `check_pgcron_recent_failures()` → `rpc-allday-cross-source-sales-dedup` (jobid 32) latest_status=failed, last_run 2026-07-04 17:40Z, 4 fails / 24 runs. `cron.job_run_details`: the 10:40Z and 17:40Z ticks cancelled at exactly 120.0s ("canceling statement due to statement timeout"); every other recent tick 10–20s.
- **Risk read:** LOW. This is the BACKSTOP sweeper only — the primary cross-source dedup is now the BEFORE INSERT trigger `trg_zzz_allday_cross_source_dedup` (shipped 07-02, `6b7cda1`), so a timed-out sweep tick just skips one hour's cleanup; AllDay dup backlog is negligible (~25–300 groups, ~0.08% of 90d AllDay sales, collapsed on the next successful tick). Same DB-growth-contention / statement-timeout family as the carried CLASSIFY-ACQ-ALLDAY item. Recovers next tick; not user-facing, not FMV/pricing/deal-boards. NOT tracked in the ledger as a timeout finding (only the job's purpose+revert are recorded).
- **Suggested action (night pass):** same lever family as classify-acq — either raise the dedup fn's `statement_timeout` above 120s, OR bound its candidate scan to a recent window (older sales are already deduped, so a full-history scan every tick is what hits the cliff as `sales` grows). ALSO reconcile a cadence discrepancy: the job now runs HOURLY (`40 * * * *`) but the ledger records it as daily (`40 12 * * *`) — confirm the hourly cadence is intended; a daily/windowed sweep would both fit under the timeout and match the docs. Fixing FMV/ingest route logic is off-limits, but this is a pure pg_cron fn — in scope for a bounded, verifiable DB-only change.

## Candidate 2 — LOCK-CHECK-BATCH-1740Z-CONTENTION-CLUSTER (LOW / WATCH)
- **Source:** `pipeline_runs` — `lock-check-batch` ok=false 17:38Z (125.3s) and 18:08Z (125.2s) at the ~125s cap, after ~5h of healthy 20–48s runs. Co-timed with the dedup timeout (17:40Z) and a `classify-acquisitions-multicollection` fail (18:06Z, 110.9s) — i.e. three unrelated jobs hit their duration caps inside the same ~17:40–18:10Z window.
- **Risk read:** LOW/WATCH. Reads as connection-pool / IO contention in that window rather than a lock-check-batch-specific regression. `detect_stalled_pipelines()` is [] (the job is still firing on its 30-min cadence). If `lock-check-batch` keeps failing at 125s on future ticks it graduates to the statement-timeout family; if it recovers next tick it was window contention only.
- **Suggested action (night pass):** re-measure `lock-check-batch` recent ticks. If still failing 2×+ at 125s, treat as a statement-timeout / route-maxDuration item (same family, verify whether it's a pg_cron fn or an after()-lambda route before picking the lever). If recovered, close as transient contention. No action if self-healed.
