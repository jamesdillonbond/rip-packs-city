# Daytime monitor inbox — 2026-06-26T06:19Z (last daytime tick, ~23:1x PDT Jun 25)

Run: rpc-daytime-monitor. Lock RELEASED (overnight pass closed 14:10Z 06-25). DB now() 06:07Z == shell UTC — NO clock skew.

Platform **GREEN. 0 new fix-candidates.** Headline: the TWO operator findings the 00:14Z + 03:16Z ticks queued are BOTH already RESOLVED-IN-CODE by Trevor (03:30–05:00Z window) and verified healthy — the night pass should CLOSE them, NOT action them as operator cron-job.org re-fires.

---

## RESOLVED since 03:16Z — both prior "cron-job.org" findings were code bugs, fixed by Trevor (CLOSE; do NOT re-queue as operator)

### PACK-EVENTS-CRONJOB-STALL (was 03:16Z CANDIDATE 1, HIGH) — RESOLVED by `80a9238b`, post-ship watch PASS
- Root cause was NOT a cron-job.org trigger problem. The worker batched ALL cursor writes + the `pipeline_runs` log at the very END of the run, so a run killed by the Cloudflare wall-clock/CPU limit committed NOTHING — it only *looked* cron-silent (no pipeline_runs row since 22:24Z; opens/allday cursors frozen 8h+ at block 156004422).
- Fix: `80a9238b` "fix(pack-events): per-chunk flush+cursor commit so the forward backlog self-drains" (prod READY ~04:47Z) + verified live by Trevor (`bc34015e` ledger: "wedge VERIFIED FIXED via per-chunk commit, opens/allday unfroze live").
- Post-ship watch (this tick): pack-events-ingest firing every ~15min ok=true (06:09/05:54/05:39Z); forward cursors UNFROZEN + fresh — topshot_pack_opens 156007672 (age 5m), allday_pack_purchases 156019922 (20m), topshot_pack_purchases 156048544 (5m, caught up past the 03:16Z sealed tip 156020175); `get_pipeline_alerts()` has NO cursor_stalled (the 3 HIGH from 03:16Z are gone). → CLOSE.

### LISTCACHE-CRON-DROP / topshot-listing-cache (carried 00:14Z + 03:16Z; MED/operator) — RESOLVED by `35fb466f` + `d3e931d7`
- Root cause was NOT cron-job.org. The GHA "RPC Data Pipeline" monolith's FMV-backfill step hung ~18min under the studio-backfill DB load (full-table pagination of fmv_snapshots ~296k + sales ~1.3M), hitting the job timeout and starving step 5/7 (listing-cache) → ~14h stale.
- Fix: `35fb466f` decoupled TS listing-cache into its OWN GHA workflow (immune to the monolith) + `d3e931d7` replaced fmv-backfill full-table pagination with a server-side anti-join RPC (~18min → ~2.5s).
- topshot-listing-cache resumed (ran 00:11Z + 04:44Z, both ok). → CLOSE. Supersedes the 03:16Z CANDIDATE 2 "CRONJOB-ORG-TRIGGER-SURFACE-DEGRADED" — both entries were code bugs, not a cron-job.org account/platform problem; no operator re-fire needed.

## POST-SHIP WATCH — other daytime DQ ships all PASS
- `7a978ac` TS parallel null-thumbnail recovery: canonical TS null-thumb = **8** (matches "149→8, 8 base editions remain need GQL"); not regressing.
- `729bfe42` UFC null set_name on-chain fill: ufc null set_name = **0** (matches "72/72 filled").
- `d3e931d7` fmv-backfill anti-join RPC: FMV fresh (2m), fmv_sanity 0 — no hot-path regression.

## Owned / trajectory only (NOT new candidates)
- `unmapped_resolution_backlog_max` BREACH **1405**, draining as designed (2370 overnight → 1968 @00:14Z → 1831 @03:16Z → 1405 now; topshot-flowty-unmapped-drain; focus.md: do NOT re-flag/skip/retire/raise-threshold).
- conflation guard **139** — owned, slightly above the 44–124 band, backfill-wave-elevated (parallel sales landing before their `::` row); converges on the daily refresh-conflated-editions remap. Not a finding.
- DB **6,075 MB** (+147 since 03:16Z; sustained ~50 MB/hr from the documented deep-history backfill wave — benign-attributable, ample headroom; watch-only).
- ufc-studio-sales-history-backfill watchlist row still PENDING (gate met) — ship next genuine overnight run. PINNACLE-EDITION-KEY-UUID-CAST already queued (LOW, CC route-side).

## Health snapshot (GREEN)
- security 0/0/0/0 (check_public_security_invariants 0, check_secdef_anon_execute_violations [], rls_off 0, anon-write-on-rls-off 0).
- detect_stalled_pipelines() **[]** ; check_pgcron_recent_failures() **none** ; get_pipeline_alerts() 1 INFO (ufc_sales resolving_editions, benign).
- pipeline fails 6h = **5** (transient statement-timeout micro-cluster 01:15–01:31Z: compute-topshot-pack-ev / pinnacle-nft-resolver ×2 / wmc-fmv-populate / check-alerts — ALL recovered ok on next tick); fails 24h = 6.
- trust health **8/9 ok** (lone BREACH = owned unmapped 1405).
- sentinel TS-UUID-leak 48h = **34** (inert, < WARN 250) ; ts_uuid_dupes_created_24h 34 ok. editions FLAT (TS 17,469 = +34 inert floor / AllDay 6,191 / Golazos 581 / UFC 518).
- FMV TS HIGH+MED **4,558** (HIGH 1298 + MED 3260; improving from night-pass 4,535) / AllDay 911 / fmv_sanity 0. Writers fresh (fmv 2m / pack_ev 3m / offers 9m).
- Sentry **0 new in 12h** (carried NEXTJS-J single-event smoke transient, known/benign).
- Vercel prod **`80a9238b`** READY, **0 ERROR** (CANCELED = docs-only/superseded/monitor commits, normal).
- Artifacts: rpc-live-health FULLY validated — all 11 insights boards resolve (squeeze 501 / deals 501 / trophies 501 / rookies 61 / pack_reality 7 / market 501 / first_mint 501 / offer_spread 501 / pinnacle_scarcity 501 / set_squeeze 239 / cross_collection_stats 1) + freshness fmv 2m/pack_ev 3m/offers 9m. Fragile views resolve: v_tracked_wallet_fmv_confidence 26, topshot_serial_premiums_board 260. Other active artifacts carried from 03:16Z (no schema change since). 2 tombstones skipped.
