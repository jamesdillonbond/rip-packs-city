# Daytime monitor candidate — 2026-09-03 (~11:07 PT / 18:07Z tick)

## LOW — recalibrate the `candy_scarcity_board` liveness budget; it flickers `public_board_slow_count` into BREACH on ordinary variance

- **Source:** trust-health arm `public_board_slow_count` = 1 (BREACH, breach_at 1), traced to `public_board_liveness_state` / `_history` for `candy_scarcity_board`.
- **Observed:** current sample 3292ms against this board's `max_ms` = **3000** (a 292ms / 9.7% overshoot), `err` NULL, `row_count` 125 (min_rows 31) — slowness, not emptiness. No other board is over budget.
- **Why it's noise, not a regression:** the board's own history swings 698ms↔34239ms with a stable 125-row count, and it was already back under budget on the prior probe (06:28Z = 1966ms). It crosses the 3000ms floor on a large share of samples (2939 / 4574 / 3789 / 4507 / 4294 …), so the BREACH toggles on/off with ordinary contention.
- **Class:** this is exactly the "an alarm that cries wolf on noise gets ignored" case the 2026-08-01 recalibration note in `public_board_liveness_watchlist` fixed for the other boards (3x→6x multiplier, 3000ms floor). `candy_scarcity_board` still sits AT the 3000ms floor, which is below its own routine warm cost.
- **Risk read:** LOW. Watchlist-only config change (`public_board_liveness_watchlist.max_ms`), no user surface, no schema. The board renders fine; only the trust-board arm is affected.
- **Suggested action (night pass):** re-measure a warm baseline for `candy_scarcity_board`, then raise its `max_ms` to ~6x warm (consistent with the other boards) with a floor comfortably above its normal ceiling (the 34s tail is contention, not steady-state — exclude it). Keep it well inside the ~30s read budget so a genuine cold/contended blowup still fires. Verify the arm reads `ok` at a normal sample afterward and still BREACHES on an injected slow reading.

---
### Sweep summary (this run, all read-only)
- `rpc_ops_snapshot()`: security all-clean ([] × 4); `stalled_pipelines` []; sentinel_ts_uuid_editions_48h 0; db 15,695 MB.
- Trust health 2 breaches: `unmapped_resolution_backlog_max` 201 (KNOWN structural, nfl_all_day drain, flat/declining — not re-raised) + `public_board_slow_count` 1 (above; benign).
- pg_cron `check_pgcron_recent_failures()`: [] clean.
- `pipeline_fails_24h`: all known external/upstream classes (Flow Access API 400/computation_limit on wallet-backfill*, AllDay 503 on pack-opens-backfill, projections all_upstreams_failed, offers-sweep upstream, + 3 refresh_wmc_fmv_changed statement timeouts). None stalled, none new-class.
- Cross-collection refresh (first-tick watch): cohort fresh 09-02 23:10Z (222 rows), overlap fresh 23:25Z; rpc-ccm-step1/step2 both `succeeded`. OK.
- Spell control: pg_stat_activity IO-wait 0 / active 0 — NOT a spell; causal reads above are interpretable.
- Vercel: last production READY = `7fe59d47` (= repo HEAD, the drain-ETA fix); recent CANCELEDs are docs-only tips (expected, no rebuild); no ERROR deploys.
- Artifacts: `list_artifacts` returned 11; artifact HTML lives outside the mount so embedded payload queries could not be read/validated this run — noted, DB backing surfaces are all healthy (no renamed-view/column signal).

---
## RESOLVED 2026-09-03 (15:20 PT, Claude Code on Trevor's box) — the suggested remedy was refuted by the board's own history; the board was materialised instead

**Raising `max_ms` would have hidden a 1,000× regression.** The watchlist note says the 3000 ms floor was calibrated from "125 rows / **2 ms**". `public_board_liveness_history` for `candy_scarcity_board` shows a p50 of **2.4–4 s on every day since 08-11** (n=4/day) with tails of 34 s (08-31), 46 s (08-30), 55 s (08-29) and **390 s** (08-28) — every one past the ~30 s page budget, so the Scarcity tab rendered degraded while the arm flickered. It was never "at the floor of ordinary variance"; it had stopped being a 2 ms board. EXPLAIN ANALYZE at 22:05Z: a 25,375-row index-only scan of `wallet_moments_cache` (`idx_wmc_candy_holder_cover`), **16,662 buffers, 2,230 heap fetches, 1,085 ms** on a quiet instance — re-run on every page view and every liveness probe. It was the last live-scanning Candy board; its sibling `candy_holder_board` got the same treatment on 2026-08-02.

✅ **Shipped (DB, migration `20260903221326`):** `mv_candy_scarcity_board` behind a byte-identical `candy_scarcity_board` wrapper (column list copied from `pg_get_viewdef`, `security_invoker=on` re-asserted after the replace, grants unchanged: anon/auth false, service true on both objects). Read is now **4 buffers / 0.12 ms**. Refresh: pg_cron jobid 436 `rpc-refresh-candy-scarcity-board` `26 * * * *` (minute measured free), `SET statement_timeout='300s'` per the #27 convention, `REFRESH … CONCURRENTLY` proven by hand (125 rows); watchlisted in `board_mv_refresh_watchlist` at 6 h so a dead refresh pages as STALE. `max_ms` stays **3000** — a reading near 3 s again now means the wrapper stopped reading the MV. `lib/insights/candy-board.ts` needed no change (reads by name, service role). Security snapshot after: `[] × 4`. Repo file md5 == DB statement md5 (`9618f2fa…`).
