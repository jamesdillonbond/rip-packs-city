# Weekly data-quality + reconciliation sweep — 2026-06-16 (~16:33Z / 09:33 PDT)

Read-only `rpc-data-quality-sweep`. **Status: HEALTHY.** All 8 checks pass or sit at known/stable floors. Nothing alert-grade, nothing mispriced. Two low-severity integrity notes to queue (one NEW: a UFC editions-catalog seeding gap surfaced by the new `ufc-enrichment-drain`; one known: TS UUID wmc fossils). Shipped nothing (no additive watchlist/threshold warranted — all relevant pipelines already watchlisted and green). Queued items below are for the night pass to fold into the ledger Queued section (ledger not edited directly — mount-truncation guard).

## This week (each check's headline)
1. **FMV sanity (`v_fmv_sanity_flags`): 0 rows.** No edition diverges from its set's sales-median. Clean.
2. **Offer reconciliation (`v_offer_sanity_flags`): 201 flags, all `has_sub_serial=true`, largest gap $757** (Azzi Fudd — WNBA Rookie Debut, 36 chain offers, chain max $850 vs GQL $93). Breakdown: 191 `chain_exceeds_gql` + 10 `gql_blank_chain_has` — **100% sub-serial**. This is the **by-design residual** after the 06-14 `raise_edition_offers_from_chain()` edition-grain ship (`audit_20260614_offer_sanity_raise_edition_grain_filter`), which deliberately EXCLUDES subedition/serial offers from raising the edition Best-Offer cell. **Zero edition-grain gaps remain** → reconciliation is effectively clean; the growth 132 (06-07) → 201 is benign sub-serial accrual on-chain, not a regression. (Optional refinement queued — see below.)
3. **Integrity:**
   - (a) **Unpriced editions: 0** across all 5 collections (TS 9,137 / AllDay 6,191 / Golazos 581 / UFC 446 canonical all have an `fmv_snapshot`; Pinnacle 1,847 priced in `pinnacle_catalog`). Clean.
   - (b) **wmc edition_key orphans: TS 1,686 · UFC 166** (contract: `wmc.edition_key` must match `editions.external_id`). TS = **1,683 stale UUID-format fossils** (refs to merged/deleted UUID-pair TS editions, span 2026-05-06→06-15; known residue, stable, not the retired drain regressing) + 3 int-pair (negligible). **UFC = 166 rows / 72 distinct keys, ALL appeared 06-13→06-16** — these are real UFC editions (e.g. `BRUNNO-FERREIRA-UFC-283-KO-TKO-2500`) **held by wallets but absent from the `editions` catalog**, surfaced by the new `ufc-enrichment-drain` cron resolving keys faster than the UFC editions seed. NEW — queued.
   - (c) **Sales (7d) with null edition mapping: 0** of 17,095. Every sale maps; unmapped correctly held out in `unmapped_sales`. Clean.
4. **`unmapped_sales` backlog: 43 open** (AllDay 42, Golazos 1) — down from ~246 a week ago; the `5f1a28d` on-chain resolver + recover-v1 path are draining it well. Healthy small residual.
5. **TS sentinel (UUID-keyed editions, 48h): 0** (ok<250). Clean — the int-pair writer + dupe-block trigger are holding.
6. **FMV freshness + coverage:** all fresh — TS 11.3m · AllDay 13.9m · Golazos 2.0m · UFC 10.2m · Pinnacle 6.4h (daily writer, normal). HIGH+MED: **TS 3,434** (HIGH 989 ≫ 400 floor) · AllDay 785 · Pinnacle 708 · UFC 1 · Golazos 1 (UFC/Golazos thin markets, mostly NO_DATA — expected). Improving vs the 06-15/06-16 night-pass baselines (TS 3,426 → 3,434; AllDay 774 → 785). Clean.
7. **Pack-EV staleness: TS 440/1,174 stale >3d · AllDay 4/521.** TS tail is bimodal — only 47 in the 3–30d band, **393 are >30d** (oldest 55.5d). Consistent with the known PACKEV-THROUGHPUT long tail of depleted/retired distributions the EV queue never re-reaches; active/purchasable packs stay fresh. Not corruption. (Golazos/UFC/Pinnacle have no `pack_ev_latest` — no indexed pack pools, expected.)
8. **Offer-indexer liveness:** `topshot-offers-indexer` **72/72 (100%)** + `allday-offers-indexer` **72/72 (100%)** over 24h, both last-ran minutes ago. `offers` total **23,633** / open **9,916** — healthy week-over-week growth. Clean.

## Flags (off-threshold or worth a human's eyes)
Nothing alert-grade. Two integrity items + one optional refinement:

- **UFC-EDITIONS-SEED-GAP · LOW · NEW · CC/operator (ingest/seed).** 72 distinct UFC editions (166 wmc rows) are held by wallets but missing from the `editions` catalog, surfaced 06-13→06-16 by the `ufc-enrichment-drain` cron (job 7804392, wired 06-13) converting `wmc.edition_key` from NULL → a resolved key with no matching edition. Real fighters/events/circulations (verified: `BRUNNO-FERREIRA-UFC-283-KO-TKO-2500` etc. return nothing from `editions`), so it's a genuine UFC catalog-completeness gap (~14% of the 446 UFC editions wallets hold are unseeded), not key corruption or format drift. Impact: those moments render with thin metadata + break the wmc-contract invariant for UFC; **no FMV/pricing corruption** (UFC FMV is overwhelmingly NO_DATA anyway). Suggested fix (off-limits for this sweep — seed/ingest logic): seed the 72 missing UFC editions (or give `ufc-enrichment-drain` a companion edition-seed for keys it resolves that aren't in the catalog). Distinct orphan key list is reproducible from the query in §query-appendix.
- **TS-WMC-UUID-FOSSILS · LOW · known/stable · CC (canonical-merge, off-limits to auto-fix).** 1,683 wmc rows still keyed to merged/deleted UUID-pair TS editions (span back to 2026-05-06). Stable residue of the retired `wmc_edition_key` drain era; self-heals only via a canonical-merge re-key. Not growing alarmingly; flagged for completeness, not action this week.
- **OFFER-SANITY-VIEW-REFINEMENT · LOW · optional · CC.** `v_offer_sanity_flags` is now 100% sub-serial (the edition-grain class was closed 06-14 and sub/serial are intentionally never raised). As-is the view will only ever climb with sub-serial accrual, so it no longer functions as an anomaly signal. Optional: add `WHERE NOT has_sub_serial` (or split sub-serial into its own informational view) so the flag count returns to a true edition-grain anomaly detector. View-definition change → CC, not shipped here.

## Suggested actions
- **None required this week.** Platform is healthy; FMV/offers/freshness/sentinel/coverage all green.
- **Queue for the night pass / CC:** UFC-EDITIONS-SEED-GAP (NEW, the only item with real product impact, though low), TS-WMC-UUID-FOSSILS (known), OFFER-SANITY-VIEW-REFINEMENT (optional). None touch FMV/pricing/ingest logic in a way this read-only sweep can or should ship.
- **No new monitoring shipped:** the natural tripwire for the UFC class (and the AllDay zero-write class flagged in the 15-17Z monitor inbox) is a wmc-orphan / resolver-zero-write sentinel leg, but that's a `v_rpc_trust_health` change (CC), not a `pipeline_cadence_watchlist` row — out of this sweep's additive-only lane.

## Query appendix (reproducible)
- Offer flags: `SELECT flag, has_sub_serial, count(*), max(gap_usd) FROM v_offer_sanity_flags GROUP BY 1,2;` → 191 chain_exceeds_gql(sub) / 10 gql_blank_chain_has(sub).
- UFC orphans: `SELECT count(DISTINCT edition_key), count(*) FROM wallet_moments_cache w WHERE collection_id='9b4824a8-736d-4a96-b450-8dcc0c46b023' AND edition_key IS NOT NULL AND NOT EXISTS (SELECT 1 FROM editions e WHERE e.external_id=w.edition_key AND e.collection_id=w.collection_id);` → 72 / 166.
- Pack-EV TS tail: 3–7d 1 / 7–14d 45 / 14–30d 1 / >30d 393.
