# Daytime monitor — 2026-07-27 ~06:15Z (23:15 PDT tick)

Health sweep GREEN. `rpc_ops_snapshot`: security 0/0/0/0 (invariants/anon_write/rls_off/secdef_anon all []), trust 20 metrics 0 breaches, `stalled_pipelines []`, `check_pgcron_recent_failures() []`, sentinel_ts_uuid_editions_48h 0, DB 11,204 MB (normal growth). Sentry 0 unresolved/24h. Vercel prod `5891f728` READY (0 ERROR; the 00:10Z monitor-inbox docs-only tip correctly CANCELED by ignoreCommand). Primary merged dashboard (rpc-live-health consolidated payload) re-validated live AFTER tonight's heavy interactive Cowork+CC session (~30 commits ~00:00–05:56Z, all deploys READY): every backing object resolves, FMV/pack-EV/offers all <10 min fresh, FMV TS HIGH+MED 2,857, sales 24h TS 2,122 / AllDay 176 / candy 105, leak_48h 0, insights boards populated — no dashboard-breaking schema drift (tonight's ships were additive Candy tables/views, retired-table index drops, and trigger-fn RLS revokes, none touching dashboard-read objects).

Late-evening tick (23:15 PDT) — Section 1a first-tick extras (trust-health / cross-collection pg_cron verify) deliberately skipped (not the ~08:00 local first tick).

---

## FOLLOW-UP (not a new raise) — ALLDAY-DECODE-LEG-EFFICACY: 24h confirmation, and the CC prediction is now FALSIFIED

This ADVANCES the candidate the 00:10Z tick already logged (`2026-07-27T001055Z.md`) — it supplies the 24h measurement that file asked for ("confirm the 0/60 trend holds at the 24h mark"), plus a decisive new data point. Do NOT treat as a second issue.

- **Source:** `pipeline_runs` `allday-unmapped-resolver` extra counters, full 24h window (`decode_attempted` / `resolved_via_decode` / `buyer_excluded` / `scan_new_holders_tried`). Ties to the 2026-07-26 CC ledger WATCH "ALLDAY-DECODE-LEG-EFFICACY".
- **24h measurement (94 runs since the 15:01Z resolver-fix deploy):** `decode_attempted` **3,599** → `resolved_via_decode` **0** (0.0% over a full day, ~3,600 wasted Flow-REST tx fetches). `scan_new_holders_tried` **0** (scan spends its full budget for 0 scan resolutions — `scan_ineffective` confirmed at 24h). Total promoted across all runs = 44, ALL via the Leg-A borrow / `decodeV1SaleTx` buyer path, none via the always-on decode leg. 36/94 runs ok (the rest honestly `ok:false` "degraded: 0 promoted" — the INTENDED post-fix tripwire behavior, not a regression; do NOT re-flag).
- **NEW & decisive — the CC prediction is falsified.** The 07-26 ship predicted the decode leg "pays off as the contract-address class is reached." Over this window `buyer_excluded` = **180** — the `0xe4cf4bdc1751c65d` contract-address class the fix unblocked HAS now been reached 180×, and the decode leg STILL resolved 0. So the leg is not merely "not yet paying off"; on 24h of real data across the exact class it was built for, it resolves nothing.
- **Risk:** resolver ROUTE logic (`lib/chains/flow/allday-edition-onchain.ts` + `allday-resolve-unmapped`) = ingest-adjacent, so this is off-limits for autonomous auto-ship — a Trevor / Claude-Code item, not a night-pass ship. The change itself is low-risk and additive-narrowing.
- **Suggested action (now safe to act, no longer "wait and see"):** narrow the decode leg to fire only when the stored buyer was excluded/absent rather than on every nil borrow, and reclaim `SCAN_CHUNK_BUDGET` given 24h of `scan_ineffective`. Net: removes ~3,600 wasted Flow-REST fetches/day + the scan budget (and the occasional upstream-timeout blowout the 00:10Z tick observed) with zero loss of resolutions (buyer leg produced 44/44 promotions and 196/196 in the prior window).

---

Nothing else new. Carried/known (already in ledger/inbox, not re-raised): DUNE-DATAPOINT-CAP-402 (sales-ingest-dune 12 + sales-seller-recovery-dune 15 fails/24h, HTTP 402 operator/billing), ALLDAY-UNMAPPED-SALES-BACKLOG-GROWTH (HIGH, ~28,098 actionable open nfl_all_day rows, resolver-throughput / off-limits), SET-DETAIL-PAGE-POOL-RETRY-GAP, and the standing queue. All other `pipeline_fails_24h` entries are single self-recovering contention ticks.
