# Daytime monitor candidates — 2026-07-14T06:06Z (~23:06 PDT Jul 13 tick)

Health GREEN. security 0/0/0/0 · trust_health_breaches [] · sentinel TS-UUID-48h 0 · DB 8,122 MB · Vercel prod `034e7cdd` READY (no ERROR deploys). One new low candidate below; everything else is the known contention family (already ledgered) or post-ship-watch.

## NEW candidate

### LOW — `get_active_challenges` displays just-expired challenges as active until the daily 08:10Z ingest re-syncs
- **Source:** `get_active_challenges()` fn def + `challenges` table. The `ch` CTE is `WHERE collection_id=<TS> AND status='active'` with **no `ends_at` guard**. 31 rows are `status='active'`, but 10 of them already passed `ends_at` (2026-07-14 03:00Z, ~3h before this tick) — e.g. 2026 NBA Playoffs, Clamps, Hoop Vision, Bag Work, Vintage Vibes. The 21 genuinely-live ones end 07-15/07-16 03:00Z.
- **Blast radius:** cosmetic/UX only. The public challenges tab + the `rpc-set-challenge-roi` Cowork artifact (both read `get_active_challenges`) show ~10 stale/expired challenges for the ~5h daily window between a wave's `ends_at` (03:00Z) and the next status re-sync at the ingest cron `10 8 * * *` (08:10Z). Self-heals each morning. No data corruption; `endsAt` is already in the payload so no info is lost, just not filtered.
- **Risk read:** LOW-risk, isolated. Fix = add `AND (c.ends_at IS NULL OR c.ends_at > now())` to the `ch` CTE (and mirror in `get_challenge_plan` if it shares the filter). `activeCount` would then reflect live challenges. SECDEF fn, service_role/authenticated grants unchanged — pure filter tightening.
- **Caveat for the night pass:** this is a Trevor-actively-worked surface (challenge airdrop-adjusted netEv + reward-value fallbacks shipped 07-13/07-14, ledger). If Trevor is mid-iteration on the challenge RPCs, defer — otherwise it's a clean one-line filter add. Not urgent (self-heals daily).

## Post-ship-watch note (not a candidate — night pass already does this)
- **analytics-smoke** 20 ok / 28 fail 24h (worst pipeline), last_ok 04:13Z, all failures `statement timeout` in the analytics_smoke_run. Trevor's `034e7cdd` (prod READY ~06:06Z, i.e. minutes before this tick) explicitly targets the "pack dist page has Sales History" smoke failure firing since 07-13 via a new `get_pack_market_row` SECDEF RPC (replaces a 26.4s whole-history aggregate with ~0.8s). Expect analytics-smoke fail-rate to drop after this deploy — worth confirming on the next tick / overnight watch.

## Known contention family (already ledgered — NOT new)
Timeout-class fails clustered in the ~02–06Z window, all with recent OK runs (self-recovering, not stuck):
- `wallet-username-resolver` (28 ok/20 fail, last_ok 05:08Z), `lock-check-batch` (29 ok/18 fail, last_ok 04:38Z), `run-insider-detectors` (17 ok/4 fail, last_ok 02:26Z, nfl_all_day upstream timeout — the per-collection split residual = operator de-peak lever).
- pg_cron: `rpc-backfill-pinnacle-mint-acquisitions` + `rpc-allday-resolve-rip-dist-api` "job startup timeout" (isolated); `rpc-attribute-pack-rips-empirical` statement-timeout on a heavy WITH aggregate 03:10Z (1/1 run — folds into DAYTIME-CONTENTION).
- `refresh-pack-grail-metrics-mv` cron_silent (INFO, last run 04:23Z, missed the hourly Vercel tick — same isolated class as 07-13 night).
- fmv-recalc / alerts-dispatch / compute-topshot-pack-ev all latest-OK (05:48–06:07Z) — contention window clearing normally.

Artifacts: pack_ev_latest (1816) / pack_table_rows (5439) / topshot_pack_ev_targets (823) / v_rpc_trust_health (16) all return rows via the service-role path after Trevor's 07-13 `security_invoker` flip — no artifact-read breakage from that migration.
