# Daytime monitor — 2026-07-19T18:20Z

Tick 2 of the day (prior: `2026-07-19T1512Z.md`). Lock RELEASED (night pass `night-20260719-12828`, 08:17Z) ⇒ inbox commit permitted.
No clock skew: DB `now()` 18:08:25Z ≈ newest sale 18:08:02Z ≈ newest FMV 18:06:24Z.
Concurrent interactive session ACTIVE (Trevor + Claude Code pushing all afternoon: 4 commits 16:55–18:08Z, `90363bfd`→`0e243e5e`).

Health summary: security 0/0/0/0 · trust 15 metrics / 1 BREACH (known class) · pg_cron 1 transient · Sentry 1 issue/1 event · Vercel 0 ERROR · DB 10,166 MB.

---

## HIGH — VERCEL-EMPTY-COMMIT-CANNOT-FORCE-REBUILD (blocks an activation Trevor is doing right now)

**Source:** Vercel `dpl_CkjigYpYERX7XH7dYGQ8R5VjM94j` (state CANCELED, `errorLink` -> ignored-build-step) · commit `0e243e5e` · `vercel.json:2` · `pipeline_runs` `sales-ingest-dune` 18:11:41Z.

**What happened.** Trevor set `DUNE_SALES_INGEST_QUERY_ID` in Vercel and pushed commit `0e243e5e` *"chore: rebuild to bake DUNE_SALES_INGEST_QUERY_ID"* at 18:08Z to force the rebuild. **The commit is EMPTY** (`git show --stat` -> zero files changed) and the build was **skipped in 2.5s** by the project's Ignored Build Step. Production is still `bd1c87eb` (READY, 17:54Z). The activation did **not** take effect.

**Confirmed by observation, not inference:** the very next tick of the route, `sales-ingest-dune` at **18:11:41Z**, still logs `extra = {"skipped": "dune_not_configured"}`. The pre-2026 TS sale-ingest pipeline remains INERT.

**Root cause is in-repo and exact — `vercel.json` line 2:**

    "ignoreCommand": "... git diff --quiet HEAD^ HEAD -- . ':(exclude)docs/**' ':(exclude)*.md' ':(exclude)*.mdx'"

`git diff --quiet` exits **0** when there is no diff, and Vercel skips the build when ignoreCommand exits 0. An empty commit therefore has an empty diff -> exit 0 -> **build always skipped**. This is structural: **on this project an empty commit can never force a rebuild**, and neither can a docs/`*.md`-only commit (that is the same trap CLAUDE.md records biting on 07-16 and 07-18 — this is the third instance, and the first where it silently blocked an operator activation).

**Risk:** none from acting (a rebuild is routine). Risk of *not* acting: the ingest stays inert indefinitely while looking activated — the env var is set, so a later reader would reasonably assume it is live.

**Suggested action (operator / Trevor — pick one, ranked):**
1. **`POST https://api.vercel.com/v13/deployments`** with `{"name":"rip-packs-city","gitSource":{"type":"github","repoId":"1188272071","ref":"main"}}` via PowerShell `Invoke-WebRequest`. Bypasses `ignoreCommand` entirely; this is CLAUDE.md's documented redeploy path and the one that actually re-bakes env vars.
2. Touch any non-docs file (one whitespace char in a `.ts`) and push — makes `git diff --quiet` exit 1.
3. *Not recommended alone:* dashboard "Redeploy" — CLAUDE.md notes it reuses cache and does not re-bake env vars.

**Verify after:** `sales-ingest-dune`'s next tick (cron `11 */2 * * *`) should stop logging `skipped:dune_not_configured`. Then watch FMV and eyeball `pipeline_runs.extra` (`inserted` / `skipped_unresolved` / `skipped_multimoment`) on the first real tick before letting it drain — this is the one pipeline that INSERTs into the FMV-feeding `sales` table.

**Durable lesson worth folding into CLAUDE.md:** "push an empty commit to force a rebuild" is a common reflex and is **guaranteed to fail** on this project. The reliable force-rebuild is the v13 deployments POST.

---

## MED — SALES-COUNTERPARTY-BACKFILL-CLAIM-TIMEOUT-DAYTIME-COLLAPSE

**Source:** `pipeline_runs` `sales-counterparty-backfill`, hourly rollup 04:00-18:00Z.

Recovery throughput has collapsed during the afternoon contention band. Rows written per hour:

| hour (Z) | 07 | 08 | 11 | 12 | 14 | 15 | 16 | 17 | 18* |
|---|---|---|---|---|---|---|---|---|---|
| written | 1440 | 1440 | 1426 | 1335 | 1307 | 879 | **54** | 546 | 228 |
| fails/runs | 0/12 | 0/12 | 0/12 | 0/12 | 0/12 | 2/12 | 3/12 | **7/12** | 1/3 |

Error is uniformly `Error: claim failed: canceling statement due to statement timeout` — i.e. `claim_sales_counterparty_batch` is timing out, so the tick claims nothing. The 17:00Z hour failed **58%** of ticks; the 16:00Z hour wrote only 54 rows across 12 runs, so even *succeeding* ticks are claiming near-nothing => this is claim-path contention, not only hard timeouts.

**Not data-damaging** — the claim is the first step, so a timeout means no rows claimed and no partial writes (fill-only + audited by construction). But it materially slows the documented ~9-10 week drain, and 58%/hr is well past the "occasional contention flap" the overnight pass correctly classified at 3 fails in hour 05.

**Why this is a change, not the known item:** the 07-19 overnight pass measured **100% recovery at full tick rate** (1,440/1,440) and explicitly called the hour-05 timeouts contention-class. That still holds for the night. What is new is the *daytime* profile — a monotone decline from 14:00Z onward, bottoming at 3.75% of nominal.

**Risk read:** LOW to touch, MED to leave. Candidate levers for the night pass / CC: raise `statement_timeout` on `claim_sales_counterparty_batch` (it is a pure in-DB claim, same class as the 07-16 offers-raise 600s ship), and/or shrink the claim batch (120 rows) during high-contention hours. **Not auto-shippable as-is** — it touches the `sales`-write lane, so it wants a deliberate owner decision. Re-measure on the next quiet window first; if overnight returns to 1,440/hr the correct framing is "daytime-only degradation, tune or accept," not "the worker is broken."

---

## LOW — IMPOSSIBLE-PARALLEL RE-ACCUMULATION RATE (new datapoint on a known class)

`topshot_impossible_parallel_serials` = **9** vs threshold 3 (BREACH). Known self-healing WNBA `::`-cataloging class; the fix is the sanctioned interactive/Trevor circ floor-raise, **not** autonomous.

The datapoint worth recording: the overnight pass **CLOSED this at 0** this morning (27 -> 0). It is back to **9 within ~10 hours**. Prior waves accumulated over multiple days. Either today's cataloging wave is unusually heavy, or the floor-raise clears symptoms faster than the underlying floor-seeding re-creates them. Not urgent (9 is small, self-healing), but if the next tick shows continued climb it argues for fixing the circ floor-seed at ingest rather than repeatedly raising after the fact.

---

## LOW — BACKFILL-PACK-RIP-METADATA MISSED 2 TICKS

`detect_stalled_pipelines()` returns exactly one element: `backfill-pack-rip-metadata`, **INFO**, 137 min silent vs 120 threshold, last run 15:53Z. Hourly `:53` Vercel cron; missed 16:53Z and 17:53Z. 10 runs in 12h vs 12 expected. INFO severity, denormalization-only (rip `dist_id` / `pull_value_usd`), self-heals on the next tick. Watch only; if it misses a third consecutive tick it graduates to the cron-dropout class.

---

## LOW / WATCH — SALES-SELLER-RECOVERY-DUNE RUNS NEAR THE 800s LAMBDA CAP

Successful runs are taking **720,201 / 720,340 / 724,123 / 737,073 / 735,213 ms**. The ~720s floor looks like a deliberate soft deadline, but the last three ticks overran it by 4-17s. Vercel Pro `maxDuration` hard cap is **800s**, and CLAUDE.md documents that exceeding it fails *invisibly*. Current headroom ~63s (8%). No action now — just flagging that the margin is thin and the overrun is trending up, so if window sizes grow this becomes a silent-failure risk.

---

## Post-ship watch — PASSES (no action)

- **DUNE-SELLER-RECOVERY-EXECUTE-400 (queued MED, nc1) — VERIFIABLY FIXED; recommend CLOSE.** The same-day `date`->`text` param retype on saved query `8027085` holds. Last failure **12:47Z**; every tick since is `ok=true` — 13:47Z (2 rows), 14:47Z (0), 15:47Z (26,844), 16:47Z (32,582), 17:47Z (24,640). Walking backward cleanly through windows (2026-05-14 -> 2026-02-26), `windows_done` advancing, cursor healthy. **~84k seller rows recovered this afternoon alone.**
- **candy-offers-indexer** — stable and idempotent: `candy_offers` 47 / `candy_best_offers` 24, unchanged across ticks (matches the overnight reading exactly => deactivation guard is not falsely retiring standing offers). `candy-sales-indexer` last 15:20Z, on its `20 */3` cadence.
- **Candy / editions / sentinel** — editions TS 19,451 (flat vs overnight), candy_mlb 125 (expected), sentinel TS-UUID-48h **0**, `ts_uuid_dupes_created_24h` **0**.

## Artifacts

17 artifacts enumerated. **Dependency validation clean** — all 14 backing relations resolve with the expected relkind (`v_rpc_trust_health`, `pack_table_rows`, `pack_ev_latest`, `mv_pack_ev_latest`, `fmv_current`, `candy_offers`/`candy_best_offers`, `panini_coverage_summary`/`_audit`, both `cross_collection_*_mat`, `pipeline_runs`, `pipeline_cadence_watchlist`, `v_fmv_sanity_flags`). No renamed/dropped view.

**Full payload replay deliberately deferred again** — and this time for a *measured* reason, not the access gap the earlier ticks hit. The DB is inside a contention band right now: `rpc_ops_snapshot()` and a bare `SELECT * FROM v_rpc_trust_health` both hit statement timeouts during this sweep, and six pipelines logged contention timeouts in the same window. Firing `rpc-live-health`'s heavy 14-day `MATERIALIZED` scan over `fmv_snapshots` would add load to the exact resource that is already the constraint, and would risk misattributing a contention timeout to a broken artifact. Recommend the replay run in a genuinely quiet window (<=06:00Z).

*(Noting for the record so it stops being re-raised: the artifact directory `C:\Users\TDill\Claude\Artifacts` IS readable via the `Read`/`Grep` tools with the Windows path — only sandboxed `bash` cannot see it. The overnight pass established this; the blocker is now load, not access.)*

---

## Detail — health sweep

- **Security 0/0/0/0** — `rowsecurity=false` public tables **0**; `check_public_security_invariants()` clean; `check_secdef_anon_execute_violations()` `[]`.
- **Trust health** — 15 metrics, 1 BREACH (`topshot_impossible_parallel_serials` 9/3, above). All FMV freshness green: topshot 0.0h, allday 0.0h, golazos 0.0h, pinnacle 8.1h, ufc 5.3h, pinnacle_render_floor 4.4h. `fmv_sanity_flags` 0, `pinnacle_fmv_impossible_flags` 0, `unmapped_resolution_backlog_max` 25, `edition_integrity_flags` 5 (ok), pack-EV board 0.74d stale / 0% depleted.
- **pg_cron** — 1 entry: `rpc-atlas-pack-ev`, 1 fail in 24 runs at 17:25Z, statement timeout. Single transient inside the contention band; 23/24 green. No action.
- **Pipeline fails 6h** — `sales-counterparty-backfill` 15/72 (above); `lock-check-batch` 2/12; `wallet-username-resolver` 2/12; `compute-topshot-pack-ev` 2/118; `pinnacle-nft-resolver` 2/70; `fmv-recalc` 1/27; `sales-seller-recovery-dune` 1/6 (the 12:47Z pre-fix run). All except the first are the known DAYTIME-CONTENTION family and all have a green latest run except `lock-check-batch` / `wallet-username-resolver` (18:08Z).
- **Platform-wide fail rate by hour** — 0.0-1.0% overnight, rising to 1.7% (17Z) and 3.8% (18Z, partial). Elevated but modest; comparable to the 05Z (0.9%, 10 fails) and 10Z (1.0%, 11 fails) bands. Not an outage.
- **Sentry** — 1 unresolved issue in 24h: `JAVASCRIPT-NEXTJS-1Y` *"team detail unavailable: canceling statement due to statement timeout"*, `GET /[collection]/team/[slug]`, **1 event / 1 user**, first=last seen 5h ago. Same contention class; not spiking.
- **Vercel** — prod `bd1c87eb` READY. 0 ERROR-state deploys. 3 CANCELED in the window, all by the `ignoreCommand` (two docs-only `docs(ledger)` commits — working as designed — plus the empty commit above, which is the HIGH finding).
- **Drift vs `metrics-latest.json`** — DB 10,054 -> **10,166 MB** (+112, benign); editions TS 19,451 flat; candy_mlb 125 flat; sentinel 0 flat.
