# Daytime monitor candidate — 2026-09-18 ~07:45 PT (14:45Z)

Run: rpc-daytime-monitor, first tick of day. Concurrency lock present but RELEASED (2026-09-14), not active.

## 🚨 P0 SYMPTOM — DB reads unavailable across MCP + production, concurrently (NOT the snapshot-statement-timeout false signal)

- **Source:** three independent live instruments, all at ~14:40–14:47Z:
  - MCP `execute_sql` — **`SELECT 1` fails 6× over ~2.5 min with "Connection terminated due to connection timeout"** (connection-ESTABLISHMENT failure, not a statement cancel; `rpc_ops_snapshot()` and the `pg_stat_activity` positive control both unreachable for the same reason).
  - Production `/insights/squeeze` (PostgREST path) — renders the honest degraded state: **"PARTIAL DATA · 1 of 1 section could not be loaded (Squeeze board). This is a temporary database-load failure, not an empty result."** (honesty layer working correctly — no fabricated zeros).
  - Production `/insights/pack-reality` — all headline stats "—", "PULL-VALUE DISTRIBUTION / LOADING…" stuck (client fetch failing now).
- **Not the known false signal:** the 2026-09-14 doctrine says a bare `rpc_ops_snapshot()` *statement* timeout (57014 at the 50–55 s budget) is NOT spell evidence. This is different and worse — connection establishment itself is refused, and it is corroborated by production PostgREST reads failing at the same time. This is a real, current, user-facing DB read-availability event.
- **What is UP (measured, different paths):** Supabase management API `get_project` → `status: ACTIVE_HEALTHY` (Postgres 17.6.1.111 GA, not paused). Vercel latest production deploy → `state: READY` (commit 00662ce, migration-autorecover bot). So the engine process and the app server are alive; it is the DB read/connection path that is failing.
- **Risk:** HIGH / user-facing. Public insights boards degrade to PARTIAL/unknown for anon visitors while this persists. Contained by the honesty layer (no false zeros), but the intelligence product is showing no data.

- **CAUSE NOT ASSERTED (per §1c).** Could not run the `pg_stat_activity` IO/active positive control — connections are refused, which is itself the tell but does not discriminate between (a) compute/IO saturation exhausting `max_connections`=90 on the SMALL tier and (b) a connection-layer / pooler / platform incident with the engine otherwise healthy. Every duration read now is uninterpretable. **Do NOT ship a fix derived from this observation.**

- **Suggested action (night pass / Trevor): quiet-window RE-MEASURE, do not conclude from this filing.**
  1. The moment a connection succeeds, take the positive control: `SELECT count(*) FILTER (WHERE wait_event_type='IO') AS io_wait, count(*) FILTER (WHERE state='active') AS active, count(*) AS total FROM pg_stat_activity WHERE pid <> pg_backend_pid();` — majority IO-wait / near-90 total = saturation; low counts = connection-layer incident (engine was fine, look at the pooler/platform).
  2. Confirm whether it self-cleared (re-hit `/insights/squeeze` for real rows + a trivial MCP `SELECT 1`) and note the window it spanned.
  3. If saturation: identify the reader(s) that pinned the pool in that window from `pipeline_runs` / pg_cron `job_run_details` around 14:30–15:00Z 2026-09-18. If connection-layer: nothing to fix in-repo — platform/pooler.
- **ISR caveat (honest):** `/insights/squeeze` is ISR ("refreshes hourly"), so its degraded panel *could* be a cached failure up to ~1h old. But the live MCP connection refusals and Pack Reality's stuck client fetch are current, so the event is current regardless.

## Rest of the sweep — UNMEASURED (DB unreachable; not fabricating a reading)

All DB-backed checks (rpc_ops_snapshot security block, detect_stalled_pipelines, check_pgcron_recent_failures, trust health v_rpc_trust_health, cross-collection refresh verify, artifact payload queries, drift vs metrics-latest.json) could not run — connections refused. Per the #121 exit practice, the legs would normally be re-derived individually, but that also requires a connection. Reported as UNMEASURED, not clean. First-tick-of-day 1a extras (trust health, cross-collection pg_cron verify) owed on the next tick that has DB access.

Artifact validation (§1b) skipped — every payload query hits the same unreachable DB; re-validate in a quiet window (do not re-run heavy payloads into a possible spell).
