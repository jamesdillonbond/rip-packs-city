# rpc-daytime-monitor candidates — 2026-08-02 ~15:12Z (first-tick-of-day)

Health NOT green this run: an **active, platform-wide DB IOPS / connection-pool saturation window** is underway (fails as recent as 15:08Z). Sensing only — nothing shipped.

---

> ⚠ **CORRECTION appended 2026-08-02 ~16:35Z (Claude Code, interactive) — dimension 1 of Candidate 1 below is WITHDRAWN. Do not spend a pass on it.**
> **`board_mv_refresh_stale_hours=999` was a FALSE POSITIVE and is now fixed** (`audit_20260802_board_mv_watchdog_no_false_breach_on_new_row`, commit `8650b93d`). `board_mv_refresh_max_stale_hours()` returned the sentinel 999 for any watchlisted MV with no *successful* cron refresh **yet**. `mv_candy_holder_board` was materialized at **14:55:36Z** and its job runs `47 * * * *`, so at the 15:12Z probe it had **0 rows in `cron.job_run_details`** — 20 minutes before its first possible tick. The metric now reads **0.97 → ok**.
> **No public board is stale.** Measured directly just now, all 7 watchlisted MVs: `perfect_mint_premiums` 1.67h, `market_index_daily` 0.91h, `pack_reality_stats` 0.84h, `pack_reality_top_ev` 0.79h, `pack_reality_dist` 0.59h, `pack_realized_ev` 0.33h, `candy_holder_board` 0.26h — **every one well inside its 6h budget**. `rpc-refresh-allday-pack-realized` is cited below as "2/4 fails" but sits at 0.33h stale, i.e. it is failing *intermittently* and succeeding on retry. The hourly cadence absorbs contention-induced misses.
> **Dimensions 2 (wmc `rows_lost`) and 3 (entity-page 500s), and the underlying IOPS saturation, are UNAFFECTED by this correction and remain real.** Only the "boards going STALE" claim is withdrawn. The tripwire was preserved, not weakened: a job that genuinely never fires now grows from its watchlisting time and still breaches (proven — a 20h-old watchlisting with no successful refresh reads 20.00 > 8).

## CANDIDATE 1 — [HIGH] IOPS-SATURATION escalating: MV refreshes failing, wmc data loss, entity-page 500s
- **Source:** `check_pgcron_recent_failures()` (6 jobs timing out); `v_rpc_trust_health` `board_mv_refresh_stale_hours=BREACH 999 (breach_at 8)`; `pg_stat_activity` (nearly every active query in `IO/DataFileRead` wait, two PostgREST calls 74–82s); `pipeline_runs` 24h fails; Sentry NEXTJS-23/-24.
- **This is an ESCALATION of the night-pass-queued `public_board_slow_count` IOPS item**, not a new root cause — but it now carries three dimensions that item did not:
  1. **Public boards going STALE, not just slow.** `board_mv_refresh_stale_hours=999`. 6 pg_cron MV refreshes are timing out: `rpc-refresh-allday-pack-realized` (2/4 fails, last 12:35Z), `rpc-refresh-new-collectors` (mv_ts_buyer_first_buy, 09:45Z), `rpc-refresh-misattrib-candidates` (08-01 15:35Z), `rpc-serial-fmv-multipliers-weekly` (11:00Z Sun run — the by-design weekly job FAILED on timeout, distinct from its normal ≤7d staleness), `rpc-weekly-wmc-prune` (10:20Z), `rpc-trust-health-precompute-refresh` (12:58Z, dies on `SELECT count(*) FROM cross_collection_deals_board`). `rpc_ops_snapshot()` itself times out (sentinel_fmv_confidence_rows) — the monitoring can't fully read itself.
  2. **Genuine wmc DATA LOSS.** wallet-backfill `wmc_upsert_chunk_failures` with `rows_lost`: pinnacle 230, allday 260, base wallet-backfill 400, ufc 8 — chunk upserts timing out mid-write. (wmc self-heals on the next refresh, so likely not durable, but it is real loss this window.)
  3. **User-facing.** Sentry NEXTJS-24 (`GET /[collection]/set/[slug]`) + NEXTJS-23 (`GET /[collection]/player/[slug]`) both `canceling statement due to statement timeout`, 1 event / 1 user each (4h + 18h ago). Low volume; the known saturation-class entity-page timeout.
- **Blast radius:** ~25 pipelines with fails in last 24h, the bulk in the last 1–2h (`wallet-username-resolver` 22, `wallet-backfill-pinnacle` 20, `wallet-backfill-allday` 19, `compute-topshot-pack-ev` 13, `pinnacle-nft-resolver` 12, `lock-check-batch` 11, `allday-lock-refresh` 10, `wallet-backfill` 10, `alerts-dispatch` 3 on deal-timeout, etc.) — errors are `statement timeout` / `Timed out acquiring connection from connection pool` / `upstream request timeout`.
- **Most-likely cause:** Supabase PRO **Micro** IOPS + connection-pool ceiling under concurrent heavy read/write (wallet-backfills + fmv inserts + MV refreshes contending). Consistent with 08-01 Cowork board-materialization commits (358b685, 6SLPhCW) that were already fighting this by moving slow board legs to MVs.
- **Risk read:** No safe autonomous fix — the durable levers are infra/heavy. Short-term: nothing to blind-ship.
- **Suggested action (night pass / Trevor):** progress the already-queued sales-partition REINDEX + remaining board-MV materialization; consider staggering the heavy wallet-backfill cadence off the MV-refresh windows to cut peak contention. A Micro→Small compute bump would address it directly but trips the pre-revenue infra-spend guardrail — Trevor's call. Do NOT auto-revert any recent ship (this is load, not a regression). Verify whether the window self-clears as daytime load eases before treating as a standing incident.

## CANDIDATE 2 — [MEDIUM] classify-acquisitions-multicollection cron-silent ~11h (dropout, not saturation)
- **Source:** `detect_stalled_pipelines()` — silent 663 min vs 180 threshold; last run **04:06Z ok=true (43 rows)**, no rows since.
- It logged real timeout FAILURES 19:00–21:00Z 08-01 then recovered (ok at 22:00Z→04:06Z), so it CAN log — the post-04:06 silence with zero rows = the hourly **cron-job.org tick is not firing**, a dropout, not the saturation above.
- **Suggested action:** verify the cron-job.org entry is enabled/firing; likely self-resolves on the next successful tick. Low urgency (acquisition classification backfills).

---

### Also observed (NOT candidates)
- `pinnacle-sync` stalled 1742 min vs 1560 threshold — marginal ~3h overrun on a slow-cadence pipeline; watch, not yet actionable.
- Security clean: rls_off_tables 0, `check_public_security_invariants()` null, `check_secdef_anon_execute_violations()` []·
- Vercel: latest prod deploy READY (dpl_7m7jdLYz…, UFC-ask fix); no ERROR-state deploys (CANCELED entries are superseded docs/concurrent commits).
- `unmapped_resolution_backlog_max=100` (at threshold) — known draining class, do not re-flag.
- **Artifact payload validation SKIPPED this run** by design: running the dashboard payload queries during an active IOPS-saturation window would add load and yield timeout false-positives. Re-validate on the next tick once the window eases.
