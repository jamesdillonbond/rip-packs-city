# Daytime monitor candidates — 2026-07-06 ~18:10Z (11:06 PDT tick)

Run context: no clock skew (shell 18:06Z == DB now() 18:07Z == newest sale). No lock held (overnight pass RUN 2 released 08:21Z). This is a later daytime tick; first-tick-of-day 1a extras still run (no earlier inbox file today): trust-health full view = 16/16 ok / breaches [] (via rpc_ops_snapshot); cross-collection pg_cron refresh VERIFIED — rpc-ccm-step1 04:10Z + rpc-ccm-step2 04:25Z both succeeded today, cohort_mat 154 fresh, overlap_mat fresh, both jobs active.

Health GREEN: security 0/0/0/0; trust 16/16 ok (topshot_impossible_parallel_serials 1/3, unmapped_resolution_backlog_max 30/100, edition_integrity 4/50 all under); detect_stalled_pipelines() []; check_pgcron_recent_failures() [] (clears the queued CROSS-SOURCE-DEDUP jobid-32 concern this tick); sentinel TS-UUID-48h 0; editions flat (TS 18,151 / AllDay 6,190 / Golazos 575 / UFC 518); FMV TS H+M 4,979 (1347+3632, improving from 4,934) / AllDay 826 / UFC 15 / Golazos 4; Pinnacle 2,203 renders priced; DB 8,291 MB (+86 since overnight = ownership-MV + backfill growth, benign); Sentry 0 unresolved/24h; Vercel prod d91e40a1 (pack-sniper board fix, Trevor) READY atop a heavy daytime CC ownership/Dune wave (Pipeline B on-chain walk, rookie collectors + set-completers frontends, dune-proxy re-execute, market cache/warm cron) — 20 recent deploys, 0 ERROR (CANCELED = docs-only), no new runtime-error class on current prod. Flagship rpc-live-health consolidated payload query ran clean (all panels populated, FMV/editions/insights reconcile with snapshot) => artifact estate validated (additive-only schema changes today, nothing dropped/renamed).

## LOW — GATEWAY-TIMEOUT CONTENTION FLAPS (new members of DAYTIME-CONTENTION-CLUSTERS-BROADENING; track-only, no autonomous fix)
Source: pipeline_runs. Two pipelines whose LATEST run is ok=false with "upstream request timeout" (the Supabase API-gateway ~120s cap; the lambda keeps running past the cutoff so it logs a fail with dur 125-248s). Neither STALLS (detect_stalled_pipelines() []); both self-recover.

- run-insider-detectors (hourly :26): healthy ~26s on nearly every tick, but 3 gateway timeouts/24h — 17:26Z 248s FAIL, 01:26Z 185s FAIL (+ 00:26Z 102s / 12:26Z 63s elevated). 16:26Z ok 29s immediately before the 17:26Z fail => next :26 tick recovers. Blast radius: insider-detector signals miss ~2-3 hourly ticks/day. LOW (not FMV / deal-boards / pack-EV / user-facing).
- daily-portfolio-snapshot (daily 07:05): 07-06 FAIL 125s vs 07-05 ok 8s / 07-04 ok 13s — a single gateway timeout in the 07:0xZ overnight-contention window; today's portfolio-value snapshot didn't write (one-day gap), re-runs 07-07. Not a growing cliff (8s yesterday).

Risk read: both are the known "upstream request timeout" gateway-cap class firing during I/O-contention windows (the 07:0xZ overnight cluster + the 17:2xZ heavy-daytime-deploy+backfill wave). NEW named members of the standing contention set (not previously carried by these names). Fold into DAYTIME-CONTENTION-CLUSTERS-BROADENING.

Suggested action: TRACK ONLY. No safe autonomous fix — a naive fn statement_timeout bump risks the after()-lambda-kill/invisible-failure class (route runs work in after() under maxDuration), so the real levers (coordinated route-maxDuration + fn-timeout, or query-scope reduction) are CC/operator. Graduate to a real fix only if the flap rate climbs materially (e.g. run-insider-detectors >~4 gateway timeouts/24h, or daily-portfolio-snapshot fails a second consecutive day).
