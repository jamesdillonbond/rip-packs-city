# Monitor inbox — 2026-06-27T06:06Z (23:06 PDT, late / end-of-window tick)

Platform **GREEN**. Health sweep + artifact validation clean. No clock skew (DB now() 06:06Z == shell == app-stamped sale 06:03Z / fmv 06:06Z). Post-ship watch on the late-evening CC wave since the 03:14Z tick (`182b0d1`->`40e98c3`, incl. the new public Rookie Board) ALL PASS. **1 new LOW candidate.**

## Recent-ship watch (commits since the 03:14Z monitor tick)
- **`9fc5851` public Rookie Board `/insights/rookie-board`** — NEW public surface (see candidate). Data-layer + security verified clean this tick.
- **`89992e6` DRAIN-WATCHLIST shipped** — CONFIRMED: `topshot-flowty-unmapped-drain` now in `pipeline_cadence_watchlist` (90m/medium/active). The night-pass-queued DRAIN-WATCHLIST item is RESOLVED; `detect_stalled` does not flag the drain. PASS.
- **`e75f6c3` + `40e98c3` Dune ownership Pipeline A (TopShot ownership index)** — INERT until Dune provisioned (`topshot_ownership` 0 rows; read-MVs apply-ready but unpopulated). No runtime effect, nothing to monitor yet. PASS.
- **`182b0d1` data-integrity get_fmv_coverage() RPC (replaces the 14.7s health_check 504)** — GHA/route, not pipeline_runs-tracked (same verify-limit prior ticks noted). Indirect PASS: 0 new Sentry, 0 fails/6h, security 0/0/0/0.
- **`b0a6554` vercel skip dependabot/* preview builds** (Build-CPU cost fix) — config; deploy READY = current live prod. PASS.
- **`407d68a` context hygiene** (CLAUDE.md rotate 1576->863, ledger roll, archive handoffs) + **`3c20100` carried-cluster drain record** (incl. `audit_20260627_remap_ts_wmc_uuid_fossils` = 211 TS-wmc fossils remapped) — docs/record; editions FLAT confirms the remap left no edition leak. PASS.

## NEW candidate

### ROOKIE-BOARD-INSIGHTS-QA — finish rpc-insights-qa on the brand-new public /insights/rookie-board (LOW - night-pass or interactive)
- **Source:** commit `9fc5851` (Trevor/CC, ~21:12 PDT 06-26) — second new public surface this cycle (after new-collectors), shipped after the 03:14Z tick.
- **Monitor ALREADY verified clean this tick (read-only):**
  - Backing view `topshot_rookie_edition_board`: `security_invoker=on`, anon grant = SELECT, **431 rows / 61 players / 227 parallel (::) rows / 401 with FMV** (matches commit), **0 wallet/address/owner columns** = no address leak. 203 base rows carry `has_full_economics`.
  - `sitemap.ts` line 319 (`rookie-board`); `proxy.ts` `/insights/*` (L312) + `/api/public/*` (L176) anon bypass => **NOT login-gated** (the new-surface SEO trap is clear).
  - Routes present: `/insights/rookie-board` page/layout/client, `/api/public/insights/rookie-board` (anon JSON, s-maxage=900), `/api/og/insights/rookie-board`; reciprocal link from `/insights/rookies`; Vercel prod READY; 0 attributable Sentry.
- **Remaining (needs live HTTP / visual — monitor is provenance-gated from web_fetch):** anon HTTP 200; player->set->parallel drill-downs resolve into entity hubs; the honesty contract (parallel `::` rows render FMV+circulation only, ask/offer/sale/burn/lock never $0 via `has_full_economics`); brand tokens (0 hardcoded #E03A2F outside the OG route); Burn Rankings view (#2).
- **Risk:** read-only QA, no ship risk. **Suggested:** night-pass or interactive `rpc-insights-qa` close — same basis used to close NEW-COLLECTORS / SERIAL-PREMIUMS / TOP-SALES.
- **Adjunct (LOW):** add a rookie-board row/freshness leg to the `rpc-live-health` `insights_counts` panel + `rpc-qa-scorecard` on next reinstall (pairs with the same adjunct already logged for new-collectors).

## Notes (owned/carried — NOT candidates, do NOT re-flag)
- **unmapped_resolution_backlog_max BREACH 441** (trust 8/9): OWNED/Declined. `topshot-flowty-unmapped-drain` healthy + now watchlisted; backlog plateaued ~428->441 over the day (producer inflow ~= drain rate). Do NOT re-flag/skip/retire/raise-threshold; cadence is the only lever. DRAIN-WATCHLIST now shipped.
- **ALLDAY-FMV-POPULATE-NOOP-STALL** (logged 03:14Z, still open): `detect_stalled` 1 + `get_pipeline_alerts` 1 medium = the superseded no-op writer (last fired 06-26 22:42Z; AllDay FMV fresh via fmv-recalc 1.7.0, H+M 901). Suggested DB-only fix (remove its watchlist row) still pending night-pass. Not re-logged.
- **NEW-COLLECTORS-INSIGHTS-QA** (logged 00:19Z, still open): live-HTTP/visual close still pending. Not re-logged.
- `ufc_sales resolving_editions` INFO (13/24h), ALLDAY-V1-UNMAPPED-DRIFT, SERIAL-FMV-MULT-CRON (weekly by design), evm-transfers Base-429, conflation oscillation — all carried/benign.

## Health line
security 0/0/0/0 · detect_stalled **1 (allday-fmv-populate — benign no-op, logged; AllDay FMV fresh via fmv-recalc)** · pgcron [] · alerts 1 medium (benign stall) + 1 INFO (ufc_sales) · pipeline fails 6h **0** (24h: offers-sweep x1 / topshot-badge-catalog x1, both >6h recovered) · trust 8/9 (1 owned BREACH unmapped 441) · FMV TS H+M **4579** / AllDay **901** · fmv_sanity 0 · editions FLAT 17471/6191/581/518 · ts-uuid-48h **34** inert · cross-collection mats fresh (cohort 04:10Z/152 · overlap 04:25Z) · new-collectors MVs fresh (cohorts 48 / summary 22:48Z) · rookie-board view 431 · DB **6246 MB** (+28/6h backfill, benign) · Vercel prod `b0a6554` READY, 0 ERROR (CANCELED = docs-only/rapid-superseded) · Sentry 0 unresolved/24h · 12 active artifacts validated (rpc-live-health components + new-collectors + rookie-board + cross-collection mats), none broken.
