# Daytime monitor candidates — 2026-06-22T03:16Z (5th/final evening tick, 20:05 PDT)

Run: rpc-daytime-monitor, last tick before tonight's ~01:03 PDT night pass. Lock RELEASED (08:18Z night pass, stale). 1a (trust-health + cross-collection refresh) **SKIPPED** — later-tick-only; ran a cheap trust-health glance anyway (9/9 ok). Platform **GREEN**, very clean — **0 pipeline fails in the last 6h**, 0 new Sentry in 18h, security 0/0/0/0, prod = the `next` 16.2.9 security bump READY.

**0 genuinely-new task candidates.** The substantive content is one carried-item status change (the special-serials MV refresh re-homed to pg_cron) + post-ship watch on today's `next` bump + 2 more pack-sniper rounds (all PASS).

---

## CARRIED-ITEM UPDATE (the one thing for the night pass to act on) — REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT is now RE-HOMED to pg_cron → verify first tick 04:13Z, then CLOSE

**Status change:** the long-carried item (night-count 3, framed as "operator must re-enable the disabled cron-job.org HTTP entry") is superseded by today's Cowork migration `audit_20260622_pgcron_refresh_special_serial_owners_mv` + the CC self-log fix (`audit_20260622_refresh_special_serial_owners_mv_self_log` + thinned route).

- pg_cron job `rpc-refresh-special-serial-owners-mv` is **active**, schedule `13 4,16 * * *` UTC, runs `refresh_topshot_special_serial_owners_mv()` in-DB (no ~120s API-gateway cap → the ~125s `REFRESH ... CONCURRENTLY` can't log the false ok=false anymore). The SECDEF fn self-logs its own authoritative `refresh-special-serial-owners-mv` `pipeline_runs` row.
- The route was thinned to a no-log trigger, so its only `pipeline_runs` rows are the **3 stale pre-fix failures** (last 06-21 09:13Z "upstream request timeout"; 06-20 02:16Z / 00:32Z "statement timeout"). Do NOT read those as live — they predate the fix.
- **First pg_cron tick is ~04:13 UTC June 22 (~1h after this tick, ~4h before the night pass).** `cron.job_run_details` for the job is null over the last 20h = it was registered after 16:13Z 06-21, so 04:13Z is genuinely its first run.
- MV is fresh (6,778 rows; board not broken). **NOT manually refreshed this run** (let the first pg_cron tick prove it — don't mask the signal).

**Night-pass action:** verify the 04:13Z tick — `cron.job_run_details` status='succeeded' for `rpc-refresh-special-serial-owners-mv` AND a fresh `refresh-special-serial-owners-mv` `pipeline_runs` row ok=true — then **CLOSE the item**. **Do NOT re-recommend "operator re-enable the cron-job.org entry" — that path is moot/superseded.**

---

## SATISFIED last-run candidate — SNAPSHOT-PACK-ASKS-WATCHLIST (00:13Z Candidate 1) — DONE

`snapshot-pack-asks` is now **watchlisted=true** (45 ok / 0 fail / 24h, last 03:13Z, ~5-min cadence). The watchlist gap the 00:13Z tick flagged is closed (added between ticks). Also: the brand-new `pack-ask-hourly-low-roll` cron (today's `bc3b01c` rolling-24h-low ship) is **already watchlisted** + 9 ok / 0 fail — no new watchlist gap in the pack family. Night pass: no watchlist row to ship here; drop SNAPSHOT-PACK-ASKS-WATCHLIST.

## CARRIED (night pass drains from the 00:13Z inbox — NOT re-logging) — PACK-SNIPER-INSIGHTS-QA
The 00:13Z Candidate 2 (read-only rpc-insights-qa on the new public `/insights/pack-sniper`) is still queued in that file. Data half re-confirmed healthy this tick (pack_ask_state 2,882; cross_collection_deals_board 683; snapshot + roll crons 0-fail). Night pass: run the QA, log any sitemap/OG/canonical gap for CC.

---

## POST-SHIP WATCH (this tick) — all PASS, 0 regressions

- **`next` 16.1.6 -> 16.2.9 security bump (`d54f66c8`, current prod READY).** Clears the App-Router middleware/proxy-bypass CVE (GHSA-26hh-7cqf-hhc6, directly relevant to `proxy.ts`). Verified: deploy **READY**, **0 new Sentry in 18h**, security directly **0/0/0/0** (RLS-off base none, anon-write 0, invariants [], secdef []) — the proxy-bypass class opened no anon-write hole; all backing objects return rows (no dep regression). **Residual (night-pass / browser):** anon-exercise the `proxy.ts` login-redirect + allow-list gate (can't be done from this read-only monitor). The 4 remaining HIGH advisories (onflow->viem->ws) stay monitor-only — their only fix path is a build-breaking `@onflow/*` bump.
- **2 more pack-sniper rounds today** — `bc3b01c` (mobile deal cards + rolling 24h-low signal; new Cowork `pack_ask_hourly_low` + `pack-ask-hourly-low-roll` cron + `pack_ask_state.low_ask_24h/7d`) and `f095f60` (hover-to-enlarge pack art). Both **READY**; roll cron 9/0 + watchlisted; pack_ask_state 2,882; no attributable Sentry. Additive UI + read-only signal.
- **Writer-leak fix (`6b9e89a`) HOLDING.** trust `ts_uuid_dupes_created_24h` = **2/200**, sentinel hyphen-48h **24/250** (22 of the 24 are 24-48h-old pre-fix rows; only 2 new in 24h vs the historical ~30/2h). Inert + suppressed.
- **TS mis-attribution closeout HOLDING.** conflation guard **17** (converged, stable), thin-FMV flag **96** flagged / 10 deals caveated, FMV reconciles, security clean.

## Carried unchanged (NOT re-logging — already queued)
- **refresh-conflated-editions cron** (operator wire daily cron; safe-DELETE fix live, guard 17). Note: the thin-FMV + conflation guard tables now ALSO refresh via the active pg_cron `rpc-refresh-thin-fmv-guard` job — both current (conflation 17 / thin 96).
- **NEXTJS-1C** (Sentry) — the RLS-leg smoke transient CC cause-fixed 16:13Z; **quiet 11h** (5 events, last ~16:15Z, 0 new). Security directly verified clean this tick, so it's stale-cause — operator/night-pass mark resolved (regression-arm).
- Standing backlog (unchanged): BUYERBF-PERINVOCATION-WORK, ALLDAY-V1-UNMAPPED-DRIFT (unmapped backlog 22/100, trust-green), UFC-EDITIONS-SEED-GAP, TS-WMC-UUID-FOSSILS (6,428 inert/suppressed), N1 snapshot-institutional-wallets, BADGE-CATALOG-CRONJOB-DUP, VERCEL cost family, A1-WORKER-PASSTHROUGH-CLEANUP, get_user_top_owned_moments 3-arg orphan, PIN-FMV-REKEY-WAVES 2/3, PIN-SYNC-CRON, P3-BUYERS, DUPE1, Q2/Q5/Q6, ANALYTICS-SMOKE-RESIDUAL, IPFS x2. **STEER honored:** SERIAL-FMV-MULT-CRON BY DESIGN (weekly pg_cron, Sun 11:00 UTC); evm-429 benign.

## Artifact validation
16 artifacts enumerated, **0 RETIRED**, none broken/repaired. HTML on OneDrive (outside this run's mount — MONITOR-ARTIFACT-ACCESS), validated via backing-object row-probe: serial_premiums 180, perfect_mint 13, trophies 685, top_sales 974, tracked_fmv 23 (historically-fragile view OK), underpriced 11, allday_floor 3,928, special_serial_mv 6,778, cross_deals 683, pack_ask_state 2,882, squeeze 500+ — all sane, no schema break from today's `next` bump / pg_cron-MV / thin-FMV / pack_ask low_ask migrations.

## Health one-liner
GREEN · security 0/0/0/0 · trust 9/9 (ts_uuid_dupes 2/200, unmapped 22/100) · detect_stalled [] / alerts [] · **pipeline 6h: 0 fails** · sentinel hyphen-48h 24/250 · FMV TS H+M **4317** (1206+3111) / AllDay **889** (250+639), both improving vs 4313/884 at 00:13Z · editions TS 17,318 (15,543 + 1,775 ::) / AllDay 6,191 / Golazos 581 / UFC 446 · conflation 17 (converged) · thin-FMV 96/10 · DB 5,076 MB · Vercel **0 ERROR**, prod `d54f66c8` (next 16.2.9 bump) READY · Sentry **0 new/18h**, 1 unresolved (NEXTJS-1C cause-fixed/quiet 11h) · pack family 0-fail (snapshot-pack-asks + pack-ask-hourly-low-roll both watchlisted) · 16 artifacts (0 RETIRED), all boards return rows.

## Benign note (not a finding)
FMV latest-per-edition rows sum to TS 17,292 vs 17,318 editions (gap 26) / AllDay 6,190 vs 6,191 (gap 1) — the freshly-cataloged Stage B `::` parallels awaiting their first FMV snapshot (gap was 10 at 00:13Z; moves with cataloging, repriced on the next canonical sweep). Not a break.
