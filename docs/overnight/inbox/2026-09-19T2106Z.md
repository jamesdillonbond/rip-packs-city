# RPC daytime monitor — candidate filing (CORRECTED)
**Run:** 2026-09-19 ~14:0x→15:5x PT (21:06→22:5xZ) · daytime health monitor (read-only) · not in a spell (io_wait 0 / active 0 / 41 conns at 21:06Z) · security 4/4 clean

> ⚠ **Self-correction:** an earlier draft of this file blamed jobid 506 for the `trust_precompute_max_age_hours` breach. That was a mis-attribution — the metric reads `rpc_trust_health_precompute`, which 506 does NOT write. Corrected below via the view definition. jobid 506 is a real but SEPARATE, smaller issue (Candidate 2).

## Candidate 1 — `trust_precompute_max_age_hours` BREACH (20.3h→~22h vs 13h): the `impossible-parallel` trust-precompute leg (jobid 324) is failing its scheduled runs; R109 IO-bound-scan class, NOT a budget shortfall
- **Source:** `rpc_ops_snapshot()` trust_health (`trust_precompute_max_age_hours` 20.31, breach_at 13, **BREACH**). Traced: `v_rpc_trust_health` computes this as `now() − min(computed_at)` over `rpc_trust_health_precompute`. The single stale row is metric **`topshot_impossible_parallel_serials`**, last `computed_at` **00:48Z** (~22h); every other leg refreshed within the last few hours. Writer = **jobid 324 `rpc-thp-leg-impossible-parallel`** (runs as `cron_heavy`, schedule `48 0,6,12,18` UTC, `run_thp_leg_logged('rpc_thp_leg_impossible_parallel()')`).
- **Observed (jobid 324 run history):** last SUCCESS 00:48Z (51.5s). Since then: **06:48Z FAILED 600.2s (statement timeout on `SELECT count…`), 12:48Z FAILED 600.5s (same), 18:48Z FAILED 13.1s ("job startup timeout" — pg_cron worker-slot contention).** Pattern is intermittent for days (09-18 00:48 & 06:48 also 600s timeouts, then 13:00/18:48 succeeded at 36–40s). So the leg's serial-integrity scan runs ~35–51s on a quiet box but **balloons past even its 600s ceiling under daytime IO contention** — the same IO-bound heap-scan class as R109 (cohort step1), on the SMALL tier's ~22 MB/s budget.
- **Risk read:** LOW. This is an INSTRUMENT-freshness breach ("watches the watcher"), not user-facing data corruption — the live `topshot_impossible_parallel_serials` value is 0/ok in the same snapshot. The leg SELF-HEALS whenever a tick lands in a quiet window (00:48Z did). Not a live spell right now.
- **⛔ NOT the lever:** raising the budget. The job ALREADY runs as cron_heavy (600s) and still times out — a bigger number won't help an IO-bound scan that has grown past 600s.
- **Suggested action (supervised; RE-MEASURE warm-vs-warm first; PUSH-needed — a function-body change):** make the `SELECT count(…)` serial check cheaper — bound/scope it (partition predicate) or add a supporting index so it doesn't full-scan the TS serials table; measure BUFFERS (not timings) before/after per the R109 methodology. This is the R109 neighbourhood (IO-bound scans on the SMALL tier) and should be batched with that work, supervised. ⛔ Do NOT throttle an ingest lane for it.

## Candidate 2 — jobid 506 `rpc-refresh-fmv-confidence-precompute` failing at the 120s DB-default ceiling (SEPARATE from Candidate 1)
- **Source / observed:** cron jobid 506 (runs as **postgres**, `refresh_fmv_confidence_precompute()` → writes `public.fmv_confidence_precompute`). postgres has no role statement_timeout and the function sets none, so its effective ceiling is the **database default `statement_timeout=2min`**. Runs ~8s quiet but clips 120s under load: **09:35Z FAILED 120.4s, 13:35Z FAILED 124.1s** today (05:35Z ok at 118.4s — at the edge); 09-18 01:35 & 05:35 also failed.
- **Risk read:** LOW, and it does NOT drive Candidate 1's metric — confirm a live CONSUMER of `fmv_confidence_precompute` before prioritizing (no trust arm watches it).
- **Suggested action (supervised; a no-push session CAN'T ship it cleanly — migration-parity reds until the file is committed):** unlike 324, this one genuinely just needs >120s and completes in ~128s, so the documented cron_heavy lever fits: in ONE migration, `GRANT EXECUTE ON FUNCTION public.refresh_fmv_confidence_precompute() TO cron_heavy` (verified: cron_heavy currently CANNOT execute it → would orphan silently otherwise), then `cron.unschedule` the postgres-owned job and re-register under cron_heavy (600s role default). Function is SECURITY DEFINER owned by postgres, so no data-object grants change. Verify next ticks read `succeeded`. Revert: re-register under postgres, REVOKE EXECUTE.

## Not-candidates — recorded so they are NOT re-raised
- **`cross_collection_cohort_mat` 69.9h stale** (ccm-step1 jobid 60 failed 3 consecutive daily ticks on statement timeout): **R109, OPEN & tracked** — step1 does ~40% heap fetches and can't complete in 600s even on an idle box; real fix (cheaper `_ccm_step1_next` build / supporting index) recorded and deferred pending a cost measurement.
- **`public_board_slow_count` = 2 (breach_at 1):** oscillates 0→9 over 24h (trust-health history) — load-correlated symptom of the SMALL-tier IO-contention class, not a fresh regression.
- **failure_rate cluster** (backfill-pack-rip-metadata 49%, fmv-backfill 44%, lock-check-batch 38%, price-snapshots 27%, run-insider-detectors 35%): all statement timeout, pooled over 2 days spanning the 2026-09-18 platform read-availability outage. Saturation collateral.
- **topshot-active-listings-ingest** silent (1193 min): **#125** — Cloudflare challenges Atlas from both the GHA runner and the residential IP; every caller already firing; nothing to restart.
- **match-topshot-players / allday-buyer-backfill** running-but-not-succeeding: dead-host (`public-api.nbatopshot.com`) null-name residue per #124; no cadence lever.

**Common thread (Candidates 1 & 2 + R109 + the failure cluster):** all are the same SMALL-tier IO-bound heavy-scan class under daytime contention — the platform-level lever is cheaper scans / precompute, not bigger timeouts. None is a correctness bug; all self-heal in quiet windows.

**Deploys:** production tip READY (sha 2dd592bd); no ERROR in last 8 (CANCELED = superseded rapid pushes — Trevor actively shipping). **Sentry:** no new issues 24h (SDK out of tree since 08-18 per #34 — expected; live detector is the client-error beacon). **Artifacts:** core health data path validated via `rpc_ops_snapshot`; per-artifact payload queries not each re-run (artifact HTML is outside the mounted folders).
