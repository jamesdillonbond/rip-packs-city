# RPC daytime monitor — candidate filing
**Run:** 2026-09-19 ~14:06 PT (21:06Z) · daytime health monitor (read-only) · not in a spell (io_wait 0 / active 0 / 41 conns) · security 4/4 clean

## Candidate 1 — `trust_precompute_max_age_hours` BREACH (20.3h vs 13h): the confidence/trust precompute WRITER (jobid 506) is tipping over its 120s ceiling on daytime ticks
- **Source:** `rpc_ops_snapshot()` trust_health (`trust_precompute_max_age_hours` = 20.31, breach_at 13, **BREACH**) + `cron.job_run_details` jobid **506** `rpc-refresh-fmv-confidence-precompute` (schedule `35 1,5,9,13` UTC).
- **Observed (24h run history, jobid 506):** ~8s when the box is quiet, but hits the 120s `statement_timeout` under daytime IO load. Today: 01:35Z ok (8.3s), 05:35Z ok (**118.4s — right at the edge**), **09:35Z FAILED (120.4s), 13:35Z FAILED (124.1s)**. 09-18 01:35Z & 05:35Z also FAILED. So the precompute misses ~half its daytime ticks and the trust surface serves ~20h-old confidence numbers by afternoon.
- **Risk read:** LOW→MEDIUM. Freshness/accuracy of the trust surface, not a correctness bug. Same statement-timeout-under-IO class as R109; NOT a live spell (io_wait 0 at 21:06Z, so the ceiling hits are during scheduled-window contention, not a general outage). NOT auto-shippable — a query/budget change on a precompute writer; supervised only.
- **Suggested action (supervised; RE-MEASURE the aggregate warm-vs-warm in a quiet window BEFORE acting):** the 2026-09-14 steer already named the fix for the trust-latency sibling — either (a) raise jobid 506's effective budget (verify the run-as role's `statement_timeout`; it dies at ~120s, so a role with a higher default — e.g. `cron_heavy` 600s if it owns the read objects — would let the 118s edge case finish), or (b) make the precompute aggregate (`SELECT d.conf::text, count(*) …`) cheaper (watermark/incremental or a supporting index, cost-measured first). ⛔ Do NOT throttle an ingest lane on account of it.

## Not-candidates — recorded so they are NOT re-raised
- **`cross_collection_cohort_mat` 69.9h stale** (ccm-step1 jobid 60 failed 3 consecutive daily ticks on `statement timeout`): this is **R109, OPEN and heavily tracked** — the step1 temp-table scan does ~40% heap fetches and cannot complete in 600s even on an idle box; the real fix (cheaper `_ccm_step1_next` build / supporting index on the DISTINCT ON keys) is recorded and deliberately deferred pending a cost measurement. No new action.
- **`public_board_slow_count` = 2 (breach_at 1):** oscillates 0→9 over the last 24h (trust-health history) — a load-correlated symptom of the known SMALL-tier IO-contention class, not a fresh regression.
- **failure_rate cluster** (backfill-pack-rip-metadata 49%, fmv-backfill 44%, lock-check-batch 38%, price-snapshots 27%, run-insider-detectors 35%): all `statement timeout`, pooled over the last 2 days which SPANS the 2026-09-18 platform read-availability outage. Saturation collateral, not N distinct bugs.
- **topshot-active-listings-ingest** silent (1193 min): **#125** — Cloudflare now challenges Atlas from both the GHA runner and the residential IP; every caller is already firing; nothing to restart.
- **match-topshot-players / allday-buyer-backfill** running-but-not-succeeding: dead-host (`public-api.nbatopshot.com`) null-name residue per #124; unresolvable, no cadence lever.

**Deploys:** production tip READY (sha 2dd592bd); no ERROR in last 8 (CANCELED entries are superseded rapid pushes — Trevor actively shipping). **Sentry:** no new issues 24h (SDK out of tree since 08-18 per #34 — expected; live detector is the client-error beacon). **Artifacts:** core health data path validated via `rpc_ops_snapshot`; per-artifact payload queries not each re-run this tick (artifact HTML is outside the mounted folders).
