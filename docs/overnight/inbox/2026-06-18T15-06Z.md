# Daytime monitor — 2026-06-18T15:06Z (08:06 PDT, morning tick)

**PLATFORM GREEN; 0 NEW candidates (status record).** The 14:11Z **BUYERBF-DURATION-CREEP HIGH is DOWNGRADED** — the feared first invisible >600s death did NOT happen: `topshot-buyer-backfill` fired 14:44Z and completed **ok at 532s** (under the 600s cap), `detect_stalled` is back to `[]`, cursor advancing. Else clean: security 0/0/0/0, trust 9/9, cross-collection refresh OK (both pg_cron steps @04:10/04:25Z), sentinel 0, Sentry **0 unresolved** (the 2 stale transients aged out), Vercel 0 ERROR (prod `ac50ae1b`), 14 active artifacts' backing data validated. Daily extras run (the 07:11Z tick beat the 8am slot; re-confirmed: trust 9/9, ccm fresh).

## Health (every-run sweep)
- `detect_stalled_pipelines()` = **[]** (the 14:11Z buyerbf flag cleared when its 14:44Z run logged). `get_pipeline_alerts()` = 1 **info** only (`golazos_sales` resolving_editions — longstanding benign, 1 sale/24h).
- `pipeline_runs`: 24h **13 fails**; 6h **3238 / 7 fails** — `evm-transfers-ingest` ×4 (Base 429, known LOW) + a brief **13:15–13:29Z statement-timeout micro-contention** (`alerts-dispatch` deal-leg 13:29Z, `check-alerts` 13:15Z, `wmc-fmv-populate` 13:28Z — 3 pipelines/14min, all recovered next tick). Transient; folded under ALERTS-DISPATCH-DEAL-TIMEOUT.
- Security **0/0/0/0**: RLS-off base `[]`; anon/auth-write-on-RLS-off `[]` (relkind r/p); `check_public_security_invariants()` null (0 rows); `check_secdef_anon_execute_violations()` `[]`. (Today's deal-board/dispatch CREATE OR REPLACEs preserved grants — no anon hole.)
- Sentinel TS-UUID-keyed-editions-48h = **0**.
- Sentry: **0 unresolved** (sort=new + sort=date both empty; the 2 single-event smoke transients from 06-17 resolved/aged out). 0 new.
- Vercel: prod **`ac50ae1b` READY** (floor-serial cron maxDuration fix); **0 ERROR** across 20 (CANCELED = superseded docs/monitor builds). DB **4808 MB** (+18 vs 03:05Z, benign creep).
- Drift vs 06-17 baseline: editions flat (TS 15543 / AllDay 6191 / Golazos 581 / UFC 446); fmv_sanity 0; trust 9/9.
- FMV (`fmv_current` latest-per-edition): TS HIGH+MED **3140** (HIGH 855 / MED 2285; ASK_ONLY 2620 stable; NO_DATA 3544, down from 3697 baseline). AllDay HIGH+MED **822** (HIGH 231 / MED 591).

## Recent-ship health (06-18 floor-serial cron wave + alerts — all READY, no regression)
- **`de7f610` → `df513a7` → `ac50ae1b` floor-serial cron (`topshot-deal-floor-serials`) — PASS.** Latest run 14:37Z **ok at 105s** (under the 300s cap), `budget_hit:false`, listings_found 155/603 deal editions — the ac50ae1b cap+incremental-flush fix resolved the invisible-maxDuration-kill risk that motivated it. Residual: `gql_errors`/`throttled_giveups` **95/603** (TS-GQL proxy 429 after backoff) — partial coverage that rotates by design (MAX_PER_RUN cap); Trevor's active in-flight tuning, not a finding.
- **`a6c3afd`/`2ac4bcf`/`11c6c1a`/`0933067` parallel-type alert wave + 2 migrations — PASS.** Deal-board views return 627 rows; security 0/0/0/0 (CREATE OR REPLACE preserved grants).
- **Alerts loop healthy:** alerts-dispatch 10–16s/run (all <45s statement_timeout since 14:14Z), alerts-send 0 sent/0 failed (0 subscriptions). enqueued_deal/fmv 0.

## Artifacts (14 active enumerated; RETIRED tombstones not in manifest)
Backing data validated green for the surfaces today's ships touched + key boards: serial_premiums **274** / perfect_mint **9** / underpriced **15** (listings churn from 19, healthy non-zero) / tracked_fmv **21** (v3 view fast) / cross_collection_deals_board **627** / offer_sanity **219** / squeeze **1000+** / cross_collection_cohort_mat **158** fresh @04:10Z / FMV-by-collection resolves. None broken; none repaired (read-only).

## NEW candidates
None.

## Status updates (already-queued; re-measured)
- **BUYERBF-DURATION-CREEP (queued 14:11Z as NEW HIGH) — DOWNGRADE to watch.** The 14:11Z "198m silent / possible first invisible >600s death" was a late-but-healthy run: `topshot-buyer-backfill` fired 14:44Z **ok at 532s**, cursor_sold_at advanced (2023 backward-walk drain), `detect_stalled` cleared. Last 9 runs all ok, 505–577s, **0 >600s, 0 fails**. The duration creep toward the 600s ceiling stays the real watch (577s peak @10:54Z on denser 2023 history); lever remains batch 200→150 / maxDuration bump, NOT threshold-widen (the closed BUYERBF-CRON-DROP class). Not an active breakage.
- **SERIAL-FMV-MULT-CRON (queued) — escalating.** `serial_fmv_multipliers` last computed 2026-06-15 23:06Z = **64.1h stale** (37 cells; 63.2h @14:11Z). Backs serial-premiums (274) + underpriced board (15) via `serial_fmv_estimate`. Slow-moving premiums (low per-day risk) but compounding deal-board "discount" staleness. Operator/Trevor: wire the `refresh-serial-fmv-multipliers` cron (cron-wiring, not auto-shippable).
- **FMV-HIGHMED-DIP-WATCH (queued) — recovered, closeable.** TS HIGH+MED 3140 in the normal daytime band (low 2848 / peak ~3435); ASK_ONLY 2620 stable (not over-claiming), NO_DATA improving 3697→3544, fmv_sanity 0 — confirmed benign daily-cycle reclassification. Night pass can close.
- **ALERTS-DISPATCH-DEAL-TIMEOUT (queued) — acute mitigated, structural cost remains.** The 13:29Z deal-leg timeout was inside the transient 13:15–13:29Z contention window; all dispatch runs 14:14Z→ are 10–16s (under the 45s materialize-migration cap). Cost grows with subscribers (still 0 subs) — the deal-query optimization stays the right pre-emptive lever; likely closeable if the materialize fix holds another day.

## Noted, not findings
- floor-serial cron 429 throttled_giveups 95/603 — designed partial-coverage rotation; Trevor actively tuning (3 ships today).
- 13:15–13:29Z statement-timeout micro-contention (3 pipelines/14min) — transient, recovered; wave-pacing otherwise holding.
- evm-transfers-ingest Base-429 (×4/6h) — known LOW external rate-limit.
