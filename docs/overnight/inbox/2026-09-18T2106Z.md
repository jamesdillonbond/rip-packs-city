# Daytime monitor candidates — 2026-09-18T21:06Z (~2:06 PM PT)

Context: this tick ran ~2h after the 09-18 external-read outage recovered (~19:01Z / 12:01 PM PT, per ledger + focus). DB is reachable and QUIET — positive control `pg_stat_activity` io_wait 3 / active 4 / total 25, and `rpc_ops_snapshot()` completed (no timeout). NOT a spell. Security invariants all clean, FMV split at documented baseline (TS 51.8% / AD 26.7% HM), editions stable, sentinel_ts_uuid 48h = 0, latest prod deploy READY (e613a9b). Most pipeline silences and pg_cron statement-timeouts are outage-shadow or the already-documented timeout class (R100 price-snapshots, atlas-403 pooled fails, candy-editions-ingest, ccm step1/step2, backfill-pack-rip, fmv-backfill) — NOT re-logged.

## Candidate 1 — trust-health BREACH: candy_offers_unverified_pct = 100 (breach_at 25)
- Source: rpc_ops_snapshot() trust_health_breaches (only breach this tick).
- Risk read: LOW–MEDIUM. `candy_offer_spread_board` is a PUBLIC surface (app/insights/candy-mlb). 100% unverified means the Spread tab could quote standing bids the deactivation/verification arm has not re-checked.
- Probable cause (NOT asserted): outage-shadow. candy-offers-indexer (Vercel cron 50 */6, the sweep that verifies/expires Candy offers) last logged 06:50:42Z; it missed its 12:50Z and 18:50Z ticks, both inside the 12:48–19:01Z outage window. With no verification sweep since 06:50Z, unverified_pct climbs to 100.
- Suggested action (RE-MEASURE, do not conclude): after candy-offers-indexer's next post-recovery tick (~00:50Z), re-read the metric. If it drops back under 25 → self-healed, close. If it STAYS at 100 after a clean offers-indexer run, then it is a real verification/deactivation-arm defect worth a fix — investigate the arm, not the indexer cadence.

## Candidate 2 (info) — post-outage cadence resumption watch; do NOT re-diagnose as a fault
- Source: detect_stalled_pipelines() / rpc_ops_snapshot pipeline_alerts.
- The candy + studio 3-hourly family and candy-sales-indexer (severity HIGH, sole Candy price signal) all carry last_run timestamps that PREDATE the outage onset (candy-sales-indexer 09:20:13Z; studio drains ~09:5xZ; candy-listings 09:35Z). Their post-recovery ticks are only now becoming due (candy-sales next 21:20Z).
- Risk read: NONE to observe. These silences are the outage's shadow, not a new pipeline fault — last runs are pre-onset and no post-recovery tick has come due yet.
- Suggested action: on the next tick, confirm each resumed on its own schedule. Only escalate if a lane misses its FIRST fully-post-recovery tick (i.e. a scheduled fire wholly after 19:01Z that produces no row). Do not re-tune, throttle, or re-diagnose — the outage cause is settled and platform-side (see focus.md / ledger).
