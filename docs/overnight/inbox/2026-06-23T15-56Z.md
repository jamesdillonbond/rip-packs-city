# Monitor tick — 2026-06-23T15:56Z (first tick of day)

**Verdict: ✓ GREEN — 0 new candidates.** Clean read-only sweep + first-tick daily extras + 14/14 active artifacts validated. Sandbox clock accurate this run (shell ≈ DB now(), no skew). Nothing shipped — sensing only.

## Health sweep
- **Pipelines (6h):** 3 fails, all transient/known — offers-sweep ×2 (TS GraphQL 429 rate-limit), compute-topshot-pack-ev ×1 @13:31Z (the known benign contention micro-cluster the 06-23 night pass already noted). No logic failures.
- **detect_stalled_pipelines():** `[]`.
- **check_pgcron_recent_failures():** 1 = `rpc-refresh-thin-fmv-guard` @13:30Z transient timeout. **KNOWN / NOT a new finding** — the night pass manually refreshed it this morning (healthy 3.9s, 104 flagged) and queued it as THIN-FMV-GUARD-CONTENTION (watch 06-24 13:30Z). The fn reads `cron.job_run_details`, so it stays "failed" until tomorrow's tick. Not re-logged.
- **Security:** 0/0/0/0 — RLS-off base `[]`, anon/auth-write-on-RLS-off `[]`, check_public_security_invariants `[]`, check_secdef_anon_execute_violations `[]`.
- **Sentinel TS-UUID-leak 48h:** 2 (< 250 ok).
- **Editions:** FLAT — TS 17318 / AllDay 6191 / Golazos 581 / UFC 518. No writer leak (ts_uuid_dupes_24h 0/200).
- **FMV:** TS HIGH+MED **4372** (4366→4372 improving) · AllDay **905** (flat) · Pinnacle NO_DATA 0. Reconciles to editions (TS gap 2 / AllDay gap 1 = fresh `::` parallels; UFC gap 72 = newly-seeded editions awaiting first snapshot, benign). fmv_sanity_flags 0. Writers fresh (FMV write 15:54Z, Pack EV 15:49Z, offers-sweep 15:47Z).
- **Misc drift:** DB 5173 MB (+6 benign) · conflation_guard 34 · thin_fmv_editions 104 · unmapped backlog 24/100.
- **Vercel:** 0 ERROR across 20 recent; prod 9056eff8 READY (newer commits are docs/monitor-only, CANCELED by ignore-build-step).
- **Sentry:** 1 unresolved = NEXTJS-1Q (router-state-header parse, 1 event/1 user, 19h quiet) — known benign deploy-swap RSC transient. No spike, no new issues.

## First-tick daily extras
- **Trust health:** 9/9 ok (no breach). ts_uuid_dupes_created_24h 0/200, unmapped_resolution_backlog 24/100, pinnacle_ask 0.2h/3, pinnacle_fmv 5.7h/30, pack_ev_board 1.5d/2 — all green.
- **Cross-collection pg_cron refresh:** cohort_mat fresh 04:10Z (158 rows), ts_set_overlap_mat fresh 04:25Z. Both `rpc-ccm-step1`/`step2` active + today's runs `succeeded`.

## Artifact validation
- 16 enumerated; 2 intentional tombstones skipped (pack-drops-ev-check, rpc-ts-data-mission). 14 active all return sane data.
- **rpc-live-health** (merged dashboard) payload runs clean; **CAND-1 leak-panel repair confirmed live** — `leak_48h` reads 2 (matches sentinel) via the corrected `^[0-9]+:[0-9]+(::[0-9]+)?$` predicate.
- Backing-object spot checks healthy: rewards_economy 1 · offer_ask_spread 5566 · Trevor wmc 18705 · candy-solana collection 1 · serial_premiums 182 · insights boards (squeeze/deals/trophies 501, rookies 61, pack_reality 7) all return rows.

## Recent-ship health
No new code/DB ship since the 06-23 night pass (only docs/monitor commits, no prod change). The soaked 9056eff (AllDay+Pinnacle ASK_ONLY parity: AllDay ASK_ONLY 663, Pinnacle NO_DATA 0) + 35fc464 (audit fixes) continue to hold — no regression.

## Candidates
None. Everything observed is GREEN, or already queued (THIN-FMV-GUARD-CONTENTION), benign-transient (offers-429, pack-ev contention), or known (NEXTJS-1Q).
