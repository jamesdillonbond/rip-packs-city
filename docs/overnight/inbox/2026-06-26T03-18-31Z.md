# Daytime monitor inbox — 2026-06-26T03:16Z (evening tick, ~20:06 PDT)

Run: rpc-daytime-monitor. Lock RELEASED (overnight pass closed 14:10Z). Push available, clone-flow clean.
Platform GREEN except ONE finding below (an OPERATOR item — cron-job.org trigger surface degraded). No DB/code/security regression; nothing the night pass can ship (the only lever is operator re-firing external cron entries).

---

## CANDIDATE 1 — PACK-EVENTS-CRONJOB-STALL (HIGH · operator · EXTENDS/SHARPENS ledger item 1a of the 2026-06-26 CC entry — worsened + now blocking a fix verification)

- **Title:** `pack-events-ingest` cron-job.org trigger has gone fully silent — pack purchase/open/AllDay cursors frozen 4.5–8.3h; blocks verification of CC's just-deployed worker budget fix.
- **Source:** `pipeline_runs` (pack-events-ingest last run 2026-06-25 22:24:16Z, ~4h50m silent and climbing); `event_cursor` (topshot_pack_opens + allday_pack_purchases frozen 8h18m at block 156004422, topshot_pack_purchases frozen 4h50m at 156009044, allday_pack_purchases_backfill frozen 8h11m); `get_pipeline_alerts()` = **3× HIGH `cursor_stalled`** (topshot_pack_opens, allday_pack_purchases, allday_pack_purchases_backfill); Vercel prod `52b2647c` (the worker-fix commit) READY, deployed ~03:07Z.
- **Why this is sharper than ledger item 1a (which said "cadence degraded to gaps of 75/120/220 min"):** it has WORSENED past any prior gap — fully silent ~4h50m (purchases cursor) / 8h18m (opens+allday cursors), monotonic decline = the external trigger looks dead, not merely slow. AND it now **gates the post-ship verification of CC's worker budget fix** (commit `52b2647c`, CF worker version `fdb09b59`): that fix deployed ~03:07Z but the 22:24Z run (the last one) still shows opens + allday_forward at `chunks_processed:0` because it predates the deploy — and NO post-fix tick has fired since, so the staged-budget fix is unverified AND pack ingestion stays frozen until the trigger fires again.
- **Risk read:** NOT a user-facing outage — reads/FMV unaffected; cursors are ~11k (purchases) / ~16k (opens+allday) blocks behind a sealed tip of 156020175. But pack-purchase + pack-open (rip→moment delivery) + AllDay-pack ingestion freshness is degrading the longer it stays down (wallet pack summaries, pack-rip metadata, pack lifecycle, pack-EV inputs go stale). Read-only monitor can't fix; night pass can't fix (external cron-job.org, operator-only).
- **Suggested action (OPERATOR):** re-fire / confirm-enabled the `pack-events-ingest` cron-job.org entry. The first post-fire tick both (a) verifies CC's staged-budget fix (opens + allday_forward should report `chunks>0` / cursors advance off 156004422) and (b) starts draining the frozen window. If it fires and the legs STILL show chunks:0, escalate to CC (the budget fix isn't working). Watch `get_pipeline_alerts()` to clear the 3 HIGH cursor_stalled.

## CANDIDATE 2 — CRONJOB-ORG-TRIGGER-SURFACE-DEGRADED (MED · operator · connects/absorbs the carried LISTCACHE-CRON-DROP)

- **Title:** Two cron-job.org-triggered entries are degraded today while every Vercel-cron job fires on schedule — points at a cron-job.org account/platform-side problem, not two coincidences.
- **Source:** `topshot-listing-cache` (cron-job.org) fired once at 00:11Z then silent ~3h again (1 run/6h, sparse — the carried LISTCACHE-CRON-DROP, NOT resolved); `pack-events-ingest` (cron-job.org) fully silent since 22:24Z (Candidate 1). Meanwhile Vercel-cron jobs are all healthy on their own cadence: studio backfills (allday/golazos/pinnacle */3h, 2 runs/6h, last ~140m ago), `ufc-studio-sales-history-backfill` (1,21,41 — 18 runs/6h, last 15m ago), `topshot-flowty-unmapped-drain` (9,29,49 — 18 runs/6h, last 7m ago).
- **Risk read:** LOW-MED individually; the pattern (≥2 cron-job.org entries degraded simultaneously, Vercel crons fine) suggests the operator should look at the cron-job.org account holistically (billing/quota/paused-jobs/platform incident) rather than re-firing each entry one at a time. listing-cache impact is bounded (FMV fresh via fmv-recalc; only ASK_ONLY freshness risks ~few-h staleness); pack-events impact is Candidate 1.
- **Suggested action (OPERATOR):** open the cron-job.org console, confirm both `pack-events-ingest` and `topshot-listing-cache` entries are enabled + their last-execution status, and check for an account-level issue (quota/billing/paused). Per the rpc-cron-ops skill's secret-safety rule, scope console reads narrowly — do NOT broad-read the job-edit pages (Authorization headers live in the DOM).

---

## Health snapshot (GREEN apart from the above)
- security **0/0/0/0** (check_public_security_invariants [], check_secdef_anon_execute_violations [], rls_off_tables 0, anon-write-on-rls-off 0).
- `detect_stalled_pipelines()` **[]**; `check_pgcron_recent_failures()` **[]**.
- `get_pipeline_alerts()`: 3 HIGH cursor_stalled (pack cursors, Candidate 1) + 1 INFO (ufc_sales resolving_editions, benign/known).
- pipeline fails 6h = 6 (a transient statement-timeout micro-cluster 01:15–01:31Z: compute-topshot-pack-ev / pinnacle-nft-resolver ×2 / wmc-fmv-populate / check-alerts — ALL self-recovered, ok every tick since ~01:41Z) + 1 badge-catalog 429 (benign). 24h fails = 7, all transient singletons.
- trust health **8/9 ok**; lone BREACH = `unmapped_resolution_backlog_max` **1831, DOWN from 2370 at the overnight pass** = topshot-flowty-unmapped-drain draining it as designed (focus.md: do NOT re-flag while trending down; self-clears <100 in ~1-2 days). offer_edition_gap $17 ok, fmv_sanity 0, pinnacle_fmv 17.1h ok, ts_uuid_dupes_24h 34 ok.
- sentinel TS-UUID-leak 48h = **34** (inert, well under WARN 250). editions effectively FLAT (TS 17,469 = +34 inert-leak floor / AllDay 6,191 / Golazos 581 / UFC 518).
- FMV TS HIGH+MED **4,572** (improving from 4,535) / AllDay **910** (flat) / TS NO_DATA 3,310. fmv_sanity 0.
- DB **5,928 MB** (+~494/20h — elevated but benign-attributable to the documented multi-collection deep-history backfill wave; focus.md says don't flag the backfilled-sales volume; watch the growth rate).
- Sentry **1** unresolved (JAVASCRIPT-NEXTJS-J "smoke test failed: pack-listings responds", 1 event, 6h quiet, low actionability — single-event smoke transient, not spiking).
- Vercel prod **`52b2647c`** READY, 0 ERROR (CANCELED deploys = docs-only/superseded, normal).
- Artifacts: all 15 sampled backing boards/views resolve (insights estate + v_tracked_wallet_fmv_confidence 26 / cross_collection_cohort_mat 158 / special_serial_owners 501+ / serial_premiums 259); freshness current (FMV/PackEV/offers all within ~7m). No broken artifact.
