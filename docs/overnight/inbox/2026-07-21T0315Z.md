# Daytime monitor — 2026-07-21T03:15Z (~20:10 PDT Jul 20)

Fifth tick today (1520Z, 1805Z, 2105Z, 0012Z prior). Health is **GREEN and slightly
better than the 0012Z tick** — pg_cron is now fully clean (`[]`, was 1 contention entry)
and trust is 15/15 with zero breaches. A large interactive wave (~10 commits in ~3h)
landed since the last tick and its DB-visible changes all verify correct.

No skew (shell 03:06:23Z ≈ DB `now()` 03:06:41Z ≈ newest sale 03:02Z ≈ newest FMV 03:06Z).
Lock RELEASED by the night pass. Evening tick ⇒ Section 1a first-tick extras skipped.

Health: security **0/0/0/0** plus the new broader `check_secdef_anon_exec_drift()` = `[]` ·
trust **15 metrics, 0 breaches** · `stalled_pipelines []` · pg_cron `[]` · sentinel 14 ·
DB 10,368 MB · editions TS 19,478 / AD 6,190 / GZ 575 / UFC 518 / candy 125 ·
FMV TS H+M 3,281 · prod `8c14fdd2` READY, **0 ERROR-state deploys** ·
Sentry 3 unresolved, 1 event each — the same three as 0012Z, no new issue, no spike.

---

## Post-ship watch — the ~10-commit 07-20 evening wave: ALL PASS, nothing to revert

Landed since the last tick: `6813d3c5` front-door copy · `c9cf471d` search_path pins +
SECDEF drift check · `e90df921` + `8baee393` + `ef9b6d63` concierge security boundary,
`get_insight_board`, welcome copy · `bd8c4604` signup funnel + dashboard $0 fix ·
`ab68d57b` SEO canonical 308 · `26727e97` pack_table_rows revoke + drift alert arm ·
`0ff1ed1d` weekly digest route · `8c14fdd2` CI guard fix.

Verified live, first-hand:

- **Dashboard $0 fix is real.** `get_wallet_collection_stats` now reads `fmv_current` and
  no longer references `fmv_snapshots` at all — the ~19k-correlated-subquery LATERAL is
  gone. This was the fix for portfolios silently rendering $0 via a swallowed 503.
- **`pack_table_rows` anon grant is genuinely revoked** — `has_table_privilege('anon', …)`
  = false (checked functionally, not via `role_table_grants`, per the 07-19 lesson).
- **The new SECDEF drift check is live and clean**, and its `get_pipeline_alerts()` paging
  arm is correctly quiet.
- **All 12 core artifact/board-backing objects resolve and are service_role-readable**
  (`cached_listings_v2`, `pack_ev_latest`, `edition_offers`, `pinnacle_fmv_history`,
  `pack_table_rows`, `mv_pack_ev_latest`, `sets_summary`, `editions_unified`, `fmv_current`,
  `panini_coverage_summary`, `candy_best_offers`, `wmc`), and **10/10 probed insights boards
  return data**. Nothing in the wave broke a dashboard surface.
- **The weekly-digest email path is doubly inert** — verified in a fresh clone at `8c14fdd2`:
  the route exists but is **not wired to any Vercel cron** *and* is gated on
  `WEEKLY_DIGEST_ENABLED === "1"`. Recording this so a later tick doesn't re-investigate a
  live-looking Resend path. (The mounted tree is stale at `26727e97`, so a grep there
  reports the route missing entirely — do not read that as evidence about prod.)

---

## Item 1 — signup-funnel instrumentation is unverified, and right now it is *unverifiable*; the DB half is already cleared (LOW / watch, new)

**Source:** `funnel_events` 48h aggregate · `pg_constraint` on `funnel_events` ·
deploy `dpl_A1zcDmG4LRXYckZJaZ5Jiuvvo2Qh` (`bd8c4604`, ~02:13Z, ~1h old).

`bd8c4604` wired three new funnel events — `signin_click`, `account_created`,
`email_capture_submitted`. All three currently have **zero rows**.

That number means nothing yet, and I want to be exact about why rather than let a later tick
read it as a defect: the newest `home_view` in the table is **23:50Z, before the deploy**.
There has been **no eligible traffic since the instrumentation shipped**, so zero is a
vacuous observation, not a negative one. It is not evidence the events fire, and it is not
evidence they don't.

**What I did clear, because it was the likeliest silent failure and it was cheap:** the
`funnel_events` CHECK constraint **does** whitelist all three new values. So the classic
three-way-drift failure (lib type / route allowlist / DB CHECK disagreeing, write rejected
and swallowed) is ruled out on the DB side. If these events don't appear, the cause is in
the client/route wiring, not the constraint.

**Why it's worth a line at all despite being LOW:** this is the instrumentation for the
**only gate the roadmap says counts** (50+ WAU), it went in on the same day the front door
opened to self-serve signup, and an un-firing counter on a freshly-opened funnel is exactly
the kind of thing that stays invisible for weeks. For scale: real traffic does exist —
32 distinct sessions across 44 `collection_view`s in 48h — so the funnel is not dead, it
simply hasn't been walked since the deploy.

**Suggested action:** next tick, re-query `funnel_events` for the three types **and** for
`home_view` newer than 02:13Z. If a post-deploy `home_view` exists with still-zero
`signin_click`, that becomes a real finding worth escalating (the home CTA should fire it
without completing signup). Until then, do not act and do not alarm.

**Risk read:** read-only verification, no change proposed.

---

## Item 2 — two false-positive traps recorded, one of which would have fired a real page (method, no action)

Both are near-misses from this tick. Recording them because the next tick will hit the same
surfaces.

**(a) `check_secdef_anon_exec_drift()` returns ONE row wrapping a jsonb array.** My first
pass counted rows, got `1`, and I was one step from filing a security-drift alert. The value
is `[]` — clean. This matters more than the usual version of this footgun because `26727e97`
just wired this function into `get_pipeline_alerts()` as a **high-severity arm that routes to
Telegram + email**, so a monitor reporting a phantom drift would page Trevor for nothing.
Read the value, never `count(*)` — same rule already documented for
`check_public_security_invariants()`.

**(b) The artifact folder is `Read`-able but NOT `Grep`-able this session.** The 07-19 note
says the artifact-replay gap is "UNBLOCKED — directly readable via Read/Grep with the Windows
path." That is **session-dependent**: `C:\Users\TDill\Claude\Artifacts` is outside this
session's connected folders, so `Grep` refuses it while `Read` on a full file path still
works. Practical consequence: bulk extraction of payload SQL across the estate isn't
available; per-file `Read` is. I validated the artifact estate from the DB side instead
(object resolution + board liveness above), which targets the same renamed-view breakage
class at a fraction of the cost — worth preferring anyway while the pool is contended.

Artifact estate unchanged at **15 active** (17 listed − `rpc-growth-funnel` and
`rtr-pack-finder`, both retired tombstones at 23:56Z yesterday).

---

## NOT re-raised (measured, already known — recorded so the next tick doesn't re-spend)

- **`sales-counterparty-backfill`** 36 fails / 288 runs = **12.5%**, continuing the documented
  slow improvement (18.8% 07-19 → 13.0% → 12.8% → 12.5%). `last_ok` 03:05Z is newer than
  `last_fail` 01:15Z. Connection-pool contention, self-limiting, cursor-safe.
- **DUNE-DATAPOINT-CAP-402** — unchanged. Both Dune walkers still 100% fail on HTTP 402;
  `sales_ingest_state` cursor still parked at `2025-09-03` / `updated_at` 07-20 04:15Z,
  i.e. it has not advanced since the cap engaged, exactly as the 2105Z tick described. That
  writeup already framed the real risk (unsupervised resumption on billing reset). Nothing new.
- **TOPSHOT-WMC-FOSSIL-DRAIN** — filed in full at 0012Z (unwatchlisted weekly self-heal,
  proconfig timeout inert, guarding a currently-empty backlog). No new information this tick.
- **ALLDAY-LOCK-REFRESH** `wallet_fetch` timeouts 3/22, and `wallet-username-resolver` 10.4%,
  `pinnacle-nft-resolver` 3.3%, `compute-topshot-pack-ev` 1.9%, `fmv-recalc` 2.2%,
  `wallet-backfill-ufc` 0.4% (QuickNode 429, known external), plus four singletons. Every one
  has a `last_ok` newer than its `last_fail`. One contention family, not several faults.
- **Sentry's three entity-page errors** (pack / team / player detail) — unchanged set,
  1 event each, the intended post-07-14 "throw retryable instead of silently 404" shape.
  Underlying constraint is the connection pool, already the subject of the queued
  selection-cost work.
- **Golazos `resolving_editions` info alert** — 2 unmapped in 24h, severity `info`, standing.

---

## Post-ship note — `95270af8` prewarm change: watched as the 0012Z tick asked, nothing to report

The prior tick flagged the prewarm poll as a watch target because CLAUDE.md names
`lib/allow-list/prewarm.ts` as the strongest lead for the unexplained ~48% of `wallet-backfill*`
fan-out. Per-hour volume since: 13Z 252 · 16Z 542 · 20Z 7 · 00Z 172 · 01Z 310. Hours 00/01 are
a scheduled wave (`utcHour % 12 < 2`) and 16/21 are the already-documented non-wave burst hours,
so nothing here is attributable to the change — and no signup has run through it yet anyway
(see Item 1). Carry the watch rather than closing it; it needs a real signup to say anything.
