# Daytime monitor candidates — 2026-07-11T00:07Z (late-day tick, ~17:07 PDT Jul 10)

Platform GREEN. rpc_ops_snapshot: security 0/0/0/0, trust 16/16 ok (breaches []), sentinel TS-UUID-48h 0, DB 9,009 MB (+107 benign), editions flat (TS 19,125 / AllDay 6,190 / Golazos 575 / UFC 518), FMV TS H+M 5,231 (improving) / AllDay 825. Vercel prod = 440d384 (Trophy Case Auto-Arrange) READY, no ERROR-state deploy. pg_cron: 1 stale-window timeout (`rpc-remap-misattributed-sales` 18:23Z, 1/4 runs — DAYTIME-CONTENTION family, self-recovers next tick; not new). Lock RELEASED (night RUN 2 8:08Z).

## NEW candidate (1, LOW-MED)

### SMOKE-PACK-DIST-SALES-HISTORY-FAIL — `analytics-smoke` "pack dist page has Sales History" leg failing on current prod
- **Source:** Sentry JAVASCRIPT-NEXTJS-1J (regressed; 26 occurrences, 7 users; last seen 2026-07-10T23:17Z on release `440d3840`); analytics-smoke `smoke_fails_6h=11`.
- **Signal:** The smoke leg fetches `/nba-top-shot/pack/dist/7800`, expects the HTML to contain the string "Sales History", and gets `HTTP 200, Sales History=false`. Page loads fine (200) but the Sales History section is absent from the rendered HTML for that dist.
- **Risk read:** LOW-MED. Two candidate causes, same class as the freshness leg CC just fixed (`audit_20260711_smoke_fmv_freshness_per_collection_thresholds`): (a) FALSE-FAIL — dist 7800 legitimately has no recent sales so the Sales-History component renders nothing / is conditionally hidden, and the smoke asserts a string that isn't always present (the smoke's chosen pack is brittle); (b) REAL REGRESSION — the 07-10 pack-board wave (`4969aef` stress-test hiding + `audit_20260710_*` pack_dist migrations) or the `440d384` deploy dropped the Sales History render for pack dist pages. The public `/nba-top-shot/pack/dist/7800` page would render without sales history in case (b).
- **Suggested action (night pass / CC):** Fetch `/nba-top-shot/pack/dist/7800` and confirm whether Sales History renders; check whether dist 7800 has any rows in the pack/sales-history source. If empty-and-hidden-by-design, recalibrate the smoke leg to pick a dist known to have sales (or assert a stable element), mirroring the per-collection freshness fix. If the Sales History section actually broke on pack dist pages, repair. Not autonomously actionable without the false-fail-vs-regression determination (pack-page render logic + smoke assertion). Contributes to the `pipeline_health_24h` warn already noted in the ledger.

## Carried / already-queued (context only — no new file needed)
- PACK-REALITY-TOP-EV-EMPTY — still `topshot_pack_reality_top_ev=0` (dist=6). Already queued in inbox 2026-07-10T15-11-45Z (undrained); do not re-log.
- TOPSHOT-MOMENTS-HYDRATOR-GETMINTEDMOMENT-ERRORS — 100 fails/24h, still alternating ok=true(resolves)/ok=false(0), no corruption; carried.
- ULTIMATE-FMV-RECALC-V1-MISSED-TICK — silent ~2,491 min (last 07-09 06:35Z; missed both 07-10 and approaching 07-11 06:35Z ticks), INFO, operator RPC_ADMIN_TOKEN cron, self-heals. Extended silence noted — if it misses the 07-11 tick too it graduates to operator-action.
- fmv-recalc 14 fails/24h (honest ok=false Supabase-timeout upserts), analytics-smoke 16 (this leg + the now-fixed freshness leg decaying out) — all known families.
