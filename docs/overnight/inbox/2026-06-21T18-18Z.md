# Daytime monitor candidates — 2026-06-21T18:18Z (2nd tick of day, 11:18 PDT)

Run: rpc-daytime-monitor, second tick. Since the 15:16Z first tick, TWO daytime CC sessions landed (mis-attribution closeout + a new daily Vercel drain cron). Lock RELEASED. Platform **GREEN** — security 0/0/0/0, trust 9/9, detect_stalled [] / alerts [1 INFO golazos], FMV reconciles + fresh (writers 0 fail/24h, fmv-recalc last 0.6m), Vercel 0 ERROR prod **d240fb85** READY, DB 5061 MB. One LOW sharpening below; CANDIDATE 1 from this morning is RESOLVED.

---

## RESOLVED since the 15:16Z tick — AUDIT-TABLE-RLS-20260621 (this morning's CANDIDATE 1)

CC hardened the 3 `audit_topshot_*_20260621` remap/revert tables (ENABLE RLS + REVOKE anon/authenticated). **Security back to 0/0/0/0**: `check_public_security_invariants()` [], `check_secdef_anon_execute_violations()` [], pg_tables RLS-off count 0.
- **Direct end-to-end proof:** the `analytics-smoke` `security_all_tables_rls_enabled` leg FAILED every tick 14:13Z→15:43Z, then went **green at 16:13Z** (the moment CC hardened the tables) and has stayed green.
- Sentry **JAVASCRIPT-NEXTJS-1C** ("public base tables: RLS on + no anon write") quiet since ~16:14Z (~2h, 5 events total) — its cause is fixed; it's now just resolvable / regression-arm cleanup (cosmetic, operator/night-pass).
- **Do NOT re-ship the hardening** — already live.

---

## NEW CANDIDATE (LOW, sharpening) — SALES-ON-UUID-EDITIONS-RESIDUAL

**Title:** Live + history-backfill TS sales still land on inert UUID-dupe editions AFTER today's writer fix — this is **converging residual, NOT a regression**. Heads-up so the night pass doesn't misread a non-zero `sales_on_uuid_editions` as the writer fix failing.

**Source (editions⋈sales):** 57 TS sales landed on hyphen/UUID-keyed editions in the last 2h (and **zero** in the prior 22h of the 24h window). Split: **34** `source=null` history-backfill rows (2020-dated, `topshot-sales-history-backfill` walking the tail) + **23** `source=topshot_gql` LIVE sales at 17:39–17:57Z — i.e. AFTER the writer-fix deploy `f7964471` went READY at ~16:18Z. All land on UUID-dupe fossil editions created 2026-06-05/06.

**Why (mechanism):** the writer fix only closes the **GQL-fallback (Step 4d)** keying path. The sales-indexer resolves a sale's edition via **wmc→moments FIRST**, and inherits the UUID `edition_id` when those rows still point at an un-drained UUID fossil — the GQL fallback never fires, so the fix can't prevent it. So the closeout's "0 new sales on UUID editions across all ingest windows" holds for the GQL path only, not the wmc/moments-inherited path.

**Risk read:** LOW. These editions are inert and suppressed from deal boards by the conflation guard, which is **converging** (44 → 27 → **17** now). `trust.ts_uuid_dupes_created_24h` = 2/200 (editions are NOT being re-polluted), `fmv_sanity_flags` 0, FMV reconciles. No product reader sees these.

**Convergence machinery already in place (no new code warranted):**
- pg_cron self-healer `remap_misattributed_topshot_sales()` every 6h (`23 */6 * * *`) — re-keys wmc-resolvable transients.
- NEW daily Vercel drain `/api/admin/drain-topshot-misattribution?rekey=1` — deployed READY today as **d240fb85** (`vercel.json`, `0 11 * * *`); re-keys untracked-wallet strays via on-chain `getMintedMoment`. **First AUTOMATED fire is tomorrow 11:00 UTC** (today's 11:00 window was before the ~18:10Z deploy), so there is no drain `pipeline_runs` row yet — expected, not a miss.

**Suggested action:** night-pass — do NOT treat non-zero `sales_on_uuid_editions` as a writer regression; instead confirm the conflation guard keeps trending toward 0 and that the daily drain logs a successful run after tomorrow 11:00 UTC. Operator/CC — only if convergence stalls: verify the drain cron + wire the carried detector-only guard refresh. No fix to ship now.

---

## Carried unchanged (not re-logging)
- **REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT** — identical to the 15:16Z read: only run in 24h is the 09:13Z gateway-timeout fail (no run since), MV current at 6778 rows. No new signal. (Operator must re-enable/repair the cron; fix deployed+sufficient per the overnight.)

## Post-ship watch (today's CC ships — all PASS, 0 regressions)
- **Writer fix `f7964471`** READY ~16:18Z; all 4 sales-indexers 72/0, history-backfill 11/0. (UUID-edition residual covered above — converging, not a regression.)
- **Drain route `f908c83`** + 7 `audit_20260621_*` migrations: security clean, trust 9/9, no schema break (11/11 insights boards return rows).
- **Vercel cron `d240fb85`** (vercel.json `0 11 * * *`) deployed READY. 20 recent deploys 0 ERROR (CANCELED = superseded monitor/docs commits).
- conflation guard 44→17 converging; FMV de-blending healthy (writers fresh, sanity 0, reconciles).

## Health one-liner
security 0/0/0/0 · trust 9/9 · detect_stalled [] / alerts [1 INFO golazos] · sentinel hyphen 24/250 (raw regex 1775 = benign Stage-B `::` parallels) · pipeline 6h 6/1966 = 0.31% (analytics-smoke ×5: 4 were the now-RESOLVED RLS-leg, 1 transient FMV blip @17:43Z; evm-429 ×1 benign) · FMV writers 0 fail/24h fresh <1m, reconciles · editions TS 17318 (+385 `::` parallels vs 16933 baseline)/AllDay 6191/Golazos 581/UFC 446 · unmapped 59 · DB 5061MB · Vercel 0 ERROR prod d240fb85 · Sentry 2 (NEXTJS-1C resolved-underlying/quiet 2h; NEXTJS-A FMV-smoke transient 21h, benign) · 16 artifacts (12 estate + 4 build, 0 RETIRED); MONITOR-ARTIFACT-ACCESS NOT blocking this run (OneDrive HTML readable) — rpc-live-health payload validated GREEN (11 boards return rows, freshness <11m).
