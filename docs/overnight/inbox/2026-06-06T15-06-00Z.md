# Daytime monitor inbox — 2026-06-06T15:06Z (08:06 PDT)

Run context: lock stale (6.4h, night-pass remnant — not held). Trevor ACTIVE in repo (pushed `2e8cbd1` 15:03Z + `8b8e378` docs 15:08Z while this run was sweeping). Security 0/0; 16/16 artifacts validated healthy; AF1-v2 target metric PASSES (`v_tracked_wallet_fmv_confidence` GROUP BY completes, no 57014).

## Not re-logged (already in ledger, status confirmed this run)

- **P1-CAD still live at HEAD** — `pinnacle-metadata-backfill` 5/5 fails last 6h (last 14:22Z, `cadence: Invalid character`). Verified at HEAD (`8b8e378`): the 2 em-dash lines are still inside the `PINNACLE_METADATA_SCRIPT` template literal (the b6005cb serial comments) and L243 still `btoa(PINNACLE_METADATA_SCRIPT)`. `2e8cbd1` did NOT touch the route (last commit to it = `b6005cb`). 15:22Z tick will fail again. Flagged loudly in monitor output since Trevor is at the keyboard.
- N1 `snapshot-institutional-wallets` still stalled (2,266m vs 1,800m; last run 06-05 01:21Z). Operator re-fire.
- DUPE1: sentinel TS-UUID-48h **6,339** CRITICAL (08:30Z baseline 5,840; +499/6.6h ≈ 76/hr avg, 62 in the last hour — simmering above the 27/hr trough, far below the 450–520/hr peak). TS editions 15,498. DUPE1-MIT remains the ready-to-run mitigation.
- Smoke 00:17Z cluster: 7 Sentry issues, 0 recurrence in 15h (markable resolved after 24h quiet ~00:17Z 06-07). NEXTJS-15 recurred once 2h ago (1 event, known PIN1 class, not spiking).
- Transient 12:07–12:25Z timeout cluster (9 pipelines, 1–2 fails each, upstream/pool/statement timeouts at the 12:00Z rush) — ALL recovered by 15:07Z. Known class, no action.

## NEW candidates

### C1 · Post-ship verification pack for `2e8cbd1` (Pinnacle render_id re-key) — MED priority, verification-only
- **Source:** deploy `dpl_Fmee8sMQ3EoHWoC8EMqXKCGDk9kJ` (commit `2e8cbd1`, READY 15:03Z) + docs `8b8e378`.
- **What this run already verified (read-only):** `pinnacle_catalog` = 2,079 rows, **2,079/2,079 with `thumbnail_url`** (the 06-05 "images dead-end" is overturned); wmc Pinnacle `render_id` 36,405/36,406; columns incl. `front_anim_url`, `legacy_edition_key`, `is_chaser`.
- **Not verified (couldn't):** live smoke of the new public route `/api/public/pinnacle-image/[renderId]` (sandbox fetch restriction). Sample id: `SEV1-MNF-MICK-S1`.
- **Suggested action (night pass):** smoke the image route (200 + image content-type, plus a bogus-id 404); confirm `populate_wmc_image` now fills Pinnacle `wmc.image_url` from `pinnacle_catalog` (was ~82 legacy rows — expect growth); confirm `/share/[wallet]` Pinnacle tiles render art; re-check `derive_pinnacle_wmc_from_catalog()` appears in `pipeline_runs`/cron wiring as intended. Code shipped <24h ago — **verify, don't edit**; queue any fix found.
- **Risk:** read-only verification; zero ship risk.

### C2 · `topshot_pack_reality_top_ev` thinned to 1 row — public /insights pack-reality near-empty — LOW
- **Source:** artifact `rpc-insights-health` Q_COUNTS leg (this run: 1; 06-05 note: 3 packs).
- **Read:** the honesty-gated view now qualifies a single pack, so the public board looks broken-thin even though the gate is by-design. Likely driver: FMV churn (DUPE1 NO_DATA tail / haircuts) pushing pool-coverage below the gate.
- **Suggested action:** characterize why candidates fell out (coverage %, which gate fails); only then decide whether the gate needs a floor tweak or the page needs an honest empty-state. No code change without that analysis.
- **Risk:** analysis-only first.

### C3 · Baseline `v_offer_sanity_flags` (138 rows) — LOW, observability
- **Source:** artifact `rpc-qa-scorecard` backing view (this run: 126 `chain_exceeds_gql` + 12 `gql_blank_chain_has`).
- **Read:** monitoring-by-design rows, but no recorded baseline → drift is invisible. 126 chain>GQL could be legit (chain offers GQL hides) or an indexer skew.
- **Suggested action:** record the baseline in `metrics-latest.json` (night pass owns it) and/or add a one-line breakdown to the weekly health check; flag only on step-change.
- **Risk:** doc/metrics-only.

## Headline metrics (this run, 15:06Z)

| metric | value | vs 08:30Z baseline |
|---|---|---|
| TS FMV HIGH+MED | 3,039 (569 H / 2,470 M) | flat (3,041) |
| AllDay FMV HIGH+MED | 492 | flat (490) |
| TS NO_DATA | 5,813 | +369 (DUPE1 inert tail, expected) |
| TS editions | 15,498 | +499 (DUPE1) |
| sentinel TS-UUID-48h | 6,339 CRITICAL | +499; 62/hr last hour |
| unmapped_sales open | 162 | flat |
| DB size | 6,290 MB | +33 |
| deploys | 2e8cbd1 READY 15:03Z; 8b8e378 BUILDING (docs); 949e10f ERROR known/superseded | — |
| detect_stalled_pipelines() | 1 (N1 only) | unchanged |
| security | 0 RLS-off / 0 anon-write | clean |
| artifacts | 16/16 healthy (insights-health repair holding; all 10 Q_COUNTS legs return) | — |
