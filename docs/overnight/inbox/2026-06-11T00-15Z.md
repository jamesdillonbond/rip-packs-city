# Daytime monitor inbox — 2026-06-11T00:15Z

Run context: lock RELEASED (night pass done 13:53Z); no FREEZE; prod HEAD aa728f7 READY. This run updates several 06-10 daytime items with hard post-21Z data.

## 1. HIGH — DBSAT-RESIDUAL-NOT-RECOVERED: elevated fail rate sustained 16Z→23Z despite the full fix wave
- Source: pipeline_runs hourly histogram — fails/total: 16Z 42/185, 17Z 37/214, 18Z 54/774, 19Z 31/121, 20Z 40/213, 21Z 39/210, 22Z 36/218, 23Z 47/213 (~17-25%/hr, all statement/upstream-timeout class). The 21:15Z inbox's "first clean signal at 21Z" was partial-hour optimism — 21Z finished at 39 fails.
- That is ~14h of continuous elevated fails (10:00Z onward) AFTER populate_wmc_image fix (16:20Z), d198e68 load-shed (17:52Z), the -40% DB drops ship (live: DB now 4174 MB vs 6903 at 18:20Z — drop CONFIRMED), d4b058f, 89c4891. Top failers 6h: compute-topshot-pack-ev 42, wmc-fmv-populate 33, topshot-buyer-backfill 32, pinnacle-nft-resolver 31, topshot-moments-hydrator 31.
- Risk read: no single new culprit visible from pipeline_runs; the remaining load may be the heavy drains themselves (buyer-backfill, pack-ev) colliding, or pooler/compute-bound. READ-ONLY monitor cannot see Supabase compute metrics.
- Suggested action (night pass, top priority): DBSAT re-baseline with the post-drop DB; identify the top pg_stat_statements consumers 16-24Z; consider pacing topshot-buyer-backfill (its 40m cadence + statement-timeout fails ~32/6h make it both victim and contributor). Operator: glance at Supabase dashboard compute/pooler graphs for this window.

## 2. HIGH — PACK-EVENTS-REWEDGED: topshot_pack_purchases cursor advanced once (21:24Z) then stuck on a 1796-row batch
- Source: pipeline_runs pack-events-ingest 23:09Z + 23:39Z both `topshot_pack_purchases: pack_purchases batch upsert (1796 rows): statement timeout`; event_cursor topshot_pack_purchases updated 21:24Z (block 154385136), topshot_pack_opens frozen since 17:24Z, allday_pack_purchases since 18:09Z.
- Same batch failing twice in a row = deterministic wedge under current DB conditions, not transient. Updates 21:15Z Item 1 (was "frozen 3.2h"; it briefly recovered then re-wedged).
- Risk read: ingest gap growing on the pack-history surfaces; no corruption (upsert idempotent on (tx_hash, pack_nft_id)).
- Suggested action: CC/worker — chunk the pack_purchases/pack_rips/moment_acquisitions batch upserts (e.g. 250-500 rows per statement) in workers/pack-events-ingest so one oversized batch can't permanently wedge a cursor leg; interim operator option: re-fire after DB calms and confirm cursor advances past 154385136.

## 3. MED-HIGH — AF1 ARTIFACT STILL BROKEN at 00:12Z: v_tracked_wallet_fmv_confidence direct `count(*)` times out
- Source: live probe this run (statement timeout); artifact rpc-tracked-fmv-confidence. Confirms 21:15Z Item 4 persists past the load-shed wave.
- This is the 3rd+ break of this view — per the 06-06 night-pass rule: stop optimizing the view, fix the writer (DUPE1-MIT/B2 class). May also simply be DBSAT collateral; re-probe after Item 1 resolves before shipping anything.

## 4. MED — TFP-NO-OK continues (~48h): topshot-fmv-populate last attempts 12:50Z + 18:50Z both failed (`sets read failed: upstream request timeout`)
- Cadence looks 6h (:50); next tick ~00:50Z — tonight's pass should watch whether it lands and succeeds on the calmer 00Z DB. Carry of 21:15Z Item 3; no new action beyond the queued operator re-fire/auto-disable check.

## 5. WATCH — pinnacle-wmc-render-id: logging works (d198e68 wrap) but candidate_read times out most ticks (ok 1/7 since 18:37Z). Known carry — the queued render-id index (d118252 evening audit) is the fix; no new action.

## Green this run (verifications, no action)
- badge catalog GHA: FIRST scheduled tick logged ok=true 22:30Z (`topshot-badge-catalog`) — focus 8a verified; 21:15Z Item 7 CLOSED.
- fmv-recalc silent-run class CLOSED: 89c4891 verified live — early-exit paths now log (ok=true 18:28/20:48/23:00Z interleaved with real-error fails). detect_stalled coverage restored.
- Security 0/0 all four checks — audit_lt_matches (18:20Z Item 2) RESOLVED; RLS-off list empty.
- Trust health 7/7 ok (pinnacle_ask 0.2h, pinnacle_fmv 14.0h). Sentinel TS-UUID-48h = 0. unmapped 183 (flat).
- DB 4174 MB — the deliberate -40% drops ship confirmed; creep-watch baseline RESETS here.
- Deploys: 19/20 READY; lone ERROR d118252 is docs-only and superseded by 3 later READY deploys (transient build-infra class).
- Sentry: 7 unresolved/8h, all known (smoke saturation echoes 1E/A/4/W/E last ~22Z; NEXTJS-14 quiet 5h — if quiet through 06-11 daytime, resolve with regression arming; 1N single-event).
- detect_stalled: 2 entries, both severity=info (backfill-pack-rip-metadata 494m, refresh-pack-grail-metrics-mv 284m) — both ran-and-failed rather than cron-dead; saturation casualties of Item 1.
