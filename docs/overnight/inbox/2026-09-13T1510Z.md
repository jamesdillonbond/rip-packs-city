# Daytime monitor candidates — 2026-09-13T15:10Z (08:10 PT, first-tick-of-day run)

Environment: sandbox bash/git DOWN (Sept-8 Windows Plan9 breakage, 6th day) — **inbox written to mount, push unavailable**. Read-only sweep otherwise complete. Not in a saturation spell at sweep time (pg_stat_activity: 0 IO-wait / 0 active), but the morning 05:00–06:00 PT band was spell-prone (see candidate 1 context).

Lock: RELEASED (last night's nightly pass, np-20260913-a801). A concurrent CODE session is actively pushing to `main` (Vercel tip BUILDING on a `Claude` `fix(og/player)` commit) — not a monitor collision, inbox write is a new uniquely-named file so no contention.

---

## Candidate 1 — MEDIUM — cross-collection step2 stale ~39h; `cross_collection_ts_set_overlap_mat` missed a cycle

- **Source:** 1a cross-collection refresh verify. `rpc-ccm-step2` FAILED 2026-09-12 23:25:00Z with `canceling statement due to statement timeout` (on the `CREATE TEMP TABLE _ccm_step2_next` step). `rpc-ccm-step1` succeeded same night (cohort_mat fresh, `computed_at` 2026-09-12 23:10Z, count 185 — healthy). `cross_collection_ts_set_overlap_mat.max(computed_at)` = **2026-09-11 23:25Z (~39.6h stale)** because step2 has not landed since.
- **Classification:** genuine finding, but a **single missed cycle** — step2 succeeded 09-10 and 09-11; this is the first failure in the window, caused by the 09-12-night saturation spell, not a logic error.
- **Risk read:** LOW-to-medium. The overlap mat feeds the cross-collection TS-set-overlap surface only; cohort_stats (the main cross-collection board) is backed by the fresh cohort_mat and is fine. Self-heals automatically if tonight's step2 (04:25Z-scheduled leg) completes.
- **Suggested action (night pass):** if still stale at drain time, run the 1a recovery recipe — a self-cleaning pg_cron one-shot for step2 ONLY, body ending in `cron.unschedule` of itself (note: a FAILED one-shot does NOT self-unschedule and must be cleaned next day). Durable option worth weighing: step2's `CREATE TEMP TABLE` is timing out under the nightly-band load at 23:25Z — moving it off the spell band, or bounding its cost, would stop the recurrence. Verify against BUFFERS, not wall-clock (instance is spell-prone).

## Candidate 2 — LOW — `topshot_pack_reality_top_ev` = 0 rows; confirm public surface renders an honest empty

- **Source:** 1b artifact backing-view validation (rpc-live-health `insights_counts` leg). `topshot_pack_reality_top_ev` returns **0 rows**, while `pack_ev_latest` is fully populated (4,642 rows, matches the 09-05 rewrite) and all pack-EV trust arms are green (`pack_ev_publish_shortfall_pct` 0.79, board not depleted/stale).
- **Coherent explanation, not a break:** downstream of the known `pack_distributions` `data_stale` alert ("last seen 10 days ago") — with no active Top Shot pack distributions, nothing clears the +EV bar, so 0 is the honest state, not a failed read.
- **Risk read:** LOW. Base data healthy; this is an emptiness that *looks* like the honesty-canon false-empty shape but is explained by upstream market state.
- **Suggested action:** on a browser-capable pass, confirm `/insights/pack-reality` renders an honest "no +EV packs right now / no active drops" empty state (not a failed-read masquerade), and confirm the 10-day `pack_distributions` staleness is genuinely "no new TS pack drops" vs a broken dist ingest. No code action if the empty state is honest — do NOT chase as a defect (QA sweep fails toward false positives).

---

### Known / not re-logged (already tracked)
- `topshot_impossible_parallel_serials` = 29 (trust BREACH) — #82, Trevor-gated destructive `sales` re-key; **unchanged since last night's 29**, not climbing, not new work.
- `offers-sweep` 100% fail (Top Shot GraphQL 530) — #81, high, known dead.
- `apply-fmv-haircut` cron_silent ~40h — Trevor/cron-job.org, known.
- pg_cron failure cluster (rpc-refresh-*-pack-sales-agg, ccm-step2, backfill-pinnacle-mint-acquisitions, thp-leg, allday-ev-corrected, serial-fmv-*, reconcile-saved-wallet-stats, etc.) — **all `statement timeout` / `job startup timeout`, zero logic errors, clustered in the 05:00–06:00 PT band = saturation collateral, not N distinct bugs** (Section 1c). Intermittent (each recovers on non-spell ticks).
- DB 29,915 MB (+~700 MB/24h) — retention (Atlas events + `cron.job_run_details`), Trevor.
- Sentry 5 issues: all "Consecutive HTTP" / "N+1" perf spans on wallet-backfill / pinnacle-trades crons, 0 users, 1–2 events — known shape, not a spike.
- cron-job.org dead-lane entries disabled (#80/#55) — Trevor.
