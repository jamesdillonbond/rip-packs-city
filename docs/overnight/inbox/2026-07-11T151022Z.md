# Daytime monitor — 2026-07-11T15:10Z (first-tick-of-day)

Platform GREEN except one ESCALATING operator item. No clock skew (shell 15:05:58Z ≈ DB now() 15:06:11Z; sale 14:46Z / fmv 14:57Z fresh). Security 0/0/0/0; pg_cron `check_pgcron_recent_failures()` []; sentinel TS-UUID-48h 0; Sentry 0 new/24h; cross-collection refresh fresh (cohort 04:10Z / overlap 04:25Z, both pg_cron steps succeeded, 154 rows); Vercel prod `950c793a` (Trevor pack-opens backfill) READY, no ERROR-state prod deploy.

## HIGH-PRIORITY — CRONJOB-ORG-TRIGGER-DROPOUT-20260711 has NOT self-healed (~9.5h; ESCALATION of the 08Z night-pass top finding)
- **Source:** `rpc_ops_snapshot()` stalled_pipelines + `pipeline_runs` recency sweep (this run).
- The night pass (08:03Z) logged this expecting cursor-based self-heal. It has NOT recovered ~7h later. A coherent SUBSET of cron-job.org-triggered pipelines is frozen since ~05:2x-05:43Z while the rest of the estate fires fresh at 15:0xZ (topshot-sales-indexer 15:03Z, fmv-recalc 14:57Z, offers-sweep 15:02Z, compute-topshot-pack-ev 15:07Z - so NOT a full-console outage, and core ingest/FMV are unaffected).
- **Frozen subset (last run):** `wmc-fmv-populate` 05:43Z (~1m cadence - user-facing: /share + portfolio + wallet-snapshot FMV going stale), `snapshot-pack-asks` 05:43Z (5m - Pack Sniper NEW/price-drop recency), `allday-listings-retry` 05:38Z, `golazos-listings-indexer` 05:37Z, `pinnacle-events-ingest` 05:34Z, `pinnacle-listings-reconcile` 05:24Z, plus the 3h `*-sales-history-backfill` set stuck at 03:xxZ.
- **Live consequence:** `v_rpc_trust_health` `pinnacle_ask_stale_hours` BREACH 9.7h (threshold 3h) - directly downstream of frozen pinnacle-events-ingest + pinnacle-listings-reconcile, NOT a separate data regression. Will clear on cron recovery.
- **Risk:** cursor-based => no data loss; self-heals on trigger recovery. But wmc-fmv staleness is user-visible and this is the longest instance of the recurring class (07-07 / 06-09 / 05-31). Not auto-actionable (external secret-bearing console = operator-only).
- **Suggested action (operator, TODAY - night pass at 1am is too late):** inspect cron-job.org execution history from ~05:2xZ and re-enable/re-fire the dropped entries - priority `wmc-fmv-populate`, `snapshot-pack-asks`, `pinnacle-events-ingest`, `pinnacle-listings-reconcile`. Recurring class => consider why this subset keeps dropping together.

## Carried / known (no new action)
- `topshot-moments-hydrator` 63 fails/24h (TOPSHOT-MOMENTS-HYDRATOR-GETMINTEDMOMENT-ERRORS, still alive 15:02Z, self-limiting), `analytics-smoke` 16 + `fmv-recalc` 14 (known contention/false-fail families), `topshot-buyer-backfill` 11 - all carried in ledger.
- `snapshot-institutional-wallets` / `apply-fmv-haircut` / `pinnacle-catalog-backfill` >24h cron_silent - daily jobs, folds into the dropout above or their own recurring operator watch.
- editions TS 19,126 / AllDay 6,190 / Golazos 575 / UFC 518; FMV TS H+M 5,237 / AllDay 818; DB 9,563 MB.
