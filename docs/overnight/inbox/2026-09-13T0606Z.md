# Daytime monitor candidates — 2026-09-13T06:06Z (2026-09-12 23:06 PT)

Read-only daytime sweep. DB clock `now()` = 2026-09-13 06:06:10Z. Not the ~8am first-tick, so Section 1a (trust-health full / cross-collection verify) was not run as a daily extra — but the snapshot surfaced a trust breach anyway, below. **Spell control taken: `pg_stat_activity` io_wait=0 / active=0 — NOT in a saturation spell at sweep time.** So timing findings here are interpretable; the one known timeout cluster is still treated as earlier spell-collateral, not N bugs.

Environment note: the Cowork sandbox VM is down (Sept 8 Windows-update mount failure — `device_bash`/clone unavailable). Inbox written to the mount; **push unavailable this run** — night pass picks it up locally.

---

## HIGH — trust-health BREACH: `topshot_impossible_parallel_serials` = 27 (breach_at 3) — real, fresh, NEW

- **Source:** `rpc_ops_snapshot().trust_health` + `v_rpc_trust_health` + `rpc_trust_health_precompute`.
- **Not a stale-sentinel artifact:** precompute row `computed_at` = 2026-09-13 00:48Z (age 5h20m); value is **27**, not the 999 "missing/>24h precompute" sentinel. `trust_precompute_max_age_hours` = 5.96 (breach_at 13) confirms the precompute is fresh. So this is a genuine count, not a refresh failure.
- **Not spell-collateral:** it's a data-accuracy *count*, not a timing metric; a busy instance cannot manufacture it.
- **What it catches (verbatim):** "F1 parallel mis-attribution: sales keyed onto `::` parallel editions as impossible serials (serial > parallel circ); invisible to editions-flat / UUID / FMV sentinels … Guarded at all 4 writers; pages a future regression." The last manual-only catch was the 0→6 offer_fill drift on 2026-07-02.
- **Why it's a candidate:** 27 > breach_at 3, and it is not referenced in the recent ledger/focus steers — i.e. a regression slipped past (or around) the 4 guarded writers since the metric last read clean. Blast radius: up to 27 sales rendering impossible serial/circulation on parallel-edition moment surfaces.
- **Risk read:** data-accuracy regression, user-visible on affected parallel editions. Read-only to diagnose; the fix touches writer code / a corrective migration — night-pass or Trevor, not this monitor.
- **Suggested action (night pass):** enumerate the 27 offending `(sale, edition)` rows; correlate `rpc_trust_health_precompute` history to find the change-point (did the count step from ~0 recently, and does it line up with a ship?); identify which of the 4 writers admitted them; run the manual parallel-serial sweep and either correct the rows or tighten the guard that let them through. Check against today's ships first (cursor reset on `sales-counterparty-backfill` writes `seller_address`, not serial/edition keys, so it is an unlikely source — verify, don't assume).

## HIGH — newest production deploy is ERROR: `vercel.json` `ignoreCommand` > 256 chars

- **Source:** Vercel `get_deployment(dpl_CLVTvgDwT5mMVzGmzBxLSaxhu7US)`.
- **Verified fact:** newest production deployment (git sha `15647889`, commit "docs(skill+register): the cron console's auth header is on the COMMON tab…") is `state: ERROR`, `readyState: ERROR`, with `errorMessage` = **"The `vercel.json` schema validation failed with the following message: `ignoreCommand` should NOT be longer than 256 characters"**. Deterministic config error, NOT spell-collateral.
- **Production is up:** the deploy list below the tip is ERROR → CANCELED → CANCELED → READY; production serves the last READY. Site is live; only the *deploy pipeline at the tip* is broken.
- **Discrepancy worth resolving:** the mounted working-tree `vercel.json` `ignoreCommand` is already ~216 chars (within the 256 cap) — i.e. a fix may exist locally but **unpushed** (today's push paths are degraded: VM dead, cloud needs repo attached), or the mount differs from origin tip. No newer deploy exists, so origin/main tip = the ERROR commit.
- **Risk read:** medium-high operationally — **no new code can ship via git push until a deploy succeeds** (every push re-validates `vercel.json` and will ERROR the same way if origin still carries the >256 version). User-facing impact today: none (site serving prior READY).
- **Suggested action:** confirm `vercel.json` on origin/main; if it still carries the >256 `ignoreCommand`, push the ≤256 version (the ~216-char form already on the mount works) so `main` deploys again; then verify a READY production deploy with aliases attached. Code/config change — night-pass or Trevor, not this monitor.

## LOW / SYMPTOM — `rpc-ccm-step2` failed 2026-09-12 23:25Z (statement timeout building `_ccm_step2_next` temp table)

- **Source:** `check_pgcron_recent_failures()`.
- One failed run; error is `canceling statement due to statement timeout` (no logic error) on a day the ledger records as spell-prone since 05:00Z. Not in a spell at sweep time, but this run landed earlier. `cross_collection_ts_set_overlap_mat` (step2 output) may be one cycle stale.
- **Mark as SYMPTOM, not cause.** Suggested action: on the next ~8am first-tick, re-run the Section 1a cross-collection verify (the 4 freshness/active checks); if step2 is chronically timing out (not a one-off), that's a real sizing item for the night pass — otherwise it self-clears on the next tick. Do not "fix" it from this single timeout.

---

### Swept and NOT filed (known / tracked / attributed — no action)

- Statement-timeout cluster `rpc-fmv-clamp-disconnected-ask` (08:55Z), `rpc-pinnacle-fmv-recalc-backstop` (22:37Z), `rpc-topshot-onchain-rekey` (11:33Z), `rpc-ccm-step2` — all timeouts, no logic errors = saturation collateral, not distinct bugs.
- `pipeline_alerts`: `offers-sweep` (known dead / set inactive), `sales-counterparty-backfill` 43.2% (worked today, pooled across the 12:31 PT cursor-reset fix), `lock-check-batch` / `fmv-backfill` / `price-snapshots` / `run-insider-detectors` / `allday-buyer-backfill` (timeout-dominated, known), `apply-fmv-haircut` silent (long-standing since May), `pack_distributions` data_stale 10d (known).
- Atlas 403 info rows (`atlas-editions-upstream-403`, `atlas-market-upstream-403`) and `flow-rest-moment-moved-400` — all attributed + info, escalation arms not tripped.
- `stalled_pipelines`: `ingest-topshot-challenges`, `topshot-catalog-backfill`, `topshot-misattrib-drain` — the three seeded/suppressed dead Top Shot lanes.
- Security section clean: `invariants`/`anon_write_holes`/`rls_off_base_tables`/`secdef_anon_violations` all `[]`.
- Secret leak referenced in the ERROR deploy's commit (INGEST_SECRET_TOKEN prefix, 2nd occurrence, #32) — commit states Trevor notified, rotation is his action. Already captured; not a monitor item.
