# Daytime monitor inbox — 2026-07-01 ~08:11 PDT (15:11Z, first tick of day)

Health GREEN. security 0/0/0/0 · trust 15/15 ok (breaches []) · stalled [] · pg_cron failures [] · cross-collection fresh (04:10/04:25Z, both jobs succeeded, cohort 153) · editions FLAT (TS 17,489 / AllDay 6,191 / Golazos 581 / UFC 518) · FMV H+M TS 4,695 (improving from overnight 4,679) / AllDay 896 · sentinel TS-UUID-48h 17 (inert) · leak 24h/48h 0/17 · DB 7,108 MB (+116 vs overnight, benign backfill wave) · Vercel latest prod READY (e0afec3b), 0 ERROR deploys · runtime errors all known families. Lock RELEASED (nightly, 08:21Z). All 16 pipelines with 24h fails show latest run ok=true (transient). live-health artifact validated end-to-end (12/12 insights surfaces live, freshness <1min, leak inert). Today's new DB objects landed clean + secured (get_edition_market_bundle service_role-only, get_edition_insight_links anon-read, allday_edition_floor_ask view).

Post-ship watch note (for the night pass, not a candidate): the heavy 2026-07-01 Cowork/Trevor daytime wave (~16 commits — badge-art restore + collection-scoped threading, special-serial glyphs, and the perf/pool-reduction bundles: drop dead special_serials `e46249eb`, insight-links bundle `705fb202`, edition market bundle `e0afec3b`, player Top-Sales Suspense `39407539`, overview LATERAL `6ebcc8f1`, CSP cdn.nba.com `aa7224b6`, pack-hero empty-200 `dbfce047`, Sentry tracesSampleRate 0.1 `054701fb`) is landing well: the entire edition-page connection-pool-saturation error class (get_edition_detail / sales / high_offer / notable_serials / subedition_siblings / special_serials / ipfs_assets / parallels) last occurred on the SUPERSEDED `dpl_8Gu4` (2026-06-30T22:xx), NOT recurring on today's deploys. All deploys READY; the several CANCELED entries are docs-only (ignoreCommand).

---

## Candidate 1 — DORMANT-ANON-DML-REVOKE-REGRESSED (LOW; DB-only; night-pass/Trevor decision) — NOT a live hole

**Title:** The 2026-06-23 defense-in-depth anon-DML revoke has silently regressed — anon write grants are back on 150 base tables (authenticated on 156).

**Source:** Live catalog sweep this tick (`information_schema.role_table_grants` × `pg_class` × `pg_policies`). Precise counts: anon_write_tables **150**, auth_write_tables **156**, anon_write_rls_off **0**, anon_write_with_permissive_policy **0**. Cross-ref ledger line 451 `audit_20260623_revoke_dormant_anon_dml_defense_in_depth` (shipped Cowork 2026-06-23; took anon write grants 482→46) + line 175 `audit_20260628_revoke_default_and_existing_view_writes` (ALTER DEFAULT PRIVILEGES … REVOKE … FROM anon, authenticated, but base TABLES deliberately untouched) + line 385 the 06-24 repo-synced `ALTER DEFAULT PRIVILEGES FOR ROLE postgres … REVOKE … FROM anon`.

**What I observed:** ~150 RLS-on public base tables (incl. core hot tables `editions`, `sales_2026`, `fmv_snapshots_2026`, `moments`, `wallet_moments_cache`, `pipeline_runs`, `pack_*`, and every year/month partition of sales/fmv_snapshots/marketplace_offers/evm_nft_transfers) again carry the Supabase-default anon+authenticated INSERT/UPDATE/DELETE/TRUNCATE grants. `cached_listings_v2` (the new `allday_edition_floor_ask` view's base) is one of them — its only policy is `service_role_all`, no permissive public write policy.

**Risk read: LOW / zero live risk (defense-in-depth only).** Every one of the 150 has RLS ON and **0** permissive public/anon write policies, so anon/authenticated writes are default-denied — the grants are unreachable. This is exactly why `check_public_security_invariants()` = [] and `anon_write_holes` = [] this tick (both only flag RLS-OFF write holes). NOT exploitable. The finding is that a previously-SHIPPED hardening has drifted back to ~pre-revoke breadth, almost certainly because a Supabase platform operation re-applied the schema-wide default GRANTs (a known Supabase behavior) — so CLAUDE.md/ledger currently imply this is closed when it isn't.

**Suggested action (night-pass/Trevor):** decide between (a) re-running the SCOPED defense-in-depth revoke mirroring `audit_20260623_*` — preserve the 5 intentional anon-write tables (`email_subscribers`, `outbound_clicks`, `portfolio_snapshots`, `support_conversations`, `funnel_events`) — AND, since a one-time revoke provably re-accumulates, pairing it with a durable guard (a periodic re-revoke, or a monitor assertion that anon-write-table-count stays near its intended floor), vs (b) formally ACCEPTING the RLS-on posture and correcting the stale "core tables revoked / closed" wording so future passes don't re-chase it. Scope caution if re-revoking: the destructive-op circuit-breaker + hot tables (editions/wmc/sales) mean a scoped, reviewed migration — never a blind schema-wide sweep (the exact hazard the 06-23 note called out).

---

## Candidate 2 — TEAM-PAGE-STATEMENT-TIMEOUT (LOW; perf follow-on to today's edition/player/pack wave)

**Title:** `/[collection]/team/[slug]` detail + activity queries trip statement_timeout — the one edition-sibling perf class today's wave did NOT address.

**Source:** Vercel get_runtime_errors (24h). `[team] detail error canceling statement due to statement timeout` (count 8, last 2026-07-01T14:24Z) + `[team] activity error canceling statement due to statement timeout` (count 8, last 2026-07-01T13:58Z), both on the current-ish `dpl_FnerNxecjpHdcwN9W3QEVanqFx4U`. First seen 2026-06-04/06-08 (long-standing, low-volume: 2–4 users).

**Risk read: LOW.** Long-standing, low blast radius (team pages, degrades to empty/partial rather than 500-ing the shell). NOT a regression. Called out because it is the one statement-timeout/pool sibling still firing on TODAY's deploys after the 07-01 wave quiesced the edition/player/pack classes — so it's a coherent, low-risk perf follow-on rather than fresh noise.

**Suggested action (night-pass/CC):** same treatment family the 07-01 wave used on the siblings — per-entity LATERAL … LIMIT 1 index seeks in the team detail/activity RPC, and/or Suspense-stream the Activity block so a slow query degrades to the empty state instead of tripping the page's statement_timeout. Route last-touched recently (badge/CSP wave); confirm the heavy leg is the activity scan before refactoring.
