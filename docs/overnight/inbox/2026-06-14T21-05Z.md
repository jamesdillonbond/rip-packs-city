# Daytime monitor — 2026-06-14 ~21:05Z (afternoon tick)

PLATFORM GREEN. One new candidate (TOP-SALES-INSIGHTS-QA) + green post-ship confirmation for the two Trevor ships that landed after the 15:10Z first tick.

## Health snapshot (read-only; deltas vs the 15:10Z first tick)
- pipeline_runs: 0 fails last 6h. 24h: 3 fails / 2 pipelines (wmc-fmv-populate 2, check-alerts 1) — newest 07:33Z (>13h ago), all known transient (wave-coincident lock/stmt timeout + :15-slot collision). detect_stalled_pipelines() = []. get_pipeline_alerts() = [].
- Security: 0/0 (no RLS-off base tables; no anon/auth write on RLS-off base tables, relkind r/p).
- Sentinel TS UUID-keyed editions 48h: 0.
- Trust health: 8/8 ok (re-confirmed read-only — not the daily extra). pinnacle_fmv_stale 11.1h/30 (normal drift between daily pinnacle-sync recomputes; 5.0h at 15:10Z). offer_edition_gap_max_usd 0, ts_uuid_dupes_24h 0.
- FMV (improving): TS hi+med 3361 (15:10Z 3356 / 08:15Z baseline 3350), AllDay 709 (681 / 679), Pinnacle per-render 764/1839 (flat). Editions flat (TS 15543 / AllDay 6191 / Golazos 581 / UFC 446). FMV/pack-EV/offers writers all fresh (~21:07-21:09Z). DB 4612 MB (benign +12 vs 15:10Z).
- Vercel: prod HEAD 0eee25c READY; all recent READY/CANCELED, 0 ERROR (CANCELED = docs-only build-skips via 0e7e627 ignoreCommand / superseded).
- Sentry: 0 unresolved issues in last 12h (NEXTJS-15 quiet since bd8e05c/cc75555).
- Artifacts: rpc-live-health consolidated panels ran clean — all 11 insights boards return rows (squeeze 501 / set_squeeze 235 / pack_reality_top_ev 8 / rookies 61 / market 501 / first_mint 501 / cross_collection 1 / pinnacle_scarcity 501 / offer_spread 501 / deals 501 / trophies 501); AF1 v_tracked_wallet_fmv_confidence 21 rows no-timeout; insights-fresh + trust panels current. 12 active validated, 5 RETIRED tombstones skipped. None broken/repaired.

## NEW candidate
- **TOP-SALES-INSIGHTS-QA · [LOW · night-pass, rpc-insights-qa checklist]** — `b623be2` (deploy dpl_3eqwPVQb READY) shipped a fresh public `/insights/top-sales` (Top Sales / Whale Watch) surface backed by `v_insights_top_sales`. Same class as TROPHIES-INSIGHTS-QA (closed 06-14). Monitor pre-checked the **backing-view security half = PASS**: relkind 'v' + security_invoker=on + anon & authenticated SELECT + returns 501+ rows (LIMIT-capped). Remaining for the night pass (static source review of the READY build): sitemap `top-sales` entry + insights-hub "Surface M" card (commit claims both), param-stripped canonical, OG 1200x630 PNG, WebApplication/Dataset JSON-LD, crawlable raw-HTML drill-downs (commit note: the view's moment_id is NULL so TS art keys off nft_id via assets.nbatopshot.com/media/<nft_id>/image — verify the buyer/seller @handle links + tile art resolve), price honesty (never $0 on unpriced), 15-min edge cache, brand tokens. Live HTTP-200 deferred (web_fetch provenance restriction); deploy READY + anon-public view + 0 Sentry = high confidence. Not auto-shippable — review-then-close, or flag if the checklist finds a gap. Source: Vercel deploy 3eqwPVQb / commit b623be2; v_insights_top_sales.

## Post-ship watch (two ships landed AFTER the 15:10Z tick) — CLEAN
- `b623be2` /insights/top-sales — backing view resolves (501 rows), READY, 0 Sentry. (QA candidate above.)
- `0eee25c` feat(share): profile-share referral loop (ShareProfileButtons referrerId + RefCapture credit; CURRENT prod HEAD) — app-only, no schema change, READY, 0 Sentry, no pipeline regression. Nothing to action; night-pass post-ship watch only (referral_verified credit path is self-referral-guarded server-side).

## Already-queued, unchanged (NOT re-raised)
- ALLDAY-V1-UNMAPPED-DRIFT (operator recover-v1 cron), the VERCEL-* cost family, TFP-SLOT-WAVE-COLLISION, ANALYTICS-SMOKE-RESIDUAL, OFFER-SANITY-RAISE, PIN-FMV-REKEY-WAVES, TEAM-MOMENT-DISPLAY, etc. TFP-480-RESTORE was already recommended CLOSE in the 15:10Z file (watchlist already at 480 + clean :38 ticks).
