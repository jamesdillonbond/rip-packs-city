# Daytime monitor — 2026-07-20T03:13Z

Tick 5 of the local day (priors: `2026-07-19T1512Z.md`, `T1820Z.md`, `T2115Z.md`, `2026-07-20T0010Z.md`). Lock RELEASED (night pass `night-20260719-12828`, 08:17Z) => inbox commit permitted.
No clock skew: shell 03:05:35Z ~= DB `now()` 03:05:46Z ~= newest pipeline run 03:05:01Z.
Concurrent interactive session still active (Trevor + Claude Code; `origin/main` `6688a8c7`, prod `403ad6f5` READY).

Health: security **0/0/0/0** · trust **15 metrics / 1 BREACH (known class, at threshold)** · `stalled_pipelines []` · pg_cron **[]** · Sentry **1 issue / 5 events (known, last seen 14h ago)** · Vercel **0 ERROR** · DB 10,241 MB · editions TS 19,464 · sentinel 0.

**Headline: nothing new broke. The 00:10Z DUNE-402 HIGH is CONFIRMED and now has a clock on it — the weekly `sync-topshot-ownership-dune` fires at ~11:40Z today (Mon) into the exhausted quota. But I am DOWNGRADING it HIGH -> MED on two measured corrections the prior tick could not make: the sync has a documented cached-read fallback and never deletes rows, and a second non-Dune source already backs part of the table. Worst case is frozen staleness, not breakage or wrong data. Separately: two of the prior ticks' HIGHs are now SUPERSEDED by self-recovery, and two same-day ships verified PASS.**

---

## MED — DUNE-QUOTA-402: WEEKLY OWNERSHIP SYNC IS DUE ~11:40Z TODAY INTO AN EXHAUSTED QUOTA (sharpens + downgrades the 00:10Z HIGH; do NOT double-log)

**Source:** `pipeline_runs` (both Dune routes), `topshot_ownership` source split, ledger L2085 + L1452.

Confirmed exhausted — every Dune request now 402s, on both saved queries:

| pipeline | last ok | since then |
|---|---|---|
| `sales-ingest-dune` (`11 */2 * * *`) | 22:11Z (inserted 865) | 00:11Z **402**, 02:11Z **402** |
| `sales-seller-recovery-dune` (`47 * * * *`) | 20:47Z (22,925 rows) | 21:47Z QUERY_STATE_FAILED, 22:47Z partial-then-402, 23:47Z–02:47Z **402** |

The 00:10Z tick named the right risk but could not date it or bound it. Three measured corrections:

**1. It has a due time: ~11:40Z today.** `sync-topshot-ownership-dune` has **no repo scheduler entry** (not in `vercel.json`, not in `.github/workflows/`) — it is cron-job.org-triggered, weekly `40 11 * * 1` per ledger L2085. Last Dune-sourced write was **2026-07-13 11:44Z, a Monday**. Today is Monday 07-20 => next fire ~**11:40Z, ~8.5h from this tick**.

**2. The Dune leg is ALREADY 6.7 days stale, and a previously-predicted recovery silently did not happen.** Ledger L1452 (07-13) recorded the weekly tick failing on a pool timeout and said Top Owners would be *"stale until the 07-19 tick unless CC retriggers"*. Measured now — the 07-19 tick did **not** restore it:

| source | rows | editions | last observed |
|---|---|---|---|
| `dune` | 157,114 | 2,210 | **2026-07-13 11:44Z (6.7d)** |
| `onchain_walk` | 31,702 | 1,637 | 2026-07-19 13:30Z (fresh) |

**3. Severity is MED, not HIGH — two things the prior tick assumed wrong.** (a) It said Top Owners has *"no other data source"*: `ownership-onchain-walk` is a second, non-Dune lane, ran ok 07-19 13:30Z, and is what last touched the table — partial mitigation (17% of rows but 74% of the edition count), not full redundancy. (b) More decisively, ledger L2085 documents the route's **graceful fallback: any refresh failure falls through to the cached read** — and on failure it writes nothing rather than truncating. So a 402 at 11:40Z yields *continued staleness*, not an empty or wrong leaderboard. **No user-visible break, no data loss, no wrong data.**

**Risk read:** LOW to act, MED to leave. Not corrupting anything; both sales routes park their cursors on failure (`windows_done: 0`, cursors unchanged at `2025-09-03` / `2026-01-22`), so no partial or duplicate writes. The cost of leaving it is a public surface quietly drifting toward 2+ weeks stale.

**Suggested action (operator — external billing surface, NOT autonomously shippable):**
1. Check the Dune plan's datapoint allowance and **when the billing cycle resets** — that single fact determines whether 11:40Z fails at all. Unknowable from inside the platform.
2. Re-cadence the two backfills off hourly/2-hourly. They drain a fixed backlog, not steady-state ingest; a daily tick still drains, just slower, and they are what consumed the shared pool in ~9h.
3. Give `sync-topshot-ownership-dune` a reserved budget or its own key so a backfill can never starve the one user-facing consumer.

---

## SUPERSEDED — ALLDAY-LOCK-REFRESH SELF-RECOVERED (closes the 21:15Z HIGH and the 00:10Z UPDATE; do not carry either forward)

The 00:10Z tick logged "no 23:23Z row at all" and left the 21:15Z HIGH standing. It recovered on its own the next tick and has been clean since:

| tick (Z) | 19:23 | 20:23 | 21:23 | 22:23 | 23:23 | **00:23** | **01:23** | **02:23** |
|---|---|---|---|---|---|---|---|---|
| ok | NO | NO | yes | NO | *(no row)* | **yes** | **yes** | **yes** |
| wallets | 0 | 0 | 45/60 | 0 | — | **60/60** | 43/60 | **60/60** |
| lock_flips | — | — | 706 | — | — | **2,111** | 1,397 | **1,646** |

Every failure was `wallet fetch: canceling statement due to statement timeout` at `stage: wallet_fetch` — a **server-side read timeout, not a Cadence/GQL fault** — and all of them sit inside the 19:00–22:00Z contention window. It recovered at exactly the hour the Dune lane stopped holding long connections, with no code change. **This is a second independent confirmation of the 00:10Z contention mechanism** and it means the queued lock-refresh work was aimed partly at a symptom.

**Residual real issue (LOW, unchanged):** the 270s soft deadline still truncates a tick to 43/60 wallets under load. That is a capacity question, worth re-measuring only *after* the Dune re-cadence — not a break.

---

## CONFIRMED — THE CONTENTION WAVE BROKE AND HAS STAYED BROKEN (5h of evidence)

Extending the 00:10Z table forward. Fails per hour, all pipelines:

| hour (Z) | 17 | 18 | 19 | 20 | 21 | **22** | **23** | **00** | **01** | **02** |
|---|---|---|---|---|---|---|---|---|---|---|
| fails | 7 | 12 | 11 | 11 | 18 | **2** | **2** | **2** | **5** | **2** |

From 22:00Z on, the fails are **almost entirely the two Dune routes failing fast on 402** — i.e. the residue is the cause, not new victims. `sales-counterparty-backfill` latest run **03:05Z ok** (231 ok vs 56 fail over 24h, with the fails concentrated in the pre-22Z window). `lock-check-batch` last failed 21:38Z; `compute-topshot-pack-ev` 462 ok vs 7 fail. The 00:10Z causal claim holds up over five further hours.

---

## POST-SHIP WATCH — 2 PASS, 0 reverts

- **DUNE-SELLER-RECOVERY-EXECUTE-400 fix (ledger L410, params retyped `date`->`text`) — VERIFIED WORKING; recommend CLOSING the queued item.** After the fix landed (~13:47Z) the pipeline ran **8 consecutive ok ticks, 13:47Z->20:47Z, writing ~128,800 seller rows** (22,925 / 13,922 / 7,893 / 24,640 / 32,582 / 26,844 / 2 / 0). The `execute HTTP 400` strings still visible in the 24h error aggregate are **pre-fix** (09:47Z–12:47Z ticks only) — stale, not recurring. The pipeline's only remaining problem is the shared quota above, which is a different item.
- **`sales-ingest-dune` guards — VERIFIED ON REAL DATA (this is the eyeball CLAUDE.md explicitly asked for before letting it drain).** `sales_ingest_recovered`: **62,628 audit rows, 1,276 actual inserts**, min/max `sold_at` **2025-09-03 -> 2025-12-30**, **era violations (>= 2026-01-01): 0**, price range **$1.00–$18,888** (sane for TS). Guards rejecting the overwhelming majority exactly as designed (`skipped_existing` 35,099 + 38,435; `skipped_unresolved` 72,391 + 9,562; `skipped_multimoment` 10 + 4; `map_skipped` 0). FMV undisturbed: `fmv_sanity_flags` **0**, `topshot_fmv_stale_hours` **0.3**, TS HIGH+MED **3,368** vs the 07-19 baseline 3,374 (flat). **This lane is healthy.**

---

## LOW — DOC DRIFT: CLAUDE.md STILL DESCRIBES BOTH DUNE PIPELINES AS INERT

CLAUDE.md says `sales-ingest-dune` is *"still INERT until a real v13-POST rebuild"* and lists `sync-sales-seller-recovery-dune` as needing `DUNE_SALES_SELLER_QUERY_ID`. **Both are armed and were writing today** — `sales-ingest-dune` flipped from `{"skipped":"dune_not_configured"}` at 18:11Z to a live run at 20:11Z, so the rebuild did land. A future session reading CLAUDE.md cold would wrongly conclude the Dune lane is dormant and could mis-diagnose the 402s. One-line correction to each mention. Zero risk.

---

## NOTED, NOT RE-LOGGED — known self-healing class back at threshold

`topshot_impossible_parallel_serials` = **3 / breach 3** (exactly at, not above), having been closed to 0 on 07-19 as wave 6. Same self-healing WNBA/Base `::`-cataloging class: floor-seeded circ below a real observed sale serial. The three are identified, so the next sanctioned circ floor-raise has them ready:

| external_id | name | circ | max sale serial |
|---|---|---|---|
| `218:8780::16` | Nikola Jović — Base Set | 99 | 652 |
| `218:8787::16` | Ariel Hukporti — Base Set | 99 | 1,680 |
| `258:8687::16` | Carla Leite — WNBA Base Set | 99 | 2,824 |

Per the ledger this is an interactive/Trevor circ floor-raise, **not** an autonomous ship. Not filed as a new candidate.

---

## Artifact validation — DEPENDENCY-LEVEL ONLY THIS TICK (method note)

`list_artifacts` works (**17 artifacts enumerated**), but the artifact HTML at `C:\Users\TDill\Claude\Artifacts` is **not reachable from this session** — `Grep` returns "outside this session's connected folders", so the payload SQL could not be extracted and replayed. This differs from the 07-19 night pass, which read it successfully via `Read`/`Grep`; the mount set is evidently session-dependent, so that "gap unblocked" note should not be treated as universal. I did **not** request a directory mount (non-interactive run).

Substituted a dependency-existence check over the artifact-backing objects, which catches the renamed/dropped-object break class that this section exists to guard: **20/20 resolve** — 16 relations (`v_rpc_trust_health`, `pack_table_rows`, `pack_ev_latest`, `mv_pack_ev_latest`, `fmv_current`, `panini_coverage_summary`, `candy_best_offers`, `candy_offers`, `cross_collection_cohort_mat`, `cross_collection_ts_set_overlap_mat`, `pipeline_cadence_watchlist`, `funnel_events`, `support_conversations`, `trophy_moments`, `edition_offers`, `wallet_moments_cache`) + 4 functions (`rpc_ops_snapshot`, `get_pipeline_alerts`, `detect_stalled_pipelines`, `health_check`). No schema drift from today's heavy migration wave. Full payload replay should be re-attempted next tick from a session that can read the artifact dir.

---

## Drift vs `metrics-latest.json` (07-19 08:03Z baseline) — all benign

| metric | baseline | now | delta |
|---|---|---|---|
| TS FMV HIGH+MED | 3,374 | 3,368 | −6 (flat) |
| editions TS | 19,451 | 19,464 | +13 (`::` cataloging) |
| editions candy_mlb | 125 | 125 | flat |
| DB size | 10,054 MB | 10,241 MB | +187 MB |
| sentinel TS-UUID 48h | 0 | 0 | flat |
| unmapped backlog | 25 | 25 | flat |
| edition_integrity_flags | 5 | 5 | flat |
| pack_ev_board_max_stale_days | 0.48 | 0.54 | flat |
| impossible_parallel | 0 | 3 | +3 (known class, above) |

DB +187 MB over ~19h is consistent with the ~129k seller-recovery rows + 62.6k ingest-audit rows landed today — accounted for, not unexplained growth. Relevant only because the instance is IOPS-constrained.

## Sentry

1 unresolved issue in 24h: `JAVASCRIPT-NEXTJS-1Y — team detail unavailable: canceling statement due to statement timeout` (`GET /[collection]/team/[slug]`, 5 events, 1 user, **last seen 14h ago** = inside the contention window, none since it broke). Known contention class; the 07-19 CC session already concluded the `get_team_detail` optimization is **don't-ship** (~10% only). Not spiking, not new, no action.
