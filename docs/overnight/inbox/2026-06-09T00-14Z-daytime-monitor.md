# Daytime monitor — 2026-06-09T00:14Z

Read-only sweep. **One NEW high-priority candidate: a cross-character FMV-leak regression from tonight's ~23:00Z Pinnacle concierge ship (`04011b3`) — the smoke drift guard `NEXTJS-14` re-opened.** AND the 22:21Z `PINBACKUP-RLS` security regression is STILL live and now firing on a SECOND smoke surface (escalation of the same item, not new). Otherwise GREEN: `detect_stalled_pipelines() = []`, sentinel TS-UUID-48h **0**, `check_secdef_anon_execute_violations()`/`get_pipeline_alerts()` `[]`, 20/20 deploys READY (0 ERROR), FMV flat, all key artifact backing relations resolve. Lock stale (released 08:17Z) → inbox commit permitted.

## New candidate

### PINFMV-DRIFT-14 — Pinnacle cross-character FMV leak reintroduced by the ~23:00Z concierge ship (HIGH-PRIORITY · regression · CC/Trevor-owned — NOT night-pass auto-ship/auto-revert)
- **Source:** Sentry `JAVASCRIPT-NEXTJS-14` "smoke test failed: Pinnacle FMV not borrowed across characters (drift guard)" — **substatus `regressed`**, latest event **2026-06-08T23:02:18Z**, **release `04011b32...`** (the 23:00Z ship), endpoint tag `lib:searchPinnacleDeals`, `POST /api/smoke-test`. Was "confirmed GONE" at the 08:15Z night-pass baseline (the 06-07 `3364d4e` re-key fixed it) → **re-opened by tonight's ship** (clean attribution: the guard was stable across ~10 intervening releases and broke only on `04011b3`).
- **Attribution:** commits `04011b3` ("fix(pinnacle): concierge deals off dead-Flowty cache; retire stale ASK path") + docs `2bd52ff`, deployed ~23:00Z (`dpl_3KXiX4` / `dpl_3A9LQQ`, both READY). The ship re-pointed `searchPinnacleDeals` + `searchPinnacleByName` to read ask **and** FMV from `pinnacle_catalog` (the per-render spine) instead of the frozen Flowty `pinnacle_cached_listings`. The drift guard (re-keyed by `3364d4e` to validate each priced deal row against `pinnacle_catalog` on a trimmed `(character,set,variant)` triple) now catches **1 leaked row**.
- **Concrete repro (event extra):** `detail = "FMV leaked on 1 row(s): Goofy / Walt Disney Animation Studios • Mickey & Friends Vol.1 / Standard fmv=$1.247"`, `expected = no-fmv-leak`. So `searchPinnacleDeals` is attaching a $1.247 FMV to a Goofy/Mickey&Friends Vol.1/Standard pin that doesn't match that exact triple's catalog FMV — a cross-character borrow, almost certainly on one ambiguous/colliding trimmed triple key.
- **Blast radius:** narrow but real. (a) a genuine 1-row cross-character FMV display bug on the concierge deals output (an intelligence-product surface showing a wrong FMV for one pin); (b) deterministic on current data → re-trips the smoke test each run while the leaked row persists, re-polluting `NEXTJS-14`. Not an outage; no other surface confirmed affected. Observed once so far (23:02Z) — CC should confirm whether it's a single ambiguous-triple row or systematic.
- **Risk read:** the FIX is concierge / Pinnacle-FMV route logic — **off-limits to the night pass** (focus.md item 2 "DO NOT touch Pinnacle FMV functions/views/routes"; concierge route logic off-limits per CLAUDE.md) and `04011b3` is <24h fresh + CC/Trevor-owned. **Log + flag for CC/Trevor; do NOT auto-fix and do NOT auto-revert.**
- **Suggested action (CC/Trevor):** in `searchPinnacleDeals`/`searchPinnacleByName`, confirm FMV is joined to `pinnacle_catalog` on the SAME render row as the ask (`render_id`), not re-derived via a `(character,set,variant)` triple that can collapse two characters' renders. Quick data probe: `SELECT render_id, fmv_usd, floor_ask FROM pinnacle_catalog WHERE character_name='Goofy' AND set_name ILIKE '%Mickey & Friends Vol.1%' AND variant ILIKE 'Standard'` — if >1 render shares the trimmed triple, that's the collision. Then re-resolve `NEXTJS-14` with regression arming. Last-resort revert: `git revert 04011b3` (but that re-introduces the dead-Flowty fabricated-discount bug the ship fixed — prefer the keying fix).

## Escalation — NOT a new item (already logged 2026-06-08T22:21Z as PINBACKUP-RLS; carrying the new facts)

### PINBACKUP-RLS still live + now on a 2nd smoke surface (HIGH-PRIORITY · security · persisted ~3h+)
- Re-confirmed this sweep: `pinnacle_fmv_snapshots_backup_20260608` is STILL the platform's only RLS-off / anon-write base table; `check_public_security_invariants()` = **9 rows** (1 `rls_off_base_table` + 8 `anon_write_base_table`). Unchanged since 22:21Z — not yet hardened or dropped.
- **New since 22:21Z:** it now trips BOTH smoke surfaces — `analytics-smoke` `ok=false` / `severity=fail` / **`telegram_sent=true`** on every tick through **23:43Z** (so the Telegram security alert has fired ~6+ more times, ~30-min cadence, ~2.5h+ continuous), AND the main `/api/smoke-test` security assertion **`NEXTJS-1C`** ("public base tables: RLS on + no anon write") last-seen ~23:0xZ (the 22:21Z file predicted this). Two independent smoke surfaces now red on the same backup table.
- Full detail, revert paths, and the DROP-vs-REVOKE options are in `docs/overnight/inbox/2026-06-08T22-21Z-daytime-monitor.md` — **night pass: treat as the SAME item, do not double-count.** Hardening (`REVOKE … FROM anon, authenticated;` + `ENABLE ROW LEVEL SECURITY`) or `DROP TABLE` (Trevor's call — it's a 1-day rollback net) both stop the alert and return `check_public_security_invariants()` to `[]`. Caveat unchanged: it's a backup of a CC/Trevor-owned <24h retirement, but a REVOKE/ENABLE-RLS on a *backup* table changes no FMV logic / displayed price / live reader.

## Corroborated / already-owned — checked, not new
- **Listing-resolution-failure "spike" at 00:05Z (44 new) = the known `NEXTJS-15` / Q4 gated residual + the :00 cron storm, NOT ship-attributable.** All 44 are Pinnacle (`7dd9dd11`) from a single 00:05:08Z `pinnacle-listings-indexer` tick: **37 `cadence_capped`** (by-design per-tick Cadence borrow back-pressure, retried next tick — benign), 4 `edition_key_unmapped` (the genuinely-unmappable Q4 tail), 3 `cadence_borrow_failed` (transient storm). The 23:00Z ship retired `pinnacle-listing-cache` but did NOT touch `pinnacle-listings-indexer`. `NEXTJS-15` firing ~5 min ago is the 37 `cadence_capped` crossing Q4's `>25 new/tick` spike gate during the storm — a benign false-positive, not a regression. **Minor refinement for whoever next touches Q4:** exclude `cadence_capped` from the spike gate (it's back-pressure, not a resolution failure). Not a standalone candidate.
- **23:00Z Pinnacle ship — the now-load-bearing on-chain ASK path is healthy.** With `pinnacle-listing-cache` retired to an inert no-op and ASK consolidated onto `pinnacle-listings-reconcile` (on-chain) + `pinnacle_catalog`: reconcile 6/6 ok in 90m (last 00:09Z), listings-indexer 5/5 (last 00:05Z), nft-resolver 17/17 (last 00:06Z), listing-cache still logging ok (inert). `pinnacle_catalog` 1,797 priced, `pinnacle_editions` 290 with ask. No ASK-freshness regression from the ship — only PINFMV-DRIFT-14 above.
- **Evening DB-saturation window 18:50–20:54Z = the known I1/DBSAT class, fully recovered** (already logged 22:21Z). The trailing-6h pipeline fails cluster there (`upstream request timeout` / `statement timeout` / `connection pool`: wmc-fmv-populate 15, compute-topshot-pack-ev 9, pinnacle-nft-resolver 8, pinnacle-listings-reconcile 6, topshot-moments-hydrator 5 = N2, plus the broad indexer set), all self-recovered; the current 00:xxZ hour is clean apart from the :00 storm tick. The `analytics-smoke` 10 fails in the window are dominated by the PINBACKUP-RLS security check (above), not saturation.
- **Sentry smoke transients `NEXTJS-1H` (edition Recent-Sales) + `1J` (pack-dist Sales-History) + `1E` (detect_stalled smoke)** — last seen ~3h ago (saturation tail), quiet since. 1H/1J already queued under SMOKE-EDITION-TIMEOUT. Mark resolved if quiet 24h.

## Clean (no action)
`detect_stalled_pipelines()` `[]`; `check_secdef_anon_execute_violations()` `[]`; `get_pipeline_alerts()` `[]`; sentinel TS-UUID-48h **0** (total 6,406 fossils, flat). 20/20 recent prod deploys READY, 0 ERROR (today's full wave incl. the ~23:00Z `04011b3`/`2bd52ff` concierge ship + the earlier legacy-FMV retirement, chain-two address foundation, HybridCustody verify, FCL proof-bind — all READY). FMV flat: TS editions 15,542 / AllDay 6,191; unmapped_sales 183. DB 6,636 MB (+3 since 22:21Z, creep settling). Artifact backing relations validated: `v_tracked_wallet_fmv_confidence` 20 rows (no 57014), `topshot_squeeze_board` 3,139, `pinnacle_scarcity_board` 2,079, `cross_collection_deals_board` 324, `pinnacle_catalog` priced 1,797. `rpc-security-drift` correctly surfaces PINBACKUP-RLS (its catalog SQL = the security checks above) — working as intended, not broken.
