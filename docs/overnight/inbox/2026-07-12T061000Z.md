# Daytime monitor — 2026-07-12T06:10Z (~23:10 PDT Jul 11, overnight-edge tick)

Platform GREEN with the known 07-11 pack-opens-storm contention aftermath ongoing. No clock skew (DB now() 06:09Z ≈ newest sale 06:08Z / fmv 06:07Z — ingest FRESH). Security 0/0/0/0 (invariants [] · secdef_anon [] · rls_off 0). Vercel prod latest READY = `f2c6f65` (concierge error-classifier extraction); the several ERROR/CANCELED deploys in the list are superseded intermediates from the heavy 07-11 session, none is current prod. Lock RELEASED by the 08:03Z night pass. **Inbox written to mount, push unavailable (sandbox bash could not create the clone this run — `useradd`/workspace-boot failure; wrote onto the mounted tree so the night pass finds it locally).**

## NEW candidate (1) — CCM-STEP1-FAILED / cross_collection_cohort_mat STALE ~26h (HIGH-PRIORITY per §1a; contention casualty)
- **Source:** `check_pgcron_recent_failures()` — `rpc-ccm-step1` latest_status=failed, last_run 2026-07-12 04:10Z, fails_in_window 1 / runs 1, `last_fail_message: ERROR: canceling statement due to statement timeout … INSERT INTO public.cross_collection_cohort_mat (…)`. Corroborated by the §1a freshness check: `cross_collection_cohort_mat` max(computed_at)=**2026-07-11 04:10Z** (≈26h stale), count 154 (stable); `cross_collection_ts_set_overlap_mat` max=2026-07-12 04:25Z (**step2 succeeded** — classic fresh-table-2 / stale-table-1 = step1 specifically failed).
- **What:** the daily heavy cross-collection cohort refresh (pg_cron `rpc-ccm-step1`, 04:10 UTC) timed out on its single run today. It fires once/day, so `cross_collection_cohort_mat` (feeds the rpc-cross-collection artifact + cohort insights) will stay pinned at yesterday's data until the next 04:10Z tick unless a one-shot re-runs it. overlap_mat is current.
- **Why now:** almost certainly a casualty of the 07-11 pack-opens-storm IO contention (pack_rips grew ~170k→3.6M rows; the 04:10Z window sat inside the deep overnight-contention band that also timed out the pack-EV / rollup / remap / dedup jobs below). The INSERT is a heavy full-cohort scan competing with backfill+autovacuum IO. Not a schema/column break — the query body is intact and step2 ran.
- **Risk read:** LOW-MED. No user-facing FMV/sales harm; only the cross-collection cohort surface is a day stale. Self-heals at tomorrow 04:10Z **if** contention has settled (pack-opens backfills are draining / retiring), but a single retry now would restore freshness immediately.
- **Suggested action (night pass / Trevor):** if the pass runs before contention clears, drop a self-cleaning pg_cron one-shot for JUST step1 that ends in `cron.unschedule` of itself (per the §1a recovery recipe) — e.g. schedule a one-off that runs the step1 refresh body once then unschedules; note a FAILED one-shot does NOT self-unschedule and must be cleaned next day. Alternatively TRACK and let the 04:10Z daily tick self-heal now that the pack-opens storm is winding down (job 52/53 retired on done). Verify after: `cross_collection_cohort_mat` max(computed_at) within 26h.

## Carried / known — no new action
- **DAYTIME/OVERNIGHT-CONTENTION pg_cron cluster is BROADER this deep-contention tick** (06:xxZ) but same family + root (pack-opens storm IO). `check_pgcron_recent_failures()`: `rpc-backfill-historical-pack-ev` (15/144, job startup timeout — jobid 43 family), `rpc-allday-rollup-rip-value` (8/48, statement timeout — measured NON-structural 07-11 CC, IO-contention/autovacuum-settling, self-heals), `rpc-backfill-pack-pool` (2/288, startup timeout), `rpc-refresh-mv-pack-ev-latest` (2/144, startup timeout), `rpc-recover-null-serial-sales` (2/96, statement timeout), `rpc-roll-pack-ask-hourly-low` (2/96, statement timeout on the 7-day DELETE), `rpc-remap-misattributed-sales` (2/4, statement timeout — DAYTIME-CONTENTION), `rpc-allday-badge-low-ask-refresh` (1/48, statement timeout). All statement/startup timeouts inside the ~00:00–06:00Z band; each has healthy runs outside it (e.g. pack-pool 286/288 ok). "job startup timeout" = pg_cron background-worker saturation under the storm, not code. Not a growing-table cliff. Already logged 21:16Z/00:25Z.
- **TOPSHOT-IMPOSSIBLE-PARALLEL-SERIALS-BREACH** — already logged at 21:16Z (cleared 11→0 by the Cowork wave-2 circ-floor-raise) and re-logged re-accumulated 11/3 at 00:25Z (self-healing `::`-cataloging straggler class, per-parallel circ backfill lagging the cataloging inflow). Not re-logging. **Live re-read this tick = 1/3 (status ok) — CLEARED**: the per-parallel circ backfill drained it from 11 → 1, confirming the self-healing class. The 00:25Z candidate can be closed as self-resolved.
- **TOPSHOT-MOMENTS-HYDRATOR-GETMINTEDMOMENT-ERRORS** — CC fix `c1ba51e` committed local, awaits CF worker `wrangler deploy` (operator). Self-limiting. Carried.

## Drift (headline)
- editions TS **19,210** (+84 since 15:10Z — ongoing `::` subedition cataloging, benign; sentinel/UUID-leak was 0 at 00:25Z) / AllDay 6,190 (flat). FMV TS H+M **5,216** (band-normal; 5,232 @ 07-11 overnight, 5,202 @ 00:25Z — churn, not sliding).
- DB **11,148 MB** (+129 since 00:25Z's 11,019; +2,054 vs the 07-11 overnight 9,094 baseline) — continued fill from the TopShot+AllDay historical pack-opens backfills (pack_rips ~3.6M rows). Benign/storm-attributable but **WATCH the growth tail on PRO Micro** — the pack-opens backfills are draining/retiring, so growth should flatten.

## Notes
- DB under real contention this tick: `rpc_ops_snapshot()`, `detect_stalled_pipelines()`, and `v_rpc_trust_health` all timed out on multiple attempts (nighttime storm window). Security invariants, pg_cron failures, cross-collection freshness, editions/FMV/DB size, and Vercel deploys all captured on lighter targeted reads. Artifact payload-query validation SKIPPED this tick to avoid adding load under active contention.
