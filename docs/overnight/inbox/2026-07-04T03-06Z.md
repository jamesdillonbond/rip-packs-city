# Daytime monitor inbox — 2026-07-04T03:06Z (~20:06 PDT tick)

Platform **GREEN**. security 0/0/0/0 · trust **16/16 ok** (breaches []) · `detect_stalled_pipelines()` [] · `check_pgcron_recent_failures()` [] · sentinel TS-UUID-48h **0** · editions FLAT (TS 17,490 / AllDay 6,191 / Golazos 581 / UFC 518) · FMV fresh (topshot_fmv_stale 0.3h, allday 0.2h, ufc 0.1h, golazos 0, fmv_sanity 0) · DB **7,766 MB** · prod deploy READY + serving. Nothing broken. 3 sharpening notes below (no new fixable defect).

Not the 8am tick → 1a daily extras (full trust read / cross-collection pg_cron verify) skipped by design; trust vector still read via rpc_ops_snapshot (16/16 ok).

## Candidates / sharpened watches

### 1. VERCEL-ROLLING-RELEASE + PAUSE FINDING — RESOLVED by operator (Trevor); update the post-ship-watch target
- **Source:** Vercel `list_deployments` + git origin/main. The 07-04 QA-pt3 ledger entry flagged Rolling-Releases leaving `71b1e5b` STAGED/BLOCKED with prod stuck on `6c0f9cd`, plus a transient `DEPLOYMENT_PAUSED` (VERCEL cost family). **Both addressed since the 00:13Z tick:** Trevor **raised the Vercel spend cap** and pushed `bf63a414` ("trigger fresh prod build post spend-cap raise to promote Bug 8 + Bug 12") -> `dpl_FAEh3Dy8AqDh9qynPszDPc9jHKjm` **READY and serving** (it is the `lastDeployment` on runtime errors stamped 03:08Z = live). A newer commit `eb44ac04` (pinnacle-nft-resolver 30s + wmc-fmv-populate lock fix) is `dpl_C8dX...` **BUILDING** as of this tick; origin/main HEAD == `eb44ac04`.
- **Risk read:** none live. `71b1e5b` (BLOCKED) + the 2 ERROR smoke-deploys (`ded5e7c3`, `8080fce8`) are SUPERSEDED, not serving; `bf63a414` carries the same Bug 8/12 fixes `71b1e5b` did. The DEPLOYMENT_PAUSED / intermittent-504 hypothesis is now mitigated by the spend-cap raise.
- **Suggested action (night pass):** point the perf-wave post-ship watch at **`bf63a414` -> `eb44ac04`**, NOT `def899f1`/`71b1e5b`. Confirm `eb44ac04` reaches READY (it fixes the pinnacle-nft-resolver 16-fail + wmc-fmv-populate 19-fail transient classes seen this tick — verify those fail counts DROP after it promotes). Treat the Rolling-Release/pause finding as resolved unless PAUSED recurs.

### 2. DB-GROWTH-DRIVER-UPDATE — LOW · watch · the baseline attribution is now stale
- **Source:** `sales` source breakdown (24h). DB **7,766 MB** (+208 vs 00:13Z 7,558; +342 vs 07-03 night-pass 7,424). The dominant driver is now **`ts_history_backfill_v1` = 87,939 sales rows/24h** (latest ingest 07-03 23:38Z), NOT `allday_studio_history_v1` (14,775/24h, latest 07-04 00:57Z) which the night-pass metrics-latest.json still names as the growth cause. Other sources normal (onchain 7,756, onchain_dapper_v2 631, offer_fill 544, topshot_gql 212).
- **Risk read:** benign — `ts_history_backfill_v1` is the tracked TopShot deep-history backfill draining as designed (external-id keyed; editions FLAT, sentinel 0, fmv_sanity 0, no unmapped spill). But at ~200 MB / 3h during active backfill windows on Pro Micro, disk headroom is worth a passive eye until the drain finishes.
- **Suggested action:** night pass update the metrics-latest.json growth note to name `ts_history_backfill_v1` as the primary driver; no fix. Escalate only if growth continues after the backfill sources go dormant (would indicate a non-backfill leak).

### 3. DEF899F1-PERF-WAVE — two residual timeout classes on the superseded def899f1 (folds into the existing 00:13Z post-ship-watch candidate)
- **Source:** Vercel `get_runtime_errors` (6h). The bulk of the pack/market/team DBSAT timeout classes last-seen on **superseded** deploys (`dpl_EAPkJ`/`dpl_3EHBu7`/`dpl_Gvtsnni6` def899f1) and should drop on `bf63a414`. Two small residuals attributed to `def899f1` worth a targeted look:
  - **`[api/packs] calibrated merge` statement timeout** (3x, 07-03 23:53Z->07-04 00:38Z). This is the calibrated-EV **merge** leg of `/api/packs`, a DIFFERENT path than the `pack_table_rows`->`mv_pack_ev_latest` leg Bug 3 fixed — so def899f1 did not cover it. Watch whether it grows.
  - **`/api/public/ipfs-media/[cid]` "no initial response within 25s" + `[public] / status=504`** (3+3, tight 00:34->00:51Z window). Residual slow-IPFS-gateway tail on the P3 UFC ipfs-media proxy (25s fn cap). Minor; not a new error class.
- **Risk read:** both LOW, low volume, on a now-superseded deploy. No user-facing outage; the mainline perf fixes (recent-sales 22s->0.7ms, sets 114s->143ms, allday market 66s->62ms, packs pack_table_rows 31s->3s) are the durable wins.
- **Suggested action:** night pass verify these two do NOT climb on `bf63a414`; if `[api/packs] calibrated merge` persists it's a separate calibrated-merge optimization (own item), not a def899f1 regression.

## Not re-raised (known / carried / dispositioned)
- `classify-acquisitions-multicollection` — latest tick FAIL 03:06Z (`nfl_all_day: statement timeout`), 4 fails/24h = the tracked **CLASSIFY-ACQ-ALLDAY-STATEMENT-TIMEOUT** (night-count 3, flaps-not-stalls, do-NOT-close). Still flapping as `allday_studio_history_v1` fills; LOW (moment_acquisitions cost-basis, not user-facing).
- `fmv-recalc` 11 fails/24h (latest ok 02:48Z) = the 00:13Z **FMV-RECALC-EDITION-FETCH-TIMEOUT-CREEP** watch (edition_page_fetch ~30s partial-flap; topshot_fmv_stale 0.3h GREEN, retries next tick). TS FMV H+M read **4,516** (vs 4,779 at 00:13Z) = overnight diurnal confidence rebucket (HIGH/MED->LOW in low-trading hours), NOT a regression — freshness 0.3h + fmv_sanity 0 both green. Folds into the same DB-growth-contention watch.
- wmc-fmv-populate (19) / pinnacle-nft-resolver (16) / compute-topshot-pack-ev (12) 24h fails — ALL latest-run ok=true (transient pool contention); `eb44ac04` (building) directly targets the pinnacle-nft-resolver + wmc-fmv-populate classes.
- `/api/og/insights/{deals,market}%5C` MODULE_NOT_FOUND (2+2, 00:16Z) = the dispositioned **OG-BACKSLASH-500** malformed-crawler-URL class (trailing `%5C`), benign, trending-close. Not a real route.
- ufc_sales resolving_editions INFO (35/24h) = benign known bridge-pending alert.

## Artifact validation note
`list_artifacts` = 15 present (none missing/broken at manifest level). The artifact HTML lives on a Windows OneDrive path NOT mounted into the sandbox this run, so per-artifact embedded payload queries were not individually re-run. Backing validated indirectly: `rpc_ops_snapshot()` (rpc-live-health's core) GREEN with every sub-object returning; today's CC wave was **additive-only** (new matviews `mv_pack_ev_latest` / `mv_topshot_set_play_catalog`, new indexes, an added `active_listings` key + new `get_edition_offers` — no view/RPC dropped or renamed), so no artifact query can have been broken; `check_pgcron_recent_failures()` [] confirms the 2 new matview-refresh crons are healthy.
