# Daytime monitor candidates — 2026-07-11T03:08Z (evening tick, ~20:08 PDT Jul 10)

Platform GREEN. No skew (shell 03:05Z ≈ DB now() 03:06Z ≈ newest sale/fmv ~03:07Z). Push available. Lock present but STALE (Jul 10 01:08, >24h old) → proceeded with inbox write. Security 0/0/0/0; trust 16/16 ok, breaches []; sentinel TS-UUID-48h 0; Sentry 0 unresolved/24h; Vercel prod e2f3922 (dpl_9kd98) READY, no ERROR-state deploy (later deploys CANCELED = superseded-concurrent). All rpc-live-health artifact-backing reads succeed (no schema-break from today's ships).

## NEW candidate (1, LOW — night-pass safe, DB-only)

### UFC-SALES-INDEXER-WATCHLIST-STALE-FROZEN-MARKET
- **Title:** Relax/retire the ufc-sales-indexer cadence-watchlist row — it chronically false-trips the 90-min stall threshold because the Flow UFC market is permanently frozen (migrating to Aptos).
- **Source:** detect_stalled_pipelines() + rpc_ops_snapshot().pipeline_alerts this run — ufc-sales-indexer silent 117 min vs max_silent_minutes=90 (medium). pipeline_cadence_watchlist: ufc-sales-indexer still is_active=true, max_silent_minutes=90.
- **Context:** CC already applied the frozen-market treatment to the SIBLING ufc-listings-indexer (set is_active=false, commit a54cb60, 07-11) but left ufc-sales-indexer at 90 min. UFC Flow sales now trickle at ~10/24h (ledger: sales survive only on the GHA sales-indexers-backstop.yml @18,48), so a 90-min absence threshold is guaranteed to trip most sweeps. Pure monitor/night-pass noise, not a real outage.
- **Risk read:** LOW / DB-only, single pipeline_cadence_watchlist row. No ingest/route/FMV touch. Fully reversible.
- **Suggested action (night pass to choose lever):** either (a) mirror the listings treatment — UPDATE pipeline_cadence_watchlist SET is_active=false WHERE pipeline='ufc-sales-indexer'; (loses absence detection on a frozen market — acceptable, sales are GHA-backstopped), or (b) preserve a loose signal — raise max_silent_minutes to ~240 and/or drop severity to info. Revert: restore is_active=true, max_silent_minutes=90, severity='medium'.

## Known / carried — NOT re-logged (already tracked)
- pg_cron 00:23Z timeouts: rpc-remap-misattributed-sales (2 fails) + rpc-allday-ev-corrected-refresh (1 fail) = OVERNIGHT/DAYTIME-CONTENTION family (00:23Z contention window); recover on next tick.
- topshot-moments-hydrator 93 fails/24h = queued TOPSHOT-MOMENTS-HYDRATOR-GETMINTEDMOMENT-ERRORS; verified alternating ok=true (recovered/steady since 01:22Z, NOT stuck at 0-resolution) — WATCH condition not met.
- topshot_pack_reality_top_ev empty (0 rows) = already queued in the 2026-07-10T15:10Z inbox candidate.
- ufc_sales INFO resolving_editions (10/24h) = benign, carried.
