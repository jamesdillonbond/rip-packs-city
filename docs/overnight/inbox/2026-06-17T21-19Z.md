# Daytime monitor — 2026-06-17T21:16Z (14:16 PDT, mid-afternoon tick)

**PLATFORM GREEN, one continuing watch (already logged).** Health sweep + 14/14 active-artifact backing-data validation all clean. **0 NEW candidates.** The only flag is the already-logged BUYERBF-DURATION-CREEP (`detect_stalled` topshot-buyer-backfill 143m>90m) — re-measured per the 18:16Z item's explicit ask: still grinding, last run 18:44Z **ok at 566s (NO invisible >600s death)**, ~140min degraded-but-functional cadence, finite drain, zero user/data impact. New Trevor alerts ship `4addd4b` live + PASS. Daily extras skipped (8am tick ran them: trust 9/9, ccm PASS).

## Health (every-run sweep)
- `detect_stalled_pipelines()` = **topshot-buyer-backfill** (143m silent / thr 90m, medium); `get_pipeline_alerts()` = same (cron_silent). Every other watched pipeline current.
- `pipeline_runs`: 24h **8972 / 3 fails (0.033%)**; 6h **1937 / 2** — both transient & recovered: alerts-dispatch 18:44Z ("deal: statement timeout", 30.4s, lone fail in 24 runs, contention-coincident) + offers-sweep 17:02Z (AllDay GQL, recovered).
- Security **0/0/0/0**: RLS-off base tables NONE; anon/auth-write-on-RLS-off 0 (relkind r/p); `check_public_security_invariants()` []; `check_secdef_anon_execute_violations()` [].
- Sentinel TS-UUID-keyed-editions-48h = **0**.
- Sentry: **2 unresolved**, both smoke transients last seen ~13h ago (NEXTJS-1E sales-indexer-detect_stalled smoke + NEXTJS-A fmv-healthy smoke); **0 new/spiking in 12h**.
- Vercel: prod **`4addd4b` READY** (NEW Trevor ship ~18:28Z — alerts formatter resync to serial-level deal payload + restore buy links; preceded by `ac47129d` public Underpriced #1s board + dispatcher wiring); **0 ERROR** across 20 (CANCELED = superseded docs builds). DB **4776 MB** (+12 vs 18:16Z, benign).
- Drift vs baseline: editions flat (TS 15543 / AllDay 6191 / UFC 446); fmv_sanity_flags **0**; FMV TS HIGH+MED **3228** (night low 2848 -> 8am 3216 -> now 3228, holding); AllDay H+M **818** (improving 768->818); unmapped_sales 49 (43 night, normal churn).

## Recent-ship health (since the 18:16Z tick)
- **`4addd4b` "resync alerts formatter to serial-level deal payload + restore buy links" (~18:28Z) — PASS.** Pairs with `ac47129d` which wired the per-serial `topshot_underpriced_serials_board` in as a 2nd deal source in the dispatcher. Post-ship: **alerts-send 36/36 ok** (last 21:14Z), **alerts-dispatch 23/24 ok** (lone 18:44Z statement-timeout, see Noted), deal board **43 rows**, active_listings **264**, security clean. Additive (lib/alerts/format.ts + sniper-feed tsBuyUrl + public insights page/route), no destructive schema change. Healthy.

## Artifacts (14 active validated; 5 RETIRED tombstones skipped)
Backing data resolves for all: insights_counts 11/11 (squeeze 501 / set_squeeze 239 / pack_reality_top_ev 9 / rookies 61 / market 501 / first_mint 501 / cross_collection 1 / pinnacle_scarcity 501 / offer_spread 501 / deals 501 / trophies 501) . serial-premiums 274 . perfect-mint 9 . underpriced deal board 43 . tracked-fmv-confidence 21 . v_offer_sanity_flags 213 . v_fmv_sanity_flags 0 . cross_collection_cohort_mat 158 . v_rewards_economy 1 / balances 10 . FMV-by-collection (rpc-live-health) resolves. None broken; none repaired (read-only).

## Status updates (already-queued; NO new candidate)
1. **BUYERBF-DURATION-CREEP — re-measured per the 18:16Z ask; STABLE, no escalation.** `topshot-buyer-backfill` last 4 runs 11:24/13:44/16:24/18:44Z, durations 566/555/463/**566s**, all **ok=true**, decode_failed 64/64/49/**65**. So: (a) **no run has crossed the 600s maxDuration ceiling — no invisible death** (the exact class `95c07c5` fixed is holding); (b) duration is still pinned at the **~566s ceiling-approach (>=550s, satisfies the 18:16Z escalation trigger)**; (c) the `detect_stalled` 90m flag is a **known benign artifact** — this pipeline's actual under-load cadence is ~140-160min/run (9min runtime + long idle), tighter than the threshold; the next run was due ~imminently at sweep time. Blast radius LOW (finite historical 2023 buyer backlog; TS sales-indexer fresh; no corruption). **Suggested action unchanged:** night pass — if duration stays >=550s / climbing, trim buyer-backfill `batch_size` 200->100 (CC/Trevor route constant) to restore <600s headroom in the dense 2023 region, OR accept the slow grind and annotate the detect_stalled flag as benign so it stops reading as an alarm. NOT re-raising the DECLINED watchlist-widen.
2. **SERIAL-FMV-MULT-CRON — drifting further (escalating).** `serial_fmv_multipliers` last computed 2026-06-15 23:06Z = **46.2h stale** (37 cells; 43h @18:16Z, 40h @8am). No `refresh-serial-fmv-multipliers` cron. Backs 2 live surfaces (/insights/serial-premiums 274 + underpriced deal board 43 via `serial_fmv_estimate`). Slow-moving population-level tier×circ-band premiums (low per-day risk), but the drift compounds the deal-board "discount" staleness. Operator/Trevor: wire the refresh cron (cron-wiring, not auto-shippable).
3. **FMV-HIGHMED-DIP-WATCH — recovery confirmed & holding; night pass can close.** TS HIGH+MED **3228** (from 2848 night low), fmv_sanity_flags 0, writers fresh — confirms the benign daily-cycle ASK_ONLY reclassification by canonical writers (1.7.0 fmv-recalc + cold-tail-1.0), not ship-attributable. Close once it re-confirms >=~3400 at the daytime peak.
4. **OFFER-SANITY-VIEW-REFINEMENT — slow drift, in scope.** v_offer_sanity_flags **213** (212 @18:16Z, 210 @8am). ~99% sub/serial offers correctly out of edition-grain scope; edition-grain covered by trust leg offer_edition_gap (0/50). Already queued.

## Noted, not findings
- **alerts-dispatch 18:44Z statement timeout** (30.4s, "deal:" leg) is a **lone transient**, not structural: the dispatcher is sub-second on the other 23/24 runs in 12h **even after `ac47129d` added the 2nd (per-serial) deal source** (16:14-18:29Z all 426-782ms; 18:59-20:59Z all sub-second). It coincided with an 18:44Z DB-contention blip (buyer-backfill logged its 566s run in the same minute). If statement-timeouts on the deal-dispatch leg **recur**, the night pass should look at `dispatch_due_deal_alerts` query cost now that it processes two sources — but one contention-coincident fail is not a candidate.
- 4 pipelines quiet >180m earlier today were normal ~3.4h-cadence jobs (not stalls; `detect_stalled` lists only buyer-backfill).
