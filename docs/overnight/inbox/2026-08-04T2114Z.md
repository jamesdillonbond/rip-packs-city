# Daytime monitor inbox — 2026-08-04T21:14Z (14:14 PDT, afternoon tick)

Written by `rpc-daytime-monitor`. READ-ONLY sweep. Git push unavailable this run (sandbox clone failed: "No space left on device" — same sandbox-VM class that blocked the 08-03/08-04 overnight passes and the 15:13Z monitor tick). **This file was written to the mount, push unavailable** — the night pass picks it up locally.

Health baseline: security fully clean (rls_off 0; `check_public_security_invariants()` / `check_secdef_anon_exec_drift()` / `check_anon_write_surface()` all clean); `detect_stalled_pipelines()` `[]`; Vercel latest prod deploy `dpl_AmtD8e1N…` (commit e0d800e, migration recovery) READY, no ERROR deploys (the CANCELED entries are superseded rapid-iteration builds); Sentry 0 new issues in 24h; DB 12,042 MB; editions 26,989; merged health dashboard + FMV-by-collection panel resolve cleanly (all 5 collections populated: TS 19,447 / AllDay 6,190 / Golazos 575 / UFC 465 / Candy 125).

Only **one genuinely-new candidate** this tick — nearly everything flagged today is already logged (15:13Z inbox) or already shipped (ledger).

---

## LOW — `rpc-refresh-topshot-edition-median` intermittent statement-timeout at the high-contention 18:10Z tick

- **Source:** `check_pgcron_recent_failures()` → `rpc-refresh-topshot-edition-median` (pg_cron, runs `00/06/12/18:10` UTC). Failed **2026-08-04 18:10Z** with `canceling statement due to statement timeout` on `REFRESH MATERIALIZED VIEW CONCURRENTLY public.mv_topshot_edition_median_180d`. This is **new since the 15:13Z tick** (that failure had not occurred yet).
- **Pattern (read-only, `cron.job_run_details`):** intermittent and **specifically the 18:10Z slot** — failed 08-01 18:10, 08-02 18:10, 08-04 18:10; succeeded 08-03 18:10; all `00/06/12:10` ticks in the window succeeded. So it correlates with the daily ~18:00Z IOPS-contention window, not a code regression. (08-03 12:10 also had a one-off "job startup timeout" — separate, cleared.)
- **Impact / risk read:** LOW and **self-healing** — the refresh is `CONCURRENT`, so the MV keeps serving its prior data on a failed tick (worst case ~12h stale instead of 6h until the next tick succeeds). It feeds the TopShot 180d edition median (an FMV input), so under the accuracy-is-the-gate roadmap it's worth the night pass having the specific object on record, but it is NOT urgent and NOT a new defect — it's the same IOPS-saturation contention class the 15:13Z inbox already flagged (wallet-username-resolver timeouts, entity-page Sentry saturation), surfacing on this MV refresh.
- **Suggested action (night pass / Trevor, if worth it):** either raise this job's `statement_timeout` proconfig (its budget is 600s per CLAUDE.md; confirm the refresh actually needs longer vs. the pool being saturated at 18:00Z), or nudge its slot off the 18:10Z contention peak. Measure the CONCURRENT refresh duration warm before changing anything — this may just be contention, in which case leave it (self-heals).

---

### Known / already-handled — NOT re-raised (context only)

- **`rpc-fmv-clamp-disconnected-ask` permission-denied (08:55Z):** flagged in the 15:13Z inbox and **already FIXED today** by Claude Code (`a9960e16` / `audit_20260804_grant_cron_heavy_fmv_clamp_disconnected_ask` — `GRANT EXECUTE … TO cron_heavy`; deploy `dpl_DbY6…` READY). The 08:55Z failure is a stale pre-fix run; the job runs daily at 08:55Z so it clears on tomorrow's tick. The sibling `cron_heavy`/`cron_light` EXECUTE sweep the 15:13Z inbox asked for was **also done** (ledger 08-04: all 78 cron jobs audited, 0 gaps). Closed — do not re-raise.
- **Trust-health `panini_upstream_sale_price_dry_days`=7 (BREACH, breach_at 3):** NOT a new finding — this metric was **added today (08-04)** specifically to instrument an already-diagnosed **upstream** outage (Panini stopped supplying sale prices; 0.00% supplied every day 07-29→08-04, `panini_sale_field_mapping_shortfall`=0 so our ingest is faithful). Different owner (upstream), self-clearing when upstream resumes. Its own `catches` text carries the full diagnosis + the launch-gate caveat (the "share of Panini serials with a price" coverage ratio is frozen-numerator/growing-denominator, so a launch gate on that ratio currently measures an upstream outage). No RPC-side action; watch only.
- **Trust-health `public_board_slow_count`=3 and `unmapped_resolution_backlog_max`=105:** both documented non-defects (slow-board set "plans optimal / do not re-budget autonomously"; backlog draining by design, `catches` text says DO NOT raise breach_at).
- **Pipeline alerts (all known classes):** `wallet-username-resolver` 39.8% timeouts (logged 15:13Z), `allday-unmapped-resolver-tail` 45.5% timeouts (deferred UNION-split; two resolver fixes shipped today — `0c554695` two-arm split + `8f3589f4` index steer — post-ship watch, do not re-flag), `allday-lock-refresh` 26.6% timeouts, `topshot-active-listings-ingest` 32% egress_blocked, `unmapped-sales-nfl_all_day` info/draining.
- **Sentry:** 0 new issues in 24h; no spike.
