# rpc-daytime-monitor candidates — 2026-08-03 ~00:09Z (evening tick)

> inbox written to mount, push unavailable — the public origin gitignores the entire `docs/` tree (.gitignore: "local-only reference docs ... never published"), so a sandbox clone cannot carry inbox files. The night pass reads this locally from the mount.

✓ Platform largely HEALTHY. The active IOPS-saturation window flagged at the 08-02 first-tick (15:12Z) has **EASED** — in the last 2h only 2 fails, both known non-saturation classes (topshot-active-listings egress_blocked; topshot-pack-opens-history-backfill upstream events 500). FMV/MV-stale trust metrics are all back to OK, the board-MV false positive is fixed (8650b93d), security is fully clean. Sensing only — nothing shipped.

## Resolved since the 15:12Z first-tick (do NOT re-queue)
- **IOPS saturation eased.** Last saturation timeout was 18:35Z (rpc-refresh-allday-pack-realized); nothing saturation-class in the last ~5.5h. The acute dimensions of first-tick Candidate 1 (MV refreshes going stale, wmc `rows_lost`) have cleared: `board_mv_refresh_stale_hours`=0.96 ok, all per-collection FMV stale metrics ok. The chronic `public_board_slow_count=3` BREACH remains — that is the ALREADY-NIGHT-PASS-QUEUED item, not a new root cause. First-tick's "verify whether the window self-clears as daytime load eases" is confirmed: it did.
- **classify-acquisitions-multicollection RECOVERED** (first-tick Candidate 2). Firing hourly ok=true since 22:06Z (the 04:06→21:06Z cron-job.org dropout resolved on its own; one 21:06Z saturation timeout, then clean).

## CANDIDATE 1 — [MEDIUM] pinnacle-sync silent ~38h / 2 missed daily ticks — likely the cron-job.org "RPC Pinnacle Sync" entry is still disabled from the (now-reverted) handoff item 4c
- **Source:** `detect_stalled_pipelines()` — `pinnacle-sync` silent 2279 min vs 1560 threshold; last run **2026-08-01 10:07:13Z ok=true**, nothing since. The 08-02 10:07Z daily tick did NOT fire (no `pipeline_runs` row → silent cron dropout, NOT a saturation timeout; it never logged).
- **Most-likely cause (correlated, not proven from here):** commit `47d4a244` (landed ~17:42Z 08-02) is a correction titled *"correct item 4c before it darks pinnacle-sync"* — the 08-02 Claude-Code handoff's item 4c had instructed the operator to DISABLE the cron-job.org "RPC Pinnacle Sync" entry (on the false premise that vercel.json drives the route at 06:00Z). The correction states plainly: the Vercel schedule has produced **zero** `pipeline_runs` in the retained window, so **cron-job.org is the only working driver**, and disabling it stops Pinnacle sync (~2,170 rows/run) silently. The missed 08-02 tick lines up with an operator acting on item 4c BEFORE the ~17:42Z correction landed.
- **Blast radius / severity:** NOT user-facing yet — Pinnacle FMV is kept fresh by the other Pinnacle pipelines (`pinnacle_fmv_stale_hours`=1.5, `pinnacle_render_floor_stale_hours`=4.3, `pinnacle_fmv_pct_stale_30d`=0, all ok). What's stalled is the daily **catalog + FMV catalog** sync (~2,170 rows/run) — new Pinnacle renders/editions won't be ingested, and every future 10:07Z tick keeps missing until re-enabled.
- **Risk read:** No autonomous fix — cron-job.org console is operator-only and the monitor is read-only. Re-enable-and-verify, not a code change.
- **Suggested action (operator / Trevor):** In cron-job.org, confirm the **"RPC Pinnacle Sync"** entry is ENABLED and firing at its 10:07Z daily slot (it may have been disabled per the reverted item 4c). If disabled, re-enable it; the next 10:07Z tick should log an ok=true `pinnacle-sync` run (~2,170 rows). Do NOT rely on the vercel.json 06:00Z schedule — per `47d4a244` it produces no runs. If already enabled and still silent, escalate.

---

### Also observed (NOT new candidates)
- **⚠ Sentry connector INVALIDATED this run** — `search_issues` returned "the user's connection to this connector was invalidated." New/unresolved Sentry issues could NOT be checked this tick. Reconnect the Sentry connector (claude.ai connector settings) to restore that leg of the sweep. Not an inbox work-item; a monitoring-visibility gap for Trevor.
- Security fully clean: invariants [], anon_write_holes [], rls_off_base_tables [], secdef_anon_violations []. `edition_integrity_flags`=99 (breach_at 250) and `ts_uuid_dupes_created_24h`=0 — ok.
- Vercel: latest prod deploy `dpl_HVw1Awmq8HUhCbSjtYcT42ARtnRW` READY (Golazos/Candy/Pinnacle inventory fix, c61a9d8). No ERROR-state deploys (CANCELED entries are superseded concurrent commits — normal for the heavy 08-02 evening CC/Cowork wave).
- `unmapped_resolution_backlog_max`=100 (exactly at threshold) — known draining class (nba_top_shot actionable pile ~0.4d to clear, 0 live inflow). Do NOT re-flag.
- pg_cron timeouts (rpc-refresh-new-collectors, rpc-weekly-wmc-prune, rpc-serial-fmv-multipliers-weekly, rpc-refresh-topshot-edition-median, rpc-refresh-allday-pack-realized) — all `statement timeout` inside the 09:45–18:35Z saturation window; hourly ones self-heal on retry (board metrics ok), weekly ones retry on their next scheduled tick. Dimensions of the queued IOPS item, not new.
- candy-offers-indexer "offer sweep <50% → deactivation suppressed" is the by-design honesty guard (a partial sweep never marks standing offers dead), not a defect.
- Artifact estate intact (11 artifacts, no broken/retired). Deep per-payload validation kept LIGHT this tick to avoid re-loading the DB right after the saturation window; the `rpc_ops_snapshot()` backbone (powers rpc-live-health; timed out at peak saturation) now returns cleanly, so the primary payload source is healthy.
