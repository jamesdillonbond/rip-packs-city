# Daytime monitor inbox — 2026-06-19 ~14:14 PDT tick (2pm)

Platform GREEN. One candidate — an **UPDATE / escalation** of the already-queued `ALLDAY-SERIAL-BACKFILL-CRON` item (do NOT double-count as new). New, decision-relevant evidence: the UA-reuse fix Trevor/CC shipped today did NOT resolve the Cloudflare 1009.

## Candidate

### ALLDAY-SERIAL-BACKFILL — the UA fix (`f53a9211` / edge fn v2) shipped today did NOT resolve the 1009; serial backfill still 100% blocked
- **Source:** `pipeline_runs` `allday-listing-serial-backfill` — 3 runs ever, **all ok=false / `http_403:error code: 1009` / 5-of-5 chunk_errors / 0 serials_resolved**. Edge fn `backfill-allday-listing-serials` **v2** (the UA fix) went live **15:05:38Z** (Supabase `updated_at`). Two runs executed AFTER v2 — **15:34:16Z and 18:34:20Z — and both still 1009'd on every chunk**. `allday_moment_serials` still **2 rows** (unchanged).
- **What this means:** the `f53a9211` hypothesis ("new User-Agent without reputation → nflallday WAF 1009; reuse the resolver's exact UA") is **disproven** — even on the fixed UA, all 5 chunks 1009.
- **Blast radius (verified — ISOLATED, NOT an egress/WAF block):** the sibling `allday-unmapped-resolver`, which routes through the SAME topshot-proxy `/allday-consumer` path, is **72/72 ok, 0×1009** in 24h; the whole AllDay pipeline (sales-indexer / fmv-populate / listing-cache) is **72/0/0** clean. So worker egress / region / WAF is fine for that path generally — the 1009 is specific to the `backfill-allday-listing-serials` request, meaning a residual difference vs the resolver beyond UA (request shape: `searchMomentNFTsV2(byFlowIDs)` 40/page × 5 chunks, variables, or another header).
- **Risk read:** LOW. Serial DISPLAY only — the AllDay deal **buy link already works** (`low_ask_nft_id`, Item 1). No FMV / pricing / sales / security impact; rest of platform GREEN.
- **Suggested action (CC / operator):** do NOT read `f53a9211` as "fixed" — it is not. Next diagnostic is to **diff the live `backfill-allday-listing-serials` request against the proven-working `allday-unmapped-resolver`** on the same `/allday-consumer` proxy path (request body / variables / page size / any header beyond UA) to find the residual trigger, rather than re-chasing UA/egress. Keep `ALLDAY-SERIAL-BACKFILL-CRON` OPEN until a run logs ok=true and `allday_moment_serials` grows past 2; do NOT add a `pipeline_cadence_watchlist` row before that first successful fire (L1).

## Notes (not candidates — for the record)
- **BUYERBF-PERINVOCATION-WORK watch — still PASS, but per-invocation runtime creeping up.** `topshot-buyer-backfill` 61 runs / 0 fails / 24h, all ok + logging, `detect_stalled []`. Max duration **740.2s** this window — under the 770s watch line and the 800s Pro hard cap, but up from 710.5s (night pass) / 711.8s (8am tick). Upward trend at the new ~4/hr overlapping cadence; item holds as queued (lever = stop overlap / cap rows-per-invocation; CC route + operator cron). Watch: any run >770s or that stops logging = invisible >cap-death class.
- **alerts-dispatch 1 isolated deal-leg timeout** (19:29Z; 1/96 in 24h) inside a brief ~19:15–19:29Z contention cluster (check-alerts 19:15Z = 1/72, wmc-fmv-populate 19:28Z = 1/1440), all recovered. Same transient class as the documented 06-18 13:15–13:29Z cluster; `get_pipeline_alerts []`. NOT a re-emergence of the closed ALERTS-DISPATCH-DEAL-TIMEOUT — a single 1/96 timeout with the board materializing each live-sub tick is within tolerance. No action.
- **FMV healthy, reconciles EXACTLY** — latest-per-edition TS 15543 = 15543 editions / AllDay 6191 = 6191; TS HIGH+MED **3178** (up from the 3144 baseline), NO_DATA improving 3468→3413, ASK_ONLY 2630 stable; AllDay HIGH+MED 849 stable, NO_DATA 2338 benign (8am tick verified ~all zero-90d-sale editions). Sentinel TS-UUID-48h **0**.
- **Health:** security 0/0/0/0, `detect_stalled []` / `get_pipeline_alerts []`, pipeline fails last 6h all transient/known (evm 429, the 19:1x–19:2x contention cluster). DB 4864 MB (+21 benign). Cross-collection `cohort_mat` fresh **06-19 04:10Z** (pg_cron step1 ran today). 14 artifacts GREEN (deals_board 806, allday_floor_ask 3917, serial_premiums 277, trophies 683, squeeze 9102, top_sales 576, underpriced 15). Vercel **0 ERROR** (prod `f53a9211` READY). Sentry 1 unresolved (NEXTJS-A smoke-transient, 1 event ~05:00Z super_low, FMV independently verified healthy).
