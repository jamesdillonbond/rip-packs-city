# Daytime monitor candidates — 2026-07-25T16:09Z (first-tick-of-day)

Health is GREEN overall (security 0/0/0/0, trust 15 metrics 0 breaches, `detect_stalled_pipelines()` [], FMV fresh TS 0.3h / HIGH+MED 3,325, cross-collection refresh fresh 04:10/04:25Z, DB 10,807 MB). One transient event and one small code gap below. The AllDay `unmapped_sales` backlog (43,066 open) is already logged in the 07-25 ledger entries + weekly sweep flag — NOT re-raised here.

---

## 1. PGCRON-STARTUP-TIMEOUT-CONVERGENCE-STAGGER — MED (DB-only pg_cron reschedule; night-pass appropriate)

- **Source:** `get_pipeline_alerts()` fired a **HIGH** `pgcron-startup-timeout` (8 pg_cron ticks failed "job startup timeout" in the last 30 min) at ~15:53Z; corroborated by `check_pgcron_recent_failures()` and a connection-pool saturation burst in `pipeline_runs` localized to 15:40–15:56Z (441 runs piled into the 15:40 10-min bucket vs ~60 normal; 20 pool-timeout fails). Self-recovered — the 16:00 bucket is clean.
- **What:** Eight high-frequency pg_cron backfill/refresh jobs converge on the same minutes and exceed pg_cron's background-worker startup capacity. Schedules:
  - `rpc-pinnacle-mints-backfill` `*/2`
  - `rpc-allday-pack-sales-backfill` `*/3`
  - `rpc-topshot-pack-sales-backfill` `*/3`
  - `rpc-allday-dist-opened-backfill` `2-58/4`
  - `rpc-refresh-mv-pack-ev-latest` `3,13,23,33,43,53`
  - `rpc-backfill-pack-pool` `3,8,13,…,58`
  - `rpc-pinnacle-mints-forward` `4,14,24,34,44,54`
  - `rpc-topshot-pack-opens-history` `9,24,39,54`
  - **Minute :54 collides 6 of them** (pinnacle-mints `*/2`, both `*/3` pack-sales, dist-opened, pinnacle-mints-forward, pack-opens-history); minute :53 collides backfill-pack-pool + refresh-mv-pack-ev-latest + pinnacle-mints. These convergence minutes are what trip the worker-startup ceiling.
- **Risk read:** LOW impact per event — "the function body never ran, so nothing was logged" (a skipped tick that re-runs 2–3 min later; no permanent data loss; `wallet-backfill` `rows_lost` in the co-occurring pool burst self-heals on the next walk). **BUT** the pager arm shipped 07-25 now emits **HIGH** on every convergence window, so this will page Trevor repeatedly (alarm-fatigue risk) until either the jobs are staggered or the threshold is tuned.
- **Suggested action (night pass):** Stagger these sub-5-min jobs onto offset minutes so no more than ~2–3 fire together at any minute (esp. break the :53/:54 pile-up) — the established cron-stagger discipline, DB-only `cron.schedule` re-ups (pg_cron keys per-user; preserve `cron_heavy` ownership where applicable). Alternatively/additionally an owner call: raise pg_cron worker capacity (`max_worker_processes`), or tune the pager threshold so a brief convergence blip doesn't page HIGH. Not auto-shippable blind — `get_pipeline_alerts()` is the single live pager path (hot object) and the stagger touches many live jobs, so verify each reschedule and re-check `check_pgcron_recent_failures()` after.

## 2. SET-DETAIL-PAGE-POOL-RETRY-GAP — LOW (code; mirrors an already-shipped pattern)

- **Source:** Sentry `JAVASCRIPT-NEXTJS-22` "set detail unavailable: canceling statement due to statement timeout", `GET /[collection]/set/[slug]`, 1 event ~15:48Z during the spike above (companion to `NEXTJS-20` player-detail pool-timeout, 1 event/18h ago).
- **What:** The set-detail page throws (→ 500) on the transient statement-timeout / connection-pool-acquire class **without** the `rpcWithRetry` wrapper that the ledger's earlier fix (line ~91) already added to the player/team/pack detail pages (`get_player_detail` / `get_team_detail` / `get_pack_detail_bundle`). Set detail appears to have been missed by that pass.
- **Risk read:** LOW / self-limited — single events, only during saturation windows, secondary detail page. Same family as the queued ACTIVATION-PATH-RPC-TIMEOUTS item.
- **Suggested action:** Wrap the set-detail primary RPC read (`get_set_detail` or equivalent in `app/(collections)/[collection]/set/[slug]/page.tsx`) in the existing `lib/analytics/rpc-with-retry.ts` `rpcWithRetry` (3 attempts w/ backoff) exactly as player/team/pack got, with a regression test. Verify by reading the page source first — confirm it's actually unwrapped before shipping.

---

*Notes:* Vercel prod READY on `f4e7701b` (latest of a rapid CC test-coverage push wave); one ERROR deploy `8b515e93` mid-wave is transient/superseded (its two descendant commits built READY, prod recovered immediately) — not a finding. rpc-live-health artifact validated (all insights boards + pinnacle_fmv + listings/unmapped legs resolve; no schema drift). Standing carried items unchanged: DUNE-DATAPOINT-CAP-402 (both Dune walkers still 402 on the datapoint cap — operator/billing).
