# Daytime-monitor candidates — 2026-09-18 ~08:13 PT (15:13Z)

Source: rpc-daytime-monitor first-tick run. **The DB read-availability event flagged by the nightly pass (ledger 2026-09-18) is STILL LIVE at this tick** — every sweep below that needs a DB read was refused, so this run is dominated by that one event. Nothing here asserts a cause (§1c: positive control unobtainable — the connection it needs is the one being refused).

## SYMPTOM (already ledgered — this extends the timeline, not a new item)

- **Title:** DB read-availability event NOT self-cleared as of 15:13Z (~52+ min after nightly pass first saw it 14:20–14:55Z).
- **Source:** MCP `SELECT 1` and the `pg_stat_activity` positive control both refused **5× consecutively** this run (`Connection terminated due to connection timeout`, i.e. connection **establishment**, not statement timeout). Same signature the nightly pass reported.
- **Risk read:** availability, not correctness. Read-only observation.
- **Suggested action:** **quiet-window RE-MEASURE — do NOT assert a cause.** On next successful DB access, owed items (unchanged from ledger): take io_wait/active/total positive control, confirm the self-clear + record the true end of the window, and identify the pinning reader from `pipeline_runs` / pg_cron around the event. A denied positive control is a SYMPTOM; every duration read during this window is uninterpretable.

## NEW datapoints this run (reduce the night pass's work — not conclusions)

- **Honesty layer CONFIRMED WORKING on a user surface during the live event.** Public `/insights/squeeze` rendered with the correct degraded banner: *"PARTIAL DATA — 1 of 1 section could not be loaded (Squeeze board). This is a temporary database-load failure, not an empty result — treat the affected sections as unknown rather than zero, and reload shortly."* The 0/0%/0 KPI tiles sit **under** that banner, i.e. flagged unknown, not published as fact. No fabricated-zero regression observed on this surface during degradation. (Positive control for the honesty canon; no action.)
- **Vercel ruled clean — not a deploy cause.** No ERROR deploys. The three most-recent production tips are all `CANCELED` **docs-only** commits correctly skipped by `ignoreCommand` (working as designed); the last READY production **build** is ~3.5 d old and still serving. So the event is DB-side, not a bad deploy.
- **Concurrent sessions already on it:** nightly pass `np-20260918-sbx` (monitor-only, no-push) + a Claude Code / Opus 5 session committing `docs(122): production is 503ing and the DB is unreachable`. Coordinate — don't duplicate.

## Sweeps I could NOT run this tick (all DB-read-gated by the event; re-run when DB is reachable)

`rpc_ops_snapshot()`, `detect_stalled_pipelines()`, `check_pgcron_recent_failures()`, security/RLS drift, sentinel tripwires, **1a first-tick extras** (trust health `v_rpc_trust_health`, cross-collection pg_cron refresh verify), and **1b artifact validation** (all payload queries). None validated — treat their status as UNKNOWN this tick, not healthy.

_inbox written to mount, push unavailable (sandbox has no push credential this cycle, per nightly pass); night pass picks up locally._
