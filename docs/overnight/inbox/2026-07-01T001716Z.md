# Daytime monitor candidate — 2026-07-01T00:14Z (2026-06-30 ~17:06 PDT tick)

Run: rpc-daytime-monitor. No clock skew (shell == DB now() == app-stamped rows all 00:06Z). Later tick, not the ~8am first-tick, so Section 1a daily extras skipped.

Health GREEN: security 0/0/0/0; trust 15/15 ok (breaches []); detect_stalled []; check_pgcron_recent_failures() []; sentinel TS-UUID-48h 17 (inert, < warn 250); editions FLAT (TS 17,489 / AllDay 6,191 / Golazos 581 / UFC 518); FMV TS H+M 4,688 / AllDay 915 (both improving); fmv_sanity 0; unmapped_resolution_backlog_max 29 (drained well below 100); DB 7,173 MB (+78/24h benign); prod 8f167a66 READY (React #418 hydration-chip fix).

Post-ship watch on the 06-30 daytime wave — PASS. AllDay EV matview passthrough independently re-verified: v_allday_pack_ev_corrected full scan = 7.98 ms over 2,331 rows, so the ALLDAY-CORRECTED-EV close (8b4b1872) HOLDS. Its residual `[pack-detail] allday_corrected_ev` statement timeouts (last 06-30 22:11Z, under the superseded 9b619fb8 prod) are collateral from the broad ~22:07-22:12Z connection-pool-saturation contention spike (same window timed out edition/player/wallet-backfill/pack-detail across the board) — the known DBSAT/VERCEL cost family, NOT a matview regression. Do NOT reopen ALLDAY-CORRECTED-EV. One Vercel ERROR deploy (dpl_3nAH, commit 9b619fb8) is superseded — the same commit went READY (dpl_8Gu4) 15 min later; not live.

rpc-live-health consolidated data layer validated: 12 insights backing objects + cached_listings_v2 all resolve; FMV/pack-EV/offers freshness < 10 min. (Newest 3 artifacts — rpc-moment-fmv-ev-dialin, rpc-growth-funnel, rpc-pack-lifecycle, created since the SKILL's 06-12 estate list — not individually query-validated this run; they read the same confirmed-healthy FMV/pack objects. Operator note: SKILL artifact enumeration is stale by those 3.)

## Candidate (1, LOW)

- **CLASSIFY-ACQ-ALLDAY-STATEMENT-TIMEOUT** (LOW; night-count 1; DB/route)
  - Source: pipeline_runs — `classify-acquisitions-multicollection` latest tick 2026-07-01T00:06:14Z ok=false, error `nfl_all_day: canceling statement due to statement timeout`. 24h fail count = 1 (all prior ticks ok); also present in the Vercel 300s-timeout runtime-error family for route /api/cron/classify-acquisitions-multicollection.
  - Risk read: LOW. Single isolated failure; pipeline still firing on cadence (detect_stalled []). Two candidate causes to disambiguate — (a) benign contention: it co-fired in a dense 00:06-00:13Z micro-cluster (compute-*-pack-ev 00:07, wmc-fmv-populate 00:08, pinnacle-* 00:06/00:09, offers-sweep 00:02) and the AllDay leg lost the pool race — the same window also timed out `[team] activity` at 00:06:50Z; (b) volume-creep: AllDay `sales` grew materially via the studio-history backfill (~124k+ studio rows), so the AllDay classify leg may be nearing its statement_timeout.
  - Suggested action (night pass): re-measure over 24h. If the AllDay-leg statement timeout recurs OUTSIDE dense micro-clusters, bump the classify fn's statement_timeout or scope/index the AllDay leg. If it only fails inside micro-clusters, close as DBSAT contention-collateral. No action if not recurring.
