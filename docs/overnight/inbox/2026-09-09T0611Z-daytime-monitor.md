# Daytime monitor — 2026-09-08 ~23:06 PT (2026-09-09 06:11Z)

Read-only ~3h health sweep. Estate is green with ONE new low-risk candidate: a retired pipeline's cadence-watchlist row is now a permanent false stall. Not in a saturation spell (`pg_stat_activity`: 1 IO-wait / 1 active at sweep time), so readings are interpretable. First-tick-of-day extras (1a) skipped — this is a late-night tick, not the ~08:00 run.

## Candidate (LOW) — the retired `ingest` lane's cadence-watchlist row now fires a permanent `medium` stall

- **Source:** `pipeline_cadence_watchlist` row `pipeline='ingest'` (`is_active=true`, `max_silent_minutes=1800`, `severity=medium`); surfaced by `detect_stalled_pipelines()` / `rpc_ops_snapshot().stalled_pipelines` and by the `rpc-qa-scorecard` "Pipelines stalled" card.
- **What:** `ingest` (Top Shot sales ingest via `.github/workflows/rpc-pipeline.yml`) was **intentionally retired from that workflow on 2026-09-07** (ledger #67(3): the `/api/ingest` step POSTed the decommissioned `public-api.nbatopshot.com`, 530/CF-1033, and its sales are written by `sync_sales_from_atlas` now). Its last run is therefore frozen at **2026-09-07T23:46:57Z**. As of this tick that is **~30.3h silent vs the 1800m (30h) threshold**, so `detect_stalled_pipelines()` now returns it at `medium` — and will do so **forever**, because the lane can never be invoked again. The prior monitor tick (2026-09-08T2110Z) did not flag it only because it was still under 30h then; this is the first tick past the threshold.
- **Blast radius:** two consumers now carry a benign permanent alert. (1) This monitor's `stalled_pipelines` shows 2 entries (`ingest` medium + the known `topshot-catalog-backfill` info seed). (2) The **`rpc-qa-scorecard` "Pipelines stalled" card renders RED (alert, n=2)** — it ranks any `detect_stalled_pipelines()` length > 0 as rank-3, so the scorecard has been showing a red alert since ~09-08. This is the "a permanently-red instrument is indistinguishable from a broken one" class: a stall arm that can never clear stops meaning anything and masks a real future stall of a still-live lane.
- **Risk read:** LOW. The fix is a single watchlist-row flag flip, fully reversible, no code / no schema / no route.
- **Suggested action (night pass):** set `is_active = false` on the `ingest` `pipeline_cadence_watchlist` row (an `apply_migration` / `execute_sql` UPDATE), mirroring the lane's intentional 09-07 retirement, with a `notes` append recording why (retired from `rpc-pipeline.yml`; re-arm only if an Atlas-port ingest lane lands under this name). Revert: set `is_active = true`. Do NOT instead widen `max_silent_minutes` — the lane is gone, not slow, so a threshold change would be the wrong tool and would still eventually re-fire.
  - ⚠ Related, already recorded (not part of this candidate): the ledger close-out already notes `wmc-reindex-verify` has **no** watchlist row (#56) — the mirror-image gap. And the "five lanes into the dead GQL host" filing (`2026-09-08T0530Z`) already dispositioned the *lanes*; this candidate is specifically about the *watchlist instrument* left behind by the `ingest` retirement, which that filing did not cover.

## Everything else — known / attributed, no new candidate
- **Security:** all four checks empty — invariants `[]`, anon-write holes `[]`, RLS-off base tables `[]`, secdef-anon violations `[]`. QA-scorecard sec card 0/0/0.
- **Trust health:** 38/38 arms `ok`, `trust_health_breaches` `[]`. `ts_uuid_dupes_created_24h` 0, `unmapped_resolution_backlog_max` 2 (well under 100), `public_board_slow_count` 0 (the 09-08 `pack_table_rows` flicker has cleared). Sentinel TS UUID editions 48h = **0**.
- **pg_cron:** `check_pgcron_recent_failures()` empty.
- **Vercel:** no deployment in ERROR. The burst of `CANCELED` production deploys is tonight's tight session-close push sequence superseding in-progress builds; latest code deploys (`6d42929` unschedule fix, `4fcf2a26`, `23ae79df`) are READY. The two docs-only tips (`7ecb6de7`, `7e1f7e36`) CANCELED as expected (docs-only tip does not force a rebuild).
- **Artifacts validated (2 of the highest-signal, both clean):** `rpc-live-health` merged payload runs end-to-end — every panel key sensible (FMV per collection, 14d trend 307 rows, sales_24h alive, insights counts, freshness FMV/pack-EV/offers all 06:07–06:10Z). `rpc-qa-scorecard` payload clean (fmv HM 7655 fresh 06:08Z, offidx TS 72 / AD 72, sentinel 0, db 22 GB); its only red card is the `stall`=2 covered above. `insights_counts.pack_reality_top_ev` = 0 is the already-filed frozen pack-drop-pool issue (`2026-09-07T1603Z` / `2026-09-07T2135Z`), not new. `offsan` 1340 (841 blank) is the known cross-instrument artifact (`2026-09-08T0110Z`). Remaining artifacts not individually re-run this tick (routine cadence; the two run cover the widest schema surface and no schema-breaking change shipped today).
- **Recent-ship health:** tonight's ships (81,337-sale Atlas recovery, #68 dedup, the jobid-481 unschedule after its IO spell, the unique-index / dedup migrations, CLAUDE.md/docs) show no regression — FMV/offers/pack-EV fresh, no new error class, instance quiet (1 active / 1 IO-wait), jobid 481 gone.
- **Known info alerts (unchanged, do not re-investigate):** Atlas 403 arms (`atlas-editions-upstream-403` 7.1%, `atlas-market-upstream-403` 7.5%) — request-id ATTRIBUTED Cloudflare challenge, freshness intact; `flow-rest-moment-moved-400` (17.9%, designed borrowMoment panic); `unmapped-sales-nfl_all_day` (by-design frozen multi-NFT txs, ~25.8d to clear); `golazos_sales resolving_editions` (info). `pipeline_fails_24h` top rows (`atlas-editions-refresh` 48, `atlas-market-feed` 39) are the 403-retry collateral, `upstream:0`.

**Health line:** ✓ healthy · security 4/4 clean · trust 38/38 · pg_cron clean · sentinel 0 · 2 artifacts validated OK · no ERROR deploys · not in a spell · 1 LOW candidate logged (retired `ingest` watchlist row → permanent false stall).
