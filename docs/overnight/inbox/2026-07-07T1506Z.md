# Daytime monitor candidates — 2026-07-07T15:06Z (~08:06 PDT, first tick of day)

**LEAD ALERT — the cron-job.org trigger-surface dropout the overnight pass logged as "operator/self-healing external" DID NOT self-heal; a cron-job.org-only cluster of ~15 pipelines has been silent 8.5h (since ~06:24–06:48Z) and is now BREACHING trust-health.**

Otherwise healthy: security 0/0/0/0 (invariants/secdef-anon/rls-off/anon-write all []); `check_pgcron_recent_failures()` []; sentinel TS-UUID-48h 0; DB 8,376 MB; cross-collection refresh fresh (cohort 04:10Z/154 rows, overlap 04:25Z, both ccm jobs active); Vercel prod `dpl_3rNAX` (a2e3392, pack-EV varied-pool guard) READY. Sentry quiet (1 new/12h = the same single-event OG pipe-abort NEXTJS-1T already logged 06:06Z; no spike).

## HIGH-PRIORITY — CRONJOB-ORG-TRIGGER-SURFACE-DEGRADED (persistent, 8.5h; operator; recurring class)

- **Source:** `rpc_ops_snapshot()` stalled_pipelines + get_pipeline_alerts + a `pipeline_runs` last-run sweep. The **06:06Z daytime tick was fully GREEN** (`detect_stalled_pipelines()` []); the cluster went silent right after it. The overnight pass (08:03Z, commit `ba40a80d`) caught the leading edge and characterized it as "cron-job.org broad trigger dropout ~35 pipelines ~06:43Z (operator/self-healing external)." **It has NOT self-healed — now ~8.5h and counting.**
- **What's still dead (last run ~06:24–06:48Z, none since; cron-job.org-triggered, NO GHA backstop):**
  - `wmc-fmv-populate` (last 06:48Z; ~1-min cadence) — **user-facing:** propagates fmv_snapshots→wallet_moments_cache; powers /share, portfolio, wallet snapshots. Halted 8.5h ⇒ those surfaces drift stale.
  - `snapshot-pack-asks` (06:48Z; every 5m) — pack_ask_state recency (NEW/▼ chips) frozen.
  - `wallet-backfill` + `-allday/-golazos/-ufc/-pinnacle` + `-multicollection-dispatch` (all ~06:46–06:48Z) — the 12:00Z 6-hour wave **never fired** (no atomic GHA backstop by design — ledger line 839).
  - `pinnacle-listings-reconcile` (06:24Z) + `pinnacle-events-ingest` (06:34Z) + `pinnacle-catalog-floor-refresh` (01:45Z) — **downstream BREACH below**.
  - `topshot-fmv-populate` (01:38Z; 6h @ :50 — missed 07:50 + 13:50Z), `allday-listings-retry` (06:38Z), `golazos-listings-indexer` (06:37Z), `ufc-listings-indexer` (06:42Z), the 3 studio deep-history backfills allday/golazos/pinnacle (~03:5xZ, 3h cadence — missed 06:5x/09:5x/12:5xZ).
  - **Daily morning ticks swallowed by the window:** `daily-portfolio-snapshot` (07:05Z tick — the 06:06Z "verify self-heal" watch never got its chance) and `ultimate-fmv-recalc-v1` (~06:35Z tick) both failed to fire (0 runs/30h). These two carried LOW watches are folded into THIS root cause, not separate incidents.
- **What SURVIVED (recovered / never dropped — still firing at 15:0xZ):** `topshot-sales-indexer` + `allday-sales-indexer` (GHA backstop `sales-indexers-backstop.yml` cron 18,48 — ledger 839), `fmv-recalc` (GHA), `hybrid_custody_events`, `pack-events-ingest`, `offers-sweep`, `pinnacle-listings-indexer`, `pinnacle-nft-resolver`, golazos/ufc sales indexers. So **core sales ingest is NOT down** — the split is exactly "has a GHA/Vercel backstop → alive" vs "cron-job.org-only → dead," which points squarely at the external cron-job.org trigger surface, not RPC code. pg_cron is fully healthy ([]).
- **Downstream regression (trust-health BREACH):** `pinnacle_ask_stale_hours` = **8.7h vs 3h breach** (the one trust breach; NOT the known-inert DQ4). Direct consequence of `pinnacle-listings-reconcile` being dead since 06:24Z (it writes `pinnacle_editions.ask`). Self-clears the moment the cron surface recovers.
- **Risk read:** MED. Not a data-corruption or security event and core sales/FMV ingest survived, but it's a broad, ongoing degradation of freshness across wallet/portfolio (wmc-fmv), pack-ask recency, Pinnacle ask/floor, and the daily snapshot/ultimate-FMV ticks — 8.5h and not recovering on its own. This is the recurring CRONJOB-ORG-TRIGGER-SURFACE-DEGRADED / LISTCACHE-CRON-DROP class (ledger 922) where cron-job.org auto-disables/degrades jobs that flap their 30s cap.
- **Suggested action:**
  - **Operator (primary, access-gated):** open the cron-job.org console and re-fire / re-enable the degraded jobs (wmc-fmv-populate, snapshot-pack-asks, the wallet-backfill family, pinnacle events/reconcile/floor-refresh, studio backfills, listings-retry, listings indexers, daily-portfolio-snapshot, ultimate-fmv-recalc). Confirm cadence resumes → `detect_stalled_pipelines()` shrinks and `pinnacle_ask_stale_hours` clears. cron-job.org console is operator-only + secret-gated (Advanced-tab auth header) — NOT night-pass shippable.
  - **Night pass / CC (durable, idempotent subset only):** the recurring nature argues for widening the GHA backstop (`sales-indexers-backstop.yml` pattern) to the idempotent still-dead pipelines — `wmc-fmv-populate`, the listings indexers, studio backfills — so the next cron-job.org wobble doesn't halt them. **Do NOT** auto-migrate the `wallet-backfill*` family or `snapshot-institutional-wallets`: ledger 839 flags them as unsafe to double-fire (need an atomic operator cutover).
- **Do NOT** treat the many stalled entries as separate incidents or the `pinnacle_ask` breach as a new data bug — they are one root cause.

## Notes
- `pipeline_fails_24h` top families (fmv-recalc 8, compute-topshot-pack-ev 7, lock-check-batch 4, topshot-buyer-backfill 4, wallet-username-resolver 3, …) are the known overnight-contention / DBSAT-timeout classes; every one's LATEST run is ok=true (the survivors above are firing) — not new, not flagged.
- `golazos_sales` / `ufc_sales` resolving_editions = benign INFO (1–3/24h), standing.
- Vercel: one ERROR deploy in the last 20 (`f6cd1ef` collection reducer Step 2) was **superseded** by later READY builds (Step 3a `8523932` + current prod `a2e3392`) — codebase compiles, not a live issue. The BLOCKED `dpl_HbEj` is the overnight pass's own docs commit (superseded-concurrent, benign).
- Artifact estate not deep-run this tick — `rpc_ops_snapshot()` covers the core DB-health surfaces the rpc-live-health dashboard reads; the overnight pass validated the estate ~7h ago. No view drops/renames observed in today's daytime wave (pack-EV guards, Dune ownership Top Owners, Golazos pack-EV, collection-page reducer refactor) that would break an artifact payload.
