# Daytime monitor — 2026-06-09T03:15Z

Read-only sweep. **Platform GREEN, with two confirmations and one NEW low candidate.**

- ✅ **PINBACKUP-RLS is RESOLVED** (was the live security regression at 22:21Z + 00:14Z). `check_public_security_invariants()` is back to `[]` (was 9 rows); the `audit_20260609_lock_pinnacle_backup_and_rewards_tier_search_path` migration landed (ledger Shipped, 2026-06-09). `analytics-smoke` recovered (01:13Z→02:43Z all `ok=true`; last fail 00:43Z) and `NEXTJS-1C` quiet since ~04h ago. Reconcile this as closed.
- ⚠️ **PINFMV-DRIFT-14 (NEXTJS-14) is STILL LIVE** (last event ~02:32Z, 9 events, `regressed`) — already logged 2026-06-09T00:14Z, CC/Trevor-owned, **carried here NOT re-counted**. The only Sentry issue actively firing.
- 🆕 **One new LOW candidate: PIN-SYNC-FMV-WATCH** (refines queued PIN-SYNC-CRON) — the render-FMV `statement_timeout` fix is unvalidated by a scheduled tick + there's a render-FMV staleness monitoring gap. Detail below.

All else green: `detect_stalled_pipelines()`/`get_pipeline_alerts()`/`check_secdef_anon_execute_violations()` `[]`; 0 RLS-off base tables, 0 anon-write holes; sentinel TS-UUID-48h **0**; 20/20 deploys READY (0 ERROR); 17/17 artifact backing relations resolve; `v_rpc_trust_health` all 6 metrics `ok`. Lock stale (08:17Z, ~18.9h old) → inbox commit permitted.

## New candidate

### PIN-SYNC-FMV-WATCH — render-FMV `statement_timeout` fix is unvalidated by a scheduled tick + render-FMV staleness is unmonitored (LOW · CC/operator watch · refines the queued PIN-SYNC-CRON)
- **Source:** `pipeline_runs` `pinnacle-sync` — last run **2026-06-08 10:07:08Z, `ok=false`**, error `pinnacle_fmv_recalc_render_all: canceling statement due to statement timeout`, `duration_ms` 10035 (≈ the service_role 8s authenticator cap + overhead). Daily cron at ~10:07Z; the 06-07 tick succeeded (1,794 renders priced in 2.2s). Only 2 runs in 50h → genuinely daily.
- **Attribution (clean):** the 06-08 tick failed on the EXACT timeout that commit `5880eeb` + migration `audit_20260608_pinnacle_render_all_statement_timeout` (`SET statement_timeout TO '120s'`) was shipped to fix — because `5880eeb` (deploy `dpl_M2yE2J156Wc2...`) landed AFTER the 10:07Z 06-08 tick. So the fix is currently **unvalidated by a scheduled run**; it's been verified only by the manual/post-migration re-runs (`pinnacle_catalog.fmv_computed_at` now 2026-06-08 17:38Z).
- **Blast radius: none right now.** Render FMV (`pinnacle_catalog.fmv_usd`, 1,797/2,079 priced) is ~9.5h fresh (well under 24h); ASK is fresh (`pinnacle_editions` `pinnacle_direct` newest 03:09Z; `v_rpc_trust_health.pinnacle_ask_stale_hours` = 0.1, ok). The risk is forward-looking: if the ~10:07Z 06-09 tick ALSO fails (or the daily cron drops), render FMV silently ages past 24h with nothing catching it (`pinnacle-sync` is NOT watchlisted — PIN-SYNC-CRON queued — and `v_rpc_trust_health` monitors ASK staleness but has no render-FMV companion).
- **Risk read:** off-limits to the night pass (Pinnacle FMV route/view logic + operator cron). Not auto-shippable. Log for CC/operator.
- **Suggested action:** (1) verify the ~10:07Z 06-09 `pinnacle-sync` tick logs `ok=true` now that `5880eeb` is live — that's the gate PIN-SYNC-CRON's watchlist INSERT was waiting on ("after ≥1 logged successful run"). (2) Consider adding a `pinnacle_fmv_stale_hours` row to `v_rpc_trust_health` (max age of `pinnacle_catalog.fmv_computed_at`; breach ~30h = daily + grace) so a render-FMV freeze is caught the same way the ASK freeze now is. floor_ask side-note: `pinnacle_catalog.floor_ask_updated_at` newest is 2026-06-08 09:37Z (~17.5h, daily backfill cadence — fine, not stale).

## Carry / escalation — NOT a new item (logged 2026-06-09T00:14Z as PINFMV-DRIFT-14)

### PINFMV-DRIFT-14 — Pinnacle cross-character FMV leak from the ~23:00Z `04011b3` concierge ship — STILL LIVE (HIGH-PRIORITY · regression · CC/Trevor-owned — do NOT auto-fix / auto-revert)
- Re-confirmed this sweep: Sentry `JAVASCRIPT-NEXTJS-14` "Pinnacle FMV not borrowed across characters (drift guard)" is still `unresolved`/`regressed`, **last event ~02:32Z** (9 events since the 23:00Z ship). The drift guard catches `searchPinnacleDeals` attaching a cross-character FMV to one ambiguous trimmed `(character,set,variant)` triple (repro from 00:14Z: Goofy / Mickey & Friends Vol.1 / Standard fmv=$1.247). Deterministic → re-trips each smoke run while the leaked row persists.
- Full detail + the keying-fix probe is in `docs/overnight/inbox/2026-06-09T00-14Z-daytime-monitor.md` — **night pass: treat as the SAME item, do not double-count.** Fix is in `searchPinnacleDeals`/`searchPinnacleByName` (join FMV to `pinnacle_catalog` on the same `render_id` as the ask, not a collapsible triple) — off-limits to the night pass per focus.md item 2 + concierge route-logic off-limits. CC/Trevor owns it.

## Clean / already-owned — checked, not new
- **Pipeline fails last 6h = the known I1/DBSAT rush cluster, all self-recovered.** 11 fails total, clustered ~00:43–00:54Z (the :00/:xx cron storm): `analytics-smoke` 8 (the PINBACKUP-RLS security check, now recovered), `pinnacle-listings-reconcile` 1, `topshot-moments-hydrator` 1 (= N2 `v_moments_needing_hydration` candidate-read timeout), `wmc-fmv-populate` 1 — all `statement timeout`/pool-contention, **0 fails in the last ~2.2h**. 24h: pack-ev 376ok/41fail, pinnacle-nft-resolver 220/41, hydrator 108/28 (N2), wmc-fmv-populate 1181/27, reconcile 71/22 — all rush-window, none a wedge.
- **Sentry saturation-tail cluster quiet 4–7h, markable resolved after 24h:** `NEXTJS-1C` (PINBACKUP-RLS echo, fix landed), `1J` (pack-dist Sales-History), `1H` (edition Recent-Sales = SMOKE-EDITION-TIMEOUT, queued), `1E` (detect_stalled smoke), `W`/`E`/`A`/`4` (overview/fmv/market smoke during the evening saturation window). `NEXTJS-15` = the known PIN1 `cadence_capped`-counted-toward-spike false-positive (queued), last ~1h ago, benign.
- **pack-EV healthy:** `topshot_pack_ev_targets` 800 total / 86 stale-24h / **0 stale-48h** (oldest 06-08 00:43Z) — v21 + cron-frequency lever holding (PACKEV-THROUGHPUT closed).
- **FMV flat/healthy churn:** TS HIGH+MED 2,925 (baseline 2,971), NO_DATA 5,044 (improving from 5,196); AllDay HIGH+MED 488 (flat). Writers all fresh (TS fmv write 03:08Z, fmv-recalc 03:08Z, allday-fmv-populate 03:02Z, topshot-sales-indexer 03:03Z, offers-sweep 03:02Z, pinnacle-listings-reconcile 03:09Z).
- **Editions identical to baseline** (TS 15,542 / AllDay 6,191 / Golazos 581 / UFC 446) — DUPE1 re-mint stayed stopped; sentinel TS-UUID-48h 0 (6,404 fossils, flat).
- **Brand-token Phase-2 sweep executed today** (commits `4be97c5`→`57b3def`, Batches 1–5, authored under the monitor/Trevor identity) — all deploys READY, the ledger's "deferred Phase-2" item is effectively drained.
- **DB 6,679 MB** (+43 since 00:14Z, +197 since the 08:15Z night baseline) — slow creep, watch-only (dupe-fossil roll-off + index churn).
- **Artifacts 17/17 healthy** (16 prior + new `candy-chain-two-onboarding-status`, created 02:04Z). Backing relations validated: `v_tracked_wallet_fmv_confidence` 20 (no 57014), `topshot_squeeze_board`/`pinnacle_scarcity_board`/`topshot_offer_ask_spread` 500+, `cross_collection_deals_board` 324, `topshot_deals_vs_fmv` 290, `topshot_2025_rookie_index` 61, `v_rewards_economy` 1; `v_rpc_trust_health` all 6 `ok`.
