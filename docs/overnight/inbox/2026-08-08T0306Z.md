# Daytime monitor candidates — 2026-08-08T03:06Z (2026-08-07 ~20:06 PDT)

> inbox written to mount, push unavailable — the sandbox clone checkout failed with "No space left on device" on /sessions (the documented clone-failure class). Night pass picks this up locally.

Late tick. Platform HEALTHY-with-known-noise: security all clean (`invariants`/`anon_write_holes`/`rls_off_base`/`secdef_anon` all `[]`), `stalled_pipelines` `[]`, no new Sentry issues in 12h, no Vercel deploy in ERROR (2 BUILDING + several CANCELED = Trevor is actively shipping a rapid FMV-accuracy + candy-offers wave right now, superseding pushes). Flagship artifact `rpc-live-health` payload validated live — all panels return sensible data; FMV/pack-EV/edition-offers all fresh to seconds ago; `db_active=3` (DB calm at snapshot). Public boards healthy (`public_board_empty_count`/`slow_count` = 0, board MVs fresh).

Three trust-health breaches, all KNOWN/tracked — NOT re-raising as new:
- `panini_sale_price_capture_dry_days`=10 — upstream Panini residential home-box outage (already QUEUED 08-07: "wake the box"). `panini_fmv_stale_hours` back to 1.5 (ok), so the runner has resumed FMV writes but price-capture is still dry.
- `ufc_fmv_stale_hours`=92.6 — UFC dead-market, red-by-design (market closed 2026-05-13).
- `unmapped_resolution_backlog_max`=175 — climbing 105->127->175 (tracked 08-07).

---

## NEW candidate

### 1. pg_cron `rpc-refresh-misattrib-candidates` timed out again — at the 15:35Z slot it was rescheduled TO on 07-13 to dodge contention (LOW, user-invisible)
- **Source:** `check_pgcron_recent_failures()` — jobname `rpc-refresh-misattrib-candidates`, latest_status `failed`, last_run `2026-08-07 15:35:00Z`, 1/1 runs failed. `last_fail_message`: `canceling statement due to statement timeout` on `REFRESH MATERIALIZED VIEW public.mv_topshot_misattrib_candidates` (via `refresh_topshot_misattrib_candidates()`).
- **Why it's a finding (not stale-pre-fix):** the failure timestamp is *today* and there is no same-day fix that would clear it on the next tick — this job runs ONE daily tick. Per ledger `audit_20260713_reschedule_misattrib_candidates_offpeak` (pg_cron job 70), it was already moved 03:25Z->15:35Z *specifically* because the refresh seq-scans the full Top Shot sales history (~613k cost; collection_id indexes non-selective) and dies under peak contention. The 15:35Z slot is now *also* contended, so the reschedule no longer holds — the MV is not refreshing.
- **Risk read:** low. `mv_topshot_misattrib_candidates` is an internal helper for the parallel<->base misattribution drain, not a user-facing surface; a stale/failed refresh delays misattrib candidate discovery but corrupts nothing. `cron_heavy`/600s already set.
- **Suggested action (night pass):** either (a) re-reschedule to a genuinely quiet slot (current 21:00-01:00Z + overnight 00-06Z + evening peaks all crowd it; the ledger already ruled out REINDEX — it is a seq-scan, contention-bound), or (b) the deeper fix noted in the 07-13 ledger entry (the refresh seq-scans and never uses the pristine `idx_sales_*_ts_edserial_collide` indexes — a query rework, not an index add). A single quiet-slot move is the low-risk lever.

## Context note (already-known cluster — NOT new candidates)
The 2-day `get_pipeline_alerts` window shows an elevated statement-timeout cluster: `allday-lock-refresh` 62.5% (already QUEUED — ALLDAY-LOCK-REFRESH-SELECTION-COST), `lock-check-batch` 32% (documented evening-contention class), `wallet-username-resolver` 38.9% (QUEUED 08-05), `allday-buyer-backfill` 43.8% (backfill; wraps + retries). The misattrib MV timeout above is the same peak-window pooler-contention family. DB read calm at snapshot (`db_active`=3), so these are bursty peak-window failures, not persistent saturation — and tonight's active heavy FMV-recalc + candy-offers-sweep wave (candy-offers sweeps have been hanging ~760s holding pooler connections per the 08-08 ledger, actively being fixed by Trevor right now) plausibly inflates the evening peak. `sync-nba-projections` 100% (off-season, expected) and `topshot-active-listings-ingest` egress_blocked (known GHA egress, Vercel backstop) are not findings. candy-offers deliberately omitted — Trevor is fixing it live this evening.
