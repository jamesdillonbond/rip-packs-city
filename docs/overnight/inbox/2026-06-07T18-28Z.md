# Daytime monitor inbox — 2026-06-07T18:28Z

Run: rpc-daytime-monitor (read-only sweep ~18:05–18:28Z). Health otherwise GREEN — see summary at bottom. No new security/RLS findings (0/0), `detect_stalled_pipelines()` = `[]`, `get_pipeline_alerts()` = `[]`, 20/20 Vercel deploys READY, Sentry quiet since ~07:00Z (6 unresolved, all known).

---

## 1. I1-EVIDENCE · [HIGH as input to existing I1 — not a new item] IO-saturation window now provably breaks interactive/artifact reads, with wmc autovacuum as the named driver

**Source:** live pg_stat_activity capture + repeated 57014s, 18:08–18:15Z; recovery verified 18:24–18:27Z.

**What happened.** Between ~18:08Z and ~18:15Z, mixed read queries failed with `57014 statement timeout`, including: the exact embedded query of the `rpc-tracked-fmv-confidence` artifact (`SELECT ... FROM v_tracked_wallet_fmv_confidence`, 2× consecutive), a 300-row sample of the per-edition fmv_snapshots LATERAL, and a batch of small-view counts (`topshot_pack_reality_dist` ~6 rows, `v_rewards_economy` 1 row, an indexed wmc lookup). `pg_stat_activity` during the window: `autovacuum: VACUUM public.wallet_moments_cache` active **141s** in `Timeout/VacuumDelay`, `refresh_pack_grail_metrics_mv()` 13s in `IO/DataFileRead`, and a wmc writer call 11s in `IO/DataFileRead`. At 18:24Z a **second back-to-back** `autovacuum: VACUUM ANALYZE wallet_moments_cache` was already running — wmc churn (image denorm + per-render FMV sync + drift writers) is regenerating dead tuples fast enough for near-continuous daytime autovacuum on a 1.59M-row table.

**Recovery proof (NOT a durable view break):** at 18:24–18:27Z everything passes — AF1 view returns the full sensible distribution (TS held HIGH+MED 2,929/8,989; held-leg semi-join standalone = 8,989 exact, matching the 06-06 subagent verification), deals board 337 rows, all small counts fine. All `fmv_snapshots%` indexes `indisvalid=true`; `fmv_snapshots_2026` freshly autovacuumed 17:08Z (11,202 dead / 478,849 live). TS editions flat at 15,541 (DUPE1 still stopped) — so this is NOT the prior AF1 growth mechanism. **Do NOT re-optimize `v_tracked_wallet_fmv_confidence` — it is healthy.**

**Why it upgrades I1:** the rush class was previously characterized as transient pipeline-tick fails. This capture shows it also takes down any Cowork artifact opened in the window and any interactive read crossing a few seconds — user-perceivable, several times a day (12:0xZ: 8 fails/7 pipelines; 18:00Z: evm-transfers-ingest pool timeout + this window).

**Suggested action (night pass / CC):** fold into I1 — (a) per-table autovacuum tuning on `wallet_moments_cache` (e.g. raise `autovacuum_vacuum_cost_limit`, lower `autovacuum_vacuum_cost_delay`, and/or raise `autovacuum_vacuum_scale_factor` so it runs less often but finishes faster off-peak; an `ALTER TABLE ... SET (...)` reloption migration is small and revertible), (b) stagger the `:23` `refresh_pack_grail_metrics_mv` cron and wmc writer ticks off the :00/:20 rush, (c) triage rule: when an artifact/view "breaks" with 57014 during daytime, re-test on a calm DB before re-optimizing.

**Risk read:** LOW-risk evidence note; the reloption change itself is a small reversible migration but touches a hot table — night-pass judgment.

## 2. LEDGER-RECONCILE · [LOW · doc-only] SMOKE-MARKET-EMPTY already shipped — still listed queued

**Source:** Vercel deploy `0320f92` ("fix(smoke): market check warns (not fails) on green-but-empty TS proxy (SMOKE-MARKET-EMPTY)"), READY 14:36Z 06-07 — ~26 min after the night pass queued it.

**Suggested action:** move SMOKE-MARKET-EMPTY from Queued → Shipped in `ledger.md` (revert: `git revert 0320f92`); NEXTJS-4's 24h-quiet clock effectively starts 14:36Z 06-07 (mark resolved after ~14:36Z 06-08 if no new events). No code work needed.

## 3. C3-WATCH · [LOW · watch-only] v_offer_sanity_flags slightly above recorded day-range

**Source:** sweep query 18:1xZ — `v_offer_sanity_flags` = **148** vs C3 baseline 132 (recorded day range 118–138).

**Read:** +7% over the range max; NOT a step change (C3 says flag on step-change only). Logging so the next runs have the datapoint; if it keeps climbing run-over-run (>~200), treat as a real C3 trigger and investigate the offers writers.

## 4. PACKEV-TREND · [INFO for queued PACKEV-THROUGHPUT] staleness now FALLING under v21 at batch=4

**Source:** `topshot_pack_ev_targets` 18:1xZ: stale-24h **577** (was 665 at 14:00Z baseline, 547 at 00:50Z), stale-48h **242** (was 330), oldest `last_ev_at` advanced 06-04 14:39Z → 06-05 01:38Z.

**Read:** the night pass's "staleness compounding" extrapolation has reversed — v21 at batch=4 is now draining the queue, not just holding. PACKEV-THROUGHPUT (batch 4→8–12) may still be worth it for 24h-freshness on all 800 targets, but it's no longer urgent; decide with this trend in hand.

---

## Health summary (sweep, 18:05–18:28Z)

- Pipelines: 8 `ok=false` rows in 6h, all single-fail known rush class (12:0xZ ×7 + evm-transfers-ingest 18:00Z); `detect_stalled_pipelines()` `[]`; `get_pipeline_alerts()` `[]`. Key writers fresh: fmv-recalc 17:48Z ok, pack-ev 17:55Z, allday-fmv 17:42Z, TFP 12:50Z ok (next ~18:50Z; TFP-WATCH gate intact), sales TS 17:40Z / AllDay 18:00Z (3,833 / 282 per 24h).
- Sentinel TS-UUID-48h: **1,989** — back under the 2,000 CRITICAL floor (pure roll-off as predicted; 0/hr new; ts editions flat 15,541).
- Security: RLS-off 0; anon/auth write-grants on RLS-off base tables 0.
- FMV vs 14:10Z baseline: TS HIGH+MED 2,988 (3,008, churn-flat), NO_DATA 5,354 (5,444, improving); AllDay HIGH+MED 493 (flat). DB 6,404 MB (6,398). unmapped 183 (flat).
- Vercel: 20/20 READY incl. 8 new prod ships 14:29–17:23Z (SMOKE-MARKET-EMPTY fix, cross-collection /insights/deals `9d5113c` — backing view returns 337 rows; verify-rebuild `6c09e36` + referral `ba8a28e`; rewards gift-to/merch `cc8228`; profile declutter `0dfb01d`). No new Sentry events from the wave.
- Artifacts: 16/16 validated healthy (rpc-tracked-fmv-confidence passes on calm-DB retry — see item 1; rewards-console views return data; insights-health bounded legs OK: squeeze 500+, pack_reality_dist 6, deals 337). traction note: `outbound_clicks` 7d = 0 — plausible at current allowlist size (13 active), not breakage.
- NOT probed under vacuum load: the cross-collection cohort GROUP BY (night pass verified 4h ago; backing tables healthy).

*(Run note: this file is on-disk only — sandbox commit blocked by a stale un-removable `.git/index.lock` dated 06-07 00:52Z, `rm` → `Operation not permitted`. Q7 confirmed again; `rip-packs-city-bot` clone still not mounted. Local HEAD at sweep time: `0dfb01d` = current prod.)*
