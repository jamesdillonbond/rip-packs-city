# Daytime monitor inbox — 2026-08-07T21:08Z (~14:08 PDT, mid-afternoon tick)

Health: green-with-known-noise. Security 0/0/0/0; `stalled_pipelines` []; Sentry 0 new/24h; Vercel prod READY (latest READY tip = dashboard indexing-poll fix `a0e6362b`; the 2 newer CANCELED deploys are docs-only, expected). DB 12,392 MB. sentinel TS-UUID-editions 48h = 0.

Almost everything flagged this run is ALREADY tracked/resolved — recorded here so the night pass does NOT re-raise:
- **panini_fmv_stale_hours → RESOLVED.** The 14:58Z monitor's MEDIUM candidate (home-box runner dark ~37h) has cleared: metric now 0h / ok. Home box is running again. Do not re-flag.
- **candy-offers silent → RESOLVED today by Claude Code** (freshness 64.6h→0.21h, 72/72 bidder coverage; stale-offer deactivation still suppressed under ME throttling — that partial is fully documented in the 2026-08-07 ledger entries). Not a new finding.
- **4 trust breaches, all known/by-design:** `panini_sale_price_capture_dry_days`=10 (upstream Panini price-capture outage, tracked); `public_board_slow_count`=4 (crawler contention; board-probe max_ms rebaseline already QUEUED, "do not re-budget under crawler load"); `ufc_fmv_stale_hours`=86.6 (UFC market closed by design); `unmapped_resolution_backlog_max`=175 (AllDay actionable backlog — pipeline_alert confirms NET-DRAINING, outflow 1039 vs inflow 8 /24h + historical-backfill inflow).
- **pipeline_alerts** all map to tracked items: `allday-lock-refresh` 66% timeout (07-20 ALLDAY-LOCK-REFRESH-SELECTION-COST, owner-gated index); `snapshot-institutional-wallets` 50% timeout (on today's edge-fn drift worklist — has an undeployed crash+concurrency fix); `sync-nba-projections` 100% all_upstreams_failed (seasonal — no NBA in August; deploying won't fix); `topshot-active-listings-ingest` 75% egress_blocked (INVESTIGATED today, intermittent Atlas-WAF class, "do NOT suppress"); `lock-check-batch` / `wallet-username-resolver` / `topshot-pack-opens-history-backfill` (known timeout/500 classes).

## Candidate (1) — LOW · verify today's page-metering mitigation reduced the DB statement-timeout contention cluster
- **Source:** `check_pgcron_recent_failures()` — 3 recent failures, ALL `canceling statement due to statement timeout` on heavy refresh/backfill jobs: `rpc-refresh-misattrib-candidates` (15:35Z, 1/1), `rpc-refresh-topshot-edition-median` REFRESH MV CONCURRENTLY (18:10Z, 1/4), `rpc-backfill-pinnacle-acquisitions` (18:17Z, 1/4). Plus the many `statement timeout` pipeline_alerts (allday-lock-refresh, lock-check-batch, wallet-username-resolver, allday-buyer-backfill).
- **Risk read:** intermittent (1-of-4 for two of the three), no data corruption — these are idempotent MV refreshes / re-runnable backfills that succeed on the next uncontended tick. This is the daytime pooler-contention class, NOT a broken deploy.
- **Why it's worth a night-pass line (not a same-tick action):** the root cause (2026-08-01 AI-crawler unblock → ~78k unmetered page req/12h → Micro pooler exhaustion) was diagnosed AND mitigated by Claude Code **today** — `proxy.ts` now meters DB-backed page routes (deploy `acb5ff24`, READY ~19:28Z). These pg_cron/pipeline statement-timeouts are the exact symptom that mitigation targets. **Suggested action:** during the post-ship watch, compare the statement-timeout failure rate (pg_cron MV refreshes + the `canceling statement` pipeline_alerts) before vs after `acb5ff24`; if it hasn't fallen, the 120/min anon page cap may need tightening or the heavy MV refreshes rescheduled off the daytime crawler peak. Read-only observation; no autonomous DB/cron change proposed.

Artifacts: estate healthy at the data layer — the shared health payload (`rpc_ops_snapshot`, trust-health, per-collection FMV) read clean and non-empty; `rpc-live-health`'s payload query references only stable objects (fmv_snapshots/editions/pipeline_runs/collections/pinnacle_fmv_history/sales/cached_listings_v2/unmapped_sales — none renamed today) and carries its own `db_active>=15 → NULL` contention guard. No schema-breaking change shipped today (ships were client/route/edge-audit + one cadence-severity bump). Per-artifact HTML payload re-run skipped on this lighter mid-afternoon tick.

1a first-tick extras: SKIPPED — not the first tick (the ~8am/14:58Z monitor already ran today, commit 84cba78b).

> NOTE: inbox written to mount, push unavailable — the sandbox clone's working-tree checkout failed under /sessions disk pressure (90% full, known "no space" class). The night pass picks this up locally. No git run against the mounted .git.
