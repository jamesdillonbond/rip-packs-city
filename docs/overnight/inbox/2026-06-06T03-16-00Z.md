# Daytime monitor candidates — 2026-06-06T03:16Z

Run: `rpc-daytime-monitor` (read-only). No bot clone → shared repo. Lock stale (08:35Z Jun-5 night-pass leftover, ~19h old) → proceeded, inbox commit permitted. No `FREEZE.md`, no `focus.md`. Declined list empty. Prior undrained inboxes `2026-06-05T18-25-00Z.md` (rpc-insights-health / squeeze-count timeout) + `2026-06-05T21-06-00Z.md` (refresh-cross-collection cron unwired) read — their candidates NOT re-logged. **Two NEW high-priority candidates this run: a deploy-attributable Pinnacle Cadence-script regression, and the AF1 view re-broken (timeout) under DUPE1 growth.**

---

## P1 — [HIGH · deploy-attributable regression · operator/CC (route code — NOT night-pass-shippable)] `pinnacle-metadata-backfill` fails "cadence: Invalid character" since `b6005cb` (serial-backfill ship)

- **Title:** every `pinnacle-metadata-backfill` tick on the new code fails at the Cadence layer — `cadence: Invalid character`, `catalog_upserted=0`, `serials_filled=0` — halting the PIN-CAT catalog create/repair queues AND the brand-new Q5 serial backfill it shipped with.
- **Source:** `pipeline_runs`. Timeline: hourly :22 ticks green for 14h+ (20:22Z cat_ups absent→43/4/11/46/55 across the evening — PIN-CAT working); deploy `dpl_DaDER8qNy9b2NJpZoN1d5JZSAqp5` commit `b6005cb` ("backfill wmc.serial_number from on-chain Pinnacle.NFT.serialNumber" — extends `PINNACLE_METADATA_SCRIPT` + PinInfo struct) READY **01:23:13Z**; 01:22:13Z tick (still old code) ok `cat_ups=43`; **02:22Z tick — first on new code — FAILED `cadence: Invalid character`** with zero work done. 1/1 post-deploy ticks failed at write time; the 03:22Z tick is the confirmer (see addendum at bottom of this file).
- **Likely cause:** the b6005cb extension of the Cadence read script introduced a character-level defect — either a stray/invalid character in the extended script source, or an encoding regression on the script transport (CLAUDE.md footgun: Cadence must be base64'd via `Buffer.from(str,'utf8')`, NOT `btoa()` which breaks on non-ASCII — "Invalid character" is the classic symptom of both classes). The PIN-CAT half (`373967d`) is NOT the suspect — it ran green 20:22Z–01:22Z.
- **Blast radius:** Pinnacle catalog/metadata/serial freshness only (the 23-key uncatalogued drain + ~231 stub repairs + the 21k serial backlog stop draining; mint/disagreement queues also dead since all queues share the one script). NO outage; Pinnacle FMV unaffected (separate `pinnacle_fmv_snapshots` + `populate-pinnacle-wmc-fmv`, both green). Hourly ops-noise (`ok=false` every :22) until fixed.
- **Risk read:** fix is route code (`app/api/pinnacle-metadata-backfill/...` script string) → off-limits to the night pass. Revert is clean: `git revert b6005cb` (additive feature, no migration shipped with it).
- **Suggested action:** operator/CC — diff `PINNACLE_METADATA_SCRIPT` in `b6005cb`, run the extended script standalone via the Cadence MCP against a known Pinnacle wallet (the handoff's own Step-1 pattern), fix the bad character / encoding, or revert. Verify: next :22 tick `ok=true` + `serials_filled>0`.

## P2 — [HIGH per §1b · broken artifact (regressed repair) · night-pass-shippable] `v_tracked_wallet_fmv_confidence` times out again (57014 ×2) — AF1's fix has been outgrown by DUPE1 in <19h; `rpc-tracked-fmv-confidence` artifact presumed broken again

- **Title:** the view the night pass optimized last night (AF1, `audit_20260605_v_tracked_wallet_fmv_confidence_lateral`, subagent-verified PASS ~08:00Z; monitor-confirmed fast at 18:25Z and 21:06Z) now exceeds the default statement timeout again: `SELECT confidence, count(*) ... GROUP BY 1` → **57014 at 03:06Z AND 03:15Z** (two probes, 9 min apart).
- **Source:** monitor probes this run; ledger AF1 target metric ("view query returns < statement_timeout") now FAILING.
- **Likely cause:** not the view shape — the data grew under it. DUPE1's inert re-mint added ~3,100 TS editions since the 08:15Z verification (11,498 → 13,905 @21:06Z → **14,599** now) and ~2,964 inert NO_DATA `fmv_snapshots` rows, fattening exactly the per-edition LATERAL + NO_SNAPSHOT-bucket work the view does. **Pattern (called in the 18-25Z inbox, now confirmed): DUPE1 growth is pushing FMV-snapshot-joining views over the timeout one by one** — `topshot_squeeze_board` count crossed yesterday (logged), AF1's view is the second casualty in 24h; my squeeze count DID complete this run (3,138) so that one is borderline/load-dependent rather than hard-broken.
- **Blast radius:** internal artifact only (0 production-route readers, grep-verified in the AF1 ship). But the artifact was THE thing AF1 repaired — it is now presumed broken on open again.
- **Risk read:** LOW for a night-pass re-optimization or a cheaper artifact query (additive/reversible, no public surface). Treadmill warning: re-optimizing views one at a time while the DUPE1 writer keeps minting is whack-a-mole — the durable relief is the off-limits worker fix (Item B2) and/or stopping inert rows from receiving NO_DATA snapshot stamps.
- **Suggested action (night pass):** re-EXPLAIN the view under current data; either tighten further (e.g. pre-filter held editions to int-pair external_ids so inert UUID editions never enter the LATERAL), add a bounded/materialized variant, or cheapen the artifact's embedded query (`update_artifact` is night-pass-permitted). Also consider one sweep over remaining heavy FMV-joining views (deals/offer-spread/first-mint counted fast this run) and noting in DUPE1 that it now has a measurable second-order cost (view timeouts + DB +129MB/19h).

## P3 — [MED · operator (external cron) · recurrence] `pinnacle-resolve-buyers` silently dropped ~7.3h of ticks (08:00→15:20Z Jun 5) — second multi-hour dropout in a week

- **Source:** `pipeline_runs` gap scan (max gap 440m in 26h vs its ~20m cadence; watchlisted at 180m, so `detect_stalled_pipelines()` would have flagged it during the window — same class the 2026-05-31 ledger entry caught at 284m). Fully recovered on its own (regular ok=true ticks 15:20Z → 03:00Z now; 28 runs/26h, 0 fails).
- **Risk read / action:** no current breakage; recurrence pattern says the cron-job.org entry is flaky (same external-trigger class as Q3/N1). Operator: check/recreate the entry, or note-and-watch for a third occurrence. Not night-pass-shippable (external cron).

## P4 — [LOW · operator/CC · update to Q5/A6 family] smoke run 00:17:08Z Jun 6 mass-failed 7 checks on infra timeouts; A6's single retry insufficient; 2 NEW Sentry issues

- **Source:** Sentry NEXTJS-1E + NEXTJS-14 event details (both 00:17:08Z, release `46ec1fb`): detail = "rpc error: canceling statement due to statement timeout" (the smoke's `detect_stalled_pipelines()` RPC call itself timed out — NOT a real stall; live check this run = `[]`) and "Timed out acquiring connection from connection pool" (NOT a real Pinnacle FMV leak). Same-run casualties: NEXTJS-12/A/4 re-fired + NEW **NEXTJS-W** (`/disney-pinnacle/overview` 200) + **NEW NEXTJS-E** (`/nfl-all-day/overview` 200), 1 event each. Window = the 00:00Z cron rush + 6-deploy churn 23:57Z–01:23Z.
- **Risk read / action:** no platform break — pure cry-wolf, but it manufactures new Sentry issues each time. A6 shipped 1 `rpcRetry`; evidence says insufficient under combined rush+deploy contention. Options (smoke route code, operator/CC): second retry with backoff, move the GHA smoke schedule off :00/:15-ish rush minutes, or demote DB-backed assertions to warn-tier during a detected contention burst. Fold with queued Q5. The 8-issue overnight cluster never reached 24h-quiet (recurred 00:17Z); NEXTJS-W/E/1E/14/12/A/4 all markable resolved only after a genuinely quiet 24h.

---

## Health snapshot (night-pass context — NOT new candidates)

- `detect_stalled_pipelines()` = `[]` live. Security **0/0** (RLS-off; anon/auth write grants on base tables, `relkind in (r,p)`). Sitewide deploys: current prod `b6005cb` READY (but see P1); **1 ERROR deploy 00:14Z** = `949e10f` docs+guard commit, superseded in 5min by `94ba491` ("one-shot mount-corruption cleanup") — mount-corruption build blip, Q7-class evidence, no action.
- **DUPE1 (queued, escalated — trend update):** sentinel TS-UUID-48h **5,310** (2,611 @08:15Z → 4,393 @18:15Z → 4,758 @21:06Z → 5,310 @03:16Z; ~90-130/hr, still decelerating, still CRITICAL). TS editions **14,599**; TS NO_DATA **4,974**; DB **6,107 MB** (+129/19h). All inert/trigger-held; TS HIGH+MED **3,041** still improving (2,944 → 3,037 → 3,041) — no pricing regression. NEW second-order cost: view timeouts (P2). Durable fix remains off-limits worker code (Item B2); do NOT raise the sentinel threshold.
- **Recent-ship checks:** MON-WATCH (`offers-sweep` 40m max gap, `allday-fmv-populate` 60m — within 120m, no false positives) + MON1 (`fmv-recalc` 40m max gap, 92 runs/26h) ✓. Wallet-coverage ships live and working: `a2cae0d` image denorm (03:00Z TS tick `rows_imaged=49,655`, `image_error:null`) + `b072ce7` FMV-drift refresh (AllDay tick `rows_updated=4,997`) ✓. `d547c30` catalog-backfill front-load: no errors observed. PIN-CAT was green through 01:22Z, then P1 (the NEWER ship broke the shared script).
- **Pipeline fails last 6h** (besides P1): 1 each, all known transient classes — hydrator `candidate_read` timeout 02:02Z (queued N2, recurring at rush ticks), `pinnacle-nft-resolver` pool timeout 00:10Z, `sync-nba-projections` upstream timeout 00:07Z.
- **Artifact validation note:** artifact HTML lives under the user's OneDrive path which this sandbox cannot read (outside connected folders) — validated via backing queries instead: insights views (squeeze 3,138 — completed this run, borderline per the 18:25Z inbox; deals 351; offer_spread 5,622; rookies 61; market_idx 555; pack_reality stats 1), `cross_collection_cohort_mat` 144, `edition_offers` 9,379 (climbing ✓), `v_rewards_economy` 1 row ✓, `get_rpc_traction_snapshot()` fresh (13 users; funnel events flowing, last 08:51Z Jun 5) ✓, Vercel/deploys ✓, FMV dist ✓. **Broken/at-risk: `rpc-tracked-fmv-confidence` (P2, hard-fail ×2), `rpc-insights-health` (prior P1, borderline).** Other 14 presumed healthy via backing data.
- Drift vs 08:15Z baseline: AllDay HIGH+MED 489 (flat), AllDay/Golazos/UFC editions flat, unmapped_sales 209 → **239** (mild, watch).

---

## Addendum — 03:22Z confirmation tick (filled in before commit)

- 03:22Z `pinnacle-metadata-backfill` tick: **FAILED identically** (`cadence: Invalid character`, cat_ups=0, serials=0, ~6s). **P1 regression CONFIRMED — 2/2 ticks on `b6005cb` failed; 0 failures on prior code.** Every hourly :22 tick will fail until the script is fixed or the commit reverted.
