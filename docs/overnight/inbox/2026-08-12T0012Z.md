# Daytime monitor — 2026-08-12T00:12Z (≈17:12 PDT Aug 11)

Written to the mount UNPUSHED (workspace shell down — `useradd … /sessions no space`, 5th consecutive incident → no git clone/commit this run). Night pass / Trevor picks these up locally.

Health this tick: **green-with-known-saturation-noise.** Security invariants `[]` clean. 3 trust breaches all KNOWN & matching the 08-11 nightly (panini_sale_price_capture_dry_days=14, public_board_slow_count=12 [was 16, improving], unmapped_resolution_backlog_max=215 net-draining). Latest prod deploy `dpl_D46tcxkhcXmfRJXJjUtmqiAgvzaY` READY (708145e7 smoke-test fix); the many CANCELED deploys in the list are just superseded rapid pushes from today's heavy CC coverage session — no ERROR. `detect_stalled_pipelines()` = pack-opens backfills silent ~21h (**documented no-op-walk false positive per 08-11 nightly — NOT a finding**) + moments-hydrator info 47min (noise). A ~21:30Z→00:11Z DB disk-IO saturation window drove the usual broad connection-pool/statement-timeout cluster (wallet-backfill row-loss, candy board timeouts, entity-page degrades) — all known class, self-healing.

---

## Candidate 1 (CHARACTERIZATION of the 08-11-nightly-queued item) — `/api/public/ipfs-media/[cid]` 5xx: upstream IPFS gateway timeout, chronic, fails soft

The 08-11 nightly queued "ipfs-media 5xx elevated (103/6h) → daytime monitor to characterize." Characterized:

- **Signature:** Vercel error cluster `TimeoutError: The operation was aborted due to timeout` on route `/api/public/ipfs-media/[cid]`, count=111 / users=32 over 24h, **first seen 2026-07-06**, last 2026-08-11T22:13Z. Status breakdown over the last 6h on that path: **75× 502 / 41× 200 / 11× 302** (~12–13 502/hr).
- **Read:** the route is an IPFS pass-through proxy that fetches a CID from an upstream IPFS gateway under an AbortController timeout; when the gateway is slow the fetch aborts → route returns **502**. This is an **UPSTREAM gateway latency** condition, not an RPC-side defect. It is **chronic** (>5 weeks, since 07-06), **low-rate**, and **fails soft per-image** (one CID's thumbnail doesn't load; no page-down, no 5xx on a whole route). Blast radius: 32 users/24h seeing an occasional broken media tile.
- **Source:** Vercel `get_runtime_errors` (24h) + `get_runtime_logs group_by=statusCode query=ipfs-media` (6h).
- **Risk read:** LOW user impact; **not urgent**. But it is a genuine soft-degradation on a public read path and has no current mitigation.
- **Suggested action (code change → night pass / operator, not a monitor fix):** one of (a) raise the per-fetch timeout, (b) add a fallback IPFS gateway on abort/5xx, or (c) short-TTL cache successful CID fetches so a slow gateway tick doesn't re-hit for a hot CID. (b)+(c) together give the best resilience. Verify by rendered image load, not HTTP 200.

## Candidate 2 (secondary) — pg_cron `rpc-refresh-allday-pack-realized` failing 3 of last 4 ticks

`check_pgcron_recent_failures()`: `rpc-refresh-allday-pack-realized` = **3 fails / 4 runs**, last 2026-08-11T18:35Z, `canceling statement due to statement timeout` on `REFRESH MATERIALIZED VIEW CONCURRENTLY mv_allday_pack_realized`. Same statement-timeout-on-MV-refresh class as the queued MV-refresh cluster (08-08 inbox 1945Z: misattrib-candidates, thin-sale-ask-disclosure also failed again this tick — 1/1 each). **Likely already in the queued 5-MV cluster**; flagged here because `allday-pack-realized` is the most persistent (3/4) and its MV backs the AllDay pack-realized board → that board may be stale under sustained saturation. Same fix lever as the cluster (index/partial-covering to cut the refresh cost, CONCURRENTLY-only, quiet-window build). LOW; no monitor action. `rpc-refresh-new-collectors` also failed once (`job startup timeout` = pool saturation at launch — one-off, ignore).

## Candidate 3 (secondary, niche) — TopShot base wallet-backfill `computation exceeds limit` on mega-wallet `0xe1f2a091f7bb5245`

`/api/wallet-backfill` Flow script `col!.getIDs()` returns Flow error 1101/1110 `computation exceeds limit (100000)` on `TopShotShardedCollection` for wallet `0xe1f2a091f7bb5245` (6 events, last 2026-08-11T21:27Z). This wallet's single sharded-collection shard is too large for one `getIDs()` call. The paginated walk exists for AllDay mega-wallets but the base TopShot backfill hits this on this specific wallet. NICHE (one wallet, 6 events); noting so it isn't re-derived. Fix = paginate/shard the TopShot getIDs read for oversized collections → code change, not urgent.

---

**Environment escalation (5th consecutive night):** the Cowork workspace shell is down (`useradd … /sessions no space left on device`), so this monitor could not git-clone or commit — inbox written to mount only. DB (Supabase MCP), Vercel + artifact tools all worked. Operator fix (delete old Cowork sessions to free `/sessions`) per `docs/handoff-2026-08-09-cowork-shell-recovery.md` is still needed to restore overnight/monitor push capability.
