# Daytime monitor — 2026-07-12T00:25Z (overnight-edge tick, ~17:25 PDT Jul 11)

Platform GREEN. No clock skew (shell 00:06Z ≈ DB now() 00:17Z; newest sale 00:13Z / fmv 00:09Z — ingest FRESH, NO cron-job.org dropout this run). Security 0/0/0/0 (invariants [] · secdef_anon [] · rls_off 0 · sentinel TS-UUID-48h 0). Vercel prod latest READY = `643f34ff` (CI vitest gating). Sentry 2 unresolved, both known smoke false-fails.

## NEW candidate (1) — TOPSHOT-IMPOSSIBLE-PARALLEL-SERIALS-BREACH re-accumulated to 11/3 (LOW-MED; trust-health BREACH; night-pass reconcilable)
- **Source:** `v_rpc_trust_health` — `topshot_impossible_parallel_serials` = **11** (threshold 3), status BREACH. Only breach in the view; other 15 metrics ok.
- **What:** the known self-healing `::`-cataloging straggler class — freshly-cataloged parallel editions whose floor-seeded `circulation_count` sits below a real observed sale serial (serial > parallel circ = "impossible"). Invisible to editions-flat / UUID / FMV sentinels (F1 parallel mis-attribution). Guarded at all 4 writers, so NO bad-sales harm — this is source-data hygiene, not a live regression.
- **Why now:** driven by the active `::` subedition cataloging wave (TS editions **19,210**, +84 since the 15:10Z tick, +937 over 07-08). The authoritative per-parallel circ backfill reconciles stragglers but is lagging the cataloging inflow, so the counter has climbed 0 (07-10) → 11. This is the highest recent value (prior peaks 4/3 on 07-10, 3/3 on 07-08).
- **Risk read:** LOW — self-healing class, no user-facing/FMV harm, writers guarded. Not on the Declined list; actively cleared before (07-10).
- **Suggested action (night pass):** either (a) TRACK and let the per-parallel circ backfill drain it, or (b) apply the evidence-based circ floor-raise reconcile exactly as the 07-10 interactive session did — precedent migration `audit_20260710_circ_floor_raise_impossible_parallel_stragglers` (a sale at serial N proves circ≥N; audited + reversible). CAVEAT: the ledger's "do NOT hand-edit circ on a sales-adjacent table" refers to BLIND autonomous edits; the 07-10 circ-floor-raise was evidence-based + reversible and is the sanctioned pattern. Night pass's call whether it clears to ≤3 quickly enough on its own to skip a ship.

## Carried / known — DAYTIME-CONTENTION pg_cron cluster (no new action; already logged 21:16Z, self-recovers)
- `check_pgcron_recent_failures()`: `rpc-backfill-historical-pack-ev` (14 fails/144 runs, statement timeout, last 00:03Z — jobid 43, "closed self-resolved 07-07" now re-tripping under the evening/overnight contention window; same family), `rpc-allday-cross-source-sales-dedup` (6 fails, statement timeout — CROSS-SOURCE-DEDUP), `rpc-remap-misattributed-sales` (2 fails, statement timeout, last 18:23Z — DAYTIME-CONTENTION). All statement-timeout in the ~18:00–00:00Z contention window; succeed outside it. Not a growing-table cliff.
- `detect_stalled_pipelines()`: `classify-acquisitions-multicollection` (242m/180 med — CLASSIFY-ACQ family), `topshot-moments-hydrator` (96m/30 info — TOPSHOT-MOMENTS-HYDRATOR-GETMINTEDMOMENT-ERRORS, self-limiting; CC fix `c1ba51e` committed local, awaits worker deploy per 07-11 ledger). Both carried.
- Sentry: `JAVASCRIPT-NEXTJS-1J` (smoke: pack-dist page Sales History, 30 ev/23h) + `-1E` (smoke: sales-indexers detect_stalled, 4 ev, last 10h ago) — both known smoke false-fails, no new class.

## RESOLVED since the 21:16Z tick
- **ORIGIN-MAIN-TREE-WIPE-20260711** (CRITICAL at 21:16Z) is RESOLVED — origin/main tree restored via `f001113` ("Replaces ac138b0 which wiped the tree; force-with-lease over it"), and every subsequent commit (concierge cf76857, badge art c53f0e4, CI vitest 643f34ff) built on the full 2,118-file tree with READY prod deploys. Current prod builds clean. Today's several ERROR-state deploys are all superseded intermediates from the heavy 07-11 interactive session (sitemap-split iterations dff6a7f/ac138b0/1c51d08, concierge f9ee7bf → fixed by cf76857) — none is the current prod.

## Drift (headline)
- editions TS **19,210** / AllDay 6,190 (Golazos/UFC unread this tick — contention). FMV TS H+M **5,202** (band-normal; was 5,232 @ 07-11 overnight, 5,137 @ 21:16Z — churn, not sliding).
- DB **11,019 MB** (~+1.5 GB since 15:10Z's 9,563 MB) — continued fill from Trevor's TopShot+AllDay historical pack-opens backfills (`rips_total` ~1.22M and climbing per 07-11 CC ledger). Benign but WATCH the growth tail on PRO Micro.
- NOTE: DB under real contention this tick — `v_rpc_trust_health`, editions-all, and several aggregates timed out on first attempt (5 active pgrst_call RPCs, ~15–25s each); all values above obtained on retry. Trust-health is a first-tick-of-day extra; captured anyway.
