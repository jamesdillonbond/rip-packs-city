# Daytime monitor inbox — 2026-06-18T14:11Z (07:11 PDT, first tick of day)

Run: rpc-daytime-monitor (READ-ONLY). Platform GREEN except one escalating watch.
Health: security 0/0/0/0; trust 9/9 ok (no breach, DQ4 leg 0); detect_stalled = 1 (buyer-backfill, below); get_pipeline_alerts = buyer-backfill + golazos_sales info (benign); cross-collection refresh HEALTHY (both pg_cron steps succeeded today — step1 04:10Z cohort 158, step2 04:25Z overlap 246, fresh); sentinel TS-UUID-48h 0; Sentry 0 new/14h; Vercel 0 ERROR (prod ac50ae1b READY, heavy Trevor alerts/jersey ship wave); 14 active artifacts (rpc-live-health insights panel + rpc-offers-intelligence backing objects spot-validated GREEN; rest green by prior ticks + additive-only ships); editions flat (15543/6191/581/446); DB 4807 MB (+69 benign); FMV TS HIGH+MED 3126.

## NEW candidate

### 1. BUYERBF-DURATION-CREEP — buyer-backfill runs creeping into the 600s maxDuration cap; 198m silent on a 25,427-row backlog [HIGH · CC/operator]
- Source: detect_stalled_pipelines() + get_pipeline_alerts() both flag topshot-buyer-backfill (medium); pipeline_runs duration trend.
- Evidence: last logged run 2026-06-18 10:54Z ok=true at 577,266ms = 577s — only 23s under the 600s maxDuration cap set by 95c07c5. Duration is climbing across recent runs: 503s peak on 06-17 (~97s headroom) -> 566s on 06-17 21:19Z (~34s) -> 577s now (~23s). Pipeline silent ~3h17m at run time (vs the ~140m degraded cadence the 06-17T21-19Z tick measured), and NO ok=false row exists — consistent with a possible FIRST invisible >600s maxDuration hard-kill (the no-finally-block-log silent-failure class). Backlog is NOT drained: 25,427 TS null-buyer sales remain in sales_2026, so the engine genuinely should still be running.
- Framing: this is the exact "re-approach risk" the CLOSED BUYERBF-CRON-DROP ledger entry named ("re-approach risk if the null-buyer backlog hits heavier wallets"). It is NOT a re-queue of BUYERBF-CRON-DROP, and per that entry's explicit guidance it is NOT a request to widen the 90m watchlist threshold.
- Risk read: LOW-to-MED blast radius — no outage, no user-facing break; FMV/insights independent. But buyer-backfill is "the engine" for TS buyer/payer/proposer resolution; an undetected invisible stall silently halts the 25k drain and degrades marketplace buyer coverage.
- Suggested action (night pass / CC): (a) confirm whether a run after 10:54Z actually fired-and-died >600s vs the cron simply dropping a tick — check Vercel runtime logs and whether null_buyer_ts_2026 is still ticking down; (b) the durable lever is reducing per-run work, NOT widening the threshold: lower buyer-backfill batch 200->150 (CC, route-code), or raise its maxDuration toward the 800s Pro hard cap for headroom — both CC/ingest-adjacent, not night-pass auto-shippable; (c) re-confirm cron-job.org job 7776255 is still firing.

## Continuing watches (context for already-logged items — NOT new candidates)
- ALERTS-DISPATCH-DEAL-TIMEOUT (logged 06-18T00-06Z): recurred ONCE today — alerts-dispatch "deal: canceling statement due to statement timeout" 13:29Z (49,979ms). BUT it landed inside a transient 13:15-13:29Z DB-contention window that ALSO timed out check-alerts (48,578ms) and wmc-fmv-populate (245s); all three recovered on their very next tick. Reads as collateral of a brief contention spike, not a clean per-sub deal-scan recurrence — but it means the item is NOT yet safe to downgrade (hold the 03-05Z "likely downgradeable" call open one more cycle).
- SERIAL-FMV-MULT-CRON (carried/queued, Trevor/operator): serial_fmv_multipliers now 63.2h stale (last 2026-06-15 23:06Z, 37 cells), still climbing (33h -> 46h -> 49h -> 52h -> 63h). No refresh cron wired. Mid-build dial-in.
- FMV-HIGHMED-DIP-WATCH (queued 06-17): RECOVERED — TS HIGH+MED 3126 (up from the 2848 06-17 dip), ASK_ONLY 2623 stable. Daily-cycle recovery confirmed; closeable.
