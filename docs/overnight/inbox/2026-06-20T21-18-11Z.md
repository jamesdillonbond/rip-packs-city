# Daytime monitor inbox — 2026-06-20 ~21:18Z (14:18 PDT tick)

Platform **GREEN**. security 0/0/0/0 (RLS-off [], anon-write-on-RLS-off [], `check_public_security_invariants()` [], `check_secdef_anon_execute_violations()` []); `detect_stalled_pipelines()` []; `get_pipeline_alerts()` []; **trust-health 9/9 ok**; sentinel TS-UUID-48h 22 (ok); Sentry 1 unresolved (transient, see Note D); Vercel **0 ERROR**, prod `3613a94` READY. Editions reconcile exactly (TS 16,917 = 15,543 + 1,374 `::`; AllDay 6,191 / Golazos 581 / UFC 446 flat). FMV improving as Stage B intends — **TS HIGH+MED 4,227** (HIGH 1,192 + MED 3,035; was 3,332 night-pass baseline -> 4,159 last tick -> 4,227 now), ASK_ONLY 1,823 (down from 2,421), NO_DATA 3,382; `fmv-recalc` ran ok **21:08Z**, 0 fails/24h; FMV reconciles to the 16,917 editions. All **16** active artifacts' backing objects return rows (deals_board 700, serial_premiums 183, perfect_mint 11, squeeze 9,102, trophies 683, top_sales 903, tracked_fmv 22, offer_sanity 246, underpriced_serials 14, special_serial_mv 7,307). DB 4,997 MB (+98/13h vs 4,899 night baseline — benign).

This was a quiet, post-ship-watch tick over today's heavy Stage B / Phase 3 CC wave. **One candidate is a reconciliation of last tick's CANDIDATE 1 vs CC's later ledger note (resolved by hard evidence)** + two updates to already-queued items + positives. No brand-new incident.

---

## CANDIDATE A (MEDIUM) — `refresh-conflated-editions` DOES error via its route (`sql_safe_updates=on`), reconciling last tick's CANDIDATE 1 against CC's "fn is correct / never-called" ledger note

**Source:** pipeline `refresh-conflated-editions` — its single route fire today (`2026-06-20 15:17:13Z`) logged **ok=false, error `"DELETE requires a WHERE clause"`**, wrote nothing. The 11:08Z monitor tick logged this as CANDIDATE 1 ("DELETE without WHERE -> guard frozen at 741"). CC then refreshed the guard manually and wrote in `ledger.md` (line ~21): *"the fn is correct (DELETE-all + re-INSERT...); the monitor's 'DELETE-without-WHERE bug' read was imprecise; the issue was never-called."* **These appear to contradict — the reconciliation:** the route's service-role execution context has `sql_safe_updates=on`, so the function's unqualified `DELETE FROM public.topshot_conflated_editions;` errors there (the 15:17Z proof); CC ran the same fn via the MCP/SQL path where `sql_safe_updates=off`, so it succeeded (guard -> 39, then 46). **Both observations are individually true; the fn is NOT safe via its cron route.** Not on the Declined list.

**Current state (this tick):** guard table `topshot_conflated_editions` = **46 rows** (CC's manual refresh landed — the acute 741-row over-suppression symptom is RELIEVED; TS deal board recovered to 700). So the *product harm is gone for now*, but the automated daily refresh is still broken: any future cron fire errors and writes nothing, so the guard will silently go stale as conflation drifts (CLAUDE.md Phase 4 notes it rising 30->39->46, not converging to 0).

**Risk read:** LOW-risk fix, bounded consequence. No corruption / security / FMV-writer / auth impact. If the operator wires the requested daily `refresh-conflated-editions` cron (CC's stated next step) WITHOUT this fix, it will log ok=false every day under `sql_safe_updates`, exactly as the 15:17Z fire shows.

**Suggested action:** one-line fix to the fn body — `DELETE FROM public.topshot_conflated_editions WHERE true;` (satisfies `sql_safe_updates`) or `TRUNCATE public.topshot_conflated_editions;` — via `CREATE OR REPLACE FUNCTION public.refresh_topshot_conflated_editions()` (SECDEF data-quality guard, service_role-only; preserve grants + search_path). Then a route fire should log ok=true and the guard should refresh to the live dup-serial count (~46). SECDEF guard fn, not off-limits — night-pass-shippable or quick CC migration. Pairs with the operator's "wire the daily cron" item — do the fix first. Revert: restore the prior (no-WHERE) body.

---

## NOTE B (update to queued REFRESH-SPECIAL-SERIAL-OWNERS-MV-TIMEOUT) — the night pass's recommended fix (A) is now SHIPPED by CC today + deployed; just needs a post-fix cron run + watchlisting

The queued item (ledger line ~425, night-count 2) recommended fix (A): route `maxDuration` 120->~200 + fn `statement_timeout` 180->~210. **CC shipped exactly that today:** commit `3613a94` raises `app/api/cron/refresh-special-serial-owners-mv/route.ts` `maxDuration` 120->**240** (verified in deployed code, prod `3613a94` READY) + migration `audit_20260620_special_serial_owners_mv_refresh_timeout_200s` (fn 180->**200s**). The MV is current: **7,307 rows incl. 1,715 `::` parallel rows** (up from the night-pass 5,929 — CC refreshed it after the remap; perfect-flags now per-parallel-correct).

**Two residuals for the night pass (not new bugs):** (1) **No post-fix cron run yet** — the only 2 runs in 7 days are the pre-fix failures at 00:32Z + 02:16Z (both `canceling statement due to statement timeout` at ~30s; none since 02:16Z, ~19h). Confirm the first post-fix automated fire logs ok=true, then the timeout item can close. (2) **`refresh-special-serial-owners-mv` is NOT on `pipeline_cadence_watchlist`** (the only special-serials watchlist row is the AllDay one shipped last night) — so a silent stall of this MV refresh is invisible to `detect_stalled_pipelines()`. Once a post-fix ok tick is observed (gate per the L1/BUYERBF rule), add a generous watchlist row (cron-job.org-driven, low cadence — the board captions holder as latest-seen, so a modest threshold is fine). LOW priority — MV data is current.

---

## NOTE C — positives confirmed this tick (post-ship-watch on today's wave, 0 reverts warranted)

- **Phase 3a circulation backfill (`22477b1`/`8459d38`) — first Vercel-cron fire ok.** New pipeline `topshot-subedition-circulation-backfill` fired its first scheduled run at **21:10:05Z (ok=true, ~70s)** — the daily Vercel cron (`10 21 * * *`) injected the secret correctly and the full-catalog `searchMarketplaceEditions` sweep completed. Healthy on first fire.
- **Last tick's CANDIDATE 2 (trust-health `offer_edition_gap_max_usd` $250 BREACH, LeBron 57:1909) — CLEARED** to $0/ok this tick (transient, as predicted; offers-sweep was never stalled). No action.
- **Stage B convergence healthy** — editions math + FMV de-blending + improving HIGH+MED all consistent with the intended outcome. The 1,374 `::` parallels reprice off `fmv-recalc` (canonical writer) on the normal sweep.

---

## NOTE D — knowns / no action

- **Sentry `JAVASCRIPT-NEXTJS-A`** ("smoke test failed: fmv pipeline healthy / analytics_pipeline_health", `POST /api/smoke-test`) — 1 event, ~20:54Z, Seer super_low. **Confirmed transient:** `fmv-recalc` ran ok 21:08Z (0 fails/24h), trust `fmv_sanity_flags` 0, editions reconcile. Same recurring smoke false-alarm class that "aged out" on 06-19/06-20 nights (matches the analytics-smoke 17:13Z fail that recovered ok at 17:43Z per the 11:08Z tick). Resolve after 24h quiet; watch only if it recurs across consecutive ticks.
- **pipeline_runs fails (6h):** `evm-transfers-ingest` x2 (Base 429 — benign STEER item, don't chase), `analytics-smoke` x1 17:13Z (recovered), `refresh-conflated-editions` x1 15:17Z (= Candidate A, no recurrence since). All transient/known.
- **`ts_uuid_dupes_created_24h` = 22/200 (ok):** known inert DQ4 dupe-writer leak (TS-WMC-UUID-FOSSILS/DUPE1); editions net-grew only by the +1,374 Stage B `::` rows, not UUID dupes. No action.
- **Artifact estate = 16** (4 beyond the 06-12 estate list, already noted last tick: `rpc-serial-premiums`, `rpc-fmv-dialin`, `rpc-ts-data-mission`, `pack-drops-ev-check` [Trevor's in-flight pack-drops build]). All backing objects return rows; validation this tick was a consolidated backing-object sweep (no schema break from today's add-column / Stage B migrations), not per-artifact payload execution.
