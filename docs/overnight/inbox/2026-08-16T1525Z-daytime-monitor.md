# Daytime monitor candidates — 2026-08-16T15:25Z (~08:25 PT, first-tick)

Written by `rpc-daytime-monitor`. Bash/git sandbox DOWN (9th consecutive day of the `/sessions` no-space `useradd` failure) → git clone/push unavailable; this file written to the MOUNT, push unavailable. The night pass picks it up locally.

Health this tick: security 4/4 clean (invariants `[]`, anon-write `[]`, 0 RLS-off). Cross-collection refresh fresh (cohort 04:10Z/179 rows, overlap 04:25Z — both pg_cron steps succeeded). Trust FMV legs healthy/stable (TS 55.7 / AllDay 27.0 / Candy 60.0 / Pinnacle 38.2 / Golazos 0.9 HM). No NEW trust breach — the 5 known-class breaches the 14:48Z night pass listed (fmv_sweep_wedge, trust_precompute, public_board_slow 12, panini dry 19, unmapped 258) persist. `topshot_impossible_parallel_serials` precompute stamped 00:59Z (~14h stale) = the known leg-8 partial-refresh / `trust_precompute_max_age_hours` issue, unchanged.

⚠ **ACUTE disk-IO saturation spell in progress (~14:55–15:10Z).** Last 60 min: 626 ok / 77 fail (11%), 48 connection-pool timeouts, ~4,583 `rows_lost` on wallet-backfill wmc-upsert chunks (idempotent — re-walks recover). It timed out my own `rpc_ops_snapshot`, `check_pgcron_recent_failures`, and live `v_rpc_trust_health` queries, and is throwing user-facing `get_edition_detail` / `get_player_detail` / `get_pack_detail_bundle` 45s timeouts on entity pages (escalated since 2026-08-15). This is the KNOWN chronic root cause (2 GB instance, `max_connections=90`, the `:13` cron-collision) — logged as a current CONDITION, not a new fix request; the `:13` cron-stagger remedy is already queued. **Artifact payload validation SKIPPED this tick** — running 11 heavy payload queries under active saturation would add load and yield false-broken readings; defer to a lower-saturation tick.

---

## Candidate 1 [MEDIUM] — a code commit is stranded UNSHIPPED by a saturation build failure + a docs-only push behind it

> ✅ **RESOLVED / CLOSE — verified 2026-08-16 15:20Z (Claude Code, interactive).** This self-resolved exactly as the suggested action anticipated. `ff3cae8` **is an ancestor of `77c28ac5`**, which built **READY** — so the stranded refactor shipped inside a later build and no redeploy is needed. Deployment states confirm it: `ff3cae8` ERROR, `77c28ac5` READY, `d9c564ee` BUILDING. **Do not redeploy for this.**
- **Source:** Vercel deploy `dpl_J4xD1YB7CfBtvwFABcBuhmU3SFuX`, commit `ff3cae8479885b3648ceaa02ff9cef3c74b6f627` ("refactor(client-pages): open the conversion workstream, gate ratchet 33 -> 31"), state ERROR, `errorCode=BUILD_UTILS_SPAWN_1` (`npm run build` exit 1). Build log: `Export encountered an error on /insights/deals/page`, preceded by every insights board logging `read exceeded 8000ms` — i.e. the documented saturation-ERRORs-prerender-build class (BOARD_LIVE_TIMEOUT_MS / insights-server-pages-bound-their-reads), NOT a real code error.
- **What happened:** built 14:59–15:02Z, ERRORed. The TIP of `main` is a LATER docs-only commit (`dpl_BWuEh…`, CANCELED — docs don't rebuild per `vercel.json` ignoreCommand). Current production alias `www.rippackscity.com` is held by `dpl_9JCC…` = commit **`1073691d`** (the commit BEFORE `ff3cae8`), READY. **So `ff3cae8` is not live** — its build failed and the docs push after it did not rebuild, exactly the "an ERRORed deploy is easy to miss; check the deploy state of the commit you shipped, not the tip" trap in CLAUDE.md. Both commits are from the concurrent CC Aug-16 client-page sweep.
- **Risk read:** LOW user-facing impact — `ff3cae8` is a test-gate refactor (dashboard/notifications `*Client.tsx` split + ratchet 33→31), no user-facing feature; prod on `1073691d` is fine. The cost is a stranded refactor + a ratchet lower not in effect. May SELF-RESOLVE if the CC session pushes another code commit (linear history rebuilds `ff3cae8`).
- **Suggested action (night pass):** re-check the current `main` tip vs prod first — if a later code commit has since built READY with `ff3cae8` in its history, this is already resolved, close it. Otherwise redeploy `main` via the v13 deployments POST once saturation eases (the build failure is transient); verify the resulting deploy is READY per commit, not just the tip.

## Candidate 2 [LOW] — client crash on the public profile page
- **Source:** Sentry `JAVASCRIPT-NEXTJS-2D` — `TypeError: Cannot read properties of null (reading 'style')`, culprit `/profile/:username`, 1 user / 1 event, first+last seen ~16h ago.
- **Risk read:** LOW — single occurrence, one user, has not recurred. Likely a missing null-guard on a DOM ref (a `.style` access on an element that can be null), plausibly a client bug rather than saturation. Distinct from the profile honesty class (this is a crash, not a false claim).
- **Suggested action (night pass):** low priority — grep `/profile/[username]` client for a `.style` access on a possibly-null ref (e.g. a `useRef().current.style` before mount); add the guard if found. If it does not recur, no action.

## Noted, NOT logged as new (already tracked)
- `candy-editions-ingest` stalled 1828m (missed the 08-16 08:40Z daily tick) — the KNOWN 300s-timeout class with an existing handoff (`docs/handoff-2026-08-04-candy-editions-timeout.md`) and a watchlist note; medium, editions change slowly. Recurrence, not new.
- `backfill-pack-rip-metadata` (196m) + `topshot-moments-hydrator` (47m) silent past cadence — both info-severity, both consistent with the current saturation spell; expected to clear as saturation eases.
- Sentry `NEXTJS-2C` "smoke check could not run: public base tables: RLS on + no anon write" — the documented honest-degradation path (the check could not RUN under saturation); verified live this tick that `check_public_security_invariants()` = `[]`, so NOT a real breach.
