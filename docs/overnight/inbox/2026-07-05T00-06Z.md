# Daytime monitor candidates — 2026-07-05T00:06Z (~17:06 PDT tick)

Health GREEN. security **0/0/0/0** (invariants/secdef_anon/rls_off_base/anon_write all `[]`); trust **16/16 ok** (breaches []); `detect_stalled_pipelines()` `[]`; `check_pgcron_recent_failures()` = 2 (`rpc-remap-misattributed-sales` + `rpc-allday-ev-corrected-refresh` both 18:23Z, 120s — ALREADY logged in the 21:23Z file as DAYTIME-CONTENTION-CLUSTERS-BROADENING; not re-fired since, next tick 00:23Z); sentinel TS-UUID-48h **0**; leak_48h **0**; editions TS **17,638** (+148 = F9 `::subID` by-design catalog, see below) / AllDay 6,191 / Golazos 581 / UFC 518 (non-TS flat); FMV TS H+M **4,814** (improving from nightly 4,785; == 21:23Z) / AllDay 848; DB **7,963 MB** (+146 vs nightly baseline 7,817, benign ts_history_backfill_v1 drain on Pro Micro); Sentry **0** unresolved/24h; Vercel prod **`99e82f38`** (golazos mojibake repair) READY, **0 ERROR** deploys (20 recent = READY/CANCELED-supersession only); runtime errors all 39 KNOWN families (DBSAT statement-timeouts / url.parse DEP0169 / wmc idempotent dup-key / conn-pool), **no new class** from the F9 wave.

**rpc-live-health artifact — VALIDATED (backing objects intact post-F9).** All 12 insights surfaces resolve: squeeze 501 / set_squeeze 239 / pack_reality 6 / rookies 61 / market 501 / first_mint 501 / cross_collection 1 / pinnacle_scarcity 501 / offer_spread 501 / deals 501 / trophies 501 / new_collectors 50 (identical to 21:23Z); leak_48h 0; FMV/PackEV/offers fresh 00:07-00:14Z; pinnacle_fmv_history resolves. The heavy 14-day FMV-histogram/trend leg TIMED OUT under the 00:0xZ contention — this is the known busy-guard symptom (the live dashboard self-skips heavy legs when active backends >=15), NOT a broken artifact (same class as the 21:23Z `rpc_ops_snapshot()` timeout). Light legs + all backing surfaces confirmed working.

## POST-SHIP WATCH — F9 subedition realign + base-circ reconcile wave (NEW since the 21:23Z tick; DB-side PASS)
Trevor shipped a substantial F9 wave AFTER the last inbox file (prod walked `ab670f5e`->`88e16670`->`0c9b63d9`->`548b9c99`->`99e82f38`, all READY / CANCELED-supersession, 0 ERROR):
- **F9 on-chain subedition resolution DRIVEN (`0c9b63d9`):** resolved all 21,689 pending (0 errors) -> **cataloged 147 `::N` editions** -> split 566 moments + 4,996 sales + 4,089 wmc -> realigned 392 mis-keyed moments (`::N`->base/`::Y`), 133 knots skipped. This is the source of the **+148 TS `::subID` editions** this tick — verified 148/148 are `setID:playID::subID` form, **0 hyphen-UUID leaks**, sentinel 0, leak_48h 0. By-design + reversible. Guard 546->504.
- **F9 write-time split in both sales writers (`88e16670`):** symmetric sales-indexer Step 4e + `/api/ingest` `::sub` circulation guard — stops NEW parallel sales re-populating the conflation guard.
- **323-edition base-circulation reconcile (`548b9c99`, `audit_20260705_base_circ_reconcile`, reversible):** base editions that stored on-chain GROSS (base+parallels) reset to Standard-only (gross - sum parallel circs); editions + badge_editions.
- **Golazos mojibake repair (`99e82f38`, `audit_20260704_golazos_title_repair`):** 156 team-field rows Latin-1-as-UTF-8 fixed (home_team/away_team/team_name only; player/set names were correct UTF-8).
- **DB-side verification this tick:** security invariants **0/0/0/0** after all migrations; `edition_integrity_flags` **4** (ok, breach 50); `topshot_impossible_parallel_serials` **0**; `fmv_sanity_flags` **0**; sentinel 0; leak_48h 0; no new Sentry/Vercel error class. **No regression.**

## Self-heals since prior ticks (no action)
- **18:16Z Cand 1 (ALLDAY-CROSS-SOURCE-DEDUP-STMT-TIMEOUT-FLAP):** RESOLVED — `rpc-allday-cross-source-sales-dedup` no longer surfaced by `check_pgcron_recent_failures()` (latest tick ok). Self-healed.
- **18:16Z Cand 2 (LOCK-CHECK-BATCH contention):** RESOLVED — `lock-check-batch` 00:08Z ok=true (recovered from the 17:38/18:08Z 125s caps).
- **allday-pack-opens fix (`26982cc3`) HOLDS:** backfill 00:02Z ok / forward 00:09Z ok. Night pass can CLOSE ALLDAY-PACK-OPENS-BACKFILL-404 (already recommended 18:16Z + 21:23Z).

---

## Candidate 1 — F9-REALIGN-WAVE-POST-SHIP-WATCH (LOW; night-pass verify-only focus, not a code task)
- **Source:** the F9 realign / base-circ-reconcile wave above — shipped after the last inbox file, so it lands squarely in the night pass's 24h post-ship window.
- **Risk read:** LOW. DB-side already verified clean this tick (security 0/0/0/0, integrity 4/50, impossible-parallel 0, fmv_sanity 0, leak 0, guard 546->504). Additive `::subID` catalog + reversible reconcile; no view/RPC dropped or renamed (rpc-live-health backing all resolve).
- **Suggested action (night pass, verify-only):** (1) conflation guard trends toward ~0 from 504 and does NOT re-climb (the write-time split `88e16670` is the guard); (2) the +148 `::subID` editions acquire FMV on the normal recalc sweep — they catalog with circ=max-serial floor and FMV NO_DATA initially (expected), watch they don't get stuck STALE; (3) the 323 base-circ reconcile HOLDS against the F7 circulation-normalizing trigger (`audit_20260704_trg_normalize_circulation_v3_per_family`) on the next sale-driven upsert — i.e. the two circulation mechanisms don't fight (`edition_integrity_flags` is the tripwire, 4/50 now). No standalone fix indicated.

## Carried / already-logged (do NOT re-log)
- **DAYTIME-CONTENTION-CLUSTERS-BROADENING** (21:23Z Cand 1): remap + allday-ev-corrected-refresh failed AGAIN at the 18:23Z tick (both 120s), consistent with the logged escalation; recovers next 00:23Z tick; root = Pro Micro IO headroom vs DB 7,963 MB climbing from the ts_history_backfill drain. Both are pure pg_cron fns (bounded `statement_timeout` raise OR recent-window scan bound is in scope for a verifiable DB-only change — verify each is a plain pg_cron `SELECT` not an after()-lambda before picking the lever). `classify-acquisitions-multicollection` flapped again 00:07Z (night-count 4, carried, do NOT close). This tick consolidates — no re-log.
