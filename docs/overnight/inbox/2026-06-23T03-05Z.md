# Daytime monitor inbox — late-evening tick (2026-06-22 ~20:05 PDT / 2026-06-23 03:05Z)

Platform GREEN. Sweep: pipeline_runs **1 fail / 6h** (evm-transfers-ingest base_mainnet 429 — known-benign evm-429 class); detect_stalled **[]**; check_pgcron_recent_failures() **[]** (null); security **0/0/0/0** (rls-off [], anon/auth-write-on-rls-off [], public_invariants [], secdef []); trust-health **9/9 ok** (ts_uuid_dupes_created_24h **0**/200, unmapped_resolution_backlog 24/100, pinnacle_ask 0.2h/3, pinnacle_fmv 17.0h/30, pack_ev_board_stale 1.0d/2, edition_integrity 4/50, fmv_sanity 0); sentinel hyphen-UUID-48h **2/250**; FMV **TS H+M 4359** (1219+3140, flat vs 4361 @00:05Z / +31 vs 4328 baseline) / **AllDay 903** (251+652, improving vs 896), both reconcile (TS latest-per-edition sum 17,316 vs 17,318 editions = 2 fresh `::` parallels awaiting first snapshot, benign; AllDay 6,191 exact); editions **TS 17,318 / AllDay 6,191 / Golazos 581 / UFC 518** (all FLAT vs 00:05Z — UFC stable at the post-seed 518, no writer leak); DB **5,140 MB** (+8, benign); Vercel **0 ERROR**, prod **75ee62f READY**; Sentry **1 unresolved** (NEXTJS-1Q, benign/transient — Watch). 12/12 insights/dashboard backing objects return rows (squeeze 501+/deals 704/serial-prem 185/perfect-mint 14/underpriced 11/top-sales 999/trophies 687/rookies 61/special-serial-mv 6784/cohort 158/set-overlap 246/tracked-fmv-conf 24); artifact HTML on OneDrive unreachable (MONITOR-ARTIFACT-ACCESS, known — validated via backing objects).

## Candidates (0 new)
No new low-risk candidate work this tick. The one standing artifact-accuracy item — **rpc-live-health `leak_24h`/`leak_48h` panel over-counting `::` parallels** (predicate `^[0-9]+:[0-9]+$` matches benign subedition rows; shows ~1,775 vs the ~2 real hyphen-UUID leak) — was logged in `2026-06-22T15-17Z.md` (CAND-1) and re-acknowledged at 21:05Z + 00:05Z. NOT re-logged; still the only standing artifact-repair candidate for the night pass (update both predicates to `^[0-9]+:[0-9]+(::[0-9]+)?$`).

## POST-SHIP WATCH (night pass) — soak confirmation, no new ship since 00:05Z
- **`75ee62f` (current prod, READY) — CC-queue drain, shipped ~21:56Z — now ~5h soaked and STILL CLEAN.** UFC editions stable at **518** (the +72 seed holds; no further edition drift, ts_uuid_dupes 0); the dropped orphaned 3-arg `get_user_top_owned_moments` shows no missing-overload errors in 6h of pipeline_runs/Sentry; the `softIfTransientRpc` smoke hardening correlates with NEXTJS-A staying out of the unresolved set (only NEXTJS-1Q remains, unrelated). Earlier wave **`dbdbd0dd`** (user-facing Flowty teardown) superseded as prod but its surfaces stay clean (0 new Sentry cluster). The night pass should still do its own deeper re-measure of the collection/moment/analytics public surfaces + a concierge buy-FAQ probe (no Flowty recommendation) per the 00:05Z handoff.
- **Only new commit since the 00:05Z monitor tick is `8d3b620b`** "docs(memory): record the 2026-06-22 Cowork audit thread" — docs-only, build CANCELED by ignore-build-step, no deploy, no code/data change. Nothing new to watch.
- Minor cosmetic (NOT a candidate, carried from 00:05Z): the 72 UFC seed rows carry `set_name = NULL`; UFC has no `/sets` surface so this does not affect rendering/tier.

## Watch (no action — tracked/benign)
- **Sentry NEXTJS-1Q** "router state header could not be parsed" on `/[collection]/packs/page`: still **1 event / 1 user**, first+last seen ~6h ago (the ~21:0xZ deploy-swap window) — generic App-Router RSC transient, NOT recurring/spiking. Flag only if it climbs. (Carried from 21:05Z / 00:05Z.)
- **pg_cron** healthy: `check_pgcron_recent_failures()` returns no rows; special-serial-owners-mv next tick 04:13Z (MV 6,784, force-hashjoin fix proven on the 16:13Z cron path); remap converged.
