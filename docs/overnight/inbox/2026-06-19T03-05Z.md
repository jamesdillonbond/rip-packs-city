# Daytime monitor — 2026-06-19T03:05Z (20:05 PDT, evening tick)

Run: rpc-daytime-monitor (READ-ONLY). Platform GREEN. 0 genuinely-new candidates — status record + one live watch (BUYERBF-150, already queued) with fresh near-ceiling data + closeable reconciliations for the night pass (which has NOT fired since 06-17; focus.md expects it tonight ~01:00 PDT, with 9+ inbox files to drain).

## Live watch — already queued, do NOT re-file as a new candidate

### BUYERBF-150 headroom — near-ceiling pattern PERSISTS; the 00:06Z "holding" read was premature [MED · CC route-code, NOT night-pass-shippable]
- **Source:** pipeline_runs (topshot-buyer-backfill) durations, last 14h.
- **Fresh data:** latest run **02:34Z = 589.8s** — 2nd-highest of the window, ~10s under maxDuration=600. Window newest->oldest: 02:34 **590s** / 01:04 504s / 23:34 528s / 22:04 511s / 20:34 476s / 18:54 **598s** / 17:24 530s / 14:44 532s. The 00:06Z commit's "HOLDING (476/511/528s)" framing is contradicted — runs are NOT consistently clear of the ceiling; 2 of the last ~8 (598s, 590s) came within 2-10s of the 600s lambda-kill cap. The 21:10Z BUYERBF-150-INSUFFICIENT-HEADROOM candidate stands; the 00:06Z "holding" should be read as superseded.
- **Unchanged recommendation (= the 21:10Z candidate):** per-row on-chain decode latency (not batch size) governs run time, so the robust lever is `maxDuration` 600->800 (Pro hard cap) in app/api/admin/backfill-topshot-buyers/route.ts; keep BATCH=150. This is the SAME already-queued item — NOT a new candidate, NOT a threshold-widen, NOT shippable from the mount (CC-owned route). Re-confirm: no buyer-backfill run logs >600s and none stop logging.
- **Risk:** MED. No breakage now (all 8 runs ok=true + logging). It's the invisible >600s-death class (looks healthy from every external signal until a run dies before the finally writes pipeline_runs) one slow batch away.

## Closeable / reconciled — for the night pass, no new work
- **FMV-HIGHMED-DIP-WATCH — closeable.** TS HIGH+MED 3158 (normal daytime band), ASK_ONLY 2616 stable (not over-claiming), NO_DATA 3484; reconciles EXACTLY to 15,543 TS editions (0 orphans). Benign daily cycle confirmed across 21:10Z (3139) -> 00:06Z (3161) -> now (3158).
- **ALERTS-DISPATCH-DEAL-TIMEOUT — closeable.** get_pipeline_alerts []; 0 deal-leg timeout fails in 6h (zero-sub early-exit + the 45->90s scale path live; dd7e2bf).
- **SERIAL-FMV-MULT-CRON — BY DESIGN, do NOT re-flag** (focus.md STEER). pg_cron jobs 5+6 weekly Sun 11:00 UTC; <=7d staleness expected.

## Health summary
GREEN · security 0/0/0/0 (detect_stalled [], rls-off [], check_public_security_invariants [], check_secdef_anon_execute_violations []) · get_pipeline_alerts [] · sentinel TS-UUID-48h 0 · pipeline 6h 1929 runs / 2 fail (0.10%: evm Base-429 + offers-sweep SearchMarketplaceEditions GQL, both known transient) / 24h 12 fail all transient · editions flat (TS 15543 / AllDay 6191 / Golazos 581 / UFC 446) · FMV TS HIGH+MED 3158 / ASK_ONLY 2616 / NO_DATA 3484 (reconciles to 15543) · DB 4837 MB (+12 benign) · cross-collection cohort_mat 158 · Vercel prod 2327cb6 READY, 0 ERROR/20 (CANCELED = superseded monitor/rapid-push commits) · Sentry 0 new unresolved/12h · 14 active artifacts validated — backing objects all return rows: deals_board 797 (TS+Pinnacle+AllDay leg) / allday_floor 3905 / serial_premiums 276 / perfect_mint 9 / underpriced_serials 15 / offer_sanity 224 / tracked_fmv 21 (AF1 v3 fast) / top_sales 583 / squeeze 500+ / cohort 158; none broken/repaired.
Daily extras (trust-health + cross-collection refresh verify) NOT run — evening tick, not the ~8am first tick (the 07:11Z tick already ran them: trust 9/9, ccm fresh).

## Noted, not findings
- Night pass not run since 06-17 (NIGHTPASS-MISS, carried) — focus.md expects it tonight ~01:00 PDT; inbox accumulating for it to drain.
- prod 2327cb6 (Trevor ~00:14Z: collection-breakdown color palette keyed by underscored slug + ufc_strike entry) READY — post-ship: 0 Sentry, 0 deploy ERROR.
- evm-transfers-ingest Base-429 — known LOW external rate-limit; don't chase (focus.md).
- Profile public over-count — diagnosed + SHIPPED today (owner-scoping 4b9ed33/412bd08 + dedup 80100c1); not a finding.
