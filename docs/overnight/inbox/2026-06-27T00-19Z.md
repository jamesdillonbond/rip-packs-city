# Monitor inbox — 2026-06-27T00:19Z (17:19 PDT, mid-day tick)

Platform **GREEN**. Health sweep + artifact validation clean. Post-ship watch on the afternoon CC wave that landed AFTER the night-pass close-out (unseen by prior monitors) ALL PASS. **1 new LOW candidate.**

Recent-ship watch (the post-19:08Z-close-out commits, `48bee26`->`3f4c2c1`):
- **`3f4c2c1` New Collectors public surface (17:04 PDT)** — healthy end-to-end (see candidate below; this is the candidate).
- **`e56e4e3` AllDay per-moment badges via residential Atlas ingest** — new pipeline `allday-badge-ingest` ran 1x clean (22:24Z, 0 fails). Healthy.
- **`1cc9d42` ops-monitor stale-fmv-monitor 504 fix** — GHA-side (not pipeline_runs-tracked); 0 related Sentry/errors. Can't directly verify GHA run status from monitor.
- Pinnacle render_id re-key `43769cc` / set-count reconcile `65800e3` / Flowty-loans removal `7741a21` / username-resolver+eager-img `419e7d9` — the 18:19Z monitor already post-ship-watched these PASS; re-confirmed editions FLAT + FMV improving + 0 Sentry.

## NEW candidate

### NEW-COLLECTORS-INSIGHTS-QA — finish rpc-insights-qa on the brand-new public /insights/new-collectors (LOW · night-pass or interactive)
- **Source:** commit `3f4c2c1` (Trevor/CC, 17:04 PDT 06-26) — first public surface shipped since the 19:08Z night-pass close-out; unseen by every prior monitor.
- **Monitor ALREADY verified clean this tick (read-only):**
  - 4 anon MVs populated + fresh: `mv_insights_new_collectors_summary` 3 rows / computed 22:48Z, `_spend` 3, `_gateway` 40, `_cohorts` 48; all `anon SELECT = true`.
  - Address-bearing buyer spine `mv_ts_buyer_first_buy` correctly **service_role-only (anon SELECT = false)**. NOTE: MVs bypass RLS, so `check_public_security_invariants()` cannot see this — hand-checked the grant, posture is correct (no wallet-address leak).
  - pg_cron `rpc-refresh-new-collectors` exists + active.
  - `sitemap.ts` line 330 (`new-collectors`); `proxy.ts` `/insights/*` (L318) + `/api/public/*` (L176) anon bypass => **NOT login-gated** (the usual new-surface SEO trap is clear).
  - Param-stripped canonical in `app/insights/new-collectors/layout.tsx`; dedicated OG route `app/api/og/insights/new-collectors/route.tsx`; route + `/api/public/new-collectors` present; Vercel prod `3f4c2c1` READY; 0 attributable Sentry.
- **Remaining (needs live HTTP / visual — the monitor is provenance-gated from web_fetch):** confirm anon HTTP 200; gateway sets/players drill-down links resolve into the entity hubs; brand tokens (0 hardcoded `#E03A2F` outside the universal OG route); buyer-coverage caveat renders inline + in methodology.
- **Risk:** read-only QA, no ship risk. **Suggested:** night-pass or interactive `rpc-insights-qa` close — same basis the pass used to close SERIAL-PREMIUMS / TOP-SALES / TROPHIES.
- **Adjunct (LOW, pairs with the queued WEEKLY-SURFACE-QA-PROSE):** the `rpc-live-health` `insights_counts` panel + `rpc-qa-scorecard` don't yet track new-collectors. Add a freshness/row leg for the 4 MVs when the live-health artifact is next reinstalled, so future monitors auto-validate the newest public surface.

## Notes (owned/carried — NOT candidates, do NOT re-flag)
- **unmapped_resolution_backlog_max BREACH 428** (trust 8/9): `topshot-flowty-unmapped-drain` HEALTHY (9 runs/3h, all ok, promoted ~198/3h, last 00:09Z). Backlog has **PLATEAUED ~413->428 over ~5h** (producer inflow ~= drain rate) rather than clearing <100 as the night pass predicted. OWNED/Declined — do NOT re-flag/skip/retire/raise-threshold; cadence (drain more often than 20-min, or raise the per-tick getMintedMoment proxy budget) is the only lever if <100 is wanted. Night pass owns DRAIN-WATCHLIST + re-measures.
- `allday-badge-ingest` (new): 1 clean run, residential-runner cadence — too new / wrong class to watchlist (gate not met).
- PINNACLE-EDITION-KEY-UUID-CAST: addressed by `43769cc` (Pinnacle render_id re-key, deploy READY) — night pass to formally close.
- AllDay `unmapped_open` 462 = carried ALLDAY-V1-UNMAPPED-DRIFT (budget-exhausted V1 fossils, held out of `sales`).
- conflation 166 (oscillation, converges on `23 */6` remap) / DB 6218 MB (+42/5h backfill wave) — both benign/owned.

## Health line
security 0/0/0/0 · detect_stalled [] · pgcron [] · alerts 1 INFO (ufc_sales, benign) · pipeline fails 6h **0** · trust 8/9 (1 owned BREACH unmapped 428 draining) · FMV TS H+M **4585** (up from 4561) / AllDay 905 · sales 24h TS 4121/AllDay 118/Golazos 12 · editions FLAT 17471/6191/581/518 · ts-uuid-48h **34** inert · sentinel leak_48h 34 · writers fresh (FMV 00:14Z / PackEV 00:13Z / offers 00:07Z) · DB 6218 MB · Vercel prod `3f4c2c1` READY 0 ERROR · Sentry 0 new (5 stale smoke transients >=1d) · 14 active artifacts validated (rpc-live-health full payload + tracked-fmv-confidence + offers + cross-collection mat fresh 04:10Z + pinnacle 2170 renders) none broken.
