# Daytime health-monitor harvest — 2026-09-24 ~2:09 PM PT (21:09Z)

Read-only daytime sweep. Platform HEALTHY (see summary at bottom). One low-risk candidate for the night pass to confirm.

## Candidates

### C1 · LOW · `topshot_pack_reality_top_ev` view returns 0 rows — empty public "pack reality top-EV" insights surface
- **Source:** rpc-live-health payload `insights_counts.pack_reality_top_ev = 0`; direct check `SELECT count(*) FROM topshot_pack_reality_top_ev = 0` (relkind='v', a live view — it executed fine, it is simply returning no rows). For contrast `cross_collection_cohort_stats` = 1 row is a by-design summary over the healthy 190-row `cross_collection_cohort_mat`, NOT a finding.
- **Not yet distinguished:** honestly-empty (under the honest-EV regime, chase-incomplete/depleted packs show "no EV", so a "+EV top" filter can legitimately qualify zero packs right now) **vs** a regressed filter/backing (a renamed column or a predicate that now excludes everything). `trust_health.public_board_empty_count = 0` did NOT flag it, so either it is not in the public-board watchset or trust-health tolerates it — that ambiguity is the reason to confirm.
- **Risk read:** low. It is a read-only view; nothing is being written wrong. Worst user-facing impact is an empty board on the pack-reality insights surface. No urgency.
- **Suggested action (night pass):** RE-MEASURE / confirm. Read the view definition, check whether its filter (likely a +EV / ratio threshold over `pack_ev_latest`/`pack_table_rows`) currently has zero qualifying rows by data vs a broken predicate; compare against a recent known-good rowcount if one exists. If honestly empty, note it and (optionally) add it to the public-board watchset or Declined so it stops re-surfacing. Do NOT "fix" by loosening the honest-EV guard.

## Health summary (this tick)
- Security invariants/anon-write/RLS-off/secdef-anon: all `[]` (0/0/0/0).
- Trust health: 38/38 metrics `ok`, breaches `[]`; `ts_uuid_dupes_created_24h = 0`.
- Stalled pipelines `[]`; pg_cron recent failures `[]`; sentinel TS-UUID editions 48h = 0; edition-writer leak 24h/48h = 0.
- pipeline_runs 6h ok=false: all known/info class — Atlas 403 upstream challenges (editions-refresh, market-feed; no rows lost, re-walked), Flow Access-API transient wallet-backfill, external `sync-nba-projections all_upstreams_failed`, one benign `topshot-moments-hydrate-wmc` duplicate-key race.
- pipeline_alerts: 5, all severity `info`, escalation conditions unmet (Atlas freshness current, 0/281 sets stale >6h, compute-allday-pack-ev failure_rate cleared by streak split).
- Positive control: `db_active = 2`, snapshot did not time out — NOT in a saturation spell; all readings interpretable.
- Vercel prod: latest `519f5c90` READY (CANCELED builds are superseded in-flight pushes, no ERROR).
- Sentry: 0 new (24h), 0 escalating (7d).
- Artifacts spot-checked: rpc-qa-scorecard + rpc-live-health single-payload queries both executed clean, all panel keys sensible.
- DB size ~25.1 GB. FMV TS H+M ≈ 7,861.

---

## Disposition — C1 confirmed HONESTLY EMPTY (Claude Code, Windows box, ~4:15 PM PT 09-24)

The view is `SELECT … FROM mv_topshot_pack_reality_top_ev` (refreshed by pg_cron `rpc-refresh-pack-reality-top-ev`, `34 */2 * * *`), and the MV itself holds 0 rows. Funnel over live `pack_ev_latest` (Top Shot): 1,210 rows → 151 snapshotted in the last 48 h → **10 fresh +EV** → 10 priced/non-reward → **0 under the depletion gate**.

All 10 have `depletion_pct`, `total_unopened`, `primary_available` and `secondary_available` **NULL** (Got Game S1 Drops 1–4 at 2.26–6.28×, Freshman Gems, Metallic Gold LE, two 2024 Playoff Packs …), and `COALESCE(depletion_pct, 100) < 90` fails them CLOSED. That is the right call: they are the **#50 population** (sold-out packs valued at retail price — 6.28× at retail vs 0.23× at the live ask). **No predicate regression; nothing to fix here.** An unknown-depletion row must not headline a +EV board. ⚠ #50's fix (`6cbe957ae`, same afternoon) rebases the packs BOARD on the live ask. It does NOT change `pack_ev_latest`: re-read after it applied, all 10 rows are still retail-based with NULL depletion. So this MV stays empty until a +EV pack with KNOWN depletion under 90 % exists. That is correct, not a gap. ⛔ Do not "fix" it by loosening the COALESCE default.
