{
  "name": "rpc-mcp-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Rip Packs City collector intelligence as a Flow MCP server. Streamable HTTP transport (MCP 2025-06-18).",
  "scripts": {
    "deploy": "wrangler deploy --var BUILD_SHA:$(git rev-parse --short HEAD)",
    "tail": "wrangler tail --name rpc-mcp",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260501.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
